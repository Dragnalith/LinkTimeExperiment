#include "lib.h"
#include <iostream>
int func_24000(int& val) {
    func_in_24000(val);
    const int x = 24000;
    std::cout << 24000;
    for (int i = 0; i < 24000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24000(int& val) {
    const int x = 24000;
    std::cout << 24000;
    for (int i = 0; i < 24000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24001(int& val) {
    func_in_24001(val);
    const int x = 24001;
    std::cout << 24001;
    for (int i = 0; i < 24001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24001(int& val) {
    const int x = 24001;
    std::cout << 24001;
    for (int i = 0; i < 24001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24002(int& val) {
    func_in_24002(val);
    const int x = 24002;
    std::cout << 24002;
    for (int i = 0; i < 24002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24002(int& val) {
    const int x = 24002;
    std::cout << 24002;
    for (int i = 0; i < 24002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24003(int& val) {
    func_in_24003(val);
    const int x = 24003;
    std::cout << 24003;
    for (int i = 0; i < 24003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24003(int& val) {
    const int x = 24003;
    std::cout << 24003;
    for (int i = 0; i < 24003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24004(int& val) {
    func_in_24004(val);
    const int x = 24004;
    std::cout << 24004;
    for (int i = 0; i < 24004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24004(int& val) {
    const int x = 24004;
    std::cout << 24004;
    for (int i = 0; i < 24004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24005(int& val) {
    func_in_24005(val);
    const int x = 24005;
    std::cout << 24005;
    for (int i = 0; i < 24005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24005(int& val) {
    const int x = 24005;
    std::cout << 24005;
    for (int i = 0; i < 24005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24006(int& val) {
    func_in_24006(val);
    const int x = 24006;
    std::cout << 24006;
    for (int i = 0; i < 24006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24006(int& val) {
    const int x = 24006;
    std::cout << 24006;
    for (int i = 0; i < 24006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24007(int& val) {
    func_in_24007(val);
    const int x = 24007;
    std::cout << 24007;
    for (int i = 0; i < 24007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24007(int& val) {
    const int x = 24007;
    std::cout << 24007;
    for (int i = 0; i < 24007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24008(int& val) {
    func_in_24008(val);
    const int x = 24008;
    std::cout << 24008;
    for (int i = 0; i < 24008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24008(int& val) {
    const int x = 24008;
    std::cout << 24008;
    for (int i = 0; i < 24008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24009(int& val) {
    func_in_24009(val);
    const int x = 24009;
    std::cout << 24009;
    for (int i = 0; i < 24009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24009(int& val) {
    const int x = 24009;
    std::cout << 24009;
    for (int i = 0; i < 24009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24010(int& val) {
    func_in_24010(val);
    const int x = 24010;
    std::cout << 24010;
    for (int i = 0; i < 24010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24010(int& val) {
    const int x = 24010;
    std::cout << 24010;
    for (int i = 0; i < 24010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24011(int& val) {
    func_in_24011(val);
    const int x = 24011;
    std::cout << 24011;
    for (int i = 0; i < 24011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24011(int& val) {
    const int x = 24011;
    std::cout << 24011;
    for (int i = 0; i < 24011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24012(int& val) {
    func_in_24012(val);
    const int x = 24012;
    std::cout << 24012;
    for (int i = 0; i < 24012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24012(int& val) {
    const int x = 24012;
    std::cout << 24012;
    for (int i = 0; i < 24012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24013(int& val) {
    func_in_24013(val);
    const int x = 24013;
    std::cout << 24013;
    for (int i = 0; i < 24013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24013(int& val) {
    const int x = 24013;
    std::cout << 24013;
    for (int i = 0; i < 24013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24014(int& val) {
    func_in_24014(val);
    const int x = 24014;
    std::cout << 24014;
    for (int i = 0; i < 24014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24014(int& val) {
    const int x = 24014;
    std::cout << 24014;
    for (int i = 0; i < 24014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24015(int& val) {
    func_in_24015(val);
    const int x = 24015;
    std::cout << 24015;
    for (int i = 0; i < 24015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24015(int& val) {
    const int x = 24015;
    std::cout << 24015;
    for (int i = 0; i < 24015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24016(int& val) {
    func_in_24016(val);
    const int x = 24016;
    std::cout << 24016;
    for (int i = 0; i < 24016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24016(int& val) {
    const int x = 24016;
    std::cout << 24016;
    for (int i = 0; i < 24016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24017(int& val) {
    func_in_24017(val);
    const int x = 24017;
    std::cout << 24017;
    for (int i = 0; i < 24017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24017(int& val) {
    const int x = 24017;
    std::cout << 24017;
    for (int i = 0; i < 24017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24018(int& val) {
    func_in_24018(val);
    const int x = 24018;
    std::cout << 24018;
    for (int i = 0; i < 24018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24018(int& val) {
    const int x = 24018;
    std::cout << 24018;
    for (int i = 0; i < 24018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24019(int& val) {
    func_in_24019(val);
    const int x = 24019;
    std::cout << 24019;
    for (int i = 0; i < 24019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24019(int& val) {
    const int x = 24019;
    std::cout << 24019;
    for (int i = 0; i < 24019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24020(int& val) {
    func_in_24020(val);
    const int x = 24020;
    std::cout << 24020;
    for (int i = 0; i < 24020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24020(int& val) {
    const int x = 24020;
    std::cout << 24020;
    for (int i = 0; i < 24020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24021(int& val) {
    func_in_24021(val);
    const int x = 24021;
    std::cout << 24021;
    for (int i = 0; i < 24021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24021(int& val) {
    const int x = 24021;
    std::cout << 24021;
    for (int i = 0; i < 24021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24022(int& val) {
    func_in_24022(val);
    const int x = 24022;
    std::cout << 24022;
    for (int i = 0; i < 24022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24022(int& val) {
    const int x = 24022;
    std::cout << 24022;
    for (int i = 0; i < 24022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24023(int& val) {
    func_in_24023(val);
    const int x = 24023;
    std::cout << 24023;
    for (int i = 0; i < 24023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24023(int& val) {
    const int x = 24023;
    std::cout << 24023;
    for (int i = 0; i < 24023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24024(int& val) {
    func_in_24024(val);
    const int x = 24024;
    std::cout << 24024;
    for (int i = 0; i < 24024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24024(int& val) {
    const int x = 24024;
    std::cout << 24024;
    for (int i = 0; i < 24024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24025(int& val) {
    func_in_24025(val);
    const int x = 24025;
    std::cout << 24025;
    for (int i = 0; i < 24025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24025(int& val) {
    const int x = 24025;
    std::cout << 24025;
    for (int i = 0; i < 24025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24026(int& val) {
    func_in_24026(val);
    const int x = 24026;
    std::cout << 24026;
    for (int i = 0; i < 24026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24026(int& val) {
    const int x = 24026;
    std::cout << 24026;
    for (int i = 0; i < 24026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24027(int& val) {
    func_in_24027(val);
    const int x = 24027;
    std::cout << 24027;
    for (int i = 0; i < 24027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24027(int& val) {
    const int x = 24027;
    std::cout << 24027;
    for (int i = 0; i < 24027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24028(int& val) {
    func_in_24028(val);
    const int x = 24028;
    std::cout << 24028;
    for (int i = 0; i < 24028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24028(int& val) {
    const int x = 24028;
    std::cout << 24028;
    for (int i = 0; i < 24028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24029(int& val) {
    func_in_24029(val);
    const int x = 24029;
    std::cout << 24029;
    for (int i = 0; i < 24029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24029(int& val) {
    const int x = 24029;
    std::cout << 24029;
    for (int i = 0; i < 24029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24030(int& val) {
    func_in_24030(val);
    const int x = 24030;
    std::cout << 24030;
    for (int i = 0; i < 24030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24030(int& val) {
    const int x = 24030;
    std::cout << 24030;
    for (int i = 0; i < 24030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24031(int& val) {
    func_in_24031(val);
    const int x = 24031;
    std::cout << 24031;
    for (int i = 0; i < 24031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24031(int& val) {
    const int x = 24031;
    std::cout << 24031;
    for (int i = 0; i < 24031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24032(int& val) {
    func_in_24032(val);
    const int x = 24032;
    std::cout << 24032;
    for (int i = 0; i < 24032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24032(int& val) {
    const int x = 24032;
    std::cout << 24032;
    for (int i = 0; i < 24032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24033(int& val) {
    func_in_24033(val);
    const int x = 24033;
    std::cout << 24033;
    for (int i = 0; i < 24033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24033(int& val) {
    const int x = 24033;
    std::cout << 24033;
    for (int i = 0; i < 24033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24034(int& val) {
    func_in_24034(val);
    const int x = 24034;
    std::cout << 24034;
    for (int i = 0; i < 24034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24034(int& val) {
    const int x = 24034;
    std::cout << 24034;
    for (int i = 0; i < 24034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24035(int& val) {
    func_in_24035(val);
    const int x = 24035;
    std::cout << 24035;
    for (int i = 0; i < 24035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24035(int& val) {
    const int x = 24035;
    std::cout << 24035;
    for (int i = 0; i < 24035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24036(int& val) {
    func_in_24036(val);
    const int x = 24036;
    std::cout << 24036;
    for (int i = 0; i < 24036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24036(int& val) {
    const int x = 24036;
    std::cout << 24036;
    for (int i = 0; i < 24036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24037(int& val) {
    func_in_24037(val);
    const int x = 24037;
    std::cout << 24037;
    for (int i = 0; i < 24037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24037(int& val) {
    const int x = 24037;
    std::cout << 24037;
    for (int i = 0; i < 24037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24038(int& val) {
    func_in_24038(val);
    const int x = 24038;
    std::cout << 24038;
    for (int i = 0; i < 24038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24038(int& val) {
    const int x = 24038;
    std::cout << 24038;
    for (int i = 0; i < 24038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24039(int& val) {
    func_in_24039(val);
    const int x = 24039;
    std::cout << 24039;
    for (int i = 0; i < 24039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24039(int& val) {
    const int x = 24039;
    std::cout << 24039;
    for (int i = 0; i < 24039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24040(int& val) {
    func_in_24040(val);
    const int x = 24040;
    std::cout << 24040;
    for (int i = 0; i < 24040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24040(int& val) {
    const int x = 24040;
    std::cout << 24040;
    for (int i = 0; i < 24040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24041(int& val) {
    func_in_24041(val);
    const int x = 24041;
    std::cout << 24041;
    for (int i = 0; i < 24041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24041(int& val) {
    const int x = 24041;
    std::cout << 24041;
    for (int i = 0; i < 24041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24042(int& val) {
    func_in_24042(val);
    const int x = 24042;
    std::cout << 24042;
    for (int i = 0; i < 24042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24042(int& val) {
    const int x = 24042;
    std::cout << 24042;
    for (int i = 0; i < 24042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24043(int& val) {
    func_in_24043(val);
    const int x = 24043;
    std::cout << 24043;
    for (int i = 0; i < 24043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24043(int& val) {
    const int x = 24043;
    std::cout << 24043;
    for (int i = 0; i < 24043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24044(int& val) {
    func_in_24044(val);
    const int x = 24044;
    std::cout << 24044;
    for (int i = 0; i < 24044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24044(int& val) {
    const int x = 24044;
    std::cout << 24044;
    for (int i = 0; i < 24044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24045(int& val) {
    func_in_24045(val);
    const int x = 24045;
    std::cout << 24045;
    for (int i = 0; i < 24045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24045(int& val) {
    const int x = 24045;
    std::cout << 24045;
    for (int i = 0; i < 24045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24046(int& val) {
    func_in_24046(val);
    const int x = 24046;
    std::cout << 24046;
    for (int i = 0; i < 24046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24046(int& val) {
    const int x = 24046;
    std::cout << 24046;
    for (int i = 0; i < 24046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24047(int& val) {
    func_in_24047(val);
    const int x = 24047;
    std::cout << 24047;
    for (int i = 0; i < 24047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24047(int& val) {
    const int x = 24047;
    std::cout << 24047;
    for (int i = 0; i < 24047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24048(int& val) {
    func_in_24048(val);
    const int x = 24048;
    std::cout << 24048;
    for (int i = 0; i < 24048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24048(int& val) {
    const int x = 24048;
    std::cout << 24048;
    for (int i = 0; i < 24048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24049(int& val) {
    func_in_24049(val);
    const int x = 24049;
    std::cout << 24049;
    for (int i = 0; i < 24049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24049(int& val) {
    const int x = 24049;
    std::cout << 24049;
    for (int i = 0; i < 24049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24050(int& val) {
    func_in_24050(val);
    const int x = 24050;
    std::cout << 24050;
    for (int i = 0; i < 24050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24050(int& val) {
    const int x = 24050;
    std::cout << 24050;
    for (int i = 0; i < 24050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24051(int& val) {
    func_in_24051(val);
    const int x = 24051;
    std::cout << 24051;
    for (int i = 0; i < 24051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24051(int& val) {
    const int x = 24051;
    std::cout << 24051;
    for (int i = 0; i < 24051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24052(int& val) {
    func_in_24052(val);
    const int x = 24052;
    std::cout << 24052;
    for (int i = 0; i < 24052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24052(int& val) {
    const int x = 24052;
    std::cout << 24052;
    for (int i = 0; i < 24052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24053(int& val) {
    func_in_24053(val);
    const int x = 24053;
    std::cout << 24053;
    for (int i = 0; i < 24053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24053(int& val) {
    const int x = 24053;
    std::cout << 24053;
    for (int i = 0; i < 24053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24054(int& val) {
    func_in_24054(val);
    const int x = 24054;
    std::cout << 24054;
    for (int i = 0; i < 24054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24054(int& val) {
    const int x = 24054;
    std::cout << 24054;
    for (int i = 0; i < 24054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24055(int& val) {
    func_in_24055(val);
    const int x = 24055;
    std::cout << 24055;
    for (int i = 0; i < 24055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24055(int& val) {
    const int x = 24055;
    std::cout << 24055;
    for (int i = 0; i < 24055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24056(int& val) {
    func_in_24056(val);
    const int x = 24056;
    std::cout << 24056;
    for (int i = 0; i < 24056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24056(int& val) {
    const int x = 24056;
    std::cout << 24056;
    for (int i = 0; i < 24056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24057(int& val) {
    func_in_24057(val);
    const int x = 24057;
    std::cout << 24057;
    for (int i = 0; i < 24057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24057(int& val) {
    const int x = 24057;
    std::cout << 24057;
    for (int i = 0; i < 24057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24058(int& val) {
    func_in_24058(val);
    const int x = 24058;
    std::cout << 24058;
    for (int i = 0; i < 24058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24058(int& val) {
    const int x = 24058;
    std::cout << 24058;
    for (int i = 0; i < 24058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24059(int& val) {
    func_in_24059(val);
    const int x = 24059;
    std::cout << 24059;
    for (int i = 0; i < 24059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24059(int& val) {
    const int x = 24059;
    std::cout << 24059;
    for (int i = 0; i < 24059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24060(int& val) {
    func_in_24060(val);
    const int x = 24060;
    std::cout << 24060;
    for (int i = 0; i < 24060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24060(int& val) {
    const int x = 24060;
    std::cout << 24060;
    for (int i = 0; i < 24060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24061(int& val) {
    func_in_24061(val);
    const int x = 24061;
    std::cout << 24061;
    for (int i = 0; i < 24061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24061(int& val) {
    const int x = 24061;
    std::cout << 24061;
    for (int i = 0; i < 24061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24062(int& val) {
    func_in_24062(val);
    const int x = 24062;
    std::cout << 24062;
    for (int i = 0; i < 24062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24062(int& val) {
    const int x = 24062;
    std::cout << 24062;
    for (int i = 0; i < 24062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24063(int& val) {
    func_in_24063(val);
    const int x = 24063;
    std::cout << 24063;
    for (int i = 0; i < 24063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24063(int& val) {
    const int x = 24063;
    std::cout << 24063;
    for (int i = 0; i < 24063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24064(int& val) {
    func_in_24064(val);
    const int x = 24064;
    std::cout << 24064;
    for (int i = 0; i < 24064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24064(int& val) {
    const int x = 24064;
    std::cout << 24064;
    for (int i = 0; i < 24064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24065(int& val) {
    func_in_24065(val);
    const int x = 24065;
    std::cout << 24065;
    for (int i = 0; i < 24065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24065(int& val) {
    const int x = 24065;
    std::cout << 24065;
    for (int i = 0; i < 24065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24066(int& val) {
    func_in_24066(val);
    const int x = 24066;
    std::cout << 24066;
    for (int i = 0; i < 24066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24066(int& val) {
    const int x = 24066;
    std::cout << 24066;
    for (int i = 0; i < 24066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24067(int& val) {
    func_in_24067(val);
    const int x = 24067;
    std::cout << 24067;
    for (int i = 0; i < 24067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24067(int& val) {
    const int x = 24067;
    std::cout << 24067;
    for (int i = 0; i < 24067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24068(int& val) {
    func_in_24068(val);
    const int x = 24068;
    std::cout << 24068;
    for (int i = 0; i < 24068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24068(int& val) {
    const int x = 24068;
    std::cout << 24068;
    for (int i = 0; i < 24068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24069(int& val) {
    func_in_24069(val);
    const int x = 24069;
    std::cout << 24069;
    for (int i = 0; i < 24069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24069(int& val) {
    const int x = 24069;
    std::cout << 24069;
    for (int i = 0; i < 24069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24070(int& val) {
    func_in_24070(val);
    const int x = 24070;
    std::cout << 24070;
    for (int i = 0; i < 24070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24070(int& val) {
    const int x = 24070;
    std::cout << 24070;
    for (int i = 0; i < 24070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24071(int& val) {
    func_in_24071(val);
    const int x = 24071;
    std::cout << 24071;
    for (int i = 0; i < 24071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24071(int& val) {
    const int x = 24071;
    std::cout << 24071;
    for (int i = 0; i < 24071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24072(int& val) {
    func_in_24072(val);
    const int x = 24072;
    std::cout << 24072;
    for (int i = 0; i < 24072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24072(int& val) {
    const int x = 24072;
    std::cout << 24072;
    for (int i = 0; i < 24072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24073(int& val) {
    func_in_24073(val);
    const int x = 24073;
    std::cout << 24073;
    for (int i = 0; i < 24073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24073(int& val) {
    const int x = 24073;
    std::cout << 24073;
    for (int i = 0; i < 24073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24074(int& val) {
    func_in_24074(val);
    const int x = 24074;
    std::cout << 24074;
    for (int i = 0; i < 24074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24074(int& val) {
    const int x = 24074;
    std::cout << 24074;
    for (int i = 0; i < 24074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24075(int& val) {
    func_in_24075(val);
    const int x = 24075;
    std::cout << 24075;
    for (int i = 0; i < 24075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24075(int& val) {
    const int x = 24075;
    std::cout << 24075;
    for (int i = 0; i < 24075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24076(int& val) {
    func_in_24076(val);
    const int x = 24076;
    std::cout << 24076;
    for (int i = 0; i < 24076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24076(int& val) {
    const int x = 24076;
    std::cout << 24076;
    for (int i = 0; i < 24076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24077(int& val) {
    func_in_24077(val);
    const int x = 24077;
    std::cout << 24077;
    for (int i = 0; i < 24077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24077(int& val) {
    const int x = 24077;
    std::cout << 24077;
    for (int i = 0; i < 24077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24078(int& val) {
    func_in_24078(val);
    const int x = 24078;
    std::cout << 24078;
    for (int i = 0; i < 24078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24078(int& val) {
    const int x = 24078;
    std::cout << 24078;
    for (int i = 0; i < 24078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24079(int& val) {
    func_in_24079(val);
    const int x = 24079;
    std::cout << 24079;
    for (int i = 0; i < 24079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24079(int& val) {
    const int x = 24079;
    std::cout << 24079;
    for (int i = 0; i < 24079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24080(int& val) {
    func_in_24080(val);
    const int x = 24080;
    std::cout << 24080;
    for (int i = 0; i < 24080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24080(int& val) {
    const int x = 24080;
    std::cout << 24080;
    for (int i = 0; i < 24080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24081(int& val) {
    func_in_24081(val);
    const int x = 24081;
    std::cout << 24081;
    for (int i = 0; i < 24081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24081(int& val) {
    const int x = 24081;
    std::cout << 24081;
    for (int i = 0; i < 24081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24082(int& val) {
    func_in_24082(val);
    const int x = 24082;
    std::cout << 24082;
    for (int i = 0; i < 24082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24082(int& val) {
    const int x = 24082;
    std::cout << 24082;
    for (int i = 0; i < 24082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24083(int& val) {
    func_in_24083(val);
    const int x = 24083;
    std::cout << 24083;
    for (int i = 0; i < 24083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24083(int& val) {
    const int x = 24083;
    std::cout << 24083;
    for (int i = 0; i < 24083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24084(int& val) {
    func_in_24084(val);
    const int x = 24084;
    std::cout << 24084;
    for (int i = 0; i < 24084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24084(int& val) {
    const int x = 24084;
    std::cout << 24084;
    for (int i = 0; i < 24084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24085(int& val) {
    func_in_24085(val);
    const int x = 24085;
    std::cout << 24085;
    for (int i = 0; i < 24085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24085(int& val) {
    const int x = 24085;
    std::cout << 24085;
    for (int i = 0; i < 24085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24086(int& val) {
    func_in_24086(val);
    const int x = 24086;
    std::cout << 24086;
    for (int i = 0; i < 24086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24086(int& val) {
    const int x = 24086;
    std::cout << 24086;
    for (int i = 0; i < 24086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24087(int& val) {
    func_in_24087(val);
    const int x = 24087;
    std::cout << 24087;
    for (int i = 0; i < 24087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24087(int& val) {
    const int x = 24087;
    std::cout << 24087;
    for (int i = 0; i < 24087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24088(int& val) {
    func_in_24088(val);
    const int x = 24088;
    std::cout << 24088;
    for (int i = 0; i < 24088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24088(int& val) {
    const int x = 24088;
    std::cout << 24088;
    for (int i = 0; i < 24088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24089(int& val) {
    func_in_24089(val);
    const int x = 24089;
    std::cout << 24089;
    for (int i = 0; i < 24089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24089(int& val) {
    const int x = 24089;
    std::cout << 24089;
    for (int i = 0; i < 24089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24090(int& val) {
    func_in_24090(val);
    const int x = 24090;
    std::cout << 24090;
    for (int i = 0; i < 24090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24090(int& val) {
    const int x = 24090;
    std::cout << 24090;
    for (int i = 0; i < 24090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24091(int& val) {
    func_in_24091(val);
    const int x = 24091;
    std::cout << 24091;
    for (int i = 0; i < 24091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24091(int& val) {
    const int x = 24091;
    std::cout << 24091;
    for (int i = 0; i < 24091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24092(int& val) {
    func_in_24092(val);
    const int x = 24092;
    std::cout << 24092;
    for (int i = 0; i < 24092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24092(int& val) {
    const int x = 24092;
    std::cout << 24092;
    for (int i = 0; i < 24092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24093(int& val) {
    func_in_24093(val);
    const int x = 24093;
    std::cout << 24093;
    for (int i = 0; i < 24093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24093(int& val) {
    const int x = 24093;
    std::cout << 24093;
    for (int i = 0; i < 24093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24094(int& val) {
    func_in_24094(val);
    const int x = 24094;
    std::cout << 24094;
    for (int i = 0; i < 24094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24094(int& val) {
    const int x = 24094;
    std::cout << 24094;
    for (int i = 0; i < 24094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24095(int& val) {
    func_in_24095(val);
    const int x = 24095;
    std::cout << 24095;
    for (int i = 0; i < 24095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24095(int& val) {
    const int x = 24095;
    std::cout << 24095;
    for (int i = 0; i < 24095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24096(int& val) {
    func_in_24096(val);
    const int x = 24096;
    std::cout << 24096;
    for (int i = 0; i < 24096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24096(int& val) {
    const int x = 24096;
    std::cout << 24096;
    for (int i = 0; i < 24096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24097(int& val) {
    func_in_24097(val);
    const int x = 24097;
    std::cout << 24097;
    for (int i = 0; i < 24097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24097(int& val) {
    const int x = 24097;
    std::cout << 24097;
    for (int i = 0; i < 24097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24098(int& val) {
    func_in_24098(val);
    const int x = 24098;
    std::cout << 24098;
    for (int i = 0; i < 24098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24098(int& val) {
    const int x = 24098;
    std::cout << 24098;
    for (int i = 0; i < 24098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24099(int& val) {
    func_in_24099(val);
    const int x = 24099;
    std::cout << 24099;
    for (int i = 0; i < 24099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24099(int& val) {
    const int x = 24099;
    std::cout << 24099;
    for (int i = 0; i < 24099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24100(int& val) {
    func_in_24100(val);
    const int x = 24100;
    std::cout << 24100;
    for (int i = 0; i < 24100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24100(int& val) {
    const int x = 24100;
    std::cout << 24100;
    for (int i = 0; i < 24100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24101(int& val) {
    func_in_24101(val);
    const int x = 24101;
    std::cout << 24101;
    for (int i = 0; i < 24101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24101(int& val) {
    const int x = 24101;
    std::cout << 24101;
    for (int i = 0; i < 24101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24102(int& val) {
    func_in_24102(val);
    const int x = 24102;
    std::cout << 24102;
    for (int i = 0; i < 24102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24102(int& val) {
    const int x = 24102;
    std::cout << 24102;
    for (int i = 0; i < 24102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24103(int& val) {
    func_in_24103(val);
    const int x = 24103;
    std::cout << 24103;
    for (int i = 0; i < 24103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24103(int& val) {
    const int x = 24103;
    std::cout << 24103;
    for (int i = 0; i < 24103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24104(int& val) {
    func_in_24104(val);
    const int x = 24104;
    std::cout << 24104;
    for (int i = 0; i < 24104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24104(int& val) {
    const int x = 24104;
    std::cout << 24104;
    for (int i = 0; i < 24104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24105(int& val) {
    func_in_24105(val);
    const int x = 24105;
    std::cout << 24105;
    for (int i = 0; i < 24105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24105(int& val) {
    const int x = 24105;
    std::cout << 24105;
    for (int i = 0; i < 24105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24106(int& val) {
    func_in_24106(val);
    const int x = 24106;
    std::cout << 24106;
    for (int i = 0; i < 24106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24106(int& val) {
    const int x = 24106;
    std::cout << 24106;
    for (int i = 0; i < 24106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24107(int& val) {
    func_in_24107(val);
    const int x = 24107;
    std::cout << 24107;
    for (int i = 0; i < 24107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24107(int& val) {
    const int x = 24107;
    std::cout << 24107;
    for (int i = 0; i < 24107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24108(int& val) {
    func_in_24108(val);
    const int x = 24108;
    std::cout << 24108;
    for (int i = 0; i < 24108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24108(int& val) {
    const int x = 24108;
    std::cout << 24108;
    for (int i = 0; i < 24108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24109(int& val) {
    func_in_24109(val);
    const int x = 24109;
    std::cout << 24109;
    for (int i = 0; i < 24109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24109(int& val) {
    const int x = 24109;
    std::cout << 24109;
    for (int i = 0; i < 24109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24110(int& val) {
    func_in_24110(val);
    const int x = 24110;
    std::cout << 24110;
    for (int i = 0; i < 24110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24110(int& val) {
    const int x = 24110;
    std::cout << 24110;
    for (int i = 0; i < 24110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24111(int& val) {
    func_in_24111(val);
    const int x = 24111;
    std::cout << 24111;
    for (int i = 0; i < 24111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24111(int& val) {
    const int x = 24111;
    std::cout << 24111;
    for (int i = 0; i < 24111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24112(int& val) {
    func_in_24112(val);
    const int x = 24112;
    std::cout << 24112;
    for (int i = 0; i < 24112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24112(int& val) {
    const int x = 24112;
    std::cout << 24112;
    for (int i = 0; i < 24112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24113(int& val) {
    func_in_24113(val);
    const int x = 24113;
    std::cout << 24113;
    for (int i = 0; i < 24113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24113(int& val) {
    const int x = 24113;
    std::cout << 24113;
    for (int i = 0; i < 24113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24114(int& val) {
    func_in_24114(val);
    const int x = 24114;
    std::cout << 24114;
    for (int i = 0; i < 24114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24114(int& val) {
    const int x = 24114;
    std::cout << 24114;
    for (int i = 0; i < 24114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24115(int& val) {
    func_in_24115(val);
    const int x = 24115;
    std::cout << 24115;
    for (int i = 0; i < 24115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24115(int& val) {
    const int x = 24115;
    std::cout << 24115;
    for (int i = 0; i < 24115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24116(int& val) {
    func_in_24116(val);
    const int x = 24116;
    std::cout << 24116;
    for (int i = 0; i < 24116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24116(int& val) {
    const int x = 24116;
    std::cout << 24116;
    for (int i = 0; i < 24116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24117(int& val) {
    func_in_24117(val);
    const int x = 24117;
    std::cout << 24117;
    for (int i = 0; i < 24117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24117(int& val) {
    const int x = 24117;
    std::cout << 24117;
    for (int i = 0; i < 24117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24118(int& val) {
    func_in_24118(val);
    const int x = 24118;
    std::cout << 24118;
    for (int i = 0; i < 24118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24118(int& val) {
    const int x = 24118;
    std::cout << 24118;
    for (int i = 0; i < 24118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24119(int& val) {
    func_in_24119(val);
    const int x = 24119;
    std::cout << 24119;
    for (int i = 0; i < 24119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24119(int& val) {
    const int x = 24119;
    std::cout << 24119;
    for (int i = 0; i < 24119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24120(int& val) {
    func_in_24120(val);
    const int x = 24120;
    std::cout << 24120;
    for (int i = 0; i < 24120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24120(int& val) {
    const int x = 24120;
    std::cout << 24120;
    for (int i = 0; i < 24120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24121(int& val) {
    func_in_24121(val);
    const int x = 24121;
    std::cout << 24121;
    for (int i = 0; i < 24121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24121(int& val) {
    const int x = 24121;
    std::cout << 24121;
    for (int i = 0; i < 24121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24122(int& val) {
    func_in_24122(val);
    const int x = 24122;
    std::cout << 24122;
    for (int i = 0; i < 24122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24122(int& val) {
    const int x = 24122;
    std::cout << 24122;
    for (int i = 0; i < 24122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24123(int& val) {
    func_in_24123(val);
    const int x = 24123;
    std::cout << 24123;
    for (int i = 0; i < 24123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24123(int& val) {
    const int x = 24123;
    std::cout << 24123;
    for (int i = 0; i < 24123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24124(int& val) {
    func_in_24124(val);
    const int x = 24124;
    std::cout << 24124;
    for (int i = 0; i < 24124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24124(int& val) {
    const int x = 24124;
    std::cout << 24124;
    for (int i = 0; i < 24124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24125(int& val) {
    func_in_24125(val);
    const int x = 24125;
    std::cout << 24125;
    for (int i = 0; i < 24125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24125(int& val) {
    const int x = 24125;
    std::cout << 24125;
    for (int i = 0; i < 24125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24126(int& val) {
    func_in_24126(val);
    const int x = 24126;
    std::cout << 24126;
    for (int i = 0; i < 24126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24126(int& val) {
    const int x = 24126;
    std::cout << 24126;
    for (int i = 0; i < 24126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24127(int& val) {
    func_in_24127(val);
    const int x = 24127;
    std::cout << 24127;
    for (int i = 0; i < 24127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24127(int& val) {
    const int x = 24127;
    std::cout << 24127;
    for (int i = 0; i < 24127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24128(int& val) {
    func_in_24128(val);
    const int x = 24128;
    std::cout << 24128;
    for (int i = 0; i < 24128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24128(int& val) {
    const int x = 24128;
    std::cout << 24128;
    for (int i = 0; i < 24128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24129(int& val) {
    func_in_24129(val);
    const int x = 24129;
    std::cout << 24129;
    for (int i = 0; i < 24129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24129(int& val) {
    const int x = 24129;
    std::cout << 24129;
    for (int i = 0; i < 24129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24130(int& val) {
    func_in_24130(val);
    const int x = 24130;
    std::cout << 24130;
    for (int i = 0; i < 24130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24130(int& val) {
    const int x = 24130;
    std::cout << 24130;
    for (int i = 0; i < 24130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24131(int& val) {
    func_in_24131(val);
    const int x = 24131;
    std::cout << 24131;
    for (int i = 0; i < 24131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24131(int& val) {
    const int x = 24131;
    std::cout << 24131;
    for (int i = 0; i < 24131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24132(int& val) {
    func_in_24132(val);
    const int x = 24132;
    std::cout << 24132;
    for (int i = 0; i < 24132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24132(int& val) {
    const int x = 24132;
    std::cout << 24132;
    for (int i = 0; i < 24132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24133(int& val) {
    func_in_24133(val);
    const int x = 24133;
    std::cout << 24133;
    for (int i = 0; i < 24133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24133(int& val) {
    const int x = 24133;
    std::cout << 24133;
    for (int i = 0; i < 24133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24134(int& val) {
    func_in_24134(val);
    const int x = 24134;
    std::cout << 24134;
    for (int i = 0; i < 24134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24134(int& val) {
    const int x = 24134;
    std::cout << 24134;
    for (int i = 0; i < 24134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24135(int& val) {
    func_in_24135(val);
    const int x = 24135;
    std::cout << 24135;
    for (int i = 0; i < 24135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24135(int& val) {
    const int x = 24135;
    std::cout << 24135;
    for (int i = 0; i < 24135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24136(int& val) {
    func_in_24136(val);
    const int x = 24136;
    std::cout << 24136;
    for (int i = 0; i < 24136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24136(int& val) {
    const int x = 24136;
    std::cout << 24136;
    for (int i = 0; i < 24136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24137(int& val) {
    func_in_24137(val);
    const int x = 24137;
    std::cout << 24137;
    for (int i = 0; i < 24137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24137(int& val) {
    const int x = 24137;
    std::cout << 24137;
    for (int i = 0; i < 24137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24138(int& val) {
    func_in_24138(val);
    const int x = 24138;
    std::cout << 24138;
    for (int i = 0; i < 24138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24138(int& val) {
    const int x = 24138;
    std::cout << 24138;
    for (int i = 0; i < 24138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24139(int& val) {
    func_in_24139(val);
    const int x = 24139;
    std::cout << 24139;
    for (int i = 0; i < 24139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24139(int& val) {
    const int x = 24139;
    std::cout << 24139;
    for (int i = 0; i < 24139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24140(int& val) {
    func_in_24140(val);
    const int x = 24140;
    std::cout << 24140;
    for (int i = 0; i < 24140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24140(int& val) {
    const int x = 24140;
    std::cout << 24140;
    for (int i = 0; i < 24140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24141(int& val) {
    func_in_24141(val);
    const int x = 24141;
    std::cout << 24141;
    for (int i = 0; i < 24141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24141(int& val) {
    const int x = 24141;
    std::cout << 24141;
    for (int i = 0; i < 24141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24142(int& val) {
    func_in_24142(val);
    const int x = 24142;
    std::cout << 24142;
    for (int i = 0; i < 24142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24142(int& val) {
    const int x = 24142;
    std::cout << 24142;
    for (int i = 0; i < 24142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24143(int& val) {
    func_in_24143(val);
    const int x = 24143;
    std::cout << 24143;
    for (int i = 0; i < 24143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24143(int& val) {
    const int x = 24143;
    std::cout << 24143;
    for (int i = 0; i < 24143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24144(int& val) {
    func_in_24144(val);
    const int x = 24144;
    std::cout << 24144;
    for (int i = 0; i < 24144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24144(int& val) {
    const int x = 24144;
    std::cout << 24144;
    for (int i = 0; i < 24144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24145(int& val) {
    func_in_24145(val);
    const int x = 24145;
    std::cout << 24145;
    for (int i = 0; i < 24145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24145(int& val) {
    const int x = 24145;
    std::cout << 24145;
    for (int i = 0; i < 24145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24146(int& val) {
    func_in_24146(val);
    const int x = 24146;
    std::cout << 24146;
    for (int i = 0; i < 24146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24146(int& val) {
    const int x = 24146;
    std::cout << 24146;
    for (int i = 0; i < 24146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24147(int& val) {
    func_in_24147(val);
    const int x = 24147;
    std::cout << 24147;
    for (int i = 0; i < 24147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24147(int& val) {
    const int x = 24147;
    std::cout << 24147;
    for (int i = 0; i < 24147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24148(int& val) {
    func_in_24148(val);
    const int x = 24148;
    std::cout << 24148;
    for (int i = 0; i < 24148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24148(int& val) {
    const int x = 24148;
    std::cout << 24148;
    for (int i = 0; i < 24148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24149(int& val) {
    func_in_24149(val);
    const int x = 24149;
    std::cout << 24149;
    for (int i = 0; i < 24149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24149(int& val) {
    const int x = 24149;
    std::cout << 24149;
    for (int i = 0; i < 24149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24150(int& val) {
    func_in_24150(val);
    const int x = 24150;
    std::cout << 24150;
    for (int i = 0; i < 24150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24150(int& val) {
    const int x = 24150;
    std::cout << 24150;
    for (int i = 0; i < 24150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24151(int& val) {
    func_in_24151(val);
    const int x = 24151;
    std::cout << 24151;
    for (int i = 0; i < 24151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24151(int& val) {
    const int x = 24151;
    std::cout << 24151;
    for (int i = 0; i < 24151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24152(int& val) {
    func_in_24152(val);
    const int x = 24152;
    std::cout << 24152;
    for (int i = 0; i < 24152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24152(int& val) {
    const int x = 24152;
    std::cout << 24152;
    for (int i = 0; i < 24152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24153(int& val) {
    func_in_24153(val);
    const int x = 24153;
    std::cout << 24153;
    for (int i = 0; i < 24153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24153(int& val) {
    const int x = 24153;
    std::cout << 24153;
    for (int i = 0; i < 24153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24154(int& val) {
    func_in_24154(val);
    const int x = 24154;
    std::cout << 24154;
    for (int i = 0; i < 24154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24154(int& val) {
    const int x = 24154;
    std::cout << 24154;
    for (int i = 0; i < 24154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24155(int& val) {
    func_in_24155(val);
    const int x = 24155;
    std::cout << 24155;
    for (int i = 0; i < 24155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24155(int& val) {
    const int x = 24155;
    std::cout << 24155;
    for (int i = 0; i < 24155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24156(int& val) {
    func_in_24156(val);
    const int x = 24156;
    std::cout << 24156;
    for (int i = 0; i < 24156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24156(int& val) {
    const int x = 24156;
    std::cout << 24156;
    for (int i = 0; i < 24156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24157(int& val) {
    func_in_24157(val);
    const int x = 24157;
    std::cout << 24157;
    for (int i = 0; i < 24157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24157(int& val) {
    const int x = 24157;
    std::cout << 24157;
    for (int i = 0; i < 24157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24158(int& val) {
    func_in_24158(val);
    const int x = 24158;
    std::cout << 24158;
    for (int i = 0; i < 24158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24158(int& val) {
    const int x = 24158;
    std::cout << 24158;
    for (int i = 0; i < 24158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24159(int& val) {
    func_in_24159(val);
    const int x = 24159;
    std::cout << 24159;
    for (int i = 0; i < 24159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24159(int& val) {
    const int x = 24159;
    std::cout << 24159;
    for (int i = 0; i < 24159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24160(int& val) {
    func_in_24160(val);
    const int x = 24160;
    std::cout << 24160;
    for (int i = 0; i < 24160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24160(int& val) {
    const int x = 24160;
    std::cout << 24160;
    for (int i = 0; i < 24160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24161(int& val) {
    func_in_24161(val);
    const int x = 24161;
    std::cout << 24161;
    for (int i = 0; i < 24161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24161(int& val) {
    const int x = 24161;
    std::cout << 24161;
    for (int i = 0; i < 24161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24162(int& val) {
    func_in_24162(val);
    const int x = 24162;
    std::cout << 24162;
    for (int i = 0; i < 24162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24162(int& val) {
    const int x = 24162;
    std::cout << 24162;
    for (int i = 0; i < 24162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24163(int& val) {
    func_in_24163(val);
    const int x = 24163;
    std::cout << 24163;
    for (int i = 0; i < 24163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24163(int& val) {
    const int x = 24163;
    std::cout << 24163;
    for (int i = 0; i < 24163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24164(int& val) {
    func_in_24164(val);
    const int x = 24164;
    std::cout << 24164;
    for (int i = 0; i < 24164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24164(int& val) {
    const int x = 24164;
    std::cout << 24164;
    for (int i = 0; i < 24164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24165(int& val) {
    func_in_24165(val);
    const int x = 24165;
    std::cout << 24165;
    for (int i = 0; i < 24165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24165(int& val) {
    const int x = 24165;
    std::cout << 24165;
    for (int i = 0; i < 24165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24166(int& val) {
    func_in_24166(val);
    const int x = 24166;
    std::cout << 24166;
    for (int i = 0; i < 24166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24166(int& val) {
    const int x = 24166;
    std::cout << 24166;
    for (int i = 0; i < 24166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24167(int& val) {
    func_in_24167(val);
    const int x = 24167;
    std::cout << 24167;
    for (int i = 0; i < 24167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24167(int& val) {
    const int x = 24167;
    std::cout << 24167;
    for (int i = 0; i < 24167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24168(int& val) {
    func_in_24168(val);
    const int x = 24168;
    std::cout << 24168;
    for (int i = 0; i < 24168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24168(int& val) {
    const int x = 24168;
    std::cout << 24168;
    for (int i = 0; i < 24168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24169(int& val) {
    func_in_24169(val);
    const int x = 24169;
    std::cout << 24169;
    for (int i = 0; i < 24169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24169(int& val) {
    const int x = 24169;
    std::cout << 24169;
    for (int i = 0; i < 24169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24170(int& val) {
    func_in_24170(val);
    const int x = 24170;
    std::cout << 24170;
    for (int i = 0; i < 24170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24170(int& val) {
    const int x = 24170;
    std::cout << 24170;
    for (int i = 0; i < 24170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24171(int& val) {
    func_in_24171(val);
    const int x = 24171;
    std::cout << 24171;
    for (int i = 0; i < 24171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24171(int& val) {
    const int x = 24171;
    std::cout << 24171;
    for (int i = 0; i < 24171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24172(int& val) {
    func_in_24172(val);
    const int x = 24172;
    std::cout << 24172;
    for (int i = 0; i < 24172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24172(int& val) {
    const int x = 24172;
    std::cout << 24172;
    for (int i = 0; i < 24172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24173(int& val) {
    func_in_24173(val);
    const int x = 24173;
    std::cout << 24173;
    for (int i = 0; i < 24173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24173(int& val) {
    const int x = 24173;
    std::cout << 24173;
    for (int i = 0; i < 24173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24174(int& val) {
    func_in_24174(val);
    const int x = 24174;
    std::cout << 24174;
    for (int i = 0; i < 24174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24174(int& val) {
    const int x = 24174;
    std::cout << 24174;
    for (int i = 0; i < 24174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24175(int& val) {
    func_in_24175(val);
    const int x = 24175;
    std::cout << 24175;
    for (int i = 0; i < 24175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24175(int& val) {
    const int x = 24175;
    std::cout << 24175;
    for (int i = 0; i < 24175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24176(int& val) {
    func_in_24176(val);
    const int x = 24176;
    std::cout << 24176;
    for (int i = 0; i < 24176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24176(int& val) {
    const int x = 24176;
    std::cout << 24176;
    for (int i = 0; i < 24176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24177(int& val) {
    func_in_24177(val);
    const int x = 24177;
    std::cout << 24177;
    for (int i = 0; i < 24177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24177(int& val) {
    const int x = 24177;
    std::cout << 24177;
    for (int i = 0; i < 24177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24178(int& val) {
    func_in_24178(val);
    const int x = 24178;
    std::cout << 24178;
    for (int i = 0; i < 24178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24178(int& val) {
    const int x = 24178;
    std::cout << 24178;
    for (int i = 0; i < 24178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24179(int& val) {
    func_in_24179(val);
    const int x = 24179;
    std::cout << 24179;
    for (int i = 0; i < 24179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24179(int& val) {
    const int x = 24179;
    std::cout << 24179;
    for (int i = 0; i < 24179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24180(int& val) {
    func_in_24180(val);
    const int x = 24180;
    std::cout << 24180;
    for (int i = 0; i < 24180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24180(int& val) {
    const int x = 24180;
    std::cout << 24180;
    for (int i = 0; i < 24180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24181(int& val) {
    func_in_24181(val);
    const int x = 24181;
    std::cout << 24181;
    for (int i = 0; i < 24181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24181(int& val) {
    const int x = 24181;
    std::cout << 24181;
    for (int i = 0; i < 24181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24182(int& val) {
    func_in_24182(val);
    const int x = 24182;
    std::cout << 24182;
    for (int i = 0; i < 24182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24182(int& val) {
    const int x = 24182;
    std::cout << 24182;
    for (int i = 0; i < 24182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24183(int& val) {
    func_in_24183(val);
    const int x = 24183;
    std::cout << 24183;
    for (int i = 0; i < 24183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24183(int& val) {
    const int x = 24183;
    std::cout << 24183;
    for (int i = 0; i < 24183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24184(int& val) {
    func_in_24184(val);
    const int x = 24184;
    std::cout << 24184;
    for (int i = 0; i < 24184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24184(int& val) {
    const int x = 24184;
    std::cout << 24184;
    for (int i = 0; i < 24184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24185(int& val) {
    func_in_24185(val);
    const int x = 24185;
    std::cout << 24185;
    for (int i = 0; i < 24185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24185(int& val) {
    const int x = 24185;
    std::cout << 24185;
    for (int i = 0; i < 24185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24186(int& val) {
    func_in_24186(val);
    const int x = 24186;
    std::cout << 24186;
    for (int i = 0; i < 24186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24186(int& val) {
    const int x = 24186;
    std::cout << 24186;
    for (int i = 0; i < 24186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24187(int& val) {
    func_in_24187(val);
    const int x = 24187;
    std::cout << 24187;
    for (int i = 0; i < 24187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24187(int& val) {
    const int x = 24187;
    std::cout << 24187;
    for (int i = 0; i < 24187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24188(int& val) {
    func_in_24188(val);
    const int x = 24188;
    std::cout << 24188;
    for (int i = 0; i < 24188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24188(int& val) {
    const int x = 24188;
    std::cout << 24188;
    for (int i = 0; i < 24188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24189(int& val) {
    func_in_24189(val);
    const int x = 24189;
    std::cout << 24189;
    for (int i = 0; i < 24189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24189(int& val) {
    const int x = 24189;
    std::cout << 24189;
    for (int i = 0; i < 24189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24190(int& val) {
    func_in_24190(val);
    const int x = 24190;
    std::cout << 24190;
    for (int i = 0; i < 24190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24190(int& val) {
    const int x = 24190;
    std::cout << 24190;
    for (int i = 0; i < 24190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24191(int& val) {
    func_in_24191(val);
    const int x = 24191;
    std::cout << 24191;
    for (int i = 0; i < 24191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24191(int& val) {
    const int x = 24191;
    std::cout << 24191;
    for (int i = 0; i < 24191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24192(int& val) {
    func_in_24192(val);
    const int x = 24192;
    std::cout << 24192;
    for (int i = 0; i < 24192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24192(int& val) {
    const int x = 24192;
    std::cout << 24192;
    for (int i = 0; i < 24192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24193(int& val) {
    func_in_24193(val);
    const int x = 24193;
    std::cout << 24193;
    for (int i = 0; i < 24193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24193(int& val) {
    const int x = 24193;
    std::cout << 24193;
    for (int i = 0; i < 24193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24194(int& val) {
    func_in_24194(val);
    const int x = 24194;
    std::cout << 24194;
    for (int i = 0; i < 24194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24194(int& val) {
    const int x = 24194;
    std::cout << 24194;
    for (int i = 0; i < 24194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24195(int& val) {
    func_in_24195(val);
    const int x = 24195;
    std::cout << 24195;
    for (int i = 0; i < 24195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24195(int& val) {
    const int x = 24195;
    std::cout << 24195;
    for (int i = 0; i < 24195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24196(int& val) {
    func_in_24196(val);
    const int x = 24196;
    std::cout << 24196;
    for (int i = 0; i < 24196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24196(int& val) {
    const int x = 24196;
    std::cout << 24196;
    for (int i = 0; i < 24196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24197(int& val) {
    func_in_24197(val);
    const int x = 24197;
    std::cout << 24197;
    for (int i = 0; i < 24197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24197(int& val) {
    const int x = 24197;
    std::cout << 24197;
    for (int i = 0; i < 24197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24198(int& val) {
    func_in_24198(val);
    const int x = 24198;
    std::cout << 24198;
    for (int i = 0; i < 24198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24198(int& val) {
    const int x = 24198;
    std::cout << 24198;
    for (int i = 0; i < 24198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24199(int& val) {
    func_in_24199(val);
    const int x = 24199;
    std::cout << 24199;
    for (int i = 0; i < 24199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24199(int& val) {
    const int x = 24199;
    std::cout << 24199;
    for (int i = 0; i < 24199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24200(int& val) {
    func_in_24200(val);
    const int x = 24200;
    std::cout << 24200;
    for (int i = 0; i < 24200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24200(int& val) {
    const int x = 24200;
    std::cout << 24200;
    for (int i = 0; i < 24200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24201(int& val) {
    func_in_24201(val);
    const int x = 24201;
    std::cout << 24201;
    for (int i = 0; i < 24201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24201(int& val) {
    const int x = 24201;
    std::cout << 24201;
    for (int i = 0; i < 24201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24202(int& val) {
    func_in_24202(val);
    const int x = 24202;
    std::cout << 24202;
    for (int i = 0; i < 24202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24202(int& val) {
    const int x = 24202;
    std::cout << 24202;
    for (int i = 0; i < 24202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24203(int& val) {
    func_in_24203(val);
    const int x = 24203;
    std::cout << 24203;
    for (int i = 0; i < 24203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24203(int& val) {
    const int x = 24203;
    std::cout << 24203;
    for (int i = 0; i < 24203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24204(int& val) {
    func_in_24204(val);
    const int x = 24204;
    std::cout << 24204;
    for (int i = 0; i < 24204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24204(int& val) {
    const int x = 24204;
    std::cout << 24204;
    for (int i = 0; i < 24204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24205(int& val) {
    func_in_24205(val);
    const int x = 24205;
    std::cout << 24205;
    for (int i = 0; i < 24205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24205(int& val) {
    const int x = 24205;
    std::cout << 24205;
    for (int i = 0; i < 24205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24206(int& val) {
    func_in_24206(val);
    const int x = 24206;
    std::cout << 24206;
    for (int i = 0; i < 24206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24206(int& val) {
    const int x = 24206;
    std::cout << 24206;
    for (int i = 0; i < 24206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24207(int& val) {
    func_in_24207(val);
    const int x = 24207;
    std::cout << 24207;
    for (int i = 0; i < 24207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24207(int& val) {
    const int x = 24207;
    std::cout << 24207;
    for (int i = 0; i < 24207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24208(int& val) {
    func_in_24208(val);
    const int x = 24208;
    std::cout << 24208;
    for (int i = 0; i < 24208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24208(int& val) {
    const int x = 24208;
    std::cout << 24208;
    for (int i = 0; i < 24208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24209(int& val) {
    func_in_24209(val);
    const int x = 24209;
    std::cout << 24209;
    for (int i = 0; i < 24209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24209(int& val) {
    const int x = 24209;
    std::cout << 24209;
    for (int i = 0; i < 24209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24210(int& val) {
    func_in_24210(val);
    const int x = 24210;
    std::cout << 24210;
    for (int i = 0; i < 24210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24210(int& val) {
    const int x = 24210;
    std::cout << 24210;
    for (int i = 0; i < 24210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24211(int& val) {
    func_in_24211(val);
    const int x = 24211;
    std::cout << 24211;
    for (int i = 0; i < 24211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24211(int& val) {
    const int x = 24211;
    std::cout << 24211;
    for (int i = 0; i < 24211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24212(int& val) {
    func_in_24212(val);
    const int x = 24212;
    std::cout << 24212;
    for (int i = 0; i < 24212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24212(int& val) {
    const int x = 24212;
    std::cout << 24212;
    for (int i = 0; i < 24212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24213(int& val) {
    func_in_24213(val);
    const int x = 24213;
    std::cout << 24213;
    for (int i = 0; i < 24213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24213(int& val) {
    const int x = 24213;
    std::cout << 24213;
    for (int i = 0; i < 24213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24214(int& val) {
    func_in_24214(val);
    const int x = 24214;
    std::cout << 24214;
    for (int i = 0; i < 24214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24214(int& val) {
    const int x = 24214;
    std::cout << 24214;
    for (int i = 0; i < 24214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24215(int& val) {
    func_in_24215(val);
    const int x = 24215;
    std::cout << 24215;
    for (int i = 0; i < 24215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24215(int& val) {
    const int x = 24215;
    std::cout << 24215;
    for (int i = 0; i < 24215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24216(int& val) {
    func_in_24216(val);
    const int x = 24216;
    std::cout << 24216;
    for (int i = 0; i < 24216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24216(int& val) {
    const int x = 24216;
    std::cout << 24216;
    for (int i = 0; i < 24216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24217(int& val) {
    func_in_24217(val);
    const int x = 24217;
    std::cout << 24217;
    for (int i = 0; i < 24217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24217(int& val) {
    const int x = 24217;
    std::cout << 24217;
    for (int i = 0; i < 24217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24218(int& val) {
    func_in_24218(val);
    const int x = 24218;
    std::cout << 24218;
    for (int i = 0; i < 24218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24218(int& val) {
    const int x = 24218;
    std::cout << 24218;
    for (int i = 0; i < 24218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24219(int& val) {
    func_in_24219(val);
    const int x = 24219;
    std::cout << 24219;
    for (int i = 0; i < 24219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24219(int& val) {
    const int x = 24219;
    std::cout << 24219;
    for (int i = 0; i < 24219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24220(int& val) {
    func_in_24220(val);
    const int x = 24220;
    std::cout << 24220;
    for (int i = 0; i < 24220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24220(int& val) {
    const int x = 24220;
    std::cout << 24220;
    for (int i = 0; i < 24220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24221(int& val) {
    func_in_24221(val);
    const int x = 24221;
    std::cout << 24221;
    for (int i = 0; i < 24221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24221(int& val) {
    const int x = 24221;
    std::cout << 24221;
    for (int i = 0; i < 24221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24222(int& val) {
    func_in_24222(val);
    const int x = 24222;
    std::cout << 24222;
    for (int i = 0; i < 24222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24222(int& val) {
    const int x = 24222;
    std::cout << 24222;
    for (int i = 0; i < 24222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24223(int& val) {
    func_in_24223(val);
    const int x = 24223;
    std::cout << 24223;
    for (int i = 0; i < 24223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24223(int& val) {
    const int x = 24223;
    std::cout << 24223;
    for (int i = 0; i < 24223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24224(int& val) {
    func_in_24224(val);
    const int x = 24224;
    std::cout << 24224;
    for (int i = 0; i < 24224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24224(int& val) {
    const int x = 24224;
    std::cout << 24224;
    for (int i = 0; i < 24224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24225(int& val) {
    func_in_24225(val);
    const int x = 24225;
    std::cout << 24225;
    for (int i = 0; i < 24225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24225(int& val) {
    const int x = 24225;
    std::cout << 24225;
    for (int i = 0; i < 24225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24226(int& val) {
    func_in_24226(val);
    const int x = 24226;
    std::cout << 24226;
    for (int i = 0; i < 24226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24226(int& val) {
    const int x = 24226;
    std::cout << 24226;
    for (int i = 0; i < 24226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24227(int& val) {
    func_in_24227(val);
    const int x = 24227;
    std::cout << 24227;
    for (int i = 0; i < 24227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24227(int& val) {
    const int x = 24227;
    std::cout << 24227;
    for (int i = 0; i < 24227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24228(int& val) {
    func_in_24228(val);
    const int x = 24228;
    std::cout << 24228;
    for (int i = 0; i < 24228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24228(int& val) {
    const int x = 24228;
    std::cout << 24228;
    for (int i = 0; i < 24228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24229(int& val) {
    func_in_24229(val);
    const int x = 24229;
    std::cout << 24229;
    for (int i = 0; i < 24229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24229(int& val) {
    const int x = 24229;
    std::cout << 24229;
    for (int i = 0; i < 24229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24230(int& val) {
    func_in_24230(val);
    const int x = 24230;
    std::cout << 24230;
    for (int i = 0; i < 24230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24230(int& val) {
    const int x = 24230;
    std::cout << 24230;
    for (int i = 0; i < 24230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24231(int& val) {
    func_in_24231(val);
    const int x = 24231;
    std::cout << 24231;
    for (int i = 0; i < 24231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24231(int& val) {
    const int x = 24231;
    std::cout << 24231;
    for (int i = 0; i < 24231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24232(int& val) {
    func_in_24232(val);
    const int x = 24232;
    std::cout << 24232;
    for (int i = 0; i < 24232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24232(int& val) {
    const int x = 24232;
    std::cout << 24232;
    for (int i = 0; i < 24232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24233(int& val) {
    func_in_24233(val);
    const int x = 24233;
    std::cout << 24233;
    for (int i = 0; i < 24233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24233(int& val) {
    const int x = 24233;
    std::cout << 24233;
    for (int i = 0; i < 24233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24234(int& val) {
    func_in_24234(val);
    const int x = 24234;
    std::cout << 24234;
    for (int i = 0; i < 24234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24234(int& val) {
    const int x = 24234;
    std::cout << 24234;
    for (int i = 0; i < 24234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24235(int& val) {
    func_in_24235(val);
    const int x = 24235;
    std::cout << 24235;
    for (int i = 0; i < 24235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24235(int& val) {
    const int x = 24235;
    std::cout << 24235;
    for (int i = 0; i < 24235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24236(int& val) {
    func_in_24236(val);
    const int x = 24236;
    std::cout << 24236;
    for (int i = 0; i < 24236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24236(int& val) {
    const int x = 24236;
    std::cout << 24236;
    for (int i = 0; i < 24236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24237(int& val) {
    func_in_24237(val);
    const int x = 24237;
    std::cout << 24237;
    for (int i = 0; i < 24237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24237(int& val) {
    const int x = 24237;
    std::cout << 24237;
    for (int i = 0; i < 24237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24238(int& val) {
    func_in_24238(val);
    const int x = 24238;
    std::cout << 24238;
    for (int i = 0; i < 24238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24238(int& val) {
    const int x = 24238;
    std::cout << 24238;
    for (int i = 0; i < 24238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24239(int& val) {
    func_in_24239(val);
    const int x = 24239;
    std::cout << 24239;
    for (int i = 0; i < 24239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24239(int& val) {
    const int x = 24239;
    std::cout << 24239;
    for (int i = 0; i < 24239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24240(int& val) {
    func_in_24240(val);
    const int x = 24240;
    std::cout << 24240;
    for (int i = 0; i < 24240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24240(int& val) {
    const int x = 24240;
    std::cout << 24240;
    for (int i = 0; i < 24240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24241(int& val) {
    func_in_24241(val);
    const int x = 24241;
    std::cout << 24241;
    for (int i = 0; i < 24241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24241(int& val) {
    const int x = 24241;
    std::cout << 24241;
    for (int i = 0; i < 24241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24242(int& val) {
    func_in_24242(val);
    const int x = 24242;
    std::cout << 24242;
    for (int i = 0; i < 24242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24242(int& val) {
    const int x = 24242;
    std::cout << 24242;
    for (int i = 0; i < 24242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24243(int& val) {
    func_in_24243(val);
    const int x = 24243;
    std::cout << 24243;
    for (int i = 0; i < 24243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24243(int& val) {
    const int x = 24243;
    std::cout << 24243;
    for (int i = 0; i < 24243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24244(int& val) {
    func_in_24244(val);
    const int x = 24244;
    std::cout << 24244;
    for (int i = 0; i < 24244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24244(int& val) {
    const int x = 24244;
    std::cout << 24244;
    for (int i = 0; i < 24244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24245(int& val) {
    func_in_24245(val);
    const int x = 24245;
    std::cout << 24245;
    for (int i = 0; i < 24245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24245(int& val) {
    const int x = 24245;
    std::cout << 24245;
    for (int i = 0; i < 24245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24246(int& val) {
    func_in_24246(val);
    const int x = 24246;
    std::cout << 24246;
    for (int i = 0; i < 24246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24246(int& val) {
    const int x = 24246;
    std::cout << 24246;
    for (int i = 0; i < 24246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24247(int& val) {
    func_in_24247(val);
    const int x = 24247;
    std::cout << 24247;
    for (int i = 0; i < 24247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24247(int& val) {
    const int x = 24247;
    std::cout << 24247;
    for (int i = 0; i < 24247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24248(int& val) {
    func_in_24248(val);
    const int x = 24248;
    std::cout << 24248;
    for (int i = 0; i < 24248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24248(int& val) {
    const int x = 24248;
    std::cout << 24248;
    for (int i = 0; i < 24248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24249(int& val) {
    func_in_24249(val);
    const int x = 24249;
    std::cout << 24249;
    for (int i = 0; i < 24249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24249(int& val) {
    const int x = 24249;
    std::cout << 24249;
    for (int i = 0; i < 24249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24250(int& val) {
    func_in_24250(val);
    const int x = 24250;
    std::cout << 24250;
    for (int i = 0; i < 24250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24250(int& val) {
    const int x = 24250;
    std::cout << 24250;
    for (int i = 0; i < 24250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24251(int& val) {
    func_in_24251(val);
    const int x = 24251;
    std::cout << 24251;
    for (int i = 0; i < 24251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24251(int& val) {
    const int x = 24251;
    std::cout << 24251;
    for (int i = 0; i < 24251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24252(int& val) {
    func_in_24252(val);
    const int x = 24252;
    std::cout << 24252;
    for (int i = 0; i < 24252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24252(int& val) {
    const int x = 24252;
    std::cout << 24252;
    for (int i = 0; i < 24252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24253(int& val) {
    func_in_24253(val);
    const int x = 24253;
    std::cout << 24253;
    for (int i = 0; i < 24253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24253(int& val) {
    const int x = 24253;
    std::cout << 24253;
    for (int i = 0; i < 24253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24254(int& val) {
    func_in_24254(val);
    const int x = 24254;
    std::cout << 24254;
    for (int i = 0; i < 24254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24254(int& val) {
    const int x = 24254;
    std::cout << 24254;
    for (int i = 0; i < 24254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24255(int& val) {
    func_in_24255(val);
    const int x = 24255;
    std::cout << 24255;
    for (int i = 0; i < 24255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24255(int& val) {
    const int x = 24255;
    std::cout << 24255;
    for (int i = 0; i < 24255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24256(int& val) {
    func_in_24256(val);
    const int x = 24256;
    std::cout << 24256;
    for (int i = 0; i < 24256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24256(int& val) {
    const int x = 24256;
    std::cout << 24256;
    for (int i = 0; i < 24256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24257(int& val) {
    func_in_24257(val);
    const int x = 24257;
    std::cout << 24257;
    for (int i = 0; i < 24257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24257(int& val) {
    const int x = 24257;
    std::cout << 24257;
    for (int i = 0; i < 24257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24258(int& val) {
    func_in_24258(val);
    const int x = 24258;
    std::cout << 24258;
    for (int i = 0; i < 24258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24258(int& val) {
    const int x = 24258;
    std::cout << 24258;
    for (int i = 0; i < 24258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24259(int& val) {
    func_in_24259(val);
    const int x = 24259;
    std::cout << 24259;
    for (int i = 0; i < 24259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24259(int& val) {
    const int x = 24259;
    std::cout << 24259;
    for (int i = 0; i < 24259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24260(int& val) {
    func_in_24260(val);
    const int x = 24260;
    std::cout << 24260;
    for (int i = 0; i < 24260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24260(int& val) {
    const int x = 24260;
    std::cout << 24260;
    for (int i = 0; i < 24260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24261(int& val) {
    func_in_24261(val);
    const int x = 24261;
    std::cout << 24261;
    for (int i = 0; i < 24261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24261(int& val) {
    const int x = 24261;
    std::cout << 24261;
    for (int i = 0; i < 24261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24262(int& val) {
    func_in_24262(val);
    const int x = 24262;
    std::cout << 24262;
    for (int i = 0; i < 24262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24262(int& val) {
    const int x = 24262;
    std::cout << 24262;
    for (int i = 0; i < 24262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24263(int& val) {
    func_in_24263(val);
    const int x = 24263;
    std::cout << 24263;
    for (int i = 0; i < 24263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24263(int& val) {
    const int x = 24263;
    std::cout << 24263;
    for (int i = 0; i < 24263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24264(int& val) {
    func_in_24264(val);
    const int x = 24264;
    std::cout << 24264;
    for (int i = 0; i < 24264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24264(int& val) {
    const int x = 24264;
    std::cout << 24264;
    for (int i = 0; i < 24264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24265(int& val) {
    func_in_24265(val);
    const int x = 24265;
    std::cout << 24265;
    for (int i = 0; i < 24265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24265(int& val) {
    const int x = 24265;
    std::cout << 24265;
    for (int i = 0; i < 24265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24266(int& val) {
    func_in_24266(val);
    const int x = 24266;
    std::cout << 24266;
    for (int i = 0; i < 24266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24266(int& val) {
    const int x = 24266;
    std::cout << 24266;
    for (int i = 0; i < 24266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24267(int& val) {
    func_in_24267(val);
    const int x = 24267;
    std::cout << 24267;
    for (int i = 0; i < 24267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24267(int& val) {
    const int x = 24267;
    std::cout << 24267;
    for (int i = 0; i < 24267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24268(int& val) {
    func_in_24268(val);
    const int x = 24268;
    std::cout << 24268;
    for (int i = 0; i < 24268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24268(int& val) {
    const int x = 24268;
    std::cout << 24268;
    for (int i = 0; i < 24268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24269(int& val) {
    func_in_24269(val);
    const int x = 24269;
    std::cout << 24269;
    for (int i = 0; i < 24269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24269(int& val) {
    const int x = 24269;
    std::cout << 24269;
    for (int i = 0; i < 24269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24270(int& val) {
    func_in_24270(val);
    const int x = 24270;
    std::cout << 24270;
    for (int i = 0; i < 24270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24270(int& val) {
    const int x = 24270;
    std::cout << 24270;
    for (int i = 0; i < 24270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24271(int& val) {
    func_in_24271(val);
    const int x = 24271;
    std::cout << 24271;
    for (int i = 0; i < 24271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24271(int& val) {
    const int x = 24271;
    std::cout << 24271;
    for (int i = 0; i < 24271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24272(int& val) {
    func_in_24272(val);
    const int x = 24272;
    std::cout << 24272;
    for (int i = 0; i < 24272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24272(int& val) {
    const int x = 24272;
    std::cout << 24272;
    for (int i = 0; i < 24272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24273(int& val) {
    func_in_24273(val);
    const int x = 24273;
    std::cout << 24273;
    for (int i = 0; i < 24273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24273(int& val) {
    const int x = 24273;
    std::cout << 24273;
    for (int i = 0; i < 24273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24274(int& val) {
    func_in_24274(val);
    const int x = 24274;
    std::cout << 24274;
    for (int i = 0; i < 24274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24274(int& val) {
    const int x = 24274;
    std::cout << 24274;
    for (int i = 0; i < 24274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24275(int& val) {
    func_in_24275(val);
    const int x = 24275;
    std::cout << 24275;
    for (int i = 0; i < 24275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24275(int& val) {
    const int x = 24275;
    std::cout << 24275;
    for (int i = 0; i < 24275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24276(int& val) {
    func_in_24276(val);
    const int x = 24276;
    std::cout << 24276;
    for (int i = 0; i < 24276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24276(int& val) {
    const int x = 24276;
    std::cout << 24276;
    for (int i = 0; i < 24276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24277(int& val) {
    func_in_24277(val);
    const int x = 24277;
    std::cout << 24277;
    for (int i = 0; i < 24277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24277(int& val) {
    const int x = 24277;
    std::cout << 24277;
    for (int i = 0; i < 24277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24278(int& val) {
    func_in_24278(val);
    const int x = 24278;
    std::cout << 24278;
    for (int i = 0; i < 24278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24278(int& val) {
    const int x = 24278;
    std::cout << 24278;
    for (int i = 0; i < 24278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24279(int& val) {
    func_in_24279(val);
    const int x = 24279;
    std::cout << 24279;
    for (int i = 0; i < 24279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24279(int& val) {
    const int x = 24279;
    std::cout << 24279;
    for (int i = 0; i < 24279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24280(int& val) {
    func_in_24280(val);
    const int x = 24280;
    std::cout << 24280;
    for (int i = 0; i < 24280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24280(int& val) {
    const int x = 24280;
    std::cout << 24280;
    for (int i = 0; i < 24280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24281(int& val) {
    func_in_24281(val);
    const int x = 24281;
    std::cout << 24281;
    for (int i = 0; i < 24281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24281(int& val) {
    const int x = 24281;
    std::cout << 24281;
    for (int i = 0; i < 24281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24282(int& val) {
    func_in_24282(val);
    const int x = 24282;
    std::cout << 24282;
    for (int i = 0; i < 24282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24282(int& val) {
    const int x = 24282;
    std::cout << 24282;
    for (int i = 0; i < 24282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24283(int& val) {
    func_in_24283(val);
    const int x = 24283;
    std::cout << 24283;
    for (int i = 0; i < 24283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24283(int& val) {
    const int x = 24283;
    std::cout << 24283;
    for (int i = 0; i < 24283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24284(int& val) {
    func_in_24284(val);
    const int x = 24284;
    std::cout << 24284;
    for (int i = 0; i < 24284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24284(int& val) {
    const int x = 24284;
    std::cout << 24284;
    for (int i = 0; i < 24284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24285(int& val) {
    func_in_24285(val);
    const int x = 24285;
    std::cout << 24285;
    for (int i = 0; i < 24285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24285(int& val) {
    const int x = 24285;
    std::cout << 24285;
    for (int i = 0; i < 24285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24286(int& val) {
    func_in_24286(val);
    const int x = 24286;
    std::cout << 24286;
    for (int i = 0; i < 24286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24286(int& val) {
    const int x = 24286;
    std::cout << 24286;
    for (int i = 0; i < 24286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24287(int& val) {
    func_in_24287(val);
    const int x = 24287;
    std::cout << 24287;
    for (int i = 0; i < 24287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24287(int& val) {
    const int x = 24287;
    std::cout << 24287;
    for (int i = 0; i < 24287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24288(int& val) {
    func_in_24288(val);
    const int x = 24288;
    std::cout << 24288;
    for (int i = 0; i < 24288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24288(int& val) {
    const int x = 24288;
    std::cout << 24288;
    for (int i = 0; i < 24288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24289(int& val) {
    func_in_24289(val);
    const int x = 24289;
    std::cout << 24289;
    for (int i = 0; i < 24289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24289(int& val) {
    const int x = 24289;
    std::cout << 24289;
    for (int i = 0; i < 24289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24290(int& val) {
    func_in_24290(val);
    const int x = 24290;
    std::cout << 24290;
    for (int i = 0; i < 24290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24290(int& val) {
    const int x = 24290;
    std::cout << 24290;
    for (int i = 0; i < 24290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24291(int& val) {
    func_in_24291(val);
    const int x = 24291;
    std::cout << 24291;
    for (int i = 0; i < 24291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24291(int& val) {
    const int x = 24291;
    std::cout << 24291;
    for (int i = 0; i < 24291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24292(int& val) {
    func_in_24292(val);
    const int x = 24292;
    std::cout << 24292;
    for (int i = 0; i < 24292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24292(int& val) {
    const int x = 24292;
    std::cout << 24292;
    for (int i = 0; i < 24292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24293(int& val) {
    func_in_24293(val);
    const int x = 24293;
    std::cout << 24293;
    for (int i = 0; i < 24293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24293(int& val) {
    const int x = 24293;
    std::cout << 24293;
    for (int i = 0; i < 24293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24294(int& val) {
    func_in_24294(val);
    const int x = 24294;
    std::cout << 24294;
    for (int i = 0; i < 24294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24294(int& val) {
    const int x = 24294;
    std::cout << 24294;
    for (int i = 0; i < 24294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24295(int& val) {
    func_in_24295(val);
    const int x = 24295;
    std::cout << 24295;
    for (int i = 0; i < 24295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24295(int& val) {
    const int x = 24295;
    std::cout << 24295;
    for (int i = 0; i < 24295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24296(int& val) {
    func_in_24296(val);
    const int x = 24296;
    std::cout << 24296;
    for (int i = 0; i < 24296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24296(int& val) {
    const int x = 24296;
    std::cout << 24296;
    for (int i = 0; i < 24296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24297(int& val) {
    func_in_24297(val);
    const int x = 24297;
    std::cout << 24297;
    for (int i = 0; i < 24297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24297(int& val) {
    const int x = 24297;
    std::cout << 24297;
    for (int i = 0; i < 24297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24298(int& val) {
    func_in_24298(val);
    const int x = 24298;
    std::cout << 24298;
    for (int i = 0; i < 24298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24298(int& val) {
    const int x = 24298;
    std::cout << 24298;
    for (int i = 0; i < 24298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24299(int& val) {
    func_in_24299(val);
    const int x = 24299;
    std::cout << 24299;
    for (int i = 0; i < 24299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24299(int& val) {
    const int x = 24299;
    std::cout << 24299;
    for (int i = 0; i < 24299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24300(int& val) {
    func_in_24300(val);
    const int x = 24300;
    std::cout << 24300;
    for (int i = 0; i < 24300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24300(int& val) {
    const int x = 24300;
    std::cout << 24300;
    for (int i = 0; i < 24300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24301(int& val) {
    func_in_24301(val);
    const int x = 24301;
    std::cout << 24301;
    for (int i = 0; i < 24301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24301(int& val) {
    const int x = 24301;
    std::cout << 24301;
    for (int i = 0; i < 24301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24302(int& val) {
    func_in_24302(val);
    const int x = 24302;
    std::cout << 24302;
    for (int i = 0; i < 24302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24302(int& val) {
    const int x = 24302;
    std::cout << 24302;
    for (int i = 0; i < 24302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24303(int& val) {
    func_in_24303(val);
    const int x = 24303;
    std::cout << 24303;
    for (int i = 0; i < 24303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24303(int& val) {
    const int x = 24303;
    std::cout << 24303;
    for (int i = 0; i < 24303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24304(int& val) {
    func_in_24304(val);
    const int x = 24304;
    std::cout << 24304;
    for (int i = 0; i < 24304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24304(int& val) {
    const int x = 24304;
    std::cout << 24304;
    for (int i = 0; i < 24304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24305(int& val) {
    func_in_24305(val);
    const int x = 24305;
    std::cout << 24305;
    for (int i = 0; i < 24305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24305(int& val) {
    const int x = 24305;
    std::cout << 24305;
    for (int i = 0; i < 24305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24306(int& val) {
    func_in_24306(val);
    const int x = 24306;
    std::cout << 24306;
    for (int i = 0; i < 24306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24306(int& val) {
    const int x = 24306;
    std::cout << 24306;
    for (int i = 0; i < 24306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24307(int& val) {
    func_in_24307(val);
    const int x = 24307;
    std::cout << 24307;
    for (int i = 0; i < 24307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24307(int& val) {
    const int x = 24307;
    std::cout << 24307;
    for (int i = 0; i < 24307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24308(int& val) {
    func_in_24308(val);
    const int x = 24308;
    std::cout << 24308;
    for (int i = 0; i < 24308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24308(int& val) {
    const int x = 24308;
    std::cout << 24308;
    for (int i = 0; i < 24308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24309(int& val) {
    func_in_24309(val);
    const int x = 24309;
    std::cout << 24309;
    for (int i = 0; i < 24309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24309(int& val) {
    const int x = 24309;
    std::cout << 24309;
    for (int i = 0; i < 24309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24310(int& val) {
    func_in_24310(val);
    const int x = 24310;
    std::cout << 24310;
    for (int i = 0; i < 24310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24310(int& val) {
    const int x = 24310;
    std::cout << 24310;
    for (int i = 0; i < 24310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24311(int& val) {
    func_in_24311(val);
    const int x = 24311;
    std::cout << 24311;
    for (int i = 0; i < 24311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24311(int& val) {
    const int x = 24311;
    std::cout << 24311;
    for (int i = 0; i < 24311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24312(int& val) {
    func_in_24312(val);
    const int x = 24312;
    std::cout << 24312;
    for (int i = 0; i < 24312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24312(int& val) {
    const int x = 24312;
    std::cout << 24312;
    for (int i = 0; i < 24312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24313(int& val) {
    func_in_24313(val);
    const int x = 24313;
    std::cout << 24313;
    for (int i = 0; i < 24313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24313(int& val) {
    const int x = 24313;
    std::cout << 24313;
    for (int i = 0; i < 24313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24314(int& val) {
    func_in_24314(val);
    const int x = 24314;
    std::cout << 24314;
    for (int i = 0; i < 24314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24314(int& val) {
    const int x = 24314;
    std::cout << 24314;
    for (int i = 0; i < 24314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24315(int& val) {
    func_in_24315(val);
    const int x = 24315;
    std::cout << 24315;
    for (int i = 0; i < 24315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24315(int& val) {
    const int x = 24315;
    std::cout << 24315;
    for (int i = 0; i < 24315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24316(int& val) {
    func_in_24316(val);
    const int x = 24316;
    std::cout << 24316;
    for (int i = 0; i < 24316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24316(int& val) {
    const int x = 24316;
    std::cout << 24316;
    for (int i = 0; i < 24316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24317(int& val) {
    func_in_24317(val);
    const int x = 24317;
    std::cout << 24317;
    for (int i = 0; i < 24317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24317(int& val) {
    const int x = 24317;
    std::cout << 24317;
    for (int i = 0; i < 24317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24318(int& val) {
    func_in_24318(val);
    const int x = 24318;
    std::cout << 24318;
    for (int i = 0; i < 24318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24318(int& val) {
    const int x = 24318;
    std::cout << 24318;
    for (int i = 0; i < 24318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24319(int& val) {
    func_in_24319(val);
    const int x = 24319;
    std::cout << 24319;
    for (int i = 0; i < 24319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24319(int& val) {
    const int x = 24319;
    std::cout << 24319;
    for (int i = 0; i < 24319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24320(int& val) {
    func_in_24320(val);
    const int x = 24320;
    std::cout << 24320;
    for (int i = 0; i < 24320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24320(int& val) {
    const int x = 24320;
    std::cout << 24320;
    for (int i = 0; i < 24320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24321(int& val) {
    func_in_24321(val);
    const int x = 24321;
    std::cout << 24321;
    for (int i = 0; i < 24321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24321(int& val) {
    const int x = 24321;
    std::cout << 24321;
    for (int i = 0; i < 24321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24322(int& val) {
    func_in_24322(val);
    const int x = 24322;
    std::cout << 24322;
    for (int i = 0; i < 24322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24322(int& val) {
    const int x = 24322;
    std::cout << 24322;
    for (int i = 0; i < 24322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24323(int& val) {
    func_in_24323(val);
    const int x = 24323;
    std::cout << 24323;
    for (int i = 0; i < 24323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24323(int& val) {
    const int x = 24323;
    std::cout << 24323;
    for (int i = 0; i < 24323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24324(int& val) {
    func_in_24324(val);
    const int x = 24324;
    std::cout << 24324;
    for (int i = 0; i < 24324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24324(int& val) {
    const int x = 24324;
    std::cout << 24324;
    for (int i = 0; i < 24324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24325(int& val) {
    func_in_24325(val);
    const int x = 24325;
    std::cout << 24325;
    for (int i = 0; i < 24325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24325(int& val) {
    const int x = 24325;
    std::cout << 24325;
    for (int i = 0; i < 24325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24326(int& val) {
    func_in_24326(val);
    const int x = 24326;
    std::cout << 24326;
    for (int i = 0; i < 24326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24326(int& val) {
    const int x = 24326;
    std::cout << 24326;
    for (int i = 0; i < 24326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24327(int& val) {
    func_in_24327(val);
    const int x = 24327;
    std::cout << 24327;
    for (int i = 0; i < 24327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24327(int& val) {
    const int x = 24327;
    std::cout << 24327;
    for (int i = 0; i < 24327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24328(int& val) {
    func_in_24328(val);
    const int x = 24328;
    std::cout << 24328;
    for (int i = 0; i < 24328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24328(int& val) {
    const int x = 24328;
    std::cout << 24328;
    for (int i = 0; i < 24328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24329(int& val) {
    func_in_24329(val);
    const int x = 24329;
    std::cout << 24329;
    for (int i = 0; i < 24329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24329(int& val) {
    const int x = 24329;
    std::cout << 24329;
    for (int i = 0; i < 24329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24330(int& val) {
    func_in_24330(val);
    const int x = 24330;
    std::cout << 24330;
    for (int i = 0; i < 24330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24330(int& val) {
    const int x = 24330;
    std::cout << 24330;
    for (int i = 0; i < 24330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24331(int& val) {
    func_in_24331(val);
    const int x = 24331;
    std::cout << 24331;
    for (int i = 0; i < 24331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24331(int& val) {
    const int x = 24331;
    std::cout << 24331;
    for (int i = 0; i < 24331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24332(int& val) {
    func_in_24332(val);
    const int x = 24332;
    std::cout << 24332;
    for (int i = 0; i < 24332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24332(int& val) {
    const int x = 24332;
    std::cout << 24332;
    for (int i = 0; i < 24332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24333(int& val) {
    func_in_24333(val);
    const int x = 24333;
    std::cout << 24333;
    for (int i = 0; i < 24333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24333(int& val) {
    const int x = 24333;
    std::cout << 24333;
    for (int i = 0; i < 24333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24334(int& val) {
    func_in_24334(val);
    const int x = 24334;
    std::cout << 24334;
    for (int i = 0; i < 24334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24334(int& val) {
    const int x = 24334;
    std::cout << 24334;
    for (int i = 0; i < 24334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24335(int& val) {
    func_in_24335(val);
    const int x = 24335;
    std::cout << 24335;
    for (int i = 0; i < 24335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24335(int& val) {
    const int x = 24335;
    std::cout << 24335;
    for (int i = 0; i < 24335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24336(int& val) {
    func_in_24336(val);
    const int x = 24336;
    std::cout << 24336;
    for (int i = 0; i < 24336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24336(int& val) {
    const int x = 24336;
    std::cout << 24336;
    for (int i = 0; i < 24336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24337(int& val) {
    func_in_24337(val);
    const int x = 24337;
    std::cout << 24337;
    for (int i = 0; i < 24337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24337(int& val) {
    const int x = 24337;
    std::cout << 24337;
    for (int i = 0; i < 24337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24338(int& val) {
    func_in_24338(val);
    const int x = 24338;
    std::cout << 24338;
    for (int i = 0; i < 24338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24338(int& val) {
    const int x = 24338;
    std::cout << 24338;
    for (int i = 0; i < 24338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24339(int& val) {
    func_in_24339(val);
    const int x = 24339;
    std::cout << 24339;
    for (int i = 0; i < 24339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24339(int& val) {
    const int x = 24339;
    std::cout << 24339;
    for (int i = 0; i < 24339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24340(int& val) {
    func_in_24340(val);
    const int x = 24340;
    std::cout << 24340;
    for (int i = 0; i < 24340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24340(int& val) {
    const int x = 24340;
    std::cout << 24340;
    for (int i = 0; i < 24340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24341(int& val) {
    func_in_24341(val);
    const int x = 24341;
    std::cout << 24341;
    for (int i = 0; i < 24341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24341(int& val) {
    const int x = 24341;
    std::cout << 24341;
    for (int i = 0; i < 24341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24342(int& val) {
    func_in_24342(val);
    const int x = 24342;
    std::cout << 24342;
    for (int i = 0; i < 24342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24342(int& val) {
    const int x = 24342;
    std::cout << 24342;
    for (int i = 0; i < 24342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24343(int& val) {
    func_in_24343(val);
    const int x = 24343;
    std::cout << 24343;
    for (int i = 0; i < 24343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24343(int& val) {
    const int x = 24343;
    std::cout << 24343;
    for (int i = 0; i < 24343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24344(int& val) {
    func_in_24344(val);
    const int x = 24344;
    std::cout << 24344;
    for (int i = 0; i < 24344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24344(int& val) {
    const int x = 24344;
    std::cout << 24344;
    for (int i = 0; i < 24344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24345(int& val) {
    func_in_24345(val);
    const int x = 24345;
    std::cout << 24345;
    for (int i = 0; i < 24345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24345(int& val) {
    const int x = 24345;
    std::cout << 24345;
    for (int i = 0; i < 24345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24346(int& val) {
    func_in_24346(val);
    const int x = 24346;
    std::cout << 24346;
    for (int i = 0; i < 24346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24346(int& val) {
    const int x = 24346;
    std::cout << 24346;
    for (int i = 0; i < 24346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24347(int& val) {
    func_in_24347(val);
    const int x = 24347;
    std::cout << 24347;
    for (int i = 0; i < 24347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24347(int& val) {
    const int x = 24347;
    std::cout << 24347;
    for (int i = 0; i < 24347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24348(int& val) {
    func_in_24348(val);
    const int x = 24348;
    std::cout << 24348;
    for (int i = 0; i < 24348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24348(int& val) {
    const int x = 24348;
    std::cout << 24348;
    for (int i = 0; i < 24348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24349(int& val) {
    func_in_24349(val);
    const int x = 24349;
    std::cout << 24349;
    for (int i = 0; i < 24349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24349(int& val) {
    const int x = 24349;
    std::cout << 24349;
    for (int i = 0; i < 24349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24350(int& val) {
    func_in_24350(val);
    const int x = 24350;
    std::cout << 24350;
    for (int i = 0; i < 24350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24350(int& val) {
    const int x = 24350;
    std::cout << 24350;
    for (int i = 0; i < 24350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24351(int& val) {
    func_in_24351(val);
    const int x = 24351;
    std::cout << 24351;
    for (int i = 0; i < 24351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24351(int& val) {
    const int x = 24351;
    std::cout << 24351;
    for (int i = 0; i < 24351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24352(int& val) {
    func_in_24352(val);
    const int x = 24352;
    std::cout << 24352;
    for (int i = 0; i < 24352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24352(int& val) {
    const int x = 24352;
    std::cout << 24352;
    for (int i = 0; i < 24352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24353(int& val) {
    func_in_24353(val);
    const int x = 24353;
    std::cout << 24353;
    for (int i = 0; i < 24353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24353(int& val) {
    const int x = 24353;
    std::cout << 24353;
    for (int i = 0; i < 24353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24354(int& val) {
    func_in_24354(val);
    const int x = 24354;
    std::cout << 24354;
    for (int i = 0; i < 24354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24354(int& val) {
    const int x = 24354;
    std::cout << 24354;
    for (int i = 0; i < 24354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24355(int& val) {
    func_in_24355(val);
    const int x = 24355;
    std::cout << 24355;
    for (int i = 0; i < 24355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24355(int& val) {
    const int x = 24355;
    std::cout << 24355;
    for (int i = 0; i < 24355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24356(int& val) {
    func_in_24356(val);
    const int x = 24356;
    std::cout << 24356;
    for (int i = 0; i < 24356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24356(int& val) {
    const int x = 24356;
    std::cout << 24356;
    for (int i = 0; i < 24356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24357(int& val) {
    func_in_24357(val);
    const int x = 24357;
    std::cout << 24357;
    for (int i = 0; i < 24357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24357(int& val) {
    const int x = 24357;
    std::cout << 24357;
    for (int i = 0; i < 24357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24358(int& val) {
    func_in_24358(val);
    const int x = 24358;
    std::cout << 24358;
    for (int i = 0; i < 24358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24358(int& val) {
    const int x = 24358;
    std::cout << 24358;
    for (int i = 0; i < 24358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24359(int& val) {
    func_in_24359(val);
    const int x = 24359;
    std::cout << 24359;
    for (int i = 0; i < 24359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24359(int& val) {
    const int x = 24359;
    std::cout << 24359;
    for (int i = 0; i < 24359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24360(int& val) {
    func_in_24360(val);
    const int x = 24360;
    std::cout << 24360;
    for (int i = 0; i < 24360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24360(int& val) {
    const int x = 24360;
    std::cout << 24360;
    for (int i = 0; i < 24360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24361(int& val) {
    func_in_24361(val);
    const int x = 24361;
    std::cout << 24361;
    for (int i = 0; i < 24361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24361(int& val) {
    const int x = 24361;
    std::cout << 24361;
    for (int i = 0; i < 24361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24362(int& val) {
    func_in_24362(val);
    const int x = 24362;
    std::cout << 24362;
    for (int i = 0; i < 24362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24362(int& val) {
    const int x = 24362;
    std::cout << 24362;
    for (int i = 0; i < 24362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24363(int& val) {
    func_in_24363(val);
    const int x = 24363;
    std::cout << 24363;
    for (int i = 0; i < 24363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24363(int& val) {
    const int x = 24363;
    std::cout << 24363;
    for (int i = 0; i < 24363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24364(int& val) {
    func_in_24364(val);
    const int x = 24364;
    std::cout << 24364;
    for (int i = 0; i < 24364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24364(int& val) {
    const int x = 24364;
    std::cout << 24364;
    for (int i = 0; i < 24364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24365(int& val) {
    func_in_24365(val);
    const int x = 24365;
    std::cout << 24365;
    for (int i = 0; i < 24365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24365(int& val) {
    const int x = 24365;
    std::cout << 24365;
    for (int i = 0; i < 24365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24366(int& val) {
    func_in_24366(val);
    const int x = 24366;
    std::cout << 24366;
    for (int i = 0; i < 24366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24366(int& val) {
    const int x = 24366;
    std::cout << 24366;
    for (int i = 0; i < 24366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24367(int& val) {
    func_in_24367(val);
    const int x = 24367;
    std::cout << 24367;
    for (int i = 0; i < 24367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24367(int& val) {
    const int x = 24367;
    std::cout << 24367;
    for (int i = 0; i < 24367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24368(int& val) {
    func_in_24368(val);
    const int x = 24368;
    std::cout << 24368;
    for (int i = 0; i < 24368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24368(int& val) {
    const int x = 24368;
    std::cout << 24368;
    for (int i = 0; i < 24368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24369(int& val) {
    func_in_24369(val);
    const int x = 24369;
    std::cout << 24369;
    for (int i = 0; i < 24369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24369(int& val) {
    const int x = 24369;
    std::cout << 24369;
    for (int i = 0; i < 24369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24370(int& val) {
    func_in_24370(val);
    const int x = 24370;
    std::cout << 24370;
    for (int i = 0; i < 24370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24370(int& val) {
    const int x = 24370;
    std::cout << 24370;
    for (int i = 0; i < 24370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24371(int& val) {
    func_in_24371(val);
    const int x = 24371;
    std::cout << 24371;
    for (int i = 0; i < 24371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24371(int& val) {
    const int x = 24371;
    std::cout << 24371;
    for (int i = 0; i < 24371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24372(int& val) {
    func_in_24372(val);
    const int x = 24372;
    std::cout << 24372;
    for (int i = 0; i < 24372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24372(int& val) {
    const int x = 24372;
    std::cout << 24372;
    for (int i = 0; i < 24372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24373(int& val) {
    func_in_24373(val);
    const int x = 24373;
    std::cout << 24373;
    for (int i = 0; i < 24373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24373(int& val) {
    const int x = 24373;
    std::cout << 24373;
    for (int i = 0; i < 24373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24374(int& val) {
    func_in_24374(val);
    const int x = 24374;
    std::cout << 24374;
    for (int i = 0; i < 24374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24374(int& val) {
    const int x = 24374;
    std::cout << 24374;
    for (int i = 0; i < 24374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24375(int& val) {
    func_in_24375(val);
    const int x = 24375;
    std::cout << 24375;
    for (int i = 0; i < 24375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24375(int& val) {
    const int x = 24375;
    std::cout << 24375;
    for (int i = 0; i < 24375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24376(int& val) {
    func_in_24376(val);
    const int x = 24376;
    std::cout << 24376;
    for (int i = 0; i < 24376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24376(int& val) {
    const int x = 24376;
    std::cout << 24376;
    for (int i = 0; i < 24376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24377(int& val) {
    func_in_24377(val);
    const int x = 24377;
    std::cout << 24377;
    for (int i = 0; i < 24377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24377(int& val) {
    const int x = 24377;
    std::cout << 24377;
    for (int i = 0; i < 24377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24378(int& val) {
    func_in_24378(val);
    const int x = 24378;
    std::cout << 24378;
    for (int i = 0; i < 24378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24378(int& val) {
    const int x = 24378;
    std::cout << 24378;
    for (int i = 0; i < 24378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24379(int& val) {
    func_in_24379(val);
    const int x = 24379;
    std::cout << 24379;
    for (int i = 0; i < 24379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24379(int& val) {
    const int x = 24379;
    std::cout << 24379;
    for (int i = 0; i < 24379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24380(int& val) {
    func_in_24380(val);
    const int x = 24380;
    std::cout << 24380;
    for (int i = 0; i < 24380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24380(int& val) {
    const int x = 24380;
    std::cout << 24380;
    for (int i = 0; i < 24380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24381(int& val) {
    func_in_24381(val);
    const int x = 24381;
    std::cout << 24381;
    for (int i = 0; i < 24381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24381(int& val) {
    const int x = 24381;
    std::cout << 24381;
    for (int i = 0; i < 24381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24382(int& val) {
    func_in_24382(val);
    const int x = 24382;
    std::cout << 24382;
    for (int i = 0; i < 24382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24382(int& val) {
    const int x = 24382;
    std::cout << 24382;
    for (int i = 0; i < 24382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24383(int& val) {
    func_in_24383(val);
    const int x = 24383;
    std::cout << 24383;
    for (int i = 0; i < 24383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24383(int& val) {
    const int x = 24383;
    std::cout << 24383;
    for (int i = 0; i < 24383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24384(int& val) {
    func_in_24384(val);
    const int x = 24384;
    std::cout << 24384;
    for (int i = 0; i < 24384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24384(int& val) {
    const int x = 24384;
    std::cout << 24384;
    for (int i = 0; i < 24384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24385(int& val) {
    func_in_24385(val);
    const int x = 24385;
    std::cout << 24385;
    for (int i = 0; i < 24385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24385(int& val) {
    const int x = 24385;
    std::cout << 24385;
    for (int i = 0; i < 24385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24386(int& val) {
    func_in_24386(val);
    const int x = 24386;
    std::cout << 24386;
    for (int i = 0; i < 24386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24386(int& val) {
    const int x = 24386;
    std::cout << 24386;
    for (int i = 0; i < 24386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24387(int& val) {
    func_in_24387(val);
    const int x = 24387;
    std::cout << 24387;
    for (int i = 0; i < 24387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24387(int& val) {
    const int x = 24387;
    std::cout << 24387;
    for (int i = 0; i < 24387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24388(int& val) {
    func_in_24388(val);
    const int x = 24388;
    std::cout << 24388;
    for (int i = 0; i < 24388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24388(int& val) {
    const int x = 24388;
    std::cout << 24388;
    for (int i = 0; i < 24388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24389(int& val) {
    func_in_24389(val);
    const int x = 24389;
    std::cout << 24389;
    for (int i = 0; i < 24389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24389(int& val) {
    const int x = 24389;
    std::cout << 24389;
    for (int i = 0; i < 24389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24390(int& val) {
    func_in_24390(val);
    const int x = 24390;
    std::cout << 24390;
    for (int i = 0; i < 24390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24390(int& val) {
    const int x = 24390;
    std::cout << 24390;
    for (int i = 0; i < 24390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24391(int& val) {
    func_in_24391(val);
    const int x = 24391;
    std::cout << 24391;
    for (int i = 0; i < 24391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24391(int& val) {
    const int x = 24391;
    std::cout << 24391;
    for (int i = 0; i < 24391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24392(int& val) {
    func_in_24392(val);
    const int x = 24392;
    std::cout << 24392;
    for (int i = 0; i < 24392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24392(int& val) {
    const int x = 24392;
    std::cout << 24392;
    for (int i = 0; i < 24392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24393(int& val) {
    func_in_24393(val);
    const int x = 24393;
    std::cout << 24393;
    for (int i = 0; i < 24393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24393(int& val) {
    const int x = 24393;
    std::cout << 24393;
    for (int i = 0; i < 24393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24394(int& val) {
    func_in_24394(val);
    const int x = 24394;
    std::cout << 24394;
    for (int i = 0; i < 24394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24394(int& val) {
    const int x = 24394;
    std::cout << 24394;
    for (int i = 0; i < 24394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24395(int& val) {
    func_in_24395(val);
    const int x = 24395;
    std::cout << 24395;
    for (int i = 0; i < 24395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24395(int& val) {
    const int x = 24395;
    std::cout << 24395;
    for (int i = 0; i < 24395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24396(int& val) {
    func_in_24396(val);
    const int x = 24396;
    std::cout << 24396;
    for (int i = 0; i < 24396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24396(int& val) {
    const int x = 24396;
    std::cout << 24396;
    for (int i = 0; i < 24396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24397(int& val) {
    func_in_24397(val);
    const int x = 24397;
    std::cout << 24397;
    for (int i = 0; i < 24397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24397(int& val) {
    const int x = 24397;
    std::cout << 24397;
    for (int i = 0; i < 24397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24398(int& val) {
    func_in_24398(val);
    const int x = 24398;
    std::cout << 24398;
    for (int i = 0; i < 24398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24398(int& val) {
    const int x = 24398;
    std::cout << 24398;
    for (int i = 0; i < 24398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24399(int& val) {
    func_in_24399(val);
    const int x = 24399;
    std::cout << 24399;
    for (int i = 0; i < 24399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24399(int& val) {
    const int x = 24399;
    std::cout << 24399;
    for (int i = 0; i < 24399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24400(int& val) {
    func_in_24400(val);
    const int x = 24400;
    std::cout << 24400;
    for (int i = 0; i < 24400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24400(int& val) {
    const int x = 24400;
    std::cout << 24400;
    for (int i = 0; i < 24400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24401(int& val) {
    func_in_24401(val);
    const int x = 24401;
    std::cout << 24401;
    for (int i = 0; i < 24401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24401(int& val) {
    const int x = 24401;
    std::cout << 24401;
    for (int i = 0; i < 24401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24402(int& val) {
    func_in_24402(val);
    const int x = 24402;
    std::cout << 24402;
    for (int i = 0; i < 24402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24402(int& val) {
    const int x = 24402;
    std::cout << 24402;
    for (int i = 0; i < 24402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24403(int& val) {
    func_in_24403(val);
    const int x = 24403;
    std::cout << 24403;
    for (int i = 0; i < 24403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24403(int& val) {
    const int x = 24403;
    std::cout << 24403;
    for (int i = 0; i < 24403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24404(int& val) {
    func_in_24404(val);
    const int x = 24404;
    std::cout << 24404;
    for (int i = 0; i < 24404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24404(int& val) {
    const int x = 24404;
    std::cout << 24404;
    for (int i = 0; i < 24404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24405(int& val) {
    func_in_24405(val);
    const int x = 24405;
    std::cout << 24405;
    for (int i = 0; i < 24405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24405(int& val) {
    const int x = 24405;
    std::cout << 24405;
    for (int i = 0; i < 24405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24406(int& val) {
    func_in_24406(val);
    const int x = 24406;
    std::cout << 24406;
    for (int i = 0; i < 24406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24406(int& val) {
    const int x = 24406;
    std::cout << 24406;
    for (int i = 0; i < 24406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24407(int& val) {
    func_in_24407(val);
    const int x = 24407;
    std::cout << 24407;
    for (int i = 0; i < 24407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24407(int& val) {
    const int x = 24407;
    std::cout << 24407;
    for (int i = 0; i < 24407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24408(int& val) {
    func_in_24408(val);
    const int x = 24408;
    std::cout << 24408;
    for (int i = 0; i < 24408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24408(int& val) {
    const int x = 24408;
    std::cout << 24408;
    for (int i = 0; i < 24408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24409(int& val) {
    func_in_24409(val);
    const int x = 24409;
    std::cout << 24409;
    for (int i = 0; i < 24409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24409(int& val) {
    const int x = 24409;
    std::cout << 24409;
    for (int i = 0; i < 24409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24410(int& val) {
    func_in_24410(val);
    const int x = 24410;
    std::cout << 24410;
    for (int i = 0; i < 24410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24410(int& val) {
    const int x = 24410;
    std::cout << 24410;
    for (int i = 0; i < 24410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24411(int& val) {
    func_in_24411(val);
    const int x = 24411;
    std::cout << 24411;
    for (int i = 0; i < 24411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24411(int& val) {
    const int x = 24411;
    std::cout << 24411;
    for (int i = 0; i < 24411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24412(int& val) {
    func_in_24412(val);
    const int x = 24412;
    std::cout << 24412;
    for (int i = 0; i < 24412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24412(int& val) {
    const int x = 24412;
    std::cout << 24412;
    for (int i = 0; i < 24412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24413(int& val) {
    func_in_24413(val);
    const int x = 24413;
    std::cout << 24413;
    for (int i = 0; i < 24413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24413(int& val) {
    const int x = 24413;
    std::cout << 24413;
    for (int i = 0; i < 24413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24414(int& val) {
    func_in_24414(val);
    const int x = 24414;
    std::cout << 24414;
    for (int i = 0; i < 24414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24414(int& val) {
    const int x = 24414;
    std::cout << 24414;
    for (int i = 0; i < 24414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24415(int& val) {
    func_in_24415(val);
    const int x = 24415;
    std::cout << 24415;
    for (int i = 0; i < 24415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24415(int& val) {
    const int x = 24415;
    std::cout << 24415;
    for (int i = 0; i < 24415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24416(int& val) {
    func_in_24416(val);
    const int x = 24416;
    std::cout << 24416;
    for (int i = 0; i < 24416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24416(int& val) {
    const int x = 24416;
    std::cout << 24416;
    for (int i = 0; i < 24416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24417(int& val) {
    func_in_24417(val);
    const int x = 24417;
    std::cout << 24417;
    for (int i = 0; i < 24417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24417(int& val) {
    const int x = 24417;
    std::cout << 24417;
    for (int i = 0; i < 24417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24418(int& val) {
    func_in_24418(val);
    const int x = 24418;
    std::cout << 24418;
    for (int i = 0; i < 24418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24418(int& val) {
    const int x = 24418;
    std::cout << 24418;
    for (int i = 0; i < 24418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24419(int& val) {
    func_in_24419(val);
    const int x = 24419;
    std::cout << 24419;
    for (int i = 0; i < 24419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24419(int& val) {
    const int x = 24419;
    std::cout << 24419;
    for (int i = 0; i < 24419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24420(int& val) {
    func_in_24420(val);
    const int x = 24420;
    std::cout << 24420;
    for (int i = 0; i < 24420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24420(int& val) {
    const int x = 24420;
    std::cout << 24420;
    for (int i = 0; i < 24420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24421(int& val) {
    func_in_24421(val);
    const int x = 24421;
    std::cout << 24421;
    for (int i = 0; i < 24421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24421(int& val) {
    const int x = 24421;
    std::cout << 24421;
    for (int i = 0; i < 24421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24422(int& val) {
    func_in_24422(val);
    const int x = 24422;
    std::cout << 24422;
    for (int i = 0; i < 24422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24422(int& val) {
    const int x = 24422;
    std::cout << 24422;
    for (int i = 0; i < 24422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24423(int& val) {
    func_in_24423(val);
    const int x = 24423;
    std::cout << 24423;
    for (int i = 0; i < 24423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24423(int& val) {
    const int x = 24423;
    std::cout << 24423;
    for (int i = 0; i < 24423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24424(int& val) {
    func_in_24424(val);
    const int x = 24424;
    std::cout << 24424;
    for (int i = 0; i < 24424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24424(int& val) {
    const int x = 24424;
    std::cout << 24424;
    for (int i = 0; i < 24424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24425(int& val) {
    func_in_24425(val);
    const int x = 24425;
    std::cout << 24425;
    for (int i = 0; i < 24425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24425(int& val) {
    const int x = 24425;
    std::cout << 24425;
    for (int i = 0; i < 24425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24426(int& val) {
    func_in_24426(val);
    const int x = 24426;
    std::cout << 24426;
    for (int i = 0; i < 24426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24426(int& val) {
    const int x = 24426;
    std::cout << 24426;
    for (int i = 0; i < 24426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24427(int& val) {
    func_in_24427(val);
    const int x = 24427;
    std::cout << 24427;
    for (int i = 0; i < 24427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24427(int& val) {
    const int x = 24427;
    std::cout << 24427;
    for (int i = 0; i < 24427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24428(int& val) {
    func_in_24428(val);
    const int x = 24428;
    std::cout << 24428;
    for (int i = 0; i < 24428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24428(int& val) {
    const int x = 24428;
    std::cout << 24428;
    for (int i = 0; i < 24428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24429(int& val) {
    func_in_24429(val);
    const int x = 24429;
    std::cout << 24429;
    for (int i = 0; i < 24429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24429(int& val) {
    const int x = 24429;
    std::cout << 24429;
    for (int i = 0; i < 24429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24430(int& val) {
    func_in_24430(val);
    const int x = 24430;
    std::cout << 24430;
    for (int i = 0; i < 24430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24430(int& val) {
    const int x = 24430;
    std::cout << 24430;
    for (int i = 0; i < 24430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24431(int& val) {
    func_in_24431(val);
    const int x = 24431;
    std::cout << 24431;
    for (int i = 0; i < 24431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24431(int& val) {
    const int x = 24431;
    std::cout << 24431;
    for (int i = 0; i < 24431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24432(int& val) {
    func_in_24432(val);
    const int x = 24432;
    std::cout << 24432;
    for (int i = 0; i < 24432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24432(int& val) {
    const int x = 24432;
    std::cout << 24432;
    for (int i = 0; i < 24432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24433(int& val) {
    func_in_24433(val);
    const int x = 24433;
    std::cout << 24433;
    for (int i = 0; i < 24433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24433(int& val) {
    const int x = 24433;
    std::cout << 24433;
    for (int i = 0; i < 24433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24434(int& val) {
    func_in_24434(val);
    const int x = 24434;
    std::cout << 24434;
    for (int i = 0; i < 24434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24434(int& val) {
    const int x = 24434;
    std::cout << 24434;
    for (int i = 0; i < 24434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24435(int& val) {
    func_in_24435(val);
    const int x = 24435;
    std::cout << 24435;
    for (int i = 0; i < 24435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24435(int& val) {
    const int x = 24435;
    std::cout << 24435;
    for (int i = 0; i < 24435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24436(int& val) {
    func_in_24436(val);
    const int x = 24436;
    std::cout << 24436;
    for (int i = 0; i < 24436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24436(int& val) {
    const int x = 24436;
    std::cout << 24436;
    for (int i = 0; i < 24436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24437(int& val) {
    func_in_24437(val);
    const int x = 24437;
    std::cout << 24437;
    for (int i = 0; i < 24437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24437(int& val) {
    const int x = 24437;
    std::cout << 24437;
    for (int i = 0; i < 24437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24438(int& val) {
    func_in_24438(val);
    const int x = 24438;
    std::cout << 24438;
    for (int i = 0; i < 24438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24438(int& val) {
    const int x = 24438;
    std::cout << 24438;
    for (int i = 0; i < 24438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24439(int& val) {
    func_in_24439(val);
    const int x = 24439;
    std::cout << 24439;
    for (int i = 0; i < 24439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24439(int& val) {
    const int x = 24439;
    std::cout << 24439;
    for (int i = 0; i < 24439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24440(int& val) {
    func_in_24440(val);
    const int x = 24440;
    std::cout << 24440;
    for (int i = 0; i < 24440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24440(int& val) {
    const int x = 24440;
    std::cout << 24440;
    for (int i = 0; i < 24440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24441(int& val) {
    func_in_24441(val);
    const int x = 24441;
    std::cout << 24441;
    for (int i = 0; i < 24441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24441(int& val) {
    const int x = 24441;
    std::cout << 24441;
    for (int i = 0; i < 24441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24442(int& val) {
    func_in_24442(val);
    const int x = 24442;
    std::cout << 24442;
    for (int i = 0; i < 24442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24442(int& val) {
    const int x = 24442;
    std::cout << 24442;
    for (int i = 0; i < 24442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24443(int& val) {
    func_in_24443(val);
    const int x = 24443;
    std::cout << 24443;
    for (int i = 0; i < 24443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24443(int& val) {
    const int x = 24443;
    std::cout << 24443;
    for (int i = 0; i < 24443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24444(int& val) {
    func_in_24444(val);
    const int x = 24444;
    std::cout << 24444;
    for (int i = 0; i < 24444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24444(int& val) {
    const int x = 24444;
    std::cout << 24444;
    for (int i = 0; i < 24444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24445(int& val) {
    func_in_24445(val);
    const int x = 24445;
    std::cout << 24445;
    for (int i = 0; i < 24445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24445(int& val) {
    const int x = 24445;
    std::cout << 24445;
    for (int i = 0; i < 24445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24446(int& val) {
    func_in_24446(val);
    const int x = 24446;
    std::cout << 24446;
    for (int i = 0; i < 24446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24446(int& val) {
    const int x = 24446;
    std::cout << 24446;
    for (int i = 0; i < 24446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24447(int& val) {
    func_in_24447(val);
    const int x = 24447;
    std::cout << 24447;
    for (int i = 0; i < 24447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24447(int& val) {
    const int x = 24447;
    std::cout << 24447;
    for (int i = 0; i < 24447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24448(int& val) {
    func_in_24448(val);
    const int x = 24448;
    std::cout << 24448;
    for (int i = 0; i < 24448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24448(int& val) {
    const int x = 24448;
    std::cout << 24448;
    for (int i = 0; i < 24448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24449(int& val) {
    func_in_24449(val);
    const int x = 24449;
    std::cout << 24449;
    for (int i = 0; i < 24449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24449(int& val) {
    const int x = 24449;
    std::cout << 24449;
    for (int i = 0; i < 24449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24450(int& val) {
    func_in_24450(val);
    const int x = 24450;
    std::cout << 24450;
    for (int i = 0; i < 24450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24450(int& val) {
    const int x = 24450;
    std::cout << 24450;
    for (int i = 0; i < 24450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24451(int& val) {
    func_in_24451(val);
    const int x = 24451;
    std::cout << 24451;
    for (int i = 0; i < 24451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24451(int& val) {
    const int x = 24451;
    std::cout << 24451;
    for (int i = 0; i < 24451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24452(int& val) {
    func_in_24452(val);
    const int x = 24452;
    std::cout << 24452;
    for (int i = 0; i < 24452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24452(int& val) {
    const int x = 24452;
    std::cout << 24452;
    for (int i = 0; i < 24452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24453(int& val) {
    func_in_24453(val);
    const int x = 24453;
    std::cout << 24453;
    for (int i = 0; i < 24453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24453(int& val) {
    const int x = 24453;
    std::cout << 24453;
    for (int i = 0; i < 24453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24454(int& val) {
    func_in_24454(val);
    const int x = 24454;
    std::cout << 24454;
    for (int i = 0; i < 24454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24454(int& val) {
    const int x = 24454;
    std::cout << 24454;
    for (int i = 0; i < 24454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24455(int& val) {
    func_in_24455(val);
    const int x = 24455;
    std::cout << 24455;
    for (int i = 0; i < 24455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24455(int& val) {
    const int x = 24455;
    std::cout << 24455;
    for (int i = 0; i < 24455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24456(int& val) {
    func_in_24456(val);
    const int x = 24456;
    std::cout << 24456;
    for (int i = 0; i < 24456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24456(int& val) {
    const int x = 24456;
    std::cout << 24456;
    for (int i = 0; i < 24456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24457(int& val) {
    func_in_24457(val);
    const int x = 24457;
    std::cout << 24457;
    for (int i = 0; i < 24457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24457(int& val) {
    const int x = 24457;
    std::cout << 24457;
    for (int i = 0; i < 24457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24458(int& val) {
    func_in_24458(val);
    const int x = 24458;
    std::cout << 24458;
    for (int i = 0; i < 24458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24458(int& val) {
    const int x = 24458;
    std::cout << 24458;
    for (int i = 0; i < 24458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24459(int& val) {
    func_in_24459(val);
    const int x = 24459;
    std::cout << 24459;
    for (int i = 0; i < 24459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24459(int& val) {
    const int x = 24459;
    std::cout << 24459;
    for (int i = 0; i < 24459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24460(int& val) {
    func_in_24460(val);
    const int x = 24460;
    std::cout << 24460;
    for (int i = 0; i < 24460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24460(int& val) {
    const int x = 24460;
    std::cout << 24460;
    for (int i = 0; i < 24460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24461(int& val) {
    func_in_24461(val);
    const int x = 24461;
    std::cout << 24461;
    for (int i = 0; i < 24461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24461(int& val) {
    const int x = 24461;
    std::cout << 24461;
    for (int i = 0; i < 24461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24462(int& val) {
    func_in_24462(val);
    const int x = 24462;
    std::cout << 24462;
    for (int i = 0; i < 24462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24462(int& val) {
    const int x = 24462;
    std::cout << 24462;
    for (int i = 0; i < 24462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24463(int& val) {
    func_in_24463(val);
    const int x = 24463;
    std::cout << 24463;
    for (int i = 0; i < 24463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24463(int& val) {
    const int x = 24463;
    std::cout << 24463;
    for (int i = 0; i < 24463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24464(int& val) {
    func_in_24464(val);
    const int x = 24464;
    std::cout << 24464;
    for (int i = 0; i < 24464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24464(int& val) {
    const int x = 24464;
    std::cout << 24464;
    for (int i = 0; i < 24464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24465(int& val) {
    func_in_24465(val);
    const int x = 24465;
    std::cout << 24465;
    for (int i = 0; i < 24465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24465(int& val) {
    const int x = 24465;
    std::cout << 24465;
    for (int i = 0; i < 24465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24466(int& val) {
    func_in_24466(val);
    const int x = 24466;
    std::cout << 24466;
    for (int i = 0; i < 24466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24466(int& val) {
    const int x = 24466;
    std::cout << 24466;
    for (int i = 0; i < 24466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24467(int& val) {
    func_in_24467(val);
    const int x = 24467;
    std::cout << 24467;
    for (int i = 0; i < 24467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24467(int& val) {
    const int x = 24467;
    std::cout << 24467;
    for (int i = 0; i < 24467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24468(int& val) {
    func_in_24468(val);
    const int x = 24468;
    std::cout << 24468;
    for (int i = 0; i < 24468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24468(int& val) {
    const int x = 24468;
    std::cout << 24468;
    for (int i = 0; i < 24468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24469(int& val) {
    func_in_24469(val);
    const int x = 24469;
    std::cout << 24469;
    for (int i = 0; i < 24469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24469(int& val) {
    const int x = 24469;
    std::cout << 24469;
    for (int i = 0; i < 24469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24470(int& val) {
    func_in_24470(val);
    const int x = 24470;
    std::cout << 24470;
    for (int i = 0; i < 24470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24470(int& val) {
    const int x = 24470;
    std::cout << 24470;
    for (int i = 0; i < 24470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24471(int& val) {
    func_in_24471(val);
    const int x = 24471;
    std::cout << 24471;
    for (int i = 0; i < 24471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24471(int& val) {
    const int x = 24471;
    std::cout << 24471;
    for (int i = 0; i < 24471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24472(int& val) {
    func_in_24472(val);
    const int x = 24472;
    std::cout << 24472;
    for (int i = 0; i < 24472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24472(int& val) {
    const int x = 24472;
    std::cout << 24472;
    for (int i = 0; i < 24472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24473(int& val) {
    func_in_24473(val);
    const int x = 24473;
    std::cout << 24473;
    for (int i = 0; i < 24473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24473(int& val) {
    const int x = 24473;
    std::cout << 24473;
    for (int i = 0; i < 24473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24474(int& val) {
    func_in_24474(val);
    const int x = 24474;
    std::cout << 24474;
    for (int i = 0; i < 24474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24474(int& val) {
    const int x = 24474;
    std::cout << 24474;
    for (int i = 0; i < 24474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24475(int& val) {
    func_in_24475(val);
    const int x = 24475;
    std::cout << 24475;
    for (int i = 0; i < 24475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24475(int& val) {
    const int x = 24475;
    std::cout << 24475;
    for (int i = 0; i < 24475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24476(int& val) {
    func_in_24476(val);
    const int x = 24476;
    std::cout << 24476;
    for (int i = 0; i < 24476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24476(int& val) {
    const int x = 24476;
    std::cout << 24476;
    for (int i = 0; i < 24476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24477(int& val) {
    func_in_24477(val);
    const int x = 24477;
    std::cout << 24477;
    for (int i = 0; i < 24477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24477(int& val) {
    const int x = 24477;
    std::cout << 24477;
    for (int i = 0; i < 24477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24478(int& val) {
    func_in_24478(val);
    const int x = 24478;
    std::cout << 24478;
    for (int i = 0; i < 24478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24478(int& val) {
    const int x = 24478;
    std::cout << 24478;
    for (int i = 0; i < 24478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24479(int& val) {
    func_in_24479(val);
    const int x = 24479;
    std::cout << 24479;
    for (int i = 0; i < 24479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24479(int& val) {
    const int x = 24479;
    std::cout << 24479;
    for (int i = 0; i < 24479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24480(int& val) {
    func_in_24480(val);
    const int x = 24480;
    std::cout << 24480;
    for (int i = 0; i < 24480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24480(int& val) {
    const int x = 24480;
    std::cout << 24480;
    for (int i = 0; i < 24480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24481(int& val) {
    func_in_24481(val);
    const int x = 24481;
    std::cout << 24481;
    for (int i = 0; i < 24481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24481(int& val) {
    const int x = 24481;
    std::cout << 24481;
    for (int i = 0; i < 24481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24482(int& val) {
    func_in_24482(val);
    const int x = 24482;
    std::cout << 24482;
    for (int i = 0; i < 24482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24482(int& val) {
    const int x = 24482;
    std::cout << 24482;
    for (int i = 0; i < 24482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24483(int& val) {
    func_in_24483(val);
    const int x = 24483;
    std::cout << 24483;
    for (int i = 0; i < 24483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24483(int& val) {
    const int x = 24483;
    std::cout << 24483;
    for (int i = 0; i < 24483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24484(int& val) {
    func_in_24484(val);
    const int x = 24484;
    std::cout << 24484;
    for (int i = 0; i < 24484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24484(int& val) {
    const int x = 24484;
    std::cout << 24484;
    for (int i = 0; i < 24484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24485(int& val) {
    func_in_24485(val);
    const int x = 24485;
    std::cout << 24485;
    for (int i = 0; i < 24485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24485(int& val) {
    const int x = 24485;
    std::cout << 24485;
    for (int i = 0; i < 24485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24486(int& val) {
    func_in_24486(val);
    const int x = 24486;
    std::cout << 24486;
    for (int i = 0; i < 24486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24486(int& val) {
    const int x = 24486;
    std::cout << 24486;
    for (int i = 0; i < 24486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24487(int& val) {
    func_in_24487(val);
    const int x = 24487;
    std::cout << 24487;
    for (int i = 0; i < 24487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24487(int& val) {
    const int x = 24487;
    std::cout << 24487;
    for (int i = 0; i < 24487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24488(int& val) {
    func_in_24488(val);
    const int x = 24488;
    std::cout << 24488;
    for (int i = 0; i < 24488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24488(int& val) {
    const int x = 24488;
    std::cout << 24488;
    for (int i = 0; i < 24488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24489(int& val) {
    func_in_24489(val);
    const int x = 24489;
    std::cout << 24489;
    for (int i = 0; i < 24489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24489(int& val) {
    const int x = 24489;
    std::cout << 24489;
    for (int i = 0; i < 24489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24490(int& val) {
    func_in_24490(val);
    const int x = 24490;
    std::cout << 24490;
    for (int i = 0; i < 24490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24490(int& val) {
    const int x = 24490;
    std::cout << 24490;
    for (int i = 0; i < 24490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24491(int& val) {
    func_in_24491(val);
    const int x = 24491;
    std::cout << 24491;
    for (int i = 0; i < 24491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24491(int& val) {
    const int x = 24491;
    std::cout << 24491;
    for (int i = 0; i < 24491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24492(int& val) {
    func_in_24492(val);
    const int x = 24492;
    std::cout << 24492;
    for (int i = 0; i < 24492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24492(int& val) {
    const int x = 24492;
    std::cout << 24492;
    for (int i = 0; i < 24492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24493(int& val) {
    func_in_24493(val);
    const int x = 24493;
    std::cout << 24493;
    for (int i = 0; i < 24493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24493(int& val) {
    const int x = 24493;
    std::cout << 24493;
    for (int i = 0; i < 24493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24494(int& val) {
    func_in_24494(val);
    const int x = 24494;
    std::cout << 24494;
    for (int i = 0; i < 24494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24494(int& val) {
    const int x = 24494;
    std::cout << 24494;
    for (int i = 0; i < 24494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24495(int& val) {
    func_in_24495(val);
    const int x = 24495;
    std::cout << 24495;
    for (int i = 0; i < 24495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24495(int& val) {
    const int x = 24495;
    std::cout << 24495;
    for (int i = 0; i < 24495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24496(int& val) {
    func_in_24496(val);
    const int x = 24496;
    std::cout << 24496;
    for (int i = 0; i < 24496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24496(int& val) {
    const int x = 24496;
    std::cout << 24496;
    for (int i = 0; i < 24496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24497(int& val) {
    func_in_24497(val);
    const int x = 24497;
    std::cout << 24497;
    for (int i = 0; i < 24497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24497(int& val) {
    const int x = 24497;
    std::cout << 24497;
    for (int i = 0; i < 24497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24498(int& val) {
    func_in_24498(val);
    const int x = 24498;
    std::cout << 24498;
    for (int i = 0; i < 24498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24498(int& val) {
    const int x = 24498;
    std::cout << 24498;
    for (int i = 0; i < 24498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24499(int& val) {
    func_in_24499(val);
    const int x = 24499;
    std::cout << 24499;
    for (int i = 0; i < 24499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24499(int& val) {
    const int x = 24499;
    std::cout << 24499;
    for (int i = 0; i < 24499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24500(int& val) {
    func_in_24500(val);
    const int x = 24500;
    std::cout << 24500;
    for (int i = 0; i < 24500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24500(int& val) {
    const int x = 24500;
    std::cout << 24500;
    for (int i = 0; i < 24500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24501(int& val) {
    func_in_24501(val);
    const int x = 24501;
    std::cout << 24501;
    for (int i = 0; i < 24501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24501(int& val) {
    const int x = 24501;
    std::cout << 24501;
    for (int i = 0; i < 24501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24502(int& val) {
    func_in_24502(val);
    const int x = 24502;
    std::cout << 24502;
    for (int i = 0; i < 24502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24502(int& val) {
    const int x = 24502;
    std::cout << 24502;
    for (int i = 0; i < 24502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24503(int& val) {
    func_in_24503(val);
    const int x = 24503;
    std::cout << 24503;
    for (int i = 0; i < 24503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24503(int& val) {
    const int x = 24503;
    std::cout << 24503;
    for (int i = 0; i < 24503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24504(int& val) {
    func_in_24504(val);
    const int x = 24504;
    std::cout << 24504;
    for (int i = 0; i < 24504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24504(int& val) {
    const int x = 24504;
    std::cout << 24504;
    for (int i = 0; i < 24504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24505(int& val) {
    func_in_24505(val);
    const int x = 24505;
    std::cout << 24505;
    for (int i = 0; i < 24505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24505(int& val) {
    const int x = 24505;
    std::cout << 24505;
    for (int i = 0; i < 24505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24506(int& val) {
    func_in_24506(val);
    const int x = 24506;
    std::cout << 24506;
    for (int i = 0; i < 24506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24506(int& val) {
    const int x = 24506;
    std::cout << 24506;
    for (int i = 0; i < 24506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24507(int& val) {
    func_in_24507(val);
    const int x = 24507;
    std::cout << 24507;
    for (int i = 0; i < 24507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24507(int& val) {
    const int x = 24507;
    std::cout << 24507;
    for (int i = 0; i < 24507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24508(int& val) {
    func_in_24508(val);
    const int x = 24508;
    std::cout << 24508;
    for (int i = 0; i < 24508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24508(int& val) {
    const int x = 24508;
    std::cout << 24508;
    for (int i = 0; i < 24508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24509(int& val) {
    func_in_24509(val);
    const int x = 24509;
    std::cout << 24509;
    for (int i = 0; i < 24509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24509(int& val) {
    const int x = 24509;
    std::cout << 24509;
    for (int i = 0; i < 24509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24510(int& val) {
    func_in_24510(val);
    const int x = 24510;
    std::cout << 24510;
    for (int i = 0; i < 24510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24510(int& val) {
    const int x = 24510;
    std::cout << 24510;
    for (int i = 0; i < 24510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24511(int& val) {
    func_in_24511(val);
    const int x = 24511;
    std::cout << 24511;
    for (int i = 0; i < 24511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24511(int& val) {
    const int x = 24511;
    std::cout << 24511;
    for (int i = 0; i < 24511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24512(int& val) {
    func_in_24512(val);
    const int x = 24512;
    std::cout << 24512;
    for (int i = 0; i < 24512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24512(int& val) {
    const int x = 24512;
    std::cout << 24512;
    for (int i = 0; i < 24512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24513(int& val) {
    func_in_24513(val);
    const int x = 24513;
    std::cout << 24513;
    for (int i = 0; i < 24513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24513(int& val) {
    const int x = 24513;
    std::cout << 24513;
    for (int i = 0; i < 24513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24514(int& val) {
    func_in_24514(val);
    const int x = 24514;
    std::cout << 24514;
    for (int i = 0; i < 24514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24514(int& val) {
    const int x = 24514;
    std::cout << 24514;
    for (int i = 0; i < 24514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24515(int& val) {
    func_in_24515(val);
    const int x = 24515;
    std::cout << 24515;
    for (int i = 0; i < 24515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24515(int& val) {
    const int x = 24515;
    std::cout << 24515;
    for (int i = 0; i < 24515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24516(int& val) {
    func_in_24516(val);
    const int x = 24516;
    std::cout << 24516;
    for (int i = 0; i < 24516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24516(int& val) {
    const int x = 24516;
    std::cout << 24516;
    for (int i = 0; i < 24516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24517(int& val) {
    func_in_24517(val);
    const int x = 24517;
    std::cout << 24517;
    for (int i = 0; i < 24517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24517(int& val) {
    const int x = 24517;
    std::cout << 24517;
    for (int i = 0; i < 24517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24518(int& val) {
    func_in_24518(val);
    const int x = 24518;
    std::cout << 24518;
    for (int i = 0; i < 24518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24518(int& val) {
    const int x = 24518;
    std::cout << 24518;
    for (int i = 0; i < 24518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24519(int& val) {
    func_in_24519(val);
    const int x = 24519;
    std::cout << 24519;
    for (int i = 0; i < 24519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24519(int& val) {
    const int x = 24519;
    std::cout << 24519;
    for (int i = 0; i < 24519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24520(int& val) {
    func_in_24520(val);
    const int x = 24520;
    std::cout << 24520;
    for (int i = 0; i < 24520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24520(int& val) {
    const int x = 24520;
    std::cout << 24520;
    for (int i = 0; i < 24520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24521(int& val) {
    func_in_24521(val);
    const int x = 24521;
    std::cout << 24521;
    for (int i = 0; i < 24521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24521(int& val) {
    const int x = 24521;
    std::cout << 24521;
    for (int i = 0; i < 24521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24522(int& val) {
    func_in_24522(val);
    const int x = 24522;
    std::cout << 24522;
    for (int i = 0; i < 24522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24522(int& val) {
    const int x = 24522;
    std::cout << 24522;
    for (int i = 0; i < 24522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24523(int& val) {
    func_in_24523(val);
    const int x = 24523;
    std::cout << 24523;
    for (int i = 0; i < 24523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24523(int& val) {
    const int x = 24523;
    std::cout << 24523;
    for (int i = 0; i < 24523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24524(int& val) {
    func_in_24524(val);
    const int x = 24524;
    std::cout << 24524;
    for (int i = 0; i < 24524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24524(int& val) {
    const int x = 24524;
    std::cout << 24524;
    for (int i = 0; i < 24524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24525(int& val) {
    func_in_24525(val);
    const int x = 24525;
    std::cout << 24525;
    for (int i = 0; i < 24525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24525(int& val) {
    const int x = 24525;
    std::cout << 24525;
    for (int i = 0; i < 24525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24526(int& val) {
    func_in_24526(val);
    const int x = 24526;
    std::cout << 24526;
    for (int i = 0; i < 24526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24526(int& val) {
    const int x = 24526;
    std::cout << 24526;
    for (int i = 0; i < 24526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24527(int& val) {
    func_in_24527(val);
    const int x = 24527;
    std::cout << 24527;
    for (int i = 0; i < 24527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24527(int& val) {
    const int x = 24527;
    std::cout << 24527;
    for (int i = 0; i < 24527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24528(int& val) {
    func_in_24528(val);
    const int x = 24528;
    std::cout << 24528;
    for (int i = 0; i < 24528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24528(int& val) {
    const int x = 24528;
    std::cout << 24528;
    for (int i = 0; i < 24528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24529(int& val) {
    func_in_24529(val);
    const int x = 24529;
    std::cout << 24529;
    for (int i = 0; i < 24529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24529(int& val) {
    const int x = 24529;
    std::cout << 24529;
    for (int i = 0; i < 24529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24530(int& val) {
    func_in_24530(val);
    const int x = 24530;
    std::cout << 24530;
    for (int i = 0; i < 24530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24530(int& val) {
    const int x = 24530;
    std::cout << 24530;
    for (int i = 0; i < 24530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24531(int& val) {
    func_in_24531(val);
    const int x = 24531;
    std::cout << 24531;
    for (int i = 0; i < 24531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24531(int& val) {
    const int x = 24531;
    std::cout << 24531;
    for (int i = 0; i < 24531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24532(int& val) {
    func_in_24532(val);
    const int x = 24532;
    std::cout << 24532;
    for (int i = 0; i < 24532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24532(int& val) {
    const int x = 24532;
    std::cout << 24532;
    for (int i = 0; i < 24532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24533(int& val) {
    func_in_24533(val);
    const int x = 24533;
    std::cout << 24533;
    for (int i = 0; i < 24533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24533(int& val) {
    const int x = 24533;
    std::cout << 24533;
    for (int i = 0; i < 24533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24534(int& val) {
    func_in_24534(val);
    const int x = 24534;
    std::cout << 24534;
    for (int i = 0; i < 24534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24534(int& val) {
    const int x = 24534;
    std::cout << 24534;
    for (int i = 0; i < 24534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24535(int& val) {
    func_in_24535(val);
    const int x = 24535;
    std::cout << 24535;
    for (int i = 0; i < 24535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24535(int& val) {
    const int x = 24535;
    std::cout << 24535;
    for (int i = 0; i < 24535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24536(int& val) {
    func_in_24536(val);
    const int x = 24536;
    std::cout << 24536;
    for (int i = 0; i < 24536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24536(int& val) {
    const int x = 24536;
    std::cout << 24536;
    for (int i = 0; i < 24536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24537(int& val) {
    func_in_24537(val);
    const int x = 24537;
    std::cout << 24537;
    for (int i = 0; i < 24537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24537(int& val) {
    const int x = 24537;
    std::cout << 24537;
    for (int i = 0; i < 24537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24538(int& val) {
    func_in_24538(val);
    const int x = 24538;
    std::cout << 24538;
    for (int i = 0; i < 24538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24538(int& val) {
    const int x = 24538;
    std::cout << 24538;
    for (int i = 0; i < 24538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24539(int& val) {
    func_in_24539(val);
    const int x = 24539;
    std::cout << 24539;
    for (int i = 0; i < 24539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24539(int& val) {
    const int x = 24539;
    std::cout << 24539;
    for (int i = 0; i < 24539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24540(int& val) {
    func_in_24540(val);
    const int x = 24540;
    std::cout << 24540;
    for (int i = 0; i < 24540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24540(int& val) {
    const int x = 24540;
    std::cout << 24540;
    for (int i = 0; i < 24540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24541(int& val) {
    func_in_24541(val);
    const int x = 24541;
    std::cout << 24541;
    for (int i = 0; i < 24541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24541(int& val) {
    const int x = 24541;
    std::cout << 24541;
    for (int i = 0; i < 24541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24542(int& val) {
    func_in_24542(val);
    const int x = 24542;
    std::cout << 24542;
    for (int i = 0; i < 24542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24542(int& val) {
    const int x = 24542;
    std::cout << 24542;
    for (int i = 0; i < 24542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24543(int& val) {
    func_in_24543(val);
    const int x = 24543;
    std::cout << 24543;
    for (int i = 0; i < 24543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24543(int& val) {
    const int x = 24543;
    std::cout << 24543;
    for (int i = 0; i < 24543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24544(int& val) {
    func_in_24544(val);
    const int x = 24544;
    std::cout << 24544;
    for (int i = 0; i < 24544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24544(int& val) {
    const int x = 24544;
    std::cout << 24544;
    for (int i = 0; i < 24544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24545(int& val) {
    func_in_24545(val);
    const int x = 24545;
    std::cout << 24545;
    for (int i = 0; i < 24545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24545(int& val) {
    const int x = 24545;
    std::cout << 24545;
    for (int i = 0; i < 24545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24546(int& val) {
    func_in_24546(val);
    const int x = 24546;
    std::cout << 24546;
    for (int i = 0; i < 24546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24546(int& val) {
    const int x = 24546;
    std::cout << 24546;
    for (int i = 0; i < 24546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24547(int& val) {
    func_in_24547(val);
    const int x = 24547;
    std::cout << 24547;
    for (int i = 0; i < 24547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24547(int& val) {
    const int x = 24547;
    std::cout << 24547;
    for (int i = 0; i < 24547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24548(int& val) {
    func_in_24548(val);
    const int x = 24548;
    std::cout << 24548;
    for (int i = 0; i < 24548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24548(int& val) {
    const int x = 24548;
    std::cout << 24548;
    for (int i = 0; i < 24548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24549(int& val) {
    func_in_24549(val);
    const int x = 24549;
    std::cout << 24549;
    for (int i = 0; i < 24549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24549(int& val) {
    const int x = 24549;
    std::cout << 24549;
    for (int i = 0; i < 24549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24550(int& val) {
    func_in_24550(val);
    const int x = 24550;
    std::cout << 24550;
    for (int i = 0; i < 24550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24550(int& val) {
    const int x = 24550;
    std::cout << 24550;
    for (int i = 0; i < 24550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24551(int& val) {
    func_in_24551(val);
    const int x = 24551;
    std::cout << 24551;
    for (int i = 0; i < 24551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24551(int& val) {
    const int x = 24551;
    std::cout << 24551;
    for (int i = 0; i < 24551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24552(int& val) {
    func_in_24552(val);
    const int x = 24552;
    std::cout << 24552;
    for (int i = 0; i < 24552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24552(int& val) {
    const int x = 24552;
    std::cout << 24552;
    for (int i = 0; i < 24552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24553(int& val) {
    func_in_24553(val);
    const int x = 24553;
    std::cout << 24553;
    for (int i = 0; i < 24553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24553(int& val) {
    const int x = 24553;
    std::cout << 24553;
    for (int i = 0; i < 24553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24554(int& val) {
    func_in_24554(val);
    const int x = 24554;
    std::cout << 24554;
    for (int i = 0; i < 24554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24554(int& val) {
    const int x = 24554;
    std::cout << 24554;
    for (int i = 0; i < 24554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24555(int& val) {
    func_in_24555(val);
    const int x = 24555;
    std::cout << 24555;
    for (int i = 0; i < 24555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24555(int& val) {
    const int x = 24555;
    std::cout << 24555;
    for (int i = 0; i < 24555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24556(int& val) {
    func_in_24556(val);
    const int x = 24556;
    std::cout << 24556;
    for (int i = 0; i < 24556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24556(int& val) {
    const int x = 24556;
    std::cout << 24556;
    for (int i = 0; i < 24556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24557(int& val) {
    func_in_24557(val);
    const int x = 24557;
    std::cout << 24557;
    for (int i = 0; i < 24557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24557(int& val) {
    const int x = 24557;
    std::cout << 24557;
    for (int i = 0; i < 24557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24558(int& val) {
    func_in_24558(val);
    const int x = 24558;
    std::cout << 24558;
    for (int i = 0; i < 24558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24558(int& val) {
    const int x = 24558;
    std::cout << 24558;
    for (int i = 0; i < 24558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24559(int& val) {
    func_in_24559(val);
    const int x = 24559;
    std::cout << 24559;
    for (int i = 0; i < 24559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24559(int& val) {
    const int x = 24559;
    std::cout << 24559;
    for (int i = 0; i < 24559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24560(int& val) {
    func_in_24560(val);
    const int x = 24560;
    std::cout << 24560;
    for (int i = 0; i < 24560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24560(int& val) {
    const int x = 24560;
    std::cout << 24560;
    for (int i = 0; i < 24560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24561(int& val) {
    func_in_24561(val);
    const int x = 24561;
    std::cout << 24561;
    for (int i = 0; i < 24561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24561(int& val) {
    const int x = 24561;
    std::cout << 24561;
    for (int i = 0; i < 24561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24562(int& val) {
    func_in_24562(val);
    const int x = 24562;
    std::cout << 24562;
    for (int i = 0; i < 24562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24562(int& val) {
    const int x = 24562;
    std::cout << 24562;
    for (int i = 0; i < 24562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24563(int& val) {
    func_in_24563(val);
    const int x = 24563;
    std::cout << 24563;
    for (int i = 0; i < 24563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24563(int& val) {
    const int x = 24563;
    std::cout << 24563;
    for (int i = 0; i < 24563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24564(int& val) {
    func_in_24564(val);
    const int x = 24564;
    std::cout << 24564;
    for (int i = 0; i < 24564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24564(int& val) {
    const int x = 24564;
    std::cout << 24564;
    for (int i = 0; i < 24564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24565(int& val) {
    func_in_24565(val);
    const int x = 24565;
    std::cout << 24565;
    for (int i = 0; i < 24565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24565(int& val) {
    const int x = 24565;
    std::cout << 24565;
    for (int i = 0; i < 24565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24566(int& val) {
    func_in_24566(val);
    const int x = 24566;
    std::cout << 24566;
    for (int i = 0; i < 24566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24566(int& val) {
    const int x = 24566;
    std::cout << 24566;
    for (int i = 0; i < 24566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24567(int& val) {
    func_in_24567(val);
    const int x = 24567;
    std::cout << 24567;
    for (int i = 0; i < 24567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24567(int& val) {
    const int x = 24567;
    std::cout << 24567;
    for (int i = 0; i < 24567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24568(int& val) {
    func_in_24568(val);
    const int x = 24568;
    std::cout << 24568;
    for (int i = 0; i < 24568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24568(int& val) {
    const int x = 24568;
    std::cout << 24568;
    for (int i = 0; i < 24568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24569(int& val) {
    func_in_24569(val);
    const int x = 24569;
    std::cout << 24569;
    for (int i = 0; i < 24569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24569(int& val) {
    const int x = 24569;
    std::cout << 24569;
    for (int i = 0; i < 24569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24570(int& val) {
    func_in_24570(val);
    const int x = 24570;
    std::cout << 24570;
    for (int i = 0; i < 24570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24570(int& val) {
    const int x = 24570;
    std::cout << 24570;
    for (int i = 0; i < 24570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24571(int& val) {
    func_in_24571(val);
    const int x = 24571;
    std::cout << 24571;
    for (int i = 0; i < 24571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24571(int& val) {
    const int x = 24571;
    std::cout << 24571;
    for (int i = 0; i < 24571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24572(int& val) {
    func_in_24572(val);
    const int x = 24572;
    std::cout << 24572;
    for (int i = 0; i < 24572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24572(int& val) {
    const int x = 24572;
    std::cout << 24572;
    for (int i = 0; i < 24572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24573(int& val) {
    func_in_24573(val);
    const int x = 24573;
    std::cout << 24573;
    for (int i = 0; i < 24573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24573(int& val) {
    const int x = 24573;
    std::cout << 24573;
    for (int i = 0; i < 24573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24574(int& val) {
    func_in_24574(val);
    const int x = 24574;
    std::cout << 24574;
    for (int i = 0; i < 24574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24574(int& val) {
    const int x = 24574;
    std::cout << 24574;
    for (int i = 0; i < 24574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24575(int& val) {
    func_in_24575(val);
    const int x = 24575;
    std::cout << 24575;
    for (int i = 0; i < 24575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24575(int& val) {
    const int x = 24575;
    std::cout << 24575;
    for (int i = 0; i < 24575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24576(int& val) {
    func_in_24576(val);
    const int x = 24576;
    std::cout << 24576;
    for (int i = 0; i < 24576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24576(int& val) {
    const int x = 24576;
    std::cout << 24576;
    for (int i = 0; i < 24576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24577(int& val) {
    func_in_24577(val);
    const int x = 24577;
    std::cout << 24577;
    for (int i = 0; i < 24577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24577(int& val) {
    const int x = 24577;
    std::cout << 24577;
    for (int i = 0; i < 24577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24578(int& val) {
    func_in_24578(val);
    const int x = 24578;
    std::cout << 24578;
    for (int i = 0; i < 24578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24578(int& val) {
    const int x = 24578;
    std::cout << 24578;
    for (int i = 0; i < 24578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24579(int& val) {
    func_in_24579(val);
    const int x = 24579;
    std::cout << 24579;
    for (int i = 0; i < 24579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24579(int& val) {
    const int x = 24579;
    std::cout << 24579;
    for (int i = 0; i < 24579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24580(int& val) {
    func_in_24580(val);
    const int x = 24580;
    std::cout << 24580;
    for (int i = 0; i < 24580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24580(int& val) {
    const int x = 24580;
    std::cout << 24580;
    for (int i = 0; i < 24580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24581(int& val) {
    func_in_24581(val);
    const int x = 24581;
    std::cout << 24581;
    for (int i = 0; i < 24581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24581(int& val) {
    const int x = 24581;
    std::cout << 24581;
    for (int i = 0; i < 24581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24582(int& val) {
    func_in_24582(val);
    const int x = 24582;
    std::cout << 24582;
    for (int i = 0; i < 24582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24582(int& val) {
    const int x = 24582;
    std::cout << 24582;
    for (int i = 0; i < 24582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24583(int& val) {
    func_in_24583(val);
    const int x = 24583;
    std::cout << 24583;
    for (int i = 0; i < 24583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24583(int& val) {
    const int x = 24583;
    std::cout << 24583;
    for (int i = 0; i < 24583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24584(int& val) {
    func_in_24584(val);
    const int x = 24584;
    std::cout << 24584;
    for (int i = 0; i < 24584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24584(int& val) {
    const int x = 24584;
    std::cout << 24584;
    for (int i = 0; i < 24584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24585(int& val) {
    func_in_24585(val);
    const int x = 24585;
    std::cout << 24585;
    for (int i = 0; i < 24585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24585(int& val) {
    const int x = 24585;
    std::cout << 24585;
    for (int i = 0; i < 24585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24586(int& val) {
    func_in_24586(val);
    const int x = 24586;
    std::cout << 24586;
    for (int i = 0; i < 24586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24586(int& val) {
    const int x = 24586;
    std::cout << 24586;
    for (int i = 0; i < 24586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24587(int& val) {
    func_in_24587(val);
    const int x = 24587;
    std::cout << 24587;
    for (int i = 0; i < 24587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24587(int& val) {
    const int x = 24587;
    std::cout << 24587;
    for (int i = 0; i < 24587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24588(int& val) {
    func_in_24588(val);
    const int x = 24588;
    std::cout << 24588;
    for (int i = 0; i < 24588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24588(int& val) {
    const int x = 24588;
    std::cout << 24588;
    for (int i = 0; i < 24588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24589(int& val) {
    func_in_24589(val);
    const int x = 24589;
    std::cout << 24589;
    for (int i = 0; i < 24589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24589(int& val) {
    const int x = 24589;
    std::cout << 24589;
    for (int i = 0; i < 24589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24590(int& val) {
    func_in_24590(val);
    const int x = 24590;
    std::cout << 24590;
    for (int i = 0; i < 24590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24590(int& val) {
    const int x = 24590;
    std::cout << 24590;
    for (int i = 0; i < 24590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24591(int& val) {
    func_in_24591(val);
    const int x = 24591;
    std::cout << 24591;
    for (int i = 0; i < 24591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24591(int& val) {
    const int x = 24591;
    std::cout << 24591;
    for (int i = 0; i < 24591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24592(int& val) {
    func_in_24592(val);
    const int x = 24592;
    std::cout << 24592;
    for (int i = 0; i < 24592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24592(int& val) {
    const int x = 24592;
    std::cout << 24592;
    for (int i = 0; i < 24592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24593(int& val) {
    func_in_24593(val);
    const int x = 24593;
    std::cout << 24593;
    for (int i = 0; i < 24593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24593(int& val) {
    const int x = 24593;
    std::cout << 24593;
    for (int i = 0; i < 24593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24594(int& val) {
    func_in_24594(val);
    const int x = 24594;
    std::cout << 24594;
    for (int i = 0; i < 24594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24594(int& val) {
    const int x = 24594;
    std::cout << 24594;
    for (int i = 0; i < 24594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24595(int& val) {
    func_in_24595(val);
    const int x = 24595;
    std::cout << 24595;
    for (int i = 0; i < 24595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24595(int& val) {
    const int x = 24595;
    std::cout << 24595;
    for (int i = 0; i < 24595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24596(int& val) {
    func_in_24596(val);
    const int x = 24596;
    std::cout << 24596;
    for (int i = 0; i < 24596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24596(int& val) {
    const int x = 24596;
    std::cout << 24596;
    for (int i = 0; i < 24596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24597(int& val) {
    func_in_24597(val);
    const int x = 24597;
    std::cout << 24597;
    for (int i = 0; i < 24597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24597(int& val) {
    const int x = 24597;
    std::cout << 24597;
    for (int i = 0; i < 24597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24598(int& val) {
    func_in_24598(val);
    const int x = 24598;
    std::cout << 24598;
    for (int i = 0; i < 24598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24598(int& val) {
    const int x = 24598;
    std::cout << 24598;
    for (int i = 0; i < 24598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24599(int& val) {
    func_in_24599(val);
    const int x = 24599;
    std::cout << 24599;
    for (int i = 0; i < 24599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24599(int& val) {
    const int x = 24599;
    std::cout << 24599;
    for (int i = 0; i < 24599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24600(int& val) {
    func_in_24600(val);
    const int x = 24600;
    std::cout << 24600;
    for (int i = 0; i < 24600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24600(int& val) {
    const int x = 24600;
    std::cout << 24600;
    for (int i = 0; i < 24600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24601(int& val) {
    func_in_24601(val);
    const int x = 24601;
    std::cout << 24601;
    for (int i = 0; i < 24601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24601(int& val) {
    const int x = 24601;
    std::cout << 24601;
    for (int i = 0; i < 24601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24602(int& val) {
    func_in_24602(val);
    const int x = 24602;
    std::cout << 24602;
    for (int i = 0; i < 24602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24602(int& val) {
    const int x = 24602;
    std::cout << 24602;
    for (int i = 0; i < 24602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24603(int& val) {
    func_in_24603(val);
    const int x = 24603;
    std::cout << 24603;
    for (int i = 0; i < 24603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24603(int& val) {
    const int x = 24603;
    std::cout << 24603;
    for (int i = 0; i < 24603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24604(int& val) {
    func_in_24604(val);
    const int x = 24604;
    std::cout << 24604;
    for (int i = 0; i < 24604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24604(int& val) {
    const int x = 24604;
    std::cout << 24604;
    for (int i = 0; i < 24604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24605(int& val) {
    func_in_24605(val);
    const int x = 24605;
    std::cout << 24605;
    for (int i = 0; i < 24605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24605(int& val) {
    const int x = 24605;
    std::cout << 24605;
    for (int i = 0; i < 24605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24606(int& val) {
    func_in_24606(val);
    const int x = 24606;
    std::cout << 24606;
    for (int i = 0; i < 24606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24606(int& val) {
    const int x = 24606;
    std::cout << 24606;
    for (int i = 0; i < 24606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24607(int& val) {
    func_in_24607(val);
    const int x = 24607;
    std::cout << 24607;
    for (int i = 0; i < 24607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24607(int& val) {
    const int x = 24607;
    std::cout << 24607;
    for (int i = 0; i < 24607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24608(int& val) {
    func_in_24608(val);
    const int x = 24608;
    std::cout << 24608;
    for (int i = 0; i < 24608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24608(int& val) {
    const int x = 24608;
    std::cout << 24608;
    for (int i = 0; i < 24608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24609(int& val) {
    func_in_24609(val);
    const int x = 24609;
    std::cout << 24609;
    for (int i = 0; i < 24609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24609(int& val) {
    const int x = 24609;
    std::cout << 24609;
    for (int i = 0; i < 24609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24610(int& val) {
    func_in_24610(val);
    const int x = 24610;
    std::cout << 24610;
    for (int i = 0; i < 24610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24610(int& val) {
    const int x = 24610;
    std::cout << 24610;
    for (int i = 0; i < 24610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24611(int& val) {
    func_in_24611(val);
    const int x = 24611;
    std::cout << 24611;
    for (int i = 0; i < 24611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24611(int& val) {
    const int x = 24611;
    std::cout << 24611;
    for (int i = 0; i < 24611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24612(int& val) {
    func_in_24612(val);
    const int x = 24612;
    std::cout << 24612;
    for (int i = 0; i < 24612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24612(int& val) {
    const int x = 24612;
    std::cout << 24612;
    for (int i = 0; i < 24612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24613(int& val) {
    func_in_24613(val);
    const int x = 24613;
    std::cout << 24613;
    for (int i = 0; i < 24613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24613(int& val) {
    const int x = 24613;
    std::cout << 24613;
    for (int i = 0; i < 24613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24614(int& val) {
    func_in_24614(val);
    const int x = 24614;
    std::cout << 24614;
    for (int i = 0; i < 24614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24614(int& val) {
    const int x = 24614;
    std::cout << 24614;
    for (int i = 0; i < 24614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24615(int& val) {
    func_in_24615(val);
    const int x = 24615;
    std::cout << 24615;
    for (int i = 0; i < 24615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24615(int& val) {
    const int x = 24615;
    std::cout << 24615;
    for (int i = 0; i < 24615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24616(int& val) {
    func_in_24616(val);
    const int x = 24616;
    std::cout << 24616;
    for (int i = 0; i < 24616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24616(int& val) {
    const int x = 24616;
    std::cout << 24616;
    for (int i = 0; i < 24616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24617(int& val) {
    func_in_24617(val);
    const int x = 24617;
    std::cout << 24617;
    for (int i = 0; i < 24617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24617(int& val) {
    const int x = 24617;
    std::cout << 24617;
    for (int i = 0; i < 24617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24618(int& val) {
    func_in_24618(val);
    const int x = 24618;
    std::cout << 24618;
    for (int i = 0; i < 24618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24618(int& val) {
    const int x = 24618;
    std::cout << 24618;
    for (int i = 0; i < 24618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24619(int& val) {
    func_in_24619(val);
    const int x = 24619;
    std::cout << 24619;
    for (int i = 0; i < 24619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24619(int& val) {
    const int x = 24619;
    std::cout << 24619;
    for (int i = 0; i < 24619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24620(int& val) {
    func_in_24620(val);
    const int x = 24620;
    std::cout << 24620;
    for (int i = 0; i < 24620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24620(int& val) {
    const int x = 24620;
    std::cout << 24620;
    for (int i = 0; i < 24620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24621(int& val) {
    func_in_24621(val);
    const int x = 24621;
    std::cout << 24621;
    for (int i = 0; i < 24621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24621(int& val) {
    const int x = 24621;
    std::cout << 24621;
    for (int i = 0; i < 24621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24622(int& val) {
    func_in_24622(val);
    const int x = 24622;
    std::cout << 24622;
    for (int i = 0; i < 24622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24622(int& val) {
    const int x = 24622;
    std::cout << 24622;
    for (int i = 0; i < 24622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24623(int& val) {
    func_in_24623(val);
    const int x = 24623;
    std::cout << 24623;
    for (int i = 0; i < 24623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24623(int& val) {
    const int x = 24623;
    std::cout << 24623;
    for (int i = 0; i < 24623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24624(int& val) {
    func_in_24624(val);
    const int x = 24624;
    std::cout << 24624;
    for (int i = 0; i < 24624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24624(int& val) {
    const int x = 24624;
    std::cout << 24624;
    for (int i = 0; i < 24624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24625(int& val) {
    func_in_24625(val);
    const int x = 24625;
    std::cout << 24625;
    for (int i = 0; i < 24625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24625(int& val) {
    const int x = 24625;
    std::cout << 24625;
    for (int i = 0; i < 24625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24626(int& val) {
    func_in_24626(val);
    const int x = 24626;
    std::cout << 24626;
    for (int i = 0; i < 24626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24626(int& val) {
    const int x = 24626;
    std::cout << 24626;
    for (int i = 0; i < 24626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24627(int& val) {
    func_in_24627(val);
    const int x = 24627;
    std::cout << 24627;
    for (int i = 0; i < 24627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24627(int& val) {
    const int x = 24627;
    std::cout << 24627;
    for (int i = 0; i < 24627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24628(int& val) {
    func_in_24628(val);
    const int x = 24628;
    std::cout << 24628;
    for (int i = 0; i < 24628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24628(int& val) {
    const int x = 24628;
    std::cout << 24628;
    for (int i = 0; i < 24628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24629(int& val) {
    func_in_24629(val);
    const int x = 24629;
    std::cout << 24629;
    for (int i = 0; i < 24629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24629(int& val) {
    const int x = 24629;
    std::cout << 24629;
    for (int i = 0; i < 24629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24630(int& val) {
    func_in_24630(val);
    const int x = 24630;
    std::cout << 24630;
    for (int i = 0; i < 24630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24630(int& val) {
    const int x = 24630;
    std::cout << 24630;
    for (int i = 0; i < 24630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24631(int& val) {
    func_in_24631(val);
    const int x = 24631;
    std::cout << 24631;
    for (int i = 0; i < 24631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24631(int& val) {
    const int x = 24631;
    std::cout << 24631;
    for (int i = 0; i < 24631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24632(int& val) {
    func_in_24632(val);
    const int x = 24632;
    std::cout << 24632;
    for (int i = 0; i < 24632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24632(int& val) {
    const int x = 24632;
    std::cout << 24632;
    for (int i = 0; i < 24632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24633(int& val) {
    func_in_24633(val);
    const int x = 24633;
    std::cout << 24633;
    for (int i = 0; i < 24633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24633(int& val) {
    const int x = 24633;
    std::cout << 24633;
    for (int i = 0; i < 24633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24634(int& val) {
    func_in_24634(val);
    const int x = 24634;
    std::cout << 24634;
    for (int i = 0; i < 24634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24634(int& val) {
    const int x = 24634;
    std::cout << 24634;
    for (int i = 0; i < 24634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24635(int& val) {
    func_in_24635(val);
    const int x = 24635;
    std::cout << 24635;
    for (int i = 0; i < 24635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24635(int& val) {
    const int x = 24635;
    std::cout << 24635;
    for (int i = 0; i < 24635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24636(int& val) {
    func_in_24636(val);
    const int x = 24636;
    std::cout << 24636;
    for (int i = 0; i < 24636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24636(int& val) {
    const int x = 24636;
    std::cout << 24636;
    for (int i = 0; i < 24636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24637(int& val) {
    func_in_24637(val);
    const int x = 24637;
    std::cout << 24637;
    for (int i = 0; i < 24637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24637(int& val) {
    const int x = 24637;
    std::cout << 24637;
    for (int i = 0; i < 24637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24638(int& val) {
    func_in_24638(val);
    const int x = 24638;
    std::cout << 24638;
    for (int i = 0; i < 24638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24638(int& val) {
    const int x = 24638;
    std::cout << 24638;
    for (int i = 0; i < 24638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24639(int& val) {
    func_in_24639(val);
    const int x = 24639;
    std::cout << 24639;
    for (int i = 0; i < 24639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24639(int& val) {
    const int x = 24639;
    std::cout << 24639;
    for (int i = 0; i < 24639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24640(int& val) {
    func_in_24640(val);
    const int x = 24640;
    std::cout << 24640;
    for (int i = 0; i < 24640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24640(int& val) {
    const int x = 24640;
    std::cout << 24640;
    for (int i = 0; i < 24640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24641(int& val) {
    func_in_24641(val);
    const int x = 24641;
    std::cout << 24641;
    for (int i = 0; i < 24641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24641(int& val) {
    const int x = 24641;
    std::cout << 24641;
    for (int i = 0; i < 24641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24642(int& val) {
    func_in_24642(val);
    const int x = 24642;
    std::cout << 24642;
    for (int i = 0; i < 24642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24642(int& val) {
    const int x = 24642;
    std::cout << 24642;
    for (int i = 0; i < 24642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24643(int& val) {
    func_in_24643(val);
    const int x = 24643;
    std::cout << 24643;
    for (int i = 0; i < 24643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24643(int& val) {
    const int x = 24643;
    std::cout << 24643;
    for (int i = 0; i < 24643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24644(int& val) {
    func_in_24644(val);
    const int x = 24644;
    std::cout << 24644;
    for (int i = 0; i < 24644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24644(int& val) {
    const int x = 24644;
    std::cout << 24644;
    for (int i = 0; i < 24644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24645(int& val) {
    func_in_24645(val);
    const int x = 24645;
    std::cout << 24645;
    for (int i = 0; i < 24645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24645(int& val) {
    const int x = 24645;
    std::cout << 24645;
    for (int i = 0; i < 24645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24646(int& val) {
    func_in_24646(val);
    const int x = 24646;
    std::cout << 24646;
    for (int i = 0; i < 24646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24646(int& val) {
    const int x = 24646;
    std::cout << 24646;
    for (int i = 0; i < 24646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24647(int& val) {
    func_in_24647(val);
    const int x = 24647;
    std::cout << 24647;
    for (int i = 0; i < 24647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24647(int& val) {
    const int x = 24647;
    std::cout << 24647;
    for (int i = 0; i < 24647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24648(int& val) {
    func_in_24648(val);
    const int x = 24648;
    std::cout << 24648;
    for (int i = 0; i < 24648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24648(int& val) {
    const int x = 24648;
    std::cout << 24648;
    for (int i = 0; i < 24648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24649(int& val) {
    func_in_24649(val);
    const int x = 24649;
    std::cout << 24649;
    for (int i = 0; i < 24649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24649(int& val) {
    const int x = 24649;
    std::cout << 24649;
    for (int i = 0; i < 24649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24650(int& val) {
    func_in_24650(val);
    const int x = 24650;
    std::cout << 24650;
    for (int i = 0; i < 24650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24650(int& val) {
    const int x = 24650;
    std::cout << 24650;
    for (int i = 0; i < 24650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24651(int& val) {
    func_in_24651(val);
    const int x = 24651;
    std::cout << 24651;
    for (int i = 0; i < 24651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24651(int& val) {
    const int x = 24651;
    std::cout << 24651;
    for (int i = 0; i < 24651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24652(int& val) {
    func_in_24652(val);
    const int x = 24652;
    std::cout << 24652;
    for (int i = 0; i < 24652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24652(int& val) {
    const int x = 24652;
    std::cout << 24652;
    for (int i = 0; i < 24652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24653(int& val) {
    func_in_24653(val);
    const int x = 24653;
    std::cout << 24653;
    for (int i = 0; i < 24653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24653(int& val) {
    const int x = 24653;
    std::cout << 24653;
    for (int i = 0; i < 24653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24654(int& val) {
    func_in_24654(val);
    const int x = 24654;
    std::cout << 24654;
    for (int i = 0; i < 24654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24654(int& val) {
    const int x = 24654;
    std::cout << 24654;
    for (int i = 0; i < 24654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24655(int& val) {
    func_in_24655(val);
    const int x = 24655;
    std::cout << 24655;
    for (int i = 0; i < 24655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24655(int& val) {
    const int x = 24655;
    std::cout << 24655;
    for (int i = 0; i < 24655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24656(int& val) {
    func_in_24656(val);
    const int x = 24656;
    std::cout << 24656;
    for (int i = 0; i < 24656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24656(int& val) {
    const int x = 24656;
    std::cout << 24656;
    for (int i = 0; i < 24656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24657(int& val) {
    func_in_24657(val);
    const int x = 24657;
    std::cout << 24657;
    for (int i = 0; i < 24657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24657(int& val) {
    const int x = 24657;
    std::cout << 24657;
    for (int i = 0; i < 24657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24658(int& val) {
    func_in_24658(val);
    const int x = 24658;
    std::cout << 24658;
    for (int i = 0; i < 24658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24658(int& val) {
    const int x = 24658;
    std::cout << 24658;
    for (int i = 0; i < 24658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24659(int& val) {
    func_in_24659(val);
    const int x = 24659;
    std::cout << 24659;
    for (int i = 0; i < 24659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24659(int& val) {
    const int x = 24659;
    std::cout << 24659;
    for (int i = 0; i < 24659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24660(int& val) {
    func_in_24660(val);
    const int x = 24660;
    std::cout << 24660;
    for (int i = 0; i < 24660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24660(int& val) {
    const int x = 24660;
    std::cout << 24660;
    for (int i = 0; i < 24660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24661(int& val) {
    func_in_24661(val);
    const int x = 24661;
    std::cout << 24661;
    for (int i = 0; i < 24661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24661(int& val) {
    const int x = 24661;
    std::cout << 24661;
    for (int i = 0; i < 24661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24662(int& val) {
    func_in_24662(val);
    const int x = 24662;
    std::cout << 24662;
    for (int i = 0; i < 24662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24662(int& val) {
    const int x = 24662;
    std::cout << 24662;
    for (int i = 0; i < 24662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24663(int& val) {
    func_in_24663(val);
    const int x = 24663;
    std::cout << 24663;
    for (int i = 0; i < 24663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24663(int& val) {
    const int x = 24663;
    std::cout << 24663;
    for (int i = 0; i < 24663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24664(int& val) {
    func_in_24664(val);
    const int x = 24664;
    std::cout << 24664;
    for (int i = 0; i < 24664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24664(int& val) {
    const int x = 24664;
    std::cout << 24664;
    for (int i = 0; i < 24664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24665(int& val) {
    func_in_24665(val);
    const int x = 24665;
    std::cout << 24665;
    for (int i = 0; i < 24665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24665(int& val) {
    const int x = 24665;
    std::cout << 24665;
    for (int i = 0; i < 24665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24666(int& val) {
    func_in_24666(val);
    const int x = 24666;
    std::cout << 24666;
    for (int i = 0; i < 24666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24666(int& val) {
    const int x = 24666;
    std::cout << 24666;
    for (int i = 0; i < 24666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24667(int& val) {
    func_in_24667(val);
    const int x = 24667;
    std::cout << 24667;
    for (int i = 0; i < 24667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24667(int& val) {
    const int x = 24667;
    std::cout << 24667;
    for (int i = 0; i < 24667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24668(int& val) {
    func_in_24668(val);
    const int x = 24668;
    std::cout << 24668;
    for (int i = 0; i < 24668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24668(int& val) {
    const int x = 24668;
    std::cout << 24668;
    for (int i = 0; i < 24668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24669(int& val) {
    func_in_24669(val);
    const int x = 24669;
    std::cout << 24669;
    for (int i = 0; i < 24669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24669(int& val) {
    const int x = 24669;
    std::cout << 24669;
    for (int i = 0; i < 24669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24670(int& val) {
    func_in_24670(val);
    const int x = 24670;
    std::cout << 24670;
    for (int i = 0; i < 24670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24670(int& val) {
    const int x = 24670;
    std::cout << 24670;
    for (int i = 0; i < 24670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24671(int& val) {
    func_in_24671(val);
    const int x = 24671;
    std::cout << 24671;
    for (int i = 0; i < 24671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24671(int& val) {
    const int x = 24671;
    std::cout << 24671;
    for (int i = 0; i < 24671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24672(int& val) {
    func_in_24672(val);
    const int x = 24672;
    std::cout << 24672;
    for (int i = 0; i < 24672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24672(int& val) {
    const int x = 24672;
    std::cout << 24672;
    for (int i = 0; i < 24672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24673(int& val) {
    func_in_24673(val);
    const int x = 24673;
    std::cout << 24673;
    for (int i = 0; i < 24673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24673(int& val) {
    const int x = 24673;
    std::cout << 24673;
    for (int i = 0; i < 24673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24674(int& val) {
    func_in_24674(val);
    const int x = 24674;
    std::cout << 24674;
    for (int i = 0; i < 24674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24674(int& val) {
    const int x = 24674;
    std::cout << 24674;
    for (int i = 0; i < 24674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24675(int& val) {
    func_in_24675(val);
    const int x = 24675;
    std::cout << 24675;
    for (int i = 0; i < 24675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24675(int& val) {
    const int x = 24675;
    std::cout << 24675;
    for (int i = 0; i < 24675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24676(int& val) {
    func_in_24676(val);
    const int x = 24676;
    std::cout << 24676;
    for (int i = 0; i < 24676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24676(int& val) {
    const int x = 24676;
    std::cout << 24676;
    for (int i = 0; i < 24676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24677(int& val) {
    func_in_24677(val);
    const int x = 24677;
    std::cout << 24677;
    for (int i = 0; i < 24677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24677(int& val) {
    const int x = 24677;
    std::cout << 24677;
    for (int i = 0; i < 24677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24678(int& val) {
    func_in_24678(val);
    const int x = 24678;
    std::cout << 24678;
    for (int i = 0; i < 24678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24678(int& val) {
    const int x = 24678;
    std::cout << 24678;
    for (int i = 0; i < 24678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24679(int& val) {
    func_in_24679(val);
    const int x = 24679;
    std::cout << 24679;
    for (int i = 0; i < 24679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24679(int& val) {
    const int x = 24679;
    std::cout << 24679;
    for (int i = 0; i < 24679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24680(int& val) {
    func_in_24680(val);
    const int x = 24680;
    std::cout << 24680;
    for (int i = 0; i < 24680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24680(int& val) {
    const int x = 24680;
    std::cout << 24680;
    for (int i = 0; i < 24680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24681(int& val) {
    func_in_24681(val);
    const int x = 24681;
    std::cout << 24681;
    for (int i = 0; i < 24681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24681(int& val) {
    const int x = 24681;
    std::cout << 24681;
    for (int i = 0; i < 24681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24682(int& val) {
    func_in_24682(val);
    const int x = 24682;
    std::cout << 24682;
    for (int i = 0; i < 24682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24682(int& val) {
    const int x = 24682;
    std::cout << 24682;
    for (int i = 0; i < 24682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24683(int& val) {
    func_in_24683(val);
    const int x = 24683;
    std::cout << 24683;
    for (int i = 0; i < 24683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24683(int& val) {
    const int x = 24683;
    std::cout << 24683;
    for (int i = 0; i < 24683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24684(int& val) {
    func_in_24684(val);
    const int x = 24684;
    std::cout << 24684;
    for (int i = 0; i < 24684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24684(int& val) {
    const int x = 24684;
    std::cout << 24684;
    for (int i = 0; i < 24684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24685(int& val) {
    func_in_24685(val);
    const int x = 24685;
    std::cout << 24685;
    for (int i = 0; i < 24685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24685(int& val) {
    const int x = 24685;
    std::cout << 24685;
    for (int i = 0; i < 24685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24686(int& val) {
    func_in_24686(val);
    const int x = 24686;
    std::cout << 24686;
    for (int i = 0; i < 24686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24686(int& val) {
    const int x = 24686;
    std::cout << 24686;
    for (int i = 0; i < 24686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24687(int& val) {
    func_in_24687(val);
    const int x = 24687;
    std::cout << 24687;
    for (int i = 0; i < 24687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24687(int& val) {
    const int x = 24687;
    std::cout << 24687;
    for (int i = 0; i < 24687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24688(int& val) {
    func_in_24688(val);
    const int x = 24688;
    std::cout << 24688;
    for (int i = 0; i < 24688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24688(int& val) {
    const int x = 24688;
    std::cout << 24688;
    for (int i = 0; i < 24688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24689(int& val) {
    func_in_24689(val);
    const int x = 24689;
    std::cout << 24689;
    for (int i = 0; i < 24689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24689(int& val) {
    const int x = 24689;
    std::cout << 24689;
    for (int i = 0; i < 24689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24690(int& val) {
    func_in_24690(val);
    const int x = 24690;
    std::cout << 24690;
    for (int i = 0; i < 24690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24690(int& val) {
    const int x = 24690;
    std::cout << 24690;
    for (int i = 0; i < 24690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24691(int& val) {
    func_in_24691(val);
    const int x = 24691;
    std::cout << 24691;
    for (int i = 0; i < 24691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24691(int& val) {
    const int x = 24691;
    std::cout << 24691;
    for (int i = 0; i < 24691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24692(int& val) {
    func_in_24692(val);
    const int x = 24692;
    std::cout << 24692;
    for (int i = 0; i < 24692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24692(int& val) {
    const int x = 24692;
    std::cout << 24692;
    for (int i = 0; i < 24692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24693(int& val) {
    func_in_24693(val);
    const int x = 24693;
    std::cout << 24693;
    for (int i = 0; i < 24693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24693(int& val) {
    const int x = 24693;
    std::cout << 24693;
    for (int i = 0; i < 24693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24694(int& val) {
    func_in_24694(val);
    const int x = 24694;
    std::cout << 24694;
    for (int i = 0; i < 24694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24694(int& val) {
    const int x = 24694;
    std::cout << 24694;
    for (int i = 0; i < 24694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24695(int& val) {
    func_in_24695(val);
    const int x = 24695;
    std::cout << 24695;
    for (int i = 0; i < 24695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24695(int& val) {
    const int x = 24695;
    std::cout << 24695;
    for (int i = 0; i < 24695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24696(int& val) {
    func_in_24696(val);
    const int x = 24696;
    std::cout << 24696;
    for (int i = 0; i < 24696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24696(int& val) {
    const int x = 24696;
    std::cout << 24696;
    for (int i = 0; i < 24696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24697(int& val) {
    func_in_24697(val);
    const int x = 24697;
    std::cout << 24697;
    for (int i = 0; i < 24697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24697(int& val) {
    const int x = 24697;
    std::cout << 24697;
    for (int i = 0; i < 24697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24698(int& val) {
    func_in_24698(val);
    const int x = 24698;
    std::cout << 24698;
    for (int i = 0; i < 24698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24698(int& val) {
    const int x = 24698;
    std::cout << 24698;
    for (int i = 0; i < 24698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24699(int& val) {
    func_in_24699(val);
    const int x = 24699;
    std::cout << 24699;
    for (int i = 0; i < 24699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24699(int& val) {
    const int x = 24699;
    std::cout << 24699;
    for (int i = 0; i < 24699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24700(int& val) {
    func_in_24700(val);
    const int x = 24700;
    std::cout << 24700;
    for (int i = 0; i < 24700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24700(int& val) {
    const int x = 24700;
    std::cout << 24700;
    for (int i = 0; i < 24700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24701(int& val) {
    func_in_24701(val);
    const int x = 24701;
    std::cout << 24701;
    for (int i = 0; i < 24701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24701(int& val) {
    const int x = 24701;
    std::cout << 24701;
    for (int i = 0; i < 24701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24702(int& val) {
    func_in_24702(val);
    const int x = 24702;
    std::cout << 24702;
    for (int i = 0; i < 24702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24702(int& val) {
    const int x = 24702;
    std::cout << 24702;
    for (int i = 0; i < 24702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24703(int& val) {
    func_in_24703(val);
    const int x = 24703;
    std::cout << 24703;
    for (int i = 0; i < 24703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24703(int& val) {
    const int x = 24703;
    std::cout << 24703;
    for (int i = 0; i < 24703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24704(int& val) {
    func_in_24704(val);
    const int x = 24704;
    std::cout << 24704;
    for (int i = 0; i < 24704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24704(int& val) {
    const int x = 24704;
    std::cout << 24704;
    for (int i = 0; i < 24704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24705(int& val) {
    func_in_24705(val);
    const int x = 24705;
    std::cout << 24705;
    for (int i = 0; i < 24705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24705(int& val) {
    const int x = 24705;
    std::cout << 24705;
    for (int i = 0; i < 24705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24706(int& val) {
    func_in_24706(val);
    const int x = 24706;
    std::cout << 24706;
    for (int i = 0; i < 24706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24706(int& val) {
    const int x = 24706;
    std::cout << 24706;
    for (int i = 0; i < 24706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24707(int& val) {
    func_in_24707(val);
    const int x = 24707;
    std::cout << 24707;
    for (int i = 0; i < 24707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24707(int& val) {
    const int x = 24707;
    std::cout << 24707;
    for (int i = 0; i < 24707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24708(int& val) {
    func_in_24708(val);
    const int x = 24708;
    std::cout << 24708;
    for (int i = 0; i < 24708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24708(int& val) {
    const int x = 24708;
    std::cout << 24708;
    for (int i = 0; i < 24708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24709(int& val) {
    func_in_24709(val);
    const int x = 24709;
    std::cout << 24709;
    for (int i = 0; i < 24709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24709(int& val) {
    const int x = 24709;
    std::cout << 24709;
    for (int i = 0; i < 24709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24710(int& val) {
    func_in_24710(val);
    const int x = 24710;
    std::cout << 24710;
    for (int i = 0; i < 24710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24710(int& val) {
    const int x = 24710;
    std::cout << 24710;
    for (int i = 0; i < 24710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24711(int& val) {
    func_in_24711(val);
    const int x = 24711;
    std::cout << 24711;
    for (int i = 0; i < 24711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24711(int& val) {
    const int x = 24711;
    std::cout << 24711;
    for (int i = 0; i < 24711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24712(int& val) {
    func_in_24712(val);
    const int x = 24712;
    std::cout << 24712;
    for (int i = 0; i < 24712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24712(int& val) {
    const int x = 24712;
    std::cout << 24712;
    for (int i = 0; i < 24712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24713(int& val) {
    func_in_24713(val);
    const int x = 24713;
    std::cout << 24713;
    for (int i = 0; i < 24713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24713(int& val) {
    const int x = 24713;
    std::cout << 24713;
    for (int i = 0; i < 24713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24714(int& val) {
    func_in_24714(val);
    const int x = 24714;
    std::cout << 24714;
    for (int i = 0; i < 24714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24714(int& val) {
    const int x = 24714;
    std::cout << 24714;
    for (int i = 0; i < 24714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24715(int& val) {
    func_in_24715(val);
    const int x = 24715;
    std::cout << 24715;
    for (int i = 0; i < 24715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24715(int& val) {
    const int x = 24715;
    std::cout << 24715;
    for (int i = 0; i < 24715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24716(int& val) {
    func_in_24716(val);
    const int x = 24716;
    std::cout << 24716;
    for (int i = 0; i < 24716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24716(int& val) {
    const int x = 24716;
    std::cout << 24716;
    for (int i = 0; i < 24716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24717(int& val) {
    func_in_24717(val);
    const int x = 24717;
    std::cout << 24717;
    for (int i = 0; i < 24717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24717(int& val) {
    const int x = 24717;
    std::cout << 24717;
    for (int i = 0; i < 24717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24718(int& val) {
    func_in_24718(val);
    const int x = 24718;
    std::cout << 24718;
    for (int i = 0; i < 24718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24718(int& val) {
    const int x = 24718;
    std::cout << 24718;
    for (int i = 0; i < 24718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24719(int& val) {
    func_in_24719(val);
    const int x = 24719;
    std::cout << 24719;
    for (int i = 0; i < 24719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24719(int& val) {
    const int x = 24719;
    std::cout << 24719;
    for (int i = 0; i < 24719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24720(int& val) {
    func_in_24720(val);
    const int x = 24720;
    std::cout << 24720;
    for (int i = 0; i < 24720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24720(int& val) {
    const int x = 24720;
    std::cout << 24720;
    for (int i = 0; i < 24720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24721(int& val) {
    func_in_24721(val);
    const int x = 24721;
    std::cout << 24721;
    for (int i = 0; i < 24721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24721(int& val) {
    const int x = 24721;
    std::cout << 24721;
    for (int i = 0; i < 24721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24722(int& val) {
    func_in_24722(val);
    const int x = 24722;
    std::cout << 24722;
    for (int i = 0; i < 24722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24722(int& val) {
    const int x = 24722;
    std::cout << 24722;
    for (int i = 0; i < 24722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24723(int& val) {
    func_in_24723(val);
    const int x = 24723;
    std::cout << 24723;
    for (int i = 0; i < 24723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24723(int& val) {
    const int x = 24723;
    std::cout << 24723;
    for (int i = 0; i < 24723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24724(int& val) {
    func_in_24724(val);
    const int x = 24724;
    std::cout << 24724;
    for (int i = 0; i < 24724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24724(int& val) {
    const int x = 24724;
    std::cout << 24724;
    for (int i = 0; i < 24724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24725(int& val) {
    func_in_24725(val);
    const int x = 24725;
    std::cout << 24725;
    for (int i = 0; i < 24725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24725(int& val) {
    const int x = 24725;
    std::cout << 24725;
    for (int i = 0; i < 24725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24726(int& val) {
    func_in_24726(val);
    const int x = 24726;
    std::cout << 24726;
    for (int i = 0; i < 24726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24726(int& val) {
    const int x = 24726;
    std::cout << 24726;
    for (int i = 0; i < 24726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24727(int& val) {
    func_in_24727(val);
    const int x = 24727;
    std::cout << 24727;
    for (int i = 0; i < 24727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24727(int& val) {
    const int x = 24727;
    std::cout << 24727;
    for (int i = 0; i < 24727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24728(int& val) {
    func_in_24728(val);
    const int x = 24728;
    std::cout << 24728;
    for (int i = 0; i < 24728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24728(int& val) {
    const int x = 24728;
    std::cout << 24728;
    for (int i = 0; i < 24728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24729(int& val) {
    func_in_24729(val);
    const int x = 24729;
    std::cout << 24729;
    for (int i = 0; i < 24729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24729(int& val) {
    const int x = 24729;
    std::cout << 24729;
    for (int i = 0; i < 24729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24730(int& val) {
    func_in_24730(val);
    const int x = 24730;
    std::cout << 24730;
    for (int i = 0; i < 24730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24730(int& val) {
    const int x = 24730;
    std::cout << 24730;
    for (int i = 0; i < 24730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24731(int& val) {
    func_in_24731(val);
    const int x = 24731;
    std::cout << 24731;
    for (int i = 0; i < 24731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24731(int& val) {
    const int x = 24731;
    std::cout << 24731;
    for (int i = 0; i < 24731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24732(int& val) {
    func_in_24732(val);
    const int x = 24732;
    std::cout << 24732;
    for (int i = 0; i < 24732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24732(int& val) {
    const int x = 24732;
    std::cout << 24732;
    for (int i = 0; i < 24732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24733(int& val) {
    func_in_24733(val);
    const int x = 24733;
    std::cout << 24733;
    for (int i = 0; i < 24733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24733(int& val) {
    const int x = 24733;
    std::cout << 24733;
    for (int i = 0; i < 24733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24734(int& val) {
    func_in_24734(val);
    const int x = 24734;
    std::cout << 24734;
    for (int i = 0; i < 24734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24734(int& val) {
    const int x = 24734;
    std::cout << 24734;
    for (int i = 0; i < 24734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24735(int& val) {
    func_in_24735(val);
    const int x = 24735;
    std::cout << 24735;
    for (int i = 0; i < 24735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24735(int& val) {
    const int x = 24735;
    std::cout << 24735;
    for (int i = 0; i < 24735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24736(int& val) {
    func_in_24736(val);
    const int x = 24736;
    std::cout << 24736;
    for (int i = 0; i < 24736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24736(int& val) {
    const int x = 24736;
    std::cout << 24736;
    for (int i = 0; i < 24736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24737(int& val) {
    func_in_24737(val);
    const int x = 24737;
    std::cout << 24737;
    for (int i = 0; i < 24737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24737(int& val) {
    const int x = 24737;
    std::cout << 24737;
    for (int i = 0; i < 24737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24738(int& val) {
    func_in_24738(val);
    const int x = 24738;
    std::cout << 24738;
    for (int i = 0; i < 24738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24738(int& val) {
    const int x = 24738;
    std::cout << 24738;
    for (int i = 0; i < 24738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24739(int& val) {
    func_in_24739(val);
    const int x = 24739;
    std::cout << 24739;
    for (int i = 0; i < 24739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24739(int& val) {
    const int x = 24739;
    std::cout << 24739;
    for (int i = 0; i < 24739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24740(int& val) {
    func_in_24740(val);
    const int x = 24740;
    std::cout << 24740;
    for (int i = 0; i < 24740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24740(int& val) {
    const int x = 24740;
    std::cout << 24740;
    for (int i = 0; i < 24740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24741(int& val) {
    func_in_24741(val);
    const int x = 24741;
    std::cout << 24741;
    for (int i = 0; i < 24741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24741(int& val) {
    const int x = 24741;
    std::cout << 24741;
    for (int i = 0; i < 24741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24742(int& val) {
    func_in_24742(val);
    const int x = 24742;
    std::cout << 24742;
    for (int i = 0; i < 24742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24742(int& val) {
    const int x = 24742;
    std::cout << 24742;
    for (int i = 0; i < 24742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24743(int& val) {
    func_in_24743(val);
    const int x = 24743;
    std::cout << 24743;
    for (int i = 0; i < 24743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24743(int& val) {
    const int x = 24743;
    std::cout << 24743;
    for (int i = 0; i < 24743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24744(int& val) {
    func_in_24744(val);
    const int x = 24744;
    std::cout << 24744;
    for (int i = 0; i < 24744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24744(int& val) {
    const int x = 24744;
    std::cout << 24744;
    for (int i = 0; i < 24744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24745(int& val) {
    func_in_24745(val);
    const int x = 24745;
    std::cout << 24745;
    for (int i = 0; i < 24745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24745(int& val) {
    const int x = 24745;
    std::cout << 24745;
    for (int i = 0; i < 24745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24746(int& val) {
    func_in_24746(val);
    const int x = 24746;
    std::cout << 24746;
    for (int i = 0; i < 24746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24746(int& val) {
    const int x = 24746;
    std::cout << 24746;
    for (int i = 0; i < 24746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24747(int& val) {
    func_in_24747(val);
    const int x = 24747;
    std::cout << 24747;
    for (int i = 0; i < 24747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24747(int& val) {
    const int x = 24747;
    std::cout << 24747;
    for (int i = 0; i < 24747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24748(int& val) {
    func_in_24748(val);
    const int x = 24748;
    std::cout << 24748;
    for (int i = 0; i < 24748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24748(int& val) {
    const int x = 24748;
    std::cout << 24748;
    for (int i = 0; i < 24748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24749(int& val) {
    func_in_24749(val);
    const int x = 24749;
    std::cout << 24749;
    for (int i = 0; i < 24749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24749(int& val) {
    const int x = 24749;
    std::cout << 24749;
    for (int i = 0; i < 24749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24750(int& val) {
    func_in_24750(val);
    const int x = 24750;
    std::cout << 24750;
    for (int i = 0; i < 24750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24750(int& val) {
    const int x = 24750;
    std::cout << 24750;
    for (int i = 0; i < 24750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24751(int& val) {
    func_in_24751(val);
    const int x = 24751;
    std::cout << 24751;
    for (int i = 0; i < 24751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24751(int& val) {
    const int x = 24751;
    std::cout << 24751;
    for (int i = 0; i < 24751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24752(int& val) {
    func_in_24752(val);
    const int x = 24752;
    std::cout << 24752;
    for (int i = 0; i < 24752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24752(int& val) {
    const int x = 24752;
    std::cout << 24752;
    for (int i = 0; i < 24752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24753(int& val) {
    func_in_24753(val);
    const int x = 24753;
    std::cout << 24753;
    for (int i = 0; i < 24753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24753(int& val) {
    const int x = 24753;
    std::cout << 24753;
    for (int i = 0; i < 24753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24754(int& val) {
    func_in_24754(val);
    const int x = 24754;
    std::cout << 24754;
    for (int i = 0; i < 24754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24754(int& val) {
    const int x = 24754;
    std::cout << 24754;
    for (int i = 0; i < 24754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24755(int& val) {
    func_in_24755(val);
    const int x = 24755;
    std::cout << 24755;
    for (int i = 0; i < 24755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24755(int& val) {
    const int x = 24755;
    std::cout << 24755;
    for (int i = 0; i < 24755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24756(int& val) {
    func_in_24756(val);
    const int x = 24756;
    std::cout << 24756;
    for (int i = 0; i < 24756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24756(int& val) {
    const int x = 24756;
    std::cout << 24756;
    for (int i = 0; i < 24756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24757(int& val) {
    func_in_24757(val);
    const int x = 24757;
    std::cout << 24757;
    for (int i = 0; i < 24757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24757(int& val) {
    const int x = 24757;
    std::cout << 24757;
    for (int i = 0; i < 24757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24758(int& val) {
    func_in_24758(val);
    const int x = 24758;
    std::cout << 24758;
    for (int i = 0; i < 24758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24758(int& val) {
    const int x = 24758;
    std::cout << 24758;
    for (int i = 0; i < 24758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24759(int& val) {
    func_in_24759(val);
    const int x = 24759;
    std::cout << 24759;
    for (int i = 0; i < 24759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24759(int& val) {
    const int x = 24759;
    std::cout << 24759;
    for (int i = 0; i < 24759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24760(int& val) {
    func_in_24760(val);
    const int x = 24760;
    std::cout << 24760;
    for (int i = 0; i < 24760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24760(int& val) {
    const int x = 24760;
    std::cout << 24760;
    for (int i = 0; i < 24760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24761(int& val) {
    func_in_24761(val);
    const int x = 24761;
    std::cout << 24761;
    for (int i = 0; i < 24761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24761(int& val) {
    const int x = 24761;
    std::cout << 24761;
    for (int i = 0; i < 24761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24762(int& val) {
    func_in_24762(val);
    const int x = 24762;
    std::cout << 24762;
    for (int i = 0; i < 24762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24762(int& val) {
    const int x = 24762;
    std::cout << 24762;
    for (int i = 0; i < 24762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24763(int& val) {
    func_in_24763(val);
    const int x = 24763;
    std::cout << 24763;
    for (int i = 0; i < 24763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24763(int& val) {
    const int x = 24763;
    std::cout << 24763;
    for (int i = 0; i < 24763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24764(int& val) {
    func_in_24764(val);
    const int x = 24764;
    std::cout << 24764;
    for (int i = 0; i < 24764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24764(int& val) {
    const int x = 24764;
    std::cout << 24764;
    for (int i = 0; i < 24764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24765(int& val) {
    func_in_24765(val);
    const int x = 24765;
    std::cout << 24765;
    for (int i = 0; i < 24765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24765(int& val) {
    const int x = 24765;
    std::cout << 24765;
    for (int i = 0; i < 24765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24766(int& val) {
    func_in_24766(val);
    const int x = 24766;
    std::cout << 24766;
    for (int i = 0; i < 24766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24766(int& val) {
    const int x = 24766;
    std::cout << 24766;
    for (int i = 0; i < 24766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24767(int& val) {
    func_in_24767(val);
    const int x = 24767;
    std::cout << 24767;
    for (int i = 0; i < 24767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24767(int& val) {
    const int x = 24767;
    std::cout << 24767;
    for (int i = 0; i < 24767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24768(int& val) {
    func_in_24768(val);
    const int x = 24768;
    std::cout << 24768;
    for (int i = 0; i < 24768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24768(int& val) {
    const int x = 24768;
    std::cout << 24768;
    for (int i = 0; i < 24768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24769(int& val) {
    func_in_24769(val);
    const int x = 24769;
    std::cout << 24769;
    for (int i = 0; i < 24769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24769(int& val) {
    const int x = 24769;
    std::cout << 24769;
    for (int i = 0; i < 24769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24770(int& val) {
    func_in_24770(val);
    const int x = 24770;
    std::cout << 24770;
    for (int i = 0; i < 24770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24770(int& val) {
    const int x = 24770;
    std::cout << 24770;
    for (int i = 0; i < 24770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24771(int& val) {
    func_in_24771(val);
    const int x = 24771;
    std::cout << 24771;
    for (int i = 0; i < 24771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24771(int& val) {
    const int x = 24771;
    std::cout << 24771;
    for (int i = 0; i < 24771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24772(int& val) {
    func_in_24772(val);
    const int x = 24772;
    std::cout << 24772;
    for (int i = 0; i < 24772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24772(int& val) {
    const int x = 24772;
    std::cout << 24772;
    for (int i = 0; i < 24772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24773(int& val) {
    func_in_24773(val);
    const int x = 24773;
    std::cout << 24773;
    for (int i = 0; i < 24773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24773(int& val) {
    const int x = 24773;
    std::cout << 24773;
    for (int i = 0; i < 24773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24774(int& val) {
    func_in_24774(val);
    const int x = 24774;
    std::cout << 24774;
    for (int i = 0; i < 24774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24774(int& val) {
    const int x = 24774;
    std::cout << 24774;
    for (int i = 0; i < 24774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24775(int& val) {
    func_in_24775(val);
    const int x = 24775;
    std::cout << 24775;
    for (int i = 0; i < 24775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24775(int& val) {
    const int x = 24775;
    std::cout << 24775;
    for (int i = 0; i < 24775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24776(int& val) {
    func_in_24776(val);
    const int x = 24776;
    std::cout << 24776;
    for (int i = 0; i < 24776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24776(int& val) {
    const int x = 24776;
    std::cout << 24776;
    for (int i = 0; i < 24776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24777(int& val) {
    func_in_24777(val);
    const int x = 24777;
    std::cout << 24777;
    for (int i = 0; i < 24777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24777(int& val) {
    const int x = 24777;
    std::cout << 24777;
    for (int i = 0; i < 24777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24778(int& val) {
    func_in_24778(val);
    const int x = 24778;
    std::cout << 24778;
    for (int i = 0; i < 24778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24778(int& val) {
    const int x = 24778;
    std::cout << 24778;
    for (int i = 0; i < 24778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24779(int& val) {
    func_in_24779(val);
    const int x = 24779;
    std::cout << 24779;
    for (int i = 0; i < 24779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24779(int& val) {
    const int x = 24779;
    std::cout << 24779;
    for (int i = 0; i < 24779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24780(int& val) {
    func_in_24780(val);
    const int x = 24780;
    std::cout << 24780;
    for (int i = 0; i < 24780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24780(int& val) {
    const int x = 24780;
    std::cout << 24780;
    for (int i = 0; i < 24780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24781(int& val) {
    func_in_24781(val);
    const int x = 24781;
    std::cout << 24781;
    for (int i = 0; i < 24781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24781(int& val) {
    const int x = 24781;
    std::cout << 24781;
    for (int i = 0; i < 24781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24782(int& val) {
    func_in_24782(val);
    const int x = 24782;
    std::cout << 24782;
    for (int i = 0; i < 24782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24782(int& val) {
    const int x = 24782;
    std::cout << 24782;
    for (int i = 0; i < 24782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24783(int& val) {
    func_in_24783(val);
    const int x = 24783;
    std::cout << 24783;
    for (int i = 0; i < 24783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24783(int& val) {
    const int x = 24783;
    std::cout << 24783;
    for (int i = 0; i < 24783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24784(int& val) {
    func_in_24784(val);
    const int x = 24784;
    std::cout << 24784;
    for (int i = 0; i < 24784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24784(int& val) {
    const int x = 24784;
    std::cout << 24784;
    for (int i = 0; i < 24784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24785(int& val) {
    func_in_24785(val);
    const int x = 24785;
    std::cout << 24785;
    for (int i = 0; i < 24785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24785(int& val) {
    const int x = 24785;
    std::cout << 24785;
    for (int i = 0; i < 24785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24786(int& val) {
    func_in_24786(val);
    const int x = 24786;
    std::cout << 24786;
    for (int i = 0; i < 24786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24786(int& val) {
    const int x = 24786;
    std::cout << 24786;
    for (int i = 0; i < 24786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24787(int& val) {
    func_in_24787(val);
    const int x = 24787;
    std::cout << 24787;
    for (int i = 0; i < 24787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24787(int& val) {
    const int x = 24787;
    std::cout << 24787;
    for (int i = 0; i < 24787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24788(int& val) {
    func_in_24788(val);
    const int x = 24788;
    std::cout << 24788;
    for (int i = 0; i < 24788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24788(int& val) {
    const int x = 24788;
    std::cout << 24788;
    for (int i = 0; i < 24788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24789(int& val) {
    func_in_24789(val);
    const int x = 24789;
    std::cout << 24789;
    for (int i = 0; i < 24789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24789(int& val) {
    const int x = 24789;
    std::cout << 24789;
    for (int i = 0; i < 24789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24790(int& val) {
    func_in_24790(val);
    const int x = 24790;
    std::cout << 24790;
    for (int i = 0; i < 24790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24790(int& val) {
    const int x = 24790;
    std::cout << 24790;
    for (int i = 0; i < 24790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24791(int& val) {
    func_in_24791(val);
    const int x = 24791;
    std::cout << 24791;
    for (int i = 0; i < 24791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24791(int& val) {
    const int x = 24791;
    std::cout << 24791;
    for (int i = 0; i < 24791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24792(int& val) {
    func_in_24792(val);
    const int x = 24792;
    std::cout << 24792;
    for (int i = 0; i < 24792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24792(int& val) {
    const int x = 24792;
    std::cout << 24792;
    for (int i = 0; i < 24792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24793(int& val) {
    func_in_24793(val);
    const int x = 24793;
    std::cout << 24793;
    for (int i = 0; i < 24793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24793(int& val) {
    const int x = 24793;
    std::cout << 24793;
    for (int i = 0; i < 24793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24794(int& val) {
    func_in_24794(val);
    const int x = 24794;
    std::cout << 24794;
    for (int i = 0; i < 24794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24794(int& val) {
    const int x = 24794;
    std::cout << 24794;
    for (int i = 0; i < 24794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24795(int& val) {
    func_in_24795(val);
    const int x = 24795;
    std::cout << 24795;
    for (int i = 0; i < 24795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24795(int& val) {
    const int x = 24795;
    std::cout << 24795;
    for (int i = 0; i < 24795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24796(int& val) {
    func_in_24796(val);
    const int x = 24796;
    std::cout << 24796;
    for (int i = 0; i < 24796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24796(int& val) {
    const int x = 24796;
    std::cout << 24796;
    for (int i = 0; i < 24796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24797(int& val) {
    func_in_24797(val);
    const int x = 24797;
    std::cout << 24797;
    for (int i = 0; i < 24797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24797(int& val) {
    const int x = 24797;
    std::cout << 24797;
    for (int i = 0; i < 24797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24798(int& val) {
    func_in_24798(val);
    const int x = 24798;
    std::cout << 24798;
    for (int i = 0; i < 24798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24798(int& val) {
    const int x = 24798;
    std::cout << 24798;
    for (int i = 0; i < 24798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24799(int& val) {
    func_in_24799(val);
    const int x = 24799;
    std::cout << 24799;
    for (int i = 0; i < 24799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24799(int& val) {
    const int x = 24799;
    std::cout << 24799;
    for (int i = 0; i < 24799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24800(int& val) {
    func_in_24800(val);
    const int x = 24800;
    std::cout << 24800;
    for (int i = 0; i < 24800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24800(int& val) {
    const int x = 24800;
    std::cout << 24800;
    for (int i = 0; i < 24800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24801(int& val) {
    func_in_24801(val);
    const int x = 24801;
    std::cout << 24801;
    for (int i = 0; i < 24801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24801(int& val) {
    const int x = 24801;
    std::cout << 24801;
    for (int i = 0; i < 24801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24802(int& val) {
    func_in_24802(val);
    const int x = 24802;
    std::cout << 24802;
    for (int i = 0; i < 24802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24802(int& val) {
    const int x = 24802;
    std::cout << 24802;
    for (int i = 0; i < 24802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24803(int& val) {
    func_in_24803(val);
    const int x = 24803;
    std::cout << 24803;
    for (int i = 0; i < 24803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24803(int& val) {
    const int x = 24803;
    std::cout << 24803;
    for (int i = 0; i < 24803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24804(int& val) {
    func_in_24804(val);
    const int x = 24804;
    std::cout << 24804;
    for (int i = 0; i < 24804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24804(int& val) {
    const int x = 24804;
    std::cout << 24804;
    for (int i = 0; i < 24804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24805(int& val) {
    func_in_24805(val);
    const int x = 24805;
    std::cout << 24805;
    for (int i = 0; i < 24805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24805(int& val) {
    const int x = 24805;
    std::cout << 24805;
    for (int i = 0; i < 24805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24806(int& val) {
    func_in_24806(val);
    const int x = 24806;
    std::cout << 24806;
    for (int i = 0; i < 24806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24806(int& val) {
    const int x = 24806;
    std::cout << 24806;
    for (int i = 0; i < 24806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24807(int& val) {
    func_in_24807(val);
    const int x = 24807;
    std::cout << 24807;
    for (int i = 0; i < 24807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24807(int& val) {
    const int x = 24807;
    std::cout << 24807;
    for (int i = 0; i < 24807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24808(int& val) {
    func_in_24808(val);
    const int x = 24808;
    std::cout << 24808;
    for (int i = 0; i < 24808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24808(int& val) {
    const int x = 24808;
    std::cout << 24808;
    for (int i = 0; i < 24808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24809(int& val) {
    func_in_24809(val);
    const int x = 24809;
    std::cout << 24809;
    for (int i = 0; i < 24809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24809(int& val) {
    const int x = 24809;
    std::cout << 24809;
    for (int i = 0; i < 24809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24810(int& val) {
    func_in_24810(val);
    const int x = 24810;
    std::cout << 24810;
    for (int i = 0; i < 24810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24810(int& val) {
    const int x = 24810;
    std::cout << 24810;
    for (int i = 0; i < 24810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24811(int& val) {
    func_in_24811(val);
    const int x = 24811;
    std::cout << 24811;
    for (int i = 0; i < 24811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24811(int& val) {
    const int x = 24811;
    std::cout << 24811;
    for (int i = 0; i < 24811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24812(int& val) {
    func_in_24812(val);
    const int x = 24812;
    std::cout << 24812;
    for (int i = 0; i < 24812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24812(int& val) {
    const int x = 24812;
    std::cout << 24812;
    for (int i = 0; i < 24812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24813(int& val) {
    func_in_24813(val);
    const int x = 24813;
    std::cout << 24813;
    for (int i = 0; i < 24813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24813(int& val) {
    const int x = 24813;
    std::cout << 24813;
    for (int i = 0; i < 24813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24814(int& val) {
    func_in_24814(val);
    const int x = 24814;
    std::cout << 24814;
    for (int i = 0; i < 24814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24814(int& val) {
    const int x = 24814;
    std::cout << 24814;
    for (int i = 0; i < 24814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24815(int& val) {
    func_in_24815(val);
    const int x = 24815;
    std::cout << 24815;
    for (int i = 0; i < 24815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24815(int& val) {
    const int x = 24815;
    std::cout << 24815;
    for (int i = 0; i < 24815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24816(int& val) {
    func_in_24816(val);
    const int x = 24816;
    std::cout << 24816;
    for (int i = 0; i < 24816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24816(int& val) {
    const int x = 24816;
    std::cout << 24816;
    for (int i = 0; i < 24816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24817(int& val) {
    func_in_24817(val);
    const int x = 24817;
    std::cout << 24817;
    for (int i = 0; i < 24817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24817(int& val) {
    const int x = 24817;
    std::cout << 24817;
    for (int i = 0; i < 24817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24818(int& val) {
    func_in_24818(val);
    const int x = 24818;
    std::cout << 24818;
    for (int i = 0; i < 24818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24818(int& val) {
    const int x = 24818;
    std::cout << 24818;
    for (int i = 0; i < 24818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24819(int& val) {
    func_in_24819(val);
    const int x = 24819;
    std::cout << 24819;
    for (int i = 0; i < 24819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24819(int& val) {
    const int x = 24819;
    std::cout << 24819;
    for (int i = 0; i < 24819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24820(int& val) {
    func_in_24820(val);
    const int x = 24820;
    std::cout << 24820;
    for (int i = 0; i < 24820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24820(int& val) {
    const int x = 24820;
    std::cout << 24820;
    for (int i = 0; i < 24820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24821(int& val) {
    func_in_24821(val);
    const int x = 24821;
    std::cout << 24821;
    for (int i = 0; i < 24821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24821(int& val) {
    const int x = 24821;
    std::cout << 24821;
    for (int i = 0; i < 24821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24822(int& val) {
    func_in_24822(val);
    const int x = 24822;
    std::cout << 24822;
    for (int i = 0; i < 24822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24822(int& val) {
    const int x = 24822;
    std::cout << 24822;
    for (int i = 0; i < 24822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24823(int& val) {
    func_in_24823(val);
    const int x = 24823;
    std::cout << 24823;
    for (int i = 0; i < 24823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24823(int& val) {
    const int x = 24823;
    std::cout << 24823;
    for (int i = 0; i < 24823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24824(int& val) {
    func_in_24824(val);
    const int x = 24824;
    std::cout << 24824;
    for (int i = 0; i < 24824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24824(int& val) {
    const int x = 24824;
    std::cout << 24824;
    for (int i = 0; i < 24824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24825(int& val) {
    func_in_24825(val);
    const int x = 24825;
    std::cout << 24825;
    for (int i = 0; i < 24825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24825(int& val) {
    const int x = 24825;
    std::cout << 24825;
    for (int i = 0; i < 24825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24826(int& val) {
    func_in_24826(val);
    const int x = 24826;
    std::cout << 24826;
    for (int i = 0; i < 24826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24826(int& val) {
    const int x = 24826;
    std::cout << 24826;
    for (int i = 0; i < 24826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24827(int& val) {
    func_in_24827(val);
    const int x = 24827;
    std::cout << 24827;
    for (int i = 0; i < 24827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24827(int& val) {
    const int x = 24827;
    std::cout << 24827;
    for (int i = 0; i < 24827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24828(int& val) {
    func_in_24828(val);
    const int x = 24828;
    std::cout << 24828;
    for (int i = 0; i < 24828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24828(int& val) {
    const int x = 24828;
    std::cout << 24828;
    for (int i = 0; i < 24828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24829(int& val) {
    func_in_24829(val);
    const int x = 24829;
    std::cout << 24829;
    for (int i = 0; i < 24829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24829(int& val) {
    const int x = 24829;
    std::cout << 24829;
    for (int i = 0; i < 24829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24830(int& val) {
    func_in_24830(val);
    const int x = 24830;
    std::cout << 24830;
    for (int i = 0; i < 24830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24830(int& val) {
    const int x = 24830;
    std::cout << 24830;
    for (int i = 0; i < 24830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24831(int& val) {
    func_in_24831(val);
    const int x = 24831;
    std::cout << 24831;
    for (int i = 0; i < 24831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24831(int& val) {
    const int x = 24831;
    std::cout << 24831;
    for (int i = 0; i < 24831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24832(int& val) {
    func_in_24832(val);
    const int x = 24832;
    std::cout << 24832;
    for (int i = 0; i < 24832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24832(int& val) {
    const int x = 24832;
    std::cout << 24832;
    for (int i = 0; i < 24832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24833(int& val) {
    func_in_24833(val);
    const int x = 24833;
    std::cout << 24833;
    for (int i = 0; i < 24833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24833(int& val) {
    const int x = 24833;
    std::cout << 24833;
    for (int i = 0; i < 24833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24834(int& val) {
    func_in_24834(val);
    const int x = 24834;
    std::cout << 24834;
    for (int i = 0; i < 24834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24834(int& val) {
    const int x = 24834;
    std::cout << 24834;
    for (int i = 0; i < 24834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24835(int& val) {
    func_in_24835(val);
    const int x = 24835;
    std::cout << 24835;
    for (int i = 0; i < 24835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24835(int& val) {
    const int x = 24835;
    std::cout << 24835;
    for (int i = 0; i < 24835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24836(int& val) {
    func_in_24836(val);
    const int x = 24836;
    std::cout << 24836;
    for (int i = 0; i < 24836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24836(int& val) {
    const int x = 24836;
    std::cout << 24836;
    for (int i = 0; i < 24836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24837(int& val) {
    func_in_24837(val);
    const int x = 24837;
    std::cout << 24837;
    for (int i = 0; i < 24837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24837(int& val) {
    const int x = 24837;
    std::cout << 24837;
    for (int i = 0; i < 24837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24838(int& val) {
    func_in_24838(val);
    const int x = 24838;
    std::cout << 24838;
    for (int i = 0; i < 24838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24838(int& val) {
    const int x = 24838;
    std::cout << 24838;
    for (int i = 0; i < 24838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24839(int& val) {
    func_in_24839(val);
    const int x = 24839;
    std::cout << 24839;
    for (int i = 0; i < 24839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24839(int& val) {
    const int x = 24839;
    std::cout << 24839;
    for (int i = 0; i < 24839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24840(int& val) {
    func_in_24840(val);
    const int x = 24840;
    std::cout << 24840;
    for (int i = 0; i < 24840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24840(int& val) {
    const int x = 24840;
    std::cout << 24840;
    for (int i = 0; i < 24840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24841(int& val) {
    func_in_24841(val);
    const int x = 24841;
    std::cout << 24841;
    for (int i = 0; i < 24841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24841(int& val) {
    const int x = 24841;
    std::cout << 24841;
    for (int i = 0; i < 24841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24842(int& val) {
    func_in_24842(val);
    const int x = 24842;
    std::cout << 24842;
    for (int i = 0; i < 24842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24842(int& val) {
    const int x = 24842;
    std::cout << 24842;
    for (int i = 0; i < 24842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24843(int& val) {
    func_in_24843(val);
    const int x = 24843;
    std::cout << 24843;
    for (int i = 0; i < 24843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24843(int& val) {
    const int x = 24843;
    std::cout << 24843;
    for (int i = 0; i < 24843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24844(int& val) {
    func_in_24844(val);
    const int x = 24844;
    std::cout << 24844;
    for (int i = 0; i < 24844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24844(int& val) {
    const int x = 24844;
    std::cout << 24844;
    for (int i = 0; i < 24844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24845(int& val) {
    func_in_24845(val);
    const int x = 24845;
    std::cout << 24845;
    for (int i = 0; i < 24845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24845(int& val) {
    const int x = 24845;
    std::cout << 24845;
    for (int i = 0; i < 24845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24846(int& val) {
    func_in_24846(val);
    const int x = 24846;
    std::cout << 24846;
    for (int i = 0; i < 24846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24846(int& val) {
    const int x = 24846;
    std::cout << 24846;
    for (int i = 0; i < 24846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24847(int& val) {
    func_in_24847(val);
    const int x = 24847;
    std::cout << 24847;
    for (int i = 0; i < 24847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24847(int& val) {
    const int x = 24847;
    std::cout << 24847;
    for (int i = 0; i < 24847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24848(int& val) {
    func_in_24848(val);
    const int x = 24848;
    std::cout << 24848;
    for (int i = 0; i < 24848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24848(int& val) {
    const int x = 24848;
    std::cout << 24848;
    for (int i = 0; i < 24848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24849(int& val) {
    func_in_24849(val);
    const int x = 24849;
    std::cout << 24849;
    for (int i = 0; i < 24849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24849(int& val) {
    const int x = 24849;
    std::cout << 24849;
    for (int i = 0; i < 24849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24850(int& val) {
    func_in_24850(val);
    const int x = 24850;
    std::cout << 24850;
    for (int i = 0; i < 24850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24850(int& val) {
    const int x = 24850;
    std::cout << 24850;
    for (int i = 0; i < 24850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24851(int& val) {
    func_in_24851(val);
    const int x = 24851;
    std::cout << 24851;
    for (int i = 0; i < 24851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24851(int& val) {
    const int x = 24851;
    std::cout << 24851;
    for (int i = 0; i < 24851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24852(int& val) {
    func_in_24852(val);
    const int x = 24852;
    std::cout << 24852;
    for (int i = 0; i < 24852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24852(int& val) {
    const int x = 24852;
    std::cout << 24852;
    for (int i = 0; i < 24852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24853(int& val) {
    func_in_24853(val);
    const int x = 24853;
    std::cout << 24853;
    for (int i = 0; i < 24853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24853(int& val) {
    const int x = 24853;
    std::cout << 24853;
    for (int i = 0; i < 24853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24854(int& val) {
    func_in_24854(val);
    const int x = 24854;
    std::cout << 24854;
    for (int i = 0; i < 24854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24854(int& val) {
    const int x = 24854;
    std::cout << 24854;
    for (int i = 0; i < 24854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24855(int& val) {
    func_in_24855(val);
    const int x = 24855;
    std::cout << 24855;
    for (int i = 0; i < 24855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24855(int& val) {
    const int x = 24855;
    std::cout << 24855;
    for (int i = 0; i < 24855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24856(int& val) {
    func_in_24856(val);
    const int x = 24856;
    std::cout << 24856;
    for (int i = 0; i < 24856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24856(int& val) {
    const int x = 24856;
    std::cout << 24856;
    for (int i = 0; i < 24856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24857(int& val) {
    func_in_24857(val);
    const int x = 24857;
    std::cout << 24857;
    for (int i = 0; i < 24857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24857(int& val) {
    const int x = 24857;
    std::cout << 24857;
    for (int i = 0; i < 24857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24858(int& val) {
    func_in_24858(val);
    const int x = 24858;
    std::cout << 24858;
    for (int i = 0; i < 24858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24858(int& val) {
    const int x = 24858;
    std::cout << 24858;
    for (int i = 0; i < 24858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24859(int& val) {
    func_in_24859(val);
    const int x = 24859;
    std::cout << 24859;
    for (int i = 0; i < 24859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24859(int& val) {
    const int x = 24859;
    std::cout << 24859;
    for (int i = 0; i < 24859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24860(int& val) {
    func_in_24860(val);
    const int x = 24860;
    std::cout << 24860;
    for (int i = 0; i < 24860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24860(int& val) {
    const int x = 24860;
    std::cout << 24860;
    for (int i = 0; i < 24860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24861(int& val) {
    func_in_24861(val);
    const int x = 24861;
    std::cout << 24861;
    for (int i = 0; i < 24861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24861(int& val) {
    const int x = 24861;
    std::cout << 24861;
    for (int i = 0; i < 24861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24862(int& val) {
    func_in_24862(val);
    const int x = 24862;
    std::cout << 24862;
    for (int i = 0; i < 24862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24862(int& val) {
    const int x = 24862;
    std::cout << 24862;
    for (int i = 0; i < 24862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24863(int& val) {
    func_in_24863(val);
    const int x = 24863;
    std::cout << 24863;
    for (int i = 0; i < 24863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24863(int& val) {
    const int x = 24863;
    std::cout << 24863;
    for (int i = 0; i < 24863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24864(int& val) {
    func_in_24864(val);
    const int x = 24864;
    std::cout << 24864;
    for (int i = 0; i < 24864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24864(int& val) {
    const int x = 24864;
    std::cout << 24864;
    for (int i = 0; i < 24864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24865(int& val) {
    func_in_24865(val);
    const int x = 24865;
    std::cout << 24865;
    for (int i = 0; i < 24865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24865(int& val) {
    const int x = 24865;
    std::cout << 24865;
    for (int i = 0; i < 24865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24866(int& val) {
    func_in_24866(val);
    const int x = 24866;
    std::cout << 24866;
    for (int i = 0; i < 24866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24866(int& val) {
    const int x = 24866;
    std::cout << 24866;
    for (int i = 0; i < 24866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24867(int& val) {
    func_in_24867(val);
    const int x = 24867;
    std::cout << 24867;
    for (int i = 0; i < 24867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24867(int& val) {
    const int x = 24867;
    std::cout << 24867;
    for (int i = 0; i < 24867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24868(int& val) {
    func_in_24868(val);
    const int x = 24868;
    std::cout << 24868;
    for (int i = 0; i < 24868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24868(int& val) {
    const int x = 24868;
    std::cout << 24868;
    for (int i = 0; i < 24868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24869(int& val) {
    func_in_24869(val);
    const int x = 24869;
    std::cout << 24869;
    for (int i = 0; i < 24869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24869(int& val) {
    const int x = 24869;
    std::cout << 24869;
    for (int i = 0; i < 24869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24870(int& val) {
    func_in_24870(val);
    const int x = 24870;
    std::cout << 24870;
    for (int i = 0; i < 24870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24870(int& val) {
    const int x = 24870;
    std::cout << 24870;
    for (int i = 0; i < 24870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24871(int& val) {
    func_in_24871(val);
    const int x = 24871;
    std::cout << 24871;
    for (int i = 0; i < 24871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24871(int& val) {
    const int x = 24871;
    std::cout << 24871;
    for (int i = 0; i < 24871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24872(int& val) {
    func_in_24872(val);
    const int x = 24872;
    std::cout << 24872;
    for (int i = 0; i < 24872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24872(int& val) {
    const int x = 24872;
    std::cout << 24872;
    for (int i = 0; i < 24872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24873(int& val) {
    func_in_24873(val);
    const int x = 24873;
    std::cout << 24873;
    for (int i = 0; i < 24873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24873(int& val) {
    const int x = 24873;
    std::cout << 24873;
    for (int i = 0; i < 24873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24874(int& val) {
    func_in_24874(val);
    const int x = 24874;
    std::cout << 24874;
    for (int i = 0; i < 24874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24874(int& val) {
    const int x = 24874;
    std::cout << 24874;
    for (int i = 0; i < 24874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24875(int& val) {
    func_in_24875(val);
    const int x = 24875;
    std::cout << 24875;
    for (int i = 0; i < 24875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24875(int& val) {
    const int x = 24875;
    std::cout << 24875;
    for (int i = 0; i < 24875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24876(int& val) {
    func_in_24876(val);
    const int x = 24876;
    std::cout << 24876;
    for (int i = 0; i < 24876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24876(int& val) {
    const int x = 24876;
    std::cout << 24876;
    for (int i = 0; i < 24876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24877(int& val) {
    func_in_24877(val);
    const int x = 24877;
    std::cout << 24877;
    for (int i = 0; i < 24877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24877(int& val) {
    const int x = 24877;
    std::cout << 24877;
    for (int i = 0; i < 24877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24878(int& val) {
    func_in_24878(val);
    const int x = 24878;
    std::cout << 24878;
    for (int i = 0; i < 24878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24878(int& val) {
    const int x = 24878;
    std::cout << 24878;
    for (int i = 0; i < 24878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24879(int& val) {
    func_in_24879(val);
    const int x = 24879;
    std::cout << 24879;
    for (int i = 0; i < 24879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24879(int& val) {
    const int x = 24879;
    std::cout << 24879;
    for (int i = 0; i < 24879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24880(int& val) {
    func_in_24880(val);
    const int x = 24880;
    std::cout << 24880;
    for (int i = 0; i < 24880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24880(int& val) {
    const int x = 24880;
    std::cout << 24880;
    for (int i = 0; i < 24880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24881(int& val) {
    func_in_24881(val);
    const int x = 24881;
    std::cout << 24881;
    for (int i = 0; i < 24881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24881(int& val) {
    const int x = 24881;
    std::cout << 24881;
    for (int i = 0; i < 24881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24882(int& val) {
    func_in_24882(val);
    const int x = 24882;
    std::cout << 24882;
    for (int i = 0; i < 24882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24882(int& val) {
    const int x = 24882;
    std::cout << 24882;
    for (int i = 0; i < 24882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24883(int& val) {
    func_in_24883(val);
    const int x = 24883;
    std::cout << 24883;
    for (int i = 0; i < 24883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24883(int& val) {
    const int x = 24883;
    std::cout << 24883;
    for (int i = 0; i < 24883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24884(int& val) {
    func_in_24884(val);
    const int x = 24884;
    std::cout << 24884;
    for (int i = 0; i < 24884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24884(int& val) {
    const int x = 24884;
    std::cout << 24884;
    for (int i = 0; i < 24884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24885(int& val) {
    func_in_24885(val);
    const int x = 24885;
    std::cout << 24885;
    for (int i = 0; i < 24885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24885(int& val) {
    const int x = 24885;
    std::cout << 24885;
    for (int i = 0; i < 24885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24886(int& val) {
    func_in_24886(val);
    const int x = 24886;
    std::cout << 24886;
    for (int i = 0; i < 24886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24886(int& val) {
    const int x = 24886;
    std::cout << 24886;
    for (int i = 0; i < 24886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24887(int& val) {
    func_in_24887(val);
    const int x = 24887;
    std::cout << 24887;
    for (int i = 0; i < 24887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24887(int& val) {
    const int x = 24887;
    std::cout << 24887;
    for (int i = 0; i < 24887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24888(int& val) {
    func_in_24888(val);
    const int x = 24888;
    std::cout << 24888;
    for (int i = 0; i < 24888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24888(int& val) {
    const int x = 24888;
    std::cout << 24888;
    for (int i = 0; i < 24888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24889(int& val) {
    func_in_24889(val);
    const int x = 24889;
    std::cout << 24889;
    for (int i = 0; i < 24889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24889(int& val) {
    const int x = 24889;
    std::cout << 24889;
    for (int i = 0; i < 24889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24890(int& val) {
    func_in_24890(val);
    const int x = 24890;
    std::cout << 24890;
    for (int i = 0; i < 24890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24890(int& val) {
    const int x = 24890;
    std::cout << 24890;
    for (int i = 0; i < 24890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24891(int& val) {
    func_in_24891(val);
    const int x = 24891;
    std::cout << 24891;
    for (int i = 0; i < 24891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24891(int& val) {
    const int x = 24891;
    std::cout << 24891;
    for (int i = 0; i < 24891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24892(int& val) {
    func_in_24892(val);
    const int x = 24892;
    std::cout << 24892;
    for (int i = 0; i < 24892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24892(int& val) {
    const int x = 24892;
    std::cout << 24892;
    for (int i = 0; i < 24892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24893(int& val) {
    func_in_24893(val);
    const int x = 24893;
    std::cout << 24893;
    for (int i = 0; i < 24893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24893(int& val) {
    const int x = 24893;
    std::cout << 24893;
    for (int i = 0; i < 24893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24894(int& val) {
    func_in_24894(val);
    const int x = 24894;
    std::cout << 24894;
    for (int i = 0; i < 24894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24894(int& val) {
    const int x = 24894;
    std::cout << 24894;
    for (int i = 0; i < 24894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24895(int& val) {
    func_in_24895(val);
    const int x = 24895;
    std::cout << 24895;
    for (int i = 0; i < 24895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24895(int& val) {
    const int x = 24895;
    std::cout << 24895;
    for (int i = 0; i < 24895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24896(int& val) {
    func_in_24896(val);
    const int x = 24896;
    std::cout << 24896;
    for (int i = 0; i < 24896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24896(int& val) {
    const int x = 24896;
    std::cout << 24896;
    for (int i = 0; i < 24896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24897(int& val) {
    func_in_24897(val);
    const int x = 24897;
    std::cout << 24897;
    for (int i = 0; i < 24897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24897(int& val) {
    const int x = 24897;
    std::cout << 24897;
    for (int i = 0; i < 24897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24898(int& val) {
    func_in_24898(val);
    const int x = 24898;
    std::cout << 24898;
    for (int i = 0; i < 24898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24898(int& val) {
    const int x = 24898;
    std::cout << 24898;
    for (int i = 0; i < 24898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24899(int& val) {
    func_in_24899(val);
    const int x = 24899;
    std::cout << 24899;
    for (int i = 0; i < 24899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24899(int& val) {
    const int x = 24899;
    std::cout << 24899;
    for (int i = 0; i < 24899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24900(int& val) {
    func_in_24900(val);
    const int x = 24900;
    std::cout << 24900;
    for (int i = 0; i < 24900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24900(int& val) {
    const int x = 24900;
    std::cout << 24900;
    for (int i = 0; i < 24900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24901(int& val) {
    func_in_24901(val);
    const int x = 24901;
    std::cout << 24901;
    for (int i = 0; i < 24901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24901(int& val) {
    const int x = 24901;
    std::cout << 24901;
    for (int i = 0; i < 24901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24902(int& val) {
    func_in_24902(val);
    const int x = 24902;
    std::cout << 24902;
    for (int i = 0; i < 24902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24902(int& val) {
    const int x = 24902;
    std::cout << 24902;
    for (int i = 0; i < 24902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24903(int& val) {
    func_in_24903(val);
    const int x = 24903;
    std::cout << 24903;
    for (int i = 0; i < 24903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24903(int& val) {
    const int x = 24903;
    std::cout << 24903;
    for (int i = 0; i < 24903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24904(int& val) {
    func_in_24904(val);
    const int x = 24904;
    std::cout << 24904;
    for (int i = 0; i < 24904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24904(int& val) {
    const int x = 24904;
    std::cout << 24904;
    for (int i = 0; i < 24904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24905(int& val) {
    func_in_24905(val);
    const int x = 24905;
    std::cout << 24905;
    for (int i = 0; i < 24905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24905(int& val) {
    const int x = 24905;
    std::cout << 24905;
    for (int i = 0; i < 24905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24906(int& val) {
    func_in_24906(val);
    const int x = 24906;
    std::cout << 24906;
    for (int i = 0; i < 24906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24906(int& val) {
    const int x = 24906;
    std::cout << 24906;
    for (int i = 0; i < 24906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24907(int& val) {
    func_in_24907(val);
    const int x = 24907;
    std::cout << 24907;
    for (int i = 0; i < 24907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24907(int& val) {
    const int x = 24907;
    std::cout << 24907;
    for (int i = 0; i < 24907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24908(int& val) {
    func_in_24908(val);
    const int x = 24908;
    std::cout << 24908;
    for (int i = 0; i < 24908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24908(int& val) {
    const int x = 24908;
    std::cout << 24908;
    for (int i = 0; i < 24908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24909(int& val) {
    func_in_24909(val);
    const int x = 24909;
    std::cout << 24909;
    for (int i = 0; i < 24909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24909(int& val) {
    const int x = 24909;
    std::cout << 24909;
    for (int i = 0; i < 24909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24910(int& val) {
    func_in_24910(val);
    const int x = 24910;
    std::cout << 24910;
    for (int i = 0; i < 24910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24910(int& val) {
    const int x = 24910;
    std::cout << 24910;
    for (int i = 0; i < 24910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24911(int& val) {
    func_in_24911(val);
    const int x = 24911;
    std::cout << 24911;
    for (int i = 0; i < 24911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24911(int& val) {
    const int x = 24911;
    std::cout << 24911;
    for (int i = 0; i < 24911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24912(int& val) {
    func_in_24912(val);
    const int x = 24912;
    std::cout << 24912;
    for (int i = 0; i < 24912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24912(int& val) {
    const int x = 24912;
    std::cout << 24912;
    for (int i = 0; i < 24912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24913(int& val) {
    func_in_24913(val);
    const int x = 24913;
    std::cout << 24913;
    for (int i = 0; i < 24913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24913(int& val) {
    const int x = 24913;
    std::cout << 24913;
    for (int i = 0; i < 24913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24914(int& val) {
    func_in_24914(val);
    const int x = 24914;
    std::cout << 24914;
    for (int i = 0; i < 24914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24914(int& val) {
    const int x = 24914;
    std::cout << 24914;
    for (int i = 0; i < 24914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24915(int& val) {
    func_in_24915(val);
    const int x = 24915;
    std::cout << 24915;
    for (int i = 0; i < 24915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24915(int& val) {
    const int x = 24915;
    std::cout << 24915;
    for (int i = 0; i < 24915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24916(int& val) {
    func_in_24916(val);
    const int x = 24916;
    std::cout << 24916;
    for (int i = 0; i < 24916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24916(int& val) {
    const int x = 24916;
    std::cout << 24916;
    for (int i = 0; i < 24916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24917(int& val) {
    func_in_24917(val);
    const int x = 24917;
    std::cout << 24917;
    for (int i = 0; i < 24917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24917(int& val) {
    const int x = 24917;
    std::cout << 24917;
    for (int i = 0; i < 24917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24918(int& val) {
    func_in_24918(val);
    const int x = 24918;
    std::cout << 24918;
    for (int i = 0; i < 24918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24918(int& val) {
    const int x = 24918;
    std::cout << 24918;
    for (int i = 0; i < 24918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24919(int& val) {
    func_in_24919(val);
    const int x = 24919;
    std::cout << 24919;
    for (int i = 0; i < 24919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24919(int& val) {
    const int x = 24919;
    std::cout << 24919;
    for (int i = 0; i < 24919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24920(int& val) {
    func_in_24920(val);
    const int x = 24920;
    std::cout << 24920;
    for (int i = 0; i < 24920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24920(int& val) {
    const int x = 24920;
    std::cout << 24920;
    for (int i = 0; i < 24920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24921(int& val) {
    func_in_24921(val);
    const int x = 24921;
    std::cout << 24921;
    for (int i = 0; i < 24921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24921(int& val) {
    const int x = 24921;
    std::cout << 24921;
    for (int i = 0; i < 24921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24922(int& val) {
    func_in_24922(val);
    const int x = 24922;
    std::cout << 24922;
    for (int i = 0; i < 24922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24922(int& val) {
    const int x = 24922;
    std::cout << 24922;
    for (int i = 0; i < 24922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24923(int& val) {
    func_in_24923(val);
    const int x = 24923;
    std::cout << 24923;
    for (int i = 0; i < 24923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24923(int& val) {
    const int x = 24923;
    std::cout << 24923;
    for (int i = 0; i < 24923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24924(int& val) {
    func_in_24924(val);
    const int x = 24924;
    std::cout << 24924;
    for (int i = 0; i < 24924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24924(int& val) {
    const int x = 24924;
    std::cout << 24924;
    for (int i = 0; i < 24924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24925(int& val) {
    func_in_24925(val);
    const int x = 24925;
    std::cout << 24925;
    for (int i = 0; i < 24925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24925(int& val) {
    const int x = 24925;
    std::cout << 24925;
    for (int i = 0; i < 24925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24926(int& val) {
    func_in_24926(val);
    const int x = 24926;
    std::cout << 24926;
    for (int i = 0; i < 24926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24926(int& val) {
    const int x = 24926;
    std::cout << 24926;
    for (int i = 0; i < 24926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24927(int& val) {
    func_in_24927(val);
    const int x = 24927;
    std::cout << 24927;
    for (int i = 0; i < 24927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24927(int& val) {
    const int x = 24927;
    std::cout << 24927;
    for (int i = 0; i < 24927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24928(int& val) {
    func_in_24928(val);
    const int x = 24928;
    std::cout << 24928;
    for (int i = 0; i < 24928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24928(int& val) {
    const int x = 24928;
    std::cout << 24928;
    for (int i = 0; i < 24928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24929(int& val) {
    func_in_24929(val);
    const int x = 24929;
    std::cout << 24929;
    for (int i = 0; i < 24929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24929(int& val) {
    const int x = 24929;
    std::cout << 24929;
    for (int i = 0; i < 24929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24930(int& val) {
    func_in_24930(val);
    const int x = 24930;
    std::cout << 24930;
    for (int i = 0; i < 24930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24930(int& val) {
    const int x = 24930;
    std::cout << 24930;
    for (int i = 0; i < 24930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24931(int& val) {
    func_in_24931(val);
    const int x = 24931;
    std::cout << 24931;
    for (int i = 0; i < 24931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24931(int& val) {
    const int x = 24931;
    std::cout << 24931;
    for (int i = 0; i < 24931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24932(int& val) {
    func_in_24932(val);
    const int x = 24932;
    std::cout << 24932;
    for (int i = 0; i < 24932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24932(int& val) {
    const int x = 24932;
    std::cout << 24932;
    for (int i = 0; i < 24932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24933(int& val) {
    func_in_24933(val);
    const int x = 24933;
    std::cout << 24933;
    for (int i = 0; i < 24933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24933(int& val) {
    const int x = 24933;
    std::cout << 24933;
    for (int i = 0; i < 24933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24934(int& val) {
    func_in_24934(val);
    const int x = 24934;
    std::cout << 24934;
    for (int i = 0; i < 24934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24934(int& val) {
    const int x = 24934;
    std::cout << 24934;
    for (int i = 0; i < 24934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24935(int& val) {
    func_in_24935(val);
    const int x = 24935;
    std::cout << 24935;
    for (int i = 0; i < 24935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24935(int& val) {
    const int x = 24935;
    std::cout << 24935;
    for (int i = 0; i < 24935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24936(int& val) {
    func_in_24936(val);
    const int x = 24936;
    std::cout << 24936;
    for (int i = 0; i < 24936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24936(int& val) {
    const int x = 24936;
    std::cout << 24936;
    for (int i = 0; i < 24936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24937(int& val) {
    func_in_24937(val);
    const int x = 24937;
    std::cout << 24937;
    for (int i = 0; i < 24937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24937(int& val) {
    const int x = 24937;
    std::cout << 24937;
    for (int i = 0; i < 24937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24938(int& val) {
    func_in_24938(val);
    const int x = 24938;
    std::cout << 24938;
    for (int i = 0; i < 24938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24938(int& val) {
    const int x = 24938;
    std::cout << 24938;
    for (int i = 0; i < 24938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24939(int& val) {
    func_in_24939(val);
    const int x = 24939;
    std::cout << 24939;
    for (int i = 0; i < 24939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24939(int& val) {
    const int x = 24939;
    std::cout << 24939;
    for (int i = 0; i < 24939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24940(int& val) {
    func_in_24940(val);
    const int x = 24940;
    std::cout << 24940;
    for (int i = 0; i < 24940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24940(int& val) {
    const int x = 24940;
    std::cout << 24940;
    for (int i = 0; i < 24940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24941(int& val) {
    func_in_24941(val);
    const int x = 24941;
    std::cout << 24941;
    for (int i = 0; i < 24941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24941(int& val) {
    const int x = 24941;
    std::cout << 24941;
    for (int i = 0; i < 24941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24942(int& val) {
    func_in_24942(val);
    const int x = 24942;
    std::cout << 24942;
    for (int i = 0; i < 24942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24942(int& val) {
    const int x = 24942;
    std::cout << 24942;
    for (int i = 0; i < 24942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24943(int& val) {
    func_in_24943(val);
    const int x = 24943;
    std::cout << 24943;
    for (int i = 0; i < 24943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24943(int& val) {
    const int x = 24943;
    std::cout << 24943;
    for (int i = 0; i < 24943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24944(int& val) {
    func_in_24944(val);
    const int x = 24944;
    std::cout << 24944;
    for (int i = 0; i < 24944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24944(int& val) {
    const int x = 24944;
    std::cout << 24944;
    for (int i = 0; i < 24944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24945(int& val) {
    func_in_24945(val);
    const int x = 24945;
    std::cout << 24945;
    for (int i = 0; i < 24945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24945(int& val) {
    const int x = 24945;
    std::cout << 24945;
    for (int i = 0; i < 24945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24946(int& val) {
    func_in_24946(val);
    const int x = 24946;
    std::cout << 24946;
    for (int i = 0; i < 24946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24946(int& val) {
    const int x = 24946;
    std::cout << 24946;
    for (int i = 0; i < 24946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24947(int& val) {
    func_in_24947(val);
    const int x = 24947;
    std::cout << 24947;
    for (int i = 0; i < 24947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24947(int& val) {
    const int x = 24947;
    std::cout << 24947;
    for (int i = 0; i < 24947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24948(int& val) {
    func_in_24948(val);
    const int x = 24948;
    std::cout << 24948;
    for (int i = 0; i < 24948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24948(int& val) {
    const int x = 24948;
    std::cout << 24948;
    for (int i = 0; i < 24948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24949(int& val) {
    func_in_24949(val);
    const int x = 24949;
    std::cout << 24949;
    for (int i = 0; i < 24949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24949(int& val) {
    const int x = 24949;
    std::cout << 24949;
    for (int i = 0; i < 24949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24950(int& val) {
    func_in_24950(val);
    const int x = 24950;
    std::cout << 24950;
    for (int i = 0; i < 24950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24950(int& val) {
    const int x = 24950;
    std::cout << 24950;
    for (int i = 0; i < 24950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24951(int& val) {
    func_in_24951(val);
    const int x = 24951;
    std::cout << 24951;
    for (int i = 0; i < 24951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24951(int& val) {
    const int x = 24951;
    std::cout << 24951;
    for (int i = 0; i < 24951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24952(int& val) {
    func_in_24952(val);
    const int x = 24952;
    std::cout << 24952;
    for (int i = 0; i < 24952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24952(int& val) {
    const int x = 24952;
    std::cout << 24952;
    for (int i = 0; i < 24952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24953(int& val) {
    func_in_24953(val);
    const int x = 24953;
    std::cout << 24953;
    for (int i = 0; i < 24953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24953(int& val) {
    const int x = 24953;
    std::cout << 24953;
    for (int i = 0; i < 24953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24954(int& val) {
    func_in_24954(val);
    const int x = 24954;
    std::cout << 24954;
    for (int i = 0; i < 24954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24954(int& val) {
    const int x = 24954;
    std::cout << 24954;
    for (int i = 0; i < 24954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24955(int& val) {
    func_in_24955(val);
    const int x = 24955;
    std::cout << 24955;
    for (int i = 0; i < 24955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24955(int& val) {
    const int x = 24955;
    std::cout << 24955;
    for (int i = 0; i < 24955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24956(int& val) {
    func_in_24956(val);
    const int x = 24956;
    std::cout << 24956;
    for (int i = 0; i < 24956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24956(int& val) {
    const int x = 24956;
    std::cout << 24956;
    for (int i = 0; i < 24956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24957(int& val) {
    func_in_24957(val);
    const int x = 24957;
    std::cout << 24957;
    for (int i = 0; i < 24957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24957(int& val) {
    const int x = 24957;
    std::cout << 24957;
    for (int i = 0; i < 24957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24958(int& val) {
    func_in_24958(val);
    const int x = 24958;
    std::cout << 24958;
    for (int i = 0; i < 24958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24958(int& val) {
    const int x = 24958;
    std::cout << 24958;
    for (int i = 0; i < 24958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24959(int& val) {
    func_in_24959(val);
    const int x = 24959;
    std::cout << 24959;
    for (int i = 0; i < 24959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24959(int& val) {
    const int x = 24959;
    std::cout << 24959;
    for (int i = 0; i < 24959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24960(int& val) {
    func_in_24960(val);
    const int x = 24960;
    std::cout << 24960;
    for (int i = 0; i < 24960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24960(int& val) {
    const int x = 24960;
    std::cout << 24960;
    for (int i = 0; i < 24960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24961(int& val) {
    func_in_24961(val);
    const int x = 24961;
    std::cout << 24961;
    for (int i = 0; i < 24961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24961(int& val) {
    const int x = 24961;
    std::cout << 24961;
    for (int i = 0; i < 24961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24962(int& val) {
    func_in_24962(val);
    const int x = 24962;
    std::cout << 24962;
    for (int i = 0; i < 24962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24962(int& val) {
    const int x = 24962;
    std::cout << 24962;
    for (int i = 0; i < 24962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24963(int& val) {
    func_in_24963(val);
    const int x = 24963;
    std::cout << 24963;
    for (int i = 0; i < 24963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24963(int& val) {
    const int x = 24963;
    std::cout << 24963;
    for (int i = 0; i < 24963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24964(int& val) {
    func_in_24964(val);
    const int x = 24964;
    std::cout << 24964;
    for (int i = 0; i < 24964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24964(int& val) {
    const int x = 24964;
    std::cout << 24964;
    for (int i = 0; i < 24964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24965(int& val) {
    func_in_24965(val);
    const int x = 24965;
    std::cout << 24965;
    for (int i = 0; i < 24965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24965(int& val) {
    const int x = 24965;
    std::cout << 24965;
    for (int i = 0; i < 24965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24966(int& val) {
    func_in_24966(val);
    const int x = 24966;
    std::cout << 24966;
    for (int i = 0; i < 24966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24966(int& val) {
    const int x = 24966;
    std::cout << 24966;
    for (int i = 0; i < 24966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24967(int& val) {
    func_in_24967(val);
    const int x = 24967;
    std::cout << 24967;
    for (int i = 0; i < 24967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24967(int& val) {
    const int x = 24967;
    std::cout << 24967;
    for (int i = 0; i < 24967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24968(int& val) {
    func_in_24968(val);
    const int x = 24968;
    std::cout << 24968;
    for (int i = 0; i < 24968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24968(int& val) {
    const int x = 24968;
    std::cout << 24968;
    for (int i = 0; i < 24968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24969(int& val) {
    func_in_24969(val);
    const int x = 24969;
    std::cout << 24969;
    for (int i = 0; i < 24969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24969(int& val) {
    const int x = 24969;
    std::cout << 24969;
    for (int i = 0; i < 24969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24970(int& val) {
    func_in_24970(val);
    const int x = 24970;
    std::cout << 24970;
    for (int i = 0; i < 24970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24970(int& val) {
    const int x = 24970;
    std::cout << 24970;
    for (int i = 0; i < 24970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24971(int& val) {
    func_in_24971(val);
    const int x = 24971;
    std::cout << 24971;
    for (int i = 0; i < 24971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24971(int& val) {
    const int x = 24971;
    std::cout << 24971;
    for (int i = 0; i < 24971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24972(int& val) {
    func_in_24972(val);
    const int x = 24972;
    std::cout << 24972;
    for (int i = 0; i < 24972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24972(int& val) {
    const int x = 24972;
    std::cout << 24972;
    for (int i = 0; i < 24972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24973(int& val) {
    func_in_24973(val);
    const int x = 24973;
    std::cout << 24973;
    for (int i = 0; i < 24973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24973(int& val) {
    const int x = 24973;
    std::cout << 24973;
    for (int i = 0; i < 24973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24974(int& val) {
    func_in_24974(val);
    const int x = 24974;
    std::cout << 24974;
    for (int i = 0; i < 24974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24974(int& val) {
    const int x = 24974;
    std::cout << 24974;
    for (int i = 0; i < 24974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24975(int& val) {
    func_in_24975(val);
    const int x = 24975;
    std::cout << 24975;
    for (int i = 0; i < 24975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24975(int& val) {
    const int x = 24975;
    std::cout << 24975;
    for (int i = 0; i < 24975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24976(int& val) {
    func_in_24976(val);
    const int x = 24976;
    std::cout << 24976;
    for (int i = 0; i < 24976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24976(int& val) {
    const int x = 24976;
    std::cout << 24976;
    for (int i = 0; i < 24976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24977(int& val) {
    func_in_24977(val);
    const int x = 24977;
    std::cout << 24977;
    for (int i = 0; i < 24977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24977(int& val) {
    const int x = 24977;
    std::cout << 24977;
    for (int i = 0; i < 24977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24978(int& val) {
    func_in_24978(val);
    const int x = 24978;
    std::cout << 24978;
    for (int i = 0; i < 24978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24978(int& val) {
    const int x = 24978;
    std::cout << 24978;
    for (int i = 0; i < 24978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24979(int& val) {
    func_in_24979(val);
    const int x = 24979;
    std::cout << 24979;
    for (int i = 0; i < 24979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24979(int& val) {
    const int x = 24979;
    std::cout << 24979;
    for (int i = 0; i < 24979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24980(int& val) {
    func_in_24980(val);
    const int x = 24980;
    std::cout << 24980;
    for (int i = 0; i < 24980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24980(int& val) {
    const int x = 24980;
    std::cout << 24980;
    for (int i = 0; i < 24980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24981(int& val) {
    func_in_24981(val);
    const int x = 24981;
    std::cout << 24981;
    for (int i = 0; i < 24981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24981(int& val) {
    const int x = 24981;
    std::cout << 24981;
    for (int i = 0; i < 24981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24982(int& val) {
    func_in_24982(val);
    const int x = 24982;
    std::cout << 24982;
    for (int i = 0; i < 24982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24982(int& val) {
    const int x = 24982;
    std::cout << 24982;
    for (int i = 0; i < 24982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24983(int& val) {
    func_in_24983(val);
    const int x = 24983;
    std::cout << 24983;
    for (int i = 0; i < 24983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24983(int& val) {
    const int x = 24983;
    std::cout << 24983;
    for (int i = 0; i < 24983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24984(int& val) {
    func_in_24984(val);
    const int x = 24984;
    std::cout << 24984;
    for (int i = 0; i < 24984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24984(int& val) {
    const int x = 24984;
    std::cout << 24984;
    for (int i = 0; i < 24984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24985(int& val) {
    func_in_24985(val);
    const int x = 24985;
    std::cout << 24985;
    for (int i = 0; i < 24985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24985(int& val) {
    const int x = 24985;
    std::cout << 24985;
    for (int i = 0; i < 24985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24986(int& val) {
    func_in_24986(val);
    const int x = 24986;
    std::cout << 24986;
    for (int i = 0; i < 24986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24986(int& val) {
    const int x = 24986;
    std::cout << 24986;
    for (int i = 0; i < 24986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24987(int& val) {
    func_in_24987(val);
    const int x = 24987;
    std::cout << 24987;
    for (int i = 0; i < 24987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24987(int& val) {
    const int x = 24987;
    std::cout << 24987;
    for (int i = 0; i < 24987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24988(int& val) {
    func_in_24988(val);
    const int x = 24988;
    std::cout << 24988;
    for (int i = 0; i < 24988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24988(int& val) {
    const int x = 24988;
    std::cout << 24988;
    for (int i = 0; i < 24988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24989(int& val) {
    func_in_24989(val);
    const int x = 24989;
    std::cout << 24989;
    for (int i = 0; i < 24989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24989(int& val) {
    const int x = 24989;
    std::cout << 24989;
    for (int i = 0; i < 24989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24990(int& val) {
    func_in_24990(val);
    const int x = 24990;
    std::cout << 24990;
    for (int i = 0; i < 24990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24990(int& val) {
    const int x = 24990;
    std::cout << 24990;
    for (int i = 0; i < 24990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24991(int& val) {
    func_in_24991(val);
    const int x = 24991;
    std::cout << 24991;
    for (int i = 0; i < 24991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24991(int& val) {
    const int x = 24991;
    std::cout << 24991;
    for (int i = 0; i < 24991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24992(int& val) {
    func_in_24992(val);
    const int x = 24992;
    std::cout << 24992;
    for (int i = 0; i < 24992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24992(int& val) {
    const int x = 24992;
    std::cout << 24992;
    for (int i = 0; i < 24992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24993(int& val) {
    func_in_24993(val);
    const int x = 24993;
    std::cout << 24993;
    for (int i = 0; i < 24993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24993(int& val) {
    const int x = 24993;
    std::cout << 24993;
    for (int i = 0; i < 24993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24994(int& val) {
    func_in_24994(val);
    const int x = 24994;
    std::cout << 24994;
    for (int i = 0; i < 24994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24994(int& val) {
    const int x = 24994;
    std::cout << 24994;
    for (int i = 0; i < 24994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24995(int& val) {
    func_in_24995(val);
    const int x = 24995;
    std::cout << 24995;
    for (int i = 0; i < 24995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24995(int& val) {
    const int x = 24995;
    std::cout << 24995;
    for (int i = 0; i < 24995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24996(int& val) {
    func_in_24996(val);
    const int x = 24996;
    std::cout << 24996;
    for (int i = 0; i < 24996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24996(int& val) {
    const int x = 24996;
    std::cout << 24996;
    for (int i = 0; i < 24996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24997(int& val) {
    func_in_24997(val);
    const int x = 24997;
    std::cout << 24997;
    for (int i = 0; i < 24997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24997(int& val) {
    const int x = 24997;
    std::cout << 24997;
    for (int i = 0; i < 24997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24998(int& val) {
    func_in_24998(val);
    const int x = 24998;
    std::cout << 24998;
    for (int i = 0; i < 24998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24998(int& val) {
    const int x = 24998;
    std::cout << 24998;
    for (int i = 0; i < 24998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24999(int& val) {
    func_in_24999(val);
    const int x = 24999;
    std::cout << 24999;
    for (int i = 0; i < 24999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24999(int& val) {
    const int x = 24999;
    std::cout << 24999;
    for (int i = 0; i < 24999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25000(int& val) {
    func_in_25000(val);
    const int x = 25000;
    std::cout << 25000;
    for (int i = 0; i < 25000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25000(int& val) {
    const int x = 25000;
    std::cout << 25000;
    for (int i = 0; i < 25000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25001(int& val) {
    func_in_25001(val);
    const int x = 25001;
    std::cout << 25001;
    for (int i = 0; i < 25001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25001(int& val) {
    const int x = 25001;
    std::cout << 25001;
    for (int i = 0; i < 25001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25002(int& val) {
    func_in_25002(val);
    const int x = 25002;
    std::cout << 25002;
    for (int i = 0; i < 25002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25002(int& val) {
    const int x = 25002;
    std::cout << 25002;
    for (int i = 0; i < 25002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25003(int& val) {
    func_in_25003(val);
    const int x = 25003;
    std::cout << 25003;
    for (int i = 0; i < 25003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25003(int& val) {
    const int x = 25003;
    std::cout << 25003;
    for (int i = 0; i < 25003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25004(int& val) {
    func_in_25004(val);
    const int x = 25004;
    std::cout << 25004;
    for (int i = 0; i < 25004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25004(int& val) {
    const int x = 25004;
    std::cout << 25004;
    for (int i = 0; i < 25004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25005(int& val) {
    func_in_25005(val);
    const int x = 25005;
    std::cout << 25005;
    for (int i = 0; i < 25005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25005(int& val) {
    const int x = 25005;
    std::cout << 25005;
    for (int i = 0; i < 25005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25006(int& val) {
    func_in_25006(val);
    const int x = 25006;
    std::cout << 25006;
    for (int i = 0; i < 25006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25006(int& val) {
    const int x = 25006;
    std::cout << 25006;
    for (int i = 0; i < 25006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25007(int& val) {
    func_in_25007(val);
    const int x = 25007;
    std::cout << 25007;
    for (int i = 0; i < 25007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25007(int& val) {
    const int x = 25007;
    std::cout << 25007;
    for (int i = 0; i < 25007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25008(int& val) {
    func_in_25008(val);
    const int x = 25008;
    std::cout << 25008;
    for (int i = 0; i < 25008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25008(int& val) {
    const int x = 25008;
    std::cout << 25008;
    for (int i = 0; i < 25008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25009(int& val) {
    func_in_25009(val);
    const int x = 25009;
    std::cout << 25009;
    for (int i = 0; i < 25009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25009(int& val) {
    const int x = 25009;
    std::cout << 25009;
    for (int i = 0; i < 25009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25010(int& val) {
    func_in_25010(val);
    const int x = 25010;
    std::cout << 25010;
    for (int i = 0; i < 25010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25010(int& val) {
    const int x = 25010;
    std::cout << 25010;
    for (int i = 0; i < 25010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25011(int& val) {
    func_in_25011(val);
    const int x = 25011;
    std::cout << 25011;
    for (int i = 0; i < 25011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25011(int& val) {
    const int x = 25011;
    std::cout << 25011;
    for (int i = 0; i < 25011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25012(int& val) {
    func_in_25012(val);
    const int x = 25012;
    std::cout << 25012;
    for (int i = 0; i < 25012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25012(int& val) {
    const int x = 25012;
    std::cout << 25012;
    for (int i = 0; i < 25012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25013(int& val) {
    func_in_25013(val);
    const int x = 25013;
    std::cout << 25013;
    for (int i = 0; i < 25013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25013(int& val) {
    const int x = 25013;
    std::cout << 25013;
    for (int i = 0; i < 25013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25014(int& val) {
    func_in_25014(val);
    const int x = 25014;
    std::cout << 25014;
    for (int i = 0; i < 25014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25014(int& val) {
    const int x = 25014;
    std::cout << 25014;
    for (int i = 0; i < 25014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25015(int& val) {
    func_in_25015(val);
    const int x = 25015;
    std::cout << 25015;
    for (int i = 0; i < 25015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25015(int& val) {
    const int x = 25015;
    std::cout << 25015;
    for (int i = 0; i < 25015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25016(int& val) {
    func_in_25016(val);
    const int x = 25016;
    std::cout << 25016;
    for (int i = 0; i < 25016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25016(int& val) {
    const int x = 25016;
    std::cout << 25016;
    for (int i = 0; i < 25016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25017(int& val) {
    func_in_25017(val);
    const int x = 25017;
    std::cout << 25017;
    for (int i = 0; i < 25017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25017(int& val) {
    const int x = 25017;
    std::cout << 25017;
    for (int i = 0; i < 25017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25018(int& val) {
    func_in_25018(val);
    const int x = 25018;
    std::cout << 25018;
    for (int i = 0; i < 25018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25018(int& val) {
    const int x = 25018;
    std::cout << 25018;
    for (int i = 0; i < 25018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25019(int& val) {
    func_in_25019(val);
    const int x = 25019;
    std::cout << 25019;
    for (int i = 0; i < 25019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25019(int& val) {
    const int x = 25019;
    std::cout << 25019;
    for (int i = 0; i < 25019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25020(int& val) {
    func_in_25020(val);
    const int x = 25020;
    std::cout << 25020;
    for (int i = 0; i < 25020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25020(int& val) {
    const int x = 25020;
    std::cout << 25020;
    for (int i = 0; i < 25020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25021(int& val) {
    func_in_25021(val);
    const int x = 25021;
    std::cout << 25021;
    for (int i = 0; i < 25021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25021(int& val) {
    const int x = 25021;
    std::cout << 25021;
    for (int i = 0; i < 25021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25022(int& val) {
    func_in_25022(val);
    const int x = 25022;
    std::cout << 25022;
    for (int i = 0; i < 25022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25022(int& val) {
    const int x = 25022;
    std::cout << 25022;
    for (int i = 0; i < 25022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25023(int& val) {
    func_in_25023(val);
    const int x = 25023;
    std::cout << 25023;
    for (int i = 0; i < 25023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25023(int& val) {
    const int x = 25023;
    std::cout << 25023;
    for (int i = 0; i < 25023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25024(int& val) {
    func_in_25024(val);
    const int x = 25024;
    std::cout << 25024;
    for (int i = 0; i < 25024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25024(int& val) {
    const int x = 25024;
    std::cout << 25024;
    for (int i = 0; i < 25024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25025(int& val) {
    func_in_25025(val);
    const int x = 25025;
    std::cout << 25025;
    for (int i = 0; i < 25025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25025(int& val) {
    const int x = 25025;
    std::cout << 25025;
    for (int i = 0; i < 25025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25026(int& val) {
    func_in_25026(val);
    const int x = 25026;
    std::cout << 25026;
    for (int i = 0; i < 25026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25026(int& val) {
    const int x = 25026;
    std::cout << 25026;
    for (int i = 0; i < 25026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25027(int& val) {
    func_in_25027(val);
    const int x = 25027;
    std::cout << 25027;
    for (int i = 0; i < 25027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25027(int& val) {
    const int x = 25027;
    std::cout << 25027;
    for (int i = 0; i < 25027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25028(int& val) {
    func_in_25028(val);
    const int x = 25028;
    std::cout << 25028;
    for (int i = 0; i < 25028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25028(int& val) {
    const int x = 25028;
    std::cout << 25028;
    for (int i = 0; i < 25028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25029(int& val) {
    func_in_25029(val);
    const int x = 25029;
    std::cout << 25029;
    for (int i = 0; i < 25029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25029(int& val) {
    const int x = 25029;
    std::cout << 25029;
    for (int i = 0; i < 25029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25030(int& val) {
    func_in_25030(val);
    const int x = 25030;
    std::cout << 25030;
    for (int i = 0; i < 25030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25030(int& val) {
    const int x = 25030;
    std::cout << 25030;
    for (int i = 0; i < 25030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25031(int& val) {
    func_in_25031(val);
    const int x = 25031;
    std::cout << 25031;
    for (int i = 0; i < 25031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25031(int& val) {
    const int x = 25031;
    std::cout << 25031;
    for (int i = 0; i < 25031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25032(int& val) {
    func_in_25032(val);
    const int x = 25032;
    std::cout << 25032;
    for (int i = 0; i < 25032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25032(int& val) {
    const int x = 25032;
    std::cout << 25032;
    for (int i = 0; i < 25032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25033(int& val) {
    func_in_25033(val);
    const int x = 25033;
    std::cout << 25033;
    for (int i = 0; i < 25033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25033(int& val) {
    const int x = 25033;
    std::cout << 25033;
    for (int i = 0; i < 25033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25034(int& val) {
    func_in_25034(val);
    const int x = 25034;
    std::cout << 25034;
    for (int i = 0; i < 25034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25034(int& val) {
    const int x = 25034;
    std::cout << 25034;
    for (int i = 0; i < 25034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25035(int& val) {
    func_in_25035(val);
    const int x = 25035;
    std::cout << 25035;
    for (int i = 0; i < 25035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25035(int& val) {
    const int x = 25035;
    std::cout << 25035;
    for (int i = 0; i < 25035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25036(int& val) {
    func_in_25036(val);
    const int x = 25036;
    std::cout << 25036;
    for (int i = 0; i < 25036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25036(int& val) {
    const int x = 25036;
    std::cout << 25036;
    for (int i = 0; i < 25036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25037(int& val) {
    func_in_25037(val);
    const int x = 25037;
    std::cout << 25037;
    for (int i = 0; i < 25037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25037(int& val) {
    const int x = 25037;
    std::cout << 25037;
    for (int i = 0; i < 25037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25038(int& val) {
    func_in_25038(val);
    const int x = 25038;
    std::cout << 25038;
    for (int i = 0; i < 25038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25038(int& val) {
    const int x = 25038;
    std::cout << 25038;
    for (int i = 0; i < 25038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25039(int& val) {
    func_in_25039(val);
    const int x = 25039;
    std::cout << 25039;
    for (int i = 0; i < 25039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25039(int& val) {
    const int x = 25039;
    std::cout << 25039;
    for (int i = 0; i < 25039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25040(int& val) {
    func_in_25040(val);
    const int x = 25040;
    std::cout << 25040;
    for (int i = 0; i < 25040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25040(int& val) {
    const int x = 25040;
    std::cout << 25040;
    for (int i = 0; i < 25040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25041(int& val) {
    func_in_25041(val);
    const int x = 25041;
    std::cout << 25041;
    for (int i = 0; i < 25041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25041(int& val) {
    const int x = 25041;
    std::cout << 25041;
    for (int i = 0; i < 25041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25042(int& val) {
    func_in_25042(val);
    const int x = 25042;
    std::cout << 25042;
    for (int i = 0; i < 25042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25042(int& val) {
    const int x = 25042;
    std::cout << 25042;
    for (int i = 0; i < 25042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25043(int& val) {
    func_in_25043(val);
    const int x = 25043;
    std::cout << 25043;
    for (int i = 0; i < 25043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25043(int& val) {
    const int x = 25043;
    std::cout << 25043;
    for (int i = 0; i < 25043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25044(int& val) {
    func_in_25044(val);
    const int x = 25044;
    std::cout << 25044;
    for (int i = 0; i < 25044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25044(int& val) {
    const int x = 25044;
    std::cout << 25044;
    for (int i = 0; i < 25044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25045(int& val) {
    func_in_25045(val);
    const int x = 25045;
    std::cout << 25045;
    for (int i = 0; i < 25045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25045(int& val) {
    const int x = 25045;
    std::cout << 25045;
    for (int i = 0; i < 25045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25046(int& val) {
    func_in_25046(val);
    const int x = 25046;
    std::cout << 25046;
    for (int i = 0; i < 25046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25046(int& val) {
    const int x = 25046;
    std::cout << 25046;
    for (int i = 0; i < 25046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25047(int& val) {
    func_in_25047(val);
    const int x = 25047;
    std::cout << 25047;
    for (int i = 0; i < 25047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25047(int& val) {
    const int x = 25047;
    std::cout << 25047;
    for (int i = 0; i < 25047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25048(int& val) {
    func_in_25048(val);
    const int x = 25048;
    std::cout << 25048;
    for (int i = 0; i < 25048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25048(int& val) {
    const int x = 25048;
    std::cout << 25048;
    for (int i = 0; i < 25048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25049(int& val) {
    func_in_25049(val);
    const int x = 25049;
    std::cout << 25049;
    for (int i = 0; i < 25049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25049(int& val) {
    const int x = 25049;
    std::cout << 25049;
    for (int i = 0; i < 25049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25050(int& val) {
    func_in_25050(val);
    const int x = 25050;
    std::cout << 25050;
    for (int i = 0; i < 25050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25050(int& val) {
    const int x = 25050;
    std::cout << 25050;
    for (int i = 0; i < 25050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25051(int& val) {
    func_in_25051(val);
    const int x = 25051;
    std::cout << 25051;
    for (int i = 0; i < 25051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25051(int& val) {
    const int x = 25051;
    std::cout << 25051;
    for (int i = 0; i < 25051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25052(int& val) {
    func_in_25052(val);
    const int x = 25052;
    std::cout << 25052;
    for (int i = 0; i < 25052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25052(int& val) {
    const int x = 25052;
    std::cout << 25052;
    for (int i = 0; i < 25052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25053(int& val) {
    func_in_25053(val);
    const int x = 25053;
    std::cout << 25053;
    for (int i = 0; i < 25053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25053(int& val) {
    const int x = 25053;
    std::cout << 25053;
    for (int i = 0; i < 25053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25054(int& val) {
    func_in_25054(val);
    const int x = 25054;
    std::cout << 25054;
    for (int i = 0; i < 25054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25054(int& val) {
    const int x = 25054;
    std::cout << 25054;
    for (int i = 0; i < 25054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25055(int& val) {
    func_in_25055(val);
    const int x = 25055;
    std::cout << 25055;
    for (int i = 0; i < 25055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25055(int& val) {
    const int x = 25055;
    std::cout << 25055;
    for (int i = 0; i < 25055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25056(int& val) {
    func_in_25056(val);
    const int x = 25056;
    std::cout << 25056;
    for (int i = 0; i < 25056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25056(int& val) {
    const int x = 25056;
    std::cout << 25056;
    for (int i = 0; i < 25056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25057(int& val) {
    func_in_25057(val);
    const int x = 25057;
    std::cout << 25057;
    for (int i = 0; i < 25057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25057(int& val) {
    const int x = 25057;
    std::cout << 25057;
    for (int i = 0; i < 25057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25058(int& val) {
    func_in_25058(val);
    const int x = 25058;
    std::cout << 25058;
    for (int i = 0; i < 25058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25058(int& val) {
    const int x = 25058;
    std::cout << 25058;
    for (int i = 0; i < 25058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25059(int& val) {
    func_in_25059(val);
    const int x = 25059;
    std::cout << 25059;
    for (int i = 0; i < 25059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25059(int& val) {
    const int x = 25059;
    std::cout << 25059;
    for (int i = 0; i < 25059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25060(int& val) {
    func_in_25060(val);
    const int x = 25060;
    std::cout << 25060;
    for (int i = 0; i < 25060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25060(int& val) {
    const int x = 25060;
    std::cout << 25060;
    for (int i = 0; i < 25060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25061(int& val) {
    func_in_25061(val);
    const int x = 25061;
    std::cout << 25061;
    for (int i = 0; i < 25061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25061(int& val) {
    const int x = 25061;
    std::cout << 25061;
    for (int i = 0; i < 25061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25062(int& val) {
    func_in_25062(val);
    const int x = 25062;
    std::cout << 25062;
    for (int i = 0; i < 25062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25062(int& val) {
    const int x = 25062;
    std::cout << 25062;
    for (int i = 0; i < 25062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25063(int& val) {
    func_in_25063(val);
    const int x = 25063;
    std::cout << 25063;
    for (int i = 0; i < 25063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25063(int& val) {
    const int x = 25063;
    std::cout << 25063;
    for (int i = 0; i < 25063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25064(int& val) {
    func_in_25064(val);
    const int x = 25064;
    std::cout << 25064;
    for (int i = 0; i < 25064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25064(int& val) {
    const int x = 25064;
    std::cout << 25064;
    for (int i = 0; i < 25064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25065(int& val) {
    func_in_25065(val);
    const int x = 25065;
    std::cout << 25065;
    for (int i = 0; i < 25065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25065(int& val) {
    const int x = 25065;
    std::cout << 25065;
    for (int i = 0; i < 25065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25066(int& val) {
    func_in_25066(val);
    const int x = 25066;
    std::cout << 25066;
    for (int i = 0; i < 25066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25066(int& val) {
    const int x = 25066;
    std::cout << 25066;
    for (int i = 0; i < 25066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25067(int& val) {
    func_in_25067(val);
    const int x = 25067;
    std::cout << 25067;
    for (int i = 0; i < 25067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25067(int& val) {
    const int x = 25067;
    std::cout << 25067;
    for (int i = 0; i < 25067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25068(int& val) {
    func_in_25068(val);
    const int x = 25068;
    std::cout << 25068;
    for (int i = 0; i < 25068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25068(int& val) {
    const int x = 25068;
    std::cout << 25068;
    for (int i = 0; i < 25068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25069(int& val) {
    func_in_25069(val);
    const int x = 25069;
    std::cout << 25069;
    for (int i = 0; i < 25069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25069(int& val) {
    const int x = 25069;
    std::cout << 25069;
    for (int i = 0; i < 25069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25070(int& val) {
    func_in_25070(val);
    const int x = 25070;
    std::cout << 25070;
    for (int i = 0; i < 25070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25070(int& val) {
    const int x = 25070;
    std::cout << 25070;
    for (int i = 0; i < 25070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25071(int& val) {
    func_in_25071(val);
    const int x = 25071;
    std::cout << 25071;
    for (int i = 0; i < 25071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25071(int& val) {
    const int x = 25071;
    std::cout << 25071;
    for (int i = 0; i < 25071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25072(int& val) {
    func_in_25072(val);
    const int x = 25072;
    std::cout << 25072;
    for (int i = 0; i < 25072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25072(int& val) {
    const int x = 25072;
    std::cout << 25072;
    for (int i = 0; i < 25072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25073(int& val) {
    func_in_25073(val);
    const int x = 25073;
    std::cout << 25073;
    for (int i = 0; i < 25073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25073(int& val) {
    const int x = 25073;
    std::cout << 25073;
    for (int i = 0; i < 25073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25074(int& val) {
    func_in_25074(val);
    const int x = 25074;
    std::cout << 25074;
    for (int i = 0; i < 25074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25074(int& val) {
    const int x = 25074;
    std::cout << 25074;
    for (int i = 0; i < 25074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25075(int& val) {
    func_in_25075(val);
    const int x = 25075;
    std::cout << 25075;
    for (int i = 0; i < 25075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25075(int& val) {
    const int x = 25075;
    std::cout << 25075;
    for (int i = 0; i < 25075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25076(int& val) {
    func_in_25076(val);
    const int x = 25076;
    std::cout << 25076;
    for (int i = 0; i < 25076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25076(int& val) {
    const int x = 25076;
    std::cout << 25076;
    for (int i = 0; i < 25076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25077(int& val) {
    func_in_25077(val);
    const int x = 25077;
    std::cout << 25077;
    for (int i = 0; i < 25077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25077(int& val) {
    const int x = 25077;
    std::cout << 25077;
    for (int i = 0; i < 25077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25078(int& val) {
    func_in_25078(val);
    const int x = 25078;
    std::cout << 25078;
    for (int i = 0; i < 25078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25078(int& val) {
    const int x = 25078;
    std::cout << 25078;
    for (int i = 0; i < 25078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25079(int& val) {
    func_in_25079(val);
    const int x = 25079;
    std::cout << 25079;
    for (int i = 0; i < 25079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25079(int& val) {
    const int x = 25079;
    std::cout << 25079;
    for (int i = 0; i < 25079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25080(int& val) {
    func_in_25080(val);
    const int x = 25080;
    std::cout << 25080;
    for (int i = 0; i < 25080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25080(int& val) {
    const int x = 25080;
    std::cout << 25080;
    for (int i = 0; i < 25080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25081(int& val) {
    func_in_25081(val);
    const int x = 25081;
    std::cout << 25081;
    for (int i = 0; i < 25081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25081(int& val) {
    const int x = 25081;
    std::cout << 25081;
    for (int i = 0; i < 25081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25082(int& val) {
    func_in_25082(val);
    const int x = 25082;
    std::cout << 25082;
    for (int i = 0; i < 25082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25082(int& val) {
    const int x = 25082;
    std::cout << 25082;
    for (int i = 0; i < 25082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25083(int& val) {
    func_in_25083(val);
    const int x = 25083;
    std::cout << 25083;
    for (int i = 0; i < 25083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25083(int& val) {
    const int x = 25083;
    std::cout << 25083;
    for (int i = 0; i < 25083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25084(int& val) {
    func_in_25084(val);
    const int x = 25084;
    std::cout << 25084;
    for (int i = 0; i < 25084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25084(int& val) {
    const int x = 25084;
    std::cout << 25084;
    for (int i = 0; i < 25084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25085(int& val) {
    func_in_25085(val);
    const int x = 25085;
    std::cout << 25085;
    for (int i = 0; i < 25085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25085(int& val) {
    const int x = 25085;
    std::cout << 25085;
    for (int i = 0; i < 25085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25086(int& val) {
    func_in_25086(val);
    const int x = 25086;
    std::cout << 25086;
    for (int i = 0; i < 25086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25086(int& val) {
    const int x = 25086;
    std::cout << 25086;
    for (int i = 0; i < 25086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25087(int& val) {
    func_in_25087(val);
    const int x = 25087;
    std::cout << 25087;
    for (int i = 0; i < 25087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25087(int& val) {
    const int x = 25087;
    std::cout << 25087;
    for (int i = 0; i < 25087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25088(int& val) {
    func_in_25088(val);
    const int x = 25088;
    std::cout << 25088;
    for (int i = 0; i < 25088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25088(int& val) {
    const int x = 25088;
    std::cout << 25088;
    for (int i = 0; i < 25088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25089(int& val) {
    func_in_25089(val);
    const int x = 25089;
    std::cout << 25089;
    for (int i = 0; i < 25089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25089(int& val) {
    const int x = 25089;
    std::cout << 25089;
    for (int i = 0; i < 25089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25090(int& val) {
    func_in_25090(val);
    const int x = 25090;
    std::cout << 25090;
    for (int i = 0; i < 25090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25090(int& val) {
    const int x = 25090;
    std::cout << 25090;
    for (int i = 0; i < 25090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25091(int& val) {
    func_in_25091(val);
    const int x = 25091;
    std::cout << 25091;
    for (int i = 0; i < 25091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25091(int& val) {
    const int x = 25091;
    std::cout << 25091;
    for (int i = 0; i < 25091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25092(int& val) {
    func_in_25092(val);
    const int x = 25092;
    std::cout << 25092;
    for (int i = 0; i < 25092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25092(int& val) {
    const int x = 25092;
    std::cout << 25092;
    for (int i = 0; i < 25092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25093(int& val) {
    func_in_25093(val);
    const int x = 25093;
    std::cout << 25093;
    for (int i = 0; i < 25093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25093(int& val) {
    const int x = 25093;
    std::cout << 25093;
    for (int i = 0; i < 25093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25094(int& val) {
    func_in_25094(val);
    const int x = 25094;
    std::cout << 25094;
    for (int i = 0; i < 25094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25094(int& val) {
    const int x = 25094;
    std::cout << 25094;
    for (int i = 0; i < 25094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25095(int& val) {
    func_in_25095(val);
    const int x = 25095;
    std::cout << 25095;
    for (int i = 0; i < 25095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25095(int& val) {
    const int x = 25095;
    std::cout << 25095;
    for (int i = 0; i < 25095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25096(int& val) {
    func_in_25096(val);
    const int x = 25096;
    std::cout << 25096;
    for (int i = 0; i < 25096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25096(int& val) {
    const int x = 25096;
    std::cout << 25096;
    for (int i = 0; i < 25096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25097(int& val) {
    func_in_25097(val);
    const int x = 25097;
    std::cout << 25097;
    for (int i = 0; i < 25097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25097(int& val) {
    const int x = 25097;
    std::cout << 25097;
    for (int i = 0; i < 25097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25098(int& val) {
    func_in_25098(val);
    const int x = 25098;
    std::cout << 25098;
    for (int i = 0; i < 25098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25098(int& val) {
    const int x = 25098;
    std::cout << 25098;
    for (int i = 0; i < 25098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25099(int& val) {
    func_in_25099(val);
    const int x = 25099;
    std::cout << 25099;
    for (int i = 0; i < 25099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25099(int& val) {
    const int x = 25099;
    std::cout << 25099;
    for (int i = 0; i < 25099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25100(int& val) {
    func_in_25100(val);
    const int x = 25100;
    std::cout << 25100;
    for (int i = 0; i < 25100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25100(int& val) {
    const int x = 25100;
    std::cout << 25100;
    for (int i = 0; i < 25100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25101(int& val) {
    func_in_25101(val);
    const int x = 25101;
    std::cout << 25101;
    for (int i = 0; i < 25101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25101(int& val) {
    const int x = 25101;
    std::cout << 25101;
    for (int i = 0; i < 25101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25102(int& val) {
    func_in_25102(val);
    const int x = 25102;
    std::cout << 25102;
    for (int i = 0; i < 25102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25102(int& val) {
    const int x = 25102;
    std::cout << 25102;
    for (int i = 0; i < 25102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25103(int& val) {
    func_in_25103(val);
    const int x = 25103;
    std::cout << 25103;
    for (int i = 0; i < 25103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25103(int& val) {
    const int x = 25103;
    std::cout << 25103;
    for (int i = 0; i < 25103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25104(int& val) {
    func_in_25104(val);
    const int x = 25104;
    std::cout << 25104;
    for (int i = 0; i < 25104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25104(int& val) {
    const int x = 25104;
    std::cout << 25104;
    for (int i = 0; i < 25104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25105(int& val) {
    func_in_25105(val);
    const int x = 25105;
    std::cout << 25105;
    for (int i = 0; i < 25105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25105(int& val) {
    const int x = 25105;
    std::cout << 25105;
    for (int i = 0; i < 25105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25106(int& val) {
    func_in_25106(val);
    const int x = 25106;
    std::cout << 25106;
    for (int i = 0; i < 25106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25106(int& val) {
    const int x = 25106;
    std::cout << 25106;
    for (int i = 0; i < 25106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25107(int& val) {
    func_in_25107(val);
    const int x = 25107;
    std::cout << 25107;
    for (int i = 0; i < 25107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25107(int& val) {
    const int x = 25107;
    std::cout << 25107;
    for (int i = 0; i < 25107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25108(int& val) {
    func_in_25108(val);
    const int x = 25108;
    std::cout << 25108;
    for (int i = 0; i < 25108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25108(int& val) {
    const int x = 25108;
    std::cout << 25108;
    for (int i = 0; i < 25108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25109(int& val) {
    func_in_25109(val);
    const int x = 25109;
    std::cout << 25109;
    for (int i = 0; i < 25109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25109(int& val) {
    const int x = 25109;
    std::cout << 25109;
    for (int i = 0; i < 25109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25110(int& val) {
    func_in_25110(val);
    const int x = 25110;
    std::cout << 25110;
    for (int i = 0; i < 25110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25110(int& val) {
    const int x = 25110;
    std::cout << 25110;
    for (int i = 0; i < 25110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25111(int& val) {
    func_in_25111(val);
    const int x = 25111;
    std::cout << 25111;
    for (int i = 0; i < 25111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25111(int& val) {
    const int x = 25111;
    std::cout << 25111;
    for (int i = 0; i < 25111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25112(int& val) {
    func_in_25112(val);
    const int x = 25112;
    std::cout << 25112;
    for (int i = 0; i < 25112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25112(int& val) {
    const int x = 25112;
    std::cout << 25112;
    for (int i = 0; i < 25112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25113(int& val) {
    func_in_25113(val);
    const int x = 25113;
    std::cout << 25113;
    for (int i = 0; i < 25113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25113(int& val) {
    const int x = 25113;
    std::cout << 25113;
    for (int i = 0; i < 25113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25114(int& val) {
    func_in_25114(val);
    const int x = 25114;
    std::cout << 25114;
    for (int i = 0; i < 25114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25114(int& val) {
    const int x = 25114;
    std::cout << 25114;
    for (int i = 0; i < 25114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25115(int& val) {
    func_in_25115(val);
    const int x = 25115;
    std::cout << 25115;
    for (int i = 0; i < 25115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25115(int& val) {
    const int x = 25115;
    std::cout << 25115;
    for (int i = 0; i < 25115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25116(int& val) {
    func_in_25116(val);
    const int x = 25116;
    std::cout << 25116;
    for (int i = 0; i < 25116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25116(int& val) {
    const int x = 25116;
    std::cout << 25116;
    for (int i = 0; i < 25116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25117(int& val) {
    func_in_25117(val);
    const int x = 25117;
    std::cout << 25117;
    for (int i = 0; i < 25117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25117(int& val) {
    const int x = 25117;
    std::cout << 25117;
    for (int i = 0; i < 25117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25118(int& val) {
    func_in_25118(val);
    const int x = 25118;
    std::cout << 25118;
    for (int i = 0; i < 25118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25118(int& val) {
    const int x = 25118;
    std::cout << 25118;
    for (int i = 0; i < 25118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25119(int& val) {
    func_in_25119(val);
    const int x = 25119;
    std::cout << 25119;
    for (int i = 0; i < 25119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25119(int& val) {
    const int x = 25119;
    std::cout << 25119;
    for (int i = 0; i < 25119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25120(int& val) {
    func_in_25120(val);
    const int x = 25120;
    std::cout << 25120;
    for (int i = 0; i < 25120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25120(int& val) {
    const int x = 25120;
    std::cout << 25120;
    for (int i = 0; i < 25120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25121(int& val) {
    func_in_25121(val);
    const int x = 25121;
    std::cout << 25121;
    for (int i = 0; i < 25121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25121(int& val) {
    const int x = 25121;
    std::cout << 25121;
    for (int i = 0; i < 25121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25122(int& val) {
    func_in_25122(val);
    const int x = 25122;
    std::cout << 25122;
    for (int i = 0; i < 25122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25122(int& val) {
    const int x = 25122;
    std::cout << 25122;
    for (int i = 0; i < 25122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25123(int& val) {
    func_in_25123(val);
    const int x = 25123;
    std::cout << 25123;
    for (int i = 0; i < 25123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25123(int& val) {
    const int x = 25123;
    std::cout << 25123;
    for (int i = 0; i < 25123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25124(int& val) {
    func_in_25124(val);
    const int x = 25124;
    std::cout << 25124;
    for (int i = 0; i < 25124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25124(int& val) {
    const int x = 25124;
    std::cout << 25124;
    for (int i = 0; i < 25124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25125(int& val) {
    func_in_25125(val);
    const int x = 25125;
    std::cout << 25125;
    for (int i = 0; i < 25125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25125(int& val) {
    const int x = 25125;
    std::cout << 25125;
    for (int i = 0; i < 25125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25126(int& val) {
    func_in_25126(val);
    const int x = 25126;
    std::cout << 25126;
    for (int i = 0; i < 25126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25126(int& val) {
    const int x = 25126;
    std::cout << 25126;
    for (int i = 0; i < 25126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25127(int& val) {
    func_in_25127(val);
    const int x = 25127;
    std::cout << 25127;
    for (int i = 0; i < 25127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25127(int& val) {
    const int x = 25127;
    std::cout << 25127;
    for (int i = 0; i < 25127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25128(int& val) {
    func_in_25128(val);
    const int x = 25128;
    std::cout << 25128;
    for (int i = 0; i < 25128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25128(int& val) {
    const int x = 25128;
    std::cout << 25128;
    for (int i = 0; i < 25128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25129(int& val) {
    func_in_25129(val);
    const int x = 25129;
    std::cout << 25129;
    for (int i = 0; i < 25129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25129(int& val) {
    const int x = 25129;
    std::cout << 25129;
    for (int i = 0; i < 25129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25130(int& val) {
    func_in_25130(val);
    const int x = 25130;
    std::cout << 25130;
    for (int i = 0; i < 25130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25130(int& val) {
    const int x = 25130;
    std::cout << 25130;
    for (int i = 0; i < 25130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25131(int& val) {
    func_in_25131(val);
    const int x = 25131;
    std::cout << 25131;
    for (int i = 0; i < 25131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25131(int& val) {
    const int x = 25131;
    std::cout << 25131;
    for (int i = 0; i < 25131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25132(int& val) {
    func_in_25132(val);
    const int x = 25132;
    std::cout << 25132;
    for (int i = 0; i < 25132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25132(int& val) {
    const int x = 25132;
    std::cout << 25132;
    for (int i = 0; i < 25132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25133(int& val) {
    func_in_25133(val);
    const int x = 25133;
    std::cout << 25133;
    for (int i = 0; i < 25133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25133(int& val) {
    const int x = 25133;
    std::cout << 25133;
    for (int i = 0; i < 25133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25134(int& val) {
    func_in_25134(val);
    const int x = 25134;
    std::cout << 25134;
    for (int i = 0; i < 25134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25134(int& val) {
    const int x = 25134;
    std::cout << 25134;
    for (int i = 0; i < 25134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25135(int& val) {
    func_in_25135(val);
    const int x = 25135;
    std::cout << 25135;
    for (int i = 0; i < 25135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25135(int& val) {
    const int x = 25135;
    std::cout << 25135;
    for (int i = 0; i < 25135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25136(int& val) {
    func_in_25136(val);
    const int x = 25136;
    std::cout << 25136;
    for (int i = 0; i < 25136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25136(int& val) {
    const int x = 25136;
    std::cout << 25136;
    for (int i = 0; i < 25136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25137(int& val) {
    func_in_25137(val);
    const int x = 25137;
    std::cout << 25137;
    for (int i = 0; i < 25137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25137(int& val) {
    const int x = 25137;
    std::cout << 25137;
    for (int i = 0; i < 25137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25138(int& val) {
    func_in_25138(val);
    const int x = 25138;
    std::cout << 25138;
    for (int i = 0; i < 25138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25138(int& val) {
    const int x = 25138;
    std::cout << 25138;
    for (int i = 0; i < 25138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25139(int& val) {
    func_in_25139(val);
    const int x = 25139;
    std::cout << 25139;
    for (int i = 0; i < 25139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25139(int& val) {
    const int x = 25139;
    std::cout << 25139;
    for (int i = 0; i < 25139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25140(int& val) {
    func_in_25140(val);
    const int x = 25140;
    std::cout << 25140;
    for (int i = 0; i < 25140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25140(int& val) {
    const int x = 25140;
    std::cout << 25140;
    for (int i = 0; i < 25140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25141(int& val) {
    func_in_25141(val);
    const int x = 25141;
    std::cout << 25141;
    for (int i = 0; i < 25141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25141(int& val) {
    const int x = 25141;
    std::cout << 25141;
    for (int i = 0; i < 25141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25142(int& val) {
    func_in_25142(val);
    const int x = 25142;
    std::cout << 25142;
    for (int i = 0; i < 25142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25142(int& val) {
    const int x = 25142;
    std::cout << 25142;
    for (int i = 0; i < 25142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25143(int& val) {
    func_in_25143(val);
    const int x = 25143;
    std::cout << 25143;
    for (int i = 0; i < 25143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25143(int& val) {
    const int x = 25143;
    std::cout << 25143;
    for (int i = 0; i < 25143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25144(int& val) {
    func_in_25144(val);
    const int x = 25144;
    std::cout << 25144;
    for (int i = 0; i < 25144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25144(int& val) {
    const int x = 25144;
    std::cout << 25144;
    for (int i = 0; i < 25144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25145(int& val) {
    func_in_25145(val);
    const int x = 25145;
    std::cout << 25145;
    for (int i = 0; i < 25145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25145(int& val) {
    const int x = 25145;
    std::cout << 25145;
    for (int i = 0; i < 25145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25146(int& val) {
    func_in_25146(val);
    const int x = 25146;
    std::cout << 25146;
    for (int i = 0; i < 25146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25146(int& val) {
    const int x = 25146;
    std::cout << 25146;
    for (int i = 0; i < 25146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25147(int& val) {
    func_in_25147(val);
    const int x = 25147;
    std::cout << 25147;
    for (int i = 0; i < 25147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25147(int& val) {
    const int x = 25147;
    std::cout << 25147;
    for (int i = 0; i < 25147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25148(int& val) {
    func_in_25148(val);
    const int x = 25148;
    std::cout << 25148;
    for (int i = 0; i < 25148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25148(int& val) {
    const int x = 25148;
    std::cout << 25148;
    for (int i = 0; i < 25148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25149(int& val) {
    func_in_25149(val);
    const int x = 25149;
    std::cout << 25149;
    for (int i = 0; i < 25149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25149(int& val) {
    const int x = 25149;
    std::cout << 25149;
    for (int i = 0; i < 25149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25150(int& val) {
    func_in_25150(val);
    const int x = 25150;
    std::cout << 25150;
    for (int i = 0; i < 25150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25150(int& val) {
    const int x = 25150;
    std::cout << 25150;
    for (int i = 0; i < 25150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25151(int& val) {
    func_in_25151(val);
    const int x = 25151;
    std::cout << 25151;
    for (int i = 0; i < 25151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25151(int& val) {
    const int x = 25151;
    std::cout << 25151;
    for (int i = 0; i < 25151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25152(int& val) {
    func_in_25152(val);
    const int x = 25152;
    std::cout << 25152;
    for (int i = 0; i < 25152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25152(int& val) {
    const int x = 25152;
    std::cout << 25152;
    for (int i = 0; i < 25152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25153(int& val) {
    func_in_25153(val);
    const int x = 25153;
    std::cout << 25153;
    for (int i = 0; i < 25153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25153(int& val) {
    const int x = 25153;
    std::cout << 25153;
    for (int i = 0; i < 25153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25154(int& val) {
    func_in_25154(val);
    const int x = 25154;
    std::cout << 25154;
    for (int i = 0; i < 25154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25154(int& val) {
    const int x = 25154;
    std::cout << 25154;
    for (int i = 0; i < 25154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25155(int& val) {
    func_in_25155(val);
    const int x = 25155;
    std::cout << 25155;
    for (int i = 0; i < 25155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25155(int& val) {
    const int x = 25155;
    std::cout << 25155;
    for (int i = 0; i < 25155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25156(int& val) {
    func_in_25156(val);
    const int x = 25156;
    std::cout << 25156;
    for (int i = 0; i < 25156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25156(int& val) {
    const int x = 25156;
    std::cout << 25156;
    for (int i = 0; i < 25156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25157(int& val) {
    func_in_25157(val);
    const int x = 25157;
    std::cout << 25157;
    for (int i = 0; i < 25157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25157(int& val) {
    const int x = 25157;
    std::cout << 25157;
    for (int i = 0; i < 25157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25158(int& val) {
    func_in_25158(val);
    const int x = 25158;
    std::cout << 25158;
    for (int i = 0; i < 25158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25158(int& val) {
    const int x = 25158;
    std::cout << 25158;
    for (int i = 0; i < 25158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25159(int& val) {
    func_in_25159(val);
    const int x = 25159;
    std::cout << 25159;
    for (int i = 0; i < 25159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25159(int& val) {
    const int x = 25159;
    std::cout << 25159;
    for (int i = 0; i < 25159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25160(int& val) {
    func_in_25160(val);
    const int x = 25160;
    std::cout << 25160;
    for (int i = 0; i < 25160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25160(int& val) {
    const int x = 25160;
    std::cout << 25160;
    for (int i = 0; i < 25160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25161(int& val) {
    func_in_25161(val);
    const int x = 25161;
    std::cout << 25161;
    for (int i = 0; i < 25161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25161(int& val) {
    const int x = 25161;
    std::cout << 25161;
    for (int i = 0; i < 25161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25162(int& val) {
    func_in_25162(val);
    const int x = 25162;
    std::cout << 25162;
    for (int i = 0; i < 25162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25162(int& val) {
    const int x = 25162;
    std::cout << 25162;
    for (int i = 0; i < 25162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25163(int& val) {
    func_in_25163(val);
    const int x = 25163;
    std::cout << 25163;
    for (int i = 0; i < 25163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25163(int& val) {
    const int x = 25163;
    std::cout << 25163;
    for (int i = 0; i < 25163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25164(int& val) {
    func_in_25164(val);
    const int x = 25164;
    std::cout << 25164;
    for (int i = 0; i < 25164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25164(int& val) {
    const int x = 25164;
    std::cout << 25164;
    for (int i = 0; i < 25164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25165(int& val) {
    func_in_25165(val);
    const int x = 25165;
    std::cout << 25165;
    for (int i = 0; i < 25165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25165(int& val) {
    const int x = 25165;
    std::cout << 25165;
    for (int i = 0; i < 25165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25166(int& val) {
    func_in_25166(val);
    const int x = 25166;
    std::cout << 25166;
    for (int i = 0; i < 25166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25166(int& val) {
    const int x = 25166;
    std::cout << 25166;
    for (int i = 0; i < 25166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25167(int& val) {
    func_in_25167(val);
    const int x = 25167;
    std::cout << 25167;
    for (int i = 0; i < 25167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25167(int& val) {
    const int x = 25167;
    std::cout << 25167;
    for (int i = 0; i < 25167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25168(int& val) {
    func_in_25168(val);
    const int x = 25168;
    std::cout << 25168;
    for (int i = 0; i < 25168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25168(int& val) {
    const int x = 25168;
    std::cout << 25168;
    for (int i = 0; i < 25168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25169(int& val) {
    func_in_25169(val);
    const int x = 25169;
    std::cout << 25169;
    for (int i = 0; i < 25169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25169(int& val) {
    const int x = 25169;
    std::cout << 25169;
    for (int i = 0; i < 25169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25170(int& val) {
    func_in_25170(val);
    const int x = 25170;
    std::cout << 25170;
    for (int i = 0; i < 25170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25170(int& val) {
    const int x = 25170;
    std::cout << 25170;
    for (int i = 0; i < 25170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25171(int& val) {
    func_in_25171(val);
    const int x = 25171;
    std::cout << 25171;
    for (int i = 0; i < 25171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25171(int& val) {
    const int x = 25171;
    std::cout << 25171;
    for (int i = 0; i < 25171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25172(int& val) {
    func_in_25172(val);
    const int x = 25172;
    std::cout << 25172;
    for (int i = 0; i < 25172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25172(int& val) {
    const int x = 25172;
    std::cout << 25172;
    for (int i = 0; i < 25172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25173(int& val) {
    func_in_25173(val);
    const int x = 25173;
    std::cout << 25173;
    for (int i = 0; i < 25173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25173(int& val) {
    const int x = 25173;
    std::cout << 25173;
    for (int i = 0; i < 25173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25174(int& val) {
    func_in_25174(val);
    const int x = 25174;
    std::cout << 25174;
    for (int i = 0; i < 25174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25174(int& val) {
    const int x = 25174;
    std::cout << 25174;
    for (int i = 0; i < 25174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25175(int& val) {
    func_in_25175(val);
    const int x = 25175;
    std::cout << 25175;
    for (int i = 0; i < 25175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25175(int& val) {
    const int x = 25175;
    std::cout << 25175;
    for (int i = 0; i < 25175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25176(int& val) {
    func_in_25176(val);
    const int x = 25176;
    std::cout << 25176;
    for (int i = 0; i < 25176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25176(int& val) {
    const int x = 25176;
    std::cout << 25176;
    for (int i = 0; i < 25176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25177(int& val) {
    func_in_25177(val);
    const int x = 25177;
    std::cout << 25177;
    for (int i = 0; i < 25177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25177(int& val) {
    const int x = 25177;
    std::cout << 25177;
    for (int i = 0; i < 25177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25178(int& val) {
    func_in_25178(val);
    const int x = 25178;
    std::cout << 25178;
    for (int i = 0; i < 25178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25178(int& val) {
    const int x = 25178;
    std::cout << 25178;
    for (int i = 0; i < 25178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25179(int& val) {
    func_in_25179(val);
    const int x = 25179;
    std::cout << 25179;
    for (int i = 0; i < 25179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25179(int& val) {
    const int x = 25179;
    std::cout << 25179;
    for (int i = 0; i < 25179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25180(int& val) {
    func_in_25180(val);
    const int x = 25180;
    std::cout << 25180;
    for (int i = 0; i < 25180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25180(int& val) {
    const int x = 25180;
    std::cout << 25180;
    for (int i = 0; i < 25180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25181(int& val) {
    func_in_25181(val);
    const int x = 25181;
    std::cout << 25181;
    for (int i = 0; i < 25181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25181(int& val) {
    const int x = 25181;
    std::cout << 25181;
    for (int i = 0; i < 25181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25182(int& val) {
    func_in_25182(val);
    const int x = 25182;
    std::cout << 25182;
    for (int i = 0; i < 25182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25182(int& val) {
    const int x = 25182;
    std::cout << 25182;
    for (int i = 0; i < 25182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25183(int& val) {
    func_in_25183(val);
    const int x = 25183;
    std::cout << 25183;
    for (int i = 0; i < 25183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25183(int& val) {
    const int x = 25183;
    std::cout << 25183;
    for (int i = 0; i < 25183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25184(int& val) {
    func_in_25184(val);
    const int x = 25184;
    std::cout << 25184;
    for (int i = 0; i < 25184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25184(int& val) {
    const int x = 25184;
    std::cout << 25184;
    for (int i = 0; i < 25184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25185(int& val) {
    func_in_25185(val);
    const int x = 25185;
    std::cout << 25185;
    for (int i = 0; i < 25185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25185(int& val) {
    const int x = 25185;
    std::cout << 25185;
    for (int i = 0; i < 25185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25186(int& val) {
    func_in_25186(val);
    const int x = 25186;
    std::cout << 25186;
    for (int i = 0; i < 25186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25186(int& val) {
    const int x = 25186;
    std::cout << 25186;
    for (int i = 0; i < 25186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25187(int& val) {
    func_in_25187(val);
    const int x = 25187;
    std::cout << 25187;
    for (int i = 0; i < 25187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25187(int& val) {
    const int x = 25187;
    std::cout << 25187;
    for (int i = 0; i < 25187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25188(int& val) {
    func_in_25188(val);
    const int x = 25188;
    std::cout << 25188;
    for (int i = 0; i < 25188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25188(int& val) {
    const int x = 25188;
    std::cout << 25188;
    for (int i = 0; i < 25188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25189(int& val) {
    func_in_25189(val);
    const int x = 25189;
    std::cout << 25189;
    for (int i = 0; i < 25189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25189(int& val) {
    const int x = 25189;
    std::cout << 25189;
    for (int i = 0; i < 25189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25190(int& val) {
    func_in_25190(val);
    const int x = 25190;
    std::cout << 25190;
    for (int i = 0; i < 25190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25190(int& val) {
    const int x = 25190;
    std::cout << 25190;
    for (int i = 0; i < 25190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25191(int& val) {
    func_in_25191(val);
    const int x = 25191;
    std::cout << 25191;
    for (int i = 0; i < 25191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25191(int& val) {
    const int x = 25191;
    std::cout << 25191;
    for (int i = 0; i < 25191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25192(int& val) {
    func_in_25192(val);
    const int x = 25192;
    std::cout << 25192;
    for (int i = 0; i < 25192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25192(int& val) {
    const int x = 25192;
    std::cout << 25192;
    for (int i = 0; i < 25192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25193(int& val) {
    func_in_25193(val);
    const int x = 25193;
    std::cout << 25193;
    for (int i = 0; i < 25193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25193(int& val) {
    const int x = 25193;
    std::cout << 25193;
    for (int i = 0; i < 25193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25194(int& val) {
    func_in_25194(val);
    const int x = 25194;
    std::cout << 25194;
    for (int i = 0; i < 25194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25194(int& val) {
    const int x = 25194;
    std::cout << 25194;
    for (int i = 0; i < 25194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25195(int& val) {
    func_in_25195(val);
    const int x = 25195;
    std::cout << 25195;
    for (int i = 0; i < 25195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25195(int& val) {
    const int x = 25195;
    std::cout << 25195;
    for (int i = 0; i < 25195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25196(int& val) {
    func_in_25196(val);
    const int x = 25196;
    std::cout << 25196;
    for (int i = 0; i < 25196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25196(int& val) {
    const int x = 25196;
    std::cout << 25196;
    for (int i = 0; i < 25196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25197(int& val) {
    func_in_25197(val);
    const int x = 25197;
    std::cout << 25197;
    for (int i = 0; i < 25197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25197(int& val) {
    const int x = 25197;
    std::cout << 25197;
    for (int i = 0; i < 25197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25198(int& val) {
    func_in_25198(val);
    const int x = 25198;
    std::cout << 25198;
    for (int i = 0; i < 25198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25198(int& val) {
    const int x = 25198;
    std::cout << 25198;
    for (int i = 0; i < 25198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25199(int& val) {
    func_in_25199(val);
    const int x = 25199;
    std::cout << 25199;
    for (int i = 0; i < 25199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25199(int& val) {
    const int x = 25199;
    std::cout << 25199;
    for (int i = 0; i < 25199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25200(int& val) {
    func_in_25200(val);
    const int x = 25200;
    std::cout << 25200;
    for (int i = 0; i < 25200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25200(int& val) {
    const int x = 25200;
    std::cout << 25200;
    for (int i = 0; i < 25200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25201(int& val) {
    func_in_25201(val);
    const int x = 25201;
    std::cout << 25201;
    for (int i = 0; i < 25201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25201(int& val) {
    const int x = 25201;
    std::cout << 25201;
    for (int i = 0; i < 25201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25202(int& val) {
    func_in_25202(val);
    const int x = 25202;
    std::cout << 25202;
    for (int i = 0; i < 25202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25202(int& val) {
    const int x = 25202;
    std::cout << 25202;
    for (int i = 0; i < 25202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25203(int& val) {
    func_in_25203(val);
    const int x = 25203;
    std::cout << 25203;
    for (int i = 0; i < 25203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25203(int& val) {
    const int x = 25203;
    std::cout << 25203;
    for (int i = 0; i < 25203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25204(int& val) {
    func_in_25204(val);
    const int x = 25204;
    std::cout << 25204;
    for (int i = 0; i < 25204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25204(int& val) {
    const int x = 25204;
    std::cout << 25204;
    for (int i = 0; i < 25204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25205(int& val) {
    func_in_25205(val);
    const int x = 25205;
    std::cout << 25205;
    for (int i = 0; i < 25205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25205(int& val) {
    const int x = 25205;
    std::cout << 25205;
    for (int i = 0; i < 25205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25206(int& val) {
    func_in_25206(val);
    const int x = 25206;
    std::cout << 25206;
    for (int i = 0; i < 25206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25206(int& val) {
    const int x = 25206;
    std::cout << 25206;
    for (int i = 0; i < 25206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25207(int& val) {
    func_in_25207(val);
    const int x = 25207;
    std::cout << 25207;
    for (int i = 0; i < 25207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25207(int& val) {
    const int x = 25207;
    std::cout << 25207;
    for (int i = 0; i < 25207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25208(int& val) {
    func_in_25208(val);
    const int x = 25208;
    std::cout << 25208;
    for (int i = 0; i < 25208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25208(int& val) {
    const int x = 25208;
    std::cout << 25208;
    for (int i = 0; i < 25208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25209(int& val) {
    func_in_25209(val);
    const int x = 25209;
    std::cout << 25209;
    for (int i = 0; i < 25209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25209(int& val) {
    const int x = 25209;
    std::cout << 25209;
    for (int i = 0; i < 25209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25210(int& val) {
    func_in_25210(val);
    const int x = 25210;
    std::cout << 25210;
    for (int i = 0; i < 25210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25210(int& val) {
    const int x = 25210;
    std::cout << 25210;
    for (int i = 0; i < 25210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25211(int& val) {
    func_in_25211(val);
    const int x = 25211;
    std::cout << 25211;
    for (int i = 0; i < 25211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25211(int& val) {
    const int x = 25211;
    std::cout << 25211;
    for (int i = 0; i < 25211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25212(int& val) {
    func_in_25212(val);
    const int x = 25212;
    std::cout << 25212;
    for (int i = 0; i < 25212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25212(int& val) {
    const int x = 25212;
    std::cout << 25212;
    for (int i = 0; i < 25212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25213(int& val) {
    func_in_25213(val);
    const int x = 25213;
    std::cout << 25213;
    for (int i = 0; i < 25213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25213(int& val) {
    const int x = 25213;
    std::cout << 25213;
    for (int i = 0; i < 25213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25214(int& val) {
    func_in_25214(val);
    const int x = 25214;
    std::cout << 25214;
    for (int i = 0; i < 25214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25214(int& val) {
    const int x = 25214;
    std::cout << 25214;
    for (int i = 0; i < 25214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25215(int& val) {
    func_in_25215(val);
    const int x = 25215;
    std::cout << 25215;
    for (int i = 0; i < 25215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25215(int& val) {
    const int x = 25215;
    std::cout << 25215;
    for (int i = 0; i < 25215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25216(int& val) {
    func_in_25216(val);
    const int x = 25216;
    std::cout << 25216;
    for (int i = 0; i < 25216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25216(int& val) {
    const int x = 25216;
    std::cout << 25216;
    for (int i = 0; i < 25216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25217(int& val) {
    func_in_25217(val);
    const int x = 25217;
    std::cout << 25217;
    for (int i = 0; i < 25217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25217(int& val) {
    const int x = 25217;
    std::cout << 25217;
    for (int i = 0; i < 25217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25218(int& val) {
    func_in_25218(val);
    const int x = 25218;
    std::cout << 25218;
    for (int i = 0; i < 25218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25218(int& val) {
    const int x = 25218;
    std::cout << 25218;
    for (int i = 0; i < 25218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25219(int& val) {
    func_in_25219(val);
    const int x = 25219;
    std::cout << 25219;
    for (int i = 0; i < 25219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25219(int& val) {
    const int x = 25219;
    std::cout << 25219;
    for (int i = 0; i < 25219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25220(int& val) {
    func_in_25220(val);
    const int x = 25220;
    std::cout << 25220;
    for (int i = 0; i < 25220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25220(int& val) {
    const int x = 25220;
    std::cout << 25220;
    for (int i = 0; i < 25220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25221(int& val) {
    func_in_25221(val);
    const int x = 25221;
    std::cout << 25221;
    for (int i = 0; i < 25221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25221(int& val) {
    const int x = 25221;
    std::cout << 25221;
    for (int i = 0; i < 25221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25222(int& val) {
    func_in_25222(val);
    const int x = 25222;
    std::cout << 25222;
    for (int i = 0; i < 25222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25222(int& val) {
    const int x = 25222;
    std::cout << 25222;
    for (int i = 0; i < 25222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25223(int& val) {
    func_in_25223(val);
    const int x = 25223;
    std::cout << 25223;
    for (int i = 0; i < 25223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25223(int& val) {
    const int x = 25223;
    std::cout << 25223;
    for (int i = 0; i < 25223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25224(int& val) {
    func_in_25224(val);
    const int x = 25224;
    std::cout << 25224;
    for (int i = 0; i < 25224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25224(int& val) {
    const int x = 25224;
    std::cout << 25224;
    for (int i = 0; i < 25224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25225(int& val) {
    func_in_25225(val);
    const int x = 25225;
    std::cout << 25225;
    for (int i = 0; i < 25225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25225(int& val) {
    const int x = 25225;
    std::cout << 25225;
    for (int i = 0; i < 25225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25226(int& val) {
    func_in_25226(val);
    const int x = 25226;
    std::cout << 25226;
    for (int i = 0; i < 25226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25226(int& val) {
    const int x = 25226;
    std::cout << 25226;
    for (int i = 0; i < 25226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25227(int& val) {
    func_in_25227(val);
    const int x = 25227;
    std::cout << 25227;
    for (int i = 0; i < 25227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25227(int& val) {
    const int x = 25227;
    std::cout << 25227;
    for (int i = 0; i < 25227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25228(int& val) {
    func_in_25228(val);
    const int x = 25228;
    std::cout << 25228;
    for (int i = 0; i < 25228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25228(int& val) {
    const int x = 25228;
    std::cout << 25228;
    for (int i = 0; i < 25228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25229(int& val) {
    func_in_25229(val);
    const int x = 25229;
    std::cout << 25229;
    for (int i = 0; i < 25229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25229(int& val) {
    const int x = 25229;
    std::cout << 25229;
    for (int i = 0; i < 25229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25230(int& val) {
    func_in_25230(val);
    const int x = 25230;
    std::cout << 25230;
    for (int i = 0; i < 25230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25230(int& val) {
    const int x = 25230;
    std::cout << 25230;
    for (int i = 0; i < 25230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25231(int& val) {
    func_in_25231(val);
    const int x = 25231;
    std::cout << 25231;
    for (int i = 0; i < 25231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25231(int& val) {
    const int x = 25231;
    std::cout << 25231;
    for (int i = 0; i < 25231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25232(int& val) {
    func_in_25232(val);
    const int x = 25232;
    std::cout << 25232;
    for (int i = 0; i < 25232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25232(int& val) {
    const int x = 25232;
    std::cout << 25232;
    for (int i = 0; i < 25232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25233(int& val) {
    func_in_25233(val);
    const int x = 25233;
    std::cout << 25233;
    for (int i = 0; i < 25233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25233(int& val) {
    const int x = 25233;
    std::cout << 25233;
    for (int i = 0; i < 25233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25234(int& val) {
    func_in_25234(val);
    const int x = 25234;
    std::cout << 25234;
    for (int i = 0; i < 25234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25234(int& val) {
    const int x = 25234;
    std::cout << 25234;
    for (int i = 0; i < 25234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25235(int& val) {
    func_in_25235(val);
    const int x = 25235;
    std::cout << 25235;
    for (int i = 0; i < 25235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25235(int& val) {
    const int x = 25235;
    std::cout << 25235;
    for (int i = 0; i < 25235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25236(int& val) {
    func_in_25236(val);
    const int x = 25236;
    std::cout << 25236;
    for (int i = 0; i < 25236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25236(int& val) {
    const int x = 25236;
    std::cout << 25236;
    for (int i = 0; i < 25236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25237(int& val) {
    func_in_25237(val);
    const int x = 25237;
    std::cout << 25237;
    for (int i = 0; i < 25237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25237(int& val) {
    const int x = 25237;
    std::cout << 25237;
    for (int i = 0; i < 25237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25238(int& val) {
    func_in_25238(val);
    const int x = 25238;
    std::cout << 25238;
    for (int i = 0; i < 25238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25238(int& val) {
    const int x = 25238;
    std::cout << 25238;
    for (int i = 0; i < 25238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25239(int& val) {
    func_in_25239(val);
    const int x = 25239;
    std::cout << 25239;
    for (int i = 0; i < 25239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25239(int& val) {
    const int x = 25239;
    std::cout << 25239;
    for (int i = 0; i < 25239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25240(int& val) {
    func_in_25240(val);
    const int x = 25240;
    std::cout << 25240;
    for (int i = 0; i < 25240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25240(int& val) {
    const int x = 25240;
    std::cout << 25240;
    for (int i = 0; i < 25240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25241(int& val) {
    func_in_25241(val);
    const int x = 25241;
    std::cout << 25241;
    for (int i = 0; i < 25241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25241(int& val) {
    const int x = 25241;
    std::cout << 25241;
    for (int i = 0; i < 25241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25242(int& val) {
    func_in_25242(val);
    const int x = 25242;
    std::cout << 25242;
    for (int i = 0; i < 25242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25242(int& val) {
    const int x = 25242;
    std::cout << 25242;
    for (int i = 0; i < 25242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25243(int& val) {
    func_in_25243(val);
    const int x = 25243;
    std::cout << 25243;
    for (int i = 0; i < 25243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25243(int& val) {
    const int x = 25243;
    std::cout << 25243;
    for (int i = 0; i < 25243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25244(int& val) {
    func_in_25244(val);
    const int x = 25244;
    std::cout << 25244;
    for (int i = 0; i < 25244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25244(int& val) {
    const int x = 25244;
    std::cout << 25244;
    for (int i = 0; i < 25244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25245(int& val) {
    func_in_25245(val);
    const int x = 25245;
    std::cout << 25245;
    for (int i = 0; i < 25245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25245(int& val) {
    const int x = 25245;
    std::cout << 25245;
    for (int i = 0; i < 25245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25246(int& val) {
    func_in_25246(val);
    const int x = 25246;
    std::cout << 25246;
    for (int i = 0; i < 25246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25246(int& val) {
    const int x = 25246;
    std::cout << 25246;
    for (int i = 0; i < 25246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25247(int& val) {
    func_in_25247(val);
    const int x = 25247;
    std::cout << 25247;
    for (int i = 0; i < 25247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25247(int& val) {
    const int x = 25247;
    std::cout << 25247;
    for (int i = 0; i < 25247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25248(int& val) {
    func_in_25248(val);
    const int x = 25248;
    std::cout << 25248;
    for (int i = 0; i < 25248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25248(int& val) {
    const int x = 25248;
    std::cout << 25248;
    for (int i = 0; i < 25248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25249(int& val) {
    func_in_25249(val);
    const int x = 25249;
    std::cout << 25249;
    for (int i = 0; i < 25249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25249(int& val) {
    const int x = 25249;
    std::cout << 25249;
    for (int i = 0; i < 25249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25250(int& val) {
    func_in_25250(val);
    const int x = 25250;
    std::cout << 25250;
    for (int i = 0; i < 25250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25250(int& val) {
    const int x = 25250;
    std::cout << 25250;
    for (int i = 0; i < 25250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25251(int& val) {
    func_in_25251(val);
    const int x = 25251;
    std::cout << 25251;
    for (int i = 0; i < 25251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25251(int& val) {
    const int x = 25251;
    std::cout << 25251;
    for (int i = 0; i < 25251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25252(int& val) {
    func_in_25252(val);
    const int x = 25252;
    std::cout << 25252;
    for (int i = 0; i < 25252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25252(int& val) {
    const int x = 25252;
    std::cout << 25252;
    for (int i = 0; i < 25252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25253(int& val) {
    func_in_25253(val);
    const int x = 25253;
    std::cout << 25253;
    for (int i = 0; i < 25253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25253(int& val) {
    const int x = 25253;
    std::cout << 25253;
    for (int i = 0; i < 25253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25254(int& val) {
    func_in_25254(val);
    const int x = 25254;
    std::cout << 25254;
    for (int i = 0; i < 25254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25254(int& val) {
    const int x = 25254;
    std::cout << 25254;
    for (int i = 0; i < 25254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25255(int& val) {
    func_in_25255(val);
    const int x = 25255;
    std::cout << 25255;
    for (int i = 0; i < 25255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25255(int& val) {
    const int x = 25255;
    std::cout << 25255;
    for (int i = 0; i < 25255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25256(int& val) {
    func_in_25256(val);
    const int x = 25256;
    std::cout << 25256;
    for (int i = 0; i < 25256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25256(int& val) {
    const int x = 25256;
    std::cout << 25256;
    for (int i = 0; i < 25256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25257(int& val) {
    func_in_25257(val);
    const int x = 25257;
    std::cout << 25257;
    for (int i = 0; i < 25257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25257(int& val) {
    const int x = 25257;
    std::cout << 25257;
    for (int i = 0; i < 25257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25258(int& val) {
    func_in_25258(val);
    const int x = 25258;
    std::cout << 25258;
    for (int i = 0; i < 25258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25258(int& val) {
    const int x = 25258;
    std::cout << 25258;
    for (int i = 0; i < 25258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25259(int& val) {
    func_in_25259(val);
    const int x = 25259;
    std::cout << 25259;
    for (int i = 0; i < 25259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25259(int& val) {
    const int x = 25259;
    std::cout << 25259;
    for (int i = 0; i < 25259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25260(int& val) {
    func_in_25260(val);
    const int x = 25260;
    std::cout << 25260;
    for (int i = 0; i < 25260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25260(int& val) {
    const int x = 25260;
    std::cout << 25260;
    for (int i = 0; i < 25260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25261(int& val) {
    func_in_25261(val);
    const int x = 25261;
    std::cout << 25261;
    for (int i = 0; i < 25261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25261(int& val) {
    const int x = 25261;
    std::cout << 25261;
    for (int i = 0; i < 25261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25262(int& val) {
    func_in_25262(val);
    const int x = 25262;
    std::cout << 25262;
    for (int i = 0; i < 25262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25262(int& val) {
    const int x = 25262;
    std::cout << 25262;
    for (int i = 0; i < 25262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25263(int& val) {
    func_in_25263(val);
    const int x = 25263;
    std::cout << 25263;
    for (int i = 0; i < 25263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25263(int& val) {
    const int x = 25263;
    std::cout << 25263;
    for (int i = 0; i < 25263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25264(int& val) {
    func_in_25264(val);
    const int x = 25264;
    std::cout << 25264;
    for (int i = 0; i < 25264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25264(int& val) {
    const int x = 25264;
    std::cout << 25264;
    for (int i = 0; i < 25264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25265(int& val) {
    func_in_25265(val);
    const int x = 25265;
    std::cout << 25265;
    for (int i = 0; i < 25265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25265(int& val) {
    const int x = 25265;
    std::cout << 25265;
    for (int i = 0; i < 25265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25266(int& val) {
    func_in_25266(val);
    const int x = 25266;
    std::cout << 25266;
    for (int i = 0; i < 25266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25266(int& val) {
    const int x = 25266;
    std::cout << 25266;
    for (int i = 0; i < 25266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25267(int& val) {
    func_in_25267(val);
    const int x = 25267;
    std::cout << 25267;
    for (int i = 0; i < 25267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25267(int& val) {
    const int x = 25267;
    std::cout << 25267;
    for (int i = 0; i < 25267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25268(int& val) {
    func_in_25268(val);
    const int x = 25268;
    std::cout << 25268;
    for (int i = 0; i < 25268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25268(int& val) {
    const int x = 25268;
    std::cout << 25268;
    for (int i = 0; i < 25268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25269(int& val) {
    func_in_25269(val);
    const int x = 25269;
    std::cout << 25269;
    for (int i = 0; i < 25269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25269(int& val) {
    const int x = 25269;
    std::cout << 25269;
    for (int i = 0; i < 25269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25270(int& val) {
    func_in_25270(val);
    const int x = 25270;
    std::cout << 25270;
    for (int i = 0; i < 25270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25270(int& val) {
    const int x = 25270;
    std::cout << 25270;
    for (int i = 0; i < 25270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25271(int& val) {
    func_in_25271(val);
    const int x = 25271;
    std::cout << 25271;
    for (int i = 0; i < 25271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25271(int& val) {
    const int x = 25271;
    std::cout << 25271;
    for (int i = 0; i < 25271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25272(int& val) {
    func_in_25272(val);
    const int x = 25272;
    std::cout << 25272;
    for (int i = 0; i < 25272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25272(int& val) {
    const int x = 25272;
    std::cout << 25272;
    for (int i = 0; i < 25272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25273(int& val) {
    func_in_25273(val);
    const int x = 25273;
    std::cout << 25273;
    for (int i = 0; i < 25273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25273(int& val) {
    const int x = 25273;
    std::cout << 25273;
    for (int i = 0; i < 25273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25274(int& val) {
    func_in_25274(val);
    const int x = 25274;
    std::cout << 25274;
    for (int i = 0; i < 25274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25274(int& val) {
    const int x = 25274;
    std::cout << 25274;
    for (int i = 0; i < 25274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25275(int& val) {
    func_in_25275(val);
    const int x = 25275;
    std::cout << 25275;
    for (int i = 0; i < 25275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25275(int& val) {
    const int x = 25275;
    std::cout << 25275;
    for (int i = 0; i < 25275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25276(int& val) {
    func_in_25276(val);
    const int x = 25276;
    std::cout << 25276;
    for (int i = 0; i < 25276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25276(int& val) {
    const int x = 25276;
    std::cout << 25276;
    for (int i = 0; i < 25276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25277(int& val) {
    func_in_25277(val);
    const int x = 25277;
    std::cout << 25277;
    for (int i = 0; i < 25277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25277(int& val) {
    const int x = 25277;
    std::cout << 25277;
    for (int i = 0; i < 25277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25278(int& val) {
    func_in_25278(val);
    const int x = 25278;
    std::cout << 25278;
    for (int i = 0; i < 25278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25278(int& val) {
    const int x = 25278;
    std::cout << 25278;
    for (int i = 0; i < 25278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25279(int& val) {
    func_in_25279(val);
    const int x = 25279;
    std::cout << 25279;
    for (int i = 0; i < 25279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25279(int& val) {
    const int x = 25279;
    std::cout << 25279;
    for (int i = 0; i < 25279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25280(int& val) {
    func_in_25280(val);
    const int x = 25280;
    std::cout << 25280;
    for (int i = 0; i < 25280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25280(int& val) {
    const int x = 25280;
    std::cout << 25280;
    for (int i = 0; i < 25280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25281(int& val) {
    func_in_25281(val);
    const int x = 25281;
    std::cout << 25281;
    for (int i = 0; i < 25281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25281(int& val) {
    const int x = 25281;
    std::cout << 25281;
    for (int i = 0; i < 25281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25282(int& val) {
    func_in_25282(val);
    const int x = 25282;
    std::cout << 25282;
    for (int i = 0; i < 25282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25282(int& val) {
    const int x = 25282;
    std::cout << 25282;
    for (int i = 0; i < 25282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25283(int& val) {
    func_in_25283(val);
    const int x = 25283;
    std::cout << 25283;
    for (int i = 0; i < 25283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25283(int& val) {
    const int x = 25283;
    std::cout << 25283;
    for (int i = 0; i < 25283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25284(int& val) {
    func_in_25284(val);
    const int x = 25284;
    std::cout << 25284;
    for (int i = 0; i < 25284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25284(int& val) {
    const int x = 25284;
    std::cout << 25284;
    for (int i = 0; i < 25284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25285(int& val) {
    func_in_25285(val);
    const int x = 25285;
    std::cout << 25285;
    for (int i = 0; i < 25285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25285(int& val) {
    const int x = 25285;
    std::cout << 25285;
    for (int i = 0; i < 25285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25286(int& val) {
    func_in_25286(val);
    const int x = 25286;
    std::cout << 25286;
    for (int i = 0; i < 25286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25286(int& val) {
    const int x = 25286;
    std::cout << 25286;
    for (int i = 0; i < 25286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25287(int& val) {
    func_in_25287(val);
    const int x = 25287;
    std::cout << 25287;
    for (int i = 0; i < 25287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25287(int& val) {
    const int x = 25287;
    std::cout << 25287;
    for (int i = 0; i < 25287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25288(int& val) {
    func_in_25288(val);
    const int x = 25288;
    std::cout << 25288;
    for (int i = 0; i < 25288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25288(int& val) {
    const int x = 25288;
    std::cout << 25288;
    for (int i = 0; i < 25288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25289(int& val) {
    func_in_25289(val);
    const int x = 25289;
    std::cout << 25289;
    for (int i = 0; i < 25289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25289(int& val) {
    const int x = 25289;
    std::cout << 25289;
    for (int i = 0; i < 25289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25290(int& val) {
    func_in_25290(val);
    const int x = 25290;
    std::cout << 25290;
    for (int i = 0; i < 25290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25290(int& val) {
    const int x = 25290;
    std::cout << 25290;
    for (int i = 0; i < 25290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25291(int& val) {
    func_in_25291(val);
    const int x = 25291;
    std::cout << 25291;
    for (int i = 0; i < 25291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25291(int& val) {
    const int x = 25291;
    std::cout << 25291;
    for (int i = 0; i < 25291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25292(int& val) {
    func_in_25292(val);
    const int x = 25292;
    std::cout << 25292;
    for (int i = 0; i < 25292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25292(int& val) {
    const int x = 25292;
    std::cout << 25292;
    for (int i = 0; i < 25292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25293(int& val) {
    func_in_25293(val);
    const int x = 25293;
    std::cout << 25293;
    for (int i = 0; i < 25293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25293(int& val) {
    const int x = 25293;
    std::cout << 25293;
    for (int i = 0; i < 25293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25294(int& val) {
    func_in_25294(val);
    const int x = 25294;
    std::cout << 25294;
    for (int i = 0; i < 25294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25294(int& val) {
    const int x = 25294;
    std::cout << 25294;
    for (int i = 0; i < 25294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25295(int& val) {
    func_in_25295(val);
    const int x = 25295;
    std::cout << 25295;
    for (int i = 0; i < 25295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25295(int& val) {
    const int x = 25295;
    std::cout << 25295;
    for (int i = 0; i < 25295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25296(int& val) {
    func_in_25296(val);
    const int x = 25296;
    std::cout << 25296;
    for (int i = 0; i < 25296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25296(int& val) {
    const int x = 25296;
    std::cout << 25296;
    for (int i = 0; i < 25296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25297(int& val) {
    func_in_25297(val);
    const int x = 25297;
    std::cout << 25297;
    for (int i = 0; i < 25297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25297(int& val) {
    const int x = 25297;
    std::cout << 25297;
    for (int i = 0; i < 25297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25298(int& val) {
    func_in_25298(val);
    const int x = 25298;
    std::cout << 25298;
    for (int i = 0; i < 25298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25298(int& val) {
    const int x = 25298;
    std::cout << 25298;
    for (int i = 0; i < 25298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25299(int& val) {
    func_in_25299(val);
    const int x = 25299;
    std::cout << 25299;
    for (int i = 0; i < 25299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25299(int& val) {
    const int x = 25299;
    std::cout << 25299;
    for (int i = 0; i < 25299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25300(int& val) {
    func_in_25300(val);
    const int x = 25300;
    std::cout << 25300;
    for (int i = 0; i < 25300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25300(int& val) {
    const int x = 25300;
    std::cout << 25300;
    for (int i = 0; i < 25300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25301(int& val) {
    func_in_25301(val);
    const int x = 25301;
    std::cout << 25301;
    for (int i = 0; i < 25301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25301(int& val) {
    const int x = 25301;
    std::cout << 25301;
    for (int i = 0; i < 25301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25302(int& val) {
    func_in_25302(val);
    const int x = 25302;
    std::cout << 25302;
    for (int i = 0; i < 25302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25302(int& val) {
    const int x = 25302;
    std::cout << 25302;
    for (int i = 0; i < 25302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25303(int& val) {
    func_in_25303(val);
    const int x = 25303;
    std::cout << 25303;
    for (int i = 0; i < 25303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25303(int& val) {
    const int x = 25303;
    std::cout << 25303;
    for (int i = 0; i < 25303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25304(int& val) {
    func_in_25304(val);
    const int x = 25304;
    std::cout << 25304;
    for (int i = 0; i < 25304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25304(int& val) {
    const int x = 25304;
    std::cout << 25304;
    for (int i = 0; i < 25304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25305(int& val) {
    func_in_25305(val);
    const int x = 25305;
    std::cout << 25305;
    for (int i = 0; i < 25305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25305(int& val) {
    const int x = 25305;
    std::cout << 25305;
    for (int i = 0; i < 25305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25306(int& val) {
    func_in_25306(val);
    const int x = 25306;
    std::cout << 25306;
    for (int i = 0; i < 25306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25306(int& val) {
    const int x = 25306;
    std::cout << 25306;
    for (int i = 0; i < 25306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25307(int& val) {
    func_in_25307(val);
    const int x = 25307;
    std::cout << 25307;
    for (int i = 0; i < 25307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25307(int& val) {
    const int x = 25307;
    std::cout << 25307;
    for (int i = 0; i < 25307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25308(int& val) {
    func_in_25308(val);
    const int x = 25308;
    std::cout << 25308;
    for (int i = 0; i < 25308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25308(int& val) {
    const int x = 25308;
    std::cout << 25308;
    for (int i = 0; i < 25308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25309(int& val) {
    func_in_25309(val);
    const int x = 25309;
    std::cout << 25309;
    for (int i = 0; i < 25309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25309(int& val) {
    const int x = 25309;
    std::cout << 25309;
    for (int i = 0; i < 25309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25310(int& val) {
    func_in_25310(val);
    const int x = 25310;
    std::cout << 25310;
    for (int i = 0; i < 25310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25310(int& val) {
    const int x = 25310;
    std::cout << 25310;
    for (int i = 0; i < 25310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25311(int& val) {
    func_in_25311(val);
    const int x = 25311;
    std::cout << 25311;
    for (int i = 0; i < 25311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25311(int& val) {
    const int x = 25311;
    std::cout << 25311;
    for (int i = 0; i < 25311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25312(int& val) {
    func_in_25312(val);
    const int x = 25312;
    std::cout << 25312;
    for (int i = 0; i < 25312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25312(int& val) {
    const int x = 25312;
    std::cout << 25312;
    for (int i = 0; i < 25312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25313(int& val) {
    func_in_25313(val);
    const int x = 25313;
    std::cout << 25313;
    for (int i = 0; i < 25313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25313(int& val) {
    const int x = 25313;
    std::cout << 25313;
    for (int i = 0; i < 25313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25314(int& val) {
    func_in_25314(val);
    const int x = 25314;
    std::cout << 25314;
    for (int i = 0; i < 25314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25314(int& val) {
    const int x = 25314;
    std::cout << 25314;
    for (int i = 0; i < 25314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25315(int& val) {
    func_in_25315(val);
    const int x = 25315;
    std::cout << 25315;
    for (int i = 0; i < 25315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25315(int& val) {
    const int x = 25315;
    std::cout << 25315;
    for (int i = 0; i < 25315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25316(int& val) {
    func_in_25316(val);
    const int x = 25316;
    std::cout << 25316;
    for (int i = 0; i < 25316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25316(int& val) {
    const int x = 25316;
    std::cout << 25316;
    for (int i = 0; i < 25316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25317(int& val) {
    func_in_25317(val);
    const int x = 25317;
    std::cout << 25317;
    for (int i = 0; i < 25317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25317(int& val) {
    const int x = 25317;
    std::cout << 25317;
    for (int i = 0; i < 25317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25318(int& val) {
    func_in_25318(val);
    const int x = 25318;
    std::cout << 25318;
    for (int i = 0; i < 25318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25318(int& val) {
    const int x = 25318;
    std::cout << 25318;
    for (int i = 0; i < 25318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25319(int& val) {
    func_in_25319(val);
    const int x = 25319;
    std::cout << 25319;
    for (int i = 0; i < 25319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25319(int& val) {
    const int x = 25319;
    std::cout << 25319;
    for (int i = 0; i < 25319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25320(int& val) {
    func_in_25320(val);
    const int x = 25320;
    std::cout << 25320;
    for (int i = 0; i < 25320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25320(int& val) {
    const int x = 25320;
    std::cout << 25320;
    for (int i = 0; i < 25320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25321(int& val) {
    func_in_25321(val);
    const int x = 25321;
    std::cout << 25321;
    for (int i = 0; i < 25321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25321(int& val) {
    const int x = 25321;
    std::cout << 25321;
    for (int i = 0; i < 25321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25322(int& val) {
    func_in_25322(val);
    const int x = 25322;
    std::cout << 25322;
    for (int i = 0; i < 25322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25322(int& val) {
    const int x = 25322;
    std::cout << 25322;
    for (int i = 0; i < 25322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25323(int& val) {
    func_in_25323(val);
    const int x = 25323;
    std::cout << 25323;
    for (int i = 0; i < 25323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25323(int& val) {
    const int x = 25323;
    std::cout << 25323;
    for (int i = 0; i < 25323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25324(int& val) {
    func_in_25324(val);
    const int x = 25324;
    std::cout << 25324;
    for (int i = 0; i < 25324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25324(int& val) {
    const int x = 25324;
    std::cout << 25324;
    for (int i = 0; i < 25324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25325(int& val) {
    func_in_25325(val);
    const int x = 25325;
    std::cout << 25325;
    for (int i = 0; i < 25325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25325(int& val) {
    const int x = 25325;
    std::cout << 25325;
    for (int i = 0; i < 25325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25326(int& val) {
    func_in_25326(val);
    const int x = 25326;
    std::cout << 25326;
    for (int i = 0; i < 25326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25326(int& val) {
    const int x = 25326;
    std::cout << 25326;
    for (int i = 0; i < 25326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25327(int& val) {
    func_in_25327(val);
    const int x = 25327;
    std::cout << 25327;
    for (int i = 0; i < 25327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25327(int& val) {
    const int x = 25327;
    std::cout << 25327;
    for (int i = 0; i < 25327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25328(int& val) {
    func_in_25328(val);
    const int x = 25328;
    std::cout << 25328;
    for (int i = 0; i < 25328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25328(int& val) {
    const int x = 25328;
    std::cout << 25328;
    for (int i = 0; i < 25328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25329(int& val) {
    func_in_25329(val);
    const int x = 25329;
    std::cout << 25329;
    for (int i = 0; i < 25329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25329(int& val) {
    const int x = 25329;
    std::cout << 25329;
    for (int i = 0; i < 25329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25330(int& val) {
    func_in_25330(val);
    const int x = 25330;
    std::cout << 25330;
    for (int i = 0; i < 25330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25330(int& val) {
    const int x = 25330;
    std::cout << 25330;
    for (int i = 0; i < 25330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25331(int& val) {
    func_in_25331(val);
    const int x = 25331;
    std::cout << 25331;
    for (int i = 0; i < 25331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25331(int& val) {
    const int x = 25331;
    std::cout << 25331;
    for (int i = 0; i < 25331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25332(int& val) {
    func_in_25332(val);
    const int x = 25332;
    std::cout << 25332;
    for (int i = 0; i < 25332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25332(int& val) {
    const int x = 25332;
    std::cout << 25332;
    for (int i = 0; i < 25332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25333(int& val) {
    func_in_25333(val);
    const int x = 25333;
    std::cout << 25333;
    for (int i = 0; i < 25333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25333(int& val) {
    const int x = 25333;
    std::cout << 25333;
    for (int i = 0; i < 25333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25334(int& val) {
    func_in_25334(val);
    const int x = 25334;
    std::cout << 25334;
    for (int i = 0; i < 25334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25334(int& val) {
    const int x = 25334;
    std::cout << 25334;
    for (int i = 0; i < 25334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25335(int& val) {
    func_in_25335(val);
    const int x = 25335;
    std::cout << 25335;
    for (int i = 0; i < 25335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25335(int& val) {
    const int x = 25335;
    std::cout << 25335;
    for (int i = 0; i < 25335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25336(int& val) {
    func_in_25336(val);
    const int x = 25336;
    std::cout << 25336;
    for (int i = 0; i < 25336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25336(int& val) {
    const int x = 25336;
    std::cout << 25336;
    for (int i = 0; i < 25336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25337(int& val) {
    func_in_25337(val);
    const int x = 25337;
    std::cout << 25337;
    for (int i = 0; i < 25337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25337(int& val) {
    const int x = 25337;
    std::cout << 25337;
    for (int i = 0; i < 25337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25338(int& val) {
    func_in_25338(val);
    const int x = 25338;
    std::cout << 25338;
    for (int i = 0; i < 25338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25338(int& val) {
    const int x = 25338;
    std::cout << 25338;
    for (int i = 0; i < 25338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25339(int& val) {
    func_in_25339(val);
    const int x = 25339;
    std::cout << 25339;
    for (int i = 0; i < 25339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25339(int& val) {
    const int x = 25339;
    std::cout << 25339;
    for (int i = 0; i < 25339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25340(int& val) {
    func_in_25340(val);
    const int x = 25340;
    std::cout << 25340;
    for (int i = 0; i < 25340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25340(int& val) {
    const int x = 25340;
    std::cout << 25340;
    for (int i = 0; i < 25340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25341(int& val) {
    func_in_25341(val);
    const int x = 25341;
    std::cout << 25341;
    for (int i = 0; i < 25341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25341(int& val) {
    const int x = 25341;
    std::cout << 25341;
    for (int i = 0; i < 25341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25342(int& val) {
    func_in_25342(val);
    const int x = 25342;
    std::cout << 25342;
    for (int i = 0; i < 25342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25342(int& val) {
    const int x = 25342;
    std::cout << 25342;
    for (int i = 0; i < 25342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25343(int& val) {
    func_in_25343(val);
    const int x = 25343;
    std::cout << 25343;
    for (int i = 0; i < 25343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25343(int& val) {
    const int x = 25343;
    std::cout << 25343;
    for (int i = 0; i < 25343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25344(int& val) {
    func_in_25344(val);
    const int x = 25344;
    std::cout << 25344;
    for (int i = 0; i < 25344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25344(int& val) {
    const int x = 25344;
    std::cout << 25344;
    for (int i = 0; i < 25344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25345(int& val) {
    func_in_25345(val);
    const int x = 25345;
    std::cout << 25345;
    for (int i = 0; i < 25345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25345(int& val) {
    const int x = 25345;
    std::cout << 25345;
    for (int i = 0; i < 25345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25346(int& val) {
    func_in_25346(val);
    const int x = 25346;
    std::cout << 25346;
    for (int i = 0; i < 25346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25346(int& val) {
    const int x = 25346;
    std::cout << 25346;
    for (int i = 0; i < 25346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25347(int& val) {
    func_in_25347(val);
    const int x = 25347;
    std::cout << 25347;
    for (int i = 0; i < 25347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25347(int& val) {
    const int x = 25347;
    std::cout << 25347;
    for (int i = 0; i < 25347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25348(int& val) {
    func_in_25348(val);
    const int x = 25348;
    std::cout << 25348;
    for (int i = 0; i < 25348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25348(int& val) {
    const int x = 25348;
    std::cout << 25348;
    for (int i = 0; i < 25348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25349(int& val) {
    func_in_25349(val);
    const int x = 25349;
    std::cout << 25349;
    for (int i = 0; i < 25349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25349(int& val) {
    const int x = 25349;
    std::cout << 25349;
    for (int i = 0; i < 25349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25350(int& val) {
    func_in_25350(val);
    const int x = 25350;
    std::cout << 25350;
    for (int i = 0; i < 25350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25350(int& val) {
    const int x = 25350;
    std::cout << 25350;
    for (int i = 0; i < 25350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25351(int& val) {
    func_in_25351(val);
    const int x = 25351;
    std::cout << 25351;
    for (int i = 0; i < 25351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25351(int& val) {
    const int x = 25351;
    std::cout << 25351;
    for (int i = 0; i < 25351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25352(int& val) {
    func_in_25352(val);
    const int x = 25352;
    std::cout << 25352;
    for (int i = 0; i < 25352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25352(int& val) {
    const int x = 25352;
    std::cout << 25352;
    for (int i = 0; i < 25352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25353(int& val) {
    func_in_25353(val);
    const int x = 25353;
    std::cout << 25353;
    for (int i = 0; i < 25353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25353(int& val) {
    const int x = 25353;
    std::cout << 25353;
    for (int i = 0; i < 25353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25354(int& val) {
    func_in_25354(val);
    const int x = 25354;
    std::cout << 25354;
    for (int i = 0; i < 25354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25354(int& val) {
    const int x = 25354;
    std::cout << 25354;
    for (int i = 0; i < 25354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25355(int& val) {
    func_in_25355(val);
    const int x = 25355;
    std::cout << 25355;
    for (int i = 0; i < 25355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25355(int& val) {
    const int x = 25355;
    std::cout << 25355;
    for (int i = 0; i < 25355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25356(int& val) {
    func_in_25356(val);
    const int x = 25356;
    std::cout << 25356;
    for (int i = 0; i < 25356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25356(int& val) {
    const int x = 25356;
    std::cout << 25356;
    for (int i = 0; i < 25356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25357(int& val) {
    func_in_25357(val);
    const int x = 25357;
    std::cout << 25357;
    for (int i = 0; i < 25357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25357(int& val) {
    const int x = 25357;
    std::cout << 25357;
    for (int i = 0; i < 25357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25358(int& val) {
    func_in_25358(val);
    const int x = 25358;
    std::cout << 25358;
    for (int i = 0; i < 25358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25358(int& val) {
    const int x = 25358;
    std::cout << 25358;
    for (int i = 0; i < 25358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25359(int& val) {
    func_in_25359(val);
    const int x = 25359;
    std::cout << 25359;
    for (int i = 0; i < 25359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25359(int& val) {
    const int x = 25359;
    std::cout << 25359;
    for (int i = 0; i < 25359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25360(int& val) {
    func_in_25360(val);
    const int x = 25360;
    std::cout << 25360;
    for (int i = 0; i < 25360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25360(int& val) {
    const int x = 25360;
    std::cout << 25360;
    for (int i = 0; i < 25360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25361(int& val) {
    func_in_25361(val);
    const int x = 25361;
    std::cout << 25361;
    for (int i = 0; i < 25361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25361(int& val) {
    const int x = 25361;
    std::cout << 25361;
    for (int i = 0; i < 25361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25362(int& val) {
    func_in_25362(val);
    const int x = 25362;
    std::cout << 25362;
    for (int i = 0; i < 25362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25362(int& val) {
    const int x = 25362;
    std::cout << 25362;
    for (int i = 0; i < 25362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25363(int& val) {
    func_in_25363(val);
    const int x = 25363;
    std::cout << 25363;
    for (int i = 0; i < 25363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25363(int& val) {
    const int x = 25363;
    std::cout << 25363;
    for (int i = 0; i < 25363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25364(int& val) {
    func_in_25364(val);
    const int x = 25364;
    std::cout << 25364;
    for (int i = 0; i < 25364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25364(int& val) {
    const int x = 25364;
    std::cout << 25364;
    for (int i = 0; i < 25364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25365(int& val) {
    func_in_25365(val);
    const int x = 25365;
    std::cout << 25365;
    for (int i = 0; i < 25365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25365(int& val) {
    const int x = 25365;
    std::cout << 25365;
    for (int i = 0; i < 25365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25366(int& val) {
    func_in_25366(val);
    const int x = 25366;
    std::cout << 25366;
    for (int i = 0; i < 25366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25366(int& val) {
    const int x = 25366;
    std::cout << 25366;
    for (int i = 0; i < 25366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25367(int& val) {
    func_in_25367(val);
    const int x = 25367;
    std::cout << 25367;
    for (int i = 0; i < 25367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25367(int& val) {
    const int x = 25367;
    std::cout << 25367;
    for (int i = 0; i < 25367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25368(int& val) {
    func_in_25368(val);
    const int x = 25368;
    std::cout << 25368;
    for (int i = 0; i < 25368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25368(int& val) {
    const int x = 25368;
    std::cout << 25368;
    for (int i = 0; i < 25368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25369(int& val) {
    func_in_25369(val);
    const int x = 25369;
    std::cout << 25369;
    for (int i = 0; i < 25369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25369(int& val) {
    const int x = 25369;
    std::cout << 25369;
    for (int i = 0; i < 25369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25370(int& val) {
    func_in_25370(val);
    const int x = 25370;
    std::cout << 25370;
    for (int i = 0; i < 25370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25370(int& val) {
    const int x = 25370;
    std::cout << 25370;
    for (int i = 0; i < 25370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25371(int& val) {
    func_in_25371(val);
    const int x = 25371;
    std::cout << 25371;
    for (int i = 0; i < 25371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25371(int& val) {
    const int x = 25371;
    std::cout << 25371;
    for (int i = 0; i < 25371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25372(int& val) {
    func_in_25372(val);
    const int x = 25372;
    std::cout << 25372;
    for (int i = 0; i < 25372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25372(int& val) {
    const int x = 25372;
    std::cout << 25372;
    for (int i = 0; i < 25372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25373(int& val) {
    func_in_25373(val);
    const int x = 25373;
    std::cout << 25373;
    for (int i = 0; i < 25373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25373(int& val) {
    const int x = 25373;
    std::cout << 25373;
    for (int i = 0; i < 25373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25374(int& val) {
    func_in_25374(val);
    const int x = 25374;
    std::cout << 25374;
    for (int i = 0; i < 25374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25374(int& val) {
    const int x = 25374;
    std::cout << 25374;
    for (int i = 0; i < 25374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25375(int& val) {
    func_in_25375(val);
    const int x = 25375;
    std::cout << 25375;
    for (int i = 0; i < 25375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25375(int& val) {
    const int x = 25375;
    std::cout << 25375;
    for (int i = 0; i < 25375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25376(int& val) {
    func_in_25376(val);
    const int x = 25376;
    std::cout << 25376;
    for (int i = 0; i < 25376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25376(int& val) {
    const int x = 25376;
    std::cout << 25376;
    for (int i = 0; i < 25376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25377(int& val) {
    func_in_25377(val);
    const int x = 25377;
    std::cout << 25377;
    for (int i = 0; i < 25377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25377(int& val) {
    const int x = 25377;
    std::cout << 25377;
    for (int i = 0; i < 25377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25378(int& val) {
    func_in_25378(val);
    const int x = 25378;
    std::cout << 25378;
    for (int i = 0; i < 25378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25378(int& val) {
    const int x = 25378;
    std::cout << 25378;
    for (int i = 0; i < 25378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25379(int& val) {
    func_in_25379(val);
    const int x = 25379;
    std::cout << 25379;
    for (int i = 0; i < 25379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25379(int& val) {
    const int x = 25379;
    std::cout << 25379;
    for (int i = 0; i < 25379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25380(int& val) {
    func_in_25380(val);
    const int x = 25380;
    std::cout << 25380;
    for (int i = 0; i < 25380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25380(int& val) {
    const int x = 25380;
    std::cout << 25380;
    for (int i = 0; i < 25380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25381(int& val) {
    func_in_25381(val);
    const int x = 25381;
    std::cout << 25381;
    for (int i = 0; i < 25381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25381(int& val) {
    const int x = 25381;
    std::cout << 25381;
    for (int i = 0; i < 25381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25382(int& val) {
    func_in_25382(val);
    const int x = 25382;
    std::cout << 25382;
    for (int i = 0; i < 25382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25382(int& val) {
    const int x = 25382;
    std::cout << 25382;
    for (int i = 0; i < 25382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25383(int& val) {
    func_in_25383(val);
    const int x = 25383;
    std::cout << 25383;
    for (int i = 0; i < 25383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25383(int& val) {
    const int x = 25383;
    std::cout << 25383;
    for (int i = 0; i < 25383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25384(int& val) {
    func_in_25384(val);
    const int x = 25384;
    std::cout << 25384;
    for (int i = 0; i < 25384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25384(int& val) {
    const int x = 25384;
    std::cout << 25384;
    for (int i = 0; i < 25384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25385(int& val) {
    func_in_25385(val);
    const int x = 25385;
    std::cout << 25385;
    for (int i = 0; i < 25385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25385(int& val) {
    const int x = 25385;
    std::cout << 25385;
    for (int i = 0; i < 25385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25386(int& val) {
    func_in_25386(val);
    const int x = 25386;
    std::cout << 25386;
    for (int i = 0; i < 25386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25386(int& val) {
    const int x = 25386;
    std::cout << 25386;
    for (int i = 0; i < 25386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25387(int& val) {
    func_in_25387(val);
    const int x = 25387;
    std::cout << 25387;
    for (int i = 0; i < 25387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25387(int& val) {
    const int x = 25387;
    std::cout << 25387;
    for (int i = 0; i < 25387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25388(int& val) {
    func_in_25388(val);
    const int x = 25388;
    std::cout << 25388;
    for (int i = 0; i < 25388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25388(int& val) {
    const int x = 25388;
    std::cout << 25388;
    for (int i = 0; i < 25388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25389(int& val) {
    func_in_25389(val);
    const int x = 25389;
    std::cout << 25389;
    for (int i = 0; i < 25389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25389(int& val) {
    const int x = 25389;
    std::cout << 25389;
    for (int i = 0; i < 25389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25390(int& val) {
    func_in_25390(val);
    const int x = 25390;
    std::cout << 25390;
    for (int i = 0; i < 25390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25390(int& val) {
    const int x = 25390;
    std::cout << 25390;
    for (int i = 0; i < 25390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25391(int& val) {
    func_in_25391(val);
    const int x = 25391;
    std::cout << 25391;
    for (int i = 0; i < 25391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25391(int& val) {
    const int x = 25391;
    std::cout << 25391;
    for (int i = 0; i < 25391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25392(int& val) {
    func_in_25392(val);
    const int x = 25392;
    std::cout << 25392;
    for (int i = 0; i < 25392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25392(int& val) {
    const int x = 25392;
    std::cout << 25392;
    for (int i = 0; i < 25392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25393(int& val) {
    func_in_25393(val);
    const int x = 25393;
    std::cout << 25393;
    for (int i = 0; i < 25393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25393(int& val) {
    const int x = 25393;
    std::cout << 25393;
    for (int i = 0; i < 25393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25394(int& val) {
    func_in_25394(val);
    const int x = 25394;
    std::cout << 25394;
    for (int i = 0; i < 25394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25394(int& val) {
    const int x = 25394;
    std::cout << 25394;
    for (int i = 0; i < 25394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25395(int& val) {
    func_in_25395(val);
    const int x = 25395;
    std::cout << 25395;
    for (int i = 0; i < 25395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25395(int& val) {
    const int x = 25395;
    std::cout << 25395;
    for (int i = 0; i < 25395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25396(int& val) {
    func_in_25396(val);
    const int x = 25396;
    std::cout << 25396;
    for (int i = 0; i < 25396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25396(int& val) {
    const int x = 25396;
    std::cout << 25396;
    for (int i = 0; i < 25396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25397(int& val) {
    func_in_25397(val);
    const int x = 25397;
    std::cout << 25397;
    for (int i = 0; i < 25397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25397(int& val) {
    const int x = 25397;
    std::cout << 25397;
    for (int i = 0; i < 25397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25398(int& val) {
    func_in_25398(val);
    const int x = 25398;
    std::cout << 25398;
    for (int i = 0; i < 25398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25398(int& val) {
    const int x = 25398;
    std::cout << 25398;
    for (int i = 0; i < 25398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25399(int& val) {
    func_in_25399(val);
    const int x = 25399;
    std::cout << 25399;
    for (int i = 0; i < 25399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25399(int& val) {
    const int x = 25399;
    std::cout << 25399;
    for (int i = 0; i < 25399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25400(int& val) {
    func_in_25400(val);
    const int x = 25400;
    std::cout << 25400;
    for (int i = 0; i < 25400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25400(int& val) {
    const int x = 25400;
    std::cout << 25400;
    for (int i = 0; i < 25400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25401(int& val) {
    func_in_25401(val);
    const int x = 25401;
    std::cout << 25401;
    for (int i = 0; i < 25401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25401(int& val) {
    const int x = 25401;
    std::cout << 25401;
    for (int i = 0; i < 25401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25402(int& val) {
    func_in_25402(val);
    const int x = 25402;
    std::cout << 25402;
    for (int i = 0; i < 25402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25402(int& val) {
    const int x = 25402;
    std::cout << 25402;
    for (int i = 0; i < 25402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25403(int& val) {
    func_in_25403(val);
    const int x = 25403;
    std::cout << 25403;
    for (int i = 0; i < 25403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25403(int& val) {
    const int x = 25403;
    std::cout << 25403;
    for (int i = 0; i < 25403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25404(int& val) {
    func_in_25404(val);
    const int x = 25404;
    std::cout << 25404;
    for (int i = 0; i < 25404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25404(int& val) {
    const int x = 25404;
    std::cout << 25404;
    for (int i = 0; i < 25404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25405(int& val) {
    func_in_25405(val);
    const int x = 25405;
    std::cout << 25405;
    for (int i = 0; i < 25405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25405(int& val) {
    const int x = 25405;
    std::cout << 25405;
    for (int i = 0; i < 25405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25406(int& val) {
    func_in_25406(val);
    const int x = 25406;
    std::cout << 25406;
    for (int i = 0; i < 25406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25406(int& val) {
    const int x = 25406;
    std::cout << 25406;
    for (int i = 0; i < 25406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25407(int& val) {
    func_in_25407(val);
    const int x = 25407;
    std::cout << 25407;
    for (int i = 0; i < 25407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25407(int& val) {
    const int x = 25407;
    std::cout << 25407;
    for (int i = 0; i < 25407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25408(int& val) {
    func_in_25408(val);
    const int x = 25408;
    std::cout << 25408;
    for (int i = 0; i < 25408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25408(int& val) {
    const int x = 25408;
    std::cout << 25408;
    for (int i = 0; i < 25408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25409(int& val) {
    func_in_25409(val);
    const int x = 25409;
    std::cout << 25409;
    for (int i = 0; i < 25409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25409(int& val) {
    const int x = 25409;
    std::cout << 25409;
    for (int i = 0; i < 25409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25410(int& val) {
    func_in_25410(val);
    const int x = 25410;
    std::cout << 25410;
    for (int i = 0; i < 25410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25410(int& val) {
    const int x = 25410;
    std::cout << 25410;
    for (int i = 0; i < 25410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25411(int& val) {
    func_in_25411(val);
    const int x = 25411;
    std::cout << 25411;
    for (int i = 0; i < 25411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25411(int& val) {
    const int x = 25411;
    std::cout << 25411;
    for (int i = 0; i < 25411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25412(int& val) {
    func_in_25412(val);
    const int x = 25412;
    std::cout << 25412;
    for (int i = 0; i < 25412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25412(int& val) {
    const int x = 25412;
    std::cout << 25412;
    for (int i = 0; i < 25412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25413(int& val) {
    func_in_25413(val);
    const int x = 25413;
    std::cout << 25413;
    for (int i = 0; i < 25413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25413(int& val) {
    const int x = 25413;
    std::cout << 25413;
    for (int i = 0; i < 25413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25414(int& val) {
    func_in_25414(val);
    const int x = 25414;
    std::cout << 25414;
    for (int i = 0; i < 25414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25414(int& val) {
    const int x = 25414;
    std::cout << 25414;
    for (int i = 0; i < 25414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25415(int& val) {
    func_in_25415(val);
    const int x = 25415;
    std::cout << 25415;
    for (int i = 0; i < 25415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25415(int& val) {
    const int x = 25415;
    std::cout << 25415;
    for (int i = 0; i < 25415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25416(int& val) {
    func_in_25416(val);
    const int x = 25416;
    std::cout << 25416;
    for (int i = 0; i < 25416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25416(int& val) {
    const int x = 25416;
    std::cout << 25416;
    for (int i = 0; i < 25416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25417(int& val) {
    func_in_25417(val);
    const int x = 25417;
    std::cout << 25417;
    for (int i = 0; i < 25417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25417(int& val) {
    const int x = 25417;
    std::cout << 25417;
    for (int i = 0; i < 25417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25418(int& val) {
    func_in_25418(val);
    const int x = 25418;
    std::cout << 25418;
    for (int i = 0; i < 25418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25418(int& val) {
    const int x = 25418;
    std::cout << 25418;
    for (int i = 0; i < 25418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25419(int& val) {
    func_in_25419(val);
    const int x = 25419;
    std::cout << 25419;
    for (int i = 0; i < 25419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25419(int& val) {
    const int x = 25419;
    std::cout << 25419;
    for (int i = 0; i < 25419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25420(int& val) {
    func_in_25420(val);
    const int x = 25420;
    std::cout << 25420;
    for (int i = 0; i < 25420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25420(int& val) {
    const int x = 25420;
    std::cout << 25420;
    for (int i = 0; i < 25420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25421(int& val) {
    func_in_25421(val);
    const int x = 25421;
    std::cout << 25421;
    for (int i = 0; i < 25421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25421(int& val) {
    const int x = 25421;
    std::cout << 25421;
    for (int i = 0; i < 25421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25422(int& val) {
    func_in_25422(val);
    const int x = 25422;
    std::cout << 25422;
    for (int i = 0; i < 25422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25422(int& val) {
    const int x = 25422;
    std::cout << 25422;
    for (int i = 0; i < 25422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25423(int& val) {
    func_in_25423(val);
    const int x = 25423;
    std::cout << 25423;
    for (int i = 0; i < 25423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25423(int& val) {
    const int x = 25423;
    std::cout << 25423;
    for (int i = 0; i < 25423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25424(int& val) {
    func_in_25424(val);
    const int x = 25424;
    std::cout << 25424;
    for (int i = 0; i < 25424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25424(int& val) {
    const int x = 25424;
    std::cout << 25424;
    for (int i = 0; i < 25424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25425(int& val) {
    func_in_25425(val);
    const int x = 25425;
    std::cout << 25425;
    for (int i = 0; i < 25425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25425(int& val) {
    const int x = 25425;
    std::cout << 25425;
    for (int i = 0; i < 25425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25426(int& val) {
    func_in_25426(val);
    const int x = 25426;
    std::cout << 25426;
    for (int i = 0; i < 25426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25426(int& val) {
    const int x = 25426;
    std::cout << 25426;
    for (int i = 0; i < 25426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25427(int& val) {
    func_in_25427(val);
    const int x = 25427;
    std::cout << 25427;
    for (int i = 0; i < 25427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25427(int& val) {
    const int x = 25427;
    std::cout << 25427;
    for (int i = 0; i < 25427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25428(int& val) {
    func_in_25428(val);
    const int x = 25428;
    std::cout << 25428;
    for (int i = 0; i < 25428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25428(int& val) {
    const int x = 25428;
    std::cout << 25428;
    for (int i = 0; i < 25428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25429(int& val) {
    func_in_25429(val);
    const int x = 25429;
    std::cout << 25429;
    for (int i = 0; i < 25429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25429(int& val) {
    const int x = 25429;
    std::cout << 25429;
    for (int i = 0; i < 25429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25430(int& val) {
    func_in_25430(val);
    const int x = 25430;
    std::cout << 25430;
    for (int i = 0; i < 25430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25430(int& val) {
    const int x = 25430;
    std::cout << 25430;
    for (int i = 0; i < 25430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25431(int& val) {
    func_in_25431(val);
    const int x = 25431;
    std::cout << 25431;
    for (int i = 0; i < 25431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25431(int& val) {
    const int x = 25431;
    std::cout << 25431;
    for (int i = 0; i < 25431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25432(int& val) {
    func_in_25432(val);
    const int x = 25432;
    std::cout << 25432;
    for (int i = 0; i < 25432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25432(int& val) {
    const int x = 25432;
    std::cout << 25432;
    for (int i = 0; i < 25432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25433(int& val) {
    func_in_25433(val);
    const int x = 25433;
    std::cout << 25433;
    for (int i = 0; i < 25433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25433(int& val) {
    const int x = 25433;
    std::cout << 25433;
    for (int i = 0; i < 25433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25434(int& val) {
    func_in_25434(val);
    const int x = 25434;
    std::cout << 25434;
    for (int i = 0; i < 25434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25434(int& val) {
    const int x = 25434;
    std::cout << 25434;
    for (int i = 0; i < 25434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25435(int& val) {
    func_in_25435(val);
    const int x = 25435;
    std::cout << 25435;
    for (int i = 0; i < 25435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25435(int& val) {
    const int x = 25435;
    std::cout << 25435;
    for (int i = 0; i < 25435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25436(int& val) {
    func_in_25436(val);
    const int x = 25436;
    std::cout << 25436;
    for (int i = 0; i < 25436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25436(int& val) {
    const int x = 25436;
    std::cout << 25436;
    for (int i = 0; i < 25436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25437(int& val) {
    func_in_25437(val);
    const int x = 25437;
    std::cout << 25437;
    for (int i = 0; i < 25437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25437(int& val) {
    const int x = 25437;
    std::cout << 25437;
    for (int i = 0; i < 25437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25438(int& val) {
    func_in_25438(val);
    const int x = 25438;
    std::cout << 25438;
    for (int i = 0; i < 25438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25438(int& val) {
    const int x = 25438;
    std::cout << 25438;
    for (int i = 0; i < 25438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25439(int& val) {
    func_in_25439(val);
    const int x = 25439;
    std::cout << 25439;
    for (int i = 0; i < 25439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25439(int& val) {
    const int x = 25439;
    std::cout << 25439;
    for (int i = 0; i < 25439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25440(int& val) {
    func_in_25440(val);
    const int x = 25440;
    std::cout << 25440;
    for (int i = 0; i < 25440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25440(int& val) {
    const int x = 25440;
    std::cout << 25440;
    for (int i = 0; i < 25440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25441(int& val) {
    func_in_25441(val);
    const int x = 25441;
    std::cout << 25441;
    for (int i = 0; i < 25441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25441(int& val) {
    const int x = 25441;
    std::cout << 25441;
    for (int i = 0; i < 25441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25442(int& val) {
    func_in_25442(val);
    const int x = 25442;
    std::cout << 25442;
    for (int i = 0; i < 25442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25442(int& val) {
    const int x = 25442;
    std::cout << 25442;
    for (int i = 0; i < 25442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25443(int& val) {
    func_in_25443(val);
    const int x = 25443;
    std::cout << 25443;
    for (int i = 0; i < 25443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25443(int& val) {
    const int x = 25443;
    std::cout << 25443;
    for (int i = 0; i < 25443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25444(int& val) {
    func_in_25444(val);
    const int x = 25444;
    std::cout << 25444;
    for (int i = 0; i < 25444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25444(int& val) {
    const int x = 25444;
    std::cout << 25444;
    for (int i = 0; i < 25444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25445(int& val) {
    func_in_25445(val);
    const int x = 25445;
    std::cout << 25445;
    for (int i = 0; i < 25445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25445(int& val) {
    const int x = 25445;
    std::cout << 25445;
    for (int i = 0; i < 25445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25446(int& val) {
    func_in_25446(val);
    const int x = 25446;
    std::cout << 25446;
    for (int i = 0; i < 25446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25446(int& val) {
    const int x = 25446;
    std::cout << 25446;
    for (int i = 0; i < 25446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25447(int& val) {
    func_in_25447(val);
    const int x = 25447;
    std::cout << 25447;
    for (int i = 0; i < 25447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25447(int& val) {
    const int x = 25447;
    std::cout << 25447;
    for (int i = 0; i < 25447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25448(int& val) {
    func_in_25448(val);
    const int x = 25448;
    std::cout << 25448;
    for (int i = 0; i < 25448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25448(int& val) {
    const int x = 25448;
    std::cout << 25448;
    for (int i = 0; i < 25448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25449(int& val) {
    func_in_25449(val);
    const int x = 25449;
    std::cout << 25449;
    for (int i = 0; i < 25449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25449(int& val) {
    const int x = 25449;
    std::cout << 25449;
    for (int i = 0; i < 25449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25450(int& val) {
    func_in_25450(val);
    const int x = 25450;
    std::cout << 25450;
    for (int i = 0; i < 25450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25450(int& val) {
    const int x = 25450;
    std::cout << 25450;
    for (int i = 0; i < 25450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25451(int& val) {
    func_in_25451(val);
    const int x = 25451;
    std::cout << 25451;
    for (int i = 0; i < 25451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25451(int& val) {
    const int x = 25451;
    std::cout << 25451;
    for (int i = 0; i < 25451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25452(int& val) {
    func_in_25452(val);
    const int x = 25452;
    std::cout << 25452;
    for (int i = 0; i < 25452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25452(int& val) {
    const int x = 25452;
    std::cout << 25452;
    for (int i = 0; i < 25452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25453(int& val) {
    func_in_25453(val);
    const int x = 25453;
    std::cout << 25453;
    for (int i = 0; i < 25453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25453(int& val) {
    const int x = 25453;
    std::cout << 25453;
    for (int i = 0; i < 25453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25454(int& val) {
    func_in_25454(val);
    const int x = 25454;
    std::cout << 25454;
    for (int i = 0; i < 25454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25454(int& val) {
    const int x = 25454;
    std::cout << 25454;
    for (int i = 0; i < 25454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25455(int& val) {
    func_in_25455(val);
    const int x = 25455;
    std::cout << 25455;
    for (int i = 0; i < 25455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25455(int& val) {
    const int x = 25455;
    std::cout << 25455;
    for (int i = 0; i < 25455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25456(int& val) {
    func_in_25456(val);
    const int x = 25456;
    std::cout << 25456;
    for (int i = 0; i < 25456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25456(int& val) {
    const int x = 25456;
    std::cout << 25456;
    for (int i = 0; i < 25456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25457(int& val) {
    func_in_25457(val);
    const int x = 25457;
    std::cout << 25457;
    for (int i = 0; i < 25457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25457(int& val) {
    const int x = 25457;
    std::cout << 25457;
    for (int i = 0; i < 25457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25458(int& val) {
    func_in_25458(val);
    const int x = 25458;
    std::cout << 25458;
    for (int i = 0; i < 25458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25458(int& val) {
    const int x = 25458;
    std::cout << 25458;
    for (int i = 0; i < 25458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25459(int& val) {
    func_in_25459(val);
    const int x = 25459;
    std::cout << 25459;
    for (int i = 0; i < 25459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25459(int& val) {
    const int x = 25459;
    std::cout << 25459;
    for (int i = 0; i < 25459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25460(int& val) {
    func_in_25460(val);
    const int x = 25460;
    std::cout << 25460;
    for (int i = 0; i < 25460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25460(int& val) {
    const int x = 25460;
    std::cout << 25460;
    for (int i = 0; i < 25460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25461(int& val) {
    func_in_25461(val);
    const int x = 25461;
    std::cout << 25461;
    for (int i = 0; i < 25461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25461(int& val) {
    const int x = 25461;
    std::cout << 25461;
    for (int i = 0; i < 25461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25462(int& val) {
    func_in_25462(val);
    const int x = 25462;
    std::cout << 25462;
    for (int i = 0; i < 25462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25462(int& val) {
    const int x = 25462;
    std::cout << 25462;
    for (int i = 0; i < 25462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25463(int& val) {
    func_in_25463(val);
    const int x = 25463;
    std::cout << 25463;
    for (int i = 0; i < 25463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25463(int& val) {
    const int x = 25463;
    std::cout << 25463;
    for (int i = 0; i < 25463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25464(int& val) {
    func_in_25464(val);
    const int x = 25464;
    std::cout << 25464;
    for (int i = 0; i < 25464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25464(int& val) {
    const int x = 25464;
    std::cout << 25464;
    for (int i = 0; i < 25464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25465(int& val) {
    func_in_25465(val);
    const int x = 25465;
    std::cout << 25465;
    for (int i = 0; i < 25465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25465(int& val) {
    const int x = 25465;
    std::cout << 25465;
    for (int i = 0; i < 25465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25466(int& val) {
    func_in_25466(val);
    const int x = 25466;
    std::cout << 25466;
    for (int i = 0; i < 25466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25466(int& val) {
    const int x = 25466;
    std::cout << 25466;
    for (int i = 0; i < 25466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25467(int& val) {
    func_in_25467(val);
    const int x = 25467;
    std::cout << 25467;
    for (int i = 0; i < 25467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25467(int& val) {
    const int x = 25467;
    std::cout << 25467;
    for (int i = 0; i < 25467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25468(int& val) {
    func_in_25468(val);
    const int x = 25468;
    std::cout << 25468;
    for (int i = 0; i < 25468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25468(int& val) {
    const int x = 25468;
    std::cout << 25468;
    for (int i = 0; i < 25468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25469(int& val) {
    func_in_25469(val);
    const int x = 25469;
    std::cout << 25469;
    for (int i = 0; i < 25469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25469(int& val) {
    const int x = 25469;
    std::cout << 25469;
    for (int i = 0; i < 25469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25470(int& val) {
    func_in_25470(val);
    const int x = 25470;
    std::cout << 25470;
    for (int i = 0; i < 25470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25470(int& val) {
    const int x = 25470;
    std::cout << 25470;
    for (int i = 0; i < 25470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25471(int& val) {
    func_in_25471(val);
    const int x = 25471;
    std::cout << 25471;
    for (int i = 0; i < 25471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25471(int& val) {
    const int x = 25471;
    std::cout << 25471;
    for (int i = 0; i < 25471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25472(int& val) {
    func_in_25472(val);
    const int x = 25472;
    std::cout << 25472;
    for (int i = 0; i < 25472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25472(int& val) {
    const int x = 25472;
    std::cout << 25472;
    for (int i = 0; i < 25472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25473(int& val) {
    func_in_25473(val);
    const int x = 25473;
    std::cout << 25473;
    for (int i = 0; i < 25473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25473(int& val) {
    const int x = 25473;
    std::cout << 25473;
    for (int i = 0; i < 25473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25474(int& val) {
    func_in_25474(val);
    const int x = 25474;
    std::cout << 25474;
    for (int i = 0; i < 25474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25474(int& val) {
    const int x = 25474;
    std::cout << 25474;
    for (int i = 0; i < 25474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25475(int& val) {
    func_in_25475(val);
    const int x = 25475;
    std::cout << 25475;
    for (int i = 0; i < 25475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25475(int& val) {
    const int x = 25475;
    std::cout << 25475;
    for (int i = 0; i < 25475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25476(int& val) {
    func_in_25476(val);
    const int x = 25476;
    std::cout << 25476;
    for (int i = 0; i < 25476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25476(int& val) {
    const int x = 25476;
    std::cout << 25476;
    for (int i = 0; i < 25476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25477(int& val) {
    func_in_25477(val);
    const int x = 25477;
    std::cout << 25477;
    for (int i = 0; i < 25477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25477(int& val) {
    const int x = 25477;
    std::cout << 25477;
    for (int i = 0; i < 25477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25478(int& val) {
    func_in_25478(val);
    const int x = 25478;
    std::cout << 25478;
    for (int i = 0; i < 25478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25478(int& val) {
    const int x = 25478;
    std::cout << 25478;
    for (int i = 0; i < 25478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25479(int& val) {
    func_in_25479(val);
    const int x = 25479;
    std::cout << 25479;
    for (int i = 0; i < 25479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25479(int& val) {
    const int x = 25479;
    std::cout << 25479;
    for (int i = 0; i < 25479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25480(int& val) {
    func_in_25480(val);
    const int x = 25480;
    std::cout << 25480;
    for (int i = 0; i < 25480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25480(int& val) {
    const int x = 25480;
    std::cout << 25480;
    for (int i = 0; i < 25480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25481(int& val) {
    func_in_25481(val);
    const int x = 25481;
    std::cout << 25481;
    for (int i = 0; i < 25481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25481(int& val) {
    const int x = 25481;
    std::cout << 25481;
    for (int i = 0; i < 25481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25482(int& val) {
    func_in_25482(val);
    const int x = 25482;
    std::cout << 25482;
    for (int i = 0; i < 25482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25482(int& val) {
    const int x = 25482;
    std::cout << 25482;
    for (int i = 0; i < 25482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25483(int& val) {
    func_in_25483(val);
    const int x = 25483;
    std::cout << 25483;
    for (int i = 0; i < 25483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25483(int& val) {
    const int x = 25483;
    std::cout << 25483;
    for (int i = 0; i < 25483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25484(int& val) {
    func_in_25484(val);
    const int x = 25484;
    std::cout << 25484;
    for (int i = 0; i < 25484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25484(int& val) {
    const int x = 25484;
    std::cout << 25484;
    for (int i = 0; i < 25484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25485(int& val) {
    func_in_25485(val);
    const int x = 25485;
    std::cout << 25485;
    for (int i = 0; i < 25485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25485(int& val) {
    const int x = 25485;
    std::cout << 25485;
    for (int i = 0; i < 25485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25486(int& val) {
    func_in_25486(val);
    const int x = 25486;
    std::cout << 25486;
    for (int i = 0; i < 25486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25486(int& val) {
    const int x = 25486;
    std::cout << 25486;
    for (int i = 0; i < 25486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25487(int& val) {
    func_in_25487(val);
    const int x = 25487;
    std::cout << 25487;
    for (int i = 0; i < 25487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25487(int& val) {
    const int x = 25487;
    std::cout << 25487;
    for (int i = 0; i < 25487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25488(int& val) {
    func_in_25488(val);
    const int x = 25488;
    std::cout << 25488;
    for (int i = 0; i < 25488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25488(int& val) {
    const int x = 25488;
    std::cout << 25488;
    for (int i = 0; i < 25488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25489(int& val) {
    func_in_25489(val);
    const int x = 25489;
    std::cout << 25489;
    for (int i = 0; i < 25489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25489(int& val) {
    const int x = 25489;
    std::cout << 25489;
    for (int i = 0; i < 25489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25490(int& val) {
    func_in_25490(val);
    const int x = 25490;
    std::cout << 25490;
    for (int i = 0; i < 25490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25490(int& val) {
    const int x = 25490;
    std::cout << 25490;
    for (int i = 0; i < 25490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25491(int& val) {
    func_in_25491(val);
    const int x = 25491;
    std::cout << 25491;
    for (int i = 0; i < 25491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25491(int& val) {
    const int x = 25491;
    std::cout << 25491;
    for (int i = 0; i < 25491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25492(int& val) {
    func_in_25492(val);
    const int x = 25492;
    std::cout << 25492;
    for (int i = 0; i < 25492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25492(int& val) {
    const int x = 25492;
    std::cout << 25492;
    for (int i = 0; i < 25492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25493(int& val) {
    func_in_25493(val);
    const int x = 25493;
    std::cout << 25493;
    for (int i = 0; i < 25493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25493(int& val) {
    const int x = 25493;
    std::cout << 25493;
    for (int i = 0; i < 25493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25494(int& val) {
    func_in_25494(val);
    const int x = 25494;
    std::cout << 25494;
    for (int i = 0; i < 25494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25494(int& val) {
    const int x = 25494;
    std::cout << 25494;
    for (int i = 0; i < 25494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25495(int& val) {
    func_in_25495(val);
    const int x = 25495;
    std::cout << 25495;
    for (int i = 0; i < 25495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25495(int& val) {
    const int x = 25495;
    std::cout << 25495;
    for (int i = 0; i < 25495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25496(int& val) {
    func_in_25496(val);
    const int x = 25496;
    std::cout << 25496;
    for (int i = 0; i < 25496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25496(int& val) {
    const int x = 25496;
    std::cout << 25496;
    for (int i = 0; i < 25496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25497(int& val) {
    func_in_25497(val);
    const int x = 25497;
    std::cout << 25497;
    for (int i = 0; i < 25497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25497(int& val) {
    const int x = 25497;
    std::cout << 25497;
    for (int i = 0; i < 25497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25498(int& val) {
    func_in_25498(val);
    const int x = 25498;
    std::cout << 25498;
    for (int i = 0; i < 25498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25498(int& val) {
    const int x = 25498;
    std::cout << 25498;
    for (int i = 0; i < 25498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25499(int& val) {
    func_in_25499(val);
    const int x = 25499;
    std::cout << 25499;
    for (int i = 0; i < 25499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25499(int& val) {
    const int x = 25499;
    std::cout << 25499;
    for (int i = 0; i < 25499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25500(int& val) {
    func_in_25500(val);
    const int x = 25500;
    std::cout << 25500;
    for (int i = 0; i < 25500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25500(int& val) {
    const int x = 25500;
    std::cout << 25500;
    for (int i = 0; i < 25500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25501(int& val) {
    func_in_25501(val);
    const int x = 25501;
    std::cout << 25501;
    for (int i = 0; i < 25501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25501(int& val) {
    const int x = 25501;
    std::cout << 25501;
    for (int i = 0; i < 25501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25502(int& val) {
    func_in_25502(val);
    const int x = 25502;
    std::cout << 25502;
    for (int i = 0; i < 25502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25502(int& val) {
    const int x = 25502;
    std::cout << 25502;
    for (int i = 0; i < 25502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25503(int& val) {
    func_in_25503(val);
    const int x = 25503;
    std::cout << 25503;
    for (int i = 0; i < 25503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25503(int& val) {
    const int x = 25503;
    std::cout << 25503;
    for (int i = 0; i < 25503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25504(int& val) {
    func_in_25504(val);
    const int x = 25504;
    std::cout << 25504;
    for (int i = 0; i < 25504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25504(int& val) {
    const int x = 25504;
    std::cout << 25504;
    for (int i = 0; i < 25504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25505(int& val) {
    func_in_25505(val);
    const int x = 25505;
    std::cout << 25505;
    for (int i = 0; i < 25505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25505(int& val) {
    const int x = 25505;
    std::cout << 25505;
    for (int i = 0; i < 25505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25506(int& val) {
    func_in_25506(val);
    const int x = 25506;
    std::cout << 25506;
    for (int i = 0; i < 25506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25506(int& val) {
    const int x = 25506;
    std::cout << 25506;
    for (int i = 0; i < 25506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25507(int& val) {
    func_in_25507(val);
    const int x = 25507;
    std::cout << 25507;
    for (int i = 0; i < 25507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25507(int& val) {
    const int x = 25507;
    std::cout << 25507;
    for (int i = 0; i < 25507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25508(int& val) {
    func_in_25508(val);
    const int x = 25508;
    std::cout << 25508;
    for (int i = 0; i < 25508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25508(int& val) {
    const int x = 25508;
    std::cout << 25508;
    for (int i = 0; i < 25508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25509(int& val) {
    func_in_25509(val);
    const int x = 25509;
    std::cout << 25509;
    for (int i = 0; i < 25509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25509(int& val) {
    const int x = 25509;
    std::cout << 25509;
    for (int i = 0; i < 25509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25510(int& val) {
    func_in_25510(val);
    const int x = 25510;
    std::cout << 25510;
    for (int i = 0; i < 25510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25510(int& val) {
    const int x = 25510;
    std::cout << 25510;
    for (int i = 0; i < 25510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25511(int& val) {
    func_in_25511(val);
    const int x = 25511;
    std::cout << 25511;
    for (int i = 0; i < 25511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25511(int& val) {
    const int x = 25511;
    std::cout << 25511;
    for (int i = 0; i < 25511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25512(int& val) {
    func_in_25512(val);
    const int x = 25512;
    std::cout << 25512;
    for (int i = 0; i < 25512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25512(int& val) {
    const int x = 25512;
    std::cout << 25512;
    for (int i = 0; i < 25512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25513(int& val) {
    func_in_25513(val);
    const int x = 25513;
    std::cout << 25513;
    for (int i = 0; i < 25513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25513(int& val) {
    const int x = 25513;
    std::cout << 25513;
    for (int i = 0; i < 25513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25514(int& val) {
    func_in_25514(val);
    const int x = 25514;
    std::cout << 25514;
    for (int i = 0; i < 25514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25514(int& val) {
    const int x = 25514;
    std::cout << 25514;
    for (int i = 0; i < 25514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25515(int& val) {
    func_in_25515(val);
    const int x = 25515;
    std::cout << 25515;
    for (int i = 0; i < 25515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25515(int& val) {
    const int x = 25515;
    std::cout << 25515;
    for (int i = 0; i < 25515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25516(int& val) {
    func_in_25516(val);
    const int x = 25516;
    std::cout << 25516;
    for (int i = 0; i < 25516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25516(int& val) {
    const int x = 25516;
    std::cout << 25516;
    for (int i = 0; i < 25516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25517(int& val) {
    func_in_25517(val);
    const int x = 25517;
    std::cout << 25517;
    for (int i = 0; i < 25517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25517(int& val) {
    const int x = 25517;
    std::cout << 25517;
    for (int i = 0; i < 25517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25518(int& val) {
    func_in_25518(val);
    const int x = 25518;
    std::cout << 25518;
    for (int i = 0; i < 25518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25518(int& val) {
    const int x = 25518;
    std::cout << 25518;
    for (int i = 0; i < 25518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25519(int& val) {
    func_in_25519(val);
    const int x = 25519;
    std::cout << 25519;
    for (int i = 0; i < 25519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25519(int& val) {
    const int x = 25519;
    std::cout << 25519;
    for (int i = 0; i < 25519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25520(int& val) {
    func_in_25520(val);
    const int x = 25520;
    std::cout << 25520;
    for (int i = 0; i < 25520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25520(int& val) {
    const int x = 25520;
    std::cout << 25520;
    for (int i = 0; i < 25520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25521(int& val) {
    func_in_25521(val);
    const int x = 25521;
    std::cout << 25521;
    for (int i = 0; i < 25521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25521(int& val) {
    const int x = 25521;
    std::cout << 25521;
    for (int i = 0; i < 25521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25522(int& val) {
    func_in_25522(val);
    const int x = 25522;
    std::cout << 25522;
    for (int i = 0; i < 25522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25522(int& val) {
    const int x = 25522;
    std::cout << 25522;
    for (int i = 0; i < 25522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25523(int& val) {
    func_in_25523(val);
    const int x = 25523;
    std::cout << 25523;
    for (int i = 0; i < 25523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25523(int& val) {
    const int x = 25523;
    std::cout << 25523;
    for (int i = 0; i < 25523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25524(int& val) {
    func_in_25524(val);
    const int x = 25524;
    std::cout << 25524;
    for (int i = 0; i < 25524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25524(int& val) {
    const int x = 25524;
    std::cout << 25524;
    for (int i = 0; i < 25524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25525(int& val) {
    func_in_25525(val);
    const int x = 25525;
    std::cout << 25525;
    for (int i = 0; i < 25525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25525(int& val) {
    const int x = 25525;
    std::cout << 25525;
    for (int i = 0; i < 25525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25526(int& val) {
    func_in_25526(val);
    const int x = 25526;
    std::cout << 25526;
    for (int i = 0; i < 25526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25526(int& val) {
    const int x = 25526;
    std::cout << 25526;
    for (int i = 0; i < 25526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25527(int& val) {
    func_in_25527(val);
    const int x = 25527;
    std::cout << 25527;
    for (int i = 0; i < 25527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25527(int& val) {
    const int x = 25527;
    std::cout << 25527;
    for (int i = 0; i < 25527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25528(int& val) {
    func_in_25528(val);
    const int x = 25528;
    std::cout << 25528;
    for (int i = 0; i < 25528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25528(int& val) {
    const int x = 25528;
    std::cout << 25528;
    for (int i = 0; i < 25528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25529(int& val) {
    func_in_25529(val);
    const int x = 25529;
    std::cout << 25529;
    for (int i = 0; i < 25529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25529(int& val) {
    const int x = 25529;
    std::cout << 25529;
    for (int i = 0; i < 25529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25530(int& val) {
    func_in_25530(val);
    const int x = 25530;
    std::cout << 25530;
    for (int i = 0; i < 25530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25530(int& val) {
    const int x = 25530;
    std::cout << 25530;
    for (int i = 0; i < 25530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25531(int& val) {
    func_in_25531(val);
    const int x = 25531;
    std::cout << 25531;
    for (int i = 0; i < 25531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25531(int& val) {
    const int x = 25531;
    std::cout << 25531;
    for (int i = 0; i < 25531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25532(int& val) {
    func_in_25532(val);
    const int x = 25532;
    std::cout << 25532;
    for (int i = 0; i < 25532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25532(int& val) {
    const int x = 25532;
    std::cout << 25532;
    for (int i = 0; i < 25532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25533(int& val) {
    func_in_25533(val);
    const int x = 25533;
    std::cout << 25533;
    for (int i = 0; i < 25533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25533(int& val) {
    const int x = 25533;
    std::cout << 25533;
    for (int i = 0; i < 25533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25534(int& val) {
    func_in_25534(val);
    const int x = 25534;
    std::cout << 25534;
    for (int i = 0; i < 25534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25534(int& val) {
    const int x = 25534;
    std::cout << 25534;
    for (int i = 0; i < 25534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25535(int& val) {
    func_in_25535(val);
    const int x = 25535;
    std::cout << 25535;
    for (int i = 0; i < 25535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25535(int& val) {
    const int x = 25535;
    std::cout << 25535;
    for (int i = 0; i < 25535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25536(int& val) {
    func_in_25536(val);
    const int x = 25536;
    std::cout << 25536;
    for (int i = 0; i < 25536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25536(int& val) {
    const int x = 25536;
    std::cout << 25536;
    for (int i = 0; i < 25536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25537(int& val) {
    func_in_25537(val);
    const int x = 25537;
    std::cout << 25537;
    for (int i = 0; i < 25537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25537(int& val) {
    const int x = 25537;
    std::cout << 25537;
    for (int i = 0; i < 25537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25538(int& val) {
    func_in_25538(val);
    const int x = 25538;
    std::cout << 25538;
    for (int i = 0; i < 25538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25538(int& val) {
    const int x = 25538;
    std::cout << 25538;
    for (int i = 0; i < 25538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25539(int& val) {
    func_in_25539(val);
    const int x = 25539;
    std::cout << 25539;
    for (int i = 0; i < 25539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25539(int& val) {
    const int x = 25539;
    std::cout << 25539;
    for (int i = 0; i < 25539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25540(int& val) {
    func_in_25540(val);
    const int x = 25540;
    std::cout << 25540;
    for (int i = 0; i < 25540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25540(int& val) {
    const int x = 25540;
    std::cout << 25540;
    for (int i = 0; i < 25540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25541(int& val) {
    func_in_25541(val);
    const int x = 25541;
    std::cout << 25541;
    for (int i = 0; i < 25541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25541(int& val) {
    const int x = 25541;
    std::cout << 25541;
    for (int i = 0; i < 25541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25542(int& val) {
    func_in_25542(val);
    const int x = 25542;
    std::cout << 25542;
    for (int i = 0; i < 25542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25542(int& val) {
    const int x = 25542;
    std::cout << 25542;
    for (int i = 0; i < 25542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25543(int& val) {
    func_in_25543(val);
    const int x = 25543;
    std::cout << 25543;
    for (int i = 0; i < 25543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25543(int& val) {
    const int x = 25543;
    std::cout << 25543;
    for (int i = 0; i < 25543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25544(int& val) {
    func_in_25544(val);
    const int x = 25544;
    std::cout << 25544;
    for (int i = 0; i < 25544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25544(int& val) {
    const int x = 25544;
    std::cout << 25544;
    for (int i = 0; i < 25544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25545(int& val) {
    func_in_25545(val);
    const int x = 25545;
    std::cout << 25545;
    for (int i = 0; i < 25545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25545(int& val) {
    const int x = 25545;
    std::cout << 25545;
    for (int i = 0; i < 25545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25546(int& val) {
    func_in_25546(val);
    const int x = 25546;
    std::cout << 25546;
    for (int i = 0; i < 25546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25546(int& val) {
    const int x = 25546;
    std::cout << 25546;
    for (int i = 0; i < 25546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25547(int& val) {
    func_in_25547(val);
    const int x = 25547;
    std::cout << 25547;
    for (int i = 0; i < 25547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25547(int& val) {
    const int x = 25547;
    std::cout << 25547;
    for (int i = 0; i < 25547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25548(int& val) {
    func_in_25548(val);
    const int x = 25548;
    std::cout << 25548;
    for (int i = 0; i < 25548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25548(int& val) {
    const int x = 25548;
    std::cout << 25548;
    for (int i = 0; i < 25548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25549(int& val) {
    func_in_25549(val);
    const int x = 25549;
    std::cout << 25549;
    for (int i = 0; i < 25549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25549(int& val) {
    const int x = 25549;
    std::cout << 25549;
    for (int i = 0; i < 25549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25550(int& val) {
    func_in_25550(val);
    const int x = 25550;
    std::cout << 25550;
    for (int i = 0; i < 25550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25550(int& val) {
    const int x = 25550;
    std::cout << 25550;
    for (int i = 0; i < 25550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25551(int& val) {
    func_in_25551(val);
    const int x = 25551;
    std::cout << 25551;
    for (int i = 0; i < 25551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25551(int& val) {
    const int x = 25551;
    std::cout << 25551;
    for (int i = 0; i < 25551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25552(int& val) {
    func_in_25552(val);
    const int x = 25552;
    std::cout << 25552;
    for (int i = 0; i < 25552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25552(int& val) {
    const int x = 25552;
    std::cout << 25552;
    for (int i = 0; i < 25552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25553(int& val) {
    func_in_25553(val);
    const int x = 25553;
    std::cout << 25553;
    for (int i = 0; i < 25553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25553(int& val) {
    const int x = 25553;
    std::cout << 25553;
    for (int i = 0; i < 25553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25554(int& val) {
    func_in_25554(val);
    const int x = 25554;
    std::cout << 25554;
    for (int i = 0; i < 25554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25554(int& val) {
    const int x = 25554;
    std::cout << 25554;
    for (int i = 0; i < 25554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25555(int& val) {
    func_in_25555(val);
    const int x = 25555;
    std::cout << 25555;
    for (int i = 0; i < 25555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25555(int& val) {
    const int x = 25555;
    std::cout << 25555;
    for (int i = 0; i < 25555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25556(int& val) {
    func_in_25556(val);
    const int x = 25556;
    std::cout << 25556;
    for (int i = 0; i < 25556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25556(int& val) {
    const int x = 25556;
    std::cout << 25556;
    for (int i = 0; i < 25556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25557(int& val) {
    func_in_25557(val);
    const int x = 25557;
    std::cout << 25557;
    for (int i = 0; i < 25557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25557(int& val) {
    const int x = 25557;
    std::cout << 25557;
    for (int i = 0; i < 25557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25558(int& val) {
    func_in_25558(val);
    const int x = 25558;
    std::cout << 25558;
    for (int i = 0; i < 25558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25558(int& val) {
    const int x = 25558;
    std::cout << 25558;
    for (int i = 0; i < 25558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25559(int& val) {
    func_in_25559(val);
    const int x = 25559;
    std::cout << 25559;
    for (int i = 0; i < 25559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25559(int& val) {
    const int x = 25559;
    std::cout << 25559;
    for (int i = 0; i < 25559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25560(int& val) {
    func_in_25560(val);
    const int x = 25560;
    std::cout << 25560;
    for (int i = 0; i < 25560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25560(int& val) {
    const int x = 25560;
    std::cout << 25560;
    for (int i = 0; i < 25560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25561(int& val) {
    func_in_25561(val);
    const int x = 25561;
    std::cout << 25561;
    for (int i = 0; i < 25561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25561(int& val) {
    const int x = 25561;
    std::cout << 25561;
    for (int i = 0; i < 25561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25562(int& val) {
    func_in_25562(val);
    const int x = 25562;
    std::cout << 25562;
    for (int i = 0; i < 25562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25562(int& val) {
    const int x = 25562;
    std::cout << 25562;
    for (int i = 0; i < 25562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25563(int& val) {
    func_in_25563(val);
    const int x = 25563;
    std::cout << 25563;
    for (int i = 0; i < 25563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25563(int& val) {
    const int x = 25563;
    std::cout << 25563;
    for (int i = 0; i < 25563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25564(int& val) {
    func_in_25564(val);
    const int x = 25564;
    std::cout << 25564;
    for (int i = 0; i < 25564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25564(int& val) {
    const int x = 25564;
    std::cout << 25564;
    for (int i = 0; i < 25564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25565(int& val) {
    func_in_25565(val);
    const int x = 25565;
    std::cout << 25565;
    for (int i = 0; i < 25565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25565(int& val) {
    const int x = 25565;
    std::cout << 25565;
    for (int i = 0; i < 25565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25566(int& val) {
    func_in_25566(val);
    const int x = 25566;
    std::cout << 25566;
    for (int i = 0; i < 25566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25566(int& val) {
    const int x = 25566;
    std::cout << 25566;
    for (int i = 0; i < 25566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25567(int& val) {
    func_in_25567(val);
    const int x = 25567;
    std::cout << 25567;
    for (int i = 0; i < 25567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25567(int& val) {
    const int x = 25567;
    std::cout << 25567;
    for (int i = 0; i < 25567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25568(int& val) {
    func_in_25568(val);
    const int x = 25568;
    std::cout << 25568;
    for (int i = 0; i < 25568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25568(int& val) {
    const int x = 25568;
    std::cout << 25568;
    for (int i = 0; i < 25568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25569(int& val) {
    func_in_25569(val);
    const int x = 25569;
    std::cout << 25569;
    for (int i = 0; i < 25569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25569(int& val) {
    const int x = 25569;
    std::cout << 25569;
    for (int i = 0; i < 25569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25570(int& val) {
    func_in_25570(val);
    const int x = 25570;
    std::cout << 25570;
    for (int i = 0; i < 25570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25570(int& val) {
    const int x = 25570;
    std::cout << 25570;
    for (int i = 0; i < 25570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25571(int& val) {
    func_in_25571(val);
    const int x = 25571;
    std::cout << 25571;
    for (int i = 0; i < 25571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25571(int& val) {
    const int x = 25571;
    std::cout << 25571;
    for (int i = 0; i < 25571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25572(int& val) {
    func_in_25572(val);
    const int x = 25572;
    std::cout << 25572;
    for (int i = 0; i < 25572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25572(int& val) {
    const int x = 25572;
    std::cout << 25572;
    for (int i = 0; i < 25572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25573(int& val) {
    func_in_25573(val);
    const int x = 25573;
    std::cout << 25573;
    for (int i = 0; i < 25573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25573(int& val) {
    const int x = 25573;
    std::cout << 25573;
    for (int i = 0; i < 25573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25574(int& val) {
    func_in_25574(val);
    const int x = 25574;
    std::cout << 25574;
    for (int i = 0; i < 25574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25574(int& val) {
    const int x = 25574;
    std::cout << 25574;
    for (int i = 0; i < 25574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25575(int& val) {
    func_in_25575(val);
    const int x = 25575;
    std::cout << 25575;
    for (int i = 0; i < 25575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25575(int& val) {
    const int x = 25575;
    std::cout << 25575;
    for (int i = 0; i < 25575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25576(int& val) {
    func_in_25576(val);
    const int x = 25576;
    std::cout << 25576;
    for (int i = 0; i < 25576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25576(int& val) {
    const int x = 25576;
    std::cout << 25576;
    for (int i = 0; i < 25576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25577(int& val) {
    func_in_25577(val);
    const int x = 25577;
    std::cout << 25577;
    for (int i = 0; i < 25577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25577(int& val) {
    const int x = 25577;
    std::cout << 25577;
    for (int i = 0; i < 25577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25578(int& val) {
    func_in_25578(val);
    const int x = 25578;
    std::cout << 25578;
    for (int i = 0; i < 25578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25578(int& val) {
    const int x = 25578;
    std::cout << 25578;
    for (int i = 0; i < 25578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25579(int& val) {
    func_in_25579(val);
    const int x = 25579;
    std::cout << 25579;
    for (int i = 0; i < 25579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25579(int& val) {
    const int x = 25579;
    std::cout << 25579;
    for (int i = 0; i < 25579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25580(int& val) {
    func_in_25580(val);
    const int x = 25580;
    std::cout << 25580;
    for (int i = 0; i < 25580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25580(int& val) {
    const int x = 25580;
    std::cout << 25580;
    for (int i = 0; i < 25580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25581(int& val) {
    func_in_25581(val);
    const int x = 25581;
    std::cout << 25581;
    for (int i = 0; i < 25581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25581(int& val) {
    const int x = 25581;
    std::cout << 25581;
    for (int i = 0; i < 25581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25582(int& val) {
    func_in_25582(val);
    const int x = 25582;
    std::cout << 25582;
    for (int i = 0; i < 25582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25582(int& val) {
    const int x = 25582;
    std::cout << 25582;
    for (int i = 0; i < 25582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25583(int& val) {
    func_in_25583(val);
    const int x = 25583;
    std::cout << 25583;
    for (int i = 0; i < 25583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25583(int& val) {
    const int x = 25583;
    std::cout << 25583;
    for (int i = 0; i < 25583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25584(int& val) {
    func_in_25584(val);
    const int x = 25584;
    std::cout << 25584;
    for (int i = 0; i < 25584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25584(int& val) {
    const int x = 25584;
    std::cout << 25584;
    for (int i = 0; i < 25584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25585(int& val) {
    func_in_25585(val);
    const int x = 25585;
    std::cout << 25585;
    for (int i = 0; i < 25585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25585(int& val) {
    const int x = 25585;
    std::cout << 25585;
    for (int i = 0; i < 25585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25586(int& val) {
    func_in_25586(val);
    const int x = 25586;
    std::cout << 25586;
    for (int i = 0; i < 25586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25586(int& val) {
    const int x = 25586;
    std::cout << 25586;
    for (int i = 0; i < 25586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25587(int& val) {
    func_in_25587(val);
    const int x = 25587;
    std::cout << 25587;
    for (int i = 0; i < 25587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25587(int& val) {
    const int x = 25587;
    std::cout << 25587;
    for (int i = 0; i < 25587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25588(int& val) {
    func_in_25588(val);
    const int x = 25588;
    std::cout << 25588;
    for (int i = 0; i < 25588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25588(int& val) {
    const int x = 25588;
    std::cout << 25588;
    for (int i = 0; i < 25588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25589(int& val) {
    func_in_25589(val);
    const int x = 25589;
    std::cout << 25589;
    for (int i = 0; i < 25589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25589(int& val) {
    const int x = 25589;
    std::cout << 25589;
    for (int i = 0; i < 25589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25590(int& val) {
    func_in_25590(val);
    const int x = 25590;
    std::cout << 25590;
    for (int i = 0; i < 25590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25590(int& val) {
    const int x = 25590;
    std::cout << 25590;
    for (int i = 0; i < 25590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25591(int& val) {
    func_in_25591(val);
    const int x = 25591;
    std::cout << 25591;
    for (int i = 0; i < 25591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25591(int& val) {
    const int x = 25591;
    std::cout << 25591;
    for (int i = 0; i < 25591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25592(int& val) {
    func_in_25592(val);
    const int x = 25592;
    std::cout << 25592;
    for (int i = 0; i < 25592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25592(int& val) {
    const int x = 25592;
    std::cout << 25592;
    for (int i = 0; i < 25592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25593(int& val) {
    func_in_25593(val);
    const int x = 25593;
    std::cout << 25593;
    for (int i = 0; i < 25593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25593(int& val) {
    const int x = 25593;
    std::cout << 25593;
    for (int i = 0; i < 25593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25594(int& val) {
    func_in_25594(val);
    const int x = 25594;
    std::cout << 25594;
    for (int i = 0; i < 25594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25594(int& val) {
    const int x = 25594;
    std::cout << 25594;
    for (int i = 0; i < 25594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25595(int& val) {
    func_in_25595(val);
    const int x = 25595;
    std::cout << 25595;
    for (int i = 0; i < 25595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25595(int& val) {
    const int x = 25595;
    std::cout << 25595;
    for (int i = 0; i < 25595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25596(int& val) {
    func_in_25596(val);
    const int x = 25596;
    std::cout << 25596;
    for (int i = 0; i < 25596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25596(int& val) {
    const int x = 25596;
    std::cout << 25596;
    for (int i = 0; i < 25596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25597(int& val) {
    func_in_25597(val);
    const int x = 25597;
    std::cout << 25597;
    for (int i = 0; i < 25597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25597(int& val) {
    const int x = 25597;
    std::cout << 25597;
    for (int i = 0; i < 25597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25598(int& val) {
    func_in_25598(val);
    const int x = 25598;
    std::cout << 25598;
    for (int i = 0; i < 25598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25598(int& val) {
    const int x = 25598;
    std::cout << 25598;
    for (int i = 0; i < 25598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25599(int& val) {
    func_in_25599(val);
    const int x = 25599;
    std::cout << 25599;
    for (int i = 0; i < 25599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25599(int& val) {
    const int x = 25599;
    std::cout << 25599;
    for (int i = 0; i < 25599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25600(int& val) {
    func_in_25600(val);
    const int x = 25600;
    std::cout << 25600;
    for (int i = 0; i < 25600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25600(int& val) {
    const int x = 25600;
    std::cout << 25600;
    for (int i = 0; i < 25600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25601(int& val) {
    func_in_25601(val);
    const int x = 25601;
    std::cout << 25601;
    for (int i = 0; i < 25601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25601(int& val) {
    const int x = 25601;
    std::cout << 25601;
    for (int i = 0; i < 25601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25602(int& val) {
    func_in_25602(val);
    const int x = 25602;
    std::cout << 25602;
    for (int i = 0; i < 25602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25602(int& val) {
    const int x = 25602;
    std::cout << 25602;
    for (int i = 0; i < 25602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25603(int& val) {
    func_in_25603(val);
    const int x = 25603;
    std::cout << 25603;
    for (int i = 0; i < 25603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25603(int& val) {
    const int x = 25603;
    std::cout << 25603;
    for (int i = 0; i < 25603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25604(int& val) {
    func_in_25604(val);
    const int x = 25604;
    std::cout << 25604;
    for (int i = 0; i < 25604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25604(int& val) {
    const int x = 25604;
    std::cout << 25604;
    for (int i = 0; i < 25604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25605(int& val) {
    func_in_25605(val);
    const int x = 25605;
    std::cout << 25605;
    for (int i = 0; i < 25605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25605(int& val) {
    const int x = 25605;
    std::cout << 25605;
    for (int i = 0; i < 25605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25606(int& val) {
    func_in_25606(val);
    const int x = 25606;
    std::cout << 25606;
    for (int i = 0; i < 25606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25606(int& val) {
    const int x = 25606;
    std::cout << 25606;
    for (int i = 0; i < 25606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25607(int& val) {
    func_in_25607(val);
    const int x = 25607;
    std::cout << 25607;
    for (int i = 0; i < 25607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25607(int& val) {
    const int x = 25607;
    std::cout << 25607;
    for (int i = 0; i < 25607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25608(int& val) {
    func_in_25608(val);
    const int x = 25608;
    std::cout << 25608;
    for (int i = 0; i < 25608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25608(int& val) {
    const int x = 25608;
    std::cout << 25608;
    for (int i = 0; i < 25608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25609(int& val) {
    func_in_25609(val);
    const int x = 25609;
    std::cout << 25609;
    for (int i = 0; i < 25609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25609(int& val) {
    const int x = 25609;
    std::cout << 25609;
    for (int i = 0; i < 25609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25610(int& val) {
    func_in_25610(val);
    const int x = 25610;
    std::cout << 25610;
    for (int i = 0; i < 25610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25610(int& val) {
    const int x = 25610;
    std::cout << 25610;
    for (int i = 0; i < 25610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25611(int& val) {
    func_in_25611(val);
    const int x = 25611;
    std::cout << 25611;
    for (int i = 0; i < 25611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25611(int& val) {
    const int x = 25611;
    std::cout << 25611;
    for (int i = 0; i < 25611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25612(int& val) {
    func_in_25612(val);
    const int x = 25612;
    std::cout << 25612;
    for (int i = 0; i < 25612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25612(int& val) {
    const int x = 25612;
    std::cout << 25612;
    for (int i = 0; i < 25612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25613(int& val) {
    func_in_25613(val);
    const int x = 25613;
    std::cout << 25613;
    for (int i = 0; i < 25613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25613(int& val) {
    const int x = 25613;
    std::cout << 25613;
    for (int i = 0; i < 25613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25614(int& val) {
    func_in_25614(val);
    const int x = 25614;
    std::cout << 25614;
    for (int i = 0; i < 25614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25614(int& val) {
    const int x = 25614;
    std::cout << 25614;
    for (int i = 0; i < 25614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25615(int& val) {
    func_in_25615(val);
    const int x = 25615;
    std::cout << 25615;
    for (int i = 0; i < 25615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25615(int& val) {
    const int x = 25615;
    std::cout << 25615;
    for (int i = 0; i < 25615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25616(int& val) {
    func_in_25616(val);
    const int x = 25616;
    std::cout << 25616;
    for (int i = 0; i < 25616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25616(int& val) {
    const int x = 25616;
    std::cout << 25616;
    for (int i = 0; i < 25616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25617(int& val) {
    func_in_25617(val);
    const int x = 25617;
    std::cout << 25617;
    for (int i = 0; i < 25617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25617(int& val) {
    const int x = 25617;
    std::cout << 25617;
    for (int i = 0; i < 25617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25618(int& val) {
    func_in_25618(val);
    const int x = 25618;
    std::cout << 25618;
    for (int i = 0; i < 25618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25618(int& val) {
    const int x = 25618;
    std::cout << 25618;
    for (int i = 0; i < 25618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25619(int& val) {
    func_in_25619(val);
    const int x = 25619;
    std::cout << 25619;
    for (int i = 0; i < 25619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25619(int& val) {
    const int x = 25619;
    std::cout << 25619;
    for (int i = 0; i < 25619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25620(int& val) {
    func_in_25620(val);
    const int x = 25620;
    std::cout << 25620;
    for (int i = 0; i < 25620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25620(int& val) {
    const int x = 25620;
    std::cout << 25620;
    for (int i = 0; i < 25620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25621(int& val) {
    func_in_25621(val);
    const int x = 25621;
    std::cout << 25621;
    for (int i = 0; i < 25621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25621(int& val) {
    const int x = 25621;
    std::cout << 25621;
    for (int i = 0; i < 25621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25622(int& val) {
    func_in_25622(val);
    const int x = 25622;
    std::cout << 25622;
    for (int i = 0; i < 25622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25622(int& val) {
    const int x = 25622;
    std::cout << 25622;
    for (int i = 0; i < 25622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25623(int& val) {
    func_in_25623(val);
    const int x = 25623;
    std::cout << 25623;
    for (int i = 0; i < 25623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25623(int& val) {
    const int x = 25623;
    std::cout << 25623;
    for (int i = 0; i < 25623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25624(int& val) {
    func_in_25624(val);
    const int x = 25624;
    std::cout << 25624;
    for (int i = 0; i < 25624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25624(int& val) {
    const int x = 25624;
    std::cout << 25624;
    for (int i = 0; i < 25624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25625(int& val) {
    func_in_25625(val);
    const int x = 25625;
    std::cout << 25625;
    for (int i = 0; i < 25625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25625(int& val) {
    const int x = 25625;
    std::cout << 25625;
    for (int i = 0; i < 25625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25626(int& val) {
    func_in_25626(val);
    const int x = 25626;
    std::cout << 25626;
    for (int i = 0; i < 25626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25626(int& val) {
    const int x = 25626;
    std::cout << 25626;
    for (int i = 0; i < 25626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25627(int& val) {
    func_in_25627(val);
    const int x = 25627;
    std::cout << 25627;
    for (int i = 0; i < 25627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25627(int& val) {
    const int x = 25627;
    std::cout << 25627;
    for (int i = 0; i < 25627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25628(int& val) {
    func_in_25628(val);
    const int x = 25628;
    std::cout << 25628;
    for (int i = 0; i < 25628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25628(int& val) {
    const int x = 25628;
    std::cout << 25628;
    for (int i = 0; i < 25628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25629(int& val) {
    func_in_25629(val);
    const int x = 25629;
    std::cout << 25629;
    for (int i = 0; i < 25629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25629(int& val) {
    const int x = 25629;
    std::cout << 25629;
    for (int i = 0; i < 25629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25630(int& val) {
    func_in_25630(val);
    const int x = 25630;
    std::cout << 25630;
    for (int i = 0; i < 25630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25630(int& val) {
    const int x = 25630;
    std::cout << 25630;
    for (int i = 0; i < 25630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25631(int& val) {
    func_in_25631(val);
    const int x = 25631;
    std::cout << 25631;
    for (int i = 0; i < 25631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25631(int& val) {
    const int x = 25631;
    std::cout << 25631;
    for (int i = 0; i < 25631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25632(int& val) {
    func_in_25632(val);
    const int x = 25632;
    std::cout << 25632;
    for (int i = 0; i < 25632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25632(int& val) {
    const int x = 25632;
    std::cout << 25632;
    for (int i = 0; i < 25632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25633(int& val) {
    func_in_25633(val);
    const int x = 25633;
    std::cout << 25633;
    for (int i = 0; i < 25633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25633(int& val) {
    const int x = 25633;
    std::cout << 25633;
    for (int i = 0; i < 25633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25634(int& val) {
    func_in_25634(val);
    const int x = 25634;
    std::cout << 25634;
    for (int i = 0; i < 25634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25634(int& val) {
    const int x = 25634;
    std::cout << 25634;
    for (int i = 0; i < 25634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25635(int& val) {
    func_in_25635(val);
    const int x = 25635;
    std::cout << 25635;
    for (int i = 0; i < 25635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25635(int& val) {
    const int x = 25635;
    std::cout << 25635;
    for (int i = 0; i < 25635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25636(int& val) {
    func_in_25636(val);
    const int x = 25636;
    std::cout << 25636;
    for (int i = 0; i < 25636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25636(int& val) {
    const int x = 25636;
    std::cout << 25636;
    for (int i = 0; i < 25636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25637(int& val) {
    func_in_25637(val);
    const int x = 25637;
    std::cout << 25637;
    for (int i = 0; i < 25637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25637(int& val) {
    const int x = 25637;
    std::cout << 25637;
    for (int i = 0; i < 25637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25638(int& val) {
    func_in_25638(val);
    const int x = 25638;
    std::cout << 25638;
    for (int i = 0; i < 25638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25638(int& val) {
    const int x = 25638;
    std::cout << 25638;
    for (int i = 0; i < 25638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25639(int& val) {
    func_in_25639(val);
    const int x = 25639;
    std::cout << 25639;
    for (int i = 0; i < 25639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25639(int& val) {
    const int x = 25639;
    std::cout << 25639;
    for (int i = 0; i < 25639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25640(int& val) {
    func_in_25640(val);
    const int x = 25640;
    std::cout << 25640;
    for (int i = 0; i < 25640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25640(int& val) {
    const int x = 25640;
    std::cout << 25640;
    for (int i = 0; i < 25640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25641(int& val) {
    func_in_25641(val);
    const int x = 25641;
    std::cout << 25641;
    for (int i = 0; i < 25641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25641(int& val) {
    const int x = 25641;
    std::cout << 25641;
    for (int i = 0; i < 25641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25642(int& val) {
    func_in_25642(val);
    const int x = 25642;
    std::cout << 25642;
    for (int i = 0; i < 25642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25642(int& val) {
    const int x = 25642;
    std::cout << 25642;
    for (int i = 0; i < 25642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25643(int& val) {
    func_in_25643(val);
    const int x = 25643;
    std::cout << 25643;
    for (int i = 0; i < 25643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25643(int& val) {
    const int x = 25643;
    std::cout << 25643;
    for (int i = 0; i < 25643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25644(int& val) {
    func_in_25644(val);
    const int x = 25644;
    std::cout << 25644;
    for (int i = 0; i < 25644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25644(int& val) {
    const int x = 25644;
    std::cout << 25644;
    for (int i = 0; i < 25644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25645(int& val) {
    func_in_25645(val);
    const int x = 25645;
    std::cout << 25645;
    for (int i = 0; i < 25645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25645(int& val) {
    const int x = 25645;
    std::cout << 25645;
    for (int i = 0; i < 25645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25646(int& val) {
    func_in_25646(val);
    const int x = 25646;
    std::cout << 25646;
    for (int i = 0; i < 25646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25646(int& val) {
    const int x = 25646;
    std::cout << 25646;
    for (int i = 0; i < 25646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25647(int& val) {
    func_in_25647(val);
    const int x = 25647;
    std::cout << 25647;
    for (int i = 0; i < 25647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25647(int& val) {
    const int x = 25647;
    std::cout << 25647;
    for (int i = 0; i < 25647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25648(int& val) {
    func_in_25648(val);
    const int x = 25648;
    std::cout << 25648;
    for (int i = 0; i < 25648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25648(int& val) {
    const int x = 25648;
    std::cout << 25648;
    for (int i = 0; i < 25648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25649(int& val) {
    func_in_25649(val);
    const int x = 25649;
    std::cout << 25649;
    for (int i = 0; i < 25649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25649(int& val) {
    const int x = 25649;
    std::cout << 25649;
    for (int i = 0; i < 25649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25650(int& val) {
    func_in_25650(val);
    const int x = 25650;
    std::cout << 25650;
    for (int i = 0; i < 25650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25650(int& val) {
    const int x = 25650;
    std::cout << 25650;
    for (int i = 0; i < 25650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25651(int& val) {
    func_in_25651(val);
    const int x = 25651;
    std::cout << 25651;
    for (int i = 0; i < 25651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25651(int& val) {
    const int x = 25651;
    std::cout << 25651;
    for (int i = 0; i < 25651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25652(int& val) {
    func_in_25652(val);
    const int x = 25652;
    std::cout << 25652;
    for (int i = 0; i < 25652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25652(int& val) {
    const int x = 25652;
    std::cout << 25652;
    for (int i = 0; i < 25652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25653(int& val) {
    func_in_25653(val);
    const int x = 25653;
    std::cout << 25653;
    for (int i = 0; i < 25653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25653(int& val) {
    const int x = 25653;
    std::cout << 25653;
    for (int i = 0; i < 25653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25654(int& val) {
    func_in_25654(val);
    const int x = 25654;
    std::cout << 25654;
    for (int i = 0; i < 25654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25654(int& val) {
    const int x = 25654;
    std::cout << 25654;
    for (int i = 0; i < 25654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25655(int& val) {
    func_in_25655(val);
    const int x = 25655;
    std::cout << 25655;
    for (int i = 0; i < 25655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25655(int& val) {
    const int x = 25655;
    std::cout << 25655;
    for (int i = 0; i < 25655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25656(int& val) {
    func_in_25656(val);
    const int x = 25656;
    std::cout << 25656;
    for (int i = 0; i < 25656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25656(int& val) {
    const int x = 25656;
    std::cout << 25656;
    for (int i = 0; i < 25656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25657(int& val) {
    func_in_25657(val);
    const int x = 25657;
    std::cout << 25657;
    for (int i = 0; i < 25657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25657(int& val) {
    const int x = 25657;
    std::cout << 25657;
    for (int i = 0; i < 25657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25658(int& val) {
    func_in_25658(val);
    const int x = 25658;
    std::cout << 25658;
    for (int i = 0; i < 25658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25658(int& val) {
    const int x = 25658;
    std::cout << 25658;
    for (int i = 0; i < 25658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25659(int& val) {
    func_in_25659(val);
    const int x = 25659;
    std::cout << 25659;
    for (int i = 0; i < 25659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25659(int& val) {
    const int x = 25659;
    std::cout << 25659;
    for (int i = 0; i < 25659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25660(int& val) {
    func_in_25660(val);
    const int x = 25660;
    std::cout << 25660;
    for (int i = 0; i < 25660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25660(int& val) {
    const int x = 25660;
    std::cout << 25660;
    for (int i = 0; i < 25660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25661(int& val) {
    func_in_25661(val);
    const int x = 25661;
    std::cout << 25661;
    for (int i = 0; i < 25661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25661(int& val) {
    const int x = 25661;
    std::cout << 25661;
    for (int i = 0; i < 25661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25662(int& val) {
    func_in_25662(val);
    const int x = 25662;
    std::cout << 25662;
    for (int i = 0; i < 25662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25662(int& val) {
    const int x = 25662;
    std::cout << 25662;
    for (int i = 0; i < 25662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25663(int& val) {
    func_in_25663(val);
    const int x = 25663;
    std::cout << 25663;
    for (int i = 0; i < 25663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25663(int& val) {
    const int x = 25663;
    std::cout << 25663;
    for (int i = 0; i < 25663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25664(int& val) {
    func_in_25664(val);
    const int x = 25664;
    std::cout << 25664;
    for (int i = 0; i < 25664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25664(int& val) {
    const int x = 25664;
    std::cout << 25664;
    for (int i = 0; i < 25664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25665(int& val) {
    func_in_25665(val);
    const int x = 25665;
    std::cout << 25665;
    for (int i = 0; i < 25665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25665(int& val) {
    const int x = 25665;
    std::cout << 25665;
    for (int i = 0; i < 25665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25666(int& val) {
    func_in_25666(val);
    const int x = 25666;
    std::cout << 25666;
    for (int i = 0; i < 25666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25666(int& val) {
    const int x = 25666;
    std::cout << 25666;
    for (int i = 0; i < 25666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25667(int& val) {
    func_in_25667(val);
    const int x = 25667;
    std::cout << 25667;
    for (int i = 0; i < 25667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25667(int& val) {
    const int x = 25667;
    std::cout << 25667;
    for (int i = 0; i < 25667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25668(int& val) {
    func_in_25668(val);
    const int x = 25668;
    std::cout << 25668;
    for (int i = 0; i < 25668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25668(int& val) {
    const int x = 25668;
    std::cout << 25668;
    for (int i = 0; i < 25668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25669(int& val) {
    func_in_25669(val);
    const int x = 25669;
    std::cout << 25669;
    for (int i = 0; i < 25669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25669(int& val) {
    const int x = 25669;
    std::cout << 25669;
    for (int i = 0; i < 25669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25670(int& val) {
    func_in_25670(val);
    const int x = 25670;
    std::cout << 25670;
    for (int i = 0; i < 25670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25670(int& val) {
    const int x = 25670;
    std::cout << 25670;
    for (int i = 0; i < 25670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25671(int& val) {
    func_in_25671(val);
    const int x = 25671;
    std::cout << 25671;
    for (int i = 0; i < 25671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25671(int& val) {
    const int x = 25671;
    std::cout << 25671;
    for (int i = 0; i < 25671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25672(int& val) {
    func_in_25672(val);
    const int x = 25672;
    std::cout << 25672;
    for (int i = 0; i < 25672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25672(int& val) {
    const int x = 25672;
    std::cout << 25672;
    for (int i = 0; i < 25672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25673(int& val) {
    func_in_25673(val);
    const int x = 25673;
    std::cout << 25673;
    for (int i = 0; i < 25673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25673(int& val) {
    const int x = 25673;
    std::cout << 25673;
    for (int i = 0; i < 25673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25674(int& val) {
    func_in_25674(val);
    const int x = 25674;
    std::cout << 25674;
    for (int i = 0; i < 25674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25674(int& val) {
    const int x = 25674;
    std::cout << 25674;
    for (int i = 0; i < 25674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25675(int& val) {
    func_in_25675(val);
    const int x = 25675;
    std::cout << 25675;
    for (int i = 0; i < 25675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25675(int& val) {
    const int x = 25675;
    std::cout << 25675;
    for (int i = 0; i < 25675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25676(int& val) {
    func_in_25676(val);
    const int x = 25676;
    std::cout << 25676;
    for (int i = 0; i < 25676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25676(int& val) {
    const int x = 25676;
    std::cout << 25676;
    for (int i = 0; i < 25676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25677(int& val) {
    func_in_25677(val);
    const int x = 25677;
    std::cout << 25677;
    for (int i = 0; i < 25677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25677(int& val) {
    const int x = 25677;
    std::cout << 25677;
    for (int i = 0; i < 25677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25678(int& val) {
    func_in_25678(val);
    const int x = 25678;
    std::cout << 25678;
    for (int i = 0; i < 25678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25678(int& val) {
    const int x = 25678;
    std::cout << 25678;
    for (int i = 0; i < 25678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25679(int& val) {
    func_in_25679(val);
    const int x = 25679;
    std::cout << 25679;
    for (int i = 0; i < 25679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25679(int& val) {
    const int x = 25679;
    std::cout << 25679;
    for (int i = 0; i < 25679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25680(int& val) {
    func_in_25680(val);
    const int x = 25680;
    std::cout << 25680;
    for (int i = 0; i < 25680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25680(int& val) {
    const int x = 25680;
    std::cout << 25680;
    for (int i = 0; i < 25680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25681(int& val) {
    func_in_25681(val);
    const int x = 25681;
    std::cout << 25681;
    for (int i = 0; i < 25681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25681(int& val) {
    const int x = 25681;
    std::cout << 25681;
    for (int i = 0; i < 25681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25682(int& val) {
    func_in_25682(val);
    const int x = 25682;
    std::cout << 25682;
    for (int i = 0; i < 25682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25682(int& val) {
    const int x = 25682;
    std::cout << 25682;
    for (int i = 0; i < 25682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25683(int& val) {
    func_in_25683(val);
    const int x = 25683;
    std::cout << 25683;
    for (int i = 0; i < 25683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25683(int& val) {
    const int x = 25683;
    std::cout << 25683;
    for (int i = 0; i < 25683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25684(int& val) {
    func_in_25684(val);
    const int x = 25684;
    std::cout << 25684;
    for (int i = 0; i < 25684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25684(int& val) {
    const int x = 25684;
    std::cout << 25684;
    for (int i = 0; i < 25684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25685(int& val) {
    func_in_25685(val);
    const int x = 25685;
    std::cout << 25685;
    for (int i = 0; i < 25685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25685(int& val) {
    const int x = 25685;
    std::cout << 25685;
    for (int i = 0; i < 25685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25686(int& val) {
    func_in_25686(val);
    const int x = 25686;
    std::cout << 25686;
    for (int i = 0; i < 25686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25686(int& val) {
    const int x = 25686;
    std::cout << 25686;
    for (int i = 0; i < 25686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25687(int& val) {
    func_in_25687(val);
    const int x = 25687;
    std::cout << 25687;
    for (int i = 0; i < 25687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25687(int& val) {
    const int x = 25687;
    std::cout << 25687;
    for (int i = 0; i < 25687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25688(int& val) {
    func_in_25688(val);
    const int x = 25688;
    std::cout << 25688;
    for (int i = 0; i < 25688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25688(int& val) {
    const int x = 25688;
    std::cout << 25688;
    for (int i = 0; i < 25688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25689(int& val) {
    func_in_25689(val);
    const int x = 25689;
    std::cout << 25689;
    for (int i = 0; i < 25689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25689(int& val) {
    const int x = 25689;
    std::cout << 25689;
    for (int i = 0; i < 25689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25690(int& val) {
    func_in_25690(val);
    const int x = 25690;
    std::cout << 25690;
    for (int i = 0; i < 25690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25690(int& val) {
    const int x = 25690;
    std::cout << 25690;
    for (int i = 0; i < 25690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25691(int& val) {
    func_in_25691(val);
    const int x = 25691;
    std::cout << 25691;
    for (int i = 0; i < 25691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25691(int& val) {
    const int x = 25691;
    std::cout << 25691;
    for (int i = 0; i < 25691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25692(int& val) {
    func_in_25692(val);
    const int x = 25692;
    std::cout << 25692;
    for (int i = 0; i < 25692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25692(int& val) {
    const int x = 25692;
    std::cout << 25692;
    for (int i = 0; i < 25692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25693(int& val) {
    func_in_25693(val);
    const int x = 25693;
    std::cout << 25693;
    for (int i = 0; i < 25693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25693(int& val) {
    const int x = 25693;
    std::cout << 25693;
    for (int i = 0; i < 25693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25694(int& val) {
    func_in_25694(val);
    const int x = 25694;
    std::cout << 25694;
    for (int i = 0; i < 25694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25694(int& val) {
    const int x = 25694;
    std::cout << 25694;
    for (int i = 0; i < 25694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25695(int& val) {
    func_in_25695(val);
    const int x = 25695;
    std::cout << 25695;
    for (int i = 0; i < 25695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25695(int& val) {
    const int x = 25695;
    std::cout << 25695;
    for (int i = 0; i < 25695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25696(int& val) {
    func_in_25696(val);
    const int x = 25696;
    std::cout << 25696;
    for (int i = 0; i < 25696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25696(int& val) {
    const int x = 25696;
    std::cout << 25696;
    for (int i = 0; i < 25696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25697(int& val) {
    func_in_25697(val);
    const int x = 25697;
    std::cout << 25697;
    for (int i = 0; i < 25697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25697(int& val) {
    const int x = 25697;
    std::cout << 25697;
    for (int i = 0; i < 25697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25698(int& val) {
    func_in_25698(val);
    const int x = 25698;
    std::cout << 25698;
    for (int i = 0; i < 25698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25698(int& val) {
    const int x = 25698;
    std::cout << 25698;
    for (int i = 0; i < 25698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25699(int& val) {
    func_in_25699(val);
    const int x = 25699;
    std::cout << 25699;
    for (int i = 0; i < 25699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25699(int& val) {
    const int x = 25699;
    std::cout << 25699;
    for (int i = 0; i < 25699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25700(int& val) {
    func_in_25700(val);
    const int x = 25700;
    std::cout << 25700;
    for (int i = 0; i < 25700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25700(int& val) {
    const int x = 25700;
    std::cout << 25700;
    for (int i = 0; i < 25700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25701(int& val) {
    func_in_25701(val);
    const int x = 25701;
    std::cout << 25701;
    for (int i = 0; i < 25701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25701(int& val) {
    const int x = 25701;
    std::cout << 25701;
    for (int i = 0; i < 25701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25702(int& val) {
    func_in_25702(val);
    const int x = 25702;
    std::cout << 25702;
    for (int i = 0; i < 25702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25702(int& val) {
    const int x = 25702;
    std::cout << 25702;
    for (int i = 0; i < 25702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25703(int& val) {
    func_in_25703(val);
    const int x = 25703;
    std::cout << 25703;
    for (int i = 0; i < 25703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25703(int& val) {
    const int x = 25703;
    std::cout << 25703;
    for (int i = 0; i < 25703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25704(int& val) {
    func_in_25704(val);
    const int x = 25704;
    std::cout << 25704;
    for (int i = 0; i < 25704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25704(int& val) {
    const int x = 25704;
    std::cout << 25704;
    for (int i = 0; i < 25704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25705(int& val) {
    func_in_25705(val);
    const int x = 25705;
    std::cout << 25705;
    for (int i = 0; i < 25705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25705(int& val) {
    const int x = 25705;
    std::cout << 25705;
    for (int i = 0; i < 25705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25706(int& val) {
    func_in_25706(val);
    const int x = 25706;
    std::cout << 25706;
    for (int i = 0; i < 25706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25706(int& val) {
    const int x = 25706;
    std::cout << 25706;
    for (int i = 0; i < 25706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25707(int& val) {
    func_in_25707(val);
    const int x = 25707;
    std::cout << 25707;
    for (int i = 0; i < 25707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25707(int& val) {
    const int x = 25707;
    std::cout << 25707;
    for (int i = 0; i < 25707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25708(int& val) {
    func_in_25708(val);
    const int x = 25708;
    std::cout << 25708;
    for (int i = 0; i < 25708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25708(int& val) {
    const int x = 25708;
    std::cout << 25708;
    for (int i = 0; i < 25708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25709(int& val) {
    func_in_25709(val);
    const int x = 25709;
    std::cout << 25709;
    for (int i = 0; i < 25709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25709(int& val) {
    const int x = 25709;
    std::cout << 25709;
    for (int i = 0; i < 25709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25710(int& val) {
    func_in_25710(val);
    const int x = 25710;
    std::cout << 25710;
    for (int i = 0; i < 25710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25710(int& val) {
    const int x = 25710;
    std::cout << 25710;
    for (int i = 0; i < 25710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25711(int& val) {
    func_in_25711(val);
    const int x = 25711;
    std::cout << 25711;
    for (int i = 0; i < 25711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25711(int& val) {
    const int x = 25711;
    std::cout << 25711;
    for (int i = 0; i < 25711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25712(int& val) {
    func_in_25712(val);
    const int x = 25712;
    std::cout << 25712;
    for (int i = 0; i < 25712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25712(int& val) {
    const int x = 25712;
    std::cout << 25712;
    for (int i = 0; i < 25712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25713(int& val) {
    func_in_25713(val);
    const int x = 25713;
    std::cout << 25713;
    for (int i = 0; i < 25713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25713(int& val) {
    const int x = 25713;
    std::cout << 25713;
    for (int i = 0; i < 25713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25714(int& val) {
    func_in_25714(val);
    const int x = 25714;
    std::cout << 25714;
    for (int i = 0; i < 25714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25714(int& val) {
    const int x = 25714;
    std::cout << 25714;
    for (int i = 0; i < 25714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25715(int& val) {
    func_in_25715(val);
    const int x = 25715;
    std::cout << 25715;
    for (int i = 0; i < 25715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25715(int& val) {
    const int x = 25715;
    std::cout << 25715;
    for (int i = 0; i < 25715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25716(int& val) {
    func_in_25716(val);
    const int x = 25716;
    std::cout << 25716;
    for (int i = 0; i < 25716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25716(int& val) {
    const int x = 25716;
    std::cout << 25716;
    for (int i = 0; i < 25716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25717(int& val) {
    func_in_25717(val);
    const int x = 25717;
    std::cout << 25717;
    for (int i = 0; i < 25717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25717(int& val) {
    const int x = 25717;
    std::cout << 25717;
    for (int i = 0; i < 25717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25718(int& val) {
    func_in_25718(val);
    const int x = 25718;
    std::cout << 25718;
    for (int i = 0; i < 25718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25718(int& val) {
    const int x = 25718;
    std::cout << 25718;
    for (int i = 0; i < 25718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25719(int& val) {
    func_in_25719(val);
    const int x = 25719;
    std::cout << 25719;
    for (int i = 0; i < 25719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25719(int& val) {
    const int x = 25719;
    std::cout << 25719;
    for (int i = 0; i < 25719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25720(int& val) {
    func_in_25720(val);
    const int x = 25720;
    std::cout << 25720;
    for (int i = 0; i < 25720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25720(int& val) {
    const int x = 25720;
    std::cout << 25720;
    for (int i = 0; i < 25720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25721(int& val) {
    func_in_25721(val);
    const int x = 25721;
    std::cout << 25721;
    for (int i = 0; i < 25721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25721(int& val) {
    const int x = 25721;
    std::cout << 25721;
    for (int i = 0; i < 25721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25722(int& val) {
    func_in_25722(val);
    const int x = 25722;
    std::cout << 25722;
    for (int i = 0; i < 25722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25722(int& val) {
    const int x = 25722;
    std::cout << 25722;
    for (int i = 0; i < 25722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25723(int& val) {
    func_in_25723(val);
    const int x = 25723;
    std::cout << 25723;
    for (int i = 0; i < 25723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25723(int& val) {
    const int x = 25723;
    std::cout << 25723;
    for (int i = 0; i < 25723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25724(int& val) {
    func_in_25724(val);
    const int x = 25724;
    std::cout << 25724;
    for (int i = 0; i < 25724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25724(int& val) {
    const int x = 25724;
    std::cout << 25724;
    for (int i = 0; i < 25724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25725(int& val) {
    func_in_25725(val);
    const int x = 25725;
    std::cout << 25725;
    for (int i = 0; i < 25725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25725(int& val) {
    const int x = 25725;
    std::cout << 25725;
    for (int i = 0; i < 25725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25726(int& val) {
    func_in_25726(val);
    const int x = 25726;
    std::cout << 25726;
    for (int i = 0; i < 25726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25726(int& val) {
    const int x = 25726;
    std::cout << 25726;
    for (int i = 0; i < 25726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25727(int& val) {
    func_in_25727(val);
    const int x = 25727;
    std::cout << 25727;
    for (int i = 0; i < 25727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25727(int& val) {
    const int x = 25727;
    std::cout << 25727;
    for (int i = 0; i < 25727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25728(int& val) {
    func_in_25728(val);
    const int x = 25728;
    std::cout << 25728;
    for (int i = 0; i < 25728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25728(int& val) {
    const int x = 25728;
    std::cout << 25728;
    for (int i = 0; i < 25728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25729(int& val) {
    func_in_25729(val);
    const int x = 25729;
    std::cout << 25729;
    for (int i = 0; i < 25729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25729(int& val) {
    const int x = 25729;
    std::cout << 25729;
    for (int i = 0; i < 25729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25730(int& val) {
    func_in_25730(val);
    const int x = 25730;
    std::cout << 25730;
    for (int i = 0; i < 25730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25730(int& val) {
    const int x = 25730;
    std::cout << 25730;
    for (int i = 0; i < 25730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25731(int& val) {
    func_in_25731(val);
    const int x = 25731;
    std::cout << 25731;
    for (int i = 0; i < 25731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25731(int& val) {
    const int x = 25731;
    std::cout << 25731;
    for (int i = 0; i < 25731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25732(int& val) {
    func_in_25732(val);
    const int x = 25732;
    std::cout << 25732;
    for (int i = 0; i < 25732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25732(int& val) {
    const int x = 25732;
    std::cout << 25732;
    for (int i = 0; i < 25732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25733(int& val) {
    func_in_25733(val);
    const int x = 25733;
    std::cout << 25733;
    for (int i = 0; i < 25733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25733(int& val) {
    const int x = 25733;
    std::cout << 25733;
    for (int i = 0; i < 25733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25734(int& val) {
    func_in_25734(val);
    const int x = 25734;
    std::cout << 25734;
    for (int i = 0; i < 25734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25734(int& val) {
    const int x = 25734;
    std::cout << 25734;
    for (int i = 0; i < 25734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25735(int& val) {
    func_in_25735(val);
    const int x = 25735;
    std::cout << 25735;
    for (int i = 0; i < 25735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25735(int& val) {
    const int x = 25735;
    std::cout << 25735;
    for (int i = 0; i < 25735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25736(int& val) {
    func_in_25736(val);
    const int x = 25736;
    std::cout << 25736;
    for (int i = 0; i < 25736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25736(int& val) {
    const int x = 25736;
    std::cout << 25736;
    for (int i = 0; i < 25736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25737(int& val) {
    func_in_25737(val);
    const int x = 25737;
    std::cout << 25737;
    for (int i = 0; i < 25737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25737(int& val) {
    const int x = 25737;
    std::cout << 25737;
    for (int i = 0; i < 25737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25738(int& val) {
    func_in_25738(val);
    const int x = 25738;
    std::cout << 25738;
    for (int i = 0; i < 25738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25738(int& val) {
    const int x = 25738;
    std::cout << 25738;
    for (int i = 0; i < 25738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25739(int& val) {
    func_in_25739(val);
    const int x = 25739;
    std::cout << 25739;
    for (int i = 0; i < 25739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25739(int& val) {
    const int x = 25739;
    std::cout << 25739;
    for (int i = 0; i < 25739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25740(int& val) {
    func_in_25740(val);
    const int x = 25740;
    std::cout << 25740;
    for (int i = 0; i < 25740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25740(int& val) {
    const int x = 25740;
    std::cout << 25740;
    for (int i = 0; i < 25740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25741(int& val) {
    func_in_25741(val);
    const int x = 25741;
    std::cout << 25741;
    for (int i = 0; i < 25741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25741(int& val) {
    const int x = 25741;
    std::cout << 25741;
    for (int i = 0; i < 25741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25742(int& val) {
    func_in_25742(val);
    const int x = 25742;
    std::cout << 25742;
    for (int i = 0; i < 25742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25742(int& val) {
    const int x = 25742;
    std::cout << 25742;
    for (int i = 0; i < 25742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25743(int& val) {
    func_in_25743(val);
    const int x = 25743;
    std::cout << 25743;
    for (int i = 0; i < 25743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25743(int& val) {
    const int x = 25743;
    std::cout << 25743;
    for (int i = 0; i < 25743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25744(int& val) {
    func_in_25744(val);
    const int x = 25744;
    std::cout << 25744;
    for (int i = 0; i < 25744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25744(int& val) {
    const int x = 25744;
    std::cout << 25744;
    for (int i = 0; i < 25744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25745(int& val) {
    func_in_25745(val);
    const int x = 25745;
    std::cout << 25745;
    for (int i = 0; i < 25745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25745(int& val) {
    const int x = 25745;
    std::cout << 25745;
    for (int i = 0; i < 25745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25746(int& val) {
    func_in_25746(val);
    const int x = 25746;
    std::cout << 25746;
    for (int i = 0; i < 25746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25746(int& val) {
    const int x = 25746;
    std::cout << 25746;
    for (int i = 0; i < 25746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25747(int& val) {
    func_in_25747(val);
    const int x = 25747;
    std::cout << 25747;
    for (int i = 0; i < 25747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25747(int& val) {
    const int x = 25747;
    std::cout << 25747;
    for (int i = 0; i < 25747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25748(int& val) {
    func_in_25748(val);
    const int x = 25748;
    std::cout << 25748;
    for (int i = 0; i < 25748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25748(int& val) {
    const int x = 25748;
    std::cout << 25748;
    for (int i = 0; i < 25748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25749(int& val) {
    func_in_25749(val);
    const int x = 25749;
    std::cout << 25749;
    for (int i = 0; i < 25749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25749(int& val) {
    const int x = 25749;
    std::cout << 25749;
    for (int i = 0; i < 25749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25750(int& val) {
    func_in_25750(val);
    const int x = 25750;
    std::cout << 25750;
    for (int i = 0; i < 25750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25750(int& val) {
    const int x = 25750;
    std::cout << 25750;
    for (int i = 0; i < 25750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25751(int& val) {
    func_in_25751(val);
    const int x = 25751;
    std::cout << 25751;
    for (int i = 0; i < 25751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25751(int& val) {
    const int x = 25751;
    std::cout << 25751;
    for (int i = 0; i < 25751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25752(int& val) {
    func_in_25752(val);
    const int x = 25752;
    std::cout << 25752;
    for (int i = 0; i < 25752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25752(int& val) {
    const int x = 25752;
    std::cout << 25752;
    for (int i = 0; i < 25752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25753(int& val) {
    func_in_25753(val);
    const int x = 25753;
    std::cout << 25753;
    for (int i = 0; i < 25753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25753(int& val) {
    const int x = 25753;
    std::cout << 25753;
    for (int i = 0; i < 25753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25754(int& val) {
    func_in_25754(val);
    const int x = 25754;
    std::cout << 25754;
    for (int i = 0; i < 25754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25754(int& val) {
    const int x = 25754;
    std::cout << 25754;
    for (int i = 0; i < 25754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25755(int& val) {
    func_in_25755(val);
    const int x = 25755;
    std::cout << 25755;
    for (int i = 0; i < 25755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25755(int& val) {
    const int x = 25755;
    std::cout << 25755;
    for (int i = 0; i < 25755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25756(int& val) {
    func_in_25756(val);
    const int x = 25756;
    std::cout << 25756;
    for (int i = 0; i < 25756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25756(int& val) {
    const int x = 25756;
    std::cout << 25756;
    for (int i = 0; i < 25756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25757(int& val) {
    func_in_25757(val);
    const int x = 25757;
    std::cout << 25757;
    for (int i = 0; i < 25757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25757(int& val) {
    const int x = 25757;
    std::cout << 25757;
    for (int i = 0; i < 25757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25758(int& val) {
    func_in_25758(val);
    const int x = 25758;
    std::cout << 25758;
    for (int i = 0; i < 25758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25758(int& val) {
    const int x = 25758;
    std::cout << 25758;
    for (int i = 0; i < 25758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25759(int& val) {
    func_in_25759(val);
    const int x = 25759;
    std::cout << 25759;
    for (int i = 0; i < 25759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25759(int& val) {
    const int x = 25759;
    std::cout << 25759;
    for (int i = 0; i < 25759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25760(int& val) {
    func_in_25760(val);
    const int x = 25760;
    std::cout << 25760;
    for (int i = 0; i < 25760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25760(int& val) {
    const int x = 25760;
    std::cout << 25760;
    for (int i = 0; i < 25760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25761(int& val) {
    func_in_25761(val);
    const int x = 25761;
    std::cout << 25761;
    for (int i = 0; i < 25761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25761(int& val) {
    const int x = 25761;
    std::cout << 25761;
    for (int i = 0; i < 25761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25762(int& val) {
    func_in_25762(val);
    const int x = 25762;
    std::cout << 25762;
    for (int i = 0; i < 25762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25762(int& val) {
    const int x = 25762;
    std::cout << 25762;
    for (int i = 0; i < 25762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25763(int& val) {
    func_in_25763(val);
    const int x = 25763;
    std::cout << 25763;
    for (int i = 0; i < 25763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25763(int& val) {
    const int x = 25763;
    std::cout << 25763;
    for (int i = 0; i < 25763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25764(int& val) {
    func_in_25764(val);
    const int x = 25764;
    std::cout << 25764;
    for (int i = 0; i < 25764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25764(int& val) {
    const int x = 25764;
    std::cout << 25764;
    for (int i = 0; i < 25764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25765(int& val) {
    func_in_25765(val);
    const int x = 25765;
    std::cout << 25765;
    for (int i = 0; i < 25765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25765(int& val) {
    const int x = 25765;
    std::cout << 25765;
    for (int i = 0; i < 25765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25766(int& val) {
    func_in_25766(val);
    const int x = 25766;
    std::cout << 25766;
    for (int i = 0; i < 25766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25766(int& val) {
    const int x = 25766;
    std::cout << 25766;
    for (int i = 0; i < 25766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25767(int& val) {
    func_in_25767(val);
    const int x = 25767;
    std::cout << 25767;
    for (int i = 0; i < 25767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25767(int& val) {
    const int x = 25767;
    std::cout << 25767;
    for (int i = 0; i < 25767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25768(int& val) {
    func_in_25768(val);
    const int x = 25768;
    std::cout << 25768;
    for (int i = 0; i < 25768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25768(int& val) {
    const int x = 25768;
    std::cout << 25768;
    for (int i = 0; i < 25768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25769(int& val) {
    func_in_25769(val);
    const int x = 25769;
    std::cout << 25769;
    for (int i = 0; i < 25769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25769(int& val) {
    const int x = 25769;
    std::cout << 25769;
    for (int i = 0; i < 25769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25770(int& val) {
    func_in_25770(val);
    const int x = 25770;
    std::cout << 25770;
    for (int i = 0; i < 25770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25770(int& val) {
    const int x = 25770;
    std::cout << 25770;
    for (int i = 0; i < 25770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25771(int& val) {
    func_in_25771(val);
    const int x = 25771;
    std::cout << 25771;
    for (int i = 0; i < 25771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25771(int& val) {
    const int x = 25771;
    std::cout << 25771;
    for (int i = 0; i < 25771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25772(int& val) {
    func_in_25772(val);
    const int x = 25772;
    std::cout << 25772;
    for (int i = 0; i < 25772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25772(int& val) {
    const int x = 25772;
    std::cout << 25772;
    for (int i = 0; i < 25772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25773(int& val) {
    func_in_25773(val);
    const int x = 25773;
    std::cout << 25773;
    for (int i = 0; i < 25773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25773(int& val) {
    const int x = 25773;
    std::cout << 25773;
    for (int i = 0; i < 25773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25774(int& val) {
    func_in_25774(val);
    const int x = 25774;
    std::cout << 25774;
    for (int i = 0; i < 25774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25774(int& val) {
    const int x = 25774;
    std::cout << 25774;
    for (int i = 0; i < 25774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25775(int& val) {
    func_in_25775(val);
    const int x = 25775;
    std::cout << 25775;
    for (int i = 0; i < 25775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25775(int& val) {
    const int x = 25775;
    std::cout << 25775;
    for (int i = 0; i < 25775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25776(int& val) {
    func_in_25776(val);
    const int x = 25776;
    std::cout << 25776;
    for (int i = 0; i < 25776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25776(int& val) {
    const int x = 25776;
    std::cout << 25776;
    for (int i = 0; i < 25776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25777(int& val) {
    func_in_25777(val);
    const int x = 25777;
    std::cout << 25777;
    for (int i = 0; i < 25777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25777(int& val) {
    const int x = 25777;
    std::cout << 25777;
    for (int i = 0; i < 25777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25778(int& val) {
    func_in_25778(val);
    const int x = 25778;
    std::cout << 25778;
    for (int i = 0; i < 25778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25778(int& val) {
    const int x = 25778;
    std::cout << 25778;
    for (int i = 0; i < 25778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25779(int& val) {
    func_in_25779(val);
    const int x = 25779;
    std::cout << 25779;
    for (int i = 0; i < 25779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25779(int& val) {
    const int x = 25779;
    std::cout << 25779;
    for (int i = 0; i < 25779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25780(int& val) {
    func_in_25780(val);
    const int x = 25780;
    std::cout << 25780;
    for (int i = 0; i < 25780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25780(int& val) {
    const int x = 25780;
    std::cout << 25780;
    for (int i = 0; i < 25780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25781(int& val) {
    func_in_25781(val);
    const int x = 25781;
    std::cout << 25781;
    for (int i = 0; i < 25781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25781(int& val) {
    const int x = 25781;
    std::cout << 25781;
    for (int i = 0; i < 25781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25782(int& val) {
    func_in_25782(val);
    const int x = 25782;
    std::cout << 25782;
    for (int i = 0; i < 25782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25782(int& val) {
    const int x = 25782;
    std::cout << 25782;
    for (int i = 0; i < 25782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25783(int& val) {
    func_in_25783(val);
    const int x = 25783;
    std::cout << 25783;
    for (int i = 0; i < 25783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25783(int& val) {
    const int x = 25783;
    std::cout << 25783;
    for (int i = 0; i < 25783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25784(int& val) {
    func_in_25784(val);
    const int x = 25784;
    std::cout << 25784;
    for (int i = 0; i < 25784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25784(int& val) {
    const int x = 25784;
    std::cout << 25784;
    for (int i = 0; i < 25784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25785(int& val) {
    func_in_25785(val);
    const int x = 25785;
    std::cout << 25785;
    for (int i = 0; i < 25785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25785(int& val) {
    const int x = 25785;
    std::cout << 25785;
    for (int i = 0; i < 25785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25786(int& val) {
    func_in_25786(val);
    const int x = 25786;
    std::cout << 25786;
    for (int i = 0; i < 25786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25786(int& val) {
    const int x = 25786;
    std::cout << 25786;
    for (int i = 0; i < 25786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25787(int& val) {
    func_in_25787(val);
    const int x = 25787;
    std::cout << 25787;
    for (int i = 0; i < 25787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25787(int& val) {
    const int x = 25787;
    std::cout << 25787;
    for (int i = 0; i < 25787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25788(int& val) {
    func_in_25788(val);
    const int x = 25788;
    std::cout << 25788;
    for (int i = 0; i < 25788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25788(int& val) {
    const int x = 25788;
    std::cout << 25788;
    for (int i = 0; i < 25788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25789(int& val) {
    func_in_25789(val);
    const int x = 25789;
    std::cout << 25789;
    for (int i = 0; i < 25789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25789(int& val) {
    const int x = 25789;
    std::cout << 25789;
    for (int i = 0; i < 25789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25790(int& val) {
    func_in_25790(val);
    const int x = 25790;
    std::cout << 25790;
    for (int i = 0; i < 25790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25790(int& val) {
    const int x = 25790;
    std::cout << 25790;
    for (int i = 0; i < 25790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25791(int& val) {
    func_in_25791(val);
    const int x = 25791;
    std::cout << 25791;
    for (int i = 0; i < 25791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25791(int& val) {
    const int x = 25791;
    std::cout << 25791;
    for (int i = 0; i < 25791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25792(int& val) {
    func_in_25792(val);
    const int x = 25792;
    std::cout << 25792;
    for (int i = 0; i < 25792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25792(int& val) {
    const int x = 25792;
    std::cout << 25792;
    for (int i = 0; i < 25792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25793(int& val) {
    func_in_25793(val);
    const int x = 25793;
    std::cout << 25793;
    for (int i = 0; i < 25793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25793(int& val) {
    const int x = 25793;
    std::cout << 25793;
    for (int i = 0; i < 25793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25794(int& val) {
    func_in_25794(val);
    const int x = 25794;
    std::cout << 25794;
    for (int i = 0; i < 25794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25794(int& val) {
    const int x = 25794;
    std::cout << 25794;
    for (int i = 0; i < 25794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25795(int& val) {
    func_in_25795(val);
    const int x = 25795;
    std::cout << 25795;
    for (int i = 0; i < 25795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25795(int& val) {
    const int x = 25795;
    std::cout << 25795;
    for (int i = 0; i < 25795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25796(int& val) {
    func_in_25796(val);
    const int x = 25796;
    std::cout << 25796;
    for (int i = 0; i < 25796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25796(int& val) {
    const int x = 25796;
    std::cout << 25796;
    for (int i = 0; i < 25796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25797(int& val) {
    func_in_25797(val);
    const int x = 25797;
    std::cout << 25797;
    for (int i = 0; i < 25797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25797(int& val) {
    const int x = 25797;
    std::cout << 25797;
    for (int i = 0; i < 25797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25798(int& val) {
    func_in_25798(val);
    const int x = 25798;
    std::cout << 25798;
    for (int i = 0; i < 25798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25798(int& val) {
    const int x = 25798;
    std::cout << 25798;
    for (int i = 0; i < 25798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25799(int& val) {
    func_in_25799(val);
    const int x = 25799;
    std::cout << 25799;
    for (int i = 0; i < 25799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25799(int& val) {
    const int x = 25799;
    std::cout << 25799;
    for (int i = 0; i < 25799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25800(int& val) {
    func_in_25800(val);
    const int x = 25800;
    std::cout << 25800;
    for (int i = 0; i < 25800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25800(int& val) {
    const int x = 25800;
    std::cout << 25800;
    for (int i = 0; i < 25800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25801(int& val) {
    func_in_25801(val);
    const int x = 25801;
    std::cout << 25801;
    for (int i = 0; i < 25801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25801(int& val) {
    const int x = 25801;
    std::cout << 25801;
    for (int i = 0; i < 25801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25802(int& val) {
    func_in_25802(val);
    const int x = 25802;
    std::cout << 25802;
    for (int i = 0; i < 25802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25802(int& val) {
    const int x = 25802;
    std::cout << 25802;
    for (int i = 0; i < 25802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25803(int& val) {
    func_in_25803(val);
    const int x = 25803;
    std::cout << 25803;
    for (int i = 0; i < 25803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25803(int& val) {
    const int x = 25803;
    std::cout << 25803;
    for (int i = 0; i < 25803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25804(int& val) {
    func_in_25804(val);
    const int x = 25804;
    std::cout << 25804;
    for (int i = 0; i < 25804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25804(int& val) {
    const int x = 25804;
    std::cout << 25804;
    for (int i = 0; i < 25804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25805(int& val) {
    func_in_25805(val);
    const int x = 25805;
    std::cout << 25805;
    for (int i = 0; i < 25805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25805(int& val) {
    const int x = 25805;
    std::cout << 25805;
    for (int i = 0; i < 25805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25806(int& val) {
    func_in_25806(val);
    const int x = 25806;
    std::cout << 25806;
    for (int i = 0; i < 25806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25806(int& val) {
    const int x = 25806;
    std::cout << 25806;
    for (int i = 0; i < 25806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25807(int& val) {
    func_in_25807(val);
    const int x = 25807;
    std::cout << 25807;
    for (int i = 0; i < 25807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25807(int& val) {
    const int x = 25807;
    std::cout << 25807;
    for (int i = 0; i < 25807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25808(int& val) {
    func_in_25808(val);
    const int x = 25808;
    std::cout << 25808;
    for (int i = 0; i < 25808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25808(int& val) {
    const int x = 25808;
    std::cout << 25808;
    for (int i = 0; i < 25808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25809(int& val) {
    func_in_25809(val);
    const int x = 25809;
    std::cout << 25809;
    for (int i = 0; i < 25809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25809(int& val) {
    const int x = 25809;
    std::cout << 25809;
    for (int i = 0; i < 25809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25810(int& val) {
    func_in_25810(val);
    const int x = 25810;
    std::cout << 25810;
    for (int i = 0; i < 25810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25810(int& val) {
    const int x = 25810;
    std::cout << 25810;
    for (int i = 0; i < 25810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25811(int& val) {
    func_in_25811(val);
    const int x = 25811;
    std::cout << 25811;
    for (int i = 0; i < 25811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25811(int& val) {
    const int x = 25811;
    std::cout << 25811;
    for (int i = 0; i < 25811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25812(int& val) {
    func_in_25812(val);
    const int x = 25812;
    std::cout << 25812;
    for (int i = 0; i < 25812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25812(int& val) {
    const int x = 25812;
    std::cout << 25812;
    for (int i = 0; i < 25812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25813(int& val) {
    func_in_25813(val);
    const int x = 25813;
    std::cout << 25813;
    for (int i = 0; i < 25813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25813(int& val) {
    const int x = 25813;
    std::cout << 25813;
    for (int i = 0; i < 25813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25814(int& val) {
    func_in_25814(val);
    const int x = 25814;
    std::cout << 25814;
    for (int i = 0; i < 25814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25814(int& val) {
    const int x = 25814;
    std::cout << 25814;
    for (int i = 0; i < 25814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25815(int& val) {
    func_in_25815(val);
    const int x = 25815;
    std::cout << 25815;
    for (int i = 0; i < 25815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25815(int& val) {
    const int x = 25815;
    std::cout << 25815;
    for (int i = 0; i < 25815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25816(int& val) {
    func_in_25816(val);
    const int x = 25816;
    std::cout << 25816;
    for (int i = 0; i < 25816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25816(int& val) {
    const int x = 25816;
    std::cout << 25816;
    for (int i = 0; i < 25816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25817(int& val) {
    func_in_25817(val);
    const int x = 25817;
    std::cout << 25817;
    for (int i = 0; i < 25817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25817(int& val) {
    const int x = 25817;
    std::cout << 25817;
    for (int i = 0; i < 25817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25818(int& val) {
    func_in_25818(val);
    const int x = 25818;
    std::cout << 25818;
    for (int i = 0; i < 25818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25818(int& val) {
    const int x = 25818;
    std::cout << 25818;
    for (int i = 0; i < 25818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25819(int& val) {
    func_in_25819(val);
    const int x = 25819;
    std::cout << 25819;
    for (int i = 0; i < 25819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25819(int& val) {
    const int x = 25819;
    std::cout << 25819;
    for (int i = 0; i < 25819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25820(int& val) {
    func_in_25820(val);
    const int x = 25820;
    std::cout << 25820;
    for (int i = 0; i < 25820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25820(int& val) {
    const int x = 25820;
    std::cout << 25820;
    for (int i = 0; i < 25820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25821(int& val) {
    func_in_25821(val);
    const int x = 25821;
    std::cout << 25821;
    for (int i = 0; i < 25821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25821(int& val) {
    const int x = 25821;
    std::cout << 25821;
    for (int i = 0; i < 25821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25822(int& val) {
    func_in_25822(val);
    const int x = 25822;
    std::cout << 25822;
    for (int i = 0; i < 25822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25822(int& val) {
    const int x = 25822;
    std::cout << 25822;
    for (int i = 0; i < 25822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25823(int& val) {
    func_in_25823(val);
    const int x = 25823;
    std::cout << 25823;
    for (int i = 0; i < 25823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25823(int& val) {
    const int x = 25823;
    std::cout << 25823;
    for (int i = 0; i < 25823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25824(int& val) {
    func_in_25824(val);
    const int x = 25824;
    std::cout << 25824;
    for (int i = 0; i < 25824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25824(int& val) {
    const int x = 25824;
    std::cout << 25824;
    for (int i = 0; i < 25824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25825(int& val) {
    func_in_25825(val);
    const int x = 25825;
    std::cout << 25825;
    for (int i = 0; i < 25825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25825(int& val) {
    const int x = 25825;
    std::cout << 25825;
    for (int i = 0; i < 25825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25826(int& val) {
    func_in_25826(val);
    const int x = 25826;
    std::cout << 25826;
    for (int i = 0; i < 25826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25826(int& val) {
    const int x = 25826;
    std::cout << 25826;
    for (int i = 0; i < 25826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25827(int& val) {
    func_in_25827(val);
    const int x = 25827;
    std::cout << 25827;
    for (int i = 0; i < 25827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25827(int& val) {
    const int x = 25827;
    std::cout << 25827;
    for (int i = 0; i < 25827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25828(int& val) {
    func_in_25828(val);
    const int x = 25828;
    std::cout << 25828;
    for (int i = 0; i < 25828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25828(int& val) {
    const int x = 25828;
    std::cout << 25828;
    for (int i = 0; i < 25828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25829(int& val) {
    func_in_25829(val);
    const int x = 25829;
    std::cout << 25829;
    for (int i = 0; i < 25829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25829(int& val) {
    const int x = 25829;
    std::cout << 25829;
    for (int i = 0; i < 25829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25830(int& val) {
    func_in_25830(val);
    const int x = 25830;
    std::cout << 25830;
    for (int i = 0; i < 25830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25830(int& val) {
    const int x = 25830;
    std::cout << 25830;
    for (int i = 0; i < 25830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25831(int& val) {
    func_in_25831(val);
    const int x = 25831;
    std::cout << 25831;
    for (int i = 0; i < 25831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25831(int& val) {
    const int x = 25831;
    std::cout << 25831;
    for (int i = 0; i < 25831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25832(int& val) {
    func_in_25832(val);
    const int x = 25832;
    std::cout << 25832;
    for (int i = 0; i < 25832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25832(int& val) {
    const int x = 25832;
    std::cout << 25832;
    for (int i = 0; i < 25832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25833(int& val) {
    func_in_25833(val);
    const int x = 25833;
    std::cout << 25833;
    for (int i = 0; i < 25833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25833(int& val) {
    const int x = 25833;
    std::cout << 25833;
    for (int i = 0; i < 25833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25834(int& val) {
    func_in_25834(val);
    const int x = 25834;
    std::cout << 25834;
    for (int i = 0; i < 25834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25834(int& val) {
    const int x = 25834;
    std::cout << 25834;
    for (int i = 0; i < 25834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25835(int& val) {
    func_in_25835(val);
    const int x = 25835;
    std::cout << 25835;
    for (int i = 0; i < 25835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25835(int& val) {
    const int x = 25835;
    std::cout << 25835;
    for (int i = 0; i < 25835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25836(int& val) {
    func_in_25836(val);
    const int x = 25836;
    std::cout << 25836;
    for (int i = 0; i < 25836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25836(int& val) {
    const int x = 25836;
    std::cout << 25836;
    for (int i = 0; i < 25836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25837(int& val) {
    func_in_25837(val);
    const int x = 25837;
    std::cout << 25837;
    for (int i = 0; i < 25837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25837(int& val) {
    const int x = 25837;
    std::cout << 25837;
    for (int i = 0; i < 25837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25838(int& val) {
    func_in_25838(val);
    const int x = 25838;
    std::cout << 25838;
    for (int i = 0; i < 25838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25838(int& val) {
    const int x = 25838;
    std::cout << 25838;
    for (int i = 0; i < 25838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25839(int& val) {
    func_in_25839(val);
    const int x = 25839;
    std::cout << 25839;
    for (int i = 0; i < 25839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25839(int& val) {
    const int x = 25839;
    std::cout << 25839;
    for (int i = 0; i < 25839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25840(int& val) {
    func_in_25840(val);
    const int x = 25840;
    std::cout << 25840;
    for (int i = 0; i < 25840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25840(int& val) {
    const int x = 25840;
    std::cout << 25840;
    for (int i = 0; i < 25840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25841(int& val) {
    func_in_25841(val);
    const int x = 25841;
    std::cout << 25841;
    for (int i = 0; i < 25841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25841(int& val) {
    const int x = 25841;
    std::cout << 25841;
    for (int i = 0; i < 25841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25842(int& val) {
    func_in_25842(val);
    const int x = 25842;
    std::cout << 25842;
    for (int i = 0; i < 25842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25842(int& val) {
    const int x = 25842;
    std::cout << 25842;
    for (int i = 0; i < 25842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25843(int& val) {
    func_in_25843(val);
    const int x = 25843;
    std::cout << 25843;
    for (int i = 0; i < 25843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25843(int& val) {
    const int x = 25843;
    std::cout << 25843;
    for (int i = 0; i < 25843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25844(int& val) {
    func_in_25844(val);
    const int x = 25844;
    std::cout << 25844;
    for (int i = 0; i < 25844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25844(int& val) {
    const int x = 25844;
    std::cout << 25844;
    for (int i = 0; i < 25844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25845(int& val) {
    func_in_25845(val);
    const int x = 25845;
    std::cout << 25845;
    for (int i = 0; i < 25845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25845(int& val) {
    const int x = 25845;
    std::cout << 25845;
    for (int i = 0; i < 25845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25846(int& val) {
    func_in_25846(val);
    const int x = 25846;
    std::cout << 25846;
    for (int i = 0; i < 25846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25846(int& val) {
    const int x = 25846;
    std::cout << 25846;
    for (int i = 0; i < 25846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25847(int& val) {
    func_in_25847(val);
    const int x = 25847;
    std::cout << 25847;
    for (int i = 0; i < 25847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25847(int& val) {
    const int x = 25847;
    std::cout << 25847;
    for (int i = 0; i < 25847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25848(int& val) {
    func_in_25848(val);
    const int x = 25848;
    std::cout << 25848;
    for (int i = 0; i < 25848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25848(int& val) {
    const int x = 25848;
    std::cout << 25848;
    for (int i = 0; i < 25848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25849(int& val) {
    func_in_25849(val);
    const int x = 25849;
    std::cout << 25849;
    for (int i = 0; i < 25849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25849(int& val) {
    const int x = 25849;
    std::cout << 25849;
    for (int i = 0; i < 25849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25850(int& val) {
    func_in_25850(val);
    const int x = 25850;
    std::cout << 25850;
    for (int i = 0; i < 25850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25850(int& val) {
    const int x = 25850;
    std::cout << 25850;
    for (int i = 0; i < 25850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25851(int& val) {
    func_in_25851(val);
    const int x = 25851;
    std::cout << 25851;
    for (int i = 0; i < 25851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25851(int& val) {
    const int x = 25851;
    std::cout << 25851;
    for (int i = 0; i < 25851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25852(int& val) {
    func_in_25852(val);
    const int x = 25852;
    std::cout << 25852;
    for (int i = 0; i < 25852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25852(int& val) {
    const int x = 25852;
    std::cout << 25852;
    for (int i = 0; i < 25852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25853(int& val) {
    func_in_25853(val);
    const int x = 25853;
    std::cout << 25853;
    for (int i = 0; i < 25853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25853(int& val) {
    const int x = 25853;
    std::cout << 25853;
    for (int i = 0; i < 25853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25854(int& val) {
    func_in_25854(val);
    const int x = 25854;
    std::cout << 25854;
    for (int i = 0; i < 25854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25854(int& val) {
    const int x = 25854;
    std::cout << 25854;
    for (int i = 0; i < 25854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25855(int& val) {
    func_in_25855(val);
    const int x = 25855;
    std::cout << 25855;
    for (int i = 0; i < 25855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25855(int& val) {
    const int x = 25855;
    std::cout << 25855;
    for (int i = 0; i < 25855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25856(int& val) {
    func_in_25856(val);
    const int x = 25856;
    std::cout << 25856;
    for (int i = 0; i < 25856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25856(int& val) {
    const int x = 25856;
    std::cout << 25856;
    for (int i = 0; i < 25856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25857(int& val) {
    func_in_25857(val);
    const int x = 25857;
    std::cout << 25857;
    for (int i = 0; i < 25857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25857(int& val) {
    const int x = 25857;
    std::cout << 25857;
    for (int i = 0; i < 25857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25858(int& val) {
    func_in_25858(val);
    const int x = 25858;
    std::cout << 25858;
    for (int i = 0; i < 25858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25858(int& val) {
    const int x = 25858;
    std::cout << 25858;
    for (int i = 0; i < 25858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25859(int& val) {
    func_in_25859(val);
    const int x = 25859;
    std::cout << 25859;
    for (int i = 0; i < 25859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25859(int& val) {
    const int x = 25859;
    std::cout << 25859;
    for (int i = 0; i < 25859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25860(int& val) {
    func_in_25860(val);
    const int x = 25860;
    std::cout << 25860;
    for (int i = 0; i < 25860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25860(int& val) {
    const int x = 25860;
    std::cout << 25860;
    for (int i = 0; i < 25860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25861(int& val) {
    func_in_25861(val);
    const int x = 25861;
    std::cout << 25861;
    for (int i = 0; i < 25861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25861(int& val) {
    const int x = 25861;
    std::cout << 25861;
    for (int i = 0; i < 25861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25862(int& val) {
    func_in_25862(val);
    const int x = 25862;
    std::cout << 25862;
    for (int i = 0; i < 25862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25862(int& val) {
    const int x = 25862;
    std::cout << 25862;
    for (int i = 0; i < 25862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25863(int& val) {
    func_in_25863(val);
    const int x = 25863;
    std::cout << 25863;
    for (int i = 0; i < 25863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25863(int& val) {
    const int x = 25863;
    std::cout << 25863;
    for (int i = 0; i < 25863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25864(int& val) {
    func_in_25864(val);
    const int x = 25864;
    std::cout << 25864;
    for (int i = 0; i < 25864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25864(int& val) {
    const int x = 25864;
    std::cout << 25864;
    for (int i = 0; i < 25864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25865(int& val) {
    func_in_25865(val);
    const int x = 25865;
    std::cout << 25865;
    for (int i = 0; i < 25865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25865(int& val) {
    const int x = 25865;
    std::cout << 25865;
    for (int i = 0; i < 25865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25866(int& val) {
    func_in_25866(val);
    const int x = 25866;
    std::cout << 25866;
    for (int i = 0; i < 25866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25866(int& val) {
    const int x = 25866;
    std::cout << 25866;
    for (int i = 0; i < 25866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25867(int& val) {
    func_in_25867(val);
    const int x = 25867;
    std::cout << 25867;
    for (int i = 0; i < 25867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25867(int& val) {
    const int x = 25867;
    std::cout << 25867;
    for (int i = 0; i < 25867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25868(int& val) {
    func_in_25868(val);
    const int x = 25868;
    std::cout << 25868;
    for (int i = 0; i < 25868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25868(int& val) {
    const int x = 25868;
    std::cout << 25868;
    for (int i = 0; i < 25868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25869(int& val) {
    func_in_25869(val);
    const int x = 25869;
    std::cout << 25869;
    for (int i = 0; i < 25869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25869(int& val) {
    const int x = 25869;
    std::cout << 25869;
    for (int i = 0; i < 25869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25870(int& val) {
    func_in_25870(val);
    const int x = 25870;
    std::cout << 25870;
    for (int i = 0; i < 25870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25870(int& val) {
    const int x = 25870;
    std::cout << 25870;
    for (int i = 0; i < 25870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25871(int& val) {
    func_in_25871(val);
    const int x = 25871;
    std::cout << 25871;
    for (int i = 0; i < 25871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25871(int& val) {
    const int x = 25871;
    std::cout << 25871;
    for (int i = 0; i < 25871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25872(int& val) {
    func_in_25872(val);
    const int x = 25872;
    std::cout << 25872;
    for (int i = 0; i < 25872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25872(int& val) {
    const int x = 25872;
    std::cout << 25872;
    for (int i = 0; i < 25872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25873(int& val) {
    func_in_25873(val);
    const int x = 25873;
    std::cout << 25873;
    for (int i = 0; i < 25873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25873(int& val) {
    const int x = 25873;
    std::cout << 25873;
    for (int i = 0; i < 25873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25874(int& val) {
    func_in_25874(val);
    const int x = 25874;
    std::cout << 25874;
    for (int i = 0; i < 25874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25874(int& val) {
    const int x = 25874;
    std::cout << 25874;
    for (int i = 0; i < 25874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25875(int& val) {
    func_in_25875(val);
    const int x = 25875;
    std::cout << 25875;
    for (int i = 0; i < 25875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25875(int& val) {
    const int x = 25875;
    std::cout << 25875;
    for (int i = 0; i < 25875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25876(int& val) {
    func_in_25876(val);
    const int x = 25876;
    std::cout << 25876;
    for (int i = 0; i < 25876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25876(int& val) {
    const int x = 25876;
    std::cout << 25876;
    for (int i = 0; i < 25876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25877(int& val) {
    func_in_25877(val);
    const int x = 25877;
    std::cout << 25877;
    for (int i = 0; i < 25877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25877(int& val) {
    const int x = 25877;
    std::cout << 25877;
    for (int i = 0; i < 25877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25878(int& val) {
    func_in_25878(val);
    const int x = 25878;
    std::cout << 25878;
    for (int i = 0; i < 25878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25878(int& val) {
    const int x = 25878;
    std::cout << 25878;
    for (int i = 0; i < 25878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25879(int& val) {
    func_in_25879(val);
    const int x = 25879;
    std::cout << 25879;
    for (int i = 0; i < 25879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25879(int& val) {
    const int x = 25879;
    std::cout << 25879;
    for (int i = 0; i < 25879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25880(int& val) {
    func_in_25880(val);
    const int x = 25880;
    std::cout << 25880;
    for (int i = 0; i < 25880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25880(int& val) {
    const int x = 25880;
    std::cout << 25880;
    for (int i = 0; i < 25880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25881(int& val) {
    func_in_25881(val);
    const int x = 25881;
    std::cout << 25881;
    for (int i = 0; i < 25881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25881(int& val) {
    const int x = 25881;
    std::cout << 25881;
    for (int i = 0; i < 25881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25882(int& val) {
    func_in_25882(val);
    const int x = 25882;
    std::cout << 25882;
    for (int i = 0; i < 25882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25882(int& val) {
    const int x = 25882;
    std::cout << 25882;
    for (int i = 0; i < 25882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25883(int& val) {
    func_in_25883(val);
    const int x = 25883;
    std::cout << 25883;
    for (int i = 0; i < 25883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25883(int& val) {
    const int x = 25883;
    std::cout << 25883;
    for (int i = 0; i < 25883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25884(int& val) {
    func_in_25884(val);
    const int x = 25884;
    std::cout << 25884;
    for (int i = 0; i < 25884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25884(int& val) {
    const int x = 25884;
    std::cout << 25884;
    for (int i = 0; i < 25884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25885(int& val) {
    func_in_25885(val);
    const int x = 25885;
    std::cout << 25885;
    for (int i = 0; i < 25885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25885(int& val) {
    const int x = 25885;
    std::cout << 25885;
    for (int i = 0; i < 25885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25886(int& val) {
    func_in_25886(val);
    const int x = 25886;
    std::cout << 25886;
    for (int i = 0; i < 25886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25886(int& val) {
    const int x = 25886;
    std::cout << 25886;
    for (int i = 0; i < 25886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25887(int& val) {
    func_in_25887(val);
    const int x = 25887;
    std::cout << 25887;
    for (int i = 0; i < 25887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25887(int& val) {
    const int x = 25887;
    std::cout << 25887;
    for (int i = 0; i < 25887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25888(int& val) {
    func_in_25888(val);
    const int x = 25888;
    std::cout << 25888;
    for (int i = 0; i < 25888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25888(int& val) {
    const int x = 25888;
    std::cout << 25888;
    for (int i = 0; i < 25888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25889(int& val) {
    func_in_25889(val);
    const int x = 25889;
    std::cout << 25889;
    for (int i = 0; i < 25889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25889(int& val) {
    const int x = 25889;
    std::cout << 25889;
    for (int i = 0; i < 25889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25890(int& val) {
    func_in_25890(val);
    const int x = 25890;
    std::cout << 25890;
    for (int i = 0; i < 25890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25890(int& val) {
    const int x = 25890;
    std::cout << 25890;
    for (int i = 0; i < 25890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25891(int& val) {
    func_in_25891(val);
    const int x = 25891;
    std::cout << 25891;
    for (int i = 0; i < 25891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25891(int& val) {
    const int x = 25891;
    std::cout << 25891;
    for (int i = 0; i < 25891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25892(int& val) {
    func_in_25892(val);
    const int x = 25892;
    std::cout << 25892;
    for (int i = 0; i < 25892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25892(int& val) {
    const int x = 25892;
    std::cout << 25892;
    for (int i = 0; i < 25892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25893(int& val) {
    func_in_25893(val);
    const int x = 25893;
    std::cout << 25893;
    for (int i = 0; i < 25893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25893(int& val) {
    const int x = 25893;
    std::cout << 25893;
    for (int i = 0; i < 25893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25894(int& val) {
    func_in_25894(val);
    const int x = 25894;
    std::cout << 25894;
    for (int i = 0; i < 25894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25894(int& val) {
    const int x = 25894;
    std::cout << 25894;
    for (int i = 0; i < 25894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25895(int& val) {
    func_in_25895(val);
    const int x = 25895;
    std::cout << 25895;
    for (int i = 0; i < 25895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25895(int& val) {
    const int x = 25895;
    std::cout << 25895;
    for (int i = 0; i < 25895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25896(int& val) {
    func_in_25896(val);
    const int x = 25896;
    std::cout << 25896;
    for (int i = 0; i < 25896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25896(int& val) {
    const int x = 25896;
    std::cout << 25896;
    for (int i = 0; i < 25896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25897(int& val) {
    func_in_25897(val);
    const int x = 25897;
    std::cout << 25897;
    for (int i = 0; i < 25897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25897(int& val) {
    const int x = 25897;
    std::cout << 25897;
    for (int i = 0; i < 25897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25898(int& val) {
    func_in_25898(val);
    const int x = 25898;
    std::cout << 25898;
    for (int i = 0; i < 25898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25898(int& val) {
    const int x = 25898;
    std::cout << 25898;
    for (int i = 0; i < 25898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25899(int& val) {
    func_in_25899(val);
    const int x = 25899;
    std::cout << 25899;
    for (int i = 0; i < 25899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25899(int& val) {
    const int x = 25899;
    std::cout << 25899;
    for (int i = 0; i < 25899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25900(int& val) {
    func_in_25900(val);
    const int x = 25900;
    std::cout << 25900;
    for (int i = 0; i < 25900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25900(int& val) {
    const int x = 25900;
    std::cout << 25900;
    for (int i = 0; i < 25900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25901(int& val) {
    func_in_25901(val);
    const int x = 25901;
    std::cout << 25901;
    for (int i = 0; i < 25901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25901(int& val) {
    const int x = 25901;
    std::cout << 25901;
    for (int i = 0; i < 25901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25902(int& val) {
    func_in_25902(val);
    const int x = 25902;
    std::cout << 25902;
    for (int i = 0; i < 25902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25902(int& val) {
    const int x = 25902;
    std::cout << 25902;
    for (int i = 0; i < 25902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25903(int& val) {
    func_in_25903(val);
    const int x = 25903;
    std::cout << 25903;
    for (int i = 0; i < 25903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25903(int& val) {
    const int x = 25903;
    std::cout << 25903;
    for (int i = 0; i < 25903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25904(int& val) {
    func_in_25904(val);
    const int x = 25904;
    std::cout << 25904;
    for (int i = 0; i < 25904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25904(int& val) {
    const int x = 25904;
    std::cout << 25904;
    for (int i = 0; i < 25904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25905(int& val) {
    func_in_25905(val);
    const int x = 25905;
    std::cout << 25905;
    for (int i = 0; i < 25905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25905(int& val) {
    const int x = 25905;
    std::cout << 25905;
    for (int i = 0; i < 25905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25906(int& val) {
    func_in_25906(val);
    const int x = 25906;
    std::cout << 25906;
    for (int i = 0; i < 25906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25906(int& val) {
    const int x = 25906;
    std::cout << 25906;
    for (int i = 0; i < 25906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25907(int& val) {
    func_in_25907(val);
    const int x = 25907;
    std::cout << 25907;
    for (int i = 0; i < 25907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25907(int& val) {
    const int x = 25907;
    std::cout << 25907;
    for (int i = 0; i < 25907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25908(int& val) {
    func_in_25908(val);
    const int x = 25908;
    std::cout << 25908;
    for (int i = 0; i < 25908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25908(int& val) {
    const int x = 25908;
    std::cout << 25908;
    for (int i = 0; i < 25908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25909(int& val) {
    func_in_25909(val);
    const int x = 25909;
    std::cout << 25909;
    for (int i = 0; i < 25909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25909(int& val) {
    const int x = 25909;
    std::cout << 25909;
    for (int i = 0; i < 25909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25910(int& val) {
    func_in_25910(val);
    const int x = 25910;
    std::cout << 25910;
    for (int i = 0; i < 25910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25910(int& val) {
    const int x = 25910;
    std::cout << 25910;
    for (int i = 0; i < 25910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25911(int& val) {
    func_in_25911(val);
    const int x = 25911;
    std::cout << 25911;
    for (int i = 0; i < 25911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25911(int& val) {
    const int x = 25911;
    std::cout << 25911;
    for (int i = 0; i < 25911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25912(int& val) {
    func_in_25912(val);
    const int x = 25912;
    std::cout << 25912;
    for (int i = 0; i < 25912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25912(int& val) {
    const int x = 25912;
    std::cout << 25912;
    for (int i = 0; i < 25912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25913(int& val) {
    func_in_25913(val);
    const int x = 25913;
    std::cout << 25913;
    for (int i = 0; i < 25913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25913(int& val) {
    const int x = 25913;
    std::cout << 25913;
    for (int i = 0; i < 25913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25914(int& val) {
    func_in_25914(val);
    const int x = 25914;
    std::cout << 25914;
    for (int i = 0; i < 25914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25914(int& val) {
    const int x = 25914;
    std::cout << 25914;
    for (int i = 0; i < 25914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25915(int& val) {
    func_in_25915(val);
    const int x = 25915;
    std::cout << 25915;
    for (int i = 0; i < 25915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25915(int& val) {
    const int x = 25915;
    std::cout << 25915;
    for (int i = 0; i < 25915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25916(int& val) {
    func_in_25916(val);
    const int x = 25916;
    std::cout << 25916;
    for (int i = 0; i < 25916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25916(int& val) {
    const int x = 25916;
    std::cout << 25916;
    for (int i = 0; i < 25916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25917(int& val) {
    func_in_25917(val);
    const int x = 25917;
    std::cout << 25917;
    for (int i = 0; i < 25917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25917(int& val) {
    const int x = 25917;
    std::cout << 25917;
    for (int i = 0; i < 25917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25918(int& val) {
    func_in_25918(val);
    const int x = 25918;
    std::cout << 25918;
    for (int i = 0; i < 25918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25918(int& val) {
    const int x = 25918;
    std::cout << 25918;
    for (int i = 0; i < 25918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25919(int& val) {
    func_in_25919(val);
    const int x = 25919;
    std::cout << 25919;
    for (int i = 0; i < 25919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25919(int& val) {
    const int x = 25919;
    std::cout << 25919;
    for (int i = 0; i < 25919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25920(int& val) {
    func_in_25920(val);
    const int x = 25920;
    std::cout << 25920;
    for (int i = 0; i < 25920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25920(int& val) {
    const int x = 25920;
    std::cout << 25920;
    for (int i = 0; i < 25920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25921(int& val) {
    func_in_25921(val);
    const int x = 25921;
    std::cout << 25921;
    for (int i = 0; i < 25921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25921(int& val) {
    const int x = 25921;
    std::cout << 25921;
    for (int i = 0; i < 25921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25922(int& val) {
    func_in_25922(val);
    const int x = 25922;
    std::cout << 25922;
    for (int i = 0; i < 25922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25922(int& val) {
    const int x = 25922;
    std::cout << 25922;
    for (int i = 0; i < 25922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25923(int& val) {
    func_in_25923(val);
    const int x = 25923;
    std::cout << 25923;
    for (int i = 0; i < 25923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25923(int& val) {
    const int x = 25923;
    std::cout << 25923;
    for (int i = 0; i < 25923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25924(int& val) {
    func_in_25924(val);
    const int x = 25924;
    std::cout << 25924;
    for (int i = 0; i < 25924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25924(int& val) {
    const int x = 25924;
    std::cout << 25924;
    for (int i = 0; i < 25924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25925(int& val) {
    func_in_25925(val);
    const int x = 25925;
    std::cout << 25925;
    for (int i = 0; i < 25925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25925(int& val) {
    const int x = 25925;
    std::cout << 25925;
    for (int i = 0; i < 25925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25926(int& val) {
    func_in_25926(val);
    const int x = 25926;
    std::cout << 25926;
    for (int i = 0; i < 25926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25926(int& val) {
    const int x = 25926;
    std::cout << 25926;
    for (int i = 0; i < 25926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25927(int& val) {
    func_in_25927(val);
    const int x = 25927;
    std::cout << 25927;
    for (int i = 0; i < 25927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25927(int& val) {
    const int x = 25927;
    std::cout << 25927;
    for (int i = 0; i < 25927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25928(int& val) {
    func_in_25928(val);
    const int x = 25928;
    std::cout << 25928;
    for (int i = 0; i < 25928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25928(int& val) {
    const int x = 25928;
    std::cout << 25928;
    for (int i = 0; i < 25928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25929(int& val) {
    func_in_25929(val);
    const int x = 25929;
    std::cout << 25929;
    for (int i = 0; i < 25929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25929(int& val) {
    const int x = 25929;
    std::cout << 25929;
    for (int i = 0; i < 25929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25930(int& val) {
    func_in_25930(val);
    const int x = 25930;
    std::cout << 25930;
    for (int i = 0; i < 25930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25930(int& val) {
    const int x = 25930;
    std::cout << 25930;
    for (int i = 0; i < 25930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25931(int& val) {
    func_in_25931(val);
    const int x = 25931;
    std::cout << 25931;
    for (int i = 0; i < 25931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25931(int& val) {
    const int x = 25931;
    std::cout << 25931;
    for (int i = 0; i < 25931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25932(int& val) {
    func_in_25932(val);
    const int x = 25932;
    std::cout << 25932;
    for (int i = 0; i < 25932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25932(int& val) {
    const int x = 25932;
    std::cout << 25932;
    for (int i = 0; i < 25932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25933(int& val) {
    func_in_25933(val);
    const int x = 25933;
    std::cout << 25933;
    for (int i = 0; i < 25933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25933(int& val) {
    const int x = 25933;
    std::cout << 25933;
    for (int i = 0; i < 25933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25934(int& val) {
    func_in_25934(val);
    const int x = 25934;
    std::cout << 25934;
    for (int i = 0; i < 25934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25934(int& val) {
    const int x = 25934;
    std::cout << 25934;
    for (int i = 0; i < 25934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25935(int& val) {
    func_in_25935(val);
    const int x = 25935;
    std::cout << 25935;
    for (int i = 0; i < 25935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25935(int& val) {
    const int x = 25935;
    std::cout << 25935;
    for (int i = 0; i < 25935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25936(int& val) {
    func_in_25936(val);
    const int x = 25936;
    std::cout << 25936;
    for (int i = 0; i < 25936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25936(int& val) {
    const int x = 25936;
    std::cout << 25936;
    for (int i = 0; i < 25936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25937(int& val) {
    func_in_25937(val);
    const int x = 25937;
    std::cout << 25937;
    for (int i = 0; i < 25937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25937(int& val) {
    const int x = 25937;
    std::cout << 25937;
    for (int i = 0; i < 25937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25938(int& val) {
    func_in_25938(val);
    const int x = 25938;
    std::cout << 25938;
    for (int i = 0; i < 25938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25938(int& val) {
    const int x = 25938;
    std::cout << 25938;
    for (int i = 0; i < 25938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25939(int& val) {
    func_in_25939(val);
    const int x = 25939;
    std::cout << 25939;
    for (int i = 0; i < 25939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25939(int& val) {
    const int x = 25939;
    std::cout << 25939;
    for (int i = 0; i < 25939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25940(int& val) {
    func_in_25940(val);
    const int x = 25940;
    std::cout << 25940;
    for (int i = 0; i < 25940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25940(int& val) {
    const int x = 25940;
    std::cout << 25940;
    for (int i = 0; i < 25940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25941(int& val) {
    func_in_25941(val);
    const int x = 25941;
    std::cout << 25941;
    for (int i = 0; i < 25941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25941(int& val) {
    const int x = 25941;
    std::cout << 25941;
    for (int i = 0; i < 25941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25942(int& val) {
    func_in_25942(val);
    const int x = 25942;
    std::cout << 25942;
    for (int i = 0; i < 25942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25942(int& val) {
    const int x = 25942;
    std::cout << 25942;
    for (int i = 0; i < 25942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25943(int& val) {
    func_in_25943(val);
    const int x = 25943;
    std::cout << 25943;
    for (int i = 0; i < 25943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25943(int& val) {
    const int x = 25943;
    std::cout << 25943;
    for (int i = 0; i < 25943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25944(int& val) {
    func_in_25944(val);
    const int x = 25944;
    std::cout << 25944;
    for (int i = 0; i < 25944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25944(int& val) {
    const int x = 25944;
    std::cout << 25944;
    for (int i = 0; i < 25944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25945(int& val) {
    func_in_25945(val);
    const int x = 25945;
    std::cout << 25945;
    for (int i = 0; i < 25945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25945(int& val) {
    const int x = 25945;
    std::cout << 25945;
    for (int i = 0; i < 25945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25946(int& val) {
    func_in_25946(val);
    const int x = 25946;
    std::cout << 25946;
    for (int i = 0; i < 25946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25946(int& val) {
    const int x = 25946;
    std::cout << 25946;
    for (int i = 0; i < 25946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25947(int& val) {
    func_in_25947(val);
    const int x = 25947;
    std::cout << 25947;
    for (int i = 0; i < 25947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25947(int& val) {
    const int x = 25947;
    std::cout << 25947;
    for (int i = 0; i < 25947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25948(int& val) {
    func_in_25948(val);
    const int x = 25948;
    std::cout << 25948;
    for (int i = 0; i < 25948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25948(int& val) {
    const int x = 25948;
    std::cout << 25948;
    for (int i = 0; i < 25948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25949(int& val) {
    func_in_25949(val);
    const int x = 25949;
    std::cout << 25949;
    for (int i = 0; i < 25949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25949(int& val) {
    const int x = 25949;
    std::cout << 25949;
    for (int i = 0; i < 25949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25950(int& val) {
    func_in_25950(val);
    const int x = 25950;
    std::cout << 25950;
    for (int i = 0; i < 25950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25950(int& val) {
    const int x = 25950;
    std::cout << 25950;
    for (int i = 0; i < 25950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25951(int& val) {
    func_in_25951(val);
    const int x = 25951;
    std::cout << 25951;
    for (int i = 0; i < 25951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25951(int& val) {
    const int x = 25951;
    std::cout << 25951;
    for (int i = 0; i < 25951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25952(int& val) {
    func_in_25952(val);
    const int x = 25952;
    std::cout << 25952;
    for (int i = 0; i < 25952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25952(int& val) {
    const int x = 25952;
    std::cout << 25952;
    for (int i = 0; i < 25952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25953(int& val) {
    func_in_25953(val);
    const int x = 25953;
    std::cout << 25953;
    for (int i = 0; i < 25953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25953(int& val) {
    const int x = 25953;
    std::cout << 25953;
    for (int i = 0; i < 25953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25954(int& val) {
    func_in_25954(val);
    const int x = 25954;
    std::cout << 25954;
    for (int i = 0; i < 25954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25954(int& val) {
    const int x = 25954;
    std::cout << 25954;
    for (int i = 0; i < 25954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25955(int& val) {
    func_in_25955(val);
    const int x = 25955;
    std::cout << 25955;
    for (int i = 0; i < 25955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25955(int& val) {
    const int x = 25955;
    std::cout << 25955;
    for (int i = 0; i < 25955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25956(int& val) {
    func_in_25956(val);
    const int x = 25956;
    std::cout << 25956;
    for (int i = 0; i < 25956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25956(int& val) {
    const int x = 25956;
    std::cout << 25956;
    for (int i = 0; i < 25956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25957(int& val) {
    func_in_25957(val);
    const int x = 25957;
    std::cout << 25957;
    for (int i = 0; i < 25957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25957(int& val) {
    const int x = 25957;
    std::cout << 25957;
    for (int i = 0; i < 25957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25958(int& val) {
    func_in_25958(val);
    const int x = 25958;
    std::cout << 25958;
    for (int i = 0; i < 25958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25958(int& val) {
    const int x = 25958;
    std::cout << 25958;
    for (int i = 0; i < 25958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25959(int& val) {
    func_in_25959(val);
    const int x = 25959;
    std::cout << 25959;
    for (int i = 0; i < 25959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25959(int& val) {
    const int x = 25959;
    std::cout << 25959;
    for (int i = 0; i < 25959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25960(int& val) {
    func_in_25960(val);
    const int x = 25960;
    std::cout << 25960;
    for (int i = 0; i < 25960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25960(int& val) {
    const int x = 25960;
    std::cout << 25960;
    for (int i = 0; i < 25960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25961(int& val) {
    func_in_25961(val);
    const int x = 25961;
    std::cout << 25961;
    for (int i = 0; i < 25961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25961(int& val) {
    const int x = 25961;
    std::cout << 25961;
    for (int i = 0; i < 25961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25962(int& val) {
    func_in_25962(val);
    const int x = 25962;
    std::cout << 25962;
    for (int i = 0; i < 25962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25962(int& val) {
    const int x = 25962;
    std::cout << 25962;
    for (int i = 0; i < 25962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25963(int& val) {
    func_in_25963(val);
    const int x = 25963;
    std::cout << 25963;
    for (int i = 0; i < 25963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25963(int& val) {
    const int x = 25963;
    std::cout << 25963;
    for (int i = 0; i < 25963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25964(int& val) {
    func_in_25964(val);
    const int x = 25964;
    std::cout << 25964;
    for (int i = 0; i < 25964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25964(int& val) {
    const int x = 25964;
    std::cout << 25964;
    for (int i = 0; i < 25964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25965(int& val) {
    func_in_25965(val);
    const int x = 25965;
    std::cout << 25965;
    for (int i = 0; i < 25965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25965(int& val) {
    const int x = 25965;
    std::cout << 25965;
    for (int i = 0; i < 25965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25966(int& val) {
    func_in_25966(val);
    const int x = 25966;
    std::cout << 25966;
    for (int i = 0; i < 25966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25966(int& val) {
    const int x = 25966;
    std::cout << 25966;
    for (int i = 0; i < 25966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25967(int& val) {
    func_in_25967(val);
    const int x = 25967;
    std::cout << 25967;
    for (int i = 0; i < 25967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25967(int& val) {
    const int x = 25967;
    std::cout << 25967;
    for (int i = 0; i < 25967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25968(int& val) {
    func_in_25968(val);
    const int x = 25968;
    std::cout << 25968;
    for (int i = 0; i < 25968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25968(int& val) {
    const int x = 25968;
    std::cout << 25968;
    for (int i = 0; i < 25968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25969(int& val) {
    func_in_25969(val);
    const int x = 25969;
    std::cout << 25969;
    for (int i = 0; i < 25969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25969(int& val) {
    const int x = 25969;
    std::cout << 25969;
    for (int i = 0; i < 25969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25970(int& val) {
    func_in_25970(val);
    const int x = 25970;
    std::cout << 25970;
    for (int i = 0; i < 25970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25970(int& val) {
    const int x = 25970;
    std::cout << 25970;
    for (int i = 0; i < 25970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25971(int& val) {
    func_in_25971(val);
    const int x = 25971;
    std::cout << 25971;
    for (int i = 0; i < 25971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25971(int& val) {
    const int x = 25971;
    std::cout << 25971;
    for (int i = 0; i < 25971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25972(int& val) {
    func_in_25972(val);
    const int x = 25972;
    std::cout << 25972;
    for (int i = 0; i < 25972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25972(int& val) {
    const int x = 25972;
    std::cout << 25972;
    for (int i = 0; i < 25972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25973(int& val) {
    func_in_25973(val);
    const int x = 25973;
    std::cout << 25973;
    for (int i = 0; i < 25973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25973(int& val) {
    const int x = 25973;
    std::cout << 25973;
    for (int i = 0; i < 25973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25974(int& val) {
    func_in_25974(val);
    const int x = 25974;
    std::cout << 25974;
    for (int i = 0; i < 25974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25974(int& val) {
    const int x = 25974;
    std::cout << 25974;
    for (int i = 0; i < 25974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25975(int& val) {
    func_in_25975(val);
    const int x = 25975;
    std::cout << 25975;
    for (int i = 0; i < 25975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25975(int& val) {
    const int x = 25975;
    std::cout << 25975;
    for (int i = 0; i < 25975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25976(int& val) {
    func_in_25976(val);
    const int x = 25976;
    std::cout << 25976;
    for (int i = 0; i < 25976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25976(int& val) {
    const int x = 25976;
    std::cout << 25976;
    for (int i = 0; i < 25976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25977(int& val) {
    func_in_25977(val);
    const int x = 25977;
    std::cout << 25977;
    for (int i = 0; i < 25977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25977(int& val) {
    const int x = 25977;
    std::cout << 25977;
    for (int i = 0; i < 25977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25978(int& val) {
    func_in_25978(val);
    const int x = 25978;
    std::cout << 25978;
    for (int i = 0; i < 25978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25978(int& val) {
    const int x = 25978;
    std::cout << 25978;
    for (int i = 0; i < 25978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25979(int& val) {
    func_in_25979(val);
    const int x = 25979;
    std::cout << 25979;
    for (int i = 0; i < 25979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25979(int& val) {
    const int x = 25979;
    std::cout << 25979;
    for (int i = 0; i < 25979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25980(int& val) {
    func_in_25980(val);
    const int x = 25980;
    std::cout << 25980;
    for (int i = 0; i < 25980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25980(int& val) {
    const int x = 25980;
    std::cout << 25980;
    for (int i = 0; i < 25980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25981(int& val) {
    func_in_25981(val);
    const int x = 25981;
    std::cout << 25981;
    for (int i = 0; i < 25981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25981(int& val) {
    const int x = 25981;
    std::cout << 25981;
    for (int i = 0; i < 25981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25982(int& val) {
    func_in_25982(val);
    const int x = 25982;
    std::cout << 25982;
    for (int i = 0; i < 25982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25982(int& val) {
    const int x = 25982;
    std::cout << 25982;
    for (int i = 0; i < 25982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25983(int& val) {
    func_in_25983(val);
    const int x = 25983;
    std::cout << 25983;
    for (int i = 0; i < 25983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25983(int& val) {
    const int x = 25983;
    std::cout << 25983;
    for (int i = 0; i < 25983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25984(int& val) {
    func_in_25984(val);
    const int x = 25984;
    std::cout << 25984;
    for (int i = 0; i < 25984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25984(int& val) {
    const int x = 25984;
    std::cout << 25984;
    for (int i = 0; i < 25984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25985(int& val) {
    func_in_25985(val);
    const int x = 25985;
    std::cout << 25985;
    for (int i = 0; i < 25985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25985(int& val) {
    const int x = 25985;
    std::cout << 25985;
    for (int i = 0; i < 25985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25986(int& val) {
    func_in_25986(val);
    const int x = 25986;
    std::cout << 25986;
    for (int i = 0; i < 25986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25986(int& val) {
    const int x = 25986;
    std::cout << 25986;
    for (int i = 0; i < 25986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25987(int& val) {
    func_in_25987(val);
    const int x = 25987;
    std::cout << 25987;
    for (int i = 0; i < 25987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25987(int& val) {
    const int x = 25987;
    std::cout << 25987;
    for (int i = 0; i < 25987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25988(int& val) {
    func_in_25988(val);
    const int x = 25988;
    std::cout << 25988;
    for (int i = 0; i < 25988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25988(int& val) {
    const int x = 25988;
    std::cout << 25988;
    for (int i = 0; i < 25988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25989(int& val) {
    func_in_25989(val);
    const int x = 25989;
    std::cout << 25989;
    for (int i = 0; i < 25989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25989(int& val) {
    const int x = 25989;
    std::cout << 25989;
    for (int i = 0; i < 25989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25990(int& val) {
    func_in_25990(val);
    const int x = 25990;
    std::cout << 25990;
    for (int i = 0; i < 25990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25990(int& val) {
    const int x = 25990;
    std::cout << 25990;
    for (int i = 0; i < 25990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25991(int& val) {
    func_in_25991(val);
    const int x = 25991;
    std::cout << 25991;
    for (int i = 0; i < 25991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25991(int& val) {
    const int x = 25991;
    std::cout << 25991;
    for (int i = 0; i < 25991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25992(int& val) {
    func_in_25992(val);
    const int x = 25992;
    std::cout << 25992;
    for (int i = 0; i < 25992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25992(int& val) {
    const int x = 25992;
    std::cout << 25992;
    for (int i = 0; i < 25992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25993(int& val) {
    func_in_25993(val);
    const int x = 25993;
    std::cout << 25993;
    for (int i = 0; i < 25993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25993(int& val) {
    const int x = 25993;
    std::cout << 25993;
    for (int i = 0; i < 25993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25994(int& val) {
    func_in_25994(val);
    const int x = 25994;
    std::cout << 25994;
    for (int i = 0; i < 25994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25994(int& val) {
    const int x = 25994;
    std::cout << 25994;
    for (int i = 0; i < 25994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25995(int& val) {
    func_in_25995(val);
    const int x = 25995;
    std::cout << 25995;
    for (int i = 0; i < 25995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25995(int& val) {
    const int x = 25995;
    std::cout << 25995;
    for (int i = 0; i < 25995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25996(int& val) {
    func_in_25996(val);
    const int x = 25996;
    std::cout << 25996;
    for (int i = 0; i < 25996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25996(int& val) {
    const int x = 25996;
    std::cout << 25996;
    for (int i = 0; i < 25996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25997(int& val) {
    func_in_25997(val);
    const int x = 25997;
    std::cout << 25997;
    for (int i = 0; i < 25997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25997(int& val) {
    const int x = 25997;
    std::cout << 25997;
    for (int i = 0; i < 25997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25998(int& val) {
    func_in_25998(val);
    const int x = 25998;
    std::cout << 25998;
    for (int i = 0; i < 25998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25998(int& val) {
    const int x = 25998;
    std::cout << 25998;
    for (int i = 0; i < 25998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25999(int& val) {
    func_in_25999(val);
    const int x = 25999;
    std::cout << 25999;
    for (int i = 0; i < 25999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25999(int& val) {
    const int x = 25999;
    std::cout << 25999;
    for (int i = 0; i < 25999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26000(int& val) {
    func_in_26000(val);
    const int x = 26000;
    std::cout << 26000;
    for (int i = 0; i < 26000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26000(int& val) {
    const int x = 26000;
    std::cout << 26000;
    for (int i = 0; i < 26000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26001(int& val) {
    func_in_26001(val);
    const int x = 26001;
    std::cout << 26001;
    for (int i = 0; i < 26001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26001(int& val) {
    const int x = 26001;
    std::cout << 26001;
    for (int i = 0; i < 26001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26002(int& val) {
    func_in_26002(val);
    const int x = 26002;
    std::cout << 26002;
    for (int i = 0; i < 26002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26002(int& val) {
    const int x = 26002;
    std::cout << 26002;
    for (int i = 0; i < 26002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26003(int& val) {
    func_in_26003(val);
    const int x = 26003;
    std::cout << 26003;
    for (int i = 0; i < 26003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26003(int& val) {
    const int x = 26003;
    std::cout << 26003;
    for (int i = 0; i < 26003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26004(int& val) {
    func_in_26004(val);
    const int x = 26004;
    std::cout << 26004;
    for (int i = 0; i < 26004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26004(int& val) {
    const int x = 26004;
    std::cout << 26004;
    for (int i = 0; i < 26004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26005(int& val) {
    func_in_26005(val);
    const int x = 26005;
    std::cout << 26005;
    for (int i = 0; i < 26005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26005(int& val) {
    const int x = 26005;
    std::cout << 26005;
    for (int i = 0; i < 26005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26006(int& val) {
    func_in_26006(val);
    const int x = 26006;
    std::cout << 26006;
    for (int i = 0; i < 26006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26006(int& val) {
    const int x = 26006;
    std::cout << 26006;
    for (int i = 0; i < 26006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26007(int& val) {
    func_in_26007(val);
    const int x = 26007;
    std::cout << 26007;
    for (int i = 0; i < 26007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26007(int& val) {
    const int x = 26007;
    std::cout << 26007;
    for (int i = 0; i < 26007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26008(int& val) {
    func_in_26008(val);
    const int x = 26008;
    std::cout << 26008;
    for (int i = 0; i < 26008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26008(int& val) {
    const int x = 26008;
    std::cout << 26008;
    for (int i = 0; i < 26008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26009(int& val) {
    func_in_26009(val);
    const int x = 26009;
    std::cout << 26009;
    for (int i = 0; i < 26009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26009(int& val) {
    const int x = 26009;
    std::cout << 26009;
    for (int i = 0; i < 26009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26010(int& val) {
    func_in_26010(val);
    const int x = 26010;
    std::cout << 26010;
    for (int i = 0; i < 26010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26010(int& val) {
    const int x = 26010;
    std::cout << 26010;
    for (int i = 0; i < 26010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26011(int& val) {
    func_in_26011(val);
    const int x = 26011;
    std::cout << 26011;
    for (int i = 0; i < 26011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26011(int& val) {
    const int x = 26011;
    std::cout << 26011;
    for (int i = 0; i < 26011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26012(int& val) {
    func_in_26012(val);
    const int x = 26012;
    std::cout << 26012;
    for (int i = 0; i < 26012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26012(int& val) {
    const int x = 26012;
    std::cout << 26012;
    for (int i = 0; i < 26012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26013(int& val) {
    func_in_26013(val);
    const int x = 26013;
    std::cout << 26013;
    for (int i = 0; i < 26013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26013(int& val) {
    const int x = 26013;
    std::cout << 26013;
    for (int i = 0; i < 26013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26014(int& val) {
    func_in_26014(val);
    const int x = 26014;
    std::cout << 26014;
    for (int i = 0; i < 26014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26014(int& val) {
    const int x = 26014;
    std::cout << 26014;
    for (int i = 0; i < 26014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26015(int& val) {
    func_in_26015(val);
    const int x = 26015;
    std::cout << 26015;
    for (int i = 0; i < 26015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26015(int& val) {
    const int x = 26015;
    std::cout << 26015;
    for (int i = 0; i < 26015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26016(int& val) {
    func_in_26016(val);
    const int x = 26016;
    std::cout << 26016;
    for (int i = 0; i < 26016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26016(int& val) {
    const int x = 26016;
    std::cout << 26016;
    for (int i = 0; i < 26016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26017(int& val) {
    func_in_26017(val);
    const int x = 26017;
    std::cout << 26017;
    for (int i = 0; i < 26017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26017(int& val) {
    const int x = 26017;
    std::cout << 26017;
    for (int i = 0; i < 26017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26018(int& val) {
    func_in_26018(val);
    const int x = 26018;
    std::cout << 26018;
    for (int i = 0; i < 26018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26018(int& val) {
    const int x = 26018;
    std::cout << 26018;
    for (int i = 0; i < 26018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26019(int& val) {
    func_in_26019(val);
    const int x = 26019;
    std::cout << 26019;
    for (int i = 0; i < 26019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26019(int& val) {
    const int x = 26019;
    std::cout << 26019;
    for (int i = 0; i < 26019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26020(int& val) {
    func_in_26020(val);
    const int x = 26020;
    std::cout << 26020;
    for (int i = 0; i < 26020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26020(int& val) {
    const int x = 26020;
    std::cout << 26020;
    for (int i = 0; i < 26020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26021(int& val) {
    func_in_26021(val);
    const int x = 26021;
    std::cout << 26021;
    for (int i = 0; i < 26021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26021(int& val) {
    const int x = 26021;
    std::cout << 26021;
    for (int i = 0; i < 26021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26022(int& val) {
    func_in_26022(val);
    const int x = 26022;
    std::cout << 26022;
    for (int i = 0; i < 26022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26022(int& val) {
    const int x = 26022;
    std::cout << 26022;
    for (int i = 0; i < 26022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26023(int& val) {
    func_in_26023(val);
    const int x = 26023;
    std::cout << 26023;
    for (int i = 0; i < 26023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26023(int& val) {
    const int x = 26023;
    std::cout << 26023;
    for (int i = 0; i < 26023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26024(int& val) {
    func_in_26024(val);
    const int x = 26024;
    std::cout << 26024;
    for (int i = 0; i < 26024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26024(int& val) {
    const int x = 26024;
    std::cout << 26024;
    for (int i = 0; i < 26024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26025(int& val) {
    func_in_26025(val);
    const int x = 26025;
    std::cout << 26025;
    for (int i = 0; i < 26025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26025(int& val) {
    const int x = 26025;
    std::cout << 26025;
    for (int i = 0; i < 26025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26026(int& val) {
    func_in_26026(val);
    const int x = 26026;
    std::cout << 26026;
    for (int i = 0; i < 26026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26026(int& val) {
    const int x = 26026;
    std::cout << 26026;
    for (int i = 0; i < 26026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26027(int& val) {
    func_in_26027(val);
    const int x = 26027;
    std::cout << 26027;
    for (int i = 0; i < 26027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26027(int& val) {
    const int x = 26027;
    std::cout << 26027;
    for (int i = 0; i < 26027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26028(int& val) {
    func_in_26028(val);
    const int x = 26028;
    std::cout << 26028;
    for (int i = 0; i < 26028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26028(int& val) {
    const int x = 26028;
    std::cout << 26028;
    for (int i = 0; i < 26028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26029(int& val) {
    func_in_26029(val);
    const int x = 26029;
    std::cout << 26029;
    for (int i = 0; i < 26029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26029(int& val) {
    const int x = 26029;
    std::cout << 26029;
    for (int i = 0; i < 26029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26030(int& val) {
    func_in_26030(val);
    const int x = 26030;
    std::cout << 26030;
    for (int i = 0; i < 26030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26030(int& val) {
    const int x = 26030;
    std::cout << 26030;
    for (int i = 0; i < 26030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26031(int& val) {
    func_in_26031(val);
    const int x = 26031;
    std::cout << 26031;
    for (int i = 0; i < 26031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26031(int& val) {
    const int x = 26031;
    std::cout << 26031;
    for (int i = 0; i < 26031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26032(int& val) {
    func_in_26032(val);
    const int x = 26032;
    std::cout << 26032;
    for (int i = 0; i < 26032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26032(int& val) {
    const int x = 26032;
    std::cout << 26032;
    for (int i = 0; i < 26032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26033(int& val) {
    func_in_26033(val);
    const int x = 26033;
    std::cout << 26033;
    for (int i = 0; i < 26033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26033(int& val) {
    const int x = 26033;
    std::cout << 26033;
    for (int i = 0; i < 26033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26034(int& val) {
    func_in_26034(val);
    const int x = 26034;
    std::cout << 26034;
    for (int i = 0; i < 26034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26034(int& val) {
    const int x = 26034;
    std::cout << 26034;
    for (int i = 0; i < 26034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26035(int& val) {
    func_in_26035(val);
    const int x = 26035;
    std::cout << 26035;
    for (int i = 0; i < 26035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26035(int& val) {
    const int x = 26035;
    std::cout << 26035;
    for (int i = 0; i < 26035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26036(int& val) {
    func_in_26036(val);
    const int x = 26036;
    std::cout << 26036;
    for (int i = 0; i < 26036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26036(int& val) {
    const int x = 26036;
    std::cout << 26036;
    for (int i = 0; i < 26036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26037(int& val) {
    func_in_26037(val);
    const int x = 26037;
    std::cout << 26037;
    for (int i = 0; i < 26037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26037(int& val) {
    const int x = 26037;
    std::cout << 26037;
    for (int i = 0; i < 26037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26038(int& val) {
    func_in_26038(val);
    const int x = 26038;
    std::cout << 26038;
    for (int i = 0; i < 26038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26038(int& val) {
    const int x = 26038;
    std::cout << 26038;
    for (int i = 0; i < 26038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26039(int& val) {
    func_in_26039(val);
    const int x = 26039;
    std::cout << 26039;
    for (int i = 0; i < 26039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26039(int& val) {
    const int x = 26039;
    std::cout << 26039;
    for (int i = 0; i < 26039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26040(int& val) {
    func_in_26040(val);
    const int x = 26040;
    std::cout << 26040;
    for (int i = 0; i < 26040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26040(int& val) {
    const int x = 26040;
    std::cout << 26040;
    for (int i = 0; i < 26040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26041(int& val) {
    func_in_26041(val);
    const int x = 26041;
    std::cout << 26041;
    for (int i = 0; i < 26041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26041(int& val) {
    const int x = 26041;
    std::cout << 26041;
    for (int i = 0; i < 26041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26042(int& val) {
    func_in_26042(val);
    const int x = 26042;
    std::cout << 26042;
    for (int i = 0; i < 26042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26042(int& val) {
    const int x = 26042;
    std::cout << 26042;
    for (int i = 0; i < 26042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26043(int& val) {
    func_in_26043(val);
    const int x = 26043;
    std::cout << 26043;
    for (int i = 0; i < 26043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26043(int& val) {
    const int x = 26043;
    std::cout << 26043;
    for (int i = 0; i < 26043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26044(int& val) {
    func_in_26044(val);
    const int x = 26044;
    std::cout << 26044;
    for (int i = 0; i < 26044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26044(int& val) {
    const int x = 26044;
    std::cout << 26044;
    for (int i = 0; i < 26044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26045(int& val) {
    func_in_26045(val);
    const int x = 26045;
    std::cout << 26045;
    for (int i = 0; i < 26045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26045(int& val) {
    const int x = 26045;
    std::cout << 26045;
    for (int i = 0; i < 26045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26046(int& val) {
    func_in_26046(val);
    const int x = 26046;
    std::cout << 26046;
    for (int i = 0; i < 26046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26046(int& val) {
    const int x = 26046;
    std::cout << 26046;
    for (int i = 0; i < 26046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26047(int& val) {
    func_in_26047(val);
    const int x = 26047;
    std::cout << 26047;
    for (int i = 0; i < 26047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26047(int& val) {
    const int x = 26047;
    std::cout << 26047;
    for (int i = 0; i < 26047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26048(int& val) {
    func_in_26048(val);
    const int x = 26048;
    std::cout << 26048;
    for (int i = 0; i < 26048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26048(int& val) {
    const int x = 26048;
    std::cout << 26048;
    for (int i = 0; i < 26048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26049(int& val) {
    func_in_26049(val);
    const int x = 26049;
    std::cout << 26049;
    for (int i = 0; i < 26049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26049(int& val) {
    const int x = 26049;
    std::cout << 26049;
    for (int i = 0; i < 26049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26050(int& val) {
    func_in_26050(val);
    const int x = 26050;
    std::cout << 26050;
    for (int i = 0; i < 26050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26050(int& val) {
    const int x = 26050;
    std::cout << 26050;
    for (int i = 0; i < 26050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26051(int& val) {
    func_in_26051(val);
    const int x = 26051;
    std::cout << 26051;
    for (int i = 0; i < 26051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26051(int& val) {
    const int x = 26051;
    std::cout << 26051;
    for (int i = 0; i < 26051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26052(int& val) {
    func_in_26052(val);
    const int x = 26052;
    std::cout << 26052;
    for (int i = 0; i < 26052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26052(int& val) {
    const int x = 26052;
    std::cout << 26052;
    for (int i = 0; i < 26052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26053(int& val) {
    func_in_26053(val);
    const int x = 26053;
    std::cout << 26053;
    for (int i = 0; i < 26053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26053(int& val) {
    const int x = 26053;
    std::cout << 26053;
    for (int i = 0; i < 26053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26054(int& val) {
    func_in_26054(val);
    const int x = 26054;
    std::cout << 26054;
    for (int i = 0; i < 26054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26054(int& val) {
    const int x = 26054;
    std::cout << 26054;
    for (int i = 0; i < 26054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26055(int& val) {
    func_in_26055(val);
    const int x = 26055;
    std::cout << 26055;
    for (int i = 0; i < 26055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26055(int& val) {
    const int x = 26055;
    std::cout << 26055;
    for (int i = 0; i < 26055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26056(int& val) {
    func_in_26056(val);
    const int x = 26056;
    std::cout << 26056;
    for (int i = 0; i < 26056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26056(int& val) {
    const int x = 26056;
    std::cout << 26056;
    for (int i = 0; i < 26056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26057(int& val) {
    func_in_26057(val);
    const int x = 26057;
    std::cout << 26057;
    for (int i = 0; i < 26057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26057(int& val) {
    const int x = 26057;
    std::cout << 26057;
    for (int i = 0; i < 26057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26058(int& val) {
    func_in_26058(val);
    const int x = 26058;
    std::cout << 26058;
    for (int i = 0; i < 26058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26058(int& val) {
    const int x = 26058;
    std::cout << 26058;
    for (int i = 0; i < 26058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26059(int& val) {
    func_in_26059(val);
    const int x = 26059;
    std::cout << 26059;
    for (int i = 0; i < 26059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26059(int& val) {
    const int x = 26059;
    std::cout << 26059;
    for (int i = 0; i < 26059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26060(int& val) {
    func_in_26060(val);
    const int x = 26060;
    std::cout << 26060;
    for (int i = 0; i < 26060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26060(int& val) {
    const int x = 26060;
    std::cout << 26060;
    for (int i = 0; i < 26060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26061(int& val) {
    func_in_26061(val);
    const int x = 26061;
    std::cout << 26061;
    for (int i = 0; i < 26061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26061(int& val) {
    const int x = 26061;
    std::cout << 26061;
    for (int i = 0; i < 26061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26062(int& val) {
    func_in_26062(val);
    const int x = 26062;
    std::cout << 26062;
    for (int i = 0; i < 26062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26062(int& val) {
    const int x = 26062;
    std::cout << 26062;
    for (int i = 0; i < 26062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26063(int& val) {
    func_in_26063(val);
    const int x = 26063;
    std::cout << 26063;
    for (int i = 0; i < 26063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26063(int& val) {
    const int x = 26063;
    std::cout << 26063;
    for (int i = 0; i < 26063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26064(int& val) {
    func_in_26064(val);
    const int x = 26064;
    std::cout << 26064;
    for (int i = 0; i < 26064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26064(int& val) {
    const int x = 26064;
    std::cout << 26064;
    for (int i = 0; i < 26064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26065(int& val) {
    func_in_26065(val);
    const int x = 26065;
    std::cout << 26065;
    for (int i = 0; i < 26065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26065(int& val) {
    const int x = 26065;
    std::cout << 26065;
    for (int i = 0; i < 26065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26066(int& val) {
    func_in_26066(val);
    const int x = 26066;
    std::cout << 26066;
    for (int i = 0; i < 26066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26066(int& val) {
    const int x = 26066;
    std::cout << 26066;
    for (int i = 0; i < 26066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26067(int& val) {
    func_in_26067(val);
    const int x = 26067;
    std::cout << 26067;
    for (int i = 0; i < 26067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26067(int& val) {
    const int x = 26067;
    std::cout << 26067;
    for (int i = 0; i < 26067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26068(int& val) {
    func_in_26068(val);
    const int x = 26068;
    std::cout << 26068;
    for (int i = 0; i < 26068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26068(int& val) {
    const int x = 26068;
    std::cout << 26068;
    for (int i = 0; i < 26068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26069(int& val) {
    func_in_26069(val);
    const int x = 26069;
    std::cout << 26069;
    for (int i = 0; i < 26069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26069(int& val) {
    const int x = 26069;
    std::cout << 26069;
    for (int i = 0; i < 26069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26070(int& val) {
    func_in_26070(val);
    const int x = 26070;
    std::cout << 26070;
    for (int i = 0; i < 26070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26070(int& val) {
    const int x = 26070;
    std::cout << 26070;
    for (int i = 0; i < 26070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26071(int& val) {
    func_in_26071(val);
    const int x = 26071;
    std::cout << 26071;
    for (int i = 0; i < 26071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26071(int& val) {
    const int x = 26071;
    std::cout << 26071;
    for (int i = 0; i < 26071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26072(int& val) {
    func_in_26072(val);
    const int x = 26072;
    std::cout << 26072;
    for (int i = 0; i < 26072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26072(int& val) {
    const int x = 26072;
    std::cout << 26072;
    for (int i = 0; i < 26072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26073(int& val) {
    func_in_26073(val);
    const int x = 26073;
    std::cout << 26073;
    for (int i = 0; i < 26073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26073(int& val) {
    const int x = 26073;
    std::cout << 26073;
    for (int i = 0; i < 26073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26074(int& val) {
    func_in_26074(val);
    const int x = 26074;
    std::cout << 26074;
    for (int i = 0; i < 26074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26074(int& val) {
    const int x = 26074;
    std::cout << 26074;
    for (int i = 0; i < 26074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26075(int& val) {
    func_in_26075(val);
    const int x = 26075;
    std::cout << 26075;
    for (int i = 0; i < 26075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26075(int& val) {
    const int x = 26075;
    std::cout << 26075;
    for (int i = 0; i < 26075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26076(int& val) {
    func_in_26076(val);
    const int x = 26076;
    std::cout << 26076;
    for (int i = 0; i < 26076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26076(int& val) {
    const int x = 26076;
    std::cout << 26076;
    for (int i = 0; i < 26076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26077(int& val) {
    func_in_26077(val);
    const int x = 26077;
    std::cout << 26077;
    for (int i = 0; i < 26077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26077(int& val) {
    const int x = 26077;
    std::cout << 26077;
    for (int i = 0; i < 26077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26078(int& val) {
    func_in_26078(val);
    const int x = 26078;
    std::cout << 26078;
    for (int i = 0; i < 26078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26078(int& val) {
    const int x = 26078;
    std::cout << 26078;
    for (int i = 0; i < 26078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26079(int& val) {
    func_in_26079(val);
    const int x = 26079;
    std::cout << 26079;
    for (int i = 0; i < 26079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26079(int& val) {
    const int x = 26079;
    std::cout << 26079;
    for (int i = 0; i < 26079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26080(int& val) {
    func_in_26080(val);
    const int x = 26080;
    std::cout << 26080;
    for (int i = 0; i < 26080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26080(int& val) {
    const int x = 26080;
    std::cout << 26080;
    for (int i = 0; i < 26080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26081(int& val) {
    func_in_26081(val);
    const int x = 26081;
    std::cout << 26081;
    for (int i = 0; i < 26081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26081(int& val) {
    const int x = 26081;
    std::cout << 26081;
    for (int i = 0; i < 26081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26082(int& val) {
    func_in_26082(val);
    const int x = 26082;
    std::cout << 26082;
    for (int i = 0; i < 26082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26082(int& val) {
    const int x = 26082;
    std::cout << 26082;
    for (int i = 0; i < 26082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26083(int& val) {
    func_in_26083(val);
    const int x = 26083;
    std::cout << 26083;
    for (int i = 0; i < 26083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26083(int& val) {
    const int x = 26083;
    std::cout << 26083;
    for (int i = 0; i < 26083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26084(int& val) {
    func_in_26084(val);
    const int x = 26084;
    std::cout << 26084;
    for (int i = 0; i < 26084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26084(int& val) {
    const int x = 26084;
    std::cout << 26084;
    for (int i = 0; i < 26084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26085(int& val) {
    func_in_26085(val);
    const int x = 26085;
    std::cout << 26085;
    for (int i = 0; i < 26085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26085(int& val) {
    const int x = 26085;
    std::cout << 26085;
    for (int i = 0; i < 26085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26086(int& val) {
    func_in_26086(val);
    const int x = 26086;
    std::cout << 26086;
    for (int i = 0; i < 26086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26086(int& val) {
    const int x = 26086;
    std::cout << 26086;
    for (int i = 0; i < 26086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26087(int& val) {
    func_in_26087(val);
    const int x = 26087;
    std::cout << 26087;
    for (int i = 0; i < 26087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26087(int& val) {
    const int x = 26087;
    std::cout << 26087;
    for (int i = 0; i < 26087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26088(int& val) {
    func_in_26088(val);
    const int x = 26088;
    std::cout << 26088;
    for (int i = 0; i < 26088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26088(int& val) {
    const int x = 26088;
    std::cout << 26088;
    for (int i = 0; i < 26088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26089(int& val) {
    func_in_26089(val);
    const int x = 26089;
    std::cout << 26089;
    for (int i = 0; i < 26089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26089(int& val) {
    const int x = 26089;
    std::cout << 26089;
    for (int i = 0; i < 26089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26090(int& val) {
    func_in_26090(val);
    const int x = 26090;
    std::cout << 26090;
    for (int i = 0; i < 26090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26090(int& val) {
    const int x = 26090;
    std::cout << 26090;
    for (int i = 0; i < 26090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26091(int& val) {
    func_in_26091(val);
    const int x = 26091;
    std::cout << 26091;
    for (int i = 0; i < 26091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26091(int& val) {
    const int x = 26091;
    std::cout << 26091;
    for (int i = 0; i < 26091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26092(int& val) {
    func_in_26092(val);
    const int x = 26092;
    std::cout << 26092;
    for (int i = 0; i < 26092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26092(int& val) {
    const int x = 26092;
    std::cout << 26092;
    for (int i = 0; i < 26092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26093(int& val) {
    func_in_26093(val);
    const int x = 26093;
    std::cout << 26093;
    for (int i = 0; i < 26093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26093(int& val) {
    const int x = 26093;
    std::cout << 26093;
    for (int i = 0; i < 26093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26094(int& val) {
    func_in_26094(val);
    const int x = 26094;
    std::cout << 26094;
    for (int i = 0; i < 26094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26094(int& val) {
    const int x = 26094;
    std::cout << 26094;
    for (int i = 0; i < 26094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26095(int& val) {
    func_in_26095(val);
    const int x = 26095;
    std::cout << 26095;
    for (int i = 0; i < 26095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26095(int& val) {
    const int x = 26095;
    std::cout << 26095;
    for (int i = 0; i < 26095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26096(int& val) {
    func_in_26096(val);
    const int x = 26096;
    std::cout << 26096;
    for (int i = 0; i < 26096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26096(int& val) {
    const int x = 26096;
    std::cout << 26096;
    for (int i = 0; i < 26096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26097(int& val) {
    func_in_26097(val);
    const int x = 26097;
    std::cout << 26097;
    for (int i = 0; i < 26097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26097(int& val) {
    const int x = 26097;
    std::cout << 26097;
    for (int i = 0; i < 26097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26098(int& val) {
    func_in_26098(val);
    const int x = 26098;
    std::cout << 26098;
    for (int i = 0; i < 26098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26098(int& val) {
    const int x = 26098;
    std::cout << 26098;
    for (int i = 0; i < 26098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26099(int& val) {
    func_in_26099(val);
    const int x = 26099;
    std::cout << 26099;
    for (int i = 0; i < 26099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26099(int& val) {
    const int x = 26099;
    std::cout << 26099;
    for (int i = 0; i < 26099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26100(int& val) {
    func_in_26100(val);
    const int x = 26100;
    std::cout << 26100;
    for (int i = 0; i < 26100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26100(int& val) {
    const int x = 26100;
    std::cout << 26100;
    for (int i = 0; i < 26100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26101(int& val) {
    func_in_26101(val);
    const int x = 26101;
    std::cout << 26101;
    for (int i = 0; i < 26101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26101(int& val) {
    const int x = 26101;
    std::cout << 26101;
    for (int i = 0; i < 26101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26102(int& val) {
    func_in_26102(val);
    const int x = 26102;
    std::cout << 26102;
    for (int i = 0; i < 26102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26102(int& val) {
    const int x = 26102;
    std::cout << 26102;
    for (int i = 0; i < 26102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26103(int& val) {
    func_in_26103(val);
    const int x = 26103;
    std::cout << 26103;
    for (int i = 0; i < 26103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26103(int& val) {
    const int x = 26103;
    std::cout << 26103;
    for (int i = 0; i < 26103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26104(int& val) {
    func_in_26104(val);
    const int x = 26104;
    std::cout << 26104;
    for (int i = 0; i < 26104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26104(int& val) {
    const int x = 26104;
    std::cout << 26104;
    for (int i = 0; i < 26104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26105(int& val) {
    func_in_26105(val);
    const int x = 26105;
    std::cout << 26105;
    for (int i = 0; i < 26105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26105(int& val) {
    const int x = 26105;
    std::cout << 26105;
    for (int i = 0; i < 26105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26106(int& val) {
    func_in_26106(val);
    const int x = 26106;
    std::cout << 26106;
    for (int i = 0; i < 26106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26106(int& val) {
    const int x = 26106;
    std::cout << 26106;
    for (int i = 0; i < 26106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26107(int& val) {
    func_in_26107(val);
    const int x = 26107;
    std::cout << 26107;
    for (int i = 0; i < 26107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26107(int& val) {
    const int x = 26107;
    std::cout << 26107;
    for (int i = 0; i < 26107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26108(int& val) {
    func_in_26108(val);
    const int x = 26108;
    std::cout << 26108;
    for (int i = 0; i < 26108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26108(int& val) {
    const int x = 26108;
    std::cout << 26108;
    for (int i = 0; i < 26108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26109(int& val) {
    func_in_26109(val);
    const int x = 26109;
    std::cout << 26109;
    for (int i = 0; i < 26109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26109(int& val) {
    const int x = 26109;
    std::cout << 26109;
    for (int i = 0; i < 26109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26110(int& val) {
    func_in_26110(val);
    const int x = 26110;
    std::cout << 26110;
    for (int i = 0; i < 26110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26110(int& val) {
    const int x = 26110;
    std::cout << 26110;
    for (int i = 0; i < 26110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26111(int& val) {
    func_in_26111(val);
    const int x = 26111;
    std::cout << 26111;
    for (int i = 0; i < 26111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26111(int& val) {
    const int x = 26111;
    std::cout << 26111;
    for (int i = 0; i < 26111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26112(int& val) {
    func_in_26112(val);
    const int x = 26112;
    std::cout << 26112;
    for (int i = 0; i < 26112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26112(int& val) {
    const int x = 26112;
    std::cout << 26112;
    for (int i = 0; i < 26112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26113(int& val) {
    func_in_26113(val);
    const int x = 26113;
    std::cout << 26113;
    for (int i = 0; i < 26113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26113(int& val) {
    const int x = 26113;
    std::cout << 26113;
    for (int i = 0; i < 26113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26114(int& val) {
    func_in_26114(val);
    const int x = 26114;
    std::cout << 26114;
    for (int i = 0; i < 26114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26114(int& val) {
    const int x = 26114;
    std::cout << 26114;
    for (int i = 0; i < 26114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26115(int& val) {
    func_in_26115(val);
    const int x = 26115;
    std::cout << 26115;
    for (int i = 0; i < 26115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26115(int& val) {
    const int x = 26115;
    std::cout << 26115;
    for (int i = 0; i < 26115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26116(int& val) {
    func_in_26116(val);
    const int x = 26116;
    std::cout << 26116;
    for (int i = 0; i < 26116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26116(int& val) {
    const int x = 26116;
    std::cout << 26116;
    for (int i = 0; i < 26116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26117(int& val) {
    func_in_26117(val);
    const int x = 26117;
    std::cout << 26117;
    for (int i = 0; i < 26117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26117(int& val) {
    const int x = 26117;
    std::cout << 26117;
    for (int i = 0; i < 26117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26118(int& val) {
    func_in_26118(val);
    const int x = 26118;
    std::cout << 26118;
    for (int i = 0; i < 26118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26118(int& val) {
    const int x = 26118;
    std::cout << 26118;
    for (int i = 0; i < 26118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26119(int& val) {
    func_in_26119(val);
    const int x = 26119;
    std::cout << 26119;
    for (int i = 0; i < 26119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26119(int& val) {
    const int x = 26119;
    std::cout << 26119;
    for (int i = 0; i < 26119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26120(int& val) {
    func_in_26120(val);
    const int x = 26120;
    std::cout << 26120;
    for (int i = 0; i < 26120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26120(int& val) {
    const int x = 26120;
    std::cout << 26120;
    for (int i = 0; i < 26120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26121(int& val) {
    func_in_26121(val);
    const int x = 26121;
    std::cout << 26121;
    for (int i = 0; i < 26121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26121(int& val) {
    const int x = 26121;
    std::cout << 26121;
    for (int i = 0; i < 26121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26122(int& val) {
    func_in_26122(val);
    const int x = 26122;
    std::cout << 26122;
    for (int i = 0; i < 26122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26122(int& val) {
    const int x = 26122;
    std::cout << 26122;
    for (int i = 0; i < 26122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26123(int& val) {
    func_in_26123(val);
    const int x = 26123;
    std::cout << 26123;
    for (int i = 0; i < 26123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26123(int& val) {
    const int x = 26123;
    std::cout << 26123;
    for (int i = 0; i < 26123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26124(int& val) {
    func_in_26124(val);
    const int x = 26124;
    std::cout << 26124;
    for (int i = 0; i < 26124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26124(int& val) {
    const int x = 26124;
    std::cout << 26124;
    for (int i = 0; i < 26124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26125(int& val) {
    func_in_26125(val);
    const int x = 26125;
    std::cout << 26125;
    for (int i = 0; i < 26125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26125(int& val) {
    const int x = 26125;
    std::cout << 26125;
    for (int i = 0; i < 26125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26126(int& val) {
    func_in_26126(val);
    const int x = 26126;
    std::cout << 26126;
    for (int i = 0; i < 26126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26126(int& val) {
    const int x = 26126;
    std::cout << 26126;
    for (int i = 0; i < 26126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26127(int& val) {
    func_in_26127(val);
    const int x = 26127;
    std::cout << 26127;
    for (int i = 0; i < 26127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26127(int& val) {
    const int x = 26127;
    std::cout << 26127;
    for (int i = 0; i < 26127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26128(int& val) {
    func_in_26128(val);
    const int x = 26128;
    std::cout << 26128;
    for (int i = 0; i < 26128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26128(int& val) {
    const int x = 26128;
    std::cout << 26128;
    for (int i = 0; i < 26128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26129(int& val) {
    func_in_26129(val);
    const int x = 26129;
    std::cout << 26129;
    for (int i = 0; i < 26129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26129(int& val) {
    const int x = 26129;
    std::cout << 26129;
    for (int i = 0; i < 26129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26130(int& val) {
    func_in_26130(val);
    const int x = 26130;
    std::cout << 26130;
    for (int i = 0; i < 26130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26130(int& val) {
    const int x = 26130;
    std::cout << 26130;
    for (int i = 0; i < 26130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26131(int& val) {
    func_in_26131(val);
    const int x = 26131;
    std::cout << 26131;
    for (int i = 0; i < 26131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26131(int& val) {
    const int x = 26131;
    std::cout << 26131;
    for (int i = 0; i < 26131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26132(int& val) {
    func_in_26132(val);
    const int x = 26132;
    std::cout << 26132;
    for (int i = 0; i < 26132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26132(int& val) {
    const int x = 26132;
    std::cout << 26132;
    for (int i = 0; i < 26132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26133(int& val) {
    func_in_26133(val);
    const int x = 26133;
    std::cout << 26133;
    for (int i = 0; i < 26133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26133(int& val) {
    const int x = 26133;
    std::cout << 26133;
    for (int i = 0; i < 26133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26134(int& val) {
    func_in_26134(val);
    const int x = 26134;
    std::cout << 26134;
    for (int i = 0; i < 26134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26134(int& val) {
    const int x = 26134;
    std::cout << 26134;
    for (int i = 0; i < 26134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26135(int& val) {
    func_in_26135(val);
    const int x = 26135;
    std::cout << 26135;
    for (int i = 0; i < 26135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26135(int& val) {
    const int x = 26135;
    std::cout << 26135;
    for (int i = 0; i < 26135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26136(int& val) {
    func_in_26136(val);
    const int x = 26136;
    std::cout << 26136;
    for (int i = 0; i < 26136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26136(int& val) {
    const int x = 26136;
    std::cout << 26136;
    for (int i = 0; i < 26136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26137(int& val) {
    func_in_26137(val);
    const int x = 26137;
    std::cout << 26137;
    for (int i = 0; i < 26137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26137(int& val) {
    const int x = 26137;
    std::cout << 26137;
    for (int i = 0; i < 26137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26138(int& val) {
    func_in_26138(val);
    const int x = 26138;
    std::cout << 26138;
    for (int i = 0; i < 26138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26138(int& val) {
    const int x = 26138;
    std::cout << 26138;
    for (int i = 0; i < 26138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26139(int& val) {
    func_in_26139(val);
    const int x = 26139;
    std::cout << 26139;
    for (int i = 0; i < 26139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26139(int& val) {
    const int x = 26139;
    std::cout << 26139;
    for (int i = 0; i < 26139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26140(int& val) {
    func_in_26140(val);
    const int x = 26140;
    std::cout << 26140;
    for (int i = 0; i < 26140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26140(int& val) {
    const int x = 26140;
    std::cout << 26140;
    for (int i = 0; i < 26140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26141(int& val) {
    func_in_26141(val);
    const int x = 26141;
    std::cout << 26141;
    for (int i = 0; i < 26141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26141(int& val) {
    const int x = 26141;
    std::cout << 26141;
    for (int i = 0; i < 26141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26142(int& val) {
    func_in_26142(val);
    const int x = 26142;
    std::cout << 26142;
    for (int i = 0; i < 26142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26142(int& val) {
    const int x = 26142;
    std::cout << 26142;
    for (int i = 0; i < 26142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26143(int& val) {
    func_in_26143(val);
    const int x = 26143;
    std::cout << 26143;
    for (int i = 0; i < 26143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26143(int& val) {
    const int x = 26143;
    std::cout << 26143;
    for (int i = 0; i < 26143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26144(int& val) {
    func_in_26144(val);
    const int x = 26144;
    std::cout << 26144;
    for (int i = 0; i < 26144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26144(int& val) {
    const int x = 26144;
    std::cout << 26144;
    for (int i = 0; i < 26144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26145(int& val) {
    func_in_26145(val);
    const int x = 26145;
    std::cout << 26145;
    for (int i = 0; i < 26145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26145(int& val) {
    const int x = 26145;
    std::cout << 26145;
    for (int i = 0; i < 26145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26146(int& val) {
    func_in_26146(val);
    const int x = 26146;
    std::cout << 26146;
    for (int i = 0; i < 26146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26146(int& val) {
    const int x = 26146;
    std::cout << 26146;
    for (int i = 0; i < 26146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26147(int& val) {
    func_in_26147(val);
    const int x = 26147;
    std::cout << 26147;
    for (int i = 0; i < 26147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26147(int& val) {
    const int x = 26147;
    std::cout << 26147;
    for (int i = 0; i < 26147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26148(int& val) {
    func_in_26148(val);
    const int x = 26148;
    std::cout << 26148;
    for (int i = 0; i < 26148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26148(int& val) {
    const int x = 26148;
    std::cout << 26148;
    for (int i = 0; i < 26148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26149(int& val) {
    func_in_26149(val);
    const int x = 26149;
    std::cout << 26149;
    for (int i = 0; i < 26149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26149(int& val) {
    const int x = 26149;
    std::cout << 26149;
    for (int i = 0; i < 26149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26150(int& val) {
    func_in_26150(val);
    const int x = 26150;
    std::cout << 26150;
    for (int i = 0; i < 26150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26150(int& val) {
    const int x = 26150;
    std::cout << 26150;
    for (int i = 0; i < 26150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26151(int& val) {
    func_in_26151(val);
    const int x = 26151;
    std::cout << 26151;
    for (int i = 0; i < 26151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26151(int& val) {
    const int x = 26151;
    std::cout << 26151;
    for (int i = 0; i < 26151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26152(int& val) {
    func_in_26152(val);
    const int x = 26152;
    std::cout << 26152;
    for (int i = 0; i < 26152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26152(int& val) {
    const int x = 26152;
    std::cout << 26152;
    for (int i = 0; i < 26152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26153(int& val) {
    func_in_26153(val);
    const int x = 26153;
    std::cout << 26153;
    for (int i = 0; i < 26153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26153(int& val) {
    const int x = 26153;
    std::cout << 26153;
    for (int i = 0; i < 26153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26154(int& val) {
    func_in_26154(val);
    const int x = 26154;
    std::cout << 26154;
    for (int i = 0; i < 26154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26154(int& val) {
    const int x = 26154;
    std::cout << 26154;
    for (int i = 0; i < 26154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26155(int& val) {
    func_in_26155(val);
    const int x = 26155;
    std::cout << 26155;
    for (int i = 0; i < 26155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26155(int& val) {
    const int x = 26155;
    std::cout << 26155;
    for (int i = 0; i < 26155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26156(int& val) {
    func_in_26156(val);
    const int x = 26156;
    std::cout << 26156;
    for (int i = 0; i < 26156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26156(int& val) {
    const int x = 26156;
    std::cout << 26156;
    for (int i = 0; i < 26156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26157(int& val) {
    func_in_26157(val);
    const int x = 26157;
    std::cout << 26157;
    for (int i = 0; i < 26157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26157(int& val) {
    const int x = 26157;
    std::cout << 26157;
    for (int i = 0; i < 26157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26158(int& val) {
    func_in_26158(val);
    const int x = 26158;
    std::cout << 26158;
    for (int i = 0; i < 26158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26158(int& val) {
    const int x = 26158;
    std::cout << 26158;
    for (int i = 0; i < 26158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26159(int& val) {
    func_in_26159(val);
    const int x = 26159;
    std::cout << 26159;
    for (int i = 0; i < 26159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26159(int& val) {
    const int x = 26159;
    std::cout << 26159;
    for (int i = 0; i < 26159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26160(int& val) {
    func_in_26160(val);
    const int x = 26160;
    std::cout << 26160;
    for (int i = 0; i < 26160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26160(int& val) {
    const int x = 26160;
    std::cout << 26160;
    for (int i = 0; i < 26160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26161(int& val) {
    func_in_26161(val);
    const int x = 26161;
    std::cout << 26161;
    for (int i = 0; i < 26161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26161(int& val) {
    const int x = 26161;
    std::cout << 26161;
    for (int i = 0; i < 26161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26162(int& val) {
    func_in_26162(val);
    const int x = 26162;
    std::cout << 26162;
    for (int i = 0; i < 26162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26162(int& val) {
    const int x = 26162;
    std::cout << 26162;
    for (int i = 0; i < 26162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26163(int& val) {
    func_in_26163(val);
    const int x = 26163;
    std::cout << 26163;
    for (int i = 0; i < 26163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26163(int& val) {
    const int x = 26163;
    std::cout << 26163;
    for (int i = 0; i < 26163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26164(int& val) {
    func_in_26164(val);
    const int x = 26164;
    std::cout << 26164;
    for (int i = 0; i < 26164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26164(int& val) {
    const int x = 26164;
    std::cout << 26164;
    for (int i = 0; i < 26164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26165(int& val) {
    func_in_26165(val);
    const int x = 26165;
    std::cout << 26165;
    for (int i = 0; i < 26165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26165(int& val) {
    const int x = 26165;
    std::cout << 26165;
    for (int i = 0; i < 26165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26166(int& val) {
    func_in_26166(val);
    const int x = 26166;
    std::cout << 26166;
    for (int i = 0; i < 26166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26166(int& val) {
    const int x = 26166;
    std::cout << 26166;
    for (int i = 0; i < 26166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26167(int& val) {
    func_in_26167(val);
    const int x = 26167;
    std::cout << 26167;
    for (int i = 0; i < 26167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26167(int& val) {
    const int x = 26167;
    std::cout << 26167;
    for (int i = 0; i < 26167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26168(int& val) {
    func_in_26168(val);
    const int x = 26168;
    std::cout << 26168;
    for (int i = 0; i < 26168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26168(int& val) {
    const int x = 26168;
    std::cout << 26168;
    for (int i = 0; i < 26168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26169(int& val) {
    func_in_26169(val);
    const int x = 26169;
    std::cout << 26169;
    for (int i = 0; i < 26169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26169(int& val) {
    const int x = 26169;
    std::cout << 26169;
    for (int i = 0; i < 26169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26170(int& val) {
    func_in_26170(val);
    const int x = 26170;
    std::cout << 26170;
    for (int i = 0; i < 26170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26170(int& val) {
    const int x = 26170;
    std::cout << 26170;
    for (int i = 0; i < 26170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26171(int& val) {
    func_in_26171(val);
    const int x = 26171;
    std::cout << 26171;
    for (int i = 0; i < 26171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26171(int& val) {
    const int x = 26171;
    std::cout << 26171;
    for (int i = 0; i < 26171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26172(int& val) {
    func_in_26172(val);
    const int x = 26172;
    std::cout << 26172;
    for (int i = 0; i < 26172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26172(int& val) {
    const int x = 26172;
    std::cout << 26172;
    for (int i = 0; i < 26172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26173(int& val) {
    func_in_26173(val);
    const int x = 26173;
    std::cout << 26173;
    for (int i = 0; i < 26173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26173(int& val) {
    const int x = 26173;
    std::cout << 26173;
    for (int i = 0; i < 26173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26174(int& val) {
    func_in_26174(val);
    const int x = 26174;
    std::cout << 26174;
    for (int i = 0; i < 26174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26174(int& val) {
    const int x = 26174;
    std::cout << 26174;
    for (int i = 0; i < 26174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26175(int& val) {
    func_in_26175(val);
    const int x = 26175;
    std::cout << 26175;
    for (int i = 0; i < 26175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26175(int& val) {
    const int x = 26175;
    std::cout << 26175;
    for (int i = 0; i < 26175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26176(int& val) {
    func_in_26176(val);
    const int x = 26176;
    std::cout << 26176;
    for (int i = 0; i < 26176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26176(int& val) {
    const int x = 26176;
    std::cout << 26176;
    for (int i = 0; i < 26176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26177(int& val) {
    func_in_26177(val);
    const int x = 26177;
    std::cout << 26177;
    for (int i = 0; i < 26177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26177(int& val) {
    const int x = 26177;
    std::cout << 26177;
    for (int i = 0; i < 26177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26178(int& val) {
    func_in_26178(val);
    const int x = 26178;
    std::cout << 26178;
    for (int i = 0; i < 26178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26178(int& val) {
    const int x = 26178;
    std::cout << 26178;
    for (int i = 0; i < 26178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26179(int& val) {
    func_in_26179(val);
    const int x = 26179;
    std::cout << 26179;
    for (int i = 0; i < 26179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26179(int& val) {
    const int x = 26179;
    std::cout << 26179;
    for (int i = 0; i < 26179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26180(int& val) {
    func_in_26180(val);
    const int x = 26180;
    std::cout << 26180;
    for (int i = 0; i < 26180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26180(int& val) {
    const int x = 26180;
    std::cout << 26180;
    for (int i = 0; i < 26180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26181(int& val) {
    func_in_26181(val);
    const int x = 26181;
    std::cout << 26181;
    for (int i = 0; i < 26181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26181(int& val) {
    const int x = 26181;
    std::cout << 26181;
    for (int i = 0; i < 26181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26182(int& val) {
    func_in_26182(val);
    const int x = 26182;
    std::cout << 26182;
    for (int i = 0; i < 26182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26182(int& val) {
    const int x = 26182;
    std::cout << 26182;
    for (int i = 0; i < 26182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26183(int& val) {
    func_in_26183(val);
    const int x = 26183;
    std::cout << 26183;
    for (int i = 0; i < 26183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26183(int& val) {
    const int x = 26183;
    std::cout << 26183;
    for (int i = 0; i < 26183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26184(int& val) {
    func_in_26184(val);
    const int x = 26184;
    std::cout << 26184;
    for (int i = 0; i < 26184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26184(int& val) {
    const int x = 26184;
    std::cout << 26184;
    for (int i = 0; i < 26184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26185(int& val) {
    func_in_26185(val);
    const int x = 26185;
    std::cout << 26185;
    for (int i = 0; i < 26185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26185(int& val) {
    const int x = 26185;
    std::cout << 26185;
    for (int i = 0; i < 26185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26186(int& val) {
    func_in_26186(val);
    const int x = 26186;
    std::cout << 26186;
    for (int i = 0; i < 26186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26186(int& val) {
    const int x = 26186;
    std::cout << 26186;
    for (int i = 0; i < 26186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26187(int& val) {
    func_in_26187(val);
    const int x = 26187;
    std::cout << 26187;
    for (int i = 0; i < 26187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26187(int& val) {
    const int x = 26187;
    std::cout << 26187;
    for (int i = 0; i < 26187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26188(int& val) {
    func_in_26188(val);
    const int x = 26188;
    std::cout << 26188;
    for (int i = 0; i < 26188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26188(int& val) {
    const int x = 26188;
    std::cout << 26188;
    for (int i = 0; i < 26188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26189(int& val) {
    func_in_26189(val);
    const int x = 26189;
    std::cout << 26189;
    for (int i = 0; i < 26189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26189(int& val) {
    const int x = 26189;
    std::cout << 26189;
    for (int i = 0; i < 26189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26190(int& val) {
    func_in_26190(val);
    const int x = 26190;
    std::cout << 26190;
    for (int i = 0; i < 26190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26190(int& val) {
    const int x = 26190;
    std::cout << 26190;
    for (int i = 0; i < 26190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26191(int& val) {
    func_in_26191(val);
    const int x = 26191;
    std::cout << 26191;
    for (int i = 0; i < 26191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26191(int& val) {
    const int x = 26191;
    std::cout << 26191;
    for (int i = 0; i < 26191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26192(int& val) {
    func_in_26192(val);
    const int x = 26192;
    std::cout << 26192;
    for (int i = 0; i < 26192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26192(int& val) {
    const int x = 26192;
    std::cout << 26192;
    for (int i = 0; i < 26192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26193(int& val) {
    func_in_26193(val);
    const int x = 26193;
    std::cout << 26193;
    for (int i = 0; i < 26193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26193(int& val) {
    const int x = 26193;
    std::cout << 26193;
    for (int i = 0; i < 26193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26194(int& val) {
    func_in_26194(val);
    const int x = 26194;
    std::cout << 26194;
    for (int i = 0; i < 26194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26194(int& val) {
    const int x = 26194;
    std::cout << 26194;
    for (int i = 0; i < 26194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26195(int& val) {
    func_in_26195(val);
    const int x = 26195;
    std::cout << 26195;
    for (int i = 0; i < 26195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26195(int& val) {
    const int x = 26195;
    std::cout << 26195;
    for (int i = 0; i < 26195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26196(int& val) {
    func_in_26196(val);
    const int x = 26196;
    std::cout << 26196;
    for (int i = 0; i < 26196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26196(int& val) {
    const int x = 26196;
    std::cout << 26196;
    for (int i = 0; i < 26196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26197(int& val) {
    func_in_26197(val);
    const int x = 26197;
    std::cout << 26197;
    for (int i = 0; i < 26197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26197(int& val) {
    const int x = 26197;
    std::cout << 26197;
    for (int i = 0; i < 26197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26198(int& val) {
    func_in_26198(val);
    const int x = 26198;
    std::cout << 26198;
    for (int i = 0; i < 26198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26198(int& val) {
    const int x = 26198;
    std::cout << 26198;
    for (int i = 0; i < 26198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26199(int& val) {
    func_in_26199(val);
    const int x = 26199;
    std::cout << 26199;
    for (int i = 0; i < 26199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26199(int& val) {
    const int x = 26199;
    std::cout << 26199;
    for (int i = 0; i < 26199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26200(int& val) {
    func_in_26200(val);
    const int x = 26200;
    std::cout << 26200;
    for (int i = 0; i < 26200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26200(int& val) {
    const int x = 26200;
    std::cout << 26200;
    for (int i = 0; i < 26200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26201(int& val) {
    func_in_26201(val);
    const int x = 26201;
    std::cout << 26201;
    for (int i = 0; i < 26201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26201(int& val) {
    const int x = 26201;
    std::cout << 26201;
    for (int i = 0; i < 26201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26202(int& val) {
    func_in_26202(val);
    const int x = 26202;
    std::cout << 26202;
    for (int i = 0; i < 26202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26202(int& val) {
    const int x = 26202;
    std::cout << 26202;
    for (int i = 0; i < 26202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26203(int& val) {
    func_in_26203(val);
    const int x = 26203;
    std::cout << 26203;
    for (int i = 0; i < 26203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26203(int& val) {
    const int x = 26203;
    std::cout << 26203;
    for (int i = 0; i < 26203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26204(int& val) {
    func_in_26204(val);
    const int x = 26204;
    std::cout << 26204;
    for (int i = 0; i < 26204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26204(int& val) {
    const int x = 26204;
    std::cout << 26204;
    for (int i = 0; i < 26204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26205(int& val) {
    func_in_26205(val);
    const int x = 26205;
    std::cout << 26205;
    for (int i = 0; i < 26205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26205(int& val) {
    const int x = 26205;
    std::cout << 26205;
    for (int i = 0; i < 26205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26206(int& val) {
    func_in_26206(val);
    const int x = 26206;
    std::cout << 26206;
    for (int i = 0; i < 26206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26206(int& val) {
    const int x = 26206;
    std::cout << 26206;
    for (int i = 0; i < 26206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26207(int& val) {
    func_in_26207(val);
    const int x = 26207;
    std::cout << 26207;
    for (int i = 0; i < 26207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26207(int& val) {
    const int x = 26207;
    std::cout << 26207;
    for (int i = 0; i < 26207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26208(int& val) {
    func_in_26208(val);
    const int x = 26208;
    std::cout << 26208;
    for (int i = 0; i < 26208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26208(int& val) {
    const int x = 26208;
    std::cout << 26208;
    for (int i = 0; i < 26208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26209(int& val) {
    func_in_26209(val);
    const int x = 26209;
    std::cout << 26209;
    for (int i = 0; i < 26209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26209(int& val) {
    const int x = 26209;
    std::cout << 26209;
    for (int i = 0; i < 26209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26210(int& val) {
    func_in_26210(val);
    const int x = 26210;
    std::cout << 26210;
    for (int i = 0; i < 26210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26210(int& val) {
    const int x = 26210;
    std::cout << 26210;
    for (int i = 0; i < 26210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26211(int& val) {
    func_in_26211(val);
    const int x = 26211;
    std::cout << 26211;
    for (int i = 0; i < 26211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26211(int& val) {
    const int x = 26211;
    std::cout << 26211;
    for (int i = 0; i < 26211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26212(int& val) {
    func_in_26212(val);
    const int x = 26212;
    std::cout << 26212;
    for (int i = 0; i < 26212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26212(int& val) {
    const int x = 26212;
    std::cout << 26212;
    for (int i = 0; i < 26212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26213(int& val) {
    func_in_26213(val);
    const int x = 26213;
    std::cout << 26213;
    for (int i = 0; i < 26213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26213(int& val) {
    const int x = 26213;
    std::cout << 26213;
    for (int i = 0; i < 26213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26214(int& val) {
    func_in_26214(val);
    const int x = 26214;
    std::cout << 26214;
    for (int i = 0; i < 26214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26214(int& val) {
    const int x = 26214;
    std::cout << 26214;
    for (int i = 0; i < 26214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26215(int& val) {
    func_in_26215(val);
    const int x = 26215;
    std::cout << 26215;
    for (int i = 0; i < 26215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26215(int& val) {
    const int x = 26215;
    std::cout << 26215;
    for (int i = 0; i < 26215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26216(int& val) {
    func_in_26216(val);
    const int x = 26216;
    std::cout << 26216;
    for (int i = 0; i < 26216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26216(int& val) {
    const int x = 26216;
    std::cout << 26216;
    for (int i = 0; i < 26216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26217(int& val) {
    func_in_26217(val);
    const int x = 26217;
    std::cout << 26217;
    for (int i = 0; i < 26217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26217(int& val) {
    const int x = 26217;
    std::cout << 26217;
    for (int i = 0; i < 26217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26218(int& val) {
    func_in_26218(val);
    const int x = 26218;
    std::cout << 26218;
    for (int i = 0; i < 26218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26218(int& val) {
    const int x = 26218;
    std::cout << 26218;
    for (int i = 0; i < 26218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26219(int& val) {
    func_in_26219(val);
    const int x = 26219;
    std::cout << 26219;
    for (int i = 0; i < 26219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26219(int& val) {
    const int x = 26219;
    std::cout << 26219;
    for (int i = 0; i < 26219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26220(int& val) {
    func_in_26220(val);
    const int x = 26220;
    std::cout << 26220;
    for (int i = 0; i < 26220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26220(int& val) {
    const int x = 26220;
    std::cout << 26220;
    for (int i = 0; i < 26220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26221(int& val) {
    func_in_26221(val);
    const int x = 26221;
    std::cout << 26221;
    for (int i = 0; i < 26221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26221(int& val) {
    const int x = 26221;
    std::cout << 26221;
    for (int i = 0; i < 26221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26222(int& val) {
    func_in_26222(val);
    const int x = 26222;
    std::cout << 26222;
    for (int i = 0; i < 26222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26222(int& val) {
    const int x = 26222;
    std::cout << 26222;
    for (int i = 0; i < 26222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26223(int& val) {
    func_in_26223(val);
    const int x = 26223;
    std::cout << 26223;
    for (int i = 0; i < 26223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26223(int& val) {
    const int x = 26223;
    std::cout << 26223;
    for (int i = 0; i < 26223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26224(int& val) {
    func_in_26224(val);
    const int x = 26224;
    std::cout << 26224;
    for (int i = 0; i < 26224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26224(int& val) {
    const int x = 26224;
    std::cout << 26224;
    for (int i = 0; i < 26224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26225(int& val) {
    func_in_26225(val);
    const int x = 26225;
    std::cout << 26225;
    for (int i = 0; i < 26225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26225(int& val) {
    const int x = 26225;
    std::cout << 26225;
    for (int i = 0; i < 26225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26226(int& val) {
    func_in_26226(val);
    const int x = 26226;
    std::cout << 26226;
    for (int i = 0; i < 26226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26226(int& val) {
    const int x = 26226;
    std::cout << 26226;
    for (int i = 0; i < 26226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26227(int& val) {
    func_in_26227(val);
    const int x = 26227;
    std::cout << 26227;
    for (int i = 0; i < 26227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26227(int& val) {
    const int x = 26227;
    std::cout << 26227;
    for (int i = 0; i < 26227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26228(int& val) {
    func_in_26228(val);
    const int x = 26228;
    std::cout << 26228;
    for (int i = 0; i < 26228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26228(int& val) {
    const int x = 26228;
    std::cout << 26228;
    for (int i = 0; i < 26228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26229(int& val) {
    func_in_26229(val);
    const int x = 26229;
    std::cout << 26229;
    for (int i = 0; i < 26229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26229(int& val) {
    const int x = 26229;
    std::cout << 26229;
    for (int i = 0; i < 26229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26230(int& val) {
    func_in_26230(val);
    const int x = 26230;
    std::cout << 26230;
    for (int i = 0; i < 26230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26230(int& val) {
    const int x = 26230;
    std::cout << 26230;
    for (int i = 0; i < 26230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26231(int& val) {
    func_in_26231(val);
    const int x = 26231;
    std::cout << 26231;
    for (int i = 0; i < 26231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26231(int& val) {
    const int x = 26231;
    std::cout << 26231;
    for (int i = 0; i < 26231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26232(int& val) {
    func_in_26232(val);
    const int x = 26232;
    std::cout << 26232;
    for (int i = 0; i < 26232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26232(int& val) {
    const int x = 26232;
    std::cout << 26232;
    for (int i = 0; i < 26232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26233(int& val) {
    func_in_26233(val);
    const int x = 26233;
    std::cout << 26233;
    for (int i = 0; i < 26233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26233(int& val) {
    const int x = 26233;
    std::cout << 26233;
    for (int i = 0; i < 26233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26234(int& val) {
    func_in_26234(val);
    const int x = 26234;
    std::cout << 26234;
    for (int i = 0; i < 26234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26234(int& val) {
    const int x = 26234;
    std::cout << 26234;
    for (int i = 0; i < 26234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26235(int& val) {
    func_in_26235(val);
    const int x = 26235;
    std::cout << 26235;
    for (int i = 0; i < 26235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26235(int& val) {
    const int x = 26235;
    std::cout << 26235;
    for (int i = 0; i < 26235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26236(int& val) {
    func_in_26236(val);
    const int x = 26236;
    std::cout << 26236;
    for (int i = 0; i < 26236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26236(int& val) {
    const int x = 26236;
    std::cout << 26236;
    for (int i = 0; i < 26236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26237(int& val) {
    func_in_26237(val);
    const int x = 26237;
    std::cout << 26237;
    for (int i = 0; i < 26237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26237(int& val) {
    const int x = 26237;
    std::cout << 26237;
    for (int i = 0; i < 26237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26238(int& val) {
    func_in_26238(val);
    const int x = 26238;
    std::cout << 26238;
    for (int i = 0; i < 26238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26238(int& val) {
    const int x = 26238;
    std::cout << 26238;
    for (int i = 0; i < 26238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26239(int& val) {
    func_in_26239(val);
    const int x = 26239;
    std::cout << 26239;
    for (int i = 0; i < 26239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26239(int& val) {
    const int x = 26239;
    std::cout << 26239;
    for (int i = 0; i < 26239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26240(int& val) {
    func_in_26240(val);
    const int x = 26240;
    std::cout << 26240;
    for (int i = 0; i < 26240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26240(int& val) {
    const int x = 26240;
    std::cout << 26240;
    for (int i = 0; i < 26240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26241(int& val) {
    func_in_26241(val);
    const int x = 26241;
    std::cout << 26241;
    for (int i = 0; i < 26241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26241(int& val) {
    const int x = 26241;
    std::cout << 26241;
    for (int i = 0; i < 26241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26242(int& val) {
    func_in_26242(val);
    const int x = 26242;
    std::cout << 26242;
    for (int i = 0; i < 26242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26242(int& val) {
    const int x = 26242;
    std::cout << 26242;
    for (int i = 0; i < 26242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26243(int& val) {
    func_in_26243(val);
    const int x = 26243;
    std::cout << 26243;
    for (int i = 0; i < 26243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26243(int& val) {
    const int x = 26243;
    std::cout << 26243;
    for (int i = 0; i < 26243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26244(int& val) {
    func_in_26244(val);
    const int x = 26244;
    std::cout << 26244;
    for (int i = 0; i < 26244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26244(int& val) {
    const int x = 26244;
    std::cout << 26244;
    for (int i = 0; i < 26244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26245(int& val) {
    func_in_26245(val);
    const int x = 26245;
    std::cout << 26245;
    for (int i = 0; i < 26245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26245(int& val) {
    const int x = 26245;
    std::cout << 26245;
    for (int i = 0; i < 26245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26246(int& val) {
    func_in_26246(val);
    const int x = 26246;
    std::cout << 26246;
    for (int i = 0; i < 26246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26246(int& val) {
    const int x = 26246;
    std::cout << 26246;
    for (int i = 0; i < 26246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26247(int& val) {
    func_in_26247(val);
    const int x = 26247;
    std::cout << 26247;
    for (int i = 0; i < 26247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26247(int& val) {
    const int x = 26247;
    std::cout << 26247;
    for (int i = 0; i < 26247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26248(int& val) {
    func_in_26248(val);
    const int x = 26248;
    std::cout << 26248;
    for (int i = 0; i < 26248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26248(int& val) {
    const int x = 26248;
    std::cout << 26248;
    for (int i = 0; i < 26248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26249(int& val) {
    func_in_26249(val);
    const int x = 26249;
    std::cout << 26249;
    for (int i = 0; i < 26249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26249(int& val) {
    const int x = 26249;
    std::cout << 26249;
    for (int i = 0; i < 26249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26250(int& val) {
    func_in_26250(val);
    const int x = 26250;
    std::cout << 26250;
    for (int i = 0; i < 26250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26250(int& val) {
    const int x = 26250;
    std::cout << 26250;
    for (int i = 0; i < 26250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26251(int& val) {
    func_in_26251(val);
    const int x = 26251;
    std::cout << 26251;
    for (int i = 0; i < 26251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26251(int& val) {
    const int x = 26251;
    std::cout << 26251;
    for (int i = 0; i < 26251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26252(int& val) {
    func_in_26252(val);
    const int x = 26252;
    std::cout << 26252;
    for (int i = 0; i < 26252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26252(int& val) {
    const int x = 26252;
    std::cout << 26252;
    for (int i = 0; i < 26252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26253(int& val) {
    func_in_26253(val);
    const int x = 26253;
    std::cout << 26253;
    for (int i = 0; i < 26253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26253(int& val) {
    const int x = 26253;
    std::cout << 26253;
    for (int i = 0; i < 26253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26254(int& val) {
    func_in_26254(val);
    const int x = 26254;
    std::cout << 26254;
    for (int i = 0; i < 26254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26254(int& val) {
    const int x = 26254;
    std::cout << 26254;
    for (int i = 0; i < 26254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26255(int& val) {
    func_in_26255(val);
    const int x = 26255;
    std::cout << 26255;
    for (int i = 0; i < 26255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26255(int& val) {
    const int x = 26255;
    std::cout << 26255;
    for (int i = 0; i < 26255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26256(int& val) {
    func_in_26256(val);
    const int x = 26256;
    std::cout << 26256;
    for (int i = 0; i < 26256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26256(int& val) {
    const int x = 26256;
    std::cout << 26256;
    for (int i = 0; i < 26256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26257(int& val) {
    func_in_26257(val);
    const int x = 26257;
    std::cout << 26257;
    for (int i = 0; i < 26257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26257(int& val) {
    const int x = 26257;
    std::cout << 26257;
    for (int i = 0; i < 26257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26258(int& val) {
    func_in_26258(val);
    const int x = 26258;
    std::cout << 26258;
    for (int i = 0; i < 26258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26258(int& val) {
    const int x = 26258;
    std::cout << 26258;
    for (int i = 0; i < 26258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26259(int& val) {
    func_in_26259(val);
    const int x = 26259;
    std::cout << 26259;
    for (int i = 0; i < 26259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26259(int& val) {
    const int x = 26259;
    std::cout << 26259;
    for (int i = 0; i < 26259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26260(int& val) {
    func_in_26260(val);
    const int x = 26260;
    std::cout << 26260;
    for (int i = 0; i < 26260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26260(int& val) {
    const int x = 26260;
    std::cout << 26260;
    for (int i = 0; i < 26260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26261(int& val) {
    func_in_26261(val);
    const int x = 26261;
    std::cout << 26261;
    for (int i = 0; i < 26261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26261(int& val) {
    const int x = 26261;
    std::cout << 26261;
    for (int i = 0; i < 26261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26262(int& val) {
    func_in_26262(val);
    const int x = 26262;
    std::cout << 26262;
    for (int i = 0; i < 26262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26262(int& val) {
    const int x = 26262;
    std::cout << 26262;
    for (int i = 0; i < 26262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26263(int& val) {
    func_in_26263(val);
    const int x = 26263;
    std::cout << 26263;
    for (int i = 0; i < 26263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26263(int& val) {
    const int x = 26263;
    std::cout << 26263;
    for (int i = 0; i < 26263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26264(int& val) {
    func_in_26264(val);
    const int x = 26264;
    std::cout << 26264;
    for (int i = 0; i < 26264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26264(int& val) {
    const int x = 26264;
    std::cout << 26264;
    for (int i = 0; i < 26264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26265(int& val) {
    func_in_26265(val);
    const int x = 26265;
    std::cout << 26265;
    for (int i = 0; i < 26265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26265(int& val) {
    const int x = 26265;
    std::cout << 26265;
    for (int i = 0; i < 26265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26266(int& val) {
    func_in_26266(val);
    const int x = 26266;
    std::cout << 26266;
    for (int i = 0; i < 26266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26266(int& val) {
    const int x = 26266;
    std::cout << 26266;
    for (int i = 0; i < 26266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26267(int& val) {
    func_in_26267(val);
    const int x = 26267;
    std::cout << 26267;
    for (int i = 0; i < 26267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26267(int& val) {
    const int x = 26267;
    std::cout << 26267;
    for (int i = 0; i < 26267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26268(int& val) {
    func_in_26268(val);
    const int x = 26268;
    std::cout << 26268;
    for (int i = 0; i < 26268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26268(int& val) {
    const int x = 26268;
    std::cout << 26268;
    for (int i = 0; i < 26268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26269(int& val) {
    func_in_26269(val);
    const int x = 26269;
    std::cout << 26269;
    for (int i = 0; i < 26269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26269(int& val) {
    const int x = 26269;
    std::cout << 26269;
    for (int i = 0; i < 26269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26270(int& val) {
    func_in_26270(val);
    const int x = 26270;
    std::cout << 26270;
    for (int i = 0; i < 26270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26270(int& val) {
    const int x = 26270;
    std::cout << 26270;
    for (int i = 0; i < 26270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26271(int& val) {
    func_in_26271(val);
    const int x = 26271;
    std::cout << 26271;
    for (int i = 0; i < 26271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26271(int& val) {
    const int x = 26271;
    std::cout << 26271;
    for (int i = 0; i < 26271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26272(int& val) {
    func_in_26272(val);
    const int x = 26272;
    std::cout << 26272;
    for (int i = 0; i < 26272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26272(int& val) {
    const int x = 26272;
    std::cout << 26272;
    for (int i = 0; i < 26272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26273(int& val) {
    func_in_26273(val);
    const int x = 26273;
    std::cout << 26273;
    for (int i = 0; i < 26273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26273(int& val) {
    const int x = 26273;
    std::cout << 26273;
    for (int i = 0; i < 26273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26274(int& val) {
    func_in_26274(val);
    const int x = 26274;
    std::cout << 26274;
    for (int i = 0; i < 26274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26274(int& val) {
    const int x = 26274;
    std::cout << 26274;
    for (int i = 0; i < 26274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26275(int& val) {
    func_in_26275(val);
    const int x = 26275;
    std::cout << 26275;
    for (int i = 0; i < 26275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26275(int& val) {
    const int x = 26275;
    std::cout << 26275;
    for (int i = 0; i < 26275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26276(int& val) {
    func_in_26276(val);
    const int x = 26276;
    std::cout << 26276;
    for (int i = 0; i < 26276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26276(int& val) {
    const int x = 26276;
    std::cout << 26276;
    for (int i = 0; i < 26276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26277(int& val) {
    func_in_26277(val);
    const int x = 26277;
    std::cout << 26277;
    for (int i = 0; i < 26277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26277(int& val) {
    const int x = 26277;
    std::cout << 26277;
    for (int i = 0; i < 26277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26278(int& val) {
    func_in_26278(val);
    const int x = 26278;
    std::cout << 26278;
    for (int i = 0; i < 26278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26278(int& val) {
    const int x = 26278;
    std::cout << 26278;
    for (int i = 0; i < 26278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26279(int& val) {
    func_in_26279(val);
    const int x = 26279;
    std::cout << 26279;
    for (int i = 0; i < 26279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26279(int& val) {
    const int x = 26279;
    std::cout << 26279;
    for (int i = 0; i < 26279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26280(int& val) {
    func_in_26280(val);
    const int x = 26280;
    std::cout << 26280;
    for (int i = 0; i < 26280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26280(int& val) {
    const int x = 26280;
    std::cout << 26280;
    for (int i = 0; i < 26280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26281(int& val) {
    func_in_26281(val);
    const int x = 26281;
    std::cout << 26281;
    for (int i = 0; i < 26281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26281(int& val) {
    const int x = 26281;
    std::cout << 26281;
    for (int i = 0; i < 26281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26282(int& val) {
    func_in_26282(val);
    const int x = 26282;
    std::cout << 26282;
    for (int i = 0; i < 26282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26282(int& val) {
    const int x = 26282;
    std::cout << 26282;
    for (int i = 0; i < 26282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26283(int& val) {
    func_in_26283(val);
    const int x = 26283;
    std::cout << 26283;
    for (int i = 0; i < 26283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26283(int& val) {
    const int x = 26283;
    std::cout << 26283;
    for (int i = 0; i < 26283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26284(int& val) {
    func_in_26284(val);
    const int x = 26284;
    std::cout << 26284;
    for (int i = 0; i < 26284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26284(int& val) {
    const int x = 26284;
    std::cout << 26284;
    for (int i = 0; i < 26284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26285(int& val) {
    func_in_26285(val);
    const int x = 26285;
    std::cout << 26285;
    for (int i = 0; i < 26285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26285(int& val) {
    const int x = 26285;
    std::cout << 26285;
    for (int i = 0; i < 26285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26286(int& val) {
    func_in_26286(val);
    const int x = 26286;
    std::cout << 26286;
    for (int i = 0; i < 26286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26286(int& val) {
    const int x = 26286;
    std::cout << 26286;
    for (int i = 0; i < 26286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26287(int& val) {
    func_in_26287(val);
    const int x = 26287;
    std::cout << 26287;
    for (int i = 0; i < 26287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26287(int& val) {
    const int x = 26287;
    std::cout << 26287;
    for (int i = 0; i < 26287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26288(int& val) {
    func_in_26288(val);
    const int x = 26288;
    std::cout << 26288;
    for (int i = 0; i < 26288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26288(int& val) {
    const int x = 26288;
    std::cout << 26288;
    for (int i = 0; i < 26288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26289(int& val) {
    func_in_26289(val);
    const int x = 26289;
    std::cout << 26289;
    for (int i = 0; i < 26289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26289(int& val) {
    const int x = 26289;
    std::cout << 26289;
    for (int i = 0; i < 26289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26290(int& val) {
    func_in_26290(val);
    const int x = 26290;
    std::cout << 26290;
    for (int i = 0; i < 26290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26290(int& val) {
    const int x = 26290;
    std::cout << 26290;
    for (int i = 0; i < 26290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26291(int& val) {
    func_in_26291(val);
    const int x = 26291;
    std::cout << 26291;
    for (int i = 0; i < 26291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26291(int& val) {
    const int x = 26291;
    std::cout << 26291;
    for (int i = 0; i < 26291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26292(int& val) {
    func_in_26292(val);
    const int x = 26292;
    std::cout << 26292;
    for (int i = 0; i < 26292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26292(int& val) {
    const int x = 26292;
    std::cout << 26292;
    for (int i = 0; i < 26292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26293(int& val) {
    func_in_26293(val);
    const int x = 26293;
    std::cout << 26293;
    for (int i = 0; i < 26293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26293(int& val) {
    const int x = 26293;
    std::cout << 26293;
    for (int i = 0; i < 26293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26294(int& val) {
    func_in_26294(val);
    const int x = 26294;
    std::cout << 26294;
    for (int i = 0; i < 26294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26294(int& val) {
    const int x = 26294;
    std::cout << 26294;
    for (int i = 0; i < 26294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26295(int& val) {
    func_in_26295(val);
    const int x = 26295;
    std::cout << 26295;
    for (int i = 0; i < 26295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26295(int& val) {
    const int x = 26295;
    std::cout << 26295;
    for (int i = 0; i < 26295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26296(int& val) {
    func_in_26296(val);
    const int x = 26296;
    std::cout << 26296;
    for (int i = 0; i < 26296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26296(int& val) {
    const int x = 26296;
    std::cout << 26296;
    for (int i = 0; i < 26296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26297(int& val) {
    func_in_26297(val);
    const int x = 26297;
    std::cout << 26297;
    for (int i = 0; i < 26297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26297(int& val) {
    const int x = 26297;
    std::cout << 26297;
    for (int i = 0; i < 26297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26298(int& val) {
    func_in_26298(val);
    const int x = 26298;
    std::cout << 26298;
    for (int i = 0; i < 26298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26298(int& val) {
    const int x = 26298;
    std::cout << 26298;
    for (int i = 0; i < 26298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26299(int& val) {
    func_in_26299(val);
    const int x = 26299;
    std::cout << 26299;
    for (int i = 0; i < 26299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26299(int& val) {
    const int x = 26299;
    std::cout << 26299;
    for (int i = 0; i < 26299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26300(int& val) {
    func_in_26300(val);
    const int x = 26300;
    std::cout << 26300;
    for (int i = 0; i < 26300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26300(int& val) {
    const int x = 26300;
    std::cout << 26300;
    for (int i = 0; i < 26300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26301(int& val) {
    func_in_26301(val);
    const int x = 26301;
    std::cout << 26301;
    for (int i = 0; i < 26301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26301(int& val) {
    const int x = 26301;
    std::cout << 26301;
    for (int i = 0; i < 26301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26302(int& val) {
    func_in_26302(val);
    const int x = 26302;
    std::cout << 26302;
    for (int i = 0; i < 26302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26302(int& val) {
    const int x = 26302;
    std::cout << 26302;
    for (int i = 0; i < 26302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26303(int& val) {
    func_in_26303(val);
    const int x = 26303;
    std::cout << 26303;
    for (int i = 0; i < 26303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26303(int& val) {
    const int x = 26303;
    std::cout << 26303;
    for (int i = 0; i < 26303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26304(int& val) {
    func_in_26304(val);
    const int x = 26304;
    std::cout << 26304;
    for (int i = 0; i < 26304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26304(int& val) {
    const int x = 26304;
    std::cout << 26304;
    for (int i = 0; i < 26304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26305(int& val) {
    func_in_26305(val);
    const int x = 26305;
    std::cout << 26305;
    for (int i = 0; i < 26305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26305(int& val) {
    const int x = 26305;
    std::cout << 26305;
    for (int i = 0; i < 26305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26306(int& val) {
    func_in_26306(val);
    const int x = 26306;
    std::cout << 26306;
    for (int i = 0; i < 26306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26306(int& val) {
    const int x = 26306;
    std::cout << 26306;
    for (int i = 0; i < 26306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26307(int& val) {
    func_in_26307(val);
    const int x = 26307;
    std::cout << 26307;
    for (int i = 0; i < 26307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26307(int& val) {
    const int x = 26307;
    std::cout << 26307;
    for (int i = 0; i < 26307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26308(int& val) {
    func_in_26308(val);
    const int x = 26308;
    std::cout << 26308;
    for (int i = 0; i < 26308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26308(int& val) {
    const int x = 26308;
    std::cout << 26308;
    for (int i = 0; i < 26308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26309(int& val) {
    func_in_26309(val);
    const int x = 26309;
    std::cout << 26309;
    for (int i = 0; i < 26309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26309(int& val) {
    const int x = 26309;
    std::cout << 26309;
    for (int i = 0; i < 26309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26310(int& val) {
    func_in_26310(val);
    const int x = 26310;
    std::cout << 26310;
    for (int i = 0; i < 26310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26310(int& val) {
    const int x = 26310;
    std::cout << 26310;
    for (int i = 0; i < 26310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26311(int& val) {
    func_in_26311(val);
    const int x = 26311;
    std::cout << 26311;
    for (int i = 0; i < 26311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26311(int& val) {
    const int x = 26311;
    std::cout << 26311;
    for (int i = 0; i < 26311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26312(int& val) {
    func_in_26312(val);
    const int x = 26312;
    std::cout << 26312;
    for (int i = 0; i < 26312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26312(int& val) {
    const int x = 26312;
    std::cout << 26312;
    for (int i = 0; i < 26312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26313(int& val) {
    func_in_26313(val);
    const int x = 26313;
    std::cout << 26313;
    for (int i = 0; i < 26313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26313(int& val) {
    const int x = 26313;
    std::cout << 26313;
    for (int i = 0; i < 26313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26314(int& val) {
    func_in_26314(val);
    const int x = 26314;
    std::cout << 26314;
    for (int i = 0; i < 26314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26314(int& val) {
    const int x = 26314;
    std::cout << 26314;
    for (int i = 0; i < 26314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26315(int& val) {
    func_in_26315(val);
    const int x = 26315;
    std::cout << 26315;
    for (int i = 0; i < 26315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26315(int& val) {
    const int x = 26315;
    std::cout << 26315;
    for (int i = 0; i < 26315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26316(int& val) {
    func_in_26316(val);
    const int x = 26316;
    std::cout << 26316;
    for (int i = 0; i < 26316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26316(int& val) {
    const int x = 26316;
    std::cout << 26316;
    for (int i = 0; i < 26316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26317(int& val) {
    func_in_26317(val);
    const int x = 26317;
    std::cout << 26317;
    for (int i = 0; i < 26317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26317(int& val) {
    const int x = 26317;
    std::cout << 26317;
    for (int i = 0; i < 26317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26318(int& val) {
    func_in_26318(val);
    const int x = 26318;
    std::cout << 26318;
    for (int i = 0; i < 26318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26318(int& val) {
    const int x = 26318;
    std::cout << 26318;
    for (int i = 0; i < 26318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26319(int& val) {
    func_in_26319(val);
    const int x = 26319;
    std::cout << 26319;
    for (int i = 0; i < 26319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26319(int& val) {
    const int x = 26319;
    std::cout << 26319;
    for (int i = 0; i < 26319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26320(int& val) {
    func_in_26320(val);
    const int x = 26320;
    std::cout << 26320;
    for (int i = 0; i < 26320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26320(int& val) {
    const int x = 26320;
    std::cout << 26320;
    for (int i = 0; i < 26320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26321(int& val) {
    func_in_26321(val);
    const int x = 26321;
    std::cout << 26321;
    for (int i = 0; i < 26321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26321(int& val) {
    const int x = 26321;
    std::cout << 26321;
    for (int i = 0; i < 26321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26322(int& val) {
    func_in_26322(val);
    const int x = 26322;
    std::cout << 26322;
    for (int i = 0; i < 26322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26322(int& val) {
    const int x = 26322;
    std::cout << 26322;
    for (int i = 0; i < 26322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26323(int& val) {
    func_in_26323(val);
    const int x = 26323;
    std::cout << 26323;
    for (int i = 0; i < 26323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26323(int& val) {
    const int x = 26323;
    std::cout << 26323;
    for (int i = 0; i < 26323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26324(int& val) {
    func_in_26324(val);
    const int x = 26324;
    std::cout << 26324;
    for (int i = 0; i < 26324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26324(int& val) {
    const int x = 26324;
    std::cout << 26324;
    for (int i = 0; i < 26324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26325(int& val) {
    func_in_26325(val);
    const int x = 26325;
    std::cout << 26325;
    for (int i = 0; i < 26325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26325(int& val) {
    const int x = 26325;
    std::cout << 26325;
    for (int i = 0; i < 26325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26326(int& val) {
    func_in_26326(val);
    const int x = 26326;
    std::cout << 26326;
    for (int i = 0; i < 26326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26326(int& val) {
    const int x = 26326;
    std::cout << 26326;
    for (int i = 0; i < 26326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26327(int& val) {
    func_in_26327(val);
    const int x = 26327;
    std::cout << 26327;
    for (int i = 0; i < 26327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26327(int& val) {
    const int x = 26327;
    std::cout << 26327;
    for (int i = 0; i < 26327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26328(int& val) {
    func_in_26328(val);
    const int x = 26328;
    std::cout << 26328;
    for (int i = 0; i < 26328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26328(int& val) {
    const int x = 26328;
    std::cout << 26328;
    for (int i = 0; i < 26328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26329(int& val) {
    func_in_26329(val);
    const int x = 26329;
    std::cout << 26329;
    for (int i = 0; i < 26329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26329(int& val) {
    const int x = 26329;
    std::cout << 26329;
    for (int i = 0; i < 26329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26330(int& val) {
    func_in_26330(val);
    const int x = 26330;
    std::cout << 26330;
    for (int i = 0; i < 26330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26330(int& val) {
    const int x = 26330;
    std::cout << 26330;
    for (int i = 0; i < 26330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26331(int& val) {
    func_in_26331(val);
    const int x = 26331;
    std::cout << 26331;
    for (int i = 0; i < 26331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26331(int& val) {
    const int x = 26331;
    std::cout << 26331;
    for (int i = 0; i < 26331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26332(int& val) {
    func_in_26332(val);
    const int x = 26332;
    std::cout << 26332;
    for (int i = 0; i < 26332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26332(int& val) {
    const int x = 26332;
    std::cout << 26332;
    for (int i = 0; i < 26332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26333(int& val) {
    func_in_26333(val);
    const int x = 26333;
    std::cout << 26333;
    for (int i = 0; i < 26333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26333(int& val) {
    const int x = 26333;
    std::cout << 26333;
    for (int i = 0; i < 26333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26334(int& val) {
    func_in_26334(val);
    const int x = 26334;
    std::cout << 26334;
    for (int i = 0; i < 26334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26334(int& val) {
    const int x = 26334;
    std::cout << 26334;
    for (int i = 0; i < 26334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26335(int& val) {
    func_in_26335(val);
    const int x = 26335;
    std::cout << 26335;
    for (int i = 0; i < 26335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26335(int& val) {
    const int x = 26335;
    std::cout << 26335;
    for (int i = 0; i < 26335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26336(int& val) {
    func_in_26336(val);
    const int x = 26336;
    std::cout << 26336;
    for (int i = 0; i < 26336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26336(int& val) {
    const int x = 26336;
    std::cout << 26336;
    for (int i = 0; i < 26336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26337(int& val) {
    func_in_26337(val);
    const int x = 26337;
    std::cout << 26337;
    for (int i = 0; i < 26337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26337(int& val) {
    const int x = 26337;
    std::cout << 26337;
    for (int i = 0; i < 26337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26338(int& val) {
    func_in_26338(val);
    const int x = 26338;
    std::cout << 26338;
    for (int i = 0; i < 26338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26338(int& val) {
    const int x = 26338;
    std::cout << 26338;
    for (int i = 0; i < 26338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26339(int& val) {
    func_in_26339(val);
    const int x = 26339;
    std::cout << 26339;
    for (int i = 0; i < 26339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26339(int& val) {
    const int x = 26339;
    std::cout << 26339;
    for (int i = 0; i < 26339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26340(int& val) {
    func_in_26340(val);
    const int x = 26340;
    std::cout << 26340;
    for (int i = 0; i < 26340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26340(int& val) {
    const int x = 26340;
    std::cout << 26340;
    for (int i = 0; i < 26340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26341(int& val) {
    func_in_26341(val);
    const int x = 26341;
    std::cout << 26341;
    for (int i = 0; i < 26341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26341(int& val) {
    const int x = 26341;
    std::cout << 26341;
    for (int i = 0; i < 26341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26342(int& val) {
    func_in_26342(val);
    const int x = 26342;
    std::cout << 26342;
    for (int i = 0; i < 26342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26342(int& val) {
    const int x = 26342;
    std::cout << 26342;
    for (int i = 0; i < 26342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26343(int& val) {
    func_in_26343(val);
    const int x = 26343;
    std::cout << 26343;
    for (int i = 0; i < 26343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26343(int& val) {
    const int x = 26343;
    std::cout << 26343;
    for (int i = 0; i < 26343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26344(int& val) {
    func_in_26344(val);
    const int x = 26344;
    std::cout << 26344;
    for (int i = 0; i < 26344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26344(int& val) {
    const int x = 26344;
    std::cout << 26344;
    for (int i = 0; i < 26344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26345(int& val) {
    func_in_26345(val);
    const int x = 26345;
    std::cout << 26345;
    for (int i = 0; i < 26345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26345(int& val) {
    const int x = 26345;
    std::cout << 26345;
    for (int i = 0; i < 26345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26346(int& val) {
    func_in_26346(val);
    const int x = 26346;
    std::cout << 26346;
    for (int i = 0; i < 26346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26346(int& val) {
    const int x = 26346;
    std::cout << 26346;
    for (int i = 0; i < 26346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26347(int& val) {
    func_in_26347(val);
    const int x = 26347;
    std::cout << 26347;
    for (int i = 0; i < 26347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26347(int& val) {
    const int x = 26347;
    std::cout << 26347;
    for (int i = 0; i < 26347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26348(int& val) {
    func_in_26348(val);
    const int x = 26348;
    std::cout << 26348;
    for (int i = 0; i < 26348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26348(int& val) {
    const int x = 26348;
    std::cout << 26348;
    for (int i = 0; i < 26348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26349(int& val) {
    func_in_26349(val);
    const int x = 26349;
    std::cout << 26349;
    for (int i = 0; i < 26349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26349(int& val) {
    const int x = 26349;
    std::cout << 26349;
    for (int i = 0; i < 26349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26350(int& val) {
    func_in_26350(val);
    const int x = 26350;
    std::cout << 26350;
    for (int i = 0; i < 26350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26350(int& val) {
    const int x = 26350;
    std::cout << 26350;
    for (int i = 0; i < 26350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26351(int& val) {
    func_in_26351(val);
    const int x = 26351;
    std::cout << 26351;
    for (int i = 0; i < 26351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26351(int& val) {
    const int x = 26351;
    std::cout << 26351;
    for (int i = 0; i < 26351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26352(int& val) {
    func_in_26352(val);
    const int x = 26352;
    std::cout << 26352;
    for (int i = 0; i < 26352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26352(int& val) {
    const int x = 26352;
    std::cout << 26352;
    for (int i = 0; i < 26352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26353(int& val) {
    func_in_26353(val);
    const int x = 26353;
    std::cout << 26353;
    for (int i = 0; i < 26353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26353(int& val) {
    const int x = 26353;
    std::cout << 26353;
    for (int i = 0; i < 26353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26354(int& val) {
    func_in_26354(val);
    const int x = 26354;
    std::cout << 26354;
    for (int i = 0; i < 26354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26354(int& val) {
    const int x = 26354;
    std::cout << 26354;
    for (int i = 0; i < 26354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26355(int& val) {
    func_in_26355(val);
    const int x = 26355;
    std::cout << 26355;
    for (int i = 0; i < 26355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26355(int& val) {
    const int x = 26355;
    std::cout << 26355;
    for (int i = 0; i < 26355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26356(int& val) {
    func_in_26356(val);
    const int x = 26356;
    std::cout << 26356;
    for (int i = 0; i < 26356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26356(int& val) {
    const int x = 26356;
    std::cout << 26356;
    for (int i = 0; i < 26356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26357(int& val) {
    func_in_26357(val);
    const int x = 26357;
    std::cout << 26357;
    for (int i = 0; i < 26357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26357(int& val) {
    const int x = 26357;
    std::cout << 26357;
    for (int i = 0; i < 26357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26358(int& val) {
    func_in_26358(val);
    const int x = 26358;
    std::cout << 26358;
    for (int i = 0; i < 26358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26358(int& val) {
    const int x = 26358;
    std::cout << 26358;
    for (int i = 0; i < 26358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26359(int& val) {
    func_in_26359(val);
    const int x = 26359;
    std::cout << 26359;
    for (int i = 0; i < 26359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26359(int& val) {
    const int x = 26359;
    std::cout << 26359;
    for (int i = 0; i < 26359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26360(int& val) {
    func_in_26360(val);
    const int x = 26360;
    std::cout << 26360;
    for (int i = 0; i < 26360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26360(int& val) {
    const int x = 26360;
    std::cout << 26360;
    for (int i = 0; i < 26360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26361(int& val) {
    func_in_26361(val);
    const int x = 26361;
    std::cout << 26361;
    for (int i = 0; i < 26361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26361(int& val) {
    const int x = 26361;
    std::cout << 26361;
    for (int i = 0; i < 26361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26362(int& val) {
    func_in_26362(val);
    const int x = 26362;
    std::cout << 26362;
    for (int i = 0; i < 26362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26362(int& val) {
    const int x = 26362;
    std::cout << 26362;
    for (int i = 0; i < 26362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26363(int& val) {
    func_in_26363(val);
    const int x = 26363;
    std::cout << 26363;
    for (int i = 0; i < 26363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26363(int& val) {
    const int x = 26363;
    std::cout << 26363;
    for (int i = 0; i < 26363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26364(int& val) {
    func_in_26364(val);
    const int x = 26364;
    std::cout << 26364;
    for (int i = 0; i < 26364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26364(int& val) {
    const int x = 26364;
    std::cout << 26364;
    for (int i = 0; i < 26364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26365(int& val) {
    func_in_26365(val);
    const int x = 26365;
    std::cout << 26365;
    for (int i = 0; i < 26365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26365(int& val) {
    const int x = 26365;
    std::cout << 26365;
    for (int i = 0; i < 26365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26366(int& val) {
    func_in_26366(val);
    const int x = 26366;
    std::cout << 26366;
    for (int i = 0; i < 26366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26366(int& val) {
    const int x = 26366;
    std::cout << 26366;
    for (int i = 0; i < 26366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26367(int& val) {
    func_in_26367(val);
    const int x = 26367;
    std::cout << 26367;
    for (int i = 0; i < 26367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26367(int& val) {
    const int x = 26367;
    std::cout << 26367;
    for (int i = 0; i < 26367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26368(int& val) {
    func_in_26368(val);
    const int x = 26368;
    std::cout << 26368;
    for (int i = 0; i < 26368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26368(int& val) {
    const int x = 26368;
    std::cout << 26368;
    for (int i = 0; i < 26368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26369(int& val) {
    func_in_26369(val);
    const int x = 26369;
    std::cout << 26369;
    for (int i = 0; i < 26369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26369(int& val) {
    const int x = 26369;
    std::cout << 26369;
    for (int i = 0; i < 26369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26370(int& val) {
    func_in_26370(val);
    const int x = 26370;
    std::cout << 26370;
    for (int i = 0; i < 26370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26370(int& val) {
    const int x = 26370;
    std::cout << 26370;
    for (int i = 0; i < 26370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26371(int& val) {
    func_in_26371(val);
    const int x = 26371;
    std::cout << 26371;
    for (int i = 0; i < 26371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26371(int& val) {
    const int x = 26371;
    std::cout << 26371;
    for (int i = 0; i < 26371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26372(int& val) {
    func_in_26372(val);
    const int x = 26372;
    std::cout << 26372;
    for (int i = 0; i < 26372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26372(int& val) {
    const int x = 26372;
    std::cout << 26372;
    for (int i = 0; i < 26372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26373(int& val) {
    func_in_26373(val);
    const int x = 26373;
    std::cout << 26373;
    for (int i = 0; i < 26373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26373(int& val) {
    const int x = 26373;
    std::cout << 26373;
    for (int i = 0; i < 26373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26374(int& val) {
    func_in_26374(val);
    const int x = 26374;
    std::cout << 26374;
    for (int i = 0; i < 26374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26374(int& val) {
    const int x = 26374;
    std::cout << 26374;
    for (int i = 0; i < 26374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26375(int& val) {
    func_in_26375(val);
    const int x = 26375;
    std::cout << 26375;
    for (int i = 0; i < 26375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26375(int& val) {
    const int x = 26375;
    std::cout << 26375;
    for (int i = 0; i < 26375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26376(int& val) {
    func_in_26376(val);
    const int x = 26376;
    std::cout << 26376;
    for (int i = 0; i < 26376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26376(int& val) {
    const int x = 26376;
    std::cout << 26376;
    for (int i = 0; i < 26376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26377(int& val) {
    func_in_26377(val);
    const int x = 26377;
    std::cout << 26377;
    for (int i = 0; i < 26377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26377(int& val) {
    const int x = 26377;
    std::cout << 26377;
    for (int i = 0; i < 26377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26378(int& val) {
    func_in_26378(val);
    const int x = 26378;
    std::cout << 26378;
    for (int i = 0; i < 26378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26378(int& val) {
    const int x = 26378;
    std::cout << 26378;
    for (int i = 0; i < 26378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26379(int& val) {
    func_in_26379(val);
    const int x = 26379;
    std::cout << 26379;
    for (int i = 0; i < 26379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26379(int& val) {
    const int x = 26379;
    std::cout << 26379;
    for (int i = 0; i < 26379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26380(int& val) {
    func_in_26380(val);
    const int x = 26380;
    std::cout << 26380;
    for (int i = 0; i < 26380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26380(int& val) {
    const int x = 26380;
    std::cout << 26380;
    for (int i = 0; i < 26380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26381(int& val) {
    func_in_26381(val);
    const int x = 26381;
    std::cout << 26381;
    for (int i = 0; i < 26381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26381(int& val) {
    const int x = 26381;
    std::cout << 26381;
    for (int i = 0; i < 26381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26382(int& val) {
    func_in_26382(val);
    const int x = 26382;
    std::cout << 26382;
    for (int i = 0; i < 26382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26382(int& val) {
    const int x = 26382;
    std::cout << 26382;
    for (int i = 0; i < 26382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26383(int& val) {
    func_in_26383(val);
    const int x = 26383;
    std::cout << 26383;
    for (int i = 0; i < 26383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26383(int& val) {
    const int x = 26383;
    std::cout << 26383;
    for (int i = 0; i < 26383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26384(int& val) {
    func_in_26384(val);
    const int x = 26384;
    std::cout << 26384;
    for (int i = 0; i < 26384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26384(int& val) {
    const int x = 26384;
    std::cout << 26384;
    for (int i = 0; i < 26384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26385(int& val) {
    func_in_26385(val);
    const int x = 26385;
    std::cout << 26385;
    for (int i = 0; i < 26385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26385(int& val) {
    const int x = 26385;
    std::cout << 26385;
    for (int i = 0; i < 26385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26386(int& val) {
    func_in_26386(val);
    const int x = 26386;
    std::cout << 26386;
    for (int i = 0; i < 26386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26386(int& val) {
    const int x = 26386;
    std::cout << 26386;
    for (int i = 0; i < 26386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26387(int& val) {
    func_in_26387(val);
    const int x = 26387;
    std::cout << 26387;
    for (int i = 0; i < 26387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26387(int& val) {
    const int x = 26387;
    std::cout << 26387;
    for (int i = 0; i < 26387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26388(int& val) {
    func_in_26388(val);
    const int x = 26388;
    std::cout << 26388;
    for (int i = 0; i < 26388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26388(int& val) {
    const int x = 26388;
    std::cout << 26388;
    for (int i = 0; i < 26388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26389(int& val) {
    func_in_26389(val);
    const int x = 26389;
    std::cout << 26389;
    for (int i = 0; i < 26389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26389(int& val) {
    const int x = 26389;
    std::cout << 26389;
    for (int i = 0; i < 26389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26390(int& val) {
    func_in_26390(val);
    const int x = 26390;
    std::cout << 26390;
    for (int i = 0; i < 26390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26390(int& val) {
    const int x = 26390;
    std::cout << 26390;
    for (int i = 0; i < 26390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26391(int& val) {
    func_in_26391(val);
    const int x = 26391;
    std::cout << 26391;
    for (int i = 0; i < 26391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26391(int& val) {
    const int x = 26391;
    std::cout << 26391;
    for (int i = 0; i < 26391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26392(int& val) {
    func_in_26392(val);
    const int x = 26392;
    std::cout << 26392;
    for (int i = 0; i < 26392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26392(int& val) {
    const int x = 26392;
    std::cout << 26392;
    for (int i = 0; i < 26392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26393(int& val) {
    func_in_26393(val);
    const int x = 26393;
    std::cout << 26393;
    for (int i = 0; i < 26393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26393(int& val) {
    const int x = 26393;
    std::cout << 26393;
    for (int i = 0; i < 26393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26394(int& val) {
    func_in_26394(val);
    const int x = 26394;
    std::cout << 26394;
    for (int i = 0; i < 26394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26394(int& val) {
    const int x = 26394;
    std::cout << 26394;
    for (int i = 0; i < 26394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26395(int& val) {
    func_in_26395(val);
    const int x = 26395;
    std::cout << 26395;
    for (int i = 0; i < 26395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26395(int& val) {
    const int x = 26395;
    std::cout << 26395;
    for (int i = 0; i < 26395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26396(int& val) {
    func_in_26396(val);
    const int x = 26396;
    std::cout << 26396;
    for (int i = 0; i < 26396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26396(int& val) {
    const int x = 26396;
    std::cout << 26396;
    for (int i = 0; i < 26396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26397(int& val) {
    func_in_26397(val);
    const int x = 26397;
    std::cout << 26397;
    for (int i = 0; i < 26397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26397(int& val) {
    const int x = 26397;
    std::cout << 26397;
    for (int i = 0; i < 26397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26398(int& val) {
    func_in_26398(val);
    const int x = 26398;
    std::cout << 26398;
    for (int i = 0; i < 26398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26398(int& val) {
    const int x = 26398;
    std::cout << 26398;
    for (int i = 0; i < 26398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26399(int& val) {
    func_in_26399(val);
    const int x = 26399;
    std::cout << 26399;
    for (int i = 0; i < 26399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26399(int& val) {
    const int x = 26399;
    std::cout << 26399;
    for (int i = 0; i < 26399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26400(int& val) {
    func_in_26400(val);
    const int x = 26400;
    std::cout << 26400;
    for (int i = 0; i < 26400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26400(int& val) {
    const int x = 26400;
    std::cout << 26400;
    for (int i = 0; i < 26400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26401(int& val) {
    func_in_26401(val);
    const int x = 26401;
    std::cout << 26401;
    for (int i = 0; i < 26401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26401(int& val) {
    const int x = 26401;
    std::cout << 26401;
    for (int i = 0; i < 26401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26402(int& val) {
    func_in_26402(val);
    const int x = 26402;
    std::cout << 26402;
    for (int i = 0; i < 26402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26402(int& val) {
    const int x = 26402;
    std::cout << 26402;
    for (int i = 0; i < 26402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26403(int& val) {
    func_in_26403(val);
    const int x = 26403;
    std::cout << 26403;
    for (int i = 0; i < 26403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26403(int& val) {
    const int x = 26403;
    std::cout << 26403;
    for (int i = 0; i < 26403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26404(int& val) {
    func_in_26404(val);
    const int x = 26404;
    std::cout << 26404;
    for (int i = 0; i < 26404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26404(int& val) {
    const int x = 26404;
    std::cout << 26404;
    for (int i = 0; i < 26404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26405(int& val) {
    func_in_26405(val);
    const int x = 26405;
    std::cout << 26405;
    for (int i = 0; i < 26405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26405(int& val) {
    const int x = 26405;
    std::cout << 26405;
    for (int i = 0; i < 26405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26406(int& val) {
    func_in_26406(val);
    const int x = 26406;
    std::cout << 26406;
    for (int i = 0; i < 26406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26406(int& val) {
    const int x = 26406;
    std::cout << 26406;
    for (int i = 0; i < 26406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26407(int& val) {
    func_in_26407(val);
    const int x = 26407;
    std::cout << 26407;
    for (int i = 0; i < 26407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26407(int& val) {
    const int x = 26407;
    std::cout << 26407;
    for (int i = 0; i < 26407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26408(int& val) {
    func_in_26408(val);
    const int x = 26408;
    std::cout << 26408;
    for (int i = 0; i < 26408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26408(int& val) {
    const int x = 26408;
    std::cout << 26408;
    for (int i = 0; i < 26408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26409(int& val) {
    func_in_26409(val);
    const int x = 26409;
    std::cout << 26409;
    for (int i = 0; i < 26409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26409(int& val) {
    const int x = 26409;
    std::cout << 26409;
    for (int i = 0; i < 26409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26410(int& val) {
    func_in_26410(val);
    const int x = 26410;
    std::cout << 26410;
    for (int i = 0; i < 26410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26410(int& val) {
    const int x = 26410;
    std::cout << 26410;
    for (int i = 0; i < 26410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26411(int& val) {
    func_in_26411(val);
    const int x = 26411;
    std::cout << 26411;
    for (int i = 0; i < 26411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26411(int& val) {
    const int x = 26411;
    std::cout << 26411;
    for (int i = 0; i < 26411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26412(int& val) {
    func_in_26412(val);
    const int x = 26412;
    std::cout << 26412;
    for (int i = 0; i < 26412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26412(int& val) {
    const int x = 26412;
    std::cout << 26412;
    for (int i = 0; i < 26412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26413(int& val) {
    func_in_26413(val);
    const int x = 26413;
    std::cout << 26413;
    for (int i = 0; i < 26413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26413(int& val) {
    const int x = 26413;
    std::cout << 26413;
    for (int i = 0; i < 26413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26414(int& val) {
    func_in_26414(val);
    const int x = 26414;
    std::cout << 26414;
    for (int i = 0; i < 26414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26414(int& val) {
    const int x = 26414;
    std::cout << 26414;
    for (int i = 0; i < 26414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26415(int& val) {
    func_in_26415(val);
    const int x = 26415;
    std::cout << 26415;
    for (int i = 0; i < 26415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26415(int& val) {
    const int x = 26415;
    std::cout << 26415;
    for (int i = 0; i < 26415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26416(int& val) {
    func_in_26416(val);
    const int x = 26416;
    std::cout << 26416;
    for (int i = 0; i < 26416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26416(int& val) {
    const int x = 26416;
    std::cout << 26416;
    for (int i = 0; i < 26416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26417(int& val) {
    func_in_26417(val);
    const int x = 26417;
    std::cout << 26417;
    for (int i = 0; i < 26417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26417(int& val) {
    const int x = 26417;
    std::cout << 26417;
    for (int i = 0; i < 26417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26418(int& val) {
    func_in_26418(val);
    const int x = 26418;
    std::cout << 26418;
    for (int i = 0; i < 26418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26418(int& val) {
    const int x = 26418;
    std::cout << 26418;
    for (int i = 0; i < 26418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26419(int& val) {
    func_in_26419(val);
    const int x = 26419;
    std::cout << 26419;
    for (int i = 0; i < 26419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26419(int& val) {
    const int x = 26419;
    std::cout << 26419;
    for (int i = 0; i < 26419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26420(int& val) {
    func_in_26420(val);
    const int x = 26420;
    std::cout << 26420;
    for (int i = 0; i < 26420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26420(int& val) {
    const int x = 26420;
    std::cout << 26420;
    for (int i = 0; i < 26420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26421(int& val) {
    func_in_26421(val);
    const int x = 26421;
    std::cout << 26421;
    for (int i = 0; i < 26421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26421(int& val) {
    const int x = 26421;
    std::cout << 26421;
    for (int i = 0; i < 26421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26422(int& val) {
    func_in_26422(val);
    const int x = 26422;
    std::cout << 26422;
    for (int i = 0; i < 26422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26422(int& val) {
    const int x = 26422;
    std::cout << 26422;
    for (int i = 0; i < 26422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26423(int& val) {
    func_in_26423(val);
    const int x = 26423;
    std::cout << 26423;
    for (int i = 0; i < 26423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26423(int& val) {
    const int x = 26423;
    std::cout << 26423;
    for (int i = 0; i < 26423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26424(int& val) {
    func_in_26424(val);
    const int x = 26424;
    std::cout << 26424;
    for (int i = 0; i < 26424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26424(int& val) {
    const int x = 26424;
    std::cout << 26424;
    for (int i = 0; i < 26424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26425(int& val) {
    func_in_26425(val);
    const int x = 26425;
    std::cout << 26425;
    for (int i = 0; i < 26425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26425(int& val) {
    const int x = 26425;
    std::cout << 26425;
    for (int i = 0; i < 26425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26426(int& val) {
    func_in_26426(val);
    const int x = 26426;
    std::cout << 26426;
    for (int i = 0; i < 26426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26426(int& val) {
    const int x = 26426;
    std::cout << 26426;
    for (int i = 0; i < 26426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26427(int& val) {
    func_in_26427(val);
    const int x = 26427;
    std::cout << 26427;
    for (int i = 0; i < 26427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26427(int& val) {
    const int x = 26427;
    std::cout << 26427;
    for (int i = 0; i < 26427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26428(int& val) {
    func_in_26428(val);
    const int x = 26428;
    std::cout << 26428;
    for (int i = 0; i < 26428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26428(int& val) {
    const int x = 26428;
    std::cout << 26428;
    for (int i = 0; i < 26428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26429(int& val) {
    func_in_26429(val);
    const int x = 26429;
    std::cout << 26429;
    for (int i = 0; i < 26429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26429(int& val) {
    const int x = 26429;
    std::cout << 26429;
    for (int i = 0; i < 26429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26430(int& val) {
    func_in_26430(val);
    const int x = 26430;
    std::cout << 26430;
    for (int i = 0; i < 26430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26430(int& val) {
    const int x = 26430;
    std::cout << 26430;
    for (int i = 0; i < 26430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26431(int& val) {
    func_in_26431(val);
    const int x = 26431;
    std::cout << 26431;
    for (int i = 0; i < 26431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26431(int& val) {
    const int x = 26431;
    std::cout << 26431;
    for (int i = 0; i < 26431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26432(int& val) {
    func_in_26432(val);
    const int x = 26432;
    std::cout << 26432;
    for (int i = 0; i < 26432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26432(int& val) {
    const int x = 26432;
    std::cout << 26432;
    for (int i = 0; i < 26432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26433(int& val) {
    func_in_26433(val);
    const int x = 26433;
    std::cout << 26433;
    for (int i = 0; i < 26433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26433(int& val) {
    const int x = 26433;
    std::cout << 26433;
    for (int i = 0; i < 26433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26434(int& val) {
    func_in_26434(val);
    const int x = 26434;
    std::cout << 26434;
    for (int i = 0; i < 26434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26434(int& val) {
    const int x = 26434;
    std::cout << 26434;
    for (int i = 0; i < 26434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26435(int& val) {
    func_in_26435(val);
    const int x = 26435;
    std::cout << 26435;
    for (int i = 0; i < 26435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26435(int& val) {
    const int x = 26435;
    std::cout << 26435;
    for (int i = 0; i < 26435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26436(int& val) {
    func_in_26436(val);
    const int x = 26436;
    std::cout << 26436;
    for (int i = 0; i < 26436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26436(int& val) {
    const int x = 26436;
    std::cout << 26436;
    for (int i = 0; i < 26436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26437(int& val) {
    func_in_26437(val);
    const int x = 26437;
    std::cout << 26437;
    for (int i = 0; i < 26437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26437(int& val) {
    const int x = 26437;
    std::cout << 26437;
    for (int i = 0; i < 26437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26438(int& val) {
    func_in_26438(val);
    const int x = 26438;
    std::cout << 26438;
    for (int i = 0; i < 26438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26438(int& val) {
    const int x = 26438;
    std::cout << 26438;
    for (int i = 0; i < 26438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26439(int& val) {
    func_in_26439(val);
    const int x = 26439;
    std::cout << 26439;
    for (int i = 0; i < 26439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26439(int& val) {
    const int x = 26439;
    std::cout << 26439;
    for (int i = 0; i < 26439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26440(int& val) {
    func_in_26440(val);
    const int x = 26440;
    std::cout << 26440;
    for (int i = 0; i < 26440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26440(int& val) {
    const int x = 26440;
    std::cout << 26440;
    for (int i = 0; i < 26440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26441(int& val) {
    func_in_26441(val);
    const int x = 26441;
    std::cout << 26441;
    for (int i = 0; i < 26441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26441(int& val) {
    const int x = 26441;
    std::cout << 26441;
    for (int i = 0; i < 26441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26442(int& val) {
    func_in_26442(val);
    const int x = 26442;
    std::cout << 26442;
    for (int i = 0; i < 26442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26442(int& val) {
    const int x = 26442;
    std::cout << 26442;
    for (int i = 0; i < 26442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26443(int& val) {
    func_in_26443(val);
    const int x = 26443;
    std::cout << 26443;
    for (int i = 0; i < 26443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26443(int& val) {
    const int x = 26443;
    std::cout << 26443;
    for (int i = 0; i < 26443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26444(int& val) {
    func_in_26444(val);
    const int x = 26444;
    std::cout << 26444;
    for (int i = 0; i < 26444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26444(int& val) {
    const int x = 26444;
    std::cout << 26444;
    for (int i = 0; i < 26444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26445(int& val) {
    func_in_26445(val);
    const int x = 26445;
    std::cout << 26445;
    for (int i = 0; i < 26445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26445(int& val) {
    const int x = 26445;
    std::cout << 26445;
    for (int i = 0; i < 26445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26446(int& val) {
    func_in_26446(val);
    const int x = 26446;
    std::cout << 26446;
    for (int i = 0; i < 26446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26446(int& val) {
    const int x = 26446;
    std::cout << 26446;
    for (int i = 0; i < 26446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26447(int& val) {
    func_in_26447(val);
    const int x = 26447;
    std::cout << 26447;
    for (int i = 0; i < 26447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26447(int& val) {
    const int x = 26447;
    std::cout << 26447;
    for (int i = 0; i < 26447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26448(int& val) {
    func_in_26448(val);
    const int x = 26448;
    std::cout << 26448;
    for (int i = 0; i < 26448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26448(int& val) {
    const int x = 26448;
    std::cout << 26448;
    for (int i = 0; i < 26448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26449(int& val) {
    func_in_26449(val);
    const int x = 26449;
    std::cout << 26449;
    for (int i = 0; i < 26449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26449(int& val) {
    const int x = 26449;
    std::cout << 26449;
    for (int i = 0; i < 26449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26450(int& val) {
    func_in_26450(val);
    const int x = 26450;
    std::cout << 26450;
    for (int i = 0; i < 26450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26450(int& val) {
    const int x = 26450;
    std::cout << 26450;
    for (int i = 0; i < 26450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26451(int& val) {
    func_in_26451(val);
    const int x = 26451;
    std::cout << 26451;
    for (int i = 0; i < 26451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26451(int& val) {
    const int x = 26451;
    std::cout << 26451;
    for (int i = 0; i < 26451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26452(int& val) {
    func_in_26452(val);
    const int x = 26452;
    std::cout << 26452;
    for (int i = 0; i < 26452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26452(int& val) {
    const int x = 26452;
    std::cout << 26452;
    for (int i = 0; i < 26452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26453(int& val) {
    func_in_26453(val);
    const int x = 26453;
    std::cout << 26453;
    for (int i = 0; i < 26453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26453(int& val) {
    const int x = 26453;
    std::cout << 26453;
    for (int i = 0; i < 26453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26454(int& val) {
    func_in_26454(val);
    const int x = 26454;
    std::cout << 26454;
    for (int i = 0; i < 26454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26454(int& val) {
    const int x = 26454;
    std::cout << 26454;
    for (int i = 0; i < 26454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26455(int& val) {
    func_in_26455(val);
    const int x = 26455;
    std::cout << 26455;
    for (int i = 0; i < 26455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26455(int& val) {
    const int x = 26455;
    std::cout << 26455;
    for (int i = 0; i < 26455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26456(int& val) {
    func_in_26456(val);
    const int x = 26456;
    std::cout << 26456;
    for (int i = 0; i < 26456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26456(int& val) {
    const int x = 26456;
    std::cout << 26456;
    for (int i = 0; i < 26456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26457(int& val) {
    func_in_26457(val);
    const int x = 26457;
    std::cout << 26457;
    for (int i = 0; i < 26457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26457(int& val) {
    const int x = 26457;
    std::cout << 26457;
    for (int i = 0; i < 26457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26458(int& val) {
    func_in_26458(val);
    const int x = 26458;
    std::cout << 26458;
    for (int i = 0; i < 26458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26458(int& val) {
    const int x = 26458;
    std::cout << 26458;
    for (int i = 0; i < 26458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26459(int& val) {
    func_in_26459(val);
    const int x = 26459;
    std::cout << 26459;
    for (int i = 0; i < 26459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26459(int& val) {
    const int x = 26459;
    std::cout << 26459;
    for (int i = 0; i < 26459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26460(int& val) {
    func_in_26460(val);
    const int x = 26460;
    std::cout << 26460;
    for (int i = 0; i < 26460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26460(int& val) {
    const int x = 26460;
    std::cout << 26460;
    for (int i = 0; i < 26460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26461(int& val) {
    func_in_26461(val);
    const int x = 26461;
    std::cout << 26461;
    for (int i = 0; i < 26461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26461(int& val) {
    const int x = 26461;
    std::cout << 26461;
    for (int i = 0; i < 26461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26462(int& val) {
    func_in_26462(val);
    const int x = 26462;
    std::cout << 26462;
    for (int i = 0; i < 26462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26462(int& val) {
    const int x = 26462;
    std::cout << 26462;
    for (int i = 0; i < 26462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26463(int& val) {
    func_in_26463(val);
    const int x = 26463;
    std::cout << 26463;
    for (int i = 0; i < 26463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26463(int& val) {
    const int x = 26463;
    std::cout << 26463;
    for (int i = 0; i < 26463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26464(int& val) {
    func_in_26464(val);
    const int x = 26464;
    std::cout << 26464;
    for (int i = 0; i < 26464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26464(int& val) {
    const int x = 26464;
    std::cout << 26464;
    for (int i = 0; i < 26464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26465(int& val) {
    func_in_26465(val);
    const int x = 26465;
    std::cout << 26465;
    for (int i = 0; i < 26465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26465(int& val) {
    const int x = 26465;
    std::cout << 26465;
    for (int i = 0; i < 26465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26466(int& val) {
    func_in_26466(val);
    const int x = 26466;
    std::cout << 26466;
    for (int i = 0; i < 26466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26466(int& val) {
    const int x = 26466;
    std::cout << 26466;
    for (int i = 0; i < 26466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26467(int& val) {
    func_in_26467(val);
    const int x = 26467;
    std::cout << 26467;
    for (int i = 0; i < 26467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26467(int& val) {
    const int x = 26467;
    std::cout << 26467;
    for (int i = 0; i < 26467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26468(int& val) {
    func_in_26468(val);
    const int x = 26468;
    std::cout << 26468;
    for (int i = 0; i < 26468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26468(int& val) {
    const int x = 26468;
    std::cout << 26468;
    for (int i = 0; i < 26468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26469(int& val) {
    func_in_26469(val);
    const int x = 26469;
    std::cout << 26469;
    for (int i = 0; i < 26469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26469(int& val) {
    const int x = 26469;
    std::cout << 26469;
    for (int i = 0; i < 26469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26470(int& val) {
    func_in_26470(val);
    const int x = 26470;
    std::cout << 26470;
    for (int i = 0; i < 26470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26470(int& val) {
    const int x = 26470;
    std::cout << 26470;
    for (int i = 0; i < 26470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26471(int& val) {
    func_in_26471(val);
    const int x = 26471;
    std::cout << 26471;
    for (int i = 0; i < 26471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26471(int& val) {
    const int x = 26471;
    std::cout << 26471;
    for (int i = 0; i < 26471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26472(int& val) {
    func_in_26472(val);
    const int x = 26472;
    std::cout << 26472;
    for (int i = 0; i < 26472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26472(int& val) {
    const int x = 26472;
    std::cout << 26472;
    for (int i = 0; i < 26472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26473(int& val) {
    func_in_26473(val);
    const int x = 26473;
    std::cout << 26473;
    for (int i = 0; i < 26473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26473(int& val) {
    const int x = 26473;
    std::cout << 26473;
    for (int i = 0; i < 26473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26474(int& val) {
    func_in_26474(val);
    const int x = 26474;
    std::cout << 26474;
    for (int i = 0; i < 26474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26474(int& val) {
    const int x = 26474;
    std::cout << 26474;
    for (int i = 0; i < 26474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26475(int& val) {
    func_in_26475(val);
    const int x = 26475;
    std::cout << 26475;
    for (int i = 0; i < 26475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26475(int& val) {
    const int x = 26475;
    std::cout << 26475;
    for (int i = 0; i < 26475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26476(int& val) {
    func_in_26476(val);
    const int x = 26476;
    std::cout << 26476;
    for (int i = 0; i < 26476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26476(int& val) {
    const int x = 26476;
    std::cout << 26476;
    for (int i = 0; i < 26476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26477(int& val) {
    func_in_26477(val);
    const int x = 26477;
    std::cout << 26477;
    for (int i = 0; i < 26477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26477(int& val) {
    const int x = 26477;
    std::cout << 26477;
    for (int i = 0; i < 26477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26478(int& val) {
    func_in_26478(val);
    const int x = 26478;
    std::cout << 26478;
    for (int i = 0; i < 26478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26478(int& val) {
    const int x = 26478;
    std::cout << 26478;
    for (int i = 0; i < 26478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26479(int& val) {
    func_in_26479(val);
    const int x = 26479;
    std::cout << 26479;
    for (int i = 0; i < 26479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26479(int& val) {
    const int x = 26479;
    std::cout << 26479;
    for (int i = 0; i < 26479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26480(int& val) {
    func_in_26480(val);
    const int x = 26480;
    std::cout << 26480;
    for (int i = 0; i < 26480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26480(int& val) {
    const int x = 26480;
    std::cout << 26480;
    for (int i = 0; i < 26480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26481(int& val) {
    func_in_26481(val);
    const int x = 26481;
    std::cout << 26481;
    for (int i = 0; i < 26481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26481(int& val) {
    const int x = 26481;
    std::cout << 26481;
    for (int i = 0; i < 26481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26482(int& val) {
    func_in_26482(val);
    const int x = 26482;
    std::cout << 26482;
    for (int i = 0; i < 26482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26482(int& val) {
    const int x = 26482;
    std::cout << 26482;
    for (int i = 0; i < 26482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26483(int& val) {
    func_in_26483(val);
    const int x = 26483;
    std::cout << 26483;
    for (int i = 0; i < 26483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26483(int& val) {
    const int x = 26483;
    std::cout << 26483;
    for (int i = 0; i < 26483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26484(int& val) {
    func_in_26484(val);
    const int x = 26484;
    std::cout << 26484;
    for (int i = 0; i < 26484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26484(int& val) {
    const int x = 26484;
    std::cout << 26484;
    for (int i = 0; i < 26484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26485(int& val) {
    func_in_26485(val);
    const int x = 26485;
    std::cout << 26485;
    for (int i = 0; i < 26485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26485(int& val) {
    const int x = 26485;
    std::cout << 26485;
    for (int i = 0; i < 26485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26486(int& val) {
    func_in_26486(val);
    const int x = 26486;
    std::cout << 26486;
    for (int i = 0; i < 26486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26486(int& val) {
    const int x = 26486;
    std::cout << 26486;
    for (int i = 0; i < 26486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26487(int& val) {
    func_in_26487(val);
    const int x = 26487;
    std::cout << 26487;
    for (int i = 0; i < 26487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26487(int& val) {
    const int x = 26487;
    std::cout << 26487;
    for (int i = 0; i < 26487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26488(int& val) {
    func_in_26488(val);
    const int x = 26488;
    std::cout << 26488;
    for (int i = 0; i < 26488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26488(int& val) {
    const int x = 26488;
    std::cout << 26488;
    for (int i = 0; i < 26488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26489(int& val) {
    func_in_26489(val);
    const int x = 26489;
    std::cout << 26489;
    for (int i = 0; i < 26489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26489(int& val) {
    const int x = 26489;
    std::cout << 26489;
    for (int i = 0; i < 26489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26490(int& val) {
    func_in_26490(val);
    const int x = 26490;
    std::cout << 26490;
    for (int i = 0; i < 26490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26490(int& val) {
    const int x = 26490;
    std::cout << 26490;
    for (int i = 0; i < 26490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26491(int& val) {
    func_in_26491(val);
    const int x = 26491;
    std::cout << 26491;
    for (int i = 0; i < 26491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26491(int& val) {
    const int x = 26491;
    std::cout << 26491;
    for (int i = 0; i < 26491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26492(int& val) {
    func_in_26492(val);
    const int x = 26492;
    std::cout << 26492;
    for (int i = 0; i < 26492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26492(int& val) {
    const int x = 26492;
    std::cout << 26492;
    for (int i = 0; i < 26492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26493(int& val) {
    func_in_26493(val);
    const int x = 26493;
    std::cout << 26493;
    for (int i = 0; i < 26493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26493(int& val) {
    const int x = 26493;
    std::cout << 26493;
    for (int i = 0; i < 26493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26494(int& val) {
    func_in_26494(val);
    const int x = 26494;
    std::cout << 26494;
    for (int i = 0; i < 26494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26494(int& val) {
    const int x = 26494;
    std::cout << 26494;
    for (int i = 0; i < 26494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26495(int& val) {
    func_in_26495(val);
    const int x = 26495;
    std::cout << 26495;
    for (int i = 0; i < 26495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26495(int& val) {
    const int x = 26495;
    std::cout << 26495;
    for (int i = 0; i < 26495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26496(int& val) {
    func_in_26496(val);
    const int x = 26496;
    std::cout << 26496;
    for (int i = 0; i < 26496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26496(int& val) {
    const int x = 26496;
    std::cout << 26496;
    for (int i = 0; i < 26496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26497(int& val) {
    func_in_26497(val);
    const int x = 26497;
    std::cout << 26497;
    for (int i = 0; i < 26497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26497(int& val) {
    const int x = 26497;
    std::cout << 26497;
    for (int i = 0; i < 26497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26498(int& val) {
    func_in_26498(val);
    const int x = 26498;
    std::cout << 26498;
    for (int i = 0; i < 26498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26498(int& val) {
    const int x = 26498;
    std::cout << 26498;
    for (int i = 0; i < 26498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26499(int& val) {
    func_in_26499(val);
    const int x = 26499;
    std::cout << 26499;
    for (int i = 0; i < 26499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26499(int& val) {
    const int x = 26499;
    std::cout << 26499;
    for (int i = 0; i < 26499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26500(int& val) {
    func_in_26500(val);
    const int x = 26500;
    std::cout << 26500;
    for (int i = 0; i < 26500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26500(int& val) {
    const int x = 26500;
    std::cout << 26500;
    for (int i = 0; i < 26500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26501(int& val) {
    func_in_26501(val);
    const int x = 26501;
    std::cout << 26501;
    for (int i = 0; i < 26501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26501(int& val) {
    const int x = 26501;
    std::cout << 26501;
    for (int i = 0; i < 26501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26502(int& val) {
    func_in_26502(val);
    const int x = 26502;
    std::cout << 26502;
    for (int i = 0; i < 26502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26502(int& val) {
    const int x = 26502;
    std::cout << 26502;
    for (int i = 0; i < 26502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26503(int& val) {
    func_in_26503(val);
    const int x = 26503;
    std::cout << 26503;
    for (int i = 0; i < 26503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26503(int& val) {
    const int x = 26503;
    std::cout << 26503;
    for (int i = 0; i < 26503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26504(int& val) {
    func_in_26504(val);
    const int x = 26504;
    std::cout << 26504;
    for (int i = 0; i < 26504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26504(int& val) {
    const int x = 26504;
    std::cout << 26504;
    for (int i = 0; i < 26504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26505(int& val) {
    func_in_26505(val);
    const int x = 26505;
    std::cout << 26505;
    for (int i = 0; i < 26505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26505(int& val) {
    const int x = 26505;
    std::cout << 26505;
    for (int i = 0; i < 26505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26506(int& val) {
    func_in_26506(val);
    const int x = 26506;
    std::cout << 26506;
    for (int i = 0; i < 26506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26506(int& val) {
    const int x = 26506;
    std::cout << 26506;
    for (int i = 0; i < 26506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26507(int& val) {
    func_in_26507(val);
    const int x = 26507;
    std::cout << 26507;
    for (int i = 0; i < 26507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26507(int& val) {
    const int x = 26507;
    std::cout << 26507;
    for (int i = 0; i < 26507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26508(int& val) {
    func_in_26508(val);
    const int x = 26508;
    std::cout << 26508;
    for (int i = 0; i < 26508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26508(int& val) {
    const int x = 26508;
    std::cout << 26508;
    for (int i = 0; i < 26508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26509(int& val) {
    func_in_26509(val);
    const int x = 26509;
    std::cout << 26509;
    for (int i = 0; i < 26509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26509(int& val) {
    const int x = 26509;
    std::cout << 26509;
    for (int i = 0; i < 26509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26510(int& val) {
    func_in_26510(val);
    const int x = 26510;
    std::cout << 26510;
    for (int i = 0; i < 26510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26510(int& val) {
    const int x = 26510;
    std::cout << 26510;
    for (int i = 0; i < 26510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26511(int& val) {
    func_in_26511(val);
    const int x = 26511;
    std::cout << 26511;
    for (int i = 0; i < 26511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26511(int& val) {
    const int x = 26511;
    std::cout << 26511;
    for (int i = 0; i < 26511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26512(int& val) {
    func_in_26512(val);
    const int x = 26512;
    std::cout << 26512;
    for (int i = 0; i < 26512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26512(int& val) {
    const int x = 26512;
    std::cout << 26512;
    for (int i = 0; i < 26512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26513(int& val) {
    func_in_26513(val);
    const int x = 26513;
    std::cout << 26513;
    for (int i = 0; i < 26513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26513(int& val) {
    const int x = 26513;
    std::cout << 26513;
    for (int i = 0; i < 26513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26514(int& val) {
    func_in_26514(val);
    const int x = 26514;
    std::cout << 26514;
    for (int i = 0; i < 26514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26514(int& val) {
    const int x = 26514;
    std::cout << 26514;
    for (int i = 0; i < 26514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26515(int& val) {
    func_in_26515(val);
    const int x = 26515;
    std::cout << 26515;
    for (int i = 0; i < 26515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26515(int& val) {
    const int x = 26515;
    std::cout << 26515;
    for (int i = 0; i < 26515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26516(int& val) {
    func_in_26516(val);
    const int x = 26516;
    std::cout << 26516;
    for (int i = 0; i < 26516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26516(int& val) {
    const int x = 26516;
    std::cout << 26516;
    for (int i = 0; i < 26516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26517(int& val) {
    func_in_26517(val);
    const int x = 26517;
    std::cout << 26517;
    for (int i = 0; i < 26517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26517(int& val) {
    const int x = 26517;
    std::cout << 26517;
    for (int i = 0; i < 26517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26518(int& val) {
    func_in_26518(val);
    const int x = 26518;
    std::cout << 26518;
    for (int i = 0; i < 26518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26518(int& val) {
    const int x = 26518;
    std::cout << 26518;
    for (int i = 0; i < 26518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26519(int& val) {
    func_in_26519(val);
    const int x = 26519;
    std::cout << 26519;
    for (int i = 0; i < 26519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26519(int& val) {
    const int x = 26519;
    std::cout << 26519;
    for (int i = 0; i < 26519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26520(int& val) {
    func_in_26520(val);
    const int x = 26520;
    std::cout << 26520;
    for (int i = 0; i < 26520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26520(int& val) {
    const int x = 26520;
    std::cout << 26520;
    for (int i = 0; i < 26520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26521(int& val) {
    func_in_26521(val);
    const int x = 26521;
    std::cout << 26521;
    for (int i = 0; i < 26521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26521(int& val) {
    const int x = 26521;
    std::cout << 26521;
    for (int i = 0; i < 26521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26522(int& val) {
    func_in_26522(val);
    const int x = 26522;
    std::cout << 26522;
    for (int i = 0; i < 26522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26522(int& val) {
    const int x = 26522;
    std::cout << 26522;
    for (int i = 0; i < 26522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26523(int& val) {
    func_in_26523(val);
    const int x = 26523;
    std::cout << 26523;
    for (int i = 0; i < 26523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26523(int& val) {
    const int x = 26523;
    std::cout << 26523;
    for (int i = 0; i < 26523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26524(int& val) {
    func_in_26524(val);
    const int x = 26524;
    std::cout << 26524;
    for (int i = 0; i < 26524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26524(int& val) {
    const int x = 26524;
    std::cout << 26524;
    for (int i = 0; i < 26524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26525(int& val) {
    func_in_26525(val);
    const int x = 26525;
    std::cout << 26525;
    for (int i = 0; i < 26525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26525(int& val) {
    const int x = 26525;
    std::cout << 26525;
    for (int i = 0; i < 26525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26526(int& val) {
    func_in_26526(val);
    const int x = 26526;
    std::cout << 26526;
    for (int i = 0; i < 26526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26526(int& val) {
    const int x = 26526;
    std::cout << 26526;
    for (int i = 0; i < 26526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26527(int& val) {
    func_in_26527(val);
    const int x = 26527;
    std::cout << 26527;
    for (int i = 0; i < 26527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26527(int& val) {
    const int x = 26527;
    std::cout << 26527;
    for (int i = 0; i < 26527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26528(int& val) {
    func_in_26528(val);
    const int x = 26528;
    std::cout << 26528;
    for (int i = 0; i < 26528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26528(int& val) {
    const int x = 26528;
    std::cout << 26528;
    for (int i = 0; i < 26528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26529(int& val) {
    func_in_26529(val);
    const int x = 26529;
    std::cout << 26529;
    for (int i = 0; i < 26529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26529(int& val) {
    const int x = 26529;
    std::cout << 26529;
    for (int i = 0; i < 26529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26530(int& val) {
    func_in_26530(val);
    const int x = 26530;
    std::cout << 26530;
    for (int i = 0; i < 26530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26530(int& val) {
    const int x = 26530;
    std::cout << 26530;
    for (int i = 0; i < 26530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26531(int& val) {
    func_in_26531(val);
    const int x = 26531;
    std::cout << 26531;
    for (int i = 0; i < 26531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26531(int& val) {
    const int x = 26531;
    std::cout << 26531;
    for (int i = 0; i < 26531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26532(int& val) {
    func_in_26532(val);
    const int x = 26532;
    std::cout << 26532;
    for (int i = 0; i < 26532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26532(int& val) {
    const int x = 26532;
    std::cout << 26532;
    for (int i = 0; i < 26532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26533(int& val) {
    func_in_26533(val);
    const int x = 26533;
    std::cout << 26533;
    for (int i = 0; i < 26533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26533(int& val) {
    const int x = 26533;
    std::cout << 26533;
    for (int i = 0; i < 26533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26534(int& val) {
    func_in_26534(val);
    const int x = 26534;
    std::cout << 26534;
    for (int i = 0; i < 26534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26534(int& val) {
    const int x = 26534;
    std::cout << 26534;
    for (int i = 0; i < 26534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26535(int& val) {
    func_in_26535(val);
    const int x = 26535;
    std::cout << 26535;
    for (int i = 0; i < 26535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26535(int& val) {
    const int x = 26535;
    std::cout << 26535;
    for (int i = 0; i < 26535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26536(int& val) {
    func_in_26536(val);
    const int x = 26536;
    std::cout << 26536;
    for (int i = 0; i < 26536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26536(int& val) {
    const int x = 26536;
    std::cout << 26536;
    for (int i = 0; i < 26536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26537(int& val) {
    func_in_26537(val);
    const int x = 26537;
    std::cout << 26537;
    for (int i = 0; i < 26537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26537(int& val) {
    const int x = 26537;
    std::cout << 26537;
    for (int i = 0; i < 26537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26538(int& val) {
    func_in_26538(val);
    const int x = 26538;
    std::cout << 26538;
    for (int i = 0; i < 26538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26538(int& val) {
    const int x = 26538;
    std::cout << 26538;
    for (int i = 0; i < 26538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26539(int& val) {
    func_in_26539(val);
    const int x = 26539;
    std::cout << 26539;
    for (int i = 0; i < 26539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26539(int& val) {
    const int x = 26539;
    std::cout << 26539;
    for (int i = 0; i < 26539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26540(int& val) {
    func_in_26540(val);
    const int x = 26540;
    std::cout << 26540;
    for (int i = 0; i < 26540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26540(int& val) {
    const int x = 26540;
    std::cout << 26540;
    for (int i = 0; i < 26540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26541(int& val) {
    func_in_26541(val);
    const int x = 26541;
    std::cout << 26541;
    for (int i = 0; i < 26541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26541(int& val) {
    const int x = 26541;
    std::cout << 26541;
    for (int i = 0; i < 26541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26542(int& val) {
    func_in_26542(val);
    const int x = 26542;
    std::cout << 26542;
    for (int i = 0; i < 26542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26542(int& val) {
    const int x = 26542;
    std::cout << 26542;
    for (int i = 0; i < 26542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26543(int& val) {
    func_in_26543(val);
    const int x = 26543;
    std::cout << 26543;
    for (int i = 0; i < 26543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26543(int& val) {
    const int x = 26543;
    std::cout << 26543;
    for (int i = 0; i < 26543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26544(int& val) {
    func_in_26544(val);
    const int x = 26544;
    std::cout << 26544;
    for (int i = 0; i < 26544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26544(int& val) {
    const int x = 26544;
    std::cout << 26544;
    for (int i = 0; i < 26544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26545(int& val) {
    func_in_26545(val);
    const int x = 26545;
    std::cout << 26545;
    for (int i = 0; i < 26545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26545(int& val) {
    const int x = 26545;
    std::cout << 26545;
    for (int i = 0; i < 26545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26546(int& val) {
    func_in_26546(val);
    const int x = 26546;
    std::cout << 26546;
    for (int i = 0; i < 26546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26546(int& val) {
    const int x = 26546;
    std::cout << 26546;
    for (int i = 0; i < 26546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26547(int& val) {
    func_in_26547(val);
    const int x = 26547;
    std::cout << 26547;
    for (int i = 0; i < 26547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26547(int& val) {
    const int x = 26547;
    std::cout << 26547;
    for (int i = 0; i < 26547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26548(int& val) {
    func_in_26548(val);
    const int x = 26548;
    std::cout << 26548;
    for (int i = 0; i < 26548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26548(int& val) {
    const int x = 26548;
    std::cout << 26548;
    for (int i = 0; i < 26548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26549(int& val) {
    func_in_26549(val);
    const int x = 26549;
    std::cout << 26549;
    for (int i = 0; i < 26549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26549(int& val) {
    const int x = 26549;
    std::cout << 26549;
    for (int i = 0; i < 26549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26550(int& val) {
    func_in_26550(val);
    const int x = 26550;
    std::cout << 26550;
    for (int i = 0; i < 26550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26550(int& val) {
    const int x = 26550;
    std::cout << 26550;
    for (int i = 0; i < 26550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26551(int& val) {
    func_in_26551(val);
    const int x = 26551;
    std::cout << 26551;
    for (int i = 0; i < 26551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26551(int& val) {
    const int x = 26551;
    std::cout << 26551;
    for (int i = 0; i < 26551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26552(int& val) {
    func_in_26552(val);
    const int x = 26552;
    std::cout << 26552;
    for (int i = 0; i < 26552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26552(int& val) {
    const int x = 26552;
    std::cout << 26552;
    for (int i = 0; i < 26552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26553(int& val) {
    func_in_26553(val);
    const int x = 26553;
    std::cout << 26553;
    for (int i = 0; i < 26553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26553(int& val) {
    const int x = 26553;
    std::cout << 26553;
    for (int i = 0; i < 26553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26554(int& val) {
    func_in_26554(val);
    const int x = 26554;
    std::cout << 26554;
    for (int i = 0; i < 26554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26554(int& val) {
    const int x = 26554;
    std::cout << 26554;
    for (int i = 0; i < 26554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26555(int& val) {
    func_in_26555(val);
    const int x = 26555;
    std::cout << 26555;
    for (int i = 0; i < 26555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26555(int& val) {
    const int x = 26555;
    std::cout << 26555;
    for (int i = 0; i < 26555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26556(int& val) {
    func_in_26556(val);
    const int x = 26556;
    std::cout << 26556;
    for (int i = 0; i < 26556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26556(int& val) {
    const int x = 26556;
    std::cout << 26556;
    for (int i = 0; i < 26556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26557(int& val) {
    func_in_26557(val);
    const int x = 26557;
    std::cout << 26557;
    for (int i = 0; i < 26557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26557(int& val) {
    const int x = 26557;
    std::cout << 26557;
    for (int i = 0; i < 26557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26558(int& val) {
    func_in_26558(val);
    const int x = 26558;
    std::cout << 26558;
    for (int i = 0; i < 26558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26558(int& val) {
    const int x = 26558;
    std::cout << 26558;
    for (int i = 0; i < 26558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26559(int& val) {
    func_in_26559(val);
    const int x = 26559;
    std::cout << 26559;
    for (int i = 0; i < 26559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26559(int& val) {
    const int x = 26559;
    std::cout << 26559;
    for (int i = 0; i < 26559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26560(int& val) {
    func_in_26560(val);
    const int x = 26560;
    std::cout << 26560;
    for (int i = 0; i < 26560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26560(int& val) {
    const int x = 26560;
    std::cout << 26560;
    for (int i = 0; i < 26560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26561(int& val) {
    func_in_26561(val);
    const int x = 26561;
    std::cout << 26561;
    for (int i = 0; i < 26561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26561(int& val) {
    const int x = 26561;
    std::cout << 26561;
    for (int i = 0; i < 26561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26562(int& val) {
    func_in_26562(val);
    const int x = 26562;
    std::cout << 26562;
    for (int i = 0; i < 26562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26562(int& val) {
    const int x = 26562;
    std::cout << 26562;
    for (int i = 0; i < 26562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26563(int& val) {
    func_in_26563(val);
    const int x = 26563;
    std::cout << 26563;
    for (int i = 0; i < 26563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26563(int& val) {
    const int x = 26563;
    std::cout << 26563;
    for (int i = 0; i < 26563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26564(int& val) {
    func_in_26564(val);
    const int x = 26564;
    std::cout << 26564;
    for (int i = 0; i < 26564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26564(int& val) {
    const int x = 26564;
    std::cout << 26564;
    for (int i = 0; i < 26564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26565(int& val) {
    func_in_26565(val);
    const int x = 26565;
    std::cout << 26565;
    for (int i = 0; i < 26565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26565(int& val) {
    const int x = 26565;
    std::cout << 26565;
    for (int i = 0; i < 26565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26566(int& val) {
    func_in_26566(val);
    const int x = 26566;
    std::cout << 26566;
    for (int i = 0; i < 26566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26566(int& val) {
    const int x = 26566;
    std::cout << 26566;
    for (int i = 0; i < 26566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26567(int& val) {
    func_in_26567(val);
    const int x = 26567;
    std::cout << 26567;
    for (int i = 0; i < 26567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26567(int& val) {
    const int x = 26567;
    std::cout << 26567;
    for (int i = 0; i < 26567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26568(int& val) {
    func_in_26568(val);
    const int x = 26568;
    std::cout << 26568;
    for (int i = 0; i < 26568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26568(int& val) {
    const int x = 26568;
    std::cout << 26568;
    for (int i = 0; i < 26568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26569(int& val) {
    func_in_26569(val);
    const int x = 26569;
    std::cout << 26569;
    for (int i = 0; i < 26569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26569(int& val) {
    const int x = 26569;
    std::cout << 26569;
    for (int i = 0; i < 26569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26570(int& val) {
    func_in_26570(val);
    const int x = 26570;
    std::cout << 26570;
    for (int i = 0; i < 26570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26570(int& val) {
    const int x = 26570;
    std::cout << 26570;
    for (int i = 0; i < 26570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26571(int& val) {
    func_in_26571(val);
    const int x = 26571;
    std::cout << 26571;
    for (int i = 0; i < 26571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26571(int& val) {
    const int x = 26571;
    std::cout << 26571;
    for (int i = 0; i < 26571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26572(int& val) {
    func_in_26572(val);
    const int x = 26572;
    std::cout << 26572;
    for (int i = 0; i < 26572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26572(int& val) {
    const int x = 26572;
    std::cout << 26572;
    for (int i = 0; i < 26572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26573(int& val) {
    func_in_26573(val);
    const int x = 26573;
    std::cout << 26573;
    for (int i = 0; i < 26573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26573(int& val) {
    const int x = 26573;
    std::cout << 26573;
    for (int i = 0; i < 26573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26574(int& val) {
    func_in_26574(val);
    const int x = 26574;
    std::cout << 26574;
    for (int i = 0; i < 26574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26574(int& val) {
    const int x = 26574;
    std::cout << 26574;
    for (int i = 0; i < 26574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26575(int& val) {
    func_in_26575(val);
    const int x = 26575;
    std::cout << 26575;
    for (int i = 0; i < 26575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26575(int& val) {
    const int x = 26575;
    std::cout << 26575;
    for (int i = 0; i < 26575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26576(int& val) {
    func_in_26576(val);
    const int x = 26576;
    std::cout << 26576;
    for (int i = 0; i < 26576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26576(int& val) {
    const int x = 26576;
    std::cout << 26576;
    for (int i = 0; i < 26576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26577(int& val) {
    func_in_26577(val);
    const int x = 26577;
    std::cout << 26577;
    for (int i = 0; i < 26577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26577(int& val) {
    const int x = 26577;
    std::cout << 26577;
    for (int i = 0; i < 26577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26578(int& val) {
    func_in_26578(val);
    const int x = 26578;
    std::cout << 26578;
    for (int i = 0; i < 26578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26578(int& val) {
    const int x = 26578;
    std::cout << 26578;
    for (int i = 0; i < 26578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26579(int& val) {
    func_in_26579(val);
    const int x = 26579;
    std::cout << 26579;
    for (int i = 0; i < 26579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26579(int& val) {
    const int x = 26579;
    std::cout << 26579;
    for (int i = 0; i < 26579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26580(int& val) {
    func_in_26580(val);
    const int x = 26580;
    std::cout << 26580;
    for (int i = 0; i < 26580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26580(int& val) {
    const int x = 26580;
    std::cout << 26580;
    for (int i = 0; i < 26580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26581(int& val) {
    func_in_26581(val);
    const int x = 26581;
    std::cout << 26581;
    for (int i = 0; i < 26581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26581(int& val) {
    const int x = 26581;
    std::cout << 26581;
    for (int i = 0; i < 26581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26582(int& val) {
    func_in_26582(val);
    const int x = 26582;
    std::cout << 26582;
    for (int i = 0; i < 26582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26582(int& val) {
    const int x = 26582;
    std::cout << 26582;
    for (int i = 0; i < 26582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26583(int& val) {
    func_in_26583(val);
    const int x = 26583;
    std::cout << 26583;
    for (int i = 0; i < 26583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26583(int& val) {
    const int x = 26583;
    std::cout << 26583;
    for (int i = 0; i < 26583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26584(int& val) {
    func_in_26584(val);
    const int x = 26584;
    std::cout << 26584;
    for (int i = 0; i < 26584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26584(int& val) {
    const int x = 26584;
    std::cout << 26584;
    for (int i = 0; i < 26584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26585(int& val) {
    func_in_26585(val);
    const int x = 26585;
    std::cout << 26585;
    for (int i = 0; i < 26585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26585(int& val) {
    const int x = 26585;
    std::cout << 26585;
    for (int i = 0; i < 26585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26586(int& val) {
    func_in_26586(val);
    const int x = 26586;
    std::cout << 26586;
    for (int i = 0; i < 26586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26586(int& val) {
    const int x = 26586;
    std::cout << 26586;
    for (int i = 0; i < 26586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26587(int& val) {
    func_in_26587(val);
    const int x = 26587;
    std::cout << 26587;
    for (int i = 0; i < 26587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26587(int& val) {
    const int x = 26587;
    std::cout << 26587;
    for (int i = 0; i < 26587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26588(int& val) {
    func_in_26588(val);
    const int x = 26588;
    std::cout << 26588;
    for (int i = 0; i < 26588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26588(int& val) {
    const int x = 26588;
    std::cout << 26588;
    for (int i = 0; i < 26588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26589(int& val) {
    func_in_26589(val);
    const int x = 26589;
    std::cout << 26589;
    for (int i = 0; i < 26589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26589(int& val) {
    const int x = 26589;
    std::cout << 26589;
    for (int i = 0; i < 26589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26590(int& val) {
    func_in_26590(val);
    const int x = 26590;
    std::cout << 26590;
    for (int i = 0; i < 26590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26590(int& val) {
    const int x = 26590;
    std::cout << 26590;
    for (int i = 0; i < 26590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26591(int& val) {
    func_in_26591(val);
    const int x = 26591;
    std::cout << 26591;
    for (int i = 0; i < 26591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26591(int& val) {
    const int x = 26591;
    std::cout << 26591;
    for (int i = 0; i < 26591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26592(int& val) {
    func_in_26592(val);
    const int x = 26592;
    std::cout << 26592;
    for (int i = 0; i < 26592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26592(int& val) {
    const int x = 26592;
    std::cout << 26592;
    for (int i = 0; i < 26592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26593(int& val) {
    func_in_26593(val);
    const int x = 26593;
    std::cout << 26593;
    for (int i = 0; i < 26593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26593(int& val) {
    const int x = 26593;
    std::cout << 26593;
    for (int i = 0; i < 26593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26594(int& val) {
    func_in_26594(val);
    const int x = 26594;
    std::cout << 26594;
    for (int i = 0; i < 26594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26594(int& val) {
    const int x = 26594;
    std::cout << 26594;
    for (int i = 0; i < 26594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26595(int& val) {
    func_in_26595(val);
    const int x = 26595;
    std::cout << 26595;
    for (int i = 0; i < 26595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26595(int& val) {
    const int x = 26595;
    std::cout << 26595;
    for (int i = 0; i < 26595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26596(int& val) {
    func_in_26596(val);
    const int x = 26596;
    std::cout << 26596;
    for (int i = 0; i < 26596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26596(int& val) {
    const int x = 26596;
    std::cout << 26596;
    for (int i = 0; i < 26596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26597(int& val) {
    func_in_26597(val);
    const int x = 26597;
    std::cout << 26597;
    for (int i = 0; i < 26597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26597(int& val) {
    const int x = 26597;
    std::cout << 26597;
    for (int i = 0; i < 26597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26598(int& val) {
    func_in_26598(val);
    const int x = 26598;
    std::cout << 26598;
    for (int i = 0; i < 26598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26598(int& val) {
    const int x = 26598;
    std::cout << 26598;
    for (int i = 0; i < 26598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26599(int& val) {
    func_in_26599(val);
    const int x = 26599;
    std::cout << 26599;
    for (int i = 0; i < 26599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26599(int& val) {
    const int x = 26599;
    std::cout << 26599;
    for (int i = 0; i < 26599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26600(int& val) {
    func_in_26600(val);
    const int x = 26600;
    std::cout << 26600;
    for (int i = 0; i < 26600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26600(int& val) {
    const int x = 26600;
    std::cout << 26600;
    for (int i = 0; i < 26600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26601(int& val) {
    func_in_26601(val);
    const int x = 26601;
    std::cout << 26601;
    for (int i = 0; i < 26601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26601(int& val) {
    const int x = 26601;
    std::cout << 26601;
    for (int i = 0; i < 26601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26602(int& val) {
    func_in_26602(val);
    const int x = 26602;
    std::cout << 26602;
    for (int i = 0; i < 26602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26602(int& val) {
    const int x = 26602;
    std::cout << 26602;
    for (int i = 0; i < 26602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26603(int& val) {
    func_in_26603(val);
    const int x = 26603;
    std::cout << 26603;
    for (int i = 0; i < 26603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26603(int& val) {
    const int x = 26603;
    std::cout << 26603;
    for (int i = 0; i < 26603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26604(int& val) {
    func_in_26604(val);
    const int x = 26604;
    std::cout << 26604;
    for (int i = 0; i < 26604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26604(int& val) {
    const int x = 26604;
    std::cout << 26604;
    for (int i = 0; i < 26604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26605(int& val) {
    func_in_26605(val);
    const int x = 26605;
    std::cout << 26605;
    for (int i = 0; i < 26605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26605(int& val) {
    const int x = 26605;
    std::cout << 26605;
    for (int i = 0; i < 26605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26606(int& val) {
    func_in_26606(val);
    const int x = 26606;
    std::cout << 26606;
    for (int i = 0; i < 26606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26606(int& val) {
    const int x = 26606;
    std::cout << 26606;
    for (int i = 0; i < 26606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26607(int& val) {
    func_in_26607(val);
    const int x = 26607;
    std::cout << 26607;
    for (int i = 0; i < 26607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26607(int& val) {
    const int x = 26607;
    std::cout << 26607;
    for (int i = 0; i < 26607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26608(int& val) {
    func_in_26608(val);
    const int x = 26608;
    std::cout << 26608;
    for (int i = 0; i < 26608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26608(int& val) {
    const int x = 26608;
    std::cout << 26608;
    for (int i = 0; i < 26608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26609(int& val) {
    func_in_26609(val);
    const int x = 26609;
    std::cout << 26609;
    for (int i = 0; i < 26609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26609(int& val) {
    const int x = 26609;
    std::cout << 26609;
    for (int i = 0; i < 26609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26610(int& val) {
    func_in_26610(val);
    const int x = 26610;
    std::cout << 26610;
    for (int i = 0; i < 26610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26610(int& val) {
    const int x = 26610;
    std::cout << 26610;
    for (int i = 0; i < 26610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26611(int& val) {
    func_in_26611(val);
    const int x = 26611;
    std::cout << 26611;
    for (int i = 0; i < 26611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26611(int& val) {
    const int x = 26611;
    std::cout << 26611;
    for (int i = 0; i < 26611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26612(int& val) {
    func_in_26612(val);
    const int x = 26612;
    std::cout << 26612;
    for (int i = 0; i < 26612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26612(int& val) {
    const int x = 26612;
    std::cout << 26612;
    for (int i = 0; i < 26612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26613(int& val) {
    func_in_26613(val);
    const int x = 26613;
    std::cout << 26613;
    for (int i = 0; i < 26613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26613(int& val) {
    const int x = 26613;
    std::cout << 26613;
    for (int i = 0; i < 26613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26614(int& val) {
    func_in_26614(val);
    const int x = 26614;
    std::cout << 26614;
    for (int i = 0; i < 26614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26614(int& val) {
    const int x = 26614;
    std::cout << 26614;
    for (int i = 0; i < 26614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26615(int& val) {
    func_in_26615(val);
    const int x = 26615;
    std::cout << 26615;
    for (int i = 0; i < 26615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26615(int& val) {
    const int x = 26615;
    std::cout << 26615;
    for (int i = 0; i < 26615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26616(int& val) {
    func_in_26616(val);
    const int x = 26616;
    std::cout << 26616;
    for (int i = 0; i < 26616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26616(int& val) {
    const int x = 26616;
    std::cout << 26616;
    for (int i = 0; i < 26616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26617(int& val) {
    func_in_26617(val);
    const int x = 26617;
    std::cout << 26617;
    for (int i = 0; i < 26617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26617(int& val) {
    const int x = 26617;
    std::cout << 26617;
    for (int i = 0; i < 26617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26618(int& val) {
    func_in_26618(val);
    const int x = 26618;
    std::cout << 26618;
    for (int i = 0; i < 26618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26618(int& val) {
    const int x = 26618;
    std::cout << 26618;
    for (int i = 0; i < 26618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26619(int& val) {
    func_in_26619(val);
    const int x = 26619;
    std::cout << 26619;
    for (int i = 0; i < 26619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26619(int& val) {
    const int x = 26619;
    std::cout << 26619;
    for (int i = 0; i < 26619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26620(int& val) {
    func_in_26620(val);
    const int x = 26620;
    std::cout << 26620;
    for (int i = 0; i < 26620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26620(int& val) {
    const int x = 26620;
    std::cout << 26620;
    for (int i = 0; i < 26620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26621(int& val) {
    func_in_26621(val);
    const int x = 26621;
    std::cout << 26621;
    for (int i = 0; i < 26621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26621(int& val) {
    const int x = 26621;
    std::cout << 26621;
    for (int i = 0; i < 26621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26622(int& val) {
    func_in_26622(val);
    const int x = 26622;
    std::cout << 26622;
    for (int i = 0; i < 26622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26622(int& val) {
    const int x = 26622;
    std::cout << 26622;
    for (int i = 0; i < 26622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26623(int& val) {
    func_in_26623(val);
    const int x = 26623;
    std::cout << 26623;
    for (int i = 0; i < 26623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26623(int& val) {
    const int x = 26623;
    std::cout << 26623;
    for (int i = 0; i < 26623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26624(int& val) {
    func_in_26624(val);
    const int x = 26624;
    std::cout << 26624;
    for (int i = 0; i < 26624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26624(int& val) {
    const int x = 26624;
    std::cout << 26624;
    for (int i = 0; i < 26624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26625(int& val) {
    func_in_26625(val);
    const int x = 26625;
    std::cout << 26625;
    for (int i = 0; i < 26625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26625(int& val) {
    const int x = 26625;
    std::cout << 26625;
    for (int i = 0; i < 26625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26626(int& val) {
    func_in_26626(val);
    const int x = 26626;
    std::cout << 26626;
    for (int i = 0; i < 26626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26626(int& val) {
    const int x = 26626;
    std::cout << 26626;
    for (int i = 0; i < 26626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26627(int& val) {
    func_in_26627(val);
    const int x = 26627;
    std::cout << 26627;
    for (int i = 0; i < 26627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26627(int& val) {
    const int x = 26627;
    std::cout << 26627;
    for (int i = 0; i < 26627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26628(int& val) {
    func_in_26628(val);
    const int x = 26628;
    std::cout << 26628;
    for (int i = 0; i < 26628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26628(int& val) {
    const int x = 26628;
    std::cout << 26628;
    for (int i = 0; i < 26628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26629(int& val) {
    func_in_26629(val);
    const int x = 26629;
    std::cout << 26629;
    for (int i = 0; i < 26629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26629(int& val) {
    const int x = 26629;
    std::cout << 26629;
    for (int i = 0; i < 26629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26630(int& val) {
    func_in_26630(val);
    const int x = 26630;
    std::cout << 26630;
    for (int i = 0; i < 26630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26630(int& val) {
    const int x = 26630;
    std::cout << 26630;
    for (int i = 0; i < 26630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26631(int& val) {
    func_in_26631(val);
    const int x = 26631;
    std::cout << 26631;
    for (int i = 0; i < 26631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26631(int& val) {
    const int x = 26631;
    std::cout << 26631;
    for (int i = 0; i < 26631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26632(int& val) {
    func_in_26632(val);
    const int x = 26632;
    std::cout << 26632;
    for (int i = 0; i < 26632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26632(int& val) {
    const int x = 26632;
    std::cout << 26632;
    for (int i = 0; i < 26632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26633(int& val) {
    func_in_26633(val);
    const int x = 26633;
    std::cout << 26633;
    for (int i = 0; i < 26633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26633(int& val) {
    const int x = 26633;
    std::cout << 26633;
    for (int i = 0; i < 26633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26634(int& val) {
    func_in_26634(val);
    const int x = 26634;
    std::cout << 26634;
    for (int i = 0; i < 26634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26634(int& val) {
    const int x = 26634;
    std::cout << 26634;
    for (int i = 0; i < 26634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26635(int& val) {
    func_in_26635(val);
    const int x = 26635;
    std::cout << 26635;
    for (int i = 0; i < 26635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26635(int& val) {
    const int x = 26635;
    std::cout << 26635;
    for (int i = 0; i < 26635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26636(int& val) {
    func_in_26636(val);
    const int x = 26636;
    std::cout << 26636;
    for (int i = 0; i < 26636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26636(int& val) {
    const int x = 26636;
    std::cout << 26636;
    for (int i = 0; i < 26636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26637(int& val) {
    func_in_26637(val);
    const int x = 26637;
    std::cout << 26637;
    for (int i = 0; i < 26637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26637(int& val) {
    const int x = 26637;
    std::cout << 26637;
    for (int i = 0; i < 26637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26638(int& val) {
    func_in_26638(val);
    const int x = 26638;
    std::cout << 26638;
    for (int i = 0; i < 26638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26638(int& val) {
    const int x = 26638;
    std::cout << 26638;
    for (int i = 0; i < 26638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26639(int& val) {
    func_in_26639(val);
    const int x = 26639;
    std::cout << 26639;
    for (int i = 0; i < 26639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26639(int& val) {
    const int x = 26639;
    std::cout << 26639;
    for (int i = 0; i < 26639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26640(int& val) {
    func_in_26640(val);
    const int x = 26640;
    std::cout << 26640;
    for (int i = 0; i < 26640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26640(int& val) {
    const int x = 26640;
    std::cout << 26640;
    for (int i = 0; i < 26640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26641(int& val) {
    func_in_26641(val);
    const int x = 26641;
    std::cout << 26641;
    for (int i = 0; i < 26641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26641(int& val) {
    const int x = 26641;
    std::cout << 26641;
    for (int i = 0; i < 26641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26642(int& val) {
    func_in_26642(val);
    const int x = 26642;
    std::cout << 26642;
    for (int i = 0; i < 26642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26642(int& val) {
    const int x = 26642;
    std::cout << 26642;
    for (int i = 0; i < 26642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26643(int& val) {
    func_in_26643(val);
    const int x = 26643;
    std::cout << 26643;
    for (int i = 0; i < 26643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26643(int& val) {
    const int x = 26643;
    std::cout << 26643;
    for (int i = 0; i < 26643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26644(int& val) {
    func_in_26644(val);
    const int x = 26644;
    std::cout << 26644;
    for (int i = 0; i < 26644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26644(int& val) {
    const int x = 26644;
    std::cout << 26644;
    for (int i = 0; i < 26644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26645(int& val) {
    func_in_26645(val);
    const int x = 26645;
    std::cout << 26645;
    for (int i = 0; i < 26645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26645(int& val) {
    const int x = 26645;
    std::cout << 26645;
    for (int i = 0; i < 26645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26646(int& val) {
    func_in_26646(val);
    const int x = 26646;
    std::cout << 26646;
    for (int i = 0; i < 26646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26646(int& val) {
    const int x = 26646;
    std::cout << 26646;
    for (int i = 0; i < 26646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26647(int& val) {
    func_in_26647(val);
    const int x = 26647;
    std::cout << 26647;
    for (int i = 0; i < 26647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26647(int& val) {
    const int x = 26647;
    std::cout << 26647;
    for (int i = 0; i < 26647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26648(int& val) {
    func_in_26648(val);
    const int x = 26648;
    std::cout << 26648;
    for (int i = 0; i < 26648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26648(int& val) {
    const int x = 26648;
    std::cout << 26648;
    for (int i = 0; i < 26648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26649(int& val) {
    func_in_26649(val);
    const int x = 26649;
    std::cout << 26649;
    for (int i = 0; i < 26649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26649(int& val) {
    const int x = 26649;
    std::cout << 26649;
    for (int i = 0; i < 26649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26650(int& val) {
    func_in_26650(val);
    const int x = 26650;
    std::cout << 26650;
    for (int i = 0; i < 26650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26650(int& val) {
    const int x = 26650;
    std::cout << 26650;
    for (int i = 0; i < 26650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26651(int& val) {
    func_in_26651(val);
    const int x = 26651;
    std::cout << 26651;
    for (int i = 0; i < 26651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26651(int& val) {
    const int x = 26651;
    std::cout << 26651;
    for (int i = 0; i < 26651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26652(int& val) {
    func_in_26652(val);
    const int x = 26652;
    std::cout << 26652;
    for (int i = 0; i < 26652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26652(int& val) {
    const int x = 26652;
    std::cout << 26652;
    for (int i = 0; i < 26652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26653(int& val) {
    func_in_26653(val);
    const int x = 26653;
    std::cout << 26653;
    for (int i = 0; i < 26653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26653(int& val) {
    const int x = 26653;
    std::cout << 26653;
    for (int i = 0; i < 26653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26654(int& val) {
    func_in_26654(val);
    const int x = 26654;
    std::cout << 26654;
    for (int i = 0; i < 26654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26654(int& val) {
    const int x = 26654;
    std::cout << 26654;
    for (int i = 0; i < 26654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26655(int& val) {
    func_in_26655(val);
    const int x = 26655;
    std::cout << 26655;
    for (int i = 0; i < 26655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26655(int& val) {
    const int x = 26655;
    std::cout << 26655;
    for (int i = 0; i < 26655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26656(int& val) {
    func_in_26656(val);
    const int x = 26656;
    std::cout << 26656;
    for (int i = 0; i < 26656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26656(int& val) {
    const int x = 26656;
    std::cout << 26656;
    for (int i = 0; i < 26656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26657(int& val) {
    func_in_26657(val);
    const int x = 26657;
    std::cout << 26657;
    for (int i = 0; i < 26657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26657(int& val) {
    const int x = 26657;
    std::cout << 26657;
    for (int i = 0; i < 26657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26658(int& val) {
    func_in_26658(val);
    const int x = 26658;
    std::cout << 26658;
    for (int i = 0; i < 26658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26658(int& val) {
    const int x = 26658;
    std::cout << 26658;
    for (int i = 0; i < 26658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26659(int& val) {
    func_in_26659(val);
    const int x = 26659;
    std::cout << 26659;
    for (int i = 0; i < 26659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26659(int& val) {
    const int x = 26659;
    std::cout << 26659;
    for (int i = 0; i < 26659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26660(int& val) {
    func_in_26660(val);
    const int x = 26660;
    std::cout << 26660;
    for (int i = 0; i < 26660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26660(int& val) {
    const int x = 26660;
    std::cout << 26660;
    for (int i = 0; i < 26660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26661(int& val) {
    func_in_26661(val);
    const int x = 26661;
    std::cout << 26661;
    for (int i = 0; i < 26661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26661(int& val) {
    const int x = 26661;
    std::cout << 26661;
    for (int i = 0; i < 26661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26662(int& val) {
    func_in_26662(val);
    const int x = 26662;
    std::cout << 26662;
    for (int i = 0; i < 26662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26662(int& val) {
    const int x = 26662;
    std::cout << 26662;
    for (int i = 0; i < 26662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26663(int& val) {
    func_in_26663(val);
    const int x = 26663;
    std::cout << 26663;
    for (int i = 0; i < 26663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26663(int& val) {
    const int x = 26663;
    std::cout << 26663;
    for (int i = 0; i < 26663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26664(int& val) {
    func_in_26664(val);
    const int x = 26664;
    std::cout << 26664;
    for (int i = 0; i < 26664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26664(int& val) {
    const int x = 26664;
    std::cout << 26664;
    for (int i = 0; i < 26664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26665(int& val) {
    func_in_26665(val);
    const int x = 26665;
    std::cout << 26665;
    for (int i = 0; i < 26665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26665(int& val) {
    const int x = 26665;
    std::cout << 26665;
    for (int i = 0; i < 26665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26666(int& val) {
    func_in_26666(val);
    const int x = 26666;
    std::cout << 26666;
    for (int i = 0; i < 26666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26666(int& val) {
    const int x = 26666;
    std::cout << 26666;
    for (int i = 0; i < 26666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26667(int& val) {
    func_in_26667(val);
    const int x = 26667;
    std::cout << 26667;
    for (int i = 0; i < 26667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26667(int& val) {
    const int x = 26667;
    std::cout << 26667;
    for (int i = 0; i < 26667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26668(int& val) {
    func_in_26668(val);
    const int x = 26668;
    std::cout << 26668;
    for (int i = 0; i < 26668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26668(int& val) {
    const int x = 26668;
    std::cout << 26668;
    for (int i = 0; i < 26668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26669(int& val) {
    func_in_26669(val);
    const int x = 26669;
    std::cout << 26669;
    for (int i = 0; i < 26669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26669(int& val) {
    const int x = 26669;
    std::cout << 26669;
    for (int i = 0; i < 26669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26670(int& val) {
    func_in_26670(val);
    const int x = 26670;
    std::cout << 26670;
    for (int i = 0; i < 26670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26670(int& val) {
    const int x = 26670;
    std::cout << 26670;
    for (int i = 0; i < 26670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26671(int& val) {
    func_in_26671(val);
    const int x = 26671;
    std::cout << 26671;
    for (int i = 0; i < 26671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26671(int& val) {
    const int x = 26671;
    std::cout << 26671;
    for (int i = 0; i < 26671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26672(int& val) {
    func_in_26672(val);
    const int x = 26672;
    std::cout << 26672;
    for (int i = 0; i < 26672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26672(int& val) {
    const int x = 26672;
    std::cout << 26672;
    for (int i = 0; i < 26672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26673(int& val) {
    func_in_26673(val);
    const int x = 26673;
    std::cout << 26673;
    for (int i = 0; i < 26673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26673(int& val) {
    const int x = 26673;
    std::cout << 26673;
    for (int i = 0; i < 26673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26674(int& val) {
    func_in_26674(val);
    const int x = 26674;
    std::cout << 26674;
    for (int i = 0; i < 26674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26674(int& val) {
    const int x = 26674;
    std::cout << 26674;
    for (int i = 0; i < 26674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26675(int& val) {
    func_in_26675(val);
    const int x = 26675;
    std::cout << 26675;
    for (int i = 0; i < 26675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26675(int& val) {
    const int x = 26675;
    std::cout << 26675;
    for (int i = 0; i < 26675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26676(int& val) {
    func_in_26676(val);
    const int x = 26676;
    std::cout << 26676;
    for (int i = 0; i < 26676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26676(int& val) {
    const int x = 26676;
    std::cout << 26676;
    for (int i = 0; i < 26676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26677(int& val) {
    func_in_26677(val);
    const int x = 26677;
    std::cout << 26677;
    for (int i = 0; i < 26677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26677(int& val) {
    const int x = 26677;
    std::cout << 26677;
    for (int i = 0; i < 26677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26678(int& val) {
    func_in_26678(val);
    const int x = 26678;
    std::cout << 26678;
    for (int i = 0; i < 26678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26678(int& val) {
    const int x = 26678;
    std::cout << 26678;
    for (int i = 0; i < 26678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26679(int& val) {
    func_in_26679(val);
    const int x = 26679;
    std::cout << 26679;
    for (int i = 0; i < 26679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26679(int& val) {
    const int x = 26679;
    std::cout << 26679;
    for (int i = 0; i < 26679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26680(int& val) {
    func_in_26680(val);
    const int x = 26680;
    std::cout << 26680;
    for (int i = 0; i < 26680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26680(int& val) {
    const int x = 26680;
    std::cout << 26680;
    for (int i = 0; i < 26680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26681(int& val) {
    func_in_26681(val);
    const int x = 26681;
    std::cout << 26681;
    for (int i = 0; i < 26681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26681(int& val) {
    const int x = 26681;
    std::cout << 26681;
    for (int i = 0; i < 26681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26682(int& val) {
    func_in_26682(val);
    const int x = 26682;
    std::cout << 26682;
    for (int i = 0; i < 26682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26682(int& val) {
    const int x = 26682;
    std::cout << 26682;
    for (int i = 0; i < 26682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26683(int& val) {
    func_in_26683(val);
    const int x = 26683;
    std::cout << 26683;
    for (int i = 0; i < 26683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26683(int& val) {
    const int x = 26683;
    std::cout << 26683;
    for (int i = 0; i < 26683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26684(int& val) {
    func_in_26684(val);
    const int x = 26684;
    std::cout << 26684;
    for (int i = 0; i < 26684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26684(int& val) {
    const int x = 26684;
    std::cout << 26684;
    for (int i = 0; i < 26684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26685(int& val) {
    func_in_26685(val);
    const int x = 26685;
    std::cout << 26685;
    for (int i = 0; i < 26685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26685(int& val) {
    const int x = 26685;
    std::cout << 26685;
    for (int i = 0; i < 26685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26686(int& val) {
    func_in_26686(val);
    const int x = 26686;
    std::cout << 26686;
    for (int i = 0; i < 26686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26686(int& val) {
    const int x = 26686;
    std::cout << 26686;
    for (int i = 0; i < 26686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26687(int& val) {
    func_in_26687(val);
    const int x = 26687;
    std::cout << 26687;
    for (int i = 0; i < 26687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26687(int& val) {
    const int x = 26687;
    std::cout << 26687;
    for (int i = 0; i < 26687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26688(int& val) {
    func_in_26688(val);
    const int x = 26688;
    std::cout << 26688;
    for (int i = 0; i < 26688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26688(int& val) {
    const int x = 26688;
    std::cout << 26688;
    for (int i = 0; i < 26688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26689(int& val) {
    func_in_26689(val);
    const int x = 26689;
    std::cout << 26689;
    for (int i = 0; i < 26689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26689(int& val) {
    const int x = 26689;
    std::cout << 26689;
    for (int i = 0; i < 26689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26690(int& val) {
    func_in_26690(val);
    const int x = 26690;
    std::cout << 26690;
    for (int i = 0; i < 26690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26690(int& val) {
    const int x = 26690;
    std::cout << 26690;
    for (int i = 0; i < 26690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26691(int& val) {
    func_in_26691(val);
    const int x = 26691;
    std::cout << 26691;
    for (int i = 0; i < 26691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26691(int& val) {
    const int x = 26691;
    std::cout << 26691;
    for (int i = 0; i < 26691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26692(int& val) {
    func_in_26692(val);
    const int x = 26692;
    std::cout << 26692;
    for (int i = 0; i < 26692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26692(int& val) {
    const int x = 26692;
    std::cout << 26692;
    for (int i = 0; i < 26692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26693(int& val) {
    func_in_26693(val);
    const int x = 26693;
    std::cout << 26693;
    for (int i = 0; i < 26693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26693(int& val) {
    const int x = 26693;
    std::cout << 26693;
    for (int i = 0; i < 26693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26694(int& val) {
    func_in_26694(val);
    const int x = 26694;
    std::cout << 26694;
    for (int i = 0; i < 26694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26694(int& val) {
    const int x = 26694;
    std::cout << 26694;
    for (int i = 0; i < 26694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26695(int& val) {
    func_in_26695(val);
    const int x = 26695;
    std::cout << 26695;
    for (int i = 0; i < 26695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26695(int& val) {
    const int x = 26695;
    std::cout << 26695;
    for (int i = 0; i < 26695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26696(int& val) {
    func_in_26696(val);
    const int x = 26696;
    std::cout << 26696;
    for (int i = 0; i < 26696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26696(int& val) {
    const int x = 26696;
    std::cout << 26696;
    for (int i = 0; i < 26696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26697(int& val) {
    func_in_26697(val);
    const int x = 26697;
    std::cout << 26697;
    for (int i = 0; i < 26697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26697(int& val) {
    const int x = 26697;
    std::cout << 26697;
    for (int i = 0; i < 26697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26698(int& val) {
    func_in_26698(val);
    const int x = 26698;
    std::cout << 26698;
    for (int i = 0; i < 26698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26698(int& val) {
    const int x = 26698;
    std::cout << 26698;
    for (int i = 0; i < 26698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26699(int& val) {
    func_in_26699(val);
    const int x = 26699;
    std::cout << 26699;
    for (int i = 0; i < 26699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26699(int& val) {
    const int x = 26699;
    std::cout << 26699;
    for (int i = 0; i < 26699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26700(int& val) {
    func_in_26700(val);
    const int x = 26700;
    std::cout << 26700;
    for (int i = 0; i < 26700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26700(int& val) {
    const int x = 26700;
    std::cout << 26700;
    for (int i = 0; i < 26700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26701(int& val) {
    func_in_26701(val);
    const int x = 26701;
    std::cout << 26701;
    for (int i = 0; i < 26701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26701(int& val) {
    const int x = 26701;
    std::cout << 26701;
    for (int i = 0; i < 26701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26702(int& val) {
    func_in_26702(val);
    const int x = 26702;
    std::cout << 26702;
    for (int i = 0; i < 26702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26702(int& val) {
    const int x = 26702;
    std::cout << 26702;
    for (int i = 0; i < 26702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26703(int& val) {
    func_in_26703(val);
    const int x = 26703;
    std::cout << 26703;
    for (int i = 0; i < 26703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26703(int& val) {
    const int x = 26703;
    std::cout << 26703;
    for (int i = 0; i < 26703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26704(int& val) {
    func_in_26704(val);
    const int x = 26704;
    std::cout << 26704;
    for (int i = 0; i < 26704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26704(int& val) {
    const int x = 26704;
    std::cout << 26704;
    for (int i = 0; i < 26704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26705(int& val) {
    func_in_26705(val);
    const int x = 26705;
    std::cout << 26705;
    for (int i = 0; i < 26705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26705(int& val) {
    const int x = 26705;
    std::cout << 26705;
    for (int i = 0; i < 26705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26706(int& val) {
    func_in_26706(val);
    const int x = 26706;
    std::cout << 26706;
    for (int i = 0; i < 26706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26706(int& val) {
    const int x = 26706;
    std::cout << 26706;
    for (int i = 0; i < 26706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26707(int& val) {
    func_in_26707(val);
    const int x = 26707;
    std::cout << 26707;
    for (int i = 0; i < 26707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26707(int& val) {
    const int x = 26707;
    std::cout << 26707;
    for (int i = 0; i < 26707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26708(int& val) {
    func_in_26708(val);
    const int x = 26708;
    std::cout << 26708;
    for (int i = 0; i < 26708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26708(int& val) {
    const int x = 26708;
    std::cout << 26708;
    for (int i = 0; i < 26708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26709(int& val) {
    func_in_26709(val);
    const int x = 26709;
    std::cout << 26709;
    for (int i = 0; i < 26709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26709(int& val) {
    const int x = 26709;
    std::cout << 26709;
    for (int i = 0; i < 26709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26710(int& val) {
    func_in_26710(val);
    const int x = 26710;
    std::cout << 26710;
    for (int i = 0; i < 26710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26710(int& val) {
    const int x = 26710;
    std::cout << 26710;
    for (int i = 0; i < 26710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26711(int& val) {
    func_in_26711(val);
    const int x = 26711;
    std::cout << 26711;
    for (int i = 0; i < 26711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26711(int& val) {
    const int x = 26711;
    std::cout << 26711;
    for (int i = 0; i < 26711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26712(int& val) {
    func_in_26712(val);
    const int x = 26712;
    std::cout << 26712;
    for (int i = 0; i < 26712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26712(int& val) {
    const int x = 26712;
    std::cout << 26712;
    for (int i = 0; i < 26712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26713(int& val) {
    func_in_26713(val);
    const int x = 26713;
    std::cout << 26713;
    for (int i = 0; i < 26713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26713(int& val) {
    const int x = 26713;
    std::cout << 26713;
    for (int i = 0; i < 26713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26714(int& val) {
    func_in_26714(val);
    const int x = 26714;
    std::cout << 26714;
    for (int i = 0; i < 26714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26714(int& val) {
    const int x = 26714;
    std::cout << 26714;
    for (int i = 0; i < 26714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26715(int& val) {
    func_in_26715(val);
    const int x = 26715;
    std::cout << 26715;
    for (int i = 0; i < 26715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26715(int& val) {
    const int x = 26715;
    std::cout << 26715;
    for (int i = 0; i < 26715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26716(int& val) {
    func_in_26716(val);
    const int x = 26716;
    std::cout << 26716;
    for (int i = 0; i < 26716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26716(int& val) {
    const int x = 26716;
    std::cout << 26716;
    for (int i = 0; i < 26716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26717(int& val) {
    func_in_26717(val);
    const int x = 26717;
    std::cout << 26717;
    for (int i = 0; i < 26717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26717(int& val) {
    const int x = 26717;
    std::cout << 26717;
    for (int i = 0; i < 26717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26718(int& val) {
    func_in_26718(val);
    const int x = 26718;
    std::cout << 26718;
    for (int i = 0; i < 26718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26718(int& val) {
    const int x = 26718;
    std::cout << 26718;
    for (int i = 0; i < 26718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26719(int& val) {
    func_in_26719(val);
    const int x = 26719;
    std::cout << 26719;
    for (int i = 0; i < 26719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26719(int& val) {
    const int x = 26719;
    std::cout << 26719;
    for (int i = 0; i < 26719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26720(int& val) {
    func_in_26720(val);
    const int x = 26720;
    std::cout << 26720;
    for (int i = 0; i < 26720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26720(int& val) {
    const int x = 26720;
    std::cout << 26720;
    for (int i = 0; i < 26720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26721(int& val) {
    func_in_26721(val);
    const int x = 26721;
    std::cout << 26721;
    for (int i = 0; i < 26721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26721(int& val) {
    const int x = 26721;
    std::cout << 26721;
    for (int i = 0; i < 26721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26722(int& val) {
    func_in_26722(val);
    const int x = 26722;
    std::cout << 26722;
    for (int i = 0; i < 26722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26722(int& val) {
    const int x = 26722;
    std::cout << 26722;
    for (int i = 0; i < 26722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26723(int& val) {
    func_in_26723(val);
    const int x = 26723;
    std::cout << 26723;
    for (int i = 0; i < 26723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26723(int& val) {
    const int x = 26723;
    std::cout << 26723;
    for (int i = 0; i < 26723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26724(int& val) {
    func_in_26724(val);
    const int x = 26724;
    std::cout << 26724;
    for (int i = 0; i < 26724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26724(int& val) {
    const int x = 26724;
    std::cout << 26724;
    for (int i = 0; i < 26724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26725(int& val) {
    func_in_26725(val);
    const int x = 26725;
    std::cout << 26725;
    for (int i = 0; i < 26725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26725(int& val) {
    const int x = 26725;
    std::cout << 26725;
    for (int i = 0; i < 26725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26726(int& val) {
    func_in_26726(val);
    const int x = 26726;
    std::cout << 26726;
    for (int i = 0; i < 26726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26726(int& val) {
    const int x = 26726;
    std::cout << 26726;
    for (int i = 0; i < 26726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26727(int& val) {
    func_in_26727(val);
    const int x = 26727;
    std::cout << 26727;
    for (int i = 0; i < 26727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26727(int& val) {
    const int x = 26727;
    std::cout << 26727;
    for (int i = 0; i < 26727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26728(int& val) {
    func_in_26728(val);
    const int x = 26728;
    std::cout << 26728;
    for (int i = 0; i < 26728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26728(int& val) {
    const int x = 26728;
    std::cout << 26728;
    for (int i = 0; i < 26728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26729(int& val) {
    func_in_26729(val);
    const int x = 26729;
    std::cout << 26729;
    for (int i = 0; i < 26729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26729(int& val) {
    const int x = 26729;
    std::cout << 26729;
    for (int i = 0; i < 26729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26730(int& val) {
    func_in_26730(val);
    const int x = 26730;
    std::cout << 26730;
    for (int i = 0; i < 26730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26730(int& val) {
    const int x = 26730;
    std::cout << 26730;
    for (int i = 0; i < 26730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26731(int& val) {
    func_in_26731(val);
    const int x = 26731;
    std::cout << 26731;
    for (int i = 0; i < 26731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26731(int& val) {
    const int x = 26731;
    std::cout << 26731;
    for (int i = 0; i < 26731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26732(int& val) {
    func_in_26732(val);
    const int x = 26732;
    std::cout << 26732;
    for (int i = 0; i < 26732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26732(int& val) {
    const int x = 26732;
    std::cout << 26732;
    for (int i = 0; i < 26732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26733(int& val) {
    func_in_26733(val);
    const int x = 26733;
    std::cout << 26733;
    for (int i = 0; i < 26733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26733(int& val) {
    const int x = 26733;
    std::cout << 26733;
    for (int i = 0; i < 26733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26734(int& val) {
    func_in_26734(val);
    const int x = 26734;
    std::cout << 26734;
    for (int i = 0; i < 26734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26734(int& val) {
    const int x = 26734;
    std::cout << 26734;
    for (int i = 0; i < 26734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26735(int& val) {
    func_in_26735(val);
    const int x = 26735;
    std::cout << 26735;
    for (int i = 0; i < 26735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26735(int& val) {
    const int x = 26735;
    std::cout << 26735;
    for (int i = 0; i < 26735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26736(int& val) {
    func_in_26736(val);
    const int x = 26736;
    std::cout << 26736;
    for (int i = 0; i < 26736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26736(int& val) {
    const int x = 26736;
    std::cout << 26736;
    for (int i = 0; i < 26736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26737(int& val) {
    func_in_26737(val);
    const int x = 26737;
    std::cout << 26737;
    for (int i = 0; i < 26737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26737(int& val) {
    const int x = 26737;
    std::cout << 26737;
    for (int i = 0; i < 26737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26738(int& val) {
    func_in_26738(val);
    const int x = 26738;
    std::cout << 26738;
    for (int i = 0; i < 26738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26738(int& val) {
    const int x = 26738;
    std::cout << 26738;
    for (int i = 0; i < 26738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26739(int& val) {
    func_in_26739(val);
    const int x = 26739;
    std::cout << 26739;
    for (int i = 0; i < 26739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26739(int& val) {
    const int x = 26739;
    std::cout << 26739;
    for (int i = 0; i < 26739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26740(int& val) {
    func_in_26740(val);
    const int x = 26740;
    std::cout << 26740;
    for (int i = 0; i < 26740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26740(int& val) {
    const int x = 26740;
    std::cout << 26740;
    for (int i = 0; i < 26740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26741(int& val) {
    func_in_26741(val);
    const int x = 26741;
    std::cout << 26741;
    for (int i = 0; i < 26741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26741(int& val) {
    const int x = 26741;
    std::cout << 26741;
    for (int i = 0; i < 26741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26742(int& val) {
    func_in_26742(val);
    const int x = 26742;
    std::cout << 26742;
    for (int i = 0; i < 26742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26742(int& val) {
    const int x = 26742;
    std::cout << 26742;
    for (int i = 0; i < 26742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26743(int& val) {
    func_in_26743(val);
    const int x = 26743;
    std::cout << 26743;
    for (int i = 0; i < 26743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26743(int& val) {
    const int x = 26743;
    std::cout << 26743;
    for (int i = 0; i < 26743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26744(int& val) {
    func_in_26744(val);
    const int x = 26744;
    std::cout << 26744;
    for (int i = 0; i < 26744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26744(int& val) {
    const int x = 26744;
    std::cout << 26744;
    for (int i = 0; i < 26744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26745(int& val) {
    func_in_26745(val);
    const int x = 26745;
    std::cout << 26745;
    for (int i = 0; i < 26745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26745(int& val) {
    const int x = 26745;
    std::cout << 26745;
    for (int i = 0; i < 26745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26746(int& val) {
    func_in_26746(val);
    const int x = 26746;
    std::cout << 26746;
    for (int i = 0; i < 26746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26746(int& val) {
    const int x = 26746;
    std::cout << 26746;
    for (int i = 0; i < 26746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26747(int& val) {
    func_in_26747(val);
    const int x = 26747;
    std::cout << 26747;
    for (int i = 0; i < 26747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26747(int& val) {
    const int x = 26747;
    std::cout << 26747;
    for (int i = 0; i < 26747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26748(int& val) {
    func_in_26748(val);
    const int x = 26748;
    std::cout << 26748;
    for (int i = 0; i < 26748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26748(int& val) {
    const int x = 26748;
    std::cout << 26748;
    for (int i = 0; i < 26748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26749(int& val) {
    func_in_26749(val);
    const int x = 26749;
    std::cout << 26749;
    for (int i = 0; i < 26749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26749(int& val) {
    const int x = 26749;
    std::cout << 26749;
    for (int i = 0; i < 26749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26750(int& val) {
    func_in_26750(val);
    const int x = 26750;
    std::cout << 26750;
    for (int i = 0; i < 26750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26750(int& val) {
    const int x = 26750;
    std::cout << 26750;
    for (int i = 0; i < 26750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26751(int& val) {
    func_in_26751(val);
    const int x = 26751;
    std::cout << 26751;
    for (int i = 0; i < 26751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26751(int& val) {
    const int x = 26751;
    std::cout << 26751;
    for (int i = 0; i < 26751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26752(int& val) {
    func_in_26752(val);
    const int x = 26752;
    std::cout << 26752;
    for (int i = 0; i < 26752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26752(int& val) {
    const int x = 26752;
    std::cout << 26752;
    for (int i = 0; i < 26752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26753(int& val) {
    func_in_26753(val);
    const int x = 26753;
    std::cout << 26753;
    for (int i = 0; i < 26753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26753(int& val) {
    const int x = 26753;
    std::cout << 26753;
    for (int i = 0; i < 26753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26754(int& val) {
    func_in_26754(val);
    const int x = 26754;
    std::cout << 26754;
    for (int i = 0; i < 26754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26754(int& val) {
    const int x = 26754;
    std::cout << 26754;
    for (int i = 0; i < 26754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26755(int& val) {
    func_in_26755(val);
    const int x = 26755;
    std::cout << 26755;
    for (int i = 0; i < 26755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26755(int& val) {
    const int x = 26755;
    std::cout << 26755;
    for (int i = 0; i < 26755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26756(int& val) {
    func_in_26756(val);
    const int x = 26756;
    std::cout << 26756;
    for (int i = 0; i < 26756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26756(int& val) {
    const int x = 26756;
    std::cout << 26756;
    for (int i = 0; i < 26756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26757(int& val) {
    func_in_26757(val);
    const int x = 26757;
    std::cout << 26757;
    for (int i = 0; i < 26757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26757(int& val) {
    const int x = 26757;
    std::cout << 26757;
    for (int i = 0; i < 26757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26758(int& val) {
    func_in_26758(val);
    const int x = 26758;
    std::cout << 26758;
    for (int i = 0; i < 26758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26758(int& val) {
    const int x = 26758;
    std::cout << 26758;
    for (int i = 0; i < 26758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26759(int& val) {
    func_in_26759(val);
    const int x = 26759;
    std::cout << 26759;
    for (int i = 0; i < 26759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26759(int& val) {
    const int x = 26759;
    std::cout << 26759;
    for (int i = 0; i < 26759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26760(int& val) {
    func_in_26760(val);
    const int x = 26760;
    std::cout << 26760;
    for (int i = 0; i < 26760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26760(int& val) {
    const int x = 26760;
    std::cout << 26760;
    for (int i = 0; i < 26760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26761(int& val) {
    func_in_26761(val);
    const int x = 26761;
    std::cout << 26761;
    for (int i = 0; i < 26761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26761(int& val) {
    const int x = 26761;
    std::cout << 26761;
    for (int i = 0; i < 26761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26762(int& val) {
    func_in_26762(val);
    const int x = 26762;
    std::cout << 26762;
    for (int i = 0; i < 26762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26762(int& val) {
    const int x = 26762;
    std::cout << 26762;
    for (int i = 0; i < 26762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26763(int& val) {
    func_in_26763(val);
    const int x = 26763;
    std::cout << 26763;
    for (int i = 0; i < 26763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26763(int& val) {
    const int x = 26763;
    std::cout << 26763;
    for (int i = 0; i < 26763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26764(int& val) {
    func_in_26764(val);
    const int x = 26764;
    std::cout << 26764;
    for (int i = 0; i < 26764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26764(int& val) {
    const int x = 26764;
    std::cout << 26764;
    for (int i = 0; i < 26764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26765(int& val) {
    func_in_26765(val);
    const int x = 26765;
    std::cout << 26765;
    for (int i = 0; i < 26765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26765(int& val) {
    const int x = 26765;
    std::cout << 26765;
    for (int i = 0; i < 26765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26766(int& val) {
    func_in_26766(val);
    const int x = 26766;
    std::cout << 26766;
    for (int i = 0; i < 26766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26766(int& val) {
    const int x = 26766;
    std::cout << 26766;
    for (int i = 0; i < 26766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26767(int& val) {
    func_in_26767(val);
    const int x = 26767;
    std::cout << 26767;
    for (int i = 0; i < 26767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26767(int& val) {
    const int x = 26767;
    std::cout << 26767;
    for (int i = 0; i < 26767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26768(int& val) {
    func_in_26768(val);
    const int x = 26768;
    std::cout << 26768;
    for (int i = 0; i < 26768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26768(int& val) {
    const int x = 26768;
    std::cout << 26768;
    for (int i = 0; i < 26768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26769(int& val) {
    func_in_26769(val);
    const int x = 26769;
    std::cout << 26769;
    for (int i = 0; i < 26769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26769(int& val) {
    const int x = 26769;
    std::cout << 26769;
    for (int i = 0; i < 26769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26770(int& val) {
    func_in_26770(val);
    const int x = 26770;
    std::cout << 26770;
    for (int i = 0; i < 26770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26770(int& val) {
    const int x = 26770;
    std::cout << 26770;
    for (int i = 0; i < 26770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26771(int& val) {
    func_in_26771(val);
    const int x = 26771;
    std::cout << 26771;
    for (int i = 0; i < 26771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26771(int& val) {
    const int x = 26771;
    std::cout << 26771;
    for (int i = 0; i < 26771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26772(int& val) {
    func_in_26772(val);
    const int x = 26772;
    std::cout << 26772;
    for (int i = 0; i < 26772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26772(int& val) {
    const int x = 26772;
    std::cout << 26772;
    for (int i = 0; i < 26772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26773(int& val) {
    func_in_26773(val);
    const int x = 26773;
    std::cout << 26773;
    for (int i = 0; i < 26773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26773(int& val) {
    const int x = 26773;
    std::cout << 26773;
    for (int i = 0; i < 26773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26774(int& val) {
    func_in_26774(val);
    const int x = 26774;
    std::cout << 26774;
    for (int i = 0; i < 26774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26774(int& val) {
    const int x = 26774;
    std::cout << 26774;
    for (int i = 0; i < 26774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26775(int& val) {
    func_in_26775(val);
    const int x = 26775;
    std::cout << 26775;
    for (int i = 0; i < 26775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26775(int& val) {
    const int x = 26775;
    std::cout << 26775;
    for (int i = 0; i < 26775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26776(int& val) {
    func_in_26776(val);
    const int x = 26776;
    std::cout << 26776;
    for (int i = 0; i < 26776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26776(int& val) {
    const int x = 26776;
    std::cout << 26776;
    for (int i = 0; i < 26776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26777(int& val) {
    func_in_26777(val);
    const int x = 26777;
    std::cout << 26777;
    for (int i = 0; i < 26777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26777(int& val) {
    const int x = 26777;
    std::cout << 26777;
    for (int i = 0; i < 26777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26778(int& val) {
    func_in_26778(val);
    const int x = 26778;
    std::cout << 26778;
    for (int i = 0; i < 26778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26778(int& val) {
    const int x = 26778;
    std::cout << 26778;
    for (int i = 0; i < 26778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26779(int& val) {
    func_in_26779(val);
    const int x = 26779;
    std::cout << 26779;
    for (int i = 0; i < 26779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26779(int& val) {
    const int x = 26779;
    std::cout << 26779;
    for (int i = 0; i < 26779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26780(int& val) {
    func_in_26780(val);
    const int x = 26780;
    std::cout << 26780;
    for (int i = 0; i < 26780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26780(int& val) {
    const int x = 26780;
    std::cout << 26780;
    for (int i = 0; i < 26780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26781(int& val) {
    func_in_26781(val);
    const int x = 26781;
    std::cout << 26781;
    for (int i = 0; i < 26781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26781(int& val) {
    const int x = 26781;
    std::cout << 26781;
    for (int i = 0; i < 26781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26782(int& val) {
    func_in_26782(val);
    const int x = 26782;
    std::cout << 26782;
    for (int i = 0; i < 26782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26782(int& val) {
    const int x = 26782;
    std::cout << 26782;
    for (int i = 0; i < 26782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26783(int& val) {
    func_in_26783(val);
    const int x = 26783;
    std::cout << 26783;
    for (int i = 0; i < 26783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26783(int& val) {
    const int x = 26783;
    std::cout << 26783;
    for (int i = 0; i < 26783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26784(int& val) {
    func_in_26784(val);
    const int x = 26784;
    std::cout << 26784;
    for (int i = 0; i < 26784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26784(int& val) {
    const int x = 26784;
    std::cout << 26784;
    for (int i = 0; i < 26784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26785(int& val) {
    func_in_26785(val);
    const int x = 26785;
    std::cout << 26785;
    for (int i = 0; i < 26785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26785(int& val) {
    const int x = 26785;
    std::cout << 26785;
    for (int i = 0; i < 26785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26786(int& val) {
    func_in_26786(val);
    const int x = 26786;
    std::cout << 26786;
    for (int i = 0; i < 26786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26786(int& val) {
    const int x = 26786;
    std::cout << 26786;
    for (int i = 0; i < 26786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26787(int& val) {
    func_in_26787(val);
    const int x = 26787;
    std::cout << 26787;
    for (int i = 0; i < 26787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26787(int& val) {
    const int x = 26787;
    std::cout << 26787;
    for (int i = 0; i < 26787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26788(int& val) {
    func_in_26788(val);
    const int x = 26788;
    std::cout << 26788;
    for (int i = 0; i < 26788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26788(int& val) {
    const int x = 26788;
    std::cout << 26788;
    for (int i = 0; i < 26788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26789(int& val) {
    func_in_26789(val);
    const int x = 26789;
    std::cout << 26789;
    for (int i = 0; i < 26789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26789(int& val) {
    const int x = 26789;
    std::cout << 26789;
    for (int i = 0; i < 26789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26790(int& val) {
    func_in_26790(val);
    const int x = 26790;
    std::cout << 26790;
    for (int i = 0; i < 26790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26790(int& val) {
    const int x = 26790;
    std::cout << 26790;
    for (int i = 0; i < 26790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26791(int& val) {
    func_in_26791(val);
    const int x = 26791;
    std::cout << 26791;
    for (int i = 0; i < 26791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26791(int& val) {
    const int x = 26791;
    std::cout << 26791;
    for (int i = 0; i < 26791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26792(int& val) {
    func_in_26792(val);
    const int x = 26792;
    std::cout << 26792;
    for (int i = 0; i < 26792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26792(int& val) {
    const int x = 26792;
    std::cout << 26792;
    for (int i = 0; i < 26792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26793(int& val) {
    func_in_26793(val);
    const int x = 26793;
    std::cout << 26793;
    for (int i = 0; i < 26793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26793(int& val) {
    const int x = 26793;
    std::cout << 26793;
    for (int i = 0; i < 26793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26794(int& val) {
    func_in_26794(val);
    const int x = 26794;
    std::cout << 26794;
    for (int i = 0; i < 26794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26794(int& val) {
    const int x = 26794;
    std::cout << 26794;
    for (int i = 0; i < 26794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26795(int& val) {
    func_in_26795(val);
    const int x = 26795;
    std::cout << 26795;
    for (int i = 0; i < 26795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26795(int& val) {
    const int x = 26795;
    std::cout << 26795;
    for (int i = 0; i < 26795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26796(int& val) {
    func_in_26796(val);
    const int x = 26796;
    std::cout << 26796;
    for (int i = 0; i < 26796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26796(int& val) {
    const int x = 26796;
    std::cout << 26796;
    for (int i = 0; i < 26796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26797(int& val) {
    func_in_26797(val);
    const int x = 26797;
    std::cout << 26797;
    for (int i = 0; i < 26797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26797(int& val) {
    const int x = 26797;
    std::cout << 26797;
    for (int i = 0; i < 26797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26798(int& val) {
    func_in_26798(val);
    const int x = 26798;
    std::cout << 26798;
    for (int i = 0; i < 26798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26798(int& val) {
    const int x = 26798;
    std::cout << 26798;
    for (int i = 0; i < 26798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26799(int& val) {
    func_in_26799(val);
    const int x = 26799;
    std::cout << 26799;
    for (int i = 0; i < 26799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26799(int& val) {
    const int x = 26799;
    std::cout << 26799;
    for (int i = 0; i < 26799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26800(int& val) {
    func_in_26800(val);
    const int x = 26800;
    std::cout << 26800;
    for (int i = 0; i < 26800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26800(int& val) {
    const int x = 26800;
    std::cout << 26800;
    for (int i = 0; i < 26800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26801(int& val) {
    func_in_26801(val);
    const int x = 26801;
    std::cout << 26801;
    for (int i = 0; i < 26801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26801(int& val) {
    const int x = 26801;
    std::cout << 26801;
    for (int i = 0; i < 26801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26802(int& val) {
    func_in_26802(val);
    const int x = 26802;
    std::cout << 26802;
    for (int i = 0; i < 26802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26802(int& val) {
    const int x = 26802;
    std::cout << 26802;
    for (int i = 0; i < 26802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26803(int& val) {
    func_in_26803(val);
    const int x = 26803;
    std::cout << 26803;
    for (int i = 0; i < 26803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26803(int& val) {
    const int x = 26803;
    std::cout << 26803;
    for (int i = 0; i < 26803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26804(int& val) {
    func_in_26804(val);
    const int x = 26804;
    std::cout << 26804;
    for (int i = 0; i < 26804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26804(int& val) {
    const int x = 26804;
    std::cout << 26804;
    for (int i = 0; i < 26804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26805(int& val) {
    func_in_26805(val);
    const int x = 26805;
    std::cout << 26805;
    for (int i = 0; i < 26805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26805(int& val) {
    const int x = 26805;
    std::cout << 26805;
    for (int i = 0; i < 26805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26806(int& val) {
    func_in_26806(val);
    const int x = 26806;
    std::cout << 26806;
    for (int i = 0; i < 26806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26806(int& val) {
    const int x = 26806;
    std::cout << 26806;
    for (int i = 0; i < 26806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26807(int& val) {
    func_in_26807(val);
    const int x = 26807;
    std::cout << 26807;
    for (int i = 0; i < 26807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26807(int& val) {
    const int x = 26807;
    std::cout << 26807;
    for (int i = 0; i < 26807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26808(int& val) {
    func_in_26808(val);
    const int x = 26808;
    std::cout << 26808;
    for (int i = 0; i < 26808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26808(int& val) {
    const int x = 26808;
    std::cout << 26808;
    for (int i = 0; i < 26808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26809(int& val) {
    func_in_26809(val);
    const int x = 26809;
    std::cout << 26809;
    for (int i = 0; i < 26809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26809(int& val) {
    const int x = 26809;
    std::cout << 26809;
    for (int i = 0; i < 26809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26810(int& val) {
    func_in_26810(val);
    const int x = 26810;
    std::cout << 26810;
    for (int i = 0; i < 26810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26810(int& val) {
    const int x = 26810;
    std::cout << 26810;
    for (int i = 0; i < 26810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26811(int& val) {
    func_in_26811(val);
    const int x = 26811;
    std::cout << 26811;
    for (int i = 0; i < 26811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26811(int& val) {
    const int x = 26811;
    std::cout << 26811;
    for (int i = 0; i < 26811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26812(int& val) {
    func_in_26812(val);
    const int x = 26812;
    std::cout << 26812;
    for (int i = 0; i < 26812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26812(int& val) {
    const int x = 26812;
    std::cout << 26812;
    for (int i = 0; i < 26812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26813(int& val) {
    func_in_26813(val);
    const int x = 26813;
    std::cout << 26813;
    for (int i = 0; i < 26813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26813(int& val) {
    const int x = 26813;
    std::cout << 26813;
    for (int i = 0; i < 26813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26814(int& val) {
    func_in_26814(val);
    const int x = 26814;
    std::cout << 26814;
    for (int i = 0; i < 26814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26814(int& val) {
    const int x = 26814;
    std::cout << 26814;
    for (int i = 0; i < 26814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26815(int& val) {
    func_in_26815(val);
    const int x = 26815;
    std::cout << 26815;
    for (int i = 0; i < 26815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26815(int& val) {
    const int x = 26815;
    std::cout << 26815;
    for (int i = 0; i < 26815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26816(int& val) {
    func_in_26816(val);
    const int x = 26816;
    std::cout << 26816;
    for (int i = 0; i < 26816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26816(int& val) {
    const int x = 26816;
    std::cout << 26816;
    for (int i = 0; i < 26816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26817(int& val) {
    func_in_26817(val);
    const int x = 26817;
    std::cout << 26817;
    for (int i = 0; i < 26817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26817(int& val) {
    const int x = 26817;
    std::cout << 26817;
    for (int i = 0; i < 26817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26818(int& val) {
    func_in_26818(val);
    const int x = 26818;
    std::cout << 26818;
    for (int i = 0; i < 26818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26818(int& val) {
    const int x = 26818;
    std::cout << 26818;
    for (int i = 0; i < 26818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26819(int& val) {
    func_in_26819(val);
    const int x = 26819;
    std::cout << 26819;
    for (int i = 0; i < 26819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26819(int& val) {
    const int x = 26819;
    std::cout << 26819;
    for (int i = 0; i < 26819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26820(int& val) {
    func_in_26820(val);
    const int x = 26820;
    std::cout << 26820;
    for (int i = 0; i < 26820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26820(int& val) {
    const int x = 26820;
    std::cout << 26820;
    for (int i = 0; i < 26820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26821(int& val) {
    func_in_26821(val);
    const int x = 26821;
    std::cout << 26821;
    for (int i = 0; i < 26821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26821(int& val) {
    const int x = 26821;
    std::cout << 26821;
    for (int i = 0; i < 26821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26822(int& val) {
    func_in_26822(val);
    const int x = 26822;
    std::cout << 26822;
    for (int i = 0; i < 26822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26822(int& val) {
    const int x = 26822;
    std::cout << 26822;
    for (int i = 0; i < 26822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26823(int& val) {
    func_in_26823(val);
    const int x = 26823;
    std::cout << 26823;
    for (int i = 0; i < 26823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26823(int& val) {
    const int x = 26823;
    std::cout << 26823;
    for (int i = 0; i < 26823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26824(int& val) {
    func_in_26824(val);
    const int x = 26824;
    std::cout << 26824;
    for (int i = 0; i < 26824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26824(int& val) {
    const int x = 26824;
    std::cout << 26824;
    for (int i = 0; i < 26824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26825(int& val) {
    func_in_26825(val);
    const int x = 26825;
    std::cout << 26825;
    for (int i = 0; i < 26825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26825(int& val) {
    const int x = 26825;
    std::cout << 26825;
    for (int i = 0; i < 26825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26826(int& val) {
    func_in_26826(val);
    const int x = 26826;
    std::cout << 26826;
    for (int i = 0; i < 26826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26826(int& val) {
    const int x = 26826;
    std::cout << 26826;
    for (int i = 0; i < 26826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26827(int& val) {
    func_in_26827(val);
    const int x = 26827;
    std::cout << 26827;
    for (int i = 0; i < 26827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26827(int& val) {
    const int x = 26827;
    std::cout << 26827;
    for (int i = 0; i < 26827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26828(int& val) {
    func_in_26828(val);
    const int x = 26828;
    std::cout << 26828;
    for (int i = 0; i < 26828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26828(int& val) {
    const int x = 26828;
    std::cout << 26828;
    for (int i = 0; i < 26828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26829(int& val) {
    func_in_26829(val);
    const int x = 26829;
    std::cout << 26829;
    for (int i = 0; i < 26829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26829(int& val) {
    const int x = 26829;
    std::cout << 26829;
    for (int i = 0; i < 26829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26830(int& val) {
    func_in_26830(val);
    const int x = 26830;
    std::cout << 26830;
    for (int i = 0; i < 26830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26830(int& val) {
    const int x = 26830;
    std::cout << 26830;
    for (int i = 0; i < 26830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26831(int& val) {
    func_in_26831(val);
    const int x = 26831;
    std::cout << 26831;
    for (int i = 0; i < 26831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26831(int& val) {
    const int x = 26831;
    std::cout << 26831;
    for (int i = 0; i < 26831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26832(int& val) {
    func_in_26832(val);
    const int x = 26832;
    std::cout << 26832;
    for (int i = 0; i < 26832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26832(int& val) {
    const int x = 26832;
    std::cout << 26832;
    for (int i = 0; i < 26832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26833(int& val) {
    func_in_26833(val);
    const int x = 26833;
    std::cout << 26833;
    for (int i = 0; i < 26833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26833(int& val) {
    const int x = 26833;
    std::cout << 26833;
    for (int i = 0; i < 26833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26834(int& val) {
    func_in_26834(val);
    const int x = 26834;
    std::cout << 26834;
    for (int i = 0; i < 26834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26834(int& val) {
    const int x = 26834;
    std::cout << 26834;
    for (int i = 0; i < 26834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26835(int& val) {
    func_in_26835(val);
    const int x = 26835;
    std::cout << 26835;
    for (int i = 0; i < 26835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26835(int& val) {
    const int x = 26835;
    std::cout << 26835;
    for (int i = 0; i < 26835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26836(int& val) {
    func_in_26836(val);
    const int x = 26836;
    std::cout << 26836;
    for (int i = 0; i < 26836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26836(int& val) {
    const int x = 26836;
    std::cout << 26836;
    for (int i = 0; i < 26836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26837(int& val) {
    func_in_26837(val);
    const int x = 26837;
    std::cout << 26837;
    for (int i = 0; i < 26837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26837(int& val) {
    const int x = 26837;
    std::cout << 26837;
    for (int i = 0; i < 26837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26838(int& val) {
    func_in_26838(val);
    const int x = 26838;
    std::cout << 26838;
    for (int i = 0; i < 26838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26838(int& val) {
    const int x = 26838;
    std::cout << 26838;
    for (int i = 0; i < 26838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26839(int& val) {
    func_in_26839(val);
    const int x = 26839;
    std::cout << 26839;
    for (int i = 0; i < 26839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26839(int& val) {
    const int x = 26839;
    std::cout << 26839;
    for (int i = 0; i < 26839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26840(int& val) {
    func_in_26840(val);
    const int x = 26840;
    std::cout << 26840;
    for (int i = 0; i < 26840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26840(int& val) {
    const int x = 26840;
    std::cout << 26840;
    for (int i = 0; i < 26840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26841(int& val) {
    func_in_26841(val);
    const int x = 26841;
    std::cout << 26841;
    for (int i = 0; i < 26841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26841(int& val) {
    const int x = 26841;
    std::cout << 26841;
    for (int i = 0; i < 26841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26842(int& val) {
    func_in_26842(val);
    const int x = 26842;
    std::cout << 26842;
    for (int i = 0; i < 26842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26842(int& val) {
    const int x = 26842;
    std::cout << 26842;
    for (int i = 0; i < 26842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26843(int& val) {
    func_in_26843(val);
    const int x = 26843;
    std::cout << 26843;
    for (int i = 0; i < 26843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26843(int& val) {
    const int x = 26843;
    std::cout << 26843;
    for (int i = 0; i < 26843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26844(int& val) {
    func_in_26844(val);
    const int x = 26844;
    std::cout << 26844;
    for (int i = 0; i < 26844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26844(int& val) {
    const int x = 26844;
    std::cout << 26844;
    for (int i = 0; i < 26844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26845(int& val) {
    func_in_26845(val);
    const int x = 26845;
    std::cout << 26845;
    for (int i = 0; i < 26845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26845(int& val) {
    const int x = 26845;
    std::cout << 26845;
    for (int i = 0; i < 26845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26846(int& val) {
    func_in_26846(val);
    const int x = 26846;
    std::cout << 26846;
    for (int i = 0; i < 26846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26846(int& val) {
    const int x = 26846;
    std::cout << 26846;
    for (int i = 0; i < 26846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26847(int& val) {
    func_in_26847(val);
    const int x = 26847;
    std::cout << 26847;
    for (int i = 0; i < 26847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26847(int& val) {
    const int x = 26847;
    std::cout << 26847;
    for (int i = 0; i < 26847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26848(int& val) {
    func_in_26848(val);
    const int x = 26848;
    std::cout << 26848;
    for (int i = 0; i < 26848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26848(int& val) {
    const int x = 26848;
    std::cout << 26848;
    for (int i = 0; i < 26848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26849(int& val) {
    func_in_26849(val);
    const int x = 26849;
    std::cout << 26849;
    for (int i = 0; i < 26849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26849(int& val) {
    const int x = 26849;
    std::cout << 26849;
    for (int i = 0; i < 26849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26850(int& val) {
    func_in_26850(val);
    const int x = 26850;
    std::cout << 26850;
    for (int i = 0; i < 26850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26850(int& val) {
    const int x = 26850;
    std::cout << 26850;
    for (int i = 0; i < 26850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26851(int& val) {
    func_in_26851(val);
    const int x = 26851;
    std::cout << 26851;
    for (int i = 0; i < 26851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26851(int& val) {
    const int x = 26851;
    std::cout << 26851;
    for (int i = 0; i < 26851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26852(int& val) {
    func_in_26852(val);
    const int x = 26852;
    std::cout << 26852;
    for (int i = 0; i < 26852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26852(int& val) {
    const int x = 26852;
    std::cout << 26852;
    for (int i = 0; i < 26852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26853(int& val) {
    func_in_26853(val);
    const int x = 26853;
    std::cout << 26853;
    for (int i = 0; i < 26853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26853(int& val) {
    const int x = 26853;
    std::cout << 26853;
    for (int i = 0; i < 26853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26854(int& val) {
    func_in_26854(val);
    const int x = 26854;
    std::cout << 26854;
    for (int i = 0; i < 26854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26854(int& val) {
    const int x = 26854;
    std::cout << 26854;
    for (int i = 0; i < 26854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26855(int& val) {
    func_in_26855(val);
    const int x = 26855;
    std::cout << 26855;
    for (int i = 0; i < 26855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26855(int& val) {
    const int x = 26855;
    std::cout << 26855;
    for (int i = 0; i < 26855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26856(int& val) {
    func_in_26856(val);
    const int x = 26856;
    std::cout << 26856;
    for (int i = 0; i < 26856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26856(int& val) {
    const int x = 26856;
    std::cout << 26856;
    for (int i = 0; i < 26856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26857(int& val) {
    func_in_26857(val);
    const int x = 26857;
    std::cout << 26857;
    for (int i = 0; i < 26857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26857(int& val) {
    const int x = 26857;
    std::cout << 26857;
    for (int i = 0; i < 26857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26858(int& val) {
    func_in_26858(val);
    const int x = 26858;
    std::cout << 26858;
    for (int i = 0; i < 26858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26858(int& val) {
    const int x = 26858;
    std::cout << 26858;
    for (int i = 0; i < 26858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26859(int& val) {
    func_in_26859(val);
    const int x = 26859;
    std::cout << 26859;
    for (int i = 0; i < 26859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26859(int& val) {
    const int x = 26859;
    std::cout << 26859;
    for (int i = 0; i < 26859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26860(int& val) {
    func_in_26860(val);
    const int x = 26860;
    std::cout << 26860;
    for (int i = 0; i < 26860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26860(int& val) {
    const int x = 26860;
    std::cout << 26860;
    for (int i = 0; i < 26860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26861(int& val) {
    func_in_26861(val);
    const int x = 26861;
    std::cout << 26861;
    for (int i = 0; i < 26861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26861(int& val) {
    const int x = 26861;
    std::cout << 26861;
    for (int i = 0; i < 26861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26862(int& val) {
    func_in_26862(val);
    const int x = 26862;
    std::cout << 26862;
    for (int i = 0; i < 26862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26862(int& val) {
    const int x = 26862;
    std::cout << 26862;
    for (int i = 0; i < 26862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26863(int& val) {
    func_in_26863(val);
    const int x = 26863;
    std::cout << 26863;
    for (int i = 0; i < 26863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26863(int& val) {
    const int x = 26863;
    std::cout << 26863;
    for (int i = 0; i < 26863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26864(int& val) {
    func_in_26864(val);
    const int x = 26864;
    std::cout << 26864;
    for (int i = 0; i < 26864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26864(int& val) {
    const int x = 26864;
    std::cout << 26864;
    for (int i = 0; i < 26864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26865(int& val) {
    func_in_26865(val);
    const int x = 26865;
    std::cout << 26865;
    for (int i = 0; i < 26865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26865(int& val) {
    const int x = 26865;
    std::cout << 26865;
    for (int i = 0; i < 26865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26866(int& val) {
    func_in_26866(val);
    const int x = 26866;
    std::cout << 26866;
    for (int i = 0; i < 26866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26866(int& val) {
    const int x = 26866;
    std::cout << 26866;
    for (int i = 0; i < 26866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26867(int& val) {
    func_in_26867(val);
    const int x = 26867;
    std::cout << 26867;
    for (int i = 0; i < 26867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26867(int& val) {
    const int x = 26867;
    std::cout << 26867;
    for (int i = 0; i < 26867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26868(int& val) {
    func_in_26868(val);
    const int x = 26868;
    std::cout << 26868;
    for (int i = 0; i < 26868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26868(int& val) {
    const int x = 26868;
    std::cout << 26868;
    for (int i = 0; i < 26868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26869(int& val) {
    func_in_26869(val);
    const int x = 26869;
    std::cout << 26869;
    for (int i = 0; i < 26869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26869(int& val) {
    const int x = 26869;
    std::cout << 26869;
    for (int i = 0; i < 26869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26870(int& val) {
    func_in_26870(val);
    const int x = 26870;
    std::cout << 26870;
    for (int i = 0; i < 26870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26870(int& val) {
    const int x = 26870;
    std::cout << 26870;
    for (int i = 0; i < 26870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26871(int& val) {
    func_in_26871(val);
    const int x = 26871;
    std::cout << 26871;
    for (int i = 0; i < 26871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26871(int& val) {
    const int x = 26871;
    std::cout << 26871;
    for (int i = 0; i < 26871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26872(int& val) {
    func_in_26872(val);
    const int x = 26872;
    std::cout << 26872;
    for (int i = 0; i < 26872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26872(int& val) {
    const int x = 26872;
    std::cout << 26872;
    for (int i = 0; i < 26872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26873(int& val) {
    func_in_26873(val);
    const int x = 26873;
    std::cout << 26873;
    for (int i = 0; i < 26873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26873(int& val) {
    const int x = 26873;
    std::cout << 26873;
    for (int i = 0; i < 26873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26874(int& val) {
    func_in_26874(val);
    const int x = 26874;
    std::cout << 26874;
    for (int i = 0; i < 26874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26874(int& val) {
    const int x = 26874;
    std::cout << 26874;
    for (int i = 0; i < 26874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26875(int& val) {
    func_in_26875(val);
    const int x = 26875;
    std::cout << 26875;
    for (int i = 0; i < 26875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26875(int& val) {
    const int x = 26875;
    std::cout << 26875;
    for (int i = 0; i < 26875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26876(int& val) {
    func_in_26876(val);
    const int x = 26876;
    std::cout << 26876;
    for (int i = 0; i < 26876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26876(int& val) {
    const int x = 26876;
    std::cout << 26876;
    for (int i = 0; i < 26876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26877(int& val) {
    func_in_26877(val);
    const int x = 26877;
    std::cout << 26877;
    for (int i = 0; i < 26877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26877(int& val) {
    const int x = 26877;
    std::cout << 26877;
    for (int i = 0; i < 26877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26878(int& val) {
    func_in_26878(val);
    const int x = 26878;
    std::cout << 26878;
    for (int i = 0; i < 26878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26878(int& val) {
    const int x = 26878;
    std::cout << 26878;
    for (int i = 0; i < 26878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26879(int& val) {
    func_in_26879(val);
    const int x = 26879;
    std::cout << 26879;
    for (int i = 0; i < 26879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26879(int& val) {
    const int x = 26879;
    std::cout << 26879;
    for (int i = 0; i < 26879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26880(int& val) {
    func_in_26880(val);
    const int x = 26880;
    std::cout << 26880;
    for (int i = 0; i < 26880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26880(int& val) {
    const int x = 26880;
    std::cout << 26880;
    for (int i = 0; i < 26880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26881(int& val) {
    func_in_26881(val);
    const int x = 26881;
    std::cout << 26881;
    for (int i = 0; i < 26881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26881(int& val) {
    const int x = 26881;
    std::cout << 26881;
    for (int i = 0; i < 26881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26882(int& val) {
    func_in_26882(val);
    const int x = 26882;
    std::cout << 26882;
    for (int i = 0; i < 26882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26882(int& val) {
    const int x = 26882;
    std::cout << 26882;
    for (int i = 0; i < 26882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26883(int& val) {
    func_in_26883(val);
    const int x = 26883;
    std::cout << 26883;
    for (int i = 0; i < 26883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26883(int& val) {
    const int x = 26883;
    std::cout << 26883;
    for (int i = 0; i < 26883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26884(int& val) {
    func_in_26884(val);
    const int x = 26884;
    std::cout << 26884;
    for (int i = 0; i < 26884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26884(int& val) {
    const int x = 26884;
    std::cout << 26884;
    for (int i = 0; i < 26884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26885(int& val) {
    func_in_26885(val);
    const int x = 26885;
    std::cout << 26885;
    for (int i = 0; i < 26885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26885(int& val) {
    const int x = 26885;
    std::cout << 26885;
    for (int i = 0; i < 26885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26886(int& val) {
    func_in_26886(val);
    const int x = 26886;
    std::cout << 26886;
    for (int i = 0; i < 26886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26886(int& val) {
    const int x = 26886;
    std::cout << 26886;
    for (int i = 0; i < 26886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26887(int& val) {
    func_in_26887(val);
    const int x = 26887;
    std::cout << 26887;
    for (int i = 0; i < 26887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26887(int& val) {
    const int x = 26887;
    std::cout << 26887;
    for (int i = 0; i < 26887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26888(int& val) {
    func_in_26888(val);
    const int x = 26888;
    std::cout << 26888;
    for (int i = 0; i < 26888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26888(int& val) {
    const int x = 26888;
    std::cout << 26888;
    for (int i = 0; i < 26888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26889(int& val) {
    func_in_26889(val);
    const int x = 26889;
    std::cout << 26889;
    for (int i = 0; i < 26889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26889(int& val) {
    const int x = 26889;
    std::cout << 26889;
    for (int i = 0; i < 26889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26890(int& val) {
    func_in_26890(val);
    const int x = 26890;
    std::cout << 26890;
    for (int i = 0; i < 26890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26890(int& val) {
    const int x = 26890;
    std::cout << 26890;
    for (int i = 0; i < 26890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26891(int& val) {
    func_in_26891(val);
    const int x = 26891;
    std::cout << 26891;
    for (int i = 0; i < 26891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26891(int& val) {
    const int x = 26891;
    std::cout << 26891;
    for (int i = 0; i < 26891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26892(int& val) {
    func_in_26892(val);
    const int x = 26892;
    std::cout << 26892;
    for (int i = 0; i < 26892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26892(int& val) {
    const int x = 26892;
    std::cout << 26892;
    for (int i = 0; i < 26892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26893(int& val) {
    func_in_26893(val);
    const int x = 26893;
    std::cout << 26893;
    for (int i = 0; i < 26893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26893(int& val) {
    const int x = 26893;
    std::cout << 26893;
    for (int i = 0; i < 26893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26894(int& val) {
    func_in_26894(val);
    const int x = 26894;
    std::cout << 26894;
    for (int i = 0; i < 26894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26894(int& val) {
    const int x = 26894;
    std::cout << 26894;
    for (int i = 0; i < 26894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26895(int& val) {
    func_in_26895(val);
    const int x = 26895;
    std::cout << 26895;
    for (int i = 0; i < 26895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26895(int& val) {
    const int x = 26895;
    std::cout << 26895;
    for (int i = 0; i < 26895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26896(int& val) {
    func_in_26896(val);
    const int x = 26896;
    std::cout << 26896;
    for (int i = 0; i < 26896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26896(int& val) {
    const int x = 26896;
    std::cout << 26896;
    for (int i = 0; i < 26896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26897(int& val) {
    func_in_26897(val);
    const int x = 26897;
    std::cout << 26897;
    for (int i = 0; i < 26897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26897(int& val) {
    const int x = 26897;
    std::cout << 26897;
    for (int i = 0; i < 26897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26898(int& val) {
    func_in_26898(val);
    const int x = 26898;
    std::cout << 26898;
    for (int i = 0; i < 26898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26898(int& val) {
    const int x = 26898;
    std::cout << 26898;
    for (int i = 0; i < 26898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26899(int& val) {
    func_in_26899(val);
    const int x = 26899;
    std::cout << 26899;
    for (int i = 0; i < 26899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26899(int& val) {
    const int x = 26899;
    std::cout << 26899;
    for (int i = 0; i < 26899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26900(int& val) {
    func_in_26900(val);
    const int x = 26900;
    std::cout << 26900;
    for (int i = 0; i < 26900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26900(int& val) {
    const int x = 26900;
    std::cout << 26900;
    for (int i = 0; i < 26900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26901(int& val) {
    func_in_26901(val);
    const int x = 26901;
    std::cout << 26901;
    for (int i = 0; i < 26901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26901(int& val) {
    const int x = 26901;
    std::cout << 26901;
    for (int i = 0; i < 26901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26902(int& val) {
    func_in_26902(val);
    const int x = 26902;
    std::cout << 26902;
    for (int i = 0; i < 26902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26902(int& val) {
    const int x = 26902;
    std::cout << 26902;
    for (int i = 0; i < 26902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26903(int& val) {
    func_in_26903(val);
    const int x = 26903;
    std::cout << 26903;
    for (int i = 0; i < 26903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26903(int& val) {
    const int x = 26903;
    std::cout << 26903;
    for (int i = 0; i < 26903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26904(int& val) {
    func_in_26904(val);
    const int x = 26904;
    std::cout << 26904;
    for (int i = 0; i < 26904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26904(int& val) {
    const int x = 26904;
    std::cout << 26904;
    for (int i = 0; i < 26904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26905(int& val) {
    func_in_26905(val);
    const int x = 26905;
    std::cout << 26905;
    for (int i = 0; i < 26905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26905(int& val) {
    const int x = 26905;
    std::cout << 26905;
    for (int i = 0; i < 26905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26906(int& val) {
    func_in_26906(val);
    const int x = 26906;
    std::cout << 26906;
    for (int i = 0; i < 26906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26906(int& val) {
    const int x = 26906;
    std::cout << 26906;
    for (int i = 0; i < 26906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26907(int& val) {
    func_in_26907(val);
    const int x = 26907;
    std::cout << 26907;
    for (int i = 0; i < 26907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26907(int& val) {
    const int x = 26907;
    std::cout << 26907;
    for (int i = 0; i < 26907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26908(int& val) {
    func_in_26908(val);
    const int x = 26908;
    std::cout << 26908;
    for (int i = 0; i < 26908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26908(int& val) {
    const int x = 26908;
    std::cout << 26908;
    for (int i = 0; i < 26908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26909(int& val) {
    func_in_26909(val);
    const int x = 26909;
    std::cout << 26909;
    for (int i = 0; i < 26909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26909(int& val) {
    const int x = 26909;
    std::cout << 26909;
    for (int i = 0; i < 26909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26910(int& val) {
    func_in_26910(val);
    const int x = 26910;
    std::cout << 26910;
    for (int i = 0; i < 26910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26910(int& val) {
    const int x = 26910;
    std::cout << 26910;
    for (int i = 0; i < 26910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26911(int& val) {
    func_in_26911(val);
    const int x = 26911;
    std::cout << 26911;
    for (int i = 0; i < 26911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26911(int& val) {
    const int x = 26911;
    std::cout << 26911;
    for (int i = 0; i < 26911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26912(int& val) {
    func_in_26912(val);
    const int x = 26912;
    std::cout << 26912;
    for (int i = 0; i < 26912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26912(int& val) {
    const int x = 26912;
    std::cout << 26912;
    for (int i = 0; i < 26912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26913(int& val) {
    func_in_26913(val);
    const int x = 26913;
    std::cout << 26913;
    for (int i = 0; i < 26913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26913(int& val) {
    const int x = 26913;
    std::cout << 26913;
    for (int i = 0; i < 26913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26914(int& val) {
    func_in_26914(val);
    const int x = 26914;
    std::cout << 26914;
    for (int i = 0; i < 26914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26914(int& val) {
    const int x = 26914;
    std::cout << 26914;
    for (int i = 0; i < 26914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26915(int& val) {
    func_in_26915(val);
    const int x = 26915;
    std::cout << 26915;
    for (int i = 0; i < 26915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26915(int& val) {
    const int x = 26915;
    std::cout << 26915;
    for (int i = 0; i < 26915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26916(int& val) {
    func_in_26916(val);
    const int x = 26916;
    std::cout << 26916;
    for (int i = 0; i < 26916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26916(int& val) {
    const int x = 26916;
    std::cout << 26916;
    for (int i = 0; i < 26916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26917(int& val) {
    func_in_26917(val);
    const int x = 26917;
    std::cout << 26917;
    for (int i = 0; i < 26917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26917(int& val) {
    const int x = 26917;
    std::cout << 26917;
    for (int i = 0; i < 26917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26918(int& val) {
    func_in_26918(val);
    const int x = 26918;
    std::cout << 26918;
    for (int i = 0; i < 26918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26918(int& val) {
    const int x = 26918;
    std::cout << 26918;
    for (int i = 0; i < 26918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26919(int& val) {
    func_in_26919(val);
    const int x = 26919;
    std::cout << 26919;
    for (int i = 0; i < 26919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26919(int& val) {
    const int x = 26919;
    std::cout << 26919;
    for (int i = 0; i < 26919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26920(int& val) {
    func_in_26920(val);
    const int x = 26920;
    std::cout << 26920;
    for (int i = 0; i < 26920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26920(int& val) {
    const int x = 26920;
    std::cout << 26920;
    for (int i = 0; i < 26920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26921(int& val) {
    func_in_26921(val);
    const int x = 26921;
    std::cout << 26921;
    for (int i = 0; i < 26921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26921(int& val) {
    const int x = 26921;
    std::cout << 26921;
    for (int i = 0; i < 26921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26922(int& val) {
    func_in_26922(val);
    const int x = 26922;
    std::cout << 26922;
    for (int i = 0; i < 26922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26922(int& val) {
    const int x = 26922;
    std::cout << 26922;
    for (int i = 0; i < 26922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26923(int& val) {
    func_in_26923(val);
    const int x = 26923;
    std::cout << 26923;
    for (int i = 0; i < 26923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26923(int& val) {
    const int x = 26923;
    std::cout << 26923;
    for (int i = 0; i < 26923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26924(int& val) {
    func_in_26924(val);
    const int x = 26924;
    std::cout << 26924;
    for (int i = 0; i < 26924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26924(int& val) {
    const int x = 26924;
    std::cout << 26924;
    for (int i = 0; i < 26924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26925(int& val) {
    func_in_26925(val);
    const int x = 26925;
    std::cout << 26925;
    for (int i = 0; i < 26925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26925(int& val) {
    const int x = 26925;
    std::cout << 26925;
    for (int i = 0; i < 26925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26926(int& val) {
    func_in_26926(val);
    const int x = 26926;
    std::cout << 26926;
    for (int i = 0; i < 26926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26926(int& val) {
    const int x = 26926;
    std::cout << 26926;
    for (int i = 0; i < 26926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26927(int& val) {
    func_in_26927(val);
    const int x = 26927;
    std::cout << 26927;
    for (int i = 0; i < 26927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26927(int& val) {
    const int x = 26927;
    std::cout << 26927;
    for (int i = 0; i < 26927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26928(int& val) {
    func_in_26928(val);
    const int x = 26928;
    std::cout << 26928;
    for (int i = 0; i < 26928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26928(int& val) {
    const int x = 26928;
    std::cout << 26928;
    for (int i = 0; i < 26928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26929(int& val) {
    func_in_26929(val);
    const int x = 26929;
    std::cout << 26929;
    for (int i = 0; i < 26929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26929(int& val) {
    const int x = 26929;
    std::cout << 26929;
    for (int i = 0; i < 26929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26930(int& val) {
    func_in_26930(val);
    const int x = 26930;
    std::cout << 26930;
    for (int i = 0; i < 26930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26930(int& val) {
    const int x = 26930;
    std::cout << 26930;
    for (int i = 0; i < 26930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26931(int& val) {
    func_in_26931(val);
    const int x = 26931;
    std::cout << 26931;
    for (int i = 0; i < 26931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26931(int& val) {
    const int x = 26931;
    std::cout << 26931;
    for (int i = 0; i < 26931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26932(int& val) {
    func_in_26932(val);
    const int x = 26932;
    std::cout << 26932;
    for (int i = 0; i < 26932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26932(int& val) {
    const int x = 26932;
    std::cout << 26932;
    for (int i = 0; i < 26932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26933(int& val) {
    func_in_26933(val);
    const int x = 26933;
    std::cout << 26933;
    for (int i = 0; i < 26933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26933(int& val) {
    const int x = 26933;
    std::cout << 26933;
    for (int i = 0; i < 26933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26934(int& val) {
    func_in_26934(val);
    const int x = 26934;
    std::cout << 26934;
    for (int i = 0; i < 26934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26934(int& val) {
    const int x = 26934;
    std::cout << 26934;
    for (int i = 0; i < 26934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26935(int& val) {
    func_in_26935(val);
    const int x = 26935;
    std::cout << 26935;
    for (int i = 0; i < 26935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26935(int& val) {
    const int x = 26935;
    std::cout << 26935;
    for (int i = 0; i < 26935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26936(int& val) {
    func_in_26936(val);
    const int x = 26936;
    std::cout << 26936;
    for (int i = 0; i < 26936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26936(int& val) {
    const int x = 26936;
    std::cout << 26936;
    for (int i = 0; i < 26936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26937(int& val) {
    func_in_26937(val);
    const int x = 26937;
    std::cout << 26937;
    for (int i = 0; i < 26937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26937(int& val) {
    const int x = 26937;
    std::cout << 26937;
    for (int i = 0; i < 26937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26938(int& val) {
    func_in_26938(val);
    const int x = 26938;
    std::cout << 26938;
    for (int i = 0; i < 26938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26938(int& val) {
    const int x = 26938;
    std::cout << 26938;
    for (int i = 0; i < 26938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26939(int& val) {
    func_in_26939(val);
    const int x = 26939;
    std::cout << 26939;
    for (int i = 0; i < 26939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26939(int& val) {
    const int x = 26939;
    std::cout << 26939;
    for (int i = 0; i < 26939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26940(int& val) {
    func_in_26940(val);
    const int x = 26940;
    std::cout << 26940;
    for (int i = 0; i < 26940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26940(int& val) {
    const int x = 26940;
    std::cout << 26940;
    for (int i = 0; i < 26940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26941(int& val) {
    func_in_26941(val);
    const int x = 26941;
    std::cout << 26941;
    for (int i = 0; i < 26941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26941(int& val) {
    const int x = 26941;
    std::cout << 26941;
    for (int i = 0; i < 26941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26942(int& val) {
    func_in_26942(val);
    const int x = 26942;
    std::cout << 26942;
    for (int i = 0; i < 26942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26942(int& val) {
    const int x = 26942;
    std::cout << 26942;
    for (int i = 0; i < 26942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26943(int& val) {
    func_in_26943(val);
    const int x = 26943;
    std::cout << 26943;
    for (int i = 0; i < 26943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26943(int& val) {
    const int x = 26943;
    std::cout << 26943;
    for (int i = 0; i < 26943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26944(int& val) {
    func_in_26944(val);
    const int x = 26944;
    std::cout << 26944;
    for (int i = 0; i < 26944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26944(int& val) {
    const int x = 26944;
    std::cout << 26944;
    for (int i = 0; i < 26944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26945(int& val) {
    func_in_26945(val);
    const int x = 26945;
    std::cout << 26945;
    for (int i = 0; i < 26945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26945(int& val) {
    const int x = 26945;
    std::cout << 26945;
    for (int i = 0; i < 26945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26946(int& val) {
    func_in_26946(val);
    const int x = 26946;
    std::cout << 26946;
    for (int i = 0; i < 26946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26946(int& val) {
    const int x = 26946;
    std::cout << 26946;
    for (int i = 0; i < 26946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26947(int& val) {
    func_in_26947(val);
    const int x = 26947;
    std::cout << 26947;
    for (int i = 0; i < 26947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26947(int& val) {
    const int x = 26947;
    std::cout << 26947;
    for (int i = 0; i < 26947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26948(int& val) {
    func_in_26948(val);
    const int x = 26948;
    std::cout << 26948;
    for (int i = 0; i < 26948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26948(int& val) {
    const int x = 26948;
    std::cout << 26948;
    for (int i = 0; i < 26948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26949(int& val) {
    func_in_26949(val);
    const int x = 26949;
    std::cout << 26949;
    for (int i = 0; i < 26949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26949(int& val) {
    const int x = 26949;
    std::cout << 26949;
    for (int i = 0; i < 26949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26950(int& val) {
    func_in_26950(val);
    const int x = 26950;
    std::cout << 26950;
    for (int i = 0; i < 26950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26950(int& val) {
    const int x = 26950;
    std::cout << 26950;
    for (int i = 0; i < 26950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26951(int& val) {
    func_in_26951(val);
    const int x = 26951;
    std::cout << 26951;
    for (int i = 0; i < 26951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26951(int& val) {
    const int x = 26951;
    std::cout << 26951;
    for (int i = 0; i < 26951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26952(int& val) {
    func_in_26952(val);
    const int x = 26952;
    std::cout << 26952;
    for (int i = 0; i < 26952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26952(int& val) {
    const int x = 26952;
    std::cout << 26952;
    for (int i = 0; i < 26952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26953(int& val) {
    func_in_26953(val);
    const int x = 26953;
    std::cout << 26953;
    for (int i = 0; i < 26953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26953(int& val) {
    const int x = 26953;
    std::cout << 26953;
    for (int i = 0; i < 26953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26954(int& val) {
    func_in_26954(val);
    const int x = 26954;
    std::cout << 26954;
    for (int i = 0; i < 26954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26954(int& val) {
    const int x = 26954;
    std::cout << 26954;
    for (int i = 0; i < 26954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26955(int& val) {
    func_in_26955(val);
    const int x = 26955;
    std::cout << 26955;
    for (int i = 0; i < 26955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26955(int& val) {
    const int x = 26955;
    std::cout << 26955;
    for (int i = 0; i < 26955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26956(int& val) {
    func_in_26956(val);
    const int x = 26956;
    std::cout << 26956;
    for (int i = 0; i < 26956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26956(int& val) {
    const int x = 26956;
    std::cout << 26956;
    for (int i = 0; i < 26956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26957(int& val) {
    func_in_26957(val);
    const int x = 26957;
    std::cout << 26957;
    for (int i = 0; i < 26957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26957(int& val) {
    const int x = 26957;
    std::cout << 26957;
    for (int i = 0; i < 26957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26958(int& val) {
    func_in_26958(val);
    const int x = 26958;
    std::cout << 26958;
    for (int i = 0; i < 26958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26958(int& val) {
    const int x = 26958;
    std::cout << 26958;
    for (int i = 0; i < 26958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26959(int& val) {
    func_in_26959(val);
    const int x = 26959;
    std::cout << 26959;
    for (int i = 0; i < 26959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26959(int& val) {
    const int x = 26959;
    std::cout << 26959;
    for (int i = 0; i < 26959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26960(int& val) {
    func_in_26960(val);
    const int x = 26960;
    std::cout << 26960;
    for (int i = 0; i < 26960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26960(int& val) {
    const int x = 26960;
    std::cout << 26960;
    for (int i = 0; i < 26960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26961(int& val) {
    func_in_26961(val);
    const int x = 26961;
    std::cout << 26961;
    for (int i = 0; i < 26961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26961(int& val) {
    const int x = 26961;
    std::cout << 26961;
    for (int i = 0; i < 26961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26962(int& val) {
    func_in_26962(val);
    const int x = 26962;
    std::cout << 26962;
    for (int i = 0; i < 26962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26962(int& val) {
    const int x = 26962;
    std::cout << 26962;
    for (int i = 0; i < 26962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26963(int& val) {
    func_in_26963(val);
    const int x = 26963;
    std::cout << 26963;
    for (int i = 0; i < 26963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26963(int& val) {
    const int x = 26963;
    std::cout << 26963;
    for (int i = 0; i < 26963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26964(int& val) {
    func_in_26964(val);
    const int x = 26964;
    std::cout << 26964;
    for (int i = 0; i < 26964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26964(int& val) {
    const int x = 26964;
    std::cout << 26964;
    for (int i = 0; i < 26964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26965(int& val) {
    func_in_26965(val);
    const int x = 26965;
    std::cout << 26965;
    for (int i = 0; i < 26965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26965(int& val) {
    const int x = 26965;
    std::cout << 26965;
    for (int i = 0; i < 26965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26966(int& val) {
    func_in_26966(val);
    const int x = 26966;
    std::cout << 26966;
    for (int i = 0; i < 26966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26966(int& val) {
    const int x = 26966;
    std::cout << 26966;
    for (int i = 0; i < 26966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26967(int& val) {
    func_in_26967(val);
    const int x = 26967;
    std::cout << 26967;
    for (int i = 0; i < 26967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26967(int& val) {
    const int x = 26967;
    std::cout << 26967;
    for (int i = 0; i < 26967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26968(int& val) {
    func_in_26968(val);
    const int x = 26968;
    std::cout << 26968;
    for (int i = 0; i < 26968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26968(int& val) {
    const int x = 26968;
    std::cout << 26968;
    for (int i = 0; i < 26968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26969(int& val) {
    func_in_26969(val);
    const int x = 26969;
    std::cout << 26969;
    for (int i = 0; i < 26969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26969(int& val) {
    const int x = 26969;
    std::cout << 26969;
    for (int i = 0; i < 26969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26970(int& val) {
    func_in_26970(val);
    const int x = 26970;
    std::cout << 26970;
    for (int i = 0; i < 26970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26970(int& val) {
    const int x = 26970;
    std::cout << 26970;
    for (int i = 0; i < 26970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26971(int& val) {
    func_in_26971(val);
    const int x = 26971;
    std::cout << 26971;
    for (int i = 0; i < 26971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26971(int& val) {
    const int x = 26971;
    std::cout << 26971;
    for (int i = 0; i < 26971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26972(int& val) {
    func_in_26972(val);
    const int x = 26972;
    std::cout << 26972;
    for (int i = 0; i < 26972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26972(int& val) {
    const int x = 26972;
    std::cout << 26972;
    for (int i = 0; i < 26972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26973(int& val) {
    func_in_26973(val);
    const int x = 26973;
    std::cout << 26973;
    for (int i = 0; i < 26973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26973(int& val) {
    const int x = 26973;
    std::cout << 26973;
    for (int i = 0; i < 26973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26974(int& val) {
    func_in_26974(val);
    const int x = 26974;
    std::cout << 26974;
    for (int i = 0; i < 26974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26974(int& val) {
    const int x = 26974;
    std::cout << 26974;
    for (int i = 0; i < 26974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26975(int& val) {
    func_in_26975(val);
    const int x = 26975;
    std::cout << 26975;
    for (int i = 0; i < 26975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26975(int& val) {
    const int x = 26975;
    std::cout << 26975;
    for (int i = 0; i < 26975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26976(int& val) {
    func_in_26976(val);
    const int x = 26976;
    std::cout << 26976;
    for (int i = 0; i < 26976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26976(int& val) {
    const int x = 26976;
    std::cout << 26976;
    for (int i = 0; i < 26976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26977(int& val) {
    func_in_26977(val);
    const int x = 26977;
    std::cout << 26977;
    for (int i = 0; i < 26977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26977(int& val) {
    const int x = 26977;
    std::cout << 26977;
    for (int i = 0; i < 26977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26978(int& val) {
    func_in_26978(val);
    const int x = 26978;
    std::cout << 26978;
    for (int i = 0; i < 26978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26978(int& val) {
    const int x = 26978;
    std::cout << 26978;
    for (int i = 0; i < 26978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26979(int& val) {
    func_in_26979(val);
    const int x = 26979;
    std::cout << 26979;
    for (int i = 0; i < 26979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26979(int& val) {
    const int x = 26979;
    std::cout << 26979;
    for (int i = 0; i < 26979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26980(int& val) {
    func_in_26980(val);
    const int x = 26980;
    std::cout << 26980;
    for (int i = 0; i < 26980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26980(int& val) {
    const int x = 26980;
    std::cout << 26980;
    for (int i = 0; i < 26980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26981(int& val) {
    func_in_26981(val);
    const int x = 26981;
    std::cout << 26981;
    for (int i = 0; i < 26981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26981(int& val) {
    const int x = 26981;
    std::cout << 26981;
    for (int i = 0; i < 26981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26982(int& val) {
    func_in_26982(val);
    const int x = 26982;
    std::cout << 26982;
    for (int i = 0; i < 26982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26982(int& val) {
    const int x = 26982;
    std::cout << 26982;
    for (int i = 0; i < 26982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26983(int& val) {
    func_in_26983(val);
    const int x = 26983;
    std::cout << 26983;
    for (int i = 0; i < 26983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26983(int& val) {
    const int x = 26983;
    std::cout << 26983;
    for (int i = 0; i < 26983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26984(int& val) {
    func_in_26984(val);
    const int x = 26984;
    std::cout << 26984;
    for (int i = 0; i < 26984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26984(int& val) {
    const int x = 26984;
    std::cout << 26984;
    for (int i = 0; i < 26984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26985(int& val) {
    func_in_26985(val);
    const int x = 26985;
    std::cout << 26985;
    for (int i = 0; i < 26985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26985(int& val) {
    const int x = 26985;
    std::cout << 26985;
    for (int i = 0; i < 26985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26986(int& val) {
    func_in_26986(val);
    const int x = 26986;
    std::cout << 26986;
    for (int i = 0; i < 26986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26986(int& val) {
    const int x = 26986;
    std::cout << 26986;
    for (int i = 0; i < 26986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26987(int& val) {
    func_in_26987(val);
    const int x = 26987;
    std::cout << 26987;
    for (int i = 0; i < 26987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26987(int& val) {
    const int x = 26987;
    std::cout << 26987;
    for (int i = 0; i < 26987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26988(int& val) {
    func_in_26988(val);
    const int x = 26988;
    std::cout << 26988;
    for (int i = 0; i < 26988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26988(int& val) {
    const int x = 26988;
    std::cout << 26988;
    for (int i = 0; i < 26988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26989(int& val) {
    func_in_26989(val);
    const int x = 26989;
    std::cout << 26989;
    for (int i = 0; i < 26989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26989(int& val) {
    const int x = 26989;
    std::cout << 26989;
    for (int i = 0; i < 26989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26990(int& val) {
    func_in_26990(val);
    const int x = 26990;
    std::cout << 26990;
    for (int i = 0; i < 26990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26990(int& val) {
    const int x = 26990;
    std::cout << 26990;
    for (int i = 0; i < 26990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26991(int& val) {
    func_in_26991(val);
    const int x = 26991;
    std::cout << 26991;
    for (int i = 0; i < 26991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26991(int& val) {
    const int x = 26991;
    std::cout << 26991;
    for (int i = 0; i < 26991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26992(int& val) {
    func_in_26992(val);
    const int x = 26992;
    std::cout << 26992;
    for (int i = 0; i < 26992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26992(int& val) {
    const int x = 26992;
    std::cout << 26992;
    for (int i = 0; i < 26992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26993(int& val) {
    func_in_26993(val);
    const int x = 26993;
    std::cout << 26993;
    for (int i = 0; i < 26993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26993(int& val) {
    const int x = 26993;
    std::cout << 26993;
    for (int i = 0; i < 26993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26994(int& val) {
    func_in_26994(val);
    const int x = 26994;
    std::cout << 26994;
    for (int i = 0; i < 26994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26994(int& val) {
    const int x = 26994;
    std::cout << 26994;
    for (int i = 0; i < 26994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26995(int& val) {
    func_in_26995(val);
    const int x = 26995;
    std::cout << 26995;
    for (int i = 0; i < 26995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26995(int& val) {
    const int x = 26995;
    std::cout << 26995;
    for (int i = 0; i < 26995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26996(int& val) {
    func_in_26996(val);
    const int x = 26996;
    std::cout << 26996;
    for (int i = 0; i < 26996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26996(int& val) {
    const int x = 26996;
    std::cout << 26996;
    for (int i = 0; i < 26996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26997(int& val) {
    func_in_26997(val);
    const int x = 26997;
    std::cout << 26997;
    for (int i = 0; i < 26997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26997(int& val) {
    const int x = 26997;
    std::cout << 26997;
    for (int i = 0; i < 26997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26998(int& val) {
    func_in_26998(val);
    const int x = 26998;
    std::cout << 26998;
    for (int i = 0; i < 26998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26998(int& val) {
    const int x = 26998;
    std::cout << 26998;
    for (int i = 0; i < 26998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26999(int& val) {
    func_in_26999(val);
    const int x = 26999;
    std::cout << 26999;
    for (int i = 0; i < 26999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26999(int& val) {
    const int x = 26999;
    std::cout << 26999;
    for (int i = 0; i < 26999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27000(int& val) {
    func_in_27000(val);
    const int x = 27000;
    std::cout << 27000;
    for (int i = 0; i < 27000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27000(int& val) {
    const int x = 27000;
    std::cout << 27000;
    for (int i = 0; i < 27000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27001(int& val) {
    func_in_27001(val);
    const int x = 27001;
    std::cout << 27001;
    for (int i = 0; i < 27001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27001(int& val) {
    const int x = 27001;
    std::cout << 27001;
    for (int i = 0; i < 27001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27002(int& val) {
    func_in_27002(val);
    const int x = 27002;
    std::cout << 27002;
    for (int i = 0; i < 27002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27002(int& val) {
    const int x = 27002;
    std::cout << 27002;
    for (int i = 0; i < 27002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27003(int& val) {
    func_in_27003(val);
    const int x = 27003;
    std::cout << 27003;
    for (int i = 0; i < 27003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27003(int& val) {
    const int x = 27003;
    std::cout << 27003;
    for (int i = 0; i < 27003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27004(int& val) {
    func_in_27004(val);
    const int x = 27004;
    std::cout << 27004;
    for (int i = 0; i < 27004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27004(int& val) {
    const int x = 27004;
    std::cout << 27004;
    for (int i = 0; i < 27004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27005(int& val) {
    func_in_27005(val);
    const int x = 27005;
    std::cout << 27005;
    for (int i = 0; i < 27005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27005(int& val) {
    const int x = 27005;
    std::cout << 27005;
    for (int i = 0; i < 27005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27006(int& val) {
    func_in_27006(val);
    const int x = 27006;
    std::cout << 27006;
    for (int i = 0; i < 27006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27006(int& val) {
    const int x = 27006;
    std::cout << 27006;
    for (int i = 0; i < 27006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27007(int& val) {
    func_in_27007(val);
    const int x = 27007;
    std::cout << 27007;
    for (int i = 0; i < 27007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27007(int& val) {
    const int x = 27007;
    std::cout << 27007;
    for (int i = 0; i < 27007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27008(int& val) {
    func_in_27008(val);
    const int x = 27008;
    std::cout << 27008;
    for (int i = 0; i < 27008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27008(int& val) {
    const int x = 27008;
    std::cout << 27008;
    for (int i = 0; i < 27008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27009(int& val) {
    func_in_27009(val);
    const int x = 27009;
    std::cout << 27009;
    for (int i = 0; i < 27009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27009(int& val) {
    const int x = 27009;
    std::cout << 27009;
    for (int i = 0; i < 27009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27010(int& val) {
    func_in_27010(val);
    const int x = 27010;
    std::cout << 27010;
    for (int i = 0; i < 27010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27010(int& val) {
    const int x = 27010;
    std::cout << 27010;
    for (int i = 0; i < 27010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27011(int& val) {
    func_in_27011(val);
    const int x = 27011;
    std::cout << 27011;
    for (int i = 0; i < 27011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27011(int& val) {
    const int x = 27011;
    std::cout << 27011;
    for (int i = 0; i < 27011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27012(int& val) {
    func_in_27012(val);
    const int x = 27012;
    std::cout << 27012;
    for (int i = 0; i < 27012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27012(int& val) {
    const int x = 27012;
    std::cout << 27012;
    for (int i = 0; i < 27012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27013(int& val) {
    func_in_27013(val);
    const int x = 27013;
    std::cout << 27013;
    for (int i = 0; i < 27013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27013(int& val) {
    const int x = 27013;
    std::cout << 27013;
    for (int i = 0; i < 27013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27014(int& val) {
    func_in_27014(val);
    const int x = 27014;
    std::cout << 27014;
    for (int i = 0; i < 27014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27014(int& val) {
    const int x = 27014;
    std::cout << 27014;
    for (int i = 0; i < 27014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27015(int& val) {
    func_in_27015(val);
    const int x = 27015;
    std::cout << 27015;
    for (int i = 0; i < 27015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27015(int& val) {
    const int x = 27015;
    std::cout << 27015;
    for (int i = 0; i < 27015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27016(int& val) {
    func_in_27016(val);
    const int x = 27016;
    std::cout << 27016;
    for (int i = 0; i < 27016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27016(int& val) {
    const int x = 27016;
    std::cout << 27016;
    for (int i = 0; i < 27016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27017(int& val) {
    func_in_27017(val);
    const int x = 27017;
    std::cout << 27017;
    for (int i = 0; i < 27017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27017(int& val) {
    const int x = 27017;
    std::cout << 27017;
    for (int i = 0; i < 27017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27018(int& val) {
    func_in_27018(val);
    const int x = 27018;
    std::cout << 27018;
    for (int i = 0; i < 27018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27018(int& val) {
    const int x = 27018;
    std::cout << 27018;
    for (int i = 0; i < 27018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27019(int& val) {
    func_in_27019(val);
    const int x = 27019;
    std::cout << 27019;
    for (int i = 0; i < 27019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27019(int& val) {
    const int x = 27019;
    std::cout << 27019;
    for (int i = 0; i < 27019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27020(int& val) {
    func_in_27020(val);
    const int x = 27020;
    std::cout << 27020;
    for (int i = 0; i < 27020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27020(int& val) {
    const int x = 27020;
    std::cout << 27020;
    for (int i = 0; i < 27020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27021(int& val) {
    func_in_27021(val);
    const int x = 27021;
    std::cout << 27021;
    for (int i = 0; i < 27021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27021(int& val) {
    const int x = 27021;
    std::cout << 27021;
    for (int i = 0; i < 27021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27022(int& val) {
    func_in_27022(val);
    const int x = 27022;
    std::cout << 27022;
    for (int i = 0; i < 27022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27022(int& val) {
    const int x = 27022;
    std::cout << 27022;
    for (int i = 0; i < 27022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27023(int& val) {
    func_in_27023(val);
    const int x = 27023;
    std::cout << 27023;
    for (int i = 0; i < 27023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27023(int& val) {
    const int x = 27023;
    std::cout << 27023;
    for (int i = 0; i < 27023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27024(int& val) {
    func_in_27024(val);
    const int x = 27024;
    std::cout << 27024;
    for (int i = 0; i < 27024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27024(int& val) {
    const int x = 27024;
    std::cout << 27024;
    for (int i = 0; i < 27024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27025(int& val) {
    func_in_27025(val);
    const int x = 27025;
    std::cout << 27025;
    for (int i = 0; i < 27025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27025(int& val) {
    const int x = 27025;
    std::cout << 27025;
    for (int i = 0; i < 27025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27026(int& val) {
    func_in_27026(val);
    const int x = 27026;
    std::cout << 27026;
    for (int i = 0; i < 27026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27026(int& val) {
    const int x = 27026;
    std::cout << 27026;
    for (int i = 0; i < 27026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27027(int& val) {
    func_in_27027(val);
    const int x = 27027;
    std::cout << 27027;
    for (int i = 0; i < 27027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27027(int& val) {
    const int x = 27027;
    std::cout << 27027;
    for (int i = 0; i < 27027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27028(int& val) {
    func_in_27028(val);
    const int x = 27028;
    std::cout << 27028;
    for (int i = 0; i < 27028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27028(int& val) {
    const int x = 27028;
    std::cout << 27028;
    for (int i = 0; i < 27028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27029(int& val) {
    func_in_27029(val);
    const int x = 27029;
    std::cout << 27029;
    for (int i = 0; i < 27029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27029(int& val) {
    const int x = 27029;
    std::cout << 27029;
    for (int i = 0; i < 27029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27030(int& val) {
    func_in_27030(val);
    const int x = 27030;
    std::cout << 27030;
    for (int i = 0; i < 27030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27030(int& val) {
    const int x = 27030;
    std::cout << 27030;
    for (int i = 0; i < 27030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27031(int& val) {
    func_in_27031(val);
    const int x = 27031;
    std::cout << 27031;
    for (int i = 0; i < 27031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27031(int& val) {
    const int x = 27031;
    std::cout << 27031;
    for (int i = 0; i < 27031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27032(int& val) {
    func_in_27032(val);
    const int x = 27032;
    std::cout << 27032;
    for (int i = 0; i < 27032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27032(int& val) {
    const int x = 27032;
    std::cout << 27032;
    for (int i = 0; i < 27032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27033(int& val) {
    func_in_27033(val);
    const int x = 27033;
    std::cout << 27033;
    for (int i = 0; i < 27033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27033(int& val) {
    const int x = 27033;
    std::cout << 27033;
    for (int i = 0; i < 27033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27034(int& val) {
    func_in_27034(val);
    const int x = 27034;
    std::cout << 27034;
    for (int i = 0; i < 27034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27034(int& val) {
    const int x = 27034;
    std::cout << 27034;
    for (int i = 0; i < 27034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27035(int& val) {
    func_in_27035(val);
    const int x = 27035;
    std::cout << 27035;
    for (int i = 0; i < 27035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27035(int& val) {
    const int x = 27035;
    std::cout << 27035;
    for (int i = 0; i < 27035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27036(int& val) {
    func_in_27036(val);
    const int x = 27036;
    std::cout << 27036;
    for (int i = 0; i < 27036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27036(int& val) {
    const int x = 27036;
    std::cout << 27036;
    for (int i = 0; i < 27036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27037(int& val) {
    func_in_27037(val);
    const int x = 27037;
    std::cout << 27037;
    for (int i = 0; i < 27037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27037(int& val) {
    const int x = 27037;
    std::cout << 27037;
    for (int i = 0; i < 27037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27038(int& val) {
    func_in_27038(val);
    const int x = 27038;
    std::cout << 27038;
    for (int i = 0; i < 27038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27038(int& val) {
    const int x = 27038;
    std::cout << 27038;
    for (int i = 0; i < 27038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27039(int& val) {
    func_in_27039(val);
    const int x = 27039;
    std::cout << 27039;
    for (int i = 0; i < 27039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27039(int& val) {
    const int x = 27039;
    std::cout << 27039;
    for (int i = 0; i < 27039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27040(int& val) {
    func_in_27040(val);
    const int x = 27040;
    std::cout << 27040;
    for (int i = 0; i < 27040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27040(int& val) {
    const int x = 27040;
    std::cout << 27040;
    for (int i = 0; i < 27040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27041(int& val) {
    func_in_27041(val);
    const int x = 27041;
    std::cout << 27041;
    for (int i = 0; i < 27041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27041(int& val) {
    const int x = 27041;
    std::cout << 27041;
    for (int i = 0; i < 27041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27042(int& val) {
    func_in_27042(val);
    const int x = 27042;
    std::cout << 27042;
    for (int i = 0; i < 27042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27042(int& val) {
    const int x = 27042;
    std::cout << 27042;
    for (int i = 0; i < 27042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27043(int& val) {
    func_in_27043(val);
    const int x = 27043;
    std::cout << 27043;
    for (int i = 0; i < 27043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27043(int& val) {
    const int x = 27043;
    std::cout << 27043;
    for (int i = 0; i < 27043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27044(int& val) {
    func_in_27044(val);
    const int x = 27044;
    std::cout << 27044;
    for (int i = 0; i < 27044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27044(int& val) {
    const int x = 27044;
    std::cout << 27044;
    for (int i = 0; i < 27044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27045(int& val) {
    func_in_27045(val);
    const int x = 27045;
    std::cout << 27045;
    for (int i = 0; i < 27045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27045(int& val) {
    const int x = 27045;
    std::cout << 27045;
    for (int i = 0; i < 27045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27046(int& val) {
    func_in_27046(val);
    const int x = 27046;
    std::cout << 27046;
    for (int i = 0; i < 27046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27046(int& val) {
    const int x = 27046;
    std::cout << 27046;
    for (int i = 0; i < 27046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27047(int& val) {
    func_in_27047(val);
    const int x = 27047;
    std::cout << 27047;
    for (int i = 0; i < 27047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27047(int& val) {
    const int x = 27047;
    std::cout << 27047;
    for (int i = 0; i < 27047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27048(int& val) {
    func_in_27048(val);
    const int x = 27048;
    std::cout << 27048;
    for (int i = 0; i < 27048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27048(int& val) {
    const int x = 27048;
    std::cout << 27048;
    for (int i = 0; i < 27048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27049(int& val) {
    func_in_27049(val);
    const int x = 27049;
    std::cout << 27049;
    for (int i = 0; i < 27049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27049(int& val) {
    const int x = 27049;
    std::cout << 27049;
    for (int i = 0; i < 27049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27050(int& val) {
    func_in_27050(val);
    const int x = 27050;
    std::cout << 27050;
    for (int i = 0; i < 27050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27050(int& val) {
    const int x = 27050;
    std::cout << 27050;
    for (int i = 0; i < 27050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27051(int& val) {
    func_in_27051(val);
    const int x = 27051;
    std::cout << 27051;
    for (int i = 0; i < 27051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27051(int& val) {
    const int x = 27051;
    std::cout << 27051;
    for (int i = 0; i < 27051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27052(int& val) {
    func_in_27052(val);
    const int x = 27052;
    std::cout << 27052;
    for (int i = 0; i < 27052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27052(int& val) {
    const int x = 27052;
    std::cout << 27052;
    for (int i = 0; i < 27052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27053(int& val) {
    func_in_27053(val);
    const int x = 27053;
    std::cout << 27053;
    for (int i = 0; i < 27053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27053(int& val) {
    const int x = 27053;
    std::cout << 27053;
    for (int i = 0; i < 27053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27054(int& val) {
    func_in_27054(val);
    const int x = 27054;
    std::cout << 27054;
    for (int i = 0; i < 27054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27054(int& val) {
    const int x = 27054;
    std::cout << 27054;
    for (int i = 0; i < 27054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27055(int& val) {
    func_in_27055(val);
    const int x = 27055;
    std::cout << 27055;
    for (int i = 0; i < 27055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27055(int& val) {
    const int x = 27055;
    std::cout << 27055;
    for (int i = 0; i < 27055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27056(int& val) {
    func_in_27056(val);
    const int x = 27056;
    std::cout << 27056;
    for (int i = 0; i < 27056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27056(int& val) {
    const int x = 27056;
    std::cout << 27056;
    for (int i = 0; i < 27056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27057(int& val) {
    func_in_27057(val);
    const int x = 27057;
    std::cout << 27057;
    for (int i = 0; i < 27057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27057(int& val) {
    const int x = 27057;
    std::cout << 27057;
    for (int i = 0; i < 27057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27058(int& val) {
    func_in_27058(val);
    const int x = 27058;
    std::cout << 27058;
    for (int i = 0; i < 27058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27058(int& val) {
    const int x = 27058;
    std::cout << 27058;
    for (int i = 0; i < 27058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27059(int& val) {
    func_in_27059(val);
    const int x = 27059;
    std::cout << 27059;
    for (int i = 0; i < 27059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27059(int& val) {
    const int x = 27059;
    std::cout << 27059;
    for (int i = 0; i < 27059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27060(int& val) {
    func_in_27060(val);
    const int x = 27060;
    std::cout << 27060;
    for (int i = 0; i < 27060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27060(int& val) {
    const int x = 27060;
    std::cout << 27060;
    for (int i = 0; i < 27060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27061(int& val) {
    func_in_27061(val);
    const int x = 27061;
    std::cout << 27061;
    for (int i = 0; i < 27061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27061(int& val) {
    const int x = 27061;
    std::cout << 27061;
    for (int i = 0; i < 27061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27062(int& val) {
    func_in_27062(val);
    const int x = 27062;
    std::cout << 27062;
    for (int i = 0; i < 27062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27062(int& val) {
    const int x = 27062;
    std::cout << 27062;
    for (int i = 0; i < 27062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27063(int& val) {
    func_in_27063(val);
    const int x = 27063;
    std::cout << 27063;
    for (int i = 0; i < 27063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27063(int& val) {
    const int x = 27063;
    std::cout << 27063;
    for (int i = 0; i < 27063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27064(int& val) {
    func_in_27064(val);
    const int x = 27064;
    std::cout << 27064;
    for (int i = 0; i < 27064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27064(int& val) {
    const int x = 27064;
    std::cout << 27064;
    for (int i = 0; i < 27064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27065(int& val) {
    func_in_27065(val);
    const int x = 27065;
    std::cout << 27065;
    for (int i = 0; i < 27065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27065(int& val) {
    const int x = 27065;
    std::cout << 27065;
    for (int i = 0; i < 27065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27066(int& val) {
    func_in_27066(val);
    const int x = 27066;
    std::cout << 27066;
    for (int i = 0; i < 27066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27066(int& val) {
    const int x = 27066;
    std::cout << 27066;
    for (int i = 0; i < 27066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27067(int& val) {
    func_in_27067(val);
    const int x = 27067;
    std::cout << 27067;
    for (int i = 0; i < 27067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27067(int& val) {
    const int x = 27067;
    std::cout << 27067;
    for (int i = 0; i < 27067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27068(int& val) {
    func_in_27068(val);
    const int x = 27068;
    std::cout << 27068;
    for (int i = 0; i < 27068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27068(int& val) {
    const int x = 27068;
    std::cout << 27068;
    for (int i = 0; i < 27068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27069(int& val) {
    func_in_27069(val);
    const int x = 27069;
    std::cout << 27069;
    for (int i = 0; i < 27069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27069(int& val) {
    const int x = 27069;
    std::cout << 27069;
    for (int i = 0; i < 27069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27070(int& val) {
    func_in_27070(val);
    const int x = 27070;
    std::cout << 27070;
    for (int i = 0; i < 27070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27070(int& val) {
    const int x = 27070;
    std::cout << 27070;
    for (int i = 0; i < 27070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27071(int& val) {
    func_in_27071(val);
    const int x = 27071;
    std::cout << 27071;
    for (int i = 0; i < 27071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27071(int& val) {
    const int x = 27071;
    std::cout << 27071;
    for (int i = 0; i < 27071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27072(int& val) {
    func_in_27072(val);
    const int x = 27072;
    std::cout << 27072;
    for (int i = 0; i < 27072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27072(int& val) {
    const int x = 27072;
    std::cout << 27072;
    for (int i = 0; i < 27072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27073(int& val) {
    func_in_27073(val);
    const int x = 27073;
    std::cout << 27073;
    for (int i = 0; i < 27073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27073(int& val) {
    const int x = 27073;
    std::cout << 27073;
    for (int i = 0; i < 27073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27074(int& val) {
    func_in_27074(val);
    const int x = 27074;
    std::cout << 27074;
    for (int i = 0; i < 27074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27074(int& val) {
    const int x = 27074;
    std::cout << 27074;
    for (int i = 0; i < 27074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27075(int& val) {
    func_in_27075(val);
    const int x = 27075;
    std::cout << 27075;
    for (int i = 0; i < 27075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27075(int& val) {
    const int x = 27075;
    std::cout << 27075;
    for (int i = 0; i < 27075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27076(int& val) {
    func_in_27076(val);
    const int x = 27076;
    std::cout << 27076;
    for (int i = 0; i < 27076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27076(int& val) {
    const int x = 27076;
    std::cout << 27076;
    for (int i = 0; i < 27076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27077(int& val) {
    func_in_27077(val);
    const int x = 27077;
    std::cout << 27077;
    for (int i = 0; i < 27077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27077(int& val) {
    const int x = 27077;
    std::cout << 27077;
    for (int i = 0; i < 27077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27078(int& val) {
    func_in_27078(val);
    const int x = 27078;
    std::cout << 27078;
    for (int i = 0; i < 27078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27078(int& val) {
    const int x = 27078;
    std::cout << 27078;
    for (int i = 0; i < 27078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27079(int& val) {
    func_in_27079(val);
    const int x = 27079;
    std::cout << 27079;
    for (int i = 0; i < 27079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27079(int& val) {
    const int x = 27079;
    std::cout << 27079;
    for (int i = 0; i < 27079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27080(int& val) {
    func_in_27080(val);
    const int x = 27080;
    std::cout << 27080;
    for (int i = 0; i < 27080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27080(int& val) {
    const int x = 27080;
    std::cout << 27080;
    for (int i = 0; i < 27080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27081(int& val) {
    func_in_27081(val);
    const int x = 27081;
    std::cout << 27081;
    for (int i = 0; i < 27081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27081(int& val) {
    const int x = 27081;
    std::cout << 27081;
    for (int i = 0; i < 27081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27082(int& val) {
    func_in_27082(val);
    const int x = 27082;
    std::cout << 27082;
    for (int i = 0; i < 27082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27082(int& val) {
    const int x = 27082;
    std::cout << 27082;
    for (int i = 0; i < 27082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27083(int& val) {
    func_in_27083(val);
    const int x = 27083;
    std::cout << 27083;
    for (int i = 0; i < 27083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27083(int& val) {
    const int x = 27083;
    std::cout << 27083;
    for (int i = 0; i < 27083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27084(int& val) {
    func_in_27084(val);
    const int x = 27084;
    std::cout << 27084;
    for (int i = 0; i < 27084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27084(int& val) {
    const int x = 27084;
    std::cout << 27084;
    for (int i = 0; i < 27084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27085(int& val) {
    func_in_27085(val);
    const int x = 27085;
    std::cout << 27085;
    for (int i = 0; i < 27085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27085(int& val) {
    const int x = 27085;
    std::cout << 27085;
    for (int i = 0; i < 27085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27086(int& val) {
    func_in_27086(val);
    const int x = 27086;
    std::cout << 27086;
    for (int i = 0; i < 27086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27086(int& val) {
    const int x = 27086;
    std::cout << 27086;
    for (int i = 0; i < 27086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27087(int& val) {
    func_in_27087(val);
    const int x = 27087;
    std::cout << 27087;
    for (int i = 0; i < 27087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27087(int& val) {
    const int x = 27087;
    std::cout << 27087;
    for (int i = 0; i < 27087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27088(int& val) {
    func_in_27088(val);
    const int x = 27088;
    std::cout << 27088;
    for (int i = 0; i < 27088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27088(int& val) {
    const int x = 27088;
    std::cout << 27088;
    for (int i = 0; i < 27088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27089(int& val) {
    func_in_27089(val);
    const int x = 27089;
    std::cout << 27089;
    for (int i = 0; i < 27089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27089(int& val) {
    const int x = 27089;
    std::cout << 27089;
    for (int i = 0; i < 27089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27090(int& val) {
    func_in_27090(val);
    const int x = 27090;
    std::cout << 27090;
    for (int i = 0; i < 27090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27090(int& val) {
    const int x = 27090;
    std::cout << 27090;
    for (int i = 0; i < 27090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27091(int& val) {
    func_in_27091(val);
    const int x = 27091;
    std::cout << 27091;
    for (int i = 0; i < 27091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27091(int& val) {
    const int x = 27091;
    std::cout << 27091;
    for (int i = 0; i < 27091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27092(int& val) {
    func_in_27092(val);
    const int x = 27092;
    std::cout << 27092;
    for (int i = 0; i < 27092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27092(int& val) {
    const int x = 27092;
    std::cout << 27092;
    for (int i = 0; i < 27092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27093(int& val) {
    func_in_27093(val);
    const int x = 27093;
    std::cout << 27093;
    for (int i = 0; i < 27093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27093(int& val) {
    const int x = 27093;
    std::cout << 27093;
    for (int i = 0; i < 27093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27094(int& val) {
    func_in_27094(val);
    const int x = 27094;
    std::cout << 27094;
    for (int i = 0; i < 27094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27094(int& val) {
    const int x = 27094;
    std::cout << 27094;
    for (int i = 0; i < 27094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27095(int& val) {
    func_in_27095(val);
    const int x = 27095;
    std::cout << 27095;
    for (int i = 0; i < 27095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27095(int& val) {
    const int x = 27095;
    std::cout << 27095;
    for (int i = 0; i < 27095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27096(int& val) {
    func_in_27096(val);
    const int x = 27096;
    std::cout << 27096;
    for (int i = 0; i < 27096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27096(int& val) {
    const int x = 27096;
    std::cout << 27096;
    for (int i = 0; i < 27096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27097(int& val) {
    func_in_27097(val);
    const int x = 27097;
    std::cout << 27097;
    for (int i = 0; i < 27097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27097(int& val) {
    const int x = 27097;
    std::cout << 27097;
    for (int i = 0; i < 27097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27098(int& val) {
    func_in_27098(val);
    const int x = 27098;
    std::cout << 27098;
    for (int i = 0; i < 27098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27098(int& val) {
    const int x = 27098;
    std::cout << 27098;
    for (int i = 0; i < 27098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27099(int& val) {
    func_in_27099(val);
    const int x = 27099;
    std::cout << 27099;
    for (int i = 0; i < 27099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27099(int& val) {
    const int x = 27099;
    std::cout << 27099;
    for (int i = 0; i < 27099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27100(int& val) {
    func_in_27100(val);
    const int x = 27100;
    std::cout << 27100;
    for (int i = 0; i < 27100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27100(int& val) {
    const int x = 27100;
    std::cout << 27100;
    for (int i = 0; i < 27100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27101(int& val) {
    func_in_27101(val);
    const int x = 27101;
    std::cout << 27101;
    for (int i = 0; i < 27101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27101(int& val) {
    const int x = 27101;
    std::cout << 27101;
    for (int i = 0; i < 27101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27102(int& val) {
    func_in_27102(val);
    const int x = 27102;
    std::cout << 27102;
    for (int i = 0; i < 27102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27102(int& val) {
    const int x = 27102;
    std::cout << 27102;
    for (int i = 0; i < 27102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27103(int& val) {
    func_in_27103(val);
    const int x = 27103;
    std::cout << 27103;
    for (int i = 0; i < 27103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27103(int& val) {
    const int x = 27103;
    std::cout << 27103;
    for (int i = 0; i < 27103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27104(int& val) {
    func_in_27104(val);
    const int x = 27104;
    std::cout << 27104;
    for (int i = 0; i < 27104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27104(int& val) {
    const int x = 27104;
    std::cout << 27104;
    for (int i = 0; i < 27104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27105(int& val) {
    func_in_27105(val);
    const int x = 27105;
    std::cout << 27105;
    for (int i = 0; i < 27105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27105(int& val) {
    const int x = 27105;
    std::cout << 27105;
    for (int i = 0; i < 27105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27106(int& val) {
    func_in_27106(val);
    const int x = 27106;
    std::cout << 27106;
    for (int i = 0; i < 27106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27106(int& val) {
    const int x = 27106;
    std::cout << 27106;
    for (int i = 0; i < 27106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27107(int& val) {
    func_in_27107(val);
    const int x = 27107;
    std::cout << 27107;
    for (int i = 0; i < 27107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27107(int& val) {
    const int x = 27107;
    std::cout << 27107;
    for (int i = 0; i < 27107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27108(int& val) {
    func_in_27108(val);
    const int x = 27108;
    std::cout << 27108;
    for (int i = 0; i < 27108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27108(int& val) {
    const int x = 27108;
    std::cout << 27108;
    for (int i = 0; i < 27108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27109(int& val) {
    func_in_27109(val);
    const int x = 27109;
    std::cout << 27109;
    for (int i = 0; i < 27109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27109(int& val) {
    const int x = 27109;
    std::cout << 27109;
    for (int i = 0; i < 27109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27110(int& val) {
    func_in_27110(val);
    const int x = 27110;
    std::cout << 27110;
    for (int i = 0; i < 27110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27110(int& val) {
    const int x = 27110;
    std::cout << 27110;
    for (int i = 0; i < 27110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27111(int& val) {
    func_in_27111(val);
    const int x = 27111;
    std::cout << 27111;
    for (int i = 0; i < 27111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27111(int& val) {
    const int x = 27111;
    std::cout << 27111;
    for (int i = 0; i < 27111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27112(int& val) {
    func_in_27112(val);
    const int x = 27112;
    std::cout << 27112;
    for (int i = 0; i < 27112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27112(int& val) {
    const int x = 27112;
    std::cout << 27112;
    for (int i = 0; i < 27112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27113(int& val) {
    func_in_27113(val);
    const int x = 27113;
    std::cout << 27113;
    for (int i = 0; i < 27113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27113(int& val) {
    const int x = 27113;
    std::cout << 27113;
    for (int i = 0; i < 27113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27114(int& val) {
    func_in_27114(val);
    const int x = 27114;
    std::cout << 27114;
    for (int i = 0; i < 27114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27114(int& val) {
    const int x = 27114;
    std::cout << 27114;
    for (int i = 0; i < 27114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27115(int& val) {
    func_in_27115(val);
    const int x = 27115;
    std::cout << 27115;
    for (int i = 0; i < 27115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27115(int& val) {
    const int x = 27115;
    std::cout << 27115;
    for (int i = 0; i < 27115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27116(int& val) {
    func_in_27116(val);
    const int x = 27116;
    std::cout << 27116;
    for (int i = 0; i < 27116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27116(int& val) {
    const int x = 27116;
    std::cout << 27116;
    for (int i = 0; i < 27116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27117(int& val) {
    func_in_27117(val);
    const int x = 27117;
    std::cout << 27117;
    for (int i = 0; i < 27117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27117(int& val) {
    const int x = 27117;
    std::cout << 27117;
    for (int i = 0; i < 27117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27118(int& val) {
    func_in_27118(val);
    const int x = 27118;
    std::cout << 27118;
    for (int i = 0; i < 27118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27118(int& val) {
    const int x = 27118;
    std::cout << 27118;
    for (int i = 0; i < 27118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27119(int& val) {
    func_in_27119(val);
    const int x = 27119;
    std::cout << 27119;
    for (int i = 0; i < 27119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27119(int& val) {
    const int x = 27119;
    std::cout << 27119;
    for (int i = 0; i < 27119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27120(int& val) {
    func_in_27120(val);
    const int x = 27120;
    std::cout << 27120;
    for (int i = 0; i < 27120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27120(int& val) {
    const int x = 27120;
    std::cout << 27120;
    for (int i = 0; i < 27120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27121(int& val) {
    func_in_27121(val);
    const int x = 27121;
    std::cout << 27121;
    for (int i = 0; i < 27121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27121(int& val) {
    const int x = 27121;
    std::cout << 27121;
    for (int i = 0; i < 27121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27122(int& val) {
    func_in_27122(val);
    const int x = 27122;
    std::cout << 27122;
    for (int i = 0; i < 27122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27122(int& val) {
    const int x = 27122;
    std::cout << 27122;
    for (int i = 0; i < 27122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27123(int& val) {
    func_in_27123(val);
    const int x = 27123;
    std::cout << 27123;
    for (int i = 0; i < 27123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27123(int& val) {
    const int x = 27123;
    std::cout << 27123;
    for (int i = 0; i < 27123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27124(int& val) {
    func_in_27124(val);
    const int x = 27124;
    std::cout << 27124;
    for (int i = 0; i < 27124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27124(int& val) {
    const int x = 27124;
    std::cout << 27124;
    for (int i = 0; i < 27124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27125(int& val) {
    func_in_27125(val);
    const int x = 27125;
    std::cout << 27125;
    for (int i = 0; i < 27125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27125(int& val) {
    const int x = 27125;
    std::cout << 27125;
    for (int i = 0; i < 27125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27126(int& val) {
    func_in_27126(val);
    const int x = 27126;
    std::cout << 27126;
    for (int i = 0; i < 27126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27126(int& val) {
    const int x = 27126;
    std::cout << 27126;
    for (int i = 0; i < 27126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27127(int& val) {
    func_in_27127(val);
    const int x = 27127;
    std::cout << 27127;
    for (int i = 0; i < 27127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27127(int& val) {
    const int x = 27127;
    std::cout << 27127;
    for (int i = 0; i < 27127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27128(int& val) {
    func_in_27128(val);
    const int x = 27128;
    std::cout << 27128;
    for (int i = 0; i < 27128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27128(int& val) {
    const int x = 27128;
    std::cout << 27128;
    for (int i = 0; i < 27128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27129(int& val) {
    func_in_27129(val);
    const int x = 27129;
    std::cout << 27129;
    for (int i = 0; i < 27129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27129(int& val) {
    const int x = 27129;
    std::cout << 27129;
    for (int i = 0; i < 27129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27130(int& val) {
    func_in_27130(val);
    const int x = 27130;
    std::cout << 27130;
    for (int i = 0; i < 27130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27130(int& val) {
    const int x = 27130;
    std::cout << 27130;
    for (int i = 0; i < 27130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27131(int& val) {
    func_in_27131(val);
    const int x = 27131;
    std::cout << 27131;
    for (int i = 0; i < 27131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27131(int& val) {
    const int x = 27131;
    std::cout << 27131;
    for (int i = 0; i < 27131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27132(int& val) {
    func_in_27132(val);
    const int x = 27132;
    std::cout << 27132;
    for (int i = 0; i < 27132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27132(int& val) {
    const int x = 27132;
    std::cout << 27132;
    for (int i = 0; i < 27132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27133(int& val) {
    func_in_27133(val);
    const int x = 27133;
    std::cout << 27133;
    for (int i = 0; i < 27133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27133(int& val) {
    const int x = 27133;
    std::cout << 27133;
    for (int i = 0; i < 27133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27134(int& val) {
    func_in_27134(val);
    const int x = 27134;
    std::cout << 27134;
    for (int i = 0; i < 27134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27134(int& val) {
    const int x = 27134;
    std::cout << 27134;
    for (int i = 0; i < 27134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27135(int& val) {
    func_in_27135(val);
    const int x = 27135;
    std::cout << 27135;
    for (int i = 0; i < 27135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27135(int& val) {
    const int x = 27135;
    std::cout << 27135;
    for (int i = 0; i < 27135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27136(int& val) {
    func_in_27136(val);
    const int x = 27136;
    std::cout << 27136;
    for (int i = 0; i < 27136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27136(int& val) {
    const int x = 27136;
    std::cout << 27136;
    for (int i = 0; i < 27136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27137(int& val) {
    func_in_27137(val);
    const int x = 27137;
    std::cout << 27137;
    for (int i = 0; i < 27137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27137(int& val) {
    const int x = 27137;
    std::cout << 27137;
    for (int i = 0; i < 27137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27138(int& val) {
    func_in_27138(val);
    const int x = 27138;
    std::cout << 27138;
    for (int i = 0; i < 27138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27138(int& val) {
    const int x = 27138;
    std::cout << 27138;
    for (int i = 0; i < 27138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27139(int& val) {
    func_in_27139(val);
    const int x = 27139;
    std::cout << 27139;
    for (int i = 0; i < 27139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27139(int& val) {
    const int x = 27139;
    std::cout << 27139;
    for (int i = 0; i < 27139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27140(int& val) {
    func_in_27140(val);
    const int x = 27140;
    std::cout << 27140;
    for (int i = 0; i < 27140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27140(int& val) {
    const int x = 27140;
    std::cout << 27140;
    for (int i = 0; i < 27140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27141(int& val) {
    func_in_27141(val);
    const int x = 27141;
    std::cout << 27141;
    for (int i = 0; i < 27141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27141(int& val) {
    const int x = 27141;
    std::cout << 27141;
    for (int i = 0; i < 27141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27142(int& val) {
    func_in_27142(val);
    const int x = 27142;
    std::cout << 27142;
    for (int i = 0; i < 27142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27142(int& val) {
    const int x = 27142;
    std::cout << 27142;
    for (int i = 0; i < 27142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27143(int& val) {
    func_in_27143(val);
    const int x = 27143;
    std::cout << 27143;
    for (int i = 0; i < 27143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27143(int& val) {
    const int x = 27143;
    std::cout << 27143;
    for (int i = 0; i < 27143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27144(int& val) {
    func_in_27144(val);
    const int x = 27144;
    std::cout << 27144;
    for (int i = 0; i < 27144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27144(int& val) {
    const int x = 27144;
    std::cout << 27144;
    for (int i = 0; i < 27144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27145(int& val) {
    func_in_27145(val);
    const int x = 27145;
    std::cout << 27145;
    for (int i = 0; i < 27145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27145(int& val) {
    const int x = 27145;
    std::cout << 27145;
    for (int i = 0; i < 27145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27146(int& val) {
    func_in_27146(val);
    const int x = 27146;
    std::cout << 27146;
    for (int i = 0; i < 27146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27146(int& val) {
    const int x = 27146;
    std::cout << 27146;
    for (int i = 0; i < 27146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27147(int& val) {
    func_in_27147(val);
    const int x = 27147;
    std::cout << 27147;
    for (int i = 0; i < 27147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27147(int& val) {
    const int x = 27147;
    std::cout << 27147;
    for (int i = 0; i < 27147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27148(int& val) {
    func_in_27148(val);
    const int x = 27148;
    std::cout << 27148;
    for (int i = 0; i < 27148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27148(int& val) {
    const int x = 27148;
    std::cout << 27148;
    for (int i = 0; i < 27148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27149(int& val) {
    func_in_27149(val);
    const int x = 27149;
    std::cout << 27149;
    for (int i = 0; i < 27149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27149(int& val) {
    const int x = 27149;
    std::cout << 27149;
    for (int i = 0; i < 27149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27150(int& val) {
    func_in_27150(val);
    const int x = 27150;
    std::cout << 27150;
    for (int i = 0; i < 27150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27150(int& val) {
    const int x = 27150;
    std::cout << 27150;
    for (int i = 0; i < 27150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27151(int& val) {
    func_in_27151(val);
    const int x = 27151;
    std::cout << 27151;
    for (int i = 0; i < 27151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27151(int& val) {
    const int x = 27151;
    std::cout << 27151;
    for (int i = 0; i < 27151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27152(int& val) {
    func_in_27152(val);
    const int x = 27152;
    std::cout << 27152;
    for (int i = 0; i < 27152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27152(int& val) {
    const int x = 27152;
    std::cout << 27152;
    for (int i = 0; i < 27152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27153(int& val) {
    func_in_27153(val);
    const int x = 27153;
    std::cout << 27153;
    for (int i = 0; i < 27153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27153(int& val) {
    const int x = 27153;
    std::cout << 27153;
    for (int i = 0; i < 27153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27154(int& val) {
    func_in_27154(val);
    const int x = 27154;
    std::cout << 27154;
    for (int i = 0; i < 27154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27154(int& val) {
    const int x = 27154;
    std::cout << 27154;
    for (int i = 0; i < 27154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27155(int& val) {
    func_in_27155(val);
    const int x = 27155;
    std::cout << 27155;
    for (int i = 0; i < 27155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27155(int& val) {
    const int x = 27155;
    std::cout << 27155;
    for (int i = 0; i < 27155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27156(int& val) {
    func_in_27156(val);
    const int x = 27156;
    std::cout << 27156;
    for (int i = 0; i < 27156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27156(int& val) {
    const int x = 27156;
    std::cout << 27156;
    for (int i = 0; i < 27156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27157(int& val) {
    func_in_27157(val);
    const int x = 27157;
    std::cout << 27157;
    for (int i = 0; i < 27157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27157(int& val) {
    const int x = 27157;
    std::cout << 27157;
    for (int i = 0; i < 27157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27158(int& val) {
    func_in_27158(val);
    const int x = 27158;
    std::cout << 27158;
    for (int i = 0; i < 27158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27158(int& val) {
    const int x = 27158;
    std::cout << 27158;
    for (int i = 0; i < 27158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27159(int& val) {
    func_in_27159(val);
    const int x = 27159;
    std::cout << 27159;
    for (int i = 0; i < 27159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27159(int& val) {
    const int x = 27159;
    std::cout << 27159;
    for (int i = 0; i < 27159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27160(int& val) {
    func_in_27160(val);
    const int x = 27160;
    std::cout << 27160;
    for (int i = 0; i < 27160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27160(int& val) {
    const int x = 27160;
    std::cout << 27160;
    for (int i = 0; i < 27160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27161(int& val) {
    func_in_27161(val);
    const int x = 27161;
    std::cout << 27161;
    for (int i = 0; i < 27161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27161(int& val) {
    const int x = 27161;
    std::cout << 27161;
    for (int i = 0; i < 27161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27162(int& val) {
    func_in_27162(val);
    const int x = 27162;
    std::cout << 27162;
    for (int i = 0; i < 27162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27162(int& val) {
    const int x = 27162;
    std::cout << 27162;
    for (int i = 0; i < 27162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27163(int& val) {
    func_in_27163(val);
    const int x = 27163;
    std::cout << 27163;
    for (int i = 0; i < 27163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27163(int& val) {
    const int x = 27163;
    std::cout << 27163;
    for (int i = 0; i < 27163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27164(int& val) {
    func_in_27164(val);
    const int x = 27164;
    std::cout << 27164;
    for (int i = 0; i < 27164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27164(int& val) {
    const int x = 27164;
    std::cout << 27164;
    for (int i = 0; i < 27164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27165(int& val) {
    func_in_27165(val);
    const int x = 27165;
    std::cout << 27165;
    for (int i = 0; i < 27165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27165(int& val) {
    const int x = 27165;
    std::cout << 27165;
    for (int i = 0; i < 27165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27166(int& val) {
    func_in_27166(val);
    const int x = 27166;
    std::cout << 27166;
    for (int i = 0; i < 27166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27166(int& val) {
    const int x = 27166;
    std::cout << 27166;
    for (int i = 0; i < 27166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27167(int& val) {
    func_in_27167(val);
    const int x = 27167;
    std::cout << 27167;
    for (int i = 0; i < 27167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27167(int& val) {
    const int x = 27167;
    std::cout << 27167;
    for (int i = 0; i < 27167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27168(int& val) {
    func_in_27168(val);
    const int x = 27168;
    std::cout << 27168;
    for (int i = 0; i < 27168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27168(int& val) {
    const int x = 27168;
    std::cout << 27168;
    for (int i = 0; i < 27168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27169(int& val) {
    func_in_27169(val);
    const int x = 27169;
    std::cout << 27169;
    for (int i = 0; i < 27169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27169(int& val) {
    const int x = 27169;
    std::cout << 27169;
    for (int i = 0; i < 27169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27170(int& val) {
    func_in_27170(val);
    const int x = 27170;
    std::cout << 27170;
    for (int i = 0; i < 27170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27170(int& val) {
    const int x = 27170;
    std::cout << 27170;
    for (int i = 0; i < 27170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27171(int& val) {
    func_in_27171(val);
    const int x = 27171;
    std::cout << 27171;
    for (int i = 0; i < 27171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27171(int& val) {
    const int x = 27171;
    std::cout << 27171;
    for (int i = 0; i < 27171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27172(int& val) {
    func_in_27172(val);
    const int x = 27172;
    std::cout << 27172;
    for (int i = 0; i < 27172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27172(int& val) {
    const int x = 27172;
    std::cout << 27172;
    for (int i = 0; i < 27172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27173(int& val) {
    func_in_27173(val);
    const int x = 27173;
    std::cout << 27173;
    for (int i = 0; i < 27173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27173(int& val) {
    const int x = 27173;
    std::cout << 27173;
    for (int i = 0; i < 27173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27174(int& val) {
    func_in_27174(val);
    const int x = 27174;
    std::cout << 27174;
    for (int i = 0; i < 27174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27174(int& val) {
    const int x = 27174;
    std::cout << 27174;
    for (int i = 0; i < 27174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27175(int& val) {
    func_in_27175(val);
    const int x = 27175;
    std::cout << 27175;
    for (int i = 0; i < 27175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27175(int& val) {
    const int x = 27175;
    std::cout << 27175;
    for (int i = 0; i < 27175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27176(int& val) {
    func_in_27176(val);
    const int x = 27176;
    std::cout << 27176;
    for (int i = 0; i < 27176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27176(int& val) {
    const int x = 27176;
    std::cout << 27176;
    for (int i = 0; i < 27176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27177(int& val) {
    func_in_27177(val);
    const int x = 27177;
    std::cout << 27177;
    for (int i = 0; i < 27177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27177(int& val) {
    const int x = 27177;
    std::cout << 27177;
    for (int i = 0; i < 27177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27178(int& val) {
    func_in_27178(val);
    const int x = 27178;
    std::cout << 27178;
    for (int i = 0; i < 27178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27178(int& val) {
    const int x = 27178;
    std::cout << 27178;
    for (int i = 0; i < 27178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27179(int& val) {
    func_in_27179(val);
    const int x = 27179;
    std::cout << 27179;
    for (int i = 0; i < 27179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27179(int& val) {
    const int x = 27179;
    std::cout << 27179;
    for (int i = 0; i < 27179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27180(int& val) {
    func_in_27180(val);
    const int x = 27180;
    std::cout << 27180;
    for (int i = 0; i < 27180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27180(int& val) {
    const int x = 27180;
    std::cout << 27180;
    for (int i = 0; i < 27180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27181(int& val) {
    func_in_27181(val);
    const int x = 27181;
    std::cout << 27181;
    for (int i = 0; i < 27181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27181(int& val) {
    const int x = 27181;
    std::cout << 27181;
    for (int i = 0; i < 27181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27182(int& val) {
    func_in_27182(val);
    const int x = 27182;
    std::cout << 27182;
    for (int i = 0; i < 27182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27182(int& val) {
    const int x = 27182;
    std::cout << 27182;
    for (int i = 0; i < 27182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27183(int& val) {
    func_in_27183(val);
    const int x = 27183;
    std::cout << 27183;
    for (int i = 0; i < 27183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27183(int& val) {
    const int x = 27183;
    std::cout << 27183;
    for (int i = 0; i < 27183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27184(int& val) {
    func_in_27184(val);
    const int x = 27184;
    std::cout << 27184;
    for (int i = 0; i < 27184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27184(int& val) {
    const int x = 27184;
    std::cout << 27184;
    for (int i = 0; i < 27184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27185(int& val) {
    func_in_27185(val);
    const int x = 27185;
    std::cout << 27185;
    for (int i = 0; i < 27185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27185(int& val) {
    const int x = 27185;
    std::cout << 27185;
    for (int i = 0; i < 27185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27186(int& val) {
    func_in_27186(val);
    const int x = 27186;
    std::cout << 27186;
    for (int i = 0; i < 27186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27186(int& val) {
    const int x = 27186;
    std::cout << 27186;
    for (int i = 0; i < 27186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27187(int& val) {
    func_in_27187(val);
    const int x = 27187;
    std::cout << 27187;
    for (int i = 0; i < 27187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27187(int& val) {
    const int x = 27187;
    std::cout << 27187;
    for (int i = 0; i < 27187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27188(int& val) {
    func_in_27188(val);
    const int x = 27188;
    std::cout << 27188;
    for (int i = 0; i < 27188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27188(int& val) {
    const int x = 27188;
    std::cout << 27188;
    for (int i = 0; i < 27188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27189(int& val) {
    func_in_27189(val);
    const int x = 27189;
    std::cout << 27189;
    for (int i = 0; i < 27189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27189(int& val) {
    const int x = 27189;
    std::cout << 27189;
    for (int i = 0; i < 27189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27190(int& val) {
    func_in_27190(val);
    const int x = 27190;
    std::cout << 27190;
    for (int i = 0; i < 27190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27190(int& val) {
    const int x = 27190;
    std::cout << 27190;
    for (int i = 0; i < 27190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27191(int& val) {
    func_in_27191(val);
    const int x = 27191;
    std::cout << 27191;
    for (int i = 0; i < 27191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27191(int& val) {
    const int x = 27191;
    std::cout << 27191;
    for (int i = 0; i < 27191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27192(int& val) {
    func_in_27192(val);
    const int x = 27192;
    std::cout << 27192;
    for (int i = 0; i < 27192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27192(int& val) {
    const int x = 27192;
    std::cout << 27192;
    for (int i = 0; i < 27192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27193(int& val) {
    func_in_27193(val);
    const int x = 27193;
    std::cout << 27193;
    for (int i = 0; i < 27193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27193(int& val) {
    const int x = 27193;
    std::cout << 27193;
    for (int i = 0; i < 27193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27194(int& val) {
    func_in_27194(val);
    const int x = 27194;
    std::cout << 27194;
    for (int i = 0; i < 27194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27194(int& val) {
    const int x = 27194;
    std::cout << 27194;
    for (int i = 0; i < 27194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27195(int& val) {
    func_in_27195(val);
    const int x = 27195;
    std::cout << 27195;
    for (int i = 0; i < 27195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27195(int& val) {
    const int x = 27195;
    std::cout << 27195;
    for (int i = 0; i < 27195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27196(int& val) {
    func_in_27196(val);
    const int x = 27196;
    std::cout << 27196;
    for (int i = 0; i < 27196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27196(int& val) {
    const int x = 27196;
    std::cout << 27196;
    for (int i = 0; i < 27196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27197(int& val) {
    func_in_27197(val);
    const int x = 27197;
    std::cout << 27197;
    for (int i = 0; i < 27197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27197(int& val) {
    const int x = 27197;
    std::cout << 27197;
    for (int i = 0; i < 27197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27198(int& val) {
    func_in_27198(val);
    const int x = 27198;
    std::cout << 27198;
    for (int i = 0; i < 27198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27198(int& val) {
    const int x = 27198;
    std::cout << 27198;
    for (int i = 0; i < 27198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27199(int& val) {
    func_in_27199(val);
    const int x = 27199;
    std::cout << 27199;
    for (int i = 0; i < 27199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27199(int& val) {
    const int x = 27199;
    std::cout << 27199;
    for (int i = 0; i < 27199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27200(int& val) {
    func_in_27200(val);
    const int x = 27200;
    std::cout << 27200;
    for (int i = 0; i < 27200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27200(int& val) {
    const int x = 27200;
    std::cout << 27200;
    for (int i = 0; i < 27200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27201(int& val) {
    func_in_27201(val);
    const int x = 27201;
    std::cout << 27201;
    for (int i = 0; i < 27201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27201(int& val) {
    const int x = 27201;
    std::cout << 27201;
    for (int i = 0; i < 27201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27202(int& val) {
    func_in_27202(val);
    const int x = 27202;
    std::cout << 27202;
    for (int i = 0; i < 27202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27202(int& val) {
    const int x = 27202;
    std::cout << 27202;
    for (int i = 0; i < 27202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27203(int& val) {
    func_in_27203(val);
    const int x = 27203;
    std::cout << 27203;
    for (int i = 0; i < 27203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27203(int& val) {
    const int x = 27203;
    std::cout << 27203;
    for (int i = 0; i < 27203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27204(int& val) {
    func_in_27204(val);
    const int x = 27204;
    std::cout << 27204;
    for (int i = 0; i < 27204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27204(int& val) {
    const int x = 27204;
    std::cout << 27204;
    for (int i = 0; i < 27204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27205(int& val) {
    func_in_27205(val);
    const int x = 27205;
    std::cout << 27205;
    for (int i = 0; i < 27205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27205(int& val) {
    const int x = 27205;
    std::cout << 27205;
    for (int i = 0; i < 27205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27206(int& val) {
    func_in_27206(val);
    const int x = 27206;
    std::cout << 27206;
    for (int i = 0; i < 27206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27206(int& val) {
    const int x = 27206;
    std::cout << 27206;
    for (int i = 0; i < 27206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27207(int& val) {
    func_in_27207(val);
    const int x = 27207;
    std::cout << 27207;
    for (int i = 0; i < 27207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27207(int& val) {
    const int x = 27207;
    std::cout << 27207;
    for (int i = 0; i < 27207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27208(int& val) {
    func_in_27208(val);
    const int x = 27208;
    std::cout << 27208;
    for (int i = 0; i < 27208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27208(int& val) {
    const int x = 27208;
    std::cout << 27208;
    for (int i = 0; i < 27208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27209(int& val) {
    func_in_27209(val);
    const int x = 27209;
    std::cout << 27209;
    for (int i = 0; i < 27209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27209(int& val) {
    const int x = 27209;
    std::cout << 27209;
    for (int i = 0; i < 27209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27210(int& val) {
    func_in_27210(val);
    const int x = 27210;
    std::cout << 27210;
    for (int i = 0; i < 27210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27210(int& val) {
    const int x = 27210;
    std::cout << 27210;
    for (int i = 0; i < 27210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27211(int& val) {
    func_in_27211(val);
    const int x = 27211;
    std::cout << 27211;
    for (int i = 0; i < 27211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27211(int& val) {
    const int x = 27211;
    std::cout << 27211;
    for (int i = 0; i < 27211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27212(int& val) {
    func_in_27212(val);
    const int x = 27212;
    std::cout << 27212;
    for (int i = 0; i < 27212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27212(int& val) {
    const int x = 27212;
    std::cout << 27212;
    for (int i = 0; i < 27212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27213(int& val) {
    func_in_27213(val);
    const int x = 27213;
    std::cout << 27213;
    for (int i = 0; i < 27213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27213(int& val) {
    const int x = 27213;
    std::cout << 27213;
    for (int i = 0; i < 27213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27214(int& val) {
    func_in_27214(val);
    const int x = 27214;
    std::cout << 27214;
    for (int i = 0; i < 27214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27214(int& val) {
    const int x = 27214;
    std::cout << 27214;
    for (int i = 0; i < 27214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27215(int& val) {
    func_in_27215(val);
    const int x = 27215;
    std::cout << 27215;
    for (int i = 0; i < 27215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27215(int& val) {
    const int x = 27215;
    std::cout << 27215;
    for (int i = 0; i < 27215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27216(int& val) {
    func_in_27216(val);
    const int x = 27216;
    std::cout << 27216;
    for (int i = 0; i < 27216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27216(int& val) {
    const int x = 27216;
    std::cout << 27216;
    for (int i = 0; i < 27216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27217(int& val) {
    func_in_27217(val);
    const int x = 27217;
    std::cout << 27217;
    for (int i = 0; i < 27217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27217(int& val) {
    const int x = 27217;
    std::cout << 27217;
    for (int i = 0; i < 27217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27218(int& val) {
    func_in_27218(val);
    const int x = 27218;
    std::cout << 27218;
    for (int i = 0; i < 27218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27218(int& val) {
    const int x = 27218;
    std::cout << 27218;
    for (int i = 0; i < 27218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27219(int& val) {
    func_in_27219(val);
    const int x = 27219;
    std::cout << 27219;
    for (int i = 0; i < 27219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27219(int& val) {
    const int x = 27219;
    std::cout << 27219;
    for (int i = 0; i < 27219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27220(int& val) {
    func_in_27220(val);
    const int x = 27220;
    std::cout << 27220;
    for (int i = 0; i < 27220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27220(int& val) {
    const int x = 27220;
    std::cout << 27220;
    for (int i = 0; i < 27220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27221(int& val) {
    func_in_27221(val);
    const int x = 27221;
    std::cout << 27221;
    for (int i = 0; i < 27221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27221(int& val) {
    const int x = 27221;
    std::cout << 27221;
    for (int i = 0; i < 27221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27222(int& val) {
    func_in_27222(val);
    const int x = 27222;
    std::cout << 27222;
    for (int i = 0; i < 27222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27222(int& val) {
    const int x = 27222;
    std::cout << 27222;
    for (int i = 0; i < 27222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27223(int& val) {
    func_in_27223(val);
    const int x = 27223;
    std::cout << 27223;
    for (int i = 0; i < 27223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27223(int& val) {
    const int x = 27223;
    std::cout << 27223;
    for (int i = 0; i < 27223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27224(int& val) {
    func_in_27224(val);
    const int x = 27224;
    std::cout << 27224;
    for (int i = 0; i < 27224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27224(int& val) {
    const int x = 27224;
    std::cout << 27224;
    for (int i = 0; i < 27224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27225(int& val) {
    func_in_27225(val);
    const int x = 27225;
    std::cout << 27225;
    for (int i = 0; i < 27225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27225(int& val) {
    const int x = 27225;
    std::cout << 27225;
    for (int i = 0; i < 27225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27226(int& val) {
    func_in_27226(val);
    const int x = 27226;
    std::cout << 27226;
    for (int i = 0; i < 27226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27226(int& val) {
    const int x = 27226;
    std::cout << 27226;
    for (int i = 0; i < 27226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27227(int& val) {
    func_in_27227(val);
    const int x = 27227;
    std::cout << 27227;
    for (int i = 0; i < 27227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27227(int& val) {
    const int x = 27227;
    std::cout << 27227;
    for (int i = 0; i < 27227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27228(int& val) {
    func_in_27228(val);
    const int x = 27228;
    std::cout << 27228;
    for (int i = 0; i < 27228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27228(int& val) {
    const int x = 27228;
    std::cout << 27228;
    for (int i = 0; i < 27228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27229(int& val) {
    func_in_27229(val);
    const int x = 27229;
    std::cout << 27229;
    for (int i = 0; i < 27229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27229(int& val) {
    const int x = 27229;
    std::cout << 27229;
    for (int i = 0; i < 27229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27230(int& val) {
    func_in_27230(val);
    const int x = 27230;
    std::cout << 27230;
    for (int i = 0; i < 27230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27230(int& val) {
    const int x = 27230;
    std::cout << 27230;
    for (int i = 0; i < 27230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27231(int& val) {
    func_in_27231(val);
    const int x = 27231;
    std::cout << 27231;
    for (int i = 0; i < 27231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27231(int& val) {
    const int x = 27231;
    std::cout << 27231;
    for (int i = 0; i < 27231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27232(int& val) {
    func_in_27232(val);
    const int x = 27232;
    std::cout << 27232;
    for (int i = 0; i < 27232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27232(int& val) {
    const int x = 27232;
    std::cout << 27232;
    for (int i = 0; i < 27232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27233(int& val) {
    func_in_27233(val);
    const int x = 27233;
    std::cout << 27233;
    for (int i = 0; i < 27233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27233(int& val) {
    const int x = 27233;
    std::cout << 27233;
    for (int i = 0; i < 27233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27234(int& val) {
    func_in_27234(val);
    const int x = 27234;
    std::cout << 27234;
    for (int i = 0; i < 27234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27234(int& val) {
    const int x = 27234;
    std::cout << 27234;
    for (int i = 0; i < 27234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27235(int& val) {
    func_in_27235(val);
    const int x = 27235;
    std::cout << 27235;
    for (int i = 0; i < 27235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27235(int& val) {
    const int x = 27235;
    std::cout << 27235;
    for (int i = 0; i < 27235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27236(int& val) {
    func_in_27236(val);
    const int x = 27236;
    std::cout << 27236;
    for (int i = 0; i < 27236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27236(int& val) {
    const int x = 27236;
    std::cout << 27236;
    for (int i = 0; i < 27236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27237(int& val) {
    func_in_27237(val);
    const int x = 27237;
    std::cout << 27237;
    for (int i = 0; i < 27237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27237(int& val) {
    const int x = 27237;
    std::cout << 27237;
    for (int i = 0; i < 27237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27238(int& val) {
    func_in_27238(val);
    const int x = 27238;
    std::cout << 27238;
    for (int i = 0; i < 27238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27238(int& val) {
    const int x = 27238;
    std::cout << 27238;
    for (int i = 0; i < 27238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27239(int& val) {
    func_in_27239(val);
    const int x = 27239;
    std::cout << 27239;
    for (int i = 0; i < 27239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27239(int& val) {
    const int x = 27239;
    std::cout << 27239;
    for (int i = 0; i < 27239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27240(int& val) {
    func_in_27240(val);
    const int x = 27240;
    std::cout << 27240;
    for (int i = 0; i < 27240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27240(int& val) {
    const int x = 27240;
    std::cout << 27240;
    for (int i = 0; i < 27240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27241(int& val) {
    func_in_27241(val);
    const int x = 27241;
    std::cout << 27241;
    for (int i = 0; i < 27241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27241(int& val) {
    const int x = 27241;
    std::cout << 27241;
    for (int i = 0; i < 27241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27242(int& val) {
    func_in_27242(val);
    const int x = 27242;
    std::cout << 27242;
    for (int i = 0; i < 27242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27242(int& val) {
    const int x = 27242;
    std::cout << 27242;
    for (int i = 0; i < 27242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27243(int& val) {
    func_in_27243(val);
    const int x = 27243;
    std::cout << 27243;
    for (int i = 0; i < 27243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27243(int& val) {
    const int x = 27243;
    std::cout << 27243;
    for (int i = 0; i < 27243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27244(int& val) {
    func_in_27244(val);
    const int x = 27244;
    std::cout << 27244;
    for (int i = 0; i < 27244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27244(int& val) {
    const int x = 27244;
    std::cout << 27244;
    for (int i = 0; i < 27244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27245(int& val) {
    func_in_27245(val);
    const int x = 27245;
    std::cout << 27245;
    for (int i = 0; i < 27245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27245(int& val) {
    const int x = 27245;
    std::cout << 27245;
    for (int i = 0; i < 27245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27246(int& val) {
    func_in_27246(val);
    const int x = 27246;
    std::cout << 27246;
    for (int i = 0; i < 27246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27246(int& val) {
    const int x = 27246;
    std::cout << 27246;
    for (int i = 0; i < 27246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27247(int& val) {
    func_in_27247(val);
    const int x = 27247;
    std::cout << 27247;
    for (int i = 0; i < 27247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27247(int& val) {
    const int x = 27247;
    std::cout << 27247;
    for (int i = 0; i < 27247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27248(int& val) {
    func_in_27248(val);
    const int x = 27248;
    std::cout << 27248;
    for (int i = 0; i < 27248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27248(int& val) {
    const int x = 27248;
    std::cout << 27248;
    for (int i = 0; i < 27248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27249(int& val) {
    func_in_27249(val);
    const int x = 27249;
    std::cout << 27249;
    for (int i = 0; i < 27249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27249(int& val) {
    const int x = 27249;
    std::cout << 27249;
    for (int i = 0; i < 27249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27250(int& val) {
    func_in_27250(val);
    const int x = 27250;
    std::cout << 27250;
    for (int i = 0; i < 27250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27250(int& val) {
    const int x = 27250;
    std::cout << 27250;
    for (int i = 0; i < 27250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27251(int& val) {
    func_in_27251(val);
    const int x = 27251;
    std::cout << 27251;
    for (int i = 0; i < 27251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27251(int& val) {
    const int x = 27251;
    std::cout << 27251;
    for (int i = 0; i < 27251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27252(int& val) {
    func_in_27252(val);
    const int x = 27252;
    std::cout << 27252;
    for (int i = 0; i < 27252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27252(int& val) {
    const int x = 27252;
    std::cout << 27252;
    for (int i = 0; i < 27252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27253(int& val) {
    func_in_27253(val);
    const int x = 27253;
    std::cout << 27253;
    for (int i = 0; i < 27253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27253(int& val) {
    const int x = 27253;
    std::cout << 27253;
    for (int i = 0; i < 27253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27254(int& val) {
    func_in_27254(val);
    const int x = 27254;
    std::cout << 27254;
    for (int i = 0; i < 27254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27254(int& val) {
    const int x = 27254;
    std::cout << 27254;
    for (int i = 0; i < 27254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27255(int& val) {
    func_in_27255(val);
    const int x = 27255;
    std::cout << 27255;
    for (int i = 0; i < 27255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27255(int& val) {
    const int x = 27255;
    std::cout << 27255;
    for (int i = 0; i < 27255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27256(int& val) {
    func_in_27256(val);
    const int x = 27256;
    std::cout << 27256;
    for (int i = 0; i < 27256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27256(int& val) {
    const int x = 27256;
    std::cout << 27256;
    for (int i = 0; i < 27256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27257(int& val) {
    func_in_27257(val);
    const int x = 27257;
    std::cout << 27257;
    for (int i = 0; i < 27257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27257(int& val) {
    const int x = 27257;
    std::cout << 27257;
    for (int i = 0; i < 27257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27258(int& val) {
    func_in_27258(val);
    const int x = 27258;
    std::cout << 27258;
    for (int i = 0; i < 27258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27258(int& val) {
    const int x = 27258;
    std::cout << 27258;
    for (int i = 0; i < 27258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27259(int& val) {
    func_in_27259(val);
    const int x = 27259;
    std::cout << 27259;
    for (int i = 0; i < 27259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27259(int& val) {
    const int x = 27259;
    std::cout << 27259;
    for (int i = 0; i < 27259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27260(int& val) {
    func_in_27260(val);
    const int x = 27260;
    std::cout << 27260;
    for (int i = 0; i < 27260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27260(int& val) {
    const int x = 27260;
    std::cout << 27260;
    for (int i = 0; i < 27260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27261(int& val) {
    func_in_27261(val);
    const int x = 27261;
    std::cout << 27261;
    for (int i = 0; i < 27261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27261(int& val) {
    const int x = 27261;
    std::cout << 27261;
    for (int i = 0; i < 27261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27262(int& val) {
    func_in_27262(val);
    const int x = 27262;
    std::cout << 27262;
    for (int i = 0; i < 27262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27262(int& val) {
    const int x = 27262;
    std::cout << 27262;
    for (int i = 0; i < 27262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27263(int& val) {
    func_in_27263(val);
    const int x = 27263;
    std::cout << 27263;
    for (int i = 0; i < 27263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27263(int& val) {
    const int x = 27263;
    std::cout << 27263;
    for (int i = 0; i < 27263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27264(int& val) {
    func_in_27264(val);
    const int x = 27264;
    std::cout << 27264;
    for (int i = 0; i < 27264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27264(int& val) {
    const int x = 27264;
    std::cout << 27264;
    for (int i = 0; i < 27264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27265(int& val) {
    func_in_27265(val);
    const int x = 27265;
    std::cout << 27265;
    for (int i = 0; i < 27265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27265(int& val) {
    const int x = 27265;
    std::cout << 27265;
    for (int i = 0; i < 27265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27266(int& val) {
    func_in_27266(val);
    const int x = 27266;
    std::cout << 27266;
    for (int i = 0; i < 27266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27266(int& val) {
    const int x = 27266;
    std::cout << 27266;
    for (int i = 0; i < 27266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27267(int& val) {
    func_in_27267(val);
    const int x = 27267;
    std::cout << 27267;
    for (int i = 0; i < 27267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27267(int& val) {
    const int x = 27267;
    std::cout << 27267;
    for (int i = 0; i < 27267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27268(int& val) {
    func_in_27268(val);
    const int x = 27268;
    std::cout << 27268;
    for (int i = 0; i < 27268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27268(int& val) {
    const int x = 27268;
    std::cout << 27268;
    for (int i = 0; i < 27268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27269(int& val) {
    func_in_27269(val);
    const int x = 27269;
    std::cout << 27269;
    for (int i = 0; i < 27269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27269(int& val) {
    const int x = 27269;
    std::cout << 27269;
    for (int i = 0; i < 27269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27270(int& val) {
    func_in_27270(val);
    const int x = 27270;
    std::cout << 27270;
    for (int i = 0; i < 27270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27270(int& val) {
    const int x = 27270;
    std::cout << 27270;
    for (int i = 0; i < 27270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27271(int& val) {
    func_in_27271(val);
    const int x = 27271;
    std::cout << 27271;
    for (int i = 0; i < 27271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27271(int& val) {
    const int x = 27271;
    std::cout << 27271;
    for (int i = 0; i < 27271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27272(int& val) {
    func_in_27272(val);
    const int x = 27272;
    std::cout << 27272;
    for (int i = 0; i < 27272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27272(int& val) {
    const int x = 27272;
    std::cout << 27272;
    for (int i = 0; i < 27272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27273(int& val) {
    func_in_27273(val);
    const int x = 27273;
    std::cout << 27273;
    for (int i = 0; i < 27273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27273(int& val) {
    const int x = 27273;
    std::cout << 27273;
    for (int i = 0; i < 27273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27274(int& val) {
    func_in_27274(val);
    const int x = 27274;
    std::cout << 27274;
    for (int i = 0; i < 27274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27274(int& val) {
    const int x = 27274;
    std::cout << 27274;
    for (int i = 0; i < 27274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27275(int& val) {
    func_in_27275(val);
    const int x = 27275;
    std::cout << 27275;
    for (int i = 0; i < 27275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27275(int& val) {
    const int x = 27275;
    std::cout << 27275;
    for (int i = 0; i < 27275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27276(int& val) {
    func_in_27276(val);
    const int x = 27276;
    std::cout << 27276;
    for (int i = 0; i < 27276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27276(int& val) {
    const int x = 27276;
    std::cout << 27276;
    for (int i = 0; i < 27276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27277(int& val) {
    func_in_27277(val);
    const int x = 27277;
    std::cout << 27277;
    for (int i = 0; i < 27277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27277(int& val) {
    const int x = 27277;
    std::cout << 27277;
    for (int i = 0; i < 27277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27278(int& val) {
    func_in_27278(val);
    const int x = 27278;
    std::cout << 27278;
    for (int i = 0; i < 27278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27278(int& val) {
    const int x = 27278;
    std::cout << 27278;
    for (int i = 0; i < 27278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27279(int& val) {
    func_in_27279(val);
    const int x = 27279;
    std::cout << 27279;
    for (int i = 0; i < 27279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27279(int& val) {
    const int x = 27279;
    std::cout << 27279;
    for (int i = 0; i < 27279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27280(int& val) {
    func_in_27280(val);
    const int x = 27280;
    std::cout << 27280;
    for (int i = 0; i < 27280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27280(int& val) {
    const int x = 27280;
    std::cout << 27280;
    for (int i = 0; i < 27280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27281(int& val) {
    func_in_27281(val);
    const int x = 27281;
    std::cout << 27281;
    for (int i = 0; i < 27281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27281(int& val) {
    const int x = 27281;
    std::cout << 27281;
    for (int i = 0; i < 27281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27282(int& val) {
    func_in_27282(val);
    const int x = 27282;
    std::cout << 27282;
    for (int i = 0; i < 27282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27282(int& val) {
    const int x = 27282;
    std::cout << 27282;
    for (int i = 0; i < 27282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27283(int& val) {
    func_in_27283(val);
    const int x = 27283;
    std::cout << 27283;
    for (int i = 0; i < 27283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27283(int& val) {
    const int x = 27283;
    std::cout << 27283;
    for (int i = 0; i < 27283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27284(int& val) {
    func_in_27284(val);
    const int x = 27284;
    std::cout << 27284;
    for (int i = 0; i < 27284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27284(int& val) {
    const int x = 27284;
    std::cout << 27284;
    for (int i = 0; i < 27284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27285(int& val) {
    func_in_27285(val);
    const int x = 27285;
    std::cout << 27285;
    for (int i = 0; i < 27285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27285(int& val) {
    const int x = 27285;
    std::cout << 27285;
    for (int i = 0; i < 27285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27286(int& val) {
    func_in_27286(val);
    const int x = 27286;
    std::cout << 27286;
    for (int i = 0; i < 27286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27286(int& val) {
    const int x = 27286;
    std::cout << 27286;
    for (int i = 0; i < 27286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27287(int& val) {
    func_in_27287(val);
    const int x = 27287;
    std::cout << 27287;
    for (int i = 0; i < 27287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27287(int& val) {
    const int x = 27287;
    std::cout << 27287;
    for (int i = 0; i < 27287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27288(int& val) {
    func_in_27288(val);
    const int x = 27288;
    std::cout << 27288;
    for (int i = 0; i < 27288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27288(int& val) {
    const int x = 27288;
    std::cout << 27288;
    for (int i = 0; i < 27288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27289(int& val) {
    func_in_27289(val);
    const int x = 27289;
    std::cout << 27289;
    for (int i = 0; i < 27289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27289(int& val) {
    const int x = 27289;
    std::cout << 27289;
    for (int i = 0; i < 27289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27290(int& val) {
    func_in_27290(val);
    const int x = 27290;
    std::cout << 27290;
    for (int i = 0; i < 27290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27290(int& val) {
    const int x = 27290;
    std::cout << 27290;
    for (int i = 0; i < 27290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27291(int& val) {
    func_in_27291(val);
    const int x = 27291;
    std::cout << 27291;
    for (int i = 0; i < 27291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27291(int& val) {
    const int x = 27291;
    std::cout << 27291;
    for (int i = 0; i < 27291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27292(int& val) {
    func_in_27292(val);
    const int x = 27292;
    std::cout << 27292;
    for (int i = 0; i < 27292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27292(int& val) {
    const int x = 27292;
    std::cout << 27292;
    for (int i = 0; i < 27292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27293(int& val) {
    func_in_27293(val);
    const int x = 27293;
    std::cout << 27293;
    for (int i = 0; i < 27293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27293(int& val) {
    const int x = 27293;
    std::cout << 27293;
    for (int i = 0; i < 27293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27294(int& val) {
    func_in_27294(val);
    const int x = 27294;
    std::cout << 27294;
    for (int i = 0; i < 27294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27294(int& val) {
    const int x = 27294;
    std::cout << 27294;
    for (int i = 0; i < 27294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27295(int& val) {
    func_in_27295(val);
    const int x = 27295;
    std::cout << 27295;
    for (int i = 0; i < 27295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27295(int& val) {
    const int x = 27295;
    std::cout << 27295;
    for (int i = 0; i < 27295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27296(int& val) {
    func_in_27296(val);
    const int x = 27296;
    std::cout << 27296;
    for (int i = 0; i < 27296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27296(int& val) {
    const int x = 27296;
    std::cout << 27296;
    for (int i = 0; i < 27296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27297(int& val) {
    func_in_27297(val);
    const int x = 27297;
    std::cout << 27297;
    for (int i = 0; i < 27297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27297(int& val) {
    const int x = 27297;
    std::cout << 27297;
    for (int i = 0; i < 27297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27298(int& val) {
    func_in_27298(val);
    const int x = 27298;
    std::cout << 27298;
    for (int i = 0; i < 27298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27298(int& val) {
    const int x = 27298;
    std::cout << 27298;
    for (int i = 0; i < 27298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27299(int& val) {
    func_in_27299(val);
    const int x = 27299;
    std::cout << 27299;
    for (int i = 0; i < 27299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27299(int& val) {
    const int x = 27299;
    std::cout << 27299;
    for (int i = 0; i < 27299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27300(int& val) {
    func_in_27300(val);
    const int x = 27300;
    std::cout << 27300;
    for (int i = 0; i < 27300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27300(int& val) {
    const int x = 27300;
    std::cout << 27300;
    for (int i = 0; i < 27300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27301(int& val) {
    func_in_27301(val);
    const int x = 27301;
    std::cout << 27301;
    for (int i = 0; i < 27301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27301(int& val) {
    const int x = 27301;
    std::cout << 27301;
    for (int i = 0; i < 27301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27302(int& val) {
    func_in_27302(val);
    const int x = 27302;
    std::cout << 27302;
    for (int i = 0; i < 27302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27302(int& val) {
    const int x = 27302;
    std::cout << 27302;
    for (int i = 0; i < 27302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27303(int& val) {
    func_in_27303(val);
    const int x = 27303;
    std::cout << 27303;
    for (int i = 0; i < 27303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27303(int& val) {
    const int x = 27303;
    std::cout << 27303;
    for (int i = 0; i < 27303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27304(int& val) {
    func_in_27304(val);
    const int x = 27304;
    std::cout << 27304;
    for (int i = 0; i < 27304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27304(int& val) {
    const int x = 27304;
    std::cout << 27304;
    for (int i = 0; i < 27304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27305(int& val) {
    func_in_27305(val);
    const int x = 27305;
    std::cout << 27305;
    for (int i = 0; i < 27305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27305(int& val) {
    const int x = 27305;
    std::cout << 27305;
    for (int i = 0; i < 27305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27306(int& val) {
    func_in_27306(val);
    const int x = 27306;
    std::cout << 27306;
    for (int i = 0; i < 27306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27306(int& val) {
    const int x = 27306;
    std::cout << 27306;
    for (int i = 0; i < 27306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27307(int& val) {
    func_in_27307(val);
    const int x = 27307;
    std::cout << 27307;
    for (int i = 0; i < 27307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27307(int& val) {
    const int x = 27307;
    std::cout << 27307;
    for (int i = 0; i < 27307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27308(int& val) {
    func_in_27308(val);
    const int x = 27308;
    std::cout << 27308;
    for (int i = 0; i < 27308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27308(int& val) {
    const int x = 27308;
    std::cout << 27308;
    for (int i = 0; i < 27308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27309(int& val) {
    func_in_27309(val);
    const int x = 27309;
    std::cout << 27309;
    for (int i = 0; i < 27309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27309(int& val) {
    const int x = 27309;
    std::cout << 27309;
    for (int i = 0; i < 27309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27310(int& val) {
    func_in_27310(val);
    const int x = 27310;
    std::cout << 27310;
    for (int i = 0; i < 27310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27310(int& val) {
    const int x = 27310;
    std::cout << 27310;
    for (int i = 0; i < 27310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27311(int& val) {
    func_in_27311(val);
    const int x = 27311;
    std::cout << 27311;
    for (int i = 0; i < 27311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27311(int& val) {
    const int x = 27311;
    std::cout << 27311;
    for (int i = 0; i < 27311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27312(int& val) {
    func_in_27312(val);
    const int x = 27312;
    std::cout << 27312;
    for (int i = 0; i < 27312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27312(int& val) {
    const int x = 27312;
    std::cout << 27312;
    for (int i = 0; i < 27312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27313(int& val) {
    func_in_27313(val);
    const int x = 27313;
    std::cout << 27313;
    for (int i = 0; i < 27313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27313(int& val) {
    const int x = 27313;
    std::cout << 27313;
    for (int i = 0; i < 27313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27314(int& val) {
    func_in_27314(val);
    const int x = 27314;
    std::cout << 27314;
    for (int i = 0; i < 27314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27314(int& val) {
    const int x = 27314;
    std::cout << 27314;
    for (int i = 0; i < 27314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27315(int& val) {
    func_in_27315(val);
    const int x = 27315;
    std::cout << 27315;
    for (int i = 0; i < 27315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27315(int& val) {
    const int x = 27315;
    std::cout << 27315;
    for (int i = 0; i < 27315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27316(int& val) {
    func_in_27316(val);
    const int x = 27316;
    std::cout << 27316;
    for (int i = 0; i < 27316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27316(int& val) {
    const int x = 27316;
    std::cout << 27316;
    for (int i = 0; i < 27316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27317(int& val) {
    func_in_27317(val);
    const int x = 27317;
    std::cout << 27317;
    for (int i = 0; i < 27317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27317(int& val) {
    const int x = 27317;
    std::cout << 27317;
    for (int i = 0; i < 27317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27318(int& val) {
    func_in_27318(val);
    const int x = 27318;
    std::cout << 27318;
    for (int i = 0; i < 27318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27318(int& val) {
    const int x = 27318;
    std::cout << 27318;
    for (int i = 0; i < 27318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27319(int& val) {
    func_in_27319(val);
    const int x = 27319;
    std::cout << 27319;
    for (int i = 0; i < 27319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27319(int& val) {
    const int x = 27319;
    std::cout << 27319;
    for (int i = 0; i < 27319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27320(int& val) {
    func_in_27320(val);
    const int x = 27320;
    std::cout << 27320;
    for (int i = 0; i < 27320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27320(int& val) {
    const int x = 27320;
    std::cout << 27320;
    for (int i = 0; i < 27320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27321(int& val) {
    func_in_27321(val);
    const int x = 27321;
    std::cout << 27321;
    for (int i = 0; i < 27321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27321(int& val) {
    const int x = 27321;
    std::cout << 27321;
    for (int i = 0; i < 27321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27322(int& val) {
    func_in_27322(val);
    const int x = 27322;
    std::cout << 27322;
    for (int i = 0; i < 27322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27322(int& val) {
    const int x = 27322;
    std::cout << 27322;
    for (int i = 0; i < 27322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27323(int& val) {
    func_in_27323(val);
    const int x = 27323;
    std::cout << 27323;
    for (int i = 0; i < 27323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27323(int& val) {
    const int x = 27323;
    std::cout << 27323;
    for (int i = 0; i < 27323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27324(int& val) {
    func_in_27324(val);
    const int x = 27324;
    std::cout << 27324;
    for (int i = 0; i < 27324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27324(int& val) {
    const int x = 27324;
    std::cout << 27324;
    for (int i = 0; i < 27324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27325(int& val) {
    func_in_27325(val);
    const int x = 27325;
    std::cout << 27325;
    for (int i = 0; i < 27325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27325(int& val) {
    const int x = 27325;
    std::cout << 27325;
    for (int i = 0; i < 27325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27326(int& val) {
    func_in_27326(val);
    const int x = 27326;
    std::cout << 27326;
    for (int i = 0; i < 27326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27326(int& val) {
    const int x = 27326;
    std::cout << 27326;
    for (int i = 0; i < 27326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27327(int& val) {
    func_in_27327(val);
    const int x = 27327;
    std::cout << 27327;
    for (int i = 0; i < 27327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27327(int& val) {
    const int x = 27327;
    std::cout << 27327;
    for (int i = 0; i < 27327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27328(int& val) {
    func_in_27328(val);
    const int x = 27328;
    std::cout << 27328;
    for (int i = 0; i < 27328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27328(int& val) {
    const int x = 27328;
    std::cout << 27328;
    for (int i = 0; i < 27328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27329(int& val) {
    func_in_27329(val);
    const int x = 27329;
    std::cout << 27329;
    for (int i = 0; i < 27329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27329(int& val) {
    const int x = 27329;
    std::cout << 27329;
    for (int i = 0; i < 27329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27330(int& val) {
    func_in_27330(val);
    const int x = 27330;
    std::cout << 27330;
    for (int i = 0; i < 27330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27330(int& val) {
    const int x = 27330;
    std::cout << 27330;
    for (int i = 0; i < 27330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27331(int& val) {
    func_in_27331(val);
    const int x = 27331;
    std::cout << 27331;
    for (int i = 0; i < 27331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27331(int& val) {
    const int x = 27331;
    std::cout << 27331;
    for (int i = 0; i < 27331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27332(int& val) {
    func_in_27332(val);
    const int x = 27332;
    std::cout << 27332;
    for (int i = 0; i < 27332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27332(int& val) {
    const int x = 27332;
    std::cout << 27332;
    for (int i = 0; i < 27332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27333(int& val) {
    func_in_27333(val);
    const int x = 27333;
    std::cout << 27333;
    for (int i = 0; i < 27333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27333(int& val) {
    const int x = 27333;
    std::cout << 27333;
    for (int i = 0; i < 27333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27334(int& val) {
    func_in_27334(val);
    const int x = 27334;
    std::cout << 27334;
    for (int i = 0; i < 27334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27334(int& val) {
    const int x = 27334;
    std::cout << 27334;
    for (int i = 0; i < 27334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27335(int& val) {
    func_in_27335(val);
    const int x = 27335;
    std::cout << 27335;
    for (int i = 0; i < 27335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27335(int& val) {
    const int x = 27335;
    std::cout << 27335;
    for (int i = 0; i < 27335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27336(int& val) {
    func_in_27336(val);
    const int x = 27336;
    std::cout << 27336;
    for (int i = 0; i < 27336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27336(int& val) {
    const int x = 27336;
    std::cout << 27336;
    for (int i = 0; i < 27336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27337(int& val) {
    func_in_27337(val);
    const int x = 27337;
    std::cout << 27337;
    for (int i = 0; i < 27337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27337(int& val) {
    const int x = 27337;
    std::cout << 27337;
    for (int i = 0; i < 27337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27338(int& val) {
    func_in_27338(val);
    const int x = 27338;
    std::cout << 27338;
    for (int i = 0; i < 27338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27338(int& val) {
    const int x = 27338;
    std::cout << 27338;
    for (int i = 0; i < 27338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27339(int& val) {
    func_in_27339(val);
    const int x = 27339;
    std::cout << 27339;
    for (int i = 0; i < 27339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27339(int& val) {
    const int x = 27339;
    std::cout << 27339;
    for (int i = 0; i < 27339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27340(int& val) {
    func_in_27340(val);
    const int x = 27340;
    std::cout << 27340;
    for (int i = 0; i < 27340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27340(int& val) {
    const int x = 27340;
    std::cout << 27340;
    for (int i = 0; i < 27340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27341(int& val) {
    func_in_27341(val);
    const int x = 27341;
    std::cout << 27341;
    for (int i = 0; i < 27341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27341(int& val) {
    const int x = 27341;
    std::cout << 27341;
    for (int i = 0; i < 27341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27342(int& val) {
    func_in_27342(val);
    const int x = 27342;
    std::cout << 27342;
    for (int i = 0; i < 27342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27342(int& val) {
    const int x = 27342;
    std::cout << 27342;
    for (int i = 0; i < 27342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27343(int& val) {
    func_in_27343(val);
    const int x = 27343;
    std::cout << 27343;
    for (int i = 0; i < 27343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27343(int& val) {
    const int x = 27343;
    std::cout << 27343;
    for (int i = 0; i < 27343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27344(int& val) {
    func_in_27344(val);
    const int x = 27344;
    std::cout << 27344;
    for (int i = 0; i < 27344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27344(int& val) {
    const int x = 27344;
    std::cout << 27344;
    for (int i = 0; i < 27344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27345(int& val) {
    func_in_27345(val);
    const int x = 27345;
    std::cout << 27345;
    for (int i = 0; i < 27345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27345(int& val) {
    const int x = 27345;
    std::cout << 27345;
    for (int i = 0; i < 27345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27346(int& val) {
    func_in_27346(val);
    const int x = 27346;
    std::cout << 27346;
    for (int i = 0; i < 27346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27346(int& val) {
    const int x = 27346;
    std::cout << 27346;
    for (int i = 0; i < 27346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27347(int& val) {
    func_in_27347(val);
    const int x = 27347;
    std::cout << 27347;
    for (int i = 0; i < 27347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27347(int& val) {
    const int x = 27347;
    std::cout << 27347;
    for (int i = 0; i < 27347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27348(int& val) {
    func_in_27348(val);
    const int x = 27348;
    std::cout << 27348;
    for (int i = 0; i < 27348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27348(int& val) {
    const int x = 27348;
    std::cout << 27348;
    for (int i = 0; i < 27348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27349(int& val) {
    func_in_27349(val);
    const int x = 27349;
    std::cout << 27349;
    for (int i = 0; i < 27349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27349(int& val) {
    const int x = 27349;
    std::cout << 27349;
    for (int i = 0; i < 27349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27350(int& val) {
    func_in_27350(val);
    const int x = 27350;
    std::cout << 27350;
    for (int i = 0; i < 27350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27350(int& val) {
    const int x = 27350;
    std::cout << 27350;
    for (int i = 0; i < 27350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27351(int& val) {
    func_in_27351(val);
    const int x = 27351;
    std::cout << 27351;
    for (int i = 0; i < 27351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27351(int& val) {
    const int x = 27351;
    std::cout << 27351;
    for (int i = 0; i < 27351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27352(int& val) {
    func_in_27352(val);
    const int x = 27352;
    std::cout << 27352;
    for (int i = 0; i < 27352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27352(int& val) {
    const int x = 27352;
    std::cout << 27352;
    for (int i = 0; i < 27352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27353(int& val) {
    func_in_27353(val);
    const int x = 27353;
    std::cout << 27353;
    for (int i = 0; i < 27353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27353(int& val) {
    const int x = 27353;
    std::cout << 27353;
    for (int i = 0; i < 27353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27354(int& val) {
    func_in_27354(val);
    const int x = 27354;
    std::cout << 27354;
    for (int i = 0; i < 27354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27354(int& val) {
    const int x = 27354;
    std::cout << 27354;
    for (int i = 0; i < 27354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27355(int& val) {
    func_in_27355(val);
    const int x = 27355;
    std::cout << 27355;
    for (int i = 0; i < 27355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27355(int& val) {
    const int x = 27355;
    std::cout << 27355;
    for (int i = 0; i < 27355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27356(int& val) {
    func_in_27356(val);
    const int x = 27356;
    std::cout << 27356;
    for (int i = 0; i < 27356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27356(int& val) {
    const int x = 27356;
    std::cout << 27356;
    for (int i = 0; i < 27356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27357(int& val) {
    func_in_27357(val);
    const int x = 27357;
    std::cout << 27357;
    for (int i = 0; i < 27357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27357(int& val) {
    const int x = 27357;
    std::cout << 27357;
    for (int i = 0; i < 27357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27358(int& val) {
    func_in_27358(val);
    const int x = 27358;
    std::cout << 27358;
    for (int i = 0; i < 27358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27358(int& val) {
    const int x = 27358;
    std::cout << 27358;
    for (int i = 0; i < 27358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27359(int& val) {
    func_in_27359(val);
    const int x = 27359;
    std::cout << 27359;
    for (int i = 0; i < 27359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27359(int& val) {
    const int x = 27359;
    std::cout << 27359;
    for (int i = 0; i < 27359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27360(int& val) {
    func_in_27360(val);
    const int x = 27360;
    std::cout << 27360;
    for (int i = 0; i < 27360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27360(int& val) {
    const int x = 27360;
    std::cout << 27360;
    for (int i = 0; i < 27360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27361(int& val) {
    func_in_27361(val);
    const int x = 27361;
    std::cout << 27361;
    for (int i = 0; i < 27361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27361(int& val) {
    const int x = 27361;
    std::cout << 27361;
    for (int i = 0; i < 27361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27362(int& val) {
    func_in_27362(val);
    const int x = 27362;
    std::cout << 27362;
    for (int i = 0; i < 27362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27362(int& val) {
    const int x = 27362;
    std::cout << 27362;
    for (int i = 0; i < 27362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27363(int& val) {
    func_in_27363(val);
    const int x = 27363;
    std::cout << 27363;
    for (int i = 0; i < 27363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27363(int& val) {
    const int x = 27363;
    std::cout << 27363;
    for (int i = 0; i < 27363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27364(int& val) {
    func_in_27364(val);
    const int x = 27364;
    std::cout << 27364;
    for (int i = 0; i < 27364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27364(int& val) {
    const int x = 27364;
    std::cout << 27364;
    for (int i = 0; i < 27364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27365(int& val) {
    func_in_27365(val);
    const int x = 27365;
    std::cout << 27365;
    for (int i = 0; i < 27365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27365(int& val) {
    const int x = 27365;
    std::cout << 27365;
    for (int i = 0; i < 27365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27366(int& val) {
    func_in_27366(val);
    const int x = 27366;
    std::cout << 27366;
    for (int i = 0; i < 27366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27366(int& val) {
    const int x = 27366;
    std::cout << 27366;
    for (int i = 0; i < 27366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27367(int& val) {
    func_in_27367(val);
    const int x = 27367;
    std::cout << 27367;
    for (int i = 0; i < 27367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27367(int& val) {
    const int x = 27367;
    std::cout << 27367;
    for (int i = 0; i < 27367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27368(int& val) {
    func_in_27368(val);
    const int x = 27368;
    std::cout << 27368;
    for (int i = 0; i < 27368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27368(int& val) {
    const int x = 27368;
    std::cout << 27368;
    for (int i = 0; i < 27368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27369(int& val) {
    func_in_27369(val);
    const int x = 27369;
    std::cout << 27369;
    for (int i = 0; i < 27369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27369(int& val) {
    const int x = 27369;
    std::cout << 27369;
    for (int i = 0; i < 27369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27370(int& val) {
    func_in_27370(val);
    const int x = 27370;
    std::cout << 27370;
    for (int i = 0; i < 27370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27370(int& val) {
    const int x = 27370;
    std::cout << 27370;
    for (int i = 0; i < 27370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27371(int& val) {
    func_in_27371(val);
    const int x = 27371;
    std::cout << 27371;
    for (int i = 0; i < 27371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27371(int& val) {
    const int x = 27371;
    std::cout << 27371;
    for (int i = 0; i < 27371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27372(int& val) {
    func_in_27372(val);
    const int x = 27372;
    std::cout << 27372;
    for (int i = 0; i < 27372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27372(int& val) {
    const int x = 27372;
    std::cout << 27372;
    for (int i = 0; i < 27372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27373(int& val) {
    func_in_27373(val);
    const int x = 27373;
    std::cout << 27373;
    for (int i = 0; i < 27373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27373(int& val) {
    const int x = 27373;
    std::cout << 27373;
    for (int i = 0; i < 27373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27374(int& val) {
    func_in_27374(val);
    const int x = 27374;
    std::cout << 27374;
    for (int i = 0; i < 27374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27374(int& val) {
    const int x = 27374;
    std::cout << 27374;
    for (int i = 0; i < 27374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27375(int& val) {
    func_in_27375(val);
    const int x = 27375;
    std::cout << 27375;
    for (int i = 0; i < 27375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27375(int& val) {
    const int x = 27375;
    std::cout << 27375;
    for (int i = 0; i < 27375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27376(int& val) {
    func_in_27376(val);
    const int x = 27376;
    std::cout << 27376;
    for (int i = 0; i < 27376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27376(int& val) {
    const int x = 27376;
    std::cout << 27376;
    for (int i = 0; i < 27376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27377(int& val) {
    func_in_27377(val);
    const int x = 27377;
    std::cout << 27377;
    for (int i = 0; i < 27377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27377(int& val) {
    const int x = 27377;
    std::cout << 27377;
    for (int i = 0; i < 27377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27378(int& val) {
    func_in_27378(val);
    const int x = 27378;
    std::cout << 27378;
    for (int i = 0; i < 27378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27378(int& val) {
    const int x = 27378;
    std::cout << 27378;
    for (int i = 0; i < 27378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27379(int& val) {
    func_in_27379(val);
    const int x = 27379;
    std::cout << 27379;
    for (int i = 0; i < 27379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27379(int& val) {
    const int x = 27379;
    std::cout << 27379;
    for (int i = 0; i < 27379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27380(int& val) {
    func_in_27380(val);
    const int x = 27380;
    std::cout << 27380;
    for (int i = 0; i < 27380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27380(int& val) {
    const int x = 27380;
    std::cout << 27380;
    for (int i = 0; i < 27380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27381(int& val) {
    func_in_27381(val);
    const int x = 27381;
    std::cout << 27381;
    for (int i = 0; i < 27381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27381(int& val) {
    const int x = 27381;
    std::cout << 27381;
    for (int i = 0; i < 27381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27382(int& val) {
    func_in_27382(val);
    const int x = 27382;
    std::cout << 27382;
    for (int i = 0; i < 27382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27382(int& val) {
    const int x = 27382;
    std::cout << 27382;
    for (int i = 0; i < 27382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27383(int& val) {
    func_in_27383(val);
    const int x = 27383;
    std::cout << 27383;
    for (int i = 0; i < 27383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27383(int& val) {
    const int x = 27383;
    std::cout << 27383;
    for (int i = 0; i < 27383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27384(int& val) {
    func_in_27384(val);
    const int x = 27384;
    std::cout << 27384;
    for (int i = 0; i < 27384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27384(int& val) {
    const int x = 27384;
    std::cout << 27384;
    for (int i = 0; i < 27384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27385(int& val) {
    func_in_27385(val);
    const int x = 27385;
    std::cout << 27385;
    for (int i = 0; i < 27385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27385(int& val) {
    const int x = 27385;
    std::cout << 27385;
    for (int i = 0; i < 27385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27386(int& val) {
    func_in_27386(val);
    const int x = 27386;
    std::cout << 27386;
    for (int i = 0; i < 27386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27386(int& val) {
    const int x = 27386;
    std::cout << 27386;
    for (int i = 0; i < 27386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27387(int& val) {
    func_in_27387(val);
    const int x = 27387;
    std::cout << 27387;
    for (int i = 0; i < 27387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27387(int& val) {
    const int x = 27387;
    std::cout << 27387;
    for (int i = 0; i < 27387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27388(int& val) {
    func_in_27388(val);
    const int x = 27388;
    std::cout << 27388;
    for (int i = 0; i < 27388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27388(int& val) {
    const int x = 27388;
    std::cout << 27388;
    for (int i = 0; i < 27388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27389(int& val) {
    func_in_27389(val);
    const int x = 27389;
    std::cout << 27389;
    for (int i = 0; i < 27389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27389(int& val) {
    const int x = 27389;
    std::cout << 27389;
    for (int i = 0; i < 27389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27390(int& val) {
    func_in_27390(val);
    const int x = 27390;
    std::cout << 27390;
    for (int i = 0; i < 27390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27390(int& val) {
    const int x = 27390;
    std::cout << 27390;
    for (int i = 0; i < 27390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27391(int& val) {
    func_in_27391(val);
    const int x = 27391;
    std::cout << 27391;
    for (int i = 0; i < 27391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27391(int& val) {
    const int x = 27391;
    std::cout << 27391;
    for (int i = 0; i < 27391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27392(int& val) {
    func_in_27392(val);
    const int x = 27392;
    std::cout << 27392;
    for (int i = 0; i < 27392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27392(int& val) {
    const int x = 27392;
    std::cout << 27392;
    for (int i = 0; i < 27392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27393(int& val) {
    func_in_27393(val);
    const int x = 27393;
    std::cout << 27393;
    for (int i = 0; i < 27393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27393(int& val) {
    const int x = 27393;
    std::cout << 27393;
    for (int i = 0; i < 27393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27394(int& val) {
    func_in_27394(val);
    const int x = 27394;
    std::cout << 27394;
    for (int i = 0; i < 27394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27394(int& val) {
    const int x = 27394;
    std::cout << 27394;
    for (int i = 0; i < 27394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27395(int& val) {
    func_in_27395(val);
    const int x = 27395;
    std::cout << 27395;
    for (int i = 0; i < 27395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27395(int& val) {
    const int x = 27395;
    std::cout << 27395;
    for (int i = 0; i < 27395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27396(int& val) {
    func_in_27396(val);
    const int x = 27396;
    std::cout << 27396;
    for (int i = 0; i < 27396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27396(int& val) {
    const int x = 27396;
    std::cout << 27396;
    for (int i = 0; i < 27396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27397(int& val) {
    func_in_27397(val);
    const int x = 27397;
    std::cout << 27397;
    for (int i = 0; i < 27397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27397(int& val) {
    const int x = 27397;
    std::cout << 27397;
    for (int i = 0; i < 27397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27398(int& val) {
    func_in_27398(val);
    const int x = 27398;
    std::cout << 27398;
    for (int i = 0; i < 27398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27398(int& val) {
    const int x = 27398;
    std::cout << 27398;
    for (int i = 0; i < 27398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27399(int& val) {
    func_in_27399(val);
    const int x = 27399;
    std::cout << 27399;
    for (int i = 0; i < 27399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27399(int& val) {
    const int x = 27399;
    std::cout << 27399;
    for (int i = 0; i < 27399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27400(int& val) {
    func_in_27400(val);
    const int x = 27400;
    std::cout << 27400;
    for (int i = 0; i < 27400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27400(int& val) {
    const int x = 27400;
    std::cout << 27400;
    for (int i = 0; i < 27400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27401(int& val) {
    func_in_27401(val);
    const int x = 27401;
    std::cout << 27401;
    for (int i = 0; i < 27401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27401(int& val) {
    const int x = 27401;
    std::cout << 27401;
    for (int i = 0; i < 27401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27402(int& val) {
    func_in_27402(val);
    const int x = 27402;
    std::cout << 27402;
    for (int i = 0; i < 27402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27402(int& val) {
    const int x = 27402;
    std::cout << 27402;
    for (int i = 0; i < 27402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27403(int& val) {
    func_in_27403(val);
    const int x = 27403;
    std::cout << 27403;
    for (int i = 0; i < 27403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27403(int& val) {
    const int x = 27403;
    std::cout << 27403;
    for (int i = 0; i < 27403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27404(int& val) {
    func_in_27404(val);
    const int x = 27404;
    std::cout << 27404;
    for (int i = 0; i < 27404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27404(int& val) {
    const int x = 27404;
    std::cout << 27404;
    for (int i = 0; i < 27404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27405(int& val) {
    func_in_27405(val);
    const int x = 27405;
    std::cout << 27405;
    for (int i = 0; i < 27405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27405(int& val) {
    const int x = 27405;
    std::cout << 27405;
    for (int i = 0; i < 27405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27406(int& val) {
    func_in_27406(val);
    const int x = 27406;
    std::cout << 27406;
    for (int i = 0; i < 27406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27406(int& val) {
    const int x = 27406;
    std::cout << 27406;
    for (int i = 0; i < 27406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27407(int& val) {
    func_in_27407(val);
    const int x = 27407;
    std::cout << 27407;
    for (int i = 0; i < 27407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27407(int& val) {
    const int x = 27407;
    std::cout << 27407;
    for (int i = 0; i < 27407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27408(int& val) {
    func_in_27408(val);
    const int x = 27408;
    std::cout << 27408;
    for (int i = 0; i < 27408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27408(int& val) {
    const int x = 27408;
    std::cout << 27408;
    for (int i = 0; i < 27408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27409(int& val) {
    func_in_27409(val);
    const int x = 27409;
    std::cout << 27409;
    for (int i = 0; i < 27409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27409(int& val) {
    const int x = 27409;
    std::cout << 27409;
    for (int i = 0; i < 27409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27410(int& val) {
    func_in_27410(val);
    const int x = 27410;
    std::cout << 27410;
    for (int i = 0; i < 27410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27410(int& val) {
    const int x = 27410;
    std::cout << 27410;
    for (int i = 0; i < 27410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27411(int& val) {
    func_in_27411(val);
    const int x = 27411;
    std::cout << 27411;
    for (int i = 0; i < 27411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27411(int& val) {
    const int x = 27411;
    std::cout << 27411;
    for (int i = 0; i < 27411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27412(int& val) {
    func_in_27412(val);
    const int x = 27412;
    std::cout << 27412;
    for (int i = 0; i < 27412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27412(int& val) {
    const int x = 27412;
    std::cout << 27412;
    for (int i = 0; i < 27412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27413(int& val) {
    func_in_27413(val);
    const int x = 27413;
    std::cout << 27413;
    for (int i = 0; i < 27413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27413(int& val) {
    const int x = 27413;
    std::cout << 27413;
    for (int i = 0; i < 27413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27414(int& val) {
    func_in_27414(val);
    const int x = 27414;
    std::cout << 27414;
    for (int i = 0; i < 27414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27414(int& val) {
    const int x = 27414;
    std::cout << 27414;
    for (int i = 0; i < 27414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27415(int& val) {
    func_in_27415(val);
    const int x = 27415;
    std::cout << 27415;
    for (int i = 0; i < 27415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27415(int& val) {
    const int x = 27415;
    std::cout << 27415;
    for (int i = 0; i < 27415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27416(int& val) {
    func_in_27416(val);
    const int x = 27416;
    std::cout << 27416;
    for (int i = 0; i < 27416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27416(int& val) {
    const int x = 27416;
    std::cout << 27416;
    for (int i = 0; i < 27416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27417(int& val) {
    func_in_27417(val);
    const int x = 27417;
    std::cout << 27417;
    for (int i = 0; i < 27417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27417(int& val) {
    const int x = 27417;
    std::cout << 27417;
    for (int i = 0; i < 27417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27418(int& val) {
    func_in_27418(val);
    const int x = 27418;
    std::cout << 27418;
    for (int i = 0; i < 27418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27418(int& val) {
    const int x = 27418;
    std::cout << 27418;
    for (int i = 0; i < 27418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27419(int& val) {
    func_in_27419(val);
    const int x = 27419;
    std::cout << 27419;
    for (int i = 0; i < 27419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27419(int& val) {
    const int x = 27419;
    std::cout << 27419;
    for (int i = 0; i < 27419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27420(int& val) {
    func_in_27420(val);
    const int x = 27420;
    std::cout << 27420;
    for (int i = 0; i < 27420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27420(int& val) {
    const int x = 27420;
    std::cout << 27420;
    for (int i = 0; i < 27420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27421(int& val) {
    func_in_27421(val);
    const int x = 27421;
    std::cout << 27421;
    for (int i = 0; i < 27421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27421(int& val) {
    const int x = 27421;
    std::cout << 27421;
    for (int i = 0; i < 27421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27422(int& val) {
    func_in_27422(val);
    const int x = 27422;
    std::cout << 27422;
    for (int i = 0; i < 27422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27422(int& val) {
    const int x = 27422;
    std::cout << 27422;
    for (int i = 0; i < 27422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27423(int& val) {
    func_in_27423(val);
    const int x = 27423;
    std::cout << 27423;
    for (int i = 0; i < 27423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27423(int& val) {
    const int x = 27423;
    std::cout << 27423;
    for (int i = 0; i < 27423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27424(int& val) {
    func_in_27424(val);
    const int x = 27424;
    std::cout << 27424;
    for (int i = 0; i < 27424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27424(int& val) {
    const int x = 27424;
    std::cout << 27424;
    for (int i = 0; i < 27424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27425(int& val) {
    func_in_27425(val);
    const int x = 27425;
    std::cout << 27425;
    for (int i = 0; i < 27425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27425(int& val) {
    const int x = 27425;
    std::cout << 27425;
    for (int i = 0; i < 27425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27426(int& val) {
    func_in_27426(val);
    const int x = 27426;
    std::cout << 27426;
    for (int i = 0; i < 27426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27426(int& val) {
    const int x = 27426;
    std::cout << 27426;
    for (int i = 0; i < 27426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27427(int& val) {
    func_in_27427(val);
    const int x = 27427;
    std::cout << 27427;
    for (int i = 0; i < 27427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27427(int& val) {
    const int x = 27427;
    std::cout << 27427;
    for (int i = 0; i < 27427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27428(int& val) {
    func_in_27428(val);
    const int x = 27428;
    std::cout << 27428;
    for (int i = 0; i < 27428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27428(int& val) {
    const int x = 27428;
    std::cout << 27428;
    for (int i = 0; i < 27428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27429(int& val) {
    func_in_27429(val);
    const int x = 27429;
    std::cout << 27429;
    for (int i = 0; i < 27429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27429(int& val) {
    const int x = 27429;
    std::cout << 27429;
    for (int i = 0; i < 27429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27430(int& val) {
    func_in_27430(val);
    const int x = 27430;
    std::cout << 27430;
    for (int i = 0; i < 27430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27430(int& val) {
    const int x = 27430;
    std::cout << 27430;
    for (int i = 0; i < 27430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27431(int& val) {
    func_in_27431(val);
    const int x = 27431;
    std::cout << 27431;
    for (int i = 0; i < 27431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27431(int& val) {
    const int x = 27431;
    std::cout << 27431;
    for (int i = 0; i < 27431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27432(int& val) {
    func_in_27432(val);
    const int x = 27432;
    std::cout << 27432;
    for (int i = 0; i < 27432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27432(int& val) {
    const int x = 27432;
    std::cout << 27432;
    for (int i = 0; i < 27432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27433(int& val) {
    func_in_27433(val);
    const int x = 27433;
    std::cout << 27433;
    for (int i = 0; i < 27433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27433(int& val) {
    const int x = 27433;
    std::cout << 27433;
    for (int i = 0; i < 27433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27434(int& val) {
    func_in_27434(val);
    const int x = 27434;
    std::cout << 27434;
    for (int i = 0; i < 27434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27434(int& val) {
    const int x = 27434;
    std::cout << 27434;
    for (int i = 0; i < 27434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27435(int& val) {
    func_in_27435(val);
    const int x = 27435;
    std::cout << 27435;
    for (int i = 0; i < 27435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27435(int& val) {
    const int x = 27435;
    std::cout << 27435;
    for (int i = 0; i < 27435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27436(int& val) {
    func_in_27436(val);
    const int x = 27436;
    std::cout << 27436;
    for (int i = 0; i < 27436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27436(int& val) {
    const int x = 27436;
    std::cout << 27436;
    for (int i = 0; i < 27436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27437(int& val) {
    func_in_27437(val);
    const int x = 27437;
    std::cout << 27437;
    for (int i = 0; i < 27437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27437(int& val) {
    const int x = 27437;
    std::cout << 27437;
    for (int i = 0; i < 27437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27438(int& val) {
    func_in_27438(val);
    const int x = 27438;
    std::cout << 27438;
    for (int i = 0; i < 27438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27438(int& val) {
    const int x = 27438;
    std::cout << 27438;
    for (int i = 0; i < 27438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27439(int& val) {
    func_in_27439(val);
    const int x = 27439;
    std::cout << 27439;
    for (int i = 0; i < 27439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27439(int& val) {
    const int x = 27439;
    std::cout << 27439;
    for (int i = 0; i < 27439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27440(int& val) {
    func_in_27440(val);
    const int x = 27440;
    std::cout << 27440;
    for (int i = 0; i < 27440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27440(int& val) {
    const int x = 27440;
    std::cout << 27440;
    for (int i = 0; i < 27440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27441(int& val) {
    func_in_27441(val);
    const int x = 27441;
    std::cout << 27441;
    for (int i = 0; i < 27441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27441(int& val) {
    const int x = 27441;
    std::cout << 27441;
    for (int i = 0; i < 27441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27442(int& val) {
    func_in_27442(val);
    const int x = 27442;
    std::cout << 27442;
    for (int i = 0; i < 27442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27442(int& val) {
    const int x = 27442;
    std::cout << 27442;
    for (int i = 0; i < 27442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27443(int& val) {
    func_in_27443(val);
    const int x = 27443;
    std::cout << 27443;
    for (int i = 0; i < 27443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27443(int& val) {
    const int x = 27443;
    std::cout << 27443;
    for (int i = 0; i < 27443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27444(int& val) {
    func_in_27444(val);
    const int x = 27444;
    std::cout << 27444;
    for (int i = 0; i < 27444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27444(int& val) {
    const int x = 27444;
    std::cout << 27444;
    for (int i = 0; i < 27444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27445(int& val) {
    func_in_27445(val);
    const int x = 27445;
    std::cout << 27445;
    for (int i = 0; i < 27445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27445(int& val) {
    const int x = 27445;
    std::cout << 27445;
    for (int i = 0; i < 27445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27446(int& val) {
    func_in_27446(val);
    const int x = 27446;
    std::cout << 27446;
    for (int i = 0; i < 27446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27446(int& val) {
    const int x = 27446;
    std::cout << 27446;
    for (int i = 0; i < 27446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27447(int& val) {
    func_in_27447(val);
    const int x = 27447;
    std::cout << 27447;
    for (int i = 0; i < 27447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27447(int& val) {
    const int x = 27447;
    std::cout << 27447;
    for (int i = 0; i < 27447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27448(int& val) {
    func_in_27448(val);
    const int x = 27448;
    std::cout << 27448;
    for (int i = 0; i < 27448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27448(int& val) {
    const int x = 27448;
    std::cout << 27448;
    for (int i = 0; i < 27448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27449(int& val) {
    func_in_27449(val);
    const int x = 27449;
    std::cout << 27449;
    for (int i = 0; i < 27449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27449(int& val) {
    const int x = 27449;
    std::cout << 27449;
    for (int i = 0; i < 27449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27450(int& val) {
    func_in_27450(val);
    const int x = 27450;
    std::cout << 27450;
    for (int i = 0; i < 27450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27450(int& val) {
    const int x = 27450;
    std::cout << 27450;
    for (int i = 0; i < 27450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27451(int& val) {
    func_in_27451(val);
    const int x = 27451;
    std::cout << 27451;
    for (int i = 0; i < 27451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27451(int& val) {
    const int x = 27451;
    std::cout << 27451;
    for (int i = 0; i < 27451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27452(int& val) {
    func_in_27452(val);
    const int x = 27452;
    std::cout << 27452;
    for (int i = 0; i < 27452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27452(int& val) {
    const int x = 27452;
    std::cout << 27452;
    for (int i = 0; i < 27452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27453(int& val) {
    func_in_27453(val);
    const int x = 27453;
    std::cout << 27453;
    for (int i = 0; i < 27453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27453(int& val) {
    const int x = 27453;
    std::cout << 27453;
    for (int i = 0; i < 27453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27454(int& val) {
    func_in_27454(val);
    const int x = 27454;
    std::cout << 27454;
    for (int i = 0; i < 27454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27454(int& val) {
    const int x = 27454;
    std::cout << 27454;
    for (int i = 0; i < 27454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27455(int& val) {
    func_in_27455(val);
    const int x = 27455;
    std::cout << 27455;
    for (int i = 0; i < 27455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27455(int& val) {
    const int x = 27455;
    std::cout << 27455;
    for (int i = 0; i < 27455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27456(int& val) {
    func_in_27456(val);
    const int x = 27456;
    std::cout << 27456;
    for (int i = 0; i < 27456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27456(int& val) {
    const int x = 27456;
    std::cout << 27456;
    for (int i = 0; i < 27456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27457(int& val) {
    func_in_27457(val);
    const int x = 27457;
    std::cout << 27457;
    for (int i = 0; i < 27457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27457(int& val) {
    const int x = 27457;
    std::cout << 27457;
    for (int i = 0; i < 27457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27458(int& val) {
    func_in_27458(val);
    const int x = 27458;
    std::cout << 27458;
    for (int i = 0; i < 27458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27458(int& val) {
    const int x = 27458;
    std::cout << 27458;
    for (int i = 0; i < 27458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27459(int& val) {
    func_in_27459(val);
    const int x = 27459;
    std::cout << 27459;
    for (int i = 0; i < 27459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27459(int& val) {
    const int x = 27459;
    std::cout << 27459;
    for (int i = 0; i < 27459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27460(int& val) {
    func_in_27460(val);
    const int x = 27460;
    std::cout << 27460;
    for (int i = 0; i < 27460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27460(int& val) {
    const int x = 27460;
    std::cout << 27460;
    for (int i = 0; i < 27460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27461(int& val) {
    func_in_27461(val);
    const int x = 27461;
    std::cout << 27461;
    for (int i = 0; i < 27461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27461(int& val) {
    const int x = 27461;
    std::cout << 27461;
    for (int i = 0; i < 27461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27462(int& val) {
    func_in_27462(val);
    const int x = 27462;
    std::cout << 27462;
    for (int i = 0; i < 27462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27462(int& val) {
    const int x = 27462;
    std::cout << 27462;
    for (int i = 0; i < 27462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27463(int& val) {
    func_in_27463(val);
    const int x = 27463;
    std::cout << 27463;
    for (int i = 0; i < 27463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27463(int& val) {
    const int x = 27463;
    std::cout << 27463;
    for (int i = 0; i < 27463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27464(int& val) {
    func_in_27464(val);
    const int x = 27464;
    std::cout << 27464;
    for (int i = 0; i < 27464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27464(int& val) {
    const int x = 27464;
    std::cout << 27464;
    for (int i = 0; i < 27464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27465(int& val) {
    func_in_27465(val);
    const int x = 27465;
    std::cout << 27465;
    for (int i = 0; i < 27465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27465(int& val) {
    const int x = 27465;
    std::cout << 27465;
    for (int i = 0; i < 27465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27466(int& val) {
    func_in_27466(val);
    const int x = 27466;
    std::cout << 27466;
    for (int i = 0; i < 27466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27466(int& val) {
    const int x = 27466;
    std::cout << 27466;
    for (int i = 0; i < 27466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27467(int& val) {
    func_in_27467(val);
    const int x = 27467;
    std::cout << 27467;
    for (int i = 0; i < 27467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27467(int& val) {
    const int x = 27467;
    std::cout << 27467;
    for (int i = 0; i < 27467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27468(int& val) {
    func_in_27468(val);
    const int x = 27468;
    std::cout << 27468;
    for (int i = 0; i < 27468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27468(int& val) {
    const int x = 27468;
    std::cout << 27468;
    for (int i = 0; i < 27468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27469(int& val) {
    func_in_27469(val);
    const int x = 27469;
    std::cout << 27469;
    for (int i = 0; i < 27469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27469(int& val) {
    const int x = 27469;
    std::cout << 27469;
    for (int i = 0; i < 27469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27470(int& val) {
    func_in_27470(val);
    const int x = 27470;
    std::cout << 27470;
    for (int i = 0; i < 27470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27470(int& val) {
    const int x = 27470;
    std::cout << 27470;
    for (int i = 0; i < 27470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27471(int& val) {
    func_in_27471(val);
    const int x = 27471;
    std::cout << 27471;
    for (int i = 0; i < 27471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27471(int& val) {
    const int x = 27471;
    std::cout << 27471;
    for (int i = 0; i < 27471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27472(int& val) {
    func_in_27472(val);
    const int x = 27472;
    std::cout << 27472;
    for (int i = 0; i < 27472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27472(int& val) {
    const int x = 27472;
    std::cout << 27472;
    for (int i = 0; i < 27472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27473(int& val) {
    func_in_27473(val);
    const int x = 27473;
    std::cout << 27473;
    for (int i = 0; i < 27473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27473(int& val) {
    const int x = 27473;
    std::cout << 27473;
    for (int i = 0; i < 27473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27474(int& val) {
    func_in_27474(val);
    const int x = 27474;
    std::cout << 27474;
    for (int i = 0; i < 27474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27474(int& val) {
    const int x = 27474;
    std::cout << 27474;
    for (int i = 0; i < 27474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27475(int& val) {
    func_in_27475(val);
    const int x = 27475;
    std::cout << 27475;
    for (int i = 0; i < 27475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27475(int& val) {
    const int x = 27475;
    std::cout << 27475;
    for (int i = 0; i < 27475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27476(int& val) {
    func_in_27476(val);
    const int x = 27476;
    std::cout << 27476;
    for (int i = 0; i < 27476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27476(int& val) {
    const int x = 27476;
    std::cout << 27476;
    for (int i = 0; i < 27476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27477(int& val) {
    func_in_27477(val);
    const int x = 27477;
    std::cout << 27477;
    for (int i = 0; i < 27477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27477(int& val) {
    const int x = 27477;
    std::cout << 27477;
    for (int i = 0; i < 27477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27478(int& val) {
    func_in_27478(val);
    const int x = 27478;
    std::cout << 27478;
    for (int i = 0; i < 27478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27478(int& val) {
    const int x = 27478;
    std::cout << 27478;
    for (int i = 0; i < 27478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27479(int& val) {
    func_in_27479(val);
    const int x = 27479;
    std::cout << 27479;
    for (int i = 0; i < 27479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27479(int& val) {
    const int x = 27479;
    std::cout << 27479;
    for (int i = 0; i < 27479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27480(int& val) {
    func_in_27480(val);
    const int x = 27480;
    std::cout << 27480;
    for (int i = 0; i < 27480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27480(int& val) {
    const int x = 27480;
    std::cout << 27480;
    for (int i = 0; i < 27480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27481(int& val) {
    func_in_27481(val);
    const int x = 27481;
    std::cout << 27481;
    for (int i = 0; i < 27481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27481(int& val) {
    const int x = 27481;
    std::cout << 27481;
    for (int i = 0; i < 27481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27482(int& val) {
    func_in_27482(val);
    const int x = 27482;
    std::cout << 27482;
    for (int i = 0; i < 27482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27482(int& val) {
    const int x = 27482;
    std::cout << 27482;
    for (int i = 0; i < 27482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27483(int& val) {
    func_in_27483(val);
    const int x = 27483;
    std::cout << 27483;
    for (int i = 0; i < 27483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27483(int& val) {
    const int x = 27483;
    std::cout << 27483;
    for (int i = 0; i < 27483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27484(int& val) {
    func_in_27484(val);
    const int x = 27484;
    std::cout << 27484;
    for (int i = 0; i < 27484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27484(int& val) {
    const int x = 27484;
    std::cout << 27484;
    for (int i = 0; i < 27484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27485(int& val) {
    func_in_27485(val);
    const int x = 27485;
    std::cout << 27485;
    for (int i = 0; i < 27485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27485(int& val) {
    const int x = 27485;
    std::cout << 27485;
    for (int i = 0; i < 27485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27486(int& val) {
    func_in_27486(val);
    const int x = 27486;
    std::cout << 27486;
    for (int i = 0; i < 27486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27486(int& val) {
    const int x = 27486;
    std::cout << 27486;
    for (int i = 0; i < 27486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27487(int& val) {
    func_in_27487(val);
    const int x = 27487;
    std::cout << 27487;
    for (int i = 0; i < 27487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27487(int& val) {
    const int x = 27487;
    std::cout << 27487;
    for (int i = 0; i < 27487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27488(int& val) {
    func_in_27488(val);
    const int x = 27488;
    std::cout << 27488;
    for (int i = 0; i < 27488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27488(int& val) {
    const int x = 27488;
    std::cout << 27488;
    for (int i = 0; i < 27488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27489(int& val) {
    func_in_27489(val);
    const int x = 27489;
    std::cout << 27489;
    for (int i = 0; i < 27489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27489(int& val) {
    const int x = 27489;
    std::cout << 27489;
    for (int i = 0; i < 27489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27490(int& val) {
    func_in_27490(val);
    const int x = 27490;
    std::cout << 27490;
    for (int i = 0; i < 27490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27490(int& val) {
    const int x = 27490;
    std::cout << 27490;
    for (int i = 0; i < 27490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27491(int& val) {
    func_in_27491(val);
    const int x = 27491;
    std::cout << 27491;
    for (int i = 0; i < 27491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27491(int& val) {
    const int x = 27491;
    std::cout << 27491;
    for (int i = 0; i < 27491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27492(int& val) {
    func_in_27492(val);
    const int x = 27492;
    std::cout << 27492;
    for (int i = 0; i < 27492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27492(int& val) {
    const int x = 27492;
    std::cout << 27492;
    for (int i = 0; i < 27492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27493(int& val) {
    func_in_27493(val);
    const int x = 27493;
    std::cout << 27493;
    for (int i = 0; i < 27493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27493(int& val) {
    const int x = 27493;
    std::cout << 27493;
    for (int i = 0; i < 27493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27494(int& val) {
    func_in_27494(val);
    const int x = 27494;
    std::cout << 27494;
    for (int i = 0; i < 27494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27494(int& val) {
    const int x = 27494;
    std::cout << 27494;
    for (int i = 0; i < 27494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27495(int& val) {
    func_in_27495(val);
    const int x = 27495;
    std::cout << 27495;
    for (int i = 0; i < 27495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27495(int& val) {
    const int x = 27495;
    std::cout << 27495;
    for (int i = 0; i < 27495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27496(int& val) {
    func_in_27496(val);
    const int x = 27496;
    std::cout << 27496;
    for (int i = 0; i < 27496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27496(int& val) {
    const int x = 27496;
    std::cout << 27496;
    for (int i = 0; i < 27496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27497(int& val) {
    func_in_27497(val);
    const int x = 27497;
    std::cout << 27497;
    for (int i = 0; i < 27497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27497(int& val) {
    const int x = 27497;
    std::cout << 27497;
    for (int i = 0; i < 27497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27498(int& val) {
    func_in_27498(val);
    const int x = 27498;
    std::cout << 27498;
    for (int i = 0; i < 27498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27498(int& val) {
    const int x = 27498;
    std::cout << 27498;
    for (int i = 0; i < 27498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27499(int& val) {
    func_in_27499(val);
    const int x = 27499;
    std::cout << 27499;
    for (int i = 0; i < 27499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27499(int& val) {
    const int x = 27499;
    std::cout << 27499;
    for (int i = 0; i < 27499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27500(int& val) {
    func_in_27500(val);
    const int x = 27500;
    std::cout << 27500;
    for (int i = 0; i < 27500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27500(int& val) {
    const int x = 27500;
    std::cout << 27500;
    for (int i = 0; i < 27500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27501(int& val) {
    func_in_27501(val);
    const int x = 27501;
    std::cout << 27501;
    for (int i = 0; i < 27501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27501(int& val) {
    const int x = 27501;
    std::cout << 27501;
    for (int i = 0; i < 27501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27502(int& val) {
    func_in_27502(val);
    const int x = 27502;
    std::cout << 27502;
    for (int i = 0; i < 27502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27502(int& val) {
    const int x = 27502;
    std::cout << 27502;
    for (int i = 0; i < 27502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27503(int& val) {
    func_in_27503(val);
    const int x = 27503;
    std::cout << 27503;
    for (int i = 0; i < 27503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27503(int& val) {
    const int x = 27503;
    std::cout << 27503;
    for (int i = 0; i < 27503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27504(int& val) {
    func_in_27504(val);
    const int x = 27504;
    std::cout << 27504;
    for (int i = 0; i < 27504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27504(int& val) {
    const int x = 27504;
    std::cout << 27504;
    for (int i = 0; i < 27504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27505(int& val) {
    func_in_27505(val);
    const int x = 27505;
    std::cout << 27505;
    for (int i = 0; i < 27505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27505(int& val) {
    const int x = 27505;
    std::cout << 27505;
    for (int i = 0; i < 27505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27506(int& val) {
    func_in_27506(val);
    const int x = 27506;
    std::cout << 27506;
    for (int i = 0; i < 27506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27506(int& val) {
    const int x = 27506;
    std::cout << 27506;
    for (int i = 0; i < 27506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27507(int& val) {
    func_in_27507(val);
    const int x = 27507;
    std::cout << 27507;
    for (int i = 0; i < 27507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27507(int& val) {
    const int x = 27507;
    std::cout << 27507;
    for (int i = 0; i < 27507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27508(int& val) {
    func_in_27508(val);
    const int x = 27508;
    std::cout << 27508;
    for (int i = 0; i < 27508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27508(int& val) {
    const int x = 27508;
    std::cout << 27508;
    for (int i = 0; i < 27508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27509(int& val) {
    func_in_27509(val);
    const int x = 27509;
    std::cout << 27509;
    for (int i = 0; i < 27509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27509(int& val) {
    const int x = 27509;
    std::cout << 27509;
    for (int i = 0; i < 27509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27510(int& val) {
    func_in_27510(val);
    const int x = 27510;
    std::cout << 27510;
    for (int i = 0; i < 27510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27510(int& val) {
    const int x = 27510;
    std::cout << 27510;
    for (int i = 0; i < 27510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27511(int& val) {
    func_in_27511(val);
    const int x = 27511;
    std::cout << 27511;
    for (int i = 0; i < 27511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27511(int& val) {
    const int x = 27511;
    std::cout << 27511;
    for (int i = 0; i < 27511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27512(int& val) {
    func_in_27512(val);
    const int x = 27512;
    std::cout << 27512;
    for (int i = 0; i < 27512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27512(int& val) {
    const int x = 27512;
    std::cout << 27512;
    for (int i = 0; i < 27512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27513(int& val) {
    func_in_27513(val);
    const int x = 27513;
    std::cout << 27513;
    for (int i = 0; i < 27513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27513(int& val) {
    const int x = 27513;
    std::cout << 27513;
    for (int i = 0; i < 27513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27514(int& val) {
    func_in_27514(val);
    const int x = 27514;
    std::cout << 27514;
    for (int i = 0; i < 27514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27514(int& val) {
    const int x = 27514;
    std::cout << 27514;
    for (int i = 0; i < 27514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27515(int& val) {
    func_in_27515(val);
    const int x = 27515;
    std::cout << 27515;
    for (int i = 0; i < 27515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27515(int& val) {
    const int x = 27515;
    std::cout << 27515;
    for (int i = 0; i < 27515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27516(int& val) {
    func_in_27516(val);
    const int x = 27516;
    std::cout << 27516;
    for (int i = 0; i < 27516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27516(int& val) {
    const int x = 27516;
    std::cout << 27516;
    for (int i = 0; i < 27516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27517(int& val) {
    func_in_27517(val);
    const int x = 27517;
    std::cout << 27517;
    for (int i = 0; i < 27517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27517(int& val) {
    const int x = 27517;
    std::cout << 27517;
    for (int i = 0; i < 27517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27518(int& val) {
    func_in_27518(val);
    const int x = 27518;
    std::cout << 27518;
    for (int i = 0; i < 27518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27518(int& val) {
    const int x = 27518;
    std::cout << 27518;
    for (int i = 0; i < 27518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27519(int& val) {
    func_in_27519(val);
    const int x = 27519;
    std::cout << 27519;
    for (int i = 0; i < 27519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27519(int& val) {
    const int x = 27519;
    std::cout << 27519;
    for (int i = 0; i < 27519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27520(int& val) {
    func_in_27520(val);
    const int x = 27520;
    std::cout << 27520;
    for (int i = 0; i < 27520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27520(int& val) {
    const int x = 27520;
    std::cout << 27520;
    for (int i = 0; i < 27520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27521(int& val) {
    func_in_27521(val);
    const int x = 27521;
    std::cout << 27521;
    for (int i = 0; i < 27521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27521(int& val) {
    const int x = 27521;
    std::cout << 27521;
    for (int i = 0; i < 27521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27522(int& val) {
    func_in_27522(val);
    const int x = 27522;
    std::cout << 27522;
    for (int i = 0; i < 27522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27522(int& val) {
    const int x = 27522;
    std::cout << 27522;
    for (int i = 0; i < 27522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27523(int& val) {
    func_in_27523(val);
    const int x = 27523;
    std::cout << 27523;
    for (int i = 0; i < 27523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27523(int& val) {
    const int x = 27523;
    std::cout << 27523;
    for (int i = 0; i < 27523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27524(int& val) {
    func_in_27524(val);
    const int x = 27524;
    std::cout << 27524;
    for (int i = 0; i < 27524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27524(int& val) {
    const int x = 27524;
    std::cout << 27524;
    for (int i = 0; i < 27524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27525(int& val) {
    func_in_27525(val);
    const int x = 27525;
    std::cout << 27525;
    for (int i = 0; i < 27525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27525(int& val) {
    const int x = 27525;
    std::cout << 27525;
    for (int i = 0; i < 27525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27526(int& val) {
    func_in_27526(val);
    const int x = 27526;
    std::cout << 27526;
    for (int i = 0; i < 27526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27526(int& val) {
    const int x = 27526;
    std::cout << 27526;
    for (int i = 0; i < 27526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27527(int& val) {
    func_in_27527(val);
    const int x = 27527;
    std::cout << 27527;
    for (int i = 0; i < 27527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27527(int& val) {
    const int x = 27527;
    std::cout << 27527;
    for (int i = 0; i < 27527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27528(int& val) {
    func_in_27528(val);
    const int x = 27528;
    std::cout << 27528;
    for (int i = 0; i < 27528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27528(int& val) {
    const int x = 27528;
    std::cout << 27528;
    for (int i = 0; i < 27528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27529(int& val) {
    func_in_27529(val);
    const int x = 27529;
    std::cout << 27529;
    for (int i = 0; i < 27529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27529(int& val) {
    const int x = 27529;
    std::cout << 27529;
    for (int i = 0; i < 27529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27530(int& val) {
    func_in_27530(val);
    const int x = 27530;
    std::cout << 27530;
    for (int i = 0; i < 27530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27530(int& val) {
    const int x = 27530;
    std::cout << 27530;
    for (int i = 0; i < 27530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27531(int& val) {
    func_in_27531(val);
    const int x = 27531;
    std::cout << 27531;
    for (int i = 0; i < 27531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27531(int& val) {
    const int x = 27531;
    std::cout << 27531;
    for (int i = 0; i < 27531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27532(int& val) {
    func_in_27532(val);
    const int x = 27532;
    std::cout << 27532;
    for (int i = 0; i < 27532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27532(int& val) {
    const int x = 27532;
    std::cout << 27532;
    for (int i = 0; i < 27532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27533(int& val) {
    func_in_27533(val);
    const int x = 27533;
    std::cout << 27533;
    for (int i = 0; i < 27533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27533(int& val) {
    const int x = 27533;
    std::cout << 27533;
    for (int i = 0; i < 27533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27534(int& val) {
    func_in_27534(val);
    const int x = 27534;
    std::cout << 27534;
    for (int i = 0; i < 27534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27534(int& val) {
    const int x = 27534;
    std::cout << 27534;
    for (int i = 0; i < 27534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27535(int& val) {
    func_in_27535(val);
    const int x = 27535;
    std::cout << 27535;
    for (int i = 0; i < 27535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27535(int& val) {
    const int x = 27535;
    std::cout << 27535;
    for (int i = 0; i < 27535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27536(int& val) {
    func_in_27536(val);
    const int x = 27536;
    std::cout << 27536;
    for (int i = 0; i < 27536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27536(int& val) {
    const int x = 27536;
    std::cout << 27536;
    for (int i = 0; i < 27536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27537(int& val) {
    func_in_27537(val);
    const int x = 27537;
    std::cout << 27537;
    for (int i = 0; i < 27537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27537(int& val) {
    const int x = 27537;
    std::cout << 27537;
    for (int i = 0; i < 27537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27538(int& val) {
    func_in_27538(val);
    const int x = 27538;
    std::cout << 27538;
    for (int i = 0; i < 27538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27538(int& val) {
    const int x = 27538;
    std::cout << 27538;
    for (int i = 0; i < 27538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27539(int& val) {
    func_in_27539(val);
    const int x = 27539;
    std::cout << 27539;
    for (int i = 0; i < 27539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27539(int& val) {
    const int x = 27539;
    std::cout << 27539;
    for (int i = 0; i < 27539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27540(int& val) {
    func_in_27540(val);
    const int x = 27540;
    std::cout << 27540;
    for (int i = 0; i < 27540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27540(int& val) {
    const int x = 27540;
    std::cout << 27540;
    for (int i = 0; i < 27540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27541(int& val) {
    func_in_27541(val);
    const int x = 27541;
    std::cout << 27541;
    for (int i = 0; i < 27541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27541(int& val) {
    const int x = 27541;
    std::cout << 27541;
    for (int i = 0; i < 27541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27542(int& val) {
    func_in_27542(val);
    const int x = 27542;
    std::cout << 27542;
    for (int i = 0; i < 27542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27542(int& val) {
    const int x = 27542;
    std::cout << 27542;
    for (int i = 0; i < 27542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27543(int& val) {
    func_in_27543(val);
    const int x = 27543;
    std::cout << 27543;
    for (int i = 0; i < 27543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27543(int& val) {
    const int x = 27543;
    std::cout << 27543;
    for (int i = 0; i < 27543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27544(int& val) {
    func_in_27544(val);
    const int x = 27544;
    std::cout << 27544;
    for (int i = 0; i < 27544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27544(int& val) {
    const int x = 27544;
    std::cout << 27544;
    for (int i = 0; i < 27544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27545(int& val) {
    func_in_27545(val);
    const int x = 27545;
    std::cout << 27545;
    for (int i = 0; i < 27545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27545(int& val) {
    const int x = 27545;
    std::cout << 27545;
    for (int i = 0; i < 27545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27546(int& val) {
    func_in_27546(val);
    const int x = 27546;
    std::cout << 27546;
    for (int i = 0; i < 27546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27546(int& val) {
    const int x = 27546;
    std::cout << 27546;
    for (int i = 0; i < 27546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27547(int& val) {
    func_in_27547(val);
    const int x = 27547;
    std::cout << 27547;
    for (int i = 0; i < 27547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27547(int& val) {
    const int x = 27547;
    std::cout << 27547;
    for (int i = 0; i < 27547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27548(int& val) {
    func_in_27548(val);
    const int x = 27548;
    std::cout << 27548;
    for (int i = 0; i < 27548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27548(int& val) {
    const int x = 27548;
    std::cout << 27548;
    for (int i = 0; i < 27548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27549(int& val) {
    func_in_27549(val);
    const int x = 27549;
    std::cout << 27549;
    for (int i = 0; i < 27549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27549(int& val) {
    const int x = 27549;
    std::cout << 27549;
    for (int i = 0; i < 27549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27550(int& val) {
    func_in_27550(val);
    const int x = 27550;
    std::cout << 27550;
    for (int i = 0; i < 27550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27550(int& val) {
    const int x = 27550;
    std::cout << 27550;
    for (int i = 0; i < 27550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27551(int& val) {
    func_in_27551(val);
    const int x = 27551;
    std::cout << 27551;
    for (int i = 0; i < 27551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27551(int& val) {
    const int x = 27551;
    std::cout << 27551;
    for (int i = 0; i < 27551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27552(int& val) {
    func_in_27552(val);
    const int x = 27552;
    std::cout << 27552;
    for (int i = 0; i < 27552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27552(int& val) {
    const int x = 27552;
    std::cout << 27552;
    for (int i = 0; i < 27552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27553(int& val) {
    func_in_27553(val);
    const int x = 27553;
    std::cout << 27553;
    for (int i = 0; i < 27553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27553(int& val) {
    const int x = 27553;
    std::cout << 27553;
    for (int i = 0; i < 27553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27554(int& val) {
    func_in_27554(val);
    const int x = 27554;
    std::cout << 27554;
    for (int i = 0; i < 27554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27554(int& val) {
    const int x = 27554;
    std::cout << 27554;
    for (int i = 0; i < 27554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27555(int& val) {
    func_in_27555(val);
    const int x = 27555;
    std::cout << 27555;
    for (int i = 0; i < 27555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27555(int& val) {
    const int x = 27555;
    std::cout << 27555;
    for (int i = 0; i < 27555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27556(int& val) {
    func_in_27556(val);
    const int x = 27556;
    std::cout << 27556;
    for (int i = 0; i < 27556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27556(int& val) {
    const int x = 27556;
    std::cout << 27556;
    for (int i = 0; i < 27556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27557(int& val) {
    func_in_27557(val);
    const int x = 27557;
    std::cout << 27557;
    for (int i = 0; i < 27557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27557(int& val) {
    const int x = 27557;
    std::cout << 27557;
    for (int i = 0; i < 27557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27558(int& val) {
    func_in_27558(val);
    const int x = 27558;
    std::cout << 27558;
    for (int i = 0; i < 27558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27558(int& val) {
    const int x = 27558;
    std::cout << 27558;
    for (int i = 0; i < 27558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27559(int& val) {
    func_in_27559(val);
    const int x = 27559;
    std::cout << 27559;
    for (int i = 0; i < 27559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27559(int& val) {
    const int x = 27559;
    std::cout << 27559;
    for (int i = 0; i < 27559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27560(int& val) {
    func_in_27560(val);
    const int x = 27560;
    std::cout << 27560;
    for (int i = 0; i < 27560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27560(int& val) {
    const int x = 27560;
    std::cout << 27560;
    for (int i = 0; i < 27560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27561(int& val) {
    func_in_27561(val);
    const int x = 27561;
    std::cout << 27561;
    for (int i = 0; i < 27561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27561(int& val) {
    const int x = 27561;
    std::cout << 27561;
    for (int i = 0; i < 27561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27562(int& val) {
    func_in_27562(val);
    const int x = 27562;
    std::cout << 27562;
    for (int i = 0; i < 27562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27562(int& val) {
    const int x = 27562;
    std::cout << 27562;
    for (int i = 0; i < 27562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27563(int& val) {
    func_in_27563(val);
    const int x = 27563;
    std::cout << 27563;
    for (int i = 0; i < 27563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27563(int& val) {
    const int x = 27563;
    std::cout << 27563;
    for (int i = 0; i < 27563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27564(int& val) {
    func_in_27564(val);
    const int x = 27564;
    std::cout << 27564;
    for (int i = 0; i < 27564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27564(int& val) {
    const int x = 27564;
    std::cout << 27564;
    for (int i = 0; i < 27564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27565(int& val) {
    func_in_27565(val);
    const int x = 27565;
    std::cout << 27565;
    for (int i = 0; i < 27565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27565(int& val) {
    const int x = 27565;
    std::cout << 27565;
    for (int i = 0; i < 27565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27566(int& val) {
    func_in_27566(val);
    const int x = 27566;
    std::cout << 27566;
    for (int i = 0; i < 27566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27566(int& val) {
    const int x = 27566;
    std::cout << 27566;
    for (int i = 0; i < 27566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27567(int& val) {
    func_in_27567(val);
    const int x = 27567;
    std::cout << 27567;
    for (int i = 0; i < 27567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27567(int& val) {
    const int x = 27567;
    std::cout << 27567;
    for (int i = 0; i < 27567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27568(int& val) {
    func_in_27568(val);
    const int x = 27568;
    std::cout << 27568;
    for (int i = 0; i < 27568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27568(int& val) {
    const int x = 27568;
    std::cout << 27568;
    for (int i = 0; i < 27568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27569(int& val) {
    func_in_27569(val);
    const int x = 27569;
    std::cout << 27569;
    for (int i = 0; i < 27569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27569(int& val) {
    const int x = 27569;
    std::cout << 27569;
    for (int i = 0; i < 27569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27570(int& val) {
    func_in_27570(val);
    const int x = 27570;
    std::cout << 27570;
    for (int i = 0; i < 27570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27570(int& val) {
    const int x = 27570;
    std::cout << 27570;
    for (int i = 0; i < 27570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27571(int& val) {
    func_in_27571(val);
    const int x = 27571;
    std::cout << 27571;
    for (int i = 0; i < 27571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27571(int& val) {
    const int x = 27571;
    std::cout << 27571;
    for (int i = 0; i < 27571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27572(int& val) {
    func_in_27572(val);
    const int x = 27572;
    std::cout << 27572;
    for (int i = 0; i < 27572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27572(int& val) {
    const int x = 27572;
    std::cout << 27572;
    for (int i = 0; i < 27572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27573(int& val) {
    func_in_27573(val);
    const int x = 27573;
    std::cout << 27573;
    for (int i = 0; i < 27573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27573(int& val) {
    const int x = 27573;
    std::cout << 27573;
    for (int i = 0; i < 27573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27574(int& val) {
    func_in_27574(val);
    const int x = 27574;
    std::cout << 27574;
    for (int i = 0; i < 27574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27574(int& val) {
    const int x = 27574;
    std::cout << 27574;
    for (int i = 0; i < 27574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27575(int& val) {
    func_in_27575(val);
    const int x = 27575;
    std::cout << 27575;
    for (int i = 0; i < 27575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27575(int& val) {
    const int x = 27575;
    std::cout << 27575;
    for (int i = 0; i < 27575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27576(int& val) {
    func_in_27576(val);
    const int x = 27576;
    std::cout << 27576;
    for (int i = 0; i < 27576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27576(int& val) {
    const int x = 27576;
    std::cout << 27576;
    for (int i = 0; i < 27576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27577(int& val) {
    func_in_27577(val);
    const int x = 27577;
    std::cout << 27577;
    for (int i = 0; i < 27577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27577(int& val) {
    const int x = 27577;
    std::cout << 27577;
    for (int i = 0; i < 27577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27578(int& val) {
    func_in_27578(val);
    const int x = 27578;
    std::cout << 27578;
    for (int i = 0; i < 27578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27578(int& val) {
    const int x = 27578;
    std::cout << 27578;
    for (int i = 0; i < 27578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27579(int& val) {
    func_in_27579(val);
    const int x = 27579;
    std::cout << 27579;
    for (int i = 0; i < 27579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27579(int& val) {
    const int x = 27579;
    std::cout << 27579;
    for (int i = 0; i < 27579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27580(int& val) {
    func_in_27580(val);
    const int x = 27580;
    std::cout << 27580;
    for (int i = 0; i < 27580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27580(int& val) {
    const int x = 27580;
    std::cout << 27580;
    for (int i = 0; i < 27580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27581(int& val) {
    func_in_27581(val);
    const int x = 27581;
    std::cout << 27581;
    for (int i = 0; i < 27581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27581(int& val) {
    const int x = 27581;
    std::cout << 27581;
    for (int i = 0; i < 27581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27582(int& val) {
    func_in_27582(val);
    const int x = 27582;
    std::cout << 27582;
    for (int i = 0; i < 27582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27582(int& val) {
    const int x = 27582;
    std::cout << 27582;
    for (int i = 0; i < 27582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27583(int& val) {
    func_in_27583(val);
    const int x = 27583;
    std::cout << 27583;
    for (int i = 0; i < 27583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27583(int& val) {
    const int x = 27583;
    std::cout << 27583;
    for (int i = 0; i < 27583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27584(int& val) {
    func_in_27584(val);
    const int x = 27584;
    std::cout << 27584;
    for (int i = 0; i < 27584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27584(int& val) {
    const int x = 27584;
    std::cout << 27584;
    for (int i = 0; i < 27584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27585(int& val) {
    func_in_27585(val);
    const int x = 27585;
    std::cout << 27585;
    for (int i = 0; i < 27585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27585(int& val) {
    const int x = 27585;
    std::cout << 27585;
    for (int i = 0; i < 27585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27586(int& val) {
    func_in_27586(val);
    const int x = 27586;
    std::cout << 27586;
    for (int i = 0; i < 27586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27586(int& val) {
    const int x = 27586;
    std::cout << 27586;
    for (int i = 0; i < 27586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27587(int& val) {
    func_in_27587(val);
    const int x = 27587;
    std::cout << 27587;
    for (int i = 0; i < 27587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27587(int& val) {
    const int x = 27587;
    std::cout << 27587;
    for (int i = 0; i < 27587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27588(int& val) {
    func_in_27588(val);
    const int x = 27588;
    std::cout << 27588;
    for (int i = 0; i < 27588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27588(int& val) {
    const int x = 27588;
    std::cout << 27588;
    for (int i = 0; i < 27588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27589(int& val) {
    func_in_27589(val);
    const int x = 27589;
    std::cout << 27589;
    for (int i = 0; i < 27589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27589(int& val) {
    const int x = 27589;
    std::cout << 27589;
    for (int i = 0; i < 27589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27590(int& val) {
    func_in_27590(val);
    const int x = 27590;
    std::cout << 27590;
    for (int i = 0; i < 27590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27590(int& val) {
    const int x = 27590;
    std::cout << 27590;
    for (int i = 0; i < 27590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27591(int& val) {
    func_in_27591(val);
    const int x = 27591;
    std::cout << 27591;
    for (int i = 0; i < 27591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27591(int& val) {
    const int x = 27591;
    std::cout << 27591;
    for (int i = 0; i < 27591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27592(int& val) {
    func_in_27592(val);
    const int x = 27592;
    std::cout << 27592;
    for (int i = 0; i < 27592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27592(int& val) {
    const int x = 27592;
    std::cout << 27592;
    for (int i = 0; i < 27592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27593(int& val) {
    func_in_27593(val);
    const int x = 27593;
    std::cout << 27593;
    for (int i = 0; i < 27593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27593(int& val) {
    const int x = 27593;
    std::cout << 27593;
    for (int i = 0; i < 27593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27594(int& val) {
    func_in_27594(val);
    const int x = 27594;
    std::cout << 27594;
    for (int i = 0; i < 27594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27594(int& val) {
    const int x = 27594;
    std::cout << 27594;
    for (int i = 0; i < 27594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27595(int& val) {
    func_in_27595(val);
    const int x = 27595;
    std::cout << 27595;
    for (int i = 0; i < 27595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27595(int& val) {
    const int x = 27595;
    std::cout << 27595;
    for (int i = 0; i < 27595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27596(int& val) {
    func_in_27596(val);
    const int x = 27596;
    std::cout << 27596;
    for (int i = 0; i < 27596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27596(int& val) {
    const int x = 27596;
    std::cout << 27596;
    for (int i = 0; i < 27596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27597(int& val) {
    func_in_27597(val);
    const int x = 27597;
    std::cout << 27597;
    for (int i = 0; i < 27597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27597(int& val) {
    const int x = 27597;
    std::cout << 27597;
    for (int i = 0; i < 27597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27598(int& val) {
    func_in_27598(val);
    const int x = 27598;
    std::cout << 27598;
    for (int i = 0; i < 27598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27598(int& val) {
    const int x = 27598;
    std::cout << 27598;
    for (int i = 0; i < 27598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27599(int& val) {
    func_in_27599(val);
    const int x = 27599;
    std::cout << 27599;
    for (int i = 0; i < 27599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27599(int& val) {
    const int x = 27599;
    std::cout << 27599;
    for (int i = 0; i < 27599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27600(int& val) {
    func_in_27600(val);
    const int x = 27600;
    std::cout << 27600;
    for (int i = 0; i < 27600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27600(int& val) {
    const int x = 27600;
    std::cout << 27600;
    for (int i = 0; i < 27600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27601(int& val) {
    func_in_27601(val);
    const int x = 27601;
    std::cout << 27601;
    for (int i = 0; i < 27601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27601(int& val) {
    const int x = 27601;
    std::cout << 27601;
    for (int i = 0; i < 27601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27602(int& val) {
    func_in_27602(val);
    const int x = 27602;
    std::cout << 27602;
    for (int i = 0; i < 27602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27602(int& val) {
    const int x = 27602;
    std::cout << 27602;
    for (int i = 0; i < 27602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27603(int& val) {
    func_in_27603(val);
    const int x = 27603;
    std::cout << 27603;
    for (int i = 0; i < 27603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27603(int& val) {
    const int x = 27603;
    std::cout << 27603;
    for (int i = 0; i < 27603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27604(int& val) {
    func_in_27604(val);
    const int x = 27604;
    std::cout << 27604;
    for (int i = 0; i < 27604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27604(int& val) {
    const int x = 27604;
    std::cout << 27604;
    for (int i = 0; i < 27604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27605(int& val) {
    func_in_27605(val);
    const int x = 27605;
    std::cout << 27605;
    for (int i = 0; i < 27605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27605(int& val) {
    const int x = 27605;
    std::cout << 27605;
    for (int i = 0; i < 27605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27606(int& val) {
    func_in_27606(val);
    const int x = 27606;
    std::cout << 27606;
    for (int i = 0; i < 27606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27606(int& val) {
    const int x = 27606;
    std::cout << 27606;
    for (int i = 0; i < 27606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27607(int& val) {
    func_in_27607(val);
    const int x = 27607;
    std::cout << 27607;
    for (int i = 0; i < 27607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27607(int& val) {
    const int x = 27607;
    std::cout << 27607;
    for (int i = 0; i < 27607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27608(int& val) {
    func_in_27608(val);
    const int x = 27608;
    std::cout << 27608;
    for (int i = 0; i < 27608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27608(int& val) {
    const int x = 27608;
    std::cout << 27608;
    for (int i = 0; i < 27608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27609(int& val) {
    func_in_27609(val);
    const int x = 27609;
    std::cout << 27609;
    for (int i = 0; i < 27609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27609(int& val) {
    const int x = 27609;
    std::cout << 27609;
    for (int i = 0; i < 27609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27610(int& val) {
    func_in_27610(val);
    const int x = 27610;
    std::cout << 27610;
    for (int i = 0; i < 27610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27610(int& val) {
    const int x = 27610;
    std::cout << 27610;
    for (int i = 0; i < 27610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27611(int& val) {
    func_in_27611(val);
    const int x = 27611;
    std::cout << 27611;
    for (int i = 0; i < 27611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27611(int& val) {
    const int x = 27611;
    std::cout << 27611;
    for (int i = 0; i < 27611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27612(int& val) {
    func_in_27612(val);
    const int x = 27612;
    std::cout << 27612;
    for (int i = 0; i < 27612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27612(int& val) {
    const int x = 27612;
    std::cout << 27612;
    for (int i = 0; i < 27612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27613(int& val) {
    func_in_27613(val);
    const int x = 27613;
    std::cout << 27613;
    for (int i = 0; i < 27613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27613(int& val) {
    const int x = 27613;
    std::cout << 27613;
    for (int i = 0; i < 27613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27614(int& val) {
    func_in_27614(val);
    const int x = 27614;
    std::cout << 27614;
    for (int i = 0; i < 27614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27614(int& val) {
    const int x = 27614;
    std::cout << 27614;
    for (int i = 0; i < 27614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27615(int& val) {
    func_in_27615(val);
    const int x = 27615;
    std::cout << 27615;
    for (int i = 0; i < 27615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27615(int& val) {
    const int x = 27615;
    std::cout << 27615;
    for (int i = 0; i < 27615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27616(int& val) {
    func_in_27616(val);
    const int x = 27616;
    std::cout << 27616;
    for (int i = 0; i < 27616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27616(int& val) {
    const int x = 27616;
    std::cout << 27616;
    for (int i = 0; i < 27616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27617(int& val) {
    func_in_27617(val);
    const int x = 27617;
    std::cout << 27617;
    for (int i = 0; i < 27617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27617(int& val) {
    const int x = 27617;
    std::cout << 27617;
    for (int i = 0; i < 27617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27618(int& val) {
    func_in_27618(val);
    const int x = 27618;
    std::cout << 27618;
    for (int i = 0; i < 27618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27618(int& val) {
    const int x = 27618;
    std::cout << 27618;
    for (int i = 0; i < 27618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27619(int& val) {
    func_in_27619(val);
    const int x = 27619;
    std::cout << 27619;
    for (int i = 0; i < 27619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27619(int& val) {
    const int x = 27619;
    std::cout << 27619;
    for (int i = 0; i < 27619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27620(int& val) {
    func_in_27620(val);
    const int x = 27620;
    std::cout << 27620;
    for (int i = 0; i < 27620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27620(int& val) {
    const int x = 27620;
    std::cout << 27620;
    for (int i = 0; i < 27620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27621(int& val) {
    func_in_27621(val);
    const int x = 27621;
    std::cout << 27621;
    for (int i = 0; i < 27621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27621(int& val) {
    const int x = 27621;
    std::cout << 27621;
    for (int i = 0; i < 27621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27622(int& val) {
    func_in_27622(val);
    const int x = 27622;
    std::cout << 27622;
    for (int i = 0; i < 27622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27622(int& val) {
    const int x = 27622;
    std::cout << 27622;
    for (int i = 0; i < 27622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27623(int& val) {
    func_in_27623(val);
    const int x = 27623;
    std::cout << 27623;
    for (int i = 0; i < 27623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27623(int& val) {
    const int x = 27623;
    std::cout << 27623;
    for (int i = 0; i < 27623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27624(int& val) {
    func_in_27624(val);
    const int x = 27624;
    std::cout << 27624;
    for (int i = 0; i < 27624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27624(int& val) {
    const int x = 27624;
    std::cout << 27624;
    for (int i = 0; i < 27624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27625(int& val) {
    func_in_27625(val);
    const int x = 27625;
    std::cout << 27625;
    for (int i = 0; i < 27625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27625(int& val) {
    const int x = 27625;
    std::cout << 27625;
    for (int i = 0; i < 27625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27626(int& val) {
    func_in_27626(val);
    const int x = 27626;
    std::cout << 27626;
    for (int i = 0; i < 27626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27626(int& val) {
    const int x = 27626;
    std::cout << 27626;
    for (int i = 0; i < 27626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27627(int& val) {
    func_in_27627(val);
    const int x = 27627;
    std::cout << 27627;
    for (int i = 0; i < 27627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27627(int& val) {
    const int x = 27627;
    std::cout << 27627;
    for (int i = 0; i < 27627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27628(int& val) {
    func_in_27628(val);
    const int x = 27628;
    std::cout << 27628;
    for (int i = 0; i < 27628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27628(int& val) {
    const int x = 27628;
    std::cout << 27628;
    for (int i = 0; i < 27628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27629(int& val) {
    func_in_27629(val);
    const int x = 27629;
    std::cout << 27629;
    for (int i = 0; i < 27629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27629(int& val) {
    const int x = 27629;
    std::cout << 27629;
    for (int i = 0; i < 27629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27630(int& val) {
    func_in_27630(val);
    const int x = 27630;
    std::cout << 27630;
    for (int i = 0; i < 27630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27630(int& val) {
    const int x = 27630;
    std::cout << 27630;
    for (int i = 0; i < 27630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27631(int& val) {
    func_in_27631(val);
    const int x = 27631;
    std::cout << 27631;
    for (int i = 0; i < 27631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27631(int& val) {
    const int x = 27631;
    std::cout << 27631;
    for (int i = 0; i < 27631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27632(int& val) {
    func_in_27632(val);
    const int x = 27632;
    std::cout << 27632;
    for (int i = 0; i < 27632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27632(int& val) {
    const int x = 27632;
    std::cout << 27632;
    for (int i = 0; i < 27632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27633(int& val) {
    func_in_27633(val);
    const int x = 27633;
    std::cout << 27633;
    for (int i = 0; i < 27633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27633(int& val) {
    const int x = 27633;
    std::cout << 27633;
    for (int i = 0; i < 27633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27634(int& val) {
    func_in_27634(val);
    const int x = 27634;
    std::cout << 27634;
    for (int i = 0; i < 27634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27634(int& val) {
    const int x = 27634;
    std::cout << 27634;
    for (int i = 0; i < 27634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27635(int& val) {
    func_in_27635(val);
    const int x = 27635;
    std::cout << 27635;
    for (int i = 0; i < 27635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27635(int& val) {
    const int x = 27635;
    std::cout << 27635;
    for (int i = 0; i < 27635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27636(int& val) {
    func_in_27636(val);
    const int x = 27636;
    std::cout << 27636;
    for (int i = 0; i < 27636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27636(int& val) {
    const int x = 27636;
    std::cout << 27636;
    for (int i = 0; i < 27636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27637(int& val) {
    func_in_27637(val);
    const int x = 27637;
    std::cout << 27637;
    for (int i = 0; i < 27637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27637(int& val) {
    const int x = 27637;
    std::cout << 27637;
    for (int i = 0; i < 27637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27638(int& val) {
    func_in_27638(val);
    const int x = 27638;
    std::cout << 27638;
    for (int i = 0; i < 27638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27638(int& val) {
    const int x = 27638;
    std::cout << 27638;
    for (int i = 0; i < 27638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27639(int& val) {
    func_in_27639(val);
    const int x = 27639;
    std::cout << 27639;
    for (int i = 0; i < 27639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27639(int& val) {
    const int x = 27639;
    std::cout << 27639;
    for (int i = 0; i < 27639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27640(int& val) {
    func_in_27640(val);
    const int x = 27640;
    std::cout << 27640;
    for (int i = 0; i < 27640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27640(int& val) {
    const int x = 27640;
    std::cout << 27640;
    for (int i = 0; i < 27640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27641(int& val) {
    func_in_27641(val);
    const int x = 27641;
    std::cout << 27641;
    for (int i = 0; i < 27641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27641(int& val) {
    const int x = 27641;
    std::cout << 27641;
    for (int i = 0; i < 27641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27642(int& val) {
    func_in_27642(val);
    const int x = 27642;
    std::cout << 27642;
    for (int i = 0; i < 27642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27642(int& val) {
    const int x = 27642;
    std::cout << 27642;
    for (int i = 0; i < 27642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27643(int& val) {
    func_in_27643(val);
    const int x = 27643;
    std::cout << 27643;
    for (int i = 0; i < 27643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27643(int& val) {
    const int x = 27643;
    std::cout << 27643;
    for (int i = 0; i < 27643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27644(int& val) {
    func_in_27644(val);
    const int x = 27644;
    std::cout << 27644;
    for (int i = 0; i < 27644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27644(int& val) {
    const int x = 27644;
    std::cout << 27644;
    for (int i = 0; i < 27644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27645(int& val) {
    func_in_27645(val);
    const int x = 27645;
    std::cout << 27645;
    for (int i = 0; i < 27645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27645(int& val) {
    const int x = 27645;
    std::cout << 27645;
    for (int i = 0; i < 27645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27646(int& val) {
    func_in_27646(val);
    const int x = 27646;
    std::cout << 27646;
    for (int i = 0; i < 27646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27646(int& val) {
    const int x = 27646;
    std::cout << 27646;
    for (int i = 0; i < 27646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27647(int& val) {
    func_in_27647(val);
    const int x = 27647;
    std::cout << 27647;
    for (int i = 0; i < 27647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27647(int& val) {
    const int x = 27647;
    std::cout << 27647;
    for (int i = 0; i < 27647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27648(int& val) {
    func_in_27648(val);
    const int x = 27648;
    std::cout << 27648;
    for (int i = 0; i < 27648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27648(int& val) {
    const int x = 27648;
    std::cout << 27648;
    for (int i = 0; i < 27648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27649(int& val) {
    func_in_27649(val);
    const int x = 27649;
    std::cout << 27649;
    for (int i = 0; i < 27649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27649(int& val) {
    const int x = 27649;
    std::cout << 27649;
    for (int i = 0; i < 27649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27650(int& val) {
    func_in_27650(val);
    const int x = 27650;
    std::cout << 27650;
    for (int i = 0; i < 27650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27650(int& val) {
    const int x = 27650;
    std::cout << 27650;
    for (int i = 0; i < 27650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27651(int& val) {
    func_in_27651(val);
    const int x = 27651;
    std::cout << 27651;
    for (int i = 0; i < 27651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27651(int& val) {
    const int x = 27651;
    std::cout << 27651;
    for (int i = 0; i < 27651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27652(int& val) {
    func_in_27652(val);
    const int x = 27652;
    std::cout << 27652;
    for (int i = 0; i < 27652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27652(int& val) {
    const int x = 27652;
    std::cout << 27652;
    for (int i = 0; i < 27652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27653(int& val) {
    func_in_27653(val);
    const int x = 27653;
    std::cout << 27653;
    for (int i = 0; i < 27653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27653(int& val) {
    const int x = 27653;
    std::cout << 27653;
    for (int i = 0; i < 27653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27654(int& val) {
    func_in_27654(val);
    const int x = 27654;
    std::cout << 27654;
    for (int i = 0; i < 27654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27654(int& val) {
    const int x = 27654;
    std::cout << 27654;
    for (int i = 0; i < 27654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27655(int& val) {
    func_in_27655(val);
    const int x = 27655;
    std::cout << 27655;
    for (int i = 0; i < 27655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27655(int& val) {
    const int x = 27655;
    std::cout << 27655;
    for (int i = 0; i < 27655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27656(int& val) {
    func_in_27656(val);
    const int x = 27656;
    std::cout << 27656;
    for (int i = 0; i < 27656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27656(int& val) {
    const int x = 27656;
    std::cout << 27656;
    for (int i = 0; i < 27656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27657(int& val) {
    func_in_27657(val);
    const int x = 27657;
    std::cout << 27657;
    for (int i = 0; i < 27657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27657(int& val) {
    const int x = 27657;
    std::cout << 27657;
    for (int i = 0; i < 27657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27658(int& val) {
    func_in_27658(val);
    const int x = 27658;
    std::cout << 27658;
    for (int i = 0; i < 27658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27658(int& val) {
    const int x = 27658;
    std::cout << 27658;
    for (int i = 0; i < 27658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27659(int& val) {
    func_in_27659(val);
    const int x = 27659;
    std::cout << 27659;
    for (int i = 0; i < 27659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27659(int& val) {
    const int x = 27659;
    std::cout << 27659;
    for (int i = 0; i < 27659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27660(int& val) {
    func_in_27660(val);
    const int x = 27660;
    std::cout << 27660;
    for (int i = 0; i < 27660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27660(int& val) {
    const int x = 27660;
    std::cout << 27660;
    for (int i = 0; i < 27660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27661(int& val) {
    func_in_27661(val);
    const int x = 27661;
    std::cout << 27661;
    for (int i = 0; i < 27661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27661(int& val) {
    const int x = 27661;
    std::cout << 27661;
    for (int i = 0; i < 27661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27662(int& val) {
    func_in_27662(val);
    const int x = 27662;
    std::cout << 27662;
    for (int i = 0; i < 27662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27662(int& val) {
    const int x = 27662;
    std::cout << 27662;
    for (int i = 0; i < 27662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27663(int& val) {
    func_in_27663(val);
    const int x = 27663;
    std::cout << 27663;
    for (int i = 0; i < 27663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27663(int& val) {
    const int x = 27663;
    std::cout << 27663;
    for (int i = 0; i < 27663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27664(int& val) {
    func_in_27664(val);
    const int x = 27664;
    std::cout << 27664;
    for (int i = 0; i < 27664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27664(int& val) {
    const int x = 27664;
    std::cout << 27664;
    for (int i = 0; i < 27664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27665(int& val) {
    func_in_27665(val);
    const int x = 27665;
    std::cout << 27665;
    for (int i = 0; i < 27665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27665(int& val) {
    const int x = 27665;
    std::cout << 27665;
    for (int i = 0; i < 27665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27666(int& val) {
    func_in_27666(val);
    const int x = 27666;
    std::cout << 27666;
    for (int i = 0; i < 27666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27666(int& val) {
    const int x = 27666;
    std::cout << 27666;
    for (int i = 0; i < 27666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27667(int& val) {
    func_in_27667(val);
    const int x = 27667;
    std::cout << 27667;
    for (int i = 0; i < 27667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27667(int& val) {
    const int x = 27667;
    std::cout << 27667;
    for (int i = 0; i < 27667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27668(int& val) {
    func_in_27668(val);
    const int x = 27668;
    std::cout << 27668;
    for (int i = 0; i < 27668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27668(int& val) {
    const int x = 27668;
    std::cout << 27668;
    for (int i = 0; i < 27668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27669(int& val) {
    func_in_27669(val);
    const int x = 27669;
    std::cout << 27669;
    for (int i = 0; i < 27669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27669(int& val) {
    const int x = 27669;
    std::cout << 27669;
    for (int i = 0; i < 27669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27670(int& val) {
    func_in_27670(val);
    const int x = 27670;
    std::cout << 27670;
    for (int i = 0; i < 27670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27670(int& val) {
    const int x = 27670;
    std::cout << 27670;
    for (int i = 0; i < 27670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27671(int& val) {
    func_in_27671(val);
    const int x = 27671;
    std::cout << 27671;
    for (int i = 0; i < 27671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27671(int& val) {
    const int x = 27671;
    std::cout << 27671;
    for (int i = 0; i < 27671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27672(int& val) {
    func_in_27672(val);
    const int x = 27672;
    std::cout << 27672;
    for (int i = 0; i < 27672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27672(int& val) {
    const int x = 27672;
    std::cout << 27672;
    for (int i = 0; i < 27672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27673(int& val) {
    func_in_27673(val);
    const int x = 27673;
    std::cout << 27673;
    for (int i = 0; i < 27673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27673(int& val) {
    const int x = 27673;
    std::cout << 27673;
    for (int i = 0; i < 27673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27674(int& val) {
    func_in_27674(val);
    const int x = 27674;
    std::cout << 27674;
    for (int i = 0; i < 27674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27674(int& val) {
    const int x = 27674;
    std::cout << 27674;
    for (int i = 0; i < 27674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27675(int& val) {
    func_in_27675(val);
    const int x = 27675;
    std::cout << 27675;
    for (int i = 0; i < 27675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27675(int& val) {
    const int x = 27675;
    std::cout << 27675;
    for (int i = 0; i < 27675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27676(int& val) {
    func_in_27676(val);
    const int x = 27676;
    std::cout << 27676;
    for (int i = 0; i < 27676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27676(int& val) {
    const int x = 27676;
    std::cout << 27676;
    for (int i = 0; i < 27676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27677(int& val) {
    func_in_27677(val);
    const int x = 27677;
    std::cout << 27677;
    for (int i = 0; i < 27677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27677(int& val) {
    const int x = 27677;
    std::cout << 27677;
    for (int i = 0; i < 27677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27678(int& val) {
    func_in_27678(val);
    const int x = 27678;
    std::cout << 27678;
    for (int i = 0; i < 27678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27678(int& val) {
    const int x = 27678;
    std::cout << 27678;
    for (int i = 0; i < 27678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27679(int& val) {
    func_in_27679(val);
    const int x = 27679;
    std::cout << 27679;
    for (int i = 0; i < 27679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27679(int& val) {
    const int x = 27679;
    std::cout << 27679;
    for (int i = 0; i < 27679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27680(int& val) {
    func_in_27680(val);
    const int x = 27680;
    std::cout << 27680;
    for (int i = 0; i < 27680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27680(int& val) {
    const int x = 27680;
    std::cout << 27680;
    for (int i = 0; i < 27680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27681(int& val) {
    func_in_27681(val);
    const int x = 27681;
    std::cout << 27681;
    for (int i = 0; i < 27681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27681(int& val) {
    const int x = 27681;
    std::cout << 27681;
    for (int i = 0; i < 27681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27682(int& val) {
    func_in_27682(val);
    const int x = 27682;
    std::cout << 27682;
    for (int i = 0; i < 27682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27682(int& val) {
    const int x = 27682;
    std::cout << 27682;
    for (int i = 0; i < 27682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27683(int& val) {
    func_in_27683(val);
    const int x = 27683;
    std::cout << 27683;
    for (int i = 0; i < 27683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27683(int& val) {
    const int x = 27683;
    std::cout << 27683;
    for (int i = 0; i < 27683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27684(int& val) {
    func_in_27684(val);
    const int x = 27684;
    std::cout << 27684;
    for (int i = 0; i < 27684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27684(int& val) {
    const int x = 27684;
    std::cout << 27684;
    for (int i = 0; i < 27684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27685(int& val) {
    func_in_27685(val);
    const int x = 27685;
    std::cout << 27685;
    for (int i = 0; i < 27685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27685(int& val) {
    const int x = 27685;
    std::cout << 27685;
    for (int i = 0; i < 27685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27686(int& val) {
    func_in_27686(val);
    const int x = 27686;
    std::cout << 27686;
    for (int i = 0; i < 27686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27686(int& val) {
    const int x = 27686;
    std::cout << 27686;
    for (int i = 0; i < 27686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27687(int& val) {
    func_in_27687(val);
    const int x = 27687;
    std::cout << 27687;
    for (int i = 0; i < 27687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27687(int& val) {
    const int x = 27687;
    std::cout << 27687;
    for (int i = 0; i < 27687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27688(int& val) {
    func_in_27688(val);
    const int x = 27688;
    std::cout << 27688;
    for (int i = 0; i < 27688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27688(int& val) {
    const int x = 27688;
    std::cout << 27688;
    for (int i = 0; i < 27688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27689(int& val) {
    func_in_27689(val);
    const int x = 27689;
    std::cout << 27689;
    for (int i = 0; i < 27689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27689(int& val) {
    const int x = 27689;
    std::cout << 27689;
    for (int i = 0; i < 27689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27690(int& val) {
    func_in_27690(val);
    const int x = 27690;
    std::cout << 27690;
    for (int i = 0; i < 27690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27690(int& val) {
    const int x = 27690;
    std::cout << 27690;
    for (int i = 0; i < 27690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27691(int& val) {
    func_in_27691(val);
    const int x = 27691;
    std::cout << 27691;
    for (int i = 0; i < 27691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27691(int& val) {
    const int x = 27691;
    std::cout << 27691;
    for (int i = 0; i < 27691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27692(int& val) {
    func_in_27692(val);
    const int x = 27692;
    std::cout << 27692;
    for (int i = 0; i < 27692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27692(int& val) {
    const int x = 27692;
    std::cout << 27692;
    for (int i = 0; i < 27692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27693(int& val) {
    func_in_27693(val);
    const int x = 27693;
    std::cout << 27693;
    for (int i = 0; i < 27693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27693(int& val) {
    const int x = 27693;
    std::cout << 27693;
    for (int i = 0; i < 27693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27694(int& val) {
    func_in_27694(val);
    const int x = 27694;
    std::cout << 27694;
    for (int i = 0; i < 27694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27694(int& val) {
    const int x = 27694;
    std::cout << 27694;
    for (int i = 0; i < 27694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27695(int& val) {
    func_in_27695(val);
    const int x = 27695;
    std::cout << 27695;
    for (int i = 0; i < 27695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27695(int& val) {
    const int x = 27695;
    std::cout << 27695;
    for (int i = 0; i < 27695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27696(int& val) {
    func_in_27696(val);
    const int x = 27696;
    std::cout << 27696;
    for (int i = 0; i < 27696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27696(int& val) {
    const int x = 27696;
    std::cout << 27696;
    for (int i = 0; i < 27696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27697(int& val) {
    func_in_27697(val);
    const int x = 27697;
    std::cout << 27697;
    for (int i = 0; i < 27697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27697(int& val) {
    const int x = 27697;
    std::cout << 27697;
    for (int i = 0; i < 27697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27698(int& val) {
    func_in_27698(val);
    const int x = 27698;
    std::cout << 27698;
    for (int i = 0; i < 27698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27698(int& val) {
    const int x = 27698;
    std::cout << 27698;
    for (int i = 0; i < 27698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27699(int& val) {
    func_in_27699(val);
    const int x = 27699;
    std::cout << 27699;
    for (int i = 0; i < 27699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27699(int& val) {
    const int x = 27699;
    std::cout << 27699;
    for (int i = 0; i < 27699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27700(int& val) {
    func_in_27700(val);
    const int x = 27700;
    std::cout << 27700;
    for (int i = 0; i < 27700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27700(int& val) {
    const int x = 27700;
    std::cout << 27700;
    for (int i = 0; i < 27700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27701(int& val) {
    func_in_27701(val);
    const int x = 27701;
    std::cout << 27701;
    for (int i = 0; i < 27701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27701(int& val) {
    const int x = 27701;
    std::cout << 27701;
    for (int i = 0; i < 27701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27702(int& val) {
    func_in_27702(val);
    const int x = 27702;
    std::cout << 27702;
    for (int i = 0; i < 27702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27702(int& val) {
    const int x = 27702;
    std::cout << 27702;
    for (int i = 0; i < 27702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27703(int& val) {
    func_in_27703(val);
    const int x = 27703;
    std::cout << 27703;
    for (int i = 0; i < 27703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27703(int& val) {
    const int x = 27703;
    std::cout << 27703;
    for (int i = 0; i < 27703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27704(int& val) {
    func_in_27704(val);
    const int x = 27704;
    std::cout << 27704;
    for (int i = 0; i < 27704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27704(int& val) {
    const int x = 27704;
    std::cout << 27704;
    for (int i = 0; i < 27704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27705(int& val) {
    func_in_27705(val);
    const int x = 27705;
    std::cout << 27705;
    for (int i = 0; i < 27705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27705(int& val) {
    const int x = 27705;
    std::cout << 27705;
    for (int i = 0; i < 27705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27706(int& val) {
    func_in_27706(val);
    const int x = 27706;
    std::cout << 27706;
    for (int i = 0; i < 27706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27706(int& val) {
    const int x = 27706;
    std::cout << 27706;
    for (int i = 0; i < 27706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27707(int& val) {
    func_in_27707(val);
    const int x = 27707;
    std::cout << 27707;
    for (int i = 0; i < 27707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27707(int& val) {
    const int x = 27707;
    std::cout << 27707;
    for (int i = 0; i < 27707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27708(int& val) {
    func_in_27708(val);
    const int x = 27708;
    std::cout << 27708;
    for (int i = 0; i < 27708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27708(int& val) {
    const int x = 27708;
    std::cout << 27708;
    for (int i = 0; i < 27708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27709(int& val) {
    func_in_27709(val);
    const int x = 27709;
    std::cout << 27709;
    for (int i = 0; i < 27709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27709(int& val) {
    const int x = 27709;
    std::cout << 27709;
    for (int i = 0; i < 27709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27710(int& val) {
    func_in_27710(val);
    const int x = 27710;
    std::cout << 27710;
    for (int i = 0; i < 27710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27710(int& val) {
    const int x = 27710;
    std::cout << 27710;
    for (int i = 0; i < 27710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27711(int& val) {
    func_in_27711(val);
    const int x = 27711;
    std::cout << 27711;
    for (int i = 0; i < 27711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27711(int& val) {
    const int x = 27711;
    std::cout << 27711;
    for (int i = 0; i < 27711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27712(int& val) {
    func_in_27712(val);
    const int x = 27712;
    std::cout << 27712;
    for (int i = 0; i < 27712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27712(int& val) {
    const int x = 27712;
    std::cout << 27712;
    for (int i = 0; i < 27712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27713(int& val) {
    func_in_27713(val);
    const int x = 27713;
    std::cout << 27713;
    for (int i = 0; i < 27713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27713(int& val) {
    const int x = 27713;
    std::cout << 27713;
    for (int i = 0; i < 27713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27714(int& val) {
    func_in_27714(val);
    const int x = 27714;
    std::cout << 27714;
    for (int i = 0; i < 27714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27714(int& val) {
    const int x = 27714;
    std::cout << 27714;
    for (int i = 0; i < 27714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27715(int& val) {
    func_in_27715(val);
    const int x = 27715;
    std::cout << 27715;
    for (int i = 0; i < 27715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27715(int& val) {
    const int x = 27715;
    std::cout << 27715;
    for (int i = 0; i < 27715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27716(int& val) {
    func_in_27716(val);
    const int x = 27716;
    std::cout << 27716;
    for (int i = 0; i < 27716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27716(int& val) {
    const int x = 27716;
    std::cout << 27716;
    for (int i = 0; i < 27716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27717(int& val) {
    func_in_27717(val);
    const int x = 27717;
    std::cout << 27717;
    for (int i = 0; i < 27717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27717(int& val) {
    const int x = 27717;
    std::cout << 27717;
    for (int i = 0; i < 27717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27718(int& val) {
    func_in_27718(val);
    const int x = 27718;
    std::cout << 27718;
    for (int i = 0; i < 27718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27718(int& val) {
    const int x = 27718;
    std::cout << 27718;
    for (int i = 0; i < 27718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27719(int& val) {
    func_in_27719(val);
    const int x = 27719;
    std::cout << 27719;
    for (int i = 0; i < 27719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27719(int& val) {
    const int x = 27719;
    std::cout << 27719;
    for (int i = 0; i < 27719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27720(int& val) {
    func_in_27720(val);
    const int x = 27720;
    std::cout << 27720;
    for (int i = 0; i < 27720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27720(int& val) {
    const int x = 27720;
    std::cout << 27720;
    for (int i = 0; i < 27720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27721(int& val) {
    func_in_27721(val);
    const int x = 27721;
    std::cout << 27721;
    for (int i = 0; i < 27721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27721(int& val) {
    const int x = 27721;
    std::cout << 27721;
    for (int i = 0; i < 27721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27722(int& val) {
    func_in_27722(val);
    const int x = 27722;
    std::cout << 27722;
    for (int i = 0; i < 27722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27722(int& val) {
    const int x = 27722;
    std::cout << 27722;
    for (int i = 0; i < 27722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27723(int& val) {
    func_in_27723(val);
    const int x = 27723;
    std::cout << 27723;
    for (int i = 0; i < 27723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27723(int& val) {
    const int x = 27723;
    std::cout << 27723;
    for (int i = 0; i < 27723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27724(int& val) {
    func_in_27724(val);
    const int x = 27724;
    std::cout << 27724;
    for (int i = 0; i < 27724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27724(int& val) {
    const int x = 27724;
    std::cout << 27724;
    for (int i = 0; i < 27724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27725(int& val) {
    func_in_27725(val);
    const int x = 27725;
    std::cout << 27725;
    for (int i = 0; i < 27725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27725(int& val) {
    const int x = 27725;
    std::cout << 27725;
    for (int i = 0; i < 27725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27726(int& val) {
    func_in_27726(val);
    const int x = 27726;
    std::cout << 27726;
    for (int i = 0; i < 27726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27726(int& val) {
    const int x = 27726;
    std::cout << 27726;
    for (int i = 0; i < 27726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27727(int& val) {
    func_in_27727(val);
    const int x = 27727;
    std::cout << 27727;
    for (int i = 0; i < 27727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27727(int& val) {
    const int x = 27727;
    std::cout << 27727;
    for (int i = 0; i < 27727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27728(int& val) {
    func_in_27728(val);
    const int x = 27728;
    std::cout << 27728;
    for (int i = 0; i < 27728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27728(int& val) {
    const int x = 27728;
    std::cout << 27728;
    for (int i = 0; i < 27728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27729(int& val) {
    func_in_27729(val);
    const int x = 27729;
    std::cout << 27729;
    for (int i = 0; i < 27729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27729(int& val) {
    const int x = 27729;
    std::cout << 27729;
    for (int i = 0; i < 27729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27730(int& val) {
    func_in_27730(val);
    const int x = 27730;
    std::cout << 27730;
    for (int i = 0; i < 27730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27730(int& val) {
    const int x = 27730;
    std::cout << 27730;
    for (int i = 0; i < 27730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27731(int& val) {
    func_in_27731(val);
    const int x = 27731;
    std::cout << 27731;
    for (int i = 0; i < 27731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27731(int& val) {
    const int x = 27731;
    std::cout << 27731;
    for (int i = 0; i < 27731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27732(int& val) {
    func_in_27732(val);
    const int x = 27732;
    std::cout << 27732;
    for (int i = 0; i < 27732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27732(int& val) {
    const int x = 27732;
    std::cout << 27732;
    for (int i = 0; i < 27732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27733(int& val) {
    func_in_27733(val);
    const int x = 27733;
    std::cout << 27733;
    for (int i = 0; i < 27733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27733(int& val) {
    const int x = 27733;
    std::cout << 27733;
    for (int i = 0; i < 27733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27734(int& val) {
    func_in_27734(val);
    const int x = 27734;
    std::cout << 27734;
    for (int i = 0; i < 27734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27734(int& val) {
    const int x = 27734;
    std::cout << 27734;
    for (int i = 0; i < 27734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27735(int& val) {
    func_in_27735(val);
    const int x = 27735;
    std::cout << 27735;
    for (int i = 0; i < 27735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27735(int& val) {
    const int x = 27735;
    std::cout << 27735;
    for (int i = 0; i < 27735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27736(int& val) {
    func_in_27736(val);
    const int x = 27736;
    std::cout << 27736;
    for (int i = 0; i < 27736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27736(int& val) {
    const int x = 27736;
    std::cout << 27736;
    for (int i = 0; i < 27736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27737(int& val) {
    func_in_27737(val);
    const int x = 27737;
    std::cout << 27737;
    for (int i = 0; i < 27737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27737(int& val) {
    const int x = 27737;
    std::cout << 27737;
    for (int i = 0; i < 27737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27738(int& val) {
    func_in_27738(val);
    const int x = 27738;
    std::cout << 27738;
    for (int i = 0; i < 27738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27738(int& val) {
    const int x = 27738;
    std::cout << 27738;
    for (int i = 0; i < 27738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27739(int& val) {
    func_in_27739(val);
    const int x = 27739;
    std::cout << 27739;
    for (int i = 0; i < 27739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27739(int& val) {
    const int x = 27739;
    std::cout << 27739;
    for (int i = 0; i < 27739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27740(int& val) {
    func_in_27740(val);
    const int x = 27740;
    std::cout << 27740;
    for (int i = 0; i < 27740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27740(int& val) {
    const int x = 27740;
    std::cout << 27740;
    for (int i = 0; i < 27740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27741(int& val) {
    func_in_27741(val);
    const int x = 27741;
    std::cout << 27741;
    for (int i = 0; i < 27741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27741(int& val) {
    const int x = 27741;
    std::cout << 27741;
    for (int i = 0; i < 27741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27742(int& val) {
    func_in_27742(val);
    const int x = 27742;
    std::cout << 27742;
    for (int i = 0; i < 27742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27742(int& val) {
    const int x = 27742;
    std::cout << 27742;
    for (int i = 0; i < 27742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27743(int& val) {
    func_in_27743(val);
    const int x = 27743;
    std::cout << 27743;
    for (int i = 0; i < 27743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27743(int& val) {
    const int x = 27743;
    std::cout << 27743;
    for (int i = 0; i < 27743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27744(int& val) {
    func_in_27744(val);
    const int x = 27744;
    std::cout << 27744;
    for (int i = 0; i < 27744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27744(int& val) {
    const int x = 27744;
    std::cout << 27744;
    for (int i = 0; i < 27744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27745(int& val) {
    func_in_27745(val);
    const int x = 27745;
    std::cout << 27745;
    for (int i = 0; i < 27745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27745(int& val) {
    const int x = 27745;
    std::cout << 27745;
    for (int i = 0; i < 27745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27746(int& val) {
    func_in_27746(val);
    const int x = 27746;
    std::cout << 27746;
    for (int i = 0; i < 27746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27746(int& val) {
    const int x = 27746;
    std::cout << 27746;
    for (int i = 0; i < 27746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27747(int& val) {
    func_in_27747(val);
    const int x = 27747;
    std::cout << 27747;
    for (int i = 0; i < 27747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27747(int& val) {
    const int x = 27747;
    std::cout << 27747;
    for (int i = 0; i < 27747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27748(int& val) {
    func_in_27748(val);
    const int x = 27748;
    std::cout << 27748;
    for (int i = 0; i < 27748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27748(int& val) {
    const int x = 27748;
    std::cout << 27748;
    for (int i = 0; i < 27748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27749(int& val) {
    func_in_27749(val);
    const int x = 27749;
    std::cout << 27749;
    for (int i = 0; i < 27749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27749(int& val) {
    const int x = 27749;
    std::cout << 27749;
    for (int i = 0; i < 27749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27750(int& val) {
    func_in_27750(val);
    const int x = 27750;
    std::cout << 27750;
    for (int i = 0; i < 27750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27750(int& val) {
    const int x = 27750;
    std::cout << 27750;
    for (int i = 0; i < 27750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27751(int& val) {
    func_in_27751(val);
    const int x = 27751;
    std::cout << 27751;
    for (int i = 0; i < 27751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27751(int& val) {
    const int x = 27751;
    std::cout << 27751;
    for (int i = 0; i < 27751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27752(int& val) {
    func_in_27752(val);
    const int x = 27752;
    std::cout << 27752;
    for (int i = 0; i < 27752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27752(int& val) {
    const int x = 27752;
    std::cout << 27752;
    for (int i = 0; i < 27752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27753(int& val) {
    func_in_27753(val);
    const int x = 27753;
    std::cout << 27753;
    for (int i = 0; i < 27753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27753(int& val) {
    const int x = 27753;
    std::cout << 27753;
    for (int i = 0; i < 27753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27754(int& val) {
    func_in_27754(val);
    const int x = 27754;
    std::cout << 27754;
    for (int i = 0; i < 27754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27754(int& val) {
    const int x = 27754;
    std::cout << 27754;
    for (int i = 0; i < 27754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27755(int& val) {
    func_in_27755(val);
    const int x = 27755;
    std::cout << 27755;
    for (int i = 0; i < 27755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27755(int& val) {
    const int x = 27755;
    std::cout << 27755;
    for (int i = 0; i < 27755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27756(int& val) {
    func_in_27756(val);
    const int x = 27756;
    std::cout << 27756;
    for (int i = 0; i < 27756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27756(int& val) {
    const int x = 27756;
    std::cout << 27756;
    for (int i = 0; i < 27756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27757(int& val) {
    func_in_27757(val);
    const int x = 27757;
    std::cout << 27757;
    for (int i = 0; i < 27757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27757(int& val) {
    const int x = 27757;
    std::cout << 27757;
    for (int i = 0; i < 27757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27758(int& val) {
    func_in_27758(val);
    const int x = 27758;
    std::cout << 27758;
    for (int i = 0; i < 27758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27758(int& val) {
    const int x = 27758;
    std::cout << 27758;
    for (int i = 0; i < 27758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27759(int& val) {
    func_in_27759(val);
    const int x = 27759;
    std::cout << 27759;
    for (int i = 0; i < 27759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27759(int& val) {
    const int x = 27759;
    std::cout << 27759;
    for (int i = 0; i < 27759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27760(int& val) {
    func_in_27760(val);
    const int x = 27760;
    std::cout << 27760;
    for (int i = 0; i < 27760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27760(int& val) {
    const int x = 27760;
    std::cout << 27760;
    for (int i = 0; i < 27760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27761(int& val) {
    func_in_27761(val);
    const int x = 27761;
    std::cout << 27761;
    for (int i = 0; i < 27761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27761(int& val) {
    const int x = 27761;
    std::cout << 27761;
    for (int i = 0; i < 27761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27762(int& val) {
    func_in_27762(val);
    const int x = 27762;
    std::cout << 27762;
    for (int i = 0; i < 27762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27762(int& val) {
    const int x = 27762;
    std::cout << 27762;
    for (int i = 0; i < 27762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27763(int& val) {
    func_in_27763(val);
    const int x = 27763;
    std::cout << 27763;
    for (int i = 0; i < 27763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27763(int& val) {
    const int x = 27763;
    std::cout << 27763;
    for (int i = 0; i < 27763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27764(int& val) {
    func_in_27764(val);
    const int x = 27764;
    std::cout << 27764;
    for (int i = 0; i < 27764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27764(int& val) {
    const int x = 27764;
    std::cout << 27764;
    for (int i = 0; i < 27764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27765(int& val) {
    func_in_27765(val);
    const int x = 27765;
    std::cout << 27765;
    for (int i = 0; i < 27765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27765(int& val) {
    const int x = 27765;
    std::cout << 27765;
    for (int i = 0; i < 27765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27766(int& val) {
    func_in_27766(val);
    const int x = 27766;
    std::cout << 27766;
    for (int i = 0; i < 27766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27766(int& val) {
    const int x = 27766;
    std::cout << 27766;
    for (int i = 0; i < 27766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27767(int& val) {
    func_in_27767(val);
    const int x = 27767;
    std::cout << 27767;
    for (int i = 0; i < 27767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27767(int& val) {
    const int x = 27767;
    std::cout << 27767;
    for (int i = 0; i < 27767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27768(int& val) {
    func_in_27768(val);
    const int x = 27768;
    std::cout << 27768;
    for (int i = 0; i < 27768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27768(int& val) {
    const int x = 27768;
    std::cout << 27768;
    for (int i = 0; i < 27768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27769(int& val) {
    func_in_27769(val);
    const int x = 27769;
    std::cout << 27769;
    for (int i = 0; i < 27769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27769(int& val) {
    const int x = 27769;
    std::cout << 27769;
    for (int i = 0; i < 27769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27770(int& val) {
    func_in_27770(val);
    const int x = 27770;
    std::cout << 27770;
    for (int i = 0; i < 27770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27770(int& val) {
    const int x = 27770;
    std::cout << 27770;
    for (int i = 0; i < 27770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27771(int& val) {
    func_in_27771(val);
    const int x = 27771;
    std::cout << 27771;
    for (int i = 0; i < 27771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27771(int& val) {
    const int x = 27771;
    std::cout << 27771;
    for (int i = 0; i < 27771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27772(int& val) {
    func_in_27772(val);
    const int x = 27772;
    std::cout << 27772;
    for (int i = 0; i < 27772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27772(int& val) {
    const int x = 27772;
    std::cout << 27772;
    for (int i = 0; i < 27772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27773(int& val) {
    func_in_27773(val);
    const int x = 27773;
    std::cout << 27773;
    for (int i = 0; i < 27773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27773(int& val) {
    const int x = 27773;
    std::cout << 27773;
    for (int i = 0; i < 27773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27774(int& val) {
    func_in_27774(val);
    const int x = 27774;
    std::cout << 27774;
    for (int i = 0; i < 27774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27774(int& val) {
    const int x = 27774;
    std::cout << 27774;
    for (int i = 0; i < 27774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27775(int& val) {
    func_in_27775(val);
    const int x = 27775;
    std::cout << 27775;
    for (int i = 0; i < 27775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27775(int& val) {
    const int x = 27775;
    std::cout << 27775;
    for (int i = 0; i < 27775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27776(int& val) {
    func_in_27776(val);
    const int x = 27776;
    std::cout << 27776;
    for (int i = 0; i < 27776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27776(int& val) {
    const int x = 27776;
    std::cout << 27776;
    for (int i = 0; i < 27776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27777(int& val) {
    func_in_27777(val);
    const int x = 27777;
    std::cout << 27777;
    for (int i = 0; i < 27777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27777(int& val) {
    const int x = 27777;
    std::cout << 27777;
    for (int i = 0; i < 27777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27778(int& val) {
    func_in_27778(val);
    const int x = 27778;
    std::cout << 27778;
    for (int i = 0; i < 27778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27778(int& val) {
    const int x = 27778;
    std::cout << 27778;
    for (int i = 0; i < 27778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27779(int& val) {
    func_in_27779(val);
    const int x = 27779;
    std::cout << 27779;
    for (int i = 0; i < 27779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27779(int& val) {
    const int x = 27779;
    std::cout << 27779;
    for (int i = 0; i < 27779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27780(int& val) {
    func_in_27780(val);
    const int x = 27780;
    std::cout << 27780;
    for (int i = 0; i < 27780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27780(int& val) {
    const int x = 27780;
    std::cout << 27780;
    for (int i = 0; i < 27780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27781(int& val) {
    func_in_27781(val);
    const int x = 27781;
    std::cout << 27781;
    for (int i = 0; i < 27781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27781(int& val) {
    const int x = 27781;
    std::cout << 27781;
    for (int i = 0; i < 27781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27782(int& val) {
    func_in_27782(val);
    const int x = 27782;
    std::cout << 27782;
    for (int i = 0; i < 27782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27782(int& val) {
    const int x = 27782;
    std::cout << 27782;
    for (int i = 0; i < 27782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27783(int& val) {
    func_in_27783(val);
    const int x = 27783;
    std::cout << 27783;
    for (int i = 0; i < 27783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27783(int& val) {
    const int x = 27783;
    std::cout << 27783;
    for (int i = 0; i < 27783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27784(int& val) {
    func_in_27784(val);
    const int x = 27784;
    std::cout << 27784;
    for (int i = 0; i < 27784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27784(int& val) {
    const int x = 27784;
    std::cout << 27784;
    for (int i = 0; i < 27784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27785(int& val) {
    func_in_27785(val);
    const int x = 27785;
    std::cout << 27785;
    for (int i = 0; i < 27785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27785(int& val) {
    const int x = 27785;
    std::cout << 27785;
    for (int i = 0; i < 27785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27786(int& val) {
    func_in_27786(val);
    const int x = 27786;
    std::cout << 27786;
    for (int i = 0; i < 27786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27786(int& val) {
    const int x = 27786;
    std::cout << 27786;
    for (int i = 0; i < 27786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27787(int& val) {
    func_in_27787(val);
    const int x = 27787;
    std::cout << 27787;
    for (int i = 0; i < 27787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27787(int& val) {
    const int x = 27787;
    std::cout << 27787;
    for (int i = 0; i < 27787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27788(int& val) {
    func_in_27788(val);
    const int x = 27788;
    std::cout << 27788;
    for (int i = 0; i < 27788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27788(int& val) {
    const int x = 27788;
    std::cout << 27788;
    for (int i = 0; i < 27788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27789(int& val) {
    func_in_27789(val);
    const int x = 27789;
    std::cout << 27789;
    for (int i = 0; i < 27789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27789(int& val) {
    const int x = 27789;
    std::cout << 27789;
    for (int i = 0; i < 27789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27790(int& val) {
    func_in_27790(val);
    const int x = 27790;
    std::cout << 27790;
    for (int i = 0; i < 27790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27790(int& val) {
    const int x = 27790;
    std::cout << 27790;
    for (int i = 0; i < 27790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27791(int& val) {
    func_in_27791(val);
    const int x = 27791;
    std::cout << 27791;
    for (int i = 0; i < 27791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27791(int& val) {
    const int x = 27791;
    std::cout << 27791;
    for (int i = 0; i < 27791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27792(int& val) {
    func_in_27792(val);
    const int x = 27792;
    std::cout << 27792;
    for (int i = 0; i < 27792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27792(int& val) {
    const int x = 27792;
    std::cout << 27792;
    for (int i = 0; i < 27792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27793(int& val) {
    func_in_27793(val);
    const int x = 27793;
    std::cout << 27793;
    for (int i = 0; i < 27793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27793(int& val) {
    const int x = 27793;
    std::cout << 27793;
    for (int i = 0; i < 27793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27794(int& val) {
    func_in_27794(val);
    const int x = 27794;
    std::cout << 27794;
    for (int i = 0; i < 27794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27794(int& val) {
    const int x = 27794;
    std::cout << 27794;
    for (int i = 0; i < 27794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27795(int& val) {
    func_in_27795(val);
    const int x = 27795;
    std::cout << 27795;
    for (int i = 0; i < 27795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27795(int& val) {
    const int x = 27795;
    std::cout << 27795;
    for (int i = 0; i < 27795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27796(int& val) {
    func_in_27796(val);
    const int x = 27796;
    std::cout << 27796;
    for (int i = 0; i < 27796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27796(int& val) {
    const int x = 27796;
    std::cout << 27796;
    for (int i = 0; i < 27796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27797(int& val) {
    func_in_27797(val);
    const int x = 27797;
    std::cout << 27797;
    for (int i = 0; i < 27797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27797(int& val) {
    const int x = 27797;
    std::cout << 27797;
    for (int i = 0; i < 27797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27798(int& val) {
    func_in_27798(val);
    const int x = 27798;
    std::cout << 27798;
    for (int i = 0; i < 27798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27798(int& val) {
    const int x = 27798;
    std::cout << 27798;
    for (int i = 0; i < 27798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27799(int& val) {
    func_in_27799(val);
    const int x = 27799;
    std::cout << 27799;
    for (int i = 0; i < 27799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27799(int& val) {
    const int x = 27799;
    std::cout << 27799;
    for (int i = 0; i < 27799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27800(int& val) {
    func_in_27800(val);
    const int x = 27800;
    std::cout << 27800;
    for (int i = 0; i < 27800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27800(int& val) {
    const int x = 27800;
    std::cout << 27800;
    for (int i = 0; i < 27800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27801(int& val) {
    func_in_27801(val);
    const int x = 27801;
    std::cout << 27801;
    for (int i = 0; i < 27801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27801(int& val) {
    const int x = 27801;
    std::cout << 27801;
    for (int i = 0; i < 27801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27802(int& val) {
    func_in_27802(val);
    const int x = 27802;
    std::cout << 27802;
    for (int i = 0; i < 27802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27802(int& val) {
    const int x = 27802;
    std::cout << 27802;
    for (int i = 0; i < 27802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27803(int& val) {
    func_in_27803(val);
    const int x = 27803;
    std::cout << 27803;
    for (int i = 0; i < 27803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27803(int& val) {
    const int x = 27803;
    std::cout << 27803;
    for (int i = 0; i < 27803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27804(int& val) {
    func_in_27804(val);
    const int x = 27804;
    std::cout << 27804;
    for (int i = 0; i < 27804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27804(int& val) {
    const int x = 27804;
    std::cout << 27804;
    for (int i = 0; i < 27804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27805(int& val) {
    func_in_27805(val);
    const int x = 27805;
    std::cout << 27805;
    for (int i = 0; i < 27805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27805(int& val) {
    const int x = 27805;
    std::cout << 27805;
    for (int i = 0; i < 27805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27806(int& val) {
    func_in_27806(val);
    const int x = 27806;
    std::cout << 27806;
    for (int i = 0; i < 27806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27806(int& val) {
    const int x = 27806;
    std::cout << 27806;
    for (int i = 0; i < 27806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27807(int& val) {
    func_in_27807(val);
    const int x = 27807;
    std::cout << 27807;
    for (int i = 0; i < 27807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27807(int& val) {
    const int x = 27807;
    std::cout << 27807;
    for (int i = 0; i < 27807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27808(int& val) {
    func_in_27808(val);
    const int x = 27808;
    std::cout << 27808;
    for (int i = 0; i < 27808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27808(int& val) {
    const int x = 27808;
    std::cout << 27808;
    for (int i = 0; i < 27808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27809(int& val) {
    func_in_27809(val);
    const int x = 27809;
    std::cout << 27809;
    for (int i = 0; i < 27809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27809(int& val) {
    const int x = 27809;
    std::cout << 27809;
    for (int i = 0; i < 27809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27810(int& val) {
    func_in_27810(val);
    const int x = 27810;
    std::cout << 27810;
    for (int i = 0; i < 27810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27810(int& val) {
    const int x = 27810;
    std::cout << 27810;
    for (int i = 0; i < 27810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27811(int& val) {
    func_in_27811(val);
    const int x = 27811;
    std::cout << 27811;
    for (int i = 0; i < 27811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27811(int& val) {
    const int x = 27811;
    std::cout << 27811;
    for (int i = 0; i < 27811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27812(int& val) {
    func_in_27812(val);
    const int x = 27812;
    std::cout << 27812;
    for (int i = 0; i < 27812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27812(int& val) {
    const int x = 27812;
    std::cout << 27812;
    for (int i = 0; i < 27812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27813(int& val) {
    func_in_27813(val);
    const int x = 27813;
    std::cout << 27813;
    for (int i = 0; i < 27813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27813(int& val) {
    const int x = 27813;
    std::cout << 27813;
    for (int i = 0; i < 27813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27814(int& val) {
    func_in_27814(val);
    const int x = 27814;
    std::cout << 27814;
    for (int i = 0; i < 27814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27814(int& val) {
    const int x = 27814;
    std::cout << 27814;
    for (int i = 0; i < 27814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27815(int& val) {
    func_in_27815(val);
    const int x = 27815;
    std::cout << 27815;
    for (int i = 0; i < 27815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27815(int& val) {
    const int x = 27815;
    std::cout << 27815;
    for (int i = 0; i < 27815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27816(int& val) {
    func_in_27816(val);
    const int x = 27816;
    std::cout << 27816;
    for (int i = 0; i < 27816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27816(int& val) {
    const int x = 27816;
    std::cout << 27816;
    for (int i = 0; i < 27816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27817(int& val) {
    func_in_27817(val);
    const int x = 27817;
    std::cout << 27817;
    for (int i = 0; i < 27817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27817(int& val) {
    const int x = 27817;
    std::cout << 27817;
    for (int i = 0; i < 27817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27818(int& val) {
    func_in_27818(val);
    const int x = 27818;
    std::cout << 27818;
    for (int i = 0; i < 27818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27818(int& val) {
    const int x = 27818;
    std::cout << 27818;
    for (int i = 0; i < 27818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27819(int& val) {
    func_in_27819(val);
    const int x = 27819;
    std::cout << 27819;
    for (int i = 0; i < 27819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27819(int& val) {
    const int x = 27819;
    std::cout << 27819;
    for (int i = 0; i < 27819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27820(int& val) {
    func_in_27820(val);
    const int x = 27820;
    std::cout << 27820;
    for (int i = 0; i < 27820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27820(int& val) {
    const int x = 27820;
    std::cout << 27820;
    for (int i = 0; i < 27820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27821(int& val) {
    func_in_27821(val);
    const int x = 27821;
    std::cout << 27821;
    for (int i = 0; i < 27821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27821(int& val) {
    const int x = 27821;
    std::cout << 27821;
    for (int i = 0; i < 27821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27822(int& val) {
    func_in_27822(val);
    const int x = 27822;
    std::cout << 27822;
    for (int i = 0; i < 27822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27822(int& val) {
    const int x = 27822;
    std::cout << 27822;
    for (int i = 0; i < 27822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27823(int& val) {
    func_in_27823(val);
    const int x = 27823;
    std::cout << 27823;
    for (int i = 0; i < 27823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27823(int& val) {
    const int x = 27823;
    std::cout << 27823;
    for (int i = 0; i < 27823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27824(int& val) {
    func_in_27824(val);
    const int x = 27824;
    std::cout << 27824;
    for (int i = 0; i < 27824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27824(int& val) {
    const int x = 27824;
    std::cout << 27824;
    for (int i = 0; i < 27824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27825(int& val) {
    func_in_27825(val);
    const int x = 27825;
    std::cout << 27825;
    for (int i = 0; i < 27825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27825(int& val) {
    const int x = 27825;
    std::cout << 27825;
    for (int i = 0; i < 27825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27826(int& val) {
    func_in_27826(val);
    const int x = 27826;
    std::cout << 27826;
    for (int i = 0; i < 27826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27826(int& val) {
    const int x = 27826;
    std::cout << 27826;
    for (int i = 0; i < 27826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27827(int& val) {
    func_in_27827(val);
    const int x = 27827;
    std::cout << 27827;
    for (int i = 0; i < 27827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27827(int& val) {
    const int x = 27827;
    std::cout << 27827;
    for (int i = 0; i < 27827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27828(int& val) {
    func_in_27828(val);
    const int x = 27828;
    std::cout << 27828;
    for (int i = 0; i < 27828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27828(int& val) {
    const int x = 27828;
    std::cout << 27828;
    for (int i = 0; i < 27828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27829(int& val) {
    func_in_27829(val);
    const int x = 27829;
    std::cout << 27829;
    for (int i = 0; i < 27829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27829(int& val) {
    const int x = 27829;
    std::cout << 27829;
    for (int i = 0; i < 27829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27830(int& val) {
    func_in_27830(val);
    const int x = 27830;
    std::cout << 27830;
    for (int i = 0; i < 27830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27830(int& val) {
    const int x = 27830;
    std::cout << 27830;
    for (int i = 0; i < 27830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27831(int& val) {
    func_in_27831(val);
    const int x = 27831;
    std::cout << 27831;
    for (int i = 0; i < 27831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27831(int& val) {
    const int x = 27831;
    std::cout << 27831;
    for (int i = 0; i < 27831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27832(int& val) {
    func_in_27832(val);
    const int x = 27832;
    std::cout << 27832;
    for (int i = 0; i < 27832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27832(int& val) {
    const int x = 27832;
    std::cout << 27832;
    for (int i = 0; i < 27832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27833(int& val) {
    func_in_27833(val);
    const int x = 27833;
    std::cout << 27833;
    for (int i = 0; i < 27833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27833(int& val) {
    const int x = 27833;
    std::cout << 27833;
    for (int i = 0; i < 27833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27834(int& val) {
    func_in_27834(val);
    const int x = 27834;
    std::cout << 27834;
    for (int i = 0; i < 27834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27834(int& val) {
    const int x = 27834;
    std::cout << 27834;
    for (int i = 0; i < 27834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27835(int& val) {
    func_in_27835(val);
    const int x = 27835;
    std::cout << 27835;
    for (int i = 0; i < 27835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27835(int& val) {
    const int x = 27835;
    std::cout << 27835;
    for (int i = 0; i < 27835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27836(int& val) {
    func_in_27836(val);
    const int x = 27836;
    std::cout << 27836;
    for (int i = 0; i < 27836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27836(int& val) {
    const int x = 27836;
    std::cout << 27836;
    for (int i = 0; i < 27836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27837(int& val) {
    func_in_27837(val);
    const int x = 27837;
    std::cout << 27837;
    for (int i = 0; i < 27837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27837(int& val) {
    const int x = 27837;
    std::cout << 27837;
    for (int i = 0; i < 27837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27838(int& val) {
    func_in_27838(val);
    const int x = 27838;
    std::cout << 27838;
    for (int i = 0; i < 27838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27838(int& val) {
    const int x = 27838;
    std::cout << 27838;
    for (int i = 0; i < 27838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27839(int& val) {
    func_in_27839(val);
    const int x = 27839;
    std::cout << 27839;
    for (int i = 0; i < 27839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27839(int& val) {
    const int x = 27839;
    std::cout << 27839;
    for (int i = 0; i < 27839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27840(int& val) {
    func_in_27840(val);
    const int x = 27840;
    std::cout << 27840;
    for (int i = 0; i < 27840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27840(int& val) {
    const int x = 27840;
    std::cout << 27840;
    for (int i = 0; i < 27840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27841(int& val) {
    func_in_27841(val);
    const int x = 27841;
    std::cout << 27841;
    for (int i = 0; i < 27841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27841(int& val) {
    const int x = 27841;
    std::cout << 27841;
    for (int i = 0; i < 27841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27842(int& val) {
    func_in_27842(val);
    const int x = 27842;
    std::cout << 27842;
    for (int i = 0; i < 27842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27842(int& val) {
    const int x = 27842;
    std::cout << 27842;
    for (int i = 0; i < 27842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27843(int& val) {
    func_in_27843(val);
    const int x = 27843;
    std::cout << 27843;
    for (int i = 0; i < 27843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27843(int& val) {
    const int x = 27843;
    std::cout << 27843;
    for (int i = 0; i < 27843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27844(int& val) {
    func_in_27844(val);
    const int x = 27844;
    std::cout << 27844;
    for (int i = 0; i < 27844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27844(int& val) {
    const int x = 27844;
    std::cout << 27844;
    for (int i = 0; i < 27844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27845(int& val) {
    func_in_27845(val);
    const int x = 27845;
    std::cout << 27845;
    for (int i = 0; i < 27845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27845(int& val) {
    const int x = 27845;
    std::cout << 27845;
    for (int i = 0; i < 27845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27846(int& val) {
    func_in_27846(val);
    const int x = 27846;
    std::cout << 27846;
    for (int i = 0; i < 27846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27846(int& val) {
    const int x = 27846;
    std::cout << 27846;
    for (int i = 0; i < 27846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27847(int& val) {
    func_in_27847(val);
    const int x = 27847;
    std::cout << 27847;
    for (int i = 0; i < 27847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27847(int& val) {
    const int x = 27847;
    std::cout << 27847;
    for (int i = 0; i < 27847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27848(int& val) {
    func_in_27848(val);
    const int x = 27848;
    std::cout << 27848;
    for (int i = 0; i < 27848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27848(int& val) {
    const int x = 27848;
    std::cout << 27848;
    for (int i = 0; i < 27848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27849(int& val) {
    func_in_27849(val);
    const int x = 27849;
    std::cout << 27849;
    for (int i = 0; i < 27849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27849(int& val) {
    const int x = 27849;
    std::cout << 27849;
    for (int i = 0; i < 27849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27850(int& val) {
    func_in_27850(val);
    const int x = 27850;
    std::cout << 27850;
    for (int i = 0; i < 27850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27850(int& val) {
    const int x = 27850;
    std::cout << 27850;
    for (int i = 0; i < 27850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27851(int& val) {
    func_in_27851(val);
    const int x = 27851;
    std::cout << 27851;
    for (int i = 0; i < 27851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27851(int& val) {
    const int x = 27851;
    std::cout << 27851;
    for (int i = 0; i < 27851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27852(int& val) {
    func_in_27852(val);
    const int x = 27852;
    std::cout << 27852;
    for (int i = 0; i < 27852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27852(int& val) {
    const int x = 27852;
    std::cout << 27852;
    for (int i = 0; i < 27852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27853(int& val) {
    func_in_27853(val);
    const int x = 27853;
    std::cout << 27853;
    for (int i = 0; i < 27853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27853(int& val) {
    const int x = 27853;
    std::cout << 27853;
    for (int i = 0; i < 27853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27854(int& val) {
    func_in_27854(val);
    const int x = 27854;
    std::cout << 27854;
    for (int i = 0; i < 27854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27854(int& val) {
    const int x = 27854;
    std::cout << 27854;
    for (int i = 0; i < 27854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27855(int& val) {
    func_in_27855(val);
    const int x = 27855;
    std::cout << 27855;
    for (int i = 0; i < 27855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27855(int& val) {
    const int x = 27855;
    std::cout << 27855;
    for (int i = 0; i < 27855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27856(int& val) {
    func_in_27856(val);
    const int x = 27856;
    std::cout << 27856;
    for (int i = 0; i < 27856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27856(int& val) {
    const int x = 27856;
    std::cout << 27856;
    for (int i = 0; i < 27856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27857(int& val) {
    func_in_27857(val);
    const int x = 27857;
    std::cout << 27857;
    for (int i = 0; i < 27857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27857(int& val) {
    const int x = 27857;
    std::cout << 27857;
    for (int i = 0; i < 27857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27858(int& val) {
    func_in_27858(val);
    const int x = 27858;
    std::cout << 27858;
    for (int i = 0; i < 27858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27858(int& val) {
    const int x = 27858;
    std::cout << 27858;
    for (int i = 0; i < 27858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27859(int& val) {
    func_in_27859(val);
    const int x = 27859;
    std::cout << 27859;
    for (int i = 0; i < 27859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27859(int& val) {
    const int x = 27859;
    std::cout << 27859;
    for (int i = 0; i < 27859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27860(int& val) {
    func_in_27860(val);
    const int x = 27860;
    std::cout << 27860;
    for (int i = 0; i < 27860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27860(int& val) {
    const int x = 27860;
    std::cout << 27860;
    for (int i = 0; i < 27860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27861(int& val) {
    func_in_27861(val);
    const int x = 27861;
    std::cout << 27861;
    for (int i = 0; i < 27861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27861(int& val) {
    const int x = 27861;
    std::cout << 27861;
    for (int i = 0; i < 27861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27862(int& val) {
    func_in_27862(val);
    const int x = 27862;
    std::cout << 27862;
    for (int i = 0; i < 27862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27862(int& val) {
    const int x = 27862;
    std::cout << 27862;
    for (int i = 0; i < 27862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27863(int& val) {
    func_in_27863(val);
    const int x = 27863;
    std::cout << 27863;
    for (int i = 0; i < 27863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27863(int& val) {
    const int x = 27863;
    std::cout << 27863;
    for (int i = 0; i < 27863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27864(int& val) {
    func_in_27864(val);
    const int x = 27864;
    std::cout << 27864;
    for (int i = 0; i < 27864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27864(int& val) {
    const int x = 27864;
    std::cout << 27864;
    for (int i = 0; i < 27864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27865(int& val) {
    func_in_27865(val);
    const int x = 27865;
    std::cout << 27865;
    for (int i = 0; i < 27865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27865(int& val) {
    const int x = 27865;
    std::cout << 27865;
    for (int i = 0; i < 27865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27866(int& val) {
    func_in_27866(val);
    const int x = 27866;
    std::cout << 27866;
    for (int i = 0; i < 27866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27866(int& val) {
    const int x = 27866;
    std::cout << 27866;
    for (int i = 0; i < 27866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27867(int& val) {
    func_in_27867(val);
    const int x = 27867;
    std::cout << 27867;
    for (int i = 0; i < 27867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27867(int& val) {
    const int x = 27867;
    std::cout << 27867;
    for (int i = 0; i < 27867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27868(int& val) {
    func_in_27868(val);
    const int x = 27868;
    std::cout << 27868;
    for (int i = 0; i < 27868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27868(int& val) {
    const int x = 27868;
    std::cout << 27868;
    for (int i = 0; i < 27868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27869(int& val) {
    func_in_27869(val);
    const int x = 27869;
    std::cout << 27869;
    for (int i = 0; i < 27869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27869(int& val) {
    const int x = 27869;
    std::cout << 27869;
    for (int i = 0; i < 27869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27870(int& val) {
    func_in_27870(val);
    const int x = 27870;
    std::cout << 27870;
    for (int i = 0; i < 27870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27870(int& val) {
    const int x = 27870;
    std::cout << 27870;
    for (int i = 0; i < 27870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27871(int& val) {
    func_in_27871(val);
    const int x = 27871;
    std::cout << 27871;
    for (int i = 0; i < 27871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27871(int& val) {
    const int x = 27871;
    std::cout << 27871;
    for (int i = 0; i < 27871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27872(int& val) {
    func_in_27872(val);
    const int x = 27872;
    std::cout << 27872;
    for (int i = 0; i < 27872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27872(int& val) {
    const int x = 27872;
    std::cout << 27872;
    for (int i = 0; i < 27872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27873(int& val) {
    func_in_27873(val);
    const int x = 27873;
    std::cout << 27873;
    for (int i = 0; i < 27873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27873(int& val) {
    const int x = 27873;
    std::cout << 27873;
    for (int i = 0; i < 27873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27874(int& val) {
    func_in_27874(val);
    const int x = 27874;
    std::cout << 27874;
    for (int i = 0; i < 27874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27874(int& val) {
    const int x = 27874;
    std::cout << 27874;
    for (int i = 0; i < 27874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27875(int& val) {
    func_in_27875(val);
    const int x = 27875;
    std::cout << 27875;
    for (int i = 0; i < 27875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27875(int& val) {
    const int x = 27875;
    std::cout << 27875;
    for (int i = 0; i < 27875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27876(int& val) {
    func_in_27876(val);
    const int x = 27876;
    std::cout << 27876;
    for (int i = 0; i < 27876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27876(int& val) {
    const int x = 27876;
    std::cout << 27876;
    for (int i = 0; i < 27876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27877(int& val) {
    func_in_27877(val);
    const int x = 27877;
    std::cout << 27877;
    for (int i = 0; i < 27877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27877(int& val) {
    const int x = 27877;
    std::cout << 27877;
    for (int i = 0; i < 27877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27878(int& val) {
    func_in_27878(val);
    const int x = 27878;
    std::cout << 27878;
    for (int i = 0; i < 27878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27878(int& val) {
    const int x = 27878;
    std::cout << 27878;
    for (int i = 0; i < 27878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27879(int& val) {
    func_in_27879(val);
    const int x = 27879;
    std::cout << 27879;
    for (int i = 0; i < 27879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27879(int& val) {
    const int x = 27879;
    std::cout << 27879;
    for (int i = 0; i < 27879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27880(int& val) {
    func_in_27880(val);
    const int x = 27880;
    std::cout << 27880;
    for (int i = 0; i < 27880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27880(int& val) {
    const int x = 27880;
    std::cout << 27880;
    for (int i = 0; i < 27880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27881(int& val) {
    func_in_27881(val);
    const int x = 27881;
    std::cout << 27881;
    for (int i = 0; i < 27881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27881(int& val) {
    const int x = 27881;
    std::cout << 27881;
    for (int i = 0; i < 27881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27882(int& val) {
    func_in_27882(val);
    const int x = 27882;
    std::cout << 27882;
    for (int i = 0; i < 27882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27882(int& val) {
    const int x = 27882;
    std::cout << 27882;
    for (int i = 0; i < 27882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27883(int& val) {
    func_in_27883(val);
    const int x = 27883;
    std::cout << 27883;
    for (int i = 0; i < 27883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27883(int& val) {
    const int x = 27883;
    std::cout << 27883;
    for (int i = 0; i < 27883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27884(int& val) {
    func_in_27884(val);
    const int x = 27884;
    std::cout << 27884;
    for (int i = 0; i < 27884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27884(int& val) {
    const int x = 27884;
    std::cout << 27884;
    for (int i = 0; i < 27884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27885(int& val) {
    func_in_27885(val);
    const int x = 27885;
    std::cout << 27885;
    for (int i = 0; i < 27885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27885(int& val) {
    const int x = 27885;
    std::cout << 27885;
    for (int i = 0; i < 27885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27886(int& val) {
    func_in_27886(val);
    const int x = 27886;
    std::cout << 27886;
    for (int i = 0; i < 27886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27886(int& val) {
    const int x = 27886;
    std::cout << 27886;
    for (int i = 0; i < 27886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27887(int& val) {
    func_in_27887(val);
    const int x = 27887;
    std::cout << 27887;
    for (int i = 0; i < 27887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27887(int& val) {
    const int x = 27887;
    std::cout << 27887;
    for (int i = 0; i < 27887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27888(int& val) {
    func_in_27888(val);
    const int x = 27888;
    std::cout << 27888;
    for (int i = 0; i < 27888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27888(int& val) {
    const int x = 27888;
    std::cout << 27888;
    for (int i = 0; i < 27888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27889(int& val) {
    func_in_27889(val);
    const int x = 27889;
    std::cout << 27889;
    for (int i = 0; i < 27889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27889(int& val) {
    const int x = 27889;
    std::cout << 27889;
    for (int i = 0; i < 27889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27890(int& val) {
    func_in_27890(val);
    const int x = 27890;
    std::cout << 27890;
    for (int i = 0; i < 27890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27890(int& val) {
    const int x = 27890;
    std::cout << 27890;
    for (int i = 0; i < 27890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27891(int& val) {
    func_in_27891(val);
    const int x = 27891;
    std::cout << 27891;
    for (int i = 0; i < 27891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27891(int& val) {
    const int x = 27891;
    std::cout << 27891;
    for (int i = 0; i < 27891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27892(int& val) {
    func_in_27892(val);
    const int x = 27892;
    std::cout << 27892;
    for (int i = 0; i < 27892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27892(int& val) {
    const int x = 27892;
    std::cout << 27892;
    for (int i = 0; i < 27892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27893(int& val) {
    func_in_27893(val);
    const int x = 27893;
    std::cout << 27893;
    for (int i = 0; i < 27893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27893(int& val) {
    const int x = 27893;
    std::cout << 27893;
    for (int i = 0; i < 27893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27894(int& val) {
    func_in_27894(val);
    const int x = 27894;
    std::cout << 27894;
    for (int i = 0; i < 27894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27894(int& val) {
    const int x = 27894;
    std::cout << 27894;
    for (int i = 0; i < 27894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27895(int& val) {
    func_in_27895(val);
    const int x = 27895;
    std::cout << 27895;
    for (int i = 0; i < 27895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27895(int& val) {
    const int x = 27895;
    std::cout << 27895;
    for (int i = 0; i < 27895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27896(int& val) {
    func_in_27896(val);
    const int x = 27896;
    std::cout << 27896;
    for (int i = 0; i < 27896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27896(int& val) {
    const int x = 27896;
    std::cout << 27896;
    for (int i = 0; i < 27896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27897(int& val) {
    func_in_27897(val);
    const int x = 27897;
    std::cout << 27897;
    for (int i = 0; i < 27897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27897(int& val) {
    const int x = 27897;
    std::cout << 27897;
    for (int i = 0; i < 27897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27898(int& val) {
    func_in_27898(val);
    const int x = 27898;
    std::cout << 27898;
    for (int i = 0; i < 27898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27898(int& val) {
    const int x = 27898;
    std::cout << 27898;
    for (int i = 0; i < 27898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27899(int& val) {
    func_in_27899(val);
    const int x = 27899;
    std::cout << 27899;
    for (int i = 0; i < 27899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27899(int& val) {
    const int x = 27899;
    std::cout << 27899;
    for (int i = 0; i < 27899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27900(int& val) {
    func_in_27900(val);
    const int x = 27900;
    std::cout << 27900;
    for (int i = 0; i < 27900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27900(int& val) {
    const int x = 27900;
    std::cout << 27900;
    for (int i = 0; i < 27900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27901(int& val) {
    func_in_27901(val);
    const int x = 27901;
    std::cout << 27901;
    for (int i = 0; i < 27901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27901(int& val) {
    const int x = 27901;
    std::cout << 27901;
    for (int i = 0; i < 27901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27902(int& val) {
    func_in_27902(val);
    const int x = 27902;
    std::cout << 27902;
    for (int i = 0; i < 27902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27902(int& val) {
    const int x = 27902;
    std::cout << 27902;
    for (int i = 0; i < 27902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27903(int& val) {
    func_in_27903(val);
    const int x = 27903;
    std::cout << 27903;
    for (int i = 0; i < 27903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27903(int& val) {
    const int x = 27903;
    std::cout << 27903;
    for (int i = 0; i < 27903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27904(int& val) {
    func_in_27904(val);
    const int x = 27904;
    std::cout << 27904;
    for (int i = 0; i < 27904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27904(int& val) {
    const int x = 27904;
    std::cout << 27904;
    for (int i = 0; i < 27904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27905(int& val) {
    func_in_27905(val);
    const int x = 27905;
    std::cout << 27905;
    for (int i = 0; i < 27905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27905(int& val) {
    const int x = 27905;
    std::cout << 27905;
    for (int i = 0; i < 27905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27906(int& val) {
    func_in_27906(val);
    const int x = 27906;
    std::cout << 27906;
    for (int i = 0; i < 27906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27906(int& val) {
    const int x = 27906;
    std::cout << 27906;
    for (int i = 0; i < 27906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27907(int& val) {
    func_in_27907(val);
    const int x = 27907;
    std::cout << 27907;
    for (int i = 0; i < 27907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27907(int& val) {
    const int x = 27907;
    std::cout << 27907;
    for (int i = 0; i < 27907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27908(int& val) {
    func_in_27908(val);
    const int x = 27908;
    std::cout << 27908;
    for (int i = 0; i < 27908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27908(int& val) {
    const int x = 27908;
    std::cout << 27908;
    for (int i = 0; i < 27908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27909(int& val) {
    func_in_27909(val);
    const int x = 27909;
    std::cout << 27909;
    for (int i = 0; i < 27909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27909(int& val) {
    const int x = 27909;
    std::cout << 27909;
    for (int i = 0; i < 27909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27910(int& val) {
    func_in_27910(val);
    const int x = 27910;
    std::cout << 27910;
    for (int i = 0; i < 27910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27910(int& val) {
    const int x = 27910;
    std::cout << 27910;
    for (int i = 0; i < 27910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27911(int& val) {
    func_in_27911(val);
    const int x = 27911;
    std::cout << 27911;
    for (int i = 0; i < 27911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27911(int& val) {
    const int x = 27911;
    std::cout << 27911;
    for (int i = 0; i < 27911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27912(int& val) {
    func_in_27912(val);
    const int x = 27912;
    std::cout << 27912;
    for (int i = 0; i < 27912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27912(int& val) {
    const int x = 27912;
    std::cout << 27912;
    for (int i = 0; i < 27912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27913(int& val) {
    func_in_27913(val);
    const int x = 27913;
    std::cout << 27913;
    for (int i = 0; i < 27913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27913(int& val) {
    const int x = 27913;
    std::cout << 27913;
    for (int i = 0; i < 27913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27914(int& val) {
    func_in_27914(val);
    const int x = 27914;
    std::cout << 27914;
    for (int i = 0; i < 27914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27914(int& val) {
    const int x = 27914;
    std::cout << 27914;
    for (int i = 0; i < 27914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27915(int& val) {
    func_in_27915(val);
    const int x = 27915;
    std::cout << 27915;
    for (int i = 0; i < 27915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27915(int& val) {
    const int x = 27915;
    std::cout << 27915;
    for (int i = 0; i < 27915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27916(int& val) {
    func_in_27916(val);
    const int x = 27916;
    std::cout << 27916;
    for (int i = 0; i < 27916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27916(int& val) {
    const int x = 27916;
    std::cout << 27916;
    for (int i = 0; i < 27916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27917(int& val) {
    func_in_27917(val);
    const int x = 27917;
    std::cout << 27917;
    for (int i = 0; i < 27917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27917(int& val) {
    const int x = 27917;
    std::cout << 27917;
    for (int i = 0; i < 27917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27918(int& val) {
    func_in_27918(val);
    const int x = 27918;
    std::cout << 27918;
    for (int i = 0; i < 27918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27918(int& val) {
    const int x = 27918;
    std::cout << 27918;
    for (int i = 0; i < 27918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27919(int& val) {
    func_in_27919(val);
    const int x = 27919;
    std::cout << 27919;
    for (int i = 0; i < 27919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27919(int& val) {
    const int x = 27919;
    std::cout << 27919;
    for (int i = 0; i < 27919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27920(int& val) {
    func_in_27920(val);
    const int x = 27920;
    std::cout << 27920;
    for (int i = 0; i < 27920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27920(int& val) {
    const int x = 27920;
    std::cout << 27920;
    for (int i = 0; i < 27920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27921(int& val) {
    func_in_27921(val);
    const int x = 27921;
    std::cout << 27921;
    for (int i = 0; i < 27921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27921(int& val) {
    const int x = 27921;
    std::cout << 27921;
    for (int i = 0; i < 27921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27922(int& val) {
    func_in_27922(val);
    const int x = 27922;
    std::cout << 27922;
    for (int i = 0; i < 27922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27922(int& val) {
    const int x = 27922;
    std::cout << 27922;
    for (int i = 0; i < 27922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27923(int& val) {
    func_in_27923(val);
    const int x = 27923;
    std::cout << 27923;
    for (int i = 0; i < 27923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27923(int& val) {
    const int x = 27923;
    std::cout << 27923;
    for (int i = 0; i < 27923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27924(int& val) {
    func_in_27924(val);
    const int x = 27924;
    std::cout << 27924;
    for (int i = 0; i < 27924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27924(int& val) {
    const int x = 27924;
    std::cout << 27924;
    for (int i = 0; i < 27924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27925(int& val) {
    func_in_27925(val);
    const int x = 27925;
    std::cout << 27925;
    for (int i = 0; i < 27925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27925(int& val) {
    const int x = 27925;
    std::cout << 27925;
    for (int i = 0; i < 27925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27926(int& val) {
    func_in_27926(val);
    const int x = 27926;
    std::cout << 27926;
    for (int i = 0; i < 27926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27926(int& val) {
    const int x = 27926;
    std::cout << 27926;
    for (int i = 0; i < 27926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27927(int& val) {
    func_in_27927(val);
    const int x = 27927;
    std::cout << 27927;
    for (int i = 0; i < 27927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27927(int& val) {
    const int x = 27927;
    std::cout << 27927;
    for (int i = 0; i < 27927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27928(int& val) {
    func_in_27928(val);
    const int x = 27928;
    std::cout << 27928;
    for (int i = 0; i < 27928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27928(int& val) {
    const int x = 27928;
    std::cout << 27928;
    for (int i = 0; i < 27928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27929(int& val) {
    func_in_27929(val);
    const int x = 27929;
    std::cout << 27929;
    for (int i = 0; i < 27929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27929(int& val) {
    const int x = 27929;
    std::cout << 27929;
    for (int i = 0; i < 27929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27930(int& val) {
    func_in_27930(val);
    const int x = 27930;
    std::cout << 27930;
    for (int i = 0; i < 27930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27930(int& val) {
    const int x = 27930;
    std::cout << 27930;
    for (int i = 0; i < 27930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27931(int& val) {
    func_in_27931(val);
    const int x = 27931;
    std::cout << 27931;
    for (int i = 0; i < 27931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27931(int& val) {
    const int x = 27931;
    std::cout << 27931;
    for (int i = 0; i < 27931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27932(int& val) {
    func_in_27932(val);
    const int x = 27932;
    std::cout << 27932;
    for (int i = 0; i < 27932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27932(int& val) {
    const int x = 27932;
    std::cout << 27932;
    for (int i = 0; i < 27932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27933(int& val) {
    func_in_27933(val);
    const int x = 27933;
    std::cout << 27933;
    for (int i = 0; i < 27933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27933(int& val) {
    const int x = 27933;
    std::cout << 27933;
    for (int i = 0; i < 27933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27934(int& val) {
    func_in_27934(val);
    const int x = 27934;
    std::cout << 27934;
    for (int i = 0; i < 27934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27934(int& val) {
    const int x = 27934;
    std::cout << 27934;
    for (int i = 0; i < 27934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27935(int& val) {
    func_in_27935(val);
    const int x = 27935;
    std::cout << 27935;
    for (int i = 0; i < 27935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27935(int& val) {
    const int x = 27935;
    std::cout << 27935;
    for (int i = 0; i < 27935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27936(int& val) {
    func_in_27936(val);
    const int x = 27936;
    std::cout << 27936;
    for (int i = 0; i < 27936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27936(int& val) {
    const int x = 27936;
    std::cout << 27936;
    for (int i = 0; i < 27936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27937(int& val) {
    func_in_27937(val);
    const int x = 27937;
    std::cout << 27937;
    for (int i = 0; i < 27937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27937(int& val) {
    const int x = 27937;
    std::cout << 27937;
    for (int i = 0; i < 27937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27938(int& val) {
    func_in_27938(val);
    const int x = 27938;
    std::cout << 27938;
    for (int i = 0; i < 27938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27938(int& val) {
    const int x = 27938;
    std::cout << 27938;
    for (int i = 0; i < 27938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27939(int& val) {
    func_in_27939(val);
    const int x = 27939;
    std::cout << 27939;
    for (int i = 0; i < 27939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27939(int& val) {
    const int x = 27939;
    std::cout << 27939;
    for (int i = 0; i < 27939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27940(int& val) {
    func_in_27940(val);
    const int x = 27940;
    std::cout << 27940;
    for (int i = 0; i < 27940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27940(int& val) {
    const int x = 27940;
    std::cout << 27940;
    for (int i = 0; i < 27940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27941(int& val) {
    func_in_27941(val);
    const int x = 27941;
    std::cout << 27941;
    for (int i = 0; i < 27941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27941(int& val) {
    const int x = 27941;
    std::cout << 27941;
    for (int i = 0; i < 27941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27942(int& val) {
    func_in_27942(val);
    const int x = 27942;
    std::cout << 27942;
    for (int i = 0; i < 27942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27942(int& val) {
    const int x = 27942;
    std::cout << 27942;
    for (int i = 0; i < 27942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27943(int& val) {
    func_in_27943(val);
    const int x = 27943;
    std::cout << 27943;
    for (int i = 0; i < 27943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27943(int& val) {
    const int x = 27943;
    std::cout << 27943;
    for (int i = 0; i < 27943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27944(int& val) {
    func_in_27944(val);
    const int x = 27944;
    std::cout << 27944;
    for (int i = 0; i < 27944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27944(int& val) {
    const int x = 27944;
    std::cout << 27944;
    for (int i = 0; i < 27944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27945(int& val) {
    func_in_27945(val);
    const int x = 27945;
    std::cout << 27945;
    for (int i = 0; i < 27945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27945(int& val) {
    const int x = 27945;
    std::cout << 27945;
    for (int i = 0; i < 27945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27946(int& val) {
    func_in_27946(val);
    const int x = 27946;
    std::cout << 27946;
    for (int i = 0; i < 27946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27946(int& val) {
    const int x = 27946;
    std::cout << 27946;
    for (int i = 0; i < 27946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27947(int& val) {
    func_in_27947(val);
    const int x = 27947;
    std::cout << 27947;
    for (int i = 0; i < 27947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27947(int& val) {
    const int x = 27947;
    std::cout << 27947;
    for (int i = 0; i < 27947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27948(int& val) {
    func_in_27948(val);
    const int x = 27948;
    std::cout << 27948;
    for (int i = 0; i < 27948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27948(int& val) {
    const int x = 27948;
    std::cout << 27948;
    for (int i = 0; i < 27948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27949(int& val) {
    func_in_27949(val);
    const int x = 27949;
    std::cout << 27949;
    for (int i = 0; i < 27949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27949(int& val) {
    const int x = 27949;
    std::cout << 27949;
    for (int i = 0; i < 27949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27950(int& val) {
    func_in_27950(val);
    const int x = 27950;
    std::cout << 27950;
    for (int i = 0; i < 27950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27950(int& val) {
    const int x = 27950;
    std::cout << 27950;
    for (int i = 0; i < 27950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27951(int& val) {
    func_in_27951(val);
    const int x = 27951;
    std::cout << 27951;
    for (int i = 0; i < 27951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27951(int& val) {
    const int x = 27951;
    std::cout << 27951;
    for (int i = 0; i < 27951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27952(int& val) {
    func_in_27952(val);
    const int x = 27952;
    std::cout << 27952;
    for (int i = 0; i < 27952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27952(int& val) {
    const int x = 27952;
    std::cout << 27952;
    for (int i = 0; i < 27952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27953(int& val) {
    func_in_27953(val);
    const int x = 27953;
    std::cout << 27953;
    for (int i = 0; i < 27953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27953(int& val) {
    const int x = 27953;
    std::cout << 27953;
    for (int i = 0; i < 27953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27954(int& val) {
    func_in_27954(val);
    const int x = 27954;
    std::cout << 27954;
    for (int i = 0; i < 27954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27954(int& val) {
    const int x = 27954;
    std::cout << 27954;
    for (int i = 0; i < 27954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27955(int& val) {
    func_in_27955(val);
    const int x = 27955;
    std::cout << 27955;
    for (int i = 0; i < 27955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27955(int& val) {
    const int x = 27955;
    std::cout << 27955;
    for (int i = 0; i < 27955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27956(int& val) {
    func_in_27956(val);
    const int x = 27956;
    std::cout << 27956;
    for (int i = 0; i < 27956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27956(int& val) {
    const int x = 27956;
    std::cout << 27956;
    for (int i = 0; i < 27956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27957(int& val) {
    func_in_27957(val);
    const int x = 27957;
    std::cout << 27957;
    for (int i = 0; i < 27957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27957(int& val) {
    const int x = 27957;
    std::cout << 27957;
    for (int i = 0; i < 27957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27958(int& val) {
    func_in_27958(val);
    const int x = 27958;
    std::cout << 27958;
    for (int i = 0; i < 27958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27958(int& val) {
    const int x = 27958;
    std::cout << 27958;
    for (int i = 0; i < 27958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27959(int& val) {
    func_in_27959(val);
    const int x = 27959;
    std::cout << 27959;
    for (int i = 0; i < 27959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27959(int& val) {
    const int x = 27959;
    std::cout << 27959;
    for (int i = 0; i < 27959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27960(int& val) {
    func_in_27960(val);
    const int x = 27960;
    std::cout << 27960;
    for (int i = 0; i < 27960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27960(int& val) {
    const int x = 27960;
    std::cout << 27960;
    for (int i = 0; i < 27960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27961(int& val) {
    func_in_27961(val);
    const int x = 27961;
    std::cout << 27961;
    for (int i = 0; i < 27961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27961(int& val) {
    const int x = 27961;
    std::cout << 27961;
    for (int i = 0; i < 27961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27962(int& val) {
    func_in_27962(val);
    const int x = 27962;
    std::cout << 27962;
    for (int i = 0; i < 27962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27962(int& val) {
    const int x = 27962;
    std::cout << 27962;
    for (int i = 0; i < 27962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27963(int& val) {
    func_in_27963(val);
    const int x = 27963;
    std::cout << 27963;
    for (int i = 0; i < 27963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27963(int& val) {
    const int x = 27963;
    std::cout << 27963;
    for (int i = 0; i < 27963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27964(int& val) {
    func_in_27964(val);
    const int x = 27964;
    std::cout << 27964;
    for (int i = 0; i < 27964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27964(int& val) {
    const int x = 27964;
    std::cout << 27964;
    for (int i = 0; i < 27964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27965(int& val) {
    func_in_27965(val);
    const int x = 27965;
    std::cout << 27965;
    for (int i = 0; i < 27965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27965(int& val) {
    const int x = 27965;
    std::cout << 27965;
    for (int i = 0; i < 27965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27966(int& val) {
    func_in_27966(val);
    const int x = 27966;
    std::cout << 27966;
    for (int i = 0; i < 27966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27966(int& val) {
    const int x = 27966;
    std::cout << 27966;
    for (int i = 0; i < 27966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27967(int& val) {
    func_in_27967(val);
    const int x = 27967;
    std::cout << 27967;
    for (int i = 0; i < 27967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27967(int& val) {
    const int x = 27967;
    std::cout << 27967;
    for (int i = 0; i < 27967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27968(int& val) {
    func_in_27968(val);
    const int x = 27968;
    std::cout << 27968;
    for (int i = 0; i < 27968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27968(int& val) {
    const int x = 27968;
    std::cout << 27968;
    for (int i = 0; i < 27968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27969(int& val) {
    func_in_27969(val);
    const int x = 27969;
    std::cout << 27969;
    for (int i = 0; i < 27969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27969(int& val) {
    const int x = 27969;
    std::cout << 27969;
    for (int i = 0; i < 27969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27970(int& val) {
    func_in_27970(val);
    const int x = 27970;
    std::cout << 27970;
    for (int i = 0; i < 27970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27970(int& val) {
    const int x = 27970;
    std::cout << 27970;
    for (int i = 0; i < 27970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27971(int& val) {
    func_in_27971(val);
    const int x = 27971;
    std::cout << 27971;
    for (int i = 0; i < 27971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27971(int& val) {
    const int x = 27971;
    std::cout << 27971;
    for (int i = 0; i < 27971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27972(int& val) {
    func_in_27972(val);
    const int x = 27972;
    std::cout << 27972;
    for (int i = 0; i < 27972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27972(int& val) {
    const int x = 27972;
    std::cout << 27972;
    for (int i = 0; i < 27972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27973(int& val) {
    func_in_27973(val);
    const int x = 27973;
    std::cout << 27973;
    for (int i = 0; i < 27973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27973(int& val) {
    const int x = 27973;
    std::cout << 27973;
    for (int i = 0; i < 27973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27974(int& val) {
    func_in_27974(val);
    const int x = 27974;
    std::cout << 27974;
    for (int i = 0; i < 27974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27974(int& val) {
    const int x = 27974;
    std::cout << 27974;
    for (int i = 0; i < 27974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27975(int& val) {
    func_in_27975(val);
    const int x = 27975;
    std::cout << 27975;
    for (int i = 0; i < 27975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27975(int& val) {
    const int x = 27975;
    std::cout << 27975;
    for (int i = 0; i < 27975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27976(int& val) {
    func_in_27976(val);
    const int x = 27976;
    std::cout << 27976;
    for (int i = 0; i < 27976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27976(int& val) {
    const int x = 27976;
    std::cout << 27976;
    for (int i = 0; i < 27976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27977(int& val) {
    func_in_27977(val);
    const int x = 27977;
    std::cout << 27977;
    for (int i = 0; i < 27977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27977(int& val) {
    const int x = 27977;
    std::cout << 27977;
    for (int i = 0; i < 27977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27978(int& val) {
    func_in_27978(val);
    const int x = 27978;
    std::cout << 27978;
    for (int i = 0; i < 27978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27978(int& val) {
    const int x = 27978;
    std::cout << 27978;
    for (int i = 0; i < 27978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27979(int& val) {
    func_in_27979(val);
    const int x = 27979;
    std::cout << 27979;
    for (int i = 0; i < 27979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27979(int& val) {
    const int x = 27979;
    std::cout << 27979;
    for (int i = 0; i < 27979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27980(int& val) {
    func_in_27980(val);
    const int x = 27980;
    std::cout << 27980;
    for (int i = 0; i < 27980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27980(int& val) {
    const int x = 27980;
    std::cout << 27980;
    for (int i = 0; i < 27980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27981(int& val) {
    func_in_27981(val);
    const int x = 27981;
    std::cout << 27981;
    for (int i = 0; i < 27981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27981(int& val) {
    const int x = 27981;
    std::cout << 27981;
    for (int i = 0; i < 27981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27982(int& val) {
    func_in_27982(val);
    const int x = 27982;
    std::cout << 27982;
    for (int i = 0; i < 27982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27982(int& val) {
    const int x = 27982;
    std::cout << 27982;
    for (int i = 0; i < 27982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27983(int& val) {
    func_in_27983(val);
    const int x = 27983;
    std::cout << 27983;
    for (int i = 0; i < 27983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27983(int& val) {
    const int x = 27983;
    std::cout << 27983;
    for (int i = 0; i < 27983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27984(int& val) {
    func_in_27984(val);
    const int x = 27984;
    std::cout << 27984;
    for (int i = 0; i < 27984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27984(int& val) {
    const int x = 27984;
    std::cout << 27984;
    for (int i = 0; i < 27984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27985(int& val) {
    func_in_27985(val);
    const int x = 27985;
    std::cout << 27985;
    for (int i = 0; i < 27985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27985(int& val) {
    const int x = 27985;
    std::cout << 27985;
    for (int i = 0; i < 27985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27986(int& val) {
    func_in_27986(val);
    const int x = 27986;
    std::cout << 27986;
    for (int i = 0; i < 27986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27986(int& val) {
    const int x = 27986;
    std::cout << 27986;
    for (int i = 0; i < 27986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27987(int& val) {
    func_in_27987(val);
    const int x = 27987;
    std::cout << 27987;
    for (int i = 0; i < 27987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27987(int& val) {
    const int x = 27987;
    std::cout << 27987;
    for (int i = 0; i < 27987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27988(int& val) {
    func_in_27988(val);
    const int x = 27988;
    std::cout << 27988;
    for (int i = 0; i < 27988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27988(int& val) {
    const int x = 27988;
    std::cout << 27988;
    for (int i = 0; i < 27988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27989(int& val) {
    func_in_27989(val);
    const int x = 27989;
    std::cout << 27989;
    for (int i = 0; i < 27989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27989(int& val) {
    const int x = 27989;
    std::cout << 27989;
    for (int i = 0; i < 27989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27990(int& val) {
    func_in_27990(val);
    const int x = 27990;
    std::cout << 27990;
    for (int i = 0; i < 27990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27990(int& val) {
    const int x = 27990;
    std::cout << 27990;
    for (int i = 0; i < 27990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27991(int& val) {
    func_in_27991(val);
    const int x = 27991;
    std::cout << 27991;
    for (int i = 0; i < 27991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27991(int& val) {
    const int x = 27991;
    std::cout << 27991;
    for (int i = 0; i < 27991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27992(int& val) {
    func_in_27992(val);
    const int x = 27992;
    std::cout << 27992;
    for (int i = 0; i < 27992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27992(int& val) {
    const int x = 27992;
    std::cout << 27992;
    for (int i = 0; i < 27992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27993(int& val) {
    func_in_27993(val);
    const int x = 27993;
    std::cout << 27993;
    for (int i = 0; i < 27993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27993(int& val) {
    const int x = 27993;
    std::cout << 27993;
    for (int i = 0; i < 27993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27994(int& val) {
    func_in_27994(val);
    const int x = 27994;
    std::cout << 27994;
    for (int i = 0; i < 27994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27994(int& val) {
    const int x = 27994;
    std::cout << 27994;
    for (int i = 0; i < 27994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27995(int& val) {
    func_in_27995(val);
    const int x = 27995;
    std::cout << 27995;
    for (int i = 0; i < 27995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27995(int& val) {
    const int x = 27995;
    std::cout << 27995;
    for (int i = 0; i < 27995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27996(int& val) {
    func_in_27996(val);
    const int x = 27996;
    std::cout << 27996;
    for (int i = 0; i < 27996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27996(int& val) {
    const int x = 27996;
    std::cout << 27996;
    for (int i = 0; i < 27996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27997(int& val) {
    func_in_27997(val);
    const int x = 27997;
    std::cout << 27997;
    for (int i = 0; i < 27997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27997(int& val) {
    const int x = 27997;
    std::cout << 27997;
    for (int i = 0; i < 27997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27998(int& val) {
    func_in_27998(val);
    const int x = 27998;
    std::cout << 27998;
    for (int i = 0; i < 27998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27998(int& val) {
    const int x = 27998;
    std::cout << 27998;
    for (int i = 0; i < 27998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27999(int& val) {
    func_in_27999(val);
    const int x = 27999;
    std::cout << 27999;
    for (int i = 0; i < 27999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27999(int& val) {
    const int x = 27999;
    std::cout << 27999;
    for (int i = 0; i < 27999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

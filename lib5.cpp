#include "lib.h"
#include <iostream>
int func_20000(int& val) {
    func_in_20000(val);
    const int x = 20000;
    std::cout << 20000;
    for (int i = 0; i < 20000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20000(int& val) {
    const int x = 20000;
    std::cout << 20000;
    for (int i = 0; i < 20000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20001(int& val) {
    func_in_20001(val);
    const int x = 20001;
    std::cout << 20001;
    for (int i = 0; i < 20001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20001(int& val) {
    const int x = 20001;
    std::cout << 20001;
    for (int i = 0; i < 20001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20002(int& val) {
    func_in_20002(val);
    const int x = 20002;
    std::cout << 20002;
    for (int i = 0; i < 20002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20002(int& val) {
    const int x = 20002;
    std::cout << 20002;
    for (int i = 0; i < 20002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20003(int& val) {
    func_in_20003(val);
    const int x = 20003;
    std::cout << 20003;
    for (int i = 0; i < 20003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20003(int& val) {
    const int x = 20003;
    std::cout << 20003;
    for (int i = 0; i < 20003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20004(int& val) {
    func_in_20004(val);
    const int x = 20004;
    std::cout << 20004;
    for (int i = 0; i < 20004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20004(int& val) {
    const int x = 20004;
    std::cout << 20004;
    for (int i = 0; i < 20004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20005(int& val) {
    func_in_20005(val);
    const int x = 20005;
    std::cout << 20005;
    for (int i = 0; i < 20005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20005(int& val) {
    const int x = 20005;
    std::cout << 20005;
    for (int i = 0; i < 20005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20006(int& val) {
    func_in_20006(val);
    const int x = 20006;
    std::cout << 20006;
    for (int i = 0; i < 20006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20006(int& val) {
    const int x = 20006;
    std::cout << 20006;
    for (int i = 0; i < 20006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20007(int& val) {
    func_in_20007(val);
    const int x = 20007;
    std::cout << 20007;
    for (int i = 0; i < 20007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20007(int& val) {
    const int x = 20007;
    std::cout << 20007;
    for (int i = 0; i < 20007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20008(int& val) {
    func_in_20008(val);
    const int x = 20008;
    std::cout << 20008;
    for (int i = 0; i < 20008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20008(int& val) {
    const int x = 20008;
    std::cout << 20008;
    for (int i = 0; i < 20008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20009(int& val) {
    func_in_20009(val);
    const int x = 20009;
    std::cout << 20009;
    for (int i = 0; i < 20009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20009(int& val) {
    const int x = 20009;
    std::cout << 20009;
    for (int i = 0; i < 20009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20010(int& val) {
    func_in_20010(val);
    const int x = 20010;
    std::cout << 20010;
    for (int i = 0; i < 20010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20010(int& val) {
    const int x = 20010;
    std::cout << 20010;
    for (int i = 0; i < 20010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20011(int& val) {
    func_in_20011(val);
    const int x = 20011;
    std::cout << 20011;
    for (int i = 0; i < 20011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20011(int& val) {
    const int x = 20011;
    std::cout << 20011;
    for (int i = 0; i < 20011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20012(int& val) {
    func_in_20012(val);
    const int x = 20012;
    std::cout << 20012;
    for (int i = 0; i < 20012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20012(int& val) {
    const int x = 20012;
    std::cout << 20012;
    for (int i = 0; i < 20012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20013(int& val) {
    func_in_20013(val);
    const int x = 20013;
    std::cout << 20013;
    for (int i = 0; i < 20013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20013(int& val) {
    const int x = 20013;
    std::cout << 20013;
    for (int i = 0; i < 20013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20014(int& val) {
    func_in_20014(val);
    const int x = 20014;
    std::cout << 20014;
    for (int i = 0; i < 20014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20014(int& val) {
    const int x = 20014;
    std::cout << 20014;
    for (int i = 0; i < 20014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20015(int& val) {
    func_in_20015(val);
    const int x = 20015;
    std::cout << 20015;
    for (int i = 0; i < 20015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20015(int& val) {
    const int x = 20015;
    std::cout << 20015;
    for (int i = 0; i < 20015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20016(int& val) {
    func_in_20016(val);
    const int x = 20016;
    std::cout << 20016;
    for (int i = 0; i < 20016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20016(int& val) {
    const int x = 20016;
    std::cout << 20016;
    for (int i = 0; i < 20016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20017(int& val) {
    func_in_20017(val);
    const int x = 20017;
    std::cout << 20017;
    for (int i = 0; i < 20017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20017(int& val) {
    const int x = 20017;
    std::cout << 20017;
    for (int i = 0; i < 20017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20018(int& val) {
    func_in_20018(val);
    const int x = 20018;
    std::cout << 20018;
    for (int i = 0; i < 20018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20018(int& val) {
    const int x = 20018;
    std::cout << 20018;
    for (int i = 0; i < 20018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20019(int& val) {
    func_in_20019(val);
    const int x = 20019;
    std::cout << 20019;
    for (int i = 0; i < 20019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20019(int& val) {
    const int x = 20019;
    std::cout << 20019;
    for (int i = 0; i < 20019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20020(int& val) {
    func_in_20020(val);
    const int x = 20020;
    std::cout << 20020;
    for (int i = 0; i < 20020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20020(int& val) {
    const int x = 20020;
    std::cout << 20020;
    for (int i = 0; i < 20020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20021(int& val) {
    func_in_20021(val);
    const int x = 20021;
    std::cout << 20021;
    for (int i = 0; i < 20021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20021(int& val) {
    const int x = 20021;
    std::cout << 20021;
    for (int i = 0; i < 20021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20022(int& val) {
    func_in_20022(val);
    const int x = 20022;
    std::cout << 20022;
    for (int i = 0; i < 20022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20022(int& val) {
    const int x = 20022;
    std::cout << 20022;
    for (int i = 0; i < 20022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20023(int& val) {
    func_in_20023(val);
    const int x = 20023;
    std::cout << 20023;
    for (int i = 0; i < 20023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20023(int& val) {
    const int x = 20023;
    std::cout << 20023;
    for (int i = 0; i < 20023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20024(int& val) {
    func_in_20024(val);
    const int x = 20024;
    std::cout << 20024;
    for (int i = 0; i < 20024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20024(int& val) {
    const int x = 20024;
    std::cout << 20024;
    for (int i = 0; i < 20024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20025(int& val) {
    func_in_20025(val);
    const int x = 20025;
    std::cout << 20025;
    for (int i = 0; i < 20025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20025(int& val) {
    const int x = 20025;
    std::cout << 20025;
    for (int i = 0; i < 20025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20026(int& val) {
    func_in_20026(val);
    const int x = 20026;
    std::cout << 20026;
    for (int i = 0; i < 20026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20026(int& val) {
    const int x = 20026;
    std::cout << 20026;
    for (int i = 0; i < 20026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20027(int& val) {
    func_in_20027(val);
    const int x = 20027;
    std::cout << 20027;
    for (int i = 0; i < 20027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20027(int& val) {
    const int x = 20027;
    std::cout << 20027;
    for (int i = 0; i < 20027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20028(int& val) {
    func_in_20028(val);
    const int x = 20028;
    std::cout << 20028;
    for (int i = 0; i < 20028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20028(int& val) {
    const int x = 20028;
    std::cout << 20028;
    for (int i = 0; i < 20028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20029(int& val) {
    func_in_20029(val);
    const int x = 20029;
    std::cout << 20029;
    for (int i = 0; i < 20029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20029(int& val) {
    const int x = 20029;
    std::cout << 20029;
    for (int i = 0; i < 20029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20030(int& val) {
    func_in_20030(val);
    const int x = 20030;
    std::cout << 20030;
    for (int i = 0; i < 20030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20030(int& val) {
    const int x = 20030;
    std::cout << 20030;
    for (int i = 0; i < 20030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20031(int& val) {
    func_in_20031(val);
    const int x = 20031;
    std::cout << 20031;
    for (int i = 0; i < 20031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20031(int& val) {
    const int x = 20031;
    std::cout << 20031;
    for (int i = 0; i < 20031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20032(int& val) {
    func_in_20032(val);
    const int x = 20032;
    std::cout << 20032;
    for (int i = 0; i < 20032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20032(int& val) {
    const int x = 20032;
    std::cout << 20032;
    for (int i = 0; i < 20032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20033(int& val) {
    func_in_20033(val);
    const int x = 20033;
    std::cout << 20033;
    for (int i = 0; i < 20033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20033(int& val) {
    const int x = 20033;
    std::cout << 20033;
    for (int i = 0; i < 20033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20034(int& val) {
    func_in_20034(val);
    const int x = 20034;
    std::cout << 20034;
    for (int i = 0; i < 20034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20034(int& val) {
    const int x = 20034;
    std::cout << 20034;
    for (int i = 0; i < 20034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20035(int& val) {
    func_in_20035(val);
    const int x = 20035;
    std::cout << 20035;
    for (int i = 0; i < 20035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20035(int& val) {
    const int x = 20035;
    std::cout << 20035;
    for (int i = 0; i < 20035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20036(int& val) {
    func_in_20036(val);
    const int x = 20036;
    std::cout << 20036;
    for (int i = 0; i < 20036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20036(int& val) {
    const int x = 20036;
    std::cout << 20036;
    for (int i = 0; i < 20036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20037(int& val) {
    func_in_20037(val);
    const int x = 20037;
    std::cout << 20037;
    for (int i = 0; i < 20037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20037(int& val) {
    const int x = 20037;
    std::cout << 20037;
    for (int i = 0; i < 20037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20038(int& val) {
    func_in_20038(val);
    const int x = 20038;
    std::cout << 20038;
    for (int i = 0; i < 20038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20038(int& val) {
    const int x = 20038;
    std::cout << 20038;
    for (int i = 0; i < 20038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20039(int& val) {
    func_in_20039(val);
    const int x = 20039;
    std::cout << 20039;
    for (int i = 0; i < 20039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20039(int& val) {
    const int x = 20039;
    std::cout << 20039;
    for (int i = 0; i < 20039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20040(int& val) {
    func_in_20040(val);
    const int x = 20040;
    std::cout << 20040;
    for (int i = 0; i < 20040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20040(int& val) {
    const int x = 20040;
    std::cout << 20040;
    for (int i = 0; i < 20040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20041(int& val) {
    func_in_20041(val);
    const int x = 20041;
    std::cout << 20041;
    for (int i = 0; i < 20041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20041(int& val) {
    const int x = 20041;
    std::cout << 20041;
    for (int i = 0; i < 20041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20042(int& val) {
    func_in_20042(val);
    const int x = 20042;
    std::cout << 20042;
    for (int i = 0; i < 20042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20042(int& val) {
    const int x = 20042;
    std::cout << 20042;
    for (int i = 0; i < 20042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20043(int& val) {
    func_in_20043(val);
    const int x = 20043;
    std::cout << 20043;
    for (int i = 0; i < 20043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20043(int& val) {
    const int x = 20043;
    std::cout << 20043;
    for (int i = 0; i < 20043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20044(int& val) {
    func_in_20044(val);
    const int x = 20044;
    std::cout << 20044;
    for (int i = 0; i < 20044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20044(int& val) {
    const int x = 20044;
    std::cout << 20044;
    for (int i = 0; i < 20044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20045(int& val) {
    func_in_20045(val);
    const int x = 20045;
    std::cout << 20045;
    for (int i = 0; i < 20045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20045(int& val) {
    const int x = 20045;
    std::cout << 20045;
    for (int i = 0; i < 20045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20046(int& val) {
    func_in_20046(val);
    const int x = 20046;
    std::cout << 20046;
    for (int i = 0; i < 20046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20046(int& val) {
    const int x = 20046;
    std::cout << 20046;
    for (int i = 0; i < 20046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20047(int& val) {
    func_in_20047(val);
    const int x = 20047;
    std::cout << 20047;
    for (int i = 0; i < 20047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20047(int& val) {
    const int x = 20047;
    std::cout << 20047;
    for (int i = 0; i < 20047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20048(int& val) {
    func_in_20048(val);
    const int x = 20048;
    std::cout << 20048;
    for (int i = 0; i < 20048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20048(int& val) {
    const int x = 20048;
    std::cout << 20048;
    for (int i = 0; i < 20048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20049(int& val) {
    func_in_20049(val);
    const int x = 20049;
    std::cout << 20049;
    for (int i = 0; i < 20049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20049(int& val) {
    const int x = 20049;
    std::cout << 20049;
    for (int i = 0; i < 20049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20050(int& val) {
    func_in_20050(val);
    const int x = 20050;
    std::cout << 20050;
    for (int i = 0; i < 20050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20050(int& val) {
    const int x = 20050;
    std::cout << 20050;
    for (int i = 0; i < 20050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20051(int& val) {
    func_in_20051(val);
    const int x = 20051;
    std::cout << 20051;
    for (int i = 0; i < 20051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20051(int& val) {
    const int x = 20051;
    std::cout << 20051;
    for (int i = 0; i < 20051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20052(int& val) {
    func_in_20052(val);
    const int x = 20052;
    std::cout << 20052;
    for (int i = 0; i < 20052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20052(int& val) {
    const int x = 20052;
    std::cout << 20052;
    for (int i = 0; i < 20052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20053(int& val) {
    func_in_20053(val);
    const int x = 20053;
    std::cout << 20053;
    for (int i = 0; i < 20053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20053(int& val) {
    const int x = 20053;
    std::cout << 20053;
    for (int i = 0; i < 20053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20054(int& val) {
    func_in_20054(val);
    const int x = 20054;
    std::cout << 20054;
    for (int i = 0; i < 20054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20054(int& val) {
    const int x = 20054;
    std::cout << 20054;
    for (int i = 0; i < 20054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20055(int& val) {
    func_in_20055(val);
    const int x = 20055;
    std::cout << 20055;
    for (int i = 0; i < 20055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20055(int& val) {
    const int x = 20055;
    std::cout << 20055;
    for (int i = 0; i < 20055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20056(int& val) {
    func_in_20056(val);
    const int x = 20056;
    std::cout << 20056;
    for (int i = 0; i < 20056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20056(int& val) {
    const int x = 20056;
    std::cout << 20056;
    for (int i = 0; i < 20056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20057(int& val) {
    func_in_20057(val);
    const int x = 20057;
    std::cout << 20057;
    for (int i = 0; i < 20057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20057(int& val) {
    const int x = 20057;
    std::cout << 20057;
    for (int i = 0; i < 20057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20058(int& val) {
    func_in_20058(val);
    const int x = 20058;
    std::cout << 20058;
    for (int i = 0; i < 20058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20058(int& val) {
    const int x = 20058;
    std::cout << 20058;
    for (int i = 0; i < 20058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20059(int& val) {
    func_in_20059(val);
    const int x = 20059;
    std::cout << 20059;
    for (int i = 0; i < 20059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20059(int& val) {
    const int x = 20059;
    std::cout << 20059;
    for (int i = 0; i < 20059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20060(int& val) {
    func_in_20060(val);
    const int x = 20060;
    std::cout << 20060;
    for (int i = 0; i < 20060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20060(int& val) {
    const int x = 20060;
    std::cout << 20060;
    for (int i = 0; i < 20060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20061(int& val) {
    func_in_20061(val);
    const int x = 20061;
    std::cout << 20061;
    for (int i = 0; i < 20061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20061(int& val) {
    const int x = 20061;
    std::cout << 20061;
    for (int i = 0; i < 20061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20062(int& val) {
    func_in_20062(val);
    const int x = 20062;
    std::cout << 20062;
    for (int i = 0; i < 20062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20062(int& val) {
    const int x = 20062;
    std::cout << 20062;
    for (int i = 0; i < 20062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20063(int& val) {
    func_in_20063(val);
    const int x = 20063;
    std::cout << 20063;
    for (int i = 0; i < 20063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20063(int& val) {
    const int x = 20063;
    std::cout << 20063;
    for (int i = 0; i < 20063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20064(int& val) {
    func_in_20064(val);
    const int x = 20064;
    std::cout << 20064;
    for (int i = 0; i < 20064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20064(int& val) {
    const int x = 20064;
    std::cout << 20064;
    for (int i = 0; i < 20064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20065(int& val) {
    func_in_20065(val);
    const int x = 20065;
    std::cout << 20065;
    for (int i = 0; i < 20065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20065(int& val) {
    const int x = 20065;
    std::cout << 20065;
    for (int i = 0; i < 20065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20066(int& val) {
    func_in_20066(val);
    const int x = 20066;
    std::cout << 20066;
    for (int i = 0; i < 20066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20066(int& val) {
    const int x = 20066;
    std::cout << 20066;
    for (int i = 0; i < 20066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20067(int& val) {
    func_in_20067(val);
    const int x = 20067;
    std::cout << 20067;
    for (int i = 0; i < 20067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20067(int& val) {
    const int x = 20067;
    std::cout << 20067;
    for (int i = 0; i < 20067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20068(int& val) {
    func_in_20068(val);
    const int x = 20068;
    std::cout << 20068;
    for (int i = 0; i < 20068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20068(int& val) {
    const int x = 20068;
    std::cout << 20068;
    for (int i = 0; i < 20068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20069(int& val) {
    func_in_20069(val);
    const int x = 20069;
    std::cout << 20069;
    for (int i = 0; i < 20069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20069(int& val) {
    const int x = 20069;
    std::cout << 20069;
    for (int i = 0; i < 20069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20070(int& val) {
    func_in_20070(val);
    const int x = 20070;
    std::cout << 20070;
    for (int i = 0; i < 20070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20070(int& val) {
    const int x = 20070;
    std::cout << 20070;
    for (int i = 0; i < 20070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20071(int& val) {
    func_in_20071(val);
    const int x = 20071;
    std::cout << 20071;
    for (int i = 0; i < 20071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20071(int& val) {
    const int x = 20071;
    std::cout << 20071;
    for (int i = 0; i < 20071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20072(int& val) {
    func_in_20072(val);
    const int x = 20072;
    std::cout << 20072;
    for (int i = 0; i < 20072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20072(int& val) {
    const int x = 20072;
    std::cout << 20072;
    for (int i = 0; i < 20072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20073(int& val) {
    func_in_20073(val);
    const int x = 20073;
    std::cout << 20073;
    for (int i = 0; i < 20073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20073(int& val) {
    const int x = 20073;
    std::cout << 20073;
    for (int i = 0; i < 20073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20074(int& val) {
    func_in_20074(val);
    const int x = 20074;
    std::cout << 20074;
    for (int i = 0; i < 20074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20074(int& val) {
    const int x = 20074;
    std::cout << 20074;
    for (int i = 0; i < 20074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20075(int& val) {
    func_in_20075(val);
    const int x = 20075;
    std::cout << 20075;
    for (int i = 0; i < 20075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20075(int& val) {
    const int x = 20075;
    std::cout << 20075;
    for (int i = 0; i < 20075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20076(int& val) {
    func_in_20076(val);
    const int x = 20076;
    std::cout << 20076;
    for (int i = 0; i < 20076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20076(int& val) {
    const int x = 20076;
    std::cout << 20076;
    for (int i = 0; i < 20076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20077(int& val) {
    func_in_20077(val);
    const int x = 20077;
    std::cout << 20077;
    for (int i = 0; i < 20077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20077(int& val) {
    const int x = 20077;
    std::cout << 20077;
    for (int i = 0; i < 20077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20078(int& val) {
    func_in_20078(val);
    const int x = 20078;
    std::cout << 20078;
    for (int i = 0; i < 20078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20078(int& val) {
    const int x = 20078;
    std::cout << 20078;
    for (int i = 0; i < 20078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20079(int& val) {
    func_in_20079(val);
    const int x = 20079;
    std::cout << 20079;
    for (int i = 0; i < 20079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20079(int& val) {
    const int x = 20079;
    std::cout << 20079;
    for (int i = 0; i < 20079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20080(int& val) {
    func_in_20080(val);
    const int x = 20080;
    std::cout << 20080;
    for (int i = 0; i < 20080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20080(int& val) {
    const int x = 20080;
    std::cout << 20080;
    for (int i = 0; i < 20080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20081(int& val) {
    func_in_20081(val);
    const int x = 20081;
    std::cout << 20081;
    for (int i = 0; i < 20081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20081(int& val) {
    const int x = 20081;
    std::cout << 20081;
    for (int i = 0; i < 20081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20082(int& val) {
    func_in_20082(val);
    const int x = 20082;
    std::cout << 20082;
    for (int i = 0; i < 20082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20082(int& val) {
    const int x = 20082;
    std::cout << 20082;
    for (int i = 0; i < 20082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20083(int& val) {
    func_in_20083(val);
    const int x = 20083;
    std::cout << 20083;
    for (int i = 0; i < 20083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20083(int& val) {
    const int x = 20083;
    std::cout << 20083;
    for (int i = 0; i < 20083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20084(int& val) {
    func_in_20084(val);
    const int x = 20084;
    std::cout << 20084;
    for (int i = 0; i < 20084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20084(int& val) {
    const int x = 20084;
    std::cout << 20084;
    for (int i = 0; i < 20084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20085(int& val) {
    func_in_20085(val);
    const int x = 20085;
    std::cout << 20085;
    for (int i = 0; i < 20085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20085(int& val) {
    const int x = 20085;
    std::cout << 20085;
    for (int i = 0; i < 20085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20086(int& val) {
    func_in_20086(val);
    const int x = 20086;
    std::cout << 20086;
    for (int i = 0; i < 20086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20086(int& val) {
    const int x = 20086;
    std::cout << 20086;
    for (int i = 0; i < 20086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20087(int& val) {
    func_in_20087(val);
    const int x = 20087;
    std::cout << 20087;
    for (int i = 0; i < 20087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20087(int& val) {
    const int x = 20087;
    std::cout << 20087;
    for (int i = 0; i < 20087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20088(int& val) {
    func_in_20088(val);
    const int x = 20088;
    std::cout << 20088;
    for (int i = 0; i < 20088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20088(int& val) {
    const int x = 20088;
    std::cout << 20088;
    for (int i = 0; i < 20088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20089(int& val) {
    func_in_20089(val);
    const int x = 20089;
    std::cout << 20089;
    for (int i = 0; i < 20089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20089(int& val) {
    const int x = 20089;
    std::cout << 20089;
    for (int i = 0; i < 20089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20090(int& val) {
    func_in_20090(val);
    const int x = 20090;
    std::cout << 20090;
    for (int i = 0; i < 20090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20090(int& val) {
    const int x = 20090;
    std::cout << 20090;
    for (int i = 0; i < 20090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20091(int& val) {
    func_in_20091(val);
    const int x = 20091;
    std::cout << 20091;
    for (int i = 0; i < 20091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20091(int& val) {
    const int x = 20091;
    std::cout << 20091;
    for (int i = 0; i < 20091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20092(int& val) {
    func_in_20092(val);
    const int x = 20092;
    std::cout << 20092;
    for (int i = 0; i < 20092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20092(int& val) {
    const int x = 20092;
    std::cout << 20092;
    for (int i = 0; i < 20092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20093(int& val) {
    func_in_20093(val);
    const int x = 20093;
    std::cout << 20093;
    for (int i = 0; i < 20093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20093(int& val) {
    const int x = 20093;
    std::cout << 20093;
    for (int i = 0; i < 20093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20094(int& val) {
    func_in_20094(val);
    const int x = 20094;
    std::cout << 20094;
    for (int i = 0; i < 20094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20094(int& val) {
    const int x = 20094;
    std::cout << 20094;
    for (int i = 0; i < 20094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20095(int& val) {
    func_in_20095(val);
    const int x = 20095;
    std::cout << 20095;
    for (int i = 0; i < 20095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20095(int& val) {
    const int x = 20095;
    std::cout << 20095;
    for (int i = 0; i < 20095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20096(int& val) {
    func_in_20096(val);
    const int x = 20096;
    std::cout << 20096;
    for (int i = 0; i < 20096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20096(int& val) {
    const int x = 20096;
    std::cout << 20096;
    for (int i = 0; i < 20096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20097(int& val) {
    func_in_20097(val);
    const int x = 20097;
    std::cout << 20097;
    for (int i = 0; i < 20097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20097(int& val) {
    const int x = 20097;
    std::cout << 20097;
    for (int i = 0; i < 20097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20098(int& val) {
    func_in_20098(val);
    const int x = 20098;
    std::cout << 20098;
    for (int i = 0; i < 20098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20098(int& val) {
    const int x = 20098;
    std::cout << 20098;
    for (int i = 0; i < 20098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20099(int& val) {
    func_in_20099(val);
    const int x = 20099;
    std::cout << 20099;
    for (int i = 0; i < 20099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20099(int& val) {
    const int x = 20099;
    std::cout << 20099;
    for (int i = 0; i < 20099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20100(int& val) {
    func_in_20100(val);
    const int x = 20100;
    std::cout << 20100;
    for (int i = 0; i < 20100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20100(int& val) {
    const int x = 20100;
    std::cout << 20100;
    for (int i = 0; i < 20100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20101(int& val) {
    func_in_20101(val);
    const int x = 20101;
    std::cout << 20101;
    for (int i = 0; i < 20101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20101(int& val) {
    const int x = 20101;
    std::cout << 20101;
    for (int i = 0; i < 20101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20102(int& val) {
    func_in_20102(val);
    const int x = 20102;
    std::cout << 20102;
    for (int i = 0; i < 20102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20102(int& val) {
    const int x = 20102;
    std::cout << 20102;
    for (int i = 0; i < 20102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20103(int& val) {
    func_in_20103(val);
    const int x = 20103;
    std::cout << 20103;
    for (int i = 0; i < 20103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20103(int& val) {
    const int x = 20103;
    std::cout << 20103;
    for (int i = 0; i < 20103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20104(int& val) {
    func_in_20104(val);
    const int x = 20104;
    std::cout << 20104;
    for (int i = 0; i < 20104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20104(int& val) {
    const int x = 20104;
    std::cout << 20104;
    for (int i = 0; i < 20104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20105(int& val) {
    func_in_20105(val);
    const int x = 20105;
    std::cout << 20105;
    for (int i = 0; i < 20105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20105(int& val) {
    const int x = 20105;
    std::cout << 20105;
    for (int i = 0; i < 20105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20106(int& val) {
    func_in_20106(val);
    const int x = 20106;
    std::cout << 20106;
    for (int i = 0; i < 20106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20106(int& val) {
    const int x = 20106;
    std::cout << 20106;
    for (int i = 0; i < 20106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20107(int& val) {
    func_in_20107(val);
    const int x = 20107;
    std::cout << 20107;
    for (int i = 0; i < 20107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20107(int& val) {
    const int x = 20107;
    std::cout << 20107;
    for (int i = 0; i < 20107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20108(int& val) {
    func_in_20108(val);
    const int x = 20108;
    std::cout << 20108;
    for (int i = 0; i < 20108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20108(int& val) {
    const int x = 20108;
    std::cout << 20108;
    for (int i = 0; i < 20108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20109(int& val) {
    func_in_20109(val);
    const int x = 20109;
    std::cout << 20109;
    for (int i = 0; i < 20109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20109(int& val) {
    const int x = 20109;
    std::cout << 20109;
    for (int i = 0; i < 20109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20110(int& val) {
    func_in_20110(val);
    const int x = 20110;
    std::cout << 20110;
    for (int i = 0; i < 20110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20110(int& val) {
    const int x = 20110;
    std::cout << 20110;
    for (int i = 0; i < 20110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20111(int& val) {
    func_in_20111(val);
    const int x = 20111;
    std::cout << 20111;
    for (int i = 0; i < 20111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20111(int& val) {
    const int x = 20111;
    std::cout << 20111;
    for (int i = 0; i < 20111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20112(int& val) {
    func_in_20112(val);
    const int x = 20112;
    std::cout << 20112;
    for (int i = 0; i < 20112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20112(int& val) {
    const int x = 20112;
    std::cout << 20112;
    for (int i = 0; i < 20112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20113(int& val) {
    func_in_20113(val);
    const int x = 20113;
    std::cout << 20113;
    for (int i = 0; i < 20113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20113(int& val) {
    const int x = 20113;
    std::cout << 20113;
    for (int i = 0; i < 20113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20114(int& val) {
    func_in_20114(val);
    const int x = 20114;
    std::cout << 20114;
    for (int i = 0; i < 20114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20114(int& val) {
    const int x = 20114;
    std::cout << 20114;
    for (int i = 0; i < 20114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20115(int& val) {
    func_in_20115(val);
    const int x = 20115;
    std::cout << 20115;
    for (int i = 0; i < 20115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20115(int& val) {
    const int x = 20115;
    std::cout << 20115;
    for (int i = 0; i < 20115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20116(int& val) {
    func_in_20116(val);
    const int x = 20116;
    std::cout << 20116;
    for (int i = 0; i < 20116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20116(int& val) {
    const int x = 20116;
    std::cout << 20116;
    for (int i = 0; i < 20116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20117(int& val) {
    func_in_20117(val);
    const int x = 20117;
    std::cout << 20117;
    for (int i = 0; i < 20117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20117(int& val) {
    const int x = 20117;
    std::cout << 20117;
    for (int i = 0; i < 20117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20118(int& val) {
    func_in_20118(val);
    const int x = 20118;
    std::cout << 20118;
    for (int i = 0; i < 20118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20118(int& val) {
    const int x = 20118;
    std::cout << 20118;
    for (int i = 0; i < 20118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20119(int& val) {
    func_in_20119(val);
    const int x = 20119;
    std::cout << 20119;
    for (int i = 0; i < 20119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20119(int& val) {
    const int x = 20119;
    std::cout << 20119;
    for (int i = 0; i < 20119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20120(int& val) {
    func_in_20120(val);
    const int x = 20120;
    std::cout << 20120;
    for (int i = 0; i < 20120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20120(int& val) {
    const int x = 20120;
    std::cout << 20120;
    for (int i = 0; i < 20120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20121(int& val) {
    func_in_20121(val);
    const int x = 20121;
    std::cout << 20121;
    for (int i = 0; i < 20121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20121(int& val) {
    const int x = 20121;
    std::cout << 20121;
    for (int i = 0; i < 20121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20122(int& val) {
    func_in_20122(val);
    const int x = 20122;
    std::cout << 20122;
    for (int i = 0; i < 20122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20122(int& val) {
    const int x = 20122;
    std::cout << 20122;
    for (int i = 0; i < 20122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20123(int& val) {
    func_in_20123(val);
    const int x = 20123;
    std::cout << 20123;
    for (int i = 0; i < 20123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20123(int& val) {
    const int x = 20123;
    std::cout << 20123;
    for (int i = 0; i < 20123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20124(int& val) {
    func_in_20124(val);
    const int x = 20124;
    std::cout << 20124;
    for (int i = 0; i < 20124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20124(int& val) {
    const int x = 20124;
    std::cout << 20124;
    for (int i = 0; i < 20124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20125(int& val) {
    func_in_20125(val);
    const int x = 20125;
    std::cout << 20125;
    for (int i = 0; i < 20125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20125(int& val) {
    const int x = 20125;
    std::cout << 20125;
    for (int i = 0; i < 20125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20126(int& val) {
    func_in_20126(val);
    const int x = 20126;
    std::cout << 20126;
    for (int i = 0; i < 20126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20126(int& val) {
    const int x = 20126;
    std::cout << 20126;
    for (int i = 0; i < 20126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20127(int& val) {
    func_in_20127(val);
    const int x = 20127;
    std::cout << 20127;
    for (int i = 0; i < 20127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20127(int& val) {
    const int x = 20127;
    std::cout << 20127;
    for (int i = 0; i < 20127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20128(int& val) {
    func_in_20128(val);
    const int x = 20128;
    std::cout << 20128;
    for (int i = 0; i < 20128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20128(int& val) {
    const int x = 20128;
    std::cout << 20128;
    for (int i = 0; i < 20128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20129(int& val) {
    func_in_20129(val);
    const int x = 20129;
    std::cout << 20129;
    for (int i = 0; i < 20129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20129(int& val) {
    const int x = 20129;
    std::cout << 20129;
    for (int i = 0; i < 20129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20130(int& val) {
    func_in_20130(val);
    const int x = 20130;
    std::cout << 20130;
    for (int i = 0; i < 20130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20130(int& val) {
    const int x = 20130;
    std::cout << 20130;
    for (int i = 0; i < 20130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20131(int& val) {
    func_in_20131(val);
    const int x = 20131;
    std::cout << 20131;
    for (int i = 0; i < 20131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20131(int& val) {
    const int x = 20131;
    std::cout << 20131;
    for (int i = 0; i < 20131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20132(int& val) {
    func_in_20132(val);
    const int x = 20132;
    std::cout << 20132;
    for (int i = 0; i < 20132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20132(int& val) {
    const int x = 20132;
    std::cout << 20132;
    for (int i = 0; i < 20132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20133(int& val) {
    func_in_20133(val);
    const int x = 20133;
    std::cout << 20133;
    for (int i = 0; i < 20133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20133(int& val) {
    const int x = 20133;
    std::cout << 20133;
    for (int i = 0; i < 20133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20134(int& val) {
    func_in_20134(val);
    const int x = 20134;
    std::cout << 20134;
    for (int i = 0; i < 20134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20134(int& val) {
    const int x = 20134;
    std::cout << 20134;
    for (int i = 0; i < 20134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20135(int& val) {
    func_in_20135(val);
    const int x = 20135;
    std::cout << 20135;
    for (int i = 0; i < 20135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20135(int& val) {
    const int x = 20135;
    std::cout << 20135;
    for (int i = 0; i < 20135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20136(int& val) {
    func_in_20136(val);
    const int x = 20136;
    std::cout << 20136;
    for (int i = 0; i < 20136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20136(int& val) {
    const int x = 20136;
    std::cout << 20136;
    for (int i = 0; i < 20136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20137(int& val) {
    func_in_20137(val);
    const int x = 20137;
    std::cout << 20137;
    for (int i = 0; i < 20137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20137(int& val) {
    const int x = 20137;
    std::cout << 20137;
    for (int i = 0; i < 20137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20138(int& val) {
    func_in_20138(val);
    const int x = 20138;
    std::cout << 20138;
    for (int i = 0; i < 20138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20138(int& val) {
    const int x = 20138;
    std::cout << 20138;
    for (int i = 0; i < 20138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20139(int& val) {
    func_in_20139(val);
    const int x = 20139;
    std::cout << 20139;
    for (int i = 0; i < 20139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20139(int& val) {
    const int x = 20139;
    std::cout << 20139;
    for (int i = 0; i < 20139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20140(int& val) {
    func_in_20140(val);
    const int x = 20140;
    std::cout << 20140;
    for (int i = 0; i < 20140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20140(int& val) {
    const int x = 20140;
    std::cout << 20140;
    for (int i = 0; i < 20140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20141(int& val) {
    func_in_20141(val);
    const int x = 20141;
    std::cout << 20141;
    for (int i = 0; i < 20141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20141(int& val) {
    const int x = 20141;
    std::cout << 20141;
    for (int i = 0; i < 20141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20142(int& val) {
    func_in_20142(val);
    const int x = 20142;
    std::cout << 20142;
    for (int i = 0; i < 20142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20142(int& val) {
    const int x = 20142;
    std::cout << 20142;
    for (int i = 0; i < 20142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20143(int& val) {
    func_in_20143(val);
    const int x = 20143;
    std::cout << 20143;
    for (int i = 0; i < 20143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20143(int& val) {
    const int x = 20143;
    std::cout << 20143;
    for (int i = 0; i < 20143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20144(int& val) {
    func_in_20144(val);
    const int x = 20144;
    std::cout << 20144;
    for (int i = 0; i < 20144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20144(int& val) {
    const int x = 20144;
    std::cout << 20144;
    for (int i = 0; i < 20144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20145(int& val) {
    func_in_20145(val);
    const int x = 20145;
    std::cout << 20145;
    for (int i = 0; i < 20145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20145(int& val) {
    const int x = 20145;
    std::cout << 20145;
    for (int i = 0; i < 20145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20146(int& val) {
    func_in_20146(val);
    const int x = 20146;
    std::cout << 20146;
    for (int i = 0; i < 20146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20146(int& val) {
    const int x = 20146;
    std::cout << 20146;
    for (int i = 0; i < 20146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20147(int& val) {
    func_in_20147(val);
    const int x = 20147;
    std::cout << 20147;
    for (int i = 0; i < 20147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20147(int& val) {
    const int x = 20147;
    std::cout << 20147;
    for (int i = 0; i < 20147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20148(int& val) {
    func_in_20148(val);
    const int x = 20148;
    std::cout << 20148;
    for (int i = 0; i < 20148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20148(int& val) {
    const int x = 20148;
    std::cout << 20148;
    for (int i = 0; i < 20148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20149(int& val) {
    func_in_20149(val);
    const int x = 20149;
    std::cout << 20149;
    for (int i = 0; i < 20149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20149(int& val) {
    const int x = 20149;
    std::cout << 20149;
    for (int i = 0; i < 20149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20150(int& val) {
    func_in_20150(val);
    const int x = 20150;
    std::cout << 20150;
    for (int i = 0; i < 20150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20150(int& val) {
    const int x = 20150;
    std::cout << 20150;
    for (int i = 0; i < 20150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20151(int& val) {
    func_in_20151(val);
    const int x = 20151;
    std::cout << 20151;
    for (int i = 0; i < 20151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20151(int& val) {
    const int x = 20151;
    std::cout << 20151;
    for (int i = 0; i < 20151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20152(int& val) {
    func_in_20152(val);
    const int x = 20152;
    std::cout << 20152;
    for (int i = 0; i < 20152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20152(int& val) {
    const int x = 20152;
    std::cout << 20152;
    for (int i = 0; i < 20152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20153(int& val) {
    func_in_20153(val);
    const int x = 20153;
    std::cout << 20153;
    for (int i = 0; i < 20153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20153(int& val) {
    const int x = 20153;
    std::cout << 20153;
    for (int i = 0; i < 20153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20154(int& val) {
    func_in_20154(val);
    const int x = 20154;
    std::cout << 20154;
    for (int i = 0; i < 20154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20154(int& val) {
    const int x = 20154;
    std::cout << 20154;
    for (int i = 0; i < 20154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20155(int& val) {
    func_in_20155(val);
    const int x = 20155;
    std::cout << 20155;
    for (int i = 0; i < 20155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20155(int& val) {
    const int x = 20155;
    std::cout << 20155;
    for (int i = 0; i < 20155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20156(int& val) {
    func_in_20156(val);
    const int x = 20156;
    std::cout << 20156;
    for (int i = 0; i < 20156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20156(int& val) {
    const int x = 20156;
    std::cout << 20156;
    for (int i = 0; i < 20156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20157(int& val) {
    func_in_20157(val);
    const int x = 20157;
    std::cout << 20157;
    for (int i = 0; i < 20157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20157(int& val) {
    const int x = 20157;
    std::cout << 20157;
    for (int i = 0; i < 20157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20158(int& val) {
    func_in_20158(val);
    const int x = 20158;
    std::cout << 20158;
    for (int i = 0; i < 20158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20158(int& val) {
    const int x = 20158;
    std::cout << 20158;
    for (int i = 0; i < 20158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20159(int& val) {
    func_in_20159(val);
    const int x = 20159;
    std::cout << 20159;
    for (int i = 0; i < 20159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20159(int& val) {
    const int x = 20159;
    std::cout << 20159;
    for (int i = 0; i < 20159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20160(int& val) {
    func_in_20160(val);
    const int x = 20160;
    std::cout << 20160;
    for (int i = 0; i < 20160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20160(int& val) {
    const int x = 20160;
    std::cout << 20160;
    for (int i = 0; i < 20160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20161(int& val) {
    func_in_20161(val);
    const int x = 20161;
    std::cout << 20161;
    for (int i = 0; i < 20161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20161(int& val) {
    const int x = 20161;
    std::cout << 20161;
    for (int i = 0; i < 20161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20162(int& val) {
    func_in_20162(val);
    const int x = 20162;
    std::cout << 20162;
    for (int i = 0; i < 20162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20162(int& val) {
    const int x = 20162;
    std::cout << 20162;
    for (int i = 0; i < 20162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20163(int& val) {
    func_in_20163(val);
    const int x = 20163;
    std::cout << 20163;
    for (int i = 0; i < 20163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20163(int& val) {
    const int x = 20163;
    std::cout << 20163;
    for (int i = 0; i < 20163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20164(int& val) {
    func_in_20164(val);
    const int x = 20164;
    std::cout << 20164;
    for (int i = 0; i < 20164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20164(int& val) {
    const int x = 20164;
    std::cout << 20164;
    for (int i = 0; i < 20164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20165(int& val) {
    func_in_20165(val);
    const int x = 20165;
    std::cout << 20165;
    for (int i = 0; i < 20165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20165(int& val) {
    const int x = 20165;
    std::cout << 20165;
    for (int i = 0; i < 20165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20166(int& val) {
    func_in_20166(val);
    const int x = 20166;
    std::cout << 20166;
    for (int i = 0; i < 20166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20166(int& val) {
    const int x = 20166;
    std::cout << 20166;
    for (int i = 0; i < 20166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20167(int& val) {
    func_in_20167(val);
    const int x = 20167;
    std::cout << 20167;
    for (int i = 0; i < 20167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20167(int& val) {
    const int x = 20167;
    std::cout << 20167;
    for (int i = 0; i < 20167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20168(int& val) {
    func_in_20168(val);
    const int x = 20168;
    std::cout << 20168;
    for (int i = 0; i < 20168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20168(int& val) {
    const int x = 20168;
    std::cout << 20168;
    for (int i = 0; i < 20168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20169(int& val) {
    func_in_20169(val);
    const int x = 20169;
    std::cout << 20169;
    for (int i = 0; i < 20169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20169(int& val) {
    const int x = 20169;
    std::cout << 20169;
    for (int i = 0; i < 20169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20170(int& val) {
    func_in_20170(val);
    const int x = 20170;
    std::cout << 20170;
    for (int i = 0; i < 20170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20170(int& val) {
    const int x = 20170;
    std::cout << 20170;
    for (int i = 0; i < 20170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20171(int& val) {
    func_in_20171(val);
    const int x = 20171;
    std::cout << 20171;
    for (int i = 0; i < 20171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20171(int& val) {
    const int x = 20171;
    std::cout << 20171;
    for (int i = 0; i < 20171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20172(int& val) {
    func_in_20172(val);
    const int x = 20172;
    std::cout << 20172;
    for (int i = 0; i < 20172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20172(int& val) {
    const int x = 20172;
    std::cout << 20172;
    for (int i = 0; i < 20172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20173(int& val) {
    func_in_20173(val);
    const int x = 20173;
    std::cout << 20173;
    for (int i = 0; i < 20173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20173(int& val) {
    const int x = 20173;
    std::cout << 20173;
    for (int i = 0; i < 20173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20174(int& val) {
    func_in_20174(val);
    const int x = 20174;
    std::cout << 20174;
    for (int i = 0; i < 20174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20174(int& val) {
    const int x = 20174;
    std::cout << 20174;
    for (int i = 0; i < 20174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20175(int& val) {
    func_in_20175(val);
    const int x = 20175;
    std::cout << 20175;
    for (int i = 0; i < 20175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20175(int& val) {
    const int x = 20175;
    std::cout << 20175;
    for (int i = 0; i < 20175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20176(int& val) {
    func_in_20176(val);
    const int x = 20176;
    std::cout << 20176;
    for (int i = 0; i < 20176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20176(int& val) {
    const int x = 20176;
    std::cout << 20176;
    for (int i = 0; i < 20176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20177(int& val) {
    func_in_20177(val);
    const int x = 20177;
    std::cout << 20177;
    for (int i = 0; i < 20177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20177(int& val) {
    const int x = 20177;
    std::cout << 20177;
    for (int i = 0; i < 20177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20178(int& val) {
    func_in_20178(val);
    const int x = 20178;
    std::cout << 20178;
    for (int i = 0; i < 20178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20178(int& val) {
    const int x = 20178;
    std::cout << 20178;
    for (int i = 0; i < 20178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20179(int& val) {
    func_in_20179(val);
    const int x = 20179;
    std::cout << 20179;
    for (int i = 0; i < 20179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20179(int& val) {
    const int x = 20179;
    std::cout << 20179;
    for (int i = 0; i < 20179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20180(int& val) {
    func_in_20180(val);
    const int x = 20180;
    std::cout << 20180;
    for (int i = 0; i < 20180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20180(int& val) {
    const int x = 20180;
    std::cout << 20180;
    for (int i = 0; i < 20180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20181(int& val) {
    func_in_20181(val);
    const int x = 20181;
    std::cout << 20181;
    for (int i = 0; i < 20181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20181(int& val) {
    const int x = 20181;
    std::cout << 20181;
    for (int i = 0; i < 20181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20182(int& val) {
    func_in_20182(val);
    const int x = 20182;
    std::cout << 20182;
    for (int i = 0; i < 20182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20182(int& val) {
    const int x = 20182;
    std::cout << 20182;
    for (int i = 0; i < 20182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20183(int& val) {
    func_in_20183(val);
    const int x = 20183;
    std::cout << 20183;
    for (int i = 0; i < 20183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20183(int& val) {
    const int x = 20183;
    std::cout << 20183;
    for (int i = 0; i < 20183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20184(int& val) {
    func_in_20184(val);
    const int x = 20184;
    std::cout << 20184;
    for (int i = 0; i < 20184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20184(int& val) {
    const int x = 20184;
    std::cout << 20184;
    for (int i = 0; i < 20184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20185(int& val) {
    func_in_20185(val);
    const int x = 20185;
    std::cout << 20185;
    for (int i = 0; i < 20185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20185(int& val) {
    const int x = 20185;
    std::cout << 20185;
    for (int i = 0; i < 20185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20186(int& val) {
    func_in_20186(val);
    const int x = 20186;
    std::cout << 20186;
    for (int i = 0; i < 20186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20186(int& val) {
    const int x = 20186;
    std::cout << 20186;
    for (int i = 0; i < 20186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20187(int& val) {
    func_in_20187(val);
    const int x = 20187;
    std::cout << 20187;
    for (int i = 0; i < 20187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20187(int& val) {
    const int x = 20187;
    std::cout << 20187;
    for (int i = 0; i < 20187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20188(int& val) {
    func_in_20188(val);
    const int x = 20188;
    std::cout << 20188;
    for (int i = 0; i < 20188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20188(int& val) {
    const int x = 20188;
    std::cout << 20188;
    for (int i = 0; i < 20188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20189(int& val) {
    func_in_20189(val);
    const int x = 20189;
    std::cout << 20189;
    for (int i = 0; i < 20189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20189(int& val) {
    const int x = 20189;
    std::cout << 20189;
    for (int i = 0; i < 20189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20190(int& val) {
    func_in_20190(val);
    const int x = 20190;
    std::cout << 20190;
    for (int i = 0; i < 20190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20190(int& val) {
    const int x = 20190;
    std::cout << 20190;
    for (int i = 0; i < 20190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20191(int& val) {
    func_in_20191(val);
    const int x = 20191;
    std::cout << 20191;
    for (int i = 0; i < 20191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20191(int& val) {
    const int x = 20191;
    std::cout << 20191;
    for (int i = 0; i < 20191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20192(int& val) {
    func_in_20192(val);
    const int x = 20192;
    std::cout << 20192;
    for (int i = 0; i < 20192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20192(int& val) {
    const int x = 20192;
    std::cout << 20192;
    for (int i = 0; i < 20192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20193(int& val) {
    func_in_20193(val);
    const int x = 20193;
    std::cout << 20193;
    for (int i = 0; i < 20193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20193(int& val) {
    const int x = 20193;
    std::cout << 20193;
    for (int i = 0; i < 20193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20194(int& val) {
    func_in_20194(val);
    const int x = 20194;
    std::cout << 20194;
    for (int i = 0; i < 20194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20194(int& val) {
    const int x = 20194;
    std::cout << 20194;
    for (int i = 0; i < 20194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20195(int& val) {
    func_in_20195(val);
    const int x = 20195;
    std::cout << 20195;
    for (int i = 0; i < 20195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20195(int& val) {
    const int x = 20195;
    std::cout << 20195;
    for (int i = 0; i < 20195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20196(int& val) {
    func_in_20196(val);
    const int x = 20196;
    std::cout << 20196;
    for (int i = 0; i < 20196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20196(int& val) {
    const int x = 20196;
    std::cout << 20196;
    for (int i = 0; i < 20196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20197(int& val) {
    func_in_20197(val);
    const int x = 20197;
    std::cout << 20197;
    for (int i = 0; i < 20197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20197(int& val) {
    const int x = 20197;
    std::cout << 20197;
    for (int i = 0; i < 20197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20198(int& val) {
    func_in_20198(val);
    const int x = 20198;
    std::cout << 20198;
    for (int i = 0; i < 20198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20198(int& val) {
    const int x = 20198;
    std::cout << 20198;
    for (int i = 0; i < 20198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20199(int& val) {
    func_in_20199(val);
    const int x = 20199;
    std::cout << 20199;
    for (int i = 0; i < 20199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20199(int& val) {
    const int x = 20199;
    std::cout << 20199;
    for (int i = 0; i < 20199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20200(int& val) {
    func_in_20200(val);
    const int x = 20200;
    std::cout << 20200;
    for (int i = 0; i < 20200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20200(int& val) {
    const int x = 20200;
    std::cout << 20200;
    for (int i = 0; i < 20200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20201(int& val) {
    func_in_20201(val);
    const int x = 20201;
    std::cout << 20201;
    for (int i = 0; i < 20201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20201(int& val) {
    const int x = 20201;
    std::cout << 20201;
    for (int i = 0; i < 20201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20202(int& val) {
    func_in_20202(val);
    const int x = 20202;
    std::cout << 20202;
    for (int i = 0; i < 20202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20202(int& val) {
    const int x = 20202;
    std::cout << 20202;
    for (int i = 0; i < 20202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20203(int& val) {
    func_in_20203(val);
    const int x = 20203;
    std::cout << 20203;
    for (int i = 0; i < 20203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20203(int& val) {
    const int x = 20203;
    std::cout << 20203;
    for (int i = 0; i < 20203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20204(int& val) {
    func_in_20204(val);
    const int x = 20204;
    std::cout << 20204;
    for (int i = 0; i < 20204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20204(int& val) {
    const int x = 20204;
    std::cout << 20204;
    for (int i = 0; i < 20204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20205(int& val) {
    func_in_20205(val);
    const int x = 20205;
    std::cout << 20205;
    for (int i = 0; i < 20205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20205(int& val) {
    const int x = 20205;
    std::cout << 20205;
    for (int i = 0; i < 20205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20206(int& val) {
    func_in_20206(val);
    const int x = 20206;
    std::cout << 20206;
    for (int i = 0; i < 20206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20206(int& val) {
    const int x = 20206;
    std::cout << 20206;
    for (int i = 0; i < 20206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20207(int& val) {
    func_in_20207(val);
    const int x = 20207;
    std::cout << 20207;
    for (int i = 0; i < 20207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20207(int& val) {
    const int x = 20207;
    std::cout << 20207;
    for (int i = 0; i < 20207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20208(int& val) {
    func_in_20208(val);
    const int x = 20208;
    std::cout << 20208;
    for (int i = 0; i < 20208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20208(int& val) {
    const int x = 20208;
    std::cout << 20208;
    for (int i = 0; i < 20208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20209(int& val) {
    func_in_20209(val);
    const int x = 20209;
    std::cout << 20209;
    for (int i = 0; i < 20209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20209(int& val) {
    const int x = 20209;
    std::cout << 20209;
    for (int i = 0; i < 20209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20210(int& val) {
    func_in_20210(val);
    const int x = 20210;
    std::cout << 20210;
    for (int i = 0; i < 20210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20210(int& val) {
    const int x = 20210;
    std::cout << 20210;
    for (int i = 0; i < 20210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20211(int& val) {
    func_in_20211(val);
    const int x = 20211;
    std::cout << 20211;
    for (int i = 0; i < 20211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20211(int& val) {
    const int x = 20211;
    std::cout << 20211;
    for (int i = 0; i < 20211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20212(int& val) {
    func_in_20212(val);
    const int x = 20212;
    std::cout << 20212;
    for (int i = 0; i < 20212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20212(int& val) {
    const int x = 20212;
    std::cout << 20212;
    for (int i = 0; i < 20212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20213(int& val) {
    func_in_20213(val);
    const int x = 20213;
    std::cout << 20213;
    for (int i = 0; i < 20213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20213(int& val) {
    const int x = 20213;
    std::cout << 20213;
    for (int i = 0; i < 20213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20214(int& val) {
    func_in_20214(val);
    const int x = 20214;
    std::cout << 20214;
    for (int i = 0; i < 20214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20214(int& val) {
    const int x = 20214;
    std::cout << 20214;
    for (int i = 0; i < 20214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20215(int& val) {
    func_in_20215(val);
    const int x = 20215;
    std::cout << 20215;
    for (int i = 0; i < 20215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20215(int& val) {
    const int x = 20215;
    std::cout << 20215;
    for (int i = 0; i < 20215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20216(int& val) {
    func_in_20216(val);
    const int x = 20216;
    std::cout << 20216;
    for (int i = 0; i < 20216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20216(int& val) {
    const int x = 20216;
    std::cout << 20216;
    for (int i = 0; i < 20216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20217(int& val) {
    func_in_20217(val);
    const int x = 20217;
    std::cout << 20217;
    for (int i = 0; i < 20217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20217(int& val) {
    const int x = 20217;
    std::cout << 20217;
    for (int i = 0; i < 20217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20218(int& val) {
    func_in_20218(val);
    const int x = 20218;
    std::cout << 20218;
    for (int i = 0; i < 20218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20218(int& val) {
    const int x = 20218;
    std::cout << 20218;
    for (int i = 0; i < 20218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20219(int& val) {
    func_in_20219(val);
    const int x = 20219;
    std::cout << 20219;
    for (int i = 0; i < 20219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20219(int& val) {
    const int x = 20219;
    std::cout << 20219;
    for (int i = 0; i < 20219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20220(int& val) {
    func_in_20220(val);
    const int x = 20220;
    std::cout << 20220;
    for (int i = 0; i < 20220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20220(int& val) {
    const int x = 20220;
    std::cout << 20220;
    for (int i = 0; i < 20220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20221(int& val) {
    func_in_20221(val);
    const int x = 20221;
    std::cout << 20221;
    for (int i = 0; i < 20221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20221(int& val) {
    const int x = 20221;
    std::cout << 20221;
    for (int i = 0; i < 20221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20222(int& val) {
    func_in_20222(val);
    const int x = 20222;
    std::cout << 20222;
    for (int i = 0; i < 20222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20222(int& val) {
    const int x = 20222;
    std::cout << 20222;
    for (int i = 0; i < 20222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20223(int& val) {
    func_in_20223(val);
    const int x = 20223;
    std::cout << 20223;
    for (int i = 0; i < 20223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20223(int& val) {
    const int x = 20223;
    std::cout << 20223;
    for (int i = 0; i < 20223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20224(int& val) {
    func_in_20224(val);
    const int x = 20224;
    std::cout << 20224;
    for (int i = 0; i < 20224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20224(int& val) {
    const int x = 20224;
    std::cout << 20224;
    for (int i = 0; i < 20224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20225(int& val) {
    func_in_20225(val);
    const int x = 20225;
    std::cout << 20225;
    for (int i = 0; i < 20225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20225(int& val) {
    const int x = 20225;
    std::cout << 20225;
    for (int i = 0; i < 20225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20226(int& val) {
    func_in_20226(val);
    const int x = 20226;
    std::cout << 20226;
    for (int i = 0; i < 20226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20226(int& val) {
    const int x = 20226;
    std::cout << 20226;
    for (int i = 0; i < 20226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20227(int& val) {
    func_in_20227(val);
    const int x = 20227;
    std::cout << 20227;
    for (int i = 0; i < 20227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20227(int& val) {
    const int x = 20227;
    std::cout << 20227;
    for (int i = 0; i < 20227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20228(int& val) {
    func_in_20228(val);
    const int x = 20228;
    std::cout << 20228;
    for (int i = 0; i < 20228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20228(int& val) {
    const int x = 20228;
    std::cout << 20228;
    for (int i = 0; i < 20228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20229(int& val) {
    func_in_20229(val);
    const int x = 20229;
    std::cout << 20229;
    for (int i = 0; i < 20229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20229(int& val) {
    const int x = 20229;
    std::cout << 20229;
    for (int i = 0; i < 20229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20230(int& val) {
    func_in_20230(val);
    const int x = 20230;
    std::cout << 20230;
    for (int i = 0; i < 20230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20230(int& val) {
    const int x = 20230;
    std::cout << 20230;
    for (int i = 0; i < 20230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20231(int& val) {
    func_in_20231(val);
    const int x = 20231;
    std::cout << 20231;
    for (int i = 0; i < 20231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20231(int& val) {
    const int x = 20231;
    std::cout << 20231;
    for (int i = 0; i < 20231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20232(int& val) {
    func_in_20232(val);
    const int x = 20232;
    std::cout << 20232;
    for (int i = 0; i < 20232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20232(int& val) {
    const int x = 20232;
    std::cout << 20232;
    for (int i = 0; i < 20232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20233(int& val) {
    func_in_20233(val);
    const int x = 20233;
    std::cout << 20233;
    for (int i = 0; i < 20233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20233(int& val) {
    const int x = 20233;
    std::cout << 20233;
    for (int i = 0; i < 20233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20234(int& val) {
    func_in_20234(val);
    const int x = 20234;
    std::cout << 20234;
    for (int i = 0; i < 20234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20234(int& val) {
    const int x = 20234;
    std::cout << 20234;
    for (int i = 0; i < 20234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20235(int& val) {
    func_in_20235(val);
    const int x = 20235;
    std::cout << 20235;
    for (int i = 0; i < 20235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20235(int& val) {
    const int x = 20235;
    std::cout << 20235;
    for (int i = 0; i < 20235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20236(int& val) {
    func_in_20236(val);
    const int x = 20236;
    std::cout << 20236;
    for (int i = 0; i < 20236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20236(int& val) {
    const int x = 20236;
    std::cout << 20236;
    for (int i = 0; i < 20236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20237(int& val) {
    func_in_20237(val);
    const int x = 20237;
    std::cout << 20237;
    for (int i = 0; i < 20237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20237(int& val) {
    const int x = 20237;
    std::cout << 20237;
    for (int i = 0; i < 20237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20238(int& val) {
    func_in_20238(val);
    const int x = 20238;
    std::cout << 20238;
    for (int i = 0; i < 20238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20238(int& val) {
    const int x = 20238;
    std::cout << 20238;
    for (int i = 0; i < 20238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20239(int& val) {
    func_in_20239(val);
    const int x = 20239;
    std::cout << 20239;
    for (int i = 0; i < 20239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20239(int& val) {
    const int x = 20239;
    std::cout << 20239;
    for (int i = 0; i < 20239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20240(int& val) {
    func_in_20240(val);
    const int x = 20240;
    std::cout << 20240;
    for (int i = 0; i < 20240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20240(int& val) {
    const int x = 20240;
    std::cout << 20240;
    for (int i = 0; i < 20240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20241(int& val) {
    func_in_20241(val);
    const int x = 20241;
    std::cout << 20241;
    for (int i = 0; i < 20241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20241(int& val) {
    const int x = 20241;
    std::cout << 20241;
    for (int i = 0; i < 20241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20242(int& val) {
    func_in_20242(val);
    const int x = 20242;
    std::cout << 20242;
    for (int i = 0; i < 20242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20242(int& val) {
    const int x = 20242;
    std::cout << 20242;
    for (int i = 0; i < 20242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20243(int& val) {
    func_in_20243(val);
    const int x = 20243;
    std::cout << 20243;
    for (int i = 0; i < 20243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20243(int& val) {
    const int x = 20243;
    std::cout << 20243;
    for (int i = 0; i < 20243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20244(int& val) {
    func_in_20244(val);
    const int x = 20244;
    std::cout << 20244;
    for (int i = 0; i < 20244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20244(int& val) {
    const int x = 20244;
    std::cout << 20244;
    for (int i = 0; i < 20244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20245(int& val) {
    func_in_20245(val);
    const int x = 20245;
    std::cout << 20245;
    for (int i = 0; i < 20245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20245(int& val) {
    const int x = 20245;
    std::cout << 20245;
    for (int i = 0; i < 20245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20246(int& val) {
    func_in_20246(val);
    const int x = 20246;
    std::cout << 20246;
    for (int i = 0; i < 20246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20246(int& val) {
    const int x = 20246;
    std::cout << 20246;
    for (int i = 0; i < 20246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20247(int& val) {
    func_in_20247(val);
    const int x = 20247;
    std::cout << 20247;
    for (int i = 0; i < 20247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20247(int& val) {
    const int x = 20247;
    std::cout << 20247;
    for (int i = 0; i < 20247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20248(int& val) {
    func_in_20248(val);
    const int x = 20248;
    std::cout << 20248;
    for (int i = 0; i < 20248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20248(int& val) {
    const int x = 20248;
    std::cout << 20248;
    for (int i = 0; i < 20248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20249(int& val) {
    func_in_20249(val);
    const int x = 20249;
    std::cout << 20249;
    for (int i = 0; i < 20249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20249(int& val) {
    const int x = 20249;
    std::cout << 20249;
    for (int i = 0; i < 20249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20250(int& val) {
    func_in_20250(val);
    const int x = 20250;
    std::cout << 20250;
    for (int i = 0; i < 20250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20250(int& val) {
    const int x = 20250;
    std::cout << 20250;
    for (int i = 0; i < 20250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20251(int& val) {
    func_in_20251(val);
    const int x = 20251;
    std::cout << 20251;
    for (int i = 0; i < 20251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20251(int& val) {
    const int x = 20251;
    std::cout << 20251;
    for (int i = 0; i < 20251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20252(int& val) {
    func_in_20252(val);
    const int x = 20252;
    std::cout << 20252;
    for (int i = 0; i < 20252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20252(int& val) {
    const int x = 20252;
    std::cout << 20252;
    for (int i = 0; i < 20252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20253(int& val) {
    func_in_20253(val);
    const int x = 20253;
    std::cout << 20253;
    for (int i = 0; i < 20253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20253(int& val) {
    const int x = 20253;
    std::cout << 20253;
    for (int i = 0; i < 20253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20254(int& val) {
    func_in_20254(val);
    const int x = 20254;
    std::cout << 20254;
    for (int i = 0; i < 20254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20254(int& val) {
    const int x = 20254;
    std::cout << 20254;
    for (int i = 0; i < 20254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20255(int& val) {
    func_in_20255(val);
    const int x = 20255;
    std::cout << 20255;
    for (int i = 0; i < 20255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20255(int& val) {
    const int x = 20255;
    std::cout << 20255;
    for (int i = 0; i < 20255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20256(int& val) {
    func_in_20256(val);
    const int x = 20256;
    std::cout << 20256;
    for (int i = 0; i < 20256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20256(int& val) {
    const int x = 20256;
    std::cout << 20256;
    for (int i = 0; i < 20256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20257(int& val) {
    func_in_20257(val);
    const int x = 20257;
    std::cout << 20257;
    for (int i = 0; i < 20257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20257(int& val) {
    const int x = 20257;
    std::cout << 20257;
    for (int i = 0; i < 20257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20258(int& val) {
    func_in_20258(val);
    const int x = 20258;
    std::cout << 20258;
    for (int i = 0; i < 20258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20258(int& val) {
    const int x = 20258;
    std::cout << 20258;
    for (int i = 0; i < 20258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20259(int& val) {
    func_in_20259(val);
    const int x = 20259;
    std::cout << 20259;
    for (int i = 0; i < 20259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20259(int& val) {
    const int x = 20259;
    std::cout << 20259;
    for (int i = 0; i < 20259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20260(int& val) {
    func_in_20260(val);
    const int x = 20260;
    std::cout << 20260;
    for (int i = 0; i < 20260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20260(int& val) {
    const int x = 20260;
    std::cout << 20260;
    for (int i = 0; i < 20260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20261(int& val) {
    func_in_20261(val);
    const int x = 20261;
    std::cout << 20261;
    for (int i = 0; i < 20261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20261(int& val) {
    const int x = 20261;
    std::cout << 20261;
    for (int i = 0; i < 20261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20262(int& val) {
    func_in_20262(val);
    const int x = 20262;
    std::cout << 20262;
    for (int i = 0; i < 20262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20262(int& val) {
    const int x = 20262;
    std::cout << 20262;
    for (int i = 0; i < 20262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20263(int& val) {
    func_in_20263(val);
    const int x = 20263;
    std::cout << 20263;
    for (int i = 0; i < 20263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20263(int& val) {
    const int x = 20263;
    std::cout << 20263;
    for (int i = 0; i < 20263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20264(int& val) {
    func_in_20264(val);
    const int x = 20264;
    std::cout << 20264;
    for (int i = 0; i < 20264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20264(int& val) {
    const int x = 20264;
    std::cout << 20264;
    for (int i = 0; i < 20264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20265(int& val) {
    func_in_20265(val);
    const int x = 20265;
    std::cout << 20265;
    for (int i = 0; i < 20265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20265(int& val) {
    const int x = 20265;
    std::cout << 20265;
    for (int i = 0; i < 20265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20266(int& val) {
    func_in_20266(val);
    const int x = 20266;
    std::cout << 20266;
    for (int i = 0; i < 20266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20266(int& val) {
    const int x = 20266;
    std::cout << 20266;
    for (int i = 0; i < 20266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20267(int& val) {
    func_in_20267(val);
    const int x = 20267;
    std::cout << 20267;
    for (int i = 0; i < 20267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20267(int& val) {
    const int x = 20267;
    std::cout << 20267;
    for (int i = 0; i < 20267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20268(int& val) {
    func_in_20268(val);
    const int x = 20268;
    std::cout << 20268;
    for (int i = 0; i < 20268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20268(int& val) {
    const int x = 20268;
    std::cout << 20268;
    for (int i = 0; i < 20268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20269(int& val) {
    func_in_20269(val);
    const int x = 20269;
    std::cout << 20269;
    for (int i = 0; i < 20269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20269(int& val) {
    const int x = 20269;
    std::cout << 20269;
    for (int i = 0; i < 20269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20270(int& val) {
    func_in_20270(val);
    const int x = 20270;
    std::cout << 20270;
    for (int i = 0; i < 20270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20270(int& val) {
    const int x = 20270;
    std::cout << 20270;
    for (int i = 0; i < 20270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20271(int& val) {
    func_in_20271(val);
    const int x = 20271;
    std::cout << 20271;
    for (int i = 0; i < 20271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20271(int& val) {
    const int x = 20271;
    std::cout << 20271;
    for (int i = 0; i < 20271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20272(int& val) {
    func_in_20272(val);
    const int x = 20272;
    std::cout << 20272;
    for (int i = 0; i < 20272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20272(int& val) {
    const int x = 20272;
    std::cout << 20272;
    for (int i = 0; i < 20272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20273(int& val) {
    func_in_20273(val);
    const int x = 20273;
    std::cout << 20273;
    for (int i = 0; i < 20273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20273(int& val) {
    const int x = 20273;
    std::cout << 20273;
    for (int i = 0; i < 20273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20274(int& val) {
    func_in_20274(val);
    const int x = 20274;
    std::cout << 20274;
    for (int i = 0; i < 20274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20274(int& val) {
    const int x = 20274;
    std::cout << 20274;
    for (int i = 0; i < 20274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20275(int& val) {
    func_in_20275(val);
    const int x = 20275;
    std::cout << 20275;
    for (int i = 0; i < 20275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20275(int& val) {
    const int x = 20275;
    std::cout << 20275;
    for (int i = 0; i < 20275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20276(int& val) {
    func_in_20276(val);
    const int x = 20276;
    std::cout << 20276;
    for (int i = 0; i < 20276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20276(int& val) {
    const int x = 20276;
    std::cout << 20276;
    for (int i = 0; i < 20276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20277(int& val) {
    func_in_20277(val);
    const int x = 20277;
    std::cout << 20277;
    for (int i = 0; i < 20277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20277(int& val) {
    const int x = 20277;
    std::cout << 20277;
    for (int i = 0; i < 20277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20278(int& val) {
    func_in_20278(val);
    const int x = 20278;
    std::cout << 20278;
    for (int i = 0; i < 20278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20278(int& val) {
    const int x = 20278;
    std::cout << 20278;
    for (int i = 0; i < 20278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20279(int& val) {
    func_in_20279(val);
    const int x = 20279;
    std::cout << 20279;
    for (int i = 0; i < 20279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20279(int& val) {
    const int x = 20279;
    std::cout << 20279;
    for (int i = 0; i < 20279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20280(int& val) {
    func_in_20280(val);
    const int x = 20280;
    std::cout << 20280;
    for (int i = 0; i < 20280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20280(int& val) {
    const int x = 20280;
    std::cout << 20280;
    for (int i = 0; i < 20280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20281(int& val) {
    func_in_20281(val);
    const int x = 20281;
    std::cout << 20281;
    for (int i = 0; i < 20281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20281(int& val) {
    const int x = 20281;
    std::cout << 20281;
    for (int i = 0; i < 20281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20282(int& val) {
    func_in_20282(val);
    const int x = 20282;
    std::cout << 20282;
    for (int i = 0; i < 20282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20282(int& val) {
    const int x = 20282;
    std::cout << 20282;
    for (int i = 0; i < 20282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20283(int& val) {
    func_in_20283(val);
    const int x = 20283;
    std::cout << 20283;
    for (int i = 0; i < 20283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20283(int& val) {
    const int x = 20283;
    std::cout << 20283;
    for (int i = 0; i < 20283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20284(int& val) {
    func_in_20284(val);
    const int x = 20284;
    std::cout << 20284;
    for (int i = 0; i < 20284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20284(int& val) {
    const int x = 20284;
    std::cout << 20284;
    for (int i = 0; i < 20284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20285(int& val) {
    func_in_20285(val);
    const int x = 20285;
    std::cout << 20285;
    for (int i = 0; i < 20285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20285(int& val) {
    const int x = 20285;
    std::cout << 20285;
    for (int i = 0; i < 20285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20286(int& val) {
    func_in_20286(val);
    const int x = 20286;
    std::cout << 20286;
    for (int i = 0; i < 20286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20286(int& val) {
    const int x = 20286;
    std::cout << 20286;
    for (int i = 0; i < 20286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20287(int& val) {
    func_in_20287(val);
    const int x = 20287;
    std::cout << 20287;
    for (int i = 0; i < 20287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20287(int& val) {
    const int x = 20287;
    std::cout << 20287;
    for (int i = 0; i < 20287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20288(int& val) {
    func_in_20288(val);
    const int x = 20288;
    std::cout << 20288;
    for (int i = 0; i < 20288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20288(int& val) {
    const int x = 20288;
    std::cout << 20288;
    for (int i = 0; i < 20288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20289(int& val) {
    func_in_20289(val);
    const int x = 20289;
    std::cout << 20289;
    for (int i = 0; i < 20289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20289(int& val) {
    const int x = 20289;
    std::cout << 20289;
    for (int i = 0; i < 20289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20290(int& val) {
    func_in_20290(val);
    const int x = 20290;
    std::cout << 20290;
    for (int i = 0; i < 20290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20290(int& val) {
    const int x = 20290;
    std::cout << 20290;
    for (int i = 0; i < 20290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20291(int& val) {
    func_in_20291(val);
    const int x = 20291;
    std::cout << 20291;
    for (int i = 0; i < 20291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20291(int& val) {
    const int x = 20291;
    std::cout << 20291;
    for (int i = 0; i < 20291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20292(int& val) {
    func_in_20292(val);
    const int x = 20292;
    std::cout << 20292;
    for (int i = 0; i < 20292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20292(int& val) {
    const int x = 20292;
    std::cout << 20292;
    for (int i = 0; i < 20292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20293(int& val) {
    func_in_20293(val);
    const int x = 20293;
    std::cout << 20293;
    for (int i = 0; i < 20293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20293(int& val) {
    const int x = 20293;
    std::cout << 20293;
    for (int i = 0; i < 20293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20294(int& val) {
    func_in_20294(val);
    const int x = 20294;
    std::cout << 20294;
    for (int i = 0; i < 20294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20294(int& val) {
    const int x = 20294;
    std::cout << 20294;
    for (int i = 0; i < 20294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20295(int& val) {
    func_in_20295(val);
    const int x = 20295;
    std::cout << 20295;
    for (int i = 0; i < 20295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20295(int& val) {
    const int x = 20295;
    std::cout << 20295;
    for (int i = 0; i < 20295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20296(int& val) {
    func_in_20296(val);
    const int x = 20296;
    std::cout << 20296;
    for (int i = 0; i < 20296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20296(int& val) {
    const int x = 20296;
    std::cout << 20296;
    for (int i = 0; i < 20296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20297(int& val) {
    func_in_20297(val);
    const int x = 20297;
    std::cout << 20297;
    for (int i = 0; i < 20297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20297(int& val) {
    const int x = 20297;
    std::cout << 20297;
    for (int i = 0; i < 20297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20298(int& val) {
    func_in_20298(val);
    const int x = 20298;
    std::cout << 20298;
    for (int i = 0; i < 20298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20298(int& val) {
    const int x = 20298;
    std::cout << 20298;
    for (int i = 0; i < 20298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20299(int& val) {
    func_in_20299(val);
    const int x = 20299;
    std::cout << 20299;
    for (int i = 0; i < 20299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20299(int& val) {
    const int x = 20299;
    std::cout << 20299;
    for (int i = 0; i < 20299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20300(int& val) {
    func_in_20300(val);
    const int x = 20300;
    std::cout << 20300;
    for (int i = 0; i < 20300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20300(int& val) {
    const int x = 20300;
    std::cout << 20300;
    for (int i = 0; i < 20300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20301(int& val) {
    func_in_20301(val);
    const int x = 20301;
    std::cout << 20301;
    for (int i = 0; i < 20301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20301(int& val) {
    const int x = 20301;
    std::cout << 20301;
    for (int i = 0; i < 20301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20302(int& val) {
    func_in_20302(val);
    const int x = 20302;
    std::cout << 20302;
    for (int i = 0; i < 20302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20302(int& val) {
    const int x = 20302;
    std::cout << 20302;
    for (int i = 0; i < 20302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20303(int& val) {
    func_in_20303(val);
    const int x = 20303;
    std::cout << 20303;
    for (int i = 0; i < 20303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20303(int& val) {
    const int x = 20303;
    std::cout << 20303;
    for (int i = 0; i < 20303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20304(int& val) {
    func_in_20304(val);
    const int x = 20304;
    std::cout << 20304;
    for (int i = 0; i < 20304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20304(int& val) {
    const int x = 20304;
    std::cout << 20304;
    for (int i = 0; i < 20304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20305(int& val) {
    func_in_20305(val);
    const int x = 20305;
    std::cout << 20305;
    for (int i = 0; i < 20305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20305(int& val) {
    const int x = 20305;
    std::cout << 20305;
    for (int i = 0; i < 20305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20306(int& val) {
    func_in_20306(val);
    const int x = 20306;
    std::cout << 20306;
    for (int i = 0; i < 20306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20306(int& val) {
    const int x = 20306;
    std::cout << 20306;
    for (int i = 0; i < 20306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20307(int& val) {
    func_in_20307(val);
    const int x = 20307;
    std::cout << 20307;
    for (int i = 0; i < 20307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20307(int& val) {
    const int x = 20307;
    std::cout << 20307;
    for (int i = 0; i < 20307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20308(int& val) {
    func_in_20308(val);
    const int x = 20308;
    std::cout << 20308;
    for (int i = 0; i < 20308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20308(int& val) {
    const int x = 20308;
    std::cout << 20308;
    for (int i = 0; i < 20308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20309(int& val) {
    func_in_20309(val);
    const int x = 20309;
    std::cout << 20309;
    for (int i = 0; i < 20309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20309(int& val) {
    const int x = 20309;
    std::cout << 20309;
    for (int i = 0; i < 20309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20310(int& val) {
    func_in_20310(val);
    const int x = 20310;
    std::cout << 20310;
    for (int i = 0; i < 20310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20310(int& val) {
    const int x = 20310;
    std::cout << 20310;
    for (int i = 0; i < 20310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20311(int& val) {
    func_in_20311(val);
    const int x = 20311;
    std::cout << 20311;
    for (int i = 0; i < 20311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20311(int& val) {
    const int x = 20311;
    std::cout << 20311;
    for (int i = 0; i < 20311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20312(int& val) {
    func_in_20312(val);
    const int x = 20312;
    std::cout << 20312;
    for (int i = 0; i < 20312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20312(int& val) {
    const int x = 20312;
    std::cout << 20312;
    for (int i = 0; i < 20312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20313(int& val) {
    func_in_20313(val);
    const int x = 20313;
    std::cout << 20313;
    for (int i = 0; i < 20313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20313(int& val) {
    const int x = 20313;
    std::cout << 20313;
    for (int i = 0; i < 20313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20314(int& val) {
    func_in_20314(val);
    const int x = 20314;
    std::cout << 20314;
    for (int i = 0; i < 20314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20314(int& val) {
    const int x = 20314;
    std::cout << 20314;
    for (int i = 0; i < 20314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20315(int& val) {
    func_in_20315(val);
    const int x = 20315;
    std::cout << 20315;
    for (int i = 0; i < 20315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20315(int& val) {
    const int x = 20315;
    std::cout << 20315;
    for (int i = 0; i < 20315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20316(int& val) {
    func_in_20316(val);
    const int x = 20316;
    std::cout << 20316;
    for (int i = 0; i < 20316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20316(int& val) {
    const int x = 20316;
    std::cout << 20316;
    for (int i = 0; i < 20316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20317(int& val) {
    func_in_20317(val);
    const int x = 20317;
    std::cout << 20317;
    for (int i = 0; i < 20317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20317(int& val) {
    const int x = 20317;
    std::cout << 20317;
    for (int i = 0; i < 20317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20318(int& val) {
    func_in_20318(val);
    const int x = 20318;
    std::cout << 20318;
    for (int i = 0; i < 20318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20318(int& val) {
    const int x = 20318;
    std::cout << 20318;
    for (int i = 0; i < 20318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20319(int& val) {
    func_in_20319(val);
    const int x = 20319;
    std::cout << 20319;
    for (int i = 0; i < 20319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20319(int& val) {
    const int x = 20319;
    std::cout << 20319;
    for (int i = 0; i < 20319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20320(int& val) {
    func_in_20320(val);
    const int x = 20320;
    std::cout << 20320;
    for (int i = 0; i < 20320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20320(int& val) {
    const int x = 20320;
    std::cout << 20320;
    for (int i = 0; i < 20320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20321(int& val) {
    func_in_20321(val);
    const int x = 20321;
    std::cout << 20321;
    for (int i = 0; i < 20321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20321(int& val) {
    const int x = 20321;
    std::cout << 20321;
    for (int i = 0; i < 20321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20322(int& val) {
    func_in_20322(val);
    const int x = 20322;
    std::cout << 20322;
    for (int i = 0; i < 20322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20322(int& val) {
    const int x = 20322;
    std::cout << 20322;
    for (int i = 0; i < 20322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20323(int& val) {
    func_in_20323(val);
    const int x = 20323;
    std::cout << 20323;
    for (int i = 0; i < 20323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20323(int& val) {
    const int x = 20323;
    std::cout << 20323;
    for (int i = 0; i < 20323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20324(int& val) {
    func_in_20324(val);
    const int x = 20324;
    std::cout << 20324;
    for (int i = 0; i < 20324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20324(int& val) {
    const int x = 20324;
    std::cout << 20324;
    for (int i = 0; i < 20324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20325(int& val) {
    func_in_20325(val);
    const int x = 20325;
    std::cout << 20325;
    for (int i = 0; i < 20325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20325(int& val) {
    const int x = 20325;
    std::cout << 20325;
    for (int i = 0; i < 20325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20326(int& val) {
    func_in_20326(val);
    const int x = 20326;
    std::cout << 20326;
    for (int i = 0; i < 20326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20326(int& val) {
    const int x = 20326;
    std::cout << 20326;
    for (int i = 0; i < 20326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20327(int& val) {
    func_in_20327(val);
    const int x = 20327;
    std::cout << 20327;
    for (int i = 0; i < 20327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20327(int& val) {
    const int x = 20327;
    std::cout << 20327;
    for (int i = 0; i < 20327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20328(int& val) {
    func_in_20328(val);
    const int x = 20328;
    std::cout << 20328;
    for (int i = 0; i < 20328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20328(int& val) {
    const int x = 20328;
    std::cout << 20328;
    for (int i = 0; i < 20328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20329(int& val) {
    func_in_20329(val);
    const int x = 20329;
    std::cout << 20329;
    for (int i = 0; i < 20329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20329(int& val) {
    const int x = 20329;
    std::cout << 20329;
    for (int i = 0; i < 20329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20330(int& val) {
    func_in_20330(val);
    const int x = 20330;
    std::cout << 20330;
    for (int i = 0; i < 20330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20330(int& val) {
    const int x = 20330;
    std::cout << 20330;
    for (int i = 0; i < 20330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20331(int& val) {
    func_in_20331(val);
    const int x = 20331;
    std::cout << 20331;
    for (int i = 0; i < 20331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20331(int& val) {
    const int x = 20331;
    std::cout << 20331;
    for (int i = 0; i < 20331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20332(int& val) {
    func_in_20332(val);
    const int x = 20332;
    std::cout << 20332;
    for (int i = 0; i < 20332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20332(int& val) {
    const int x = 20332;
    std::cout << 20332;
    for (int i = 0; i < 20332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20333(int& val) {
    func_in_20333(val);
    const int x = 20333;
    std::cout << 20333;
    for (int i = 0; i < 20333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20333(int& val) {
    const int x = 20333;
    std::cout << 20333;
    for (int i = 0; i < 20333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20334(int& val) {
    func_in_20334(val);
    const int x = 20334;
    std::cout << 20334;
    for (int i = 0; i < 20334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20334(int& val) {
    const int x = 20334;
    std::cout << 20334;
    for (int i = 0; i < 20334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20335(int& val) {
    func_in_20335(val);
    const int x = 20335;
    std::cout << 20335;
    for (int i = 0; i < 20335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20335(int& val) {
    const int x = 20335;
    std::cout << 20335;
    for (int i = 0; i < 20335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20336(int& val) {
    func_in_20336(val);
    const int x = 20336;
    std::cout << 20336;
    for (int i = 0; i < 20336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20336(int& val) {
    const int x = 20336;
    std::cout << 20336;
    for (int i = 0; i < 20336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20337(int& val) {
    func_in_20337(val);
    const int x = 20337;
    std::cout << 20337;
    for (int i = 0; i < 20337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20337(int& val) {
    const int x = 20337;
    std::cout << 20337;
    for (int i = 0; i < 20337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20338(int& val) {
    func_in_20338(val);
    const int x = 20338;
    std::cout << 20338;
    for (int i = 0; i < 20338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20338(int& val) {
    const int x = 20338;
    std::cout << 20338;
    for (int i = 0; i < 20338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20339(int& val) {
    func_in_20339(val);
    const int x = 20339;
    std::cout << 20339;
    for (int i = 0; i < 20339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20339(int& val) {
    const int x = 20339;
    std::cout << 20339;
    for (int i = 0; i < 20339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20340(int& val) {
    func_in_20340(val);
    const int x = 20340;
    std::cout << 20340;
    for (int i = 0; i < 20340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20340(int& val) {
    const int x = 20340;
    std::cout << 20340;
    for (int i = 0; i < 20340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20341(int& val) {
    func_in_20341(val);
    const int x = 20341;
    std::cout << 20341;
    for (int i = 0; i < 20341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20341(int& val) {
    const int x = 20341;
    std::cout << 20341;
    for (int i = 0; i < 20341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20342(int& val) {
    func_in_20342(val);
    const int x = 20342;
    std::cout << 20342;
    for (int i = 0; i < 20342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20342(int& val) {
    const int x = 20342;
    std::cout << 20342;
    for (int i = 0; i < 20342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20343(int& val) {
    func_in_20343(val);
    const int x = 20343;
    std::cout << 20343;
    for (int i = 0; i < 20343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20343(int& val) {
    const int x = 20343;
    std::cout << 20343;
    for (int i = 0; i < 20343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20344(int& val) {
    func_in_20344(val);
    const int x = 20344;
    std::cout << 20344;
    for (int i = 0; i < 20344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20344(int& val) {
    const int x = 20344;
    std::cout << 20344;
    for (int i = 0; i < 20344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20345(int& val) {
    func_in_20345(val);
    const int x = 20345;
    std::cout << 20345;
    for (int i = 0; i < 20345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20345(int& val) {
    const int x = 20345;
    std::cout << 20345;
    for (int i = 0; i < 20345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20346(int& val) {
    func_in_20346(val);
    const int x = 20346;
    std::cout << 20346;
    for (int i = 0; i < 20346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20346(int& val) {
    const int x = 20346;
    std::cout << 20346;
    for (int i = 0; i < 20346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20347(int& val) {
    func_in_20347(val);
    const int x = 20347;
    std::cout << 20347;
    for (int i = 0; i < 20347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20347(int& val) {
    const int x = 20347;
    std::cout << 20347;
    for (int i = 0; i < 20347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20348(int& val) {
    func_in_20348(val);
    const int x = 20348;
    std::cout << 20348;
    for (int i = 0; i < 20348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20348(int& val) {
    const int x = 20348;
    std::cout << 20348;
    for (int i = 0; i < 20348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20349(int& val) {
    func_in_20349(val);
    const int x = 20349;
    std::cout << 20349;
    for (int i = 0; i < 20349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20349(int& val) {
    const int x = 20349;
    std::cout << 20349;
    for (int i = 0; i < 20349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20350(int& val) {
    func_in_20350(val);
    const int x = 20350;
    std::cout << 20350;
    for (int i = 0; i < 20350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20350(int& val) {
    const int x = 20350;
    std::cout << 20350;
    for (int i = 0; i < 20350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20351(int& val) {
    func_in_20351(val);
    const int x = 20351;
    std::cout << 20351;
    for (int i = 0; i < 20351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20351(int& val) {
    const int x = 20351;
    std::cout << 20351;
    for (int i = 0; i < 20351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20352(int& val) {
    func_in_20352(val);
    const int x = 20352;
    std::cout << 20352;
    for (int i = 0; i < 20352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20352(int& val) {
    const int x = 20352;
    std::cout << 20352;
    for (int i = 0; i < 20352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20353(int& val) {
    func_in_20353(val);
    const int x = 20353;
    std::cout << 20353;
    for (int i = 0; i < 20353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20353(int& val) {
    const int x = 20353;
    std::cout << 20353;
    for (int i = 0; i < 20353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20354(int& val) {
    func_in_20354(val);
    const int x = 20354;
    std::cout << 20354;
    for (int i = 0; i < 20354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20354(int& val) {
    const int x = 20354;
    std::cout << 20354;
    for (int i = 0; i < 20354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20355(int& val) {
    func_in_20355(val);
    const int x = 20355;
    std::cout << 20355;
    for (int i = 0; i < 20355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20355(int& val) {
    const int x = 20355;
    std::cout << 20355;
    for (int i = 0; i < 20355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20356(int& val) {
    func_in_20356(val);
    const int x = 20356;
    std::cout << 20356;
    for (int i = 0; i < 20356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20356(int& val) {
    const int x = 20356;
    std::cout << 20356;
    for (int i = 0; i < 20356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20357(int& val) {
    func_in_20357(val);
    const int x = 20357;
    std::cout << 20357;
    for (int i = 0; i < 20357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20357(int& val) {
    const int x = 20357;
    std::cout << 20357;
    for (int i = 0; i < 20357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20358(int& val) {
    func_in_20358(val);
    const int x = 20358;
    std::cout << 20358;
    for (int i = 0; i < 20358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20358(int& val) {
    const int x = 20358;
    std::cout << 20358;
    for (int i = 0; i < 20358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20359(int& val) {
    func_in_20359(val);
    const int x = 20359;
    std::cout << 20359;
    for (int i = 0; i < 20359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20359(int& val) {
    const int x = 20359;
    std::cout << 20359;
    for (int i = 0; i < 20359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20360(int& val) {
    func_in_20360(val);
    const int x = 20360;
    std::cout << 20360;
    for (int i = 0; i < 20360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20360(int& val) {
    const int x = 20360;
    std::cout << 20360;
    for (int i = 0; i < 20360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20361(int& val) {
    func_in_20361(val);
    const int x = 20361;
    std::cout << 20361;
    for (int i = 0; i < 20361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20361(int& val) {
    const int x = 20361;
    std::cout << 20361;
    for (int i = 0; i < 20361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20362(int& val) {
    func_in_20362(val);
    const int x = 20362;
    std::cout << 20362;
    for (int i = 0; i < 20362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20362(int& val) {
    const int x = 20362;
    std::cout << 20362;
    for (int i = 0; i < 20362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20363(int& val) {
    func_in_20363(val);
    const int x = 20363;
    std::cout << 20363;
    for (int i = 0; i < 20363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20363(int& val) {
    const int x = 20363;
    std::cout << 20363;
    for (int i = 0; i < 20363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20364(int& val) {
    func_in_20364(val);
    const int x = 20364;
    std::cout << 20364;
    for (int i = 0; i < 20364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20364(int& val) {
    const int x = 20364;
    std::cout << 20364;
    for (int i = 0; i < 20364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20365(int& val) {
    func_in_20365(val);
    const int x = 20365;
    std::cout << 20365;
    for (int i = 0; i < 20365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20365(int& val) {
    const int x = 20365;
    std::cout << 20365;
    for (int i = 0; i < 20365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20366(int& val) {
    func_in_20366(val);
    const int x = 20366;
    std::cout << 20366;
    for (int i = 0; i < 20366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20366(int& val) {
    const int x = 20366;
    std::cout << 20366;
    for (int i = 0; i < 20366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20367(int& val) {
    func_in_20367(val);
    const int x = 20367;
    std::cout << 20367;
    for (int i = 0; i < 20367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20367(int& val) {
    const int x = 20367;
    std::cout << 20367;
    for (int i = 0; i < 20367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20368(int& val) {
    func_in_20368(val);
    const int x = 20368;
    std::cout << 20368;
    for (int i = 0; i < 20368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20368(int& val) {
    const int x = 20368;
    std::cout << 20368;
    for (int i = 0; i < 20368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20369(int& val) {
    func_in_20369(val);
    const int x = 20369;
    std::cout << 20369;
    for (int i = 0; i < 20369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20369(int& val) {
    const int x = 20369;
    std::cout << 20369;
    for (int i = 0; i < 20369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20370(int& val) {
    func_in_20370(val);
    const int x = 20370;
    std::cout << 20370;
    for (int i = 0; i < 20370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20370(int& val) {
    const int x = 20370;
    std::cout << 20370;
    for (int i = 0; i < 20370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20371(int& val) {
    func_in_20371(val);
    const int x = 20371;
    std::cout << 20371;
    for (int i = 0; i < 20371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20371(int& val) {
    const int x = 20371;
    std::cout << 20371;
    for (int i = 0; i < 20371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20372(int& val) {
    func_in_20372(val);
    const int x = 20372;
    std::cout << 20372;
    for (int i = 0; i < 20372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20372(int& val) {
    const int x = 20372;
    std::cout << 20372;
    for (int i = 0; i < 20372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20373(int& val) {
    func_in_20373(val);
    const int x = 20373;
    std::cout << 20373;
    for (int i = 0; i < 20373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20373(int& val) {
    const int x = 20373;
    std::cout << 20373;
    for (int i = 0; i < 20373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20374(int& val) {
    func_in_20374(val);
    const int x = 20374;
    std::cout << 20374;
    for (int i = 0; i < 20374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20374(int& val) {
    const int x = 20374;
    std::cout << 20374;
    for (int i = 0; i < 20374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20375(int& val) {
    func_in_20375(val);
    const int x = 20375;
    std::cout << 20375;
    for (int i = 0; i < 20375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20375(int& val) {
    const int x = 20375;
    std::cout << 20375;
    for (int i = 0; i < 20375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20376(int& val) {
    func_in_20376(val);
    const int x = 20376;
    std::cout << 20376;
    for (int i = 0; i < 20376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20376(int& val) {
    const int x = 20376;
    std::cout << 20376;
    for (int i = 0; i < 20376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20377(int& val) {
    func_in_20377(val);
    const int x = 20377;
    std::cout << 20377;
    for (int i = 0; i < 20377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20377(int& val) {
    const int x = 20377;
    std::cout << 20377;
    for (int i = 0; i < 20377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20378(int& val) {
    func_in_20378(val);
    const int x = 20378;
    std::cout << 20378;
    for (int i = 0; i < 20378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20378(int& val) {
    const int x = 20378;
    std::cout << 20378;
    for (int i = 0; i < 20378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20379(int& val) {
    func_in_20379(val);
    const int x = 20379;
    std::cout << 20379;
    for (int i = 0; i < 20379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20379(int& val) {
    const int x = 20379;
    std::cout << 20379;
    for (int i = 0; i < 20379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20380(int& val) {
    func_in_20380(val);
    const int x = 20380;
    std::cout << 20380;
    for (int i = 0; i < 20380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20380(int& val) {
    const int x = 20380;
    std::cout << 20380;
    for (int i = 0; i < 20380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20381(int& val) {
    func_in_20381(val);
    const int x = 20381;
    std::cout << 20381;
    for (int i = 0; i < 20381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20381(int& val) {
    const int x = 20381;
    std::cout << 20381;
    for (int i = 0; i < 20381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20382(int& val) {
    func_in_20382(val);
    const int x = 20382;
    std::cout << 20382;
    for (int i = 0; i < 20382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20382(int& val) {
    const int x = 20382;
    std::cout << 20382;
    for (int i = 0; i < 20382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20383(int& val) {
    func_in_20383(val);
    const int x = 20383;
    std::cout << 20383;
    for (int i = 0; i < 20383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20383(int& val) {
    const int x = 20383;
    std::cout << 20383;
    for (int i = 0; i < 20383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20384(int& val) {
    func_in_20384(val);
    const int x = 20384;
    std::cout << 20384;
    for (int i = 0; i < 20384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20384(int& val) {
    const int x = 20384;
    std::cout << 20384;
    for (int i = 0; i < 20384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20385(int& val) {
    func_in_20385(val);
    const int x = 20385;
    std::cout << 20385;
    for (int i = 0; i < 20385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20385(int& val) {
    const int x = 20385;
    std::cout << 20385;
    for (int i = 0; i < 20385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20386(int& val) {
    func_in_20386(val);
    const int x = 20386;
    std::cout << 20386;
    for (int i = 0; i < 20386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20386(int& val) {
    const int x = 20386;
    std::cout << 20386;
    for (int i = 0; i < 20386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20387(int& val) {
    func_in_20387(val);
    const int x = 20387;
    std::cout << 20387;
    for (int i = 0; i < 20387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20387(int& val) {
    const int x = 20387;
    std::cout << 20387;
    for (int i = 0; i < 20387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20388(int& val) {
    func_in_20388(val);
    const int x = 20388;
    std::cout << 20388;
    for (int i = 0; i < 20388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20388(int& val) {
    const int x = 20388;
    std::cout << 20388;
    for (int i = 0; i < 20388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20389(int& val) {
    func_in_20389(val);
    const int x = 20389;
    std::cout << 20389;
    for (int i = 0; i < 20389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20389(int& val) {
    const int x = 20389;
    std::cout << 20389;
    for (int i = 0; i < 20389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20390(int& val) {
    func_in_20390(val);
    const int x = 20390;
    std::cout << 20390;
    for (int i = 0; i < 20390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20390(int& val) {
    const int x = 20390;
    std::cout << 20390;
    for (int i = 0; i < 20390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20391(int& val) {
    func_in_20391(val);
    const int x = 20391;
    std::cout << 20391;
    for (int i = 0; i < 20391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20391(int& val) {
    const int x = 20391;
    std::cout << 20391;
    for (int i = 0; i < 20391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20392(int& val) {
    func_in_20392(val);
    const int x = 20392;
    std::cout << 20392;
    for (int i = 0; i < 20392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20392(int& val) {
    const int x = 20392;
    std::cout << 20392;
    for (int i = 0; i < 20392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20393(int& val) {
    func_in_20393(val);
    const int x = 20393;
    std::cout << 20393;
    for (int i = 0; i < 20393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20393(int& val) {
    const int x = 20393;
    std::cout << 20393;
    for (int i = 0; i < 20393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20394(int& val) {
    func_in_20394(val);
    const int x = 20394;
    std::cout << 20394;
    for (int i = 0; i < 20394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20394(int& val) {
    const int x = 20394;
    std::cout << 20394;
    for (int i = 0; i < 20394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20395(int& val) {
    func_in_20395(val);
    const int x = 20395;
    std::cout << 20395;
    for (int i = 0; i < 20395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20395(int& val) {
    const int x = 20395;
    std::cout << 20395;
    for (int i = 0; i < 20395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20396(int& val) {
    func_in_20396(val);
    const int x = 20396;
    std::cout << 20396;
    for (int i = 0; i < 20396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20396(int& val) {
    const int x = 20396;
    std::cout << 20396;
    for (int i = 0; i < 20396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20397(int& val) {
    func_in_20397(val);
    const int x = 20397;
    std::cout << 20397;
    for (int i = 0; i < 20397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20397(int& val) {
    const int x = 20397;
    std::cout << 20397;
    for (int i = 0; i < 20397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20398(int& val) {
    func_in_20398(val);
    const int x = 20398;
    std::cout << 20398;
    for (int i = 0; i < 20398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20398(int& val) {
    const int x = 20398;
    std::cout << 20398;
    for (int i = 0; i < 20398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20399(int& val) {
    func_in_20399(val);
    const int x = 20399;
    std::cout << 20399;
    for (int i = 0; i < 20399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20399(int& val) {
    const int x = 20399;
    std::cout << 20399;
    for (int i = 0; i < 20399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20400(int& val) {
    func_in_20400(val);
    const int x = 20400;
    std::cout << 20400;
    for (int i = 0; i < 20400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20400(int& val) {
    const int x = 20400;
    std::cout << 20400;
    for (int i = 0; i < 20400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20401(int& val) {
    func_in_20401(val);
    const int x = 20401;
    std::cout << 20401;
    for (int i = 0; i < 20401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20401(int& val) {
    const int x = 20401;
    std::cout << 20401;
    for (int i = 0; i < 20401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20402(int& val) {
    func_in_20402(val);
    const int x = 20402;
    std::cout << 20402;
    for (int i = 0; i < 20402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20402(int& val) {
    const int x = 20402;
    std::cout << 20402;
    for (int i = 0; i < 20402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20403(int& val) {
    func_in_20403(val);
    const int x = 20403;
    std::cout << 20403;
    for (int i = 0; i < 20403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20403(int& val) {
    const int x = 20403;
    std::cout << 20403;
    for (int i = 0; i < 20403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20404(int& val) {
    func_in_20404(val);
    const int x = 20404;
    std::cout << 20404;
    for (int i = 0; i < 20404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20404(int& val) {
    const int x = 20404;
    std::cout << 20404;
    for (int i = 0; i < 20404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20405(int& val) {
    func_in_20405(val);
    const int x = 20405;
    std::cout << 20405;
    for (int i = 0; i < 20405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20405(int& val) {
    const int x = 20405;
    std::cout << 20405;
    for (int i = 0; i < 20405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20406(int& val) {
    func_in_20406(val);
    const int x = 20406;
    std::cout << 20406;
    for (int i = 0; i < 20406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20406(int& val) {
    const int x = 20406;
    std::cout << 20406;
    for (int i = 0; i < 20406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20407(int& val) {
    func_in_20407(val);
    const int x = 20407;
    std::cout << 20407;
    for (int i = 0; i < 20407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20407(int& val) {
    const int x = 20407;
    std::cout << 20407;
    for (int i = 0; i < 20407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20408(int& val) {
    func_in_20408(val);
    const int x = 20408;
    std::cout << 20408;
    for (int i = 0; i < 20408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20408(int& val) {
    const int x = 20408;
    std::cout << 20408;
    for (int i = 0; i < 20408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20409(int& val) {
    func_in_20409(val);
    const int x = 20409;
    std::cout << 20409;
    for (int i = 0; i < 20409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20409(int& val) {
    const int x = 20409;
    std::cout << 20409;
    for (int i = 0; i < 20409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20410(int& val) {
    func_in_20410(val);
    const int x = 20410;
    std::cout << 20410;
    for (int i = 0; i < 20410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20410(int& val) {
    const int x = 20410;
    std::cout << 20410;
    for (int i = 0; i < 20410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20411(int& val) {
    func_in_20411(val);
    const int x = 20411;
    std::cout << 20411;
    for (int i = 0; i < 20411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20411(int& val) {
    const int x = 20411;
    std::cout << 20411;
    for (int i = 0; i < 20411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20412(int& val) {
    func_in_20412(val);
    const int x = 20412;
    std::cout << 20412;
    for (int i = 0; i < 20412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20412(int& val) {
    const int x = 20412;
    std::cout << 20412;
    for (int i = 0; i < 20412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20413(int& val) {
    func_in_20413(val);
    const int x = 20413;
    std::cout << 20413;
    for (int i = 0; i < 20413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20413(int& val) {
    const int x = 20413;
    std::cout << 20413;
    for (int i = 0; i < 20413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20414(int& val) {
    func_in_20414(val);
    const int x = 20414;
    std::cout << 20414;
    for (int i = 0; i < 20414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20414(int& val) {
    const int x = 20414;
    std::cout << 20414;
    for (int i = 0; i < 20414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20415(int& val) {
    func_in_20415(val);
    const int x = 20415;
    std::cout << 20415;
    for (int i = 0; i < 20415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20415(int& val) {
    const int x = 20415;
    std::cout << 20415;
    for (int i = 0; i < 20415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20416(int& val) {
    func_in_20416(val);
    const int x = 20416;
    std::cout << 20416;
    for (int i = 0; i < 20416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20416(int& val) {
    const int x = 20416;
    std::cout << 20416;
    for (int i = 0; i < 20416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20417(int& val) {
    func_in_20417(val);
    const int x = 20417;
    std::cout << 20417;
    for (int i = 0; i < 20417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20417(int& val) {
    const int x = 20417;
    std::cout << 20417;
    for (int i = 0; i < 20417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20418(int& val) {
    func_in_20418(val);
    const int x = 20418;
    std::cout << 20418;
    for (int i = 0; i < 20418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20418(int& val) {
    const int x = 20418;
    std::cout << 20418;
    for (int i = 0; i < 20418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20419(int& val) {
    func_in_20419(val);
    const int x = 20419;
    std::cout << 20419;
    for (int i = 0; i < 20419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20419(int& val) {
    const int x = 20419;
    std::cout << 20419;
    for (int i = 0; i < 20419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20420(int& val) {
    func_in_20420(val);
    const int x = 20420;
    std::cout << 20420;
    for (int i = 0; i < 20420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20420(int& val) {
    const int x = 20420;
    std::cout << 20420;
    for (int i = 0; i < 20420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20421(int& val) {
    func_in_20421(val);
    const int x = 20421;
    std::cout << 20421;
    for (int i = 0; i < 20421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20421(int& val) {
    const int x = 20421;
    std::cout << 20421;
    for (int i = 0; i < 20421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20422(int& val) {
    func_in_20422(val);
    const int x = 20422;
    std::cout << 20422;
    for (int i = 0; i < 20422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20422(int& val) {
    const int x = 20422;
    std::cout << 20422;
    for (int i = 0; i < 20422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20423(int& val) {
    func_in_20423(val);
    const int x = 20423;
    std::cout << 20423;
    for (int i = 0; i < 20423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20423(int& val) {
    const int x = 20423;
    std::cout << 20423;
    for (int i = 0; i < 20423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20424(int& val) {
    func_in_20424(val);
    const int x = 20424;
    std::cout << 20424;
    for (int i = 0; i < 20424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20424(int& val) {
    const int x = 20424;
    std::cout << 20424;
    for (int i = 0; i < 20424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20425(int& val) {
    func_in_20425(val);
    const int x = 20425;
    std::cout << 20425;
    for (int i = 0; i < 20425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20425(int& val) {
    const int x = 20425;
    std::cout << 20425;
    for (int i = 0; i < 20425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20426(int& val) {
    func_in_20426(val);
    const int x = 20426;
    std::cout << 20426;
    for (int i = 0; i < 20426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20426(int& val) {
    const int x = 20426;
    std::cout << 20426;
    for (int i = 0; i < 20426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20427(int& val) {
    func_in_20427(val);
    const int x = 20427;
    std::cout << 20427;
    for (int i = 0; i < 20427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20427(int& val) {
    const int x = 20427;
    std::cout << 20427;
    for (int i = 0; i < 20427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20428(int& val) {
    func_in_20428(val);
    const int x = 20428;
    std::cout << 20428;
    for (int i = 0; i < 20428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20428(int& val) {
    const int x = 20428;
    std::cout << 20428;
    for (int i = 0; i < 20428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20429(int& val) {
    func_in_20429(val);
    const int x = 20429;
    std::cout << 20429;
    for (int i = 0; i < 20429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20429(int& val) {
    const int x = 20429;
    std::cout << 20429;
    for (int i = 0; i < 20429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20430(int& val) {
    func_in_20430(val);
    const int x = 20430;
    std::cout << 20430;
    for (int i = 0; i < 20430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20430(int& val) {
    const int x = 20430;
    std::cout << 20430;
    for (int i = 0; i < 20430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20431(int& val) {
    func_in_20431(val);
    const int x = 20431;
    std::cout << 20431;
    for (int i = 0; i < 20431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20431(int& val) {
    const int x = 20431;
    std::cout << 20431;
    for (int i = 0; i < 20431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20432(int& val) {
    func_in_20432(val);
    const int x = 20432;
    std::cout << 20432;
    for (int i = 0; i < 20432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20432(int& val) {
    const int x = 20432;
    std::cout << 20432;
    for (int i = 0; i < 20432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20433(int& val) {
    func_in_20433(val);
    const int x = 20433;
    std::cout << 20433;
    for (int i = 0; i < 20433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20433(int& val) {
    const int x = 20433;
    std::cout << 20433;
    for (int i = 0; i < 20433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20434(int& val) {
    func_in_20434(val);
    const int x = 20434;
    std::cout << 20434;
    for (int i = 0; i < 20434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20434(int& val) {
    const int x = 20434;
    std::cout << 20434;
    for (int i = 0; i < 20434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20435(int& val) {
    func_in_20435(val);
    const int x = 20435;
    std::cout << 20435;
    for (int i = 0; i < 20435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20435(int& val) {
    const int x = 20435;
    std::cout << 20435;
    for (int i = 0; i < 20435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20436(int& val) {
    func_in_20436(val);
    const int x = 20436;
    std::cout << 20436;
    for (int i = 0; i < 20436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20436(int& val) {
    const int x = 20436;
    std::cout << 20436;
    for (int i = 0; i < 20436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20437(int& val) {
    func_in_20437(val);
    const int x = 20437;
    std::cout << 20437;
    for (int i = 0; i < 20437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20437(int& val) {
    const int x = 20437;
    std::cout << 20437;
    for (int i = 0; i < 20437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20438(int& val) {
    func_in_20438(val);
    const int x = 20438;
    std::cout << 20438;
    for (int i = 0; i < 20438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20438(int& val) {
    const int x = 20438;
    std::cout << 20438;
    for (int i = 0; i < 20438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20439(int& val) {
    func_in_20439(val);
    const int x = 20439;
    std::cout << 20439;
    for (int i = 0; i < 20439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20439(int& val) {
    const int x = 20439;
    std::cout << 20439;
    for (int i = 0; i < 20439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20440(int& val) {
    func_in_20440(val);
    const int x = 20440;
    std::cout << 20440;
    for (int i = 0; i < 20440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20440(int& val) {
    const int x = 20440;
    std::cout << 20440;
    for (int i = 0; i < 20440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20441(int& val) {
    func_in_20441(val);
    const int x = 20441;
    std::cout << 20441;
    for (int i = 0; i < 20441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20441(int& val) {
    const int x = 20441;
    std::cout << 20441;
    for (int i = 0; i < 20441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20442(int& val) {
    func_in_20442(val);
    const int x = 20442;
    std::cout << 20442;
    for (int i = 0; i < 20442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20442(int& val) {
    const int x = 20442;
    std::cout << 20442;
    for (int i = 0; i < 20442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20443(int& val) {
    func_in_20443(val);
    const int x = 20443;
    std::cout << 20443;
    for (int i = 0; i < 20443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20443(int& val) {
    const int x = 20443;
    std::cout << 20443;
    for (int i = 0; i < 20443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20444(int& val) {
    func_in_20444(val);
    const int x = 20444;
    std::cout << 20444;
    for (int i = 0; i < 20444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20444(int& val) {
    const int x = 20444;
    std::cout << 20444;
    for (int i = 0; i < 20444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20445(int& val) {
    func_in_20445(val);
    const int x = 20445;
    std::cout << 20445;
    for (int i = 0; i < 20445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20445(int& val) {
    const int x = 20445;
    std::cout << 20445;
    for (int i = 0; i < 20445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20446(int& val) {
    func_in_20446(val);
    const int x = 20446;
    std::cout << 20446;
    for (int i = 0; i < 20446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20446(int& val) {
    const int x = 20446;
    std::cout << 20446;
    for (int i = 0; i < 20446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20447(int& val) {
    func_in_20447(val);
    const int x = 20447;
    std::cout << 20447;
    for (int i = 0; i < 20447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20447(int& val) {
    const int x = 20447;
    std::cout << 20447;
    for (int i = 0; i < 20447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20448(int& val) {
    func_in_20448(val);
    const int x = 20448;
    std::cout << 20448;
    for (int i = 0; i < 20448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20448(int& val) {
    const int x = 20448;
    std::cout << 20448;
    for (int i = 0; i < 20448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20449(int& val) {
    func_in_20449(val);
    const int x = 20449;
    std::cout << 20449;
    for (int i = 0; i < 20449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20449(int& val) {
    const int x = 20449;
    std::cout << 20449;
    for (int i = 0; i < 20449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20450(int& val) {
    func_in_20450(val);
    const int x = 20450;
    std::cout << 20450;
    for (int i = 0; i < 20450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20450(int& val) {
    const int x = 20450;
    std::cout << 20450;
    for (int i = 0; i < 20450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20451(int& val) {
    func_in_20451(val);
    const int x = 20451;
    std::cout << 20451;
    for (int i = 0; i < 20451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20451(int& val) {
    const int x = 20451;
    std::cout << 20451;
    for (int i = 0; i < 20451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20452(int& val) {
    func_in_20452(val);
    const int x = 20452;
    std::cout << 20452;
    for (int i = 0; i < 20452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20452(int& val) {
    const int x = 20452;
    std::cout << 20452;
    for (int i = 0; i < 20452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20453(int& val) {
    func_in_20453(val);
    const int x = 20453;
    std::cout << 20453;
    for (int i = 0; i < 20453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20453(int& val) {
    const int x = 20453;
    std::cout << 20453;
    for (int i = 0; i < 20453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20454(int& val) {
    func_in_20454(val);
    const int x = 20454;
    std::cout << 20454;
    for (int i = 0; i < 20454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20454(int& val) {
    const int x = 20454;
    std::cout << 20454;
    for (int i = 0; i < 20454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20455(int& val) {
    func_in_20455(val);
    const int x = 20455;
    std::cout << 20455;
    for (int i = 0; i < 20455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20455(int& val) {
    const int x = 20455;
    std::cout << 20455;
    for (int i = 0; i < 20455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20456(int& val) {
    func_in_20456(val);
    const int x = 20456;
    std::cout << 20456;
    for (int i = 0; i < 20456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20456(int& val) {
    const int x = 20456;
    std::cout << 20456;
    for (int i = 0; i < 20456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20457(int& val) {
    func_in_20457(val);
    const int x = 20457;
    std::cout << 20457;
    for (int i = 0; i < 20457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20457(int& val) {
    const int x = 20457;
    std::cout << 20457;
    for (int i = 0; i < 20457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20458(int& val) {
    func_in_20458(val);
    const int x = 20458;
    std::cout << 20458;
    for (int i = 0; i < 20458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20458(int& val) {
    const int x = 20458;
    std::cout << 20458;
    for (int i = 0; i < 20458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20459(int& val) {
    func_in_20459(val);
    const int x = 20459;
    std::cout << 20459;
    for (int i = 0; i < 20459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20459(int& val) {
    const int x = 20459;
    std::cout << 20459;
    for (int i = 0; i < 20459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20460(int& val) {
    func_in_20460(val);
    const int x = 20460;
    std::cout << 20460;
    for (int i = 0; i < 20460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20460(int& val) {
    const int x = 20460;
    std::cout << 20460;
    for (int i = 0; i < 20460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20461(int& val) {
    func_in_20461(val);
    const int x = 20461;
    std::cout << 20461;
    for (int i = 0; i < 20461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20461(int& val) {
    const int x = 20461;
    std::cout << 20461;
    for (int i = 0; i < 20461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20462(int& val) {
    func_in_20462(val);
    const int x = 20462;
    std::cout << 20462;
    for (int i = 0; i < 20462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20462(int& val) {
    const int x = 20462;
    std::cout << 20462;
    for (int i = 0; i < 20462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20463(int& val) {
    func_in_20463(val);
    const int x = 20463;
    std::cout << 20463;
    for (int i = 0; i < 20463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20463(int& val) {
    const int x = 20463;
    std::cout << 20463;
    for (int i = 0; i < 20463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20464(int& val) {
    func_in_20464(val);
    const int x = 20464;
    std::cout << 20464;
    for (int i = 0; i < 20464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20464(int& val) {
    const int x = 20464;
    std::cout << 20464;
    for (int i = 0; i < 20464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20465(int& val) {
    func_in_20465(val);
    const int x = 20465;
    std::cout << 20465;
    for (int i = 0; i < 20465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20465(int& val) {
    const int x = 20465;
    std::cout << 20465;
    for (int i = 0; i < 20465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20466(int& val) {
    func_in_20466(val);
    const int x = 20466;
    std::cout << 20466;
    for (int i = 0; i < 20466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20466(int& val) {
    const int x = 20466;
    std::cout << 20466;
    for (int i = 0; i < 20466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20467(int& val) {
    func_in_20467(val);
    const int x = 20467;
    std::cout << 20467;
    for (int i = 0; i < 20467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20467(int& val) {
    const int x = 20467;
    std::cout << 20467;
    for (int i = 0; i < 20467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20468(int& val) {
    func_in_20468(val);
    const int x = 20468;
    std::cout << 20468;
    for (int i = 0; i < 20468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20468(int& val) {
    const int x = 20468;
    std::cout << 20468;
    for (int i = 0; i < 20468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20469(int& val) {
    func_in_20469(val);
    const int x = 20469;
    std::cout << 20469;
    for (int i = 0; i < 20469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20469(int& val) {
    const int x = 20469;
    std::cout << 20469;
    for (int i = 0; i < 20469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20470(int& val) {
    func_in_20470(val);
    const int x = 20470;
    std::cout << 20470;
    for (int i = 0; i < 20470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20470(int& val) {
    const int x = 20470;
    std::cout << 20470;
    for (int i = 0; i < 20470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20471(int& val) {
    func_in_20471(val);
    const int x = 20471;
    std::cout << 20471;
    for (int i = 0; i < 20471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20471(int& val) {
    const int x = 20471;
    std::cout << 20471;
    for (int i = 0; i < 20471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20472(int& val) {
    func_in_20472(val);
    const int x = 20472;
    std::cout << 20472;
    for (int i = 0; i < 20472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20472(int& val) {
    const int x = 20472;
    std::cout << 20472;
    for (int i = 0; i < 20472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20473(int& val) {
    func_in_20473(val);
    const int x = 20473;
    std::cout << 20473;
    for (int i = 0; i < 20473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20473(int& val) {
    const int x = 20473;
    std::cout << 20473;
    for (int i = 0; i < 20473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20474(int& val) {
    func_in_20474(val);
    const int x = 20474;
    std::cout << 20474;
    for (int i = 0; i < 20474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20474(int& val) {
    const int x = 20474;
    std::cout << 20474;
    for (int i = 0; i < 20474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20475(int& val) {
    func_in_20475(val);
    const int x = 20475;
    std::cout << 20475;
    for (int i = 0; i < 20475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20475(int& val) {
    const int x = 20475;
    std::cout << 20475;
    for (int i = 0; i < 20475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20476(int& val) {
    func_in_20476(val);
    const int x = 20476;
    std::cout << 20476;
    for (int i = 0; i < 20476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20476(int& val) {
    const int x = 20476;
    std::cout << 20476;
    for (int i = 0; i < 20476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20477(int& val) {
    func_in_20477(val);
    const int x = 20477;
    std::cout << 20477;
    for (int i = 0; i < 20477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20477(int& val) {
    const int x = 20477;
    std::cout << 20477;
    for (int i = 0; i < 20477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20478(int& val) {
    func_in_20478(val);
    const int x = 20478;
    std::cout << 20478;
    for (int i = 0; i < 20478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20478(int& val) {
    const int x = 20478;
    std::cout << 20478;
    for (int i = 0; i < 20478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20479(int& val) {
    func_in_20479(val);
    const int x = 20479;
    std::cout << 20479;
    for (int i = 0; i < 20479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20479(int& val) {
    const int x = 20479;
    std::cout << 20479;
    for (int i = 0; i < 20479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20480(int& val) {
    func_in_20480(val);
    const int x = 20480;
    std::cout << 20480;
    for (int i = 0; i < 20480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20480(int& val) {
    const int x = 20480;
    std::cout << 20480;
    for (int i = 0; i < 20480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20481(int& val) {
    func_in_20481(val);
    const int x = 20481;
    std::cout << 20481;
    for (int i = 0; i < 20481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20481(int& val) {
    const int x = 20481;
    std::cout << 20481;
    for (int i = 0; i < 20481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20482(int& val) {
    func_in_20482(val);
    const int x = 20482;
    std::cout << 20482;
    for (int i = 0; i < 20482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20482(int& val) {
    const int x = 20482;
    std::cout << 20482;
    for (int i = 0; i < 20482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20483(int& val) {
    func_in_20483(val);
    const int x = 20483;
    std::cout << 20483;
    for (int i = 0; i < 20483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20483(int& val) {
    const int x = 20483;
    std::cout << 20483;
    for (int i = 0; i < 20483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20484(int& val) {
    func_in_20484(val);
    const int x = 20484;
    std::cout << 20484;
    for (int i = 0; i < 20484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20484(int& val) {
    const int x = 20484;
    std::cout << 20484;
    for (int i = 0; i < 20484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20485(int& val) {
    func_in_20485(val);
    const int x = 20485;
    std::cout << 20485;
    for (int i = 0; i < 20485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20485(int& val) {
    const int x = 20485;
    std::cout << 20485;
    for (int i = 0; i < 20485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20486(int& val) {
    func_in_20486(val);
    const int x = 20486;
    std::cout << 20486;
    for (int i = 0; i < 20486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20486(int& val) {
    const int x = 20486;
    std::cout << 20486;
    for (int i = 0; i < 20486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20487(int& val) {
    func_in_20487(val);
    const int x = 20487;
    std::cout << 20487;
    for (int i = 0; i < 20487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20487(int& val) {
    const int x = 20487;
    std::cout << 20487;
    for (int i = 0; i < 20487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20488(int& val) {
    func_in_20488(val);
    const int x = 20488;
    std::cout << 20488;
    for (int i = 0; i < 20488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20488(int& val) {
    const int x = 20488;
    std::cout << 20488;
    for (int i = 0; i < 20488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20489(int& val) {
    func_in_20489(val);
    const int x = 20489;
    std::cout << 20489;
    for (int i = 0; i < 20489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20489(int& val) {
    const int x = 20489;
    std::cout << 20489;
    for (int i = 0; i < 20489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20490(int& val) {
    func_in_20490(val);
    const int x = 20490;
    std::cout << 20490;
    for (int i = 0; i < 20490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20490(int& val) {
    const int x = 20490;
    std::cout << 20490;
    for (int i = 0; i < 20490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20491(int& val) {
    func_in_20491(val);
    const int x = 20491;
    std::cout << 20491;
    for (int i = 0; i < 20491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20491(int& val) {
    const int x = 20491;
    std::cout << 20491;
    for (int i = 0; i < 20491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20492(int& val) {
    func_in_20492(val);
    const int x = 20492;
    std::cout << 20492;
    for (int i = 0; i < 20492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20492(int& val) {
    const int x = 20492;
    std::cout << 20492;
    for (int i = 0; i < 20492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20493(int& val) {
    func_in_20493(val);
    const int x = 20493;
    std::cout << 20493;
    for (int i = 0; i < 20493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20493(int& val) {
    const int x = 20493;
    std::cout << 20493;
    for (int i = 0; i < 20493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20494(int& val) {
    func_in_20494(val);
    const int x = 20494;
    std::cout << 20494;
    for (int i = 0; i < 20494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20494(int& val) {
    const int x = 20494;
    std::cout << 20494;
    for (int i = 0; i < 20494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20495(int& val) {
    func_in_20495(val);
    const int x = 20495;
    std::cout << 20495;
    for (int i = 0; i < 20495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20495(int& val) {
    const int x = 20495;
    std::cout << 20495;
    for (int i = 0; i < 20495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20496(int& val) {
    func_in_20496(val);
    const int x = 20496;
    std::cout << 20496;
    for (int i = 0; i < 20496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20496(int& val) {
    const int x = 20496;
    std::cout << 20496;
    for (int i = 0; i < 20496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20497(int& val) {
    func_in_20497(val);
    const int x = 20497;
    std::cout << 20497;
    for (int i = 0; i < 20497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20497(int& val) {
    const int x = 20497;
    std::cout << 20497;
    for (int i = 0; i < 20497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20498(int& val) {
    func_in_20498(val);
    const int x = 20498;
    std::cout << 20498;
    for (int i = 0; i < 20498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20498(int& val) {
    const int x = 20498;
    std::cout << 20498;
    for (int i = 0; i < 20498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20499(int& val) {
    func_in_20499(val);
    const int x = 20499;
    std::cout << 20499;
    for (int i = 0; i < 20499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20499(int& val) {
    const int x = 20499;
    std::cout << 20499;
    for (int i = 0; i < 20499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20500(int& val) {
    func_in_20500(val);
    const int x = 20500;
    std::cout << 20500;
    for (int i = 0; i < 20500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20500(int& val) {
    const int x = 20500;
    std::cout << 20500;
    for (int i = 0; i < 20500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20501(int& val) {
    func_in_20501(val);
    const int x = 20501;
    std::cout << 20501;
    for (int i = 0; i < 20501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20501(int& val) {
    const int x = 20501;
    std::cout << 20501;
    for (int i = 0; i < 20501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20502(int& val) {
    func_in_20502(val);
    const int x = 20502;
    std::cout << 20502;
    for (int i = 0; i < 20502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20502(int& val) {
    const int x = 20502;
    std::cout << 20502;
    for (int i = 0; i < 20502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20503(int& val) {
    func_in_20503(val);
    const int x = 20503;
    std::cout << 20503;
    for (int i = 0; i < 20503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20503(int& val) {
    const int x = 20503;
    std::cout << 20503;
    for (int i = 0; i < 20503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20504(int& val) {
    func_in_20504(val);
    const int x = 20504;
    std::cout << 20504;
    for (int i = 0; i < 20504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20504(int& val) {
    const int x = 20504;
    std::cout << 20504;
    for (int i = 0; i < 20504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20505(int& val) {
    func_in_20505(val);
    const int x = 20505;
    std::cout << 20505;
    for (int i = 0; i < 20505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20505(int& val) {
    const int x = 20505;
    std::cout << 20505;
    for (int i = 0; i < 20505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20506(int& val) {
    func_in_20506(val);
    const int x = 20506;
    std::cout << 20506;
    for (int i = 0; i < 20506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20506(int& val) {
    const int x = 20506;
    std::cout << 20506;
    for (int i = 0; i < 20506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20507(int& val) {
    func_in_20507(val);
    const int x = 20507;
    std::cout << 20507;
    for (int i = 0; i < 20507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20507(int& val) {
    const int x = 20507;
    std::cout << 20507;
    for (int i = 0; i < 20507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20508(int& val) {
    func_in_20508(val);
    const int x = 20508;
    std::cout << 20508;
    for (int i = 0; i < 20508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20508(int& val) {
    const int x = 20508;
    std::cout << 20508;
    for (int i = 0; i < 20508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20509(int& val) {
    func_in_20509(val);
    const int x = 20509;
    std::cout << 20509;
    for (int i = 0; i < 20509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20509(int& val) {
    const int x = 20509;
    std::cout << 20509;
    for (int i = 0; i < 20509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20510(int& val) {
    func_in_20510(val);
    const int x = 20510;
    std::cout << 20510;
    for (int i = 0; i < 20510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20510(int& val) {
    const int x = 20510;
    std::cout << 20510;
    for (int i = 0; i < 20510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20511(int& val) {
    func_in_20511(val);
    const int x = 20511;
    std::cout << 20511;
    for (int i = 0; i < 20511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20511(int& val) {
    const int x = 20511;
    std::cout << 20511;
    for (int i = 0; i < 20511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20512(int& val) {
    func_in_20512(val);
    const int x = 20512;
    std::cout << 20512;
    for (int i = 0; i < 20512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20512(int& val) {
    const int x = 20512;
    std::cout << 20512;
    for (int i = 0; i < 20512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20513(int& val) {
    func_in_20513(val);
    const int x = 20513;
    std::cout << 20513;
    for (int i = 0; i < 20513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20513(int& val) {
    const int x = 20513;
    std::cout << 20513;
    for (int i = 0; i < 20513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20514(int& val) {
    func_in_20514(val);
    const int x = 20514;
    std::cout << 20514;
    for (int i = 0; i < 20514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20514(int& val) {
    const int x = 20514;
    std::cout << 20514;
    for (int i = 0; i < 20514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20515(int& val) {
    func_in_20515(val);
    const int x = 20515;
    std::cout << 20515;
    for (int i = 0; i < 20515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20515(int& val) {
    const int x = 20515;
    std::cout << 20515;
    for (int i = 0; i < 20515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20516(int& val) {
    func_in_20516(val);
    const int x = 20516;
    std::cout << 20516;
    for (int i = 0; i < 20516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20516(int& val) {
    const int x = 20516;
    std::cout << 20516;
    for (int i = 0; i < 20516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20517(int& val) {
    func_in_20517(val);
    const int x = 20517;
    std::cout << 20517;
    for (int i = 0; i < 20517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20517(int& val) {
    const int x = 20517;
    std::cout << 20517;
    for (int i = 0; i < 20517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20518(int& val) {
    func_in_20518(val);
    const int x = 20518;
    std::cout << 20518;
    for (int i = 0; i < 20518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20518(int& val) {
    const int x = 20518;
    std::cout << 20518;
    for (int i = 0; i < 20518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20519(int& val) {
    func_in_20519(val);
    const int x = 20519;
    std::cout << 20519;
    for (int i = 0; i < 20519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20519(int& val) {
    const int x = 20519;
    std::cout << 20519;
    for (int i = 0; i < 20519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20520(int& val) {
    func_in_20520(val);
    const int x = 20520;
    std::cout << 20520;
    for (int i = 0; i < 20520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20520(int& val) {
    const int x = 20520;
    std::cout << 20520;
    for (int i = 0; i < 20520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20521(int& val) {
    func_in_20521(val);
    const int x = 20521;
    std::cout << 20521;
    for (int i = 0; i < 20521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20521(int& val) {
    const int x = 20521;
    std::cout << 20521;
    for (int i = 0; i < 20521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20522(int& val) {
    func_in_20522(val);
    const int x = 20522;
    std::cout << 20522;
    for (int i = 0; i < 20522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20522(int& val) {
    const int x = 20522;
    std::cout << 20522;
    for (int i = 0; i < 20522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20523(int& val) {
    func_in_20523(val);
    const int x = 20523;
    std::cout << 20523;
    for (int i = 0; i < 20523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20523(int& val) {
    const int x = 20523;
    std::cout << 20523;
    for (int i = 0; i < 20523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20524(int& val) {
    func_in_20524(val);
    const int x = 20524;
    std::cout << 20524;
    for (int i = 0; i < 20524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20524(int& val) {
    const int x = 20524;
    std::cout << 20524;
    for (int i = 0; i < 20524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20525(int& val) {
    func_in_20525(val);
    const int x = 20525;
    std::cout << 20525;
    for (int i = 0; i < 20525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20525(int& val) {
    const int x = 20525;
    std::cout << 20525;
    for (int i = 0; i < 20525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20526(int& val) {
    func_in_20526(val);
    const int x = 20526;
    std::cout << 20526;
    for (int i = 0; i < 20526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20526(int& val) {
    const int x = 20526;
    std::cout << 20526;
    for (int i = 0; i < 20526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20527(int& val) {
    func_in_20527(val);
    const int x = 20527;
    std::cout << 20527;
    for (int i = 0; i < 20527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20527(int& val) {
    const int x = 20527;
    std::cout << 20527;
    for (int i = 0; i < 20527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20528(int& val) {
    func_in_20528(val);
    const int x = 20528;
    std::cout << 20528;
    for (int i = 0; i < 20528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20528(int& val) {
    const int x = 20528;
    std::cout << 20528;
    for (int i = 0; i < 20528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20529(int& val) {
    func_in_20529(val);
    const int x = 20529;
    std::cout << 20529;
    for (int i = 0; i < 20529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20529(int& val) {
    const int x = 20529;
    std::cout << 20529;
    for (int i = 0; i < 20529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20530(int& val) {
    func_in_20530(val);
    const int x = 20530;
    std::cout << 20530;
    for (int i = 0; i < 20530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20530(int& val) {
    const int x = 20530;
    std::cout << 20530;
    for (int i = 0; i < 20530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20531(int& val) {
    func_in_20531(val);
    const int x = 20531;
    std::cout << 20531;
    for (int i = 0; i < 20531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20531(int& val) {
    const int x = 20531;
    std::cout << 20531;
    for (int i = 0; i < 20531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20532(int& val) {
    func_in_20532(val);
    const int x = 20532;
    std::cout << 20532;
    for (int i = 0; i < 20532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20532(int& val) {
    const int x = 20532;
    std::cout << 20532;
    for (int i = 0; i < 20532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20533(int& val) {
    func_in_20533(val);
    const int x = 20533;
    std::cout << 20533;
    for (int i = 0; i < 20533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20533(int& val) {
    const int x = 20533;
    std::cout << 20533;
    for (int i = 0; i < 20533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20534(int& val) {
    func_in_20534(val);
    const int x = 20534;
    std::cout << 20534;
    for (int i = 0; i < 20534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20534(int& val) {
    const int x = 20534;
    std::cout << 20534;
    for (int i = 0; i < 20534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20535(int& val) {
    func_in_20535(val);
    const int x = 20535;
    std::cout << 20535;
    for (int i = 0; i < 20535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20535(int& val) {
    const int x = 20535;
    std::cout << 20535;
    for (int i = 0; i < 20535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20536(int& val) {
    func_in_20536(val);
    const int x = 20536;
    std::cout << 20536;
    for (int i = 0; i < 20536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20536(int& val) {
    const int x = 20536;
    std::cout << 20536;
    for (int i = 0; i < 20536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20537(int& val) {
    func_in_20537(val);
    const int x = 20537;
    std::cout << 20537;
    for (int i = 0; i < 20537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20537(int& val) {
    const int x = 20537;
    std::cout << 20537;
    for (int i = 0; i < 20537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20538(int& val) {
    func_in_20538(val);
    const int x = 20538;
    std::cout << 20538;
    for (int i = 0; i < 20538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20538(int& val) {
    const int x = 20538;
    std::cout << 20538;
    for (int i = 0; i < 20538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20539(int& val) {
    func_in_20539(val);
    const int x = 20539;
    std::cout << 20539;
    for (int i = 0; i < 20539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20539(int& val) {
    const int x = 20539;
    std::cout << 20539;
    for (int i = 0; i < 20539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20540(int& val) {
    func_in_20540(val);
    const int x = 20540;
    std::cout << 20540;
    for (int i = 0; i < 20540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20540(int& val) {
    const int x = 20540;
    std::cout << 20540;
    for (int i = 0; i < 20540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20541(int& val) {
    func_in_20541(val);
    const int x = 20541;
    std::cout << 20541;
    for (int i = 0; i < 20541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20541(int& val) {
    const int x = 20541;
    std::cout << 20541;
    for (int i = 0; i < 20541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20542(int& val) {
    func_in_20542(val);
    const int x = 20542;
    std::cout << 20542;
    for (int i = 0; i < 20542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20542(int& val) {
    const int x = 20542;
    std::cout << 20542;
    for (int i = 0; i < 20542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20543(int& val) {
    func_in_20543(val);
    const int x = 20543;
    std::cout << 20543;
    for (int i = 0; i < 20543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20543(int& val) {
    const int x = 20543;
    std::cout << 20543;
    for (int i = 0; i < 20543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20544(int& val) {
    func_in_20544(val);
    const int x = 20544;
    std::cout << 20544;
    for (int i = 0; i < 20544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20544(int& val) {
    const int x = 20544;
    std::cout << 20544;
    for (int i = 0; i < 20544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20545(int& val) {
    func_in_20545(val);
    const int x = 20545;
    std::cout << 20545;
    for (int i = 0; i < 20545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20545(int& val) {
    const int x = 20545;
    std::cout << 20545;
    for (int i = 0; i < 20545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20546(int& val) {
    func_in_20546(val);
    const int x = 20546;
    std::cout << 20546;
    for (int i = 0; i < 20546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20546(int& val) {
    const int x = 20546;
    std::cout << 20546;
    for (int i = 0; i < 20546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20547(int& val) {
    func_in_20547(val);
    const int x = 20547;
    std::cout << 20547;
    for (int i = 0; i < 20547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20547(int& val) {
    const int x = 20547;
    std::cout << 20547;
    for (int i = 0; i < 20547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20548(int& val) {
    func_in_20548(val);
    const int x = 20548;
    std::cout << 20548;
    for (int i = 0; i < 20548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20548(int& val) {
    const int x = 20548;
    std::cout << 20548;
    for (int i = 0; i < 20548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20549(int& val) {
    func_in_20549(val);
    const int x = 20549;
    std::cout << 20549;
    for (int i = 0; i < 20549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20549(int& val) {
    const int x = 20549;
    std::cout << 20549;
    for (int i = 0; i < 20549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20550(int& val) {
    func_in_20550(val);
    const int x = 20550;
    std::cout << 20550;
    for (int i = 0; i < 20550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20550(int& val) {
    const int x = 20550;
    std::cout << 20550;
    for (int i = 0; i < 20550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20551(int& val) {
    func_in_20551(val);
    const int x = 20551;
    std::cout << 20551;
    for (int i = 0; i < 20551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20551(int& val) {
    const int x = 20551;
    std::cout << 20551;
    for (int i = 0; i < 20551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20552(int& val) {
    func_in_20552(val);
    const int x = 20552;
    std::cout << 20552;
    for (int i = 0; i < 20552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20552(int& val) {
    const int x = 20552;
    std::cout << 20552;
    for (int i = 0; i < 20552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20553(int& val) {
    func_in_20553(val);
    const int x = 20553;
    std::cout << 20553;
    for (int i = 0; i < 20553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20553(int& val) {
    const int x = 20553;
    std::cout << 20553;
    for (int i = 0; i < 20553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20554(int& val) {
    func_in_20554(val);
    const int x = 20554;
    std::cout << 20554;
    for (int i = 0; i < 20554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20554(int& val) {
    const int x = 20554;
    std::cout << 20554;
    for (int i = 0; i < 20554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20555(int& val) {
    func_in_20555(val);
    const int x = 20555;
    std::cout << 20555;
    for (int i = 0; i < 20555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20555(int& val) {
    const int x = 20555;
    std::cout << 20555;
    for (int i = 0; i < 20555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20556(int& val) {
    func_in_20556(val);
    const int x = 20556;
    std::cout << 20556;
    for (int i = 0; i < 20556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20556(int& val) {
    const int x = 20556;
    std::cout << 20556;
    for (int i = 0; i < 20556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20557(int& val) {
    func_in_20557(val);
    const int x = 20557;
    std::cout << 20557;
    for (int i = 0; i < 20557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20557(int& val) {
    const int x = 20557;
    std::cout << 20557;
    for (int i = 0; i < 20557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20558(int& val) {
    func_in_20558(val);
    const int x = 20558;
    std::cout << 20558;
    for (int i = 0; i < 20558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20558(int& val) {
    const int x = 20558;
    std::cout << 20558;
    for (int i = 0; i < 20558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20559(int& val) {
    func_in_20559(val);
    const int x = 20559;
    std::cout << 20559;
    for (int i = 0; i < 20559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20559(int& val) {
    const int x = 20559;
    std::cout << 20559;
    for (int i = 0; i < 20559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20560(int& val) {
    func_in_20560(val);
    const int x = 20560;
    std::cout << 20560;
    for (int i = 0; i < 20560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20560(int& val) {
    const int x = 20560;
    std::cout << 20560;
    for (int i = 0; i < 20560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20561(int& val) {
    func_in_20561(val);
    const int x = 20561;
    std::cout << 20561;
    for (int i = 0; i < 20561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20561(int& val) {
    const int x = 20561;
    std::cout << 20561;
    for (int i = 0; i < 20561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20562(int& val) {
    func_in_20562(val);
    const int x = 20562;
    std::cout << 20562;
    for (int i = 0; i < 20562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20562(int& val) {
    const int x = 20562;
    std::cout << 20562;
    for (int i = 0; i < 20562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20563(int& val) {
    func_in_20563(val);
    const int x = 20563;
    std::cout << 20563;
    for (int i = 0; i < 20563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20563(int& val) {
    const int x = 20563;
    std::cout << 20563;
    for (int i = 0; i < 20563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20564(int& val) {
    func_in_20564(val);
    const int x = 20564;
    std::cout << 20564;
    for (int i = 0; i < 20564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20564(int& val) {
    const int x = 20564;
    std::cout << 20564;
    for (int i = 0; i < 20564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20565(int& val) {
    func_in_20565(val);
    const int x = 20565;
    std::cout << 20565;
    for (int i = 0; i < 20565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20565(int& val) {
    const int x = 20565;
    std::cout << 20565;
    for (int i = 0; i < 20565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20566(int& val) {
    func_in_20566(val);
    const int x = 20566;
    std::cout << 20566;
    for (int i = 0; i < 20566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20566(int& val) {
    const int x = 20566;
    std::cout << 20566;
    for (int i = 0; i < 20566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20567(int& val) {
    func_in_20567(val);
    const int x = 20567;
    std::cout << 20567;
    for (int i = 0; i < 20567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20567(int& val) {
    const int x = 20567;
    std::cout << 20567;
    for (int i = 0; i < 20567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20568(int& val) {
    func_in_20568(val);
    const int x = 20568;
    std::cout << 20568;
    for (int i = 0; i < 20568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20568(int& val) {
    const int x = 20568;
    std::cout << 20568;
    for (int i = 0; i < 20568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20569(int& val) {
    func_in_20569(val);
    const int x = 20569;
    std::cout << 20569;
    for (int i = 0; i < 20569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20569(int& val) {
    const int x = 20569;
    std::cout << 20569;
    for (int i = 0; i < 20569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20570(int& val) {
    func_in_20570(val);
    const int x = 20570;
    std::cout << 20570;
    for (int i = 0; i < 20570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20570(int& val) {
    const int x = 20570;
    std::cout << 20570;
    for (int i = 0; i < 20570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20571(int& val) {
    func_in_20571(val);
    const int x = 20571;
    std::cout << 20571;
    for (int i = 0; i < 20571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20571(int& val) {
    const int x = 20571;
    std::cout << 20571;
    for (int i = 0; i < 20571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20572(int& val) {
    func_in_20572(val);
    const int x = 20572;
    std::cout << 20572;
    for (int i = 0; i < 20572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20572(int& val) {
    const int x = 20572;
    std::cout << 20572;
    for (int i = 0; i < 20572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20573(int& val) {
    func_in_20573(val);
    const int x = 20573;
    std::cout << 20573;
    for (int i = 0; i < 20573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20573(int& val) {
    const int x = 20573;
    std::cout << 20573;
    for (int i = 0; i < 20573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20574(int& val) {
    func_in_20574(val);
    const int x = 20574;
    std::cout << 20574;
    for (int i = 0; i < 20574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20574(int& val) {
    const int x = 20574;
    std::cout << 20574;
    for (int i = 0; i < 20574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20575(int& val) {
    func_in_20575(val);
    const int x = 20575;
    std::cout << 20575;
    for (int i = 0; i < 20575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20575(int& val) {
    const int x = 20575;
    std::cout << 20575;
    for (int i = 0; i < 20575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20576(int& val) {
    func_in_20576(val);
    const int x = 20576;
    std::cout << 20576;
    for (int i = 0; i < 20576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20576(int& val) {
    const int x = 20576;
    std::cout << 20576;
    for (int i = 0; i < 20576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20577(int& val) {
    func_in_20577(val);
    const int x = 20577;
    std::cout << 20577;
    for (int i = 0; i < 20577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20577(int& val) {
    const int x = 20577;
    std::cout << 20577;
    for (int i = 0; i < 20577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20578(int& val) {
    func_in_20578(val);
    const int x = 20578;
    std::cout << 20578;
    for (int i = 0; i < 20578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20578(int& val) {
    const int x = 20578;
    std::cout << 20578;
    for (int i = 0; i < 20578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20579(int& val) {
    func_in_20579(val);
    const int x = 20579;
    std::cout << 20579;
    for (int i = 0; i < 20579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20579(int& val) {
    const int x = 20579;
    std::cout << 20579;
    for (int i = 0; i < 20579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20580(int& val) {
    func_in_20580(val);
    const int x = 20580;
    std::cout << 20580;
    for (int i = 0; i < 20580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20580(int& val) {
    const int x = 20580;
    std::cout << 20580;
    for (int i = 0; i < 20580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20581(int& val) {
    func_in_20581(val);
    const int x = 20581;
    std::cout << 20581;
    for (int i = 0; i < 20581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20581(int& val) {
    const int x = 20581;
    std::cout << 20581;
    for (int i = 0; i < 20581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20582(int& val) {
    func_in_20582(val);
    const int x = 20582;
    std::cout << 20582;
    for (int i = 0; i < 20582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20582(int& val) {
    const int x = 20582;
    std::cout << 20582;
    for (int i = 0; i < 20582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20583(int& val) {
    func_in_20583(val);
    const int x = 20583;
    std::cout << 20583;
    for (int i = 0; i < 20583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20583(int& val) {
    const int x = 20583;
    std::cout << 20583;
    for (int i = 0; i < 20583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20584(int& val) {
    func_in_20584(val);
    const int x = 20584;
    std::cout << 20584;
    for (int i = 0; i < 20584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20584(int& val) {
    const int x = 20584;
    std::cout << 20584;
    for (int i = 0; i < 20584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20585(int& val) {
    func_in_20585(val);
    const int x = 20585;
    std::cout << 20585;
    for (int i = 0; i < 20585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20585(int& val) {
    const int x = 20585;
    std::cout << 20585;
    for (int i = 0; i < 20585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20586(int& val) {
    func_in_20586(val);
    const int x = 20586;
    std::cout << 20586;
    for (int i = 0; i < 20586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20586(int& val) {
    const int x = 20586;
    std::cout << 20586;
    for (int i = 0; i < 20586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20587(int& val) {
    func_in_20587(val);
    const int x = 20587;
    std::cout << 20587;
    for (int i = 0; i < 20587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20587(int& val) {
    const int x = 20587;
    std::cout << 20587;
    for (int i = 0; i < 20587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20588(int& val) {
    func_in_20588(val);
    const int x = 20588;
    std::cout << 20588;
    for (int i = 0; i < 20588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20588(int& val) {
    const int x = 20588;
    std::cout << 20588;
    for (int i = 0; i < 20588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20589(int& val) {
    func_in_20589(val);
    const int x = 20589;
    std::cout << 20589;
    for (int i = 0; i < 20589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20589(int& val) {
    const int x = 20589;
    std::cout << 20589;
    for (int i = 0; i < 20589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20590(int& val) {
    func_in_20590(val);
    const int x = 20590;
    std::cout << 20590;
    for (int i = 0; i < 20590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20590(int& val) {
    const int x = 20590;
    std::cout << 20590;
    for (int i = 0; i < 20590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20591(int& val) {
    func_in_20591(val);
    const int x = 20591;
    std::cout << 20591;
    for (int i = 0; i < 20591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20591(int& val) {
    const int x = 20591;
    std::cout << 20591;
    for (int i = 0; i < 20591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20592(int& val) {
    func_in_20592(val);
    const int x = 20592;
    std::cout << 20592;
    for (int i = 0; i < 20592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20592(int& val) {
    const int x = 20592;
    std::cout << 20592;
    for (int i = 0; i < 20592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20593(int& val) {
    func_in_20593(val);
    const int x = 20593;
    std::cout << 20593;
    for (int i = 0; i < 20593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20593(int& val) {
    const int x = 20593;
    std::cout << 20593;
    for (int i = 0; i < 20593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20594(int& val) {
    func_in_20594(val);
    const int x = 20594;
    std::cout << 20594;
    for (int i = 0; i < 20594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20594(int& val) {
    const int x = 20594;
    std::cout << 20594;
    for (int i = 0; i < 20594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20595(int& val) {
    func_in_20595(val);
    const int x = 20595;
    std::cout << 20595;
    for (int i = 0; i < 20595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20595(int& val) {
    const int x = 20595;
    std::cout << 20595;
    for (int i = 0; i < 20595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20596(int& val) {
    func_in_20596(val);
    const int x = 20596;
    std::cout << 20596;
    for (int i = 0; i < 20596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20596(int& val) {
    const int x = 20596;
    std::cout << 20596;
    for (int i = 0; i < 20596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20597(int& val) {
    func_in_20597(val);
    const int x = 20597;
    std::cout << 20597;
    for (int i = 0; i < 20597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20597(int& val) {
    const int x = 20597;
    std::cout << 20597;
    for (int i = 0; i < 20597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20598(int& val) {
    func_in_20598(val);
    const int x = 20598;
    std::cout << 20598;
    for (int i = 0; i < 20598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20598(int& val) {
    const int x = 20598;
    std::cout << 20598;
    for (int i = 0; i < 20598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20599(int& val) {
    func_in_20599(val);
    const int x = 20599;
    std::cout << 20599;
    for (int i = 0; i < 20599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20599(int& val) {
    const int x = 20599;
    std::cout << 20599;
    for (int i = 0; i < 20599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20600(int& val) {
    func_in_20600(val);
    const int x = 20600;
    std::cout << 20600;
    for (int i = 0; i < 20600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20600(int& val) {
    const int x = 20600;
    std::cout << 20600;
    for (int i = 0; i < 20600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20601(int& val) {
    func_in_20601(val);
    const int x = 20601;
    std::cout << 20601;
    for (int i = 0; i < 20601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20601(int& val) {
    const int x = 20601;
    std::cout << 20601;
    for (int i = 0; i < 20601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20602(int& val) {
    func_in_20602(val);
    const int x = 20602;
    std::cout << 20602;
    for (int i = 0; i < 20602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20602(int& val) {
    const int x = 20602;
    std::cout << 20602;
    for (int i = 0; i < 20602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20603(int& val) {
    func_in_20603(val);
    const int x = 20603;
    std::cout << 20603;
    for (int i = 0; i < 20603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20603(int& val) {
    const int x = 20603;
    std::cout << 20603;
    for (int i = 0; i < 20603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20604(int& val) {
    func_in_20604(val);
    const int x = 20604;
    std::cout << 20604;
    for (int i = 0; i < 20604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20604(int& val) {
    const int x = 20604;
    std::cout << 20604;
    for (int i = 0; i < 20604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20605(int& val) {
    func_in_20605(val);
    const int x = 20605;
    std::cout << 20605;
    for (int i = 0; i < 20605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20605(int& val) {
    const int x = 20605;
    std::cout << 20605;
    for (int i = 0; i < 20605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20606(int& val) {
    func_in_20606(val);
    const int x = 20606;
    std::cout << 20606;
    for (int i = 0; i < 20606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20606(int& val) {
    const int x = 20606;
    std::cout << 20606;
    for (int i = 0; i < 20606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20607(int& val) {
    func_in_20607(val);
    const int x = 20607;
    std::cout << 20607;
    for (int i = 0; i < 20607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20607(int& val) {
    const int x = 20607;
    std::cout << 20607;
    for (int i = 0; i < 20607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20608(int& val) {
    func_in_20608(val);
    const int x = 20608;
    std::cout << 20608;
    for (int i = 0; i < 20608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20608(int& val) {
    const int x = 20608;
    std::cout << 20608;
    for (int i = 0; i < 20608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20609(int& val) {
    func_in_20609(val);
    const int x = 20609;
    std::cout << 20609;
    for (int i = 0; i < 20609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20609(int& val) {
    const int x = 20609;
    std::cout << 20609;
    for (int i = 0; i < 20609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20610(int& val) {
    func_in_20610(val);
    const int x = 20610;
    std::cout << 20610;
    for (int i = 0; i < 20610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20610(int& val) {
    const int x = 20610;
    std::cout << 20610;
    for (int i = 0; i < 20610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20611(int& val) {
    func_in_20611(val);
    const int x = 20611;
    std::cout << 20611;
    for (int i = 0; i < 20611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20611(int& val) {
    const int x = 20611;
    std::cout << 20611;
    for (int i = 0; i < 20611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20612(int& val) {
    func_in_20612(val);
    const int x = 20612;
    std::cout << 20612;
    for (int i = 0; i < 20612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20612(int& val) {
    const int x = 20612;
    std::cout << 20612;
    for (int i = 0; i < 20612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20613(int& val) {
    func_in_20613(val);
    const int x = 20613;
    std::cout << 20613;
    for (int i = 0; i < 20613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20613(int& val) {
    const int x = 20613;
    std::cout << 20613;
    for (int i = 0; i < 20613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20614(int& val) {
    func_in_20614(val);
    const int x = 20614;
    std::cout << 20614;
    for (int i = 0; i < 20614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20614(int& val) {
    const int x = 20614;
    std::cout << 20614;
    for (int i = 0; i < 20614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20615(int& val) {
    func_in_20615(val);
    const int x = 20615;
    std::cout << 20615;
    for (int i = 0; i < 20615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20615(int& val) {
    const int x = 20615;
    std::cout << 20615;
    for (int i = 0; i < 20615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20616(int& val) {
    func_in_20616(val);
    const int x = 20616;
    std::cout << 20616;
    for (int i = 0; i < 20616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20616(int& val) {
    const int x = 20616;
    std::cout << 20616;
    for (int i = 0; i < 20616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20617(int& val) {
    func_in_20617(val);
    const int x = 20617;
    std::cout << 20617;
    for (int i = 0; i < 20617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20617(int& val) {
    const int x = 20617;
    std::cout << 20617;
    for (int i = 0; i < 20617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20618(int& val) {
    func_in_20618(val);
    const int x = 20618;
    std::cout << 20618;
    for (int i = 0; i < 20618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20618(int& val) {
    const int x = 20618;
    std::cout << 20618;
    for (int i = 0; i < 20618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20619(int& val) {
    func_in_20619(val);
    const int x = 20619;
    std::cout << 20619;
    for (int i = 0; i < 20619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20619(int& val) {
    const int x = 20619;
    std::cout << 20619;
    for (int i = 0; i < 20619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20620(int& val) {
    func_in_20620(val);
    const int x = 20620;
    std::cout << 20620;
    for (int i = 0; i < 20620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20620(int& val) {
    const int x = 20620;
    std::cout << 20620;
    for (int i = 0; i < 20620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20621(int& val) {
    func_in_20621(val);
    const int x = 20621;
    std::cout << 20621;
    for (int i = 0; i < 20621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20621(int& val) {
    const int x = 20621;
    std::cout << 20621;
    for (int i = 0; i < 20621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20622(int& val) {
    func_in_20622(val);
    const int x = 20622;
    std::cout << 20622;
    for (int i = 0; i < 20622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20622(int& val) {
    const int x = 20622;
    std::cout << 20622;
    for (int i = 0; i < 20622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20623(int& val) {
    func_in_20623(val);
    const int x = 20623;
    std::cout << 20623;
    for (int i = 0; i < 20623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20623(int& val) {
    const int x = 20623;
    std::cout << 20623;
    for (int i = 0; i < 20623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20624(int& val) {
    func_in_20624(val);
    const int x = 20624;
    std::cout << 20624;
    for (int i = 0; i < 20624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20624(int& val) {
    const int x = 20624;
    std::cout << 20624;
    for (int i = 0; i < 20624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20625(int& val) {
    func_in_20625(val);
    const int x = 20625;
    std::cout << 20625;
    for (int i = 0; i < 20625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20625(int& val) {
    const int x = 20625;
    std::cout << 20625;
    for (int i = 0; i < 20625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20626(int& val) {
    func_in_20626(val);
    const int x = 20626;
    std::cout << 20626;
    for (int i = 0; i < 20626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20626(int& val) {
    const int x = 20626;
    std::cout << 20626;
    for (int i = 0; i < 20626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20627(int& val) {
    func_in_20627(val);
    const int x = 20627;
    std::cout << 20627;
    for (int i = 0; i < 20627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20627(int& val) {
    const int x = 20627;
    std::cout << 20627;
    for (int i = 0; i < 20627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20628(int& val) {
    func_in_20628(val);
    const int x = 20628;
    std::cout << 20628;
    for (int i = 0; i < 20628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20628(int& val) {
    const int x = 20628;
    std::cout << 20628;
    for (int i = 0; i < 20628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20629(int& val) {
    func_in_20629(val);
    const int x = 20629;
    std::cout << 20629;
    for (int i = 0; i < 20629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20629(int& val) {
    const int x = 20629;
    std::cout << 20629;
    for (int i = 0; i < 20629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20630(int& val) {
    func_in_20630(val);
    const int x = 20630;
    std::cout << 20630;
    for (int i = 0; i < 20630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20630(int& val) {
    const int x = 20630;
    std::cout << 20630;
    for (int i = 0; i < 20630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20631(int& val) {
    func_in_20631(val);
    const int x = 20631;
    std::cout << 20631;
    for (int i = 0; i < 20631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20631(int& val) {
    const int x = 20631;
    std::cout << 20631;
    for (int i = 0; i < 20631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20632(int& val) {
    func_in_20632(val);
    const int x = 20632;
    std::cout << 20632;
    for (int i = 0; i < 20632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20632(int& val) {
    const int x = 20632;
    std::cout << 20632;
    for (int i = 0; i < 20632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20633(int& val) {
    func_in_20633(val);
    const int x = 20633;
    std::cout << 20633;
    for (int i = 0; i < 20633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20633(int& val) {
    const int x = 20633;
    std::cout << 20633;
    for (int i = 0; i < 20633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20634(int& val) {
    func_in_20634(val);
    const int x = 20634;
    std::cout << 20634;
    for (int i = 0; i < 20634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20634(int& val) {
    const int x = 20634;
    std::cout << 20634;
    for (int i = 0; i < 20634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20635(int& val) {
    func_in_20635(val);
    const int x = 20635;
    std::cout << 20635;
    for (int i = 0; i < 20635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20635(int& val) {
    const int x = 20635;
    std::cout << 20635;
    for (int i = 0; i < 20635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20636(int& val) {
    func_in_20636(val);
    const int x = 20636;
    std::cout << 20636;
    for (int i = 0; i < 20636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20636(int& val) {
    const int x = 20636;
    std::cout << 20636;
    for (int i = 0; i < 20636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20637(int& val) {
    func_in_20637(val);
    const int x = 20637;
    std::cout << 20637;
    for (int i = 0; i < 20637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20637(int& val) {
    const int x = 20637;
    std::cout << 20637;
    for (int i = 0; i < 20637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20638(int& val) {
    func_in_20638(val);
    const int x = 20638;
    std::cout << 20638;
    for (int i = 0; i < 20638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20638(int& val) {
    const int x = 20638;
    std::cout << 20638;
    for (int i = 0; i < 20638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20639(int& val) {
    func_in_20639(val);
    const int x = 20639;
    std::cout << 20639;
    for (int i = 0; i < 20639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20639(int& val) {
    const int x = 20639;
    std::cout << 20639;
    for (int i = 0; i < 20639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20640(int& val) {
    func_in_20640(val);
    const int x = 20640;
    std::cout << 20640;
    for (int i = 0; i < 20640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20640(int& val) {
    const int x = 20640;
    std::cout << 20640;
    for (int i = 0; i < 20640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20641(int& val) {
    func_in_20641(val);
    const int x = 20641;
    std::cout << 20641;
    for (int i = 0; i < 20641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20641(int& val) {
    const int x = 20641;
    std::cout << 20641;
    for (int i = 0; i < 20641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20642(int& val) {
    func_in_20642(val);
    const int x = 20642;
    std::cout << 20642;
    for (int i = 0; i < 20642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20642(int& val) {
    const int x = 20642;
    std::cout << 20642;
    for (int i = 0; i < 20642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20643(int& val) {
    func_in_20643(val);
    const int x = 20643;
    std::cout << 20643;
    for (int i = 0; i < 20643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20643(int& val) {
    const int x = 20643;
    std::cout << 20643;
    for (int i = 0; i < 20643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20644(int& val) {
    func_in_20644(val);
    const int x = 20644;
    std::cout << 20644;
    for (int i = 0; i < 20644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20644(int& val) {
    const int x = 20644;
    std::cout << 20644;
    for (int i = 0; i < 20644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20645(int& val) {
    func_in_20645(val);
    const int x = 20645;
    std::cout << 20645;
    for (int i = 0; i < 20645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20645(int& val) {
    const int x = 20645;
    std::cout << 20645;
    for (int i = 0; i < 20645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20646(int& val) {
    func_in_20646(val);
    const int x = 20646;
    std::cout << 20646;
    for (int i = 0; i < 20646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20646(int& val) {
    const int x = 20646;
    std::cout << 20646;
    for (int i = 0; i < 20646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20647(int& val) {
    func_in_20647(val);
    const int x = 20647;
    std::cout << 20647;
    for (int i = 0; i < 20647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20647(int& val) {
    const int x = 20647;
    std::cout << 20647;
    for (int i = 0; i < 20647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20648(int& val) {
    func_in_20648(val);
    const int x = 20648;
    std::cout << 20648;
    for (int i = 0; i < 20648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20648(int& val) {
    const int x = 20648;
    std::cout << 20648;
    for (int i = 0; i < 20648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20649(int& val) {
    func_in_20649(val);
    const int x = 20649;
    std::cout << 20649;
    for (int i = 0; i < 20649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20649(int& val) {
    const int x = 20649;
    std::cout << 20649;
    for (int i = 0; i < 20649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20650(int& val) {
    func_in_20650(val);
    const int x = 20650;
    std::cout << 20650;
    for (int i = 0; i < 20650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20650(int& val) {
    const int x = 20650;
    std::cout << 20650;
    for (int i = 0; i < 20650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20651(int& val) {
    func_in_20651(val);
    const int x = 20651;
    std::cout << 20651;
    for (int i = 0; i < 20651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20651(int& val) {
    const int x = 20651;
    std::cout << 20651;
    for (int i = 0; i < 20651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20652(int& val) {
    func_in_20652(val);
    const int x = 20652;
    std::cout << 20652;
    for (int i = 0; i < 20652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20652(int& val) {
    const int x = 20652;
    std::cout << 20652;
    for (int i = 0; i < 20652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20653(int& val) {
    func_in_20653(val);
    const int x = 20653;
    std::cout << 20653;
    for (int i = 0; i < 20653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20653(int& val) {
    const int x = 20653;
    std::cout << 20653;
    for (int i = 0; i < 20653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20654(int& val) {
    func_in_20654(val);
    const int x = 20654;
    std::cout << 20654;
    for (int i = 0; i < 20654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20654(int& val) {
    const int x = 20654;
    std::cout << 20654;
    for (int i = 0; i < 20654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20655(int& val) {
    func_in_20655(val);
    const int x = 20655;
    std::cout << 20655;
    for (int i = 0; i < 20655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20655(int& val) {
    const int x = 20655;
    std::cout << 20655;
    for (int i = 0; i < 20655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20656(int& val) {
    func_in_20656(val);
    const int x = 20656;
    std::cout << 20656;
    for (int i = 0; i < 20656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20656(int& val) {
    const int x = 20656;
    std::cout << 20656;
    for (int i = 0; i < 20656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20657(int& val) {
    func_in_20657(val);
    const int x = 20657;
    std::cout << 20657;
    for (int i = 0; i < 20657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20657(int& val) {
    const int x = 20657;
    std::cout << 20657;
    for (int i = 0; i < 20657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20658(int& val) {
    func_in_20658(val);
    const int x = 20658;
    std::cout << 20658;
    for (int i = 0; i < 20658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20658(int& val) {
    const int x = 20658;
    std::cout << 20658;
    for (int i = 0; i < 20658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20659(int& val) {
    func_in_20659(val);
    const int x = 20659;
    std::cout << 20659;
    for (int i = 0; i < 20659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20659(int& val) {
    const int x = 20659;
    std::cout << 20659;
    for (int i = 0; i < 20659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20660(int& val) {
    func_in_20660(val);
    const int x = 20660;
    std::cout << 20660;
    for (int i = 0; i < 20660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20660(int& val) {
    const int x = 20660;
    std::cout << 20660;
    for (int i = 0; i < 20660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20661(int& val) {
    func_in_20661(val);
    const int x = 20661;
    std::cout << 20661;
    for (int i = 0; i < 20661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20661(int& val) {
    const int x = 20661;
    std::cout << 20661;
    for (int i = 0; i < 20661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20662(int& val) {
    func_in_20662(val);
    const int x = 20662;
    std::cout << 20662;
    for (int i = 0; i < 20662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20662(int& val) {
    const int x = 20662;
    std::cout << 20662;
    for (int i = 0; i < 20662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20663(int& val) {
    func_in_20663(val);
    const int x = 20663;
    std::cout << 20663;
    for (int i = 0; i < 20663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20663(int& val) {
    const int x = 20663;
    std::cout << 20663;
    for (int i = 0; i < 20663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20664(int& val) {
    func_in_20664(val);
    const int x = 20664;
    std::cout << 20664;
    for (int i = 0; i < 20664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20664(int& val) {
    const int x = 20664;
    std::cout << 20664;
    for (int i = 0; i < 20664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20665(int& val) {
    func_in_20665(val);
    const int x = 20665;
    std::cout << 20665;
    for (int i = 0; i < 20665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20665(int& val) {
    const int x = 20665;
    std::cout << 20665;
    for (int i = 0; i < 20665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20666(int& val) {
    func_in_20666(val);
    const int x = 20666;
    std::cout << 20666;
    for (int i = 0; i < 20666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20666(int& val) {
    const int x = 20666;
    std::cout << 20666;
    for (int i = 0; i < 20666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20667(int& val) {
    func_in_20667(val);
    const int x = 20667;
    std::cout << 20667;
    for (int i = 0; i < 20667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20667(int& val) {
    const int x = 20667;
    std::cout << 20667;
    for (int i = 0; i < 20667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20668(int& val) {
    func_in_20668(val);
    const int x = 20668;
    std::cout << 20668;
    for (int i = 0; i < 20668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20668(int& val) {
    const int x = 20668;
    std::cout << 20668;
    for (int i = 0; i < 20668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20669(int& val) {
    func_in_20669(val);
    const int x = 20669;
    std::cout << 20669;
    for (int i = 0; i < 20669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20669(int& val) {
    const int x = 20669;
    std::cout << 20669;
    for (int i = 0; i < 20669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20670(int& val) {
    func_in_20670(val);
    const int x = 20670;
    std::cout << 20670;
    for (int i = 0; i < 20670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20670(int& val) {
    const int x = 20670;
    std::cout << 20670;
    for (int i = 0; i < 20670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20671(int& val) {
    func_in_20671(val);
    const int x = 20671;
    std::cout << 20671;
    for (int i = 0; i < 20671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20671(int& val) {
    const int x = 20671;
    std::cout << 20671;
    for (int i = 0; i < 20671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20672(int& val) {
    func_in_20672(val);
    const int x = 20672;
    std::cout << 20672;
    for (int i = 0; i < 20672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20672(int& val) {
    const int x = 20672;
    std::cout << 20672;
    for (int i = 0; i < 20672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20673(int& val) {
    func_in_20673(val);
    const int x = 20673;
    std::cout << 20673;
    for (int i = 0; i < 20673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20673(int& val) {
    const int x = 20673;
    std::cout << 20673;
    for (int i = 0; i < 20673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20674(int& val) {
    func_in_20674(val);
    const int x = 20674;
    std::cout << 20674;
    for (int i = 0; i < 20674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20674(int& val) {
    const int x = 20674;
    std::cout << 20674;
    for (int i = 0; i < 20674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20675(int& val) {
    func_in_20675(val);
    const int x = 20675;
    std::cout << 20675;
    for (int i = 0; i < 20675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20675(int& val) {
    const int x = 20675;
    std::cout << 20675;
    for (int i = 0; i < 20675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20676(int& val) {
    func_in_20676(val);
    const int x = 20676;
    std::cout << 20676;
    for (int i = 0; i < 20676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20676(int& val) {
    const int x = 20676;
    std::cout << 20676;
    for (int i = 0; i < 20676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20677(int& val) {
    func_in_20677(val);
    const int x = 20677;
    std::cout << 20677;
    for (int i = 0; i < 20677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20677(int& val) {
    const int x = 20677;
    std::cout << 20677;
    for (int i = 0; i < 20677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20678(int& val) {
    func_in_20678(val);
    const int x = 20678;
    std::cout << 20678;
    for (int i = 0; i < 20678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20678(int& val) {
    const int x = 20678;
    std::cout << 20678;
    for (int i = 0; i < 20678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20679(int& val) {
    func_in_20679(val);
    const int x = 20679;
    std::cout << 20679;
    for (int i = 0; i < 20679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20679(int& val) {
    const int x = 20679;
    std::cout << 20679;
    for (int i = 0; i < 20679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20680(int& val) {
    func_in_20680(val);
    const int x = 20680;
    std::cout << 20680;
    for (int i = 0; i < 20680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20680(int& val) {
    const int x = 20680;
    std::cout << 20680;
    for (int i = 0; i < 20680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20681(int& val) {
    func_in_20681(val);
    const int x = 20681;
    std::cout << 20681;
    for (int i = 0; i < 20681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20681(int& val) {
    const int x = 20681;
    std::cout << 20681;
    for (int i = 0; i < 20681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20682(int& val) {
    func_in_20682(val);
    const int x = 20682;
    std::cout << 20682;
    for (int i = 0; i < 20682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20682(int& val) {
    const int x = 20682;
    std::cout << 20682;
    for (int i = 0; i < 20682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20683(int& val) {
    func_in_20683(val);
    const int x = 20683;
    std::cout << 20683;
    for (int i = 0; i < 20683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20683(int& val) {
    const int x = 20683;
    std::cout << 20683;
    for (int i = 0; i < 20683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20684(int& val) {
    func_in_20684(val);
    const int x = 20684;
    std::cout << 20684;
    for (int i = 0; i < 20684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20684(int& val) {
    const int x = 20684;
    std::cout << 20684;
    for (int i = 0; i < 20684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20685(int& val) {
    func_in_20685(val);
    const int x = 20685;
    std::cout << 20685;
    for (int i = 0; i < 20685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20685(int& val) {
    const int x = 20685;
    std::cout << 20685;
    for (int i = 0; i < 20685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20686(int& val) {
    func_in_20686(val);
    const int x = 20686;
    std::cout << 20686;
    for (int i = 0; i < 20686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20686(int& val) {
    const int x = 20686;
    std::cout << 20686;
    for (int i = 0; i < 20686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20687(int& val) {
    func_in_20687(val);
    const int x = 20687;
    std::cout << 20687;
    for (int i = 0; i < 20687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20687(int& val) {
    const int x = 20687;
    std::cout << 20687;
    for (int i = 0; i < 20687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20688(int& val) {
    func_in_20688(val);
    const int x = 20688;
    std::cout << 20688;
    for (int i = 0; i < 20688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20688(int& val) {
    const int x = 20688;
    std::cout << 20688;
    for (int i = 0; i < 20688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20689(int& val) {
    func_in_20689(val);
    const int x = 20689;
    std::cout << 20689;
    for (int i = 0; i < 20689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20689(int& val) {
    const int x = 20689;
    std::cout << 20689;
    for (int i = 0; i < 20689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20690(int& val) {
    func_in_20690(val);
    const int x = 20690;
    std::cout << 20690;
    for (int i = 0; i < 20690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20690(int& val) {
    const int x = 20690;
    std::cout << 20690;
    for (int i = 0; i < 20690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20691(int& val) {
    func_in_20691(val);
    const int x = 20691;
    std::cout << 20691;
    for (int i = 0; i < 20691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20691(int& val) {
    const int x = 20691;
    std::cout << 20691;
    for (int i = 0; i < 20691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20692(int& val) {
    func_in_20692(val);
    const int x = 20692;
    std::cout << 20692;
    for (int i = 0; i < 20692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20692(int& val) {
    const int x = 20692;
    std::cout << 20692;
    for (int i = 0; i < 20692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20693(int& val) {
    func_in_20693(val);
    const int x = 20693;
    std::cout << 20693;
    for (int i = 0; i < 20693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20693(int& val) {
    const int x = 20693;
    std::cout << 20693;
    for (int i = 0; i < 20693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20694(int& val) {
    func_in_20694(val);
    const int x = 20694;
    std::cout << 20694;
    for (int i = 0; i < 20694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20694(int& val) {
    const int x = 20694;
    std::cout << 20694;
    for (int i = 0; i < 20694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20695(int& val) {
    func_in_20695(val);
    const int x = 20695;
    std::cout << 20695;
    for (int i = 0; i < 20695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20695(int& val) {
    const int x = 20695;
    std::cout << 20695;
    for (int i = 0; i < 20695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20696(int& val) {
    func_in_20696(val);
    const int x = 20696;
    std::cout << 20696;
    for (int i = 0; i < 20696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20696(int& val) {
    const int x = 20696;
    std::cout << 20696;
    for (int i = 0; i < 20696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20697(int& val) {
    func_in_20697(val);
    const int x = 20697;
    std::cout << 20697;
    for (int i = 0; i < 20697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20697(int& val) {
    const int x = 20697;
    std::cout << 20697;
    for (int i = 0; i < 20697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20698(int& val) {
    func_in_20698(val);
    const int x = 20698;
    std::cout << 20698;
    for (int i = 0; i < 20698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20698(int& val) {
    const int x = 20698;
    std::cout << 20698;
    for (int i = 0; i < 20698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20699(int& val) {
    func_in_20699(val);
    const int x = 20699;
    std::cout << 20699;
    for (int i = 0; i < 20699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20699(int& val) {
    const int x = 20699;
    std::cout << 20699;
    for (int i = 0; i < 20699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20700(int& val) {
    func_in_20700(val);
    const int x = 20700;
    std::cout << 20700;
    for (int i = 0; i < 20700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20700(int& val) {
    const int x = 20700;
    std::cout << 20700;
    for (int i = 0; i < 20700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20701(int& val) {
    func_in_20701(val);
    const int x = 20701;
    std::cout << 20701;
    for (int i = 0; i < 20701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20701(int& val) {
    const int x = 20701;
    std::cout << 20701;
    for (int i = 0; i < 20701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20702(int& val) {
    func_in_20702(val);
    const int x = 20702;
    std::cout << 20702;
    for (int i = 0; i < 20702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20702(int& val) {
    const int x = 20702;
    std::cout << 20702;
    for (int i = 0; i < 20702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20703(int& val) {
    func_in_20703(val);
    const int x = 20703;
    std::cout << 20703;
    for (int i = 0; i < 20703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20703(int& val) {
    const int x = 20703;
    std::cout << 20703;
    for (int i = 0; i < 20703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20704(int& val) {
    func_in_20704(val);
    const int x = 20704;
    std::cout << 20704;
    for (int i = 0; i < 20704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20704(int& val) {
    const int x = 20704;
    std::cout << 20704;
    for (int i = 0; i < 20704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20705(int& val) {
    func_in_20705(val);
    const int x = 20705;
    std::cout << 20705;
    for (int i = 0; i < 20705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20705(int& val) {
    const int x = 20705;
    std::cout << 20705;
    for (int i = 0; i < 20705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20706(int& val) {
    func_in_20706(val);
    const int x = 20706;
    std::cout << 20706;
    for (int i = 0; i < 20706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20706(int& val) {
    const int x = 20706;
    std::cout << 20706;
    for (int i = 0; i < 20706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20707(int& val) {
    func_in_20707(val);
    const int x = 20707;
    std::cout << 20707;
    for (int i = 0; i < 20707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20707(int& val) {
    const int x = 20707;
    std::cout << 20707;
    for (int i = 0; i < 20707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20708(int& val) {
    func_in_20708(val);
    const int x = 20708;
    std::cout << 20708;
    for (int i = 0; i < 20708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20708(int& val) {
    const int x = 20708;
    std::cout << 20708;
    for (int i = 0; i < 20708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20709(int& val) {
    func_in_20709(val);
    const int x = 20709;
    std::cout << 20709;
    for (int i = 0; i < 20709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20709(int& val) {
    const int x = 20709;
    std::cout << 20709;
    for (int i = 0; i < 20709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20710(int& val) {
    func_in_20710(val);
    const int x = 20710;
    std::cout << 20710;
    for (int i = 0; i < 20710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20710(int& val) {
    const int x = 20710;
    std::cout << 20710;
    for (int i = 0; i < 20710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20711(int& val) {
    func_in_20711(val);
    const int x = 20711;
    std::cout << 20711;
    for (int i = 0; i < 20711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20711(int& val) {
    const int x = 20711;
    std::cout << 20711;
    for (int i = 0; i < 20711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20712(int& val) {
    func_in_20712(val);
    const int x = 20712;
    std::cout << 20712;
    for (int i = 0; i < 20712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20712(int& val) {
    const int x = 20712;
    std::cout << 20712;
    for (int i = 0; i < 20712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20713(int& val) {
    func_in_20713(val);
    const int x = 20713;
    std::cout << 20713;
    for (int i = 0; i < 20713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20713(int& val) {
    const int x = 20713;
    std::cout << 20713;
    for (int i = 0; i < 20713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20714(int& val) {
    func_in_20714(val);
    const int x = 20714;
    std::cout << 20714;
    for (int i = 0; i < 20714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20714(int& val) {
    const int x = 20714;
    std::cout << 20714;
    for (int i = 0; i < 20714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20715(int& val) {
    func_in_20715(val);
    const int x = 20715;
    std::cout << 20715;
    for (int i = 0; i < 20715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20715(int& val) {
    const int x = 20715;
    std::cout << 20715;
    for (int i = 0; i < 20715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20716(int& val) {
    func_in_20716(val);
    const int x = 20716;
    std::cout << 20716;
    for (int i = 0; i < 20716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20716(int& val) {
    const int x = 20716;
    std::cout << 20716;
    for (int i = 0; i < 20716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20717(int& val) {
    func_in_20717(val);
    const int x = 20717;
    std::cout << 20717;
    for (int i = 0; i < 20717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20717(int& val) {
    const int x = 20717;
    std::cout << 20717;
    for (int i = 0; i < 20717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20718(int& val) {
    func_in_20718(val);
    const int x = 20718;
    std::cout << 20718;
    for (int i = 0; i < 20718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20718(int& val) {
    const int x = 20718;
    std::cout << 20718;
    for (int i = 0; i < 20718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20719(int& val) {
    func_in_20719(val);
    const int x = 20719;
    std::cout << 20719;
    for (int i = 0; i < 20719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20719(int& val) {
    const int x = 20719;
    std::cout << 20719;
    for (int i = 0; i < 20719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20720(int& val) {
    func_in_20720(val);
    const int x = 20720;
    std::cout << 20720;
    for (int i = 0; i < 20720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20720(int& val) {
    const int x = 20720;
    std::cout << 20720;
    for (int i = 0; i < 20720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20721(int& val) {
    func_in_20721(val);
    const int x = 20721;
    std::cout << 20721;
    for (int i = 0; i < 20721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20721(int& val) {
    const int x = 20721;
    std::cout << 20721;
    for (int i = 0; i < 20721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20722(int& val) {
    func_in_20722(val);
    const int x = 20722;
    std::cout << 20722;
    for (int i = 0; i < 20722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20722(int& val) {
    const int x = 20722;
    std::cout << 20722;
    for (int i = 0; i < 20722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20723(int& val) {
    func_in_20723(val);
    const int x = 20723;
    std::cout << 20723;
    for (int i = 0; i < 20723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20723(int& val) {
    const int x = 20723;
    std::cout << 20723;
    for (int i = 0; i < 20723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20724(int& val) {
    func_in_20724(val);
    const int x = 20724;
    std::cout << 20724;
    for (int i = 0; i < 20724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20724(int& val) {
    const int x = 20724;
    std::cout << 20724;
    for (int i = 0; i < 20724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20725(int& val) {
    func_in_20725(val);
    const int x = 20725;
    std::cout << 20725;
    for (int i = 0; i < 20725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20725(int& val) {
    const int x = 20725;
    std::cout << 20725;
    for (int i = 0; i < 20725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20726(int& val) {
    func_in_20726(val);
    const int x = 20726;
    std::cout << 20726;
    for (int i = 0; i < 20726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20726(int& val) {
    const int x = 20726;
    std::cout << 20726;
    for (int i = 0; i < 20726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20727(int& val) {
    func_in_20727(val);
    const int x = 20727;
    std::cout << 20727;
    for (int i = 0; i < 20727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20727(int& val) {
    const int x = 20727;
    std::cout << 20727;
    for (int i = 0; i < 20727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20728(int& val) {
    func_in_20728(val);
    const int x = 20728;
    std::cout << 20728;
    for (int i = 0; i < 20728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20728(int& val) {
    const int x = 20728;
    std::cout << 20728;
    for (int i = 0; i < 20728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20729(int& val) {
    func_in_20729(val);
    const int x = 20729;
    std::cout << 20729;
    for (int i = 0; i < 20729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20729(int& val) {
    const int x = 20729;
    std::cout << 20729;
    for (int i = 0; i < 20729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20730(int& val) {
    func_in_20730(val);
    const int x = 20730;
    std::cout << 20730;
    for (int i = 0; i < 20730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20730(int& val) {
    const int x = 20730;
    std::cout << 20730;
    for (int i = 0; i < 20730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20731(int& val) {
    func_in_20731(val);
    const int x = 20731;
    std::cout << 20731;
    for (int i = 0; i < 20731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20731(int& val) {
    const int x = 20731;
    std::cout << 20731;
    for (int i = 0; i < 20731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20732(int& val) {
    func_in_20732(val);
    const int x = 20732;
    std::cout << 20732;
    for (int i = 0; i < 20732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20732(int& val) {
    const int x = 20732;
    std::cout << 20732;
    for (int i = 0; i < 20732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20733(int& val) {
    func_in_20733(val);
    const int x = 20733;
    std::cout << 20733;
    for (int i = 0; i < 20733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20733(int& val) {
    const int x = 20733;
    std::cout << 20733;
    for (int i = 0; i < 20733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20734(int& val) {
    func_in_20734(val);
    const int x = 20734;
    std::cout << 20734;
    for (int i = 0; i < 20734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20734(int& val) {
    const int x = 20734;
    std::cout << 20734;
    for (int i = 0; i < 20734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20735(int& val) {
    func_in_20735(val);
    const int x = 20735;
    std::cout << 20735;
    for (int i = 0; i < 20735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20735(int& val) {
    const int x = 20735;
    std::cout << 20735;
    for (int i = 0; i < 20735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20736(int& val) {
    func_in_20736(val);
    const int x = 20736;
    std::cout << 20736;
    for (int i = 0; i < 20736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20736(int& val) {
    const int x = 20736;
    std::cout << 20736;
    for (int i = 0; i < 20736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20737(int& val) {
    func_in_20737(val);
    const int x = 20737;
    std::cout << 20737;
    for (int i = 0; i < 20737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20737(int& val) {
    const int x = 20737;
    std::cout << 20737;
    for (int i = 0; i < 20737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20738(int& val) {
    func_in_20738(val);
    const int x = 20738;
    std::cout << 20738;
    for (int i = 0; i < 20738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20738(int& val) {
    const int x = 20738;
    std::cout << 20738;
    for (int i = 0; i < 20738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20739(int& val) {
    func_in_20739(val);
    const int x = 20739;
    std::cout << 20739;
    for (int i = 0; i < 20739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20739(int& val) {
    const int x = 20739;
    std::cout << 20739;
    for (int i = 0; i < 20739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20740(int& val) {
    func_in_20740(val);
    const int x = 20740;
    std::cout << 20740;
    for (int i = 0; i < 20740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20740(int& val) {
    const int x = 20740;
    std::cout << 20740;
    for (int i = 0; i < 20740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20741(int& val) {
    func_in_20741(val);
    const int x = 20741;
    std::cout << 20741;
    for (int i = 0; i < 20741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20741(int& val) {
    const int x = 20741;
    std::cout << 20741;
    for (int i = 0; i < 20741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20742(int& val) {
    func_in_20742(val);
    const int x = 20742;
    std::cout << 20742;
    for (int i = 0; i < 20742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20742(int& val) {
    const int x = 20742;
    std::cout << 20742;
    for (int i = 0; i < 20742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20743(int& val) {
    func_in_20743(val);
    const int x = 20743;
    std::cout << 20743;
    for (int i = 0; i < 20743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20743(int& val) {
    const int x = 20743;
    std::cout << 20743;
    for (int i = 0; i < 20743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20744(int& val) {
    func_in_20744(val);
    const int x = 20744;
    std::cout << 20744;
    for (int i = 0; i < 20744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20744(int& val) {
    const int x = 20744;
    std::cout << 20744;
    for (int i = 0; i < 20744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20745(int& val) {
    func_in_20745(val);
    const int x = 20745;
    std::cout << 20745;
    for (int i = 0; i < 20745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20745(int& val) {
    const int x = 20745;
    std::cout << 20745;
    for (int i = 0; i < 20745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20746(int& val) {
    func_in_20746(val);
    const int x = 20746;
    std::cout << 20746;
    for (int i = 0; i < 20746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20746(int& val) {
    const int x = 20746;
    std::cout << 20746;
    for (int i = 0; i < 20746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20747(int& val) {
    func_in_20747(val);
    const int x = 20747;
    std::cout << 20747;
    for (int i = 0; i < 20747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20747(int& val) {
    const int x = 20747;
    std::cout << 20747;
    for (int i = 0; i < 20747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20748(int& val) {
    func_in_20748(val);
    const int x = 20748;
    std::cout << 20748;
    for (int i = 0; i < 20748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20748(int& val) {
    const int x = 20748;
    std::cout << 20748;
    for (int i = 0; i < 20748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20749(int& val) {
    func_in_20749(val);
    const int x = 20749;
    std::cout << 20749;
    for (int i = 0; i < 20749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20749(int& val) {
    const int x = 20749;
    std::cout << 20749;
    for (int i = 0; i < 20749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20750(int& val) {
    func_in_20750(val);
    const int x = 20750;
    std::cout << 20750;
    for (int i = 0; i < 20750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20750(int& val) {
    const int x = 20750;
    std::cout << 20750;
    for (int i = 0; i < 20750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20751(int& val) {
    func_in_20751(val);
    const int x = 20751;
    std::cout << 20751;
    for (int i = 0; i < 20751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20751(int& val) {
    const int x = 20751;
    std::cout << 20751;
    for (int i = 0; i < 20751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20752(int& val) {
    func_in_20752(val);
    const int x = 20752;
    std::cout << 20752;
    for (int i = 0; i < 20752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20752(int& val) {
    const int x = 20752;
    std::cout << 20752;
    for (int i = 0; i < 20752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20753(int& val) {
    func_in_20753(val);
    const int x = 20753;
    std::cout << 20753;
    for (int i = 0; i < 20753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20753(int& val) {
    const int x = 20753;
    std::cout << 20753;
    for (int i = 0; i < 20753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20754(int& val) {
    func_in_20754(val);
    const int x = 20754;
    std::cout << 20754;
    for (int i = 0; i < 20754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20754(int& val) {
    const int x = 20754;
    std::cout << 20754;
    for (int i = 0; i < 20754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20755(int& val) {
    func_in_20755(val);
    const int x = 20755;
    std::cout << 20755;
    for (int i = 0; i < 20755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20755(int& val) {
    const int x = 20755;
    std::cout << 20755;
    for (int i = 0; i < 20755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20756(int& val) {
    func_in_20756(val);
    const int x = 20756;
    std::cout << 20756;
    for (int i = 0; i < 20756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20756(int& val) {
    const int x = 20756;
    std::cout << 20756;
    for (int i = 0; i < 20756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20757(int& val) {
    func_in_20757(val);
    const int x = 20757;
    std::cout << 20757;
    for (int i = 0; i < 20757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20757(int& val) {
    const int x = 20757;
    std::cout << 20757;
    for (int i = 0; i < 20757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20758(int& val) {
    func_in_20758(val);
    const int x = 20758;
    std::cout << 20758;
    for (int i = 0; i < 20758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20758(int& val) {
    const int x = 20758;
    std::cout << 20758;
    for (int i = 0; i < 20758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20759(int& val) {
    func_in_20759(val);
    const int x = 20759;
    std::cout << 20759;
    for (int i = 0; i < 20759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20759(int& val) {
    const int x = 20759;
    std::cout << 20759;
    for (int i = 0; i < 20759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20760(int& val) {
    func_in_20760(val);
    const int x = 20760;
    std::cout << 20760;
    for (int i = 0; i < 20760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20760(int& val) {
    const int x = 20760;
    std::cout << 20760;
    for (int i = 0; i < 20760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20761(int& val) {
    func_in_20761(val);
    const int x = 20761;
    std::cout << 20761;
    for (int i = 0; i < 20761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20761(int& val) {
    const int x = 20761;
    std::cout << 20761;
    for (int i = 0; i < 20761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20762(int& val) {
    func_in_20762(val);
    const int x = 20762;
    std::cout << 20762;
    for (int i = 0; i < 20762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20762(int& val) {
    const int x = 20762;
    std::cout << 20762;
    for (int i = 0; i < 20762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20763(int& val) {
    func_in_20763(val);
    const int x = 20763;
    std::cout << 20763;
    for (int i = 0; i < 20763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20763(int& val) {
    const int x = 20763;
    std::cout << 20763;
    for (int i = 0; i < 20763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20764(int& val) {
    func_in_20764(val);
    const int x = 20764;
    std::cout << 20764;
    for (int i = 0; i < 20764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20764(int& val) {
    const int x = 20764;
    std::cout << 20764;
    for (int i = 0; i < 20764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20765(int& val) {
    func_in_20765(val);
    const int x = 20765;
    std::cout << 20765;
    for (int i = 0; i < 20765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20765(int& val) {
    const int x = 20765;
    std::cout << 20765;
    for (int i = 0; i < 20765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20766(int& val) {
    func_in_20766(val);
    const int x = 20766;
    std::cout << 20766;
    for (int i = 0; i < 20766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20766(int& val) {
    const int x = 20766;
    std::cout << 20766;
    for (int i = 0; i < 20766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20767(int& val) {
    func_in_20767(val);
    const int x = 20767;
    std::cout << 20767;
    for (int i = 0; i < 20767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20767(int& val) {
    const int x = 20767;
    std::cout << 20767;
    for (int i = 0; i < 20767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20768(int& val) {
    func_in_20768(val);
    const int x = 20768;
    std::cout << 20768;
    for (int i = 0; i < 20768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20768(int& val) {
    const int x = 20768;
    std::cout << 20768;
    for (int i = 0; i < 20768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20769(int& val) {
    func_in_20769(val);
    const int x = 20769;
    std::cout << 20769;
    for (int i = 0; i < 20769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20769(int& val) {
    const int x = 20769;
    std::cout << 20769;
    for (int i = 0; i < 20769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20770(int& val) {
    func_in_20770(val);
    const int x = 20770;
    std::cout << 20770;
    for (int i = 0; i < 20770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20770(int& val) {
    const int x = 20770;
    std::cout << 20770;
    for (int i = 0; i < 20770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20771(int& val) {
    func_in_20771(val);
    const int x = 20771;
    std::cout << 20771;
    for (int i = 0; i < 20771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20771(int& val) {
    const int x = 20771;
    std::cout << 20771;
    for (int i = 0; i < 20771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20772(int& val) {
    func_in_20772(val);
    const int x = 20772;
    std::cout << 20772;
    for (int i = 0; i < 20772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20772(int& val) {
    const int x = 20772;
    std::cout << 20772;
    for (int i = 0; i < 20772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20773(int& val) {
    func_in_20773(val);
    const int x = 20773;
    std::cout << 20773;
    for (int i = 0; i < 20773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20773(int& val) {
    const int x = 20773;
    std::cout << 20773;
    for (int i = 0; i < 20773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20774(int& val) {
    func_in_20774(val);
    const int x = 20774;
    std::cout << 20774;
    for (int i = 0; i < 20774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20774(int& val) {
    const int x = 20774;
    std::cout << 20774;
    for (int i = 0; i < 20774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20775(int& val) {
    func_in_20775(val);
    const int x = 20775;
    std::cout << 20775;
    for (int i = 0; i < 20775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20775(int& val) {
    const int x = 20775;
    std::cout << 20775;
    for (int i = 0; i < 20775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20776(int& val) {
    func_in_20776(val);
    const int x = 20776;
    std::cout << 20776;
    for (int i = 0; i < 20776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20776(int& val) {
    const int x = 20776;
    std::cout << 20776;
    for (int i = 0; i < 20776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20777(int& val) {
    func_in_20777(val);
    const int x = 20777;
    std::cout << 20777;
    for (int i = 0; i < 20777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20777(int& val) {
    const int x = 20777;
    std::cout << 20777;
    for (int i = 0; i < 20777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20778(int& val) {
    func_in_20778(val);
    const int x = 20778;
    std::cout << 20778;
    for (int i = 0; i < 20778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20778(int& val) {
    const int x = 20778;
    std::cout << 20778;
    for (int i = 0; i < 20778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20779(int& val) {
    func_in_20779(val);
    const int x = 20779;
    std::cout << 20779;
    for (int i = 0; i < 20779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20779(int& val) {
    const int x = 20779;
    std::cout << 20779;
    for (int i = 0; i < 20779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20780(int& val) {
    func_in_20780(val);
    const int x = 20780;
    std::cout << 20780;
    for (int i = 0; i < 20780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20780(int& val) {
    const int x = 20780;
    std::cout << 20780;
    for (int i = 0; i < 20780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20781(int& val) {
    func_in_20781(val);
    const int x = 20781;
    std::cout << 20781;
    for (int i = 0; i < 20781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20781(int& val) {
    const int x = 20781;
    std::cout << 20781;
    for (int i = 0; i < 20781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20782(int& val) {
    func_in_20782(val);
    const int x = 20782;
    std::cout << 20782;
    for (int i = 0; i < 20782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20782(int& val) {
    const int x = 20782;
    std::cout << 20782;
    for (int i = 0; i < 20782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20783(int& val) {
    func_in_20783(val);
    const int x = 20783;
    std::cout << 20783;
    for (int i = 0; i < 20783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20783(int& val) {
    const int x = 20783;
    std::cout << 20783;
    for (int i = 0; i < 20783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20784(int& val) {
    func_in_20784(val);
    const int x = 20784;
    std::cout << 20784;
    for (int i = 0; i < 20784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20784(int& val) {
    const int x = 20784;
    std::cout << 20784;
    for (int i = 0; i < 20784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20785(int& val) {
    func_in_20785(val);
    const int x = 20785;
    std::cout << 20785;
    for (int i = 0; i < 20785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20785(int& val) {
    const int x = 20785;
    std::cout << 20785;
    for (int i = 0; i < 20785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20786(int& val) {
    func_in_20786(val);
    const int x = 20786;
    std::cout << 20786;
    for (int i = 0; i < 20786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20786(int& val) {
    const int x = 20786;
    std::cout << 20786;
    for (int i = 0; i < 20786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20787(int& val) {
    func_in_20787(val);
    const int x = 20787;
    std::cout << 20787;
    for (int i = 0; i < 20787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20787(int& val) {
    const int x = 20787;
    std::cout << 20787;
    for (int i = 0; i < 20787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20788(int& val) {
    func_in_20788(val);
    const int x = 20788;
    std::cout << 20788;
    for (int i = 0; i < 20788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20788(int& val) {
    const int x = 20788;
    std::cout << 20788;
    for (int i = 0; i < 20788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20789(int& val) {
    func_in_20789(val);
    const int x = 20789;
    std::cout << 20789;
    for (int i = 0; i < 20789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20789(int& val) {
    const int x = 20789;
    std::cout << 20789;
    for (int i = 0; i < 20789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20790(int& val) {
    func_in_20790(val);
    const int x = 20790;
    std::cout << 20790;
    for (int i = 0; i < 20790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20790(int& val) {
    const int x = 20790;
    std::cout << 20790;
    for (int i = 0; i < 20790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20791(int& val) {
    func_in_20791(val);
    const int x = 20791;
    std::cout << 20791;
    for (int i = 0; i < 20791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20791(int& val) {
    const int x = 20791;
    std::cout << 20791;
    for (int i = 0; i < 20791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20792(int& val) {
    func_in_20792(val);
    const int x = 20792;
    std::cout << 20792;
    for (int i = 0; i < 20792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20792(int& val) {
    const int x = 20792;
    std::cout << 20792;
    for (int i = 0; i < 20792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20793(int& val) {
    func_in_20793(val);
    const int x = 20793;
    std::cout << 20793;
    for (int i = 0; i < 20793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20793(int& val) {
    const int x = 20793;
    std::cout << 20793;
    for (int i = 0; i < 20793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20794(int& val) {
    func_in_20794(val);
    const int x = 20794;
    std::cout << 20794;
    for (int i = 0; i < 20794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20794(int& val) {
    const int x = 20794;
    std::cout << 20794;
    for (int i = 0; i < 20794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20795(int& val) {
    func_in_20795(val);
    const int x = 20795;
    std::cout << 20795;
    for (int i = 0; i < 20795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20795(int& val) {
    const int x = 20795;
    std::cout << 20795;
    for (int i = 0; i < 20795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20796(int& val) {
    func_in_20796(val);
    const int x = 20796;
    std::cout << 20796;
    for (int i = 0; i < 20796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20796(int& val) {
    const int x = 20796;
    std::cout << 20796;
    for (int i = 0; i < 20796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20797(int& val) {
    func_in_20797(val);
    const int x = 20797;
    std::cout << 20797;
    for (int i = 0; i < 20797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20797(int& val) {
    const int x = 20797;
    std::cout << 20797;
    for (int i = 0; i < 20797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20798(int& val) {
    func_in_20798(val);
    const int x = 20798;
    std::cout << 20798;
    for (int i = 0; i < 20798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20798(int& val) {
    const int x = 20798;
    std::cout << 20798;
    for (int i = 0; i < 20798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20799(int& val) {
    func_in_20799(val);
    const int x = 20799;
    std::cout << 20799;
    for (int i = 0; i < 20799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20799(int& val) {
    const int x = 20799;
    std::cout << 20799;
    for (int i = 0; i < 20799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20800(int& val) {
    func_in_20800(val);
    const int x = 20800;
    std::cout << 20800;
    for (int i = 0; i < 20800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20800(int& val) {
    const int x = 20800;
    std::cout << 20800;
    for (int i = 0; i < 20800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20801(int& val) {
    func_in_20801(val);
    const int x = 20801;
    std::cout << 20801;
    for (int i = 0; i < 20801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20801(int& val) {
    const int x = 20801;
    std::cout << 20801;
    for (int i = 0; i < 20801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20802(int& val) {
    func_in_20802(val);
    const int x = 20802;
    std::cout << 20802;
    for (int i = 0; i < 20802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20802(int& val) {
    const int x = 20802;
    std::cout << 20802;
    for (int i = 0; i < 20802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20803(int& val) {
    func_in_20803(val);
    const int x = 20803;
    std::cout << 20803;
    for (int i = 0; i < 20803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20803(int& val) {
    const int x = 20803;
    std::cout << 20803;
    for (int i = 0; i < 20803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20804(int& val) {
    func_in_20804(val);
    const int x = 20804;
    std::cout << 20804;
    for (int i = 0; i < 20804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20804(int& val) {
    const int x = 20804;
    std::cout << 20804;
    for (int i = 0; i < 20804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20805(int& val) {
    func_in_20805(val);
    const int x = 20805;
    std::cout << 20805;
    for (int i = 0; i < 20805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20805(int& val) {
    const int x = 20805;
    std::cout << 20805;
    for (int i = 0; i < 20805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20806(int& val) {
    func_in_20806(val);
    const int x = 20806;
    std::cout << 20806;
    for (int i = 0; i < 20806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20806(int& val) {
    const int x = 20806;
    std::cout << 20806;
    for (int i = 0; i < 20806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20807(int& val) {
    func_in_20807(val);
    const int x = 20807;
    std::cout << 20807;
    for (int i = 0; i < 20807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20807(int& val) {
    const int x = 20807;
    std::cout << 20807;
    for (int i = 0; i < 20807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20808(int& val) {
    func_in_20808(val);
    const int x = 20808;
    std::cout << 20808;
    for (int i = 0; i < 20808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20808(int& val) {
    const int x = 20808;
    std::cout << 20808;
    for (int i = 0; i < 20808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20809(int& val) {
    func_in_20809(val);
    const int x = 20809;
    std::cout << 20809;
    for (int i = 0; i < 20809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20809(int& val) {
    const int x = 20809;
    std::cout << 20809;
    for (int i = 0; i < 20809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20810(int& val) {
    func_in_20810(val);
    const int x = 20810;
    std::cout << 20810;
    for (int i = 0; i < 20810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20810(int& val) {
    const int x = 20810;
    std::cout << 20810;
    for (int i = 0; i < 20810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20811(int& val) {
    func_in_20811(val);
    const int x = 20811;
    std::cout << 20811;
    for (int i = 0; i < 20811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20811(int& val) {
    const int x = 20811;
    std::cout << 20811;
    for (int i = 0; i < 20811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20812(int& val) {
    func_in_20812(val);
    const int x = 20812;
    std::cout << 20812;
    for (int i = 0; i < 20812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20812(int& val) {
    const int x = 20812;
    std::cout << 20812;
    for (int i = 0; i < 20812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20813(int& val) {
    func_in_20813(val);
    const int x = 20813;
    std::cout << 20813;
    for (int i = 0; i < 20813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20813(int& val) {
    const int x = 20813;
    std::cout << 20813;
    for (int i = 0; i < 20813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20814(int& val) {
    func_in_20814(val);
    const int x = 20814;
    std::cout << 20814;
    for (int i = 0; i < 20814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20814(int& val) {
    const int x = 20814;
    std::cout << 20814;
    for (int i = 0; i < 20814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20815(int& val) {
    func_in_20815(val);
    const int x = 20815;
    std::cout << 20815;
    for (int i = 0; i < 20815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20815(int& val) {
    const int x = 20815;
    std::cout << 20815;
    for (int i = 0; i < 20815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20816(int& val) {
    func_in_20816(val);
    const int x = 20816;
    std::cout << 20816;
    for (int i = 0; i < 20816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20816(int& val) {
    const int x = 20816;
    std::cout << 20816;
    for (int i = 0; i < 20816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20817(int& val) {
    func_in_20817(val);
    const int x = 20817;
    std::cout << 20817;
    for (int i = 0; i < 20817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20817(int& val) {
    const int x = 20817;
    std::cout << 20817;
    for (int i = 0; i < 20817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20818(int& val) {
    func_in_20818(val);
    const int x = 20818;
    std::cout << 20818;
    for (int i = 0; i < 20818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20818(int& val) {
    const int x = 20818;
    std::cout << 20818;
    for (int i = 0; i < 20818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20819(int& val) {
    func_in_20819(val);
    const int x = 20819;
    std::cout << 20819;
    for (int i = 0; i < 20819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20819(int& val) {
    const int x = 20819;
    std::cout << 20819;
    for (int i = 0; i < 20819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20820(int& val) {
    func_in_20820(val);
    const int x = 20820;
    std::cout << 20820;
    for (int i = 0; i < 20820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20820(int& val) {
    const int x = 20820;
    std::cout << 20820;
    for (int i = 0; i < 20820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20821(int& val) {
    func_in_20821(val);
    const int x = 20821;
    std::cout << 20821;
    for (int i = 0; i < 20821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20821(int& val) {
    const int x = 20821;
    std::cout << 20821;
    for (int i = 0; i < 20821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20822(int& val) {
    func_in_20822(val);
    const int x = 20822;
    std::cout << 20822;
    for (int i = 0; i < 20822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20822(int& val) {
    const int x = 20822;
    std::cout << 20822;
    for (int i = 0; i < 20822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20823(int& val) {
    func_in_20823(val);
    const int x = 20823;
    std::cout << 20823;
    for (int i = 0; i < 20823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20823(int& val) {
    const int x = 20823;
    std::cout << 20823;
    for (int i = 0; i < 20823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20824(int& val) {
    func_in_20824(val);
    const int x = 20824;
    std::cout << 20824;
    for (int i = 0; i < 20824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20824(int& val) {
    const int x = 20824;
    std::cout << 20824;
    for (int i = 0; i < 20824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20825(int& val) {
    func_in_20825(val);
    const int x = 20825;
    std::cout << 20825;
    for (int i = 0; i < 20825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20825(int& val) {
    const int x = 20825;
    std::cout << 20825;
    for (int i = 0; i < 20825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20826(int& val) {
    func_in_20826(val);
    const int x = 20826;
    std::cout << 20826;
    for (int i = 0; i < 20826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20826(int& val) {
    const int x = 20826;
    std::cout << 20826;
    for (int i = 0; i < 20826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20827(int& val) {
    func_in_20827(val);
    const int x = 20827;
    std::cout << 20827;
    for (int i = 0; i < 20827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20827(int& val) {
    const int x = 20827;
    std::cout << 20827;
    for (int i = 0; i < 20827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20828(int& val) {
    func_in_20828(val);
    const int x = 20828;
    std::cout << 20828;
    for (int i = 0; i < 20828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20828(int& val) {
    const int x = 20828;
    std::cout << 20828;
    for (int i = 0; i < 20828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20829(int& val) {
    func_in_20829(val);
    const int x = 20829;
    std::cout << 20829;
    for (int i = 0; i < 20829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20829(int& val) {
    const int x = 20829;
    std::cout << 20829;
    for (int i = 0; i < 20829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20830(int& val) {
    func_in_20830(val);
    const int x = 20830;
    std::cout << 20830;
    for (int i = 0; i < 20830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20830(int& val) {
    const int x = 20830;
    std::cout << 20830;
    for (int i = 0; i < 20830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20831(int& val) {
    func_in_20831(val);
    const int x = 20831;
    std::cout << 20831;
    for (int i = 0; i < 20831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20831(int& val) {
    const int x = 20831;
    std::cout << 20831;
    for (int i = 0; i < 20831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20832(int& val) {
    func_in_20832(val);
    const int x = 20832;
    std::cout << 20832;
    for (int i = 0; i < 20832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20832(int& val) {
    const int x = 20832;
    std::cout << 20832;
    for (int i = 0; i < 20832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20833(int& val) {
    func_in_20833(val);
    const int x = 20833;
    std::cout << 20833;
    for (int i = 0; i < 20833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20833(int& val) {
    const int x = 20833;
    std::cout << 20833;
    for (int i = 0; i < 20833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20834(int& val) {
    func_in_20834(val);
    const int x = 20834;
    std::cout << 20834;
    for (int i = 0; i < 20834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20834(int& val) {
    const int x = 20834;
    std::cout << 20834;
    for (int i = 0; i < 20834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20835(int& val) {
    func_in_20835(val);
    const int x = 20835;
    std::cout << 20835;
    for (int i = 0; i < 20835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20835(int& val) {
    const int x = 20835;
    std::cout << 20835;
    for (int i = 0; i < 20835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20836(int& val) {
    func_in_20836(val);
    const int x = 20836;
    std::cout << 20836;
    for (int i = 0; i < 20836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20836(int& val) {
    const int x = 20836;
    std::cout << 20836;
    for (int i = 0; i < 20836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20837(int& val) {
    func_in_20837(val);
    const int x = 20837;
    std::cout << 20837;
    for (int i = 0; i < 20837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20837(int& val) {
    const int x = 20837;
    std::cout << 20837;
    for (int i = 0; i < 20837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20838(int& val) {
    func_in_20838(val);
    const int x = 20838;
    std::cout << 20838;
    for (int i = 0; i < 20838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20838(int& val) {
    const int x = 20838;
    std::cout << 20838;
    for (int i = 0; i < 20838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20839(int& val) {
    func_in_20839(val);
    const int x = 20839;
    std::cout << 20839;
    for (int i = 0; i < 20839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20839(int& val) {
    const int x = 20839;
    std::cout << 20839;
    for (int i = 0; i < 20839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20840(int& val) {
    func_in_20840(val);
    const int x = 20840;
    std::cout << 20840;
    for (int i = 0; i < 20840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20840(int& val) {
    const int x = 20840;
    std::cout << 20840;
    for (int i = 0; i < 20840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20841(int& val) {
    func_in_20841(val);
    const int x = 20841;
    std::cout << 20841;
    for (int i = 0; i < 20841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20841(int& val) {
    const int x = 20841;
    std::cout << 20841;
    for (int i = 0; i < 20841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20842(int& val) {
    func_in_20842(val);
    const int x = 20842;
    std::cout << 20842;
    for (int i = 0; i < 20842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20842(int& val) {
    const int x = 20842;
    std::cout << 20842;
    for (int i = 0; i < 20842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20843(int& val) {
    func_in_20843(val);
    const int x = 20843;
    std::cout << 20843;
    for (int i = 0; i < 20843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20843(int& val) {
    const int x = 20843;
    std::cout << 20843;
    for (int i = 0; i < 20843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20844(int& val) {
    func_in_20844(val);
    const int x = 20844;
    std::cout << 20844;
    for (int i = 0; i < 20844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20844(int& val) {
    const int x = 20844;
    std::cout << 20844;
    for (int i = 0; i < 20844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20845(int& val) {
    func_in_20845(val);
    const int x = 20845;
    std::cout << 20845;
    for (int i = 0; i < 20845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20845(int& val) {
    const int x = 20845;
    std::cout << 20845;
    for (int i = 0; i < 20845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20846(int& val) {
    func_in_20846(val);
    const int x = 20846;
    std::cout << 20846;
    for (int i = 0; i < 20846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20846(int& val) {
    const int x = 20846;
    std::cout << 20846;
    for (int i = 0; i < 20846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20847(int& val) {
    func_in_20847(val);
    const int x = 20847;
    std::cout << 20847;
    for (int i = 0; i < 20847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20847(int& val) {
    const int x = 20847;
    std::cout << 20847;
    for (int i = 0; i < 20847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20848(int& val) {
    func_in_20848(val);
    const int x = 20848;
    std::cout << 20848;
    for (int i = 0; i < 20848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20848(int& val) {
    const int x = 20848;
    std::cout << 20848;
    for (int i = 0; i < 20848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20849(int& val) {
    func_in_20849(val);
    const int x = 20849;
    std::cout << 20849;
    for (int i = 0; i < 20849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20849(int& val) {
    const int x = 20849;
    std::cout << 20849;
    for (int i = 0; i < 20849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20850(int& val) {
    func_in_20850(val);
    const int x = 20850;
    std::cout << 20850;
    for (int i = 0; i < 20850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20850(int& val) {
    const int x = 20850;
    std::cout << 20850;
    for (int i = 0; i < 20850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20851(int& val) {
    func_in_20851(val);
    const int x = 20851;
    std::cout << 20851;
    for (int i = 0; i < 20851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20851(int& val) {
    const int x = 20851;
    std::cout << 20851;
    for (int i = 0; i < 20851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20852(int& val) {
    func_in_20852(val);
    const int x = 20852;
    std::cout << 20852;
    for (int i = 0; i < 20852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20852(int& val) {
    const int x = 20852;
    std::cout << 20852;
    for (int i = 0; i < 20852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20853(int& val) {
    func_in_20853(val);
    const int x = 20853;
    std::cout << 20853;
    for (int i = 0; i < 20853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20853(int& val) {
    const int x = 20853;
    std::cout << 20853;
    for (int i = 0; i < 20853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20854(int& val) {
    func_in_20854(val);
    const int x = 20854;
    std::cout << 20854;
    for (int i = 0; i < 20854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20854(int& val) {
    const int x = 20854;
    std::cout << 20854;
    for (int i = 0; i < 20854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20855(int& val) {
    func_in_20855(val);
    const int x = 20855;
    std::cout << 20855;
    for (int i = 0; i < 20855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20855(int& val) {
    const int x = 20855;
    std::cout << 20855;
    for (int i = 0; i < 20855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20856(int& val) {
    func_in_20856(val);
    const int x = 20856;
    std::cout << 20856;
    for (int i = 0; i < 20856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20856(int& val) {
    const int x = 20856;
    std::cout << 20856;
    for (int i = 0; i < 20856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20857(int& val) {
    func_in_20857(val);
    const int x = 20857;
    std::cout << 20857;
    for (int i = 0; i < 20857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20857(int& val) {
    const int x = 20857;
    std::cout << 20857;
    for (int i = 0; i < 20857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20858(int& val) {
    func_in_20858(val);
    const int x = 20858;
    std::cout << 20858;
    for (int i = 0; i < 20858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20858(int& val) {
    const int x = 20858;
    std::cout << 20858;
    for (int i = 0; i < 20858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20859(int& val) {
    func_in_20859(val);
    const int x = 20859;
    std::cout << 20859;
    for (int i = 0; i < 20859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20859(int& val) {
    const int x = 20859;
    std::cout << 20859;
    for (int i = 0; i < 20859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20860(int& val) {
    func_in_20860(val);
    const int x = 20860;
    std::cout << 20860;
    for (int i = 0; i < 20860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20860(int& val) {
    const int x = 20860;
    std::cout << 20860;
    for (int i = 0; i < 20860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20861(int& val) {
    func_in_20861(val);
    const int x = 20861;
    std::cout << 20861;
    for (int i = 0; i < 20861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20861(int& val) {
    const int x = 20861;
    std::cout << 20861;
    for (int i = 0; i < 20861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20862(int& val) {
    func_in_20862(val);
    const int x = 20862;
    std::cout << 20862;
    for (int i = 0; i < 20862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20862(int& val) {
    const int x = 20862;
    std::cout << 20862;
    for (int i = 0; i < 20862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20863(int& val) {
    func_in_20863(val);
    const int x = 20863;
    std::cout << 20863;
    for (int i = 0; i < 20863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20863(int& val) {
    const int x = 20863;
    std::cout << 20863;
    for (int i = 0; i < 20863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20864(int& val) {
    func_in_20864(val);
    const int x = 20864;
    std::cout << 20864;
    for (int i = 0; i < 20864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20864(int& val) {
    const int x = 20864;
    std::cout << 20864;
    for (int i = 0; i < 20864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20865(int& val) {
    func_in_20865(val);
    const int x = 20865;
    std::cout << 20865;
    for (int i = 0; i < 20865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20865(int& val) {
    const int x = 20865;
    std::cout << 20865;
    for (int i = 0; i < 20865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20866(int& val) {
    func_in_20866(val);
    const int x = 20866;
    std::cout << 20866;
    for (int i = 0; i < 20866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20866(int& val) {
    const int x = 20866;
    std::cout << 20866;
    for (int i = 0; i < 20866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20867(int& val) {
    func_in_20867(val);
    const int x = 20867;
    std::cout << 20867;
    for (int i = 0; i < 20867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20867(int& val) {
    const int x = 20867;
    std::cout << 20867;
    for (int i = 0; i < 20867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20868(int& val) {
    func_in_20868(val);
    const int x = 20868;
    std::cout << 20868;
    for (int i = 0; i < 20868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20868(int& val) {
    const int x = 20868;
    std::cout << 20868;
    for (int i = 0; i < 20868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20869(int& val) {
    func_in_20869(val);
    const int x = 20869;
    std::cout << 20869;
    for (int i = 0; i < 20869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20869(int& val) {
    const int x = 20869;
    std::cout << 20869;
    for (int i = 0; i < 20869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20870(int& val) {
    func_in_20870(val);
    const int x = 20870;
    std::cout << 20870;
    for (int i = 0; i < 20870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20870(int& val) {
    const int x = 20870;
    std::cout << 20870;
    for (int i = 0; i < 20870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20871(int& val) {
    func_in_20871(val);
    const int x = 20871;
    std::cout << 20871;
    for (int i = 0; i < 20871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20871(int& val) {
    const int x = 20871;
    std::cout << 20871;
    for (int i = 0; i < 20871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20872(int& val) {
    func_in_20872(val);
    const int x = 20872;
    std::cout << 20872;
    for (int i = 0; i < 20872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20872(int& val) {
    const int x = 20872;
    std::cout << 20872;
    for (int i = 0; i < 20872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20873(int& val) {
    func_in_20873(val);
    const int x = 20873;
    std::cout << 20873;
    for (int i = 0; i < 20873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20873(int& val) {
    const int x = 20873;
    std::cout << 20873;
    for (int i = 0; i < 20873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20874(int& val) {
    func_in_20874(val);
    const int x = 20874;
    std::cout << 20874;
    for (int i = 0; i < 20874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20874(int& val) {
    const int x = 20874;
    std::cout << 20874;
    for (int i = 0; i < 20874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20875(int& val) {
    func_in_20875(val);
    const int x = 20875;
    std::cout << 20875;
    for (int i = 0; i < 20875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20875(int& val) {
    const int x = 20875;
    std::cout << 20875;
    for (int i = 0; i < 20875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20876(int& val) {
    func_in_20876(val);
    const int x = 20876;
    std::cout << 20876;
    for (int i = 0; i < 20876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20876(int& val) {
    const int x = 20876;
    std::cout << 20876;
    for (int i = 0; i < 20876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20877(int& val) {
    func_in_20877(val);
    const int x = 20877;
    std::cout << 20877;
    for (int i = 0; i < 20877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20877(int& val) {
    const int x = 20877;
    std::cout << 20877;
    for (int i = 0; i < 20877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20878(int& val) {
    func_in_20878(val);
    const int x = 20878;
    std::cout << 20878;
    for (int i = 0; i < 20878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20878(int& val) {
    const int x = 20878;
    std::cout << 20878;
    for (int i = 0; i < 20878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20879(int& val) {
    func_in_20879(val);
    const int x = 20879;
    std::cout << 20879;
    for (int i = 0; i < 20879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20879(int& val) {
    const int x = 20879;
    std::cout << 20879;
    for (int i = 0; i < 20879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20880(int& val) {
    func_in_20880(val);
    const int x = 20880;
    std::cout << 20880;
    for (int i = 0; i < 20880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20880(int& val) {
    const int x = 20880;
    std::cout << 20880;
    for (int i = 0; i < 20880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20881(int& val) {
    func_in_20881(val);
    const int x = 20881;
    std::cout << 20881;
    for (int i = 0; i < 20881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20881(int& val) {
    const int x = 20881;
    std::cout << 20881;
    for (int i = 0; i < 20881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20882(int& val) {
    func_in_20882(val);
    const int x = 20882;
    std::cout << 20882;
    for (int i = 0; i < 20882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20882(int& val) {
    const int x = 20882;
    std::cout << 20882;
    for (int i = 0; i < 20882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20883(int& val) {
    func_in_20883(val);
    const int x = 20883;
    std::cout << 20883;
    for (int i = 0; i < 20883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20883(int& val) {
    const int x = 20883;
    std::cout << 20883;
    for (int i = 0; i < 20883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20884(int& val) {
    func_in_20884(val);
    const int x = 20884;
    std::cout << 20884;
    for (int i = 0; i < 20884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20884(int& val) {
    const int x = 20884;
    std::cout << 20884;
    for (int i = 0; i < 20884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20885(int& val) {
    func_in_20885(val);
    const int x = 20885;
    std::cout << 20885;
    for (int i = 0; i < 20885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20885(int& val) {
    const int x = 20885;
    std::cout << 20885;
    for (int i = 0; i < 20885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20886(int& val) {
    func_in_20886(val);
    const int x = 20886;
    std::cout << 20886;
    for (int i = 0; i < 20886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20886(int& val) {
    const int x = 20886;
    std::cout << 20886;
    for (int i = 0; i < 20886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20887(int& val) {
    func_in_20887(val);
    const int x = 20887;
    std::cout << 20887;
    for (int i = 0; i < 20887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20887(int& val) {
    const int x = 20887;
    std::cout << 20887;
    for (int i = 0; i < 20887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20888(int& val) {
    func_in_20888(val);
    const int x = 20888;
    std::cout << 20888;
    for (int i = 0; i < 20888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20888(int& val) {
    const int x = 20888;
    std::cout << 20888;
    for (int i = 0; i < 20888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20889(int& val) {
    func_in_20889(val);
    const int x = 20889;
    std::cout << 20889;
    for (int i = 0; i < 20889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20889(int& val) {
    const int x = 20889;
    std::cout << 20889;
    for (int i = 0; i < 20889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20890(int& val) {
    func_in_20890(val);
    const int x = 20890;
    std::cout << 20890;
    for (int i = 0; i < 20890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20890(int& val) {
    const int x = 20890;
    std::cout << 20890;
    for (int i = 0; i < 20890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20891(int& val) {
    func_in_20891(val);
    const int x = 20891;
    std::cout << 20891;
    for (int i = 0; i < 20891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20891(int& val) {
    const int x = 20891;
    std::cout << 20891;
    for (int i = 0; i < 20891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20892(int& val) {
    func_in_20892(val);
    const int x = 20892;
    std::cout << 20892;
    for (int i = 0; i < 20892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20892(int& val) {
    const int x = 20892;
    std::cout << 20892;
    for (int i = 0; i < 20892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20893(int& val) {
    func_in_20893(val);
    const int x = 20893;
    std::cout << 20893;
    for (int i = 0; i < 20893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20893(int& val) {
    const int x = 20893;
    std::cout << 20893;
    for (int i = 0; i < 20893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20894(int& val) {
    func_in_20894(val);
    const int x = 20894;
    std::cout << 20894;
    for (int i = 0; i < 20894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20894(int& val) {
    const int x = 20894;
    std::cout << 20894;
    for (int i = 0; i < 20894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20895(int& val) {
    func_in_20895(val);
    const int x = 20895;
    std::cout << 20895;
    for (int i = 0; i < 20895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20895(int& val) {
    const int x = 20895;
    std::cout << 20895;
    for (int i = 0; i < 20895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20896(int& val) {
    func_in_20896(val);
    const int x = 20896;
    std::cout << 20896;
    for (int i = 0; i < 20896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20896(int& val) {
    const int x = 20896;
    std::cout << 20896;
    for (int i = 0; i < 20896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20897(int& val) {
    func_in_20897(val);
    const int x = 20897;
    std::cout << 20897;
    for (int i = 0; i < 20897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20897(int& val) {
    const int x = 20897;
    std::cout << 20897;
    for (int i = 0; i < 20897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20898(int& val) {
    func_in_20898(val);
    const int x = 20898;
    std::cout << 20898;
    for (int i = 0; i < 20898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20898(int& val) {
    const int x = 20898;
    std::cout << 20898;
    for (int i = 0; i < 20898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20899(int& val) {
    func_in_20899(val);
    const int x = 20899;
    std::cout << 20899;
    for (int i = 0; i < 20899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20899(int& val) {
    const int x = 20899;
    std::cout << 20899;
    for (int i = 0; i < 20899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20900(int& val) {
    func_in_20900(val);
    const int x = 20900;
    std::cout << 20900;
    for (int i = 0; i < 20900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20900(int& val) {
    const int x = 20900;
    std::cout << 20900;
    for (int i = 0; i < 20900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20901(int& val) {
    func_in_20901(val);
    const int x = 20901;
    std::cout << 20901;
    for (int i = 0; i < 20901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20901(int& val) {
    const int x = 20901;
    std::cout << 20901;
    for (int i = 0; i < 20901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20902(int& val) {
    func_in_20902(val);
    const int x = 20902;
    std::cout << 20902;
    for (int i = 0; i < 20902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20902(int& val) {
    const int x = 20902;
    std::cout << 20902;
    for (int i = 0; i < 20902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20903(int& val) {
    func_in_20903(val);
    const int x = 20903;
    std::cout << 20903;
    for (int i = 0; i < 20903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20903(int& val) {
    const int x = 20903;
    std::cout << 20903;
    for (int i = 0; i < 20903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20904(int& val) {
    func_in_20904(val);
    const int x = 20904;
    std::cout << 20904;
    for (int i = 0; i < 20904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20904(int& val) {
    const int x = 20904;
    std::cout << 20904;
    for (int i = 0; i < 20904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20905(int& val) {
    func_in_20905(val);
    const int x = 20905;
    std::cout << 20905;
    for (int i = 0; i < 20905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20905(int& val) {
    const int x = 20905;
    std::cout << 20905;
    for (int i = 0; i < 20905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20906(int& val) {
    func_in_20906(val);
    const int x = 20906;
    std::cout << 20906;
    for (int i = 0; i < 20906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20906(int& val) {
    const int x = 20906;
    std::cout << 20906;
    for (int i = 0; i < 20906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20907(int& val) {
    func_in_20907(val);
    const int x = 20907;
    std::cout << 20907;
    for (int i = 0; i < 20907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20907(int& val) {
    const int x = 20907;
    std::cout << 20907;
    for (int i = 0; i < 20907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20908(int& val) {
    func_in_20908(val);
    const int x = 20908;
    std::cout << 20908;
    for (int i = 0; i < 20908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20908(int& val) {
    const int x = 20908;
    std::cout << 20908;
    for (int i = 0; i < 20908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20909(int& val) {
    func_in_20909(val);
    const int x = 20909;
    std::cout << 20909;
    for (int i = 0; i < 20909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20909(int& val) {
    const int x = 20909;
    std::cout << 20909;
    for (int i = 0; i < 20909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20910(int& val) {
    func_in_20910(val);
    const int x = 20910;
    std::cout << 20910;
    for (int i = 0; i < 20910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20910(int& val) {
    const int x = 20910;
    std::cout << 20910;
    for (int i = 0; i < 20910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20911(int& val) {
    func_in_20911(val);
    const int x = 20911;
    std::cout << 20911;
    for (int i = 0; i < 20911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20911(int& val) {
    const int x = 20911;
    std::cout << 20911;
    for (int i = 0; i < 20911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20912(int& val) {
    func_in_20912(val);
    const int x = 20912;
    std::cout << 20912;
    for (int i = 0; i < 20912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20912(int& val) {
    const int x = 20912;
    std::cout << 20912;
    for (int i = 0; i < 20912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20913(int& val) {
    func_in_20913(val);
    const int x = 20913;
    std::cout << 20913;
    for (int i = 0; i < 20913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20913(int& val) {
    const int x = 20913;
    std::cout << 20913;
    for (int i = 0; i < 20913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20914(int& val) {
    func_in_20914(val);
    const int x = 20914;
    std::cout << 20914;
    for (int i = 0; i < 20914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20914(int& val) {
    const int x = 20914;
    std::cout << 20914;
    for (int i = 0; i < 20914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20915(int& val) {
    func_in_20915(val);
    const int x = 20915;
    std::cout << 20915;
    for (int i = 0; i < 20915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20915(int& val) {
    const int x = 20915;
    std::cout << 20915;
    for (int i = 0; i < 20915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20916(int& val) {
    func_in_20916(val);
    const int x = 20916;
    std::cout << 20916;
    for (int i = 0; i < 20916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20916(int& val) {
    const int x = 20916;
    std::cout << 20916;
    for (int i = 0; i < 20916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20917(int& val) {
    func_in_20917(val);
    const int x = 20917;
    std::cout << 20917;
    for (int i = 0; i < 20917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20917(int& val) {
    const int x = 20917;
    std::cout << 20917;
    for (int i = 0; i < 20917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20918(int& val) {
    func_in_20918(val);
    const int x = 20918;
    std::cout << 20918;
    for (int i = 0; i < 20918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20918(int& val) {
    const int x = 20918;
    std::cout << 20918;
    for (int i = 0; i < 20918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20919(int& val) {
    func_in_20919(val);
    const int x = 20919;
    std::cout << 20919;
    for (int i = 0; i < 20919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20919(int& val) {
    const int x = 20919;
    std::cout << 20919;
    for (int i = 0; i < 20919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20920(int& val) {
    func_in_20920(val);
    const int x = 20920;
    std::cout << 20920;
    for (int i = 0; i < 20920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20920(int& val) {
    const int x = 20920;
    std::cout << 20920;
    for (int i = 0; i < 20920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20921(int& val) {
    func_in_20921(val);
    const int x = 20921;
    std::cout << 20921;
    for (int i = 0; i < 20921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20921(int& val) {
    const int x = 20921;
    std::cout << 20921;
    for (int i = 0; i < 20921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20922(int& val) {
    func_in_20922(val);
    const int x = 20922;
    std::cout << 20922;
    for (int i = 0; i < 20922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20922(int& val) {
    const int x = 20922;
    std::cout << 20922;
    for (int i = 0; i < 20922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20923(int& val) {
    func_in_20923(val);
    const int x = 20923;
    std::cout << 20923;
    for (int i = 0; i < 20923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20923(int& val) {
    const int x = 20923;
    std::cout << 20923;
    for (int i = 0; i < 20923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20924(int& val) {
    func_in_20924(val);
    const int x = 20924;
    std::cout << 20924;
    for (int i = 0; i < 20924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20924(int& val) {
    const int x = 20924;
    std::cout << 20924;
    for (int i = 0; i < 20924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20925(int& val) {
    func_in_20925(val);
    const int x = 20925;
    std::cout << 20925;
    for (int i = 0; i < 20925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20925(int& val) {
    const int x = 20925;
    std::cout << 20925;
    for (int i = 0; i < 20925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20926(int& val) {
    func_in_20926(val);
    const int x = 20926;
    std::cout << 20926;
    for (int i = 0; i < 20926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20926(int& val) {
    const int x = 20926;
    std::cout << 20926;
    for (int i = 0; i < 20926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20927(int& val) {
    func_in_20927(val);
    const int x = 20927;
    std::cout << 20927;
    for (int i = 0; i < 20927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20927(int& val) {
    const int x = 20927;
    std::cout << 20927;
    for (int i = 0; i < 20927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20928(int& val) {
    func_in_20928(val);
    const int x = 20928;
    std::cout << 20928;
    for (int i = 0; i < 20928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20928(int& val) {
    const int x = 20928;
    std::cout << 20928;
    for (int i = 0; i < 20928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20929(int& val) {
    func_in_20929(val);
    const int x = 20929;
    std::cout << 20929;
    for (int i = 0; i < 20929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20929(int& val) {
    const int x = 20929;
    std::cout << 20929;
    for (int i = 0; i < 20929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20930(int& val) {
    func_in_20930(val);
    const int x = 20930;
    std::cout << 20930;
    for (int i = 0; i < 20930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20930(int& val) {
    const int x = 20930;
    std::cout << 20930;
    for (int i = 0; i < 20930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20931(int& val) {
    func_in_20931(val);
    const int x = 20931;
    std::cout << 20931;
    for (int i = 0; i < 20931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20931(int& val) {
    const int x = 20931;
    std::cout << 20931;
    for (int i = 0; i < 20931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20932(int& val) {
    func_in_20932(val);
    const int x = 20932;
    std::cout << 20932;
    for (int i = 0; i < 20932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20932(int& val) {
    const int x = 20932;
    std::cout << 20932;
    for (int i = 0; i < 20932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20933(int& val) {
    func_in_20933(val);
    const int x = 20933;
    std::cout << 20933;
    for (int i = 0; i < 20933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20933(int& val) {
    const int x = 20933;
    std::cout << 20933;
    for (int i = 0; i < 20933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20934(int& val) {
    func_in_20934(val);
    const int x = 20934;
    std::cout << 20934;
    for (int i = 0; i < 20934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20934(int& val) {
    const int x = 20934;
    std::cout << 20934;
    for (int i = 0; i < 20934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20935(int& val) {
    func_in_20935(val);
    const int x = 20935;
    std::cout << 20935;
    for (int i = 0; i < 20935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20935(int& val) {
    const int x = 20935;
    std::cout << 20935;
    for (int i = 0; i < 20935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20936(int& val) {
    func_in_20936(val);
    const int x = 20936;
    std::cout << 20936;
    for (int i = 0; i < 20936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20936(int& val) {
    const int x = 20936;
    std::cout << 20936;
    for (int i = 0; i < 20936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20937(int& val) {
    func_in_20937(val);
    const int x = 20937;
    std::cout << 20937;
    for (int i = 0; i < 20937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20937(int& val) {
    const int x = 20937;
    std::cout << 20937;
    for (int i = 0; i < 20937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20938(int& val) {
    func_in_20938(val);
    const int x = 20938;
    std::cout << 20938;
    for (int i = 0; i < 20938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20938(int& val) {
    const int x = 20938;
    std::cout << 20938;
    for (int i = 0; i < 20938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20939(int& val) {
    func_in_20939(val);
    const int x = 20939;
    std::cout << 20939;
    for (int i = 0; i < 20939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20939(int& val) {
    const int x = 20939;
    std::cout << 20939;
    for (int i = 0; i < 20939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20940(int& val) {
    func_in_20940(val);
    const int x = 20940;
    std::cout << 20940;
    for (int i = 0; i < 20940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20940(int& val) {
    const int x = 20940;
    std::cout << 20940;
    for (int i = 0; i < 20940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20941(int& val) {
    func_in_20941(val);
    const int x = 20941;
    std::cout << 20941;
    for (int i = 0; i < 20941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20941(int& val) {
    const int x = 20941;
    std::cout << 20941;
    for (int i = 0; i < 20941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20942(int& val) {
    func_in_20942(val);
    const int x = 20942;
    std::cout << 20942;
    for (int i = 0; i < 20942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20942(int& val) {
    const int x = 20942;
    std::cout << 20942;
    for (int i = 0; i < 20942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20943(int& val) {
    func_in_20943(val);
    const int x = 20943;
    std::cout << 20943;
    for (int i = 0; i < 20943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20943(int& val) {
    const int x = 20943;
    std::cout << 20943;
    for (int i = 0; i < 20943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20944(int& val) {
    func_in_20944(val);
    const int x = 20944;
    std::cout << 20944;
    for (int i = 0; i < 20944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20944(int& val) {
    const int x = 20944;
    std::cout << 20944;
    for (int i = 0; i < 20944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20945(int& val) {
    func_in_20945(val);
    const int x = 20945;
    std::cout << 20945;
    for (int i = 0; i < 20945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20945(int& val) {
    const int x = 20945;
    std::cout << 20945;
    for (int i = 0; i < 20945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20946(int& val) {
    func_in_20946(val);
    const int x = 20946;
    std::cout << 20946;
    for (int i = 0; i < 20946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20946(int& val) {
    const int x = 20946;
    std::cout << 20946;
    for (int i = 0; i < 20946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20947(int& val) {
    func_in_20947(val);
    const int x = 20947;
    std::cout << 20947;
    for (int i = 0; i < 20947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20947(int& val) {
    const int x = 20947;
    std::cout << 20947;
    for (int i = 0; i < 20947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20948(int& val) {
    func_in_20948(val);
    const int x = 20948;
    std::cout << 20948;
    for (int i = 0; i < 20948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20948(int& val) {
    const int x = 20948;
    std::cout << 20948;
    for (int i = 0; i < 20948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20949(int& val) {
    func_in_20949(val);
    const int x = 20949;
    std::cout << 20949;
    for (int i = 0; i < 20949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20949(int& val) {
    const int x = 20949;
    std::cout << 20949;
    for (int i = 0; i < 20949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20950(int& val) {
    func_in_20950(val);
    const int x = 20950;
    std::cout << 20950;
    for (int i = 0; i < 20950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20950(int& val) {
    const int x = 20950;
    std::cout << 20950;
    for (int i = 0; i < 20950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20951(int& val) {
    func_in_20951(val);
    const int x = 20951;
    std::cout << 20951;
    for (int i = 0; i < 20951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20951(int& val) {
    const int x = 20951;
    std::cout << 20951;
    for (int i = 0; i < 20951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20952(int& val) {
    func_in_20952(val);
    const int x = 20952;
    std::cout << 20952;
    for (int i = 0; i < 20952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20952(int& val) {
    const int x = 20952;
    std::cout << 20952;
    for (int i = 0; i < 20952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20953(int& val) {
    func_in_20953(val);
    const int x = 20953;
    std::cout << 20953;
    for (int i = 0; i < 20953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20953(int& val) {
    const int x = 20953;
    std::cout << 20953;
    for (int i = 0; i < 20953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20954(int& val) {
    func_in_20954(val);
    const int x = 20954;
    std::cout << 20954;
    for (int i = 0; i < 20954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20954(int& val) {
    const int x = 20954;
    std::cout << 20954;
    for (int i = 0; i < 20954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20955(int& val) {
    func_in_20955(val);
    const int x = 20955;
    std::cout << 20955;
    for (int i = 0; i < 20955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20955(int& val) {
    const int x = 20955;
    std::cout << 20955;
    for (int i = 0; i < 20955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20956(int& val) {
    func_in_20956(val);
    const int x = 20956;
    std::cout << 20956;
    for (int i = 0; i < 20956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20956(int& val) {
    const int x = 20956;
    std::cout << 20956;
    for (int i = 0; i < 20956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20957(int& val) {
    func_in_20957(val);
    const int x = 20957;
    std::cout << 20957;
    for (int i = 0; i < 20957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20957(int& val) {
    const int x = 20957;
    std::cout << 20957;
    for (int i = 0; i < 20957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20958(int& val) {
    func_in_20958(val);
    const int x = 20958;
    std::cout << 20958;
    for (int i = 0; i < 20958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20958(int& val) {
    const int x = 20958;
    std::cout << 20958;
    for (int i = 0; i < 20958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20959(int& val) {
    func_in_20959(val);
    const int x = 20959;
    std::cout << 20959;
    for (int i = 0; i < 20959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20959(int& val) {
    const int x = 20959;
    std::cout << 20959;
    for (int i = 0; i < 20959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20960(int& val) {
    func_in_20960(val);
    const int x = 20960;
    std::cout << 20960;
    for (int i = 0; i < 20960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20960(int& val) {
    const int x = 20960;
    std::cout << 20960;
    for (int i = 0; i < 20960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20961(int& val) {
    func_in_20961(val);
    const int x = 20961;
    std::cout << 20961;
    for (int i = 0; i < 20961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20961(int& val) {
    const int x = 20961;
    std::cout << 20961;
    for (int i = 0; i < 20961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20962(int& val) {
    func_in_20962(val);
    const int x = 20962;
    std::cout << 20962;
    for (int i = 0; i < 20962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20962(int& val) {
    const int x = 20962;
    std::cout << 20962;
    for (int i = 0; i < 20962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20963(int& val) {
    func_in_20963(val);
    const int x = 20963;
    std::cout << 20963;
    for (int i = 0; i < 20963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20963(int& val) {
    const int x = 20963;
    std::cout << 20963;
    for (int i = 0; i < 20963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20964(int& val) {
    func_in_20964(val);
    const int x = 20964;
    std::cout << 20964;
    for (int i = 0; i < 20964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20964(int& val) {
    const int x = 20964;
    std::cout << 20964;
    for (int i = 0; i < 20964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20965(int& val) {
    func_in_20965(val);
    const int x = 20965;
    std::cout << 20965;
    for (int i = 0; i < 20965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20965(int& val) {
    const int x = 20965;
    std::cout << 20965;
    for (int i = 0; i < 20965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20966(int& val) {
    func_in_20966(val);
    const int x = 20966;
    std::cout << 20966;
    for (int i = 0; i < 20966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20966(int& val) {
    const int x = 20966;
    std::cout << 20966;
    for (int i = 0; i < 20966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20967(int& val) {
    func_in_20967(val);
    const int x = 20967;
    std::cout << 20967;
    for (int i = 0; i < 20967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20967(int& val) {
    const int x = 20967;
    std::cout << 20967;
    for (int i = 0; i < 20967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20968(int& val) {
    func_in_20968(val);
    const int x = 20968;
    std::cout << 20968;
    for (int i = 0; i < 20968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20968(int& val) {
    const int x = 20968;
    std::cout << 20968;
    for (int i = 0; i < 20968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20969(int& val) {
    func_in_20969(val);
    const int x = 20969;
    std::cout << 20969;
    for (int i = 0; i < 20969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20969(int& val) {
    const int x = 20969;
    std::cout << 20969;
    for (int i = 0; i < 20969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20970(int& val) {
    func_in_20970(val);
    const int x = 20970;
    std::cout << 20970;
    for (int i = 0; i < 20970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20970(int& val) {
    const int x = 20970;
    std::cout << 20970;
    for (int i = 0; i < 20970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20971(int& val) {
    func_in_20971(val);
    const int x = 20971;
    std::cout << 20971;
    for (int i = 0; i < 20971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20971(int& val) {
    const int x = 20971;
    std::cout << 20971;
    for (int i = 0; i < 20971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20972(int& val) {
    func_in_20972(val);
    const int x = 20972;
    std::cout << 20972;
    for (int i = 0; i < 20972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20972(int& val) {
    const int x = 20972;
    std::cout << 20972;
    for (int i = 0; i < 20972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20973(int& val) {
    func_in_20973(val);
    const int x = 20973;
    std::cout << 20973;
    for (int i = 0; i < 20973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20973(int& val) {
    const int x = 20973;
    std::cout << 20973;
    for (int i = 0; i < 20973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20974(int& val) {
    func_in_20974(val);
    const int x = 20974;
    std::cout << 20974;
    for (int i = 0; i < 20974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20974(int& val) {
    const int x = 20974;
    std::cout << 20974;
    for (int i = 0; i < 20974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20975(int& val) {
    func_in_20975(val);
    const int x = 20975;
    std::cout << 20975;
    for (int i = 0; i < 20975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20975(int& val) {
    const int x = 20975;
    std::cout << 20975;
    for (int i = 0; i < 20975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20976(int& val) {
    func_in_20976(val);
    const int x = 20976;
    std::cout << 20976;
    for (int i = 0; i < 20976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20976(int& val) {
    const int x = 20976;
    std::cout << 20976;
    for (int i = 0; i < 20976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20977(int& val) {
    func_in_20977(val);
    const int x = 20977;
    std::cout << 20977;
    for (int i = 0; i < 20977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20977(int& val) {
    const int x = 20977;
    std::cout << 20977;
    for (int i = 0; i < 20977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20978(int& val) {
    func_in_20978(val);
    const int x = 20978;
    std::cout << 20978;
    for (int i = 0; i < 20978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20978(int& val) {
    const int x = 20978;
    std::cout << 20978;
    for (int i = 0; i < 20978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20979(int& val) {
    func_in_20979(val);
    const int x = 20979;
    std::cout << 20979;
    for (int i = 0; i < 20979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20979(int& val) {
    const int x = 20979;
    std::cout << 20979;
    for (int i = 0; i < 20979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20980(int& val) {
    func_in_20980(val);
    const int x = 20980;
    std::cout << 20980;
    for (int i = 0; i < 20980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20980(int& val) {
    const int x = 20980;
    std::cout << 20980;
    for (int i = 0; i < 20980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20981(int& val) {
    func_in_20981(val);
    const int x = 20981;
    std::cout << 20981;
    for (int i = 0; i < 20981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20981(int& val) {
    const int x = 20981;
    std::cout << 20981;
    for (int i = 0; i < 20981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20982(int& val) {
    func_in_20982(val);
    const int x = 20982;
    std::cout << 20982;
    for (int i = 0; i < 20982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20982(int& val) {
    const int x = 20982;
    std::cout << 20982;
    for (int i = 0; i < 20982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20983(int& val) {
    func_in_20983(val);
    const int x = 20983;
    std::cout << 20983;
    for (int i = 0; i < 20983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20983(int& val) {
    const int x = 20983;
    std::cout << 20983;
    for (int i = 0; i < 20983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20984(int& val) {
    func_in_20984(val);
    const int x = 20984;
    std::cout << 20984;
    for (int i = 0; i < 20984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20984(int& val) {
    const int x = 20984;
    std::cout << 20984;
    for (int i = 0; i < 20984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20985(int& val) {
    func_in_20985(val);
    const int x = 20985;
    std::cout << 20985;
    for (int i = 0; i < 20985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20985(int& val) {
    const int x = 20985;
    std::cout << 20985;
    for (int i = 0; i < 20985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20986(int& val) {
    func_in_20986(val);
    const int x = 20986;
    std::cout << 20986;
    for (int i = 0; i < 20986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20986(int& val) {
    const int x = 20986;
    std::cout << 20986;
    for (int i = 0; i < 20986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20987(int& val) {
    func_in_20987(val);
    const int x = 20987;
    std::cout << 20987;
    for (int i = 0; i < 20987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20987(int& val) {
    const int x = 20987;
    std::cout << 20987;
    for (int i = 0; i < 20987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20988(int& val) {
    func_in_20988(val);
    const int x = 20988;
    std::cout << 20988;
    for (int i = 0; i < 20988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20988(int& val) {
    const int x = 20988;
    std::cout << 20988;
    for (int i = 0; i < 20988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20989(int& val) {
    func_in_20989(val);
    const int x = 20989;
    std::cout << 20989;
    for (int i = 0; i < 20989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20989(int& val) {
    const int x = 20989;
    std::cout << 20989;
    for (int i = 0; i < 20989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20990(int& val) {
    func_in_20990(val);
    const int x = 20990;
    std::cout << 20990;
    for (int i = 0; i < 20990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20990(int& val) {
    const int x = 20990;
    std::cout << 20990;
    for (int i = 0; i < 20990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20991(int& val) {
    func_in_20991(val);
    const int x = 20991;
    std::cout << 20991;
    for (int i = 0; i < 20991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20991(int& val) {
    const int x = 20991;
    std::cout << 20991;
    for (int i = 0; i < 20991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20992(int& val) {
    func_in_20992(val);
    const int x = 20992;
    std::cout << 20992;
    for (int i = 0; i < 20992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20992(int& val) {
    const int x = 20992;
    std::cout << 20992;
    for (int i = 0; i < 20992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20993(int& val) {
    func_in_20993(val);
    const int x = 20993;
    std::cout << 20993;
    for (int i = 0; i < 20993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20993(int& val) {
    const int x = 20993;
    std::cout << 20993;
    for (int i = 0; i < 20993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20994(int& val) {
    func_in_20994(val);
    const int x = 20994;
    std::cout << 20994;
    for (int i = 0; i < 20994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20994(int& val) {
    const int x = 20994;
    std::cout << 20994;
    for (int i = 0; i < 20994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20995(int& val) {
    func_in_20995(val);
    const int x = 20995;
    std::cout << 20995;
    for (int i = 0; i < 20995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20995(int& val) {
    const int x = 20995;
    std::cout << 20995;
    for (int i = 0; i < 20995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20996(int& val) {
    func_in_20996(val);
    const int x = 20996;
    std::cout << 20996;
    for (int i = 0; i < 20996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20996(int& val) {
    const int x = 20996;
    std::cout << 20996;
    for (int i = 0; i < 20996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20997(int& val) {
    func_in_20997(val);
    const int x = 20997;
    std::cout << 20997;
    for (int i = 0; i < 20997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20997(int& val) {
    const int x = 20997;
    std::cout << 20997;
    for (int i = 0; i < 20997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20998(int& val) {
    func_in_20998(val);
    const int x = 20998;
    std::cout << 20998;
    for (int i = 0; i < 20998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20998(int& val) {
    const int x = 20998;
    std::cout << 20998;
    for (int i = 0; i < 20998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20999(int& val) {
    func_in_20999(val);
    const int x = 20999;
    std::cout << 20999;
    for (int i = 0; i < 20999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20999(int& val) {
    const int x = 20999;
    std::cout << 20999;
    for (int i = 0; i < 20999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21000(int& val) {
    func_in_21000(val);
    const int x = 21000;
    std::cout << 21000;
    for (int i = 0; i < 21000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21000(int& val) {
    const int x = 21000;
    std::cout << 21000;
    for (int i = 0; i < 21000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21001(int& val) {
    func_in_21001(val);
    const int x = 21001;
    std::cout << 21001;
    for (int i = 0; i < 21001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21001(int& val) {
    const int x = 21001;
    std::cout << 21001;
    for (int i = 0; i < 21001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21002(int& val) {
    func_in_21002(val);
    const int x = 21002;
    std::cout << 21002;
    for (int i = 0; i < 21002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21002(int& val) {
    const int x = 21002;
    std::cout << 21002;
    for (int i = 0; i < 21002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21003(int& val) {
    func_in_21003(val);
    const int x = 21003;
    std::cout << 21003;
    for (int i = 0; i < 21003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21003(int& val) {
    const int x = 21003;
    std::cout << 21003;
    for (int i = 0; i < 21003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21004(int& val) {
    func_in_21004(val);
    const int x = 21004;
    std::cout << 21004;
    for (int i = 0; i < 21004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21004(int& val) {
    const int x = 21004;
    std::cout << 21004;
    for (int i = 0; i < 21004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21005(int& val) {
    func_in_21005(val);
    const int x = 21005;
    std::cout << 21005;
    for (int i = 0; i < 21005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21005(int& val) {
    const int x = 21005;
    std::cout << 21005;
    for (int i = 0; i < 21005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21006(int& val) {
    func_in_21006(val);
    const int x = 21006;
    std::cout << 21006;
    for (int i = 0; i < 21006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21006(int& val) {
    const int x = 21006;
    std::cout << 21006;
    for (int i = 0; i < 21006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21007(int& val) {
    func_in_21007(val);
    const int x = 21007;
    std::cout << 21007;
    for (int i = 0; i < 21007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21007(int& val) {
    const int x = 21007;
    std::cout << 21007;
    for (int i = 0; i < 21007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21008(int& val) {
    func_in_21008(val);
    const int x = 21008;
    std::cout << 21008;
    for (int i = 0; i < 21008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21008(int& val) {
    const int x = 21008;
    std::cout << 21008;
    for (int i = 0; i < 21008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21009(int& val) {
    func_in_21009(val);
    const int x = 21009;
    std::cout << 21009;
    for (int i = 0; i < 21009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21009(int& val) {
    const int x = 21009;
    std::cout << 21009;
    for (int i = 0; i < 21009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21010(int& val) {
    func_in_21010(val);
    const int x = 21010;
    std::cout << 21010;
    for (int i = 0; i < 21010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21010(int& val) {
    const int x = 21010;
    std::cout << 21010;
    for (int i = 0; i < 21010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21011(int& val) {
    func_in_21011(val);
    const int x = 21011;
    std::cout << 21011;
    for (int i = 0; i < 21011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21011(int& val) {
    const int x = 21011;
    std::cout << 21011;
    for (int i = 0; i < 21011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21012(int& val) {
    func_in_21012(val);
    const int x = 21012;
    std::cout << 21012;
    for (int i = 0; i < 21012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21012(int& val) {
    const int x = 21012;
    std::cout << 21012;
    for (int i = 0; i < 21012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21013(int& val) {
    func_in_21013(val);
    const int x = 21013;
    std::cout << 21013;
    for (int i = 0; i < 21013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21013(int& val) {
    const int x = 21013;
    std::cout << 21013;
    for (int i = 0; i < 21013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21014(int& val) {
    func_in_21014(val);
    const int x = 21014;
    std::cout << 21014;
    for (int i = 0; i < 21014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21014(int& val) {
    const int x = 21014;
    std::cout << 21014;
    for (int i = 0; i < 21014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21015(int& val) {
    func_in_21015(val);
    const int x = 21015;
    std::cout << 21015;
    for (int i = 0; i < 21015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21015(int& val) {
    const int x = 21015;
    std::cout << 21015;
    for (int i = 0; i < 21015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21016(int& val) {
    func_in_21016(val);
    const int x = 21016;
    std::cout << 21016;
    for (int i = 0; i < 21016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21016(int& val) {
    const int x = 21016;
    std::cout << 21016;
    for (int i = 0; i < 21016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21017(int& val) {
    func_in_21017(val);
    const int x = 21017;
    std::cout << 21017;
    for (int i = 0; i < 21017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21017(int& val) {
    const int x = 21017;
    std::cout << 21017;
    for (int i = 0; i < 21017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21018(int& val) {
    func_in_21018(val);
    const int x = 21018;
    std::cout << 21018;
    for (int i = 0; i < 21018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21018(int& val) {
    const int x = 21018;
    std::cout << 21018;
    for (int i = 0; i < 21018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21019(int& val) {
    func_in_21019(val);
    const int x = 21019;
    std::cout << 21019;
    for (int i = 0; i < 21019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21019(int& val) {
    const int x = 21019;
    std::cout << 21019;
    for (int i = 0; i < 21019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21020(int& val) {
    func_in_21020(val);
    const int x = 21020;
    std::cout << 21020;
    for (int i = 0; i < 21020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21020(int& val) {
    const int x = 21020;
    std::cout << 21020;
    for (int i = 0; i < 21020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21021(int& val) {
    func_in_21021(val);
    const int x = 21021;
    std::cout << 21021;
    for (int i = 0; i < 21021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21021(int& val) {
    const int x = 21021;
    std::cout << 21021;
    for (int i = 0; i < 21021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21022(int& val) {
    func_in_21022(val);
    const int x = 21022;
    std::cout << 21022;
    for (int i = 0; i < 21022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21022(int& val) {
    const int x = 21022;
    std::cout << 21022;
    for (int i = 0; i < 21022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21023(int& val) {
    func_in_21023(val);
    const int x = 21023;
    std::cout << 21023;
    for (int i = 0; i < 21023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21023(int& val) {
    const int x = 21023;
    std::cout << 21023;
    for (int i = 0; i < 21023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21024(int& val) {
    func_in_21024(val);
    const int x = 21024;
    std::cout << 21024;
    for (int i = 0; i < 21024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21024(int& val) {
    const int x = 21024;
    std::cout << 21024;
    for (int i = 0; i < 21024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21025(int& val) {
    func_in_21025(val);
    const int x = 21025;
    std::cout << 21025;
    for (int i = 0; i < 21025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21025(int& val) {
    const int x = 21025;
    std::cout << 21025;
    for (int i = 0; i < 21025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21026(int& val) {
    func_in_21026(val);
    const int x = 21026;
    std::cout << 21026;
    for (int i = 0; i < 21026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21026(int& val) {
    const int x = 21026;
    std::cout << 21026;
    for (int i = 0; i < 21026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21027(int& val) {
    func_in_21027(val);
    const int x = 21027;
    std::cout << 21027;
    for (int i = 0; i < 21027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21027(int& val) {
    const int x = 21027;
    std::cout << 21027;
    for (int i = 0; i < 21027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21028(int& val) {
    func_in_21028(val);
    const int x = 21028;
    std::cout << 21028;
    for (int i = 0; i < 21028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21028(int& val) {
    const int x = 21028;
    std::cout << 21028;
    for (int i = 0; i < 21028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21029(int& val) {
    func_in_21029(val);
    const int x = 21029;
    std::cout << 21029;
    for (int i = 0; i < 21029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21029(int& val) {
    const int x = 21029;
    std::cout << 21029;
    for (int i = 0; i < 21029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21030(int& val) {
    func_in_21030(val);
    const int x = 21030;
    std::cout << 21030;
    for (int i = 0; i < 21030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21030(int& val) {
    const int x = 21030;
    std::cout << 21030;
    for (int i = 0; i < 21030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21031(int& val) {
    func_in_21031(val);
    const int x = 21031;
    std::cout << 21031;
    for (int i = 0; i < 21031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21031(int& val) {
    const int x = 21031;
    std::cout << 21031;
    for (int i = 0; i < 21031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21032(int& val) {
    func_in_21032(val);
    const int x = 21032;
    std::cout << 21032;
    for (int i = 0; i < 21032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21032(int& val) {
    const int x = 21032;
    std::cout << 21032;
    for (int i = 0; i < 21032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21033(int& val) {
    func_in_21033(val);
    const int x = 21033;
    std::cout << 21033;
    for (int i = 0; i < 21033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21033(int& val) {
    const int x = 21033;
    std::cout << 21033;
    for (int i = 0; i < 21033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21034(int& val) {
    func_in_21034(val);
    const int x = 21034;
    std::cout << 21034;
    for (int i = 0; i < 21034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21034(int& val) {
    const int x = 21034;
    std::cout << 21034;
    for (int i = 0; i < 21034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21035(int& val) {
    func_in_21035(val);
    const int x = 21035;
    std::cout << 21035;
    for (int i = 0; i < 21035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21035(int& val) {
    const int x = 21035;
    std::cout << 21035;
    for (int i = 0; i < 21035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21036(int& val) {
    func_in_21036(val);
    const int x = 21036;
    std::cout << 21036;
    for (int i = 0; i < 21036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21036(int& val) {
    const int x = 21036;
    std::cout << 21036;
    for (int i = 0; i < 21036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21037(int& val) {
    func_in_21037(val);
    const int x = 21037;
    std::cout << 21037;
    for (int i = 0; i < 21037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21037(int& val) {
    const int x = 21037;
    std::cout << 21037;
    for (int i = 0; i < 21037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21038(int& val) {
    func_in_21038(val);
    const int x = 21038;
    std::cout << 21038;
    for (int i = 0; i < 21038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21038(int& val) {
    const int x = 21038;
    std::cout << 21038;
    for (int i = 0; i < 21038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21039(int& val) {
    func_in_21039(val);
    const int x = 21039;
    std::cout << 21039;
    for (int i = 0; i < 21039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21039(int& val) {
    const int x = 21039;
    std::cout << 21039;
    for (int i = 0; i < 21039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21040(int& val) {
    func_in_21040(val);
    const int x = 21040;
    std::cout << 21040;
    for (int i = 0; i < 21040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21040(int& val) {
    const int x = 21040;
    std::cout << 21040;
    for (int i = 0; i < 21040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21041(int& val) {
    func_in_21041(val);
    const int x = 21041;
    std::cout << 21041;
    for (int i = 0; i < 21041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21041(int& val) {
    const int x = 21041;
    std::cout << 21041;
    for (int i = 0; i < 21041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21042(int& val) {
    func_in_21042(val);
    const int x = 21042;
    std::cout << 21042;
    for (int i = 0; i < 21042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21042(int& val) {
    const int x = 21042;
    std::cout << 21042;
    for (int i = 0; i < 21042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21043(int& val) {
    func_in_21043(val);
    const int x = 21043;
    std::cout << 21043;
    for (int i = 0; i < 21043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21043(int& val) {
    const int x = 21043;
    std::cout << 21043;
    for (int i = 0; i < 21043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21044(int& val) {
    func_in_21044(val);
    const int x = 21044;
    std::cout << 21044;
    for (int i = 0; i < 21044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21044(int& val) {
    const int x = 21044;
    std::cout << 21044;
    for (int i = 0; i < 21044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21045(int& val) {
    func_in_21045(val);
    const int x = 21045;
    std::cout << 21045;
    for (int i = 0; i < 21045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21045(int& val) {
    const int x = 21045;
    std::cout << 21045;
    for (int i = 0; i < 21045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21046(int& val) {
    func_in_21046(val);
    const int x = 21046;
    std::cout << 21046;
    for (int i = 0; i < 21046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21046(int& val) {
    const int x = 21046;
    std::cout << 21046;
    for (int i = 0; i < 21046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21047(int& val) {
    func_in_21047(val);
    const int x = 21047;
    std::cout << 21047;
    for (int i = 0; i < 21047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21047(int& val) {
    const int x = 21047;
    std::cout << 21047;
    for (int i = 0; i < 21047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21048(int& val) {
    func_in_21048(val);
    const int x = 21048;
    std::cout << 21048;
    for (int i = 0; i < 21048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21048(int& val) {
    const int x = 21048;
    std::cout << 21048;
    for (int i = 0; i < 21048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21049(int& val) {
    func_in_21049(val);
    const int x = 21049;
    std::cout << 21049;
    for (int i = 0; i < 21049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21049(int& val) {
    const int x = 21049;
    std::cout << 21049;
    for (int i = 0; i < 21049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21050(int& val) {
    func_in_21050(val);
    const int x = 21050;
    std::cout << 21050;
    for (int i = 0; i < 21050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21050(int& val) {
    const int x = 21050;
    std::cout << 21050;
    for (int i = 0; i < 21050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21051(int& val) {
    func_in_21051(val);
    const int x = 21051;
    std::cout << 21051;
    for (int i = 0; i < 21051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21051(int& val) {
    const int x = 21051;
    std::cout << 21051;
    for (int i = 0; i < 21051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21052(int& val) {
    func_in_21052(val);
    const int x = 21052;
    std::cout << 21052;
    for (int i = 0; i < 21052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21052(int& val) {
    const int x = 21052;
    std::cout << 21052;
    for (int i = 0; i < 21052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21053(int& val) {
    func_in_21053(val);
    const int x = 21053;
    std::cout << 21053;
    for (int i = 0; i < 21053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21053(int& val) {
    const int x = 21053;
    std::cout << 21053;
    for (int i = 0; i < 21053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21054(int& val) {
    func_in_21054(val);
    const int x = 21054;
    std::cout << 21054;
    for (int i = 0; i < 21054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21054(int& val) {
    const int x = 21054;
    std::cout << 21054;
    for (int i = 0; i < 21054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21055(int& val) {
    func_in_21055(val);
    const int x = 21055;
    std::cout << 21055;
    for (int i = 0; i < 21055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21055(int& val) {
    const int x = 21055;
    std::cout << 21055;
    for (int i = 0; i < 21055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21056(int& val) {
    func_in_21056(val);
    const int x = 21056;
    std::cout << 21056;
    for (int i = 0; i < 21056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21056(int& val) {
    const int x = 21056;
    std::cout << 21056;
    for (int i = 0; i < 21056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21057(int& val) {
    func_in_21057(val);
    const int x = 21057;
    std::cout << 21057;
    for (int i = 0; i < 21057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21057(int& val) {
    const int x = 21057;
    std::cout << 21057;
    for (int i = 0; i < 21057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21058(int& val) {
    func_in_21058(val);
    const int x = 21058;
    std::cout << 21058;
    for (int i = 0; i < 21058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21058(int& val) {
    const int x = 21058;
    std::cout << 21058;
    for (int i = 0; i < 21058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21059(int& val) {
    func_in_21059(val);
    const int x = 21059;
    std::cout << 21059;
    for (int i = 0; i < 21059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21059(int& val) {
    const int x = 21059;
    std::cout << 21059;
    for (int i = 0; i < 21059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21060(int& val) {
    func_in_21060(val);
    const int x = 21060;
    std::cout << 21060;
    for (int i = 0; i < 21060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21060(int& val) {
    const int x = 21060;
    std::cout << 21060;
    for (int i = 0; i < 21060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21061(int& val) {
    func_in_21061(val);
    const int x = 21061;
    std::cout << 21061;
    for (int i = 0; i < 21061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21061(int& val) {
    const int x = 21061;
    std::cout << 21061;
    for (int i = 0; i < 21061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21062(int& val) {
    func_in_21062(val);
    const int x = 21062;
    std::cout << 21062;
    for (int i = 0; i < 21062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21062(int& val) {
    const int x = 21062;
    std::cout << 21062;
    for (int i = 0; i < 21062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21063(int& val) {
    func_in_21063(val);
    const int x = 21063;
    std::cout << 21063;
    for (int i = 0; i < 21063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21063(int& val) {
    const int x = 21063;
    std::cout << 21063;
    for (int i = 0; i < 21063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21064(int& val) {
    func_in_21064(val);
    const int x = 21064;
    std::cout << 21064;
    for (int i = 0; i < 21064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21064(int& val) {
    const int x = 21064;
    std::cout << 21064;
    for (int i = 0; i < 21064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21065(int& val) {
    func_in_21065(val);
    const int x = 21065;
    std::cout << 21065;
    for (int i = 0; i < 21065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21065(int& val) {
    const int x = 21065;
    std::cout << 21065;
    for (int i = 0; i < 21065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21066(int& val) {
    func_in_21066(val);
    const int x = 21066;
    std::cout << 21066;
    for (int i = 0; i < 21066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21066(int& val) {
    const int x = 21066;
    std::cout << 21066;
    for (int i = 0; i < 21066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21067(int& val) {
    func_in_21067(val);
    const int x = 21067;
    std::cout << 21067;
    for (int i = 0; i < 21067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21067(int& val) {
    const int x = 21067;
    std::cout << 21067;
    for (int i = 0; i < 21067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21068(int& val) {
    func_in_21068(val);
    const int x = 21068;
    std::cout << 21068;
    for (int i = 0; i < 21068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21068(int& val) {
    const int x = 21068;
    std::cout << 21068;
    for (int i = 0; i < 21068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21069(int& val) {
    func_in_21069(val);
    const int x = 21069;
    std::cout << 21069;
    for (int i = 0; i < 21069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21069(int& val) {
    const int x = 21069;
    std::cout << 21069;
    for (int i = 0; i < 21069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21070(int& val) {
    func_in_21070(val);
    const int x = 21070;
    std::cout << 21070;
    for (int i = 0; i < 21070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21070(int& val) {
    const int x = 21070;
    std::cout << 21070;
    for (int i = 0; i < 21070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21071(int& val) {
    func_in_21071(val);
    const int x = 21071;
    std::cout << 21071;
    for (int i = 0; i < 21071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21071(int& val) {
    const int x = 21071;
    std::cout << 21071;
    for (int i = 0; i < 21071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21072(int& val) {
    func_in_21072(val);
    const int x = 21072;
    std::cout << 21072;
    for (int i = 0; i < 21072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21072(int& val) {
    const int x = 21072;
    std::cout << 21072;
    for (int i = 0; i < 21072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21073(int& val) {
    func_in_21073(val);
    const int x = 21073;
    std::cout << 21073;
    for (int i = 0; i < 21073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21073(int& val) {
    const int x = 21073;
    std::cout << 21073;
    for (int i = 0; i < 21073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21074(int& val) {
    func_in_21074(val);
    const int x = 21074;
    std::cout << 21074;
    for (int i = 0; i < 21074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21074(int& val) {
    const int x = 21074;
    std::cout << 21074;
    for (int i = 0; i < 21074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21075(int& val) {
    func_in_21075(val);
    const int x = 21075;
    std::cout << 21075;
    for (int i = 0; i < 21075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21075(int& val) {
    const int x = 21075;
    std::cout << 21075;
    for (int i = 0; i < 21075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21076(int& val) {
    func_in_21076(val);
    const int x = 21076;
    std::cout << 21076;
    for (int i = 0; i < 21076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21076(int& val) {
    const int x = 21076;
    std::cout << 21076;
    for (int i = 0; i < 21076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21077(int& val) {
    func_in_21077(val);
    const int x = 21077;
    std::cout << 21077;
    for (int i = 0; i < 21077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21077(int& val) {
    const int x = 21077;
    std::cout << 21077;
    for (int i = 0; i < 21077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21078(int& val) {
    func_in_21078(val);
    const int x = 21078;
    std::cout << 21078;
    for (int i = 0; i < 21078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21078(int& val) {
    const int x = 21078;
    std::cout << 21078;
    for (int i = 0; i < 21078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21079(int& val) {
    func_in_21079(val);
    const int x = 21079;
    std::cout << 21079;
    for (int i = 0; i < 21079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21079(int& val) {
    const int x = 21079;
    std::cout << 21079;
    for (int i = 0; i < 21079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21080(int& val) {
    func_in_21080(val);
    const int x = 21080;
    std::cout << 21080;
    for (int i = 0; i < 21080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21080(int& val) {
    const int x = 21080;
    std::cout << 21080;
    for (int i = 0; i < 21080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21081(int& val) {
    func_in_21081(val);
    const int x = 21081;
    std::cout << 21081;
    for (int i = 0; i < 21081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21081(int& val) {
    const int x = 21081;
    std::cout << 21081;
    for (int i = 0; i < 21081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21082(int& val) {
    func_in_21082(val);
    const int x = 21082;
    std::cout << 21082;
    for (int i = 0; i < 21082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21082(int& val) {
    const int x = 21082;
    std::cout << 21082;
    for (int i = 0; i < 21082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21083(int& val) {
    func_in_21083(val);
    const int x = 21083;
    std::cout << 21083;
    for (int i = 0; i < 21083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21083(int& val) {
    const int x = 21083;
    std::cout << 21083;
    for (int i = 0; i < 21083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21084(int& val) {
    func_in_21084(val);
    const int x = 21084;
    std::cout << 21084;
    for (int i = 0; i < 21084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21084(int& val) {
    const int x = 21084;
    std::cout << 21084;
    for (int i = 0; i < 21084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21085(int& val) {
    func_in_21085(val);
    const int x = 21085;
    std::cout << 21085;
    for (int i = 0; i < 21085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21085(int& val) {
    const int x = 21085;
    std::cout << 21085;
    for (int i = 0; i < 21085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21086(int& val) {
    func_in_21086(val);
    const int x = 21086;
    std::cout << 21086;
    for (int i = 0; i < 21086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21086(int& val) {
    const int x = 21086;
    std::cout << 21086;
    for (int i = 0; i < 21086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21087(int& val) {
    func_in_21087(val);
    const int x = 21087;
    std::cout << 21087;
    for (int i = 0; i < 21087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21087(int& val) {
    const int x = 21087;
    std::cout << 21087;
    for (int i = 0; i < 21087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21088(int& val) {
    func_in_21088(val);
    const int x = 21088;
    std::cout << 21088;
    for (int i = 0; i < 21088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21088(int& val) {
    const int x = 21088;
    std::cout << 21088;
    for (int i = 0; i < 21088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21089(int& val) {
    func_in_21089(val);
    const int x = 21089;
    std::cout << 21089;
    for (int i = 0; i < 21089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21089(int& val) {
    const int x = 21089;
    std::cout << 21089;
    for (int i = 0; i < 21089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21090(int& val) {
    func_in_21090(val);
    const int x = 21090;
    std::cout << 21090;
    for (int i = 0; i < 21090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21090(int& val) {
    const int x = 21090;
    std::cout << 21090;
    for (int i = 0; i < 21090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21091(int& val) {
    func_in_21091(val);
    const int x = 21091;
    std::cout << 21091;
    for (int i = 0; i < 21091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21091(int& val) {
    const int x = 21091;
    std::cout << 21091;
    for (int i = 0; i < 21091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21092(int& val) {
    func_in_21092(val);
    const int x = 21092;
    std::cout << 21092;
    for (int i = 0; i < 21092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21092(int& val) {
    const int x = 21092;
    std::cout << 21092;
    for (int i = 0; i < 21092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21093(int& val) {
    func_in_21093(val);
    const int x = 21093;
    std::cout << 21093;
    for (int i = 0; i < 21093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21093(int& val) {
    const int x = 21093;
    std::cout << 21093;
    for (int i = 0; i < 21093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21094(int& val) {
    func_in_21094(val);
    const int x = 21094;
    std::cout << 21094;
    for (int i = 0; i < 21094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21094(int& val) {
    const int x = 21094;
    std::cout << 21094;
    for (int i = 0; i < 21094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21095(int& val) {
    func_in_21095(val);
    const int x = 21095;
    std::cout << 21095;
    for (int i = 0; i < 21095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21095(int& val) {
    const int x = 21095;
    std::cout << 21095;
    for (int i = 0; i < 21095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21096(int& val) {
    func_in_21096(val);
    const int x = 21096;
    std::cout << 21096;
    for (int i = 0; i < 21096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21096(int& val) {
    const int x = 21096;
    std::cout << 21096;
    for (int i = 0; i < 21096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21097(int& val) {
    func_in_21097(val);
    const int x = 21097;
    std::cout << 21097;
    for (int i = 0; i < 21097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21097(int& val) {
    const int x = 21097;
    std::cout << 21097;
    for (int i = 0; i < 21097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21098(int& val) {
    func_in_21098(val);
    const int x = 21098;
    std::cout << 21098;
    for (int i = 0; i < 21098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21098(int& val) {
    const int x = 21098;
    std::cout << 21098;
    for (int i = 0; i < 21098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21099(int& val) {
    func_in_21099(val);
    const int x = 21099;
    std::cout << 21099;
    for (int i = 0; i < 21099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21099(int& val) {
    const int x = 21099;
    std::cout << 21099;
    for (int i = 0; i < 21099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21100(int& val) {
    func_in_21100(val);
    const int x = 21100;
    std::cout << 21100;
    for (int i = 0; i < 21100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21100(int& val) {
    const int x = 21100;
    std::cout << 21100;
    for (int i = 0; i < 21100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21101(int& val) {
    func_in_21101(val);
    const int x = 21101;
    std::cout << 21101;
    for (int i = 0; i < 21101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21101(int& val) {
    const int x = 21101;
    std::cout << 21101;
    for (int i = 0; i < 21101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21102(int& val) {
    func_in_21102(val);
    const int x = 21102;
    std::cout << 21102;
    for (int i = 0; i < 21102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21102(int& val) {
    const int x = 21102;
    std::cout << 21102;
    for (int i = 0; i < 21102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21103(int& val) {
    func_in_21103(val);
    const int x = 21103;
    std::cout << 21103;
    for (int i = 0; i < 21103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21103(int& val) {
    const int x = 21103;
    std::cout << 21103;
    for (int i = 0; i < 21103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21104(int& val) {
    func_in_21104(val);
    const int x = 21104;
    std::cout << 21104;
    for (int i = 0; i < 21104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21104(int& val) {
    const int x = 21104;
    std::cout << 21104;
    for (int i = 0; i < 21104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21105(int& val) {
    func_in_21105(val);
    const int x = 21105;
    std::cout << 21105;
    for (int i = 0; i < 21105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21105(int& val) {
    const int x = 21105;
    std::cout << 21105;
    for (int i = 0; i < 21105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21106(int& val) {
    func_in_21106(val);
    const int x = 21106;
    std::cout << 21106;
    for (int i = 0; i < 21106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21106(int& val) {
    const int x = 21106;
    std::cout << 21106;
    for (int i = 0; i < 21106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21107(int& val) {
    func_in_21107(val);
    const int x = 21107;
    std::cout << 21107;
    for (int i = 0; i < 21107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21107(int& val) {
    const int x = 21107;
    std::cout << 21107;
    for (int i = 0; i < 21107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21108(int& val) {
    func_in_21108(val);
    const int x = 21108;
    std::cout << 21108;
    for (int i = 0; i < 21108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21108(int& val) {
    const int x = 21108;
    std::cout << 21108;
    for (int i = 0; i < 21108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21109(int& val) {
    func_in_21109(val);
    const int x = 21109;
    std::cout << 21109;
    for (int i = 0; i < 21109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21109(int& val) {
    const int x = 21109;
    std::cout << 21109;
    for (int i = 0; i < 21109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21110(int& val) {
    func_in_21110(val);
    const int x = 21110;
    std::cout << 21110;
    for (int i = 0; i < 21110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21110(int& val) {
    const int x = 21110;
    std::cout << 21110;
    for (int i = 0; i < 21110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21111(int& val) {
    func_in_21111(val);
    const int x = 21111;
    std::cout << 21111;
    for (int i = 0; i < 21111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21111(int& val) {
    const int x = 21111;
    std::cout << 21111;
    for (int i = 0; i < 21111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21112(int& val) {
    func_in_21112(val);
    const int x = 21112;
    std::cout << 21112;
    for (int i = 0; i < 21112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21112(int& val) {
    const int x = 21112;
    std::cout << 21112;
    for (int i = 0; i < 21112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21113(int& val) {
    func_in_21113(val);
    const int x = 21113;
    std::cout << 21113;
    for (int i = 0; i < 21113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21113(int& val) {
    const int x = 21113;
    std::cout << 21113;
    for (int i = 0; i < 21113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21114(int& val) {
    func_in_21114(val);
    const int x = 21114;
    std::cout << 21114;
    for (int i = 0; i < 21114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21114(int& val) {
    const int x = 21114;
    std::cout << 21114;
    for (int i = 0; i < 21114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21115(int& val) {
    func_in_21115(val);
    const int x = 21115;
    std::cout << 21115;
    for (int i = 0; i < 21115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21115(int& val) {
    const int x = 21115;
    std::cout << 21115;
    for (int i = 0; i < 21115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21116(int& val) {
    func_in_21116(val);
    const int x = 21116;
    std::cout << 21116;
    for (int i = 0; i < 21116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21116(int& val) {
    const int x = 21116;
    std::cout << 21116;
    for (int i = 0; i < 21116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21117(int& val) {
    func_in_21117(val);
    const int x = 21117;
    std::cout << 21117;
    for (int i = 0; i < 21117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21117(int& val) {
    const int x = 21117;
    std::cout << 21117;
    for (int i = 0; i < 21117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21118(int& val) {
    func_in_21118(val);
    const int x = 21118;
    std::cout << 21118;
    for (int i = 0; i < 21118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21118(int& val) {
    const int x = 21118;
    std::cout << 21118;
    for (int i = 0; i < 21118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21119(int& val) {
    func_in_21119(val);
    const int x = 21119;
    std::cout << 21119;
    for (int i = 0; i < 21119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21119(int& val) {
    const int x = 21119;
    std::cout << 21119;
    for (int i = 0; i < 21119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21120(int& val) {
    func_in_21120(val);
    const int x = 21120;
    std::cout << 21120;
    for (int i = 0; i < 21120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21120(int& val) {
    const int x = 21120;
    std::cout << 21120;
    for (int i = 0; i < 21120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21121(int& val) {
    func_in_21121(val);
    const int x = 21121;
    std::cout << 21121;
    for (int i = 0; i < 21121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21121(int& val) {
    const int x = 21121;
    std::cout << 21121;
    for (int i = 0; i < 21121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21122(int& val) {
    func_in_21122(val);
    const int x = 21122;
    std::cout << 21122;
    for (int i = 0; i < 21122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21122(int& val) {
    const int x = 21122;
    std::cout << 21122;
    for (int i = 0; i < 21122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21123(int& val) {
    func_in_21123(val);
    const int x = 21123;
    std::cout << 21123;
    for (int i = 0; i < 21123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21123(int& val) {
    const int x = 21123;
    std::cout << 21123;
    for (int i = 0; i < 21123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21124(int& val) {
    func_in_21124(val);
    const int x = 21124;
    std::cout << 21124;
    for (int i = 0; i < 21124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21124(int& val) {
    const int x = 21124;
    std::cout << 21124;
    for (int i = 0; i < 21124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21125(int& val) {
    func_in_21125(val);
    const int x = 21125;
    std::cout << 21125;
    for (int i = 0; i < 21125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21125(int& val) {
    const int x = 21125;
    std::cout << 21125;
    for (int i = 0; i < 21125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21126(int& val) {
    func_in_21126(val);
    const int x = 21126;
    std::cout << 21126;
    for (int i = 0; i < 21126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21126(int& val) {
    const int x = 21126;
    std::cout << 21126;
    for (int i = 0; i < 21126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21127(int& val) {
    func_in_21127(val);
    const int x = 21127;
    std::cout << 21127;
    for (int i = 0; i < 21127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21127(int& val) {
    const int x = 21127;
    std::cout << 21127;
    for (int i = 0; i < 21127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21128(int& val) {
    func_in_21128(val);
    const int x = 21128;
    std::cout << 21128;
    for (int i = 0; i < 21128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21128(int& val) {
    const int x = 21128;
    std::cout << 21128;
    for (int i = 0; i < 21128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21129(int& val) {
    func_in_21129(val);
    const int x = 21129;
    std::cout << 21129;
    for (int i = 0; i < 21129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21129(int& val) {
    const int x = 21129;
    std::cout << 21129;
    for (int i = 0; i < 21129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21130(int& val) {
    func_in_21130(val);
    const int x = 21130;
    std::cout << 21130;
    for (int i = 0; i < 21130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21130(int& val) {
    const int x = 21130;
    std::cout << 21130;
    for (int i = 0; i < 21130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21131(int& val) {
    func_in_21131(val);
    const int x = 21131;
    std::cout << 21131;
    for (int i = 0; i < 21131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21131(int& val) {
    const int x = 21131;
    std::cout << 21131;
    for (int i = 0; i < 21131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21132(int& val) {
    func_in_21132(val);
    const int x = 21132;
    std::cout << 21132;
    for (int i = 0; i < 21132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21132(int& val) {
    const int x = 21132;
    std::cout << 21132;
    for (int i = 0; i < 21132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21133(int& val) {
    func_in_21133(val);
    const int x = 21133;
    std::cout << 21133;
    for (int i = 0; i < 21133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21133(int& val) {
    const int x = 21133;
    std::cout << 21133;
    for (int i = 0; i < 21133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21134(int& val) {
    func_in_21134(val);
    const int x = 21134;
    std::cout << 21134;
    for (int i = 0; i < 21134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21134(int& val) {
    const int x = 21134;
    std::cout << 21134;
    for (int i = 0; i < 21134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21135(int& val) {
    func_in_21135(val);
    const int x = 21135;
    std::cout << 21135;
    for (int i = 0; i < 21135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21135(int& val) {
    const int x = 21135;
    std::cout << 21135;
    for (int i = 0; i < 21135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21136(int& val) {
    func_in_21136(val);
    const int x = 21136;
    std::cout << 21136;
    for (int i = 0; i < 21136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21136(int& val) {
    const int x = 21136;
    std::cout << 21136;
    for (int i = 0; i < 21136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21137(int& val) {
    func_in_21137(val);
    const int x = 21137;
    std::cout << 21137;
    for (int i = 0; i < 21137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21137(int& val) {
    const int x = 21137;
    std::cout << 21137;
    for (int i = 0; i < 21137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21138(int& val) {
    func_in_21138(val);
    const int x = 21138;
    std::cout << 21138;
    for (int i = 0; i < 21138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21138(int& val) {
    const int x = 21138;
    std::cout << 21138;
    for (int i = 0; i < 21138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21139(int& val) {
    func_in_21139(val);
    const int x = 21139;
    std::cout << 21139;
    for (int i = 0; i < 21139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21139(int& val) {
    const int x = 21139;
    std::cout << 21139;
    for (int i = 0; i < 21139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21140(int& val) {
    func_in_21140(val);
    const int x = 21140;
    std::cout << 21140;
    for (int i = 0; i < 21140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21140(int& val) {
    const int x = 21140;
    std::cout << 21140;
    for (int i = 0; i < 21140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21141(int& val) {
    func_in_21141(val);
    const int x = 21141;
    std::cout << 21141;
    for (int i = 0; i < 21141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21141(int& val) {
    const int x = 21141;
    std::cout << 21141;
    for (int i = 0; i < 21141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21142(int& val) {
    func_in_21142(val);
    const int x = 21142;
    std::cout << 21142;
    for (int i = 0; i < 21142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21142(int& val) {
    const int x = 21142;
    std::cout << 21142;
    for (int i = 0; i < 21142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21143(int& val) {
    func_in_21143(val);
    const int x = 21143;
    std::cout << 21143;
    for (int i = 0; i < 21143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21143(int& val) {
    const int x = 21143;
    std::cout << 21143;
    for (int i = 0; i < 21143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21144(int& val) {
    func_in_21144(val);
    const int x = 21144;
    std::cout << 21144;
    for (int i = 0; i < 21144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21144(int& val) {
    const int x = 21144;
    std::cout << 21144;
    for (int i = 0; i < 21144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21145(int& val) {
    func_in_21145(val);
    const int x = 21145;
    std::cout << 21145;
    for (int i = 0; i < 21145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21145(int& val) {
    const int x = 21145;
    std::cout << 21145;
    for (int i = 0; i < 21145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21146(int& val) {
    func_in_21146(val);
    const int x = 21146;
    std::cout << 21146;
    for (int i = 0; i < 21146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21146(int& val) {
    const int x = 21146;
    std::cout << 21146;
    for (int i = 0; i < 21146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21147(int& val) {
    func_in_21147(val);
    const int x = 21147;
    std::cout << 21147;
    for (int i = 0; i < 21147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21147(int& val) {
    const int x = 21147;
    std::cout << 21147;
    for (int i = 0; i < 21147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21148(int& val) {
    func_in_21148(val);
    const int x = 21148;
    std::cout << 21148;
    for (int i = 0; i < 21148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21148(int& val) {
    const int x = 21148;
    std::cout << 21148;
    for (int i = 0; i < 21148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21149(int& val) {
    func_in_21149(val);
    const int x = 21149;
    std::cout << 21149;
    for (int i = 0; i < 21149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21149(int& val) {
    const int x = 21149;
    std::cout << 21149;
    for (int i = 0; i < 21149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21150(int& val) {
    func_in_21150(val);
    const int x = 21150;
    std::cout << 21150;
    for (int i = 0; i < 21150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21150(int& val) {
    const int x = 21150;
    std::cout << 21150;
    for (int i = 0; i < 21150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21151(int& val) {
    func_in_21151(val);
    const int x = 21151;
    std::cout << 21151;
    for (int i = 0; i < 21151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21151(int& val) {
    const int x = 21151;
    std::cout << 21151;
    for (int i = 0; i < 21151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21152(int& val) {
    func_in_21152(val);
    const int x = 21152;
    std::cout << 21152;
    for (int i = 0; i < 21152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21152(int& val) {
    const int x = 21152;
    std::cout << 21152;
    for (int i = 0; i < 21152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21153(int& val) {
    func_in_21153(val);
    const int x = 21153;
    std::cout << 21153;
    for (int i = 0; i < 21153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21153(int& val) {
    const int x = 21153;
    std::cout << 21153;
    for (int i = 0; i < 21153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21154(int& val) {
    func_in_21154(val);
    const int x = 21154;
    std::cout << 21154;
    for (int i = 0; i < 21154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21154(int& val) {
    const int x = 21154;
    std::cout << 21154;
    for (int i = 0; i < 21154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21155(int& val) {
    func_in_21155(val);
    const int x = 21155;
    std::cout << 21155;
    for (int i = 0; i < 21155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21155(int& val) {
    const int x = 21155;
    std::cout << 21155;
    for (int i = 0; i < 21155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21156(int& val) {
    func_in_21156(val);
    const int x = 21156;
    std::cout << 21156;
    for (int i = 0; i < 21156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21156(int& val) {
    const int x = 21156;
    std::cout << 21156;
    for (int i = 0; i < 21156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21157(int& val) {
    func_in_21157(val);
    const int x = 21157;
    std::cout << 21157;
    for (int i = 0; i < 21157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21157(int& val) {
    const int x = 21157;
    std::cout << 21157;
    for (int i = 0; i < 21157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21158(int& val) {
    func_in_21158(val);
    const int x = 21158;
    std::cout << 21158;
    for (int i = 0; i < 21158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21158(int& val) {
    const int x = 21158;
    std::cout << 21158;
    for (int i = 0; i < 21158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21159(int& val) {
    func_in_21159(val);
    const int x = 21159;
    std::cout << 21159;
    for (int i = 0; i < 21159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21159(int& val) {
    const int x = 21159;
    std::cout << 21159;
    for (int i = 0; i < 21159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21160(int& val) {
    func_in_21160(val);
    const int x = 21160;
    std::cout << 21160;
    for (int i = 0; i < 21160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21160(int& val) {
    const int x = 21160;
    std::cout << 21160;
    for (int i = 0; i < 21160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21161(int& val) {
    func_in_21161(val);
    const int x = 21161;
    std::cout << 21161;
    for (int i = 0; i < 21161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21161(int& val) {
    const int x = 21161;
    std::cout << 21161;
    for (int i = 0; i < 21161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21162(int& val) {
    func_in_21162(val);
    const int x = 21162;
    std::cout << 21162;
    for (int i = 0; i < 21162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21162(int& val) {
    const int x = 21162;
    std::cout << 21162;
    for (int i = 0; i < 21162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21163(int& val) {
    func_in_21163(val);
    const int x = 21163;
    std::cout << 21163;
    for (int i = 0; i < 21163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21163(int& val) {
    const int x = 21163;
    std::cout << 21163;
    for (int i = 0; i < 21163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21164(int& val) {
    func_in_21164(val);
    const int x = 21164;
    std::cout << 21164;
    for (int i = 0; i < 21164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21164(int& val) {
    const int x = 21164;
    std::cout << 21164;
    for (int i = 0; i < 21164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21165(int& val) {
    func_in_21165(val);
    const int x = 21165;
    std::cout << 21165;
    for (int i = 0; i < 21165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21165(int& val) {
    const int x = 21165;
    std::cout << 21165;
    for (int i = 0; i < 21165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21166(int& val) {
    func_in_21166(val);
    const int x = 21166;
    std::cout << 21166;
    for (int i = 0; i < 21166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21166(int& val) {
    const int x = 21166;
    std::cout << 21166;
    for (int i = 0; i < 21166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21167(int& val) {
    func_in_21167(val);
    const int x = 21167;
    std::cout << 21167;
    for (int i = 0; i < 21167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21167(int& val) {
    const int x = 21167;
    std::cout << 21167;
    for (int i = 0; i < 21167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21168(int& val) {
    func_in_21168(val);
    const int x = 21168;
    std::cout << 21168;
    for (int i = 0; i < 21168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21168(int& val) {
    const int x = 21168;
    std::cout << 21168;
    for (int i = 0; i < 21168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21169(int& val) {
    func_in_21169(val);
    const int x = 21169;
    std::cout << 21169;
    for (int i = 0; i < 21169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21169(int& val) {
    const int x = 21169;
    std::cout << 21169;
    for (int i = 0; i < 21169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21170(int& val) {
    func_in_21170(val);
    const int x = 21170;
    std::cout << 21170;
    for (int i = 0; i < 21170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21170(int& val) {
    const int x = 21170;
    std::cout << 21170;
    for (int i = 0; i < 21170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21171(int& val) {
    func_in_21171(val);
    const int x = 21171;
    std::cout << 21171;
    for (int i = 0; i < 21171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21171(int& val) {
    const int x = 21171;
    std::cout << 21171;
    for (int i = 0; i < 21171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21172(int& val) {
    func_in_21172(val);
    const int x = 21172;
    std::cout << 21172;
    for (int i = 0; i < 21172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21172(int& val) {
    const int x = 21172;
    std::cout << 21172;
    for (int i = 0; i < 21172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21173(int& val) {
    func_in_21173(val);
    const int x = 21173;
    std::cout << 21173;
    for (int i = 0; i < 21173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21173(int& val) {
    const int x = 21173;
    std::cout << 21173;
    for (int i = 0; i < 21173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21174(int& val) {
    func_in_21174(val);
    const int x = 21174;
    std::cout << 21174;
    for (int i = 0; i < 21174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21174(int& val) {
    const int x = 21174;
    std::cout << 21174;
    for (int i = 0; i < 21174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21175(int& val) {
    func_in_21175(val);
    const int x = 21175;
    std::cout << 21175;
    for (int i = 0; i < 21175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21175(int& val) {
    const int x = 21175;
    std::cout << 21175;
    for (int i = 0; i < 21175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21176(int& val) {
    func_in_21176(val);
    const int x = 21176;
    std::cout << 21176;
    for (int i = 0; i < 21176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21176(int& val) {
    const int x = 21176;
    std::cout << 21176;
    for (int i = 0; i < 21176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21177(int& val) {
    func_in_21177(val);
    const int x = 21177;
    std::cout << 21177;
    for (int i = 0; i < 21177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21177(int& val) {
    const int x = 21177;
    std::cout << 21177;
    for (int i = 0; i < 21177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21178(int& val) {
    func_in_21178(val);
    const int x = 21178;
    std::cout << 21178;
    for (int i = 0; i < 21178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21178(int& val) {
    const int x = 21178;
    std::cout << 21178;
    for (int i = 0; i < 21178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21179(int& val) {
    func_in_21179(val);
    const int x = 21179;
    std::cout << 21179;
    for (int i = 0; i < 21179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21179(int& val) {
    const int x = 21179;
    std::cout << 21179;
    for (int i = 0; i < 21179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21180(int& val) {
    func_in_21180(val);
    const int x = 21180;
    std::cout << 21180;
    for (int i = 0; i < 21180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21180(int& val) {
    const int x = 21180;
    std::cout << 21180;
    for (int i = 0; i < 21180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21181(int& val) {
    func_in_21181(val);
    const int x = 21181;
    std::cout << 21181;
    for (int i = 0; i < 21181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21181(int& val) {
    const int x = 21181;
    std::cout << 21181;
    for (int i = 0; i < 21181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21182(int& val) {
    func_in_21182(val);
    const int x = 21182;
    std::cout << 21182;
    for (int i = 0; i < 21182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21182(int& val) {
    const int x = 21182;
    std::cout << 21182;
    for (int i = 0; i < 21182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21183(int& val) {
    func_in_21183(val);
    const int x = 21183;
    std::cout << 21183;
    for (int i = 0; i < 21183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21183(int& val) {
    const int x = 21183;
    std::cout << 21183;
    for (int i = 0; i < 21183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21184(int& val) {
    func_in_21184(val);
    const int x = 21184;
    std::cout << 21184;
    for (int i = 0; i < 21184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21184(int& val) {
    const int x = 21184;
    std::cout << 21184;
    for (int i = 0; i < 21184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21185(int& val) {
    func_in_21185(val);
    const int x = 21185;
    std::cout << 21185;
    for (int i = 0; i < 21185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21185(int& val) {
    const int x = 21185;
    std::cout << 21185;
    for (int i = 0; i < 21185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21186(int& val) {
    func_in_21186(val);
    const int x = 21186;
    std::cout << 21186;
    for (int i = 0; i < 21186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21186(int& val) {
    const int x = 21186;
    std::cout << 21186;
    for (int i = 0; i < 21186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21187(int& val) {
    func_in_21187(val);
    const int x = 21187;
    std::cout << 21187;
    for (int i = 0; i < 21187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21187(int& val) {
    const int x = 21187;
    std::cout << 21187;
    for (int i = 0; i < 21187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21188(int& val) {
    func_in_21188(val);
    const int x = 21188;
    std::cout << 21188;
    for (int i = 0; i < 21188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21188(int& val) {
    const int x = 21188;
    std::cout << 21188;
    for (int i = 0; i < 21188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21189(int& val) {
    func_in_21189(val);
    const int x = 21189;
    std::cout << 21189;
    for (int i = 0; i < 21189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21189(int& val) {
    const int x = 21189;
    std::cout << 21189;
    for (int i = 0; i < 21189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21190(int& val) {
    func_in_21190(val);
    const int x = 21190;
    std::cout << 21190;
    for (int i = 0; i < 21190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21190(int& val) {
    const int x = 21190;
    std::cout << 21190;
    for (int i = 0; i < 21190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21191(int& val) {
    func_in_21191(val);
    const int x = 21191;
    std::cout << 21191;
    for (int i = 0; i < 21191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21191(int& val) {
    const int x = 21191;
    std::cout << 21191;
    for (int i = 0; i < 21191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21192(int& val) {
    func_in_21192(val);
    const int x = 21192;
    std::cout << 21192;
    for (int i = 0; i < 21192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21192(int& val) {
    const int x = 21192;
    std::cout << 21192;
    for (int i = 0; i < 21192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21193(int& val) {
    func_in_21193(val);
    const int x = 21193;
    std::cout << 21193;
    for (int i = 0; i < 21193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21193(int& val) {
    const int x = 21193;
    std::cout << 21193;
    for (int i = 0; i < 21193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21194(int& val) {
    func_in_21194(val);
    const int x = 21194;
    std::cout << 21194;
    for (int i = 0; i < 21194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21194(int& val) {
    const int x = 21194;
    std::cout << 21194;
    for (int i = 0; i < 21194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21195(int& val) {
    func_in_21195(val);
    const int x = 21195;
    std::cout << 21195;
    for (int i = 0; i < 21195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21195(int& val) {
    const int x = 21195;
    std::cout << 21195;
    for (int i = 0; i < 21195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21196(int& val) {
    func_in_21196(val);
    const int x = 21196;
    std::cout << 21196;
    for (int i = 0; i < 21196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21196(int& val) {
    const int x = 21196;
    std::cout << 21196;
    for (int i = 0; i < 21196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21197(int& val) {
    func_in_21197(val);
    const int x = 21197;
    std::cout << 21197;
    for (int i = 0; i < 21197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21197(int& val) {
    const int x = 21197;
    std::cout << 21197;
    for (int i = 0; i < 21197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21198(int& val) {
    func_in_21198(val);
    const int x = 21198;
    std::cout << 21198;
    for (int i = 0; i < 21198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21198(int& val) {
    const int x = 21198;
    std::cout << 21198;
    for (int i = 0; i < 21198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21199(int& val) {
    func_in_21199(val);
    const int x = 21199;
    std::cout << 21199;
    for (int i = 0; i < 21199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21199(int& val) {
    const int x = 21199;
    std::cout << 21199;
    for (int i = 0; i < 21199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21200(int& val) {
    func_in_21200(val);
    const int x = 21200;
    std::cout << 21200;
    for (int i = 0; i < 21200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21200(int& val) {
    const int x = 21200;
    std::cout << 21200;
    for (int i = 0; i < 21200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21201(int& val) {
    func_in_21201(val);
    const int x = 21201;
    std::cout << 21201;
    for (int i = 0; i < 21201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21201(int& val) {
    const int x = 21201;
    std::cout << 21201;
    for (int i = 0; i < 21201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21202(int& val) {
    func_in_21202(val);
    const int x = 21202;
    std::cout << 21202;
    for (int i = 0; i < 21202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21202(int& val) {
    const int x = 21202;
    std::cout << 21202;
    for (int i = 0; i < 21202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21203(int& val) {
    func_in_21203(val);
    const int x = 21203;
    std::cout << 21203;
    for (int i = 0; i < 21203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21203(int& val) {
    const int x = 21203;
    std::cout << 21203;
    for (int i = 0; i < 21203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21204(int& val) {
    func_in_21204(val);
    const int x = 21204;
    std::cout << 21204;
    for (int i = 0; i < 21204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21204(int& val) {
    const int x = 21204;
    std::cout << 21204;
    for (int i = 0; i < 21204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21205(int& val) {
    func_in_21205(val);
    const int x = 21205;
    std::cout << 21205;
    for (int i = 0; i < 21205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21205(int& val) {
    const int x = 21205;
    std::cout << 21205;
    for (int i = 0; i < 21205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21206(int& val) {
    func_in_21206(val);
    const int x = 21206;
    std::cout << 21206;
    for (int i = 0; i < 21206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21206(int& val) {
    const int x = 21206;
    std::cout << 21206;
    for (int i = 0; i < 21206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21207(int& val) {
    func_in_21207(val);
    const int x = 21207;
    std::cout << 21207;
    for (int i = 0; i < 21207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21207(int& val) {
    const int x = 21207;
    std::cout << 21207;
    for (int i = 0; i < 21207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21208(int& val) {
    func_in_21208(val);
    const int x = 21208;
    std::cout << 21208;
    for (int i = 0; i < 21208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21208(int& val) {
    const int x = 21208;
    std::cout << 21208;
    for (int i = 0; i < 21208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21209(int& val) {
    func_in_21209(val);
    const int x = 21209;
    std::cout << 21209;
    for (int i = 0; i < 21209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21209(int& val) {
    const int x = 21209;
    std::cout << 21209;
    for (int i = 0; i < 21209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21210(int& val) {
    func_in_21210(val);
    const int x = 21210;
    std::cout << 21210;
    for (int i = 0; i < 21210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21210(int& val) {
    const int x = 21210;
    std::cout << 21210;
    for (int i = 0; i < 21210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21211(int& val) {
    func_in_21211(val);
    const int x = 21211;
    std::cout << 21211;
    for (int i = 0; i < 21211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21211(int& val) {
    const int x = 21211;
    std::cout << 21211;
    for (int i = 0; i < 21211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21212(int& val) {
    func_in_21212(val);
    const int x = 21212;
    std::cout << 21212;
    for (int i = 0; i < 21212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21212(int& val) {
    const int x = 21212;
    std::cout << 21212;
    for (int i = 0; i < 21212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21213(int& val) {
    func_in_21213(val);
    const int x = 21213;
    std::cout << 21213;
    for (int i = 0; i < 21213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21213(int& val) {
    const int x = 21213;
    std::cout << 21213;
    for (int i = 0; i < 21213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21214(int& val) {
    func_in_21214(val);
    const int x = 21214;
    std::cout << 21214;
    for (int i = 0; i < 21214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21214(int& val) {
    const int x = 21214;
    std::cout << 21214;
    for (int i = 0; i < 21214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21215(int& val) {
    func_in_21215(val);
    const int x = 21215;
    std::cout << 21215;
    for (int i = 0; i < 21215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21215(int& val) {
    const int x = 21215;
    std::cout << 21215;
    for (int i = 0; i < 21215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21216(int& val) {
    func_in_21216(val);
    const int x = 21216;
    std::cout << 21216;
    for (int i = 0; i < 21216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21216(int& val) {
    const int x = 21216;
    std::cout << 21216;
    for (int i = 0; i < 21216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21217(int& val) {
    func_in_21217(val);
    const int x = 21217;
    std::cout << 21217;
    for (int i = 0; i < 21217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21217(int& val) {
    const int x = 21217;
    std::cout << 21217;
    for (int i = 0; i < 21217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21218(int& val) {
    func_in_21218(val);
    const int x = 21218;
    std::cout << 21218;
    for (int i = 0; i < 21218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21218(int& val) {
    const int x = 21218;
    std::cout << 21218;
    for (int i = 0; i < 21218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21219(int& val) {
    func_in_21219(val);
    const int x = 21219;
    std::cout << 21219;
    for (int i = 0; i < 21219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21219(int& val) {
    const int x = 21219;
    std::cout << 21219;
    for (int i = 0; i < 21219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21220(int& val) {
    func_in_21220(val);
    const int x = 21220;
    std::cout << 21220;
    for (int i = 0; i < 21220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21220(int& val) {
    const int x = 21220;
    std::cout << 21220;
    for (int i = 0; i < 21220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21221(int& val) {
    func_in_21221(val);
    const int x = 21221;
    std::cout << 21221;
    for (int i = 0; i < 21221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21221(int& val) {
    const int x = 21221;
    std::cout << 21221;
    for (int i = 0; i < 21221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21222(int& val) {
    func_in_21222(val);
    const int x = 21222;
    std::cout << 21222;
    for (int i = 0; i < 21222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21222(int& val) {
    const int x = 21222;
    std::cout << 21222;
    for (int i = 0; i < 21222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21223(int& val) {
    func_in_21223(val);
    const int x = 21223;
    std::cout << 21223;
    for (int i = 0; i < 21223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21223(int& val) {
    const int x = 21223;
    std::cout << 21223;
    for (int i = 0; i < 21223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21224(int& val) {
    func_in_21224(val);
    const int x = 21224;
    std::cout << 21224;
    for (int i = 0; i < 21224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21224(int& val) {
    const int x = 21224;
    std::cout << 21224;
    for (int i = 0; i < 21224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21225(int& val) {
    func_in_21225(val);
    const int x = 21225;
    std::cout << 21225;
    for (int i = 0; i < 21225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21225(int& val) {
    const int x = 21225;
    std::cout << 21225;
    for (int i = 0; i < 21225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21226(int& val) {
    func_in_21226(val);
    const int x = 21226;
    std::cout << 21226;
    for (int i = 0; i < 21226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21226(int& val) {
    const int x = 21226;
    std::cout << 21226;
    for (int i = 0; i < 21226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21227(int& val) {
    func_in_21227(val);
    const int x = 21227;
    std::cout << 21227;
    for (int i = 0; i < 21227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21227(int& val) {
    const int x = 21227;
    std::cout << 21227;
    for (int i = 0; i < 21227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21228(int& val) {
    func_in_21228(val);
    const int x = 21228;
    std::cout << 21228;
    for (int i = 0; i < 21228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21228(int& val) {
    const int x = 21228;
    std::cout << 21228;
    for (int i = 0; i < 21228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21229(int& val) {
    func_in_21229(val);
    const int x = 21229;
    std::cout << 21229;
    for (int i = 0; i < 21229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21229(int& val) {
    const int x = 21229;
    std::cout << 21229;
    for (int i = 0; i < 21229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21230(int& val) {
    func_in_21230(val);
    const int x = 21230;
    std::cout << 21230;
    for (int i = 0; i < 21230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21230(int& val) {
    const int x = 21230;
    std::cout << 21230;
    for (int i = 0; i < 21230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21231(int& val) {
    func_in_21231(val);
    const int x = 21231;
    std::cout << 21231;
    for (int i = 0; i < 21231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21231(int& val) {
    const int x = 21231;
    std::cout << 21231;
    for (int i = 0; i < 21231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21232(int& val) {
    func_in_21232(val);
    const int x = 21232;
    std::cout << 21232;
    for (int i = 0; i < 21232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21232(int& val) {
    const int x = 21232;
    std::cout << 21232;
    for (int i = 0; i < 21232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21233(int& val) {
    func_in_21233(val);
    const int x = 21233;
    std::cout << 21233;
    for (int i = 0; i < 21233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21233(int& val) {
    const int x = 21233;
    std::cout << 21233;
    for (int i = 0; i < 21233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21234(int& val) {
    func_in_21234(val);
    const int x = 21234;
    std::cout << 21234;
    for (int i = 0; i < 21234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21234(int& val) {
    const int x = 21234;
    std::cout << 21234;
    for (int i = 0; i < 21234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21235(int& val) {
    func_in_21235(val);
    const int x = 21235;
    std::cout << 21235;
    for (int i = 0; i < 21235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21235(int& val) {
    const int x = 21235;
    std::cout << 21235;
    for (int i = 0; i < 21235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21236(int& val) {
    func_in_21236(val);
    const int x = 21236;
    std::cout << 21236;
    for (int i = 0; i < 21236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21236(int& val) {
    const int x = 21236;
    std::cout << 21236;
    for (int i = 0; i < 21236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21237(int& val) {
    func_in_21237(val);
    const int x = 21237;
    std::cout << 21237;
    for (int i = 0; i < 21237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21237(int& val) {
    const int x = 21237;
    std::cout << 21237;
    for (int i = 0; i < 21237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21238(int& val) {
    func_in_21238(val);
    const int x = 21238;
    std::cout << 21238;
    for (int i = 0; i < 21238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21238(int& val) {
    const int x = 21238;
    std::cout << 21238;
    for (int i = 0; i < 21238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21239(int& val) {
    func_in_21239(val);
    const int x = 21239;
    std::cout << 21239;
    for (int i = 0; i < 21239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21239(int& val) {
    const int x = 21239;
    std::cout << 21239;
    for (int i = 0; i < 21239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21240(int& val) {
    func_in_21240(val);
    const int x = 21240;
    std::cout << 21240;
    for (int i = 0; i < 21240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21240(int& val) {
    const int x = 21240;
    std::cout << 21240;
    for (int i = 0; i < 21240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21241(int& val) {
    func_in_21241(val);
    const int x = 21241;
    std::cout << 21241;
    for (int i = 0; i < 21241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21241(int& val) {
    const int x = 21241;
    std::cout << 21241;
    for (int i = 0; i < 21241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21242(int& val) {
    func_in_21242(val);
    const int x = 21242;
    std::cout << 21242;
    for (int i = 0; i < 21242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21242(int& val) {
    const int x = 21242;
    std::cout << 21242;
    for (int i = 0; i < 21242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21243(int& val) {
    func_in_21243(val);
    const int x = 21243;
    std::cout << 21243;
    for (int i = 0; i < 21243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21243(int& val) {
    const int x = 21243;
    std::cout << 21243;
    for (int i = 0; i < 21243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21244(int& val) {
    func_in_21244(val);
    const int x = 21244;
    std::cout << 21244;
    for (int i = 0; i < 21244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21244(int& val) {
    const int x = 21244;
    std::cout << 21244;
    for (int i = 0; i < 21244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21245(int& val) {
    func_in_21245(val);
    const int x = 21245;
    std::cout << 21245;
    for (int i = 0; i < 21245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21245(int& val) {
    const int x = 21245;
    std::cout << 21245;
    for (int i = 0; i < 21245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21246(int& val) {
    func_in_21246(val);
    const int x = 21246;
    std::cout << 21246;
    for (int i = 0; i < 21246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21246(int& val) {
    const int x = 21246;
    std::cout << 21246;
    for (int i = 0; i < 21246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21247(int& val) {
    func_in_21247(val);
    const int x = 21247;
    std::cout << 21247;
    for (int i = 0; i < 21247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21247(int& val) {
    const int x = 21247;
    std::cout << 21247;
    for (int i = 0; i < 21247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21248(int& val) {
    func_in_21248(val);
    const int x = 21248;
    std::cout << 21248;
    for (int i = 0; i < 21248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21248(int& val) {
    const int x = 21248;
    std::cout << 21248;
    for (int i = 0; i < 21248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21249(int& val) {
    func_in_21249(val);
    const int x = 21249;
    std::cout << 21249;
    for (int i = 0; i < 21249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21249(int& val) {
    const int x = 21249;
    std::cout << 21249;
    for (int i = 0; i < 21249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21250(int& val) {
    func_in_21250(val);
    const int x = 21250;
    std::cout << 21250;
    for (int i = 0; i < 21250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21250(int& val) {
    const int x = 21250;
    std::cout << 21250;
    for (int i = 0; i < 21250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21251(int& val) {
    func_in_21251(val);
    const int x = 21251;
    std::cout << 21251;
    for (int i = 0; i < 21251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21251(int& val) {
    const int x = 21251;
    std::cout << 21251;
    for (int i = 0; i < 21251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21252(int& val) {
    func_in_21252(val);
    const int x = 21252;
    std::cout << 21252;
    for (int i = 0; i < 21252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21252(int& val) {
    const int x = 21252;
    std::cout << 21252;
    for (int i = 0; i < 21252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21253(int& val) {
    func_in_21253(val);
    const int x = 21253;
    std::cout << 21253;
    for (int i = 0; i < 21253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21253(int& val) {
    const int x = 21253;
    std::cout << 21253;
    for (int i = 0; i < 21253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21254(int& val) {
    func_in_21254(val);
    const int x = 21254;
    std::cout << 21254;
    for (int i = 0; i < 21254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21254(int& val) {
    const int x = 21254;
    std::cout << 21254;
    for (int i = 0; i < 21254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21255(int& val) {
    func_in_21255(val);
    const int x = 21255;
    std::cout << 21255;
    for (int i = 0; i < 21255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21255(int& val) {
    const int x = 21255;
    std::cout << 21255;
    for (int i = 0; i < 21255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21256(int& val) {
    func_in_21256(val);
    const int x = 21256;
    std::cout << 21256;
    for (int i = 0; i < 21256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21256(int& val) {
    const int x = 21256;
    std::cout << 21256;
    for (int i = 0; i < 21256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21257(int& val) {
    func_in_21257(val);
    const int x = 21257;
    std::cout << 21257;
    for (int i = 0; i < 21257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21257(int& val) {
    const int x = 21257;
    std::cout << 21257;
    for (int i = 0; i < 21257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21258(int& val) {
    func_in_21258(val);
    const int x = 21258;
    std::cout << 21258;
    for (int i = 0; i < 21258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21258(int& val) {
    const int x = 21258;
    std::cout << 21258;
    for (int i = 0; i < 21258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21259(int& val) {
    func_in_21259(val);
    const int x = 21259;
    std::cout << 21259;
    for (int i = 0; i < 21259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21259(int& val) {
    const int x = 21259;
    std::cout << 21259;
    for (int i = 0; i < 21259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21260(int& val) {
    func_in_21260(val);
    const int x = 21260;
    std::cout << 21260;
    for (int i = 0; i < 21260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21260(int& val) {
    const int x = 21260;
    std::cout << 21260;
    for (int i = 0; i < 21260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21261(int& val) {
    func_in_21261(val);
    const int x = 21261;
    std::cout << 21261;
    for (int i = 0; i < 21261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21261(int& val) {
    const int x = 21261;
    std::cout << 21261;
    for (int i = 0; i < 21261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21262(int& val) {
    func_in_21262(val);
    const int x = 21262;
    std::cout << 21262;
    for (int i = 0; i < 21262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21262(int& val) {
    const int x = 21262;
    std::cout << 21262;
    for (int i = 0; i < 21262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21263(int& val) {
    func_in_21263(val);
    const int x = 21263;
    std::cout << 21263;
    for (int i = 0; i < 21263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21263(int& val) {
    const int x = 21263;
    std::cout << 21263;
    for (int i = 0; i < 21263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21264(int& val) {
    func_in_21264(val);
    const int x = 21264;
    std::cout << 21264;
    for (int i = 0; i < 21264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21264(int& val) {
    const int x = 21264;
    std::cout << 21264;
    for (int i = 0; i < 21264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21265(int& val) {
    func_in_21265(val);
    const int x = 21265;
    std::cout << 21265;
    for (int i = 0; i < 21265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21265(int& val) {
    const int x = 21265;
    std::cout << 21265;
    for (int i = 0; i < 21265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21266(int& val) {
    func_in_21266(val);
    const int x = 21266;
    std::cout << 21266;
    for (int i = 0; i < 21266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21266(int& val) {
    const int x = 21266;
    std::cout << 21266;
    for (int i = 0; i < 21266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21267(int& val) {
    func_in_21267(val);
    const int x = 21267;
    std::cout << 21267;
    for (int i = 0; i < 21267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21267(int& val) {
    const int x = 21267;
    std::cout << 21267;
    for (int i = 0; i < 21267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21268(int& val) {
    func_in_21268(val);
    const int x = 21268;
    std::cout << 21268;
    for (int i = 0; i < 21268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21268(int& val) {
    const int x = 21268;
    std::cout << 21268;
    for (int i = 0; i < 21268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21269(int& val) {
    func_in_21269(val);
    const int x = 21269;
    std::cout << 21269;
    for (int i = 0; i < 21269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21269(int& val) {
    const int x = 21269;
    std::cout << 21269;
    for (int i = 0; i < 21269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21270(int& val) {
    func_in_21270(val);
    const int x = 21270;
    std::cout << 21270;
    for (int i = 0; i < 21270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21270(int& val) {
    const int x = 21270;
    std::cout << 21270;
    for (int i = 0; i < 21270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21271(int& val) {
    func_in_21271(val);
    const int x = 21271;
    std::cout << 21271;
    for (int i = 0; i < 21271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21271(int& val) {
    const int x = 21271;
    std::cout << 21271;
    for (int i = 0; i < 21271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21272(int& val) {
    func_in_21272(val);
    const int x = 21272;
    std::cout << 21272;
    for (int i = 0; i < 21272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21272(int& val) {
    const int x = 21272;
    std::cout << 21272;
    for (int i = 0; i < 21272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21273(int& val) {
    func_in_21273(val);
    const int x = 21273;
    std::cout << 21273;
    for (int i = 0; i < 21273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21273(int& val) {
    const int x = 21273;
    std::cout << 21273;
    for (int i = 0; i < 21273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21274(int& val) {
    func_in_21274(val);
    const int x = 21274;
    std::cout << 21274;
    for (int i = 0; i < 21274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21274(int& val) {
    const int x = 21274;
    std::cout << 21274;
    for (int i = 0; i < 21274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21275(int& val) {
    func_in_21275(val);
    const int x = 21275;
    std::cout << 21275;
    for (int i = 0; i < 21275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21275(int& val) {
    const int x = 21275;
    std::cout << 21275;
    for (int i = 0; i < 21275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21276(int& val) {
    func_in_21276(val);
    const int x = 21276;
    std::cout << 21276;
    for (int i = 0; i < 21276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21276(int& val) {
    const int x = 21276;
    std::cout << 21276;
    for (int i = 0; i < 21276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21277(int& val) {
    func_in_21277(val);
    const int x = 21277;
    std::cout << 21277;
    for (int i = 0; i < 21277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21277(int& val) {
    const int x = 21277;
    std::cout << 21277;
    for (int i = 0; i < 21277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21278(int& val) {
    func_in_21278(val);
    const int x = 21278;
    std::cout << 21278;
    for (int i = 0; i < 21278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21278(int& val) {
    const int x = 21278;
    std::cout << 21278;
    for (int i = 0; i < 21278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21279(int& val) {
    func_in_21279(val);
    const int x = 21279;
    std::cout << 21279;
    for (int i = 0; i < 21279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21279(int& val) {
    const int x = 21279;
    std::cout << 21279;
    for (int i = 0; i < 21279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21280(int& val) {
    func_in_21280(val);
    const int x = 21280;
    std::cout << 21280;
    for (int i = 0; i < 21280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21280(int& val) {
    const int x = 21280;
    std::cout << 21280;
    for (int i = 0; i < 21280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21281(int& val) {
    func_in_21281(val);
    const int x = 21281;
    std::cout << 21281;
    for (int i = 0; i < 21281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21281(int& val) {
    const int x = 21281;
    std::cout << 21281;
    for (int i = 0; i < 21281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21282(int& val) {
    func_in_21282(val);
    const int x = 21282;
    std::cout << 21282;
    for (int i = 0; i < 21282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21282(int& val) {
    const int x = 21282;
    std::cout << 21282;
    for (int i = 0; i < 21282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21283(int& val) {
    func_in_21283(val);
    const int x = 21283;
    std::cout << 21283;
    for (int i = 0; i < 21283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21283(int& val) {
    const int x = 21283;
    std::cout << 21283;
    for (int i = 0; i < 21283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21284(int& val) {
    func_in_21284(val);
    const int x = 21284;
    std::cout << 21284;
    for (int i = 0; i < 21284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21284(int& val) {
    const int x = 21284;
    std::cout << 21284;
    for (int i = 0; i < 21284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21285(int& val) {
    func_in_21285(val);
    const int x = 21285;
    std::cout << 21285;
    for (int i = 0; i < 21285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21285(int& val) {
    const int x = 21285;
    std::cout << 21285;
    for (int i = 0; i < 21285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21286(int& val) {
    func_in_21286(val);
    const int x = 21286;
    std::cout << 21286;
    for (int i = 0; i < 21286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21286(int& val) {
    const int x = 21286;
    std::cout << 21286;
    for (int i = 0; i < 21286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21287(int& val) {
    func_in_21287(val);
    const int x = 21287;
    std::cout << 21287;
    for (int i = 0; i < 21287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21287(int& val) {
    const int x = 21287;
    std::cout << 21287;
    for (int i = 0; i < 21287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21288(int& val) {
    func_in_21288(val);
    const int x = 21288;
    std::cout << 21288;
    for (int i = 0; i < 21288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21288(int& val) {
    const int x = 21288;
    std::cout << 21288;
    for (int i = 0; i < 21288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21289(int& val) {
    func_in_21289(val);
    const int x = 21289;
    std::cout << 21289;
    for (int i = 0; i < 21289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21289(int& val) {
    const int x = 21289;
    std::cout << 21289;
    for (int i = 0; i < 21289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21290(int& val) {
    func_in_21290(val);
    const int x = 21290;
    std::cout << 21290;
    for (int i = 0; i < 21290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21290(int& val) {
    const int x = 21290;
    std::cout << 21290;
    for (int i = 0; i < 21290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21291(int& val) {
    func_in_21291(val);
    const int x = 21291;
    std::cout << 21291;
    for (int i = 0; i < 21291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21291(int& val) {
    const int x = 21291;
    std::cout << 21291;
    for (int i = 0; i < 21291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21292(int& val) {
    func_in_21292(val);
    const int x = 21292;
    std::cout << 21292;
    for (int i = 0; i < 21292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21292(int& val) {
    const int x = 21292;
    std::cout << 21292;
    for (int i = 0; i < 21292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21293(int& val) {
    func_in_21293(val);
    const int x = 21293;
    std::cout << 21293;
    for (int i = 0; i < 21293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21293(int& val) {
    const int x = 21293;
    std::cout << 21293;
    for (int i = 0; i < 21293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21294(int& val) {
    func_in_21294(val);
    const int x = 21294;
    std::cout << 21294;
    for (int i = 0; i < 21294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21294(int& val) {
    const int x = 21294;
    std::cout << 21294;
    for (int i = 0; i < 21294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21295(int& val) {
    func_in_21295(val);
    const int x = 21295;
    std::cout << 21295;
    for (int i = 0; i < 21295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21295(int& val) {
    const int x = 21295;
    std::cout << 21295;
    for (int i = 0; i < 21295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21296(int& val) {
    func_in_21296(val);
    const int x = 21296;
    std::cout << 21296;
    for (int i = 0; i < 21296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21296(int& val) {
    const int x = 21296;
    std::cout << 21296;
    for (int i = 0; i < 21296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21297(int& val) {
    func_in_21297(val);
    const int x = 21297;
    std::cout << 21297;
    for (int i = 0; i < 21297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21297(int& val) {
    const int x = 21297;
    std::cout << 21297;
    for (int i = 0; i < 21297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21298(int& val) {
    func_in_21298(val);
    const int x = 21298;
    std::cout << 21298;
    for (int i = 0; i < 21298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21298(int& val) {
    const int x = 21298;
    std::cout << 21298;
    for (int i = 0; i < 21298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21299(int& val) {
    func_in_21299(val);
    const int x = 21299;
    std::cout << 21299;
    for (int i = 0; i < 21299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21299(int& val) {
    const int x = 21299;
    std::cout << 21299;
    for (int i = 0; i < 21299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21300(int& val) {
    func_in_21300(val);
    const int x = 21300;
    std::cout << 21300;
    for (int i = 0; i < 21300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21300(int& val) {
    const int x = 21300;
    std::cout << 21300;
    for (int i = 0; i < 21300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21301(int& val) {
    func_in_21301(val);
    const int x = 21301;
    std::cout << 21301;
    for (int i = 0; i < 21301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21301(int& val) {
    const int x = 21301;
    std::cout << 21301;
    for (int i = 0; i < 21301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21302(int& val) {
    func_in_21302(val);
    const int x = 21302;
    std::cout << 21302;
    for (int i = 0; i < 21302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21302(int& val) {
    const int x = 21302;
    std::cout << 21302;
    for (int i = 0; i < 21302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21303(int& val) {
    func_in_21303(val);
    const int x = 21303;
    std::cout << 21303;
    for (int i = 0; i < 21303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21303(int& val) {
    const int x = 21303;
    std::cout << 21303;
    for (int i = 0; i < 21303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21304(int& val) {
    func_in_21304(val);
    const int x = 21304;
    std::cout << 21304;
    for (int i = 0; i < 21304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21304(int& val) {
    const int x = 21304;
    std::cout << 21304;
    for (int i = 0; i < 21304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21305(int& val) {
    func_in_21305(val);
    const int x = 21305;
    std::cout << 21305;
    for (int i = 0; i < 21305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21305(int& val) {
    const int x = 21305;
    std::cout << 21305;
    for (int i = 0; i < 21305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21306(int& val) {
    func_in_21306(val);
    const int x = 21306;
    std::cout << 21306;
    for (int i = 0; i < 21306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21306(int& val) {
    const int x = 21306;
    std::cout << 21306;
    for (int i = 0; i < 21306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21307(int& val) {
    func_in_21307(val);
    const int x = 21307;
    std::cout << 21307;
    for (int i = 0; i < 21307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21307(int& val) {
    const int x = 21307;
    std::cout << 21307;
    for (int i = 0; i < 21307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21308(int& val) {
    func_in_21308(val);
    const int x = 21308;
    std::cout << 21308;
    for (int i = 0; i < 21308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21308(int& val) {
    const int x = 21308;
    std::cout << 21308;
    for (int i = 0; i < 21308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21309(int& val) {
    func_in_21309(val);
    const int x = 21309;
    std::cout << 21309;
    for (int i = 0; i < 21309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21309(int& val) {
    const int x = 21309;
    std::cout << 21309;
    for (int i = 0; i < 21309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21310(int& val) {
    func_in_21310(val);
    const int x = 21310;
    std::cout << 21310;
    for (int i = 0; i < 21310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21310(int& val) {
    const int x = 21310;
    std::cout << 21310;
    for (int i = 0; i < 21310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21311(int& val) {
    func_in_21311(val);
    const int x = 21311;
    std::cout << 21311;
    for (int i = 0; i < 21311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21311(int& val) {
    const int x = 21311;
    std::cout << 21311;
    for (int i = 0; i < 21311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21312(int& val) {
    func_in_21312(val);
    const int x = 21312;
    std::cout << 21312;
    for (int i = 0; i < 21312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21312(int& val) {
    const int x = 21312;
    std::cout << 21312;
    for (int i = 0; i < 21312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21313(int& val) {
    func_in_21313(val);
    const int x = 21313;
    std::cout << 21313;
    for (int i = 0; i < 21313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21313(int& val) {
    const int x = 21313;
    std::cout << 21313;
    for (int i = 0; i < 21313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21314(int& val) {
    func_in_21314(val);
    const int x = 21314;
    std::cout << 21314;
    for (int i = 0; i < 21314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21314(int& val) {
    const int x = 21314;
    std::cout << 21314;
    for (int i = 0; i < 21314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21315(int& val) {
    func_in_21315(val);
    const int x = 21315;
    std::cout << 21315;
    for (int i = 0; i < 21315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21315(int& val) {
    const int x = 21315;
    std::cout << 21315;
    for (int i = 0; i < 21315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21316(int& val) {
    func_in_21316(val);
    const int x = 21316;
    std::cout << 21316;
    for (int i = 0; i < 21316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21316(int& val) {
    const int x = 21316;
    std::cout << 21316;
    for (int i = 0; i < 21316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21317(int& val) {
    func_in_21317(val);
    const int x = 21317;
    std::cout << 21317;
    for (int i = 0; i < 21317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21317(int& val) {
    const int x = 21317;
    std::cout << 21317;
    for (int i = 0; i < 21317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21318(int& val) {
    func_in_21318(val);
    const int x = 21318;
    std::cout << 21318;
    for (int i = 0; i < 21318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21318(int& val) {
    const int x = 21318;
    std::cout << 21318;
    for (int i = 0; i < 21318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21319(int& val) {
    func_in_21319(val);
    const int x = 21319;
    std::cout << 21319;
    for (int i = 0; i < 21319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21319(int& val) {
    const int x = 21319;
    std::cout << 21319;
    for (int i = 0; i < 21319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21320(int& val) {
    func_in_21320(val);
    const int x = 21320;
    std::cout << 21320;
    for (int i = 0; i < 21320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21320(int& val) {
    const int x = 21320;
    std::cout << 21320;
    for (int i = 0; i < 21320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21321(int& val) {
    func_in_21321(val);
    const int x = 21321;
    std::cout << 21321;
    for (int i = 0; i < 21321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21321(int& val) {
    const int x = 21321;
    std::cout << 21321;
    for (int i = 0; i < 21321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21322(int& val) {
    func_in_21322(val);
    const int x = 21322;
    std::cout << 21322;
    for (int i = 0; i < 21322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21322(int& val) {
    const int x = 21322;
    std::cout << 21322;
    for (int i = 0; i < 21322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21323(int& val) {
    func_in_21323(val);
    const int x = 21323;
    std::cout << 21323;
    for (int i = 0; i < 21323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21323(int& val) {
    const int x = 21323;
    std::cout << 21323;
    for (int i = 0; i < 21323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21324(int& val) {
    func_in_21324(val);
    const int x = 21324;
    std::cout << 21324;
    for (int i = 0; i < 21324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21324(int& val) {
    const int x = 21324;
    std::cout << 21324;
    for (int i = 0; i < 21324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21325(int& val) {
    func_in_21325(val);
    const int x = 21325;
    std::cout << 21325;
    for (int i = 0; i < 21325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21325(int& val) {
    const int x = 21325;
    std::cout << 21325;
    for (int i = 0; i < 21325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21326(int& val) {
    func_in_21326(val);
    const int x = 21326;
    std::cout << 21326;
    for (int i = 0; i < 21326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21326(int& val) {
    const int x = 21326;
    std::cout << 21326;
    for (int i = 0; i < 21326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21327(int& val) {
    func_in_21327(val);
    const int x = 21327;
    std::cout << 21327;
    for (int i = 0; i < 21327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21327(int& val) {
    const int x = 21327;
    std::cout << 21327;
    for (int i = 0; i < 21327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21328(int& val) {
    func_in_21328(val);
    const int x = 21328;
    std::cout << 21328;
    for (int i = 0; i < 21328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21328(int& val) {
    const int x = 21328;
    std::cout << 21328;
    for (int i = 0; i < 21328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21329(int& val) {
    func_in_21329(val);
    const int x = 21329;
    std::cout << 21329;
    for (int i = 0; i < 21329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21329(int& val) {
    const int x = 21329;
    std::cout << 21329;
    for (int i = 0; i < 21329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21330(int& val) {
    func_in_21330(val);
    const int x = 21330;
    std::cout << 21330;
    for (int i = 0; i < 21330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21330(int& val) {
    const int x = 21330;
    std::cout << 21330;
    for (int i = 0; i < 21330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21331(int& val) {
    func_in_21331(val);
    const int x = 21331;
    std::cout << 21331;
    for (int i = 0; i < 21331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21331(int& val) {
    const int x = 21331;
    std::cout << 21331;
    for (int i = 0; i < 21331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21332(int& val) {
    func_in_21332(val);
    const int x = 21332;
    std::cout << 21332;
    for (int i = 0; i < 21332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21332(int& val) {
    const int x = 21332;
    std::cout << 21332;
    for (int i = 0; i < 21332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21333(int& val) {
    func_in_21333(val);
    const int x = 21333;
    std::cout << 21333;
    for (int i = 0; i < 21333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21333(int& val) {
    const int x = 21333;
    std::cout << 21333;
    for (int i = 0; i < 21333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21334(int& val) {
    func_in_21334(val);
    const int x = 21334;
    std::cout << 21334;
    for (int i = 0; i < 21334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21334(int& val) {
    const int x = 21334;
    std::cout << 21334;
    for (int i = 0; i < 21334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21335(int& val) {
    func_in_21335(val);
    const int x = 21335;
    std::cout << 21335;
    for (int i = 0; i < 21335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21335(int& val) {
    const int x = 21335;
    std::cout << 21335;
    for (int i = 0; i < 21335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21336(int& val) {
    func_in_21336(val);
    const int x = 21336;
    std::cout << 21336;
    for (int i = 0; i < 21336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21336(int& val) {
    const int x = 21336;
    std::cout << 21336;
    for (int i = 0; i < 21336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21337(int& val) {
    func_in_21337(val);
    const int x = 21337;
    std::cout << 21337;
    for (int i = 0; i < 21337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21337(int& val) {
    const int x = 21337;
    std::cout << 21337;
    for (int i = 0; i < 21337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21338(int& val) {
    func_in_21338(val);
    const int x = 21338;
    std::cout << 21338;
    for (int i = 0; i < 21338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21338(int& val) {
    const int x = 21338;
    std::cout << 21338;
    for (int i = 0; i < 21338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21339(int& val) {
    func_in_21339(val);
    const int x = 21339;
    std::cout << 21339;
    for (int i = 0; i < 21339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21339(int& val) {
    const int x = 21339;
    std::cout << 21339;
    for (int i = 0; i < 21339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21340(int& val) {
    func_in_21340(val);
    const int x = 21340;
    std::cout << 21340;
    for (int i = 0; i < 21340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21340(int& val) {
    const int x = 21340;
    std::cout << 21340;
    for (int i = 0; i < 21340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21341(int& val) {
    func_in_21341(val);
    const int x = 21341;
    std::cout << 21341;
    for (int i = 0; i < 21341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21341(int& val) {
    const int x = 21341;
    std::cout << 21341;
    for (int i = 0; i < 21341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21342(int& val) {
    func_in_21342(val);
    const int x = 21342;
    std::cout << 21342;
    for (int i = 0; i < 21342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21342(int& val) {
    const int x = 21342;
    std::cout << 21342;
    for (int i = 0; i < 21342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21343(int& val) {
    func_in_21343(val);
    const int x = 21343;
    std::cout << 21343;
    for (int i = 0; i < 21343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21343(int& val) {
    const int x = 21343;
    std::cout << 21343;
    for (int i = 0; i < 21343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21344(int& val) {
    func_in_21344(val);
    const int x = 21344;
    std::cout << 21344;
    for (int i = 0; i < 21344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21344(int& val) {
    const int x = 21344;
    std::cout << 21344;
    for (int i = 0; i < 21344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21345(int& val) {
    func_in_21345(val);
    const int x = 21345;
    std::cout << 21345;
    for (int i = 0; i < 21345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21345(int& val) {
    const int x = 21345;
    std::cout << 21345;
    for (int i = 0; i < 21345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21346(int& val) {
    func_in_21346(val);
    const int x = 21346;
    std::cout << 21346;
    for (int i = 0; i < 21346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21346(int& val) {
    const int x = 21346;
    std::cout << 21346;
    for (int i = 0; i < 21346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21347(int& val) {
    func_in_21347(val);
    const int x = 21347;
    std::cout << 21347;
    for (int i = 0; i < 21347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21347(int& val) {
    const int x = 21347;
    std::cout << 21347;
    for (int i = 0; i < 21347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21348(int& val) {
    func_in_21348(val);
    const int x = 21348;
    std::cout << 21348;
    for (int i = 0; i < 21348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21348(int& val) {
    const int x = 21348;
    std::cout << 21348;
    for (int i = 0; i < 21348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21349(int& val) {
    func_in_21349(val);
    const int x = 21349;
    std::cout << 21349;
    for (int i = 0; i < 21349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21349(int& val) {
    const int x = 21349;
    std::cout << 21349;
    for (int i = 0; i < 21349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21350(int& val) {
    func_in_21350(val);
    const int x = 21350;
    std::cout << 21350;
    for (int i = 0; i < 21350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21350(int& val) {
    const int x = 21350;
    std::cout << 21350;
    for (int i = 0; i < 21350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21351(int& val) {
    func_in_21351(val);
    const int x = 21351;
    std::cout << 21351;
    for (int i = 0; i < 21351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21351(int& val) {
    const int x = 21351;
    std::cout << 21351;
    for (int i = 0; i < 21351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21352(int& val) {
    func_in_21352(val);
    const int x = 21352;
    std::cout << 21352;
    for (int i = 0; i < 21352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21352(int& val) {
    const int x = 21352;
    std::cout << 21352;
    for (int i = 0; i < 21352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21353(int& val) {
    func_in_21353(val);
    const int x = 21353;
    std::cout << 21353;
    for (int i = 0; i < 21353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21353(int& val) {
    const int x = 21353;
    std::cout << 21353;
    for (int i = 0; i < 21353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21354(int& val) {
    func_in_21354(val);
    const int x = 21354;
    std::cout << 21354;
    for (int i = 0; i < 21354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21354(int& val) {
    const int x = 21354;
    std::cout << 21354;
    for (int i = 0; i < 21354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21355(int& val) {
    func_in_21355(val);
    const int x = 21355;
    std::cout << 21355;
    for (int i = 0; i < 21355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21355(int& val) {
    const int x = 21355;
    std::cout << 21355;
    for (int i = 0; i < 21355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21356(int& val) {
    func_in_21356(val);
    const int x = 21356;
    std::cout << 21356;
    for (int i = 0; i < 21356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21356(int& val) {
    const int x = 21356;
    std::cout << 21356;
    for (int i = 0; i < 21356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21357(int& val) {
    func_in_21357(val);
    const int x = 21357;
    std::cout << 21357;
    for (int i = 0; i < 21357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21357(int& val) {
    const int x = 21357;
    std::cout << 21357;
    for (int i = 0; i < 21357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21358(int& val) {
    func_in_21358(val);
    const int x = 21358;
    std::cout << 21358;
    for (int i = 0; i < 21358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21358(int& val) {
    const int x = 21358;
    std::cout << 21358;
    for (int i = 0; i < 21358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21359(int& val) {
    func_in_21359(val);
    const int x = 21359;
    std::cout << 21359;
    for (int i = 0; i < 21359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21359(int& val) {
    const int x = 21359;
    std::cout << 21359;
    for (int i = 0; i < 21359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21360(int& val) {
    func_in_21360(val);
    const int x = 21360;
    std::cout << 21360;
    for (int i = 0; i < 21360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21360(int& val) {
    const int x = 21360;
    std::cout << 21360;
    for (int i = 0; i < 21360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21361(int& val) {
    func_in_21361(val);
    const int x = 21361;
    std::cout << 21361;
    for (int i = 0; i < 21361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21361(int& val) {
    const int x = 21361;
    std::cout << 21361;
    for (int i = 0; i < 21361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21362(int& val) {
    func_in_21362(val);
    const int x = 21362;
    std::cout << 21362;
    for (int i = 0; i < 21362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21362(int& val) {
    const int x = 21362;
    std::cout << 21362;
    for (int i = 0; i < 21362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21363(int& val) {
    func_in_21363(val);
    const int x = 21363;
    std::cout << 21363;
    for (int i = 0; i < 21363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21363(int& val) {
    const int x = 21363;
    std::cout << 21363;
    for (int i = 0; i < 21363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21364(int& val) {
    func_in_21364(val);
    const int x = 21364;
    std::cout << 21364;
    for (int i = 0; i < 21364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21364(int& val) {
    const int x = 21364;
    std::cout << 21364;
    for (int i = 0; i < 21364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21365(int& val) {
    func_in_21365(val);
    const int x = 21365;
    std::cout << 21365;
    for (int i = 0; i < 21365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21365(int& val) {
    const int x = 21365;
    std::cout << 21365;
    for (int i = 0; i < 21365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21366(int& val) {
    func_in_21366(val);
    const int x = 21366;
    std::cout << 21366;
    for (int i = 0; i < 21366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21366(int& val) {
    const int x = 21366;
    std::cout << 21366;
    for (int i = 0; i < 21366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21367(int& val) {
    func_in_21367(val);
    const int x = 21367;
    std::cout << 21367;
    for (int i = 0; i < 21367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21367(int& val) {
    const int x = 21367;
    std::cout << 21367;
    for (int i = 0; i < 21367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21368(int& val) {
    func_in_21368(val);
    const int x = 21368;
    std::cout << 21368;
    for (int i = 0; i < 21368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21368(int& val) {
    const int x = 21368;
    std::cout << 21368;
    for (int i = 0; i < 21368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21369(int& val) {
    func_in_21369(val);
    const int x = 21369;
    std::cout << 21369;
    for (int i = 0; i < 21369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21369(int& val) {
    const int x = 21369;
    std::cout << 21369;
    for (int i = 0; i < 21369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21370(int& val) {
    func_in_21370(val);
    const int x = 21370;
    std::cout << 21370;
    for (int i = 0; i < 21370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21370(int& val) {
    const int x = 21370;
    std::cout << 21370;
    for (int i = 0; i < 21370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21371(int& val) {
    func_in_21371(val);
    const int x = 21371;
    std::cout << 21371;
    for (int i = 0; i < 21371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21371(int& val) {
    const int x = 21371;
    std::cout << 21371;
    for (int i = 0; i < 21371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21372(int& val) {
    func_in_21372(val);
    const int x = 21372;
    std::cout << 21372;
    for (int i = 0; i < 21372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21372(int& val) {
    const int x = 21372;
    std::cout << 21372;
    for (int i = 0; i < 21372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21373(int& val) {
    func_in_21373(val);
    const int x = 21373;
    std::cout << 21373;
    for (int i = 0; i < 21373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21373(int& val) {
    const int x = 21373;
    std::cout << 21373;
    for (int i = 0; i < 21373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21374(int& val) {
    func_in_21374(val);
    const int x = 21374;
    std::cout << 21374;
    for (int i = 0; i < 21374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21374(int& val) {
    const int x = 21374;
    std::cout << 21374;
    for (int i = 0; i < 21374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21375(int& val) {
    func_in_21375(val);
    const int x = 21375;
    std::cout << 21375;
    for (int i = 0; i < 21375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21375(int& val) {
    const int x = 21375;
    std::cout << 21375;
    for (int i = 0; i < 21375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21376(int& val) {
    func_in_21376(val);
    const int x = 21376;
    std::cout << 21376;
    for (int i = 0; i < 21376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21376(int& val) {
    const int x = 21376;
    std::cout << 21376;
    for (int i = 0; i < 21376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21377(int& val) {
    func_in_21377(val);
    const int x = 21377;
    std::cout << 21377;
    for (int i = 0; i < 21377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21377(int& val) {
    const int x = 21377;
    std::cout << 21377;
    for (int i = 0; i < 21377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21378(int& val) {
    func_in_21378(val);
    const int x = 21378;
    std::cout << 21378;
    for (int i = 0; i < 21378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21378(int& val) {
    const int x = 21378;
    std::cout << 21378;
    for (int i = 0; i < 21378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21379(int& val) {
    func_in_21379(val);
    const int x = 21379;
    std::cout << 21379;
    for (int i = 0; i < 21379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21379(int& val) {
    const int x = 21379;
    std::cout << 21379;
    for (int i = 0; i < 21379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21380(int& val) {
    func_in_21380(val);
    const int x = 21380;
    std::cout << 21380;
    for (int i = 0; i < 21380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21380(int& val) {
    const int x = 21380;
    std::cout << 21380;
    for (int i = 0; i < 21380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21381(int& val) {
    func_in_21381(val);
    const int x = 21381;
    std::cout << 21381;
    for (int i = 0; i < 21381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21381(int& val) {
    const int x = 21381;
    std::cout << 21381;
    for (int i = 0; i < 21381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21382(int& val) {
    func_in_21382(val);
    const int x = 21382;
    std::cout << 21382;
    for (int i = 0; i < 21382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21382(int& val) {
    const int x = 21382;
    std::cout << 21382;
    for (int i = 0; i < 21382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21383(int& val) {
    func_in_21383(val);
    const int x = 21383;
    std::cout << 21383;
    for (int i = 0; i < 21383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21383(int& val) {
    const int x = 21383;
    std::cout << 21383;
    for (int i = 0; i < 21383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21384(int& val) {
    func_in_21384(val);
    const int x = 21384;
    std::cout << 21384;
    for (int i = 0; i < 21384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21384(int& val) {
    const int x = 21384;
    std::cout << 21384;
    for (int i = 0; i < 21384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21385(int& val) {
    func_in_21385(val);
    const int x = 21385;
    std::cout << 21385;
    for (int i = 0; i < 21385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21385(int& val) {
    const int x = 21385;
    std::cout << 21385;
    for (int i = 0; i < 21385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21386(int& val) {
    func_in_21386(val);
    const int x = 21386;
    std::cout << 21386;
    for (int i = 0; i < 21386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21386(int& val) {
    const int x = 21386;
    std::cout << 21386;
    for (int i = 0; i < 21386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21387(int& val) {
    func_in_21387(val);
    const int x = 21387;
    std::cout << 21387;
    for (int i = 0; i < 21387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21387(int& val) {
    const int x = 21387;
    std::cout << 21387;
    for (int i = 0; i < 21387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21388(int& val) {
    func_in_21388(val);
    const int x = 21388;
    std::cout << 21388;
    for (int i = 0; i < 21388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21388(int& val) {
    const int x = 21388;
    std::cout << 21388;
    for (int i = 0; i < 21388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21389(int& val) {
    func_in_21389(val);
    const int x = 21389;
    std::cout << 21389;
    for (int i = 0; i < 21389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21389(int& val) {
    const int x = 21389;
    std::cout << 21389;
    for (int i = 0; i < 21389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21390(int& val) {
    func_in_21390(val);
    const int x = 21390;
    std::cout << 21390;
    for (int i = 0; i < 21390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21390(int& val) {
    const int x = 21390;
    std::cout << 21390;
    for (int i = 0; i < 21390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21391(int& val) {
    func_in_21391(val);
    const int x = 21391;
    std::cout << 21391;
    for (int i = 0; i < 21391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21391(int& val) {
    const int x = 21391;
    std::cout << 21391;
    for (int i = 0; i < 21391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21392(int& val) {
    func_in_21392(val);
    const int x = 21392;
    std::cout << 21392;
    for (int i = 0; i < 21392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21392(int& val) {
    const int x = 21392;
    std::cout << 21392;
    for (int i = 0; i < 21392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21393(int& val) {
    func_in_21393(val);
    const int x = 21393;
    std::cout << 21393;
    for (int i = 0; i < 21393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21393(int& val) {
    const int x = 21393;
    std::cout << 21393;
    for (int i = 0; i < 21393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21394(int& val) {
    func_in_21394(val);
    const int x = 21394;
    std::cout << 21394;
    for (int i = 0; i < 21394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21394(int& val) {
    const int x = 21394;
    std::cout << 21394;
    for (int i = 0; i < 21394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21395(int& val) {
    func_in_21395(val);
    const int x = 21395;
    std::cout << 21395;
    for (int i = 0; i < 21395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21395(int& val) {
    const int x = 21395;
    std::cout << 21395;
    for (int i = 0; i < 21395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21396(int& val) {
    func_in_21396(val);
    const int x = 21396;
    std::cout << 21396;
    for (int i = 0; i < 21396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21396(int& val) {
    const int x = 21396;
    std::cout << 21396;
    for (int i = 0; i < 21396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21397(int& val) {
    func_in_21397(val);
    const int x = 21397;
    std::cout << 21397;
    for (int i = 0; i < 21397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21397(int& val) {
    const int x = 21397;
    std::cout << 21397;
    for (int i = 0; i < 21397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21398(int& val) {
    func_in_21398(val);
    const int x = 21398;
    std::cout << 21398;
    for (int i = 0; i < 21398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21398(int& val) {
    const int x = 21398;
    std::cout << 21398;
    for (int i = 0; i < 21398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21399(int& val) {
    func_in_21399(val);
    const int x = 21399;
    std::cout << 21399;
    for (int i = 0; i < 21399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21399(int& val) {
    const int x = 21399;
    std::cout << 21399;
    for (int i = 0; i < 21399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21400(int& val) {
    func_in_21400(val);
    const int x = 21400;
    std::cout << 21400;
    for (int i = 0; i < 21400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21400(int& val) {
    const int x = 21400;
    std::cout << 21400;
    for (int i = 0; i < 21400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21401(int& val) {
    func_in_21401(val);
    const int x = 21401;
    std::cout << 21401;
    for (int i = 0; i < 21401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21401(int& val) {
    const int x = 21401;
    std::cout << 21401;
    for (int i = 0; i < 21401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21402(int& val) {
    func_in_21402(val);
    const int x = 21402;
    std::cout << 21402;
    for (int i = 0; i < 21402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21402(int& val) {
    const int x = 21402;
    std::cout << 21402;
    for (int i = 0; i < 21402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21403(int& val) {
    func_in_21403(val);
    const int x = 21403;
    std::cout << 21403;
    for (int i = 0; i < 21403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21403(int& val) {
    const int x = 21403;
    std::cout << 21403;
    for (int i = 0; i < 21403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21404(int& val) {
    func_in_21404(val);
    const int x = 21404;
    std::cout << 21404;
    for (int i = 0; i < 21404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21404(int& val) {
    const int x = 21404;
    std::cout << 21404;
    for (int i = 0; i < 21404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21405(int& val) {
    func_in_21405(val);
    const int x = 21405;
    std::cout << 21405;
    for (int i = 0; i < 21405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21405(int& val) {
    const int x = 21405;
    std::cout << 21405;
    for (int i = 0; i < 21405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21406(int& val) {
    func_in_21406(val);
    const int x = 21406;
    std::cout << 21406;
    for (int i = 0; i < 21406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21406(int& val) {
    const int x = 21406;
    std::cout << 21406;
    for (int i = 0; i < 21406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21407(int& val) {
    func_in_21407(val);
    const int x = 21407;
    std::cout << 21407;
    for (int i = 0; i < 21407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21407(int& val) {
    const int x = 21407;
    std::cout << 21407;
    for (int i = 0; i < 21407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21408(int& val) {
    func_in_21408(val);
    const int x = 21408;
    std::cout << 21408;
    for (int i = 0; i < 21408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21408(int& val) {
    const int x = 21408;
    std::cout << 21408;
    for (int i = 0; i < 21408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21409(int& val) {
    func_in_21409(val);
    const int x = 21409;
    std::cout << 21409;
    for (int i = 0; i < 21409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21409(int& val) {
    const int x = 21409;
    std::cout << 21409;
    for (int i = 0; i < 21409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21410(int& val) {
    func_in_21410(val);
    const int x = 21410;
    std::cout << 21410;
    for (int i = 0; i < 21410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21410(int& val) {
    const int x = 21410;
    std::cout << 21410;
    for (int i = 0; i < 21410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21411(int& val) {
    func_in_21411(val);
    const int x = 21411;
    std::cout << 21411;
    for (int i = 0; i < 21411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21411(int& val) {
    const int x = 21411;
    std::cout << 21411;
    for (int i = 0; i < 21411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21412(int& val) {
    func_in_21412(val);
    const int x = 21412;
    std::cout << 21412;
    for (int i = 0; i < 21412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21412(int& val) {
    const int x = 21412;
    std::cout << 21412;
    for (int i = 0; i < 21412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21413(int& val) {
    func_in_21413(val);
    const int x = 21413;
    std::cout << 21413;
    for (int i = 0; i < 21413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21413(int& val) {
    const int x = 21413;
    std::cout << 21413;
    for (int i = 0; i < 21413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21414(int& val) {
    func_in_21414(val);
    const int x = 21414;
    std::cout << 21414;
    for (int i = 0; i < 21414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21414(int& val) {
    const int x = 21414;
    std::cout << 21414;
    for (int i = 0; i < 21414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21415(int& val) {
    func_in_21415(val);
    const int x = 21415;
    std::cout << 21415;
    for (int i = 0; i < 21415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21415(int& val) {
    const int x = 21415;
    std::cout << 21415;
    for (int i = 0; i < 21415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21416(int& val) {
    func_in_21416(val);
    const int x = 21416;
    std::cout << 21416;
    for (int i = 0; i < 21416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21416(int& val) {
    const int x = 21416;
    std::cout << 21416;
    for (int i = 0; i < 21416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21417(int& val) {
    func_in_21417(val);
    const int x = 21417;
    std::cout << 21417;
    for (int i = 0; i < 21417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21417(int& val) {
    const int x = 21417;
    std::cout << 21417;
    for (int i = 0; i < 21417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21418(int& val) {
    func_in_21418(val);
    const int x = 21418;
    std::cout << 21418;
    for (int i = 0; i < 21418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21418(int& val) {
    const int x = 21418;
    std::cout << 21418;
    for (int i = 0; i < 21418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21419(int& val) {
    func_in_21419(val);
    const int x = 21419;
    std::cout << 21419;
    for (int i = 0; i < 21419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21419(int& val) {
    const int x = 21419;
    std::cout << 21419;
    for (int i = 0; i < 21419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21420(int& val) {
    func_in_21420(val);
    const int x = 21420;
    std::cout << 21420;
    for (int i = 0; i < 21420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21420(int& val) {
    const int x = 21420;
    std::cout << 21420;
    for (int i = 0; i < 21420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21421(int& val) {
    func_in_21421(val);
    const int x = 21421;
    std::cout << 21421;
    for (int i = 0; i < 21421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21421(int& val) {
    const int x = 21421;
    std::cout << 21421;
    for (int i = 0; i < 21421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21422(int& val) {
    func_in_21422(val);
    const int x = 21422;
    std::cout << 21422;
    for (int i = 0; i < 21422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21422(int& val) {
    const int x = 21422;
    std::cout << 21422;
    for (int i = 0; i < 21422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21423(int& val) {
    func_in_21423(val);
    const int x = 21423;
    std::cout << 21423;
    for (int i = 0; i < 21423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21423(int& val) {
    const int x = 21423;
    std::cout << 21423;
    for (int i = 0; i < 21423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21424(int& val) {
    func_in_21424(val);
    const int x = 21424;
    std::cout << 21424;
    for (int i = 0; i < 21424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21424(int& val) {
    const int x = 21424;
    std::cout << 21424;
    for (int i = 0; i < 21424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21425(int& val) {
    func_in_21425(val);
    const int x = 21425;
    std::cout << 21425;
    for (int i = 0; i < 21425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21425(int& val) {
    const int x = 21425;
    std::cout << 21425;
    for (int i = 0; i < 21425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21426(int& val) {
    func_in_21426(val);
    const int x = 21426;
    std::cout << 21426;
    for (int i = 0; i < 21426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21426(int& val) {
    const int x = 21426;
    std::cout << 21426;
    for (int i = 0; i < 21426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21427(int& val) {
    func_in_21427(val);
    const int x = 21427;
    std::cout << 21427;
    for (int i = 0; i < 21427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21427(int& val) {
    const int x = 21427;
    std::cout << 21427;
    for (int i = 0; i < 21427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21428(int& val) {
    func_in_21428(val);
    const int x = 21428;
    std::cout << 21428;
    for (int i = 0; i < 21428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21428(int& val) {
    const int x = 21428;
    std::cout << 21428;
    for (int i = 0; i < 21428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21429(int& val) {
    func_in_21429(val);
    const int x = 21429;
    std::cout << 21429;
    for (int i = 0; i < 21429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21429(int& val) {
    const int x = 21429;
    std::cout << 21429;
    for (int i = 0; i < 21429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21430(int& val) {
    func_in_21430(val);
    const int x = 21430;
    std::cout << 21430;
    for (int i = 0; i < 21430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21430(int& val) {
    const int x = 21430;
    std::cout << 21430;
    for (int i = 0; i < 21430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21431(int& val) {
    func_in_21431(val);
    const int x = 21431;
    std::cout << 21431;
    for (int i = 0; i < 21431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21431(int& val) {
    const int x = 21431;
    std::cout << 21431;
    for (int i = 0; i < 21431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21432(int& val) {
    func_in_21432(val);
    const int x = 21432;
    std::cout << 21432;
    for (int i = 0; i < 21432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21432(int& val) {
    const int x = 21432;
    std::cout << 21432;
    for (int i = 0; i < 21432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21433(int& val) {
    func_in_21433(val);
    const int x = 21433;
    std::cout << 21433;
    for (int i = 0; i < 21433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21433(int& val) {
    const int x = 21433;
    std::cout << 21433;
    for (int i = 0; i < 21433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21434(int& val) {
    func_in_21434(val);
    const int x = 21434;
    std::cout << 21434;
    for (int i = 0; i < 21434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21434(int& val) {
    const int x = 21434;
    std::cout << 21434;
    for (int i = 0; i < 21434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21435(int& val) {
    func_in_21435(val);
    const int x = 21435;
    std::cout << 21435;
    for (int i = 0; i < 21435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21435(int& val) {
    const int x = 21435;
    std::cout << 21435;
    for (int i = 0; i < 21435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21436(int& val) {
    func_in_21436(val);
    const int x = 21436;
    std::cout << 21436;
    for (int i = 0; i < 21436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21436(int& val) {
    const int x = 21436;
    std::cout << 21436;
    for (int i = 0; i < 21436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21437(int& val) {
    func_in_21437(val);
    const int x = 21437;
    std::cout << 21437;
    for (int i = 0; i < 21437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21437(int& val) {
    const int x = 21437;
    std::cout << 21437;
    for (int i = 0; i < 21437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21438(int& val) {
    func_in_21438(val);
    const int x = 21438;
    std::cout << 21438;
    for (int i = 0; i < 21438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21438(int& val) {
    const int x = 21438;
    std::cout << 21438;
    for (int i = 0; i < 21438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21439(int& val) {
    func_in_21439(val);
    const int x = 21439;
    std::cout << 21439;
    for (int i = 0; i < 21439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21439(int& val) {
    const int x = 21439;
    std::cout << 21439;
    for (int i = 0; i < 21439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21440(int& val) {
    func_in_21440(val);
    const int x = 21440;
    std::cout << 21440;
    for (int i = 0; i < 21440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21440(int& val) {
    const int x = 21440;
    std::cout << 21440;
    for (int i = 0; i < 21440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21441(int& val) {
    func_in_21441(val);
    const int x = 21441;
    std::cout << 21441;
    for (int i = 0; i < 21441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21441(int& val) {
    const int x = 21441;
    std::cout << 21441;
    for (int i = 0; i < 21441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21442(int& val) {
    func_in_21442(val);
    const int x = 21442;
    std::cout << 21442;
    for (int i = 0; i < 21442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21442(int& val) {
    const int x = 21442;
    std::cout << 21442;
    for (int i = 0; i < 21442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21443(int& val) {
    func_in_21443(val);
    const int x = 21443;
    std::cout << 21443;
    for (int i = 0; i < 21443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21443(int& val) {
    const int x = 21443;
    std::cout << 21443;
    for (int i = 0; i < 21443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21444(int& val) {
    func_in_21444(val);
    const int x = 21444;
    std::cout << 21444;
    for (int i = 0; i < 21444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21444(int& val) {
    const int x = 21444;
    std::cout << 21444;
    for (int i = 0; i < 21444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21445(int& val) {
    func_in_21445(val);
    const int x = 21445;
    std::cout << 21445;
    for (int i = 0; i < 21445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21445(int& val) {
    const int x = 21445;
    std::cout << 21445;
    for (int i = 0; i < 21445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21446(int& val) {
    func_in_21446(val);
    const int x = 21446;
    std::cout << 21446;
    for (int i = 0; i < 21446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21446(int& val) {
    const int x = 21446;
    std::cout << 21446;
    for (int i = 0; i < 21446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21447(int& val) {
    func_in_21447(val);
    const int x = 21447;
    std::cout << 21447;
    for (int i = 0; i < 21447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21447(int& val) {
    const int x = 21447;
    std::cout << 21447;
    for (int i = 0; i < 21447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21448(int& val) {
    func_in_21448(val);
    const int x = 21448;
    std::cout << 21448;
    for (int i = 0; i < 21448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21448(int& val) {
    const int x = 21448;
    std::cout << 21448;
    for (int i = 0; i < 21448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21449(int& val) {
    func_in_21449(val);
    const int x = 21449;
    std::cout << 21449;
    for (int i = 0; i < 21449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21449(int& val) {
    const int x = 21449;
    std::cout << 21449;
    for (int i = 0; i < 21449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21450(int& val) {
    func_in_21450(val);
    const int x = 21450;
    std::cout << 21450;
    for (int i = 0; i < 21450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21450(int& val) {
    const int x = 21450;
    std::cout << 21450;
    for (int i = 0; i < 21450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21451(int& val) {
    func_in_21451(val);
    const int x = 21451;
    std::cout << 21451;
    for (int i = 0; i < 21451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21451(int& val) {
    const int x = 21451;
    std::cout << 21451;
    for (int i = 0; i < 21451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21452(int& val) {
    func_in_21452(val);
    const int x = 21452;
    std::cout << 21452;
    for (int i = 0; i < 21452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21452(int& val) {
    const int x = 21452;
    std::cout << 21452;
    for (int i = 0; i < 21452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21453(int& val) {
    func_in_21453(val);
    const int x = 21453;
    std::cout << 21453;
    for (int i = 0; i < 21453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21453(int& val) {
    const int x = 21453;
    std::cout << 21453;
    for (int i = 0; i < 21453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21454(int& val) {
    func_in_21454(val);
    const int x = 21454;
    std::cout << 21454;
    for (int i = 0; i < 21454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21454(int& val) {
    const int x = 21454;
    std::cout << 21454;
    for (int i = 0; i < 21454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21455(int& val) {
    func_in_21455(val);
    const int x = 21455;
    std::cout << 21455;
    for (int i = 0; i < 21455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21455(int& val) {
    const int x = 21455;
    std::cout << 21455;
    for (int i = 0; i < 21455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21456(int& val) {
    func_in_21456(val);
    const int x = 21456;
    std::cout << 21456;
    for (int i = 0; i < 21456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21456(int& val) {
    const int x = 21456;
    std::cout << 21456;
    for (int i = 0; i < 21456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21457(int& val) {
    func_in_21457(val);
    const int x = 21457;
    std::cout << 21457;
    for (int i = 0; i < 21457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21457(int& val) {
    const int x = 21457;
    std::cout << 21457;
    for (int i = 0; i < 21457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21458(int& val) {
    func_in_21458(val);
    const int x = 21458;
    std::cout << 21458;
    for (int i = 0; i < 21458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21458(int& val) {
    const int x = 21458;
    std::cout << 21458;
    for (int i = 0; i < 21458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21459(int& val) {
    func_in_21459(val);
    const int x = 21459;
    std::cout << 21459;
    for (int i = 0; i < 21459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21459(int& val) {
    const int x = 21459;
    std::cout << 21459;
    for (int i = 0; i < 21459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21460(int& val) {
    func_in_21460(val);
    const int x = 21460;
    std::cout << 21460;
    for (int i = 0; i < 21460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21460(int& val) {
    const int x = 21460;
    std::cout << 21460;
    for (int i = 0; i < 21460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21461(int& val) {
    func_in_21461(val);
    const int x = 21461;
    std::cout << 21461;
    for (int i = 0; i < 21461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21461(int& val) {
    const int x = 21461;
    std::cout << 21461;
    for (int i = 0; i < 21461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21462(int& val) {
    func_in_21462(val);
    const int x = 21462;
    std::cout << 21462;
    for (int i = 0; i < 21462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21462(int& val) {
    const int x = 21462;
    std::cout << 21462;
    for (int i = 0; i < 21462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21463(int& val) {
    func_in_21463(val);
    const int x = 21463;
    std::cout << 21463;
    for (int i = 0; i < 21463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21463(int& val) {
    const int x = 21463;
    std::cout << 21463;
    for (int i = 0; i < 21463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21464(int& val) {
    func_in_21464(val);
    const int x = 21464;
    std::cout << 21464;
    for (int i = 0; i < 21464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21464(int& val) {
    const int x = 21464;
    std::cout << 21464;
    for (int i = 0; i < 21464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21465(int& val) {
    func_in_21465(val);
    const int x = 21465;
    std::cout << 21465;
    for (int i = 0; i < 21465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21465(int& val) {
    const int x = 21465;
    std::cout << 21465;
    for (int i = 0; i < 21465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21466(int& val) {
    func_in_21466(val);
    const int x = 21466;
    std::cout << 21466;
    for (int i = 0; i < 21466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21466(int& val) {
    const int x = 21466;
    std::cout << 21466;
    for (int i = 0; i < 21466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21467(int& val) {
    func_in_21467(val);
    const int x = 21467;
    std::cout << 21467;
    for (int i = 0; i < 21467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21467(int& val) {
    const int x = 21467;
    std::cout << 21467;
    for (int i = 0; i < 21467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21468(int& val) {
    func_in_21468(val);
    const int x = 21468;
    std::cout << 21468;
    for (int i = 0; i < 21468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21468(int& val) {
    const int x = 21468;
    std::cout << 21468;
    for (int i = 0; i < 21468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21469(int& val) {
    func_in_21469(val);
    const int x = 21469;
    std::cout << 21469;
    for (int i = 0; i < 21469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21469(int& val) {
    const int x = 21469;
    std::cout << 21469;
    for (int i = 0; i < 21469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21470(int& val) {
    func_in_21470(val);
    const int x = 21470;
    std::cout << 21470;
    for (int i = 0; i < 21470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21470(int& val) {
    const int x = 21470;
    std::cout << 21470;
    for (int i = 0; i < 21470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21471(int& val) {
    func_in_21471(val);
    const int x = 21471;
    std::cout << 21471;
    for (int i = 0; i < 21471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21471(int& val) {
    const int x = 21471;
    std::cout << 21471;
    for (int i = 0; i < 21471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21472(int& val) {
    func_in_21472(val);
    const int x = 21472;
    std::cout << 21472;
    for (int i = 0; i < 21472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21472(int& val) {
    const int x = 21472;
    std::cout << 21472;
    for (int i = 0; i < 21472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21473(int& val) {
    func_in_21473(val);
    const int x = 21473;
    std::cout << 21473;
    for (int i = 0; i < 21473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21473(int& val) {
    const int x = 21473;
    std::cout << 21473;
    for (int i = 0; i < 21473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21474(int& val) {
    func_in_21474(val);
    const int x = 21474;
    std::cout << 21474;
    for (int i = 0; i < 21474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21474(int& val) {
    const int x = 21474;
    std::cout << 21474;
    for (int i = 0; i < 21474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21475(int& val) {
    func_in_21475(val);
    const int x = 21475;
    std::cout << 21475;
    for (int i = 0; i < 21475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21475(int& val) {
    const int x = 21475;
    std::cout << 21475;
    for (int i = 0; i < 21475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21476(int& val) {
    func_in_21476(val);
    const int x = 21476;
    std::cout << 21476;
    for (int i = 0; i < 21476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21476(int& val) {
    const int x = 21476;
    std::cout << 21476;
    for (int i = 0; i < 21476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21477(int& val) {
    func_in_21477(val);
    const int x = 21477;
    std::cout << 21477;
    for (int i = 0; i < 21477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21477(int& val) {
    const int x = 21477;
    std::cout << 21477;
    for (int i = 0; i < 21477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21478(int& val) {
    func_in_21478(val);
    const int x = 21478;
    std::cout << 21478;
    for (int i = 0; i < 21478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21478(int& val) {
    const int x = 21478;
    std::cout << 21478;
    for (int i = 0; i < 21478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21479(int& val) {
    func_in_21479(val);
    const int x = 21479;
    std::cout << 21479;
    for (int i = 0; i < 21479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21479(int& val) {
    const int x = 21479;
    std::cout << 21479;
    for (int i = 0; i < 21479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21480(int& val) {
    func_in_21480(val);
    const int x = 21480;
    std::cout << 21480;
    for (int i = 0; i < 21480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21480(int& val) {
    const int x = 21480;
    std::cout << 21480;
    for (int i = 0; i < 21480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21481(int& val) {
    func_in_21481(val);
    const int x = 21481;
    std::cout << 21481;
    for (int i = 0; i < 21481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21481(int& val) {
    const int x = 21481;
    std::cout << 21481;
    for (int i = 0; i < 21481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21482(int& val) {
    func_in_21482(val);
    const int x = 21482;
    std::cout << 21482;
    for (int i = 0; i < 21482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21482(int& val) {
    const int x = 21482;
    std::cout << 21482;
    for (int i = 0; i < 21482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21483(int& val) {
    func_in_21483(val);
    const int x = 21483;
    std::cout << 21483;
    for (int i = 0; i < 21483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21483(int& val) {
    const int x = 21483;
    std::cout << 21483;
    for (int i = 0; i < 21483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21484(int& val) {
    func_in_21484(val);
    const int x = 21484;
    std::cout << 21484;
    for (int i = 0; i < 21484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21484(int& val) {
    const int x = 21484;
    std::cout << 21484;
    for (int i = 0; i < 21484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21485(int& val) {
    func_in_21485(val);
    const int x = 21485;
    std::cout << 21485;
    for (int i = 0; i < 21485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21485(int& val) {
    const int x = 21485;
    std::cout << 21485;
    for (int i = 0; i < 21485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21486(int& val) {
    func_in_21486(val);
    const int x = 21486;
    std::cout << 21486;
    for (int i = 0; i < 21486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21486(int& val) {
    const int x = 21486;
    std::cout << 21486;
    for (int i = 0; i < 21486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21487(int& val) {
    func_in_21487(val);
    const int x = 21487;
    std::cout << 21487;
    for (int i = 0; i < 21487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21487(int& val) {
    const int x = 21487;
    std::cout << 21487;
    for (int i = 0; i < 21487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21488(int& val) {
    func_in_21488(val);
    const int x = 21488;
    std::cout << 21488;
    for (int i = 0; i < 21488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21488(int& val) {
    const int x = 21488;
    std::cout << 21488;
    for (int i = 0; i < 21488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21489(int& val) {
    func_in_21489(val);
    const int x = 21489;
    std::cout << 21489;
    for (int i = 0; i < 21489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21489(int& val) {
    const int x = 21489;
    std::cout << 21489;
    for (int i = 0; i < 21489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21490(int& val) {
    func_in_21490(val);
    const int x = 21490;
    std::cout << 21490;
    for (int i = 0; i < 21490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21490(int& val) {
    const int x = 21490;
    std::cout << 21490;
    for (int i = 0; i < 21490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21491(int& val) {
    func_in_21491(val);
    const int x = 21491;
    std::cout << 21491;
    for (int i = 0; i < 21491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21491(int& val) {
    const int x = 21491;
    std::cout << 21491;
    for (int i = 0; i < 21491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21492(int& val) {
    func_in_21492(val);
    const int x = 21492;
    std::cout << 21492;
    for (int i = 0; i < 21492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21492(int& val) {
    const int x = 21492;
    std::cout << 21492;
    for (int i = 0; i < 21492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21493(int& val) {
    func_in_21493(val);
    const int x = 21493;
    std::cout << 21493;
    for (int i = 0; i < 21493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21493(int& val) {
    const int x = 21493;
    std::cout << 21493;
    for (int i = 0; i < 21493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21494(int& val) {
    func_in_21494(val);
    const int x = 21494;
    std::cout << 21494;
    for (int i = 0; i < 21494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21494(int& val) {
    const int x = 21494;
    std::cout << 21494;
    for (int i = 0; i < 21494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21495(int& val) {
    func_in_21495(val);
    const int x = 21495;
    std::cout << 21495;
    for (int i = 0; i < 21495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21495(int& val) {
    const int x = 21495;
    std::cout << 21495;
    for (int i = 0; i < 21495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21496(int& val) {
    func_in_21496(val);
    const int x = 21496;
    std::cout << 21496;
    for (int i = 0; i < 21496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21496(int& val) {
    const int x = 21496;
    std::cout << 21496;
    for (int i = 0; i < 21496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21497(int& val) {
    func_in_21497(val);
    const int x = 21497;
    std::cout << 21497;
    for (int i = 0; i < 21497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21497(int& val) {
    const int x = 21497;
    std::cout << 21497;
    for (int i = 0; i < 21497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21498(int& val) {
    func_in_21498(val);
    const int x = 21498;
    std::cout << 21498;
    for (int i = 0; i < 21498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21498(int& val) {
    const int x = 21498;
    std::cout << 21498;
    for (int i = 0; i < 21498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21499(int& val) {
    func_in_21499(val);
    const int x = 21499;
    std::cout << 21499;
    for (int i = 0; i < 21499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21499(int& val) {
    const int x = 21499;
    std::cout << 21499;
    for (int i = 0; i < 21499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21500(int& val) {
    func_in_21500(val);
    const int x = 21500;
    std::cout << 21500;
    for (int i = 0; i < 21500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21500(int& val) {
    const int x = 21500;
    std::cout << 21500;
    for (int i = 0; i < 21500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21501(int& val) {
    func_in_21501(val);
    const int x = 21501;
    std::cout << 21501;
    for (int i = 0; i < 21501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21501(int& val) {
    const int x = 21501;
    std::cout << 21501;
    for (int i = 0; i < 21501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21502(int& val) {
    func_in_21502(val);
    const int x = 21502;
    std::cout << 21502;
    for (int i = 0; i < 21502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21502(int& val) {
    const int x = 21502;
    std::cout << 21502;
    for (int i = 0; i < 21502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21503(int& val) {
    func_in_21503(val);
    const int x = 21503;
    std::cout << 21503;
    for (int i = 0; i < 21503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21503(int& val) {
    const int x = 21503;
    std::cout << 21503;
    for (int i = 0; i < 21503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21504(int& val) {
    func_in_21504(val);
    const int x = 21504;
    std::cout << 21504;
    for (int i = 0; i < 21504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21504(int& val) {
    const int x = 21504;
    std::cout << 21504;
    for (int i = 0; i < 21504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21505(int& val) {
    func_in_21505(val);
    const int x = 21505;
    std::cout << 21505;
    for (int i = 0; i < 21505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21505(int& val) {
    const int x = 21505;
    std::cout << 21505;
    for (int i = 0; i < 21505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21506(int& val) {
    func_in_21506(val);
    const int x = 21506;
    std::cout << 21506;
    for (int i = 0; i < 21506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21506(int& val) {
    const int x = 21506;
    std::cout << 21506;
    for (int i = 0; i < 21506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21507(int& val) {
    func_in_21507(val);
    const int x = 21507;
    std::cout << 21507;
    for (int i = 0; i < 21507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21507(int& val) {
    const int x = 21507;
    std::cout << 21507;
    for (int i = 0; i < 21507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21508(int& val) {
    func_in_21508(val);
    const int x = 21508;
    std::cout << 21508;
    for (int i = 0; i < 21508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21508(int& val) {
    const int x = 21508;
    std::cout << 21508;
    for (int i = 0; i < 21508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21509(int& val) {
    func_in_21509(val);
    const int x = 21509;
    std::cout << 21509;
    for (int i = 0; i < 21509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21509(int& val) {
    const int x = 21509;
    std::cout << 21509;
    for (int i = 0; i < 21509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21510(int& val) {
    func_in_21510(val);
    const int x = 21510;
    std::cout << 21510;
    for (int i = 0; i < 21510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21510(int& val) {
    const int x = 21510;
    std::cout << 21510;
    for (int i = 0; i < 21510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21511(int& val) {
    func_in_21511(val);
    const int x = 21511;
    std::cout << 21511;
    for (int i = 0; i < 21511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21511(int& val) {
    const int x = 21511;
    std::cout << 21511;
    for (int i = 0; i < 21511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21512(int& val) {
    func_in_21512(val);
    const int x = 21512;
    std::cout << 21512;
    for (int i = 0; i < 21512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21512(int& val) {
    const int x = 21512;
    std::cout << 21512;
    for (int i = 0; i < 21512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21513(int& val) {
    func_in_21513(val);
    const int x = 21513;
    std::cout << 21513;
    for (int i = 0; i < 21513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21513(int& val) {
    const int x = 21513;
    std::cout << 21513;
    for (int i = 0; i < 21513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21514(int& val) {
    func_in_21514(val);
    const int x = 21514;
    std::cout << 21514;
    for (int i = 0; i < 21514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21514(int& val) {
    const int x = 21514;
    std::cout << 21514;
    for (int i = 0; i < 21514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21515(int& val) {
    func_in_21515(val);
    const int x = 21515;
    std::cout << 21515;
    for (int i = 0; i < 21515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21515(int& val) {
    const int x = 21515;
    std::cout << 21515;
    for (int i = 0; i < 21515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21516(int& val) {
    func_in_21516(val);
    const int x = 21516;
    std::cout << 21516;
    for (int i = 0; i < 21516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21516(int& val) {
    const int x = 21516;
    std::cout << 21516;
    for (int i = 0; i < 21516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21517(int& val) {
    func_in_21517(val);
    const int x = 21517;
    std::cout << 21517;
    for (int i = 0; i < 21517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21517(int& val) {
    const int x = 21517;
    std::cout << 21517;
    for (int i = 0; i < 21517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21518(int& val) {
    func_in_21518(val);
    const int x = 21518;
    std::cout << 21518;
    for (int i = 0; i < 21518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21518(int& val) {
    const int x = 21518;
    std::cout << 21518;
    for (int i = 0; i < 21518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21519(int& val) {
    func_in_21519(val);
    const int x = 21519;
    std::cout << 21519;
    for (int i = 0; i < 21519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21519(int& val) {
    const int x = 21519;
    std::cout << 21519;
    for (int i = 0; i < 21519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21520(int& val) {
    func_in_21520(val);
    const int x = 21520;
    std::cout << 21520;
    for (int i = 0; i < 21520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21520(int& val) {
    const int x = 21520;
    std::cout << 21520;
    for (int i = 0; i < 21520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21521(int& val) {
    func_in_21521(val);
    const int x = 21521;
    std::cout << 21521;
    for (int i = 0; i < 21521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21521(int& val) {
    const int x = 21521;
    std::cout << 21521;
    for (int i = 0; i < 21521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21522(int& val) {
    func_in_21522(val);
    const int x = 21522;
    std::cout << 21522;
    for (int i = 0; i < 21522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21522(int& val) {
    const int x = 21522;
    std::cout << 21522;
    for (int i = 0; i < 21522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21523(int& val) {
    func_in_21523(val);
    const int x = 21523;
    std::cout << 21523;
    for (int i = 0; i < 21523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21523(int& val) {
    const int x = 21523;
    std::cout << 21523;
    for (int i = 0; i < 21523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21524(int& val) {
    func_in_21524(val);
    const int x = 21524;
    std::cout << 21524;
    for (int i = 0; i < 21524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21524(int& val) {
    const int x = 21524;
    std::cout << 21524;
    for (int i = 0; i < 21524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21525(int& val) {
    func_in_21525(val);
    const int x = 21525;
    std::cout << 21525;
    for (int i = 0; i < 21525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21525(int& val) {
    const int x = 21525;
    std::cout << 21525;
    for (int i = 0; i < 21525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21526(int& val) {
    func_in_21526(val);
    const int x = 21526;
    std::cout << 21526;
    for (int i = 0; i < 21526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21526(int& val) {
    const int x = 21526;
    std::cout << 21526;
    for (int i = 0; i < 21526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21527(int& val) {
    func_in_21527(val);
    const int x = 21527;
    std::cout << 21527;
    for (int i = 0; i < 21527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21527(int& val) {
    const int x = 21527;
    std::cout << 21527;
    for (int i = 0; i < 21527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21528(int& val) {
    func_in_21528(val);
    const int x = 21528;
    std::cout << 21528;
    for (int i = 0; i < 21528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21528(int& val) {
    const int x = 21528;
    std::cout << 21528;
    for (int i = 0; i < 21528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21529(int& val) {
    func_in_21529(val);
    const int x = 21529;
    std::cout << 21529;
    for (int i = 0; i < 21529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21529(int& val) {
    const int x = 21529;
    std::cout << 21529;
    for (int i = 0; i < 21529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21530(int& val) {
    func_in_21530(val);
    const int x = 21530;
    std::cout << 21530;
    for (int i = 0; i < 21530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21530(int& val) {
    const int x = 21530;
    std::cout << 21530;
    for (int i = 0; i < 21530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21531(int& val) {
    func_in_21531(val);
    const int x = 21531;
    std::cout << 21531;
    for (int i = 0; i < 21531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21531(int& val) {
    const int x = 21531;
    std::cout << 21531;
    for (int i = 0; i < 21531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21532(int& val) {
    func_in_21532(val);
    const int x = 21532;
    std::cout << 21532;
    for (int i = 0; i < 21532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21532(int& val) {
    const int x = 21532;
    std::cout << 21532;
    for (int i = 0; i < 21532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21533(int& val) {
    func_in_21533(val);
    const int x = 21533;
    std::cout << 21533;
    for (int i = 0; i < 21533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21533(int& val) {
    const int x = 21533;
    std::cout << 21533;
    for (int i = 0; i < 21533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21534(int& val) {
    func_in_21534(val);
    const int x = 21534;
    std::cout << 21534;
    for (int i = 0; i < 21534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21534(int& val) {
    const int x = 21534;
    std::cout << 21534;
    for (int i = 0; i < 21534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21535(int& val) {
    func_in_21535(val);
    const int x = 21535;
    std::cout << 21535;
    for (int i = 0; i < 21535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21535(int& val) {
    const int x = 21535;
    std::cout << 21535;
    for (int i = 0; i < 21535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21536(int& val) {
    func_in_21536(val);
    const int x = 21536;
    std::cout << 21536;
    for (int i = 0; i < 21536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21536(int& val) {
    const int x = 21536;
    std::cout << 21536;
    for (int i = 0; i < 21536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21537(int& val) {
    func_in_21537(val);
    const int x = 21537;
    std::cout << 21537;
    for (int i = 0; i < 21537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21537(int& val) {
    const int x = 21537;
    std::cout << 21537;
    for (int i = 0; i < 21537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21538(int& val) {
    func_in_21538(val);
    const int x = 21538;
    std::cout << 21538;
    for (int i = 0; i < 21538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21538(int& val) {
    const int x = 21538;
    std::cout << 21538;
    for (int i = 0; i < 21538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21539(int& val) {
    func_in_21539(val);
    const int x = 21539;
    std::cout << 21539;
    for (int i = 0; i < 21539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21539(int& val) {
    const int x = 21539;
    std::cout << 21539;
    for (int i = 0; i < 21539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21540(int& val) {
    func_in_21540(val);
    const int x = 21540;
    std::cout << 21540;
    for (int i = 0; i < 21540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21540(int& val) {
    const int x = 21540;
    std::cout << 21540;
    for (int i = 0; i < 21540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21541(int& val) {
    func_in_21541(val);
    const int x = 21541;
    std::cout << 21541;
    for (int i = 0; i < 21541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21541(int& val) {
    const int x = 21541;
    std::cout << 21541;
    for (int i = 0; i < 21541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21542(int& val) {
    func_in_21542(val);
    const int x = 21542;
    std::cout << 21542;
    for (int i = 0; i < 21542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21542(int& val) {
    const int x = 21542;
    std::cout << 21542;
    for (int i = 0; i < 21542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21543(int& val) {
    func_in_21543(val);
    const int x = 21543;
    std::cout << 21543;
    for (int i = 0; i < 21543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21543(int& val) {
    const int x = 21543;
    std::cout << 21543;
    for (int i = 0; i < 21543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21544(int& val) {
    func_in_21544(val);
    const int x = 21544;
    std::cout << 21544;
    for (int i = 0; i < 21544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21544(int& val) {
    const int x = 21544;
    std::cout << 21544;
    for (int i = 0; i < 21544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21545(int& val) {
    func_in_21545(val);
    const int x = 21545;
    std::cout << 21545;
    for (int i = 0; i < 21545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21545(int& val) {
    const int x = 21545;
    std::cout << 21545;
    for (int i = 0; i < 21545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21546(int& val) {
    func_in_21546(val);
    const int x = 21546;
    std::cout << 21546;
    for (int i = 0; i < 21546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21546(int& val) {
    const int x = 21546;
    std::cout << 21546;
    for (int i = 0; i < 21546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21547(int& val) {
    func_in_21547(val);
    const int x = 21547;
    std::cout << 21547;
    for (int i = 0; i < 21547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21547(int& val) {
    const int x = 21547;
    std::cout << 21547;
    for (int i = 0; i < 21547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21548(int& val) {
    func_in_21548(val);
    const int x = 21548;
    std::cout << 21548;
    for (int i = 0; i < 21548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21548(int& val) {
    const int x = 21548;
    std::cout << 21548;
    for (int i = 0; i < 21548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21549(int& val) {
    func_in_21549(val);
    const int x = 21549;
    std::cout << 21549;
    for (int i = 0; i < 21549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21549(int& val) {
    const int x = 21549;
    std::cout << 21549;
    for (int i = 0; i < 21549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21550(int& val) {
    func_in_21550(val);
    const int x = 21550;
    std::cout << 21550;
    for (int i = 0; i < 21550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21550(int& val) {
    const int x = 21550;
    std::cout << 21550;
    for (int i = 0; i < 21550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21551(int& val) {
    func_in_21551(val);
    const int x = 21551;
    std::cout << 21551;
    for (int i = 0; i < 21551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21551(int& val) {
    const int x = 21551;
    std::cout << 21551;
    for (int i = 0; i < 21551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21552(int& val) {
    func_in_21552(val);
    const int x = 21552;
    std::cout << 21552;
    for (int i = 0; i < 21552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21552(int& val) {
    const int x = 21552;
    std::cout << 21552;
    for (int i = 0; i < 21552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21553(int& val) {
    func_in_21553(val);
    const int x = 21553;
    std::cout << 21553;
    for (int i = 0; i < 21553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21553(int& val) {
    const int x = 21553;
    std::cout << 21553;
    for (int i = 0; i < 21553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21554(int& val) {
    func_in_21554(val);
    const int x = 21554;
    std::cout << 21554;
    for (int i = 0; i < 21554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21554(int& val) {
    const int x = 21554;
    std::cout << 21554;
    for (int i = 0; i < 21554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21555(int& val) {
    func_in_21555(val);
    const int x = 21555;
    std::cout << 21555;
    for (int i = 0; i < 21555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21555(int& val) {
    const int x = 21555;
    std::cout << 21555;
    for (int i = 0; i < 21555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21556(int& val) {
    func_in_21556(val);
    const int x = 21556;
    std::cout << 21556;
    for (int i = 0; i < 21556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21556(int& val) {
    const int x = 21556;
    std::cout << 21556;
    for (int i = 0; i < 21556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21557(int& val) {
    func_in_21557(val);
    const int x = 21557;
    std::cout << 21557;
    for (int i = 0; i < 21557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21557(int& val) {
    const int x = 21557;
    std::cout << 21557;
    for (int i = 0; i < 21557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21558(int& val) {
    func_in_21558(val);
    const int x = 21558;
    std::cout << 21558;
    for (int i = 0; i < 21558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21558(int& val) {
    const int x = 21558;
    std::cout << 21558;
    for (int i = 0; i < 21558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21559(int& val) {
    func_in_21559(val);
    const int x = 21559;
    std::cout << 21559;
    for (int i = 0; i < 21559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21559(int& val) {
    const int x = 21559;
    std::cout << 21559;
    for (int i = 0; i < 21559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21560(int& val) {
    func_in_21560(val);
    const int x = 21560;
    std::cout << 21560;
    for (int i = 0; i < 21560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21560(int& val) {
    const int x = 21560;
    std::cout << 21560;
    for (int i = 0; i < 21560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21561(int& val) {
    func_in_21561(val);
    const int x = 21561;
    std::cout << 21561;
    for (int i = 0; i < 21561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21561(int& val) {
    const int x = 21561;
    std::cout << 21561;
    for (int i = 0; i < 21561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21562(int& val) {
    func_in_21562(val);
    const int x = 21562;
    std::cout << 21562;
    for (int i = 0; i < 21562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21562(int& val) {
    const int x = 21562;
    std::cout << 21562;
    for (int i = 0; i < 21562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21563(int& val) {
    func_in_21563(val);
    const int x = 21563;
    std::cout << 21563;
    for (int i = 0; i < 21563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21563(int& val) {
    const int x = 21563;
    std::cout << 21563;
    for (int i = 0; i < 21563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21564(int& val) {
    func_in_21564(val);
    const int x = 21564;
    std::cout << 21564;
    for (int i = 0; i < 21564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21564(int& val) {
    const int x = 21564;
    std::cout << 21564;
    for (int i = 0; i < 21564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21565(int& val) {
    func_in_21565(val);
    const int x = 21565;
    std::cout << 21565;
    for (int i = 0; i < 21565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21565(int& val) {
    const int x = 21565;
    std::cout << 21565;
    for (int i = 0; i < 21565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21566(int& val) {
    func_in_21566(val);
    const int x = 21566;
    std::cout << 21566;
    for (int i = 0; i < 21566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21566(int& val) {
    const int x = 21566;
    std::cout << 21566;
    for (int i = 0; i < 21566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21567(int& val) {
    func_in_21567(val);
    const int x = 21567;
    std::cout << 21567;
    for (int i = 0; i < 21567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21567(int& val) {
    const int x = 21567;
    std::cout << 21567;
    for (int i = 0; i < 21567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21568(int& val) {
    func_in_21568(val);
    const int x = 21568;
    std::cout << 21568;
    for (int i = 0; i < 21568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21568(int& val) {
    const int x = 21568;
    std::cout << 21568;
    for (int i = 0; i < 21568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21569(int& val) {
    func_in_21569(val);
    const int x = 21569;
    std::cout << 21569;
    for (int i = 0; i < 21569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21569(int& val) {
    const int x = 21569;
    std::cout << 21569;
    for (int i = 0; i < 21569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21570(int& val) {
    func_in_21570(val);
    const int x = 21570;
    std::cout << 21570;
    for (int i = 0; i < 21570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21570(int& val) {
    const int x = 21570;
    std::cout << 21570;
    for (int i = 0; i < 21570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21571(int& val) {
    func_in_21571(val);
    const int x = 21571;
    std::cout << 21571;
    for (int i = 0; i < 21571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21571(int& val) {
    const int x = 21571;
    std::cout << 21571;
    for (int i = 0; i < 21571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21572(int& val) {
    func_in_21572(val);
    const int x = 21572;
    std::cout << 21572;
    for (int i = 0; i < 21572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21572(int& val) {
    const int x = 21572;
    std::cout << 21572;
    for (int i = 0; i < 21572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21573(int& val) {
    func_in_21573(val);
    const int x = 21573;
    std::cout << 21573;
    for (int i = 0; i < 21573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21573(int& val) {
    const int x = 21573;
    std::cout << 21573;
    for (int i = 0; i < 21573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21574(int& val) {
    func_in_21574(val);
    const int x = 21574;
    std::cout << 21574;
    for (int i = 0; i < 21574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21574(int& val) {
    const int x = 21574;
    std::cout << 21574;
    for (int i = 0; i < 21574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21575(int& val) {
    func_in_21575(val);
    const int x = 21575;
    std::cout << 21575;
    for (int i = 0; i < 21575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21575(int& val) {
    const int x = 21575;
    std::cout << 21575;
    for (int i = 0; i < 21575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21576(int& val) {
    func_in_21576(val);
    const int x = 21576;
    std::cout << 21576;
    for (int i = 0; i < 21576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21576(int& val) {
    const int x = 21576;
    std::cout << 21576;
    for (int i = 0; i < 21576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21577(int& val) {
    func_in_21577(val);
    const int x = 21577;
    std::cout << 21577;
    for (int i = 0; i < 21577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21577(int& val) {
    const int x = 21577;
    std::cout << 21577;
    for (int i = 0; i < 21577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21578(int& val) {
    func_in_21578(val);
    const int x = 21578;
    std::cout << 21578;
    for (int i = 0; i < 21578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21578(int& val) {
    const int x = 21578;
    std::cout << 21578;
    for (int i = 0; i < 21578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21579(int& val) {
    func_in_21579(val);
    const int x = 21579;
    std::cout << 21579;
    for (int i = 0; i < 21579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21579(int& val) {
    const int x = 21579;
    std::cout << 21579;
    for (int i = 0; i < 21579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21580(int& val) {
    func_in_21580(val);
    const int x = 21580;
    std::cout << 21580;
    for (int i = 0; i < 21580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21580(int& val) {
    const int x = 21580;
    std::cout << 21580;
    for (int i = 0; i < 21580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21581(int& val) {
    func_in_21581(val);
    const int x = 21581;
    std::cout << 21581;
    for (int i = 0; i < 21581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21581(int& val) {
    const int x = 21581;
    std::cout << 21581;
    for (int i = 0; i < 21581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21582(int& val) {
    func_in_21582(val);
    const int x = 21582;
    std::cout << 21582;
    for (int i = 0; i < 21582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21582(int& val) {
    const int x = 21582;
    std::cout << 21582;
    for (int i = 0; i < 21582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21583(int& val) {
    func_in_21583(val);
    const int x = 21583;
    std::cout << 21583;
    for (int i = 0; i < 21583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21583(int& val) {
    const int x = 21583;
    std::cout << 21583;
    for (int i = 0; i < 21583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21584(int& val) {
    func_in_21584(val);
    const int x = 21584;
    std::cout << 21584;
    for (int i = 0; i < 21584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21584(int& val) {
    const int x = 21584;
    std::cout << 21584;
    for (int i = 0; i < 21584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21585(int& val) {
    func_in_21585(val);
    const int x = 21585;
    std::cout << 21585;
    for (int i = 0; i < 21585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21585(int& val) {
    const int x = 21585;
    std::cout << 21585;
    for (int i = 0; i < 21585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21586(int& val) {
    func_in_21586(val);
    const int x = 21586;
    std::cout << 21586;
    for (int i = 0; i < 21586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21586(int& val) {
    const int x = 21586;
    std::cout << 21586;
    for (int i = 0; i < 21586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21587(int& val) {
    func_in_21587(val);
    const int x = 21587;
    std::cout << 21587;
    for (int i = 0; i < 21587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21587(int& val) {
    const int x = 21587;
    std::cout << 21587;
    for (int i = 0; i < 21587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21588(int& val) {
    func_in_21588(val);
    const int x = 21588;
    std::cout << 21588;
    for (int i = 0; i < 21588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21588(int& val) {
    const int x = 21588;
    std::cout << 21588;
    for (int i = 0; i < 21588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21589(int& val) {
    func_in_21589(val);
    const int x = 21589;
    std::cout << 21589;
    for (int i = 0; i < 21589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21589(int& val) {
    const int x = 21589;
    std::cout << 21589;
    for (int i = 0; i < 21589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21590(int& val) {
    func_in_21590(val);
    const int x = 21590;
    std::cout << 21590;
    for (int i = 0; i < 21590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21590(int& val) {
    const int x = 21590;
    std::cout << 21590;
    for (int i = 0; i < 21590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21591(int& val) {
    func_in_21591(val);
    const int x = 21591;
    std::cout << 21591;
    for (int i = 0; i < 21591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21591(int& val) {
    const int x = 21591;
    std::cout << 21591;
    for (int i = 0; i < 21591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21592(int& val) {
    func_in_21592(val);
    const int x = 21592;
    std::cout << 21592;
    for (int i = 0; i < 21592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21592(int& val) {
    const int x = 21592;
    std::cout << 21592;
    for (int i = 0; i < 21592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21593(int& val) {
    func_in_21593(val);
    const int x = 21593;
    std::cout << 21593;
    for (int i = 0; i < 21593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21593(int& val) {
    const int x = 21593;
    std::cout << 21593;
    for (int i = 0; i < 21593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21594(int& val) {
    func_in_21594(val);
    const int x = 21594;
    std::cout << 21594;
    for (int i = 0; i < 21594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21594(int& val) {
    const int x = 21594;
    std::cout << 21594;
    for (int i = 0; i < 21594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21595(int& val) {
    func_in_21595(val);
    const int x = 21595;
    std::cout << 21595;
    for (int i = 0; i < 21595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21595(int& val) {
    const int x = 21595;
    std::cout << 21595;
    for (int i = 0; i < 21595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21596(int& val) {
    func_in_21596(val);
    const int x = 21596;
    std::cout << 21596;
    for (int i = 0; i < 21596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21596(int& val) {
    const int x = 21596;
    std::cout << 21596;
    for (int i = 0; i < 21596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21597(int& val) {
    func_in_21597(val);
    const int x = 21597;
    std::cout << 21597;
    for (int i = 0; i < 21597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21597(int& val) {
    const int x = 21597;
    std::cout << 21597;
    for (int i = 0; i < 21597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21598(int& val) {
    func_in_21598(val);
    const int x = 21598;
    std::cout << 21598;
    for (int i = 0; i < 21598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21598(int& val) {
    const int x = 21598;
    std::cout << 21598;
    for (int i = 0; i < 21598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21599(int& val) {
    func_in_21599(val);
    const int x = 21599;
    std::cout << 21599;
    for (int i = 0; i < 21599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21599(int& val) {
    const int x = 21599;
    std::cout << 21599;
    for (int i = 0; i < 21599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21600(int& val) {
    func_in_21600(val);
    const int x = 21600;
    std::cout << 21600;
    for (int i = 0; i < 21600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21600(int& val) {
    const int x = 21600;
    std::cout << 21600;
    for (int i = 0; i < 21600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21601(int& val) {
    func_in_21601(val);
    const int x = 21601;
    std::cout << 21601;
    for (int i = 0; i < 21601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21601(int& val) {
    const int x = 21601;
    std::cout << 21601;
    for (int i = 0; i < 21601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21602(int& val) {
    func_in_21602(val);
    const int x = 21602;
    std::cout << 21602;
    for (int i = 0; i < 21602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21602(int& val) {
    const int x = 21602;
    std::cout << 21602;
    for (int i = 0; i < 21602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21603(int& val) {
    func_in_21603(val);
    const int x = 21603;
    std::cout << 21603;
    for (int i = 0; i < 21603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21603(int& val) {
    const int x = 21603;
    std::cout << 21603;
    for (int i = 0; i < 21603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21604(int& val) {
    func_in_21604(val);
    const int x = 21604;
    std::cout << 21604;
    for (int i = 0; i < 21604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21604(int& val) {
    const int x = 21604;
    std::cout << 21604;
    for (int i = 0; i < 21604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21605(int& val) {
    func_in_21605(val);
    const int x = 21605;
    std::cout << 21605;
    for (int i = 0; i < 21605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21605(int& val) {
    const int x = 21605;
    std::cout << 21605;
    for (int i = 0; i < 21605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21606(int& val) {
    func_in_21606(val);
    const int x = 21606;
    std::cout << 21606;
    for (int i = 0; i < 21606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21606(int& val) {
    const int x = 21606;
    std::cout << 21606;
    for (int i = 0; i < 21606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21607(int& val) {
    func_in_21607(val);
    const int x = 21607;
    std::cout << 21607;
    for (int i = 0; i < 21607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21607(int& val) {
    const int x = 21607;
    std::cout << 21607;
    for (int i = 0; i < 21607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21608(int& val) {
    func_in_21608(val);
    const int x = 21608;
    std::cout << 21608;
    for (int i = 0; i < 21608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21608(int& val) {
    const int x = 21608;
    std::cout << 21608;
    for (int i = 0; i < 21608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21609(int& val) {
    func_in_21609(val);
    const int x = 21609;
    std::cout << 21609;
    for (int i = 0; i < 21609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21609(int& val) {
    const int x = 21609;
    std::cout << 21609;
    for (int i = 0; i < 21609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21610(int& val) {
    func_in_21610(val);
    const int x = 21610;
    std::cout << 21610;
    for (int i = 0; i < 21610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21610(int& val) {
    const int x = 21610;
    std::cout << 21610;
    for (int i = 0; i < 21610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21611(int& val) {
    func_in_21611(val);
    const int x = 21611;
    std::cout << 21611;
    for (int i = 0; i < 21611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21611(int& val) {
    const int x = 21611;
    std::cout << 21611;
    for (int i = 0; i < 21611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21612(int& val) {
    func_in_21612(val);
    const int x = 21612;
    std::cout << 21612;
    for (int i = 0; i < 21612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21612(int& val) {
    const int x = 21612;
    std::cout << 21612;
    for (int i = 0; i < 21612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21613(int& val) {
    func_in_21613(val);
    const int x = 21613;
    std::cout << 21613;
    for (int i = 0; i < 21613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21613(int& val) {
    const int x = 21613;
    std::cout << 21613;
    for (int i = 0; i < 21613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21614(int& val) {
    func_in_21614(val);
    const int x = 21614;
    std::cout << 21614;
    for (int i = 0; i < 21614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21614(int& val) {
    const int x = 21614;
    std::cout << 21614;
    for (int i = 0; i < 21614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21615(int& val) {
    func_in_21615(val);
    const int x = 21615;
    std::cout << 21615;
    for (int i = 0; i < 21615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21615(int& val) {
    const int x = 21615;
    std::cout << 21615;
    for (int i = 0; i < 21615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21616(int& val) {
    func_in_21616(val);
    const int x = 21616;
    std::cout << 21616;
    for (int i = 0; i < 21616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21616(int& val) {
    const int x = 21616;
    std::cout << 21616;
    for (int i = 0; i < 21616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21617(int& val) {
    func_in_21617(val);
    const int x = 21617;
    std::cout << 21617;
    for (int i = 0; i < 21617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21617(int& val) {
    const int x = 21617;
    std::cout << 21617;
    for (int i = 0; i < 21617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21618(int& val) {
    func_in_21618(val);
    const int x = 21618;
    std::cout << 21618;
    for (int i = 0; i < 21618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21618(int& val) {
    const int x = 21618;
    std::cout << 21618;
    for (int i = 0; i < 21618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21619(int& val) {
    func_in_21619(val);
    const int x = 21619;
    std::cout << 21619;
    for (int i = 0; i < 21619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21619(int& val) {
    const int x = 21619;
    std::cout << 21619;
    for (int i = 0; i < 21619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21620(int& val) {
    func_in_21620(val);
    const int x = 21620;
    std::cout << 21620;
    for (int i = 0; i < 21620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21620(int& val) {
    const int x = 21620;
    std::cout << 21620;
    for (int i = 0; i < 21620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21621(int& val) {
    func_in_21621(val);
    const int x = 21621;
    std::cout << 21621;
    for (int i = 0; i < 21621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21621(int& val) {
    const int x = 21621;
    std::cout << 21621;
    for (int i = 0; i < 21621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21622(int& val) {
    func_in_21622(val);
    const int x = 21622;
    std::cout << 21622;
    for (int i = 0; i < 21622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21622(int& val) {
    const int x = 21622;
    std::cout << 21622;
    for (int i = 0; i < 21622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21623(int& val) {
    func_in_21623(val);
    const int x = 21623;
    std::cout << 21623;
    for (int i = 0; i < 21623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21623(int& val) {
    const int x = 21623;
    std::cout << 21623;
    for (int i = 0; i < 21623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21624(int& val) {
    func_in_21624(val);
    const int x = 21624;
    std::cout << 21624;
    for (int i = 0; i < 21624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21624(int& val) {
    const int x = 21624;
    std::cout << 21624;
    for (int i = 0; i < 21624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21625(int& val) {
    func_in_21625(val);
    const int x = 21625;
    std::cout << 21625;
    for (int i = 0; i < 21625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21625(int& val) {
    const int x = 21625;
    std::cout << 21625;
    for (int i = 0; i < 21625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21626(int& val) {
    func_in_21626(val);
    const int x = 21626;
    std::cout << 21626;
    for (int i = 0; i < 21626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21626(int& val) {
    const int x = 21626;
    std::cout << 21626;
    for (int i = 0; i < 21626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21627(int& val) {
    func_in_21627(val);
    const int x = 21627;
    std::cout << 21627;
    for (int i = 0; i < 21627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21627(int& val) {
    const int x = 21627;
    std::cout << 21627;
    for (int i = 0; i < 21627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21628(int& val) {
    func_in_21628(val);
    const int x = 21628;
    std::cout << 21628;
    for (int i = 0; i < 21628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21628(int& val) {
    const int x = 21628;
    std::cout << 21628;
    for (int i = 0; i < 21628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21629(int& val) {
    func_in_21629(val);
    const int x = 21629;
    std::cout << 21629;
    for (int i = 0; i < 21629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21629(int& val) {
    const int x = 21629;
    std::cout << 21629;
    for (int i = 0; i < 21629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21630(int& val) {
    func_in_21630(val);
    const int x = 21630;
    std::cout << 21630;
    for (int i = 0; i < 21630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21630(int& val) {
    const int x = 21630;
    std::cout << 21630;
    for (int i = 0; i < 21630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21631(int& val) {
    func_in_21631(val);
    const int x = 21631;
    std::cout << 21631;
    for (int i = 0; i < 21631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21631(int& val) {
    const int x = 21631;
    std::cout << 21631;
    for (int i = 0; i < 21631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21632(int& val) {
    func_in_21632(val);
    const int x = 21632;
    std::cout << 21632;
    for (int i = 0; i < 21632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21632(int& val) {
    const int x = 21632;
    std::cout << 21632;
    for (int i = 0; i < 21632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21633(int& val) {
    func_in_21633(val);
    const int x = 21633;
    std::cout << 21633;
    for (int i = 0; i < 21633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21633(int& val) {
    const int x = 21633;
    std::cout << 21633;
    for (int i = 0; i < 21633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21634(int& val) {
    func_in_21634(val);
    const int x = 21634;
    std::cout << 21634;
    for (int i = 0; i < 21634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21634(int& val) {
    const int x = 21634;
    std::cout << 21634;
    for (int i = 0; i < 21634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21635(int& val) {
    func_in_21635(val);
    const int x = 21635;
    std::cout << 21635;
    for (int i = 0; i < 21635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21635(int& val) {
    const int x = 21635;
    std::cout << 21635;
    for (int i = 0; i < 21635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21636(int& val) {
    func_in_21636(val);
    const int x = 21636;
    std::cout << 21636;
    for (int i = 0; i < 21636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21636(int& val) {
    const int x = 21636;
    std::cout << 21636;
    for (int i = 0; i < 21636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21637(int& val) {
    func_in_21637(val);
    const int x = 21637;
    std::cout << 21637;
    for (int i = 0; i < 21637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21637(int& val) {
    const int x = 21637;
    std::cout << 21637;
    for (int i = 0; i < 21637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21638(int& val) {
    func_in_21638(val);
    const int x = 21638;
    std::cout << 21638;
    for (int i = 0; i < 21638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21638(int& val) {
    const int x = 21638;
    std::cout << 21638;
    for (int i = 0; i < 21638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21639(int& val) {
    func_in_21639(val);
    const int x = 21639;
    std::cout << 21639;
    for (int i = 0; i < 21639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21639(int& val) {
    const int x = 21639;
    std::cout << 21639;
    for (int i = 0; i < 21639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21640(int& val) {
    func_in_21640(val);
    const int x = 21640;
    std::cout << 21640;
    for (int i = 0; i < 21640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21640(int& val) {
    const int x = 21640;
    std::cout << 21640;
    for (int i = 0; i < 21640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21641(int& val) {
    func_in_21641(val);
    const int x = 21641;
    std::cout << 21641;
    for (int i = 0; i < 21641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21641(int& val) {
    const int x = 21641;
    std::cout << 21641;
    for (int i = 0; i < 21641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21642(int& val) {
    func_in_21642(val);
    const int x = 21642;
    std::cout << 21642;
    for (int i = 0; i < 21642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21642(int& val) {
    const int x = 21642;
    std::cout << 21642;
    for (int i = 0; i < 21642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21643(int& val) {
    func_in_21643(val);
    const int x = 21643;
    std::cout << 21643;
    for (int i = 0; i < 21643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21643(int& val) {
    const int x = 21643;
    std::cout << 21643;
    for (int i = 0; i < 21643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21644(int& val) {
    func_in_21644(val);
    const int x = 21644;
    std::cout << 21644;
    for (int i = 0; i < 21644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21644(int& val) {
    const int x = 21644;
    std::cout << 21644;
    for (int i = 0; i < 21644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21645(int& val) {
    func_in_21645(val);
    const int x = 21645;
    std::cout << 21645;
    for (int i = 0; i < 21645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21645(int& val) {
    const int x = 21645;
    std::cout << 21645;
    for (int i = 0; i < 21645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21646(int& val) {
    func_in_21646(val);
    const int x = 21646;
    std::cout << 21646;
    for (int i = 0; i < 21646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21646(int& val) {
    const int x = 21646;
    std::cout << 21646;
    for (int i = 0; i < 21646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21647(int& val) {
    func_in_21647(val);
    const int x = 21647;
    std::cout << 21647;
    for (int i = 0; i < 21647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21647(int& val) {
    const int x = 21647;
    std::cout << 21647;
    for (int i = 0; i < 21647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21648(int& val) {
    func_in_21648(val);
    const int x = 21648;
    std::cout << 21648;
    for (int i = 0; i < 21648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21648(int& val) {
    const int x = 21648;
    std::cout << 21648;
    for (int i = 0; i < 21648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21649(int& val) {
    func_in_21649(val);
    const int x = 21649;
    std::cout << 21649;
    for (int i = 0; i < 21649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21649(int& val) {
    const int x = 21649;
    std::cout << 21649;
    for (int i = 0; i < 21649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21650(int& val) {
    func_in_21650(val);
    const int x = 21650;
    std::cout << 21650;
    for (int i = 0; i < 21650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21650(int& val) {
    const int x = 21650;
    std::cout << 21650;
    for (int i = 0; i < 21650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21651(int& val) {
    func_in_21651(val);
    const int x = 21651;
    std::cout << 21651;
    for (int i = 0; i < 21651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21651(int& val) {
    const int x = 21651;
    std::cout << 21651;
    for (int i = 0; i < 21651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21652(int& val) {
    func_in_21652(val);
    const int x = 21652;
    std::cout << 21652;
    for (int i = 0; i < 21652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21652(int& val) {
    const int x = 21652;
    std::cout << 21652;
    for (int i = 0; i < 21652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21653(int& val) {
    func_in_21653(val);
    const int x = 21653;
    std::cout << 21653;
    for (int i = 0; i < 21653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21653(int& val) {
    const int x = 21653;
    std::cout << 21653;
    for (int i = 0; i < 21653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21654(int& val) {
    func_in_21654(val);
    const int x = 21654;
    std::cout << 21654;
    for (int i = 0; i < 21654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21654(int& val) {
    const int x = 21654;
    std::cout << 21654;
    for (int i = 0; i < 21654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21655(int& val) {
    func_in_21655(val);
    const int x = 21655;
    std::cout << 21655;
    for (int i = 0; i < 21655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21655(int& val) {
    const int x = 21655;
    std::cout << 21655;
    for (int i = 0; i < 21655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21656(int& val) {
    func_in_21656(val);
    const int x = 21656;
    std::cout << 21656;
    for (int i = 0; i < 21656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21656(int& val) {
    const int x = 21656;
    std::cout << 21656;
    for (int i = 0; i < 21656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21657(int& val) {
    func_in_21657(val);
    const int x = 21657;
    std::cout << 21657;
    for (int i = 0; i < 21657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21657(int& val) {
    const int x = 21657;
    std::cout << 21657;
    for (int i = 0; i < 21657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21658(int& val) {
    func_in_21658(val);
    const int x = 21658;
    std::cout << 21658;
    for (int i = 0; i < 21658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21658(int& val) {
    const int x = 21658;
    std::cout << 21658;
    for (int i = 0; i < 21658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21659(int& val) {
    func_in_21659(val);
    const int x = 21659;
    std::cout << 21659;
    for (int i = 0; i < 21659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21659(int& val) {
    const int x = 21659;
    std::cout << 21659;
    for (int i = 0; i < 21659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21660(int& val) {
    func_in_21660(val);
    const int x = 21660;
    std::cout << 21660;
    for (int i = 0; i < 21660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21660(int& val) {
    const int x = 21660;
    std::cout << 21660;
    for (int i = 0; i < 21660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21661(int& val) {
    func_in_21661(val);
    const int x = 21661;
    std::cout << 21661;
    for (int i = 0; i < 21661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21661(int& val) {
    const int x = 21661;
    std::cout << 21661;
    for (int i = 0; i < 21661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21662(int& val) {
    func_in_21662(val);
    const int x = 21662;
    std::cout << 21662;
    for (int i = 0; i < 21662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21662(int& val) {
    const int x = 21662;
    std::cout << 21662;
    for (int i = 0; i < 21662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21663(int& val) {
    func_in_21663(val);
    const int x = 21663;
    std::cout << 21663;
    for (int i = 0; i < 21663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21663(int& val) {
    const int x = 21663;
    std::cout << 21663;
    for (int i = 0; i < 21663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21664(int& val) {
    func_in_21664(val);
    const int x = 21664;
    std::cout << 21664;
    for (int i = 0; i < 21664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21664(int& val) {
    const int x = 21664;
    std::cout << 21664;
    for (int i = 0; i < 21664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21665(int& val) {
    func_in_21665(val);
    const int x = 21665;
    std::cout << 21665;
    for (int i = 0; i < 21665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21665(int& val) {
    const int x = 21665;
    std::cout << 21665;
    for (int i = 0; i < 21665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21666(int& val) {
    func_in_21666(val);
    const int x = 21666;
    std::cout << 21666;
    for (int i = 0; i < 21666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21666(int& val) {
    const int x = 21666;
    std::cout << 21666;
    for (int i = 0; i < 21666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21667(int& val) {
    func_in_21667(val);
    const int x = 21667;
    std::cout << 21667;
    for (int i = 0; i < 21667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21667(int& val) {
    const int x = 21667;
    std::cout << 21667;
    for (int i = 0; i < 21667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21668(int& val) {
    func_in_21668(val);
    const int x = 21668;
    std::cout << 21668;
    for (int i = 0; i < 21668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21668(int& val) {
    const int x = 21668;
    std::cout << 21668;
    for (int i = 0; i < 21668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21669(int& val) {
    func_in_21669(val);
    const int x = 21669;
    std::cout << 21669;
    for (int i = 0; i < 21669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21669(int& val) {
    const int x = 21669;
    std::cout << 21669;
    for (int i = 0; i < 21669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21670(int& val) {
    func_in_21670(val);
    const int x = 21670;
    std::cout << 21670;
    for (int i = 0; i < 21670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21670(int& val) {
    const int x = 21670;
    std::cout << 21670;
    for (int i = 0; i < 21670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21671(int& val) {
    func_in_21671(val);
    const int x = 21671;
    std::cout << 21671;
    for (int i = 0; i < 21671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21671(int& val) {
    const int x = 21671;
    std::cout << 21671;
    for (int i = 0; i < 21671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21672(int& val) {
    func_in_21672(val);
    const int x = 21672;
    std::cout << 21672;
    for (int i = 0; i < 21672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21672(int& val) {
    const int x = 21672;
    std::cout << 21672;
    for (int i = 0; i < 21672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21673(int& val) {
    func_in_21673(val);
    const int x = 21673;
    std::cout << 21673;
    for (int i = 0; i < 21673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21673(int& val) {
    const int x = 21673;
    std::cout << 21673;
    for (int i = 0; i < 21673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21674(int& val) {
    func_in_21674(val);
    const int x = 21674;
    std::cout << 21674;
    for (int i = 0; i < 21674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21674(int& val) {
    const int x = 21674;
    std::cout << 21674;
    for (int i = 0; i < 21674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21675(int& val) {
    func_in_21675(val);
    const int x = 21675;
    std::cout << 21675;
    for (int i = 0; i < 21675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21675(int& val) {
    const int x = 21675;
    std::cout << 21675;
    for (int i = 0; i < 21675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21676(int& val) {
    func_in_21676(val);
    const int x = 21676;
    std::cout << 21676;
    for (int i = 0; i < 21676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21676(int& val) {
    const int x = 21676;
    std::cout << 21676;
    for (int i = 0; i < 21676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21677(int& val) {
    func_in_21677(val);
    const int x = 21677;
    std::cout << 21677;
    for (int i = 0; i < 21677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21677(int& val) {
    const int x = 21677;
    std::cout << 21677;
    for (int i = 0; i < 21677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21678(int& val) {
    func_in_21678(val);
    const int x = 21678;
    std::cout << 21678;
    for (int i = 0; i < 21678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21678(int& val) {
    const int x = 21678;
    std::cout << 21678;
    for (int i = 0; i < 21678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21679(int& val) {
    func_in_21679(val);
    const int x = 21679;
    std::cout << 21679;
    for (int i = 0; i < 21679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21679(int& val) {
    const int x = 21679;
    std::cout << 21679;
    for (int i = 0; i < 21679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21680(int& val) {
    func_in_21680(val);
    const int x = 21680;
    std::cout << 21680;
    for (int i = 0; i < 21680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21680(int& val) {
    const int x = 21680;
    std::cout << 21680;
    for (int i = 0; i < 21680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21681(int& val) {
    func_in_21681(val);
    const int x = 21681;
    std::cout << 21681;
    for (int i = 0; i < 21681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21681(int& val) {
    const int x = 21681;
    std::cout << 21681;
    for (int i = 0; i < 21681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21682(int& val) {
    func_in_21682(val);
    const int x = 21682;
    std::cout << 21682;
    for (int i = 0; i < 21682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21682(int& val) {
    const int x = 21682;
    std::cout << 21682;
    for (int i = 0; i < 21682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21683(int& val) {
    func_in_21683(val);
    const int x = 21683;
    std::cout << 21683;
    for (int i = 0; i < 21683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21683(int& val) {
    const int x = 21683;
    std::cout << 21683;
    for (int i = 0; i < 21683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21684(int& val) {
    func_in_21684(val);
    const int x = 21684;
    std::cout << 21684;
    for (int i = 0; i < 21684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21684(int& val) {
    const int x = 21684;
    std::cout << 21684;
    for (int i = 0; i < 21684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21685(int& val) {
    func_in_21685(val);
    const int x = 21685;
    std::cout << 21685;
    for (int i = 0; i < 21685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21685(int& val) {
    const int x = 21685;
    std::cout << 21685;
    for (int i = 0; i < 21685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21686(int& val) {
    func_in_21686(val);
    const int x = 21686;
    std::cout << 21686;
    for (int i = 0; i < 21686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21686(int& val) {
    const int x = 21686;
    std::cout << 21686;
    for (int i = 0; i < 21686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21687(int& val) {
    func_in_21687(val);
    const int x = 21687;
    std::cout << 21687;
    for (int i = 0; i < 21687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21687(int& val) {
    const int x = 21687;
    std::cout << 21687;
    for (int i = 0; i < 21687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21688(int& val) {
    func_in_21688(val);
    const int x = 21688;
    std::cout << 21688;
    for (int i = 0; i < 21688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21688(int& val) {
    const int x = 21688;
    std::cout << 21688;
    for (int i = 0; i < 21688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21689(int& val) {
    func_in_21689(val);
    const int x = 21689;
    std::cout << 21689;
    for (int i = 0; i < 21689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21689(int& val) {
    const int x = 21689;
    std::cout << 21689;
    for (int i = 0; i < 21689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21690(int& val) {
    func_in_21690(val);
    const int x = 21690;
    std::cout << 21690;
    for (int i = 0; i < 21690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21690(int& val) {
    const int x = 21690;
    std::cout << 21690;
    for (int i = 0; i < 21690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21691(int& val) {
    func_in_21691(val);
    const int x = 21691;
    std::cout << 21691;
    for (int i = 0; i < 21691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21691(int& val) {
    const int x = 21691;
    std::cout << 21691;
    for (int i = 0; i < 21691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21692(int& val) {
    func_in_21692(val);
    const int x = 21692;
    std::cout << 21692;
    for (int i = 0; i < 21692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21692(int& val) {
    const int x = 21692;
    std::cout << 21692;
    for (int i = 0; i < 21692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21693(int& val) {
    func_in_21693(val);
    const int x = 21693;
    std::cout << 21693;
    for (int i = 0; i < 21693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21693(int& val) {
    const int x = 21693;
    std::cout << 21693;
    for (int i = 0; i < 21693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21694(int& val) {
    func_in_21694(val);
    const int x = 21694;
    std::cout << 21694;
    for (int i = 0; i < 21694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21694(int& val) {
    const int x = 21694;
    std::cout << 21694;
    for (int i = 0; i < 21694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21695(int& val) {
    func_in_21695(val);
    const int x = 21695;
    std::cout << 21695;
    for (int i = 0; i < 21695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21695(int& val) {
    const int x = 21695;
    std::cout << 21695;
    for (int i = 0; i < 21695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21696(int& val) {
    func_in_21696(val);
    const int x = 21696;
    std::cout << 21696;
    for (int i = 0; i < 21696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21696(int& val) {
    const int x = 21696;
    std::cout << 21696;
    for (int i = 0; i < 21696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21697(int& val) {
    func_in_21697(val);
    const int x = 21697;
    std::cout << 21697;
    for (int i = 0; i < 21697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21697(int& val) {
    const int x = 21697;
    std::cout << 21697;
    for (int i = 0; i < 21697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21698(int& val) {
    func_in_21698(val);
    const int x = 21698;
    std::cout << 21698;
    for (int i = 0; i < 21698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21698(int& val) {
    const int x = 21698;
    std::cout << 21698;
    for (int i = 0; i < 21698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21699(int& val) {
    func_in_21699(val);
    const int x = 21699;
    std::cout << 21699;
    for (int i = 0; i < 21699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21699(int& val) {
    const int x = 21699;
    std::cout << 21699;
    for (int i = 0; i < 21699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21700(int& val) {
    func_in_21700(val);
    const int x = 21700;
    std::cout << 21700;
    for (int i = 0; i < 21700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21700(int& val) {
    const int x = 21700;
    std::cout << 21700;
    for (int i = 0; i < 21700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21701(int& val) {
    func_in_21701(val);
    const int x = 21701;
    std::cout << 21701;
    for (int i = 0; i < 21701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21701(int& val) {
    const int x = 21701;
    std::cout << 21701;
    for (int i = 0; i < 21701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21702(int& val) {
    func_in_21702(val);
    const int x = 21702;
    std::cout << 21702;
    for (int i = 0; i < 21702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21702(int& val) {
    const int x = 21702;
    std::cout << 21702;
    for (int i = 0; i < 21702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21703(int& val) {
    func_in_21703(val);
    const int x = 21703;
    std::cout << 21703;
    for (int i = 0; i < 21703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21703(int& val) {
    const int x = 21703;
    std::cout << 21703;
    for (int i = 0; i < 21703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21704(int& val) {
    func_in_21704(val);
    const int x = 21704;
    std::cout << 21704;
    for (int i = 0; i < 21704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21704(int& val) {
    const int x = 21704;
    std::cout << 21704;
    for (int i = 0; i < 21704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21705(int& val) {
    func_in_21705(val);
    const int x = 21705;
    std::cout << 21705;
    for (int i = 0; i < 21705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21705(int& val) {
    const int x = 21705;
    std::cout << 21705;
    for (int i = 0; i < 21705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21706(int& val) {
    func_in_21706(val);
    const int x = 21706;
    std::cout << 21706;
    for (int i = 0; i < 21706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21706(int& val) {
    const int x = 21706;
    std::cout << 21706;
    for (int i = 0; i < 21706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21707(int& val) {
    func_in_21707(val);
    const int x = 21707;
    std::cout << 21707;
    for (int i = 0; i < 21707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21707(int& val) {
    const int x = 21707;
    std::cout << 21707;
    for (int i = 0; i < 21707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21708(int& val) {
    func_in_21708(val);
    const int x = 21708;
    std::cout << 21708;
    for (int i = 0; i < 21708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21708(int& val) {
    const int x = 21708;
    std::cout << 21708;
    for (int i = 0; i < 21708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21709(int& val) {
    func_in_21709(val);
    const int x = 21709;
    std::cout << 21709;
    for (int i = 0; i < 21709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21709(int& val) {
    const int x = 21709;
    std::cout << 21709;
    for (int i = 0; i < 21709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21710(int& val) {
    func_in_21710(val);
    const int x = 21710;
    std::cout << 21710;
    for (int i = 0; i < 21710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21710(int& val) {
    const int x = 21710;
    std::cout << 21710;
    for (int i = 0; i < 21710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21711(int& val) {
    func_in_21711(val);
    const int x = 21711;
    std::cout << 21711;
    for (int i = 0; i < 21711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21711(int& val) {
    const int x = 21711;
    std::cout << 21711;
    for (int i = 0; i < 21711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21712(int& val) {
    func_in_21712(val);
    const int x = 21712;
    std::cout << 21712;
    for (int i = 0; i < 21712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21712(int& val) {
    const int x = 21712;
    std::cout << 21712;
    for (int i = 0; i < 21712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21713(int& val) {
    func_in_21713(val);
    const int x = 21713;
    std::cout << 21713;
    for (int i = 0; i < 21713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21713(int& val) {
    const int x = 21713;
    std::cout << 21713;
    for (int i = 0; i < 21713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21714(int& val) {
    func_in_21714(val);
    const int x = 21714;
    std::cout << 21714;
    for (int i = 0; i < 21714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21714(int& val) {
    const int x = 21714;
    std::cout << 21714;
    for (int i = 0; i < 21714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21715(int& val) {
    func_in_21715(val);
    const int x = 21715;
    std::cout << 21715;
    for (int i = 0; i < 21715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21715(int& val) {
    const int x = 21715;
    std::cout << 21715;
    for (int i = 0; i < 21715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21716(int& val) {
    func_in_21716(val);
    const int x = 21716;
    std::cout << 21716;
    for (int i = 0; i < 21716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21716(int& val) {
    const int x = 21716;
    std::cout << 21716;
    for (int i = 0; i < 21716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21717(int& val) {
    func_in_21717(val);
    const int x = 21717;
    std::cout << 21717;
    for (int i = 0; i < 21717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21717(int& val) {
    const int x = 21717;
    std::cout << 21717;
    for (int i = 0; i < 21717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21718(int& val) {
    func_in_21718(val);
    const int x = 21718;
    std::cout << 21718;
    for (int i = 0; i < 21718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21718(int& val) {
    const int x = 21718;
    std::cout << 21718;
    for (int i = 0; i < 21718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21719(int& val) {
    func_in_21719(val);
    const int x = 21719;
    std::cout << 21719;
    for (int i = 0; i < 21719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21719(int& val) {
    const int x = 21719;
    std::cout << 21719;
    for (int i = 0; i < 21719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21720(int& val) {
    func_in_21720(val);
    const int x = 21720;
    std::cout << 21720;
    for (int i = 0; i < 21720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21720(int& val) {
    const int x = 21720;
    std::cout << 21720;
    for (int i = 0; i < 21720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21721(int& val) {
    func_in_21721(val);
    const int x = 21721;
    std::cout << 21721;
    for (int i = 0; i < 21721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21721(int& val) {
    const int x = 21721;
    std::cout << 21721;
    for (int i = 0; i < 21721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21722(int& val) {
    func_in_21722(val);
    const int x = 21722;
    std::cout << 21722;
    for (int i = 0; i < 21722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21722(int& val) {
    const int x = 21722;
    std::cout << 21722;
    for (int i = 0; i < 21722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21723(int& val) {
    func_in_21723(val);
    const int x = 21723;
    std::cout << 21723;
    for (int i = 0; i < 21723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21723(int& val) {
    const int x = 21723;
    std::cout << 21723;
    for (int i = 0; i < 21723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21724(int& val) {
    func_in_21724(val);
    const int x = 21724;
    std::cout << 21724;
    for (int i = 0; i < 21724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21724(int& val) {
    const int x = 21724;
    std::cout << 21724;
    for (int i = 0; i < 21724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21725(int& val) {
    func_in_21725(val);
    const int x = 21725;
    std::cout << 21725;
    for (int i = 0; i < 21725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21725(int& val) {
    const int x = 21725;
    std::cout << 21725;
    for (int i = 0; i < 21725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21726(int& val) {
    func_in_21726(val);
    const int x = 21726;
    std::cout << 21726;
    for (int i = 0; i < 21726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21726(int& val) {
    const int x = 21726;
    std::cout << 21726;
    for (int i = 0; i < 21726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21727(int& val) {
    func_in_21727(val);
    const int x = 21727;
    std::cout << 21727;
    for (int i = 0; i < 21727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21727(int& val) {
    const int x = 21727;
    std::cout << 21727;
    for (int i = 0; i < 21727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21728(int& val) {
    func_in_21728(val);
    const int x = 21728;
    std::cout << 21728;
    for (int i = 0; i < 21728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21728(int& val) {
    const int x = 21728;
    std::cout << 21728;
    for (int i = 0; i < 21728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21729(int& val) {
    func_in_21729(val);
    const int x = 21729;
    std::cout << 21729;
    for (int i = 0; i < 21729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21729(int& val) {
    const int x = 21729;
    std::cout << 21729;
    for (int i = 0; i < 21729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21730(int& val) {
    func_in_21730(val);
    const int x = 21730;
    std::cout << 21730;
    for (int i = 0; i < 21730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21730(int& val) {
    const int x = 21730;
    std::cout << 21730;
    for (int i = 0; i < 21730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21731(int& val) {
    func_in_21731(val);
    const int x = 21731;
    std::cout << 21731;
    for (int i = 0; i < 21731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21731(int& val) {
    const int x = 21731;
    std::cout << 21731;
    for (int i = 0; i < 21731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21732(int& val) {
    func_in_21732(val);
    const int x = 21732;
    std::cout << 21732;
    for (int i = 0; i < 21732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21732(int& val) {
    const int x = 21732;
    std::cout << 21732;
    for (int i = 0; i < 21732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21733(int& val) {
    func_in_21733(val);
    const int x = 21733;
    std::cout << 21733;
    for (int i = 0; i < 21733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21733(int& val) {
    const int x = 21733;
    std::cout << 21733;
    for (int i = 0; i < 21733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21734(int& val) {
    func_in_21734(val);
    const int x = 21734;
    std::cout << 21734;
    for (int i = 0; i < 21734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21734(int& val) {
    const int x = 21734;
    std::cout << 21734;
    for (int i = 0; i < 21734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21735(int& val) {
    func_in_21735(val);
    const int x = 21735;
    std::cout << 21735;
    for (int i = 0; i < 21735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21735(int& val) {
    const int x = 21735;
    std::cout << 21735;
    for (int i = 0; i < 21735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21736(int& val) {
    func_in_21736(val);
    const int x = 21736;
    std::cout << 21736;
    for (int i = 0; i < 21736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21736(int& val) {
    const int x = 21736;
    std::cout << 21736;
    for (int i = 0; i < 21736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21737(int& val) {
    func_in_21737(val);
    const int x = 21737;
    std::cout << 21737;
    for (int i = 0; i < 21737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21737(int& val) {
    const int x = 21737;
    std::cout << 21737;
    for (int i = 0; i < 21737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21738(int& val) {
    func_in_21738(val);
    const int x = 21738;
    std::cout << 21738;
    for (int i = 0; i < 21738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21738(int& val) {
    const int x = 21738;
    std::cout << 21738;
    for (int i = 0; i < 21738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21739(int& val) {
    func_in_21739(val);
    const int x = 21739;
    std::cout << 21739;
    for (int i = 0; i < 21739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21739(int& val) {
    const int x = 21739;
    std::cout << 21739;
    for (int i = 0; i < 21739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21740(int& val) {
    func_in_21740(val);
    const int x = 21740;
    std::cout << 21740;
    for (int i = 0; i < 21740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21740(int& val) {
    const int x = 21740;
    std::cout << 21740;
    for (int i = 0; i < 21740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21741(int& val) {
    func_in_21741(val);
    const int x = 21741;
    std::cout << 21741;
    for (int i = 0; i < 21741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21741(int& val) {
    const int x = 21741;
    std::cout << 21741;
    for (int i = 0; i < 21741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21742(int& val) {
    func_in_21742(val);
    const int x = 21742;
    std::cout << 21742;
    for (int i = 0; i < 21742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21742(int& val) {
    const int x = 21742;
    std::cout << 21742;
    for (int i = 0; i < 21742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21743(int& val) {
    func_in_21743(val);
    const int x = 21743;
    std::cout << 21743;
    for (int i = 0; i < 21743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21743(int& val) {
    const int x = 21743;
    std::cout << 21743;
    for (int i = 0; i < 21743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21744(int& val) {
    func_in_21744(val);
    const int x = 21744;
    std::cout << 21744;
    for (int i = 0; i < 21744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21744(int& val) {
    const int x = 21744;
    std::cout << 21744;
    for (int i = 0; i < 21744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21745(int& val) {
    func_in_21745(val);
    const int x = 21745;
    std::cout << 21745;
    for (int i = 0; i < 21745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21745(int& val) {
    const int x = 21745;
    std::cout << 21745;
    for (int i = 0; i < 21745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21746(int& val) {
    func_in_21746(val);
    const int x = 21746;
    std::cout << 21746;
    for (int i = 0; i < 21746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21746(int& val) {
    const int x = 21746;
    std::cout << 21746;
    for (int i = 0; i < 21746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21747(int& val) {
    func_in_21747(val);
    const int x = 21747;
    std::cout << 21747;
    for (int i = 0; i < 21747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21747(int& val) {
    const int x = 21747;
    std::cout << 21747;
    for (int i = 0; i < 21747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21748(int& val) {
    func_in_21748(val);
    const int x = 21748;
    std::cout << 21748;
    for (int i = 0; i < 21748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21748(int& val) {
    const int x = 21748;
    std::cout << 21748;
    for (int i = 0; i < 21748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21749(int& val) {
    func_in_21749(val);
    const int x = 21749;
    std::cout << 21749;
    for (int i = 0; i < 21749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21749(int& val) {
    const int x = 21749;
    std::cout << 21749;
    for (int i = 0; i < 21749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21750(int& val) {
    func_in_21750(val);
    const int x = 21750;
    std::cout << 21750;
    for (int i = 0; i < 21750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21750(int& val) {
    const int x = 21750;
    std::cout << 21750;
    for (int i = 0; i < 21750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21751(int& val) {
    func_in_21751(val);
    const int x = 21751;
    std::cout << 21751;
    for (int i = 0; i < 21751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21751(int& val) {
    const int x = 21751;
    std::cout << 21751;
    for (int i = 0; i < 21751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21752(int& val) {
    func_in_21752(val);
    const int x = 21752;
    std::cout << 21752;
    for (int i = 0; i < 21752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21752(int& val) {
    const int x = 21752;
    std::cout << 21752;
    for (int i = 0; i < 21752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21753(int& val) {
    func_in_21753(val);
    const int x = 21753;
    std::cout << 21753;
    for (int i = 0; i < 21753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21753(int& val) {
    const int x = 21753;
    std::cout << 21753;
    for (int i = 0; i < 21753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21754(int& val) {
    func_in_21754(val);
    const int x = 21754;
    std::cout << 21754;
    for (int i = 0; i < 21754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21754(int& val) {
    const int x = 21754;
    std::cout << 21754;
    for (int i = 0; i < 21754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21755(int& val) {
    func_in_21755(val);
    const int x = 21755;
    std::cout << 21755;
    for (int i = 0; i < 21755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21755(int& val) {
    const int x = 21755;
    std::cout << 21755;
    for (int i = 0; i < 21755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21756(int& val) {
    func_in_21756(val);
    const int x = 21756;
    std::cout << 21756;
    for (int i = 0; i < 21756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21756(int& val) {
    const int x = 21756;
    std::cout << 21756;
    for (int i = 0; i < 21756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21757(int& val) {
    func_in_21757(val);
    const int x = 21757;
    std::cout << 21757;
    for (int i = 0; i < 21757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21757(int& val) {
    const int x = 21757;
    std::cout << 21757;
    for (int i = 0; i < 21757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21758(int& val) {
    func_in_21758(val);
    const int x = 21758;
    std::cout << 21758;
    for (int i = 0; i < 21758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21758(int& val) {
    const int x = 21758;
    std::cout << 21758;
    for (int i = 0; i < 21758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21759(int& val) {
    func_in_21759(val);
    const int x = 21759;
    std::cout << 21759;
    for (int i = 0; i < 21759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21759(int& val) {
    const int x = 21759;
    std::cout << 21759;
    for (int i = 0; i < 21759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21760(int& val) {
    func_in_21760(val);
    const int x = 21760;
    std::cout << 21760;
    for (int i = 0; i < 21760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21760(int& val) {
    const int x = 21760;
    std::cout << 21760;
    for (int i = 0; i < 21760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21761(int& val) {
    func_in_21761(val);
    const int x = 21761;
    std::cout << 21761;
    for (int i = 0; i < 21761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21761(int& val) {
    const int x = 21761;
    std::cout << 21761;
    for (int i = 0; i < 21761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21762(int& val) {
    func_in_21762(val);
    const int x = 21762;
    std::cout << 21762;
    for (int i = 0; i < 21762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21762(int& val) {
    const int x = 21762;
    std::cout << 21762;
    for (int i = 0; i < 21762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21763(int& val) {
    func_in_21763(val);
    const int x = 21763;
    std::cout << 21763;
    for (int i = 0; i < 21763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21763(int& val) {
    const int x = 21763;
    std::cout << 21763;
    for (int i = 0; i < 21763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21764(int& val) {
    func_in_21764(val);
    const int x = 21764;
    std::cout << 21764;
    for (int i = 0; i < 21764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21764(int& val) {
    const int x = 21764;
    std::cout << 21764;
    for (int i = 0; i < 21764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21765(int& val) {
    func_in_21765(val);
    const int x = 21765;
    std::cout << 21765;
    for (int i = 0; i < 21765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21765(int& val) {
    const int x = 21765;
    std::cout << 21765;
    for (int i = 0; i < 21765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21766(int& val) {
    func_in_21766(val);
    const int x = 21766;
    std::cout << 21766;
    for (int i = 0; i < 21766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21766(int& val) {
    const int x = 21766;
    std::cout << 21766;
    for (int i = 0; i < 21766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21767(int& val) {
    func_in_21767(val);
    const int x = 21767;
    std::cout << 21767;
    for (int i = 0; i < 21767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21767(int& val) {
    const int x = 21767;
    std::cout << 21767;
    for (int i = 0; i < 21767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21768(int& val) {
    func_in_21768(val);
    const int x = 21768;
    std::cout << 21768;
    for (int i = 0; i < 21768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21768(int& val) {
    const int x = 21768;
    std::cout << 21768;
    for (int i = 0; i < 21768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21769(int& val) {
    func_in_21769(val);
    const int x = 21769;
    std::cout << 21769;
    for (int i = 0; i < 21769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21769(int& val) {
    const int x = 21769;
    std::cout << 21769;
    for (int i = 0; i < 21769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21770(int& val) {
    func_in_21770(val);
    const int x = 21770;
    std::cout << 21770;
    for (int i = 0; i < 21770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21770(int& val) {
    const int x = 21770;
    std::cout << 21770;
    for (int i = 0; i < 21770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21771(int& val) {
    func_in_21771(val);
    const int x = 21771;
    std::cout << 21771;
    for (int i = 0; i < 21771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21771(int& val) {
    const int x = 21771;
    std::cout << 21771;
    for (int i = 0; i < 21771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21772(int& val) {
    func_in_21772(val);
    const int x = 21772;
    std::cout << 21772;
    for (int i = 0; i < 21772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21772(int& val) {
    const int x = 21772;
    std::cout << 21772;
    for (int i = 0; i < 21772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21773(int& val) {
    func_in_21773(val);
    const int x = 21773;
    std::cout << 21773;
    for (int i = 0; i < 21773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21773(int& val) {
    const int x = 21773;
    std::cout << 21773;
    for (int i = 0; i < 21773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21774(int& val) {
    func_in_21774(val);
    const int x = 21774;
    std::cout << 21774;
    for (int i = 0; i < 21774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21774(int& val) {
    const int x = 21774;
    std::cout << 21774;
    for (int i = 0; i < 21774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21775(int& val) {
    func_in_21775(val);
    const int x = 21775;
    std::cout << 21775;
    for (int i = 0; i < 21775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21775(int& val) {
    const int x = 21775;
    std::cout << 21775;
    for (int i = 0; i < 21775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21776(int& val) {
    func_in_21776(val);
    const int x = 21776;
    std::cout << 21776;
    for (int i = 0; i < 21776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21776(int& val) {
    const int x = 21776;
    std::cout << 21776;
    for (int i = 0; i < 21776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21777(int& val) {
    func_in_21777(val);
    const int x = 21777;
    std::cout << 21777;
    for (int i = 0; i < 21777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21777(int& val) {
    const int x = 21777;
    std::cout << 21777;
    for (int i = 0; i < 21777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21778(int& val) {
    func_in_21778(val);
    const int x = 21778;
    std::cout << 21778;
    for (int i = 0; i < 21778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21778(int& val) {
    const int x = 21778;
    std::cout << 21778;
    for (int i = 0; i < 21778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21779(int& val) {
    func_in_21779(val);
    const int x = 21779;
    std::cout << 21779;
    for (int i = 0; i < 21779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21779(int& val) {
    const int x = 21779;
    std::cout << 21779;
    for (int i = 0; i < 21779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21780(int& val) {
    func_in_21780(val);
    const int x = 21780;
    std::cout << 21780;
    for (int i = 0; i < 21780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21780(int& val) {
    const int x = 21780;
    std::cout << 21780;
    for (int i = 0; i < 21780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21781(int& val) {
    func_in_21781(val);
    const int x = 21781;
    std::cout << 21781;
    for (int i = 0; i < 21781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21781(int& val) {
    const int x = 21781;
    std::cout << 21781;
    for (int i = 0; i < 21781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21782(int& val) {
    func_in_21782(val);
    const int x = 21782;
    std::cout << 21782;
    for (int i = 0; i < 21782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21782(int& val) {
    const int x = 21782;
    std::cout << 21782;
    for (int i = 0; i < 21782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21783(int& val) {
    func_in_21783(val);
    const int x = 21783;
    std::cout << 21783;
    for (int i = 0; i < 21783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21783(int& val) {
    const int x = 21783;
    std::cout << 21783;
    for (int i = 0; i < 21783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21784(int& val) {
    func_in_21784(val);
    const int x = 21784;
    std::cout << 21784;
    for (int i = 0; i < 21784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21784(int& val) {
    const int x = 21784;
    std::cout << 21784;
    for (int i = 0; i < 21784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21785(int& val) {
    func_in_21785(val);
    const int x = 21785;
    std::cout << 21785;
    for (int i = 0; i < 21785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21785(int& val) {
    const int x = 21785;
    std::cout << 21785;
    for (int i = 0; i < 21785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21786(int& val) {
    func_in_21786(val);
    const int x = 21786;
    std::cout << 21786;
    for (int i = 0; i < 21786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21786(int& val) {
    const int x = 21786;
    std::cout << 21786;
    for (int i = 0; i < 21786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21787(int& val) {
    func_in_21787(val);
    const int x = 21787;
    std::cout << 21787;
    for (int i = 0; i < 21787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21787(int& val) {
    const int x = 21787;
    std::cout << 21787;
    for (int i = 0; i < 21787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21788(int& val) {
    func_in_21788(val);
    const int x = 21788;
    std::cout << 21788;
    for (int i = 0; i < 21788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21788(int& val) {
    const int x = 21788;
    std::cout << 21788;
    for (int i = 0; i < 21788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21789(int& val) {
    func_in_21789(val);
    const int x = 21789;
    std::cout << 21789;
    for (int i = 0; i < 21789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21789(int& val) {
    const int x = 21789;
    std::cout << 21789;
    for (int i = 0; i < 21789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21790(int& val) {
    func_in_21790(val);
    const int x = 21790;
    std::cout << 21790;
    for (int i = 0; i < 21790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21790(int& val) {
    const int x = 21790;
    std::cout << 21790;
    for (int i = 0; i < 21790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21791(int& val) {
    func_in_21791(val);
    const int x = 21791;
    std::cout << 21791;
    for (int i = 0; i < 21791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21791(int& val) {
    const int x = 21791;
    std::cout << 21791;
    for (int i = 0; i < 21791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21792(int& val) {
    func_in_21792(val);
    const int x = 21792;
    std::cout << 21792;
    for (int i = 0; i < 21792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21792(int& val) {
    const int x = 21792;
    std::cout << 21792;
    for (int i = 0; i < 21792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21793(int& val) {
    func_in_21793(val);
    const int x = 21793;
    std::cout << 21793;
    for (int i = 0; i < 21793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21793(int& val) {
    const int x = 21793;
    std::cout << 21793;
    for (int i = 0; i < 21793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21794(int& val) {
    func_in_21794(val);
    const int x = 21794;
    std::cout << 21794;
    for (int i = 0; i < 21794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21794(int& val) {
    const int x = 21794;
    std::cout << 21794;
    for (int i = 0; i < 21794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21795(int& val) {
    func_in_21795(val);
    const int x = 21795;
    std::cout << 21795;
    for (int i = 0; i < 21795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21795(int& val) {
    const int x = 21795;
    std::cout << 21795;
    for (int i = 0; i < 21795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21796(int& val) {
    func_in_21796(val);
    const int x = 21796;
    std::cout << 21796;
    for (int i = 0; i < 21796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21796(int& val) {
    const int x = 21796;
    std::cout << 21796;
    for (int i = 0; i < 21796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21797(int& val) {
    func_in_21797(val);
    const int x = 21797;
    std::cout << 21797;
    for (int i = 0; i < 21797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21797(int& val) {
    const int x = 21797;
    std::cout << 21797;
    for (int i = 0; i < 21797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21798(int& val) {
    func_in_21798(val);
    const int x = 21798;
    std::cout << 21798;
    for (int i = 0; i < 21798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21798(int& val) {
    const int x = 21798;
    std::cout << 21798;
    for (int i = 0; i < 21798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21799(int& val) {
    func_in_21799(val);
    const int x = 21799;
    std::cout << 21799;
    for (int i = 0; i < 21799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21799(int& val) {
    const int x = 21799;
    std::cout << 21799;
    for (int i = 0; i < 21799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21800(int& val) {
    func_in_21800(val);
    const int x = 21800;
    std::cout << 21800;
    for (int i = 0; i < 21800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21800(int& val) {
    const int x = 21800;
    std::cout << 21800;
    for (int i = 0; i < 21800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21801(int& val) {
    func_in_21801(val);
    const int x = 21801;
    std::cout << 21801;
    for (int i = 0; i < 21801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21801(int& val) {
    const int x = 21801;
    std::cout << 21801;
    for (int i = 0; i < 21801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21802(int& val) {
    func_in_21802(val);
    const int x = 21802;
    std::cout << 21802;
    for (int i = 0; i < 21802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21802(int& val) {
    const int x = 21802;
    std::cout << 21802;
    for (int i = 0; i < 21802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21803(int& val) {
    func_in_21803(val);
    const int x = 21803;
    std::cout << 21803;
    for (int i = 0; i < 21803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21803(int& val) {
    const int x = 21803;
    std::cout << 21803;
    for (int i = 0; i < 21803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21804(int& val) {
    func_in_21804(val);
    const int x = 21804;
    std::cout << 21804;
    for (int i = 0; i < 21804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21804(int& val) {
    const int x = 21804;
    std::cout << 21804;
    for (int i = 0; i < 21804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21805(int& val) {
    func_in_21805(val);
    const int x = 21805;
    std::cout << 21805;
    for (int i = 0; i < 21805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21805(int& val) {
    const int x = 21805;
    std::cout << 21805;
    for (int i = 0; i < 21805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21806(int& val) {
    func_in_21806(val);
    const int x = 21806;
    std::cout << 21806;
    for (int i = 0; i < 21806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21806(int& val) {
    const int x = 21806;
    std::cout << 21806;
    for (int i = 0; i < 21806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21807(int& val) {
    func_in_21807(val);
    const int x = 21807;
    std::cout << 21807;
    for (int i = 0; i < 21807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21807(int& val) {
    const int x = 21807;
    std::cout << 21807;
    for (int i = 0; i < 21807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21808(int& val) {
    func_in_21808(val);
    const int x = 21808;
    std::cout << 21808;
    for (int i = 0; i < 21808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21808(int& val) {
    const int x = 21808;
    std::cout << 21808;
    for (int i = 0; i < 21808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21809(int& val) {
    func_in_21809(val);
    const int x = 21809;
    std::cout << 21809;
    for (int i = 0; i < 21809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21809(int& val) {
    const int x = 21809;
    std::cout << 21809;
    for (int i = 0; i < 21809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21810(int& val) {
    func_in_21810(val);
    const int x = 21810;
    std::cout << 21810;
    for (int i = 0; i < 21810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21810(int& val) {
    const int x = 21810;
    std::cout << 21810;
    for (int i = 0; i < 21810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21811(int& val) {
    func_in_21811(val);
    const int x = 21811;
    std::cout << 21811;
    for (int i = 0; i < 21811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21811(int& val) {
    const int x = 21811;
    std::cout << 21811;
    for (int i = 0; i < 21811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21812(int& val) {
    func_in_21812(val);
    const int x = 21812;
    std::cout << 21812;
    for (int i = 0; i < 21812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21812(int& val) {
    const int x = 21812;
    std::cout << 21812;
    for (int i = 0; i < 21812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21813(int& val) {
    func_in_21813(val);
    const int x = 21813;
    std::cout << 21813;
    for (int i = 0; i < 21813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21813(int& val) {
    const int x = 21813;
    std::cout << 21813;
    for (int i = 0; i < 21813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21814(int& val) {
    func_in_21814(val);
    const int x = 21814;
    std::cout << 21814;
    for (int i = 0; i < 21814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21814(int& val) {
    const int x = 21814;
    std::cout << 21814;
    for (int i = 0; i < 21814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21815(int& val) {
    func_in_21815(val);
    const int x = 21815;
    std::cout << 21815;
    for (int i = 0; i < 21815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21815(int& val) {
    const int x = 21815;
    std::cout << 21815;
    for (int i = 0; i < 21815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21816(int& val) {
    func_in_21816(val);
    const int x = 21816;
    std::cout << 21816;
    for (int i = 0; i < 21816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21816(int& val) {
    const int x = 21816;
    std::cout << 21816;
    for (int i = 0; i < 21816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21817(int& val) {
    func_in_21817(val);
    const int x = 21817;
    std::cout << 21817;
    for (int i = 0; i < 21817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21817(int& val) {
    const int x = 21817;
    std::cout << 21817;
    for (int i = 0; i < 21817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21818(int& val) {
    func_in_21818(val);
    const int x = 21818;
    std::cout << 21818;
    for (int i = 0; i < 21818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21818(int& val) {
    const int x = 21818;
    std::cout << 21818;
    for (int i = 0; i < 21818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21819(int& val) {
    func_in_21819(val);
    const int x = 21819;
    std::cout << 21819;
    for (int i = 0; i < 21819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21819(int& val) {
    const int x = 21819;
    std::cout << 21819;
    for (int i = 0; i < 21819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21820(int& val) {
    func_in_21820(val);
    const int x = 21820;
    std::cout << 21820;
    for (int i = 0; i < 21820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21820(int& val) {
    const int x = 21820;
    std::cout << 21820;
    for (int i = 0; i < 21820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21821(int& val) {
    func_in_21821(val);
    const int x = 21821;
    std::cout << 21821;
    for (int i = 0; i < 21821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21821(int& val) {
    const int x = 21821;
    std::cout << 21821;
    for (int i = 0; i < 21821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21822(int& val) {
    func_in_21822(val);
    const int x = 21822;
    std::cout << 21822;
    for (int i = 0; i < 21822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21822(int& val) {
    const int x = 21822;
    std::cout << 21822;
    for (int i = 0; i < 21822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21823(int& val) {
    func_in_21823(val);
    const int x = 21823;
    std::cout << 21823;
    for (int i = 0; i < 21823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21823(int& val) {
    const int x = 21823;
    std::cout << 21823;
    for (int i = 0; i < 21823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21824(int& val) {
    func_in_21824(val);
    const int x = 21824;
    std::cout << 21824;
    for (int i = 0; i < 21824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21824(int& val) {
    const int x = 21824;
    std::cout << 21824;
    for (int i = 0; i < 21824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21825(int& val) {
    func_in_21825(val);
    const int x = 21825;
    std::cout << 21825;
    for (int i = 0; i < 21825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21825(int& val) {
    const int x = 21825;
    std::cout << 21825;
    for (int i = 0; i < 21825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21826(int& val) {
    func_in_21826(val);
    const int x = 21826;
    std::cout << 21826;
    for (int i = 0; i < 21826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21826(int& val) {
    const int x = 21826;
    std::cout << 21826;
    for (int i = 0; i < 21826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21827(int& val) {
    func_in_21827(val);
    const int x = 21827;
    std::cout << 21827;
    for (int i = 0; i < 21827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21827(int& val) {
    const int x = 21827;
    std::cout << 21827;
    for (int i = 0; i < 21827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21828(int& val) {
    func_in_21828(val);
    const int x = 21828;
    std::cout << 21828;
    for (int i = 0; i < 21828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21828(int& val) {
    const int x = 21828;
    std::cout << 21828;
    for (int i = 0; i < 21828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21829(int& val) {
    func_in_21829(val);
    const int x = 21829;
    std::cout << 21829;
    for (int i = 0; i < 21829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21829(int& val) {
    const int x = 21829;
    std::cout << 21829;
    for (int i = 0; i < 21829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21830(int& val) {
    func_in_21830(val);
    const int x = 21830;
    std::cout << 21830;
    for (int i = 0; i < 21830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21830(int& val) {
    const int x = 21830;
    std::cout << 21830;
    for (int i = 0; i < 21830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21831(int& val) {
    func_in_21831(val);
    const int x = 21831;
    std::cout << 21831;
    for (int i = 0; i < 21831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21831(int& val) {
    const int x = 21831;
    std::cout << 21831;
    for (int i = 0; i < 21831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21832(int& val) {
    func_in_21832(val);
    const int x = 21832;
    std::cout << 21832;
    for (int i = 0; i < 21832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21832(int& val) {
    const int x = 21832;
    std::cout << 21832;
    for (int i = 0; i < 21832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21833(int& val) {
    func_in_21833(val);
    const int x = 21833;
    std::cout << 21833;
    for (int i = 0; i < 21833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21833(int& val) {
    const int x = 21833;
    std::cout << 21833;
    for (int i = 0; i < 21833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21834(int& val) {
    func_in_21834(val);
    const int x = 21834;
    std::cout << 21834;
    for (int i = 0; i < 21834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21834(int& val) {
    const int x = 21834;
    std::cout << 21834;
    for (int i = 0; i < 21834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21835(int& val) {
    func_in_21835(val);
    const int x = 21835;
    std::cout << 21835;
    for (int i = 0; i < 21835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21835(int& val) {
    const int x = 21835;
    std::cout << 21835;
    for (int i = 0; i < 21835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21836(int& val) {
    func_in_21836(val);
    const int x = 21836;
    std::cout << 21836;
    for (int i = 0; i < 21836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21836(int& val) {
    const int x = 21836;
    std::cout << 21836;
    for (int i = 0; i < 21836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21837(int& val) {
    func_in_21837(val);
    const int x = 21837;
    std::cout << 21837;
    for (int i = 0; i < 21837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21837(int& val) {
    const int x = 21837;
    std::cout << 21837;
    for (int i = 0; i < 21837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21838(int& val) {
    func_in_21838(val);
    const int x = 21838;
    std::cout << 21838;
    for (int i = 0; i < 21838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21838(int& val) {
    const int x = 21838;
    std::cout << 21838;
    for (int i = 0; i < 21838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21839(int& val) {
    func_in_21839(val);
    const int x = 21839;
    std::cout << 21839;
    for (int i = 0; i < 21839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21839(int& val) {
    const int x = 21839;
    std::cout << 21839;
    for (int i = 0; i < 21839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21840(int& val) {
    func_in_21840(val);
    const int x = 21840;
    std::cout << 21840;
    for (int i = 0; i < 21840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21840(int& val) {
    const int x = 21840;
    std::cout << 21840;
    for (int i = 0; i < 21840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21841(int& val) {
    func_in_21841(val);
    const int x = 21841;
    std::cout << 21841;
    for (int i = 0; i < 21841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21841(int& val) {
    const int x = 21841;
    std::cout << 21841;
    for (int i = 0; i < 21841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21842(int& val) {
    func_in_21842(val);
    const int x = 21842;
    std::cout << 21842;
    for (int i = 0; i < 21842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21842(int& val) {
    const int x = 21842;
    std::cout << 21842;
    for (int i = 0; i < 21842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21843(int& val) {
    func_in_21843(val);
    const int x = 21843;
    std::cout << 21843;
    for (int i = 0; i < 21843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21843(int& val) {
    const int x = 21843;
    std::cout << 21843;
    for (int i = 0; i < 21843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21844(int& val) {
    func_in_21844(val);
    const int x = 21844;
    std::cout << 21844;
    for (int i = 0; i < 21844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21844(int& val) {
    const int x = 21844;
    std::cout << 21844;
    for (int i = 0; i < 21844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21845(int& val) {
    func_in_21845(val);
    const int x = 21845;
    std::cout << 21845;
    for (int i = 0; i < 21845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21845(int& val) {
    const int x = 21845;
    std::cout << 21845;
    for (int i = 0; i < 21845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21846(int& val) {
    func_in_21846(val);
    const int x = 21846;
    std::cout << 21846;
    for (int i = 0; i < 21846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21846(int& val) {
    const int x = 21846;
    std::cout << 21846;
    for (int i = 0; i < 21846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21847(int& val) {
    func_in_21847(val);
    const int x = 21847;
    std::cout << 21847;
    for (int i = 0; i < 21847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21847(int& val) {
    const int x = 21847;
    std::cout << 21847;
    for (int i = 0; i < 21847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21848(int& val) {
    func_in_21848(val);
    const int x = 21848;
    std::cout << 21848;
    for (int i = 0; i < 21848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21848(int& val) {
    const int x = 21848;
    std::cout << 21848;
    for (int i = 0; i < 21848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21849(int& val) {
    func_in_21849(val);
    const int x = 21849;
    std::cout << 21849;
    for (int i = 0; i < 21849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21849(int& val) {
    const int x = 21849;
    std::cout << 21849;
    for (int i = 0; i < 21849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21850(int& val) {
    func_in_21850(val);
    const int x = 21850;
    std::cout << 21850;
    for (int i = 0; i < 21850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21850(int& val) {
    const int x = 21850;
    std::cout << 21850;
    for (int i = 0; i < 21850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21851(int& val) {
    func_in_21851(val);
    const int x = 21851;
    std::cout << 21851;
    for (int i = 0; i < 21851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21851(int& val) {
    const int x = 21851;
    std::cout << 21851;
    for (int i = 0; i < 21851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21852(int& val) {
    func_in_21852(val);
    const int x = 21852;
    std::cout << 21852;
    for (int i = 0; i < 21852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21852(int& val) {
    const int x = 21852;
    std::cout << 21852;
    for (int i = 0; i < 21852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21853(int& val) {
    func_in_21853(val);
    const int x = 21853;
    std::cout << 21853;
    for (int i = 0; i < 21853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21853(int& val) {
    const int x = 21853;
    std::cout << 21853;
    for (int i = 0; i < 21853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21854(int& val) {
    func_in_21854(val);
    const int x = 21854;
    std::cout << 21854;
    for (int i = 0; i < 21854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21854(int& val) {
    const int x = 21854;
    std::cout << 21854;
    for (int i = 0; i < 21854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21855(int& val) {
    func_in_21855(val);
    const int x = 21855;
    std::cout << 21855;
    for (int i = 0; i < 21855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21855(int& val) {
    const int x = 21855;
    std::cout << 21855;
    for (int i = 0; i < 21855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21856(int& val) {
    func_in_21856(val);
    const int x = 21856;
    std::cout << 21856;
    for (int i = 0; i < 21856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21856(int& val) {
    const int x = 21856;
    std::cout << 21856;
    for (int i = 0; i < 21856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21857(int& val) {
    func_in_21857(val);
    const int x = 21857;
    std::cout << 21857;
    for (int i = 0; i < 21857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21857(int& val) {
    const int x = 21857;
    std::cout << 21857;
    for (int i = 0; i < 21857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21858(int& val) {
    func_in_21858(val);
    const int x = 21858;
    std::cout << 21858;
    for (int i = 0; i < 21858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21858(int& val) {
    const int x = 21858;
    std::cout << 21858;
    for (int i = 0; i < 21858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21859(int& val) {
    func_in_21859(val);
    const int x = 21859;
    std::cout << 21859;
    for (int i = 0; i < 21859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21859(int& val) {
    const int x = 21859;
    std::cout << 21859;
    for (int i = 0; i < 21859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21860(int& val) {
    func_in_21860(val);
    const int x = 21860;
    std::cout << 21860;
    for (int i = 0; i < 21860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21860(int& val) {
    const int x = 21860;
    std::cout << 21860;
    for (int i = 0; i < 21860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21861(int& val) {
    func_in_21861(val);
    const int x = 21861;
    std::cout << 21861;
    for (int i = 0; i < 21861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21861(int& val) {
    const int x = 21861;
    std::cout << 21861;
    for (int i = 0; i < 21861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21862(int& val) {
    func_in_21862(val);
    const int x = 21862;
    std::cout << 21862;
    for (int i = 0; i < 21862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21862(int& val) {
    const int x = 21862;
    std::cout << 21862;
    for (int i = 0; i < 21862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21863(int& val) {
    func_in_21863(val);
    const int x = 21863;
    std::cout << 21863;
    for (int i = 0; i < 21863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21863(int& val) {
    const int x = 21863;
    std::cout << 21863;
    for (int i = 0; i < 21863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21864(int& val) {
    func_in_21864(val);
    const int x = 21864;
    std::cout << 21864;
    for (int i = 0; i < 21864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21864(int& val) {
    const int x = 21864;
    std::cout << 21864;
    for (int i = 0; i < 21864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21865(int& val) {
    func_in_21865(val);
    const int x = 21865;
    std::cout << 21865;
    for (int i = 0; i < 21865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21865(int& val) {
    const int x = 21865;
    std::cout << 21865;
    for (int i = 0; i < 21865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21866(int& val) {
    func_in_21866(val);
    const int x = 21866;
    std::cout << 21866;
    for (int i = 0; i < 21866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21866(int& val) {
    const int x = 21866;
    std::cout << 21866;
    for (int i = 0; i < 21866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21867(int& val) {
    func_in_21867(val);
    const int x = 21867;
    std::cout << 21867;
    for (int i = 0; i < 21867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21867(int& val) {
    const int x = 21867;
    std::cout << 21867;
    for (int i = 0; i < 21867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21868(int& val) {
    func_in_21868(val);
    const int x = 21868;
    std::cout << 21868;
    for (int i = 0; i < 21868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21868(int& val) {
    const int x = 21868;
    std::cout << 21868;
    for (int i = 0; i < 21868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21869(int& val) {
    func_in_21869(val);
    const int x = 21869;
    std::cout << 21869;
    for (int i = 0; i < 21869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21869(int& val) {
    const int x = 21869;
    std::cout << 21869;
    for (int i = 0; i < 21869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21870(int& val) {
    func_in_21870(val);
    const int x = 21870;
    std::cout << 21870;
    for (int i = 0; i < 21870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21870(int& val) {
    const int x = 21870;
    std::cout << 21870;
    for (int i = 0; i < 21870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21871(int& val) {
    func_in_21871(val);
    const int x = 21871;
    std::cout << 21871;
    for (int i = 0; i < 21871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21871(int& val) {
    const int x = 21871;
    std::cout << 21871;
    for (int i = 0; i < 21871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21872(int& val) {
    func_in_21872(val);
    const int x = 21872;
    std::cout << 21872;
    for (int i = 0; i < 21872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21872(int& val) {
    const int x = 21872;
    std::cout << 21872;
    for (int i = 0; i < 21872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21873(int& val) {
    func_in_21873(val);
    const int x = 21873;
    std::cout << 21873;
    for (int i = 0; i < 21873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21873(int& val) {
    const int x = 21873;
    std::cout << 21873;
    for (int i = 0; i < 21873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21874(int& val) {
    func_in_21874(val);
    const int x = 21874;
    std::cout << 21874;
    for (int i = 0; i < 21874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21874(int& val) {
    const int x = 21874;
    std::cout << 21874;
    for (int i = 0; i < 21874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21875(int& val) {
    func_in_21875(val);
    const int x = 21875;
    std::cout << 21875;
    for (int i = 0; i < 21875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21875(int& val) {
    const int x = 21875;
    std::cout << 21875;
    for (int i = 0; i < 21875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21876(int& val) {
    func_in_21876(val);
    const int x = 21876;
    std::cout << 21876;
    for (int i = 0; i < 21876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21876(int& val) {
    const int x = 21876;
    std::cout << 21876;
    for (int i = 0; i < 21876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21877(int& val) {
    func_in_21877(val);
    const int x = 21877;
    std::cout << 21877;
    for (int i = 0; i < 21877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21877(int& val) {
    const int x = 21877;
    std::cout << 21877;
    for (int i = 0; i < 21877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21878(int& val) {
    func_in_21878(val);
    const int x = 21878;
    std::cout << 21878;
    for (int i = 0; i < 21878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21878(int& val) {
    const int x = 21878;
    std::cout << 21878;
    for (int i = 0; i < 21878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21879(int& val) {
    func_in_21879(val);
    const int x = 21879;
    std::cout << 21879;
    for (int i = 0; i < 21879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21879(int& val) {
    const int x = 21879;
    std::cout << 21879;
    for (int i = 0; i < 21879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21880(int& val) {
    func_in_21880(val);
    const int x = 21880;
    std::cout << 21880;
    for (int i = 0; i < 21880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21880(int& val) {
    const int x = 21880;
    std::cout << 21880;
    for (int i = 0; i < 21880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21881(int& val) {
    func_in_21881(val);
    const int x = 21881;
    std::cout << 21881;
    for (int i = 0; i < 21881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21881(int& val) {
    const int x = 21881;
    std::cout << 21881;
    for (int i = 0; i < 21881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21882(int& val) {
    func_in_21882(val);
    const int x = 21882;
    std::cout << 21882;
    for (int i = 0; i < 21882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21882(int& val) {
    const int x = 21882;
    std::cout << 21882;
    for (int i = 0; i < 21882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21883(int& val) {
    func_in_21883(val);
    const int x = 21883;
    std::cout << 21883;
    for (int i = 0; i < 21883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21883(int& val) {
    const int x = 21883;
    std::cout << 21883;
    for (int i = 0; i < 21883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21884(int& val) {
    func_in_21884(val);
    const int x = 21884;
    std::cout << 21884;
    for (int i = 0; i < 21884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21884(int& val) {
    const int x = 21884;
    std::cout << 21884;
    for (int i = 0; i < 21884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21885(int& val) {
    func_in_21885(val);
    const int x = 21885;
    std::cout << 21885;
    for (int i = 0; i < 21885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21885(int& val) {
    const int x = 21885;
    std::cout << 21885;
    for (int i = 0; i < 21885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21886(int& val) {
    func_in_21886(val);
    const int x = 21886;
    std::cout << 21886;
    for (int i = 0; i < 21886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21886(int& val) {
    const int x = 21886;
    std::cout << 21886;
    for (int i = 0; i < 21886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21887(int& val) {
    func_in_21887(val);
    const int x = 21887;
    std::cout << 21887;
    for (int i = 0; i < 21887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21887(int& val) {
    const int x = 21887;
    std::cout << 21887;
    for (int i = 0; i < 21887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21888(int& val) {
    func_in_21888(val);
    const int x = 21888;
    std::cout << 21888;
    for (int i = 0; i < 21888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21888(int& val) {
    const int x = 21888;
    std::cout << 21888;
    for (int i = 0; i < 21888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21889(int& val) {
    func_in_21889(val);
    const int x = 21889;
    std::cout << 21889;
    for (int i = 0; i < 21889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21889(int& val) {
    const int x = 21889;
    std::cout << 21889;
    for (int i = 0; i < 21889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21890(int& val) {
    func_in_21890(val);
    const int x = 21890;
    std::cout << 21890;
    for (int i = 0; i < 21890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21890(int& val) {
    const int x = 21890;
    std::cout << 21890;
    for (int i = 0; i < 21890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21891(int& val) {
    func_in_21891(val);
    const int x = 21891;
    std::cout << 21891;
    for (int i = 0; i < 21891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21891(int& val) {
    const int x = 21891;
    std::cout << 21891;
    for (int i = 0; i < 21891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21892(int& val) {
    func_in_21892(val);
    const int x = 21892;
    std::cout << 21892;
    for (int i = 0; i < 21892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21892(int& val) {
    const int x = 21892;
    std::cout << 21892;
    for (int i = 0; i < 21892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21893(int& val) {
    func_in_21893(val);
    const int x = 21893;
    std::cout << 21893;
    for (int i = 0; i < 21893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21893(int& val) {
    const int x = 21893;
    std::cout << 21893;
    for (int i = 0; i < 21893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21894(int& val) {
    func_in_21894(val);
    const int x = 21894;
    std::cout << 21894;
    for (int i = 0; i < 21894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21894(int& val) {
    const int x = 21894;
    std::cout << 21894;
    for (int i = 0; i < 21894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21895(int& val) {
    func_in_21895(val);
    const int x = 21895;
    std::cout << 21895;
    for (int i = 0; i < 21895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21895(int& val) {
    const int x = 21895;
    std::cout << 21895;
    for (int i = 0; i < 21895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21896(int& val) {
    func_in_21896(val);
    const int x = 21896;
    std::cout << 21896;
    for (int i = 0; i < 21896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21896(int& val) {
    const int x = 21896;
    std::cout << 21896;
    for (int i = 0; i < 21896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21897(int& val) {
    func_in_21897(val);
    const int x = 21897;
    std::cout << 21897;
    for (int i = 0; i < 21897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21897(int& val) {
    const int x = 21897;
    std::cout << 21897;
    for (int i = 0; i < 21897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21898(int& val) {
    func_in_21898(val);
    const int x = 21898;
    std::cout << 21898;
    for (int i = 0; i < 21898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21898(int& val) {
    const int x = 21898;
    std::cout << 21898;
    for (int i = 0; i < 21898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21899(int& val) {
    func_in_21899(val);
    const int x = 21899;
    std::cout << 21899;
    for (int i = 0; i < 21899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21899(int& val) {
    const int x = 21899;
    std::cout << 21899;
    for (int i = 0; i < 21899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21900(int& val) {
    func_in_21900(val);
    const int x = 21900;
    std::cout << 21900;
    for (int i = 0; i < 21900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21900(int& val) {
    const int x = 21900;
    std::cout << 21900;
    for (int i = 0; i < 21900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21901(int& val) {
    func_in_21901(val);
    const int x = 21901;
    std::cout << 21901;
    for (int i = 0; i < 21901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21901(int& val) {
    const int x = 21901;
    std::cout << 21901;
    for (int i = 0; i < 21901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21902(int& val) {
    func_in_21902(val);
    const int x = 21902;
    std::cout << 21902;
    for (int i = 0; i < 21902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21902(int& val) {
    const int x = 21902;
    std::cout << 21902;
    for (int i = 0; i < 21902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21903(int& val) {
    func_in_21903(val);
    const int x = 21903;
    std::cout << 21903;
    for (int i = 0; i < 21903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21903(int& val) {
    const int x = 21903;
    std::cout << 21903;
    for (int i = 0; i < 21903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21904(int& val) {
    func_in_21904(val);
    const int x = 21904;
    std::cout << 21904;
    for (int i = 0; i < 21904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21904(int& val) {
    const int x = 21904;
    std::cout << 21904;
    for (int i = 0; i < 21904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21905(int& val) {
    func_in_21905(val);
    const int x = 21905;
    std::cout << 21905;
    for (int i = 0; i < 21905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21905(int& val) {
    const int x = 21905;
    std::cout << 21905;
    for (int i = 0; i < 21905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21906(int& val) {
    func_in_21906(val);
    const int x = 21906;
    std::cout << 21906;
    for (int i = 0; i < 21906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21906(int& val) {
    const int x = 21906;
    std::cout << 21906;
    for (int i = 0; i < 21906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21907(int& val) {
    func_in_21907(val);
    const int x = 21907;
    std::cout << 21907;
    for (int i = 0; i < 21907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21907(int& val) {
    const int x = 21907;
    std::cout << 21907;
    for (int i = 0; i < 21907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21908(int& val) {
    func_in_21908(val);
    const int x = 21908;
    std::cout << 21908;
    for (int i = 0; i < 21908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21908(int& val) {
    const int x = 21908;
    std::cout << 21908;
    for (int i = 0; i < 21908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21909(int& val) {
    func_in_21909(val);
    const int x = 21909;
    std::cout << 21909;
    for (int i = 0; i < 21909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21909(int& val) {
    const int x = 21909;
    std::cout << 21909;
    for (int i = 0; i < 21909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21910(int& val) {
    func_in_21910(val);
    const int x = 21910;
    std::cout << 21910;
    for (int i = 0; i < 21910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21910(int& val) {
    const int x = 21910;
    std::cout << 21910;
    for (int i = 0; i < 21910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21911(int& val) {
    func_in_21911(val);
    const int x = 21911;
    std::cout << 21911;
    for (int i = 0; i < 21911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21911(int& val) {
    const int x = 21911;
    std::cout << 21911;
    for (int i = 0; i < 21911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21912(int& val) {
    func_in_21912(val);
    const int x = 21912;
    std::cout << 21912;
    for (int i = 0; i < 21912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21912(int& val) {
    const int x = 21912;
    std::cout << 21912;
    for (int i = 0; i < 21912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21913(int& val) {
    func_in_21913(val);
    const int x = 21913;
    std::cout << 21913;
    for (int i = 0; i < 21913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21913(int& val) {
    const int x = 21913;
    std::cout << 21913;
    for (int i = 0; i < 21913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21914(int& val) {
    func_in_21914(val);
    const int x = 21914;
    std::cout << 21914;
    for (int i = 0; i < 21914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21914(int& val) {
    const int x = 21914;
    std::cout << 21914;
    for (int i = 0; i < 21914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21915(int& val) {
    func_in_21915(val);
    const int x = 21915;
    std::cout << 21915;
    for (int i = 0; i < 21915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21915(int& val) {
    const int x = 21915;
    std::cout << 21915;
    for (int i = 0; i < 21915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21916(int& val) {
    func_in_21916(val);
    const int x = 21916;
    std::cout << 21916;
    for (int i = 0; i < 21916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21916(int& val) {
    const int x = 21916;
    std::cout << 21916;
    for (int i = 0; i < 21916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21917(int& val) {
    func_in_21917(val);
    const int x = 21917;
    std::cout << 21917;
    for (int i = 0; i < 21917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21917(int& val) {
    const int x = 21917;
    std::cout << 21917;
    for (int i = 0; i < 21917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21918(int& val) {
    func_in_21918(val);
    const int x = 21918;
    std::cout << 21918;
    for (int i = 0; i < 21918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21918(int& val) {
    const int x = 21918;
    std::cout << 21918;
    for (int i = 0; i < 21918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21919(int& val) {
    func_in_21919(val);
    const int x = 21919;
    std::cout << 21919;
    for (int i = 0; i < 21919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21919(int& val) {
    const int x = 21919;
    std::cout << 21919;
    for (int i = 0; i < 21919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21920(int& val) {
    func_in_21920(val);
    const int x = 21920;
    std::cout << 21920;
    for (int i = 0; i < 21920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21920(int& val) {
    const int x = 21920;
    std::cout << 21920;
    for (int i = 0; i < 21920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21921(int& val) {
    func_in_21921(val);
    const int x = 21921;
    std::cout << 21921;
    for (int i = 0; i < 21921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21921(int& val) {
    const int x = 21921;
    std::cout << 21921;
    for (int i = 0; i < 21921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21922(int& val) {
    func_in_21922(val);
    const int x = 21922;
    std::cout << 21922;
    for (int i = 0; i < 21922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21922(int& val) {
    const int x = 21922;
    std::cout << 21922;
    for (int i = 0; i < 21922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21923(int& val) {
    func_in_21923(val);
    const int x = 21923;
    std::cout << 21923;
    for (int i = 0; i < 21923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21923(int& val) {
    const int x = 21923;
    std::cout << 21923;
    for (int i = 0; i < 21923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21924(int& val) {
    func_in_21924(val);
    const int x = 21924;
    std::cout << 21924;
    for (int i = 0; i < 21924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21924(int& val) {
    const int x = 21924;
    std::cout << 21924;
    for (int i = 0; i < 21924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21925(int& val) {
    func_in_21925(val);
    const int x = 21925;
    std::cout << 21925;
    for (int i = 0; i < 21925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21925(int& val) {
    const int x = 21925;
    std::cout << 21925;
    for (int i = 0; i < 21925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21926(int& val) {
    func_in_21926(val);
    const int x = 21926;
    std::cout << 21926;
    for (int i = 0; i < 21926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21926(int& val) {
    const int x = 21926;
    std::cout << 21926;
    for (int i = 0; i < 21926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21927(int& val) {
    func_in_21927(val);
    const int x = 21927;
    std::cout << 21927;
    for (int i = 0; i < 21927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21927(int& val) {
    const int x = 21927;
    std::cout << 21927;
    for (int i = 0; i < 21927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21928(int& val) {
    func_in_21928(val);
    const int x = 21928;
    std::cout << 21928;
    for (int i = 0; i < 21928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21928(int& val) {
    const int x = 21928;
    std::cout << 21928;
    for (int i = 0; i < 21928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21929(int& val) {
    func_in_21929(val);
    const int x = 21929;
    std::cout << 21929;
    for (int i = 0; i < 21929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21929(int& val) {
    const int x = 21929;
    std::cout << 21929;
    for (int i = 0; i < 21929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21930(int& val) {
    func_in_21930(val);
    const int x = 21930;
    std::cout << 21930;
    for (int i = 0; i < 21930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21930(int& val) {
    const int x = 21930;
    std::cout << 21930;
    for (int i = 0; i < 21930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21931(int& val) {
    func_in_21931(val);
    const int x = 21931;
    std::cout << 21931;
    for (int i = 0; i < 21931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21931(int& val) {
    const int x = 21931;
    std::cout << 21931;
    for (int i = 0; i < 21931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21932(int& val) {
    func_in_21932(val);
    const int x = 21932;
    std::cout << 21932;
    for (int i = 0; i < 21932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21932(int& val) {
    const int x = 21932;
    std::cout << 21932;
    for (int i = 0; i < 21932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21933(int& val) {
    func_in_21933(val);
    const int x = 21933;
    std::cout << 21933;
    for (int i = 0; i < 21933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21933(int& val) {
    const int x = 21933;
    std::cout << 21933;
    for (int i = 0; i < 21933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21934(int& val) {
    func_in_21934(val);
    const int x = 21934;
    std::cout << 21934;
    for (int i = 0; i < 21934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21934(int& val) {
    const int x = 21934;
    std::cout << 21934;
    for (int i = 0; i < 21934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21935(int& val) {
    func_in_21935(val);
    const int x = 21935;
    std::cout << 21935;
    for (int i = 0; i < 21935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21935(int& val) {
    const int x = 21935;
    std::cout << 21935;
    for (int i = 0; i < 21935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21936(int& val) {
    func_in_21936(val);
    const int x = 21936;
    std::cout << 21936;
    for (int i = 0; i < 21936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21936(int& val) {
    const int x = 21936;
    std::cout << 21936;
    for (int i = 0; i < 21936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21937(int& val) {
    func_in_21937(val);
    const int x = 21937;
    std::cout << 21937;
    for (int i = 0; i < 21937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21937(int& val) {
    const int x = 21937;
    std::cout << 21937;
    for (int i = 0; i < 21937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21938(int& val) {
    func_in_21938(val);
    const int x = 21938;
    std::cout << 21938;
    for (int i = 0; i < 21938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21938(int& val) {
    const int x = 21938;
    std::cout << 21938;
    for (int i = 0; i < 21938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21939(int& val) {
    func_in_21939(val);
    const int x = 21939;
    std::cout << 21939;
    for (int i = 0; i < 21939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21939(int& val) {
    const int x = 21939;
    std::cout << 21939;
    for (int i = 0; i < 21939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21940(int& val) {
    func_in_21940(val);
    const int x = 21940;
    std::cout << 21940;
    for (int i = 0; i < 21940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21940(int& val) {
    const int x = 21940;
    std::cout << 21940;
    for (int i = 0; i < 21940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21941(int& val) {
    func_in_21941(val);
    const int x = 21941;
    std::cout << 21941;
    for (int i = 0; i < 21941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21941(int& val) {
    const int x = 21941;
    std::cout << 21941;
    for (int i = 0; i < 21941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21942(int& val) {
    func_in_21942(val);
    const int x = 21942;
    std::cout << 21942;
    for (int i = 0; i < 21942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21942(int& val) {
    const int x = 21942;
    std::cout << 21942;
    for (int i = 0; i < 21942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21943(int& val) {
    func_in_21943(val);
    const int x = 21943;
    std::cout << 21943;
    for (int i = 0; i < 21943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21943(int& val) {
    const int x = 21943;
    std::cout << 21943;
    for (int i = 0; i < 21943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21944(int& val) {
    func_in_21944(val);
    const int x = 21944;
    std::cout << 21944;
    for (int i = 0; i < 21944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21944(int& val) {
    const int x = 21944;
    std::cout << 21944;
    for (int i = 0; i < 21944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21945(int& val) {
    func_in_21945(val);
    const int x = 21945;
    std::cout << 21945;
    for (int i = 0; i < 21945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21945(int& val) {
    const int x = 21945;
    std::cout << 21945;
    for (int i = 0; i < 21945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21946(int& val) {
    func_in_21946(val);
    const int x = 21946;
    std::cout << 21946;
    for (int i = 0; i < 21946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21946(int& val) {
    const int x = 21946;
    std::cout << 21946;
    for (int i = 0; i < 21946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21947(int& val) {
    func_in_21947(val);
    const int x = 21947;
    std::cout << 21947;
    for (int i = 0; i < 21947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21947(int& val) {
    const int x = 21947;
    std::cout << 21947;
    for (int i = 0; i < 21947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21948(int& val) {
    func_in_21948(val);
    const int x = 21948;
    std::cout << 21948;
    for (int i = 0; i < 21948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21948(int& val) {
    const int x = 21948;
    std::cout << 21948;
    for (int i = 0; i < 21948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21949(int& val) {
    func_in_21949(val);
    const int x = 21949;
    std::cout << 21949;
    for (int i = 0; i < 21949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21949(int& val) {
    const int x = 21949;
    std::cout << 21949;
    for (int i = 0; i < 21949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21950(int& val) {
    func_in_21950(val);
    const int x = 21950;
    std::cout << 21950;
    for (int i = 0; i < 21950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21950(int& val) {
    const int x = 21950;
    std::cout << 21950;
    for (int i = 0; i < 21950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21951(int& val) {
    func_in_21951(val);
    const int x = 21951;
    std::cout << 21951;
    for (int i = 0; i < 21951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21951(int& val) {
    const int x = 21951;
    std::cout << 21951;
    for (int i = 0; i < 21951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21952(int& val) {
    func_in_21952(val);
    const int x = 21952;
    std::cout << 21952;
    for (int i = 0; i < 21952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21952(int& val) {
    const int x = 21952;
    std::cout << 21952;
    for (int i = 0; i < 21952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21953(int& val) {
    func_in_21953(val);
    const int x = 21953;
    std::cout << 21953;
    for (int i = 0; i < 21953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21953(int& val) {
    const int x = 21953;
    std::cout << 21953;
    for (int i = 0; i < 21953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21954(int& val) {
    func_in_21954(val);
    const int x = 21954;
    std::cout << 21954;
    for (int i = 0; i < 21954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21954(int& val) {
    const int x = 21954;
    std::cout << 21954;
    for (int i = 0; i < 21954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21955(int& val) {
    func_in_21955(val);
    const int x = 21955;
    std::cout << 21955;
    for (int i = 0; i < 21955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21955(int& val) {
    const int x = 21955;
    std::cout << 21955;
    for (int i = 0; i < 21955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21956(int& val) {
    func_in_21956(val);
    const int x = 21956;
    std::cout << 21956;
    for (int i = 0; i < 21956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21956(int& val) {
    const int x = 21956;
    std::cout << 21956;
    for (int i = 0; i < 21956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21957(int& val) {
    func_in_21957(val);
    const int x = 21957;
    std::cout << 21957;
    for (int i = 0; i < 21957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21957(int& val) {
    const int x = 21957;
    std::cout << 21957;
    for (int i = 0; i < 21957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21958(int& val) {
    func_in_21958(val);
    const int x = 21958;
    std::cout << 21958;
    for (int i = 0; i < 21958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21958(int& val) {
    const int x = 21958;
    std::cout << 21958;
    for (int i = 0; i < 21958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21959(int& val) {
    func_in_21959(val);
    const int x = 21959;
    std::cout << 21959;
    for (int i = 0; i < 21959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21959(int& val) {
    const int x = 21959;
    std::cout << 21959;
    for (int i = 0; i < 21959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21960(int& val) {
    func_in_21960(val);
    const int x = 21960;
    std::cout << 21960;
    for (int i = 0; i < 21960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21960(int& val) {
    const int x = 21960;
    std::cout << 21960;
    for (int i = 0; i < 21960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21961(int& val) {
    func_in_21961(val);
    const int x = 21961;
    std::cout << 21961;
    for (int i = 0; i < 21961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21961(int& val) {
    const int x = 21961;
    std::cout << 21961;
    for (int i = 0; i < 21961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21962(int& val) {
    func_in_21962(val);
    const int x = 21962;
    std::cout << 21962;
    for (int i = 0; i < 21962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21962(int& val) {
    const int x = 21962;
    std::cout << 21962;
    for (int i = 0; i < 21962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21963(int& val) {
    func_in_21963(val);
    const int x = 21963;
    std::cout << 21963;
    for (int i = 0; i < 21963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21963(int& val) {
    const int x = 21963;
    std::cout << 21963;
    for (int i = 0; i < 21963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21964(int& val) {
    func_in_21964(val);
    const int x = 21964;
    std::cout << 21964;
    for (int i = 0; i < 21964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21964(int& val) {
    const int x = 21964;
    std::cout << 21964;
    for (int i = 0; i < 21964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21965(int& val) {
    func_in_21965(val);
    const int x = 21965;
    std::cout << 21965;
    for (int i = 0; i < 21965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21965(int& val) {
    const int x = 21965;
    std::cout << 21965;
    for (int i = 0; i < 21965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21966(int& val) {
    func_in_21966(val);
    const int x = 21966;
    std::cout << 21966;
    for (int i = 0; i < 21966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21966(int& val) {
    const int x = 21966;
    std::cout << 21966;
    for (int i = 0; i < 21966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21967(int& val) {
    func_in_21967(val);
    const int x = 21967;
    std::cout << 21967;
    for (int i = 0; i < 21967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21967(int& val) {
    const int x = 21967;
    std::cout << 21967;
    for (int i = 0; i < 21967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21968(int& val) {
    func_in_21968(val);
    const int x = 21968;
    std::cout << 21968;
    for (int i = 0; i < 21968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21968(int& val) {
    const int x = 21968;
    std::cout << 21968;
    for (int i = 0; i < 21968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21969(int& val) {
    func_in_21969(val);
    const int x = 21969;
    std::cout << 21969;
    for (int i = 0; i < 21969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21969(int& val) {
    const int x = 21969;
    std::cout << 21969;
    for (int i = 0; i < 21969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21970(int& val) {
    func_in_21970(val);
    const int x = 21970;
    std::cout << 21970;
    for (int i = 0; i < 21970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21970(int& val) {
    const int x = 21970;
    std::cout << 21970;
    for (int i = 0; i < 21970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21971(int& val) {
    func_in_21971(val);
    const int x = 21971;
    std::cout << 21971;
    for (int i = 0; i < 21971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21971(int& val) {
    const int x = 21971;
    std::cout << 21971;
    for (int i = 0; i < 21971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21972(int& val) {
    func_in_21972(val);
    const int x = 21972;
    std::cout << 21972;
    for (int i = 0; i < 21972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21972(int& val) {
    const int x = 21972;
    std::cout << 21972;
    for (int i = 0; i < 21972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21973(int& val) {
    func_in_21973(val);
    const int x = 21973;
    std::cout << 21973;
    for (int i = 0; i < 21973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21973(int& val) {
    const int x = 21973;
    std::cout << 21973;
    for (int i = 0; i < 21973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21974(int& val) {
    func_in_21974(val);
    const int x = 21974;
    std::cout << 21974;
    for (int i = 0; i < 21974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21974(int& val) {
    const int x = 21974;
    std::cout << 21974;
    for (int i = 0; i < 21974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21975(int& val) {
    func_in_21975(val);
    const int x = 21975;
    std::cout << 21975;
    for (int i = 0; i < 21975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21975(int& val) {
    const int x = 21975;
    std::cout << 21975;
    for (int i = 0; i < 21975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21976(int& val) {
    func_in_21976(val);
    const int x = 21976;
    std::cout << 21976;
    for (int i = 0; i < 21976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21976(int& val) {
    const int x = 21976;
    std::cout << 21976;
    for (int i = 0; i < 21976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21977(int& val) {
    func_in_21977(val);
    const int x = 21977;
    std::cout << 21977;
    for (int i = 0; i < 21977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21977(int& val) {
    const int x = 21977;
    std::cout << 21977;
    for (int i = 0; i < 21977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21978(int& val) {
    func_in_21978(val);
    const int x = 21978;
    std::cout << 21978;
    for (int i = 0; i < 21978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21978(int& val) {
    const int x = 21978;
    std::cout << 21978;
    for (int i = 0; i < 21978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21979(int& val) {
    func_in_21979(val);
    const int x = 21979;
    std::cout << 21979;
    for (int i = 0; i < 21979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21979(int& val) {
    const int x = 21979;
    std::cout << 21979;
    for (int i = 0; i < 21979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21980(int& val) {
    func_in_21980(val);
    const int x = 21980;
    std::cout << 21980;
    for (int i = 0; i < 21980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21980(int& val) {
    const int x = 21980;
    std::cout << 21980;
    for (int i = 0; i < 21980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21981(int& val) {
    func_in_21981(val);
    const int x = 21981;
    std::cout << 21981;
    for (int i = 0; i < 21981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21981(int& val) {
    const int x = 21981;
    std::cout << 21981;
    for (int i = 0; i < 21981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21982(int& val) {
    func_in_21982(val);
    const int x = 21982;
    std::cout << 21982;
    for (int i = 0; i < 21982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21982(int& val) {
    const int x = 21982;
    std::cout << 21982;
    for (int i = 0; i < 21982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21983(int& val) {
    func_in_21983(val);
    const int x = 21983;
    std::cout << 21983;
    for (int i = 0; i < 21983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21983(int& val) {
    const int x = 21983;
    std::cout << 21983;
    for (int i = 0; i < 21983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21984(int& val) {
    func_in_21984(val);
    const int x = 21984;
    std::cout << 21984;
    for (int i = 0; i < 21984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21984(int& val) {
    const int x = 21984;
    std::cout << 21984;
    for (int i = 0; i < 21984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21985(int& val) {
    func_in_21985(val);
    const int x = 21985;
    std::cout << 21985;
    for (int i = 0; i < 21985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21985(int& val) {
    const int x = 21985;
    std::cout << 21985;
    for (int i = 0; i < 21985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21986(int& val) {
    func_in_21986(val);
    const int x = 21986;
    std::cout << 21986;
    for (int i = 0; i < 21986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21986(int& val) {
    const int x = 21986;
    std::cout << 21986;
    for (int i = 0; i < 21986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21987(int& val) {
    func_in_21987(val);
    const int x = 21987;
    std::cout << 21987;
    for (int i = 0; i < 21987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21987(int& val) {
    const int x = 21987;
    std::cout << 21987;
    for (int i = 0; i < 21987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21988(int& val) {
    func_in_21988(val);
    const int x = 21988;
    std::cout << 21988;
    for (int i = 0; i < 21988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21988(int& val) {
    const int x = 21988;
    std::cout << 21988;
    for (int i = 0; i < 21988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21989(int& val) {
    func_in_21989(val);
    const int x = 21989;
    std::cout << 21989;
    for (int i = 0; i < 21989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21989(int& val) {
    const int x = 21989;
    std::cout << 21989;
    for (int i = 0; i < 21989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21990(int& val) {
    func_in_21990(val);
    const int x = 21990;
    std::cout << 21990;
    for (int i = 0; i < 21990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21990(int& val) {
    const int x = 21990;
    std::cout << 21990;
    for (int i = 0; i < 21990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21991(int& val) {
    func_in_21991(val);
    const int x = 21991;
    std::cout << 21991;
    for (int i = 0; i < 21991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21991(int& val) {
    const int x = 21991;
    std::cout << 21991;
    for (int i = 0; i < 21991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21992(int& val) {
    func_in_21992(val);
    const int x = 21992;
    std::cout << 21992;
    for (int i = 0; i < 21992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21992(int& val) {
    const int x = 21992;
    std::cout << 21992;
    for (int i = 0; i < 21992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21993(int& val) {
    func_in_21993(val);
    const int x = 21993;
    std::cout << 21993;
    for (int i = 0; i < 21993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21993(int& val) {
    const int x = 21993;
    std::cout << 21993;
    for (int i = 0; i < 21993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21994(int& val) {
    func_in_21994(val);
    const int x = 21994;
    std::cout << 21994;
    for (int i = 0; i < 21994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21994(int& val) {
    const int x = 21994;
    std::cout << 21994;
    for (int i = 0; i < 21994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21995(int& val) {
    func_in_21995(val);
    const int x = 21995;
    std::cout << 21995;
    for (int i = 0; i < 21995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21995(int& val) {
    const int x = 21995;
    std::cout << 21995;
    for (int i = 0; i < 21995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21996(int& val) {
    func_in_21996(val);
    const int x = 21996;
    std::cout << 21996;
    for (int i = 0; i < 21996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21996(int& val) {
    const int x = 21996;
    std::cout << 21996;
    for (int i = 0; i < 21996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21997(int& val) {
    func_in_21997(val);
    const int x = 21997;
    std::cout << 21997;
    for (int i = 0; i < 21997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21997(int& val) {
    const int x = 21997;
    std::cout << 21997;
    for (int i = 0; i < 21997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21998(int& val) {
    func_in_21998(val);
    const int x = 21998;
    std::cout << 21998;
    for (int i = 0; i < 21998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21998(int& val) {
    const int x = 21998;
    std::cout << 21998;
    for (int i = 0; i < 21998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21999(int& val) {
    func_in_21999(val);
    const int x = 21999;
    std::cout << 21999;
    for (int i = 0; i < 21999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21999(int& val) {
    const int x = 21999;
    std::cout << 21999;
    for (int i = 0; i < 21999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22000(int& val) {
    func_in_22000(val);
    const int x = 22000;
    std::cout << 22000;
    for (int i = 0; i < 22000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22000(int& val) {
    const int x = 22000;
    std::cout << 22000;
    for (int i = 0; i < 22000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22001(int& val) {
    func_in_22001(val);
    const int x = 22001;
    std::cout << 22001;
    for (int i = 0; i < 22001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22001(int& val) {
    const int x = 22001;
    std::cout << 22001;
    for (int i = 0; i < 22001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22002(int& val) {
    func_in_22002(val);
    const int x = 22002;
    std::cout << 22002;
    for (int i = 0; i < 22002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22002(int& val) {
    const int x = 22002;
    std::cout << 22002;
    for (int i = 0; i < 22002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22003(int& val) {
    func_in_22003(val);
    const int x = 22003;
    std::cout << 22003;
    for (int i = 0; i < 22003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22003(int& val) {
    const int x = 22003;
    std::cout << 22003;
    for (int i = 0; i < 22003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22004(int& val) {
    func_in_22004(val);
    const int x = 22004;
    std::cout << 22004;
    for (int i = 0; i < 22004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22004(int& val) {
    const int x = 22004;
    std::cout << 22004;
    for (int i = 0; i < 22004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22005(int& val) {
    func_in_22005(val);
    const int x = 22005;
    std::cout << 22005;
    for (int i = 0; i < 22005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22005(int& val) {
    const int x = 22005;
    std::cout << 22005;
    for (int i = 0; i < 22005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22006(int& val) {
    func_in_22006(val);
    const int x = 22006;
    std::cout << 22006;
    for (int i = 0; i < 22006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22006(int& val) {
    const int x = 22006;
    std::cout << 22006;
    for (int i = 0; i < 22006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22007(int& val) {
    func_in_22007(val);
    const int x = 22007;
    std::cout << 22007;
    for (int i = 0; i < 22007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22007(int& val) {
    const int x = 22007;
    std::cout << 22007;
    for (int i = 0; i < 22007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22008(int& val) {
    func_in_22008(val);
    const int x = 22008;
    std::cout << 22008;
    for (int i = 0; i < 22008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22008(int& val) {
    const int x = 22008;
    std::cout << 22008;
    for (int i = 0; i < 22008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22009(int& val) {
    func_in_22009(val);
    const int x = 22009;
    std::cout << 22009;
    for (int i = 0; i < 22009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22009(int& val) {
    const int x = 22009;
    std::cout << 22009;
    for (int i = 0; i < 22009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22010(int& val) {
    func_in_22010(val);
    const int x = 22010;
    std::cout << 22010;
    for (int i = 0; i < 22010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22010(int& val) {
    const int x = 22010;
    std::cout << 22010;
    for (int i = 0; i < 22010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22011(int& val) {
    func_in_22011(val);
    const int x = 22011;
    std::cout << 22011;
    for (int i = 0; i < 22011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22011(int& val) {
    const int x = 22011;
    std::cout << 22011;
    for (int i = 0; i < 22011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22012(int& val) {
    func_in_22012(val);
    const int x = 22012;
    std::cout << 22012;
    for (int i = 0; i < 22012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22012(int& val) {
    const int x = 22012;
    std::cout << 22012;
    for (int i = 0; i < 22012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22013(int& val) {
    func_in_22013(val);
    const int x = 22013;
    std::cout << 22013;
    for (int i = 0; i < 22013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22013(int& val) {
    const int x = 22013;
    std::cout << 22013;
    for (int i = 0; i < 22013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22014(int& val) {
    func_in_22014(val);
    const int x = 22014;
    std::cout << 22014;
    for (int i = 0; i < 22014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22014(int& val) {
    const int x = 22014;
    std::cout << 22014;
    for (int i = 0; i < 22014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22015(int& val) {
    func_in_22015(val);
    const int x = 22015;
    std::cout << 22015;
    for (int i = 0; i < 22015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22015(int& val) {
    const int x = 22015;
    std::cout << 22015;
    for (int i = 0; i < 22015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22016(int& val) {
    func_in_22016(val);
    const int x = 22016;
    std::cout << 22016;
    for (int i = 0; i < 22016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22016(int& val) {
    const int x = 22016;
    std::cout << 22016;
    for (int i = 0; i < 22016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22017(int& val) {
    func_in_22017(val);
    const int x = 22017;
    std::cout << 22017;
    for (int i = 0; i < 22017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22017(int& val) {
    const int x = 22017;
    std::cout << 22017;
    for (int i = 0; i < 22017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22018(int& val) {
    func_in_22018(val);
    const int x = 22018;
    std::cout << 22018;
    for (int i = 0; i < 22018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22018(int& val) {
    const int x = 22018;
    std::cout << 22018;
    for (int i = 0; i < 22018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22019(int& val) {
    func_in_22019(val);
    const int x = 22019;
    std::cout << 22019;
    for (int i = 0; i < 22019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22019(int& val) {
    const int x = 22019;
    std::cout << 22019;
    for (int i = 0; i < 22019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22020(int& val) {
    func_in_22020(val);
    const int x = 22020;
    std::cout << 22020;
    for (int i = 0; i < 22020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22020(int& val) {
    const int x = 22020;
    std::cout << 22020;
    for (int i = 0; i < 22020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22021(int& val) {
    func_in_22021(val);
    const int x = 22021;
    std::cout << 22021;
    for (int i = 0; i < 22021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22021(int& val) {
    const int x = 22021;
    std::cout << 22021;
    for (int i = 0; i < 22021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22022(int& val) {
    func_in_22022(val);
    const int x = 22022;
    std::cout << 22022;
    for (int i = 0; i < 22022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22022(int& val) {
    const int x = 22022;
    std::cout << 22022;
    for (int i = 0; i < 22022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22023(int& val) {
    func_in_22023(val);
    const int x = 22023;
    std::cout << 22023;
    for (int i = 0; i < 22023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22023(int& val) {
    const int x = 22023;
    std::cout << 22023;
    for (int i = 0; i < 22023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22024(int& val) {
    func_in_22024(val);
    const int x = 22024;
    std::cout << 22024;
    for (int i = 0; i < 22024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22024(int& val) {
    const int x = 22024;
    std::cout << 22024;
    for (int i = 0; i < 22024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22025(int& val) {
    func_in_22025(val);
    const int x = 22025;
    std::cout << 22025;
    for (int i = 0; i < 22025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22025(int& val) {
    const int x = 22025;
    std::cout << 22025;
    for (int i = 0; i < 22025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22026(int& val) {
    func_in_22026(val);
    const int x = 22026;
    std::cout << 22026;
    for (int i = 0; i < 22026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22026(int& val) {
    const int x = 22026;
    std::cout << 22026;
    for (int i = 0; i < 22026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22027(int& val) {
    func_in_22027(val);
    const int x = 22027;
    std::cout << 22027;
    for (int i = 0; i < 22027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22027(int& val) {
    const int x = 22027;
    std::cout << 22027;
    for (int i = 0; i < 22027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22028(int& val) {
    func_in_22028(val);
    const int x = 22028;
    std::cout << 22028;
    for (int i = 0; i < 22028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22028(int& val) {
    const int x = 22028;
    std::cout << 22028;
    for (int i = 0; i < 22028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22029(int& val) {
    func_in_22029(val);
    const int x = 22029;
    std::cout << 22029;
    for (int i = 0; i < 22029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22029(int& val) {
    const int x = 22029;
    std::cout << 22029;
    for (int i = 0; i < 22029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22030(int& val) {
    func_in_22030(val);
    const int x = 22030;
    std::cout << 22030;
    for (int i = 0; i < 22030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22030(int& val) {
    const int x = 22030;
    std::cout << 22030;
    for (int i = 0; i < 22030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22031(int& val) {
    func_in_22031(val);
    const int x = 22031;
    std::cout << 22031;
    for (int i = 0; i < 22031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22031(int& val) {
    const int x = 22031;
    std::cout << 22031;
    for (int i = 0; i < 22031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22032(int& val) {
    func_in_22032(val);
    const int x = 22032;
    std::cout << 22032;
    for (int i = 0; i < 22032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22032(int& val) {
    const int x = 22032;
    std::cout << 22032;
    for (int i = 0; i < 22032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22033(int& val) {
    func_in_22033(val);
    const int x = 22033;
    std::cout << 22033;
    for (int i = 0; i < 22033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22033(int& val) {
    const int x = 22033;
    std::cout << 22033;
    for (int i = 0; i < 22033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22034(int& val) {
    func_in_22034(val);
    const int x = 22034;
    std::cout << 22034;
    for (int i = 0; i < 22034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22034(int& val) {
    const int x = 22034;
    std::cout << 22034;
    for (int i = 0; i < 22034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22035(int& val) {
    func_in_22035(val);
    const int x = 22035;
    std::cout << 22035;
    for (int i = 0; i < 22035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22035(int& val) {
    const int x = 22035;
    std::cout << 22035;
    for (int i = 0; i < 22035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22036(int& val) {
    func_in_22036(val);
    const int x = 22036;
    std::cout << 22036;
    for (int i = 0; i < 22036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22036(int& val) {
    const int x = 22036;
    std::cout << 22036;
    for (int i = 0; i < 22036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22037(int& val) {
    func_in_22037(val);
    const int x = 22037;
    std::cout << 22037;
    for (int i = 0; i < 22037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22037(int& val) {
    const int x = 22037;
    std::cout << 22037;
    for (int i = 0; i < 22037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22038(int& val) {
    func_in_22038(val);
    const int x = 22038;
    std::cout << 22038;
    for (int i = 0; i < 22038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22038(int& val) {
    const int x = 22038;
    std::cout << 22038;
    for (int i = 0; i < 22038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22039(int& val) {
    func_in_22039(val);
    const int x = 22039;
    std::cout << 22039;
    for (int i = 0; i < 22039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22039(int& val) {
    const int x = 22039;
    std::cout << 22039;
    for (int i = 0; i < 22039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22040(int& val) {
    func_in_22040(val);
    const int x = 22040;
    std::cout << 22040;
    for (int i = 0; i < 22040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22040(int& val) {
    const int x = 22040;
    std::cout << 22040;
    for (int i = 0; i < 22040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22041(int& val) {
    func_in_22041(val);
    const int x = 22041;
    std::cout << 22041;
    for (int i = 0; i < 22041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22041(int& val) {
    const int x = 22041;
    std::cout << 22041;
    for (int i = 0; i < 22041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22042(int& val) {
    func_in_22042(val);
    const int x = 22042;
    std::cout << 22042;
    for (int i = 0; i < 22042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22042(int& val) {
    const int x = 22042;
    std::cout << 22042;
    for (int i = 0; i < 22042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22043(int& val) {
    func_in_22043(val);
    const int x = 22043;
    std::cout << 22043;
    for (int i = 0; i < 22043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22043(int& val) {
    const int x = 22043;
    std::cout << 22043;
    for (int i = 0; i < 22043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22044(int& val) {
    func_in_22044(val);
    const int x = 22044;
    std::cout << 22044;
    for (int i = 0; i < 22044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22044(int& val) {
    const int x = 22044;
    std::cout << 22044;
    for (int i = 0; i < 22044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22045(int& val) {
    func_in_22045(val);
    const int x = 22045;
    std::cout << 22045;
    for (int i = 0; i < 22045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22045(int& val) {
    const int x = 22045;
    std::cout << 22045;
    for (int i = 0; i < 22045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22046(int& val) {
    func_in_22046(val);
    const int x = 22046;
    std::cout << 22046;
    for (int i = 0; i < 22046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22046(int& val) {
    const int x = 22046;
    std::cout << 22046;
    for (int i = 0; i < 22046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22047(int& val) {
    func_in_22047(val);
    const int x = 22047;
    std::cout << 22047;
    for (int i = 0; i < 22047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22047(int& val) {
    const int x = 22047;
    std::cout << 22047;
    for (int i = 0; i < 22047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22048(int& val) {
    func_in_22048(val);
    const int x = 22048;
    std::cout << 22048;
    for (int i = 0; i < 22048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22048(int& val) {
    const int x = 22048;
    std::cout << 22048;
    for (int i = 0; i < 22048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22049(int& val) {
    func_in_22049(val);
    const int x = 22049;
    std::cout << 22049;
    for (int i = 0; i < 22049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22049(int& val) {
    const int x = 22049;
    std::cout << 22049;
    for (int i = 0; i < 22049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22050(int& val) {
    func_in_22050(val);
    const int x = 22050;
    std::cout << 22050;
    for (int i = 0; i < 22050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22050(int& val) {
    const int x = 22050;
    std::cout << 22050;
    for (int i = 0; i < 22050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22051(int& val) {
    func_in_22051(val);
    const int x = 22051;
    std::cout << 22051;
    for (int i = 0; i < 22051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22051(int& val) {
    const int x = 22051;
    std::cout << 22051;
    for (int i = 0; i < 22051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22052(int& val) {
    func_in_22052(val);
    const int x = 22052;
    std::cout << 22052;
    for (int i = 0; i < 22052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22052(int& val) {
    const int x = 22052;
    std::cout << 22052;
    for (int i = 0; i < 22052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22053(int& val) {
    func_in_22053(val);
    const int x = 22053;
    std::cout << 22053;
    for (int i = 0; i < 22053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22053(int& val) {
    const int x = 22053;
    std::cout << 22053;
    for (int i = 0; i < 22053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22054(int& val) {
    func_in_22054(val);
    const int x = 22054;
    std::cout << 22054;
    for (int i = 0; i < 22054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22054(int& val) {
    const int x = 22054;
    std::cout << 22054;
    for (int i = 0; i < 22054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22055(int& val) {
    func_in_22055(val);
    const int x = 22055;
    std::cout << 22055;
    for (int i = 0; i < 22055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22055(int& val) {
    const int x = 22055;
    std::cout << 22055;
    for (int i = 0; i < 22055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22056(int& val) {
    func_in_22056(val);
    const int x = 22056;
    std::cout << 22056;
    for (int i = 0; i < 22056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22056(int& val) {
    const int x = 22056;
    std::cout << 22056;
    for (int i = 0; i < 22056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22057(int& val) {
    func_in_22057(val);
    const int x = 22057;
    std::cout << 22057;
    for (int i = 0; i < 22057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22057(int& val) {
    const int x = 22057;
    std::cout << 22057;
    for (int i = 0; i < 22057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22058(int& val) {
    func_in_22058(val);
    const int x = 22058;
    std::cout << 22058;
    for (int i = 0; i < 22058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22058(int& val) {
    const int x = 22058;
    std::cout << 22058;
    for (int i = 0; i < 22058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22059(int& val) {
    func_in_22059(val);
    const int x = 22059;
    std::cout << 22059;
    for (int i = 0; i < 22059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22059(int& val) {
    const int x = 22059;
    std::cout << 22059;
    for (int i = 0; i < 22059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22060(int& val) {
    func_in_22060(val);
    const int x = 22060;
    std::cout << 22060;
    for (int i = 0; i < 22060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22060(int& val) {
    const int x = 22060;
    std::cout << 22060;
    for (int i = 0; i < 22060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22061(int& val) {
    func_in_22061(val);
    const int x = 22061;
    std::cout << 22061;
    for (int i = 0; i < 22061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22061(int& val) {
    const int x = 22061;
    std::cout << 22061;
    for (int i = 0; i < 22061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22062(int& val) {
    func_in_22062(val);
    const int x = 22062;
    std::cout << 22062;
    for (int i = 0; i < 22062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22062(int& val) {
    const int x = 22062;
    std::cout << 22062;
    for (int i = 0; i < 22062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22063(int& val) {
    func_in_22063(val);
    const int x = 22063;
    std::cout << 22063;
    for (int i = 0; i < 22063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22063(int& val) {
    const int x = 22063;
    std::cout << 22063;
    for (int i = 0; i < 22063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22064(int& val) {
    func_in_22064(val);
    const int x = 22064;
    std::cout << 22064;
    for (int i = 0; i < 22064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22064(int& val) {
    const int x = 22064;
    std::cout << 22064;
    for (int i = 0; i < 22064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22065(int& val) {
    func_in_22065(val);
    const int x = 22065;
    std::cout << 22065;
    for (int i = 0; i < 22065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22065(int& val) {
    const int x = 22065;
    std::cout << 22065;
    for (int i = 0; i < 22065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22066(int& val) {
    func_in_22066(val);
    const int x = 22066;
    std::cout << 22066;
    for (int i = 0; i < 22066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22066(int& val) {
    const int x = 22066;
    std::cout << 22066;
    for (int i = 0; i < 22066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22067(int& val) {
    func_in_22067(val);
    const int x = 22067;
    std::cout << 22067;
    for (int i = 0; i < 22067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22067(int& val) {
    const int x = 22067;
    std::cout << 22067;
    for (int i = 0; i < 22067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22068(int& val) {
    func_in_22068(val);
    const int x = 22068;
    std::cout << 22068;
    for (int i = 0; i < 22068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22068(int& val) {
    const int x = 22068;
    std::cout << 22068;
    for (int i = 0; i < 22068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22069(int& val) {
    func_in_22069(val);
    const int x = 22069;
    std::cout << 22069;
    for (int i = 0; i < 22069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22069(int& val) {
    const int x = 22069;
    std::cout << 22069;
    for (int i = 0; i < 22069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22070(int& val) {
    func_in_22070(val);
    const int x = 22070;
    std::cout << 22070;
    for (int i = 0; i < 22070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22070(int& val) {
    const int x = 22070;
    std::cout << 22070;
    for (int i = 0; i < 22070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22071(int& val) {
    func_in_22071(val);
    const int x = 22071;
    std::cout << 22071;
    for (int i = 0; i < 22071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22071(int& val) {
    const int x = 22071;
    std::cout << 22071;
    for (int i = 0; i < 22071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22072(int& val) {
    func_in_22072(val);
    const int x = 22072;
    std::cout << 22072;
    for (int i = 0; i < 22072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22072(int& val) {
    const int x = 22072;
    std::cout << 22072;
    for (int i = 0; i < 22072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22073(int& val) {
    func_in_22073(val);
    const int x = 22073;
    std::cout << 22073;
    for (int i = 0; i < 22073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22073(int& val) {
    const int x = 22073;
    std::cout << 22073;
    for (int i = 0; i < 22073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22074(int& val) {
    func_in_22074(val);
    const int x = 22074;
    std::cout << 22074;
    for (int i = 0; i < 22074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22074(int& val) {
    const int x = 22074;
    std::cout << 22074;
    for (int i = 0; i < 22074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22075(int& val) {
    func_in_22075(val);
    const int x = 22075;
    std::cout << 22075;
    for (int i = 0; i < 22075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22075(int& val) {
    const int x = 22075;
    std::cout << 22075;
    for (int i = 0; i < 22075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22076(int& val) {
    func_in_22076(val);
    const int x = 22076;
    std::cout << 22076;
    for (int i = 0; i < 22076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22076(int& val) {
    const int x = 22076;
    std::cout << 22076;
    for (int i = 0; i < 22076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22077(int& val) {
    func_in_22077(val);
    const int x = 22077;
    std::cout << 22077;
    for (int i = 0; i < 22077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22077(int& val) {
    const int x = 22077;
    std::cout << 22077;
    for (int i = 0; i < 22077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22078(int& val) {
    func_in_22078(val);
    const int x = 22078;
    std::cout << 22078;
    for (int i = 0; i < 22078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22078(int& val) {
    const int x = 22078;
    std::cout << 22078;
    for (int i = 0; i < 22078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22079(int& val) {
    func_in_22079(val);
    const int x = 22079;
    std::cout << 22079;
    for (int i = 0; i < 22079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22079(int& val) {
    const int x = 22079;
    std::cout << 22079;
    for (int i = 0; i < 22079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22080(int& val) {
    func_in_22080(val);
    const int x = 22080;
    std::cout << 22080;
    for (int i = 0; i < 22080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22080(int& val) {
    const int x = 22080;
    std::cout << 22080;
    for (int i = 0; i < 22080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22081(int& val) {
    func_in_22081(val);
    const int x = 22081;
    std::cout << 22081;
    for (int i = 0; i < 22081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22081(int& val) {
    const int x = 22081;
    std::cout << 22081;
    for (int i = 0; i < 22081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22082(int& val) {
    func_in_22082(val);
    const int x = 22082;
    std::cout << 22082;
    for (int i = 0; i < 22082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22082(int& val) {
    const int x = 22082;
    std::cout << 22082;
    for (int i = 0; i < 22082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22083(int& val) {
    func_in_22083(val);
    const int x = 22083;
    std::cout << 22083;
    for (int i = 0; i < 22083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22083(int& val) {
    const int x = 22083;
    std::cout << 22083;
    for (int i = 0; i < 22083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22084(int& val) {
    func_in_22084(val);
    const int x = 22084;
    std::cout << 22084;
    for (int i = 0; i < 22084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22084(int& val) {
    const int x = 22084;
    std::cout << 22084;
    for (int i = 0; i < 22084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22085(int& val) {
    func_in_22085(val);
    const int x = 22085;
    std::cout << 22085;
    for (int i = 0; i < 22085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22085(int& val) {
    const int x = 22085;
    std::cout << 22085;
    for (int i = 0; i < 22085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22086(int& val) {
    func_in_22086(val);
    const int x = 22086;
    std::cout << 22086;
    for (int i = 0; i < 22086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22086(int& val) {
    const int x = 22086;
    std::cout << 22086;
    for (int i = 0; i < 22086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22087(int& val) {
    func_in_22087(val);
    const int x = 22087;
    std::cout << 22087;
    for (int i = 0; i < 22087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22087(int& val) {
    const int x = 22087;
    std::cout << 22087;
    for (int i = 0; i < 22087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22088(int& val) {
    func_in_22088(val);
    const int x = 22088;
    std::cout << 22088;
    for (int i = 0; i < 22088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22088(int& val) {
    const int x = 22088;
    std::cout << 22088;
    for (int i = 0; i < 22088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22089(int& val) {
    func_in_22089(val);
    const int x = 22089;
    std::cout << 22089;
    for (int i = 0; i < 22089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22089(int& val) {
    const int x = 22089;
    std::cout << 22089;
    for (int i = 0; i < 22089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22090(int& val) {
    func_in_22090(val);
    const int x = 22090;
    std::cout << 22090;
    for (int i = 0; i < 22090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22090(int& val) {
    const int x = 22090;
    std::cout << 22090;
    for (int i = 0; i < 22090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22091(int& val) {
    func_in_22091(val);
    const int x = 22091;
    std::cout << 22091;
    for (int i = 0; i < 22091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22091(int& val) {
    const int x = 22091;
    std::cout << 22091;
    for (int i = 0; i < 22091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22092(int& val) {
    func_in_22092(val);
    const int x = 22092;
    std::cout << 22092;
    for (int i = 0; i < 22092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22092(int& val) {
    const int x = 22092;
    std::cout << 22092;
    for (int i = 0; i < 22092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22093(int& val) {
    func_in_22093(val);
    const int x = 22093;
    std::cout << 22093;
    for (int i = 0; i < 22093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22093(int& val) {
    const int x = 22093;
    std::cout << 22093;
    for (int i = 0; i < 22093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22094(int& val) {
    func_in_22094(val);
    const int x = 22094;
    std::cout << 22094;
    for (int i = 0; i < 22094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22094(int& val) {
    const int x = 22094;
    std::cout << 22094;
    for (int i = 0; i < 22094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22095(int& val) {
    func_in_22095(val);
    const int x = 22095;
    std::cout << 22095;
    for (int i = 0; i < 22095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22095(int& val) {
    const int x = 22095;
    std::cout << 22095;
    for (int i = 0; i < 22095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22096(int& val) {
    func_in_22096(val);
    const int x = 22096;
    std::cout << 22096;
    for (int i = 0; i < 22096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22096(int& val) {
    const int x = 22096;
    std::cout << 22096;
    for (int i = 0; i < 22096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22097(int& val) {
    func_in_22097(val);
    const int x = 22097;
    std::cout << 22097;
    for (int i = 0; i < 22097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22097(int& val) {
    const int x = 22097;
    std::cout << 22097;
    for (int i = 0; i < 22097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22098(int& val) {
    func_in_22098(val);
    const int x = 22098;
    std::cout << 22098;
    for (int i = 0; i < 22098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22098(int& val) {
    const int x = 22098;
    std::cout << 22098;
    for (int i = 0; i < 22098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22099(int& val) {
    func_in_22099(val);
    const int x = 22099;
    std::cout << 22099;
    for (int i = 0; i < 22099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22099(int& val) {
    const int x = 22099;
    std::cout << 22099;
    for (int i = 0; i < 22099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22100(int& val) {
    func_in_22100(val);
    const int x = 22100;
    std::cout << 22100;
    for (int i = 0; i < 22100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22100(int& val) {
    const int x = 22100;
    std::cout << 22100;
    for (int i = 0; i < 22100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22101(int& val) {
    func_in_22101(val);
    const int x = 22101;
    std::cout << 22101;
    for (int i = 0; i < 22101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22101(int& val) {
    const int x = 22101;
    std::cout << 22101;
    for (int i = 0; i < 22101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22102(int& val) {
    func_in_22102(val);
    const int x = 22102;
    std::cout << 22102;
    for (int i = 0; i < 22102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22102(int& val) {
    const int x = 22102;
    std::cout << 22102;
    for (int i = 0; i < 22102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22103(int& val) {
    func_in_22103(val);
    const int x = 22103;
    std::cout << 22103;
    for (int i = 0; i < 22103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22103(int& val) {
    const int x = 22103;
    std::cout << 22103;
    for (int i = 0; i < 22103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22104(int& val) {
    func_in_22104(val);
    const int x = 22104;
    std::cout << 22104;
    for (int i = 0; i < 22104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22104(int& val) {
    const int x = 22104;
    std::cout << 22104;
    for (int i = 0; i < 22104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22105(int& val) {
    func_in_22105(val);
    const int x = 22105;
    std::cout << 22105;
    for (int i = 0; i < 22105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22105(int& val) {
    const int x = 22105;
    std::cout << 22105;
    for (int i = 0; i < 22105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22106(int& val) {
    func_in_22106(val);
    const int x = 22106;
    std::cout << 22106;
    for (int i = 0; i < 22106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22106(int& val) {
    const int x = 22106;
    std::cout << 22106;
    for (int i = 0; i < 22106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22107(int& val) {
    func_in_22107(val);
    const int x = 22107;
    std::cout << 22107;
    for (int i = 0; i < 22107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22107(int& val) {
    const int x = 22107;
    std::cout << 22107;
    for (int i = 0; i < 22107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22108(int& val) {
    func_in_22108(val);
    const int x = 22108;
    std::cout << 22108;
    for (int i = 0; i < 22108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22108(int& val) {
    const int x = 22108;
    std::cout << 22108;
    for (int i = 0; i < 22108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22109(int& val) {
    func_in_22109(val);
    const int x = 22109;
    std::cout << 22109;
    for (int i = 0; i < 22109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22109(int& val) {
    const int x = 22109;
    std::cout << 22109;
    for (int i = 0; i < 22109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22110(int& val) {
    func_in_22110(val);
    const int x = 22110;
    std::cout << 22110;
    for (int i = 0; i < 22110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22110(int& val) {
    const int x = 22110;
    std::cout << 22110;
    for (int i = 0; i < 22110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22111(int& val) {
    func_in_22111(val);
    const int x = 22111;
    std::cout << 22111;
    for (int i = 0; i < 22111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22111(int& val) {
    const int x = 22111;
    std::cout << 22111;
    for (int i = 0; i < 22111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22112(int& val) {
    func_in_22112(val);
    const int x = 22112;
    std::cout << 22112;
    for (int i = 0; i < 22112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22112(int& val) {
    const int x = 22112;
    std::cout << 22112;
    for (int i = 0; i < 22112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22113(int& val) {
    func_in_22113(val);
    const int x = 22113;
    std::cout << 22113;
    for (int i = 0; i < 22113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22113(int& val) {
    const int x = 22113;
    std::cout << 22113;
    for (int i = 0; i < 22113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22114(int& val) {
    func_in_22114(val);
    const int x = 22114;
    std::cout << 22114;
    for (int i = 0; i < 22114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22114(int& val) {
    const int x = 22114;
    std::cout << 22114;
    for (int i = 0; i < 22114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22115(int& val) {
    func_in_22115(val);
    const int x = 22115;
    std::cout << 22115;
    for (int i = 0; i < 22115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22115(int& val) {
    const int x = 22115;
    std::cout << 22115;
    for (int i = 0; i < 22115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22116(int& val) {
    func_in_22116(val);
    const int x = 22116;
    std::cout << 22116;
    for (int i = 0; i < 22116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22116(int& val) {
    const int x = 22116;
    std::cout << 22116;
    for (int i = 0; i < 22116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22117(int& val) {
    func_in_22117(val);
    const int x = 22117;
    std::cout << 22117;
    for (int i = 0; i < 22117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22117(int& val) {
    const int x = 22117;
    std::cout << 22117;
    for (int i = 0; i < 22117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22118(int& val) {
    func_in_22118(val);
    const int x = 22118;
    std::cout << 22118;
    for (int i = 0; i < 22118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22118(int& val) {
    const int x = 22118;
    std::cout << 22118;
    for (int i = 0; i < 22118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22119(int& val) {
    func_in_22119(val);
    const int x = 22119;
    std::cout << 22119;
    for (int i = 0; i < 22119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22119(int& val) {
    const int x = 22119;
    std::cout << 22119;
    for (int i = 0; i < 22119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22120(int& val) {
    func_in_22120(val);
    const int x = 22120;
    std::cout << 22120;
    for (int i = 0; i < 22120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22120(int& val) {
    const int x = 22120;
    std::cout << 22120;
    for (int i = 0; i < 22120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22121(int& val) {
    func_in_22121(val);
    const int x = 22121;
    std::cout << 22121;
    for (int i = 0; i < 22121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22121(int& val) {
    const int x = 22121;
    std::cout << 22121;
    for (int i = 0; i < 22121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22122(int& val) {
    func_in_22122(val);
    const int x = 22122;
    std::cout << 22122;
    for (int i = 0; i < 22122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22122(int& val) {
    const int x = 22122;
    std::cout << 22122;
    for (int i = 0; i < 22122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22123(int& val) {
    func_in_22123(val);
    const int x = 22123;
    std::cout << 22123;
    for (int i = 0; i < 22123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22123(int& val) {
    const int x = 22123;
    std::cout << 22123;
    for (int i = 0; i < 22123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22124(int& val) {
    func_in_22124(val);
    const int x = 22124;
    std::cout << 22124;
    for (int i = 0; i < 22124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22124(int& val) {
    const int x = 22124;
    std::cout << 22124;
    for (int i = 0; i < 22124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22125(int& val) {
    func_in_22125(val);
    const int x = 22125;
    std::cout << 22125;
    for (int i = 0; i < 22125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22125(int& val) {
    const int x = 22125;
    std::cout << 22125;
    for (int i = 0; i < 22125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22126(int& val) {
    func_in_22126(val);
    const int x = 22126;
    std::cout << 22126;
    for (int i = 0; i < 22126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22126(int& val) {
    const int x = 22126;
    std::cout << 22126;
    for (int i = 0; i < 22126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22127(int& val) {
    func_in_22127(val);
    const int x = 22127;
    std::cout << 22127;
    for (int i = 0; i < 22127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22127(int& val) {
    const int x = 22127;
    std::cout << 22127;
    for (int i = 0; i < 22127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22128(int& val) {
    func_in_22128(val);
    const int x = 22128;
    std::cout << 22128;
    for (int i = 0; i < 22128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22128(int& val) {
    const int x = 22128;
    std::cout << 22128;
    for (int i = 0; i < 22128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22129(int& val) {
    func_in_22129(val);
    const int x = 22129;
    std::cout << 22129;
    for (int i = 0; i < 22129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22129(int& val) {
    const int x = 22129;
    std::cout << 22129;
    for (int i = 0; i < 22129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22130(int& val) {
    func_in_22130(val);
    const int x = 22130;
    std::cout << 22130;
    for (int i = 0; i < 22130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22130(int& val) {
    const int x = 22130;
    std::cout << 22130;
    for (int i = 0; i < 22130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22131(int& val) {
    func_in_22131(val);
    const int x = 22131;
    std::cout << 22131;
    for (int i = 0; i < 22131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22131(int& val) {
    const int x = 22131;
    std::cout << 22131;
    for (int i = 0; i < 22131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22132(int& val) {
    func_in_22132(val);
    const int x = 22132;
    std::cout << 22132;
    for (int i = 0; i < 22132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22132(int& val) {
    const int x = 22132;
    std::cout << 22132;
    for (int i = 0; i < 22132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22133(int& val) {
    func_in_22133(val);
    const int x = 22133;
    std::cout << 22133;
    for (int i = 0; i < 22133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22133(int& val) {
    const int x = 22133;
    std::cout << 22133;
    for (int i = 0; i < 22133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22134(int& val) {
    func_in_22134(val);
    const int x = 22134;
    std::cout << 22134;
    for (int i = 0; i < 22134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22134(int& val) {
    const int x = 22134;
    std::cout << 22134;
    for (int i = 0; i < 22134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22135(int& val) {
    func_in_22135(val);
    const int x = 22135;
    std::cout << 22135;
    for (int i = 0; i < 22135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22135(int& val) {
    const int x = 22135;
    std::cout << 22135;
    for (int i = 0; i < 22135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22136(int& val) {
    func_in_22136(val);
    const int x = 22136;
    std::cout << 22136;
    for (int i = 0; i < 22136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22136(int& val) {
    const int x = 22136;
    std::cout << 22136;
    for (int i = 0; i < 22136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22137(int& val) {
    func_in_22137(val);
    const int x = 22137;
    std::cout << 22137;
    for (int i = 0; i < 22137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22137(int& val) {
    const int x = 22137;
    std::cout << 22137;
    for (int i = 0; i < 22137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22138(int& val) {
    func_in_22138(val);
    const int x = 22138;
    std::cout << 22138;
    for (int i = 0; i < 22138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22138(int& val) {
    const int x = 22138;
    std::cout << 22138;
    for (int i = 0; i < 22138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22139(int& val) {
    func_in_22139(val);
    const int x = 22139;
    std::cout << 22139;
    for (int i = 0; i < 22139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22139(int& val) {
    const int x = 22139;
    std::cout << 22139;
    for (int i = 0; i < 22139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22140(int& val) {
    func_in_22140(val);
    const int x = 22140;
    std::cout << 22140;
    for (int i = 0; i < 22140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22140(int& val) {
    const int x = 22140;
    std::cout << 22140;
    for (int i = 0; i < 22140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22141(int& val) {
    func_in_22141(val);
    const int x = 22141;
    std::cout << 22141;
    for (int i = 0; i < 22141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22141(int& val) {
    const int x = 22141;
    std::cout << 22141;
    for (int i = 0; i < 22141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22142(int& val) {
    func_in_22142(val);
    const int x = 22142;
    std::cout << 22142;
    for (int i = 0; i < 22142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22142(int& val) {
    const int x = 22142;
    std::cout << 22142;
    for (int i = 0; i < 22142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22143(int& val) {
    func_in_22143(val);
    const int x = 22143;
    std::cout << 22143;
    for (int i = 0; i < 22143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22143(int& val) {
    const int x = 22143;
    std::cout << 22143;
    for (int i = 0; i < 22143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22144(int& val) {
    func_in_22144(val);
    const int x = 22144;
    std::cout << 22144;
    for (int i = 0; i < 22144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22144(int& val) {
    const int x = 22144;
    std::cout << 22144;
    for (int i = 0; i < 22144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22145(int& val) {
    func_in_22145(val);
    const int x = 22145;
    std::cout << 22145;
    for (int i = 0; i < 22145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22145(int& val) {
    const int x = 22145;
    std::cout << 22145;
    for (int i = 0; i < 22145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22146(int& val) {
    func_in_22146(val);
    const int x = 22146;
    std::cout << 22146;
    for (int i = 0; i < 22146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22146(int& val) {
    const int x = 22146;
    std::cout << 22146;
    for (int i = 0; i < 22146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22147(int& val) {
    func_in_22147(val);
    const int x = 22147;
    std::cout << 22147;
    for (int i = 0; i < 22147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22147(int& val) {
    const int x = 22147;
    std::cout << 22147;
    for (int i = 0; i < 22147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22148(int& val) {
    func_in_22148(val);
    const int x = 22148;
    std::cout << 22148;
    for (int i = 0; i < 22148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22148(int& val) {
    const int x = 22148;
    std::cout << 22148;
    for (int i = 0; i < 22148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22149(int& val) {
    func_in_22149(val);
    const int x = 22149;
    std::cout << 22149;
    for (int i = 0; i < 22149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22149(int& val) {
    const int x = 22149;
    std::cout << 22149;
    for (int i = 0; i < 22149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22150(int& val) {
    func_in_22150(val);
    const int x = 22150;
    std::cout << 22150;
    for (int i = 0; i < 22150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22150(int& val) {
    const int x = 22150;
    std::cout << 22150;
    for (int i = 0; i < 22150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22151(int& val) {
    func_in_22151(val);
    const int x = 22151;
    std::cout << 22151;
    for (int i = 0; i < 22151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22151(int& val) {
    const int x = 22151;
    std::cout << 22151;
    for (int i = 0; i < 22151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22152(int& val) {
    func_in_22152(val);
    const int x = 22152;
    std::cout << 22152;
    for (int i = 0; i < 22152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22152(int& val) {
    const int x = 22152;
    std::cout << 22152;
    for (int i = 0; i < 22152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22153(int& val) {
    func_in_22153(val);
    const int x = 22153;
    std::cout << 22153;
    for (int i = 0; i < 22153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22153(int& val) {
    const int x = 22153;
    std::cout << 22153;
    for (int i = 0; i < 22153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22154(int& val) {
    func_in_22154(val);
    const int x = 22154;
    std::cout << 22154;
    for (int i = 0; i < 22154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22154(int& val) {
    const int x = 22154;
    std::cout << 22154;
    for (int i = 0; i < 22154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22155(int& val) {
    func_in_22155(val);
    const int x = 22155;
    std::cout << 22155;
    for (int i = 0; i < 22155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22155(int& val) {
    const int x = 22155;
    std::cout << 22155;
    for (int i = 0; i < 22155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22156(int& val) {
    func_in_22156(val);
    const int x = 22156;
    std::cout << 22156;
    for (int i = 0; i < 22156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22156(int& val) {
    const int x = 22156;
    std::cout << 22156;
    for (int i = 0; i < 22156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22157(int& val) {
    func_in_22157(val);
    const int x = 22157;
    std::cout << 22157;
    for (int i = 0; i < 22157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22157(int& val) {
    const int x = 22157;
    std::cout << 22157;
    for (int i = 0; i < 22157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22158(int& val) {
    func_in_22158(val);
    const int x = 22158;
    std::cout << 22158;
    for (int i = 0; i < 22158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22158(int& val) {
    const int x = 22158;
    std::cout << 22158;
    for (int i = 0; i < 22158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22159(int& val) {
    func_in_22159(val);
    const int x = 22159;
    std::cout << 22159;
    for (int i = 0; i < 22159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22159(int& val) {
    const int x = 22159;
    std::cout << 22159;
    for (int i = 0; i < 22159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22160(int& val) {
    func_in_22160(val);
    const int x = 22160;
    std::cout << 22160;
    for (int i = 0; i < 22160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22160(int& val) {
    const int x = 22160;
    std::cout << 22160;
    for (int i = 0; i < 22160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22161(int& val) {
    func_in_22161(val);
    const int x = 22161;
    std::cout << 22161;
    for (int i = 0; i < 22161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22161(int& val) {
    const int x = 22161;
    std::cout << 22161;
    for (int i = 0; i < 22161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22162(int& val) {
    func_in_22162(val);
    const int x = 22162;
    std::cout << 22162;
    for (int i = 0; i < 22162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22162(int& val) {
    const int x = 22162;
    std::cout << 22162;
    for (int i = 0; i < 22162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22163(int& val) {
    func_in_22163(val);
    const int x = 22163;
    std::cout << 22163;
    for (int i = 0; i < 22163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22163(int& val) {
    const int x = 22163;
    std::cout << 22163;
    for (int i = 0; i < 22163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22164(int& val) {
    func_in_22164(val);
    const int x = 22164;
    std::cout << 22164;
    for (int i = 0; i < 22164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22164(int& val) {
    const int x = 22164;
    std::cout << 22164;
    for (int i = 0; i < 22164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22165(int& val) {
    func_in_22165(val);
    const int x = 22165;
    std::cout << 22165;
    for (int i = 0; i < 22165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22165(int& val) {
    const int x = 22165;
    std::cout << 22165;
    for (int i = 0; i < 22165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22166(int& val) {
    func_in_22166(val);
    const int x = 22166;
    std::cout << 22166;
    for (int i = 0; i < 22166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22166(int& val) {
    const int x = 22166;
    std::cout << 22166;
    for (int i = 0; i < 22166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22167(int& val) {
    func_in_22167(val);
    const int x = 22167;
    std::cout << 22167;
    for (int i = 0; i < 22167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22167(int& val) {
    const int x = 22167;
    std::cout << 22167;
    for (int i = 0; i < 22167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22168(int& val) {
    func_in_22168(val);
    const int x = 22168;
    std::cout << 22168;
    for (int i = 0; i < 22168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22168(int& val) {
    const int x = 22168;
    std::cout << 22168;
    for (int i = 0; i < 22168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22169(int& val) {
    func_in_22169(val);
    const int x = 22169;
    std::cout << 22169;
    for (int i = 0; i < 22169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22169(int& val) {
    const int x = 22169;
    std::cout << 22169;
    for (int i = 0; i < 22169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22170(int& val) {
    func_in_22170(val);
    const int x = 22170;
    std::cout << 22170;
    for (int i = 0; i < 22170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22170(int& val) {
    const int x = 22170;
    std::cout << 22170;
    for (int i = 0; i < 22170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22171(int& val) {
    func_in_22171(val);
    const int x = 22171;
    std::cout << 22171;
    for (int i = 0; i < 22171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22171(int& val) {
    const int x = 22171;
    std::cout << 22171;
    for (int i = 0; i < 22171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22172(int& val) {
    func_in_22172(val);
    const int x = 22172;
    std::cout << 22172;
    for (int i = 0; i < 22172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22172(int& val) {
    const int x = 22172;
    std::cout << 22172;
    for (int i = 0; i < 22172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22173(int& val) {
    func_in_22173(val);
    const int x = 22173;
    std::cout << 22173;
    for (int i = 0; i < 22173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22173(int& val) {
    const int x = 22173;
    std::cout << 22173;
    for (int i = 0; i < 22173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22174(int& val) {
    func_in_22174(val);
    const int x = 22174;
    std::cout << 22174;
    for (int i = 0; i < 22174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22174(int& val) {
    const int x = 22174;
    std::cout << 22174;
    for (int i = 0; i < 22174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22175(int& val) {
    func_in_22175(val);
    const int x = 22175;
    std::cout << 22175;
    for (int i = 0; i < 22175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22175(int& val) {
    const int x = 22175;
    std::cout << 22175;
    for (int i = 0; i < 22175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22176(int& val) {
    func_in_22176(val);
    const int x = 22176;
    std::cout << 22176;
    for (int i = 0; i < 22176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22176(int& val) {
    const int x = 22176;
    std::cout << 22176;
    for (int i = 0; i < 22176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22177(int& val) {
    func_in_22177(val);
    const int x = 22177;
    std::cout << 22177;
    for (int i = 0; i < 22177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22177(int& val) {
    const int x = 22177;
    std::cout << 22177;
    for (int i = 0; i < 22177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22178(int& val) {
    func_in_22178(val);
    const int x = 22178;
    std::cout << 22178;
    for (int i = 0; i < 22178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22178(int& val) {
    const int x = 22178;
    std::cout << 22178;
    for (int i = 0; i < 22178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22179(int& val) {
    func_in_22179(val);
    const int x = 22179;
    std::cout << 22179;
    for (int i = 0; i < 22179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22179(int& val) {
    const int x = 22179;
    std::cout << 22179;
    for (int i = 0; i < 22179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22180(int& val) {
    func_in_22180(val);
    const int x = 22180;
    std::cout << 22180;
    for (int i = 0; i < 22180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22180(int& val) {
    const int x = 22180;
    std::cout << 22180;
    for (int i = 0; i < 22180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22181(int& val) {
    func_in_22181(val);
    const int x = 22181;
    std::cout << 22181;
    for (int i = 0; i < 22181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22181(int& val) {
    const int x = 22181;
    std::cout << 22181;
    for (int i = 0; i < 22181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22182(int& val) {
    func_in_22182(val);
    const int x = 22182;
    std::cout << 22182;
    for (int i = 0; i < 22182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22182(int& val) {
    const int x = 22182;
    std::cout << 22182;
    for (int i = 0; i < 22182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22183(int& val) {
    func_in_22183(val);
    const int x = 22183;
    std::cout << 22183;
    for (int i = 0; i < 22183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22183(int& val) {
    const int x = 22183;
    std::cout << 22183;
    for (int i = 0; i < 22183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22184(int& val) {
    func_in_22184(val);
    const int x = 22184;
    std::cout << 22184;
    for (int i = 0; i < 22184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22184(int& val) {
    const int x = 22184;
    std::cout << 22184;
    for (int i = 0; i < 22184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22185(int& val) {
    func_in_22185(val);
    const int x = 22185;
    std::cout << 22185;
    for (int i = 0; i < 22185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22185(int& val) {
    const int x = 22185;
    std::cout << 22185;
    for (int i = 0; i < 22185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22186(int& val) {
    func_in_22186(val);
    const int x = 22186;
    std::cout << 22186;
    for (int i = 0; i < 22186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22186(int& val) {
    const int x = 22186;
    std::cout << 22186;
    for (int i = 0; i < 22186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22187(int& val) {
    func_in_22187(val);
    const int x = 22187;
    std::cout << 22187;
    for (int i = 0; i < 22187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22187(int& val) {
    const int x = 22187;
    std::cout << 22187;
    for (int i = 0; i < 22187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22188(int& val) {
    func_in_22188(val);
    const int x = 22188;
    std::cout << 22188;
    for (int i = 0; i < 22188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22188(int& val) {
    const int x = 22188;
    std::cout << 22188;
    for (int i = 0; i < 22188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22189(int& val) {
    func_in_22189(val);
    const int x = 22189;
    std::cout << 22189;
    for (int i = 0; i < 22189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22189(int& val) {
    const int x = 22189;
    std::cout << 22189;
    for (int i = 0; i < 22189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22190(int& val) {
    func_in_22190(val);
    const int x = 22190;
    std::cout << 22190;
    for (int i = 0; i < 22190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22190(int& val) {
    const int x = 22190;
    std::cout << 22190;
    for (int i = 0; i < 22190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22191(int& val) {
    func_in_22191(val);
    const int x = 22191;
    std::cout << 22191;
    for (int i = 0; i < 22191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22191(int& val) {
    const int x = 22191;
    std::cout << 22191;
    for (int i = 0; i < 22191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22192(int& val) {
    func_in_22192(val);
    const int x = 22192;
    std::cout << 22192;
    for (int i = 0; i < 22192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22192(int& val) {
    const int x = 22192;
    std::cout << 22192;
    for (int i = 0; i < 22192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22193(int& val) {
    func_in_22193(val);
    const int x = 22193;
    std::cout << 22193;
    for (int i = 0; i < 22193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22193(int& val) {
    const int x = 22193;
    std::cout << 22193;
    for (int i = 0; i < 22193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22194(int& val) {
    func_in_22194(val);
    const int x = 22194;
    std::cout << 22194;
    for (int i = 0; i < 22194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22194(int& val) {
    const int x = 22194;
    std::cout << 22194;
    for (int i = 0; i < 22194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22195(int& val) {
    func_in_22195(val);
    const int x = 22195;
    std::cout << 22195;
    for (int i = 0; i < 22195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22195(int& val) {
    const int x = 22195;
    std::cout << 22195;
    for (int i = 0; i < 22195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22196(int& val) {
    func_in_22196(val);
    const int x = 22196;
    std::cout << 22196;
    for (int i = 0; i < 22196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22196(int& val) {
    const int x = 22196;
    std::cout << 22196;
    for (int i = 0; i < 22196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22197(int& val) {
    func_in_22197(val);
    const int x = 22197;
    std::cout << 22197;
    for (int i = 0; i < 22197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22197(int& val) {
    const int x = 22197;
    std::cout << 22197;
    for (int i = 0; i < 22197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22198(int& val) {
    func_in_22198(val);
    const int x = 22198;
    std::cout << 22198;
    for (int i = 0; i < 22198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22198(int& val) {
    const int x = 22198;
    std::cout << 22198;
    for (int i = 0; i < 22198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22199(int& val) {
    func_in_22199(val);
    const int x = 22199;
    std::cout << 22199;
    for (int i = 0; i < 22199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22199(int& val) {
    const int x = 22199;
    std::cout << 22199;
    for (int i = 0; i < 22199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22200(int& val) {
    func_in_22200(val);
    const int x = 22200;
    std::cout << 22200;
    for (int i = 0; i < 22200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22200(int& val) {
    const int x = 22200;
    std::cout << 22200;
    for (int i = 0; i < 22200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22201(int& val) {
    func_in_22201(val);
    const int x = 22201;
    std::cout << 22201;
    for (int i = 0; i < 22201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22201(int& val) {
    const int x = 22201;
    std::cout << 22201;
    for (int i = 0; i < 22201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22202(int& val) {
    func_in_22202(val);
    const int x = 22202;
    std::cout << 22202;
    for (int i = 0; i < 22202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22202(int& val) {
    const int x = 22202;
    std::cout << 22202;
    for (int i = 0; i < 22202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22203(int& val) {
    func_in_22203(val);
    const int x = 22203;
    std::cout << 22203;
    for (int i = 0; i < 22203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22203(int& val) {
    const int x = 22203;
    std::cout << 22203;
    for (int i = 0; i < 22203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22204(int& val) {
    func_in_22204(val);
    const int x = 22204;
    std::cout << 22204;
    for (int i = 0; i < 22204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22204(int& val) {
    const int x = 22204;
    std::cout << 22204;
    for (int i = 0; i < 22204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22205(int& val) {
    func_in_22205(val);
    const int x = 22205;
    std::cout << 22205;
    for (int i = 0; i < 22205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22205(int& val) {
    const int x = 22205;
    std::cout << 22205;
    for (int i = 0; i < 22205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22206(int& val) {
    func_in_22206(val);
    const int x = 22206;
    std::cout << 22206;
    for (int i = 0; i < 22206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22206(int& val) {
    const int x = 22206;
    std::cout << 22206;
    for (int i = 0; i < 22206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22207(int& val) {
    func_in_22207(val);
    const int x = 22207;
    std::cout << 22207;
    for (int i = 0; i < 22207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22207(int& val) {
    const int x = 22207;
    std::cout << 22207;
    for (int i = 0; i < 22207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22208(int& val) {
    func_in_22208(val);
    const int x = 22208;
    std::cout << 22208;
    for (int i = 0; i < 22208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22208(int& val) {
    const int x = 22208;
    std::cout << 22208;
    for (int i = 0; i < 22208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22209(int& val) {
    func_in_22209(val);
    const int x = 22209;
    std::cout << 22209;
    for (int i = 0; i < 22209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22209(int& val) {
    const int x = 22209;
    std::cout << 22209;
    for (int i = 0; i < 22209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22210(int& val) {
    func_in_22210(val);
    const int x = 22210;
    std::cout << 22210;
    for (int i = 0; i < 22210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22210(int& val) {
    const int x = 22210;
    std::cout << 22210;
    for (int i = 0; i < 22210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22211(int& val) {
    func_in_22211(val);
    const int x = 22211;
    std::cout << 22211;
    for (int i = 0; i < 22211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22211(int& val) {
    const int x = 22211;
    std::cout << 22211;
    for (int i = 0; i < 22211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22212(int& val) {
    func_in_22212(val);
    const int x = 22212;
    std::cout << 22212;
    for (int i = 0; i < 22212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22212(int& val) {
    const int x = 22212;
    std::cout << 22212;
    for (int i = 0; i < 22212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22213(int& val) {
    func_in_22213(val);
    const int x = 22213;
    std::cout << 22213;
    for (int i = 0; i < 22213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22213(int& val) {
    const int x = 22213;
    std::cout << 22213;
    for (int i = 0; i < 22213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22214(int& val) {
    func_in_22214(val);
    const int x = 22214;
    std::cout << 22214;
    for (int i = 0; i < 22214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22214(int& val) {
    const int x = 22214;
    std::cout << 22214;
    for (int i = 0; i < 22214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22215(int& val) {
    func_in_22215(val);
    const int x = 22215;
    std::cout << 22215;
    for (int i = 0; i < 22215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22215(int& val) {
    const int x = 22215;
    std::cout << 22215;
    for (int i = 0; i < 22215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22216(int& val) {
    func_in_22216(val);
    const int x = 22216;
    std::cout << 22216;
    for (int i = 0; i < 22216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22216(int& val) {
    const int x = 22216;
    std::cout << 22216;
    for (int i = 0; i < 22216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22217(int& val) {
    func_in_22217(val);
    const int x = 22217;
    std::cout << 22217;
    for (int i = 0; i < 22217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22217(int& val) {
    const int x = 22217;
    std::cout << 22217;
    for (int i = 0; i < 22217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22218(int& val) {
    func_in_22218(val);
    const int x = 22218;
    std::cout << 22218;
    for (int i = 0; i < 22218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22218(int& val) {
    const int x = 22218;
    std::cout << 22218;
    for (int i = 0; i < 22218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22219(int& val) {
    func_in_22219(val);
    const int x = 22219;
    std::cout << 22219;
    for (int i = 0; i < 22219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22219(int& val) {
    const int x = 22219;
    std::cout << 22219;
    for (int i = 0; i < 22219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22220(int& val) {
    func_in_22220(val);
    const int x = 22220;
    std::cout << 22220;
    for (int i = 0; i < 22220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22220(int& val) {
    const int x = 22220;
    std::cout << 22220;
    for (int i = 0; i < 22220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22221(int& val) {
    func_in_22221(val);
    const int x = 22221;
    std::cout << 22221;
    for (int i = 0; i < 22221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22221(int& val) {
    const int x = 22221;
    std::cout << 22221;
    for (int i = 0; i < 22221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22222(int& val) {
    func_in_22222(val);
    const int x = 22222;
    std::cout << 22222;
    for (int i = 0; i < 22222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22222(int& val) {
    const int x = 22222;
    std::cout << 22222;
    for (int i = 0; i < 22222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22223(int& val) {
    func_in_22223(val);
    const int x = 22223;
    std::cout << 22223;
    for (int i = 0; i < 22223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22223(int& val) {
    const int x = 22223;
    std::cout << 22223;
    for (int i = 0; i < 22223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22224(int& val) {
    func_in_22224(val);
    const int x = 22224;
    std::cout << 22224;
    for (int i = 0; i < 22224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22224(int& val) {
    const int x = 22224;
    std::cout << 22224;
    for (int i = 0; i < 22224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22225(int& val) {
    func_in_22225(val);
    const int x = 22225;
    std::cout << 22225;
    for (int i = 0; i < 22225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22225(int& val) {
    const int x = 22225;
    std::cout << 22225;
    for (int i = 0; i < 22225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22226(int& val) {
    func_in_22226(val);
    const int x = 22226;
    std::cout << 22226;
    for (int i = 0; i < 22226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22226(int& val) {
    const int x = 22226;
    std::cout << 22226;
    for (int i = 0; i < 22226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22227(int& val) {
    func_in_22227(val);
    const int x = 22227;
    std::cout << 22227;
    for (int i = 0; i < 22227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22227(int& val) {
    const int x = 22227;
    std::cout << 22227;
    for (int i = 0; i < 22227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22228(int& val) {
    func_in_22228(val);
    const int x = 22228;
    std::cout << 22228;
    for (int i = 0; i < 22228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22228(int& val) {
    const int x = 22228;
    std::cout << 22228;
    for (int i = 0; i < 22228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22229(int& val) {
    func_in_22229(val);
    const int x = 22229;
    std::cout << 22229;
    for (int i = 0; i < 22229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22229(int& val) {
    const int x = 22229;
    std::cout << 22229;
    for (int i = 0; i < 22229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22230(int& val) {
    func_in_22230(val);
    const int x = 22230;
    std::cout << 22230;
    for (int i = 0; i < 22230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22230(int& val) {
    const int x = 22230;
    std::cout << 22230;
    for (int i = 0; i < 22230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22231(int& val) {
    func_in_22231(val);
    const int x = 22231;
    std::cout << 22231;
    for (int i = 0; i < 22231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22231(int& val) {
    const int x = 22231;
    std::cout << 22231;
    for (int i = 0; i < 22231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22232(int& val) {
    func_in_22232(val);
    const int x = 22232;
    std::cout << 22232;
    for (int i = 0; i < 22232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22232(int& val) {
    const int x = 22232;
    std::cout << 22232;
    for (int i = 0; i < 22232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22233(int& val) {
    func_in_22233(val);
    const int x = 22233;
    std::cout << 22233;
    for (int i = 0; i < 22233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22233(int& val) {
    const int x = 22233;
    std::cout << 22233;
    for (int i = 0; i < 22233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22234(int& val) {
    func_in_22234(val);
    const int x = 22234;
    std::cout << 22234;
    for (int i = 0; i < 22234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22234(int& val) {
    const int x = 22234;
    std::cout << 22234;
    for (int i = 0; i < 22234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22235(int& val) {
    func_in_22235(val);
    const int x = 22235;
    std::cout << 22235;
    for (int i = 0; i < 22235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22235(int& val) {
    const int x = 22235;
    std::cout << 22235;
    for (int i = 0; i < 22235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22236(int& val) {
    func_in_22236(val);
    const int x = 22236;
    std::cout << 22236;
    for (int i = 0; i < 22236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22236(int& val) {
    const int x = 22236;
    std::cout << 22236;
    for (int i = 0; i < 22236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22237(int& val) {
    func_in_22237(val);
    const int x = 22237;
    std::cout << 22237;
    for (int i = 0; i < 22237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22237(int& val) {
    const int x = 22237;
    std::cout << 22237;
    for (int i = 0; i < 22237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22238(int& val) {
    func_in_22238(val);
    const int x = 22238;
    std::cout << 22238;
    for (int i = 0; i < 22238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22238(int& val) {
    const int x = 22238;
    std::cout << 22238;
    for (int i = 0; i < 22238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22239(int& val) {
    func_in_22239(val);
    const int x = 22239;
    std::cout << 22239;
    for (int i = 0; i < 22239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22239(int& val) {
    const int x = 22239;
    std::cout << 22239;
    for (int i = 0; i < 22239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22240(int& val) {
    func_in_22240(val);
    const int x = 22240;
    std::cout << 22240;
    for (int i = 0; i < 22240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22240(int& val) {
    const int x = 22240;
    std::cout << 22240;
    for (int i = 0; i < 22240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22241(int& val) {
    func_in_22241(val);
    const int x = 22241;
    std::cout << 22241;
    for (int i = 0; i < 22241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22241(int& val) {
    const int x = 22241;
    std::cout << 22241;
    for (int i = 0; i < 22241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22242(int& val) {
    func_in_22242(val);
    const int x = 22242;
    std::cout << 22242;
    for (int i = 0; i < 22242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22242(int& val) {
    const int x = 22242;
    std::cout << 22242;
    for (int i = 0; i < 22242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22243(int& val) {
    func_in_22243(val);
    const int x = 22243;
    std::cout << 22243;
    for (int i = 0; i < 22243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22243(int& val) {
    const int x = 22243;
    std::cout << 22243;
    for (int i = 0; i < 22243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22244(int& val) {
    func_in_22244(val);
    const int x = 22244;
    std::cout << 22244;
    for (int i = 0; i < 22244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22244(int& val) {
    const int x = 22244;
    std::cout << 22244;
    for (int i = 0; i < 22244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22245(int& val) {
    func_in_22245(val);
    const int x = 22245;
    std::cout << 22245;
    for (int i = 0; i < 22245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22245(int& val) {
    const int x = 22245;
    std::cout << 22245;
    for (int i = 0; i < 22245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22246(int& val) {
    func_in_22246(val);
    const int x = 22246;
    std::cout << 22246;
    for (int i = 0; i < 22246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22246(int& val) {
    const int x = 22246;
    std::cout << 22246;
    for (int i = 0; i < 22246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22247(int& val) {
    func_in_22247(val);
    const int x = 22247;
    std::cout << 22247;
    for (int i = 0; i < 22247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22247(int& val) {
    const int x = 22247;
    std::cout << 22247;
    for (int i = 0; i < 22247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22248(int& val) {
    func_in_22248(val);
    const int x = 22248;
    std::cout << 22248;
    for (int i = 0; i < 22248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22248(int& val) {
    const int x = 22248;
    std::cout << 22248;
    for (int i = 0; i < 22248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22249(int& val) {
    func_in_22249(val);
    const int x = 22249;
    std::cout << 22249;
    for (int i = 0; i < 22249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22249(int& val) {
    const int x = 22249;
    std::cout << 22249;
    for (int i = 0; i < 22249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22250(int& val) {
    func_in_22250(val);
    const int x = 22250;
    std::cout << 22250;
    for (int i = 0; i < 22250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22250(int& val) {
    const int x = 22250;
    std::cout << 22250;
    for (int i = 0; i < 22250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22251(int& val) {
    func_in_22251(val);
    const int x = 22251;
    std::cout << 22251;
    for (int i = 0; i < 22251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22251(int& val) {
    const int x = 22251;
    std::cout << 22251;
    for (int i = 0; i < 22251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22252(int& val) {
    func_in_22252(val);
    const int x = 22252;
    std::cout << 22252;
    for (int i = 0; i < 22252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22252(int& val) {
    const int x = 22252;
    std::cout << 22252;
    for (int i = 0; i < 22252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22253(int& val) {
    func_in_22253(val);
    const int x = 22253;
    std::cout << 22253;
    for (int i = 0; i < 22253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22253(int& val) {
    const int x = 22253;
    std::cout << 22253;
    for (int i = 0; i < 22253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22254(int& val) {
    func_in_22254(val);
    const int x = 22254;
    std::cout << 22254;
    for (int i = 0; i < 22254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22254(int& val) {
    const int x = 22254;
    std::cout << 22254;
    for (int i = 0; i < 22254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22255(int& val) {
    func_in_22255(val);
    const int x = 22255;
    std::cout << 22255;
    for (int i = 0; i < 22255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22255(int& val) {
    const int x = 22255;
    std::cout << 22255;
    for (int i = 0; i < 22255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22256(int& val) {
    func_in_22256(val);
    const int x = 22256;
    std::cout << 22256;
    for (int i = 0; i < 22256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22256(int& val) {
    const int x = 22256;
    std::cout << 22256;
    for (int i = 0; i < 22256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22257(int& val) {
    func_in_22257(val);
    const int x = 22257;
    std::cout << 22257;
    for (int i = 0; i < 22257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22257(int& val) {
    const int x = 22257;
    std::cout << 22257;
    for (int i = 0; i < 22257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22258(int& val) {
    func_in_22258(val);
    const int x = 22258;
    std::cout << 22258;
    for (int i = 0; i < 22258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22258(int& val) {
    const int x = 22258;
    std::cout << 22258;
    for (int i = 0; i < 22258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22259(int& val) {
    func_in_22259(val);
    const int x = 22259;
    std::cout << 22259;
    for (int i = 0; i < 22259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22259(int& val) {
    const int x = 22259;
    std::cout << 22259;
    for (int i = 0; i < 22259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22260(int& val) {
    func_in_22260(val);
    const int x = 22260;
    std::cout << 22260;
    for (int i = 0; i < 22260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22260(int& val) {
    const int x = 22260;
    std::cout << 22260;
    for (int i = 0; i < 22260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22261(int& val) {
    func_in_22261(val);
    const int x = 22261;
    std::cout << 22261;
    for (int i = 0; i < 22261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22261(int& val) {
    const int x = 22261;
    std::cout << 22261;
    for (int i = 0; i < 22261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22262(int& val) {
    func_in_22262(val);
    const int x = 22262;
    std::cout << 22262;
    for (int i = 0; i < 22262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22262(int& val) {
    const int x = 22262;
    std::cout << 22262;
    for (int i = 0; i < 22262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22263(int& val) {
    func_in_22263(val);
    const int x = 22263;
    std::cout << 22263;
    for (int i = 0; i < 22263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22263(int& val) {
    const int x = 22263;
    std::cout << 22263;
    for (int i = 0; i < 22263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22264(int& val) {
    func_in_22264(val);
    const int x = 22264;
    std::cout << 22264;
    for (int i = 0; i < 22264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22264(int& val) {
    const int x = 22264;
    std::cout << 22264;
    for (int i = 0; i < 22264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22265(int& val) {
    func_in_22265(val);
    const int x = 22265;
    std::cout << 22265;
    for (int i = 0; i < 22265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22265(int& val) {
    const int x = 22265;
    std::cout << 22265;
    for (int i = 0; i < 22265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22266(int& val) {
    func_in_22266(val);
    const int x = 22266;
    std::cout << 22266;
    for (int i = 0; i < 22266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22266(int& val) {
    const int x = 22266;
    std::cout << 22266;
    for (int i = 0; i < 22266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22267(int& val) {
    func_in_22267(val);
    const int x = 22267;
    std::cout << 22267;
    for (int i = 0; i < 22267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22267(int& val) {
    const int x = 22267;
    std::cout << 22267;
    for (int i = 0; i < 22267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22268(int& val) {
    func_in_22268(val);
    const int x = 22268;
    std::cout << 22268;
    for (int i = 0; i < 22268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22268(int& val) {
    const int x = 22268;
    std::cout << 22268;
    for (int i = 0; i < 22268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22269(int& val) {
    func_in_22269(val);
    const int x = 22269;
    std::cout << 22269;
    for (int i = 0; i < 22269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22269(int& val) {
    const int x = 22269;
    std::cout << 22269;
    for (int i = 0; i < 22269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22270(int& val) {
    func_in_22270(val);
    const int x = 22270;
    std::cout << 22270;
    for (int i = 0; i < 22270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22270(int& val) {
    const int x = 22270;
    std::cout << 22270;
    for (int i = 0; i < 22270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22271(int& val) {
    func_in_22271(val);
    const int x = 22271;
    std::cout << 22271;
    for (int i = 0; i < 22271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22271(int& val) {
    const int x = 22271;
    std::cout << 22271;
    for (int i = 0; i < 22271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22272(int& val) {
    func_in_22272(val);
    const int x = 22272;
    std::cout << 22272;
    for (int i = 0; i < 22272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22272(int& val) {
    const int x = 22272;
    std::cout << 22272;
    for (int i = 0; i < 22272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22273(int& val) {
    func_in_22273(val);
    const int x = 22273;
    std::cout << 22273;
    for (int i = 0; i < 22273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22273(int& val) {
    const int x = 22273;
    std::cout << 22273;
    for (int i = 0; i < 22273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22274(int& val) {
    func_in_22274(val);
    const int x = 22274;
    std::cout << 22274;
    for (int i = 0; i < 22274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22274(int& val) {
    const int x = 22274;
    std::cout << 22274;
    for (int i = 0; i < 22274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22275(int& val) {
    func_in_22275(val);
    const int x = 22275;
    std::cout << 22275;
    for (int i = 0; i < 22275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22275(int& val) {
    const int x = 22275;
    std::cout << 22275;
    for (int i = 0; i < 22275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22276(int& val) {
    func_in_22276(val);
    const int x = 22276;
    std::cout << 22276;
    for (int i = 0; i < 22276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22276(int& val) {
    const int x = 22276;
    std::cout << 22276;
    for (int i = 0; i < 22276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22277(int& val) {
    func_in_22277(val);
    const int x = 22277;
    std::cout << 22277;
    for (int i = 0; i < 22277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22277(int& val) {
    const int x = 22277;
    std::cout << 22277;
    for (int i = 0; i < 22277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22278(int& val) {
    func_in_22278(val);
    const int x = 22278;
    std::cout << 22278;
    for (int i = 0; i < 22278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22278(int& val) {
    const int x = 22278;
    std::cout << 22278;
    for (int i = 0; i < 22278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22279(int& val) {
    func_in_22279(val);
    const int x = 22279;
    std::cout << 22279;
    for (int i = 0; i < 22279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22279(int& val) {
    const int x = 22279;
    std::cout << 22279;
    for (int i = 0; i < 22279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22280(int& val) {
    func_in_22280(val);
    const int x = 22280;
    std::cout << 22280;
    for (int i = 0; i < 22280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22280(int& val) {
    const int x = 22280;
    std::cout << 22280;
    for (int i = 0; i < 22280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22281(int& val) {
    func_in_22281(val);
    const int x = 22281;
    std::cout << 22281;
    for (int i = 0; i < 22281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22281(int& val) {
    const int x = 22281;
    std::cout << 22281;
    for (int i = 0; i < 22281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22282(int& val) {
    func_in_22282(val);
    const int x = 22282;
    std::cout << 22282;
    for (int i = 0; i < 22282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22282(int& val) {
    const int x = 22282;
    std::cout << 22282;
    for (int i = 0; i < 22282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22283(int& val) {
    func_in_22283(val);
    const int x = 22283;
    std::cout << 22283;
    for (int i = 0; i < 22283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22283(int& val) {
    const int x = 22283;
    std::cout << 22283;
    for (int i = 0; i < 22283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22284(int& val) {
    func_in_22284(val);
    const int x = 22284;
    std::cout << 22284;
    for (int i = 0; i < 22284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22284(int& val) {
    const int x = 22284;
    std::cout << 22284;
    for (int i = 0; i < 22284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22285(int& val) {
    func_in_22285(val);
    const int x = 22285;
    std::cout << 22285;
    for (int i = 0; i < 22285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22285(int& val) {
    const int x = 22285;
    std::cout << 22285;
    for (int i = 0; i < 22285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22286(int& val) {
    func_in_22286(val);
    const int x = 22286;
    std::cout << 22286;
    for (int i = 0; i < 22286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22286(int& val) {
    const int x = 22286;
    std::cout << 22286;
    for (int i = 0; i < 22286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22287(int& val) {
    func_in_22287(val);
    const int x = 22287;
    std::cout << 22287;
    for (int i = 0; i < 22287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22287(int& val) {
    const int x = 22287;
    std::cout << 22287;
    for (int i = 0; i < 22287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22288(int& val) {
    func_in_22288(val);
    const int x = 22288;
    std::cout << 22288;
    for (int i = 0; i < 22288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22288(int& val) {
    const int x = 22288;
    std::cout << 22288;
    for (int i = 0; i < 22288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22289(int& val) {
    func_in_22289(val);
    const int x = 22289;
    std::cout << 22289;
    for (int i = 0; i < 22289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22289(int& val) {
    const int x = 22289;
    std::cout << 22289;
    for (int i = 0; i < 22289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22290(int& val) {
    func_in_22290(val);
    const int x = 22290;
    std::cout << 22290;
    for (int i = 0; i < 22290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22290(int& val) {
    const int x = 22290;
    std::cout << 22290;
    for (int i = 0; i < 22290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22291(int& val) {
    func_in_22291(val);
    const int x = 22291;
    std::cout << 22291;
    for (int i = 0; i < 22291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22291(int& val) {
    const int x = 22291;
    std::cout << 22291;
    for (int i = 0; i < 22291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22292(int& val) {
    func_in_22292(val);
    const int x = 22292;
    std::cout << 22292;
    for (int i = 0; i < 22292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22292(int& val) {
    const int x = 22292;
    std::cout << 22292;
    for (int i = 0; i < 22292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22293(int& val) {
    func_in_22293(val);
    const int x = 22293;
    std::cout << 22293;
    for (int i = 0; i < 22293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22293(int& val) {
    const int x = 22293;
    std::cout << 22293;
    for (int i = 0; i < 22293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22294(int& val) {
    func_in_22294(val);
    const int x = 22294;
    std::cout << 22294;
    for (int i = 0; i < 22294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22294(int& val) {
    const int x = 22294;
    std::cout << 22294;
    for (int i = 0; i < 22294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22295(int& val) {
    func_in_22295(val);
    const int x = 22295;
    std::cout << 22295;
    for (int i = 0; i < 22295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22295(int& val) {
    const int x = 22295;
    std::cout << 22295;
    for (int i = 0; i < 22295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22296(int& val) {
    func_in_22296(val);
    const int x = 22296;
    std::cout << 22296;
    for (int i = 0; i < 22296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22296(int& val) {
    const int x = 22296;
    std::cout << 22296;
    for (int i = 0; i < 22296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22297(int& val) {
    func_in_22297(val);
    const int x = 22297;
    std::cout << 22297;
    for (int i = 0; i < 22297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22297(int& val) {
    const int x = 22297;
    std::cout << 22297;
    for (int i = 0; i < 22297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22298(int& val) {
    func_in_22298(val);
    const int x = 22298;
    std::cout << 22298;
    for (int i = 0; i < 22298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22298(int& val) {
    const int x = 22298;
    std::cout << 22298;
    for (int i = 0; i < 22298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22299(int& val) {
    func_in_22299(val);
    const int x = 22299;
    std::cout << 22299;
    for (int i = 0; i < 22299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22299(int& val) {
    const int x = 22299;
    std::cout << 22299;
    for (int i = 0; i < 22299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22300(int& val) {
    func_in_22300(val);
    const int x = 22300;
    std::cout << 22300;
    for (int i = 0; i < 22300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22300(int& val) {
    const int x = 22300;
    std::cout << 22300;
    for (int i = 0; i < 22300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22301(int& val) {
    func_in_22301(val);
    const int x = 22301;
    std::cout << 22301;
    for (int i = 0; i < 22301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22301(int& val) {
    const int x = 22301;
    std::cout << 22301;
    for (int i = 0; i < 22301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22302(int& val) {
    func_in_22302(val);
    const int x = 22302;
    std::cout << 22302;
    for (int i = 0; i < 22302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22302(int& val) {
    const int x = 22302;
    std::cout << 22302;
    for (int i = 0; i < 22302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22303(int& val) {
    func_in_22303(val);
    const int x = 22303;
    std::cout << 22303;
    for (int i = 0; i < 22303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22303(int& val) {
    const int x = 22303;
    std::cout << 22303;
    for (int i = 0; i < 22303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22304(int& val) {
    func_in_22304(val);
    const int x = 22304;
    std::cout << 22304;
    for (int i = 0; i < 22304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22304(int& val) {
    const int x = 22304;
    std::cout << 22304;
    for (int i = 0; i < 22304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22305(int& val) {
    func_in_22305(val);
    const int x = 22305;
    std::cout << 22305;
    for (int i = 0; i < 22305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22305(int& val) {
    const int x = 22305;
    std::cout << 22305;
    for (int i = 0; i < 22305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22306(int& val) {
    func_in_22306(val);
    const int x = 22306;
    std::cout << 22306;
    for (int i = 0; i < 22306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22306(int& val) {
    const int x = 22306;
    std::cout << 22306;
    for (int i = 0; i < 22306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22307(int& val) {
    func_in_22307(val);
    const int x = 22307;
    std::cout << 22307;
    for (int i = 0; i < 22307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22307(int& val) {
    const int x = 22307;
    std::cout << 22307;
    for (int i = 0; i < 22307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22308(int& val) {
    func_in_22308(val);
    const int x = 22308;
    std::cout << 22308;
    for (int i = 0; i < 22308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22308(int& val) {
    const int x = 22308;
    std::cout << 22308;
    for (int i = 0; i < 22308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22309(int& val) {
    func_in_22309(val);
    const int x = 22309;
    std::cout << 22309;
    for (int i = 0; i < 22309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22309(int& val) {
    const int x = 22309;
    std::cout << 22309;
    for (int i = 0; i < 22309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22310(int& val) {
    func_in_22310(val);
    const int x = 22310;
    std::cout << 22310;
    for (int i = 0; i < 22310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22310(int& val) {
    const int x = 22310;
    std::cout << 22310;
    for (int i = 0; i < 22310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22311(int& val) {
    func_in_22311(val);
    const int x = 22311;
    std::cout << 22311;
    for (int i = 0; i < 22311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22311(int& val) {
    const int x = 22311;
    std::cout << 22311;
    for (int i = 0; i < 22311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22312(int& val) {
    func_in_22312(val);
    const int x = 22312;
    std::cout << 22312;
    for (int i = 0; i < 22312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22312(int& val) {
    const int x = 22312;
    std::cout << 22312;
    for (int i = 0; i < 22312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22313(int& val) {
    func_in_22313(val);
    const int x = 22313;
    std::cout << 22313;
    for (int i = 0; i < 22313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22313(int& val) {
    const int x = 22313;
    std::cout << 22313;
    for (int i = 0; i < 22313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22314(int& val) {
    func_in_22314(val);
    const int x = 22314;
    std::cout << 22314;
    for (int i = 0; i < 22314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22314(int& val) {
    const int x = 22314;
    std::cout << 22314;
    for (int i = 0; i < 22314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22315(int& val) {
    func_in_22315(val);
    const int x = 22315;
    std::cout << 22315;
    for (int i = 0; i < 22315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22315(int& val) {
    const int x = 22315;
    std::cout << 22315;
    for (int i = 0; i < 22315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22316(int& val) {
    func_in_22316(val);
    const int x = 22316;
    std::cout << 22316;
    for (int i = 0; i < 22316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22316(int& val) {
    const int x = 22316;
    std::cout << 22316;
    for (int i = 0; i < 22316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22317(int& val) {
    func_in_22317(val);
    const int x = 22317;
    std::cout << 22317;
    for (int i = 0; i < 22317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22317(int& val) {
    const int x = 22317;
    std::cout << 22317;
    for (int i = 0; i < 22317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22318(int& val) {
    func_in_22318(val);
    const int x = 22318;
    std::cout << 22318;
    for (int i = 0; i < 22318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22318(int& val) {
    const int x = 22318;
    std::cout << 22318;
    for (int i = 0; i < 22318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22319(int& val) {
    func_in_22319(val);
    const int x = 22319;
    std::cout << 22319;
    for (int i = 0; i < 22319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22319(int& val) {
    const int x = 22319;
    std::cout << 22319;
    for (int i = 0; i < 22319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22320(int& val) {
    func_in_22320(val);
    const int x = 22320;
    std::cout << 22320;
    for (int i = 0; i < 22320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22320(int& val) {
    const int x = 22320;
    std::cout << 22320;
    for (int i = 0; i < 22320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22321(int& val) {
    func_in_22321(val);
    const int x = 22321;
    std::cout << 22321;
    for (int i = 0; i < 22321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22321(int& val) {
    const int x = 22321;
    std::cout << 22321;
    for (int i = 0; i < 22321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22322(int& val) {
    func_in_22322(val);
    const int x = 22322;
    std::cout << 22322;
    for (int i = 0; i < 22322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22322(int& val) {
    const int x = 22322;
    std::cout << 22322;
    for (int i = 0; i < 22322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22323(int& val) {
    func_in_22323(val);
    const int x = 22323;
    std::cout << 22323;
    for (int i = 0; i < 22323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22323(int& val) {
    const int x = 22323;
    std::cout << 22323;
    for (int i = 0; i < 22323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22324(int& val) {
    func_in_22324(val);
    const int x = 22324;
    std::cout << 22324;
    for (int i = 0; i < 22324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22324(int& val) {
    const int x = 22324;
    std::cout << 22324;
    for (int i = 0; i < 22324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22325(int& val) {
    func_in_22325(val);
    const int x = 22325;
    std::cout << 22325;
    for (int i = 0; i < 22325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22325(int& val) {
    const int x = 22325;
    std::cout << 22325;
    for (int i = 0; i < 22325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22326(int& val) {
    func_in_22326(val);
    const int x = 22326;
    std::cout << 22326;
    for (int i = 0; i < 22326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22326(int& val) {
    const int x = 22326;
    std::cout << 22326;
    for (int i = 0; i < 22326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22327(int& val) {
    func_in_22327(val);
    const int x = 22327;
    std::cout << 22327;
    for (int i = 0; i < 22327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22327(int& val) {
    const int x = 22327;
    std::cout << 22327;
    for (int i = 0; i < 22327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22328(int& val) {
    func_in_22328(val);
    const int x = 22328;
    std::cout << 22328;
    for (int i = 0; i < 22328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22328(int& val) {
    const int x = 22328;
    std::cout << 22328;
    for (int i = 0; i < 22328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22329(int& val) {
    func_in_22329(val);
    const int x = 22329;
    std::cout << 22329;
    for (int i = 0; i < 22329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22329(int& val) {
    const int x = 22329;
    std::cout << 22329;
    for (int i = 0; i < 22329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22330(int& val) {
    func_in_22330(val);
    const int x = 22330;
    std::cout << 22330;
    for (int i = 0; i < 22330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22330(int& val) {
    const int x = 22330;
    std::cout << 22330;
    for (int i = 0; i < 22330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22331(int& val) {
    func_in_22331(val);
    const int x = 22331;
    std::cout << 22331;
    for (int i = 0; i < 22331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22331(int& val) {
    const int x = 22331;
    std::cout << 22331;
    for (int i = 0; i < 22331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22332(int& val) {
    func_in_22332(val);
    const int x = 22332;
    std::cout << 22332;
    for (int i = 0; i < 22332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22332(int& val) {
    const int x = 22332;
    std::cout << 22332;
    for (int i = 0; i < 22332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22333(int& val) {
    func_in_22333(val);
    const int x = 22333;
    std::cout << 22333;
    for (int i = 0; i < 22333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22333(int& val) {
    const int x = 22333;
    std::cout << 22333;
    for (int i = 0; i < 22333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22334(int& val) {
    func_in_22334(val);
    const int x = 22334;
    std::cout << 22334;
    for (int i = 0; i < 22334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22334(int& val) {
    const int x = 22334;
    std::cout << 22334;
    for (int i = 0; i < 22334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22335(int& val) {
    func_in_22335(val);
    const int x = 22335;
    std::cout << 22335;
    for (int i = 0; i < 22335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22335(int& val) {
    const int x = 22335;
    std::cout << 22335;
    for (int i = 0; i < 22335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22336(int& val) {
    func_in_22336(val);
    const int x = 22336;
    std::cout << 22336;
    for (int i = 0; i < 22336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22336(int& val) {
    const int x = 22336;
    std::cout << 22336;
    for (int i = 0; i < 22336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22337(int& val) {
    func_in_22337(val);
    const int x = 22337;
    std::cout << 22337;
    for (int i = 0; i < 22337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22337(int& val) {
    const int x = 22337;
    std::cout << 22337;
    for (int i = 0; i < 22337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22338(int& val) {
    func_in_22338(val);
    const int x = 22338;
    std::cout << 22338;
    for (int i = 0; i < 22338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22338(int& val) {
    const int x = 22338;
    std::cout << 22338;
    for (int i = 0; i < 22338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22339(int& val) {
    func_in_22339(val);
    const int x = 22339;
    std::cout << 22339;
    for (int i = 0; i < 22339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22339(int& val) {
    const int x = 22339;
    std::cout << 22339;
    for (int i = 0; i < 22339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22340(int& val) {
    func_in_22340(val);
    const int x = 22340;
    std::cout << 22340;
    for (int i = 0; i < 22340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22340(int& val) {
    const int x = 22340;
    std::cout << 22340;
    for (int i = 0; i < 22340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22341(int& val) {
    func_in_22341(val);
    const int x = 22341;
    std::cout << 22341;
    for (int i = 0; i < 22341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22341(int& val) {
    const int x = 22341;
    std::cout << 22341;
    for (int i = 0; i < 22341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22342(int& val) {
    func_in_22342(val);
    const int x = 22342;
    std::cout << 22342;
    for (int i = 0; i < 22342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22342(int& val) {
    const int x = 22342;
    std::cout << 22342;
    for (int i = 0; i < 22342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22343(int& val) {
    func_in_22343(val);
    const int x = 22343;
    std::cout << 22343;
    for (int i = 0; i < 22343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22343(int& val) {
    const int x = 22343;
    std::cout << 22343;
    for (int i = 0; i < 22343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22344(int& val) {
    func_in_22344(val);
    const int x = 22344;
    std::cout << 22344;
    for (int i = 0; i < 22344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22344(int& val) {
    const int x = 22344;
    std::cout << 22344;
    for (int i = 0; i < 22344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22345(int& val) {
    func_in_22345(val);
    const int x = 22345;
    std::cout << 22345;
    for (int i = 0; i < 22345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22345(int& val) {
    const int x = 22345;
    std::cout << 22345;
    for (int i = 0; i < 22345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22346(int& val) {
    func_in_22346(val);
    const int x = 22346;
    std::cout << 22346;
    for (int i = 0; i < 22346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22346(int& val) {
    const int x = 22346;
    std::cout << 22346;
    for (int i = 0; i < 22346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22347(int& val) {
    func_in_22347(val);
    const int x = 22347;
    std::cout << 22347;
    for (int i = 0; i < 22347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22347(int& val) {
    const int x = 22347;
    std::cout << 22347;
    for (int i = 0; i < 22347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22348(int& val) {
    func_in_22348(val);
    const int x = 22348;
    std::cout << 22348;
    for (int i = 0; i < 22348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22348(int& val) {
    const int x = 22348;
    std::cout << 22348;
    for (int i = 0; i < 22348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22349(int& val) {
    func_in_22349(val);
    const int x = 22349;
    std::cout << 22349;
    for (int i = 0; i < 22349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22349(int& val) {
    const int x = 22349;
    std::cout << 22349;
    for (int i = 0; i < 22349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22350(int& val) {
    func_in_22350(val);
    const int x = 22350;
    std::cout << 22350;
    for (int i = 0; i < 22350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22350(int& val) {
    const int x = 22350;
    std::cout << 22350;
    for (int i = 0; i < 22350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22351(int& val) {
    func_in_22351(val);
    const int x = 22351;
    std::cout << 22351;
    for (int i = 0; i < 22351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22351(int& val) {
    const int x = 22351;
    std::cout << 22351;
    for (int i = 0; i < 22351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22352(int& val) {
    func_in_22352(val);
    const int x = 22352;
    std::cout << 22352;
    for (int i = 0; i < 22352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22352(int& val) {
    const int x = 22352;
    std::cout << 22352;
    for (int i = 0; i < 22352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22353(int& val) {
    func_in_22353(val);
    const int x = 22353;
    std::cout << 22353;
    for (int i = 0; i < 22353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22353(int& val) {
    const int x = 22353;
    std::cout << 22353;
    for (int i = 0; i < 22353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22354(int& val) {
    func_in_22354(val);
    const int x = 22354;
    std::cout << 22354;
    for (int i = 0; i < 22354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22354(int& val) {
    const int x = 22354;
    std::cout << 22354;
    for (int i = 0; i < 22354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22355(int& val) {
    func_in_22355(val);
    const int x = 22355;
    std::cout << 22355;
    for (int i = 0; i < 22355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22355(int& val) {
    const int x = 22355;
    std::cout << 22355;
    for (int i = 0; i < 22355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22356(int& val) {
    func_in_22356(val);
    const int x = 22356;
    std::cout << 22356;
    for (int i = 0; i < 22356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22356(int& val) {
    const int x = 22356;
    std::cout << 22356;
    for (int i = 0; i < 22356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22357(int& val) {
    func_in_22357(val);
    const int x = 22357;
    std::cout << 22357;
    for (int i = 0; i < 22357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22357(int& val) {
    const int x = 22357;
    std::cout << 22357;
    for (int i = 0; i < 22357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22358(int& val) {
    func_in_22358(val);
    const int x = 22358;
    std::cout << 22358;
    for (int i = 0; i < 22358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22358(int& val) {
    const int x = 22358;
    std::cout << 22358;
    for (int i = 0; i < 22358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22359(int& val) {
    func_in_22359(val);
    const int x = 22359;
    std::cout << 22359;
    for (int i = 0; i < 22359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22359(int& val) {
    const int x = 22359;
    std::cout << 22359;
    for (int i = 0; i < 22359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22360(int& val) {
    func_in_22360(val);
    const int x = 22360;
    std::cout << 22360;
    for (int i = 0; i < 22360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22360(int& val) {
    const int x = 22360;
    std::cout << 22360;
    for (int i = 0; i < 22360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22361(int& val) {
    func_in_22361(val);
    const int x = 22361;
    std::cout << 22361;
    for (int i = 0; i < 22361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22361(int& val) {
    const int x = 22361;
    std::cout << 22361;
    for (int i = 0; i < 22361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22362(int& val) {
    func_in_22362(val);
    const int x = 22362;
    std::cout << 22362;
    for (int i = 0; i < 22362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22362(int& val) {
    const int x = 22362;
    std::cout << 22362;
    for (int i = 0; i < 22362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22363(int& val) {
    func_in_22363(val);
    const int x = 22363;
    std::cout << 22363;
    for (int i = 0; i < 22363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22363(int& val) {
    const int x = 22363;
    std::cout << 22363;
    for (int i = 0; i < 22363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22364(int& val) {
    func_in_22364(val);
    const int x = 22364;
    std::cout << 22364;
    for (int i = 0; i < 22364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22364(int& val) {
    const int x = 22364;
    std::cout << 22364;
    for (int i = 0; i < 22364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22365(int& val) {
    func_in_22365(val);
    const int x = 22365;
    std::cout << 22365;
    for (int i = 0; i < 22365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22365(int& val) {
    const int x = 22365;
    std::cout << 22365;
    for (int i = 0; i < 22365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22366(int& val) {
    func_in_22366(val);
    const int x = 22366;
    std::cout << 22366;
    for (int i = 0; i < 22366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22366(int& val) {
    const int x = 22366;
    std::cout << 22366;
    for (int i = 0; i < 22366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22367(int& val) {
    func_in_22367(val);
    const int x = 22367;
    std::cout << 22367;
    for (int i = 0; i < 22367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22367(int& val) {
    const int x = 22367;
    std::cout << 22367;
    for (int i = 0; i < 22367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22368(int& val) {
    func_in_22368(val);
    const int x = 22368;
    std::cout << 22368;
    for (int i = 0; i < 22368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22368(int& val) {
    const int x = 22368;
    std::cout << 22368;
    for (int i = 0; i < 22368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22369(int& val) {
    func_in_22369(val);
    const int x = 22369;
    std::cout << 22369;
    for (int i = 0; i < 22369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22369(int& val) {
    const int x = 22369;
    std::cout << 22369;
    for (int i = 0; i < 22369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22370(int& val) {
    func_in_22370(val);
    const int x = 22370;
    std::cout << 22370;
    for (int i = 0; i < 22370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22370(int& val) {
    const int x = 22370;
    std::cout << 22370;
    for (int i = 0; i < 22370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22371(int& val) {
    func_in_22371(val);
    const int x = 22371;
    std::cout << 22371;
    for (int i = 0; i < 22371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22371(int& val) {
    const int x = 22371;
    std::cout << 22371;
    for (int i = 0; i < 22371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22372(int& val) {
    func_in_22372(val);
    const int x = 22372;
    std::cout << 22372;
    for (int i = 0; i < 22372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22372(int& val) {
    const int x = 22372;
    std::cout << 22372;
    for (int i = 0; i < 22372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22373(int& val) {
    func_in_22373(val);
    const int x = 22373;
    std::cout << 22373;
    for (int i = 0; i < 22373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22373(int& val) {
    const int x = 22373;
    std::cout << 22373;
    for (int i = 0; i < 22373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22374(int& val) {
    func_in_22374(val);
    const int x = 22374;
    std::cout << 22374;
    for (int i = 0; i < 22374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22374(int& val) {
    const int x = 22374;
    std::cout << 22374;
    for (int i = 0; i < 22374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22375(int& val) {
    func_in_22375(val);
    const int x = 22375;
    std::cout << 22375;
    for (int i = 0; i < 22375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22375(int& val) {
    const int x = 22375;
    std::cout << 22375;
    for (int i = 0; i < 22375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22376(int& val) {
    func_in_22376(val);
    const int x = 22376;
    std::cout << 22376;
    for (int i = 0; i < 22376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22376(int& val) {
    const int x = 22376;
    std::cout << 22376;
    for (int i = 0; i < 22376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22377(int& val) {
    func_in_22377(val);
    const int x = 22377;
    std::cout << 22377;
    for (int i = 0; i < 22377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22377(int& val) {
    const int x = 22377;
    std::cout << 22377;
    for (int i = 0; i < 22377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22378(int& val) {
    func_in_22378(val);
    const int x = 22378;
    std::cout << 22378;
    for (int i = 0; i < 22378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22378(int& val) {
    const int x = 22378;
    std::cout << 22378;
    for (int i = 0; i < 22378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22379(int& val) {
    func_in_22379(val);
    const int x = 22379;
    std::cout << 22379;
    for (int i = 0; i < 22379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22379(int& val) {
    const int x = 22379;
    std::cout << 22379;
    for (int i = 0; i < 22379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22380(int& val) {
    func_in_22380(val);
    const int x = 22380;
    std::cout << 22380;
    for (int i = 0; i < 22380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22380(int& val) {
    const int x = 22380;
    std::cout << 22380;
    for (int i = 0; i < 22380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22381(int& val) {
    func_in_22381(val);
    const int x = 22381;
    std::cout << 22381;
    for (int i = 0; i < 22381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22381(int& val) {
    const int x = 22381;
    std::cout << 22381;
    for (int i = 0; i < 22381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22382(int& val) {
    func_in_22382(val);
    const int x = 22382;
    std::cout << 22382;
    for (int i = 0; i < 22382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22382(int& val) {
    const int x = 22382;
    std::cout << 22382;
    for (int i = 0; i < 22382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22383(int& val) {
    func_in_22383(val);
    const int x = 22383;
    std::cout << 22383;
    for (int i = 0; i < 22383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22383(int& val) {
    const int x = 22383;
    std::cout << 22383;
    for (int i = 0; i < 22383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22384(int& val) {
    func_in_22384(val);
    const int x = 22384;
    std::cout << 22384;
    for (int i = 0; i < 22384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22384(int& val) {
    const int x = 22384;
    std::cout << 22384;
    for (int i = 0; i < 22384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22385(int& val) {
    func_in_22385(val);
    const int x = 22385;
    std::cout << 22385;
    for (int i = 0; i < 22385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22385(int& val) {
    const int x = 22385;
    std::cout << 22385;
    for (int i = 0; i < 22385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22386(int& val) {
    func_in_22386(val);
    const int x = 22386;
    std::cout << 22386;
    for (int i = 0; i < 22386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22386(int& val) {
    const int x = 22386;
    std::cout << 22386;
    for (int i = 0; i < 22386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22387(int& val) {
    func_in_22387(val);
    const int x = 22387;
    std::cout << 22387;
    for (int i = 0; i < 22387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22387(int& val) {
    const int x = 22387;
    std::cout << 22387;
    for (int i = 0; i < 22387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22388(int& val) {
    func_in_22388(val);
    const int x = 22388;
    std::cout << 22388;
    for (int i = 0; i < 22388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22388(int& val) {
    const int x = 22388;
    std::cout << 22388;
    for (int i = 0; i < 22388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22389(int& val) {
    func_in_22389(val);
    const int x = 22389;
    std::cout << 22389;
    for (int i = 0; i < 22389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22389(int& val) {
    const int x = 22389;
    std::cout << 22389;
    for (int i = 0; i < 22389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22390(int& val) {
    func_in_22390(val);
    const int x = 22390;
    std::cout << 22390;
    for (int i = 0; i < 22390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22390(int& val) {
    const int x = 22390;
    std::cout << 22390;
    for (int i = 0; i < 22390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22391(int& val) {
    func_in_22391(val);
    const int x = 22391;
    std::cout << 22391;
    for (int i = 0; i < 22391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22391(int& val) {
    const int x = 22391;
    std::cout << 22391;
    for (int i = 0; i < 22391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22392(int& val) {
    func_in_22392(val);
    const int x = 22392;
    std::cout << 22392;
    for (int i = 0; i < 22392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22392(int& val) {
    const int x = 22392;
    std::cout << 22392;
    for (int i = 0; i < 22392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22393(int& val) {
    func_in_22393(val);
    const int x = 22393;
    std::cout << 22393;
    for (int i = 0; i < 22393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22393(int& val) {
    const int x = 22393;
    std::cout << 22393;
    for (int i = 0; i < 22393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22394(int& val) {
    func_in_22394(val);
    const int x = 22394;
    std::cout << 22394;
    for (int i = 0; i < 22394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22394(int& val) {
    const int x = 22394;
    std::cout << 22394;
    for (int i = 0; i < 22394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22395(int& val) {
    func_in_22395(val);
    const int x = 22395;
    std::cout << 22395;
    for (int i = 0; i < 22395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22395(int& val) {
    const int x = 22395;
    std::cout << 22395;
    for (int i = 0; i < 22395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22396(int& val) {
    func_in_22396(val);
    const int x = 22396;
    std::cout << 22396;
    for (int i = 0; i < 22396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22396(int& val) {
    const int x = 22396;
    std::cout << 22396;
    for (int i = 0; i < 22396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22397(int& val) {
    func_in_22397(val);
    const int x = 22397;
    std::cout << 22397;
    for (int i = 0; i < 22397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22397(int& val) {
    const int x = 22397;
    std::cout << 22397;
    for (int i = 0; i < 22397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22398(int& val) {
    func_in_22398(val);
    const int x = 22398;
    std::cout << 22398;
    for (int i = 0; i < 22398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22398(int& val) {
    const int x = 22398;
    std::cout << 22398;
    for (int i = 0; i < 22398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22399(int& val) {
    func_in_22399(val);
    const int x = 22399;
    std::cout << 22399;
    for (int i = 0; i < 22399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22399(int& val) {
    const int x = 22399;
    std::cout << 22399;
    for (int i = 0; i < 22399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22400(int& val) {
    func_in_22400(val);
    const int x = 22400;
    std::cout << 22400;
    for (int i = 0; i < 22400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22400(int& val) {
    const int x = 22400;
    std::cout << 22400;
    for (int i = 0; i < 22400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22401(int& val) {
    func_in_22401(val);
    const int x = 22401;
    std::cout << 22401;
    for (int i = 0; i < 22401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22401(int& val) {
    const int x = 22401;
    std::cout << 22401;
    for (int i = 0; i < 22401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22402(int& val) {
    func_in_22402(val);
    const int x = 22402;
    std::cout << 22402;
    for (int i = 0; i < 22402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22402(int& val) {
    const int x = 22402;
    std::cout << 22402;
    for (int i = 0; i < 22402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22403(int& val) {
    func_in_22403(val);
    const int x = 22403;
    std::cout << 22403;
    for (int i = 0; i < 22403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22403(int& val) {
    const int x = 22403;
    std::cout << 22403;
    for (int i = 0; i < 22403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22404(int& val) {
    func_in_22404(val);
    const int x = 22404;
    std::cout << 22404;
    for (int i = 0; i < 22404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22404(int& val) {
    const int x = 22404;
    std::cout << 22404;
    for (int i = 0; i < 22404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22405(int& val) {
    func_in_22405(val);
    const int x = 22405;
    std::cout << 22405;
    for (int i = 0; i < 22405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22405(int& val) {
    const int x = 22405;
    std::cout << 22405;
    for (int i = 0; i < 22405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22406(int& val) {
    func_in_22406(val);
    const int x = 22406;
    std::cout << 22406;
    for (int i = 0; i < 22406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22406(int& val) {
    const int x = 22406;
    std::cout << 22406;
    for (int i = 0; i < 22406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22407(int& val) {
    func_in_22407(val);
    const int x = 22407;
    std::cout << 22407;
    for (int i = 0; i < 22407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22407(int& val) {
    const int x = 22407;
    std::cout << 22407;
    for (int i = 0; i < 22407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22408(int& val) {
    func_in_22408(val);
    const int x = 22408;
    std::cout << 22408;
    for (int i = 0; i < 22408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22408(int& val) {
    const int x = 22408;
    std::cout << 22408;
    for (int i = 0; i < 22408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22409(int& val) {
    func_in_22409(val);
    const int x = 22409;
    std::cout << 22409;
    for (int i = 0; i < 22409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22409(int& val) {
    const int x = 22409;
    std::cout << 22409;
    for (int i = 0; i < 22409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22410(int& val) {
    func_in_22410(val);
    const int x = 22410;
    std::cout << 22410;
    for (int i = 0; i < 22410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22410(int& val) {
    const int x = 22410;
    std::cout << 22410;
    for (int i = 0; i < 22410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22411(int& val) {
    func_in_22411(val);
    const int x = 22411;
    std::cout << 22411;
    for (int i = 0; i < 22411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22411(int& val) {
    const int x = 22411;
    std::cout << 22411;
    for (int i = 0; i < 22411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22412(int& val) {
    func_in_22412(val);
    const int x = 22412;
    std::cout << 22412;
    for (int i = 0; i < 22412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22412(int& val) {
    const int x = 22412;
    std::cout << 22412;
    for (int i = 0; i < 22412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22413(int& val) {
    func_in_22413(val);
    const int x = 22413;
    std::cout << 22413;
    for (int i = 0; i < 22413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22413(int& val) {
    const int x = 22413;
    std::cout << 22413;
    for (int i = 0; i < 22413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22414(int& val) {
    func_in_22414(val);
    const int x = 22414;
    std::cout << 22414;
    for (int i = 0; i < 22414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22414(int& val) {
    const int x = 22414;
    std::cout << 22414;
    for (int i = 0; i < 22414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22415(int& val) {
    func_in_22415(val);
    const int x = 22415;
    std::cout << 22415;
    for (int i = 0; i < 22415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22415(int& val) {
    const int x = 22415;
    std::cout << 22415;
    for (int i = 0; i < 22415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22416(int& val) {
    func_in_22416(val);
    const int x = 22416;
    std::cout << 22416;
    for (int i = 0; i < 22416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22416(int& val) {
    const int x = 22416;
    std::cout << 22416;
    for (int i = 0; i < 22416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22417(int& val) {
    func_in_22417(val);
    const int x = 22417;
    std::cout << 22417;
    for (int i = 0; i < 22417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22417(int& val) {
    const int x = 22417;
    std::cout << 22417;
    for (int i = 0; i < 22417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22418(int& val) {
    func_in_22418(val);
    const int x = 22418;
    std::cout << 22418;
    for (int i = 0; i < 22418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22418(int& val) {
    const int x = 22418;
    std::cout << 22418;
    for (int i = 0; i < 22418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22419(int& val) {
    func_in_22419(val);
    const int x = 22419;
    std::cout << 22419;
    for (int i = 0; i < 22419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22419(int& val) {
    const int x = 22419;
    std::cout << 22419;
    for (int i = 0; i < 22419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22420(int& val) {
    func_in_22420(val);
    const int x = 22420;
    std::cout << 22420;
    for (int i = 0; i < 22420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22420(int& val) {
    const int x = 22420;
    std::cout << 22420;
    for (int i = 0; i < 22420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22421(int& val) {
    func_in_22421(val);
    const int x = 22421;
    std::cout << 22421;
    for (int i = 0; i < 22421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22421(int& val) {
    const int x = 22421;
    std::cout << 22421;
    for (int i = 0; i < 22421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22422(int& val) {
    func_in_22422(val);
    const int x = 22422;
    std::cout << 22422;
    for (int i = 0; i < 22422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22422(int& val) {
    const int x = 22422;
    std::cout << 22422;
    for (int i = 0; i < 22422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22423(int& val) {
    func_in_22423(val);
    const int x = 22423;
    std::cout << 22423;
    for (int i = 0; i < 22423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22423(int& val) {
    const int x = 22423;
    std::cout << 22423;
    for (int i = 0; i < 22423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22424(int& val) {
    func_in_22424(val);
    const int x = 22424;
    std::cout << 22424;
    for (int i = 0; i < 22424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22424(int& val) {
    const int x = 22424;
    std::cout << 22424;
    for (int i = 0; i < 22424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22425(int& val) {
    func_in_22425(val);
    const int x = 22425;
    std::cout << 22425;
    for (int i = 0; i < 22425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22425(int& val) {
    const int x = 22425;
    std::cout << 22425;
    for (int i = 0; i < 22425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22426(int& val) {
    func_in_22426(val);
    const int x = 22426;
    std::cout << 22426;
    for (int i = 0; i < 22426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22426(int& val) {
    const int x = 22426;
    std::cout << 22426;
    for (int i = 0; i < 22426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22427(int& val) {
    func_in_22427(val);
    const int x = 22427;
    std::cout << 22427;
    for (int i = 0; i < 22427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22427(int& val) {
    const int x = 22427;
    std::cout << 22427;
    for (int i = 0; i < 22427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22428(int& val) {
    func_in_22428(val);
    const int x = 22428;
    std::cout << 22428;
    for (int i = 0; i < 22428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22428(int& val) {
    const int x = 22428;
    std::cout << 22428;
    for (int i = 0; i < 22428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22429(int& val) {
    func_in_22429(val);
    const int x = 22429;
    std::cout << 22429;
    for (int i = 0; i < 22429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22429(int& val) {
    const int x = 22429;
    std::cout << 22429;
    for (int i = 0; i < 22429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22430(int& val) {
    func_in_22430(val);
    const int x = 22430;
    std::cout << 22430;
    for (int i = 0; i < 22430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22430(int& val) {
    const int x = 22430;
    std::cout << 22430;
    for (int i = 0; i < 22430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22431(int& val) {
    func_in_22431(val);
    const int x = 22431;
    std::cout << 22431;
    for (int i = 0; i < 22431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22431(int& val) {
    const int x = 22431;
    std::cout << 22431;
    for (int i = 0; i < 22431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22432(int& val) {
    func_in_22432(val);
    const int x = 22432;
    std::cout << 22432;
    for (int i = 0; i < 22432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22432(int& val) {
    const int x = 22432;
    std::cout << 22432;
    for (int i = 0; i < 22432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22433(int& val) {
    func_in_22433(val);
    const int x = 22433;
    std::cout << 22433;
    for (int i = 0; i < 22433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22433(int& val) {
    const int x = 22433;
    std::cout << 22433;
    for (int i = 0; i < 22433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22434(int& val) {
    func_in_22434(val);
    const int x = 22434;
    std::cout << 22434;
    for (int i = 0; i < 22434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22434(int& val) {
    const int x = 22434;
    std::cout << 22434;
    for (int i = 0; i < 22434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22435(int& val) {
    func_in_22435(val);
    const int x = 22435;
    std::cout << 22435;
    for (int i = 0; i < 22435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22435(int& val) {
    const int x = 22435;
    std::cout << 22435;
    for (int i = 0; i < 22435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22436(int& val) {
    func_in_22436(val);
    const int x = 22436;
    std::cout << 22436;
    for (int i = 0; i < 22436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22436(int& val) {
    const int x = 22436;
    std::cout << 22436;
    for (int i = 0; i < 22436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22437(int& val) {
    func_in_22437(val);
    const int x = 22437;
    std::cout << 22437;
    for (int i = 0; i < 22437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22437(int& val) {
    const int x = 22437;
    std::cout << 22437;
    for (int i = 0; i < 22437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22438(int& val) {
    func_in_22438(val);
    const int x = 22438;
    std::cout << 22438;
    for (int i = 0; i < 22438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22438(int& val) {
    const int x = 22438;
    std::cout << 22438;
    for (int i = 0; i < 22438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22439(int& val) {
    func_in_22439(val);
    const int x = 22439;
    std::cout << 22439;
    for (int i = 0; i < 22439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22439(int& val) {
    const int x = 22439;
    std::cout << 22439;
    for (int i = 0; i < 22439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22440(int& val) {
    func_in_22440(val);
    const int x = 22440;
    std::cout << 22440;
    for (int i = 0; i < 22440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22440(int& val) {
    const int x = 22440;
    std::cout << 22440;
    for (int i = 0; i < 22440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22441(int& val) {
    func_in_22441(val);
    const int x = 22441;
    std::cout << 22441;
    for (int i = 0; i < 22441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22441(int& val) {
    const int x = 22441;
    std::cout << 22441;
    for (int i = 0; i < 22441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22442(int& val) {
    func_in_22442(val);
    const int x = 22442;
    std::cout << 22442;
    for (int i = 0; i < 22442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22442(int& val) {
    const int x = 22442;
    std::cout << 22442;
    for (int i = 0; i < 22442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22443(int& val) {
    func_in_22443(val);
    const int x = 22443;
    std::cout << 22443;
    for (int i = 0; i < 22443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22443(int& val) {
    const int x = 22443;
    std::cout << 22443;
    for (int i = 0; i < 22443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22444(int& val) {
    func_in_22444(val);
    const int x = 22444;
    std::cout << 22444;
    for (int i = 0; i < 22444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22444(int& val) {
    const int x = 22444;
    std::cout << 22444;
    for (int i = 0; i < 22444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22445(int& val) {
    func_in_22445(val);
    const int x = 22445;
    std::cout << 22445;
    for (int i = 0; i < 22445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22445(int& val) {
    const int x = 22445;
    std::cout << 22445;
    for (int i = 0; i < 22445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22446(int& val) {
    func_in_22446(val);
    const int x = 22446;
    std::cout << 22446;
    for (int i = 0; i < 22446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22446(int& val) {
    const int x = 22446;
    std::cout << 22446;
    for (int i = 0; i < 22446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22447(int& val) {
    func_in_22447(val);
    const int x = 22447;
    std::cout << 22447;
    for (int i = 0; i < 22447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22447(int& val) {
    const int x = 22447;
    std::cout << 22447;
    for (int i = 0; i < 22447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22448(int& val) {
    func_in_22448(val);
    const int x = 22448;
    std::cout << 22448;
    for (int i = 0; i < 22448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22448(int& val) {
    const int x = 22448;
    std::cout << 22448;
    for (int i = 0; i < 22448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22449(int& val) {
    func_in_22449(val);
    const int x = 22449;
    std::cout << 22449;
    for (int i = 0; i < 22449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22449(int& val) {
    const int x = 22449;
    std::cout << 22449;
    for (int i = 0; i < 22449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22450(int& val) {
    func_in_22450(val);
    const int x = 22450;
    std::cout << 22450;
    for (int i = 0; i < 22450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22450(int& val) {
    const int x = 22450;
    std::cout << 22450;
    for (int i = 0; i < 22450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22451(int& val) {
    func_in_22451(val);
    const int x = 22451;
    std::cout << 22451;
    for (int i = 0; i < 22451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22451(int& val) {
    const int x = 22451;
    std::cout << 22451;
    for (int i = 0; i < 22451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22452(int& val) {
    func_in_22452(val);
    const int x = 22452;
    std::cout << 22452;
    for (int i = 0; i < 22452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22452(int& val) {
    const int x = 22452;
    std::cout << 22452;
    for (int i = 0; i < 22452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22453(int& val) {
    func_in_22453(val);
    const int x = 22453;
    std::cout << 22453;
    for (int i = 0; i < 22453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22453(int& val) {
    const int x = 22453;
    std::cout << 22453;
    for (int i = 0; i < 22453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22454(int& val) {
    func_in_22454(val);
    const int x = 22454;
    std::cout << 22454;
    for (int i = 0; i < 22454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22454(int& val) {
    const int x = 22454;
    std::cout << 22454;
    for (int i = 0; i < 22454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22455(int& val) {
    func_in_22455(val);
    const int x = 22455;
    std::cout << 22455;
    for (int i = 0; i < 22455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22455(int& val) {
    const int x = 22455;
    std::cout << 22455;
    for (int i = 0; i < 22455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22456(int& val) {
    func_in_22456(val);
    const int x = 22456;
    std::cout << 22456;
    for (int i = 0; i < 22456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22456(int& val) {
    const int x = 22456;
    std::cout << 22456;
    for (int i = 0; i < 22456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22457(int& val) {
    func_in_22457(val);
    const int x = 22457;
    std::cout << 22457;
    for (int i = 0; i < 22457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22457(int& val) {
    const int x = 22457;
    std::cout << 22457;
    for (int i = 0; i < 22457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22458(int& val) {
    func_in_22458(val);
    const int x = 22458;
    std::cout << 22458;
    for (int i = 0; i < 22458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22458(int& val) {
    const int x = 22458;
    std::cout << 22458;
    for (int i = 0; i < 22458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22459(int& val) {
    func_in_22459(val);
    const int x = 22459;
    std::cout << 22459;
    for (int i = 0; i < 22459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22459(int& val) {
    const int x = 22459;
    std::cout << 22459;
    for (int i = 0; i < 22459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22460(int& val) {
    func_in_22460(val);
    const int x = 22460;
    std::cout << 22460;
    for (int i = 0; i < 22460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22460(int& val) {
    const int x = 22460;
    std::cout << 22460;
    for (int i = 0; i < 22460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22461(int& val) {
    func_in_22461(val);
    const int x = 22461;
    std::cout << 22461;
    for (int i = 0; i < 22461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22461(int& val) {
    const int x = 22461;
    std::cout << 22461;
    for (int i = 0; i < 22461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22462(int& val) {
    func_in_22462(val);
    const int x = 22462;
    std::cout << 22462;
    for (int i = 0; i < 22462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22462(int& val) {
    const int x = 22462;
    std::cout << 22462;
    for (int i = 0; i < 22462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22463(int& val) {
    func_in_22463(val);
    const int x = 22463;
    std::cout << 22463;
    for (int i = 0; i < 22463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22463(int& val) {
    const int x = 22463;
    std::cout << 22463;
    for (int i = 0; i < 22463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22464(int& val) {
    func_in_22464(val);
    const int x = 22464;
    std::cout << 22464;
    for (int i = 0; i < 22464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22464(int& val) {
    const int x = 22464;
    std::cout << 22464;
    for (int i = 0; i < 22464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22465(int& val) {
    func_in_22465(val);
    const int x = 22465;
    std::cout << 22465;
    for (int i = 0; i < 22465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22465(int& val) {
    const int x = 22465;
    std::cout << 22465;
    for (int i = 0; i < 22465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22466(int& val) {
    func_in_22466(val);
    const int x = 22466;
    std::cout << 22466;
    for (int i = 0; i < 22466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22466(int& val) {
    const int x = 22466;
    std::cout << 22466;
    for (int i = 0; i < 22466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22467(int& val) {
    func_in_22467(val);
    const int x = 22467;
    std::cout << 22467;
    for (int i = 0; i < 22467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22467(int& val) {
    const int x = 22467;
    std::cout << 22467;
    for (int i = 0; i < 22467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22468(int& val) {
    func_in_22468(val);
    const int x = 22468;
    std::cout << 22468;
    for (int i = 0; i < 22468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22468(int& val) {
    const int x = 22468;
    std::cout << 22468;
    for (int i = 0; i < 22468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22469(int& val) {
    func_in_22469(val);
    const int x = 22469;
    std::cout << 22469;
    for (int i = 0; i < 22469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22469(int& val) {
    const int x = 22469;
    std::cout << 22469;
    for (int i = 0; i < 22469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22470(int& val) {
    func_in_22470(val);
    const int x = 22470;
    std::cout << 22470;
    for (int i = 0; i < 22470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22470(int& val) {
    const int x = 22470;
    std::cout << 22470;
    for (int i = 0; i < 22470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22471(int& val) {
    func_in_22471(val);
    const int x = 22471;
    std::cout << 22471;
    for (int i = 0; i < 22471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22471(int& val) {
    const int x = 22471;
    std::cout << 22471;
    for (int i = 0; i < 22471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22472(int& val) {
    func_in_22472(val);
    const int x = 22472;
    std::cout << 22472;
    for (int i = 0; i < 22472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22472(int& val) {
    const int x = 22472;
    std::cout << 22472;
    for (int i = 0; i < 22472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22473(int& val) {
    func_in_22473(val);
    const int x = 22473;
    std::cout << 22473;
    for (int i = 0; i < 22473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22473(int& val) {
    const int x = 22473;
    std::cout << 22473;
    for (int i = 0; i < 22473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22474(int& val) {
    func_in_22474(val);
    const int x = 22474;
    std::cout << 22474;
    for (int i = 0; i < 22474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22474(int& val) {
    const int x = 22474;
    std::cout << 22474;
    for (int i = 0; i < 22474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22475(int& val) {
    func_in_22475(val);
    const int x = 22475;
    std::cout << 22475;
    for (int i = 0; i < 22475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22475(int& val) {
    const int x = 22475;
    std::cout << 22475;
    for (int i = 0; i < 22475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22476(int& val) {
    func_in_22476(val);
    const int x = 22476;
    std::cout << 22476;
    for (int i = 0; i < 22476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22476(int& val) {
    const int x = 22476;
    std::cout << 22476;
    for (int i = 0; i < 22476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22477(int& val) {
    func_in_22477(val);
    const int x = 22477;
    std::cout << 22477;
    for (int i = 0; i < 22477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22477(int& val) {
    const int x = 22477;
    std::cout << 22477;
    for (int i = 0; i < 22477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22478(int& val) {
    func_in_22478(val);
    const int x = 22478;
    std::cout << 22478;
    for (int i = 0; i < 22478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22478(int& val) {
    const int x = 22478;
    std::cout << 22478;
    for (int i = 0; i < 22478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22479(int& val) {
    func_in_22479(val);
    const int x = 22479;
    std::cout << 22479;
    for (int i = 0; i < 22479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22479(int& val) {
    const int x = 22479;
    std::cout << 22479;
    for (int i = 0; i < 22479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22480(int& val) {
    func_in_22480(val);
    const int x = 22480;
    std::cout << 22480;
    for (int i = 0; i < 22480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22480(int& val) {
    const int x = 22480;
    std::cout << 22480;
    for (int i = 0; i < 22480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22481(int& val) {
    func_in_22481(val);
    const int x = 22481;
    std::cout << 22481;
    for (int i = 0; i < 22481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22481(int& val) {
    const int x = 22481;
    std::cout << 22481;
    for (int i = 0; i < 22481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22482(int& val) {
    func_in_22482(val);
    const int x = 22482;
    std::cout << 22482;
    for (int i = 0; i < 22482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22482(int& val) {
    const int x = 22482;
    std::cout << 22482;
    for (int i = 0; i < 22482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22483(int& val) {
    func_in_22483(val);
    const int x = 22483;
    std::cout << 22483;
    for (int i = 0; i < 22483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22483(int& val) {
    const int x = 22483;
    std::cout << 22483;
    for (int i = 0; i < 22483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22484(int& val) {
    func_in_22484(val);
    const int x = 22484;
    std::cout << 22484;
    for (int i = 0; i < 22484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22484(int& val) {
    const int x = 22484;
    std::cout << 22484;
    for (int i = 0; i < 22484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22485(int& val) {
    func_in_22485(val);
    const int x = 22485;
    std::cout << 22485;
    for (int i = 0; i < 22485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22485(int& val) {
    const int x = 22485;
    std::cout << 22485;
    for (int i = 0; i < 22485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22486(int& val) {
    func_in_22486(val);
    const int x = 22486;
    std::cout << 22486;
    for (int i = 0; i < 22486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22486(int& val) {
    const int x = 22486;
    std::cout << 22486;
    for (int i = 0; i < 22486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22487(int& val) {
    func_in_22487(val);
    const int x = 22487;
    std::cout << 22487;
    for (int i = 0; i < 22487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22487(int& val) {
    const int x = 22487;
    std::cout << 22487;
    for (int i = 0; i < 22487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22488(int& val) {
    func_in_22488(val);
    const int x = 22488;
    std::cout << 22488;
    for (int i = 0; i < 22488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22488(int& val) {
    const int x = 22488;
    std::cout << 22488;
    for (int i = 0; i < 22488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22489(int& val) {
    func_in_22489(val);
    const int x = 22489;
    std::cout << 22489;
    for (int i = 0; i < 22489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22489(int& val) {
    const int x = 22489;
    std::cout << 22489;
    for (int i = 0; i < 22489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22490(int& val) {
    func_in_22490(val);
    const int x = 22490;
    std::cout << 22490;
    for (int i = 0; i < 22490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22490(int& val) {
    const int x = 22490;
    std::cout << 22490;
    for (int i = 0; i < 22490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22491(int& val) {
    func_in_22491(val);
    const int x = 22491;
    std::cout << 22491;
    for (int i = 0; i < 22491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22491(int& val) {
    const int x = 22491;
    std::cout << 22491;
    for (int i = 0; i < 22491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22492(int& val) {
    func_in_22492(val);
    const int x = 22492;
    std::cout << 22492;
    for (int i = 0; i < 22492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22492(int& val) {
    const int x = 22492;
    std::cout << 22492;
    for (int i = 0; i < 22492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22493(int& val) {
    func_in_22493(val);
    const int x = 22493;
    std::cout << 22493;
    for (int i = 0; i < 22493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22493(int& val) {
    const int x = 22493;
    std::cout << 22493;
    for (int i = 0; i < 22493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22494(int& val) {
    func_in_22494(val);
    const int x = 22494;
    std::cout << 22494;
    for (int i = 0; i < 22494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22494(int& val) {
    const int x = 22494;
    std::cout << 22494;
    for (int i = 0; i < 22494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22495(int& val) {
    func_in_22495(val);
    const int x = 22495;
    std::cout << 22495;
    for (int i = 0; i < 22495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22495(int& val) {
    const int x = 22495;
    std::cout << 22495;
    for (int i = 0; i < 22495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22496(int& val) {
    func_in_22496(val);
    const int x = 22496;
    std::cout << 22496;
    for (int i = 0; i < 22496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22496(int& val) {
    const int x = 22496;
    std::cout << 22496;
    for (int i = 0; i < 22496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22497(int& val) {
    func_in_22497(val);
    const int x = 22497;
    std::cout << 22497;
    for (int i = 0; i < 22497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22497(int& val) {
    const int x = 22497;
    std::cout << 22497;
    for (int i = 0; i < 22497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22498(int& val) {
    func_in_22498(val);
    const int x = 22498;
    std::cout << 22498;
    for (int i = 0; i < 22498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22498(int& val) {
    const int x = 22498;
    std::cout << 22498;
    for (int i = 0; i < 22498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22499(int& val) {
    func_in_22499(val);
    const int x = 22499;
    std::cout << 22499;
    for (int i = 0; i < 22499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22499(int& val) {
    const int x = 22499;
    std::cout << 22499;
    for (int i = 0; i < 22499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22500(int& val) {
    func_in_22500(val);
    const int x = 22500;
    std::cout << 22500;
    for (int i = 0; i < 22500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22500(int& val) {
    const int x = 22500;
    std::cout << 22500;
    for (int i = 0; i < 22500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22501(int& val) {
    func_in_22501(val);
    const int x = 22501;
    std::cout << 22501;
    for (int i = 0; i < 22501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22501(int& val) {
    const int x = 22501;
    std::cout << 22501;
    for (int i = 0; i < 22501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22502(int& val) {
    func_in_22502(val);
    const int x = 22502;
    std::cout << 22502;
    for (int i = 0; i < 22502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22502(int& val) {
    const int x = 22502;
    std::cout << 22502;
    for (int i = 0; i < 22502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22503(int& val) {
    func_in_22503(val);
    const int x = 22503;
    std::cout << 22503;
    for (int i = 0; i < 22503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22503(int& val) {
    const int x = 22503;
    std::cout << 22503;
    for (int i = 0; i < 22503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22504(int& val) {
    func_in_22504(val);
    const int x = 22504;
    std::cout << 22504;
    for (int i = 0; i < 22504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22504(int& val) {
    const int x = 22504;
    std::cout << 22504;
    for (int i = 0; i < 22504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22505(int& val) {
    func_in_22505(val);
    const int x = 22505;
    std::cout << 22505;
    for (int i = 0; i < 22505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22505(int& val) {
    const int x = 22505;
    std::cout << 22505;
    for (int i = 0; i < 22505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22506(int& val) {
    func_in_22506(val);
    const int x = 22506;
    std::cout << 22506;
    for (int i = 0; i < 22506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22506(int& val) {
    const int x = 22506;
    std::cout << 22506;
    for (int i = 0; i < 22506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22507(int& val) {
    func_in_22507(val);
    const int x = 22507;
    std::cout << 22507;
    for (int i = 0; i < 22507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22507(int& val) {
    const int x = 22507;
    std::cout << 22507;
    for (int i = 0; i < 22507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22508(int& val) {
    func_in_22508(val);
    const int x = 22508;
    std::cout << 22508;
    for (int i = 0; i < 22508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22508(int& val) {
    const int x = 22508;
    std::cout << 22508;
    for (int i = 0; i < 22508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22509(int& val) {
    func_in_22509(val);
    const int x = 22509;
    std::cout << 22509;
    for (int i = 0; i < 22509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22509(int& val) {
    const int x = 22509;
    std::cout << 22509;
    for (int i = 0; i < 22509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22510(int& val) {
    func_in_22510(val);
    const int x = 22510;
    std::cout << 22510;
    for (int i = 0; i < 22510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22510(int& val) {
    const int x = 22510;
    std::cout << 22510;
    for (int i = 0; i < 22510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22511(int& val) {
    func_in_22511(val);
    const int x = 22511;
    std::cout << 22511;
    for (int i = 0; i < 22511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22511(int& val) {
    const int x = 22511;
    std::cout << 22511;
    for (int i = 0; i < 22511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22512(int& val) {
    func_in_22512(val);
    const int x = 22512;
    std::cout << 22512;
    for (int i = 0; i < 22512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22512(int& val) {
    const int x = 22512;
    std::cout << 22512;
    for (int i = 0; i < 22512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22513(int& val) {
    func_in_22513(val);
    const int x = 22513;
    std::cout << 22513;
    for (int i = 0; i < 22513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22513(int& val) {
    const int x = 22513;
    std::cout << 22513;
    for (int i = 0; i < 22513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22514(int& val) {
    func_in_22514(val);
    const int x = 22514;
    std::cout << 22514;
    for (int i = 0; i < 22514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22514(int& val) {
    const int x = 22514;
    std::cout << 22514;
    for (int i = 0; i < 22514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22515(int& val) {
    func_in_22515(val);
    const int x = 22515;
    std::cout << 22515;
    for (int i = 0; i < 22515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22515(int& val) {
    const int x = 22515;
    std::cout << 22515;
    for (int i = 0; i < 22515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22516(int& val) {
    func_in_22516(val);
    const int x = 22516;
    std::cout << 22516;
    for (int i = 0; i < 22516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22516(int& val) {
    const int x = 22516;
    std::cout << 22516;
    for (int i = 0; i < 22516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22517(int& val) {
    func_in_22517(val);
    const int x = 22517;
    std::cout << 22517;
    for (int i = 0; i < 22517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22517(int& val) {
    const int x = 22517;
    std::cout << 22517;
    for (int i = 0; i < 22517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22518(int& val) {
    func_in_22518(val);
    const int x = 22518;
    std::cout << 22518;
    for (int i = 0; i < 22518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22518(int& val) {
    const int x = 22518;
    std::cout << 22518;
    for (int i = 0; i < 22518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22519(int& val) {
    func_in_22519(val);
    const int x = 22519;
    std::cout << 22519;
    for (int i = 0; i < 22519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22519(int& val) {
    const int x = 22519;
    std::cout << 22519;
    for (int i = 0; i < 22519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22520(int& val) {
    func_in_22520(val);
    const int x = 22520;
    std::cout << 22520;
    for (int i = 0; i < 22520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22520(int& val) {
    const int x = 22520;
    std::cout << 22520;
    for (int i = 0; i < 22520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22521(int& val) {
    func_in_22521(val);
    const int x = 22521;
    std::cout << 22521;
    for (int i = 0; i < 22521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22521(int& val) {
    const int x = 22521;
    std::cout << 22521;
    for (int i = 0; i < 22521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22522(int& val) {
    func_in_22522(val);
    const int x = 22522;
    std::cout << 22522;
    for (int i = 0; i < 22522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22522(int& val) {
    const int x = 22522;
    std::cout << 22522;
    for (int i = 0; i < 22522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22523(int& val) {
    func_in_22523(val);
    const int x = 22523;
    std::cout << 22523;
    for (int i = 0; i < 22523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22523(int& val) {
    const int x = 22523;
    std::cout << 22523;
    for (int i = 0; i < 22523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22524(int& val) {
    func_in_22524(val);
    const int x = 22524;
    std::cout << 22524;
    for (int i = 0; i < 22524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22524(int& val) {
    const int x = 22524;
    std::cout << 22524;
    for (int i = 0; i < 22524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22525(int& val) {
    func_in_22525(val);
    const int x = 22525;
    std::cout << 22525;
    for (int i = 0; i < 22525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22525(int& val) {
    const int x = 22525;
    std::cout << 22525;
    for (int i = 0; i < 22525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22526(int& val) {
    func_in_22526(val);
    const int x = 22526;
    std::cout << 22526;
    for (int i = 0; i < 22526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22526(int& val) {
    const int x = 22526;
    std::cout << 22526;
    for (int i = 0; i < 22526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22527(int& val) {
    func_in_22527(val);
    const int x = 22527;
    std::cout << 22527;
    for (int i = 0; i < 22527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22527(int& val) {
    const int x = 22527;
    std::cout << 22527;
    for (int i = 0; i < 22527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22528(int& val) {
    func_in_22528(val);
    const int x = 22528;
    std::cout << 22528;
    for (int i = 0; i < 22528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22528(int& val) {
    const int x = 22528;
    std::cout << 22528;
    for (int i = 0; i < 22528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22529(int& val) {
    func_in_22529(val);
    const int x = 22529;
    std::cout << 22529;
    for (int i = 0; i < 22529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22529(int& val) {
    const int x = 22529;
    std::cout << 22529;
    for (int i = 0; i < 22529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22530(int& val) {
    func_in_22530(val);
    const int x = 22530;
    std::cout << 22530;
    for (int i = 0; i < 22530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22530(int& val) {
    const int x = 22530;
    std::cout << 22530;
    for (int i = 0; i < 22530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22531(int& val) {
    func_in_22531(val);
    const int x = 22531;
    std::cout << 22531;
    for (int i = 0; i < 22531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22531(int& val) {
    const int x = 22531;
    std::cout << 22531;
    for (int i = 0; i < 22531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22532(int& val) {
    func_in_22532(val);
    const int x = 22532;
    std::cout << 22532;
    for (int i = 0; i < 22532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22532(int& val) {
    const int x = 22532;
    std::cout << 22532;
    for (int i = 0; i < 22532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22533(int& val) {
    func_in_22533(val);
    const int x = 22533;
    std::cout << 22533;
    for (int i = 0; i < 22533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22533(int& val) {
    const int x = 22533;
    std::cout << 22533;
    for (int i = 0; i < 22533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22534(int& val) {
    func_in_22534(val);
    const int x = 22534;
    std::cout << 22534;
    for (int i = 0; i < 22534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22534(int& val) {
    const int x = 22534;
    std::cout << 22534;
    for (int i = 0; i < 22534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22535(int& val) {
    func_in_22535(val);
    const int x = 22535;
    std::cout << 22535;
    for (int i = 0; i < 22535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22535(int& val) {
    const int x = 22535;
    std::cout << 22535;
    for (int i = 0; i < 22535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22536(int& val) {
    func_in_22536(val);
    const int x = 22536;
    std::cout << 22536;
    for (int i = 0; i < 22536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22536(int& val) {
    const int x = 22536;
    std::cout << 22536;
    for (int i = 0; i < 22536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22537(int& val) {
    func_in_22537(val);
    const int x = 22537;
    std::cout << 22537;
    for (int i = 0; i < 22537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22537(int& val) {
    const int x = 22537;
    std::cout << 22537;
    for (int i = 0; i < 22537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22538(int& val) {
    func_in_22538(val);
    const int x = 22538;
    std::cout << 22538;
    for (int i = 0; i < 22538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22538(int& val) {
    const int x = 22538;
    std::cout << 22538;
    for (int i = 0; i < 22538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22539(int& val) {
    func_in_22539(val);
    const int x = 22539;
    std::cout << 22539;
    for (int i = 0; i < 22539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22539(int& val) {
    const int x = 22539;
    std::cout << 22539;
    for (int i = 0; i < 22539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22540(int& val) {
    func_in_22540(val);
    const int x = 22540;
    std::cout << 22540;
    for (int i = 0; i < 22540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22540(int& val) {
    const int x = 22540;
    std::cout << 22540;
    for (int i = 0; i < 22540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22541(int& val) {
    func_in_22541(val);
    const int x = 22541;
    std::cout << 22541;
    for (int i = 0; i < 22541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22541(int& val) {
    const int x = 22541;
    std::cout << 22541;
    for (int i = 0; i < 22541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22542(int& val) {
    func_in_22542(val);
    const int x = 22542;
    std::cout << 22542;
    for (int i = 0; i < 22542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22542(int& val) {
    const int x = 22542;
    std::cout << 22542;
    for (int i = 0; i < 22542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22543(int& val) {
    func_in_22543(val);
    const int x = 22543;
    std::cout << 22543;
    for (int i = 0; i < 22543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22543(int& val) {
    const int x = 22543;
    std::cout << 22543;
    for (int i = 0; i < 22543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22544(int& val) {
    func_in_22544(val);
    const int x = 22544;
    std::cout << 22544;
    for (int i = 0; i < 22544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22544(int& val) {
    const int x = 22544;
    std::cout << 22544;
    for (int i = 0; i < 22544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22545(int& val) {
    func_in_22545(val);
    const int x = 22545;
    std::cout << 22545;
    for (int i = 0; i < 22545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22545(int& val) {
    const int x = 22545;
    std::cout << 22545;
    for (int i = 0; i < 22545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22546(int& val) {
    func_in_22546(val);
    const int x = 22546;
    std::cout << 22546;
    for (int i = 0; i < 22546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22546(int& val) {
    const int x = 22546;
    std::cout << 22546;
    for (int i = 0; i < 22546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22547(int& val) {
    func_in_22547(val);
    const int x = 22547;
    std::cout << 22547;
    for (int i = 0; i < 22547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22547(int& val) {
    const int x = 22547;
    std::cout << 22547;
    for (int i = 0; i < 22547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22548(int& val) {
    func_in_22548(val);
    const int x = 22548;
    std::cout << 22548;
    for (int i = 0; i < 22548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22548(int& val) {
    const int x = 22548;
    std::cout << 22548;
    for (int i = 0; i < 22548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22549(int& val) {
    func_in_22549(val);
    const int x = 22549;
    std::cout << 22549;
    for (int i = 0; i < 22549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22549(int& val) {
    const int x = 22549;
    std::cout << 22549;
    for (int i = 0; i < 22549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22550(int& val) {
    func_in_22550(val);
    const int x = 22550;
    std::cout << 22550;
    for (int i = 0; i < 22550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22550(int& val) {
    const int x = 22550;
    std::cout << 22550;
    for (int i = 0; i < 22550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22551(int& val) {
    func_in_22551(val);
    const int x = 22551;
    std::cout << 22551;
    for (int i = 0; i < 22551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22551(int& val) {
    const int x = 22551;
    std::cout << 22551;
    for (int i = 0; i < 22551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22552(int& val) {
    func_in_22552(val);
    const int x = 22552;
    std::cout << 22552;
    for (int i = 0; i < 22552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22552(int& val) {
    const int x = 22552;
    std::cout << 22552;
    for (int i = 0; i < 22552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22553(int& val) {
    func_in_22553(val);
    const int x = 22553;
    std::cout << 22553;
    for (int i = 0; i < 22553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22553(int& val) {
    const int x = 22553;
    std::cout << 22553;
    for (int i = 0; i < 22553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22554(int& val) {
    func_in_22554(val);
    const int x = 22554;
    std::cout << 22554;
    for (int i = 0; i < 22554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22554(int& val) {
    const int x = 22554;
    std::cout << 22554;
    for (int i = 0; i < 22554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22555(int& val) {
    func_in_22555(val);
    const int x = 22555;
    std::cout << 22555;
    for (int i = 0; i < 22555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22555(int& val) {
    const int x = 22555;
    std::cout << 22555;
    for (int i = 0; i < 22555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22556(int& val) {
    func_in_22556(val);
    const int x = 22556;
    std::cout << 22556;
    for (int i = 0; i < 22556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22556(int& val) {
    const int x = 22556;
    std::cout << 22556;
    for (int i = 0; i < 22556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22557(int& val) {
    func_in_22557(val);
    const int x = 22557;
    std::cout << 22557;
    for (int i = 0; i < 22557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22557(int& val) {
    const int x = 22557;
    std::cout << 22557;
    for (int i = 0; i < 22557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22558(int& val) {
    func_in_22558(val);
    const int x = 22558;
    std::cout << 22558;
    for (int i = 0; i < 22558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22558(int& val) {
    const int x = 22558;
    std::cout << 22558;
    for (int i = 0; i < 22558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22559(int& val) {
    func_in_22559(val);
    const int x = 22559;
    std::cout << 22559;
    for (int i = 0; i < 22559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22559(int& val) {
    const int x = 22559;
    std::cout << 22559;
    for (int i = 0; i < 22559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22560(int& val) {
    func_in_22560(val);
    const int x = 22560;
    std::cout << 22560;
    for (int i = 0; i < 22560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22560(int& val) {
    const int x = 22560;
    std::cout << 22560;
    for (int i = 0; i < 22560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22561(int& val) {
    func_in_22561(val);
    const int x = 22561;
    std::cout << 22561;
    for (int i = 0; i < 22561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22561(int& val) {
    const int x = 22561;
    std::cout << 22561;
    for (int i = 0; i < 22561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22562(int& val) {
    func_in_22562(val);
    const int x = 22562;
    std::cout << 22562;
    for (int i = 0; i < 22562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22562(int& val) {
    const int x = 22562;
    std::cout << 22562;
    for (int i = 0; i < 22562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22563(int& val) {
    func_in_22563(val);
    const int x = 22563;
    std::cout << 22563;
    for (int i = 0; i < 22563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22563(int& val) {
    const int x = 22563;
    std::cout << 22563;
    for (int i = 0; i < 22563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22564(int& val) {
    func_in_22564(val);
    const int x = 22564;
    std::cout << 22564;
    for (int i = 0; i < 22564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22564(int& val) {
    const int x = 22564;
    std::cout << 22564;
    for (int i = 0; i < 22564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22565(int& val) {
    func_in_22565(val);
    const int x = 22565;
    std::cout << 22565;
    for (int i = 0; i < 22565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22565(int& val) {
    const int x = 22565;
    std::cout << 22565;
    for (int i = 0; i < 22565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22566(int& val) {
    func_in_22566(val);
    const int x = 22566;
    std::cout << 22566;
    for (int i = 0; i < 22566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22566(int& val) {
    const int x = 22566;
    std::cout << 22566;
    for (int i = 0; i < 22566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22567(int& val) {
    func_in_22567(val);
    const int x = 22567;
    std::cout << 22567;
    for (int i = 0; i < 22567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22567(int& val) {
    const int x = 22567;
    std::cout << 22567;
    for (int i = 0; i < 22567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22568(int& val) {
    func_in_22568(val);
    const int x = 22568;
    std::cout << 22568;
    for (int i = 0; i < 22568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22568(int& val) {
    const int x = 22568;
    std::cout << 22568;
    for (int i = 0; i < 22568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22569(int& val) {
    func_in_22569(val);
    const int x = 22569;
    std::cout << 22569;
    for (int i = 0; i < 22569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22569(int& val) {
    const int x = 22569;
    std::cout << 22569;
    for (int i = 0; i < 22569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22570(int& val) {
    func_in_22570(val);
    const int x = 22570;
    std::cout << 22570;
    for (int i = 0; i < 22570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22570(int& val) {
    const int x = 22570;
    std::cout << 22570;
    for (int i = 0; i < 22570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22571(int& val) {
    func_in_22571(val);
    const int x = 22571;
    std::cout << 22571;
    for (int i = 0; i < 22571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22571(int& val) {
    const int x = 22571;
    std::cout << 22571;
    for (int i = 0; i < 22571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22572(int& val) {
    func_in_22572(val);
    const int x = 22572;
    std::cout << 22572;
    for (int i = 0; i < 22572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22572(int& val) {
    const int x = 22572;
    std::cout << 22572;
    for (int i = 0; i < 22572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22573(int& val) {
    func_in_22573(val);
    const int x = 22573;
    std::cout << 22573;
    for (int i = 0; i < 22573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22573(int& val) {
    const int x = 22573;
    std::cout << 22573;
    for (int i = 0; i < 22573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22574(int& val) {
    func_in_22574(val);
    const int x = 22574;
    std::cout << 22574;
    for (int i = 0; i < 22574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22574(int& val) {
    const int x = 22574;
    std::cout << 22574;
    for (int i = 0; i < 22574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22575(int& val) {
    func_in_22575(val);
    const int x = 22575;
    std::cout << 22575;
    for (int i = 0; i < 22575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22575(int& val) {
    const int x = 22575;
    std::cout << 22575;
    for (int i = 0; i < 22575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22576(int& val) {
    func_in_22576(val);
    const int x = 22576;
    std::cout << 22576;
    for (int i = 0; i < 22576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22576(int& val) {
    const int x = 22576;
    std::cout << 22576;
    for (int i = 0; i < 22576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22577(int& val) {
    func_in_22577(val);
    const int x = 22577;
    std::cout << 22577;
    for (int i = 0; i < 22577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22577(int& val) {
    const int x = 22577;
    std::cout << 22577;
    for (int i = 0; i < 22577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22578(int& val) {
    func_in_22578(val);
    const int x = 22578;
    std::cout << 22578;
    for (int i = 0; i < 22578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22578(int& val) {
    const int x = 22578;
    std::cout << 22578;
    for (int i = 0; i < 22578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22579(int& val) {
    func_in_22579(val);
    const int x = 22579;
    std::cout << 22579;
    for (int i = 0; i < 22579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22579(int& val) {
    const int x = 22579;
    std::cout << 22579;
    for (int i = 0; i < 22579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22580(int& val) {
    func_in_22580(val);
    const int x = 22580;
    std::cout << 22580;
    for (int i = 0; i < 22580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22580(int& val) {
    const int x = 22580;
    std::cout << 22580;
    for (int i = 0; i < 22580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22581(int& val) {
    func_in_22581(val);
    const int x = 22581;
    std::cout << 22581;
    for (int i = 0; i < 22581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22581(int& val) {
    const int x = 22581;
    std::cout << 22581;
    for (int i = 0; i < 22581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22582(int& val) {
    func_in_22582(val);
    const int x = 22582;
    std::cout << 22582;
    for (int i = 0; i < 22582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22582(int& val) {
    const int x = 22582;
    std::cout << 22582;
    for (int i = 0; i < 22582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22583(int& val) {
    func_in_22583(val);
    const int x = 22583;
    std::cout << 22583;
    for (int i = 0; i < 22583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22583(int& val) {
    const int x = 22583;
    std::cout << 22583;
    for (int i = 0; i < 22583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22584(int& val) {
    func_in_22584(val);
    const int x = 22584;
    std::cout << 22584;
    for (int i = 0; i < 22584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22584(int& val) {
    const int x = 22584;
    std::cout << 22584;
    for (int i = 0; i < 22584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22585(int& val) {
    func_in_22585(val);
    const int x = 22585;
    std::cout << 22585;
    for (int i = 0; i < 22585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22585(int& val) {
    const int x = 22585;
    std::cout << 22585;
    for (int i = 0; i < 22585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22586(int& val) {
    func_in_22586(val);
    const int x = 22586;
    std::cout << 22586;
    for (int i = 0; i < 22586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22586(int& val) {
    const int x = 22586;
    std::cout << 22586;
    for (int i = 0; i < 22586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22587(int& val) {
    func_in_22587(val);
    const int x = 22587;
    std::cout << 22587;
    for (int i = 0; i < 22587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22587(int& val) {
    const int x = 22587;
    std::cout << 22587;
    for (int i = 0; i < 22587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22588(int& val) {
    func_in_22588(val);
    const int x = 22588;
    std::cout << 22588;
    for (int i = 0; i < 22588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22588(int& val) {
    const int x = 22588;
    std::cout << 22588;
    for (int i = 0; i < 22588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22589(int& val) {
    func_in_22589(val);
    const int x = 22589;
    std::cout << 22589;
    for (int i = 0; i < 22589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22589(int& val) {
    const int x = 22589;
    std::cout << 22589;
    for (int i = 0; i < 22589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22590(int& val) {
    func_in_22590(val);
    const int x = 22590;
    std::cout << 22590;
    for (int i = 0; i < 22590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22590(int& val) {
    const int x = 22590;
    std::cout << 22590;
    for (int i = 0; i < 22590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22591(int& val) {
    func_in_22591(val);
    const int x = 22591;
    std::cout << 22591;
    for (int i = 0; i < 22591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22591(int& val) {
    const int x = 22591;
    std::cout << 22591;
    for (int i = 0; i < 22591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22592(int& val) {
    func_in_22592(val);
    const int x = 22592;
    std::cout << 22592;
    for (int i = 0; i < 22592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22592(int& val) {
    const int x = 22592;
    std::cout << 22592;
    for (int i = 0; i < 22592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22593(int& val) {
    func_in_22593(val);
    const int x = 22593;
    std::cout << 22593;
    for (int i = 0; i < 22593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22593(int& val) {
    const int x = 22593;
    std::cout << 22593;
    for (int i = 0; i < 22593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22594(int& val) {
    func_in_22594(val);
    const int x = 22594;
    std::cout << 22594;
    for (int i = 0; i < 22594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22594(int& val) {
    const int x = 22594;
    std::cout << 22594;
    for (int i = 0; i < 22594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22595(int& val) {
    func_in_22595(val);
    const int x = 22595;
    std::cout << 22595;
    for (int i = 0; i < 22595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22595(int& val) {
    const int x = 22595;
    std::cout << 22595;
    for (int i = 0; i < 22595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22596(int& val) {
    func_in_22596(val);
    const int x = 22596;
    std::cout << 22596;
    for (int i = 0; i < 22596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22596(int& val) {
    const int x = 22596;
    std::cout << 22596;
    for (int i = 0; i < 22596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22597(int& val) {
    func_in_22597(val);
    const int x = 22597;
    std::cout << 22597;
    for (int i = 0; i < 22597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22597(int& val) {
    const int x = 22597;
    std::cout << 22597;
    for (int i = 0; i < 22597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22598(int& val) {
    func_in_22598(val);
    const int x = 22598;
    std::cout << 22598;
    for (int i = 0; i < 22598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22598(int& val) {
    const int x = 22598;
    std::cout << 22598;
    for (int i = 0; i < 22598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22599(int& val) {
    func_in_22599(val);
    const int x = 22599;
    std::cout << 22599;
    for (int i = 0; i < 22599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22599(int& val) {
    const int x = 22599;
    std::cout << 22599;
    for (int i = 0; i < 22599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22600(int& val) {
    func_in_22600(val);
    const int x = 22600;
    std::cout << 22600;
    for (int i = 0; i < 22600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22600(int& val) {
    const int x = 22600;
    std::cout << 22600;
    for (int i = 0; i < 22600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22601(int& val) {
    func_in_22601(val);
    const int x = 22601;
    std::cout << 22601;
    for (int i = 0; i < 22601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22601(int& val) {
    const int x = 22601;
    std::cout << 22601;
    for (int i = 0; i < 22601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22602(int& val) {
    func_in_22602(val);
    const int x = 22602;
    std::cout << 22602;
    for (int i = 0; i < 22602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22602(int& val) {
    const int x = 22602;
    std::cout << 22602;
    for (int i = 0; i < 22602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22603(int& val) {
    func_in_22603(val);
    const int x = 22603;
    std::cout << 22603;
    for (int i = 0; i < 22603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22603(int& val) {
    const int x = 22603;
    std::cout << 22603;
    for (int i = 0; i < 22603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22604(int& val) {
    func_in_22604(val);
    const int x = 22604;
    std::cout << 22604;
    for (int i = 0; i < 22604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22604(int& val) {
    const int x = 22604;
    std::cout << 22604;
    for (int i = 0; i < 22604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22605(int& val) {
    func_in_22605(val);
    const int x = 22605;
    std::cout << 22605;
    for (int i = 0; i < 22605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22605(int& val) {
    const int x = 22605;
    std::cout << 22605;
    for (int i = 0; i < 22605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22606(int& val) {
    func_in_22606(val);
    const int x = 22606;
    std::cout << 22606;
    for (int i = 0; i < 22606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22606(int& val) {
    const int x = 22606;
    std::cout << 22606;
    for (int i = 0; i < 22606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22607(int& val) {
    func_in_22607(val);
    const int x = 22607;
    std::cout << 22607;
    for (int i = 0; i < 22607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22607(int& val) {
    const int x = 22607;
    std::cout << 22607;
    for (int i = 0; i < 22607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22608(int& val) {
    func_in_22608(val);
    const int x = 22608;
    std::cout << 22608;
    for (int i = 0; i < 22608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22608(int& val) {
    const int x = 22608;
    std::cout << 22608;
    for (int i = 0; i < 22608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22609(int& val) {
    func_in_22609(val);
    const int x = 22609;
    std::cout << 22609;
    for (int i = 0; i < 22609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22609(int& val) {
    const int x = 22609;
    std::cout << 22609;
    for (int i = 0; i < 22609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22610(int& val) {
    func_in_22610(val);
    const int x = 22610;
    std::cout << 22610;
    for (int i = 0; i < 22610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22610(int& val) {
    const int x = 22610;
    std::cout << 22610;
    for (int i = 0; i < 22610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22611(int& val) {
    func_in_22611(val);
    const int x = 22611;
    std::cout << 22611;
    for (int i = 0; i < 22611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22611(int& val) {
    const int x = 22611;
    std::cout << 22611;
    for (int i = 0; i < 22611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22612(int& val) {
    func_in_22612(val);
    const int x = 22612;
    std::cout << 22612;
    for (int i = 0; i < 22612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22612(int& val) {
    const int x = 22612;
    std::cout << 22612;
    for (int i = 0; i < 22612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22613(int& val) {
    func_in_22613(val);
    const int x = 22613;
    std::cout << 22613;
    for (int i = 0; i < 22613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22613(int& val) {
    const int x = 22613;
    std::cout << 22613;
    for (int i = 0; i < 22613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22614(int& val) {
    func_in_22614(val);
    const int x = 22614;
    std::cout << 22614;
    for (int i = 0; i < 22614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22614(int& val) {
    const int x = 22614;
    std::cout << 22614;
    for (int i = 0; i < 22614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22615(int& val) {
    func_in_22615(val);
    const int x = 22615;
    std::cout << 22615;
    for (int i = 0; i < 22615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22615(int& val) {
    const int x = 22615;
    std::cout << 22615;
    for (int i = 0; i < 22615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22616(int& val) {
    func_in_22616(val);
    const int x = 22616;
    std::cout << 22616;
    for (int i = 0; i < 22616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22616(int& val) {
    const int x = 22616;
    std::cout << 22616;
    for (int i = 0; i < 22616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22617(int& val) {
    func_in_22617(val);
    const int x = 22617;
    std::cout << 22617;
    for (int i = 0; i < 22617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22617(int& val) {
    const int x = 22617;
    std::cout << 22617;
    for (int i = 0; i < 22617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22618(int& val) {
    func_in_22618(val);
    const int x = 22618;
    std::cout << 22618;
    for (int i = 0; i < 22618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22618(int& val) {
    const int x = 22618;
    std::cout << 22618;
    for (int i = 0; i < 22618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22619(int& val) {
    func_in_22619(val);
    const int x = 22619;
    std::cout << 22619;
    for (int i = 0; i < 22619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22619(int& val) {
    const int x = 22619;
    std::cout << 22619;
    for (int i = 0; i < 22619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22620(int& val) {
    func_in_22620(val);
    const int x = 22620;
    std::cout << 22620;
    for (int i = 0; i < 22620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22620(int& val) {
    const int x = 22620;
    std::cout << 22620;
    for (int i = 0; i < 22620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22621(int& val) {
    func_in_22621(val);
    const int x = 22621;
    std::cout << 22621;
    for (int i = 0; i < 22621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22621(int& val) {
    const int x = 22621;
    std::cout << 22621;
    for (int i = 0; i < 22621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22622(int& val) {
    func_in_22622(val);
    const int x = 22622;
    std::cout << 22622;
    for (int i = 0; i < 22622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22622(int& val) {
    const int x = 22622;
    std::cout << 22622;
    for (int i = 0; i < 22622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22623(int& val) {
    func_in_22623(val);
    const int x = 22623;
    std::cout << 22623;
    for (int i = 0; i < 22623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22623(int& val) {
    const int x = 22623;
    std::cout << 22623;
    for (int i = 0; i < 22623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22624(int& val) {
    func_in_22624(val);
    const int x = 22624;
    std::cout << 22624;
    for (int i = 0; i < 22624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22624(int& val) {
    const int x = 22624;
    std::cout << 22624;
    for (int i = 0; i < 22624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22625(int& val) {
    func_in_22625(val);
    const int x = 22625;
    std::cout << 22625;
    for (int i = 0; i < 22625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22625(int& val) {
    const int x = 22625;
    std::cout << 22625;
    for (int i = 0; i < 22625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22626(int& val) {
    func_in_22626(val);
    const int x = 22626;
    std::cout << 22626;
    for (int i = 0; i < 22626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22626(int& val) {
    const int x = 22626;
    std::cout << 22626;
    for (int i = 0; i < 22626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22627(int& val) {
    func_in_22627(val);
    const int x = 22627;
    std::cout << 22627;
    for (int i = 0; i < 22627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22627(int& val) {
    const int x = 22627;
    std::cout << 22627;
    for (int i = 0; i < 22627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22628(int& val) {
    func_in_22628(val);
    const int x = 22628;
    std::cout << 22628;
    for (int i = 0; i < 22628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22628(int& val) {
    const int x = 22628;
    std::cout << 22628;
    for (int i = 0; i < 22628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22629(int& val) {
    func_in_22629(val);
    const int x = 22629;
    std::cout << 22629;
    for (int i = 0; i < 22629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22629(int& val) {
    const int x = 22629;
    std::cout << 22629;
    for (int i = 0; i < 22629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22630(int& val) {
    func_in_22630(val);
    const int x = 22630;
    std::cout << 22630;
    for (int i = 0; i < 22630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22630(int& val) {
    const int x = 22630;
    std::cout << 22630;
    for (int i = 0; i < 22630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22631(int& val) {
    func_in_22631(val);
    const int x = 22631;
    std::cout << 22631;
    for (int i = 0; i < 22631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22631(int& val) {
    const int x = 22631;
    std::cout << 22631;
    for (int i = 0; i < 22631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22632(int& val) {
    func_in_22632(val);
    const int x = 22632;
    std::cout << 22632;
    for (int i = 0; i < 22632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22632(int& val) {
    const int x = 22632;
    std::cout << 22632;
    for (int i = 0; i < 22632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22633(int& val) {
    func_in_22633(val);
    const int x = 22633;
    std::cout << 22633;
    for (int i = 0; i < 22633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22633(int& val) {
    const int x = 22633;
    std::cout << 22633;
    for (int i = 0; i < 22633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22634(int& val) {
    func_in_22634(val);
    const int x = 22634;
    std::cout << 22634;
    for (int i = 0; i < 22634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22634(int& val) {
    const int x = 22634;
    std::cout << 22634;
    for (int i = 0; i < 22634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22635(int& val) {
    func_in_22635(val);
    const int x = 22635;
    std::cout << 22635;
    for (int i = 0; i < 22635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22635(int& val) {
    const int x = 22635;
    std::cout << 22635;
    for (int i = 0; i < 22635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22636(int& val) {
    func_in_22636(val);
    const int x = 22636;
    std::cout << 22636;
    for (int i = 0; i < 22636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22636(int& val) {
    const int x = 22636;
    std::cout << 22636;
    for (int i = 0; i < 22636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22637(int& val) {
    func_in_22637(val);
    const int x = 22637;
    std::cout << 22637;
    for (int i = 0; i < 22637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22637(int& val) {
    const int x = 22637;
    std::cout << 22637;
    for (int i = 0; i < 22637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22638(int& val) {
    func_in_22638(val);
    const int x = 22638;
    std::cout << 22638;
    for (int i = 0; i < 22638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22638(int& val) {
    const int x = 22638;
    std::cout << 22638;
    for (int i = 0; i < 22638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22639(int& val) {
    func_in_22639(val);
    const int x = 22639;
    std::cout << 22639;
    for (int i = 0; i < 22639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22639(int& val) {
    const int x = 22639;
    std::cout << 22639;
    for (int i = 0; i < 22639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22640(int& val) {
    func_in_22640(val);
    const int x = 22640;
    std::cout << 22640;
    for (int i = 0; i < 22640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22640(int& val) {
    const int x = 22640;
    std::cout << 22640;
    for (int i = 0; i < 22640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22641(int& val) {
    func_in_22641(val);
    const int x = 22641;
    std::cout << 22641;
    for (int i = 0; i < 22641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22641(int& val) {
    const int x = 22641;
    std::cout << 22641;
    for (int i = 0; i < 22641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22642(int& val) {
    func_in_22642(val);
    const int x = 22642;
    std::cout << 22642;
    for (int i = 0; i < 22642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22642(int& val) {
    const int x = 22642;
    std::cout << 22642;
    for (int i = 0; i < 22642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22643(int& val) {
    func_in_22643(val);
    const int x = 22643;
    std::cout << 22643;
    for (int i = 0; i < 22643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22643(int& val) {
    const int x = 22643;
    std::cout << 22643;
    for (int i = 0; i < 22643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22644(int& val) {
    func_in_22644(val);
    const int x = 22644;
    std::cout << 22644;
    for (int i = 0; i < 22644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22644(int& val) {
    const int x = 22644;
    std::cout << 22644;
    for (int i = 0; i < 22644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22645(int& val) {
    func_in_22645(val);
    const int x = 22645;
    std::cout << 22645;
    for (int i = 0; i < 22645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22645(int& val) {
    const int x = 22645;
    std::cout << 22645;
    for (int i = 0; i < 22645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22646(int& val) {
    func_in_22646(val);
    const int x = 22646;
    std::cout << 22646;
    for (int i = 0; i < 22646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22646(int& val) {
    const int x = 22646;
    std::cout << 22646;
    for (int i = 0; i < 22646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22647(int& val) {
    func_in_22647(val);
    const int x = 22647;
    std::cout << 22647;
    for (int i = 0; i < 22647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22647(int& val) {
    const int x = 22647;
    std::cout << 22647;
    for (int i = 0; i < 22647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22648(int& val) {
    func_in_22648(val);
    const int x = 22648;
    std::cout << 22648;
    for (int i = 0; i < 22648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22648(int& val) {
    const int x = 22648;
    std::cout << 22648;
    for (int i = 0; i < 22648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22649(int& val) {
    func_in_22649(val);
    const int x = 22649;
    std::cout << 22649;
    for (int i = 0; i < 22649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22649(int& val) {
    const int x = 22649;
    std::cout << 22649;
    for (int i = 0; i < 22649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22650(int& val) {
    func_in_22650(val);
    const int x = 22650;
    std::cout << 22650;
    for (int i = 0; i < 22650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22650(int& val) {
    const int x = 22650;
    std::cout << 22650;
    for (int i = 0; i < 22650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22651(int& val) {
    func_in_22651(val);
    const int x = 22651;
    std::cout << 22651;
    for (int i = 0; i < 22651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22651(int& val) {
    const int x = 22651;
    std::cout << 22651;
    for (int i = 0; i < 22651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22652(int& val) {
    func_in_22652(val);
    const int x = 22652;
    std::cout << 22652;
    for (int i = 0; i < 22652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22652(int& val) {
    const int x = 22652;
    std::cout << 22652;
    for (int i = 0; i < 22652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22653(int& val) {
    func_in_22653(val);
    const int x = 22653;
    std::cout << 22653;
    for (int i = 0; i < 22653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22653(int& val) {
    const int x = 22653;
    std::cout << 22653;
    for (int i = 0; i < 22653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22654(int& val) {
    func_in_22654(val);
    const int x = 22654;
    std::cout << 22654;
    for (int i = 0; i < 22654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22654(int& val) {
    const int x = 22654;
    std::cout << 22654;
    for (int i = 0; i < 22654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22655(int& val) {
    func_in_22655(val);
    const int x = 22655;
    std::cout << 22655;
    for (int i = 0; i < 22655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22655(int& val) {
    const int x = 22655;
    std::cout << 22655;
    for (int i = 0; i < 22655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22656(int& val) {
    func_in_22656(val);
    const int x = 22656;
    std::cout << 22656;
    for (int i = 0; i < 22656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22656(int& val) {
    const int x = 22656;
    std::cout << 22656;
    for (int i = 0; i < 22656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22657(int& val) {
    func_in_22657(val);
    const int x = 22657;
    std::cout << 22657;
    for (int i = 0; i < 22657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22657(int& val) {
    const int x = 22657;
    std::cout << 22657;
    for (int i = 0; i < 22657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22658(int& val) {
    func_in_22658(val);
    const int x = 22658;
    std::cout << 22658;
    for (int i = 0; i < 22658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22658(int& val) {
    const int x = 22658;
    std::cout << 22658;
    for (int i = 0; i < 22658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22659(int& val) {
    func_in_22659(val);
    const int x = 22659;
    std::cout << 22659;
    for (int i = 0; i < 22659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22659(int& val) {
    const int x = 22659;
    std::cout << 22659;
    for (int i = 0; i < 22659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22660(int& val) {
    func_in_22660(val);
    const int x = 22660;
    std::cout << 22660;
    for (int i = 0; i < 22660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22660(int& val) {
    const int x = 22660;
    std::cout << 22660;
    for (int i = 0; i < 22660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22661(int& val) {
    func_in_22661(val);
    const int x = 22661;
    std::cout << 22661;
    for (int i = 0; i < 22661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22661(int& val) {
    const int x = 22661;
    std::cout << 22661;
    for (int i = 0; i < 22661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22662(int& val) {
    func_in_22662(val);
    const int x = 22662;
    std::cout << 22662;
    for (int i = 0; i < 22662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22662(int& val) {
    const int x = 22662;
    std::cout << 22662;
    for (int i = 0; i < 22662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22663(int& val) {
    func_in_22663(val);
    const int x = 22663;
    std::cout << 22663;
    for (int i = 0; i < 22663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22663(int& val) {
    const int x = 22663;
    std::cout << 22663;
    for (int i = 0; i < 22663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22664(int& val) {
    func_in_22664(val);
    const int x = 22664;
    std::cout << 22664;
    for (int i = 0; i < 22664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22664(int& val) {
    const int x = 22664;
    std::cout << 22664;
    for (int i = 0; i < 22664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22665(int& val) {
    func_in_22665(val);
    const int x = 22665;
    std::cout << 22665;
    for (int i = 0; i < 22665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22665(int& val) {
    const int x = 22665;
    std::cout << 22665;
    for (int i = 0; i < 22665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22666(int& val) {
    func_in_22666(val);
    const int x = 22666;
    std::cout << 22666;
    for (int i = 0; i < 22666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22666(int& val) {
    const int x = 22666;
    std::cout << 22666;
    for (int i = 0; i < 22666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22667(int& val) {
    func_in_22667(val);
    const int x = 22667;
    std::cout << 22667;
    for (int i = 0; i < 22667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22667(int& val) {
    const int x = 22667;
    std::cout << 22667;
    for (int i = 0; i < 22667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22668(int& val) {
    func_in_22668(val);
    const int x = 22668;
    std::cout << 22668;
    for (int i = 0; i < 22668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22668(int& val) {
    const int x = 22668;
    std::cout << 22668;
    for (int i = 0; i < 22668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22669(int& val) {
    func_in_22669(val);
    const int x = 22669;
    std::cout << 22669;
    for (int i = 0; i < 22669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22669(int& val) {
    const int x = 22669;
    std::cout << 22669;
    for (int i = 0; i < 22669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22670(int& val) {
    func_in_22670(val);
    const int x = 22670;
    std::cout << 22670;
    for (int i = 0; i < 22670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22670(int& val) {
    const int x = 22670;
    std::cout << 22670;
    for (int i = 0; i < 22670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22671(int& val) {
    func_in_22671(val);
    const int x = 22671;
    std::cout << 22671;
    for (int i = 0; i < 22671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22671(int& val) {
    const int x = 22671;
    std::cout << 22671;
    for (int i = 0; i < 22671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22672(int& val) {
    func_in_22672(val);
    const int x = 22672;
    std::cout << 22672;
    for (int i = 0; i < 22672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22672(int& val) {
    const int x = 22672;
    std::cout << 22672;
    for (int i = 0; i < 22672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22673(int& val) {
    func_in_22673(val);
    const int x = 22673;
    std::cout << 22673;
    for (int i = 0; i < 22673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22673(int& val) {
    const int x = 22673;
    std::cout << 22673;
    for (int i = 0; i < 22673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22674(int& val) {
    func_in_22674(val);
    const int x = 22674;
    std::cout << 22674;
    for (int i = 0; i < 22674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22674(int& val) {
    const int x = 22674;
    std::cout << 22674;
    for (int i = 0; i < 22674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22675(int& val) {
    func_in_22675(val);
    const int x = 22675;
    std::cout << 22675;
    for (int i = 0; i < 22675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22675(int& val) {
    const int x = 22675;
    std::cout << 22675;
    for (int i = 0; i < 22675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22676(int& val) {
    func_in_22676(val);
    const int x = 22676;
    std::cout << 22676;
    for (int i = 0; i < 22676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22676(int& val) {
    const int x = 22676;
    std::cout << 22676;
    for (int i = 0; i < 22676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22677(int& val) {
    func_in_22677(val);
    const int x = 22677;
    std::cout << 22677;
    for (int i = 0; i < 22677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22677(int& val) {
    const int x = 22677;
    std::cout << 22677;
    for (int i = 0; i < 22677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22678(int& val) {
    func_in_22678(val);
    const int x = 22678;
    std::cout << 22678;
    for (int i = 0; i < 22678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22678(int& val) {
    const int x = 22678;
    std::cout << 22678;
    for (int i = 0; i < 22678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22679(int& val) {
    func_in_22679(val);
    const int x = 22679;
    std::cout << 22679;
    for (int i = 0; i < 22679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22679(int& val) {
    const int x = 22679;
    std::cout << 22679;
    for (int i = 0; i < 22679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22680(int& val) {
    func_in_22680(val);
    const int x = 22680;
    std::cout << 22680;
    for (int i = 0; i < 22680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22680(int& val) {
    const int x = 22680;
    std::cout << 22680;
    for (int i = 0; i < 22680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22681(int& val) {
    func_in_22681(val);
    const int x = 22681;
    std::cout << 22681;
    for (int i = 0; i < 22681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22681(int& val) {
    const int x = 22681;
    std::cout << 22681;
    for (int i = 0; i < 22681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22682(int& val) {
    func_in_22682(val);
    const int x = 22682;
    std::cout << 22682;
    for (int i = 0; i < 22682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22682(int& val) {
    const int x = 22682;
    std::cout << 22682;
    for (int i = 0; i < 22682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22683(int& val) {
    func_in_22683(val);
    const int x = 22683;
    std::cout << 22683;
    for (int i = 0; i < 22683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22683(int& val) {
    const int x = 22683;
    std::cout << 22683;
    for (int i = 0; i < 22683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22684(int& val) {
    func_in_22684(val);
    const int x = 22684;
    std::cout << 22684;
    for (int i = 0; i < 22684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22684(int& val) {
    const int x = 22684;
    std::cout << 22684;
    for (int i = 0; i < 22684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22685(int& val) {
    func_in_22685(val);
    const int x = 22685;
    std::cout << 22685;
    for (int i = 0; i < 22685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22685(int& val) {
    const int x = 22685;
    std::cout << 22685;
    for (int i = 0; i < 22685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22686(int& val) {
    func_in_22686(val);
    const int x = 22686;
    std::cout << 22686;
    for (int i = 0; i < 22686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22686(int& val) {
    const int x = 22686;
    std::cout << 22686;
    for (int i = 0; i < 22686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22687(int& val) {
    func_in_22687(val);
    const int x = 22687;
    std::cout << 22687;
    for (int i = 0; i < 22687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22687(int& val) {
    const int x = 22687;
    std::cout << 22687;
    for (int i = 0; i < 22687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22688(int& val) {
    func_in_22688(val);
    const int x = 22688;
    std::cout << 22688;
    for (int i = 0; i < 22688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22688(int& val) {
    const int x = 22688;
    std::cout << 22688;
    for (int i = 0; i < 22688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22689(int& val) {
    func_in_22689(val);
    const int x = 22689;
    std::cout << 22689;
    for (int i = 0; i < 22689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22689(int& val) {
    const int x = 22689;
    std::cout << 22689;
    for (int i = 0; i < 22689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22690(int& val) {
    func_in_22690(val);
    const int x = 22690;
    std::cout << 22690;
    for (int i = 0; i < 22690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22690(int& val) {
    const int x = 22690;
    std::cout << 22690;
    for (int i = 0; i < 22690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22691(int& val) {
    func_in_22691(val);
    const int x = 22691;
    std::cout << 22691;
    for (int i = 0; i < 22691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22691(int& val) {
    const int x = 22691;
    std::cout << 22691;
    for (int i = 0; i < 22691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22692(int& val) {
    func_in_22692(val);
    const int x = 22692;
    std::cout << 22692;
    for (int i = 0; i < 22692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22692(int& val) {
    const int x = 22692;
    std::cout << 22692;
    for (int i = 0; i < 22692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22693(int& val) {
    func_in_22693(val);
    const int x = 22693;
    std::cout << 22693;
    for (int i = 0; i < 22693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22693(int& val) {
    const int x = 22693;
    std::cout << 22693;
    for (int i = 0; i < 22693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22694(int& val) {
    func_in_22694(val);
    const int x = 22694;
    std::cout << 22694;
    for (int i = 0; i < 22694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22694(int& val) {
    const int x = 22694;
    std::cout << 22694;
    for (int i = 0; i < 22694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22695(int& val) {
    func_in_22695(val);
    const int x = 22695;
    std::cout << 22695;
    for (int i = 0; i < 22695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22695(int& val) {
    const int x = 22695;
    std::cout << 22695;
    for (int i = 0; i < 22695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22696(int& val) {
    func_in_22696(val);
    const int x = 22696;
    std::cout << 22696;
    for (int i = 0; i < 22696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22696(int& val) {
    const int x = 22696;
    std::cout << 22696;
    for (int i = 0; i < 22696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22697(int& val) {
    func_in_22697(val);
    const int x = 22697;
    std::cout << 22697;
    for (int i = 0; i < 22697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22697(int& val) {
    const int x = 22697;
    std::cout << 22697;
    for (int i = 0; i < 22697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22698(int& val) {
    func_in_22698(val);
    const int x = 22698;
    std::cout << 22698;
    for (int i = 0; i < 22698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22698(int& val) {
    const int x = 22698;
    std::cout << 22698;
    for (int i = 0; i < 22698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22699(int& val) {
    func_in_22699(val);
    const int x = 22699;
    std::cout << 22699;
    for (int i = 0; i < 22699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22699(int& val) {
    const int x = 22699;
    std::cout << 22699;
    for (int i = 0; i < 22699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22700(int& val) {
    func_in_22700(val);
    const int x = 22700;
    std::cout << 22700;
    for (int i = 0; i < 22700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22700(int& val) {
    const int x = 22700;
    std::cout << 22700;
    for (int i = 0; i < 22700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22701(int& val) {
    func_in_22701(val);
    const int x = 22701;
    std::cout << 22701;
    for (int i = 0; i < 22701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22701(int& val) {
    const int x = 22701;
    std::cout << 22701;
    for (int i = 0; i < 22701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22702(int& val) {
    func_in_22702(val);
    const int x = 22702;
    std::cout << 22702;
    for (int i = 0; i < 22702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22702(int& val) {
    const int x = 22702;
    std::cout << 22702;
    for (int i = 0; i < 22702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22703(int& val) {
    func_in_22703(val);
    const int x = 22703;
    std::cout << 22703;
    for (int i = 0; i < 22703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22703(int& val) {
    const int x = 22703;
    std::cout << 22703;
    for (int i = 0; i < 22703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22704(int& val) {
    func_in_22704(val);
    const int x = 22704;
    std::cout << 22704;
    for (int i = 0; i < 22704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22704(int& val) {
    const int x = 22704;
    std::cout << 22704;
    for (int i = 0; i < 22704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22705(int& val) {
    func_in_22705(val);
    const int x = 22705;
    std::cout << 22705;
    for (int i = 0; i < 22705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22705(int& val) {
    const int x = 22705;
    std::cout << 22705;
    for (int i = 0; i < 22705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22706(int& val) {
    func_in_22706(val);
    const int x = 22706;
    std::cout << 22706;
    for (int i = 0; i < 22706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22706(int& val) {
    const int x = 22706;
    std::cout << 22706;
    for (int i = 0; i < 22706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22707(int& val) {
    func_in_22707(val);
    const int x = 22707;
    std::cout << 22707;
    for (int i = 0; i < 22707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22707(int& val) {
    const int x = 22707;
    std::cout << 22707;
    for (int i = 0; i < 22707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22708(int& val) {
    func_in_22708(val);
    const int x = 22708;
    std::cout << 22708;
    for (int i = 0; i < 22708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22708(int& val) {
    const int x = 22708;
    std::cout << 22708;
    for (int i = 0; i < 22708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22709(int& val) {
    func_in_22709(val);
    const int x = 22709;
    std::cout << 22709;
    for (int i = 0; i < 22709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22709(int& val) {
    const int x = 22709;
    std::cout << 22709;
    for (int i = 0; i < 22709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22710(int& val) {
    func_in_22710(val);
    const int x = 22710;
    std::cout << 22710;
    for (int i = 0; i < 22710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22710(int& val) {
    const int x = 22710;
    std::cout << 22710;
    for (int i = 0; i < 22710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22711(int& val) {
    func_in_22711(val);
    const int x = 22711;
    std::cout << 22711;
    for (int i = 0; i < 22711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22711(int& val) {
    const int x = 22711;
    std::cout << 22711;
    for (int i = 0; i < 22711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22712(int& val) {
    func_in_22712(val);
    const int x = 22712;
    std::cout << 22712;
    for (int i = 0; i < 22712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22712(int& val) {
    const int x = 22712;
    std::cout << 22712;
    for (int i = 0; i < 22712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22713(int& val) {
    func_in_22713(val);
    const int x = 22713;
    std::cout << 22713;
    for (int i = 0; i < 22713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22713(int& val) {
    const int x = 22713;
    std::cout << 22713;
    for (int i = 0; i < 22713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22714(int& val) {
    func_in_22714(val);
    const int x = 22714;
    std::cout << 22714;
    for (int i = 0; i < 22714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22714(int& val) {
    const int x = 22714;
    std::cout << 22714;
    for (int i = 0; i < 22714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22715(int& val) {
    func_in_22715(val);
    const int x = 22715;
    std::cout << 22715;
    for (int i = 0; i < 22715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22715(int& val) {
    const int x = 22715;
    std::cout << 22715;
    for (int i = 0; i < 22715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22716(int& val) {
    func_in_22716(val);
    const int x = 22716;
    std::cout << 22716;
    for (int i = 0; i < 22716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22716(int& val) {
    const int x = 22716;
    std::cout << 22716;
    for (int i = 0; i < 22716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22717(int& val) {
    func_in_22717(val);
    const int x = 22717;
    std::cout << 22717;
    for (int i = 0; i < 22717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22717(int& val) {
    const int x = 22717;
    std::cout << 22717;
    for (int i = 0; i < 22717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22718(int& val) {
    func_in_22718(val);
    const int x = 22718;
    std::cout << 22718;
    for (int i = 0; i < 22718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22718(int& val) {
    const int x = 22718;
    std::cout << 22718;
    for (int i = 0; i < 22718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22719(int& val) {
    func_in_22719(val);
    const int x = 22719;
    std::cout << 22719;
    for (int i = 0; i < 22719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22719(int& val) {
    const int x = 22719;
    std::cout << 22719;
    for (int i = 0; i < 22719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22720(int& val) {
    func_in_22720(val);
    const int x = 22720;
    std::cout << 22720;
    for (int i = 0; i < 22720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22720(int& val) {
    const int x = 22720;
    std::cout << 22720;
    for (int i = 0; i < 22720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22721(int& val) {
    func_in_22721(val);
    const int x = 22721;
    std::cout << 22721;
    for (int i = 0; i < 22721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22721(int& val) {
    const int x = 22721;
    std::cout << 22721;
    for (int i = 0; i < 22721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22722(int& val) {
    func_in_22722(val);
    const int x = 22722;
    std::cout << 22722;
    for (int i = 0; i < 22722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22722(int& val) {
    const int x = 22722;
    std::cout << 22722;
    for (int i = 0; i < 22722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22723(int& val) {
    func_in_22723(val);
    const int x = 22723;
    std::cout << 22723;
    for (int i = 0; i < 22723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22723(int& val) {
    const int x = 22723;
    std::cout << 22723;
    for (int i = 0; i < 22723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22724(int& val) {
    func_in_22724(val);
    const int x = 22724;
    std::cout << 22724;
    for (int i = 0; i < 22724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22724(int& val) {
    const int x = 22724;
    std::cout << 22724;
    for (int i = 0; i < 22724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22725(int& val) {
    func_in_22725(val);
    const int x = 22725;
    std::cout << 22725;
    for (int i = 0; i < 22725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22725(int& val) {
    const int x = 22725;
    std::cout << 22725;
    for (int i = 0; i < 22725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22726(int& val) {
    func_in_22726(val);
    const int x = 22726;
    std::cout << 22726;
    for (int i = 0; i < 22726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22726(int& val) {
    const int x = 22726;
    std::cout << 22726;
    for (int i = 0; i < 22726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22727(int& val) {
    func_in_22727(val);
    const int x = 22727;
    std::cout << 22727;
    for (int i = 0; i < 22727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22727(int& val) {
    const int x = 22727;
    std::cout << 22727;
    for (int i = 0; i < 22727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22728(int& val) {
    func_in_22728(val);
    const int x = 22728;
    std::cout << 22728;
    for (int i = 0; i < 22728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22728(int& val) {
    const int x = 22728;
    std::cout << 22728;
    for (int i = 0; i < 22728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22729(int& val) {
    func_in_22729(val);
    const int x = 22729;
    std::cout << 22729;
    for (int i = 0; i < 22729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22729(int& val) {
    const int x = 22729;
    std::cout << 22729;
    for (int i = 0; i < 22729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22730(int& val) {
    func_in_22730(val);
    const int x = 22730;
    std::cout << 22730;
    for (int i = 0; i < 22730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22730(int& val) {
    const int x = 22730;
    std::cout << 22730;
    for (int i = 0; i < 22730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22731(int& val) {
    func_in_22731(val);
    const int x = 22731;
    std::cout << 22731;
    for (int i = 0; i < 22731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22731(int& val) {
    const int x = 22731;
    std::cout << 22731;
    for (int i = 0; i < 22731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22732(int& val) {
    func_in_22732(val);
    const int x = 22732;
    std::cout << 22732;
    for (int i = 0; i < 22732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22732(int& val) {
    const int x = 22732;
    std::cout << 22732;
    for (int i = 0; i < 22732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22733(int& val) {
    func_in_22733(val);
    const int x = 22733;
    std::cout << 22733;
    for (int i = 0; i < 22733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22733(int& val) {
    const int x = 22733;
    std::cout << 22733;
    for (int i = 0; i < 22733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22734(int& val) {
    func_in_22734(val);
    const int x = 22734;
    std::cout << 22734;
    for (int i = 0; i < 22734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22734(int& val) {
    const int x = 22734;
    std::cout << 22734;
    for (int i = 0; i < 22734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22735(int& val) {
    func_in_22735(val);
    const int x = 22735;
    std::cout << 22735;
    for (int i = 0; i < 22735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22735(int& val) {
    const int x = 22735;
    std::cout << 22735;
    for (int i = 0; i < 22735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22736(int& val) {
    func_in_22736(val);
    const int x = 22736;
    std::cout << 22736;
    for (int i = 0; i < 22736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22736(int& val) {
    const int x = 22736;
    std::cout << 22736;
    for (int i = 0; i < 22736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22737(int& val) {
    func_in_22737(val);
    const int x = 22737;
    std::cout << 22737;
    for (int i = 0; i < 22737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22737(int& val) {
    const int x = 22737;
    std::cout << 22737;
    for (int i = 0; i < 22737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22738(int& val) {
    func_in_22738(val);
    const int x = 22738;
    std::cout << 22738;
    for (int i = 0; i < 22738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22738(int& val) {
    const int x = 22738;
    std::cout << 22738;
    for (int i = 0; i < 22738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22739(int& val) {
    func_in_22739(val);
    const int x = 22739;
    std::cout << 22739;
    for (int i = 0; i < 22739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22739(int& val) {
    const int x = 22739;
    std::cout << 22739;
    for (int i = 0; i < 22739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22740(int& val) {
    func_in_22740(val);
    const int x = 22740;
    std::cout << 22740;
    for (int i = 0; i < 22740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22740(int& val) {
    const int x = 22740;
    std::cout << 22740;
    for (int i = 0; i < 22740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22741(int& val) {
    func_in_22741(val);
    const int x = 22741;
    std::cout << 22741;
    for (int i = 0; i < 22741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22741(int& val) {
    const int x = 22741;
    std::cout << 22741;
    for (int i = 0; i < 22741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22742(int& val) {
    func_in_22742(val);
    const int x = 22742;
    std::cout << 22742;
    for (int i = 0; i < 22742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22742(int& val) {
    const int x = 22742;
    std::cout << 22742;
    for (int i = 0; i < 22742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22743(int& val) {
    func_in_22743(val);
    const int x = 22743;
    std::cout << 22743;
    for (int i = 0; i < 22743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22743(int& val) {
    const int x = 22743;
    std::cout << 22743;
    for (int i = 0; i < 22743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22744(int& val) {
    func_in_22744(val);
    const int x = 22744;
    std::cout << 22744;
    for (int i = 0; i < 22744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22744(int& val) {
    const int x = 22744;
    std::cout << 22744;
    for (int i = 0; i < 22744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22745(int& val) {
    func_in_22745(val);
    const int x = 22745;
    std::cout << 22745;
    for (int i = 0; i < 22745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22745(int& val) {
    const int x = 22745;
    std::cout << 22745;
    for (int i = 0; i < 22745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22746(int& val) {
    func_in_22746(val);
    const int x = 22746;
    std::cout << 22746;
    for (int i = 0; i < 22746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22746(int& val) {
    const int x = 22746;
    std::cout << 22746;
    for (int i = 0; i < 22746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22747(int& val) {
    func_in_22747(val);
    const int x = 22747;
    std::cout << 22747;
    for (int i = 0; i < 22747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22747(int& val) {
    const int x = 22747;
    std::cout << 22747;
    for (int i = 0; i < 22747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22748(int& val) {
    func_in_22748(val);
    const int x = 22748;
    std::cout << 22748;
    for (int i = 0; i < 22748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22748(int& val) {
    const int x = 22748;
    std::cout << 22748;
    for (int i = 0; i < 22748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22749(int& val) {
    func_in_22749(val);
    const int x = 22749;
    std::cout << 22749;
    for (int i = 0; i < 22749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22749(int& val) {
    const int x = 22749;
    std::cout << 22749;
    for (int i = 0; i < 22749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22750(int& val) {
    func_in_22750(val);
    const int x = 22750;
    std::cout << 22750;
    for (int i = 0; i < 22750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22750(int& val) {
    const int x = 22750;
    std::cout << 22750;
    for (int i = 0; i < 22750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22751(int& val) {
    func_in_22751(val);
    const int x = 22751;
    std::cout << 22751;
    for (int i = 0; i < 22751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22751(int& val) {
    const int x = 22751;
    std::cout << 22751;
    for (int i = 0; i < 22751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22752(int& val) {
    func_in_22752(val);
    const int x = 22752;
    std::cout << 22752;
    for (int i = 0; i < 22752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22752(int& val) {
    const int x = 22752;
    std::cout << 22752;
    for (int i = 0; i < 22752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22753(int& val) {
    func_in_22753(val);
    const int x = 22753;
    std::cout << 22753;
    for (int i = 0; i < 22753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22753(int& val) {
    const int x = 22753;
    std::cout << 22753;
    for (int i = 0; i < 22753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22754(int& val) {
    func_in_22754(val);
    const int x = 22754;
    std::cout << 22754;
    for (int i = 0; i < 22754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22754(int& val) {
    const int x = 22754;
    std::cout << 22754;
    for (int i = 0; i < 22754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22755(int& val) {
    func_in_22755(val);
    const int x = 22755;
    std::cout << 22755;
    for (int i = 0; i < 22755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22755(int& val) {
    const int x = 22755;
    std::cout << 22755;
    for (int i = 0; i < 22755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22756(int& val) {
    func_in_22756(val);
    const int x = 22756;
    std::cout << 22756;
    for (int i = 0; i < 22756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22756(int& val) {
    const int x = 22756;
    std::cout << 22756;
    for (int i = 0; i < 22756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22757(int& val) {
    func_in_22757(val);
    const int x = 22757;
    std::cout << 22757;
    for (int i = 0; i < 22757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22757(int& val) {
    const int x = 22757;
    std::cout << 22757;
    for (int i = 0; i < 22757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22758(int& val) {
    func_in_22758(val);
    const int x = 22758;
    std::cout << 22758;
    for (int i = 0; i < 22758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22758(int& val) {
    const int x = 22758;
    std::cout << 22758;
    for (int i = 0; i < 22758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22759(int& val) {
    func_in_22759(val);
    const int x = 22759;
    std::cout << 22759;
    for (int i = 0; i < 22759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22759(int& val) {
    const int x = 22759;
    std::cout << 22759;
    for (int i = 0; i < 22759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22760(int& val) {
    func_in_22760(val);
    const int x = 22760;
    std::cout << 22760;
    for (int i = 0; i < 22760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22760(int& val) {
    const int x = 22760;
    std::cout << 22760;
    for (int i = 0; i < 22760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22761(int& val) {
    func_in_22761(val);
    const int x = 22761;
    std::cout << 22761;
    for (int i = 0; i < 22761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22761(int& val) {
    const int x = 22761;
    std::cout << 22761;
    for (int i = 0; i < 22761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22762(int& val) {
    func_in_22762(val);
    const int x = 22762;
    std::cout << 22762;
    for (int i = 0; i < 22762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22762(int& val) {
    const int x = 22762;
    std::cout << 22762;
    for (int i = 0; i < 22762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22763(int& val) {
    func_in_22763(val);
    const int x = 22763;
    std::cout << 22763;
    for (int i = 0; i < 22763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22763(int& val) {
    const int x = 22763;
    std::cout << 22763;
    for (int i = 0; i < 22763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22764(int& val) {
    func_in_22764(val);
    const int x = 22764;
    std::cout << 22764;
    for (int i = 0; i < 22764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22764(int& val) {
    const int x = 22764;
    std::cout << 22764;
    for (int i = 0; i < 22764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22765(int& val) {
    func_in_22765(val);
    const int x = 22765;
    std::cout << 22765;
    for (int i = 0; i < 22765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22765(int& val) {
    const int x = 22765;
    std::cout << 22765;
    for (int i = 0; i < 22765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22766(int& val) {
    func_in_22766(val);
    const int x = 22766;
    std::cout << 22766;
    for (int i = 0; i < 22766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22766(int& val) {
    const int x = 22766;
    std::cout << 22766;
    for (int i = 0; i < 22766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22767(int& val) {
    func_in_22767(val);
    const int x = 22767;
    std::cout << 22767;
    for (int i = 0; i < 22767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22767(int& val) {
    const int x = 22767;
    std::cout << 22767;
    for (int i = 0; i < 22767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22768(int& val) {
    func_in_22768(val);
    const int x = 22768;
    std::cout << 22768;
    for (int i = 0; i < 22768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22768(int& val) {
    const int x = 22768;
    std::cout << 22768;
    for (int i = 0; i < 22768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22769(int& val) {
    func_in_22769(val);
    const int x = 22769;
    std::cout << 22769;
    for (int i = 0; i < 22769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22769(int& val) {
    const int x = 22769;
    std::cout << 22769;
    for (int i = 0; i < 22769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22770(int& val) {
    func_in_22770(val);
    const int x = 22770;
    std::cout << 22770;
    for (int i = 0; i < 22770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22770(int& val) {
    const int x = 22770;
    std::cout << 22770;
    for (int i = 0; i < 22770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22771(int& val) {
    func_in_22771(val);
    const int x = 22771;
    std::cout << 22771;
    for (int i = 0; i < 22771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22771(int& val) {
    const int x = 22771;
    std::cout << 22771;
    for (int i = 0; i < 22771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22772(int& val) {
    func_in_22772(val);
    const int x = 22772;
    std::cout << 22772;
    for (int i = 0; i < 22772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22772(int& val) {
    const int x = 22772;
    std::cout << 22772;
    for (int i = 0; i < 22772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22773(int& val) {
    func_in_22773(val);
    const int x = 22773;
    std::cout << 22773;
    for (int i = 0; i < 22773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22773(int& val) {
    const int x = 22773;
    std::cout << 22773;
    for (int i = 0; i < 22773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22774(int& val) {
    func_in_22774(val);
    const int x = 22774;
    std::cout << 22774;
    for (int i = 0; i < 22774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22774(int& val) {
    const int x = 22774;
    std::cout << 22774;
    for (int i = 0; i < 22774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22775(int& val) {
    func_in_22775(val);
    const int x = 22775;
    std::cout << 22775;
    for (int i = 0; i < 22775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22775(int& val) {
    const int x = 22775;
    std::cout << 22775;
    for (int i = 0; i < 22775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22776(int& val) {
    func_in_22776(val);
    const int x = 22776;
    std::cout << 22776;
    for (int i = 0; i < 22776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22776(int& val) {
    const int x = 22776;
    std::cout << 22776;
    for (int i = 0; i < 22776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22777(int& val) {
    func_in_22777(val);
    const int x = 22777;
    std::cout << 22777;
    for (int i = 0; i < 22777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22777(int& val) {
    const int x = 22777;
    std::cout << 22777;
    for (int i = 0; i < 22777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22778(int& val) {
    func_in_22778(val);
    const int x = 22778;
    std::cout << 22778;
    for (int i = 0; i < 22778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22778(int& val) {
    const int x = 22778;
    std::cout << 22778;
    for (int i = 0; i < 22778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22779(int& val) {
    func_in_22779(val);
    const int x = 22779;
    std::cout << 22779;
    for (int i = 0; i < 22779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22779(int& val) {
    const int x = 22779;
    std::cout << 22779;
    for (int i = 0; i < 22779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22780(int& val) {
    func_in_22780(val);
    const int x = 22780;
    std::cout << 22780;
    for (int i = 0; i < 22780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22780(int& val) {
    const int x = 22780;
    std::cout << 22780;
    for (int i = 0; i < 22780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22781(int& val) {
    func_in_22781(val);
    const int x = 22781;
    std::cout << 22781;
    for (int i = 0; i < 22781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22781(int& val) {
    const int x = 22781;
    std::cout << 22781;
    for (int i = 0; i < 22781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22782(int& val) {
    func_in_22782(val);
    const int x = 22782;
    std::cout << 22782;
    for (int i = 0; i < 22782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22782(int& val) {
    const int x = 22782;
    std::cout << 22782;
    for (int i = 0; i < 22782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22783(int& val) {
    func_in_22783(val);
    const int x = 22783;
    std::cout << 22783;
    for (int i = 0; i < 22783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22783(int& val) {
    const int x = 22783;
    std::cout << 22783;
    for (int i = 0; i < 22783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22784(int& val) {
    func_in_22784(val);
    const int x = 22784;
    std::cout << 22784;
    for (int i = 0; i < 22784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22784(int& val) {
    const int x = 22784;
    std::cout << 22784;
    for (int i = 0; i < 22784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22785(int& val) {
    func_in_22785(val);
    const int x = 22785;
    std::cout << 22785;
    for (int i = 0; i < 22785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22785(int& val) {
    const int x = 22785;
    std::cout << 22785;
    for (int i = 0; i < 22785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22786(int& val) {
    func_in_22786(val);
    const int x = 22786;
    std::cout << 22786;
    for (int i = 0; i < 22786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22786(int& val) {
    const int x = 22786;
    std::cout << 22786;
    for (int i = 0; i < 22786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22787(int& val) {
    func_in_22787(val);
    const int x = 22787;
    std::cout << 22787;
    for (int i = 0; i < 22787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22787(int& val) {
    const int x = 22787;
    std::cout << 22787;
    for (int i = 0; i < 22787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22788(int& val) {
    func_in_22788(val);
    const int x = 22788;
    std::cout << 22788;
    for (int i = 0; i < 22788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22788(int& val) {
    const int x = 22788;
    std::cout << 22788;
    for (int i = 0; i < 22788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22789(int& val) {
    func_in_22789(val);
    const int x = 22789;
    std::cout << 22789;
    for (int i = 0; i < 22789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22789(int& val) {
    const int x = 22789;
    std::cout << 22789;
    for (int i = 0; i < 22789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22790(int& val) {
    func_in_22790(val);
    const int x = 22790;
    std::cout << 22790;
    for (int i = 0; i < 22790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22790(int& val) {
    const int x = 22790;
    std::cout << 22790;
    for (int i = 0; i < 22790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22791(int& val) {
    func_in_22791(val);
    const int x = 22791;
    std::cout << 22791;
    for (int i = 0; i < 22791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22791(int& val) {
    const int x = 22791;
    std::cout << 22791;
    for (int i = 0; i < 22791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22792(int& val) {
    func_in_22792(val);
    const int x = 22792;
    std::cout << 22792;
    for (int i = 0; i < 22792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22792(int& val) {
    const int x = 22792;
    std::cout << 22792;
    for (int i = 0; i < 22792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22793(int& val) {
    func_in_22793(val);
    const int x = 22793;
    std::cout << 22793;
    for (int i = 0; i < 22793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22793(int& val) {
    const int x = 22793;
    std::cout << 22793;
    for (int i = 0; i < 22793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22794(int& val) {
    func_in_22794(val);
    const int x = 22794;
    std::cout << 22794;
    for (int i = 0; i < 22794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22794(int& val) {
    const int x = 22794;
    std::cout << 22794;
    for (int i = 0; i < 22794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22795(int& val) {
    func_in_22795(val);
    const int x = 22795;
    std::cout << 22795;
    for (int i = 0; i < 22795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22795(int& val) {
    const int x = 22795;
    std::cout << 22795;
    for (int i = 0; i < 22795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22796(int& val) {
    func_in_22796(val);
    const int x = 22796;
    std::cout << 22796;
    for (int i = 0; i < 22796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22796(int& val) {
    const int x = 22796;
    std::cout << 22796;
    for (int i = 0; i < 22796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22797(int& val) {
    func_in_22797(val);
    const int x = 22797;
    std::cout << 22797;
    for (int i = 0; i < 22797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22797(int& val) {
    const int x = 22797;
    std::cout << 22797;
    for (int i = 0; i < 22797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22798(int& val) {
    func_in_22798(val);
    const int x = 22798;
    std::cout << 22798;
    for (int i = 0; i < 22798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22798(int& val) {
    const int x = 22798;
    std::cout << 22798;
    for (int i = 0; i < 22798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22799(int& val) {
    func_in_22799(val);
    const int x = 22799;
    std::cout << 22799;
    for (int i = 0; i < 22799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22799(int& val) {
    const int x = 22799;
    std::cout << 22799;
    for (int i = 0; i < 22799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22800(int& val) {
    func_in_22800(val);
    const int x = 22800;
    std::cout << 22800;
    for (int i = 0; i < 22800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22800(int& val) {
    const int x = 22800;
    std::cout << 22800;
    for (int i = 0; i < 22800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22801(int& val) {
    func_in_22801(val);
    const int x = 22801;
    std::cout << 22801;
    for (int i = 0; i < 22801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22801(int& val) {
    const int x = 22801;
    std::cout << 22801;
    for (int i = 0; i < 22801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22802(int& val) {
    func_in_22802(val);
    const int x = 22802;
    std::cout << 22802;
    for (int i = 0; i < 22802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22802(int& val) {
    const int x = 22802;
    std::cout << 22802;
    for (int i = 0; i < 22802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22803(int& val) {
    func_in_22803(val);
    const int x = 22803;
    std::cout << 22803;
    for (int i = 0; i < 22803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22803(int& val) {
    const int x = 22803;
    std::cout << 22803;
    for (int i = 0; i < 22803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22804(int& val) {
    func_in_22804(val);
    const int x = 22804;
    std::cout << 22804;
    for (int i = 0; i < 22804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22804(int& val) {
    const int x = 22804;
    std::cout << 22804;
    for (int i = 0; i < 22804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22805(int& val) {
    func_in_22805(val);
    const int x = 22805;
    std::cout << 22805;
    for (int i = 0; i < 22805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22805(int& val) {
    const int x = 22805;
    std::cout << 22805;
    for (int i = 0; i < 22805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22806(int& val) {
    func_in_22806(val);
    const int x = 22806;
    std::cout << 22806;
    for (int i = 0; i < 22806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22806(int& val) {
    const int x = 22806;
    std::cout << 22806;
    for (int i = 0; i < 22806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22807(int& val) {
    func_in_22807(val);
    const int x = 22807;
    std::cout << 22807;
    for (int i = 0; i < 22807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22807(int& val) {
    const int x = 22807;
    std::cout << 22807;
    for (int i = 0; i < 22807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22808(int& val) {
    func_in_22808(val);
    const int x = 22808;
    std::cout << 22808;
    for (int i = 0; i < 22808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22808(int& val) {
    const int x = 22808;
    std::cout << 22808;
    for (int i = 0; i < 22808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22809(int& val) {
    func_in_22809(val);
    const int x = 22809;
    std::cout << 22809;
    for (int i = 0; i < 22809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22809(int& val) {
    const int x = 22809;
    std::cout << 22809;
    for (int i = 0; i < 22809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22810(int& val) {
    func_in_22810(val);
    const int x = 22810;
    std::cout << 22810;
    for (int i = 0; i < 22810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22810(int& val) {
    const int x = 22810;
    std::cout << 22810;
    for (int i = 0; i < 22810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22811(int& val) {
    func_in_22811(val);
    const int x = 22811;
    std::cout << 22811;
    for (int i = 0; i < 22811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22811(int& val) {
    const int x = 22811;
    std::cout << 22811;
    for (int i = 0; i < 22811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22812(int& val) {
    func_in_22812(val);
    const int x = 22812;
    std::cout << 22812;
    for (int i = 0; i < 22812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22812(int& val) {
    const int x = 22812;
    std::cout << 22812;
    for (int i = 0; i < 22812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22813(int& val) {
    func_in_22813(val);
    const int x = 22813;
    std::cout << 22813;
    for (int i = 0; i < 22813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22813(int& val) {
    const int x = 22813;
    std::cout << 22813;
    for (int i = 0; i < 22813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22814(int& val) {
    func_in_22814(val);
    const int x = 22814;
    std::cout << 22814;
    for (int i = 0; i < 22814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22814(int& val) {
    const int x = 22814;
    std::cout << 22814;
    for (int i = 0; i < 22814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22815(int& val) {
    func_in_22815(val);
    const int x = 22815;
    std::cout << 22815;
    for (int i = 0; i < 22815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22815(int& val) {
    const int x = 22815;
    std::cout << 22815;
    for (int i = 0; i < 22815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22816(int& val) {
    func_in_22816(val);
    const int x = 22816;
    std::cout << 22816;
    for (int i = 0; i < 22816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22816(int& val) {
    const int x = 22816;
    std::cout << 22816;
    for (int i = 0; i < 22816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22817(int& val) {
    func_in_22817(val);
    const int x = 22817;
    std::cout << 22817;
    for (int i = 0; i < 22817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22817(int& val) {
    const int x = 22817;
    std::cout << 22817;
    for (int i = 0; i < 22817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22818(int& val) {
    func_in_22818(val);
    const int x = 22818;
    std::cout << 22818;
    for (int i = 0; i < 22818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22818(int& val) {
    const int x = 22818;
    std::cout << 22818;
    for (int i = 0; i < 22818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22819(int& val) {
    func_in_22819(val);
    const int x = 22819;
    std::cout << 22819;
    for (int i = 0; i < 22819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22819(int& val) {
    const int x = 22819;
    std::cout << 22819;
    for (int i = 0; i < 22819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22820(int& val) {
    func_in_22820(val);
    const int x = 22820;
    std::cout << 22820;
    for (int i = 0; i < 22820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22820(int& val) {
    const int x = 22820;
    std::cout << 22820;
    for (int i = 0; i < 22820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22821(int& val) {
    func_in_22821(val);
    const int x = 22821;
    std::cout << 22821;
    for (int i = 0; i < 22821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22821(int& val) {
    const int x = 22821;
    std::cout << 22821;
    for (int i = 0; i < 22821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22822(int& val) {
    func_in_22822(val);
    const int x = 22822;
    std::cout << 22822;
    for (int i = 0; i < 22822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22822(int& val) {
    const int x = 22822;
    std::cout << 22822;
    for (int i = 0; i < 22822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22823(int& val) {
    func_in_22823(val);
    const int x = 22823;
    std::cout << 22823;
    for (int i = 0; i < 22823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22823(int& val) {
    const int x = 22823;
    std::cout << 22823;
    for (int i = 0; i < 22823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22824(int& val) {
    func_in_22824(val);
    const int x = 22824;
    std::cout << 22824;
    for (int i = 0; i < 22824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22824(int& val) {
    const int x = 22824;
    std::cout << 22824;
    for (int i = 0; i < 22824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22825(int& val) {
    func_in_22825(val);
    const int x = 22825;
    std::cout << 22825;
    for (int i = 0; i < 22825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22825(int& val) {
    const int x = 22825;
    std::cout << 22825;
    for (int i = 0; i < 22825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22826(int& val) {
    func_in_22826(val);
    const int x = 22826;
    std::cout << 22826;
    for (int i = 0; i < 22826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22826(int& val) {
    const int x = 22826;
    std::cout << 22826;
    for (int i = 0; i < 22826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22827(int& val) {
    func_in_22827(val);
    const int x = 22827;
    std::cout << 22827;
    for (int i = 0; i < 22827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22827(int& val) {
    const int x = 22827;
    std::cout << 22827;
    for (int i = 0; i < 22827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22828(int& val) {
    func_in_22828(val);
    const int x = 22828;
    std::cout << 22828;
    for (int i = 0; i < 22828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22828(int& val) {
    const int x = 22828;
    std::cout << 22828;
    for (int i = 0; i < 22828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22829(int& val) {
    func_in_22829(val);
    const int x = 22829;
    std::cout << 22829;
    for (int i = 0; i < 22829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22829(int& val) {
    const int x = 22829;
    std::cout << 22829;
    for (int i = 0; i < 22829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22830(int& val) {
    func_in_22830(val);
    const int x = 22830;
    std::cout << 22830;
    for (int i = 0; i < 22830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22830(int& val) {
    const int x = 22830;
    std::cout << 22830;
    for (int i = 0; i < 22830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22831(int& val) {
    func_in_22831(val);
    const int x = 22831;
    std::cout << 22831;
    for (int i = 0; i < 22831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22831(int& val) {
    const int x = 22831;
    std::cout << 22831;
    for (int i = 0; i < 22831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22832(int& val) {
    func_in_22832(val);
    const int x = 22832;
    std::cout << 22832;
    for (int i = 0; i < 22832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22832(int& val) {
    const int x = 22832;
    std::cout << 22832;
    for (int i = 0; i < 22832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22833(int& val) {
    func_in_22833(val);
    const int x = 22833;
    std::cout << 22833;
    for (int i = 0; i < 22833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22833(int& val) {
    const int x = 22833;
    std::cout << 22833;
    for (int i = 0; i < 22833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22834(int& val) {
    func_in_22834(val);
    const int x = 22834;
    std::cout << 22834;
    for (int i = 0; i < 22834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22834(int& val) {
    const int x = 22834;
    std::cout << 22834;
    for (int i = 0; i < 22834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22835(int& val) {
    func_in_22835(val);
    const int x = 22835;
    std::cout << 22835;
    for (int i = 0; i < 22835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22835(int& val) {
    const int x = 22835;
    std::cout << 22835;
    for (int i = 0; i < 22835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22836(int& val) {
    func_in_22836(val);
    const int x = 22836;
    std::cout << 22836;
    for (int i = 0; i < 22836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22836(int& val) {
    const int x = 22836;
    std::cout << 22836;
    for (int i = 0; i < 22836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22837(int& val) {
    func_in_22837(val);
    const int x = 22837;
    std::cout << 22837;
    for (int i = 0; i < 22837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22837(int& val) {
    const int x = 22837;
    std::cout << 22837;
    for (int i = 0; i < 22837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22838(int& val) {
    func_in_22838(val);
    const int x = 22838;
    std::cout << 22838;
    for (int i = 0; i < 22838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22838(int& val) {
    const int x = 22838;
    std::cout << 22838;
    for (int i = 0; i < 22838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22839(int& val) {
    func_in_22839(val);
    const int x = 22839;
    std::cout << 22839;
    for (int i = 0; i < 22839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22839(int& val) {
    const int x = 22839;
    std::cout << 22839;
    for (int i = 0; i < 22839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22840(int& val) {
    func_in_22840(val);
    const int x = 22840;
    std::cout << 22840;
    for (int i = 0; i < 22840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22840(int& val) {
    const int x = 22840;
    std::cout << 22840;
    for (int i = 0; i < 22840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22841(int& val) {
    func_in_22841(val);
    const int x = 22841;
    std::cout << 22841;
    for (int i = 0; i < 22841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22841(int& val) {
    const int x = 22841;
    std::cout << 22841;
    for (int i = 0; i < 22841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22842(int& val) {
    func_in_22842(val);
    const int x = 22842;
    std::cout << 22842;
    for (int i = 0; i < 22842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22842(int& val) {
    const int x = 22842;
    std::cout << 22842;
    for (int i = 0; i < 22842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22843(int& val) {
    func_in_22843(val);
    const int x = 22843;
    std::cout << 22843;
    for (int i = 0; i < 22843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22843(int& val) {
    const int x = 22843;
    std::cout << 22843;
    for (int i = 0; i < 22843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22844(int& val) {
    func_in_22844(val);
    const int x = 22844;
    std::cout << 22844;
    for (int i = 0; i < 22844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22844(int& val) {
    const int x = 22844;
    std::cout << 22844;
    for (int i = 0; i < 22844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22845(int& val) {
    func_in_22845(val);
    const int x = 22845;
    std::cout << 22845;
    for (int i = 0; i < 22845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22845(int& val) {
    const int x = 22845;
    std::cout << 22845;
    for (int i = 0; i < 22845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22846(int& val) {
    func_in_22846(val);
    const int x = 22846;
    std::cout << 22846;
    for (int i = 0; i < 22846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22846(int& val) {
    const int x = 22846;
    std::cout << 22846;
    for (int i = 0; i < 22846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22847(int& val) {
    func_in_22847(val);
    const int x = 22847;
    std::cout << 22847;
    for (int i = 0; i < 22847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22847(int& val) {
    const int x = 22847;
    std::cout << 22847;
    for (int i = 0; i < 22847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22848(int& val) {
    func_in_22848(val);
    const int x = 22848;
    std::cout << 22848;
    for (int i = 0; i < 22848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22848(int& val) {
    const int x = 22848;
    std::cout << 22848;
    for (int i = 0; i < 22848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22849(int& val) {
    func_in_22849(val);
    const int x = 22849;
    std::cout << 22849;
    for (int i = 0; i < 22849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22849(int& val) {
    const int x = 22849;
    std::cout << 22849;
    for (int i = 0; i < 22849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22850(int& val) {
    func_in_22850(val);
    const int x = 22850;
    std::cout << 22850;
    for (int i = 0; i < 22850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22850(int& val) {
    const int x = 22850;
    std::cout << 22850;
    for (int i = 0; i < 22850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22851(int& val) {
    func_in_22851(val);
    const int x = 22851;
    std::cout << 22851;
    for (int i = 0; i < 22851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22851(int& val) {
    const int x = 22851;
    std::cout << 22851;
    for (int i = 0; i < 22851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22852(int& val) {
    func_in_22852(val);
    const int x = 22852;
    std::cout << 22852;
    for (int i = 0; i < 22852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22852(int& val) {
    const int x = 22852;
    std::cout << 22852;
    for (int i = 0; i < 22852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22853(int& val) {
    func_in_22853(val);
    const int x = 22853;
    std::cout << 22853;
    for (int i = 0; i < 22853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22853(int& val) {
    const int x = 22853;
    std::cout << 22853;
    for (int i = 0; i < 22853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22854(int& val) {
    func_in_22854(val);
    const int x = 22854;
    std::cout << 22854;
    for (int i = 0; i < 22854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22854(int& val) {
    const int x = 22854;
    std::cout << 22854;
    for (int i = 0; i < 22854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22855(int& val) {
    func_in_22855(val);
    const int x = 22855;
    std::cout << 22855;
    for (int i = 0; i < 22855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22855(int& val) {
    const int x = 22855;
    std::cout << 22855;
    for (int i = 0; i < 22855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22856(int& val) {
    func_in_22856(val);
    const int x = 22856;
    std::cout << 22856;
    for (int i = 0; i < 22856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22856(int& val) {
    const int x = 22856;
    std::cout << 22856;
    for (int i = 0; i < 22856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22857(int& val) {
    func_in_22857(val);
    const int x = 22857;
    std::cout << 22857;
    for (int i = 0; i < 22857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22857(int& val) {
    const int x = 22857;
    std::cout << 22857;
    for (int i = 0; i < 22857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22858(int& val) {
    func_in_22858(val);
    const int x = 22858;
    std::cout << 22858;
    for (int i = 0; i < 22858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22858(int& val) {
    const int x = 22858;
    std::cout << 22858;
    for (int i = 0; i < 22858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22859(int& val) {
    func_in_22859(val);
    const int x = 22859;
    std::cout << 22859;
    for (int i = 0; i < 22859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22859(int& val) {
    const int x = 22859;
    std::cout << 22859;
    for (int i = 0; i < 22859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22860(int& val) {
    func_in_22860(val);
    const int x = 22860;
    std::cout << 22860;
    for (int i = 0; i < 22860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22860(int& val) {
    const int x = 22860;
    std::cout << 22860;
    for (int i = 0; i < 22860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22861(int& val) {
    func_in_22861(val);
    const int x = 22861;
    std::cout << 22861;
    for (int i = 0; i < 22861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22861(int& val) {
    const int x = 22861;
    std::cout << 22861;
    for (int i = 0; i < 22861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22862(int& val) {
    func_in_22862(val);
    const int x = 22862;
    std::cout << 22862;
    for (int i = 0; i < 22862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22862(int& val) {
    const int x = 22862;
    std::cout << 22862;
    for (int i = 0; i < 22862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22863(int& val) {
    func_in_22863(val);
    const int x = 22863;
    std::cout << 22863;
    for (int i = 0; i < 22863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22863(int& val) {
    const int x = 22863;
    std::cout << 22863;
    for (int i = 0; i < 22863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22864(int& val) {
    func_in_22864(val);
    const int x = 22864;
    std::cout << 22864;
    for (int i = 0; i < 22864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22864(int& val) {
    const int x = 22864;
    std::cout << 22864;
    for (int i = 0; i < 22864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22865(int& val) {
    func_in_22865(val);
    const int x = 22865;
    std::cout << 22865;
    for (int i = 0; i < 22865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22865(int& val) {
    const int x = 22865;
    std::cout << 22865;
    for (int i = 0; i < 22865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22866(int& val) {
    func_in_22866(val);
    const int x = 22866;
    std::cout << 22866;
    for (int i = 0; i < 22866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22866(int& val) {
    const int x = 22866;
    std::cout << 22866;
    for (int i = 0; i < 22866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22867(int& val) {
    func_in_22867(val);
    const int x = 22867;
    std::cout << 22867;
    for (int i = 0; i < 22867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22867(int& val) {
    const int x = 22867;
    std::cout << 22867;
    for (int i = 0; i < 22867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22868(int& val) {
    func_in_22868(val);
    const int x = 22868;
    std::cout << 22868;
    for (int i = 0; i < 22868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22868(int& val) {
    const int x = 22868;
    std::cout << 22868;
    for (int i = 0; i < 22868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22869(int& val) {
    func_in_22869(val);
    const int x = 22869;
    std::cout << 22869;
    for (int i = 0; i < 22869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22869(int& val) {
    const int x = 22869;
    std::cout << 22869;
    for (int i = 0; i < 22869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22870(int& val) {
    func_in_22870(val);
    const int x = 22870;
    std::cout << 22870;
    for (int i = 0; i < 22870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22870(int& val) {
    const int x = 22870;
    std::cout << 22870;
    for (int i = 0; i < 22870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22871(int& val) {
    func_in_22871(val);
    const int x = 22871;
    std::cout << 22871;
    for (int i = 0; i < 22871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22871(int& val) {
    const int x = 22871;
    std::cout << 22871;
    for (int i = 0; i < 22871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22872(int& val) {
    func_in_22872(val);
    const int x = 22872;
    std::cout << 22872;
    for (int i = 0; i < 22872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22872(int& val) {
    const int x = 22872;
    std::cout << 22872;
    for (int i = 0; i < 22872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22873(int& val) {
    func_in_22873(val);
    const int x = 22873;
    std::cout << 22873;
    for (int i = 0; i < 22873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22873(int& val) {
    const int x = 22873;
    std::cout << 22873;
    for (int i = 0; i < 22873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22874(int& val) {
    func_in_22874(val);
    const int x = 22874;
    std::cout << 22874;
    for (int i = 0; i < 22874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22874(int& val) {
    const int x = 22874;
    std::cout << 22874;
    for (int i = 0; i < 22874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22875(int& val) {
    func_in_22875(val);
    const int x = 22875;
    std::cout << 22875;
    for (int i = 0; i < 22875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22875(int& val) {
    const int x = 22875;
    std::cout << 22875;
    for (int i = 0; i < 22875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22876(int& val) {
    func_in_22876(val);
    const int x = 22876;
    std::cout << 22876;
    for (int i = 0; i < 22876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22876(int& val) {
    const int x = 22876;
    std::cout << 22876;
    for (int i = 0; i < 22876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22877(int& val) {
    func_in_22877(val);
    const int x = 22877;
    std::cout << 22877;
    for (int i = 0; i < 22877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22877(int& val) {
    const int x = 22877;
    std::cout << 22877;
    for (int i = 0; i < 22877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22878(int& val) {
    func_in_22878(val);
    const int x = 22878;
    std::cout << 22878;
    for (int i = 0; i < 22878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22878(int& val) {
    const int x = 22878;
    std::cout << 22878;
    for (int i = 0; i < 22878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22879(int& val) {
    func_in_22879(val);
    const int x = 22879;
    std::cout << 22879;
    for (int i = 0; i < 22879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22879(int& val) {
    const int x = 22879;
    std::cout << 22879;
    for (int i = 0; i < 22879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22880(int& val) {
    func_in_22880(val);
    const int x = 22880;
    std::cout << 22880;
    for (int i = 0; i < 22880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22880(int& val) {
    const int x = 22880;
    std::cout << 22880;
    for (int i = 0; i < 22880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22881(int& val) {
    func_in_22881(val);
    const int x = 22881;
    std::cout << 22881;
    for (int i = 0; i < 22881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22881(int& val) {
    const int x = 22881;
    std::cout << 22881;
    for (int i = 0; i < 22881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22882(int& val) {
    func_in_22882(val);
    const int x = 22882;
    std::cout << 22882;
    for (int i = 0; i < 22882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22882(int& val) {
    const int x = 22882;
    std::cout << 22882;
    for (int i = 0; i < 22882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22883(int& val) {
    func_in_22883(val);
    const int x = 22883;
    std::cout << 22883;
    for (int i = 0; i < 22883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22883(int& val) {
    const int x = 22883;
    std::cout << 22883;
    for (int i = 0; i < 22883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22884(int& val) {
    func_in_22884(val);
    const int x = 22884;
    std::cout << 22884;
    for (int i = 0; i < 22884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22884(int& val) {
    const int x = 22884;
    std::cout << 22884;
    for (int i = 0; i < 22884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22885(int& val) {
    func_in_22885(val);
    const int x = 22885;
    std::cout << 22885;
    for (int i = 0; i < 22885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22885(int& val) {
    const int x = 22885;
    std::cout << 22885;
    for (int i = 0; i < 22885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22886(int& val) {
    func_in_22886(val);
    const int x = 22886;
    std::cout << 22886;
    for (int i = 0; i < 22886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22886(int& val) {
    const int x = 22886;
    std::cout << 22886;
    for (int i = 0; i < 22886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22887(int& val) {
    func_in_22887(val);
    const int x = 22887;
    std::cout << 22887;
    for (int i = 0; i < 22887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22887(int& val) {
    const int x = 22887;
    std::cout << 22887;
    for (int i = 0; i < 22887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22888(int& val) {
    func_in_22888(val);
    const int x = 22888;
    std::cout << 22888;
    for (int i = 0; i < 22888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22888(int& val) {
    const int x = 22888;
    std::cout << 22888;
    for (int i = 0; i < 22888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22889(int& val) {
    func_in_22889(val);
    const int x = 22889;
    std::cout << 22889;
    for (int i = 0; i < 22889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22889(int& val) {
    const int x = 22889;
    std::cout << 22889;
    for (int i = 0; i < 22889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22890(int& val) {
    func_in_22890(val);
    const int x = 22890;
    std::cout << 22890;
    for (int i = 0; i < 22890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22890(int& val) {
    const int x = 22890;
    std::cout << 22890;
    for (int i = 0; i < 22890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22891(int& val) {
    func_in_22891(val);
    const int x = 22891;
    std::cout << 22891;
    for (int i = 0; i < 22891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22891(int& val) {
    const int x = 22891;
    std::cout << 22891;
    for (int i = 0; i < 22891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22892(int& val) {
    func_in_22892(val);
    const int x = 22892;
    std::cout << 22892;
    for (int i = 0; i < 22892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22892(int& val) {
    const int x = 22892;
    std::cout << 22892;
    for (int i = 0; i < 22892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22893(int& val) {
    func_in_22893(val);
    const int x = 22893;
    std::cout << 22893;
    for (int i = 0; i < 22893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22893(int& val) {
    const int x = 22893;
    std::cout << 22893;
    for (int i = 0; i < 22893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22894(int& val) {
    func_in_22894(val);
    const int x = 22894;
    std::cout << 22894;
    for (int i = 0; i < 22894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22894(int& val) {
    const int x = 22894;
    std::cout << 22894;
    for (int i = 0; i < 22894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22895(int& val) {
    func_in_22895(val);
    const int x = 22895;
    std::cout << 22895;
    for (int i = 0; i < 22895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22895(int& val) {
    const int x = 22895;
    std::cout << 22895;
    for (int i = 0; i < 22895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22896(int& val) {
    func_in_22896(val);
    const int x = 22896;
    std::cout << 22896;
    for (int i = 0; i < 22896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22896(int& val) {
    const int x = 22896;
    std::cout << 22896;
    for (int i = 0; i < 22896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22897(int& val) {
    func_in_22897(val);
    const int x = 22897;
    std::cout << 22897;
    for (int i = 0; i < 22897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22897(int& val) {
    const int x = 22897;
    std::cout << 22897;
    for (int i = 0; i < 22897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22898(int& val) {
    func_in_22898(val);
    const int x = 22898;
    std::cout << 22898;
    for (int i = 0; i < 22898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22898(int& val) {
    const int x = 22898;
    std::cout << 22898;
    for (int i = 0; i < 22898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22899(int& val) {
    func_in_22899(val);
    const int x = 22899;
    std::cout << 22899;
    for (int i = 0; i < 22899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22899(int& val) {
    const int x = 22899;
    std::cout << 22899;
    for (int i = 0; i < 22899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22900(int& val) {
    func_in_22900(val);
    const int x = 22900;
    std::cout << 22900;
    for (int i = 0; i < 22900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22900(int& val) {
    const int x = 22900;
    std::cout << 22900;
    for (int i = 0; i < 22900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22901(int& val) {
    func_in_22901(val);
    const int x = 22901;
    std::cout << 22901;
    for (int i = 0; i < 22901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22901(int& val) {
    const int x = 22901;
    std::cout << 22901;
    for (int i = 0; i < 22901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22902(int& val) {
    func_in_22902(val);
    const int x = 22902;
    std::cout << 22902;
    for (int i = 0; i < 22902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22902(int& val) {
    const int x = 22902;
    std::cout << 22902;
    for (int i = 0; i < 22902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22903(int& val) {
    func_in_22903(val);
    const int x = 22903;
    std::cout << 22903;
    for (int i = 0; i < 22903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22903(int& val) {
    const int x = 22903;
    std::cout << 22903;
    for (int i = 0; i < 22903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22904(int& val) {
    func_in_22904(val);
    const int x = 22904;
    std::cout << 22904;
    for (int i = 0; i < 22904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22904(int& val) {
    const int x = 22904;
    std::cout << 22904;
    for (int i = 0; i < 22904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22905(int& val) {
    func_in_22905(val);
    const int x = 22905;
    std::cout << 22905;
    for (int i = 0; i < 22905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22905(int& val) {
    const int x = 22905;
    std::cout << 22905;
    for (int i = 0; i < 22905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22906(int& val) {
    func_in_22906(val);
    const int x = 22906;
    std::cout << 22906;
    for (int i = 0; i < 22906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22906(int& val) {
    const int x = 22906;
    std::cout << 22906;
    for (int i = 0; i < 22906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22907(int& val) {
    func_in_22907(val);
    const int x = 22907;
    std::cout << 22907;
    for (int i = 0; i < 22907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22907(int& val) {
    const int x = 22907;
    std::cout << 22907;
    for (int i = 0; i < 22907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22908(int& val) {
    func_in_22908(val);
    const int x = 22908;
    std::cout << 22908;
    for (int i = 0; i < 22908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22908(int& val) {
    const int x = 22908;
    std::cout << 22908;
    for (int i = 0; i < 22908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22909(int& val) {
    func_in_22909(val);
    const int x = 22909;
    std::cout << 22909;
    for (int i = 0; i < 22909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22909(int& val) {
    const int x = 22909;
    std::cout << 22909;
    for (int i = 0; i < 22909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22910(int& val) {
    func_in_22910(val);
    const int x = 22910;
    std::cout << 22910;
    for (int i = 0; i < 22910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22910(int& val) {
    const int x = 22910;
    std::cout << 22910;
    for (int i = 0; i < 22910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22911(int& val) {
    func_in_22911(val);
    const int x = 22911;
    std::cout << 22911;
    for (int i = 0; i < 22911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22911(int& val) {
    const int x = 22911;
    std::cout << 22911;
    for (int i = 0; i < 22911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22912(int& val) {
    func_in_22912(val);
    const int x = 22912;
    std::cout << 22912;
    for (int i = 0; i < 22912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22912(int& val) {
    const int x = 22912;
    std::cout << 22912;
    for (int i = 0; i < 22912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22913(int& val) {
    func_in_22913(val);
    const int x = 22913;
    std::cout << 22913;
    for (int i = 0; i < 22913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22913(int& val) {
    const int x = 22913;
    std::cout << 22913;
    for (int i = 0; i < 22913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22914(int& val) {
    func_in_22914(val);
    const int x = 22914;
    std::cout << 22914;
    for (int i = 0; i < 22914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22914(int& val) {
    const int x = 22914;
    std::cout << 22914;
    for (int i = 0; i < 22914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22915(int& val) {
    func_in_22915(val);
    const int x = 22915;
    std::cout << 22915;
    for (int i = 0; i < 22915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22915(int& val) {
    const int x = 22915;
    std::cout << 22915;
    for (int i = 0; i < 22915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22916(int& val) {
    func_in_22916(val);
    const int x = 22916;
    std::cout << 22916;
    for (int i = 0; i < 22916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22916(int& val) {
    const int x = 22916;
    std::cout << 22916;
    for (int i = 0; i < 22916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22917(int& val) {
    func_in_22917(val);
    const int x = 22917;
    std::cout << 22917;
    for (int i = 0; i < 22917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22917(int& val) {
    const int x = 22917;
    std::cout << 22917;
    for (int i = 0; i < 22917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22918(int& val) {
    func_in_22918(val);
    const int x = 22918;
    std::cout << 22918;
    for (int i = 0; i < 22918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22918(int& val) {
    const int x = 22918;
    std::cout << 22918;
    for (int i = 0; i < 22918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22919(int& val) {
    func_in_22919(val);
    const int x = 22919;
    std::cout << 22919;
    for (int i = 0; i < 22919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22919(int& val) {
    const int x = 22919;
    std::cout << 22919;
    for (int i = 0; i < 22919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22920(int& val) {
    func_in_22920(val);
    const int x = 22920;
    std::cout << 22920;
    for (int i = 0; i < 22920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22920(int& val) {
    const int x = 22920;
    std::cout << 22920;
    for (int i = 0; i < 22920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22921(int& val) {
    func_in_22921(val);
    const int x = 22921;
    std::cout << 22921;
    for (int i = 0; i < 22921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22921(int& val) {
    const int x = 22921;
    std::cout << 22921;
    for (int i = 0; i < 22921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22922(int& val) {
    func_in_22922(val);
    const int x = 22922;
    std::cout << 22922;
    for (int i = 0; i < 22922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22922(int& val) {
    const int x = 22922;
    std::cout << 22922;
    for (int i = 0; i < 22922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22923(int& val) {
    func_in_22923(val);
    const int x = 22923;
    std::cout << 22923;
    for (int i = 0; i < 22923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22923(int& val) {
    const int x = 22923;
    std::cout << 22923;
    for (int i = 0; i < 22923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22924(int& val) {
    func_in_22924(val);
    const int x = 22924;
    std::cout << 22924;
    for (int i = 0; i < 22924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22924(int& val) {
    const int x = 22924;
    std::cout << 22924;
    for (int i = 0; i < 22924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22925(int& val) {
    func_in_22925(val);
    const int x = 22925;
    std::cout << 22925;
    for (int i = 0; i < 22925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22925(int& val) {
    const int x = 22925;
    std::cout << 22925;
    for (int i = 0; i < 22925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22926(int& val) {
    func_in_22926(val);
    const int x = 22926;
    std::cout << 22926;
    for (int i = 0; i < 22926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22926(int& val) {
    const int x = 22926;
    std::cout << 22926;
    for (int i = 0; i < 22926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22927(int& val) {
    func_in_22927(val);
    const int x = 22927;
    std::cout << 22927;
    for (int i = 0; i < 22927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22927(int& val) {
    const int x = 22927;
    std::cout << 22927;
    for (int i = 0; i < 22927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22928(int& val) {
    func_in_22928(val);
    const int x = 22928;
    std::cout << 22928;
    for (int i = 0; i < 22928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22928(int& val) {
    const int x = 22928;
    std::cout << 22928;
    for (int i = 0; i < 22928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22929(int& val) {
    func_in_22929(val);
    const int x = 22929;
    std::cout << 22929;
    for (int i = 0; i < 22929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22929(int& val) {
    const int x = 22929;
    std::cout << 22929;
    for (int i = 0; i < 22929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22930(int& val) {
    func_in_22930(val);
    const int x = 22930;
    std::cout << 22930;
    for (int i = 0; i < 22930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22930(int& val) {
    const int x = 22930;
    std::cout << 22930;
    for (int i = 0; i < 22930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22931(int& val) {
    func_in_22931(val);
    const int x = 22931;
    std::cout << 22931;
    for (int i = 0; i < 22931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22931(int& val) {
    const int x = 22931;
    std::cout << 22931;
    for (int i = 0; i < 22931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22932(int& val) {
    func_in_22932(val);
    const int x = 22932;
    std::cout << 22932;
    for (int i = 0; i < 22932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22932(int& val) {
    const int x = 22932;
    std::cout << 22932;
    for (int i = 0; i < 22932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22933(int& val) {
    func_in_22933(val);
    const int x = 22933;
    std::cout << 22933;
    for (int i = 0; i < 22933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22933(int& val) {
    const int x = 22933;
    std::cout << 22933;
    for (int i = 0; i < 22933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22934(int& val) {
    func_in_22934(val);
    const int x = 22934;
    std::cout << 22934;
    for (int i = 0; i < 22934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22934(int& val) {
    const int x = 22934;
    std::cout << 22934;
    for (int i = 0; i < 22934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22935(int& val) {
    func_in_22935(val);
    const int x = 22935;
    std::cout << 22935;
    for (int i = 0; i < 22935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22935(int& val) {
    const int x = 22935;
    std::cout << 22935;
    for (int i = 0; i < 22935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22936(int& val) {
    func_in_22936(val);
    const int x = 22936;
    std::cout << 22936;
    for (int i = 0; i < 22936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22936(int& val) {
    const int x = 22936;
    std::cout << 22936;
    for (int i = 0; i < 22936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22937(int& val) {
    func_in_22937(val);
    const int x = 22937;
    std::cout << 22937;
    for (int i = 0; i < 22937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22937(int& val) {
    const int x = 22937;
    std::cout << 22937;
    for (int i = 0; i < 22937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22938(int& val) {
    func_in_22938(val);
    const int x = 22938;
    std::cout << 22938;
    for (int i = 0; i < 22938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22938(int& val) {
    const int x = 22938;
    std::cout << 22938;
    for (int i = 0; i < 22938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22939(int& val) {
    func_in_22939(val);
    const int x = 22939;
    std::cout << 22939;
    for (int i = 0; i < 22939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22939(int& val) {
    const int x = 22939;
    std::cout << 22939;
    for (int i = 0; i < 22939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22940(int& val) {
    func_in_22940(val);
    const int x = 22940;
    std::cout << 22940;
    for (int i = 0; i < 22940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22940(int& val) {
    const int x = 22940;
    std::cout << 22940;
    for (int i = 0; i < 22940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22941(int& val) {
    func_in_22941(val);
    const int x = 22941;
    std::cout << 22941;
    for (int i = 0; i < 22941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22941(int& val) {
    const int x = 22941;
    std::cout << 22941;
    for (int i = 0; i < 22941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22942(int& val) {
    func_in_22942(val);
    const int x = 22942;
    std::cout << 22942;
    for (int i = 0; i < 22942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22942(int& val) {
    const int x = 22942;
    std::cout << 22942;
    for (int i = 0; i < 22942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22943(int& val) {
    func_in_22943(val);
    const int x = 22943;
    std::cout << 22943;
    for (int i = 0; i < 22943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22943(int& val) {
    const int x = 22943;
    std::cout << 22943;
    for (int i = 0; i < 22943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22944(int& val) {
    func_in_22944(val);
    const int x = 22944;
    std::cout << 22944;
    for (int i = 0; i < 22944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22944(int& val) {
    const int x = 22944;
    std::cout << 22944;
    for (int i = 0; i < 22944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22945(int& val) {
    func_in_22945(val);
    const int x = 22945;
    std::cout << 22945;
    for (int i = 0; i < 22945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22945(int& val) {
    const int x = 22945;
    std::cout << 22945;
    for (int i = 0; i < 22945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22946(int& val) {
    func_in_22946(val);
    const int x = 22946;
    std::cout << 22946;
    for (int i = 0; i < 22946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22946(int& val) {
    const int x = 22946;
    std::cout << 22946;
    for (int i = 0; i < 22946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22947(int& val) {
    func_in_22947(val);
    const int x = 22947;
    std::cout << 22947;
    for (int i = 0; i < 22947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22947(int& val) {
    const int x = 22947;
    std::cout << 22947;
    for (int i = 0; i < 22947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22948(int& val) {
    func_in_22948(val);
    const int x = 22948;
    std::cout << 22948;
    for (int i = 0; i < 22948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22948(int& val) {
    const int x = 22948;
    std::cout << 22948;
    for (int i = 0; i < 22948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22949(int& val) {
    func_in_22949(val);
    const int x = 22949;
    std::cout << 22949;
    for (int i = 0; i < 22949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22949(int& val) {
    const int x = 22949;
    std::cout << 22949;
    for (int i = 0; i < 22949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22950(int& val) {
    func_in_22950(val);
    const int x = 22950;
    std::cout << 22950;
    for (int i = 0; i < 22950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22950(int& val) {
    const int x = 22950;
    std::cout << 22950;
    for (int i = 0; i < 22950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22951(int& val) {
    func_in_22951(val);
    const int x = 22951;
    std::cout << 22951;
    for (int i = 0; i < 22951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22951(int& val) {
    const int x = 22951;
    std::cout << 22951;
    for (int i = 0; i < 22951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22952(int& val) {
    func_in_22952(val);
    const int x = 22952;
    std::cout << 22952;
    for (int i = 0; i < 22952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22952(int& val) {
    const int x = 22952;
    std::cout << 22952;
    for (int i = 0; i < 22952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22953(int& val) {
    func_in_22953(val);
    const int x = 22953;
    std::cout << 22953;
    for (int i = 0; i < 22953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22953(int& val) {
    const int x = 22953;
    std::cout << 22953;
    for (int i = 0; i < 22953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22954(int& val) {
    func_in_22954(val);
    const int x = 22954;
    std::cout << 22954;
    for (int i = 0; i < 22954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22954(int& val) {
    const int x = 22954;
    std::cout << 22954;
    for (int i = 0; i < 22954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22955(int& val) {
    func_in_22955(val);
    const int x = 22955;
    std::cout << 22955;
    for (int i = 0; i < 22955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22955(int& val) {
    const int x = 22955;
    std::cout << 22955;
    for (int i = 0; i < 22955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22956(int& val) {
    func_in_22956(val);
    const int x = 22956;
    std::cout << 22956;
    for (int i = 0; i < 22956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22956(int& val) {
    const int x = 22956;
    std::cout << 22956;
    for (int i = 0; i < 22956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22957(int& val) {
    func_in_22957(val);
    const int x = 22957;
    std::cout << 22957;
    for (int i = 0; i < 22957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22957(int& val) {
    const int x = 22957;
    std::cout << 22957;
    for (int i = 0; i < 22957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22958(int& val) {
    func_in_22958(val);
    const int x = 22958;
    std::cout << 22958;
    for (int i = 0; i < 22958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22958(int& val) {
    const int x = 22958;
    std::cout << 22958;
    for (int i = 0; i < 22958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22959(int& val) {
    func_in_22959(val);
    const int x = 22959;
    std::cout << 22959;
    for (int i = 0; i < 22959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22959(int& val) {
    const int x = 22959;
    std::cout << 22959;
    for (int i = 0; i < 22959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22960(int& val) {
    func_in_22960(val);
    const int x = 22960;
    std::cout << 22960;
    for (int i = 0; i < 22960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22960(int& val) {
    const int x = 22960;
    std::cout << 22960;
    for (int i = 0; i < 22960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22961(int& val) {
    func_in_22961(val);
    const int x = 22961;
    std::cout << 22961;
    for (int i = 0; i < 22961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22961(int& val) {
    const int x = 22961;
    std::cout << 22961;
    for (int i = 0; i < 22961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22962(int& val) {
    func_in_22962(val);
    const int x = 22962;
    std::cout << 22962;
    for (int i = 0; i < 22962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22962(int& val) {
    const int x = 22962;
    std::cout << 22962;
    for (int i = 0; i < 22962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22963(int& val) {
    func_in_22963(val);
    const int x = 22963;
    std::cout << 22963;
    for (int i = 0; i < 22963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22963(int& val) {
    const int x = 22963;
    std::cout << 22963;
    for (int i = 0; i < 22963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22964(int& val) {
    func_in_22964(val);
    const int x = 22964;
    std::cout << 22964;
    for (int i = 0; i < 22964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22964(int& val) {
    const int x = 22964;
    std::cout << 22964;
    for (int i = 0; i < 22964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22965(int& val) {
    func_in_22965(val);
    const int x = 22965;
    std::cout << 22965;
    for (int i = 0; i < 22965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22965(int& val) {
    const int x = 22965;
    std::cout << 22965;
    for (int i = 0; i < 22965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22966(int& val) {
    func_in_22966(val);
    const int x = 22966;
    std::cout << 22966;
    for (int i = 0; i < 22966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22966(int& val) {
    const int x = 22966;
    std::cout << 22966;
    for (int i = 0; i < 22966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22967(int& val) {
    func_in_22967(val);
    const int x = 22967;
    std::cout << 22967;
    for (int i = 0; i < 22967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22967(int& val) {
    const int x = 22967;
    std::cout << 22967;
    for (int i = 0; i < 22967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22968(int& val) {
    func_in_22968(val);
    const int x = 22968;
    std::cout << 22968;
    for (int i = 0; i < 22968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22968(int& val) {
    const int x = 22968;
    std::cout << 22968;
    for (int i = 0; i < 22968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22969(int& val) {
    func_in_22969(val);
    const int x = 22969;
    std::cout << 22969;
    for (int i = 0; i < 22969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22969(int& val) {
    const int x = 22969;
    std::cout << 22969;
    for (int i = 0; i < 22969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22970(int& val) {
    func_in_22970(val);
    const int x = 22970;
    std::cout << 22970;
    for (int i = 0; i < 22970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22970(int& val) {
    const int x = 22970;
    std::cout << 22970;
    for (int i = 0; i < 22970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22971(int& val) {
    func_in_22971(val);
    const int x = 22971;
    std::cout << 22971;
    for (int i = 0; i < 22971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22971(int& val) {
    const int x = 22971;
    std::cout << 22971;
    for (int i = 0; i < 22971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22972(int& val) {
    func_in_22972(val);
    const int x = 22972;
    std::cout << 22972;
    for (int i = 0; i < 22972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22972(int& val) {
    const int x = 22972;
    std::cout << 22972;
    for (int i = 0; i < 22972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22973(int& val) {
    func_in_22973(val);
    const int x = 22973;
    std::cout << 22973;
    for (int i = 0; i < 22973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22973(int& val) {
    const int x = 22973;
    std::cout << 22973;
    for (int i = 0; i < 22973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22974(int& val) {
    func_in_22974(val);
    const int x = 22974;
    std::cout << 22974;
    for (int i = 0; i < 22974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22974(int& val) {
    const int x = 22974;
    std::cout << 22974;
    for (int i = 0; i < 22974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22975(int& val) {
    func_in_22975(val);
    const int x = 22975;
    std::cout << 22975;
    for (int i = 0; i < 22975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22975(int& val) {
    const int x = 22975;
    std::cout << 22975;
    for (int i = 0; i < 22975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22976(int& val) {
    func_in_22976(val);
    const int x = 22976;
    std::cout << 22976;
    for (int i = 0; i < 22976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22976(int& val) {
    const int x = 22976;
    std::cout << 22976;
    for (int i = 0; i < 22976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22977(int& val) {
    func_in_22977(val);
    const int x = 22977;
    std::cout << 22977;
    for (int i = 0; i < 22977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22977(int& val) {
    const int x = 22977;
    std::cout << 22977;
    for (int i = 0; i < 22977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22978(int& val) {
    func_in_22978(val);
    const int x = 22978;
    std::cout << 22978;
    for (int i = 0; i < 22978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22978(int& val) {
    const int x = 22978;
    std::cout << 22978;
    for (int i = 0; i < 22978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22979(int& val) {
    func_in_22979(val);
    const int x = 22979;
    std::cout << 22979;
    for (int i = 0; i < 22979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22979(int& val) {
    const int x = 22979;
    std::cout << 22979;
    for (int i = 0; i < 22979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22980(int& val) {
    func_in_22980(val);
    const int x = 22980;
    std::cout << 22980;
    for (int i = 0; i < 22980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22980(int& val) {
    const int x = 22980;
    std::cout << 22980;
    for (int i = 0; i < 22980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22981(int& val) {
    func_in_22981(val);
    const int x = 22981;
    std::cout << 22981;
    for (int i = 0; i < 22981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22981(int& val) {
    const int x = 22981;
    std::cout << 22981;
    for (int i = 0; i < 22981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22982(int& val) {
    func_in_22982(val);
    const int x = 22982;
    std::cout << 22982;
    for (int i = 0; i < 22982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22982(int& val) {
    const int x = 22982;
    std::cout << 22982;
    for (int i = 0; i < 22982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22983(int& val) {
    func_in_22983(val);
    const int x = 22983;
    std::cout << 22983;
    for (int i = 0; i < 22983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22983(int& val) {
    const int x = 22983;
    std::cout << 22983;
    for (int i = 0; i < 22983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22984(int& val) {
    func_in_22984(val);
    const int x = 22984;
    std::cout << 22984;
    for (int i = 0; i < 22984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22984(int& val) {
    const int x = 22984;
    std::cout << 22984;
    for (int i = 0; i < 22984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22985(int& val) {
    func_in_22985(val);
    const int x = 22985;
    std::cout << 22985;
    for (int i = 0; i < 22985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22985(int& val) {
    const int x = 22985;
    std::cout << 22985;
    for (int i = 0; i < 22985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22986(int& val) {
    func_in_22986(val);
    const int x = 22986;
    std::cout << 22986;
    for (int i = 0; i < 22986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22986(int& val) {
    const int x = 22986;
    std::cout << 22986;
    for (int i = 0; i < 22986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22987(int& val) {
    func_in_22987(val);
    const int x = 22987;
    std::cout << 22987;
    for (int i = 0; i < 22987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22987(int& val) {
    const int x = 22987;
    std::cout << 22987;
    for (int i = 0; i < 22987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22988(int& val) {
    func_in_22988(val);
    const int x = 22988;
    std::cout << 22988;
    for (int i = 0; i < 22988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22988(int& val) {
    const int x = 22988;
    std::cout << 22988;
    for (int i = 0; i < 22988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22989(int& val) {
    func_in_22989(val);
    const int x = 22989;
    std::cout << 22989;
    for (int i = 0; i < 22989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22989(int& val) {
    const int x = 22989;
    std::cout << 22989;
    for (int i = 0; i < 22989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22990(int& val) {
    func_in_22990(val);
    const int x = 22990;
    std::cout << 22990;
    for (int i = 0; i < 22990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22990(int& val) {
    const int x = 22990;
    std::cout << 22990;
    for (int i = 0; i < 22990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22991(int& val) {
    func_in_22991(val);
    const int x = 22991;
    std::cout << 22991;
    for (int i = 0; i < 22991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22991(int& val) {
    const int x = 22991;
    std::cout << 22991;
    for (int i = 0; i < 22991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22992(int& val) {
    func_in_22992(val);
    const int x = 22992;
    std::cout << 22992;
    for (int i = 0; i < 22992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22992(int& val) {
    const int x = 22992;
    std::cout << 22992;
    for (int i = 0; i < 22992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22993(int& val) {
    func_in_22993(val);
    const int x = 22993;
    std::cout << 22993;
    for (int i = 0; i < 22993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22993(int& val) {
    const int x = 22993;
    std::cout << 22993;
    for (int i = 0; i < 22993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22994(int& val) {
    func_in_22994(val);
    const int x = 22994;
    std::cout << 22994;
    for (int i = 0; i < 22994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22994(int& val) {
    const int x = 22994;
    std::cout << 22994;
    for (int i = 0; i < 22994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22995(int& val) {
    func_in_22995(val);
    const int x = 22995;
    std::cout << 22995;
    for (int i = 0; i < 22995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22995(int& val) {
    const int x = 22995;
    std::cout << 22995;
    for (int i = 0; i < 22995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22996(int& val) {
    func_in_22996(val);
    const int x = 22996;
    std::cout << 22996;
    for (int i = 0; i < 22996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22996(int& val) {
    const int x = 22996;
    std::cout << 22996;
    for (int i = 0; i < 22996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22997(int& val) {
    func_in_22997(val);
    const int x = 22997;
    std::cout << 22997;
    for (int i = 0; i < 22997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22997(int& val) {
    const int x = 22997;
    std::cout << 22997;
    for (int i = 0; i < 22997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22998(int& val) {
    func_in_22998(val);
    const int x = 22998;
    std::cout << 22998;
    for (int i = 0; i < 22998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22998(int& val) {
    const int x = 22998;
    std::cout << 22998;
    for (int i = 0; i < 22998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22999(int& val) {
    func_in_22999(val);
    const int x = 22999;
    std::cout << 22999;
    for (int i = 0; i < 22999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22999(int& val) {
    const int x = 22999;
    std::cout << 22999;
    for (int i = 0; i < 22999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23000(int& val) {
    func_in_23000(val);
    const int x = 23000;
    std::cout << 23000;
    for (int i = 0; i < 23000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23000(int& val) {
    const int x = 23000;
    std::cout << 23000;
    for (int i = 0; i < 23000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23001(int& val) {
    func_in_23001(val);
    const int x = 23001;
    std::cout << 23001;
    for (int i = 0; i < 23001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23001(int& val) {
    const int x = 23001;
    std::cout << 23001;
    for (int i = 0; i < 23001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23002(int& val) {
    func_in_23002(val);
    const int x = 23002;
    std::cout << 23002;
    for (int i = 0; i < 23002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23002(int& val) {
    const int x = 23002;
    std::cout << 23002;
    for (int i = 0; i < 23002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23003(int& val) {
    func_in_23003(val);
    const int x = 23003;
    std::cout << 23003;
    for (int i = 0; i < 23003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23003(int& val) {
    const int x = 23003;
    std::cout << 23003;
    for (int i = 0; i < 23003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23004(int& val) {
    func_in_23004(val);
    const int x = 23004;
    std::cout << 23004;
    for (int i = 0; i < 23004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23004(int& val) {
    const int x = 23004;
    std::cout << 23004;
    for (int i = 0; i < 23004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23005(int& val) {
    func_in_23005(val);
    const int x = 23005;
    std::cout << 23005;
    for (int i = 0; i < 23005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23005(int& val) {
    const int x = 23005;
    std::cout << 23005;
    for (int i = 0; i < 23005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23006(int& val) {
    func_in_23006(val);
    const int x = 23006;
    std::cout << 23006;
    for (int i = 0; i < 23006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23006(int& val) {
    const int x = 23006;
    std::cout << 23006;
    for (int i = 0; i < 23006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23007(int& val) {
    func_in_23007(val);
    const int x = 23007;
    std::cout << 23007;
    for (int i = 0; i < 23007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23007(int& val) {
    const int x = 23007;
    std::cout << 23007;
    for (int i = 0; i < 23007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23008(int& val) {
    func_in_23008(val);
    const int x = 23008;
    std::cout << 23008;
    for (int i = 0; i < 23008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23008(int& val) {
    const int x = 23008;
    std::cout << 23008;
    for (int i = 0; i < 23008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23009(int& val) {
    func_in_23009(val);
    const int x = 23009;
    std::cout << 23009;
    for (int i = 0; i < 23009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23009(int& val) {
    const int x = 23009;
    std::cout << 23009;
    for (int i = 0; i < 23009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23010(int& val) {
    func_in_23010(val);
    const int x = 23010;
    std::cout << 23010;
    for (int i = 0; i < 23010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23010(int& val) {
    const int x = 23010;
    std::cout << 23010;
    for (int i = 0; i < 23010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23011(int& val) {
    func_in_23011(val);
    const int x = 23011;
    std::cout << 23011;
    for (int i = 0; i < 23011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23011(int& val) {
    const int x = 23011;
    std::cout << 23011;
    for (int i = 0; i < 23011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23012(int& val) {
    func_in_23012(val);
    const int x = 23012;
    std::cout << 23012;
    for (int i = 0; i < 23012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23012(int& val) {
    const int x = 23012;
    std::cout << 23012;
    for (int i = 0; i < 23012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23013(int& val) {
    func_in_23013(val);
    const int x = 23013;
    std::cout << 23013;
    for (int i = 0; i < 23013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23013(int& val) {
    const int x = 23013;
    std::cout << 23013;
    for (int i = 0; i < 23013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23014(int& val) {
    func_in_23014(val);
    const int x = 23014;
    std::cout << 23014;
    for (int i = 0; i < 23014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23014(int& val) {
    const int x = 23014;
    std::cout << 23014;
    for (int i = 0; i < 23014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23015(int& val) {
    func_in_23015(val);
    const int x = 23015;
    std::cout << 23015;
    for (int i = 0; i < 23015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23015(int& val) {
    const int x = 23015;
    std::cout << 23015;
    for (int i = 0; i < 23015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23016(int& val) {
    func_in_23016(val);
    const int x = 23016;
    std::cout << 23016;
    for (int i = 0; i < 23016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23016(int& val) {
    const int x = 23016;
    std::cout << 23016;
    for (int i = 0; i < 23016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23017(int& val) {
    func_in_23017(val);
    const int x = 23017;
    std::cout << 23017;
    for (int i = 0; i < 23017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23017(int& val) {
    const int x = 23017;
    std::cout << 23017;
    for (int i = 0; i < 23017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23018(int& val) {
    func_in_23018(val);
    const int x = 23018;
    std::cout << 23018;
    for (int i = 0; i < 23018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23018(int& val) {
    const int x = 23018;
    std::cout << 23018;
    for (int i = 0; i < 23018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23019(int& val) {
    func_in_23019(val);
    const int x = 23019;
    std::cout << 23019;
    for (int i = 0; i < 23019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23019(int& val) {
    const int x = 23019;
    std::cout << 23019;
    for (int i = 0; i < 23019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23020(int& val) {
    func_in_23020(val);
    const int x = 23020;
    std::cout << 23020;
    for (int i = 0; i < 23020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23020(int& val) {
    const int x = 23020;
    std::cout << 23020;
    for (int i = 0; i < 23020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23021(int& val) {
    func_in_23021(val);
    const int x = 23021;
    std::cout << 23021;
    for (int i = 0; i < 23021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23021(int& val) {
    const int x = 23021;
    std::cout << 23021;
    for (int i = 0; i < 23021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23022(int& val) {
    func_in_23022(val);
    const int x = 23022;
    std::cout << 23022;
    for (int i = 0; i < 23022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23022(int& val) {
    const int x = 23022;
    std::cout << 23022;
    for (int i = 0; i < 23022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23023(int& val) {
    func_in_23023(val);
    const int x = 23023;
    std::cout << 23023;
    for (int i = 0; i < 23023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23023(int& val) {
    const int x = 23023;
    std::cout << 23023;
    for (int i = 0; i < 23023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23024(int& val) {
    func_in_23024(val);
    const int x = 23024;
    std::cout << 23024;
    for (int i = 0; i < 23024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23024(int& val) {
    const int x = 23024;
    std::cout << 23024;
    for (int i = 0; i < 23024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23025(int& val) {
    func_in_23025(val);
    const int x = 23025;
    std::cout << 23025;
    for (int i = 0; i < 23025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23025(int& val) {
    const int x = 23025;
    std::cout << 23025;
    for (int i = 0; i < 23025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23026(int& val) {
    func_in_23026(val);
    const int x = 23026;
    std::cout << 23026;
    for (int i = 0; i < 23026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23026(int& val) {
    const int x = 23026;
    std::cout << 23026;
    for (int i = 0; i < 23026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23027(int& val) {
    func_in_23027(val);
    const int x = 23027;
    std::cout << 23027;
    for (int i = 0; i < 23027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23027(int& val) {
    const int x = 23027;
    std::cout << 23027;
    for (int i = 0; i < 23027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23028(int& val) {
    func_in_23028(val);
    const int x = 23028;
    std::cout << 23028;
    for (int i = 0; i < 23028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23028(int& val) {
    const int x = 23028;
    std::cout << 23028;
    for (int i = 0; i < 23028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23029(int& val) {
    func_in_23029(val);
    const int x = 23029;
    std::cout << 23029;
    for (int i = 0; i < 23029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23029(int& val) {
    const int x = 23029;
    std::cout << 23029;
    for (int i = 0; i < 23029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23030(int& val) {
    func_in_23030(val);
    const int x = 23030;
    std::cout << 23030;
    for (int i = 0; i < 23030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23030(int& val) {
    const int x = 23030;
    std::cout << 23030;
    for (int i = 0; i < 23030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23031(int& val) {
    func_in_23031(val);
    const int x = 23031;
    std::cout << 23031;
    for (int i = 0; i < 23031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23031(int& val) {
    const int x = 23031;
    std::cout << 23031;
    for (int i = 0; i < 23031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23032(int& val) {
    func_in_23032(val);
    const int x = 23032;
    std::cout << 23032;
    for (int i = 0; i < 23032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23032(int& val) {
    const int x = 23032;
    std::cout << 23032;
    for (int i = 0; i < 23032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23033(int& val) {
    func_in_23033(val);
    const int x = 23033;
    std::cout << 23033;
    for (int i = 0; i < 23033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23033(int& val) {
    const int x = 23033;
    std::cout << 23033;
    for (int i = 0; i < 23033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23034(int& val) {
    func_in_23034(val);
    const int x = 23034;
    std::cout << 23034;
    for (int i = 0; i < 23034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23034(int& val) {
    const int x = 23034;
    std::cout << 23034;
    for (int i = 0; i < 23034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23035(int& val) {
    func_in_23035(val);
    const int x = 23035;
    std::cout << 23035;
    for (int i = 0; i < 23035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23035(int& val) {
    const int x = 23035;
    std::cout << 23035;
    for (int i = 0; i < 23035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23036(int& val) {
    func_in_23036(val);
    const int x = 23036;
    std::cout << 23036;
    for (int i = 0; i < 23036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23036(int& val) {
    const int x = 23036;
    std::cout << 23036;
    for (int i = 0; i < 23036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23037(int& val) {
    func_in_23037(val);
    const int x = 23037;
    std::cout << 23037;
    for (int i = 0; i < 23037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23037(int& val) {
    const int x = 23037;
    std::cout << 23037;
    for (int i = 0; i < 23037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23038(int& val) {
    func_in_23038(val);
    const int x = 23038;
    std::cout << 23038;
    for (int i = 0; i < 23038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23038(int& val) {
    const int x = 23038;
    std::cout << 23038;
    for (int i = 0; i < 23038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23039(int& val) {
    func_in_23039(val);
    const int x = 23039;
    std::cout << 23039;
    for (int i = 0; i < 23039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23039(int& val) {
    const int x = 23039;
    std::cout << 23039;
    for (int i = 0; i < 23039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23040(int& val) {
    func_in_23040(val);
    const int x = 23040;
    std::cout << 23040;
    for (int i = 0; i < 23040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23040(int& val) {
    const int x = 23040;
    std::cout << 23040;
    for (int i = 0; i < 23040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23041(int& val) {
    func_in_23041(val);
    const int x = 23041;
    std::cout << 23041;
    for (int i = 0; i < 23041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23041(int& val) {
    const int x = 23041;
    std::cout << 23041;
    for (int i = 0; i < 23041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23042(int& val) {
    func_in_23042(val);
    const int x = 23042;
    std::cout << 23042;
    for (int i = 0; i < 23042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23042(int& val) {
    const int x = 23042;
    std::cout << 23042;
    for (int i = 0; i < 23042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23043(int& val) {
    func_in_23043(val);
    const int x = 23043;
    std::cout << 23043;
    for (int i = 0; i < 23043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23043(int& val) {
    const int x = 23043;
    std::cout << 23043;
    for (int i = 0; i < 23043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23044(int& val) {
    func_in_23044(val);
    const int x = 23044;
    std::cout << 23044;
    for (int i = 0; i < 23044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23044(int& val) {
    const int x = 23044;
    std::cout << 23044;
    for (int i = 0; i < 23044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23045(int& val) {
    func_in_23045(val);
    const int x = 23045;
    std::cout << 23045;
    for (int i = 0; i < 23045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23045(int& val) {
    const int x = 23045;
    std::cout << 23045;
    for (int i = 0; i < 23045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23046(int& val) {
    func_in_23046(val);
    const int x = 23046;
    std::cout << 23046;
    for (int i = 0; i < 23046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23046(int& val) {
    const int x = 23046;
    std::cout << 23046;
    for (int i = 0; i < 23046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23047(int& val) {
    func_in_23047(val);
    const int x = 23047;
    std::cout << 23047;
    for (int i = 0; i < 23047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23047(int& val) {
    const int x = 23047;
    std::cout << 23047;
    for (int i = 0; i < 23047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23048(int& val) {
    func_in_23048(val);
    const int x = 23048;
    std::cout << 23048;
    for (int i = 0; i < 23048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23048(int& val) {
    const int x = 23048;
    std::cout << 23048;
    for (int i = 0; i < 23048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23049(int& val) {
    func_in_23049(val);
    const int x = 23049;
    std::cout << 23049;
    for (int i = 0; i < 23049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23049(int& val) {
    const int x = 23049;
    std::cout << 23049;
    for (int i = 0; i < 23049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23050(int& val) {
    func_in_23050(val);
    const int x = 23050;
    std::cout << 23050;
    for (int i = 0; i < 23050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23050(int& val) {
    const int x = 23050;
    std::cout << 23050;
    for (int i = 0; i < 23050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23051(int& val) {
    func_in_23051(val);
    const int x = 23051;
    std::cout << 23051;
    for (int i = 0; i < 23051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23051(int& val) {
    const int x = 23051;
    std::cout << 23051;
    for (int i = 0; i < 23051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23052(int& val) {
    func_in_23052(val);
    const int x = 23052;
    std::cout << 23052;
    for (int i = 0; i < 23052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23052(int& val) {
    const int x = 23052;
    std::cout << 23052;
    for (int i = 0; i < 23052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23053(int& val) {
    func_in_23053(val);
    const int x = 23053;
    std::cout << 23053;
    for (int i = 0; i < 23053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23053(int& val) {
    const int x = 23053;
    std::cout << 23053;
    for (int i = 0; i < 23053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23054(int& val) {
    func_in_23054(val);
    const int x = 23054;
    std::cout << 23054;
    for (int i = 0; i < 23054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23054(int& val) {
    const int x = 23054;
    std::cout << 23054;
    for (int i = 0; i < 23054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23055(int& val) {
    func_in_23055(val);
    const int x = 23055;
    std::cout << 23055;
    for (int i = 0; i < 23055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23055(int& val) {
    const int x = 23055;
    std::cout << 23055;
    for (int i = 0; i < 23055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23056(int& val) {
    func_in_23056(val);
    const int x = 23056;
    std::cout << 23056;
    for (int i = 0; i < 23056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23056(int& val) {
    const int x = 23056;
    std::cout << 23056;
    for (int i = 0; i < 23056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23057(int& val) {
    func_in_23057(val);
    const int x = 23057;
    std::cout << 23057;
    for (int i = 0; i < 23057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23057(int& val) {
    const int x = 23057;
    std::cout << 23057;
    for (int i = 0; i < 23057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23058(int& val) {
    func_in_23058(val);
    const int x = 23058;
    std::cout << 23058;
    for (int i = 0; i < 23058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23058(int& val) {
    const int x = 23058;
    std::cout << 23058;
    for (int i = 0; i < 23058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23059(int& val) {
    func_in_23059(val);
    const int x = 23059;
    std::cout << 23059;
    for (int i = 0; i < 23059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23059(int& val) {
    const int x = 23059;
    std::cout << 23059;
    for (int i = 0; i < 23059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23060(int& val) {
    func_in_23060(val);
    const int x = 23060;
    std::cout << 23060;
    for (int i = 0; i < 23060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23060(int& val) {
    const int x = 23060;
    std::cout << 23060;
    for (int i = 0; i < 23060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23061(int& val) {
    func_in_23061(val);
    const int x = 23061;
    std::cout << 23061;
    for (int i = 0; i < 23061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23061(int& val) {
    const int x = 23061;
    std::cout << 23061;
    for (int i = 0; i < 23061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23062(int& val) {
    func_in_23062(val);
    const int x = 23062;
    std::cout << 23062;
    for (int i = 0; i < 23062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23062(int& val) {
    const int x = 23062;
    std::cout << 23062;
    for (int i = 0; i < 23062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23063(int& val) {
    func_in_23063(val);
    const int x = 23063;
    std::cout << 23063;
    for (int i = 0; i < 23063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23063(int& val) {
    const int x = 23063;
    std::cout << 23063;
    for (int i = 0; i < 23063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23064(int& val) {
    func_in_23064(val);
    const int x = 23064;
    std::cout << 23064;
    for (int i = 0; i < 23064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23064(int& val) {
    const int x = 23064;
    std::cout << 23064;
    for (int i = 0; i < 23064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23065(int& val) {
    func_in_23065(val);
    const int x = 23065;
    std::cout << 23065;
    for (int i = 0; i < 23065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23065(int& val) {
    const int x = 23065;
    std::cout << 23065;
    for (int i = 0; i < 23065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23066(int& val) {
    func_in_23066(val);
    const int x = 23066;
    std::cout << 23066;
    for (int i = 0; i < 23066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23066(int& val) {
    const int x = 23066;
    std::cout << 23066;
    for (int i = 0; i < 23066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23067(int& val) {
    func_in_23067(val);
    const int x = 23067;
    std::cout << 23067;
    for (int i = 0; i < 23067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23067(int& val) {
    const int x = 23067;
    std::cout << 23067;
    for (int i = 0; i < 23067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23068(int& val) {
    func_in_23068(val);
    const int x = 23068;
    std::cout << 23068;
    for (int i = 0; i < 23068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23068(int& val) {
    const int x = 23068;
    std::cout << 23068;
    for (int i = 0; i < 23068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23069(int& val) {
    func_in_23069(val);
    const int x = 23069;
    std::cout << 23069;
    for (int i = 0; i < 23069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23069(int& val) {
    const int x = 23069;
    std::cout << 23069;
    for (int i = 0; i < 23069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23070(int& val) {
    func_in_23070(val);
    const int x = 23070;
    std::cout << 23070;
    for (int i = 0; i < 23070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23070(int& val) {
    const int x = 23070;
    std::cout << 23070;
    for (int i = 0; i < 23070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23071(int& val) {
    func_in_23071(val);
    const int x = 23071;
    std::cout << 23071;
    for (int i = 0; i < 23071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23071(int& val) {
    const int x = 23071;
    std::cout << 23071;
    for (int i = 0; i < 23071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23072(int& val) {
    func_in_23072(val);
    const int x = 23072;
    std::cout << 23072;
    for (int i = 0; i < 23072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23072(int& val) {
    const int x = 23072;
    std::cout << 23072;
    for (int i = 0; i < 23072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23073(int& val) {
    func_in_23073(val);
    const int x = 23073;
    std::cout << 23073;
    for (int i = 0; i < 23073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23073(int& val) {
    const int x = 23073;
    std::cout << 23073;
    for (int i = 0; i < 23073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23074(int& val) {
    func_in_23074(val);
    const int x = 23074;
    std::cout << 23074;
    for (int i = 0; i < 23074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23074(int& val) {
    const int x = 23074;
    std::cout << 23074;
    for (int i = 0; i < 23074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23075(int& val) {
    func_in_23075(val);
    const int x = 23075;
    std::cout << 23075;
    for (int i = 0; i < 23075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23075(int& val) {
    const int x = 23075;
    std::cout << 23075;
    for (int i = 0; i < 23075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23076(int& val) {
    func_in_23076(val);
    const int x = 23076;
    std::cout << 23076;
    for (int i = 0; i < 23076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23076(int& val) {
    const int x = 23076;
    std::cout << 23076;
    for (int i = 0; i < 23076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23077(int& val) {
    func_in_23077(val);
    const int x = 23077;
    std::cout << 23077;
    for (int i = 0; i < 23077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23077(int& val) {
    const int x = 23077;
    std::cout << 23077;
    for (int i = 0; i < 23077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23078(int& val) {
    func_in_23078(val);
    const int x = 23078;
    std::cout << 23078;
    for (int i = 0; i < 23078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23078(int& val) {
    const int x = 23078;
    std::cout << 23078;
    for (int i = 0; i < 23078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23079(int& val) {
    func_in_23079(val);
    const int x = 23079;
    std::cout << 23079;
    for (int i = 0; i < 23079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23079(int& val) {
    const int x = 23079;
    std::cout << 23079;
    for (int i = 0; i < 23079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23080(int& val) {
    func_in_23080(val);
    const int x = 23080;
    std::cout << 23080;
    for (int i = 0; i < 23080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23080(int& val) {
    const int x = 23080;
    std::cout << 23080;
    for (int i = 0; i < 23080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23081(int& val) {
    func_in_23081(val);
    const int x = 23081;
    std::cout << 23081;
    for (int i = 0; i < 23081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23081(int& val) {
    const int x = 23081;
    std::cout << 23081;
    for (int i = 0; i < 23081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23082(int& val) {
    func_in_23082(val);
    const int x = 23082;
    std::cout << 23082;
    for (int i = 0; i < 23082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23082(int& val) {
    const int x = 23082;
    std::cout << 23082;
    for (int i = 0; i < 23082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23083(int& val) {
    func_in_23083(val);
    const int x = 23083;
    std::cout << 23083;
    for (int i = 0; i < 23083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23083(int& val) {
    const int x = 23083;
    std::cout << 23083;
    for (int i = 0; i < 23083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23084(int& val) {
    func_in_23084(val);
    const int x = 23084;
    std::cout << 23084;
    for (int i = 0; i < 23084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23084(int& val) {
    const int x = 23084;
    std::cout << 23084;
    for (int i = 0; i < 23084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23085(int& val) {
    func_in_23085(val);
    const int x = 23085;
    std::cout << 23085;
    for (int i = 0; i < 23085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23085(int& val) {
    const int x = 23085;
    std::cout << 23085;
    for (int i = 0; i < 23085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23086(int& val) {
    func_in_23086(val);
    const int x = 23086;
    std::cout << 23086;
    for (int i = 0; i < 23086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23086(int& val) {
    const int x = 23086;
    std::cout << 23086;
    for (int i = 0; i < 23086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23087(int& val) {
    func_in_23087(val);
    const int x = 23087;
    std::cout << 23087;
    for (int i = 0; i < 23087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23087(int& val) {
    const int x = 23087;
    std::cout << 23087;
    for (int i = 0; i < 23087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23088(int& val) {
    func_in_23088(val);
    const int x = 23088;
    std::cout << 23088;
    for (int i = 0; i < 23088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23088(int& val) {
    const int x = 23088;
    std::cout << 23088;
    for (int i = 0; i < 23088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23089(int& val) {
    func_in_23089(val);
    const int x = 23089;
    std::cout << 23089;
    for (int i = 0; i < 23089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23089(int& val) {
    const int x = 23089;
    std::cout << 23089;
    for (int i = 0; i < 23089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23090(int& val) {
    func_in_23090(val);
    const int x = 23090;
    std::cout << 23090;
    for (int i = 0; i < 23090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23090(int& val) {
    const int x = 23090;
    std::cout << 23090;
    for (int i = 0; i < 23090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23091(int& val) {
    func_in_23091(val);
    const int x = 23091;
    std::cout << 23091;
    for (int i = 0; i < 23091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23091(int& val) {
    const int x = 23091;
    std::cout << 23091;
    for (int i = 0; i < 23091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23092(int& val) {
    func_in_23092(val);
    const int x = 23092;
    std::cout << 23092;
    for (int i = 0; i < 23092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23092(int& val) {
    const int x = 23092;
    std::cout << 23092;
    for (int i = 0; i < 23092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23093(int& val) {
    func_in_23093(val);
    const int x = 23093;
    std::cout << 23093;
    for (int i = 0; i < 23093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23093(int& val) {
    const int x = 23093;
    std::cout << 23093;
    for (int i = 0; i < 23093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23094(int& val) {
    func_in_23094(val);
    const int x = 23094;
    std::cout << 23094;
    for (int i = 0; i < 23094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23094(int& val) {
    const int x = 23094;
    std::cout << 23094;
    for (int i = 0; i < 23094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23095(int& val) {
    func_in_23095(val);
    const int x = 23095;
    std::cout << 23095;
    for (int i = 0; i < 23095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23095(int& val) {
    const int x = 23095;
    std::cout << 23095;
    for (int i = 0; i < 23095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23096(int& val) {
    func_in_23096(val);
    const int x = 23096;
    std::cout << 23096;
    for (int i = 0; i < 23096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23096(int& val) {
    const int x = 23096;
    std::cout << 23096;
    for (int i = 0; i < 23096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23097(int& val) {
    func_in_23097(val);
    const int x = 23097;
    std::cout << 23097;
    for (int i = 0; i < 23097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23097(int& val) {
    const int x = 23097;
    std::cout << 23097;
    for (int i = 0; i < 23097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23098(int& val) {
    func_in_23098(val);
    const int x = 23098;
    std::cout << 23098;
    for (int i = 0; i < 23098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23098(int& val) {
    const int x = 23098;
    std::cout << 23098;
    for (int i = 0; i < 23098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23099(int& val) {
    func_in_23099(val);
    const int x = 23099;
    std::cout << 23099;
    for (int i = 0; i < 23099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23099(int& val) {
    const int x = 23099;
    std::cout << 23099;
    for (int i = 0; i < 23099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23100(int& val) {
    func_in_23100(val);
    const int x = 23100;
    std::cout << 23100;
    for (int i = 0; i < 23100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23100(int& val) {
    const int x = 23100;
    std::cout << 23100;
    for (int i = 0; i < 23100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23101(int& val) {
    func_in_23101(val);
    const int x = 23101;
    std::cout << 23101;
    for (int i = 0; i < 23101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23101(int& val) {
    const int x = 23101;
    std::cout << 23101;
    for (int i = 0; i < 23101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23102(int& val) {
    func_in_23102(val);
    const int x = 23102;
    std::cout << 23102;
    for (int i = 0; i < 23102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23102(int& val) {
    const int x = 23102;
    std::cout << 23102;
    for (int i = 0; i < 23102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23103(int& val) {
    func_in_23103(val);
    const int x = 23103;
    std::cout << 23103;
    for (int i = 0; i < 23103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23103(int& val) {
    const int x = 23103;
    std::cout << 23103;
    for (int i = 0; i < 23103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23104(int& val) {
    func_in_23104(val);
    const int x = 23104;
    std::cout << 23104;
    for (int i = 0; i < 23104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23104(int& val) {
    const int x = 23104;
    std::cout << 23104;
    for (int i = 0; i < 23104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23105(int& val) {
    func_in_23105(val);
    const int x = 23105;
    std::cout << 23105;
    for (int i = 0; i < 23105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23105(int& val) {
    const int x = 23105;
    std::cout << 23105;
    for (int i = 0; i < 23105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23106(int& val) {
    func_in_23106(val);
    const int x = 23106;
    std::cout << 23106;
    for (int i = 0; i < 23106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23106(int& val) {
    const int x = 23106;
    std::cout << 23106;
    for (int i = 0; i < 23106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23107(int& val) {
    func_in_23107(val);
    const int x = 23107;
    std::cout << 23107;
    for (int i = 0; i < 23107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23107(int& val) {
    const int x = 23107;
    std::cout << 23107;
    for (int i = 0; i < 23107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23108(int& val) {
    func_in_23108(val);
    const int x = 23108;
    std::cout << 23108;
    for (int i = 0; i < 23108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23108(int& val) {
    const int x = 23108;
    std::cout << 23108;
    for (int i = 0; i < 23108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23109(int& val) {
    func_in_23109(val);
    const int x = 23109;
    std::cout << 23109;
    for (int i = 0; i < 23109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23109(int& val) {
    const int x = 23109;
    std::cout << 23109;
    for (int i = 0; i < 23109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23110(int& val) {
    func_in_23110(val);
    const int x = 23110;
    std::cout << 23110;
    for (int i = 0; i < 23110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23110(int& val) {
    const int x = 23110;
    std::cout << 23110;
    for (int i = 0; i < 23110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23111(int& val) {
    func_in_23111(val);
    const int x = 23111;
    std::cout << 23111;
    for (int i = 0; i < 23111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23111(int& val) {
    const int x = 23111;
    std::cout << 23111;
    for (int i = 0; i < 23111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23112(int& val) {
    func_in_23112(val);
    const int x = 23112;
    std::cout << 23112;
    for (int i = 0; i < 23112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23112(int& val) {
    const int x = 23112;
    std::cout << 23112;
    for (int i = 0; i < 23112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23113(int& val) {
    func_in_23113(val);
    const int x = 23113;
    std::cout << 23113;
    for (int i = 0; i < 23113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23113(int& val) {
    const int x = 23113;
    std::cout << 23113;
    for (int i = 0; i < 23113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23114(int& val) {
    func_in_23114(val);
    const int x = 23114;
    std::cout << 23114;
    for (int i = 0; i < 23114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23114(int& val) {
    const int x = 23114;
    std::cout << 23114;
    for (int i = 0; i < 23114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23115(int& val) {
    func_in_23115(val);
    const int x = 23115;
    std::cout << 23115;
    for (int i = 0; i < 23115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23115(int& val) {
    const int x = 23115;
    std::cout << 23115;
    for (int i = 0; i < 23115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23116(int& val) {
    func_in_23116(val);
    const int x = 23116;
    std::cout << 23116;
    for (int i = 0; i < 23116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23116(int& val) {
    const int x = 23116;
    std::cout << 23116;
    for (int i = 0; i < 23116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23117(int& val) {
    func_in_23117(val);
    const int x = 23117;
    std::cout << 23117;
    for (int i = 0; i < 23117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23117(int& val) {
    const int x = 23117;
    std::cout << 23117;
    for (int i = 0; i < 23117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23118(int& val) {
    func_in_23118(val);
    const int x = 23118;
    std::cout << 23118;
    for (int i = 0; i < 23118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23118(int& val) {
    const int x = 23118;
    std::cout << 23118;
    for (int i = 0; i < 23118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23119(int& val) {
    func_in_23119(val);
    const int x = 23119;
    std::cout << 23119;
    for (int i = 0; i < 23119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23119(int& val) {
    const int x = 23119;
    std::cout << 23119;
    for (int i = 0; i < 23119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23120(int& val) {
    func_in_23120(val);
    const int x = 23120;
    std::cout << 23120;
    for (int i = 0; i < 23120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23120(int& val) {
    const int x = 23120;
    std::cout << 23120;
    for (int i = 0; i < 23120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23121(int& val) {
    func_in_23121(val);
    const int x = 23121;
    std::cout << 23121;
    for (int i = 0; i < 23121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23121(int& val) {
    const int x = 23121;
    std::cout << 23121;
    for (int i = 0; i < 23121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23122(int& val) {
    func_in_23122(val);
    const int x = 23122;
    std::cout << 23122;
    for (int i = 0; i < 23122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23122(int& val) {
    const int x = 23122;
    std::cout << 23122;
    for (int i = 0; i < 23122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23123(int& val) {
    func_in_23123(val);
    const int x = 23123;
    std::cout << 23123;
    for (int i = 0; i < 23123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23123(int& val) {
    const int x = 23123;
    std::cout << 23123;
    for (int i = 0; i < 23123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23124(int& val) {
    func_in_23124(val);
    const int x = 23124;
    std::cout << 23124;
    for (int i = 0; i < 23124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23124(int& val) {
    const int x = 23124;
    std::cout << 23124;
    for (int i = 0; i < 23124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23125(int& val) {
    func_in_23125(val);
    const int x = 23125;
    std::cout << 23125;
    for (int i = 0; i < 23125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23125(int& val) {
    const int x = 23125;
    std::cout << 23125;
    for (int i = 0; i < 23125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23126(int& val) {
    func_in_23126(val);
    const int x = 23126;
    std::cout << 23126;
    for (int i = 0; i < 23126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23126(int& val) {
    const int x = 23126;
    std::cout << 23126;
    for (int i = 0; i < 23126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23127(int& val) {
    func_in_23127(val);
    const int x = 23127;
    std::cout << 23127;
    for (int i = 0; i < 23127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23127(int& val) {
    const int x = 23127;
    std::cout << 23127;
    for (int i = 0; i < 23127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23128(int& val) {
    func_in_23128(val);
    const int x = 23128;
    std::cout << 23128;
    for (int i = 0; i < 23128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23128(int& val) {
    const int x = 23128;
    std::cout << 23128;
    for (int i = 0; i < 23128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23129(int& val) {
    func_in_23129(val);
    const int x = 23129;
    std::cout << 23129;
    for (int i = 0; i < 23129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23129(int& val) {
    const int x = 23129;
    std::cout << 23129;
    for (int i = 0; i < 23129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23130(int& val) {
    func_in_23130(val);
    const int x = 23130;
    std::cout << 23130;
    for (int i = 0; i < 23130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23130(int& val) {
    const int x = 23130;
    std::cout << 23130;
    for (int i = 0; i < 23130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23131(int& val) {
    func_in_23131(val);
    const int x = 23131;
    std::cout << 23131;
    for (int i = 0; i < 23131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23131(int& val) {
    const int x = 23131;
    std::cout << 23131;
    for (int i = 0; i < 23131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23132(int& val) {
    func_in_23132(val);
    const int x = 23132;
    std::cout << 23132;
    for (int i = 0; i < 23132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23132(int& val) {
    const int x = 23132;
    std::cout << 23132;
    for (int i = 0; i < 23132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23133(int& val) {
    func_in_23133(val);
    const int x = 23133;
    std::cout << 23133;
    for (int i = 0; i < 23133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23133(int& val) {
    const int x = 23133;
    std::cout << 23133;
    for (int i = 0; i < 23133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23134(int& val) {
    func_in_23134(val);
    const int x = 23134;
    std::cout << 23134;
    for (int i = 0; i < 23134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23134(int& val) {
    const int x = 23134;
    std::cout << 23134;
    for (int i = 0; i < 23134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23135(int& val) {
    func_in_23135(val);
    const int x = 23135;
    std::cout << 23135;
    for (int i = 0; i < 23135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23135(int& val) {
    const int x = 23135;
    std::cout << 23135;
    for (int i = 0; i < 23135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23136(int& val) {
    func_in_23136(val);
    const int x = 23136;
    std::cout << 23136;
    for (int i = 0; i < 23136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23136(int& val) {
    const int x = 23136;
    std::cout << 23136;
    for (int i = 0; i < 23136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23137(int& val) {
    func_in_23137(val);
    const int x = 23137;
    std::cout << 23137;
    for (int i = 0; i < 23137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23137(int& val) {
    const int x = 23137;
    std::cout << 23137;
    for (int i = 0; i < 23137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23138(int& val) {
    func_in_23138(val);
    const int x = 23138;
    std::cout << 23138;
    for (int i = 0; i < 23138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23138(int& val) {
    const int x = 23138;
    std::cout << 23138;
    for (int i = 0; i < 23138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23139(int& val) {
    func_in_23139(val);
    const int x = 23139;
    std::cout << 23139;
    for (int i = 0; i < 23139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23139(int& val) {
    const int x = 23139;
    std::cout << 23139;
    for (int i = 0; i < 23139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23140(int& val) {
    func_in_23140(val);
    const int x = 23140;
    std::cout << 23140;
    for (int i = 0; i < 23140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23140(int& val) {
    const int x = 23140;
    std::cout << 23140;
    for (int i = 0; i < 23140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23141(int& val) {
    func_in_23141(val);
    const int x = 23141;
    std::cout << 23141;
    for (int i = 0; i < 23141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23141(int& val) {
    const int x = 23141;
    std::cout << 23141;
    for (int i = 0; i < 23141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23142(int& val) {
    func_in_23142(val);
    const int x = 23142;
    std::cout << 23142;
    for (int i = 0; i < 23142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23142(int& val) {
    const int x = 23142;
    std::cout << 23142;
    for (int i = 0; i < 23142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23143(int& val) {
    func_in_23143(val);
    const int x = 23143;
    std::cout << 23143;
    for (int i = 0; i < 23143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23143(int& val) {
    const int x = 23143;
    std::cout << 23143;
    for (int i = 0; i < 23143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23144(int& val) {
    func_in_23144(val);
    const int x = 23144;
    std::cout << 23144;
    for (int i = 0; i < 23144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23144(int& val) {
    const int x = 23144;
    std::cout << 23144;
    for (int i = 0; i < 23144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23145(int& val) {
    func_in_23145(val);
    const int x = 23145;
    std::cout << 23145;
    for (int i = 0; i < 23145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23145(int& val) {
    const int x = 23145;
    std::cout << 23145;
    for (int i = 0; i < 23145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23146(int& val) {
    func_in_23146(val);
    const int x = 23146;
    std::cout << 23146;
    for (int i = 0; i < 23146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23146(int& val) {
    const int x = 23146;
    std::cout << 23146;
    for (int i = 0; i < 23146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23147(int& val) {
    func_in_23147(val);
    const int x = 23147;
    std::cout << 23147;
    for (int i = 0; i < 23147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23147(int& val) {
    const int x = 23147;
    std::cout << 23147;
    for (int i = 0; i < 23147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23148(int& val) {
    func_in_23148(val);
    const int x = 23148;
    std::cout << 23148;
    for (int i = 0; i < 23148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23148(int& val) {
    const int x = 23148;
    std::cout << 23148;
    for (int i = 0; i < 23148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23149(int& val) {
    func_in_23149(val);
    const int x = 23149;
    std::cout << 23149;
    for (int i = 0; i < 23149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23149(int& val) {
    const int x = 23149;
    std::cout << 23149;
    for (int i = 0; i < 23149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23150(int& val) {
    func_in_23150(val);
    const int x = 23150;
    std::cout << 23150;
    for (int i = 0; i < 23150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23150(int& val) {
    const int x = 23150;
    std::cout << 23150;
    for (int i = 0; i < 23150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23151(int& val) {
    func_in_23151(val);
    const int x = 23151;
    std::cout << 23151;
    for (int i = 0; i < 23151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23151(int& val) {
    const int x = 23151;
    std::cout << 23151;
    for (int i = 0; i < 23151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23152(int& val) {
    func_in_23152(val);
    const int x = 23152;
    std::cout << 23152;
    for (int i = 0; i < 23152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23152(int& val) {
    const int x = 23152;
    std::cout << 23152;
    for (int i = 0; i < 23152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23153(int& val) {
    func_in_23153(val);
    const int x = 23153;
    std::cout << 23153;
    for (int i = 0; i < 23153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23153(int& val) {
    const int x = 23153;
    std::cout << 23153;
    for (int i = 0; i < 23153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23154(int& val) {
    func_in_23154(val);
    const int x = 23154;
    std::cout << 23154;
    for (int i = 0; i < 23154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23154(int& val) {
    const int x = 23154;
    std::cout << 23154;
    for (int i = 0; i < 23154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23155(int& val) {
    func_in_23155(val);
    const int x = 23155;
    std::cout << 23155;
    for (int i = 0; i < 23155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23155(int& val) {
    const int x = 23155;
    std::cout << 23155;
    for (int i = 0; i < 23155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23156(int& val) {
    func_in_23156(val);
    const int x = 23156;
    std::cout << 23156;
    for (int i = 0; i < 23156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23156(int& val) {
    const int x = 23156;
    std::cout << 23156;
    for (int i = 0; i < 23156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23157(int& val) {
    func_in_23157(val);
    const int x = 23157;
    std::cout << 23157;
    for (int i = 0; i < 23157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23157(int& val) {
    const int x = 23157;
    std::cout << 23157;
    for (int i = 0; i < 23157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23158(int& val) {
    func_in_23158(val);
    const int x = 23158;
    std::cout << 23158;
    for (int i = 0; i < 23158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23158(int& val) {
    const int x = 23158;
    std::cout << 23158;
    for (int i = 0; i < 23158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23159(int& val) {
    func_in_23159(val);
    const int x = 23159;
    std::cout << 23159;
    for (int i = 0; i < 23159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23159(int& val) {
    const int x = 23159;
    std::cout << 23159;
    for (int i = 0; i < 23159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23160(int& val) {
    func_in_23160(val);
    const int x = 23160;
    std::cout << 23160;
    for (int i = 0; i < 23160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23160(int& val) {
    const int x = 23160;
    std::cout << 23160;
    for (int i = 0; i < 23160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23161(int& val) {
    func_in_23161(val);
    const int x = 23161;
    std::cout << 23161;
    for (int i = 0; i < 23161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23161(int& val) {
    const int x = 23161;
    std::cout << 23161;
    for (int i = 0; i < 23161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23162(int& val) {
    func_in_23162(val);
    const int x = 23162;
    std::cout << 23162;
    for (int i = 0; i < 23162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23162(int& val) {
    const int x = 23162;
    std::cout << 23162;
    for (int i = 0; i < 23162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23163(int& val) {
    func_in_23163(val);
    const int x = 23163;
    std::cout << 23163;
    for (int i = 0; i < 23163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23163(int& val) {
    const int x = 23163;
    std::cout << 23163;
    for (int i = 0; i < 23163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23164(int& val) {
    func_in_23164(val);
    const int x = 23164;
    std::cout << 23164;
    for (int i = 0; i < 23164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23164(int& val) {
    const int x = 23164;
    std::cout << 23164;
    for (int i = 0; i < 23164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23165(int& val) {
    func_in_23165(val);
    const int x = 23165;
    std::cout << 23165;
    for (int i = 0; i < 23165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23165(int& val) {
    const int x = 23165;
    std::cout << 23165;
    for (int i = 0; i < 23165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23166(int& val) {
    func_in_23166(val);
    const int x = 23166;
    std::cout << 23166;
    for (int i = 0; i < 23166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23166(int& val) {
    const int x = 23166;
    std::cout << 23166;
    for (int i = 0; i < 23166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23167(int& val) {
    func_in_23167(val);
    const int x = 23167;
    std::cout << 23167;
    for (int i = 0; i < 23167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23167(int& val) {
    const int x = 23167;
    std::cout << 23167;
    for (int i = 0; i < 23167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23168(int& val) {
    func_in_23168(val);
    const int x = 23168;
    std::cout << 23168;
    for (int i = 0; i < 23168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23168(int& val) {
    const int x = 23168;
    std::cout << 23168;
    for (int i = 0; i < 23168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23169(int& val) {
    func_in_23169(val);
    const int x = 23169;
    std::cout << 23169;
    for (int i = 0; i < 23169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23169(int& val) {
    const int x = 23169;
    std::cout << 23169;
    for (int i = 0; i < 23169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23170(int& val) {
    func_in_23170(val);
    const int x = 23170;
    std::cout << 23170;
    for (int i = 0; i < 23170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23170(int& val) {
    const int x = 23170;
    std::cout << 23170;
    for (int i = 0; i < 23170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23171(int& val) {
    func_in_23171(val);
    const int x = 23171;
    std::cout << 23171;
    for (int i = 0; i < 23171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23171(int& val) {
    const int x = 23171;
    std::cout << 23171;
    for (int i = 0; i < 23171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23172(int& val) {
    func_in_23172(val);
    const int x = 23172;
    std::cout << 23172;
    for (int i = 0; i < 23172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23172(int& val) {
    const int x = 23172;
    std::cout << 23172;
    for (int i = 0; i < 23172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23173(int& val) {
    func_in_23173(val);
    const int x = 23173;
    std::cout << 23173;
    for (int i = 0; i < 23173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23173(int& val) {
    const int x = 23173;
    std::cout << 23173;
    for (int i = 0; i < 23173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23174(int& val) {
    func_in_23174(val);
    const int x = 23174;
    std::cout << 23174;
    for (int i = 0; i < 23174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23174(int& val) {
    const int x = 23174;
    std::cout << 23174;
    for (int i = 0; i < 23174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23175(int& val) {
    func_in_23175(val);
    const int x = 23175;
    std::cout << 23175;
    for (int i = 0; i < 23175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23175(int& val) {
    const int x = 23175;
    std::cout << 23175;
    for (int i = 0; i < 23175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23176(int& val) {
    func_in_23176(val);
    const int x = 23176;
    std::cout << 23176;
    for (int i = 0; i < 23176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23176(int& val) {
    const int x = 23176;
    std::cout << 23176;
    for (int i = 0; i < 23176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23177(int& val) {
    func_in_23177(val);
    const int x = 23177;
    std::cout << 23177;
    for (int i = 0; i < 23177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23177(int& val) {
    const int x = 23177;
    std::cout << 23177;
    for (int i = 0; i < 23177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23178(int& val) {
    func_in_23178(val);
    const int x = 23178;
    std::cout << 23178;
    for (int i = 0; i < 23178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23178(int& val) {
    const int x = 23178;
    std::cout << 23178;
    for (int i = 0; i < 23178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23179(int& val) {
    func_in_23179(val);
    const int x = 23179;
    std::cout << 23179;
    for (int i = 0; i < 23179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23179(int& val) {
    const int x = 23179;
    std::cout << 23179;
    for (int i = 0; i < 23179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23180(int& val) {
    func_in_23180(val);
    const int x = 23180;
    std::cout << 23180;
    for (int i = 0; i < 23180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23180(int& val) {
    const int x = 23180;
    std::cout << 23180;
    for (int i = 0; i < 23180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23181(int& val) {
    func_in_23181(val);
    const int x = 23181;
    std::cout << 23181;
    for (int i = 0; i < 23181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23181(int& val) {
    const int x = 23181;
    std::cout << 23181;
    for (int i = 0; i < 23181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23182(int& val) {
    func_in_23182(val);
    const int x = 23182;
    std::cout << 23182;
    for (int i = 0; i < 23182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23182(int& val) {
    const int x = 23182;
    std::cout << 23182;
    for (int i = 0; i < 23182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23183(int& val) {
    func_in_23183(val);
    const int x = 23183;
    std::cout << 23183;
    for (int i = 0; i < 23183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23183(int& val) {
    const int x = 23183;
    std::cout << 23183;
    for (int i = 0; i < 23183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23184(int& val) {
    func_in_23184(val);
    const int x = 23184;
    std::cout << 23184;
    for (int i = 0; i < 23184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23184(int& val) {
    const int x = 23184;
    std::cout << 23184;
    for (int i = 0; i < 23184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23185(int& val) {
    func_in_23185(val);
    const int x = 23185;
    std::cout << 23185;
    for (int i = 0; i < 23185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23185(int& val) {
    const int x = 23185;
    std::cout << 23185;
    for (int i = 0; i < 23185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23186(int& val) {
    func_in_23186(val);
    const int x = 23186;
    std::cout << 23186;
    for (int i = 0; i < 23186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23186(int& val) {
    const int x = 23186;
    std::cout << 23186;
    for (int i = 0; i < 23186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23187(int& val) {
    func_in_23187(val);
    const int x = 23187;
    std::cout << 23187;
    for (int i = 0; i < 23187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23187(int& val) {
    const int x = 23187;
    std::cout << 23187;
    for (int i = 0; i < 23187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23188(int& val) {
    func_in_23188(val);
    const int x = 23188;
    std::cout << 23188;
    for (int i = 0; i < 23188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23188(int& val) {
    const int x = 23188;
    std::cout << 23188;
    for (int i = 0; i < 23188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23189(int& val) {
    func_in_23189(val);
    const int x = 23189;
    std::cout << 23189;
    for (int i = 0; i < 23189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23189(int& val) {
    const int x = 23189;
    std::cout << 23189;
    for (int i = 0; i < 23189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23190(int& val) {
    func_in_23190(val);
    const int x = 23190;
    std::cout << 23190;
    for (int i = 0; i < 23190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23190(int& val) {
    const int x = 23190;
    std::cout << 23190;
    for (int i = 0; i < 23190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23191(int& val) {
    func_in_23191(val);
    const int x = 23191;
    std::cout << 23191;
    for (int i = 0; i < 23191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23191(int& val) {
    const int x = 23191;
    std::cout << 23191;
    for (int i = 0; i < 23191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23192(int& val) {
    func_in_23192(val);
    const int x = 23192;
    std::cout << 23192;
    for (int i = 0; i < 23192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23192(int& val) {
    const int x = 23192;
    std::cout << 23192;
    for (int i = 0; i < 23192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23193(int& val) {
    func_in_23193(val);
    const int x = 23193;
    std::cout << 23193;
    for (int i = 0; i < 23193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23193(int& val) {
    const int x = 23193;
    std::cout << 23193;
    for (int i = 0; i < 23193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23194(int& val) {
    func_in_23194(val);
    const int x = 23194;
    std::cout << 23194;
    for (int i = 0; i < 23194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23194(int& val) {
    const int x = 23194;
    std::cout << 23194;
    for (int i = 0; i < 23194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23195(int& val) {
    func_in_23195(val);
    const int x = 23195;
    std::cout << 23195;
    for (int i = 0; i < 23195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23195(int& val) {
    const int x = 23195;
    std::cout << 23195;
    for (int i = 0; i < 23195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23196(int& val) {
    func_in_23196(val);
    const int x = 23196;
    std::cout << 23196;
    for (int i = 0; i < 23196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23196(int& val) {
    const int x = 23196;
    std::cout << 23196;
    for (int i = 0; i < 23196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23197(int& val) {
    func_in_23197(val);
    const int x = 23197;
    std::cout << 23197;
    for (int i = 0; i < 23197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23197(int& val) {
    const int x = 23197;
    std::cout << 23197;
    for (int i = 0; i < 23197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23198(int& val) {
    func_in_23198(val);
    const int x = 23198;
    std::cout << 23198;
    for (int i = 0; i < 23198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23198(int& val) {
    const int x = 23198;
    std::cout << 23198;
    for (int i = 0; i < 23198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23199(int& val) {
    func_in_23199(val);
    const int x = 23199;
    std::cout << 23199;
    for (int i = 0; i < 23199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23199(int& val) {
    const int x = 23199;
    std::cout << 23199;
    for (int i = 0; i < 23199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23200(int& val) {
    func_in_23200(val);
    const int x = 23200;
    std::cout << 23200;
    for (int i = 0; i < 23200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23200(int& val) {
    const int x = 23200;
    std::cout << 23200;
    for (int i = 0; i < 23200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23201(int& val) {
    func_in_23201(val);
    const int x = 23201;
    std::cout << 23201;
    for (int i = 0; i < 23201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23201(int& val) {
    const int x = 23201;
    std::cout << 23201;
    for (int i = 0; i < 23201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23202(int& val) {
    func_in_23202(val);
    const int x = 23202;
    std::cout << 23202;
    for (int i = 0; i < 23202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23202(int& val) {
    const int x = 23202;
    std::cout << 23202;
    for (int i = 0; i < 23202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23203(int& val) {
    func_in_23203(val);
    const int x = 23203;
    std::cout << 23203;
    for (int i = 0; i < 23203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23203(int& val) {
    const int x = 23203;
    std::cout << 23203;
    for (int i = 0; i < 23203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23204(int& val) {
    func_in_23204(val);
    const int x = 23204;
    std::cout << 23204;
    for (int i = 0; i < 23204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23204(int& val) {
    const int x = 23204;
    std::cout << 23204;
    for (int i = 0; i < 23204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23205(int& val) {
    func_in_23205(val);
    const int x = 23205;
    std::cout << 23205;
    for (int i = 0; i < 23205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23205(int& val) {
    const int x = 23205;
    std::cout << 23205;
    for (int i = 0; i < 23205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23206(int& val) {
    func_in_23206(val);
    const int x = 23206;
    std::cout << 23206;
    for (int i = 0; i < 23206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23206(int& val) {
    const int x = 23206;
    std::cout << 23206;
    for (int i = 0; i < 23206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23207(int& val) {
    func_in_23207(val);
    const int x = 23207;
    std::cout << 23207;
    for (int i = 0; i < 23207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23207(int& val) {
    const int x = 23207;
    std::cout << 23207;
    for (int i = 0; i < 23207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23208(int& val) {
    func_in_23208(val);
    const int x = 23208;
    std::cout << 23208;
    for (int i = 0; i < 23208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23208(int& val) {
    const int x = 23208;
    std::cout << 23208;
    for (int i = 0; i < 23208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23209(int& val) {
    func_in_23209(val);
    const int x = 23209;
    std::cout << 23209;
    for (int i = 0; i < 23209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23209(int& val) {
    const int x = 23209;
    std::cout << 23209;
    for (int i = 0; i < 23209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23210(int& val) {
    func_in_23210(val);
    const int x = 23210;
    std::cout << 23210;
    for (int i = 0; i < 23210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23210(int& val) {
    const int x = 23210;
    std::cout << 23210;
    for (int i = 0; i < 23210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23211(int& val) {
    func_in_23211(val);
    const int x = 23211;
    std::cout << 23211;
    for (int i = 0; i < 23211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23211(int& val) {
    const int x = 23211;
    std::cout << 23211;
    for (int i = 0; i < 23211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23212(int& val) {
    func_in_23212(val);
    const int x = 23212;
    std::cout << 23212;
    for (int i = 0; i < 23212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23212(int& val) {
    const int x = 23212;
    std::cout << 23212;
    for (int i = 0; i < 23212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23213(int& val) {
    func_in_23213(val);
    const int x = 23213;
    std::cout << 23213;
    for (int i = 0; i < 23213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23213(int& val) {
    const int x = 23213;
    std::cout << 23213;
    for (int i = 0; i < 23213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23214(int& val) {
    func_in_23214(val);
    const int x = 23214;
    std::cout << 23214;
    for (int i = 0; i < 23214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23214(int& val) {
    const int x = 23214;
    std::cout << 23214;
    for (int i = 0; i < 23214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23215(int& val) {
    func_in_23215(val);
    const int x = 23215;
    std::cout << 23215;
    for (int i = 0; i < 23215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23215(int& val) {
    const int x = 23215;
    std::cout << 23215;
    for (int i = 0; i < 23215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23216(int& val) {
    func_in_23216(val);
    const int x = 23216;
    std::cout << 23216;
    for (int i = 0; i < 23216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23216(int& val) {
    const int x = 23216;
    std::cout << 23216;
    for (int i = 0; i < 23216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23217(int& val) {
    func_in_23217(val);
    const int x = 23217;
    std::cout << 23217;
    for (int i = 0; i < 23217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23217(int& val) {
    const int x = 23217;
    std::cout << 23217;
    for (int i = 0; i < 23217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23218(int& val) {
    func_in_23218(val);
    const int x = 23218;
    std::cout << 23218;
    for (int i = 0; i < 23218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23218(int& val) {
    const int x = 23218;
    std::cout << 23218;
    for (int i = 0; i < 23218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23219(int& val) {
    func_in_23219(val);
    const int x = 23219;
    std::cout << 23219;
    for (int i = 0; i < 23219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23219(int& val) {
    const int x = 23219;
    std::cout << 23219;
    for (int i = 0; i < 23219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23220(int& val) {
    func_in_23220(val);
    const int x = 23220;
    std::cout << 23220;
    for (int i = 0; i < 23220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23220(int& val) {
    const int x = 23220;
    std::cout << 23220;
    for (int i = 0; i < 23220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23221(int& val) {
    func_in_23221(val);
    const int x = 23221;
    std::cout << 23221;
    for (int i = 0; i < 23221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23221(int& val) {
    const int x = 23221;
    std::cout << 23221;
    for (int i = 0; i < 23221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23222(int& val) {
    func_in_23222(val);
    const int x = 23222;
    std::cout << 23222;
    for (int i = 0; i < 23222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23222(int& val) {
    const int x = 23222;
    std::cout << 23222;
    for (int i = 0; i < 23222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23223(int& val) {
    func_in_23223(val);
    const int x = 23223;
    std::cout << 23223;
    for (int i = 0; i < 23223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23223(int& val) {
    const int x = 23223;
    std::cout << 23223;
    for (int i = 0; i < 23223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23224(int& val) {
    func_in_23224(val);
    const int x = 23224;
    std::cout << 23224;
    for (int i = 0; i < 23224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23224(int& val) {
    const int x = 23224;
    std::cout << 23224;
    for (int i = 0; i < 23224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23225(int& val) {
    func_in_23225(val);
    const int x = 23225;
    std::cout << 23225;
    for (int i = 0; i < 23225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23225(int& val) {
    const int x = 23225;
    std::cout << 23225;
    for (int i = 0; i < 23225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23226(int& val) {
    func_in_23226(val);
    const int x = 23226;
    std::cout << 23226;
    for (int i = 0; i < 23226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23226(int& val) {
    const int x = 23226;
    std::cout << 23226;
    for (int i = 0; i < 23226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23227(int& val) {
    func_in_23227(val);
    const int x = 23227;
    std::cout << 23227;
    for (int i = 0; i < 23227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23227(int& val) {
    const int x = 23227;
    std::cout << 23227;
    for (int i = 0; i < 23227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23228(int& val) {
    func_in_23228(val);
    const int x = 23228;
    std::cout << 23228;
    for (int i = 0; i < 23228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23228(int& val) {
    const int x = 23228;
    std::cout << 23228;
    for (int i = 0; i < 23228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23229(int& val) {
    func_in_23229(val);
    const int x = 23229;
    std::cout << 23229;
    for (int i = 0; i < 23229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23229(int& val) {
    const int x = 23229;
    std::cout << 23229;
    for (int i = 0; i < 23229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23230(int& val) {
    func_in_23230(val);
    const int x = 23230;
    std::cout << 23230;
    for (int i = 0; i < 23230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23230(int& val) {
    const int x = 23230;
    std::cout << 23230;
    for (int i = 0; i < 23230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23231(int& val) {
    func_in_23231(val);
    const int x = 23231;
    std::cout << 23231;
    for (int i = 0; i < 23231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23231(int& val) {
    const int x = 23231;
    std::cout << 23231;
    for (int i = 0; i < 23231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23232(int& val) {
    func_in_23232(val);
    const int x = 23232;
    std::cout << 23232;
    for (int i = 0; i < 23232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23232(int& val) {
    const int x = 23232;
    std::cout << 23232;
    for (int i = 0; i < 23232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23233(int& val) {
    func_in_23233(val);
    const int x = 23233;
    std::cout << 23233;
    for (int i = 0; i < 23233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23233(int& val) {
    const int x = 23233;
    std::cout << 23233;
    for (int i = 0; i < 23233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23234(int& val) {
    func_in_23234(val);
    const int x = 23234;
    std::cout << 23234;
    for (int i = 0; i < 23234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23234(int& val) {
    const int x = 23234;
    std::cout << 23234;
    for (int i = 0; i < 23234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23235(int& val) {
    func_in_23235(val);
    const int x = 23235;
    std::cout << 23235;
    for (int i = 0; i < 23235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23235(int& val) {
    const int x = 23235;
    std::cout << 23235;
    for (int i = 0; i < 23235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23236(int& val) {
    func_in_23236(val);
    const int x = 23236;
    std::cout << 23236;
    for (int i = 0; i < 23236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23236(int& val) {
    const int x = 23236;
    std::cout << 23236;
    for (int i = 0; i < 23236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23237(int& val) {
    func_in_23237(val);
    const int x = 23237;
    std::cout << 23237;
    for (int i = 0; i < 23237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23237(int& val) {
    const int x = 23237;
    std::cout << 23237;
    for (int i = 0; i < 23237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23238(int& val) {
    func_in_23238(val);
    const int x = 23238;
    std::cout << 23238;
    for (int i = 0; i < 23238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23238(int& val) {
    const int x = 23238;
    std::cout << 23238;
    for (int i = 0; i < 23238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23239(int& val) {
    func_in_23239(val);
    const int x = 23239;
    std::cout << 23239;
    for (int i = 0; i < 23239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23239(int& val) {
    const int x = 23239;
    std::cout << 23239;
    for (int i = 0; i < 23239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23240(int& val) {
    func_in_23240(val);
    const int x = 23240;
    std::cout << 23240;
    for (int i = 0; i < 23240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23240(int& val) {
    const int x = 23240;
    std::cout << 23240;
    for (int i = 0; i < 23240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23241(int& val) {
    func_in_23241(val);
    const int x = 23241;
    std::cout << 23241;
    for (int i = 0; i < 23241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23241(int& val) {
    const int x = 23241;
    std::cout << 23241;
    for (int i = 0; i < 23241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23242(int& val) {
    func_in_23242(val);
    const int x = 23242;
    std::cout << 23242;
    for (int i = 0; i < 23242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23242(int& val) {
    const int x = 23242;
    std::cout << 23242;
    for (int i = 0; i < 23242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23243(int& val) {
    func_in_23243(val);
    const int x = 23243;
    std::cout << 23243;
    for (int i = 0; i < 23243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23243(int& val) {
    const int x = 23243;
    std::cout << 23243;
    for (int i = 0; i < 23243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23244(int& val) {
    func_in_23244(val);
    const int x = 23244;
    std::cout << 23244;
    for (int i = 0; i < 23244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23244(int& val) {
    const int x = 23244;
    std::cout << 23244;
    for (int i = 0; i < 23244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23245(int& val) {
    func_in_23245(val);
    const int x = 23245;
    std::cout << 23245;
    for (int i = 0; i < 23245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23245(int& val) {
    const int x = 23245;
    std::cout << 23245;
    for (int i = 0; i < 23245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23246(int& val) {
    func_in_23246(val);
    const int x = 23246;
    std::cout << 23246;
    for (int i = 0; i < 23246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23246(int& val) {
    const int x = 23246;
    std::cout << 23246;
    for (int i = 0; i < 23246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23247(int& val) {
    func_in_23247(val);
    const int x = 23247;
    std::cout << 23247;
    for (int i = 0; i < 23247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23247(int& val) {
    const int x = 23247;
    std::cout << 23247;
    for (int i = 0; i < 23247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23248(int& val) {
    func_in_23248(val);
    const int x = 23248;
    std::cout << 23248;
    for (int i = 0; i < 23248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23248(int& val) {
    const int x = 23248;
    std::cout << 23248;
    for (int i = 0; i < 23248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23249(int& val) {
    func_in_23249(val);
    const int x = 23249;
    std::cout << 23249;
    for (int i = 0; i < 23249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23249(int& val) {
    const int x = 23249;
    std::cout << 23249;
    for (int i = 0; i < 23249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23250(int& val) {
    func_in_23250(val);
    const int x = 23250;
    std::cout << 23250;
    for (int i = 0; i < 23250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23250(int& val) {
    const int x = 23250;
    std::cout << 23250;
    for (int i = 0; i < 23250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23251(int& val) {
    func_in_23251(val);
    const int x = 23251;
    std::cout << 23251;
    for (int i = 0; i < 23251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23251(int& val) {
    const int x = 23251;
    std::cout << 23251;
    for (int i = 0; i < 23251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23252(int& val) {
    func_in_23252(val);
    const int x = 23252;
    std::cout << 23252;
    for (int i = 0; i < 23252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23252(int& val) {
    const int x = 23252;
    std::cout << 23252;
    for (int i = 0; i < 23252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23253(int& val) {
    func_in_23253(val);
    const int x = 23253;
    std::cout << 23253;
    for (int i = 0; i < 23253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23253(int& val) {
    const int x = 23253;
    std::cout << 23253;
    for (int i = 0; i < 23253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23254(int& val) {
    func_in_23254(val);
    const int x = 23254;
    std::cout << 23254;
    for (int i = 0; i < 23254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23254(int& val) {
    const int x = 23254;
    std::cout << 23254;
    for (int i = 0; i < 23254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23255(int& val) {
    func_in_23255(val);
    const int x = 23255;
    std::cout << 23255;
    for (int i = 0; i < 23255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23255(int& val) {
    const int x = 23255;
    std::cout << 23255;
    for (int i = 0; i < 23255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23256(int& val) {
    func_in_23256(val);
    const int x = 23256;
    std::cout << 23256;
    for (int i = 0; i < 23256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23256(int& val) {
    const int x = 23256;
    std::cout << 23256;
    for (int i = 0; i < 23256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23257(int& val) {
    func_in_23257(val);
    const int x = 23257;
    std::cout << 23257;
    for (int i = 0; i < 23257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23257(int& val) {
    const int x = 23257;
    std::cout << 23257;
    for (int i = 0; i < 23257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23258(int& val) {
    func_in_23258(val);
    const int x = 23258;
    std::cout << 23258;
    for (int i = 0; i < 23258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23258(int& val) {
    const int x = 23258;
    std::cout << 23258;
    for (int i = 0; i < 23258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23259(int& val) {
    func_in_23259(val);
    const int x = 23259;
    std::cout << 23259;
    for (int i = 0; i < 23259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23259(int& val) {
    const int x = 23259;
    std::cout << 23259;
    for (int i = 0; i < 23259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23260(int& val) {
    func_in_23260(val);
    const int x = 23260;
    std::cout << 23260;
    for (int i = 0; i < 23260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23260(int& val) {
    const int x = 23260;
    std::cout << 23260;
    for (int i = 0; i < 23260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23261(int& val) {
    func_in_23261(val);
    const int x = 23261;
    std::cout << 23261;
    for (int i = 0; i < 23261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23261(int& val) {
    const int x = 23261;
    std::cout << 23261;
    for (int i = 0; i < 23261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23262(int& val) {
    func_in_23262(val);
    const int x = 23262;
    std::cout << 23262;
    for (int i = 0; i < 23262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23262(int& val) {
    const int x = 23262;
    std::cout << 23262;
    for (int i = 0; i < 23262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23263(int& val) {
    func_in_23263(val);
    const int x = 23263;
    std::cout << 23263;
    for (int i = 0; i < 23263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23263(int& val) {
    const int x = 23263;
    std::cout << 23263;
    for (int i = 0; i < 23263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23264(int& val) {
    func_in_23264(val);
    const int x = 23264;
    std::cout << 23264;
    for (int i = 0; i < 23264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23264(int& val) {
    const int x = 23264;
    std::cout << 23264;
    for (int i = 0; i < 23264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23265(int& val) {
    func_in_23265(val);
    const int x = 23265;
    std::cout << 23265;
    for (int i = 0; i < 23265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23265(int& val) {
    const int x = 23265;
    std::cout << 23265;
    for (int i = 0; i < 23265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23266(int& val) {
    func_in_23266(val);
    const int x = 23266;
    std::cout << 23266;
    for (int i = 0; i < 23266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23266(int& val) {
    const int x = 23266;
    std::cout << 23266;
    for (int i = 0; i < 23266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23267(int& val) {
    func_in_23267(val);
    const int x = 23267;
    std::cout << 23267;
    for (int i = 0; i < 23267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23267(int& val) {
    const int x = 23267;
    std::cout << 23267;
    for (int i = 0; i < 23267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23268(int& val) {
    func_in_23268(val);
    const int x = 23268;
    std::cout << 23268;
    for (int i = 0; i < 23268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23268(int& val) {
    const int x = 23268;
    std::cout << 23268;
    for (int i = 0; i < 23268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23269(int& val) {
    func_in_23269(val);
    const int x = 23269;
    std::cout << 23269;
    for (int i = 0; i < 23269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23269(int& val) {
    const int x = 23269;
    std::cout << 23269;
    for (int i = 0; i < 23269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23270(int& val) {
    func_in_23270(val);
    const int x = 23270;
    std::cout << 23270;
    for (int i = 0; i < 23270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23270(int& val) {
    const int x = 23270;
    std::cout << 23270;
    for (int i = 0; i < 23270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23271(int& val) {
    func_in_23271(val);
    const int x = 23271;
    std::cout << 23271;
    for (int i = 0; i < 23271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23271(int& val) {
    const int x = 23271;
    std::cout << 23271;
    for (int i = 0; i < 23271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23272(int& val) {
    func_in_23272(val);
    const int x = 23272;
    std::cout << 23272;
    for (int i = 0; i < 23272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23272(int& val) {
    const int x = 23272;
    std::cout << 23272;
    for (int i = 0; i < 23272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23273(int& val) {
    func_in_23273(val);
    const int x = 23273;
    std::cout << 23273;
    for (int i = 0; i < 23273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23273(int& val) {
    const int x = 23273;
    std::cout << 23273;
    for (int i = 0; i < 23273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23274(int& val) {
    func_in_23274(val);
    const int x = 23274;
    std::cout << 23274;
    for (int i = 0; i < 23274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23274(int& val) {
    const int x = 23274;
    std::cout << 23274;
    for (int i = 0; i < 23274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23275(int& val) {
    func_in_23275(val);
    const int x = 23275;
    std::cout << 23275;
    for (int i = 0; i < 23275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23275(int& val) {
    const int x = 23275;
    std::cout << 23275;
    for (int i = 0; i < 23275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23276(int& val) {
    func_in_23276(val);
    const int x = 23276;
    std::cout << 23276;
    for (int i = 0; i < 23276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23276(int& val) {
    const int x = 23276;
    std::cout << 23276;
    for (int i = 0; i < 23276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23277(int& val) {
    func_in_23277(val);
    const int x = 23277;
    std::cout << 23277;
    for (int i = 0; i < 23277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23277(int& val) {
    const int x = 23277;
    std::cout << 23277;
    for (int i = 0; i < 23277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23278(int& val) {
    func_in_23278(val);
    const int x = 23278;
    std::cout << 23278;
    for (int i = 0; i < 23278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23278(int& val) {
    const int x = 23278;
    std::cout << 23278;
    for (int i = 0; i < 23278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23279(int& val) {
    func_in_23279(val);
    const int x = 23279;
    std::cout << 23279;
    for (int i = 0; i < 23279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23279(int& val) {
    const int x = 23279;
    std::cout << 23279;
    for (int i = 0; i < 23279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23280(int& val) {
    func_in_23280(val);
    const int x = 23280;
    std::cout << 23280;
    for (int i = 0; i < 23280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23280(int& val) {
    const int x = 23280;
    std::cout << 23280;
    for (int i = 0; i < 23280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23281(int& val) {
    func_in_23281(val);
    const int x = 23281;
    std::cout << 23281;
    for (int i = 0; i < 23281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23281(int& val) {
    const int x = 23281;
    std::cout << 23281;
    for (int i = 0; i < 23281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23282(int& val) {
    func_in_23282(val);
    const int x = 23282;
    std::cout << 23282;
    for (int i = 0; i < 23282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23282(int& val) {
    const int x = 23282;
    std::cout << 23282;
    for (int i = 0; i < 23282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23283(int& val) {
    func_in_23283(val);
    const int x = 23283;
    std::cout << 23283;
    for (int i = 0; i < 23283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23283(int& val) {
    const int x = 23283;
    std::cout << 23283;
    for (int i = 0; i < 23283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23284(int& val) {
    func_in_23284(val);
    const int x = 23284;
    std::cout << 23284;
    for (int i = 0; i < 23284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23284(int& val) {
    const int x = 23284;
    std::cout << 23284;
    for (int i = 0; i < 23284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23285(int& val) {
    func_in_23285(val);
    const int x = 23285;
    std::cout << 23285;
    for (int i = 0; i < 23285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23285(int& val) {
    const int x = 23285;
    std::cout << 23285;
    for (int i = 0; i < 23285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23286(int& val) {
    func_in_23286(val);
    const int x = 23286;
    std::cout << 23286;
    for (int i = 0; i < 23286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23286(int& val) {
    const int x = 23286;
    std::cout << 23286;
    for (int i = 0; i < 23286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23287(int& val) {
    func_in_23287(val);
    const int x = 23287;
    std::cout << 23287;
    for (int i = 0; i < 23287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23287(int& val) {
    const int x = 23287;
    std::cout << 23287;
    for (int i = 0; i < 23287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23288(int& val) {
    func_in_23288(val);
    const int x = 23288;
    std::cout << 23288;
    for (int i = 0; i < 23288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23288(int& val) {
    const int x = 23288;
    std::cout << 23288;
    for (int i = 0; i < 23288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23289(int& val) {
    func_in_23289(val);
    const int x = 23289;
    std::cout << 23289;
    for (int i = 0; i < 23289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23289(int& val) {
    const int x = 23289;
    std::cout << 23289;
    for (int i = 0; i < 23289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23290(int& val) {
    func_in_23290(val);
    const int x = 23290;
    std::cout << 23290;
    for (int i = 0; i < 23290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23290(int& val) {
    const int x = 23290;
    std::cout << 23290;
    for (int i = 0; i < 23290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23291(int& val) {
    func_in_23291(val);
    const int x = 23291;
    std::cout << 23291;
    for (int i = 0; i < 23291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23291(int& val) {
    const int x = 23291;
    std::cout << 23291;
    for (int i = 0; i < 23291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23292(int& val) {
    func_in_23292(val);
    const int x = 23292;
    std::cout << 23292;
    for (int i = 0; i < 23292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23292(int& val) {
    const int x = 23292;
    std::cout << 23292;
    for (int i = 0; i < 23292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23293(int& val) {
    func_in_23293(val);
    const int x = 23293;
    std::cout << 23293;
    for (int i = 0; i < 23293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23293(int& val) {
    const int x = 23293;
    std::cout << 23293;
    for (int i = 0; i < 23293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23294(int& val) {
    func_in_23294(val);
    const int x = 23294;
    std::cout << 23294;
    for (int i = 0; i < 23294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23294(int& val) {
    const int x = 23294;
    std::cout << 23294;
    for (int i = 0; i < 23294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23295(int& val) {
    func_in_23295(val);
    const int x = 23295;
    std::cout << 23295;
    for (int i = 0; i < 23295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23295(int& val) {
    const int x = 23295;
    std::cout << 23295;
    for (int i = 0; i < 23295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23296(int& val) {
    func_in_23296(val);
    const int x = 23296;
    std::cout << 23296;
    for (int i = 0; i < 23296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23296(int& val) {
    const int x = 23296;
    std::cout << 23296;
    for (int i = 0; i < 23296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23297(int& val) {
    func_in_23297(val);
    const int x = 23297;
    std::cout << 23297;
    for (int i = 0; i < 23297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23297(int& val) {
    const int x = 23297;
    std::cout << 23297;
    for (int i = 0; i < 23297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23298(int& val) {
    func_in_23298(val);
    const int x = 23298;
    std::cout << 23298;
    for (int i = 0; i < 23298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23298(int& val) {
    const int x = 23298;
    std::cout << 23298;
    for (int i = 0; i < 23298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23299(int& val) {
    func_in_23299(val);
    const int x = 23299;
    std::cout << 23299;
    for (int i = 0; i < 23299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23299(int& val) {
    const int x = 23299;
    std::cout << 23299;
    for (int i = 0; i < 23299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23300(int& val) {
    func_in_23300(val);
    const int x = 23300;
    std::cout << 23300;
    for (int i = 0; i < 23300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23300(int& val) {
    const int x = 23300;
    std::cout << 23300;
    for (int i = 0; i < 23300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23301(int& val) {
    func_in_23301(val);
    const int x = 23301;
    std::cout << 23301;
    for (int i = 0; i < 23301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23301(int& val) {
    const int x = 23301;
    std::cout << 23301;
    for (int i = 0; i < 23301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23302(int& val) {
    func_in_23302(val);
    const int x = 23302;
    std::cout << 23302;
    for (int i = 0; i < 23302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23302(int& val) {
    const int x = 23302;
    std::cout << 23302;
    for (int i = 0; i < 23302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23303(int& val) {
    func_in_23303(val);
    const int x = 23303;
    std::cout << 23303;
    for (int i = 0; i < 23303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23303(int& val) {
    const int x = 23303;
    std::cout << 23303;
    for (int i = 0; i < 23303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23304(int& val) {
    func_in_23304(val);
    const int x = 23304;
    std::cout << 23304;
    for (int i = 0; i < 23304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23304(int& val) {
    const int x = 23304;
    std::cout << 23304;
    for (int i = 0; i < 23304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23305(int& val) {
    func_in_23305(val);
    const int x = 23305;
    std::cout << 23305;
    for (int i = 0; i < 23305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23305(int& val) {
    const int x = 23305;
    std::cout << 23305;
    for (int i = 0; i < 23305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23306(int& val) {
    func_in_23306(val);
    const int x = 23306;
    std::cout << 23306;
    for (int i = 0; i < 23306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23306(int& val) {
    const int x = 23306;
    std::cout << 23306;
    for (int i = 0; i < 23306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23307(int& val) {
    func_in_23307(val);
    const int x = 23307;
    std::cout << 23307;
    for (int i = 0; i < 23307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23307(int& val) {
    const int x = 23307;
    std::cout << 23307;
    for (int i = 0; i < 23307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23308(int& val) {
    func_in_23308(val);
    const int x = 23308;
    std::cout << 23308;
    for (int i = 0; i < 23308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23308(int& val) {
    const int x = 23308;
    std::cout << 23308;
    for (int i = 0; i < 23308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23309(int& val) {
    func_in_23309(val);
    const int x = 23309;
    std::cout << 23309;
    for (int i = 0; i < 23309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23309(int& val) {
    const int x = 23309;
    std::cout << 23309;
    for (int i = 0; i < 23309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23310(int& val) {
    func_in_23310(val);
    const int x = 23310;
    std::cout << 23310;
    for (int i = 0; i < 23310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23310(int& val) {
    const int x = 23310;
    std::cout << 23310;
    for (int i = 0; i < 23310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23311(int& val) {
    func_in_23311(val);
    const int x = 23311;
    std::cout << 23311;
    for (int i = 0; i < 23311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23311(int& val) {
    const int x = 23311;
    std::cout << 23311;
    for (int i = 0; i < 23311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23312(int& val) {
    func_in_23312(val);
    const int x = 23312;
    std::cout << 23312;
    for (int i = 0; i < 23312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23312(int& val) {
    const int x = 23312;
    std::cout << 23312;
    for (int i = 0; i < 23312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23313(int& val) {
    func_in_23313(val);
    const int x = 23313;
    std::cout << 23313;
    for (int i = 0; i < 23313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23313(int& val) {
    const int x = 23313;
    std::cout << 23313;
    for (int i = 0; i < 23313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23314(int& val) {
    func_in_23314(val);
    const int x = 23314;
    std::cout << 23314;
    for (int i = 0; i < 23314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23314(int& val) {
    const int x = 23314;
    std::cout << 23314;
    for (int i = 0; i < 23314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23315(int& val) {
    func_in_23315(val);
    const int x = 23315;
    std::cout << 23315;
    for (int i = 0; i < 23315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23315(int& val) {
    const int x = 23315;
    std::cout << 23315;
    for (int i = 0; i < 23315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23316(int& val) {
    func_in_23316(val);
    const int x = 23316;
    std::cout << 23316;
    for (int i = 0; i < 23316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23316(int& val) {
    const int x = 23316;
    std::cout << 23316;
    for (int i = 0; i < 23316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23317(int& val) {
    func_in_23317(val);
    const int x = 23317;
    std::cout << 23317;
    for (int i = 0; i < 23317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23317(int& val) {
    const int x = 23317;
    std::cout << 23317;
    for (int i = 0; i < 23317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23318(int& val) {
    func_in_23318(val);
    const int x = 23318;
    std::cout << 23318;
    for (int i = 0; i < 23318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23318(int& val) {
    const int x = 23318;
    std::cout << 23318;
    for (int i = 0; i < 23318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23319(int& val) {
    func_in_23319(val);
    const int x = 23319;
    std::cout << 23319;
    for (int i = 0; i < 23319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23319(int& val) {
    const int x = 23319;
    std::cout << 23319;
    for (int i = 0; i < 23319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23320(int& val) {
    func_in_23320(val);
    const int x = 23320;
    std::cout << 23320;
    for (int i = 0; i < 23320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23320(int& val) {
    const int x = 23320;
    std::cout << 23320;
    for (int i = 0; i < 23320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23321(int& val) {
    func_in_23321(val);
    const int x = 23321;
    std::cout << 23321;
    for (int i = 0; i < 23321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23321(int& val) {
    const int x = 23321;
    std::cout << 23321;
    for (int i = 0; i < 23321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23322(int& val) {
    func_in_23322(val);
    const int x = 23322;
    std::cout << 23322;
    for (int i = 0; i < 23322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23322(int& val) {
    const int x = 23322;
    std::cout << 23322;
    for (int i = 0; i < 23322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23323(int& val) {
    func_in_23323(val);
    const int x = 23323;
    std::cout << 23323;
    for (int i = 0; i < 23323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23323(int& val) {
    const int x = 23323;
    std::cout << 23323;
    for (int i = 0; i < 23323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23324(int& val) {
    func_in_23324(val);
    const int x = 23324;
    std::cout << 23324;
    for (int i = 0; i < 23324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23324(int& val) {
    const int x = 23324;
    std::cout << 23324;
    for (int i = 0; i < 23324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23325(int& val) {
    func_in_23325(val);
    const int x = 23325;
    std::cout << 23325;
    for (int i = 0; i < 23325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23325(int& val) {
    const int x = 23325;
    std::cout << 23325;
    for (int i = 0; i < 23325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23326(int& val) {
    func_in_23326(val);
    const int x = 23326;
    std::cout << 23326;
    for (int i = 0; i < 23326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23326(int& val) {
    const int x = 23326;
    std::cout << 23326;
    for (int i = 0; i < 23326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23327(int& val) {
    func_in_23327(val);
    const int x = 23327;
    std::cout << 23327;
    for (int i = 0; i < 23327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23327(int& val) {
    const int x = 23327;
    std::cout << 23327;
    for (int i = 0; i < 23327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23328(int& val) {
    func_in_23328(val);
    const int x = 23328;
    std::cout << 23328;
    for (int i = 0; i < 23328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23328(int& val) {
    const int x = 23328;
    std::cout << 23328;
    for (int i = 0; i < 23328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23329(int& val) {
    func_in_23329(val);
    const int x = 23329;
    std::cout << 23329;
    for (int i = 0; i < 23329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23329(int& val) {
    const int x = 23329;
    std::cout << 23329;
    for (int i = 0; i < 23329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23330(int& val) {
    func_in_23330(val);
    const int x = 23330;
    std::cout << 23330;
    for (int i = 0; i < 23330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23330(int& val) {
    const int x = 23330;
    std::cout << 23330;
    for (int i = 0; i < 23330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23331(int& val) {
    func_in_23331(val);
    const int x = 23331;
    std::cout << 23331;
    for (int i = 0; i < 23331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23331(int& val) {
    const int x = 23331;
    std::cout << 23331;
    for (int i = 0; i < 23331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23332(int& val) {
    func_in_23332(val);
    const int x = 23332;
    std::cout << 23332;
    for (int i = 0; i < 23332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23332(int& val) {
    const int x = 23332;
    std::cout << 23332;
    for (int i = 0; i < 23332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23333(int& val) {
    func_in_23333(val);
    const int x = 23333;
    std::cout << 23333;
    for (int i = 0; i < 23333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23333(int& val) {
    const int x = 23333;
    std::cout << 23333;
    for (int i = 0; i < 23333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23334(int& val) {
    func_in_23334(val);
    const int x = 23334;
    std::cout << 23334;
    for (int i = 0; i < 23334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23334(int& val) {
    const int x = 23334;
    std::cout << 23334;
    for (int i = 0; i < 23334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23335(int& val) {
    func_in_23335(val);
    const int x = 23335;
    std::cout << 23335;
    for (int i = 0; i < 23335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23335(int& val) {
    const int x = 23335;
    std::cout << 23335;
    for (int i = 0; i < 23335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23336(int& val) {
    func_in_23336(val);
    const int x = 23336;
    std::cout << 23336;
    for (int i = 0; i < 23336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23336(int& val) {
    const int x = 23336;
    std::cout << 23336;
    for (int i = 0; i < 23336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23337(int& val) {
    func_in_23337(val);
    const int x = 23337;
    std::cout << 23337;
    for (int i = 0; i < 23337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23337(int& val) {
    const int x = 23337;
    std::cout << 23337;
    for (int i = 0; i < 23337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23338(int& val) {
    func_in_23338(val);
    const int x = 23338;
    std::cout << 23338;
    for (int i = 0; i < 23338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23338(int& val) {
    const int x = 23338;
    std::cout << 23338;
    for (int i = 0; i < 23338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23339(int& val) {
    func_in_23339(val);
    const int x = 23339;
    std::cout << 23339;
    for (int i = 0; i < 23339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23339(int& val) {
    const int x = 23339;
    std::cout << 23339;
    for (int i = 0; i < 23339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23340(int& val) {
    func_in_23340(val);
    const int x = 23340;
    std::cout << 23340;
    for (int i = 0; i < 23340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23340(int& val) {
    const int x = 23340;
    std::cout << 23340;
    for (int i = 0; i < 23340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23341(int& val) {
    func_in_23341(val);
    const int x = 23341;
    std::cout << 23341;
    for (int i = 0; i < 23341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23341(int& val) {
    const int x = 23341;
    std::cout << 23341;
    for (int i = 0; i < 23341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23342(int& val) {
    func_in_23342(val);
    const int x = 23342;
    std::cout << 23342;
    for (int i = 0; i < 23342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23342(int& val) {
    const int x = 23342;
    std::cout << 23342;
    for (int i = 0; i < 23342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23343(int& val) {
    func_in_23343(val);
    const int x = 23343;
    std::cout << 23343;
    for (int i = 0; i < 23343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23343(int& val) {
    const int x = 23343;
    std::cout << 23343;
    for (int i = 0; i < 23343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23344(int& val) {
    func_in_23344(val);
    const int x = 23344;
    std::cout << 23344;
    for (int i = 0; i < 23344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23344(int& val) {
    const int x = 23344;
    std::cout << 23344;
    for (int i = 0; i < 23344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23345(int& val) {
    func_in_23345(val);
    const int x = 23345;
    std::cout << 23345;
    for (int i = 0; i < 23345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23345(int& val) {
    const int x = 23345;
    std::cout << 23345;
    for (int i = 0; i < 23345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23346(int& val) {
    func_in_23346(val);
    const int x = 23346;
    std::cout << 23346;
    for (int i = 0; i < 23346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23346(int& val) {
    const int x = 23346;
    std::cout << 23346;
    for (int i = 0; i < 23346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23347(int& val) {
    func_in_23347(val);
    const int x = 23347;
    std::cout << 23347;
    for (int i = 0; i < 23347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23347(int& val) {
    const int x = 23347;
    std::cout << 23347;
    for (int i = 0; i < 23347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23348(int& val) {
    func_in_23348(val);
    const int x = 23348;
    std::cout << 23348;
    for (int i = 0; i < 23348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23348(int& val) {
    const int x = 23348;
    std::cout << 23348;
    for (int i = 0; i < 23348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23349(int& val) {
    func_in_23349(val);
    const int x = 23349;
    std::cout << 23349;
    for (int i = 0; i < 23349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23349(int& val) {
    const int x = 23349;
    std::cout << 23349;
    for (int i = 0; i < 23349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23350(int& val) {
    func_in_23350(val);
    const int x = 23350;
    std::cout << 23350;
    for (int i = 0; i < 23350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23350(int& val) {
    const int x = 23350;
    std::cout << 23350;
    for (int i = 0; i < 23350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23351(int& val) {
    func_in_23351(val);
    const int x = 23351;
    std::cout << 23351;
    for (int i = 0; i < 23351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23351(int& val) {
    const int x = 23351;
    std::cout << 23351;
    for (int i = 0; i < 23351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23352(int& val) {
    func_in_23352(val);
    const int x = 23352;
    std::cout << 23352;
    for (int i = 0; i < 23352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23352(int& val) {
    const int x = 23352;
    std::cout << 23352;
    for (int i = 0; i < 23352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23353(int& val) {
    func_in_23353(val);
    const int x = 23353;
    std::cout << 23353;
    for (int i = 0; i < 23353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23353(int& val) {
    const int x = 23353;
    std::cout << 23353;
    for (int i = 0; i < 23353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23354(int& val) {
    func_in_23354(val);
    const int x = 23354;
    std::cout << 23354;
    for (int i = 0; i < 23354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23354(int& val) {
    const int x = 23354;
    std::cout << 23354;
    for (int i = 0; i < 23354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23355(int& val) {
    func_in_23355(val);
    const int x = 23355;
    std::cout << 23355;
    for (int i = 0; i < 23355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23355(int& val) {
    const int x = 23355;
    std::cout << 23355;
    for (int i = 0; i < 23355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23356(int& val) {
    func_in_23356(val);
    const int x = 23356;
    std::cout << 23356;
    for (int i = 0; i < 23356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23356(int& val) {
    const int x = 23356;
    std::cout << 23356;
    for (int i = 0; i < 23356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23357(int& val) {
    func_in_23357(val);
    const int x = 23357;
    std::cout << 23357;
    for (int i = 0; i < 23357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23357(int& val) {
    const int x = 23357;
    std::cout << 23357;
    for (int i = 0; i < 23357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23358(int& val) {
    func_in_23358(val);
    const int x = 23358;
    std::cout << 23358;
    for (int i = 0; i < 23358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23358(int& val) {
    const int x = 23358;
    std::cout << 23358;
    for (int i = 0; i < 23358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23359(int& val) {
    func_in_23359(val);
    const int x = 23359;
    std::cout << 23359;
    for (int i = 0; i < 23359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23359(int& val) {
    const int x = 23359;
    std::cout << 23359;
    for (int i = 0; i < 23359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23360(int& val) {
    func_in_23360(val);
    const int x = 23360;
    std::cout << 23360;
    for (int i = 0; i < 23360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23360(int& val) {
    const int x = 23360;
    std::cout << 23360;
    for (int i = 0; i < 23360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23361(int& val) {
    func_in_23361(val);
    const int x = 23361;
    std::cout << 23361;
    for (int i = 0; i < 23361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23361(int& val) {
    const int x = 23361;
    std::cout << 23361;
    for (int i = 0; i < 23361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23362(int& val) {
    func_in_23362(val);
    const int x = 23362;
    std::cout << 23362;
    for (int i = 0; i < 23362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23362(int& val) {
    const int x = 23362;
    std::cout << 23362;
    for (int i = 0; i < 23362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23363(int& val) {
    func_in_23363(val);
    const int x = 23363;
    std::cout << 23363;
    for (int i = 0; i < 23363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23363(int& val) {
    const int x = 23363;
    std::cout << 23363;
    for (int i = 0; i < 23363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23364(int& val) {
    func_in_23364(val);
    const int x = 23364;
    std::cout << 23364;
    for (int i = 0; i < 23364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23364(int& val) {
    const int x = 23364;
    std::cout << 23364;
    for (int i = 0; i < 23364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23365(int& val) {
    func_in_23365(val);
    const int x = 23365;
    std::cout << 23365;
    for (int i = 0; i < 23365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23365(int& val) {
    const int x = 23365;
    std::cout << 23365;
    for (int i = 0; i < 23365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23366(int& val) {
    func_in_23366(val);
    const int x = 23366;
    std::cout << 23366;
    for (int i = 0; i < 23366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23366(int& val) {
    const int x = 23366;
    std::cout << 23366;
    for (int i = 0; i < 23366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23367(int& val) {
    func_in_23367(val);
    const int x = 23367;
    std::cout << 23367;
    for (int i = 0; i < 23367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23367(int& val) {
    const int x = 23367;
    std::cout << 23367;
    for (int i = 0; i < 23367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23368(int& val) {
    func_in_23368(val);
    const int x = 23368;
    std::cout << 23368;
    for (int i = 0; i < 23368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23368(int& val) {
    const int x = 23368;
    std::cout << 23368;
    for (int i = 0; i < 23368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23369(int& val) {
    func_in_23369(val);
    const int x = 23369;
    std::cout << 23369;
    for (int i = 0; i < 23369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23369(int& val) {
    const int x = 23369;
    std::cout << 23369;
    for (int i = 0; i < 23369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23370(int& val) {
    func_in_23370(val);
    const int x = 23370;
    std::cout << 23370;
    for (int i = 0; i < 23370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23370(int& val) {
    const int x = 23370;
    std::cout << 23370;
    for (int i = 0; i < 23370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23371(int& val) {
    func_in_23371(val);
    const int x = 23371;
    std::cout << 23371;
    for (int i = 0; i < 23371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23371(int& val) {
    const int x = 23371;
    std::cout << 23371;
    for (int i = 0; i < 23371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23372(int& val) {
    func_in_23372(val);
    const int x = 23372;
    std::cout << 23372;
    for (int i = 0; i < 23372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23372(int& val) {
    const int x = 23372;
    std::cout << 23372;
    for (int i = 0; i < 23372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23373(int& val) {
    func_in_23373(val);
    const int x = 23373;
    std::cout << 23373;
    for (int i = 0; i < 23373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23373(int& val) {
    const int x = 23373;
    std::cout << 23373;
    for (int i = 0; i < 23373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23374(int& val) {
    func_in_23374(val);
    const int x = 23374;
    std::cout << 23374;
    for (int i = 0; i < 23374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23374(int& val) {
    const int x = 23374;
    std::cout << 23374;
    for (int i = 0; i < 23374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23375(int& val) {
    func_in_23375(val);
    const int x = 23375;
    std::cout << 23375;
    for (int i = 0; i < 23375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23375(int& val) {
    const int x = 23375;
    std::cout << 23375;
    for (int i = 0; i < 23375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23376(int& val) {
    func_in_23376(val);
    const int x = 23376;
    std::cout << 23376;
    for (int i = 0; i < 23376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23376(int& val) {
    const int x = 23376;
    std::cout << 23376;
    for (int i = 0; i < 23376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23377(int& val) {
    func_in_23377(val);
    const int x = 23377;
    std::cout << 23377;
    for (int i = 0; i < 23377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23377(int& val) {
    const int x = 23377;
    std::cout << 23377;
    for (int i = 0; i < 23377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23378(int& val) {
    func_in_23378(val);
    const int x = 23378;
    std::cout << 23378;
    for (int i = 0; i < 23378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23378(int& val) {
    const int x = 23378;
    std::cout << 23378;
    for (int i = 0; i < 23378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23379(int& val) {
    func_in_23379(val);
    const int x = 23379;
    std::cout << 23379;
    for (int i = 0; i < 23379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23379(int& val) {
    const int x = 23379;
    std::cout << 23379;
    for (int i = 0; i < 23379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23380(int& val) {
    func_in_23380(val);
    const int x = 23380;
    std::cout << 23380;
    for (int i = 0; i < 23380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23380(int& val) {
    const int x = 23380;
    std::cout << 23380;
    for (int i = 0; i < 23380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23381(int& val) {
    func_in_23381(val);
    const int x = 23381;
    std::cout << 23381;
    for (int i = 0; i < 23381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23381(int& val) {
    const int x = 23381;
    std::cout << 23381;
    for (int i = 0; i < 23381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23382(int& val) {
    func_in_23382(val);
    const int x = 23382;
    std::cout << 23382;
    for (int i = 0; i < 23382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23382(int& val) {
    const int x = 23382;
    std::cout << 23382;
    for (int i = 0; i < 23382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23383(int& val) {
    func_in_23383(val);
    const int x = 23383;
    std::cout << 23383;
    for (int i = 0; i < 23383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23383(int& val) {
    const int x = 23383;
    std::cout << 23383;
    for (int i = 0; i < 23383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23384(int& val) {
    func_in_23384(val);
    const int x = 23384;
    std::cout << 23384;
    for (int i = 0; i < 23384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23384(int& val) {
    const int x = 23384;
    std::cout << 23384;
    for (int i = 0; i < 23384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23385(int& val) {
    func_in_23385(val);
    const int x = 23385;
    std::cout << 23385;
    for (int i = 0; i < 23385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23385(int& val) {
    const int x = 23385;
    std::cout << 23385;
    for (int i = 0; i < 23385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23386(int& val) {
    func_in_23386(val);
    const int x = 23386;
    std::cout << 23386;
    for (int i = 0; i < 23386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23386(int& val) {
    const int x = 23386;
    std::cout << 23386;
    for (int i = 0; i < 23386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23387(int& val) {
    func_in_23387(val);
    const int x = 23387;
    std::cout << 23387;
    for (int i = 0; i < 23387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23387(int& val) {
    const int x = 23387;
    std::cout << 23387;
    for (int i = 0; i < 23387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23388(int& val) {
    func_in_23388(val);
    const int x = 23388;
    std::cout << 23388;
    for (int i = 0; i < 23388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23388(int& val) {
    const int x = 23388;
    std::cout << 23388;
    for (int i = 0; i < 23388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23389(int& val) {
    func_in_23389(val);
    const int x = 23389;
    std::cout << 23389;
    for (int i = 0; i < 23389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23389(int& val) {
    const int x = 23389;
    std::cout << 23389;
    for (int i = 0; i < 23389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23390(int& val) {
    func_in_23390(val);
    const int x = 23390;
    std::cout << 23390;
    for (int i = 0; i < 23390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23390(int& val) {
    const int x = 23390;
    std::cout << 23390;
    for (int i = 0; i < 23390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23391(int& val) {
    func_in_23391(val);
    const int x = 23391;
    std::cout << 23391;
    for (int i = 0; i < 23391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23391(int& val) {
    const int x = 23391;
    std::cout << 23391;
    for (int i = 0; i < 23391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23392(int& val) {
    func_in_23392(val);
    const int x = 23392;
    std::cout << 23392;
    for (int i = 0; i < 23392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23392(int& val) {
    const int x = 23392;
    std::cout << 23392;
    for (int i = 0; i < 23392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23393(int& val) {
    func_in_23393(val);
    const int x = 23393;
    std::cout << 23393;
    for (int i = 0; i < 23393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23393(int& val) {
    const int x = 23393;
    std::cout << 23393;
    for (int i = 0; i < 23393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23394(int& val) {
    func_in_23394(val);
    const int x = 23394;
    std::cout << 23394;
    for (int i = 0; i < 23394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23394(int& val) {
    const int x = 23394;
    std::cout << 23394;
    for (int i = 0; i < 23394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23395(int& val) {
    func_in_23395(val);
    const int x = 23395;
    std::cout << 23395;
    for (int i = 0; i < 23395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23395(int& val) {
    const int x = 23395;
    std::cout << 23395;
    for (int i = 0; i < 23395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23396(int& val) {
    func_in_23396(val);
    const int x = 23396;
    std::cout << 23396;
    for (int i = 0; i < 23396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23396(int& val) {
    const int x = 23396;
    std::cout << 23396;
    for (int i = 0; i < 23396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23397(int& val) {
    func_in_23397(val);
    const int x = 23397;
    std::cout << 23397;
    for (int i = 0; i < 23397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23397(int& val) {
    const int x = 23397;
    std::cout << 23397;
    for (int i = 0; i < 23397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23398(int& val) {
    func_in_23398(val);
    const int x = 23398;
    std::cout << 23398;
    for (int i = 0; i < 23398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23398(int& val) {
    const int x = 23398;
    std::cout << 23398;
    for (int i = 0; i < 23398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23399(int& val) {
    func_in_23399(val);
    const int x = 23399;
    std::cout << 23399;
    for (int i = 0; i < 23399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23399(int& val) {
    const int x = 23399;
    std::cout << 23399;
    for (int i = 0; i < 23399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23400(int& val) {
    func_in_23400(val);
    const int x = 23400;
    std::cout << 23400;
    for (int i = 0; i < 23400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23400(int& val) {
    const int x = 23400;
    std::cout << 23400;
    for (int i = 0; i < 23400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23401(int& val) {
    func_in_23401(val);
    const int x = 23401;
    std::cout << 23401;
    for (int i = 0; i < 23401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23401(int& val) {
    const int x = 23401;
    std::cout << 23401;
    for (int i = 0; i < 23401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23402(int& val) {
    func_in_23402(val);
    const int x = 23402;
    std::cout << 23402;
    for (int i = 0; i < 23402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23402(int& val) {
    const int x = 23402;
    std::cout << 23402;
    for (int i = 0; i < 23402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23403(int& val) {
    func_in_23403(val);
    const int x = 23403;
    std::cout << 23403;
    for (int i = 0; i < 23403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23403(int& val) {
    const int x = 23403;
    std::cout << 23403;
    for (int i = 0; i < 23403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23404(int& val) {
    func_in_23404(val);
    const int x = 23404;
    std::cout << 23404;
    for (int i = 0; i < 23404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23404(int& val) {
    const int x = 23404;
    std::cout << 23404;
    for (int i = 0; i < 23404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23405(int& val) {
    func_in_23405(val);
    const int x = 23405;
    std::cout << 23405;
    for (int i = 0; i < 23405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23405(int& val) {
    const int x = 23405;
    std::cout << 23405;
    for (int i = 0; i < 23405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23406(int& val) {
    func_in_23406(val);
    const int x = 23406;
    std::cout << 23406;
    for (int i = 0; i < 23406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23406(int& val) {
    const int x = 23406;
    std::cout << 23406;
    for (int i = 0; i < 23406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23407(int& val) {
    func_in_23407(val);
    const int x = 23407;
    std::cout << 23407;
    for (int i = 0; i < 23407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23407(int& val) {
    const int x = 23407;
    std::cout << 23407;
    for (int i = 0; i < 23407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23408(int& val) {
    func_in_23408(val);
    const int x = 23408;
    std::cout << 23408;
    for (int i = 0; i < 23408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23408(int& val) {
    const int x = 23408;
    std::cout << 23408;
    for (int i = 0; i < 23408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23409(int& val) {
    func_in_23409(val);
    const int x = 23409;
    std::cout << 23409;
    for (int i = 0; i < 23409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23409(int& val) {
    const int x = 23409;
    std::cout << 23409;
    for (int i = 0; i < 23409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23410(int& val) {
    func_in_23410(val);
    const int x = 23410;
    std::cout << 23410;
    for (int i = 0; i < 23410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23410(int& val) {
    const int x = 23410;
    std::cout << 23410;
    for (int i = 0; i < 23410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23411(int& val) {
    func_in_23411(val);
    const int x = 23411;
    std::cout << 23411;
    for (int i = 0; i < 23411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23411(int& val) {
    const int x = 23411;
    std::cout << 23411;
    for (int i = 0; i < 23411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23412(int& val) {
    func_in_23412(val);
    const int x = 23412;
    std::cout << 23412;
    for (int i = 0; i < 23412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23412(int& val) {
    const int x = 23412;
    std::cout << 23412;
    for (int i = 0; i < 23412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23413(int& val) {
    func_in_23413(val);
    const int x = 23413;
    std::cout << 23413;
    for (int i = 0; i < 23413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23413(int& val) {
    const int x = 23413;
    std::cout << 23413;
    for (int i = 0; i < 23413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23414(int& val) {
    func_in_23414(val);
    const int x = 23414;
    std::cout << 23414;
    for (int i = 0; i < 23414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23414(int& val) {
    const int x = 23414;
    std::cout << 23414;
    for (int i = 0; i < 23414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23415(int& val) {
    func_in_23415(val);
    const int x = 23415;
    std::cout << 23415;
    for (int i = 0; i < 23415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23415(int& val) {
    const int x = 23415;
    std::cout << 23415;
    for (int i = 0; i < 23415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23416(int& val) {
    func_in_23416(val);
    const int x = 23416;
    std::cout << 23416;
    for (int i = 0; i < 23416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23416(int& val) {
    const int x = 23416;
    std::cout << 23416;
    for (int i = 0; i < 23416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23417(int& val) {
    func_in_23417(val);
    const int x = 23417;
    std::cout << 23417;
    for (int i = 0; i < 23417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23417(int& val) {
    const int x = 23417;
    std::cout << 23417;
    for (int i = 0; i < 23417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23418(int& val) {
    func_in_23418(val);
    const int x = 23418;
    std::cout << 23418;
    for (int i = 0; i < 23418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23418(int& val) {
    const int x = 23418;
    std::cout << 23418;
    for (int i = 0; i < 23418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23419(int& val) {
    func_in_23419(val);
    const int x = 23419;
    std::cout << 23419;
    for (int i = 0; i < 23419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23419(int& val) {
    const int x = 23419;
    std::cout << 23419;
    for (int i = 0; i < 23419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23420(int& val) {
    func_in_23420(val);
    const int x = 23420;
    std::cout << 23420;
    for (int i = 0; i < 23420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23420(int& val) {
    const int x = 23420;
    std::cout << 23420;
    for (int i = 0; i < 23420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23421(int& val) {
    func_in_23421(val);
    const int x = 23421;
    std::cout << 23421;
    for (int i = 0; i < 23421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23421(int& val) {
    const int x = 23421;
    std::cout << 23421;
    for (int i = 0; i < 23421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23422(int& val) {
    func_in_23422(val);
    const int x = 23422;
    std::cout << 23422;
    for (int i = 0; i < 23422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23422(int& val) {
    const int x = 23422;
    std::cout << 23422;
    for (int i = 0; i < 23422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23423(int& val) {
    func_in_23423(val);
    const int x = 23423;
    std::cout << 23423;
    for (int i = 0; i < 23423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23423(int& val) {
    const int x = 23423;
    std::cout << 23423;
    for (int i = 0; i < 23423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23424(int& val) {
    func_in_23424(val);
    const int x = 23424;
    std::cout << 23424;
    for (int i = 0; i < 23424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23424(int& val) {
    const int x = 23424;
    std::cout << 23424;
    for (int i = 0; i < 23424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23425(int& val) {
    func_in_23425(val);
    const int x = 23425;
    std::cout << 23425;
    for (int i = 0; i < 23425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23425(int& val) {
    const int x = 23425;
    std::cout << 23425;
    for (int i = 0; i < 23425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23426(int& val) {
    func_in_23426(val);
    const int x = 23426;
    std::cout << 23426;
    for (int i = 0; i < 23426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23426(int& val) {
    const int x = 23426;
    std::cout << 23426;
    for (int i = 0; i < 23426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23427(int& val) {
    func_in_23427(val);
    const int x = 23427;
    std::cout << 23427;
    for (int i = 0; i < 23427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23427(int& val) {
    const int x = 23427;
    std::cout << 23427;
    for (int i = 0; i < 23427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23428(int& val) {
    func_in_23428(val);
    const int x = 23428;
    std::cout << 23428;
    for (int i = 0; i < 23428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23428(int& val) {
    const int x = 23428;
    std::cout << 23428;
    for (int i = 0; i < 23428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23429(int& val) {
    func_in_23429(val);
    const int x = 23429;
    std::cout << 23429;
    for (int i = 0; i < 23429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23429(int& val) {
    const int x = 23429;
    std::cout << 23429;
    for (int i = 0; i < 23429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23430(int& val) {
    func_in_23430(val);
    const int x = 23430;
    std::cout << 23430;
    for (int i = 0; i < 23430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23430(int& val) {
    const int x = 23430;
    std::cout << 23430;
    for (int i = 0; i < 23430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23431(int& val) {
    func_in_23431(val);
    const int x = 23431;
    std::cout << 23431;
    for (int i = 0; i < 23431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23431(int& val) {
    const int x = 23431;
    std::cout << 23431;
    for (int i = 0; i < 23431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23432(int& val) {
    func_in_23432(val);
    const int x = 23432;
    std::cout << 23432;
    for (int i = 0; i < 23432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23432(int& val) {
    const int x = 23432;
    std::cout << 23432;
    for (int i = 0; i < 23432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23433(int& val) {
    func_in_23433(val);
    const int x = 23433;
    std::cout << 23433;
    for (int i = 0; i < 23433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23433(int& val) {
    const int x = 23433;
    std::cout << 23433;
    for (int i = 0; i < 23433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23434(int& val) {
    func_in_23434(val);
    const int x = 23434;
    std::cout << 23434;
    for (int i = 0; i < 23434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23434(int& val) {
    const int x = 23434;
    std::cout << 23434;
    for (int i = 0; i < 23434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23435(int& val) {
    func_in_23435(val);
    const int x = 23435;
    std::cout << 23435;
    for (int i = 0; i < 23435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23435(int& val) {
    const int x = 23435;
    std::cout << 23435;
    for (int i = 0; i < 23435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23436(int& val) {
    func_in_23436(val);
    const int x = 23436;
    std::cout << 23436;
    for (int i = 0; i < 23436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23436(int& val) {
    const int x = 23436;
    std::cout << 23436;
    for (int i = 0; i < 23436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23437(int& val) {
    func_in_23437(val);
    const int x = 23437;
    std::cout << 23437;
    for (int i = 0; i < 23437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23437(int& val) {
    const int x = 23437;
    std::cout << 23437;
    for (int i = 0; i < 23437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23438(int& val) {
    func_in_23438(val);
    const int x = 23438;
    std::cout << 23438;
    for (int i = 0; i < 23438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23438(int& val) {
    const int x = 23438;
    std::cout << 23438;
    for (int i = 0; i < 23438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23439(int& val) {
    func_in_23439(val);
    const int x = 23439;
    std::cout << 23439;
    for (int i = 0; i < 23439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23439(int& val) {
    const int x = 23439;
    std::cout << 23439;
    for (int i = 0; i < 23439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23440(int& val) {
    func_in_23440(val);
    const int x = 23440;
    std::cout << 23440;
    for (int i = 0; i < 23440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23440(int& val) {
    const int x = 23440;
    std::cout << 23440;
    for (int i = 0; i < 23440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23441(int& val) {
    func_in_23441(val);
    const int x = 23441;
    std::cout << 23441;
    for (int i = 0; i < 23441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23441(int& val) {
    const int x = 23441;
    std::cout << 23441;
    for (int i = 0; i < 23441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23442(int& val) {
    func_in_23442(val);
    const int x = 23442;
    std::cout << 23442;
    for (int i = 0; i < 23442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23442(int& val) {
    const int x = 23442;
    std::cout << 23442;
    for (int i = 0; i < 23442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23443(int& val) {
    func_in_23443(val);
    const int x = 23443;
    std::cout << 23443;
    for (int i = 0; i < 23443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23443(int& val) {
    const int x = 23443;
    std::cout << 23443;
    for (int i = 0; i < 23443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23444(int& val) {
    func_in_23444(val);
    const int x = 23444;
    std::cout << 23444;
    for (int i = 0; i < 23444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23444(int& val) {
    const int x = 23444;
    std::cout << 23444;
    for (int i = 0; i < 23444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23445(int& val) {
    func_in_23445(val);
    const int x = 23445;
    std::cout << 23445;
    for (int i = 0; i < 23445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23445(int& val) {
    const int x = 23445;
    std::cout << 23445;
    for (int i = 0; i < 23445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23446(int& val) {
    func_in_23446(val);
    const int x = 23446;
    std::cout << 23446;
    for (int i = 0; i < 23446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23446(int& val) {
    const int x = 23446;
    std::cout << 23446;
    for (int i = 0; i < 23446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23447(int& val) {
    func_in_23447(val);
    const int x = 23447;
    std::cout << 23447;
    for (int i = 0; i < 23447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23447(int& val) {
    const int x = 23447;
    std::cout << 23447;
    for (int i = 0; i < 23447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23448(int& val) {
    func_in_23448(val);
    const int x = 23448;
    std::cout << 23448;
    for (int i = 0; i < 23448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23448(int& val) {
    const int x = 23448;
    std::cout << 23448;
    for (int i = 0; i < 23448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23449(int& val) {
    func_in_23449(val);
    const int x = 23449;
    std::cout << 23449;
    for (int i = 0; i < 23449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23449(int& val) {
    const int x = 23449;
    std::cout << 23449;
    for (int i = 0; i < 23449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23450(int& val) {
    func_in_23450(val);
    const int x = 23450;
    std::cout << 23450;
    for (int i = 0; i < 23450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23450(int& val) {
    const int x = 23450;
    std::cout << 23450;
    for (int i = 0; i < 23450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23451(int& val) {
    func_in_23451(val);
    const int x = 23451;
    std::cout << 23451;
    for (int i = 0; i < 23451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23451(int& val) {
    const int x = 23451;
    std::cout << 23451;
    for (int i = 0; i < 23451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23452(int& val) {
    func_in_23452(val);
    const int x = 23452;
    std::cout << 23452;
    for (int i = 0; i < 23452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23452(int& val) {
    const int x = 23452;
    std::cout << 23452;
    for (int i = 0; i < 23452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23453(int& val) {
    func_in_23453(val);
    const int x = 23453;
    std::cout << 23453;
    for (int i = 0; i < 23453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23453(int& val) {
    const int x = 23453;
    std::cout << 23453;
    for (int i = 0; i < 23453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23454(int& val) {
    func_in_23454(val);
    const int x = 23454;
    std::cout << 23454;
    for (int i = 0; i < 23454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23454(int& val) {
    const int x = 23454;
    std::cout << 23454;
    for (int i = 0; i < 23454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23455(int& val) {
    func_in_23455(val);
    const int x = 23455;
    std::cout << 23455;
    for (int i = 0; i < 23455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23455(int& val) {
    const int x = 23455;
    std::cout << 23455;
    for (int i = 0; i < 23455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23456(int& val) {
    func_in_23456(val);
    const int x = 23456;
    std::cout << 23456;
    for (int i = 0; i < 23456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23456(int& val) {
    const int x = 23456;
    std::cout << 23456;
    for (int i = 0; i < 23456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23457(int& val) {
    func_in_23457(val);
    const int x = 23457;
    std::cout << 23457;
    for (int i = 0; i < 23457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23457(int& val) {
    const int x = 23457;
    std::cout << 23457;
    for (int i = 0; i < 23457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23458(int& val) {
    func_in_23458(val);
    const int x = 23458;
    std::cout << 23458;
    for (int i = 0; i < 23458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23458(int& val) {
    const int x = 23458;
    std::cout << 23458;
    for (int i = 0; i < 23458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23459(int& val) {
    func_in_23459(val);
    const int x = 23459;
    std::cout << 23459;
    for (int i = 0; i < 23459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23459(int& val) {
    const int x = 23459;
    std::cout << 23459;
    for (int i = 0; i < 23459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23460(int& val) {
    func_in_23460(val);
    const int x = 23460;
    std::cout << 23460;
    for (int i = 0; i < 23460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23460(int& val) {
    const int x = 23460;
    std::cout << 23460;
    for (int i = 0; i < 23460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23461(int& val) {
    func_in_23461(val);
    const int x = 23461;
    std::cout << 23461;
    for (int i = 0; i < 23461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23461(int& val) {
    const int x = 23461;
    std::cout << 23461;
    for (int i = 0; i < 23461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23462(int& val) {
    func_in_23462(val);
    const int x = 23462;
    std::cout << 23462;
    for (int i = 0; i < 23462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23462(int& val) {
    const int x = 23462;
    std::cout << 23462;
    for (int i = 0; i < 23462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23463(int& val) {
    func_in_23463(val);
    const int x = 23463;
    std::cout << 23463;
    for (int i = 0; i < 23463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23463(int& val) {
    const int x = 23463;
    std::cout << 23463;
    for (int i = 0; i < 23463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23464(int& val) {
    func_in_23464(val);
    const int x = 23464;
    std::cout << 23464;
    for (int i = 0; i < 23464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23464(int& val) {
    const int x = 23464;
    std::cout << 23464;
    for (int i = 0; i < 23464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23465(int& val) {
    func_in_23465(val);
    const int x = 23465;
    std::cout << 23465;
    for (int i = 0; i < 23465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23465(int& val) {
    const int x = 23465;
    std::cout << 23465;
    for (int i = 0; i < 23465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23466(int& val) {
    func_in_23466(val);
    const int x = 23466;
    std::cout << 23466;
    for (int i = 0; i < 23466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23466(int& val) {
    const int x = 23466;
    std::cout << 23466;
    for (int i = 0; i < 23466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23467(int& val) {
    func_in_23467(val);
    const int x = 23467;
    std::cout << 23467;
    for (int i = 0; i < 23467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23467(int& val) {
    const int x = 23467;
    std::cout << 23467;
    for (int i = 0; i < 23467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23468(int& val) {
    func_in_23468(val);
    const int x = 23468;
    std::cout << 23468;
    for (int i = 0; i < 23468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23468(int& val) {
    const int x = 23468;
    std::cout << 23468;
    for (int i = 0; i < 23468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23469(int& val) {
    func_in_23469(val);
    const int x = 23469;
    std::cout << 23469;
    for (int i = 0; i < 23469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23469(int& val) {
    const int x = 23469;
    std::cout << 23469;
    for (int i = 0; i < 23469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23470(int& val) {
    func_in_23470(val);
    const int x = 23470;
    std::cout << 23470;
    for (int i = 0; i < 23470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23470(int& val) {
    const int x = 23470;
    std::cout << 23470;
    for (int i = 0; i < 23470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23471(int& val) {
    func_in_23471(val);
    const int x = 23471;
    std::cout << 23471;
    for (int i = 0; i < 23471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23471(int& val) {
    const int x = 23471;
    std::cout << 23471;
    for (int i = 0; i < 23471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23472(int& val) {
    func_in_23472(val);
    const int x = 23472;
    std::cout << 23472;
    for (int i = 0; i < 23472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23472(int& val) {
    const int x = 23472;
    std::cout << 23472;
    for (int i = 0; i < 23472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23473(int& val) {
    func_in_23473(val);
    const int x = 23473;
    std::cout << 23473;
    for (int i = 0; i < 23473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23473(int& val) {
    const int x = 23473;
    std::cout << 23473;
    for (int i = 0; i < 23473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23474(int& val) {
    func_in_23474(val);
    const int x = 23474;
    std::cout << 23474;
    for (int i = 0; i < 23474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23474(int& val) {
    const int x = 23474;
    std::cout << 23474;
    for (int i = 0; i < 23474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23475(int& val) {
    func_in_23475(val);
    const int x = 23475;
    std::cout << 23475;
    for (int i = 0; i < 23475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23475(int& val) {
    const int x = 23475;
    std::cout << 23475;
    for (int i = 0; i < 23475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23476(int& val) {
    func_in_23476(val);
    const int x = 23476;
    std::cout << 23476;
    for (int i = 0; i < 23476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23476(int& val) {
    const int x = 23476;
    std::cout << 23476;
    for (int i = 0; i < 23476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23477(int& val) {
    func_in_23477(val);
    const int x = 23477;
    std::cout << 23477;
    for (int i = 0; i < 23477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23477(int& val) {
    const int x = 23477;
    std::cout << 23477;
    for (int i = 0; i < 23477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23478(int& val) {
    func_in_23478(val);
    const int x = 23478;
    std::cout << 23478;
    for (int i = 0; i < 23478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23478(int& val) {
    const int x = 23478;
    std::cout << 23478;
    for (int i = 0; i < 23478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23479(int& val) {
    func_in_23479(val);
    const int x = 23479;
    std::cout << 23479;
    for (int i = 0; i < 23479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23479(int& val) {
    const int x = 23479;
    std::cout << 23479;
    for (int i = 0; i < 23479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23480(int& val) {
    func_in_23480(val);
    const int x = 23480;
    std::cout << 23480;
    for (int i = 0; i < 23480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23480(int& val) {
    const int x = 23480;
    std::cout << 23480;
    for (int i = 0; i < 23480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23481(int& val) {
    func_in_23481(val);
    const int x = 23481;
    std::cout << 23481;
    for (int i = 0; i < 23481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23481(int& val) {
    const int x = 23481;
    std::cout << 23481;
    for (int i = 0; i < 23481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23482(int& val) {
    func_in_23482(val);
    const int x = 23482;
    std::cout << 23482;
    for (int i = 0; i < 23482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23482(int& val) {
    const int x = 23482;
    std::cout << 23482;
    for (int i = 0; i < 23482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23483(int& val) {
    func_in_23483(val);
    const int x = 23483;
    std::cout << 23483;
    for (int i = 0; i < 23483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23483(int& val) {
    const int x = 23483;
    std::cout << 23483;
    for (int i = 0; i < 23483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23484(int& val) {
    func_in_23484(val);
    const int x = 23484;
    std::cout << 23484;
    for (int i = 0; i < 23484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23484(int& val) {
    const int x = 23484;
    std::cout << 23484;
    for (int i = 0; i < 23484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23485(int& val) {
    func_in_23485(val);
    const int x = 23485;
    std::cout << 23485;
    for (int i = 0; i < 23485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23485(int& val) {
    const int x = 23485;
    std::cout << 23485;
    for (int i = 0; i < 23485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23486(int& val) {
    func_in_23486(val);
    const int x = 23486;
    std::cout << 23486;
    for (int i = 0; i < 23486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23486(int& val) {
    const int x = 23486;
    std::cout << 23486;
    for (int i = 0; i < 23486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23487(int& val) {
    func_in_23487(val);
    const int x = 23487;
    std::cout << 23487;
    for (int i = 0; i < 23487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23487(int& val) {
    const int x = 23487;
    std::cout << 23487;
    for (int i = 0; i < 23487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23488(int& val) {
    func_in_23488(val);
    const int x = 23488;
    std::cout << 23488;
    for (int i = 0; i < 23488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23488(int& val) {
    const int x = 23488;
    std::cout << 23488;
    for (int i = 0; i < 23488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23489(int& val) {
    func_in_23489(val);
    const int x = 23489;
    std::cout << 23489;
    for (int i = 0; i < 23489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23489(int& val) {
    const int x = 23489;
    std::cout << 23489;
    for (int i = 0; i < 23489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23490(int& val) {
    func_in_23490(val);
    const int x = 23490;
    std::cout << 23490;
    for (int i = 0; i < 23490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23490(int& val) {
    const int x = 23490;
    std::cout << 23490;
    for (int i = 0; i < 23490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23491(int& val) {
    func_in_23491(val);
    const int x = 23491;
    std::cout << 23491;
    for (int i = 0; i < 23491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23491(int& val) {
    const int x = 23491;
    std::cout << 23491;
    for (int i = 0; i < 23491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23492(int& val) {
    func_in_23492(val);
    const int x = 23492;
    std::cout << 23492;
    for (int i = 0; i < 23492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23492(int& val) {
    const int x = 23492;
    std::cout << 23492;
    for (int i = 0; i < 23492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23493(int& val) {
    func_in_23493(val);
    const int x = 23493;
    std::cout << 23493;
    for (int i = 0; i < 23493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23493(int& val) {
    const int x = 23493;
    std::cout << 23493;
    for (int i = 0; i < 23493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23494(int& val) {
    func_in_23494(val);
    const int x = 23494;
    std::cout << 23494;
    for (int i = 0; i < 23494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23494(int& val) {
    const int x = 23494;
    std::cout << 23494;
    for (int i = 0; i < 23494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23495(int& val) {
    func_in_23495(val);
    const int x = 23495;
    std::cout << 23495;
    for (int i = 0; i < 23495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23495(int& val) {
    const int x = 23495;
    std::cout << 23495;
    for (int i = 0; i < 23495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23496(int& val) {
    func_in_23496(val);
    const int x = 23496;
    std::cout << 23496;
    for (int i = 0; i < 23496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23496(int& val) {
    const int x = 23496;
    std::cout << 23496;
    for (int i = 0; i < 23496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23497(int& val) {
    func_in_23497(val);
    const int x = 23497;
    std::cout << 23497;
    for (int i = 0; i < 23497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23497(int& val) {
    const int x = 23497;
    std::cout << 23497;
    for (int i = 0; i < 23497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23498(int& val) {
    func_in_23498(val);
    const int x = 23498;
    std::cout << 23498;
    for (int i = 0; i < 23498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23498(int& val) {
    const int x = 23498;
    std::cout << 23498;
    for (int i = 0; i < 23498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23499(int& val) {
    func_in_23499(val);
    const int x = 23499;
    std::cout << 23499;
    for (int i = 0; i < 23499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23499(int& val) {
    const int x = 23499;
    std::cout << 23499;
    for (int i = 0; i < 23499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23500(int& val) {
    func_in_23500(val);
    const int x = 23500;
    std::cout << 23500;
    for (int i = 0; i < 23500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23500(int& val) {
    const int x = 23500;
    std::cout << 23500;
    for (int i = 0; i < 23500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23501(int& val) {
    func_in_23501(val);
    const int x = 23501;
    std::cout << 23501;
    for (int i = 0; i < 23501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23501(int& val) {
    const int x = 23501;
    std::cout << 23501;
    for (int i = 0; i < 23501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23502(int& val) {
    func_in_23502(val);
    const int x = 23502;
    std::cout << 23502;
    for (int i = 0; i < 23502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23502(int& val) {
    const int x = 23502;
    std::cout << 23502;
    for (int i = 0; i < 23502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23503(int& val) {
    func_in_23503(val);
    const int x = 23503;
    std::cout << 23503;
    for (int i = 0; i < 23503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23503(int& val) {
    const int x = 23503;
    std::cout << 23503;
    for (int i = 0; i < 23503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23504(int& val) {
    func_in_23504(val);
    const int x = 23504;
    std::cout << 23504;
    for (int i = 0; i < 23504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23504(int& val) {
    const int x = 23504;
    std::cout << 23504;
    for (int i = 0; i < 23504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23505(int& val) {
    func_in_23505(val);
    const int x = 23505;
    std::cout << 23505;
    for (int i = 0; i < 23505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23505(int& val) {
    const int x = 23505;
    std::cout << 23505;
    for (int i = 0; i < 23505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23506(int& val) {
    func_in_23506(val);
    const int x = 23506;
    std::cout << 23506;
    for (int i = 0; i < 23506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23506(int& val) {
    const int x = 23506;
    std::cout << 23506;
    for (int i = 0; i < 23506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23507(int& val) {
    func_in_23507(val);
    const int x = 23507;
    std::cout << 23507;
    for (int i = 0; i < 23507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23507(int& val) {
    const int x = 23507;
    std::cout << 23507;
    for (int i = 0; i < 23507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23508(int& val) {
    func_in_23508(val);
    const int x = 23508;
    std::cout << 23508;
    for (int i = 0; i < 23508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23508(int& val) {
    const int x = 23508;
    std::cout << 23508;
    for (int i = 0; i < 23508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23509(int& val) {
    func_in_23509(val);
    const int x = 23509;
    std::cout << 23509;
    for (int i = 0; i < 23509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23509(int& val) {
    const int x = 23509;
    std::cout << 23509;
    for (int i = 0; i < 23509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23510(int& val) {
    func_in_23510(val);
    const int x = 23510;
    std::cout << 23510;
    for (int i = 0; i < 23510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23510(int& val) {
    const int x = 23510;
    std::cout << 23510;
    for (int i = 0; i < 23510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23511(int& val) {
    func_in_23511(val);
    const int x = 23511;
    std::cout << 23511;
    for (int i = 0; i < 23511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23511(int& val) {
    const int x = 23511;
    std::cout << 23511;
    for (int i = 0; i < 23511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23512(int& val) {
    func_in_23512(val);
    const int x = 23512;
    std::cout << 23512;
    for (int i = 0; i < 23512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23512(int& val) {
    const int x = 23512;
    std::cout << 23512;
    for (int i = 0; i < 23512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23513(int& val) {
    func_in_23513(val);
    const int x = 23513;
    std::cout << 23513;
    for (int i = 0; i < 23513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23513(int& val) {
    const int x = 23513;
    std::cout << 23513;
    for (int i = 0; i < 23513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23514(int& val) {
    func_in_23514(val);
    const int x = 23514;
    std::cout << 23514;
    for (int i = 0; i < 23514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23514(int& val) {
    const int x = 23514;
    std::cout << 23514;
    for (int i = 0; i < 23514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23515(int& val) {
    func_in_23515(val);
    const int x = 23515;
    std::cout << 23515;
    for (int i = 0; i < 23515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23515(int& val) {
    const int x = 23515;
    std::cout << 23515;
    for (int i = 0; i < 23515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23516(int& val) {
    func_in_23516(val);
    const int x = 23516;
    std::cout << 23516;
    for (int i = 0; i < 23516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23516(int& val) {
    const int x = 23516;
    std::cout << 23516;
    for (int i = 0; i < 23516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23517(int& val) {
    func_in_23517(val);
    const int x = 23517;
    std::cout << 23517;
    for (int i = 0; i < 23517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23517(int& val) {
    const int x = 23517;
    std::cout << 23517;
    for (int i = 0; i < 23517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23518(int& val) {
    func_in_23518(val);
    const int x = 23518;
    std::cout << 23518;
    for (int i = 0; i < 23518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23518(int& val) {
    const int x = 23518;
    std::cout << 23518;
    for (int i = 0; i < 23518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23519(int& val) {
    func_in_23519(val);
    const int x = 23519;
    std::cout << 23519;
    for (int i = 0; i < 23519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23519(int& val) {
    const int x = 23519;
    std::cout << 23519;
    for (int i = 0; i < 23519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23520(int& val) {
    func_in_23520(val);
    const int x = 23520;
    std::cout << 23520;
    for (int i = 0; i < 23520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23520(int& val) {
    const int x = 23520;
    std::cout << 23520;
    for (int i = 0; i < 23520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23521(int& val) {
    func_in_23521(val);
    const int x = 23521;
    std::cout << 23521;
    for (int i = 0; i < 23521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23521(int& val) {
    const int x = 23521;
    std::cout << 23521;
    for (int i = 0; i < 23521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23522(int& val) {
    func_in_23522(val);
    const int x = 23522;
    std::cout << 23522;
    for (int i = 0; i < 23522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23522(int& val) {
    const int x = 23522;
    std::cout << 23522;
    for (int i = 0; i < 23522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23523(int& val) {
    func_in_23523(val);
    const int x = 23523;
    std::cout << 23523;
    for (int i = 0; i < 23523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23523(int& val) {
    const int x = 23523;
    std::cout << 23523;
    for (int i = 0; i < 23523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23524(int& val) {
    func_in_23524(val);
    const int x = 23524;
    std::cout << 23524;
    for (int i = 0; i < 23524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23524(int& val) {
    const int x = 23524;
    std::cout << 23524;
    for (int i = 0; i < 23524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23525(int& val) {
    func_in_23525(val);
    const int x = 23525;
    std::cout << 23525;
    for (int i = 0; i < 23525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23525(int& val) {
    const int x = 23525;
    std::cout << 23525;
    for (int i = 0; i < 23525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23526(int& val) {
    func_in_23526(val);
    const int x = 23526;
    std::cout << 23526;
    for (int i = 0; i < 23526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23526(int& val) {
    const int x = 23526;
    std::cout << 23526;
    for (int i = 0; i < 23526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23527(int& val) {
    func_in_23527(val);
    const int x = 23527;
    std::cout << 23527;
    for (int i = 0; i < 23527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23527(int& val) {
    const int x = 23527;
    std::cout << 23527;
    for (int i = 0; i < 23527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23528(int& val) {
    func_in_23528(val);
    const int x = 23528;
    std::cout << 23528;
    for (int i = 0; i < 23528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23528(int& val) {
    const int x = 23528;
    std::cout << 23528;
    for (int i = 0; i < 23528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23529(int& val) {
    func_in_23529(val);
    const int x = 23529;
    std::cout << 23529;
    for (int i = 0; i < 23529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23529(int& val) {
    const int x = 23529;
    std::cout << 23529;
    for (int i = 0; i < 23529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23530(int& val) {
    func_in_23530(val);
    const int x = 23530;
    std::cout << 23530;
    for (int i = 0; i < 23530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23530(int& val) {
    const int x = 23530;
    std::cout << 23530;
    for (int i = 0; i < 23530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23531(int& val) {
    func_in_23531(val);
    const int x = 23531;
    std::cout << 23531;
    for (int i = 0; i < 23531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23531(int& val) {
    const int x = 23531;
    std::cout << 23531;
    for (int i = 0; i < 23531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23532(int& val) {
    func_in_23532(val);
    const int x = 23532;
    std::cout << 23532;
    for (int i = 0; i < 23532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23532(int& val) {
    const int x = 23532;
    std::cout << 23532;
    for (int i = 0; i < 23532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23533(int& val) {
    func_in_23533(val);
    const int x = 23533;
    std::cout << 23533;
    for (int i = 0; i < 23533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23533(int& val) {
    const int x = 23533;
    std::cout << 23533;
    for (int i = 0; i < 23533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23534(int& val) {
    func_in_23534(val);
    const int x = 23534;
    std::cout << 23534;
    for (int i = 0; i < 23534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23534(int& val) {
    const int x = 23534;
    std::cout << 23534;
    for (int i = 0; i < 23534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23535(int& val) {
    func_in_23535(val);
    const int x = 23535;
    std::cout << 23535;
    for (int i = 0; i < 23535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23535(int& val) {
    const int x = 23535;
    std::cout << 23535;
    for (int i = 0; i < 23535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23536(int& val) {
    func_in_23536(val);
    const int x = 23536;
    std::cout << 23536;
    for (int i = 0; i < 23536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23536(int& val) {
    const int x = 23536;
    std::cout << 23536;
    for (int i = 0; i < 23536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23537(int& val) {
    func_in_23537(val);
    const int x = 23537;
    std::cout << 23537;
    for (int i = 0; i < 23537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23537(int& val) {
    const int x = 23537;
    std::cout << 23537;
    for (int i = 0; i < 23537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23538(int& val) {
    func_in_23538(val);
    const int x = 23538;
    std::cout << 23538;
    for (int i = 0; i < 23538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23538(int& val) {
    const int x = 23538;
    std::cout << 23538;
    for (int i = 0; i < 23538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23539(int& val) {
    func_in_23539(val);
    const int x = 23539;
    std::cout << 23539;
    for (int i = 0; i < 23539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23539(int& val) {
    const int x = 23539;
    std::cout << 23539;
    for (int i = 0; i < 23539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23540(int& val) {
    func_in_23540(val);
    const int x = 23540;
    std::cout << 23540;
    for (int i = 0; i < 23540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23540(int& val) {
    const int x = 23540;
    std::cout << 23540;
    for (int i = 0; i < 23540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23541(int& val) {
    func_in_23541(val);
    const int x = 23541;
    std::cout << 23541;
    for (int i = 0; i < 23541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23541(int& val) {
    const int x = 23541;
    std::cout << 23541;
    for (int i = 0; i < 23541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23542(int& val) {
    func_in_23542(val);
    const int x = 23542;
    std::cout << 23542;
    for (int i = 0; i < 23542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23542(int& val) {
    const int x = 23542;
    std::cout << 23542;
    for (int i = 0; i < 23542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23543(int& val) {
    func_in_23543(val);
    const int x = 23543;
    std::cout << 23543;
    for (int i = 0; i < 23543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23543(int& val) {
    const int x = 23543;
    std::cout << 23543;
    for (int i = 0; i < 23543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23544(int& val) {
    func_in_23544(val);
    const int x = 23544;
    std::cout << 23544;
    for (int i = 0; i < 23544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23544(int& val) {
    const int x = 23544;
    std::cout << 23544;
    for (int i = 0; i < 23544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23545(int& val) {
    func_in_23545(val);
    const int x = 23545;
    std::cout << 23545;
    for (int i = 0; i < 23545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23545(int& val) {
    const int x = 23545;
    std::cout << 23545;
    for (int i = 0; i < 23545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23546(int& val) {
    func_in_23546(val);
    const int x = 23546;
    std::cout << 23546;
    for (int i = 0; i < 23546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23546(int& val) {
    const int x = 23546;
    std::cout << 23546;
    for (int i = 0; i < 23546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23547(int& val) {
    func_in_23547(val);
    const int x = 23547;
    std::cout << 23547;
    for (int i = 0; i < 23547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23547(int& val) {
    const int x = 23547;
    std::cout << 23547;
    for (int i = 0; i < 23547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23548(int& val) {
    func_in_23548(val);
    const int x = 23548;
    std::cout << 23548;
    for (int i = 0; i < 23548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23548(int& val) {
    const int x = 23548;
    std::cout << 23548;
    for (int i = 0; i < 23548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23549(int& val) {
    func_in_23549(val);
    const int x = 23549;
    std::cout << 23549;
    for (int i = 0; i < 23549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23549(int& val) {
    const int x = 23549;
    std::cout << 23549;
    for (int i = 0; i < 23549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23550(int& val) {
    func_in_23550(val);
    const int x = 23550;
    std::cout << 23550;
    for (int i = 0; i < 23550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23550(int& val) {
    const int x = 23550;
    std::cout << 23550;
    for (int i = 0; i < 23550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23551(int& val) {
    func_in_23551(val);
    const int x = 23551;
    std::cout << 23551;
    for (int i = 0; i < 23551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23551(int& val) {
    const int x = 23551;
    std::cout << 23551;
    for (int i = 0; i < 23551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23552(int& val) {
    func_in_23552(val);
    const int x = 23552;
    std::cout << 23552;
    for (int i = 0; i < 23552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23552(int& val) {
    const int x = 23552;
    std::cout << 23552;
    for (int i = 0; i < 23552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23553(int& val) {
    func_in_23553(val);
    const int x = 23553;
    std::cout << 23553;
    for (int i = 0; i < 23553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23553(int& val) {
    const int x = 23553;
    std::cout << 23553;
    for (int i = 0; i < 23553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23554(int& val) {
    func_in_23554(val);
    const int x = 23554;
    std::cout << 23554;
    for (int i = 0; i < 23554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23554(int& val) {
    const int x = 23554;
    std::cout << 23554;
    for (int i = 0; i < 23554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23555(int& val) {
    func_in_23555(val);
    const int x = 23555;
    std::cout << 23555;
    for (int i = 0; i < 23555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23555(int& val) {
    const int x = 23555;
    std::cout << 23555;
    for (int i = 0; i < 23555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23556(int& val) {
    func_in_23556(val);
    const int x = 23556;
    std::cout << 23556;
    for (int i = 0; i < 23556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23556(int& val) {
    const int x = 23556;
    std::cout << 23556;
    for (int i = 0; i < 23556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23557(int& val) {
    func_in_23557(val);
    const int x = 23557;
    std::cout << 23557;
    for (int i = 0; i < 23557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23557(int& val) {
    const int x = 23557;
    std::cout << 23557;
    for (int i = 0; i < 23557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23558(int& val) {
    func_in_23558(val);
    const int x = 23558;
    std::cout << 23558;
    for (int i = 0; i < 23558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23558(int& val) {
    const int x = 23558;
    std::cout << 23558;
    for (int i = 0; i < 23558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23559(int& val) {
    func_in_23559(val);
    const int x = 23559;
    std::cout << 23559;
    for (int i = 0; i < 23559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23559(int& val) {
    const int x = 23559;
    std::cout << 23559;
    for (int i = 0; i < 23559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23560(int& val) {
    func_in_23560(val);
    const int x = 23560;
    std::cout << 23560;
    for (int i = 0; i < 23560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23560(int& val) {
    const int x = 23560;
    std::cout << 23560;
    for (int i = 0; i < 23560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23561(int& val) {
    func_in_23561(val);
    const int x = 23561;
    std::cout << 23561;
    for (int i = 0; i < 23561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23561(int& val) {
    const int x = 23561;
    std::cout << 23561;
    for (int i = 0; i < 23561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23562(int& val) {
    func_in_23562(val);
    const int x = 23562;
    std::cout << 23562;
    for (int i = 0; i < 23562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23562(int& val) {
    const int x = 23562;
    std::cout << 23562;
    for (int i = 0; i < 23562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23563(int& val) {
    func_in_23563(val);
    const int x = 23563;
    std::cout << 23563;
    for (int i = 0; i < 23563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23563(int& val) {
    const int x = 23563;
    std::cout << 23563;
    for (int i = 0; i < 23563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23564(int& val) {
    func_in_23564(val);
    const int x = 23564;
    std::cout << 23564;
    for (int i = 0; i < 23564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23564(int& val) {
    const int x = 23564;
    std::cout << 23564;
    for (int i = 0; i < 23564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23565(int& val) {
    func_in_23565(val);
    const int x = 23565;
    std::cout << 23565;
    for (int i = 0; i < 23565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23565(int& val) {
    const int x = 23565;
    std::cout << 23565;
    for (int i = 0; i < 23565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23566(int& val) {
    func_in_23566(val);
    const int x = 23566;
    std::cout << 23566;
    for (int i = 0; i < 23566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23566(int& val) {
    const int x = 23566;
    std::cout << 23566;
    for (int i = 0; i < 23566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23567(int& val) {
    func_in_23567(val);
    const int x = 23567;
    std::cout << 23567;
    for (int i = 0; i < 23567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23567(int& val) {
    const int x = 23567;
    std::cout << 23567;
    for (int i = 0; i < 23567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23568(int& val) {
    func_in_23568(val);
    const int x = 23568;
    std::cout << 23568;
    for (int i = 0; i < 23568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23568(int& val) {
    const int x = 23568;
    std::cout << 23568;
    for (int i = 0; i < 23568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23569(int& val) {
    func_in_23569(val);
    const int x = 23569;
    std::cout << 23569;
    for (int i = 0; i < 23569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23569(int& val) {
    const int x = 23569;
    std::cout << 23569;
    for (int i = 0; i < 23569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23570(int& val) {
    func_in_23570(val);
    const int x = 23570;
    std::cout << 23570;
    for (int i = 0; i < 23570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23570(int& val) {
    const int x = 23570;
    std::cout << 23570;
    for (int i = 0; i < 23570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23571(int& val) {
    func_in_23571(val);
    const int x = 23571;
    std::cout << 23571;
    for (int i = 0; i < 23571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23571(int& val) {
    const int x = 23571;
    std::cout << 23571;
    for (int i = 0; i < 23571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23572(int& val) {
    func_in_23572(val);
    const int x = 23572;
    std::cout << 23572;
    for (int i = 0; i < 23572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23572(int& val) {
    const int x = 23572;
    std::cout << 23572;
    for (int i = 0; i < 23572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23573(int& val) {
    func_in_23573(val);
    const int x = 23573;
    std::cout << 23573;
    for (int i = 0; i < 23573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23573(int& val) {
    const int x = 23573;
    std::cout << 23573;
    for (int i = 0; i < 23573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23574(int& val) {
    func_in_23574(val);
    const int x = 23574;
    std::cout << 23574;
    for (int i = 0; i < 23574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23574(int& val) {
    const int x = 23574;
    std::cout << 23574;
    for (int i = 0; i < 23574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23575(int& val) {
    func_in_23575(val);
    const int x = 23575;
    std::cout << 23575;
    for (int i = 0; i < 23575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23575(int& val) {
    const int x = 23575;
    std::cout << 23575;
    for (int i = 0; i < 23575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23576(int& val) {
    func_in_23576(val);
    const int x = 23576;
    std::cout << 23576;
    for (int i = 0; i < 23576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23576(int& val) {
    const int x = 23576;
    std::cout << 23576;
    for (int i = 0; i < 23576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23577(int& val) {
    func_in_23577(val);
    const int x = 23577;
    std::cout << 23577;
    for (int i = 0; i < 23577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23577(int& val) {
    const int x = 23577;
    std::cout << 23577;
    for (int i = 0; i < 23577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23578(int& val) {
    func_in_23578(val);
    const int x = 23578;
    std::cout << 23578;
    for (int i = 0; i < 23578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23578(int& val) {
    const int x = 23578;
    std::cout << 23578;
    for (int i = 0; i < 23578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23579(int& val) {
    func_in_23579(val);
    const int x = 23579;
    std::cout << 23579;
    for (int i = 0; i < 23579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23579(int& val) {
    const int x = 23579;
    std::cout << 23579;
    for (int i = 0; i < 23579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23580(int& val) {
    func_in_23580(val);
    const int x = 23580;
    std::cout << 23580;
    for (int i = 0; i < 23580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23580(int& val) {
    const int x = 23580;
    std::cout << 23580;
    for (int i = 0; i < 23580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23581(int& val) {
    func_in_23581(val);
    const int x = 23581;
    std::cout << 23581;
    for (int i = 0; i < 23581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23581(int& val) {
    const int x = 23581;
    std::cout << 23581;
    for (int i = 0; i < 23581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23582(int& val) {
    func_in_23582(val);
    const int x = 23582;
    std::cout << 23582;
    for (int i = 0; i < 23582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23582(int& val) {
    const int x = 23582;
    std::cout << 23582;
    for (int i = 0; i < 23582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23583(int& val) {
    func_in_23583(val);
    const int x = 23583;
    std::cout << 23583;
    for (int i = 0; i < 23583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23583(int& val) {
    const int x = 23583;
    std::cout << 23583;
    for (int i = 0; i < 23583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23584(int& val) {
    func_in_23584(val);
    const int x = 23584;
    std::cout << 23584;
    for (int i = 0; i < 23584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23584(int& val) {
    const int x = 23584;
    std::cout << 23584;
    for (int i = 0; i < 23584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23585(int& val) {
    func_in_23585(val);
    const int x = 23585;
    std::cout << 23585;
    for (int i = 0; i < 23585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23585(int& val) {
    const int x = 23585;
    std::cout << 23585;
    for (int i = 0; i < 23585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23586(int& val) {
    func_in_23586(val);
    const int x = 23586;
    std::cout << 23586;
    for (int i = 0; i < 23586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23586(int& val) {
    const int x = 23586;
    std::cout << 23586;
    for (int i = 0; i < 23586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23587(int& val) {
    func_in_23587(val);
    const int x = 23587;
    std::cout << 23587;
    for (int i = 0; i < 23587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23587(int& val) {
    const int x = 23587;
    std::cout << 23587;
    for (int i = 0; i < 23587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23588(int& val) {
    func_in_23588(val);
    const int x = 23588;
    std::cout << 23588;
    for (int i = 0; i < 23588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23588(int& val) {
    const int x = 23588;
    std::cout << 23588;
    for (int i = 0; i < 23588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23589(int& val) {
    func_in_23589(val);
    const int x = 23589;
    std::cout << 23589;
    for (int i = 0; i < 23589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23589(int& val) {
    const int x = 23589;
    std::cout << 23589;
    for (int i = 0; i < 23589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23590(int& val) {
    func_in_23590(val);
    const int x = 23590;
    std::cout << 23590;
    for (int i = 0; i < 23590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23590(int& val) {
    const int x = 23590;
    std::cout << 23590;
    for (int i = 0; i < 23590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23591(int& val) {
    func_in_23591(val);
    const int x = 23591;
    std::cout << 23591;
    for (int i = 0; i < 23591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23591(int& val) {
    const int x = 23591;
    std::cout << 23591;
    for (int i = 0; i < 23591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23592(int& val) {
    func_in_23592(val);
    const int x = 23592;
    std::cout << 23592;
    for (int i = 0; i < 23592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23592(int& val) {
    const int x = 23592;
    std::cout << 23592;
    for (int i = 0; i < 23592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23593(int& val) {
    func_in_23593(val);
    const int x = 23593;
    std::cout << 23593;
    for (int i = 0; i < 23593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23593(int& val) {
    const int x = 23593;
    std::cout << 23593;
    for (int i = 0; i < 23593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23594(int& val) {
    func_in_23594(val);
    const int x = 23594;
    std::cout << 23594;
    for (int i = 0; i < 23594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23594(int& val) {
    const int x = 23594;
    std::cout << 23594;
    for (int i = 0; i < 23594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23595(int& val) {
    func_in_23595(val);
    const int x = 23595;
    std::cout << 23595;
    for (int i = 0; i < 23595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23595(int& val) {
    const int x = 23595;
    std::cout << 23595;
    for (int i = 0; i < 23595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23596(int& val) {
    func_in_23596(val);
    const int x = 23596;
    std::cout << 23596;
    for (int i = 0; i < 23596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23596(int& val) {
    const int x = 23596;
    std::cout << 23596;
    for (int i = 0; i < 23596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23597(int& val) {
    func_in_23597(val);
    const int x = 23597;
    std::cout << 23597;
    for (int i = 0; i < 23597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23597(int& val) {
    const int x = 23597;
    std::cout << 23597;
    for (int i = 0; i < 23597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23598(int& val) {
    func_in_23598(val);
    const int x = 23598;
    std::cout << 23598;
    for (int i = 0; i < 23598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23598(int& val) {
    const int x = 23598;
    std::cout << 23598;
    for (int i = 0; i < 23598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23599(int& val) {
    func_in_23599(val);
    const int x = 23599;
    std::cout << 23599;
    for (int i = 0; i < 23599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23599(int& val) {
    const int x = 23599;
    std::cout << 23599;
    for (int i = 0; i < 23599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23600(int& val) {
    func_in_23600(val);
    const int x = 23600;
    std::cout << 23600;
    for (int i = 0; i < 23600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23600(int& val) {
    const int x = 23600;
    std::cout << 23600;
    for (int i = 0; i < 23600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23601(int& val) {
    func_in_23601(val);
    const int x = 23601;
    std::cout << 23601;
    for (int i = 0; i < 23601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23601(int& val) {
    const int x = 23601;
    std::cout << 23601;
    for (int i = 0; i < 23601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23602(int& val) {
    func_in_23602(val);
    const int x = 23602;
    std::cout << 23602;
    for (int i = 0; i < 23602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23602(int& val) {
    const int x = 23602;
    std::cout << 23602;
    for (int i = 0; i < 23602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23603(int& val) {
    func_in_23603(val);
    const int x = 23603;
    std::cout << 23603;
    for (int i = 0; i < 23603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23603(int& val) {
    const int x = 23603;
    std::cout << 23603;
    for (int i = 0; i < 23603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23604(int& val) {
    func_in_23604(val);
    const int x = 23604;
    std::cout << 23604;
    for (int i = 0; i < 23604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23604(int& val) {
    const int x = 23604;
    std::cout << 23604;
    for (int i = 0; i < 23604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23605(int& val) {
    func_in_23605(val);
    const int x = 23605;
    std::cout << 23605;
    for (int i = 0; i < 23605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23605(int& val) {
    const int x = 23605;
    std::cout << 23605;
    for (int i = 0; i < 23605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23606(int& val) {
    func_in_23606(val);
    const int x = 23606;
    std::cout << 23606;
    for (int i = 0; i < 23606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23606(int& val) {
    const int x = 23606;
    std::cout << 23606;
    for (int i = 0; i < 23606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23607(int& val) {
    func_in_23607(val);
    const int x = 23607;
    std::cout << 23607;
    for (int i = 0; i < 23607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23607(int& val) {
    const int x = 23607;
    std::cout << 23607;
    for (int i = 0; i < 23607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23608(int& val) {
    func_in_23608(val);
    const int x = 23608;
    std::cout << 23608;
    for (int i = 0; i < 23608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23608(int& val) {
    const int x = 23608;
    std::cout << 23608;
    for (int i = 0; i < 23608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23609(int& val) {
    func_in_23609(val);
    const int x = 23609;
    std::cout << 23609;
    for (int i = 0; i < 23609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23609(int& val) {
    const int x = 23609;
    std::cout << 23609;
    for (int i = 0; i < 23609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23610(int& val) {
    func_in_23610(val);
    const int x = 23610;
    std::cout << 23610;
    for (int i = 0; i < 23610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23610(int& val) {
    const int x = 23610;
    std::cout << 23610;
    for (int i = 0; i < 23610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23611(int& val) {
    func_in_23611(val);
    const int x = 23611;
    std::cout << 23611;
    for (int i = 0; i < 23611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23611(int& val) {
    const int x = 23611;
    std::cout << 23611;
    for (int i = 0; i < 23611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23612(int& val) {
    func_in_23612(val);
    const int x = 23612;
    std::cout << 23612;
    for (int i = 0; i < 23612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23612(int& val) {
    const int x = 23612;
    std::cout << 23612;
    for (int i = 0; i < 23612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23613(int& val) {
    func_in_23613(val);
    const int x = 23613;
    std::cout << 23613;
    for (int i = 0; i < 23613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23613(int& val) {
    const int x = 23613;
    std::cout << 23613;
    for (int i = 0; i < 23613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23614(int& val) {
    func_in_23614(val);
    const int x = 23614;
    std::cout << 23614;
    for (int i = 0; i < 23614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23614(int& val) {
    const int x = 23614;
    std::cout << 23614;
    for (int i = 0; i < 23614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23615(int& val) {
    func_in_23615(val);
    const int x = 23615;
    std::cout << 23615;
    for (int i = 0; i < 23615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23615(int& val) {
    const int x = 23615;
    std::cout << 23615;
    for (int i = 0; i < 23615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23616(int& val) {
    func_in_23616(val);
    const int x = 23616;
    std::cout << 23616;
    for (int i = 0; i < 23616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23616(int& val) {
    const int x = 23616;
    std::cout << 23616;
    for (int i = 0; i < 23616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23617(int& val) {
    func_in_23617(val);
    const int x = 23617;
    std::cout << 23617;
    for (int i = 0; i < 23617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23617(int& val) {
    const int x = 23617;
    std::cout << 23617;
    for (int i = 0; i < 23617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23618(int& val) {
    func_in_23618(val);
    const int x = 23618;
    std::cout << 23618;
    for (int i = 0; i < 23618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23618(int& val) {
    const int x = 23618;
    std::cout << 23618;
    for (int i = 0; i < 23618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23619(int& val) {
    func_in_23619(val);
    const int x = 23619;
    std::cout << 23619;
    for (int i = 0; i < 23619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23619(int& val) {
    const int x = 23619;
    std::cout << 23619;
    for (int i = 0; i < 23619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23620(int& val) {
    func_in_23620(val);
    const int x = 23620;
    std::cout << 23620;
    for (int i = 0; i < 23620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23620(int& val) {
    const int x = 23620;
    std::cout << 23620;
    for (int i = 0; i < 23620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23621(int& val) {
    func_in_23621(val);
    const int x = 23621;
    std::cout << 23621;
    for (int i = 0; i < 23621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23621(int& val) {
    const int x = 23621;
    std::cout << 23621;
    for (int i = 0; i < 23621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23622(int& val) {
    func_in_23622(val);
    const int x = 23622;
    std::cout << 23622;
    for (int i = 0; i < 23622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23622(int& val) {
    const int x = 23622;
    std::cout << 23622;
    for (int i = 0; i < 23622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23623(int& val) {
    func_in_23623(val);
    const int x = 23623;
    std::cout << 23623;
    for (int i = 0; i < 23623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23623(int& val) {
    const int x = 23623;
    std::cout << 23623;
    for (int i = 0; i < 23623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23624(int& val) {
    func_in_23624(val);
    const int x = 23624;
    std::cout << 23624;
    for (int i = 0; i < 23624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23624(int& val) {
    const int x = 23624;
    std::cout << 23624;
    for (int i = 0; i < 23624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23625(int& val) {
    func_in_23625(val);
    const int x = 23625;
    std::cout << 23625;
    for (int i = 0; i < 23625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23625(int& val) {
    const int x = 23625;
    std::cout << 23625;
    for (int i = 0; i < 23625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23626(int& val) {
    func_in_23626(val);
    const int x = 23626;
    std::cout << 23626;
    for (int i = 0; i < 23626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23626(int& val) {
    const int x = 23626;
    std::cout << 23626;
    for (int i = 0; i < 23626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23627(int& val) {
    func_in_23627(val);
    const int x = 23627;
    std::cout << 23627;
    for (int i = 0; i < 23627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23627(int& val) {
    const int x = 23627;
    std::cout << 23627;
    for (int i = 0; i < 23627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23628(int& val) {
    func_in_23628(val);
    const int x = 23628;
    std::cout << 23628;
    for (int i = 0; i < 23628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23628(int& val) {
    const int x = 23628;
    std::cout << 23628;
    for (int i = 0; i < 23628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23629(int& val) {
    func_in_23629(val);
    const int x = 23629;
    std::cout << 23629;
    for (int i = 0; i < 23629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23629(int& val) {
    const int x = 23629;
    std::cout << 23629;
    for (int i = 0; i < 23629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23630(int& val) {
    func_in_23630(val);
    const int x = 23630;
    std::cout << 23630;
    for (int i = 0; i < 23630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23630(int& val) {
    const int x = 23630;
    std::cout << 23630;
    for (int i = 0; i < 23630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23631(int& val) {
    func_in_23631(val);
    const int x = 23631;
    std::cout << 23631;
    for (int i = 0; i < 23631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23631(int& val) {
    const int x = 23631;
    std::cout << 23631;
    for (int i = 0; i < 23631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23632(int& val) {
    func_in_23632(val);
    const int x = 23632;
    std::cout << 23632;
    for (int i = 0; i < 23632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23632(int& val) {
    const int x = 23632;
    std::cout << 23632;
    for (int i = 0; i < 23632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23633(int& val) {
    func_in_23633(val);
    const int x = 23633;
    std::cout << 23633;
    for (int i = 0; i < 23633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23633(int& val) {
    const int x = 23633;
    std::cout << 23633;
    for (int i = 0; i < 23633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23634(int& val) {
    func_in_23634(val);
    const int x = 23634;
    std::cout << 23634;
    for (int i = 0; i < 23634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23634(int& val) {
    const int x = 23634;
    std::cout << 23634;
    for (int i = 0; i < 23634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23635(int& val) {
    func_in_23635(val);
    const int x = 23635;
    std::cout << 23635;
    for (int i = 0; i < 23635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23635(int& val) {
    const int x = 23635;
    std::cout << 23635;
    for (int i = 0; i < 23635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23636(int& val) {
    func_in_23636(val);
    const int x = 23636;
    std::cout << 23636;
    for (int i = 0; i < 23636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23636(int& val) {
    const int x = 23636;
    std::cout << 23636;
    for (int i = 0; i < 23636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23637(int& val) {
    func_in_23637(val);
    const int x = 23637;
    std::cout << 23637;
    for (int i = 0; i < 23637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23637(int& val) {
    const int x = 23637;
    std::cout << 23637;
    for (int i = 0; i < 23637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23638(int& val) {
    func_in_23638(val);
    const int x = 23638;
    std::cout << 23638;
    for (int i = 0; i < 23638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23638(int& val) {
    const int x = 23638;
    std::cout << 23638;
    for (int i = 0; i < 23638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23639(int& val) {
    func_in_23639(val);
    const int x = 23639;
    std::cout << 23639;
    for (int i = 0; i < 23639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23639(int& val) {
    const int x = 23639;
    std::cout << 23639;
    for (int i = 0; i < 23639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23640(int& val) {
    func_in_23640(val);
    const int x = 23640;
    std::cout << 23640;
    for (int i = 0; i < 23640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23640(int& val) {
    const int x = 23640;
    std::cout << 23640;
    for (int i = 0; i < 23640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23641(int& val) {
    func_in_23641(val);
    const int x = 23641;
    std::cout << 23641;
    for (int i = 0; i < 23641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23641(int& val) {
    const int x = 23641;
    std::cout << 23641;
    for (int i = 0; i < 23641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23642(int& val) {
    func_in_23642(val);
    const int x = 23642;
    std::cout << 23642;
    for (int i = 0; i < 23642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23642(int& val) {
    const int x = 23642;
    std::cout << 23642;
    for (int i = 0; i < 23642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23643(int& val) {
    func_in_23643(val);
    const int x = 23643;
    std::cout << 23643;
    for (int i = 0; i < 23643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23643(int& val) {
    const int x = 23643;
    std::cout << 23643;
    for (int i = 0; i < 23643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23644(int& val) {
    func_in_23644(val);
    const int x = 23644;
    std::cout << 23644;
    for (int i = 0; i < 23644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23644(int& val) {
    const int x = 23644;
    std::cout << 23644;
    for (int i = 0; i < 23644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23645(int& val) {
    func_in_23645(val);
    const int x = 23645;
    std::cout << 23645;
    for (int i = 0; i < 23645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23645(int& val) {
    const int x = 23645;
    std::cout << 23645;
    for (int i = 0; i < 23645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23646(int& val) {
    func_in_23646(val);
    const int x = 23646;
    std::cout << 23646;
    for (int i = 0; i < 23646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23646(int& val) {
    const int x = 23646;
    std::cout << 23646;
    for (int i = 0; i < 23646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23647(int& val) {
    func_in_23647(val);
    const int x = 23647;
    std::cout << 23647;
    for (int i = 0; i < 23647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23647(int& val) {
    const int x = 23647;
    std::cout << 23647;
    for (int i = 0; i < 23647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23648(int& val) {
    func_in_23648(val);
    const int x = 23648;
    std::cout << 23648;
    for (int i = 0; i < 23648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23648(int& val) {
    const int x = 23648;
    std::cout << 23648;
    for (int i = 0; i < 23648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23649(int& val) {
    func_in_23649(val);
    const int x = 23649;
    std::cout << 23649;
    for (int i = 0; i < 23649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23649(int& val) {
    const int x = 23649;
    std::cout << 23649;
    for (int i = 0; i < 23649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23650(int& val) {
    func_in_23650(val);
    const int x = 23650;
    std::cout << 23650;
    for (int i = 0; i < 23650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23650(int& val) {
    const int x = 23650;
    std::cout << 23650;
    for (int i = 0; i < 23650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23651(int& val) {
    func_in_23651(val);
    const int x = 23651;
    std::cout << 23651;
    for (int i = 0; i < 23651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23651(int& val) {
    const int x = 23651;
    std::cout << 23651;
    for (int i = 0; i < 23651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23652(int& val) {
    func_in_23652(val);
    const int x = 23652;
    std::cout << 23652;
    for (int i = 0; i < 23652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23652(int& val) {
    const int x = 23652;
    std::cout << 23652;
    for (int i = 0; i < 23652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23653(int& val) {
    func_in_23653(val);
    const int x = 23653;
    std::cout << 23653;
    for (int i = 0; i < 23653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23653(int& val) {
    const int x = 23653;
    std::cout << 23653;
    for (int i = 0; i < 23653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23654(int& val) {
    func_in_23654(val);
    const int x = 23654;
    std::cout << 23654;
    for (int i = 0; i < 23654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23654(int& val) {
    const int x = 23654;
    std::cout << 23654;
    for (int i = 0; i < 23654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23655(int& val) {
    func_in_23655(val);
    const int x = 23655;
    std::cout << 23655;
    for (int i = 0; i < 23655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23655(int& val) {
    const int x = 23655;
    std::cout << 23655;
    for (int i = 0; i < 23655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23656(int& val) {
    func_in_23656(val);
    const int x = 23656;
    std::cout << 23656;
    for (int i = 0; i < 23656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23656(int& val) {
    const int x = 23656;
    std::cout << 23656;
    for (int i = 0; i < 23656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23657(int& val) {
    func_in_23657(val);
    const int x = 23657;
    std::cout << 23657;
    for (int i = 0; i < 23657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23657(int& val) {
    const int x = 23657;
    std::cout << 23657;
    for (int i = 0; i < 23657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23658(int& val) {
    func_in_23658(val);
    const int x = 23658;
    std::cout << 23658;
    for (int i = 0; i < 23658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23658(int& val) {
    const int x = 23658;
    std::cout << 23658;
    for (int i = 0; i < 23658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23659(int& val) {
    func_in_23659(val);
    const int x = 23659;
    std::cout << 23659;
    for (int i = 0; i < 23659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23659(int& val) {
    const int x = 23659;
    std::cout << 23659;
    for (int i = 0; i < 23659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23660(int& val) {
    func_in_23660(val);
    const int x = 23660;
    std::cout << 23660;
    for (int i = 0; i < 23660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23660(int& val) {
    const int x = 23660;
    std::cout << 23660;
    for (int i = 0; i < 23660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23661(int& val) {
    func_in_23661(val);
    const int x = 23661;
    std::cout << 23661;
    for (int i = 0; i < 23661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23661(int& val) {
    const int x = 23661;
    std::cout << 23661;
    for (int i = 0; i < 23661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23662(int& val) {
    func_in_23662(val);
    const int x = 23662;
    std::cout << 23662;
    for (int i = 0; i < 23662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23662(int& val) {
    const int x = 23662;
    std::cout << 23662;
    for (int i = 0; i < 23662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23663(int& val) {
    func_in_23663(val);
    const int x = 23663;
    std::cout << 23663;
    for (int i = 0; i < 23663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23663(int& val) {
    const int x = 23663;
    std::cout << 23663;
    for (int i = 0; i < 23663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23664(int& val) {
    func_in_23664(val);
    const int x = 23664;
    std::cout << 23664;
    for (int i = 0; i < 23664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23664(int& val) {
    const int x = 23664;
    std::cout << 23664;
    for (int i = 0; i < 23664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23665(int& val) {
    func_in_23665(val);
    const int x = 23665;
    std::cout << 23665;
    for (int i = 0; i < 23665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23665(int& val) {
    const int x = 23665;
    std::cout << 23665;
    for (int i = 0; i < 23665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23666(int& val) {
    func_in_23666(val);
    const int x = 23666;
    std::cout << 23666;
    for (int i = 0; i < 23666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23666(int& val) {
    const int x = 23666;
    std::cout << 23666;
    for (int i = 0; i < 23666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23667(int& val) {
    func_in_23667(val);
    const int x = 23667;
    std::cout << 23667;
    for (int i = 0; i < 23667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23667(int& val) {
    const int x = 23667;
    std::cout << 23667;
    for (int i = 0; i < 23667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23668(int& val) {
    func_in_23668(val);
    const int x = 23668;
    std::cout << 23668;
    for (int i = 0; i < 23668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23668(int& val) {
    const int x = 23668;
    std::cout << 23668;
    for (int i = 0; i < 23668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23669(int& val) {
    func_in_23669(val);
    const int x = 23669;
    std::cout << 23669;
    for (int i = 0; i < 23669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23669(int& val) {
    const int x = 23669;
    std::cout << 23669;
    for (int i = 0; i < 23669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23670(int& val) {
    func_in_23670(val);
    const int x = 23670;
    std::cout << 23670;
    for (int i = 0; i < 23670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23670(int& val) {
    const int x = 23670;
    std::cout << 23670;
    for (int i = 0; i < 23670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23671(int& val) {
    func_in_23671(val);
    const int x = 23671;
    std::cout << 23671;
    for (int i = 0; i < 23671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23671(int& val) {
    const int x = 23671;
    std::cout << 23671;
    for (int i = 0; i < 23671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23672(int& val) {
    func_in_23672(val);
    const int x = 23672;
    std::cout << 23672;
    for (int i = 0; i < 23672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23672(int& val) {
    const int x = 23672;
    std::cout << 23672;
    for (int i = 0; i < 23672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23673(int& val) {
    func_in_23673(val);
    const int x = 23673;
    std::cout << 23673;
    for (int i = 0; i < 23673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23673(int& val) {
    const int x = 23673;
    std::cout << 23673;
    for (int i = 0; i < 23673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23674(int& val) {
    func_in_23674(val);
    const int x = 23674;
    std::cout << 23674;
    for (int i = 0; i < 23674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23674(int& val) {
    const int x = 23674;
    std::cout << 23674;
    for (int i = 0; i < 23674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23675(int& val) {
    func_in_23675(val);
    const int x = 23675;
    std::cout << 23675;
    for (int i = 0; i < 23675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23675(int& val) {
    const int x = 23675;
    std::cout << 23675;
    for (int i = 0; i < 23675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23676(int& val) {
    func_in_23676(val);
    const int x = 23676;
    std::cout << 23676;
    for (int i = 0; i < 23676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23676(int& val) {
    const int x = 23676;
    std::cout << 23676;
    for (int i = 0; i < 23676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23677(int& val) {
    func_in_23677(val);
    const int x = 23677;
    std::cout << 23677;
    for (int i = 0; i < 23677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23677(int& val) {
    const int x = 23677;
    std::cout << 23677;
    for (int i = 0; i < 23677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23678(int& val) {
    func_in_23678(val);
    const int x = 23678;
    std::cout << 23678;
    for (int i = 0; i < 23678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23678(int& val) {
    const int x = 23678;
    std::cout << 23678;
    for (int i = 0; i < 23678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23679(int& val) {
    func_in_23679(val);
    const int x = 23679;
    std::cout << 23679;
    for (int i = 0; i < 23679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23679(int& val) {
    const int x = 23679;
    std::cout << 23679;
    for (int i = 0; i < 23679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23680(int& val) {
    func_in_23680(val);
    const int x = 23680;
    std::cout << 23680;
    for (int i = 0; i < 23680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23680(int& val) {
    const int x = 23680;
    std::cout << 23680;
    for (int i = 0; i < 23680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23681(int& val) {
    func_in_23681(val);
    const int x = 23681;
    std::cout << 23681;
    for (int i = 0; i < 23681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23681(int& val) {
    const int x = 23681;
    std::cout << 23681;
    for (int i = 0; i < 23681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23682(int& val) {
    func_in_23682(val);
    const int x = 23682;
    std::cout << 23682;
    for (int i = 0; i < 23682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23682(int& val) {
    const int x = 23682;
    std::cout << 23682;
    for (int i = 0; i < 23682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23683(int& val) {
    func_in_23683(val);
    const int x = 23683;
    std::cout << 23683;
    for (int i = 0; i < 23683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23683(int& val) {
    const int x = 23683;
    std::cout << 23683;
    for (int i = 0; i < 23683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23684(int& val) {
    func_in_23684(val);
    const int x = 23684;
    std::cout << 23684;
    for (int i = 0; i < 23684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23684(int& val) {
    const int x = 23684;
    std::cout << 23684;
    for (int i = 0; i < 23684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23685(int& val) {
    func_in_23685(val);
    const int x = 23685;
    std::cout << 23685;
    for (int i = 0; i < 23685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23685(int& val) {
    const int x = 23685;
    std::cout << 23685;
    for (int i = 0; i < 23685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23686(int& val) {
    func_in_23686(val);
    const int x = 23686;
    std::cout << 23686;
    for (int i = 0; i < 23686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23686(int& val) {
    const int x = 23686;
    std::cout << 23686;
    for (int i = 0; i < 23686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23687(int& val) {
    func_in_23687(val);
    const int x = 23687;
    std::cout << 23687;
    for (int i = 0; i < 23687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23687(int& val) {
    const int x = 23687;
    std::cout << 23687;
    for (int i = 0; i < 23687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23688(int& val) {
    func_in_23688(val);
    const int x = 23688;
    std::cout << 23688;
    for (int i = 0; i < 23688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23688(int& val) {
    const int x = 23688;
    std::cout << 23688;
    for (int i = 0; i < 23688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23689(int& val) {
    func_in_23689(val);
    const int x = 23689;
    std::cout << 23689;
    for (int i = 0; i < 23689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23689(int& val) {
    const int x = 23689;
    std::cout << 23689;
    for (int i = 0; i < 23689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23690(int& val) {
    func_in_23690(val);
    const int x = 23690;
    std::cout << 23690;
    for (int i = 0; i < 23690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23690(int& val) {
    const int x = 23690;
    std::cout << 23690;
    for (int i = 0; i < 23690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23691(int& val) {
    func_in_23691(val);
    const int x = 23691;
    std::cout << 23691;
    for (int i = 0; i < 23691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23691(int& val) {
    const int x = 23691;
    std::cout << 23691;
    for (int i = 0; i < 23691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23692(int& val) {
    func_in_23692(val);
    const int x = 23692;
    std::cout << 23692;
    for (int i = 0; i < 23692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23692(int& val) {
    const int x = 23692;
    std::cout << 23692;
    for (int i = 0; i < 23692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23693(int& val) {
    func_in_23693(val);
    const int x = 23693;
    std::cout << 23693;
    for (int i = 0; i < 23693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23693(int& val) {
    const int x = 23693;
    std::cout << 23693;
    for (int i = 0; i < 23693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23694(int& val) {
    func_in_23694(val);
    const int x = 23694;
    std::cout << 23694;
    for (int i = 0; i < 23694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23694(int& val) {
    const int x = 23694;
    std::cout << 23694;
    for (int i = 0; i < 23694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23695(int& val) {
    func_in_23695(val);
    const int x = 23695;
    std::cout << 23695;
    for (int i = 0; i < 23695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23695(int& val) {
    const int x = 23695;
    std::cout << 23695;
    for (int i = 0; i < 23695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23696(int& val) {
    func_in_23696(val);
    const int x = 23696;
    std::cout << 23696;
    for (int i = 0; i < 23696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23696(int& val) {
    const int x = 23696;
    std::cout << 23696;
    for (int i = 0; i < 23696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23697(int& val) {
    func_in_23697(val);
    const int x = 23697;
    std::cout << 23697;
    for (int i = 0; i < 23697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23697(int& val) {
    const int x = 23697;
    std::cout << 23697;
    for (int i = 0; i < 23697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23698(int& val) {
    func_in_23698(val);
    const int x = 23698;
    std::cout << 23698;
    for (int i = 0; i < 23698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23698(int& val) {
    const int x = 23698;
    std::cout << 23698;
    for (int i = 0; i < 23698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23699(int& val) {
    func_in_23699(val);
    const int x = 23699;
    std::cout << 23699;
    for (int i = 0; i < 23699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23699(int& val) {
    const int x = 23699;
    std::cout << 23699;
    for (int i = 0; i < 23699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23700(int& val) {
    func_in_23700(val);
    const int x = 23700;
    std::cout << 23700;
    for (int i = 0; i < 23700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23700(int& val) {
    const int x = 23700;
    std::cout << 23700;
    for (int i = 0; i < 23700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23701(int& val) {
    func_in_23701(val);
    const int x = 23701;
    std::cout << 23701;
    for (int i = 0; i < 23701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23701(int& val) {
    const int x = 23701;
    std::cout << 23701;
    for (int i = 0; i < 23701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23702(int& val) {
    func_in_23702(val);
    const int x = 23702;
    std::cout << 23702;
    for (int i = 0; i < 23702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23702(int& val) {
    const int x = 23702;
    std::cout << 23702;
    for (int i = 0; i < 23702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23703(int& val) {
    func_in_23703(val);
    const int x = 23703;
    std::cout << 23703;
    for (int i = 0; i < 23703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23703(int& val) {
    const int x = 23703;
    std::cout << 23703;
    for (int i = 0; i < 23703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23704(int& val) {
    func_in_23704(val);
    const int x = 23704;
    std::cout << 23704;
    for (int i = 0; i < 23704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23704(int& val) {
    const int x = 23704;
    std::cout << 23704;
    for (int i = 0; i < 23704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23705(int& val) {
    func_in_23705(val);
    const int x = 23705;
    std::cout << 23705;
    for (int i = 0; i < 23705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23705(int& val) {
    const int x = 23705;
    std::cout << 23705;
    for (int i = 0; i < 23705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23706(int& val) {
    func_in_23706(val);
    const int x = 23706;
    std::cout << 23706;
    for (int i = 0; i < 23706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23706(int& val) {
    const int x = 23706;
    std::cout << 23706;
    for (int i = 0; i < 23706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23707(int& val) {
    func_in_23707(val);
    const int x = 23707;
    std::cout << 23707;
    for (int i = 0; i < 23707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23707(int& val) {
    const int x = 23707;
    std::cout << 23707;
    for (int i = 0; i < 23707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23708(int& val) {
    func_in_23708(val);
    const int x = 23708;
    std::cout << 23708;
    for (int i = 0; i < 23708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23708(int& val) {
    const int x = 23708;
    std::cout << 23708;
    for (int i = 0; i < 23708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23709(int& val) {
    func_in_23709(val);
    const int x = 23709;
    std::cout << 23709;
    for (int i = 0; i < 23709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23709(int& val) {
    const int x = 23709;
    std::cout << 23709;
    for (int i = 0; i < 23709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23710(int& val) {
    func_in_23710(val);
    const int x = 23710;
    std::cout << 23710;
    for (int i = 0; i < 23710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23710(int& val) {
    const int x = 23710;
    std::cout << 23710;
    for (int i = 0; i < 23710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23711(int& val) {
    func_in_23711(val);
    const int x = 23711;
    std::cout << 23711;
    for (int i = 0; i < 23711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23711(int& val) {
    const int x = 23711;
    std::cout << 23711;
    for (int i = 0; i < 23711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23712(int& val) {
    func_in_23712(val);
    const int x = 23712;
    std::cout << 23712;
    for (int i = 0; i < 23712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23712(int& val) {
    const int x = 23712;
    std::cout << 23712;
    for (int i = 0; i < 23712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23713(int& val) {
    func_in_23713(val);
    const int x = 23713;
    std::cout << 23713;
    for (int i = 0; i < 23713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23713(int& val) {
    const int x = 23713;
    std::cout << 23713;
    for (int i = 0; i < 23713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23714(int& val) {
    func_in_23714(val);
    const int x = 23714;
    std::cout << 23714;
    for (int i = 0; i < 23714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23714(int& val) {
    const int x = 23714;
    std::cout << 23714;
    for (int i = 0; i < 23714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23715(int& val) {
    func_in_23715(val);
    const int x = 23715;
    std::cout << 23715;
    for (int i = 0; i < 23715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23715(int& val) {
    const int x = 23715;
    std::cout << 23715;
    for (int i = 0; i < 23715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23716(int& val) {
    func_in_23716(val);
    const int x = 23716;
    std::cout << 23716;
    for (int i = 0; i < 23716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23716(int& val) {
    const int x = 23716;
    std::cout << 23716;
    for (int i = 0; i < 23716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23717(int& val) {
    func_in_23717(val);
    const int x = 23717;
    std::cout << 23717;
    for (int i = 0; i < 23717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23717(int& val) {
    const int x = 23717;
    std::cout << 23717;
    for (int i = 0; i < 23717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23718(int& val) {
    func_in_23718(val);
    const int x = 23718;
    std::cout << 23718;
    for (int i = 0; i < 23718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23718(int& val) {
    const int x = 23718;
    std::cout << 23718;
    for (int i = 0; i < 23718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23719(int& val) {
    func_in_23719(val);
    const int x = 23719;
    std::cout << 23719;
    for (int i = 0; i < 23719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23719(int& val) {
    const int x = 23719;
    std::cout << 23719;
    for (int i = 0; i < 23719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23720(int& val) {
    func_in_23720(val);
    const int x = 23720;
    std::cout << 23720;
    for (int i = 0; i < 23720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23720(int& val) {
    const int x = 23720;
    std::cout << 23720;
    for (int i = 0; i < 23720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23721(int& val) {
    func_in_23721(val);
    const int x = 23721;
    std::cout << 23721;
    for (int i = 0; i < 23721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23721(int& val) {
    const int x = 23721;
    std::cout << 23721;
    for (int i = 0; i < 23721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23722(int& val) {
    func_in_23722(val);
    const int x = 23722;
    std::cout << 23722;
    for (int i = 0; i < 23722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23722(int& val) {
    const int x = 23722;
    std::cout << 23722;
    for (int i = 0; i < 23722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23723(int& val) {
    func_in_23723(val);
    const int x = 23723;
    std::cout << 23723;
    for (int i = 0; i < 23723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23723(int& val) {
    const int x = 23723;
    std::cout << 23723;
    for (int i = 0; i < 23723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23724(int& val) {
    func_in_23724(val);
    const int x = 23724;
    std::cout << 23724;
    for (int i = 0; i < 23724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23724(int& val) {
    const int x = 23724;
    std::cout << 23724;
    for (int i = 0; i < 23724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23725(int& val) {
    func_in_23725(val);
    const int x = 23725;
    std::cout << 23725;
    for (int i = 0; i < 23725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23725(int& val) {
    const int x = 23725;
    std::cout << 23725;
    for (int i = 0; i < 23725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23726(int& val) {
    func_in_23726(val);
    const int x = 23726;
    std::cout << 23726;
    for (int i = 0; i < 23726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23726(int& val) {
    const int x = 23726;
    std::cout << 23726;
    for (int i = 0; i < 23726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23727(int& val) {
    func_in_23727(val);
    const int x = 23727;
    std::cout << 23727;
    for (int i = 0; i < 23727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23727(int& val) {
    const int x = 23727;
    std::cout << 23727;
    for (int i = 0; i < 23727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23728(int& val) {
    func_in_23728(val);
    const int x = 23728;
    std::cout << 23728;
    for (int i = 0; i < 23728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23728(int& val) {
    const int x = 23728;
    std::cout << 23728;
    for (int i = 0; i < 23728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23729(int& val) {
    func_in_23729(val);
    const int x = 23729;
    std::cout << 23729;
    for (int i = 0; i < 23729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23729(int& val) {
    const int x = 23729;
    std::cout << 23729;
    for (int i = 0; i < 23729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23730(int& val) {
    func_in_23730(val);
    const int x = 23730;
    std::cout << 23730;
    for (int i = 0; i < 23730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23730(int& val) {
    const int x = 23730;
    std::cout << 23730;
    for (int i = 0; i < 23730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23731(int& val) {
    func_in_23731(val);
    const int x = 23731;
    std::cout << 23731;
    for (int i = 0; i < 23731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23731(int& val) {
    const int x = 23731;
    std::cout << 23731;
    for (int i = 0; i < 23731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23732(int& val) {
    func_in_23732(val);
    const int x = 23732;
    std::cout << 23732;
    for (int i = 0; i < 23732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23732(int& val) {
    const int x = 23732;
    std::cout << 23732;
    for (int i = 0; i < 23732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23733(int& val) {
    func_in_23733(val);
    const int x = 23733;
    std::cout << 23733;
    for (int i = 0; i < 23733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23733(int& val) {
    const int x = 23733;
    std::cout << 23733;
    for (int i = 0; i < 23733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23734(int& val) {
    func_in_23734(val);
    const int x = 23734;
    std::cout << 23734;
    for (int i = 0; i < 23734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23734(int& val) {
    const int x = 23734;
    std::cout << 23734;
    for (int i = 0; i < 23734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23735(int& val) {
    func_in_23735(val);
    const int x = 23735;
    std::cout << 23735;
    for (int i = 0; i < 23735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23735(int& val) {
    const int x = 23735;
    std::cout << 23735;
    for (int i = 0; i < 23735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23736(int& val) {
    func_in_23736(val);
    const int x = 23736;
    std::cout << 23736;
    for (int i = 0; i < 23736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23736(int& val) {
    const int x = 23736;
    std::cout << 23736;
    for (int i = 0; i < 23736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23737(int& val) {
    func_in_23737(val);
    const int x = 23737;
    std::cout << 23737;
    for (int i = 0; i < 23737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23737(int& val) {
    const int x = 23737;
    std::cout << 23737;
    for (int i = 0; i < 23737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23738(int& val) {
    func_in_23738(val);
    const int x = 23738;
    std::cout << 23738;
    for (int i = 0; i < 23738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23738(int& val) {
    const int x = 23738;
    std::cout << 23738;
    for (int i = 0; i < 23738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23739(int& val) {
    func_in_23739(val);
    const int x = 23739;
    std::cout << 23739;
    for (int i = 0; i < 23739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23739(int& val) {
    const int x = 23739;
    std::cout << 23739;
    for (int i = 0; i < 23739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23740(int& val) {
    func_in_23740(val);
    const int x = 23740;
    std::cout << 23740;
    for (int i = 0; i < 23740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23740(int& val) {
    const int x = 23740;
    std::cout << 23740;
    for (int i = 0; i < 23740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23741(int& val) {
    func_in_23741(val);
    const int x = 23741;
    std::cout << 23741;
    for (int i = 0; i < 23741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23741(int& val) {
    const int x = 23741;
    std::cout << 23741;
    for (int i = 0; i < 23741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23742(int& val) {
    func_in_23742(val);
    const int x = 23742;
    std::cout << 23742;
    for (int i = 0; i < 23742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23742(int& val) {
    const int x = 23742;
    std::cout << 23742;
    for (int i = 0; i < 23742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23743(int& val) {
    func_in_23743(val);
    const int x = 23743;
    std::cout << 23743;
    for (int i = 0; i < 23743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23743(int& val) {
    const int x = 23743;
    std::cout << 23743;
    for (int i = 0; i < 23743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23744(int& val) {
    func_in_23744(val);
    const int x = 23744;
    std::cout << 23744;
    for (int i = 0; i < 23744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23744(int& val) {
    const int x = 23744;
    std::cout << 23744;
    for (int i = 0; i < 23744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23745(int& val) {
    func_in_23745(val);
    const int x = 23745;
    std::cout << 23745;
    for (int i = 0; i < 23745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23745(int& val) {
    const int x = 23745;
    std::cout << 23745;
    for (int i = 0; i < 23745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23746(int& val) {
    func_in_23746(val);
    const int x = 23746;
    std::cout << 23746;
    for (int i = 0; i < 23746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23746(int& val) {
    const int x = 23746;
    std::cout << 23746;
    for (int i = 0; i < 23746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23747(int& val) {
    func_in_23747(val);
    const int x = 23747;
    std::cout << 23747;
    for (int i = 0; i < 23747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23747(int& val) {
    const int x = 23747;
    std::cout << 23747;
    for (int i = 0; i < 23747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23748(int& val) {
    func_in_23748(val);
    const int x = 23748;
    std::cout << 23748;
    for (int i = 0; i < 23748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23748(int& val) {
    const int x = 23748;
    std::cout << 23748;
    for (int i = 0; i < 23748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23749(int& val) {
    func_in_23749(val);
    const int x = 23749;
    std::cout << 23749;
    for (int i = 0; i < 23749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23749(int& val) {
    const int x = 23749;
    std::cout << 23749;
    for (int i = 0; i < 23749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23750(int& val) {
    func_in_23750(val);
    const int x = 23750;
    std::cout << 23750;
    for (int i = 0; i < 23750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23750(int& val) {
    const int x = 23750;
    std::cout << 23750;
    for (int i = 0; i < 23750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23751(int& val) {
    func_in_23751(val);
    const int x = 23751;
    std::cout << 23751;
    for (int i = 0; i < 23751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23751(int& val) {
    const int x = 23751;
    std::cout << 23751;
    for (int i = 0; i < 23751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23752(int& val) {
    func_in_23752(val);
    const int x = 23752;
    std::cout << 23752;
    for (int i = 0; i < 23752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23752(int& val) {
    const int x = 23752;
    std::cout << 23752;
    for (int i = 0; i < 23752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23753(int& val) {
    func_in_23753(val);
    const int x = 23753;
    std::cout << 23753;
    for (int i = 0; i < 23753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23753(int& val) {
    const int x = 23753;
    std::cout << 23753;
    for (int i = 0; i < 23753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23754(int& val) {
    func_in_23754(val);
    const int x = 23754;
    std::cout << 23754;
    for (int i = 0; i < 23754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23754(int& val) {
    const int x = 23754;
    std::cout << 23754;
    for (int i = 0; i < 23754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23755(int& val) {
    func_in_23755(val);
    const int x = 23755;
    std::cout << 23755;
    for (int i = 0; i < 23755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23755(int& val) {
    const int x = 23755;
    std::cout << 23755;
    for (int i = 0; i < 23755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23756(int& val) {
    func_in_23756(val);
    const int x = 23756;
    std::cout << 23756;
    for (int i = 0; i < 23756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23756(int& val) {
    const int x = 23756;
    std::cout << 23756;
    for (int i = 0; i < 23756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23757(int& val) {
    func_in_23757(val);
    const int x = 23757;
    std::cout << 23757;
    for (int i = 0; i < 23757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23757(int& val) {
    const int x = 23757;
    std::cout << 23757;
    for (int i = 0; i < 23757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23758(int& val) {
    func_in_23758(val);
    const int x = 23758;
    std::cout << 23758;
    for (int i = 0; i < 23758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23758(int& val) {
    const int x = 23758;
    std::cout << 23758;
    for (int i = 0; i < 23758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23759(int& val) {
    func_in_23759(val);
    const int x = 23759;
    std::cout << 23759;
    for (int i = 0; i < 23759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23759(int& val) {
    const int x = 23759;
    std::cout << 23759;
    for (int i = 0; i < 23759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23760(int& val) {
    func_in_23760(val);
    const int x = 23760;
    std::cout << 23760;
    for (int i = 0; i < 23760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23760(int& val) {
    const int x = 23760;
    std::cout << 23760;
    for (int i = 0; i < 23760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23761(int& val) {
    func_in_23761(val);
    const int x = 23761;
    std::cout << 23761;
    for (int i = 0; i < 23761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23761(int& val) {
    const int x = 23761;
    std::cout << 23761;
    for (int i = 0; i < 23761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23762(int& val) {
    func_in_23762(val);
    const int x = 23762;
    std::cout << 23762;
    for (int i = 0; i < 23762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23762(int& val) {
    const int x = 23762;
    std::cout << 23762;
    for (int i = 0; i < 23762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23763(int& val) {
    func_in_23763(val);
    const int x = 23763;
    std::cout << 23763;
    for (int i = 0; i < 23763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23763(int& val) {
    const int x = 23763;
    std::cout << 23763;
    for (int i = 0; i < 23763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23764(int& val) {
    func_in_23764(val);
    const int x = 23764;
    std::cout << 23764;
    for (int i = 0; i < 23764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23764(int& val) {
    const int x = 23764;
    std::cout << 23764;
    for (int i = 0; i < 23764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23765(int& val) {
    func_in_23765(val);
    const int x = 23765;
    std::cout << 23765;
    for (int i = 0; i < 23765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23765(int& val) {
    const int x = 23765;
    std::cout << 23765;
    for (int i = 0; i < 23765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23766(int& val) {
    func_in_23766(val);
    const int x = 23766;
    std::cout << 23766;
    for (int i = 0; i < 23766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23766(int& val) {
    const int x = 23766;
    std::cout << 23766;
    for (int i = 0; i < 23766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23767(int& val) {
    func_in_23767(val);
    const int x = 23767;
    std::cout << 23767;
    for (int i = 0; i < 23767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23767(int& val) {
    const int x = 23767;
    std::cout << 23767;
    for (int i = 0; i < 23767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23768(int& val) {
    func_in_23768(val);
    const int x = 23768;
    std::cout << 23768;
    for (int i = 0; i < 23768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23768(int& val) {
    const int x = 23768;
    std::cout << 23768;
    for (int i = 0; i < 23768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23769(int& val) {
    func_in_23769(val);
    const int x = 23769;
    std::cout << 23769;
    for (int i = 0; i < 23769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23769(int& val) {
    const int x = 23769;
    std::cout << 23769;
    for (int i = 0; i < 23769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23770(int& val) {
    func_in_23770(val);
    const int x = 23770;
    std::cout << 23770;
    for (int i = 0; i < 23770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23770(int& val) {
    const int x = 23770;
    std::cout << 23770;
    for (int i = 0; i < 23770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23771(int& val) {
    func_in_23771(val);
    const int x = 23771;
    std::cout << 23771;
    for (int i = 0; i < 23771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23771(int& val) {
    const int x = 23771;
    std::cout << 23771;
    for (int i = 0; i < 23771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23772(int& val) {
    func_in_23772(val);
    const int x = 23772;
    std::cout << 23772;
    for (int i = 0; i < 23772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23772(int& val) {
    const int x = 23772;
    std::cout << 23772;
    for (int i = 0; i < 23772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23773(int& val) {
    func_in_23773(val);
    const int x = 23773;
    std::cout << 23773;
    for (int i = 0; i < 23773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23773(int& val) {
    const int x = 23773;
    std::cout << 23773;
    for (int i = 0; i < 23773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23774(int& val) {
    func_in_23774(val);
    const int x = 23774;
    std::cout << 23774;
    for (int i = 0; i < 23774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23774(int& val) {
    const int x = 23774;
    std::cout << 23774;
    for (int i = 0; i < 23774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23775(int& val) {
    func_in_23775(val);
    const int x = 23775;
    std::cout << 23775;
    for (int i = 0; i < 23775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23775(int& val) {
    const int x = 23775;
    std::cout << 23775;
    for (int i = 0; i < 23775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23776(int& val) {
    func_in_23776(val);
    const int x = 23776;
    std::cout << 23776;
    for (int i = 0; i < 23776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23776(int& val) {
    const int x = 23776;
    std::cout << 23776;
    for (int i = 0; i < 23776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23777(int& val) {
    func_in_23777(val);
    const int x = 23777;
    std::cout << 23777;
    for (int i = 0; i < 23777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23777(int& val) {
    const int x = 23777;
    std::cout << 23777;
    for (int i = 0; i < 23777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23778(int& val) {
    func_in_23778(val);
    const int x = 23778;
    std::cout << 23778;
    for (int i = 0; i < 23778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23778(int& val) {
    const int x = 23778;
    std::cout << 23778;
    for (int i = 0; i < 23778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23779(int& val) {
    func_in_23779(val);
    const int x = 23779;
    std::cout << 23779;
    for (int i = 0; i < 23779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23779(int& val) {
    const int x = 23779;
    std::cout << 23779;
    for (int i = 0; i < 23779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23780(int& val) {
    func_in_23780(val);
    const int x = 23780;
    std::cout << 23780;
    for (int i = 0; i < 23780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23780(int& val) {
    const int x = 23780;
    std::cout << 23780;
    for (int i = 0; i < 23780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23781(int& val) {
    func_in_23781(val);
    const int x = 23781;
    std::cout << 23781;
    for (int i = 0; i < 23781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23781(int& val) {
    const int x = 23781;
    std::cout << 23781;
    for (int i = 0; i < 23781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23782(int& val) {
    func_in_23782(val);
    const int x = 23782;
    std::cout << 23782;
    for (int i = 0; i < 23782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23782(int& val) {
    const int x = 23782;
    std::cout << 23782;
    for (int i = 0; i < 23782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23783(int& val) {
    func_in_23783(val);
    const int x = 23783;
    std::cout << 23783;
    for (int i = 0; i < 23783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23783(int& val) {
    const int x = 23783;
    std::cout << 23783;
    for (int i = 0; i < 23783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23784(int& val) {
    func_in_23784(val);
    const int x = 23784;
    std::cout << 23784;
    for (int i = 0; i < 23784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23784(int& val) {
    const int x = 23784;
    std::cout << 23784;
    for (int i = 0; i < 23784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23785(int& val) {
    func_in_23785(val);
    const int x = 23785;
    std::cout << 23785;
    for (int i = 0; i < 23785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23785(int& val) {
    const int x = 23785;
    std::cout << 23785;
    for (int i = 0; i < 23785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23786(int& val) {
    func_in_23786(val);
    const int x = 23786;
    std::cout << 23786;
    for (int i = 0; i < 23786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23786(int& val) {
    const int x = 23786;
    std::cout << 23786;
    for (int i = 0; i < 23786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23787(int& val) {
    func_in_23787(val);
    const int x = 23787;
    std::cout << 23787;
    for (int i = 0; i < 23787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23787(int& val) {
    const int x = 23787;
    std::cout << 23787;
    for (int i = 0; i < 23787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23788(int& val) {
    func_in_23788(val);
    const int x = 23788;
    std::cout << 23788;
    for (int i = 0; i < 23788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23788(int& val) {
    const int x = 23788;
    std::cout << 23788;
    for (int i = 0; i < 23788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23789(int& val) {
    func_in_23789(val);
    const int x = 23789;
    std::cout << 23789;
    for (int i = 0; i < 23789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23789(int& val) {
    const int x = 23789;
    std::cout << 23789;
    for (int i = 0; i < 23789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23790(int& val) {
    func_in_23790(val);
    const int x = 23790;
    std::cout << 23790;
    for (int i = 0; i < 23790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23790(int& val) {
    const int x = 23790;
    std::cout << 23790;
    for (int i = 0; i < 23790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23791(int& val) {
    func_in_23791(val);
    const int x = 23791;
    std::cout << 23791;
    for (int i = 0; i < 23791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23791(int& val) {
    const int x = 23791;
    std::cout << 23791;
    for (int i = 0; i < 23791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23792(int& val) {
    func_in_23792(val);
    const int x = 23792;
    std::cout << 23792;
    for (int i = 0; i < 23792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23792(int& val) {
    const int x = 23792;
    std::cout << 23792;
    for (int i = 0; i < 23792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23793(int& val) {
    func_in_23793(val);
    const int x = 23793;
    std::cout << 23793;
    for (int i = 0; i < 23793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23793(int& val) {
    const int x = 23793;
    std::cout << 23793;
    for (int i = 0; i < 23793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23794(int& val) {
    func_in_23794(val);
    const int x = 23794;
    std::cout << 23794;
    for (int i = 0; i < 23794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23794(int& val) {
    const int x = 23794;
    std::cout << 23794;
    for (int i = 0; i < 23794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23795(int& val) {
    func_in_23795(val);
    const int x = 23795;
    std::cout << 23795;
    for (int i = 0; i < 23795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23795(int& val) {
    const int x = 23795;
    std::cout << 23795;
    for (int i = 0; i < 23795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23796(int& val) {
    func_in_23796(val);
    const int x = 23796;
    std::cout << 23796;
    for (int i = 0; i < 23796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23796(int& val) {
    const int x = 23796;
    std::cout << 23796;
    for (int i = 0; i < 23796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23797(int& val) {
    func_in_23797(val);
    const int x = 23797;
    std::cout << 23797;
    for (int i = 0; i < 23797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23797(int& val) {
    const int x = 23797;
    std::cout << 23797;
    for (int i = 0; i < 23797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23798(int& val) {
    func_in_23798(val);
    const int x = 23798;
    std::cout << 23798;
    for (int i = 0; i < 23798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23798(int& val) {
    const int x = 23798;
    std::cout << 23798;
    for (int i = 0; i < 23798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23799(int& val) {
    func_in_23799(val);
    const int x = 23799;
    std::cout << 23799;
    for (int i = 0; i < 23799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23799(int& val) {
    const int x = 23799;
    std::cout << 23799;
    for (int i = 0; i < 23799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23800(int& val) {
    func_in_23800(val);
    const int x = 23800;
    std::cout << 23800;
    for (int i = 0; i < 23800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23800(int& val) {
    const int x = 23800;
    std::cout << 23800;
    for (int i = 0; i < 23800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23801(int& val) {
    func_in_23801(val);
    const int x = 23801;
    std::cout << 23801;
    for (int i = 0; i < 23801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23801(int& val) {
    const int x = 23801;
    std::cout << 23801;
    for (int i = 0; i < 23801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23802(int& val) {
    func_in_23802(val);
    const int x = 23802;
    std::cout << 23802;
    for (int i = 0; i < 23802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23802(int& val) {
    const int x = 23802;
    std::cout << 23802;
    for (int i = 0; i < 23802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23803(int& val) {
    func_in_23803(val);
    const int x = 23803;
    std::cout << 23803;
    for (int i = 0; i < 23803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23803(int& val) {
    const int x = 23803;
    std::cout << 23803;
    for (int i = 0; i < 23803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23804(int& val) {
    func_in_23804(val);
    const int x = 23804;
    std::cout << 23804;
    for (int i = 0; i < 23804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23804(int& val) {
    const int x = 23804;
    std::cout << 23804;
    for (int i = 0; i < 23804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23805(int& val) {
    func_in_23805(val);
    const int x = 23805;
    std::cout << 23805;
    for (int i = 0; i < 23805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23805(int& val) {
    const int x = 23805;
    std::cout << 23805;
    for (int i = 0; i < 23805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23806(int& val) {
    func_in_23806(val);
    const int x = 23806;
    std::cout << 23806;
    for (int i = 0; i < 23806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23806(int& val) {
    const int x = 23806;
    std::cout << 23806;
    for (int i = 0; i < 23806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23807(int& val) {
    func_in_23807(val);
    const int x = 23807;
    std::cout << 23807;
    for (int i = 0; i < 23807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23807(int& val) {
    const int x = 23807;
    std::cout << 23807;
    for (int i = 0; i < 23807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23808(int& val) {
    func_in_23808(val);
    const int x = 23808;
    std::cout << 23808;
    for (int i = 0; i < 23808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23808(int& val) {
    const int x = 23808;
    std::cout << 23808;
    for (int i = 0; i < 23808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23809(int& val) {
    func_in_23809(val);
    const int x = 23809;
    std::cout << 23809;
    for (int i = 0; i < 23809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23809(int& val) {
    const int x = 23809;
    std::cout << 23809;
    for (int i = 0; i < 23809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23810(int& val) {
    func_in_23810(val);
    const int x = 23810;
    std::cout << 23810;
    for (int i = 0; i < 23810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23810(int& val) {
    const int x = 23810;
    std::cout << 23810;
    for (int i = 0; i < 23810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23811(int& val) {
    func_in_23811(val);
    const int x = 23811;
    std::cout << 23811;
    for (int i = 0; i < 23811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23811(int& val) {
    const int x = 23811;
    std::cout << 23811;
    for (int i = 0; i < 23811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23812(int& val) {
    func_in_23812(val);
    const int x = 23812;
    std::cout << 23812;
    for (int i = 0; i < 23812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23812(int& val) {
    const int x = 23812;
    std::cout << 23812;
    for (int i = 0; i < 23812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23813(int& val) {
    func_in_23813(val);
    const int x = 23813;
    std::cout << 23813;
    for (int i = 0; i < 23813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23813(int& val) {
    const int x = 23813;
    std::cout << 23813;
    for (int i = 0; i < 23813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23814(int& val) {
    func_in_23814(val);
    const int x = 23814;
    std::cout << 23814;
    for (int i = 0; i < 23814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23814(int& val) {
    const int x = 23814;
    std::cout << 23814;
    for (int i = 0; i < 23814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23815(int& val) {
    func_in_23815(val);
    const int x = 23815;
    std::cout << 23815;
    for (int i = 0; i < 23815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23815(int& val) {
    const int x = 23815;
    std::cout << 23815;
    for (int i = 0; i < 23815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23816(int& val) {
    func_in_23816(val);
    const int x = 23816;
    std::cout << 23816;
    for (int i = 0; i < 23816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23816(int& val) {
    const int x = 23816;
    std::cout << 23816;
    for (int i = 0; i < 23816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23817(int& val) {
    func_in_23817(val);
    const int x = 23817;
    std::cout << 23817;
    for (int i = 0; i < 23817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23817(int& val) {
    const int x = 23817;
    std::cout << 23817;
    for (int i = 0; i < 23817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23818(int& val) {
    func_in_23818(val);
    const int x = 23818;
    std::cout << 23818;
    for (int i = 0; i < 23818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23818(int& val) {
    const int x = 23818;
    std::cout << 23818;
    for (int i = 0; i < 23818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23819(int& val) {
    func_in_23819(val);
    const int x = 23819;
    std::cout << 23819;
    for (int i = 0; i < 23819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23819(int& val) {
    const int x = 23819;
    std::cout << 23819;
    for (int i = 0; i < 23819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23820(int& val) {
    func_in_23820(val);
    const int x = 23820;
    std::cout << 23820;
    for (int i = 0; i < 23820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23820(int& val) {
    const int x = 23820;
    std::cout << 23820;
    for (int i = 0; i < 23820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23821(int& val) {
    func_in_23821(val);
    const int x = 23821;
    std::cout << 23821;
    for (int i = 0; i < 23821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23821(int& val) {
    const int x = 23821;
    std::cout << 23821;
    for (int i = 0; i < 23821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23822(int& val) {
    func_in_23822(val);
    const int x = 23822;
    std::cout << 23822;
    for (int i = 0; i < 23822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23822(int& val) {
    const int x = 23822;
    std::cout << 23822;
    for (int i = 0; i < 23822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23823(int& val) {
    func_in_23823(val);
    const int x = 23823;
    std::cout << 23823;
    for (int i = 0; i < 23823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23823(int& val) {
    const int x = 23823;
    std::cout << 23823;
    for (int i = 0; i < 23823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23824(int& val) {
    func_in_23824(val);
    const int x = 23824;
    std::cout << 23824;
    for (int i = 0; i < 23824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23824(int& val) {
    const int x = 23824;
    std::cout << 23824;
    for (int i = 0; i < 23824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23825(int& val) {
    func_in_23825(val);
    const int x = 23825;
    std::cout << 23825;
    for (int i = 0; i < 23825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23825(int& val) {
    const int x = 23825;
    std::cout << 23825;
    for (int i = 0; i < 23825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23826(int& val) {
    func_in_23826(val);
    const int x = 23826;
    std::cout << 23826;
    for (int i = 0; i < 23826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23826(int& val) {
    const int x = 23826;
    std::cout << 23826;
    for (int i = 0; i < 23826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23827(int& val) {
    func_in_23827(val);
    const int x = 23827;
    std::cout << 23827;
    for (int i = 0; i < 23827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23827(int& val) {
    const int x = 23827;
    std::cout << 23827;
    for (int i = 0; i < 23827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23828(int& val) {
    func_in_23828(val);
    const int x = 23828;
    std::cout << 23828;
    for (int i = 0; i < 23828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23828(int& val) {
    const int x = 23828;
    std::cout << 23828;
    for (int i = 0; i < 23828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23829(int& val) {
    func_in_23829(val);
    const int x = 23829;
    std::cout << 23829;
    for (int i = 0; i < 23829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23829(int& val) {
    const int x = 23829;
    std::cout << 23829;
    for (int i = 0; i < 23829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23830(int& val) {
    func_in_23830(val);
    const int x = 23830;
    std::cout << 23830;
    for (int i = 0; i < 23830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23830(int& val) {
    const int x = 23830;
    std::cout << 23830;
    for (int i = 0; i < 23830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23831(int& val) {
    func_in_23831(val);
    const int x = 23831;
    std::cout << 23831;
    for (int i = 0; i < 23831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23831(int& val) {
    const int x = 23831;
    std::cout << 23831;
    for (int i = 0; i < 23831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23832(int& val) {
    func_in_23832(val);
    const int x = 23832;
    std::cout << 23832;
    for (int i = 0; i < 23832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23832(int& val) {
    const int x = 23832;
    std::cout << 23832;
    for (int i = 0; i < 23832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23833(int& val) {
    func_in_23833(val);
    const int x = 23833;
    std::cout << 23833;
    for (int i = 0; i < 23833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23833(int& val) {
    const int x = 23833;
    std::cout << 23833;
    for (int i = 0; i < 23833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23834(int& val) {
    func_in_23834(val);
    const int x = 23834;
    std::cout << 23834;
    for (int i = 0; i < 23834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23834(int& val) {
    const int x = 23834;
    std::cout << 23834;
    for (int i = 0; i < 23834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23835(int& val) {
    func_in_23835(val);
    const int x = 23835;
    std::cout << 23835;
    for (int i = 0; i < 23835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23835(int& val) {
    const int x = 23835;
    std::cout << 23835;
    for (int i = 0; i < 23835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23836(int& val) {
    func_in_23836(val);
    const int x = 23836;
    std::cout << 23836;
    for (int i = 0; i < 23836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23836(int& val) {
    const int x = 23836;
    std::cout << 23836;
    for (int i = 0; i < 23836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23837(int& val) {
    func_in_23837(val);
    const int x = 23837;
    std::cout << 23837;
    for (int i = 0; i < 23837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23837(int& val) {
    const int x = 23837;
    std::cout << 23837;
    for (int i = 0; i < 23837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23838(int& val) {
    func_in_23838(val);
    const int x = 23838;
    std::cout << 23838;
    for (int i = 0; i < 23838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23838(int& val) {
    const int x = 23838;
    std::cout << 23838;
    for (int i = 0; i < 23838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23839(int& val) {
    func_in_23839(val);
    const int x = 23839;
    std::cout << 23839;
    for (int i = 0; i < 23839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23839(int& val) {
    const int x = 23839;
    std::cout << 23839;
    for (int i = 0; i < 23839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23840(int& val) {
    func_in_23840(val);
    const int x = 23840;
    std::cout << 23840;
    for (int i = 0; i < 23840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23840(int& val) {
    const int x = 23840;
    std::cout << 23840;
    for (int i = 0; i < 23840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23841(int& val) {
    func_in_23841(val);
    const int x = 23841;
    std::cout << 23841;
    for (int i = 0; i < 23841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23841(int& val) {
    const int x = 23841;
    std::cout << 23841;
    for (int i = 0; i < 23841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23842(int& val) {
    func_in_23842(val);
    const int x = 23842;
    std::cout << 23842;
    for (int i = 0; i < 23842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23842(int& val) {
    const int x = 23842;
    std::cout << 23842;
    for (int i = 0; i < 23842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23843(int& val) {
    func_in_23843(val);
    const int x = 23843;
    std::cout << 23843;
    for (int i = 0; i < 23843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23843(int& val) {
    const int x = 23843;
    std::cout << 23843;
    for (int i = 0; i < 23843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23844(int& val) {
    func_in_23844(val);
    const int x = 23844;
    std::cout << 23844;
    for (int i = 0; i < 23844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23844(int& val) {
    const int x = 23844;
    std::cout << 23844;
    for (int i = 0; i < 23844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23845(int& val) {
    func_in_23845(val);
    const int x = 23845;
    std::cout << 23845;
    for (int i = 0; i < 23845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23845(int& val) {
    const int x = 23845;
    std::cout << 23845;
    for (int i = 0; i < 23845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23846(int& val) {
    func_in_23846(val);
    const int x = 23846;
    std::cout << 23846;
    for (int i = 0; i < 23846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23846(int& val) {
    const int x = 23846;
    std::cout << 23846;
    for (int i = 0; i < 23846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23847(int& val) {
    func_in_23847(val);
    const int x = 23847;
    std::cout << 23847;
    for (int i = 0; i < 23847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23847(int& val) {
    const int x = 23847;
    std::cout << 23847;
    for (int i = 0; i < 23847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23848(int& val) {
    func_in_23848(val);
    const int x = 23848;
    std::cout << 23848;
    for (int i = 0; i < 23848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23848(int& val) {
    const int x = 23848;
    std::cout << 23848;
    for (int i = 0; i < 23848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23849(int& val) {
    func_in_23849(val);
    const int x = 23849;
    std::cout << 23849;
    for (int i = 0; i < 23849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23849(int& val) {
    const int x = 23849;
    std::cout << 23849;
    for (int i = 0; i < 23849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23850(int& val) {
    func_in_23850(val);
    const int x = 23850;
    std::cout << 23850;
    for (int i = 0; i < 23850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23850(int& val) {
    const int x = 23850;
    std::cout << 23850;
    for (int i = 0; i < 23850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23851(int& val) {
    func_in_23851(val);
    const int x = 23851;
    std::cout << 23851;
    for (int i = 0; i < 23851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23851(int& val) {
    const int x = 23851;
    std::cout << 23851;
    for (int i = 0; i < 23851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23852(int& val) {
    func_in_23852(val);
    const int x = 23852;
    std::cout << 23852;
    for (int i = 0; i < 23852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23852(int& val) {
    const int x = 23852;
    std::cout << 23852;
    for (int i = 0; i < 23852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23853(int& val) {
    func_in_23853(val);
    const int x = 23853;
    std::cout << 23853;
    for (int i = 0; i < 23853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23853(int& val) {
    const int x = 23853;
    std::cout << 23853;
    for (int i = 0; i < 23853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23854(int& val) {
    func_in_23854(val);
    const int x = 23854;
    std::cout << 23854;
    for (int i = 0; i < 23854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23854(int& val) {
    const int x = 23854;
    std::cout << 23854;
    for (int i = 0; i < 23854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23855(int& val) {
    func_in_23855(val);
    const int x = 23855;
    std::cout << 23855;
    for (int i = 0; i < 23855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23855(int& val) {
    const int x = 23855;
    std::cout << 23855;
    for (int i = 0; i < 23855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23856(int& val) {
    func_in_23856(val);
    const int x = 23856;
    std::cout << 23856;
    for (int i = 0; i < 23856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23856(int& val) {
    const int x = 23856;
    std::cout << 23856;
    for (int i = 0; i < 23856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23857(int& val) {
    func_in_23857(val);
    const int x = 23857;
    std::cout << 23857;
    for (int i = 0; i < 23857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23857(int& val) {
    const int x = 23857;
    std::cout << 23857;
    for (int i = 0; i < 23857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23858(int& val) {
    func_in_23858(val);
    const int x = 23858;
    std::cout << 23858;
    for (int i = 0; i < 23858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23858(int& val) {
    const int x = 23858;
    std::cout << 23858;
    for (int i = 0; i < 23858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23859(int& val) {
    func_in_23859(val);
    const int x = 23859;
    std::cout << 23859;
    for (int i = 0; i < 23859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23859(int& val) {
    const int x = 23859;
    std::cout << 23859;
    for (int i = 0; i < 23859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23860(int& val) {
    func_in_23860(val);
    const int x = 23860;
    std::cout << 23860;
    for (int i = 0; i < 23860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23860(int& val) {
    const int x = 23860;
    std::cout << 23860;
    for (int i = 0; i < 23860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23861(int& val) {
    func_in_23861(val);
    const int x = 23861;
    std::cout << 23861;
    for (int i = 0; i < 23861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23861(int& val) {
    const int x = 23861;
    std::cout << 23861;
    for (int i = 0; i < 23861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23862(int& val) {
    func_in_23862(val);
    const int x = 23862;
    std::cout << 23862;
    for (int i = 0; i < 23862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23862(int& val) {
    const int x = 23862;
    std::cout << 23862;
    for (int i = 0; i < 23862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23863(int& val) {
    func_in_23863(val);
    const int x = 23863;
    std::cout << 23863;
    for (int i = 0; i < 23863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23863(int& val) {
    const int x = 23863;
    std::cout << 23863;
    for (int i = 0; i < 23863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23864(int& val) {
    func_in_23864(val);
    const int x = 23864;
    std::cout << 23864;
    for (int i = 0; i < 23864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23864(int& val) {
    const int x = 23864;
    std::cout << 23864;
    for (int i = 0; i < 23864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23865(int& val) {
    func_in_23865(val);
    const int x = 23865;
    std::cout << 23865;
    for (int i = 0; i < 23865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23865(int& val) {
    const int x = 23865;
    std::cout << 23865;
    for (int i = 0; i < 23865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23866(int& val) {
    func_in_23866(val);
    const int x = 23866;
    std::cout << 23866;
    for (int i = 0; i < 23866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23866(int& val) {
    const int x = 23866;
    std::cout << 23866;
    for (int i = 0; i < 23866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23867(int& val) {
    func_in_23867(val);
    const int x = 23867;
    std::cout << 23867;
    for (int i = 0; i < 23867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23867(int& val) {
    const int x = 23867;
    std::cout << 23867;
    for (int i = 0; i < 23867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23868(int& val) {
    func_in_23868(val);
    const int x = 23868;
    std::cout << 23868;
    for (int i = 0; i < 23868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23868(int& val) {
    const int x = 23868;
    std::cout << 23868;
    for (int i = 0; i < 23868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23869(int& val) {
    func_in_23869(val);
    const int x = 23869;
    std::cout << 23869;
    for (int i = 0; i < 23869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23869(int& val) {
    const int x = 23869;
    std::cout << 23869;
    for (int i = 0; i < 23869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23870(int& val) {
    func_in_23870(val);
    const int x = 23870;
    std::cout << 23870;
    for (int i = 0; i < 23870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23870(int& val) {
    const int x = 23870;
    std::cout << 23870;
    for (int i = 0; i < 23870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23871(int& val) {
    func_in_23871(val);
    const int x = 23871;
    std::cout << 23871;
    for (int i = 0; i < 23871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23871(int& val) {
    const int x = 23871;
    std::cout << 23871;
    for (int i = 0; i < 23871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23872(int& val) {
    func_in_23872(val);
    const int x = 23872;
    std::cout << 23872;
    for (int i = 0; i < 23872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23872(int& val) {
    const int x = 23872;
    std::cout << 23872;
    for (int i = 0; i < 23872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23873(int& val) {
    func_in_23873(val);
    const int x = 23873;
    std::cout << 23873;
    for (int i = 0; i < 23873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23873(int& val) {
    const int x = 23873;
    std::cout << 23873;
    for (int i = 0; i < 23873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23874(int& val) {
    func_in_23874(val);
    const int x = 23874;
    std::cout << 23874;
    for (int i = 0; i < 23874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23874(int& val) {
    const int x = 23874;
    std::cout << 23874;
    for (int i = 0; i < 23874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23875(int& val) {
    func_in_23875(val);
    const int x = 23875;
    std::cout << 23875;
    for (int i = 0; i < 23875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23875(int& val) {
    const int x = 23875;
    std::cout << 23875;
    for (int i = 0; i < 23875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23876(int& val) {
    func_in_23876(val);
    const int x = 23876;
    std::cout << 23876;
    for (int i = 0; i < 23876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23876(int& val) {
    const int x = 23876;
    std::cout << 23876;
    for (int i = 0; i < 23876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23877(int& val) {
    func_in_23877(val);
    const int x = 23877;
    std::cout << 23877;
    for (int i = 0; i < 23877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23877(int& val) {
    const int x = 23877;
    std::cout << 23877;
    for (int i = 0; i < 23877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23878(int& val) {
    func_in_23878(val);
    const int x = 23878;
    std::cout << 23878;
    for (int i = 0; i < 23878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23878(int& val) {
    const int x = 23878;
    std::cout << 23878;
    for (int i = 0; i < 23878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23879(int& val) {
    func_in_23879(val);
    const int x = 23879;
    std::cout << 23879;
    for (int i = 0; i < 23879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23879(int& val) {
    const int x = 23879;
    std::cout << 23879;
    for (int i = 0; i < 23879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23880(int& val) {
    func_in_23880(val);
    const int x = 23880;
    std::cout << 23880;
    for (int i = 0; i < 23880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23880(int& val) {
    const int x = 23880;
    std::cout << 23880;
    for (int i = 0; i < 23880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23881(int& val) {
    func_in_23881(val);
    const int x = 23881;
    std::cout << 23881;
    for (int i = 0; i < 23881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23881(int& val) {
    const int x = 23881;
    std::cout << 23881;
    for (int i = 0; i < 23881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23882(int& val) {
    func_in_23882(val);
    const int x = 23882;
    std::cout << 23882;
    for (int i = 0; i < 23882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23882(int& val) {
    const int x = 23882;
    std::cout << 23882;
    for (int i = 0; i < 23882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23883(int& val) {
    func_in_23883(val);
    const int x = 23883;
    std::cout << 23883;
    for (int i = 0; i < 23883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23883(int& val) {
    const int x = 23883;
    std::cout << 23883;
    for (int i = 0; i < 23883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23884(int& val) {
    func_in_23884(val);
    const int x = 23884;
    std::cout << 23884;
    for (int i = 0; i < 23884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23884(int& val) {
    const int x = 23884;
    std::cout << 23884;
    for (int i = 0; i < 23884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23885(int& val) {
    func_in_23885(val);
    const int x = 23885;
    std::cout << 23885;
    for (int i = 0; i < 23885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23885(int& val) {
    const int x = 23885;
    std::cout << 23885;
    for (int i = 0; i < 23885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23886(int& val) {
    func_in_23886(val);
    const int x = 23886;
    std::cout << 23886;
    for (int i = 0; i < 23886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23886(int& val) {
    const int x = 23886;
    std::cout << 23886;
    for (int i = 0; i < 23886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23887(int& val) {
    func_in_23887(val);
    const int x = 23887;
    std::cout << 23887;
    for (int i = 0; i < 23887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23887(int& val) {
    const int x = 23887;
    std::cout << 23887;
    for (int i = 0; i < 23887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23888(int& val) {
    func_in_23888(val);
    const int x = 23888;
    std::cout << 23888;
    for (int i = 0; i < 23888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23888(int& val) {
    const int x = 23888;
    std::cout << 23888;
    for (int i = 0; i < 23888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23889(int& val) {
    func_in_23889(val);
    const int x = 23889;
    std::cout << 23889;
    for (int i = 0; i < 23889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23889(int& val) {
    const int x = 23889;
    std::cout << 23889;
    for (int i = 0; i < 23889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23890(int& val) {
    func_in_23890(val);
    const int x = 23890;
    std::cout << 23890;
    for (int i = 0; i < 23890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23890(int& val) {
    const int x = 23890;
    std::cout << 23890;
    for (int i = 0; i < 23890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23891(int& val) {
    func_in_23891(val);
    const int x = 23891;
    std::cout << 23891;
    for (int i = 0; i < 23891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23891(int& val) {
    const int x = 23891;
    std::cout << 23891;
    for (int i = 0; i < 23891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23892(int& val) {
    func_in_23892(val);
    const int x = 23892;
    std::cout << 23892;
    for (int i = 0; i < 23892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23892(int& val) {
    const int x = 23892;
    std::cout << 23892;
    for (int i = 0; i < 23892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23893(int& val) {
    func_in_23893(val);
    const int x = 23893;
    std::cout << 23893;
    for (int i = 0; i < 23893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23893(int& val) {
    const int x = 23893;
    std::cout << 23893;
    for (int i = 0; i < 23893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23894(int& val) {
    func_in_23894(val);
    const int x = 23894;
    std::cout << 23894;
    for (int i = 0; i < 23894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23894(int& val) {
    const int x = 23894;
    std::cout << 23894;
    for (int i = 0; i < 23894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23895(int& val) {
    func_in_23895(val);
    const int x = 23895;
    std::cout << 23895;
    for (int i = 0; i < 23895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23895(int& val) {
    const int x = 23895;
    std::cout << 23895;
    for (int i = 0; i < 23895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23896(int& val) {
    func_in_23896(val);
    const int x = 23896;
    std::cout << 23896;
    for (int i = 0; i < 23896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23896(int& val) {
    const int x = 23896;
    std::cout << 23896;
    for (int i = 0; i < 23896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23897(int& val) {
    func_in_23897(val);
    const int x = 23897;
    std::cout << 23897;
    for (int i = 0; i < 23897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23897(int& val) {
    const int x = 23897;
    std::cout << 23897;
    for (int i = 0; i < 23897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23898(int& val) {
    func_in_23898(val);
    const int x = 23898;
    std::cout << 23898;
    for (int i = 0; i < 23898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23898(int& val) {
    const int x = 23898;
    std::cout << 23898;
    for (int i = 0; i < 23898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23899(int& val) {
    func_in_23899(val);
    const int x = 23899;
    std::cout << 23899;
    for (int i = 0; i < 23899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23899(int& val) {
    const int x = 23899;
    std::cout << 23899;
    for (int i = 0; i < 23899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23900(int& val) {
    func_in_23900(val);
    const int x = 23900;
    std::cout << 23900;
    for (int i = 0; i < 23900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23900(int& val) {
    const int x = 23900;
    std::cout << 23900;
    for (int i = 0; i < 23900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23901(int& val) {
    func_in_23901(val);
    const int x = 23901;
    std::cout << 23901;
    for (int i = 0; i < 23901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23901(int& val) {
    const int x = 23901;
    std::cout << 23901;
    for (int i = 0; i < 23901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23902(int& val) {
    func_in_23902(val);
    const int x = 23902;
    std::cout << 23902;
    for (int i = 0; i < 23902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23902(int& val) {
    const int x = 23902;
    std::cout << 23902;
    for (int i = 0; i < 23902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23903(int& val) {
    func_in_23903(val);
    const int x = 23903;
    std::cout << 23903;
    for (int i = 0; i < 23903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23903(int& val) {
    const int x = 23903;
    std::cout << 23903;
    for (int i = 0; i < 23903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23904(int& val) {
    func_in_23904(val);
    const int x = 23904;
    std::cout << 23904;
    for (int i = 0; i < 23904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23904(int& val) {
    const int x = 23904;
    std::cout << 23904;
    for (int i = 0; i < 23904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23905(int& val) {
    func_in_23905(val);
    const int x = 23905;
    std::cout << 23905;
    for (int i = 0; i < 23905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23905(int& val) {
    const int x = 23905;
    std::cout << 23905;
    for (int i = 0; i < 23905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23906(int& val) {
    func_in_23906(val);
    const int x = 23906;
    std::cout << 23906;
    for (int i = 0; i < 23906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23906(int& val) {
    const int x = 23906;
    std::cout << 23906;
    for (int i = 0; i < 23906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23907(int& val) {
    func_in_23907(val);
    const int x = 23907;
    std::cout << 23907;
    for (int i = 0; i < 23907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23907(int& val) {
    const int x = 23907;
    std::cout << 23907;
    for (int i = 0; i < 23907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23908(int& val) {
    func_in_23908(val);
    const int x = 23908;
    std::cout << 23908;
    for (int i = 0; i < 23908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23908(int& val) {
    const int x = 23908;
    std::cout << 23908;
    for (int i = 0; i < 23908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23909(int& val) {
    func_in_23909(val);
    const int x = 23909;
    std::cout << 23909;
    for (int i = 0; i < 23909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23909(int& val) {
    const int x = 23909;
    std::cout << 23909;
    for (int i = 0; i < 23909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23910(int& val) {
    func_in_23910(val);
    const int x = 23910;
    std::cout << 23910;
    for (int i = 0; i < 23910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23910(int& val) {
    const int x = 23910;
    std::cout << 23910;
    for (int i = 0; i < 23910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23911(int& val) {
    func_in_23911(val);
    const int x = 23911;
    std::cout << 23911;
    for (int i = 0; i < 23911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23911(int& val) {
    const int x = 23911;
    std::cout << 23911;
    for (int i = 0; i < 23911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23912(int& val) {
    func_in_23912(val);
    const int x = 23912;
    std::cout << 23912;
    for (int i = 0; i < 23912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23912(int& val) {
    const int x = 23912;
    std::cout << 23912;
    for (int i = 0; i < 23912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23913(int& val) {
    func_in_23913(val);
    const int x = 23913;
    std::cout << 23913;
    for (int i = 0; i < 23913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23913(int& val) {
    const int x = 23913;
    std::cout << 23913;
    for (int i = 0; i < 23913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23914(int& val) {
    func_in_23914(val);
    const int x = 23914;
    std::cout << 23914;
    for (int i = 0; i < 23914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23914(int& val) {
    const int x = 23914;
    std::cout << 23914;
    for (int i = 0; i < 23914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23915(int& val) {
    func_in_23915(val);
    const int x = 23915;
    std::cout << 23915;
    for (int i = 0; i < 23915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23915(int& val) {
    const int x = 23915;
    std::cout << 23915;
    for (int i = 0; i < 23915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23916(int& val) {
    func_in_23916(val);
    const int x = 23916;
    std::cout << 23916;
    for (int i = 0; i < 23916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23916(int& val) {
    const int x = 23916;
    std::cout << 23916;
    for (int i = 0; i < 23916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23917(int& val) {
    func_in_23917(val);
    const int x = 23917;
    std::cout << 23917;
    for (int i = 0; i < 23917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23917(int& val) {
    const int x = 23917;
    std::cout << 23917;
    for (int i = 0; i < 23917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23918(int& val) {
    func_in_23918(val);
    const int x = 23918;
    std::cout << 23918;
    for (int i = 0; i < 23918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23918(int& val) {
    const int x = 23918;
    std::cout << 23918;
    for (int i = 0; i < 23918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23919(int& val) {
    func_in_23919(val);
    const int x = 23919;
    std::cout << 23919;
    for (int i = 0; i < 23919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23919(int& val) {
    const int x = 23919;
    std::cout << 23919;
    for (int i = 0; i < 23919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23920(int& val) {
    func_in_23920(val);
    const int x = 23920;
    std::cout << 23920;
    for (int i = 0; i < 23920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23920(int& val) {
    const int x = 23920;
    std::cout << 23920;
    for (int i = 0; i < 23920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23921(int& val) {
    func_in_23921(val);
    const int x = 23921;
    std::cout << 23921;
    for (int i = 0; i < 23921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23921(int& val) {
    const int x = 23921;
    std::cout << 23921;
    for (int i = 0; i < 23921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23922(int& val) {
    func_in_23922(val);
    const int x = 23922;
    std::cout << 23922;
    for (int i = 0; i < 23922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23922(int& val) {
    const int x = 23922;
    std::cout << 23922;
    for (int i = 0; i < 23922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23923(int& val) {
    func_in_23923(val);
    const int x = 23923;
    std::cout << 23923;
    for (int i = 0; i < 23923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23923(int& val) {
    const int x = 23923;
    std::cout << 23923;
    for (int i = 0; i < 23923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23924(int& val) {
    func_in_23924(val);
    const int x = 23924;
    std::cout << 23924;
    for (int i = 0; i < 23924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23924(int& val) {
    const int x = 23924;
    std::cout << 23924;
    for (int i = 0; i < 23924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23925(int& val) {
    func_in_23925(val);
    const int x = 23925;
    std::cout << 23925;
    for (int i = 0; i < 23925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23925(int& val) {
    const int x = 23925;
    std::cout << 23925;
    for (int i = 0; i < 23925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23926(int& val) {
    func_in_23926(val);
    const int x = 23926;
    std::cout << 23926;
    for (int i = 0; i < 23926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23926(int& val) {
    const int x = 23926;
    std::cout << 23926;
    for (int i = 0; i < 23926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23927(int& val) {
    func_in_23927(val);
    const int x = 23927;
    std::cout << 23927;
    for (int i = 0; i < 23927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23927(int& val) {
    const int x = 23927;
    std::cout << 23927;
    for (int i = 0; i < 23927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23928(int& val) {
    func_in_23928(val);
    const int x = 23928;
    std::cout << 23928;
    for (int i = 0; i < 23928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23928(int& val) {
    const int x = 23928;
    std::cout << 23928;
    for (int i = 0; i < 23928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23929(int& val) {
    func_in_23929(val);
    const int x = 23929;
    std::cout << 23929;
    for (int i = 0; i < 23929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23929(int& val) {
    const int x = 23929;
    std::cout << 23929;
    for (int i = 0; i < 23929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23930(int& val) {
    func_in_23930(val);
    const int x = 23930;
    std::cout << 23930;
    for (int i = 0; i < 23930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23930(int& val) {
    const int x = 23930;
    std::cout << 23930;
    for (int i = 0; i < 23930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23931(int& val) {
    func_in_23931(val);
    const int x = 23931;
    std::cout << 23931;
    for (int i = 0; i < 23931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23931(int& val) {
    const int x = 23931;
    std::cout << 23931;
    for (int i = 0; i < 23931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23932(int& val) {
    func_in_23932(val);
    const int x = 23932;
    std::cout << 23932;
    for (int i = 0; i < 23932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23932(int& val) {
    const int x = 23932;
    std::cout << 23932;
    for (int i = 0; i < 23932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23933(int& val) {
    func_in_23933(val);
    const int x = 23933;
    std::cout << 23933;
    for (int i = 0; i < 23933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23933(int& val) {
    const int x = 23933;
    std::cout << 23933;
    for (int i = 0; i < 23933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23934(int& val) {
    func_in_23934(val);
    const int x = 23934;
    std::cout << 23934;
    for (int i = 0; i < 23934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23934(int& val) {
    const int x = 23934;
    std::cout << 23934;
    for (int i = 0; i < 23934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23935(int& val) {
    func_in_23935(val);
    const int x = 23935;
    std::cout << 23935;
    for (int i = 0; i < 23935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23935(int& val) {
    const int x = 23935;
    std::cout << 23935;
    for (int i = 0; i < 23935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23936(int& val) {
    func_in_23936(val);
    const int x = 23936;
    std::cout << 23936;
    for (int i = 0; i < 23936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23936(int& val) {
    const int x = 23936;
    std::cout << 23936;
    for (int i = 0; i < 23936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23937(int& val) {
    func_in_23937(val);
    const int x = 23937;
    std::cout << 23937;
    for (int i = 0; i < 23937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23937(int& val) {
    const int x = 23937;
    std::cout << 23937;
    for (int i = 0; i < 23937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23938(int& val) {
    func_in_23938(val);
    const int x = 23938;
    std::cout << 23938;
    for (int i = 0; i < 23938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23938(int& val) {
    const int x = 23938;
    std::cout << 23938;
    for (int i = 0; i < 23938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23939(int& val) {
    func_in_23939(val);
    const int x = 23939;
    std::cout << 23939;
    for (int i = 0; i < 23939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23939(int& val) {
    const int x = 23939;
    std::cout << 23939;
    for (int i = 0; i < 23939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23940(int& val) {
    func_in_23940(val);
    const int x = 23940;
    std::cout << 23940;
    for (int i = 0; i < 23940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23940(int& val) {
    const int x = 23940;
    std::cout << 23940;
    for (int i = 0; i < 23940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23941(int& val) {
    func_in_23941(val);
    const int x = 23941;
    std::cout << 23941;
    for (int i = 0; i < 23941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23941(int& val) {
    const int x = 23941;
    std::cout << 23941;
    for (int i = 0; i < 23941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23942(int& val) {
    func_in_23942(val);
    const int x = 23942;
    std::cout << 23942;
    for (int i = 0; i < 23942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23942(int& val) {
    const int x = 23942;
    std::cout << 23942;
    for (int i = 0; i < 23942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23943(int& val) {
    func_in_23943(val);
    const int x = 23943;
    std::cout << 23943;
    for (int i = 0; i < 23943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23943(int& val) {
    const int x = 23943;
    std::cout << 23943;
    for (int i = 0; i < 23943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23944(int& val) {
    func_in_23944(val);
    const int x = 23944;
    std::cout << 23944;
    for (int i = 0; i < 23944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23944(int& val) {
    const int x = 23944;
    std::cout << 23944;
    for (int i = 0; i < 23944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23945(int& val) {
    func_in_23945(val);
    const int x = 23945;
    std::cout << 23945;
    for (int i = 0; i < 23945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23945(int& val) {
    const int x = 23945;
    std::cout << 23945;
    for (int i = 0; i < 23945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23946(int& val) {
    func_in_23946(val);
    const int x = 23946;
    std::cout << 23946;
    for (int i = 0; i < 23946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23946(int& val) {
    const int x = 23946;
    std::cout << 23946;
    for (int i = 0; i < 23946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23947(int& val) {
    func_in_23947(val);
    const int x = 23947;
    std::cout << 23947;
    for (int i = 0; i < 23947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23947(int& val) {
    const int x = 23947;
    std::cout << 23947;
    for (int i = 0; i < 23947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23948(int& val) {
    func_in_23948(val);
    const int x = 23948;
    std::cout << 23948;
    for (int i = 0; i < 23948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23948(int& val) {
    const int x = 23948;
    std::cout << 23948;
    for (int i = 0; i < 23948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23949(int& val) {
    func_in_23949(val);
    const int x = 23949;
    std::cout << 23949;
    for (int i = 0; i < 23949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23949(int& val) {
    const int x = 23949;
    std::cout << 23949;
    for (int i = 0; i < 23949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23950(int& val) {
    func_in_23950(val);
    const int x = 23950;
    std::cout << 23950;
    for (int i = 0; i < 23950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23950(int& val) {
    const int x = 23950;
    std::cout << 23950;
    for (int i = 0; i < 23950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23951(int& val) {
    func_in_23951(val);
    const int x = 23951;
    std::cout << 23951;
    for (int i = 0; i < 23951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23951(int& val) {
    const int x = 23951;
    std::cout << 23951;
    for (int i = 0; i < 23951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23952(int& val) {
    func_in_23952(val);
    const int x = 23952;
    std::cout << 23952;
    for (int i = 0; i < 23952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23952(int& val) {
    const int x = 23952;
    std::cout << 23952;
    for (int i = 0; i < 23952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23953(int& val) {
    func_in_23953(val);
    const int x = 23953;
    std::cout << 23953;
    for (int i = 0; i < 23953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23953(int& val) {
    const int x = 23953;
    std::cout << 23953;
    for (int i = 0; i < 23953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23954(int& val) {
    func_in_23954(val);
    const int x = 23954;
    std::cout << 23954;
    for (int i = 0; i < 23954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23954(int& val) {
    const int x = 23954;
    std::cout << 23954;
    for (int i = 0; i < 23954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23955(int& val) {
    func_in_23955(val);
    const int x = 23955;
    std::cout << 23955;
    for (int i = 0; i < 23955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23955(int& val) {
    const int x = 23955;
    std::cout << 23955;
    for (int i = 0; i < 23955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23956(int& val) {
    func_in_23956(val);
    const int x = 23956;
    std::cout << 23956;
    for (int i = 0; i < 23956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23956(int& val) {
    const int x = 23956;
    std::cout << 23956;
    for (int i = 0; i < 23956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23957(int& val) {
    func_in_23957(val);
    const int x = 23957;
    std::cout << 23957;
    for (int i = 0; i < 23957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23957(int& val) {
    const int x = 23957;
    std::cout << 23957;
    for (int i = 0; i < 23957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23958(int& val) {
    func_in_23958(val);
    const int x = 23958;
    std::cout << 23958;
    for (int i = 0; i < 23958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23958(int& val) {
    const int x = 23958;
    std::cout << 23958;
    for (int i = 0; i < 23958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23959(int& val) {
    func_in_23959(val);
    const int x = 23959;
    std::cout << 23959;
    for (int i = 0; i < 23959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23959(int& val) {
    const int x = 23959;
    std::cout << 23959;
    for (int i = 0; i < 23959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23960(int& val) {
    func_in_23960(val);
    const int x = 23960;
    std::cout << 23960;
    for (int i = 0; i < 23960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23960(int& val) {
    const int x = 23960;
    std::cout << 23960;
    for (int i = 0; i < 23960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23961(int& val) {
    func_in_23961(val);
    const int x = 23961;
    std::cout << 23961;
    for (int i = 0; i < 23961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23961(int& val) {
    const int x = 23961;
    std::cout << 23961;
    for (int i = 0; i < 23961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23962(int& val) {
    func_in_23962(val);
    const int x = 23962;
    std::cout << 23962;
    for (int i = 0; i < 23962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23962(int& val) {
    const int x = 23962;
    std::cout << 23962;
    for (int i = 0; i < 23962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23963(int& val) {
    func_in_23963(val);
    const int x = 23963;
    std::cout << 23963;
    for (int i = 0; i < 23963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23963(int& val) {
    const int x = 23963;
    std::cout << 23963;
    for (int i = 0; i < 23963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23964(int& val) {
    func_in_23964(val);
    const int x = 23964;
    std::cout << 23964;
    for (int i = 0; i < 23964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23964(int& val) {
    const int x = 23964;
    std::cout << 23964;
    for (int i = 0; i < 23964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23965(int& val) {
    func_in_23965(val);
    const int x = 23965;
    std::cout << 23965;
    for (int i = 0; i < 23965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23965(int& val) {
    const int x = 23965;
    std::cout << 23965;
    for (int i = 0; i < 23965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23966(int& val) {
    func_in_23966(val);
    const int x = 23966;
    std::cout << 23966;
    for (int i = 0; i < 23966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23966(int& val) {
    const int x = 23966;
    std::cout << 23966;
    for (int i = 0; i < 23966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23967(int& val) {
    func_in_23967(val);
    const int x = 23967;
    std::cout << 23967;
    for (int i = 0; i < 23967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23967(int& val) {
    const int x = 23967;
    std::cout << 23967;
    for (int i = 0; i < 23967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23968(int& val) {
    func_in_23968(val);
    const int x = 23968;
    std::cout << 23968;
    for (int i = 0; i < 23968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23968(int& val) {
    const int x = 23968;
    std::cout << 23968;
    for (int i = 0; i < 23968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23969(int& val) {
    func_in_23969(val);
    const int x = 23969;
    std::cout << 23969;
    for (int i = 0; i < 23969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23969(int& val) {
    const int x = 23969;
    std::cout << 23969;
    for (int i = 0; i < 23969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23970(int& val) {
    func_in_23970(val);
    const int x = 23970;
    std::cout << 23970;
    for (int i = 0; i < 23970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23970(int& val) {
    const int x = 23970;
    std::cout << 23970;
    for (int i = 0; i < 23970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23971(int& val) {
    func_in_23971(val);
    const int x = 23971;
    std::cout << 23971;
    for (int i = 0; i < 23971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23971(int& val) {
    const int x = 23971;
    std::cout << 23971;
    for (int i = 0; i < 23971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23972(int& val) {
    func_in_23972(val);
    const int x = 23972;
    std::cout << 23972;
    for (int i = 0; i < 23972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23972(int& val) {
    const int x = 23972;
    std::cout << 23972;
    for (int i = 0; i < 23972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23973(int& val) {
    func_in_23973(val);
    const int x = 23973;
    std::cout << 23973;
    for (int i = 0; i < 23973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23973(int& val) {
    const int x = 23973;
    std::cout << 23973;
    for (int i = 0; i < 23973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23974(int& val) {
    func_in_23974(val);
    const int x = 23974;
    std::cout << 23974;
    for (int i = 0; i < 23974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23974(int& val) {
    const int x = 23974;
    std::cout << 23974;
    for (int i = 0; i < 23974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23975(int& val) {
    func_in_23975(val);
    const int x = 23975;
    std::cout << 23975;
    for (int i = 0; i < 23975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23975(int& val) {
    const int x = 23975;
    std::cout << 23975;
    for (int i = 0; i < 23975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23976(int& val) {
    func_in_23976(val);
    const int x = 23976;
    std::cout << 23976;
    for (int i = 0; i < 23976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23976(int& val) {
    const int x = 23976;
    std::cout << 23976;
    for (int i = 0; i < 23976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23977(int& val) {
    func_in_23977(val);
    const int x = 23977;
    std::cout << 23977;
    for (int i = 0; i < 23977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23977(int& val) {
    const int x = 23977;
    std::cout << 23977;
    for (int i = 0; i < 23977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23978(int& val) {
    func_in_23978(val);
    const int x = 23978;
    std::cout << 23978;
    for (int i = 0; i < 23978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23978(int& val) {
    const int x = 23978;
    std::cout << 23978;
    for (int i = 0; i < 23978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23979(int& val) {
    func_in_23979(val);
    const int x = 23979;
    std::cout << 23979;
    for (int i = 0; i < 23979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23979(int& val) {
    const int x = 23979;
    std::cout << 23979;
    for (int i = 0; i < 23979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23980(int& val) {
    func_in_23980(val);
    const int x = 23980;
    std::cout << 23980;
    for (int i = 0; i < 23980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23980(int& val) {
    const int x = 23980;
    std::cout << 23980;
    for (int i = 0; i < 23980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23981(int& val) {
    func_in_23981(val);
    const int x = 23981;
    std::cout << 23981;
    for (int i = 0; i < 23981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23981(int& val) {
    const int x = 23981;
    std::cout << 23981;
    for (int i = 0; i < 23981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23982(int& val) {
    func_in_23982(val);
    const int x = 23982;
    std::cout << 23982;
    for (int i = 0; i < 23982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23982(int& val) {
    const int x = 23982;
    std::cout << 23982;
    for (int i = 0; i < 23982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23983(int& val) {
    func_in_23983(val);
    const int x = 23983;
    std::cout << 23983;
    for (int i = 0; i < 23983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23983(int& val) {
    const int x = 23983;
    std::cout << 23983;
    for (int i = 0; i < 23983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23984(int& val) {
    func_in_23984(val);
    const int x = 23984;
    std::cout << 23984;
    for (int i = 0; i < 23984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23984(int& val) {
    const int x = 23984;
    std::cout << 23984;
    for (int i = 0; i < 23984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23985(int& val) {
    func_in_23985(val);
    const int x = 23985;
    std::cout << 23985;
    for (int i = 0; i < 23985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23985(int& val) {
    const int x = 23985;
    std::cout << 23985;
    for (int i = 0; i < 23985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23986(int& val) {
    func_in_23986(val);
    const int x = 23986;
    std::cout << 23986;
    for (int i = 0; i < 23986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23986(int& val) {
    const int x = 23986;
    std::cout << 23986;
    for (int i = 0; i < 23986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23987(int& val) {
    func_in_23987(val);
    const int x = 23987;
    std::cout << 23987;
    for (int i = 0; i < 23987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23987(int& val) {
    const int x = 23987;
    std::cout << 23987;
    for (int i = 0; i < 23987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23988(int& val) {
    func_in_23988(val);
    const int x = 23988;
    std::cout << 23988;
    for (int i = 0; i < 23988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23988(int& val) {
    const int x = 23988;
    std::cout << 23988;
    for (int i = 0; i < 23988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23989(int& val) {
    func_in_23989(val);
    const int x = 23989;
    std::cout << 23989;
    for (int i = 0; i < 23989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23989(int& val) {
    const int x = 23989;
    std::cout << 23989;
    for (int i = 0; i < 23989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23990(int& val) {
    func_in_23990(val);
    const int x = 23990;
    std::cout << 23990;
    for (int i = 0; i < 23990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23990(int& val) {
    const int x = 23990;
    std::cout << 23990;
    for (int i = 0; i < 23990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23991(int& val) {
    func_in_23991(val);
    const int x = 23991;
    std::cout << 23991;
    for (int i = 0; i < 23991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23991(int& val) {
    const int x = 23991;
    std::cout << 23991;
    for (int i = 0; i < 23991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23992(int& val) {
    func_in_23992(val);
    const int x = 23992;
    std::cout << 23992;
    for (int i = 0; i < 23992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23992(int& val) {
    const int x = 23992;
    std::cout << 23992;
    for (int i = 0; i < 23992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23993(int& val) {
    func_in_23993(val);
    const int x = 23993;
    std::cout << 23993;
    for (int i = 0; i < 23993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23993(int& val) {
    const int x = 23993;
    std::cout << 23993;
    for (int i = 0; i < 23993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23994(int& val) {
    func_in_23994(val);
    const int x = 23994;
    std::cout << 23994;
    for (int i = 0; i < 23994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23994(int& val) {
    const int x = 23994;
    std::cout << 23994;
    for (int i = 0; i < 23994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23995(int& val) {
    func_in_23995(val);
    const int x = 23995;
    std::cout << 23995;
    for (int i = 0; i < 23995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23995(int& val) {
    const int x = 23995;
    std::cout << 23995;
    for (int i = 0; i < 23995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23996(int& val) {
    func_in_23996(val);
    const int x = 23996;
    std::cout << 23996;
    for (int i = 0; i < 23996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23996(int& val) {
    const int x = 23996;
    std::cout << 23996;
    for (int i = 0; i < 23996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23997(int& val) {
    func_in_23997(val);
    const int x = 23997;
    std::cout << 23997;
    for (int i = 0; i < 23997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23997(int& val) {
    const int x = 23997;
    std::cout << 23997;
    for (int i = 0; i < 23997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23998(int& val) {
    func_in_23998(val);
    const int x = 23998;
    std::cout << 23998;
    for (int i = 0; i < 23998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23998(int& val) {
    const int x = 23998;
    std::cout << 23998;
    for (int i = 0; i < 23998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23999(int& val) {
    func_in_23999(val);
    const int x = 23999;
    std::cout << 23999;
    for (int i = 0; i < 23999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23999(int& val) {
    const int x = 23999;
    std::cout << 23999;
    for (int i = 0; i < 23999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

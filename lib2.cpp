#include "lib.h"
#include <iostream>
int func_8000(int& val) {
    func_in_8000(val);
    const int x = 8000;
    std::cout << 8000;
    for (int i = 0; i < 8000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8000(int& val) {
    const int x = 8000;
    std::cout << 8000;
    for (int i = 0; i < 8000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8001(int& val) {
    func_in_8001(val);
    const int x = 8001;
    std::cout << 8001;
    for (int i = 0; i < 8001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8001(int& val) {
    const int x = 8001;
    std::cout << 8001;
    for (int i = 0; i < 8001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8002(int& val) {
    func_in_8002(val);
    const int x = 8002;
    std::cout << 8002;
    for (int i = 0; i < 8002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8002(int& val) {
    const int x = 8002;
    std::cout << 8002;
    for (int i = 0; i < 8002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8003(int& val) {
    func_in_8003(val);
    const int x = 8003;
    std::cout << 8003;
    for (int i = 0; i < 8003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8003(int& val) {
    const int x = 8003;
    std::cout << 8003;
    for (int i = 0; i < 8003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8004(int& val) {
    func_in_8004(val);
    const int x = 8004;
    std::cout << 8004;
    for (int i = 0; i < 8004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8004(int& val) {
    const int x = 8004;
    std::cout << 8004;
    for (int i = 0; i < 8004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8005(int& val) {
    func_in_8005(val);
    const int x = 8005;
    std::cout << 8005;
    for (int i = 0; i < 8005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8005(int& val) {
    const int x = 8005;
    std::cout << 8005;
    for (int i = 0; i < 8005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8006(int& val) {
    func_in_8006(val);
    const int x = 8006;
    std::cout << 8006;
    for (int i = 0; i < 8006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8006(int& val) {
    const int x = 8006;
    std::cout << 8006;
    for (int i = 0; i < 8006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8007(int& val) {
    func_in_8007(val);
    const int x = 8007;
    std::cout << 8007;
    for (int i = 0; i < 8007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8007(int& val) {
    const int x = 8007;
    std::cout << 8007;
    for (int i = 0; i < 8007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8008(int& val) {
    func_in_8008(val);
    const int x = 8008;
    std::cout << 8008;
    for (int i = 0; i < 8008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8008(int& val) {
    const int x = 8008;
    std::cout << 8008;
    for (int i = 0; i < 8008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8009(int& val) {
    func_in_8009(val);
    const int x = 8009;
    std::cout << 8009;
    for (int i = 0; i < 8009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8009(int& val) {
    const int x = 8009;
    std::cout << 8009;
    for (int i = 0; i < 8009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8010(int& val) {
    func_in_8010(val);
    const int x = 8010;
    std::cout << 8010;
    for (int i = 0; i < 8010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8010(int& val) {
    const int x = 8010;
    std::cout << 8010;
    for (int i = 0; i < 8010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8011(int& val) {
    func_in_8011(val);
    const int x = 8011;
    std::cout << 8011;
    for (int i = 0; i < 8011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8011(int& val) {
    const int x = 8011;
    std::cout << 8011;
    for (int i = 0; i < 8011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8012(int& val) {
    func_in_8012(val);
    const int x = 8012;
    std::cout << 8012;
    for (int i = 0; i < 8012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8012(int& val) {
    const int x = 8012;
    std::cout << 8012;
    for (int i = 0; i < 8012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8013(int& val) {
    func_in_8013(val);
    const int x = 8013;
    std::cout << 8013;
    for (int i = 0; i < 8013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8013(int& val) {
    const int x = 8013;
    std::cout << 8013;
    for (int i = 0; i < 8013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8014(int& val) {
    func_in_8014(val);
    const int x = 8014;
    std::cout << 8014;
    for (int i = 0; i < 8014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8014(int& val) {
    const int x = 8014;
    std::cout << 8014;
    for (int i = 0; i < 8014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8015(int& val) {
    func_in_8015(val);
    const int x = 8015;
    std::cout << 8015;
    for (int i = 0; i < 8015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8015(int& val) {
    const int x = 8015;
    std::cout << 8015;
    for (int i = 0; i < 8015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8016(int& val) {
    func_in_8016(val);
    const int x = 8016;
    std::cout << 8016;
    for (int i = 0; i < 8016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8016(int& val) {
    const int x = 8016;
    std::cout << 8016;
    for (int i = 0; i < 8016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8017(int& val) {
    func_in_8017(val);
    const int x = 8017;
    std::cout << 8017;
    for (int i = 0; i < 8017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8017(int& val) {
    const int x = 8017;
    std::cout << 8017;
    for (int i = 0; i < 8017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8018(int& val) {
    func_in_8018(val);
    const int x = 8018;
    std::cout << 8018;
    for (int i = 0; i < 8018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8018(int& val) {
    const int x = 8018;
    std::cout << 8018;
    for (int i = 0; i < 8018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8019(int& val) {
    func_in_8019(val);
    const int x = 8019;
    std::cout << 8019;
    for (int i = 0; i < 8019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8019(int& val) {
    const int x = 8019;
    std::cout << 8019;
    for (int i = 0; i < 8019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8020(int& val) {
    func_in_8020(val);
    const int x = 8020;
    std::cout << 8020;
    for (int i = 0; i < 8020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8020(int& val) {
    const int x = 8020;
    std::cout << 8020;
    for (int i = 0; i < 8020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8021(int& val) {
    func_in_8021(val);
    const int x = 8021;
    std::cout << 8021;
    for (int i = 0; i < 8021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8021(int& val) {
    const int x = 8021;
    std::cout << 8021;
    for (int i = 0; i < 8021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8022(int& val) {
    func_in_8022(val);
    const int x = 8022;
    std::cout << 8022;
    for (int i = 0; i < 8022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8022(int& val) {
    const int x = 8022;
    std::cout << 8022;
    for (int i = 0; i < 8022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8023(int& val) {
    func_in_8023(val);
    const int x = 8023;
    std::cout << 8023;
    for (int i = 0; i < 8023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8023(int& val) {
    const int x = 8023;
    std::cout << 8023;
    for (int i = 0; i < 8023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8024(int& val) {
    func_in_8024(val);
    const int x = 8024;
    std::cout << 8024;
    for (int i = 0; i < 8024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8024(int& val) {
    const int x = 8024;
    std::cout << 8024;
    for (int i = 0; i < 8024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8025(int& val) {
    func_in_8025(val);
    const int x = 8025;
    std::cout << 8025;
    for (int i = 0; i < 8025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8025(int& val) {
    const int x = 8025;
    std::cout << 8025;
    for (int i = 0; i < 8025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8026(int& val) {
    func_in_8026(val);
    const int x = 8026;
    std::cout << 8026;
    for (int i = 0; i < 8026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8026(int& val) {
    const int x = 8026;
    std::cout << 8026;
    for (int i = 0; i < 8026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8027(int& val) {
    func_in_8027(val);
    const int x = 8027;
    std::cout << 8027;
    for (int i = 0; i < 8027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8027(int& val) {
    const int x = 8027;
    std::cout << 8027;
    for (int i = 0; i < 8027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8028(int& val) {
    func_in_8028(val);
    const int x = 8028;
    std::cout << 8028;
    for (int i = 0; i < 8028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8028(int& val) {
    const int x = 8028;
    std::cout << 8028;
    for (int i = 0; i < 8028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8029(int& val) {
    func_in_8029(val);
    const int x = 8029;
    std::cout << 8029;
    for (int i = 0; i < 8029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8029(int& val) {
    const int x = 8029;
    std::cout << 8029;
    for (int i = 0; i < 8029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8030(int& val) {
    func_in_8030(val);
    const int x = 8030;
    std::cout << 8030;
    for (int i = 0; i < 8030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8030(int& val) {
    const int x = 8030;
    std::cout << 8030;
    for (int i = 0; i < 8030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8031(int& val) {
    func_in_8031(val);
    const int x = 8031;
    std::cout << 8031;
    for (int i = 0; i < 8031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8031(int& val) {
    const int x = 8031;
    std::cout << 8031;
    for (int i = 0; i < 8031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8032(int& val) {
    func_in_8032(val);
    const int x = 8032;
    std::cout << 8032;
    for (int i = 0; i < 8032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8032(int& val) {
    const int x = 8032;
    std::cout << 8032;
    for (int i = 0; i < 8032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8033(int& val) {
    func_in_8033(val);
    const int x = 8033;
    std::cout << 8033;
    for (int i = 0; i < 8033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8033(int& val) {
    const int x = 8033;
    std::cout << 8033;
    for (int i = 0; i < 8033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8034(int& val) {
    func_in_8034(val);
    const int x = 8034;
    std::cout << 8034;
    for (int i = 0; i < 8034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8034(int& val) {
    const int x = 8034;
    std::cout << 8034;
    for (int i = 0; i < 8034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8035(int& val) {
    func_in_8035(val);
    const int x = 8035;
    std::cout << 8035;
    for (int i = 0; i < 8035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8035(int& val) {
    const int x = 8035;
    std::cout << 8035;
    for (int i = 0; i < 8035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8036(int& val) {
    func_in_8036(val);
    const int x = 8036;
    std::cout << 8036;
    for (int i = 0; i < 8036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8036(int& val) {
    const int x = 8036;
    std::cout << 8036;
    for (int i = 0; i < 8036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8037(int& val) {
    func_in_8037(val);
    const int x = 8037;
    std::cout << 8037;
    for (int i = 0; i < 8037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8037(int& val) {
    const int x = 8037;
    std::cout << 8037;
    for (int i = 0; i < 8037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8038(int& val) {
    func_in_8038(val);
    const int x = 8038;
    std::cout << 8038;
    for (int i = 0; i < 8038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8038(int& val) {
    const int x = 8038;
    std::cout << 8038;
    for (int i = 0; i < 8038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8039(int& val) {
    func_in_8039(val);
    const int x = 8039;
    std::cout << 8039;
    for (int i = 0; i < 8039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8039(int& val) {
    const int x = 8039;
    std::cout << 8039;
    for (int i = 0; i < 8039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8040(int& val) {
    func_in_8040(val);
    const int x = 8040;
    std::cout << 8040;
    for (int i = 0; i < 8040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8040(int& val) {
    const int x = 8040;
    std::cout << 8040;
    for (int i = 0; i < 8040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8041(int& val) {
    func_in_8041(val);
    const int x = 8041;
    std::cout << 8041;
    for (int i = 0; i < 8041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8041(int& val) {
    const int x = 8041;
    std::cout << 8041;
    for (int i = 0; i < 8041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8042(int& val) {
    func_in_8042(val);
    const int x = 8042;
    std::cout << 8042;
    for (int i = 0; i < 8042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8042(int& val) {
    const int x = 8042;
    std::cout << 8042;
    for (int i = 0; i < 8042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8043(int& val) {
    func_in_8043(val);
    const int x = 8043;
    std::cout << 8043;
    for (int i = 0; i < 8043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8043(int& val) {
    const int x = 8043;
    std::cout << 8043;
    for (int i = 0; i < 8043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8044(int& val) {
    func_in_8044(val);
    const int x = 8044;
    std::cout << 8044;
    for (int i = 0; i < 8044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8044(int& val) {
    const int x = 8044;
    std::cout << 8044;
    for (int i = 0; i < 8044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8045(int& val) {
    func_in_8045(val);
    const int x = 8045;
    std::cout << 8045;
    for (int i = 0; i < 8045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8045(int& val) {
    const int x = 8045;
    std::cout << 8045;
    for (int i = 0; i < 8045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8046(int& val) {
    func_in_8046(val);
    const int x = 8046;
    std::cout << 8046;
    for (int i = 0; i < 8046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8046(int& val) {
    const int x = 8046;
    std::cout << 8046;
    for (int i = 0; i < 8046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8047(int& val) {
    func_in_8047(val);
    const int x = 8047;
    std::cout << 8047;
    for (int i = 0; i < 8047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8047(int& val) {
    const int x = 8047;
    std::cout << 8047;
    for (int i = 0; i < 8047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8048(int& val) {
    func_in_8048(val);
    const int x = 8048;
    std::cout << 8048;
    for (int i = 0; i < 8048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8048(int& val) {
    const int x = 8048;
    std::cout << 8048;
    for (int i = 0; i < 8048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8049(int& val) {
    func_in_8049(val);
    const int x = 8049;
    std::cout << 8049;
    for (int i = 0; i < 8049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8049(int& val) {
    const int x = 8049;
    std::cout << 8049;
    for (int i = 0; i < 8049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8050(int& val) {
    func_in_8050(val);
    const int x = 8050;
    std::cout << 8050;
    for (int i = 0; i < 8050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8050(int& val) {
    const int x = 8050;
    std::cout << 8050;
    for (int i = 0; i < 8050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8051(int& val) {
    func_in_8051(val);
    const int x = 8051;
    std::cout << 8051;
    for (int i = 0; i < 8051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8051(int& val) {
    const int x = 8051;
    std::cout << 8051;
    for (int i = 0; i < 8051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8052(int& val) {
    func_in_8052(val);
    const int x = 8052;
    std::cout << 8052;
    for (int i = 0; i < 8052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8052(int& val) {
    const int x = 8052;
    std::cout << 8052;
    for (int i = 0; i < 8052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8053(int& val) {
    func_in_8053(val);
    const int x = 8053;
    std::cout << 8053;
    for (int i = 0; i < 8053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8053(int& val) {
    const int x = 8053;
    std::cout << 8053;
    for (int i = 0; i < 8053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8054(int& val) {
    func_in_8054(val);
    const int x = 8054;
    std::cout << 8054;
    for (int i = 0; i < 8054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8054(int& val) {
    const int x = 8054;
    std::cout << 8054;
    for (int i = 0; i < 8054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8055(int& val) {
    func_in_8055(val);
    const int x = 8055;
    std::cout << 8055;
    for (int i = 0; i < 8055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8055(int& val) {
    const int x = 8055;
    std::cout << 8055;
    for (int i = 0; i < 8055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8056(int& val) {
    func_in_8056(val);
    const int x = 8056;
    std::cout << 8056;
    for (int i = 0; i < 8056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8056(int& val) {
    const int x = 8056;
    std::cout << 8056;
    for (int i = 0; i < 8056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8057(int& val) {
    func_in_8057(val);
    const int x = 8057;
    std::cout << 8057;
    for (int i = 0; i < 8057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8057(int& val) {
    const int x = 8057;
    std::cout << 8057;
    for (int i = 0; i < 8057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8058(int& val) {
    func_in_8058(val);
    const int x = 8058;
    std::cout << 8058;
    for (int i = 0; i < 8058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8058(int& val) {
    const int x = 8058;
    std::cout << 8058;
    for (int i = 0; i < 8058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8059(int& val) {
    func_in_8059(val);
    const int x = 8059;
    std::cout << 8059;
    for (int i = 0; i < 8059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8059(int& val) {
    const int x = 8059;
    std::cout << 8059;
    for (int i = 0; i < 8059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8060(int& val) {
    func_in_8060(val);
    const int x = 8060;
    std::cout << 8060;
    for (int i = 0; i < 8060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8060(int& val) {
    const int x = 8060;
    std::cout << 8060;
    for (int i = 0; i < 8060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8061(int& val) {
    func_in_8061(val);
    const int x = 8061;
    std::cout << 8061;
    for (int i = 0; i < 8061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8061(int& val) {
    const int x = 8061;
    std::cout << 8061;
    for (int i = 0; i < 8061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8062(int& val) {
    func_in_8062(val);
    const int x = 8062;
    std::cout << 8062;
    for (int i = 0; i < 8062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8062(int& val) {
    const int x = 8062;
    std::cout << 8062;
    for (int i = 0; i < 8062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8063(int& val) {
    func_in_8063(val);
    const int x = 8063;
    std::cout << 8063;
    for (int i = 0; i < 8063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8063(int& val) {
    const int x = 8063;
    std::cout << 8063;
    for (int i = 0; i < 8063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8064(int& val) {
    func_in_8064(val);
    const int x = 8064;
    std::cout << 8064;
    for (int i = 0; i < 8064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8064(int& val) {
    const int x = 8064;
    std::cout << 8064;
    for (int i = 0; i < 8064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8065(int& val) {
    func_in_8065(val);
    const int x = 8065;
    std::cout << 8065;
    for (int i = 0; i < 8065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8065(int& val) {
    const int x = 8065;
    std::cout << 8065;
    for (int i = 0; i < 8065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8066(int& val) {
    func_in_8066(val);
    const int x = 8066;
    std::cout << 8066;
    for (int i = 0; i < 8066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8066(int& val) {
    const int x = 8066;
    std::cout << 8066;
    for (int i = 0; i < 8066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8067(int& val) {
    func_in_8067(val);
    const int x = 8067;
    std::cout << 8067;
    for (int i = 0; i < 8067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8067(int& val) {
    const int x = 8067;
    std::cout << 8067;
    for (int i = 0; i < 8067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8068(int& val) {
    func_in_8068(val);
    const int x = 8068;
    std::cout << 8068;
    for (int i = 0; i < 8068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8068(int& val) {
    const int x = 8068;
    std::cout << 8068;
    for (int i = 0; i < 8068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8069(int& val) {
    func_in_8069(val);
    const int x = 8069;
    std::cout << 8069;
    for (int i = 0; i < 8069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8069(int& val) {
    const int x = 8069;
    std::cout << 8069;
    for (int i = 0; i < 8069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8070(int& val) {
    func_in_8070(val);
    const int x = 8070;
    std::cout << 8070;
    for (int i = 0; i < 8070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8070(int& val) {
    const int x = 8070;
    std::cout << 8070;
    for (int i = 0; i < 8070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8071(int& val) {
    func_in_8071(val);
    const int x = 8071;
    std::cout << 8071;
    for (int i = 0; i < 8071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8071(int& val) {
    const int x = 8071;
    std::cout << 8071;
    for (int i = 0; i < 8071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8072(int& val) {
    func_in_8072(val);
    const int x = 8072;
    std::cout << 8072;
    for (int i = 0; i < 8072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8072(int& val) {
    const int x = 8072;
    std::cout << 8072;
    for (int i = 0; i < 8072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8073(int& val) {
    func_in_8073(val);
    const int x = 8073;
    std::cout << 8073;
    for (int i = 0; i < 8073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8073(int& val) {
    const int x = 8073;
    std::cout << 8073;
    for (int i = 0; i < 8073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8074(int& val) {
    func_in_8074(val);
    const int x = 8074;
    std::cout << 8074;
    for (int i = 0; i < 8074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8074(int& val) {
    const int x = 8074;
    std::cout << 8074;
    for (int i = 0; i < 8074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8075(int& val) {
    func_in_8075(val);
    const int x = 8075;
    std::cout << 8075;
    for (int i = 0; i < 8075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8075(int& val) {
    const int x = 8075;
    std::cout << 8075;
    for (int i = 0; i < 8075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8076(int& val) {
    func_in_8076(val);
    const int x = 8076;
    std::cout << 8076;
    for (int i = 0; i < 8076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8076(int& val) {
    const int x = 8076;
    std::cout << 8076;
    for (int i = 0; i < 8076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8077(int& val) {
    func_in_8077(val);
    const int x = 8077;
    std::cout << 8077;
    for (int i = 0; i < 8077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8077(int& val) {
    const int x = 8077;
    std::cout << 8077;
    for (int i = 0; i < 8077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8078(int& val) {
    func_in_8078(val);
    const int x = 8078;
    std::cout << 8078;
    for (int i = 0; i < 8078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8078(int& val) {
    const int x = 8078;
    std::cout << 8078;
    for (int i = 0; i < 8078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8079(int& val) {
    func_in_8079(val);
    const int x = 8079;
    std::cout << 8079;
    for (int i = 0; i < 8079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8079(int& val) {
    const int x = 8079;
    std::cout << 8079;
    for (int i = 0; i < 8079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8080(int& val) {
    func_in_8080(val);
    const int x = 8080;
    std::cout << 8080;
    for (int i = 0; i < 8080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8080(int& val) {
    const int x = 8080;
    std::cout << 8080;
    for (int i = 0; i < 8080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8081(int& val) {
    func_in_8081(val);
    const int x = 8081;
    std::cout << 8081;
    for (int i = 0; i < 8081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8081(int& val) {
    const int x = 8081;
    std::cout << 8081;
    for (int i = 0; i < 8081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8082(int& val) {
    func_in_8082(val);
    const int x = 8082;
    std::cout << 8082;
    for (int i = 0; i < 8082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8082(int& val) {
    const int x = 8082;
    std::cout << 8082;
    for (int i = 0; i < 8082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8083(int& val) {
    func_in_8083(val);
    const int x = 8083;
    std::cout << 8083;
    for (int i = 0; i < 8083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8083(int& val) {
    const int x = 8083;
    std::cout << 8083;
    for (int i = 0; i < 8083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8084(int& val) {
    func_in_8084(val);
    const int x = 8084;
    std::cout << 8084;
    for (int i = 0; i < 8084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8084(int& val) {
    const int x = 8084;
    std::cout << 8084;
    for (int i = 0; i < 8084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8085(int& val) {
    func_in_8085(val);
    const int x = 8085;
    std::cout << 8085;
    for (int i = 0; i < 8085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8085(int& val) {
    const int x = 8085;
    std::cout << 8085;
    for (int i = 0; i < 8085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8086(int& val) {
    func_in_8086(val);
    const int x = 8086;
    std::cout << 8086;
    for (int i = 0; i < 8086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8086(int& val) {
    const int x = 8086;
    std::cout << 8086;
    for (int i = 0; i < 8086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8087(int& val) {
    func_in_8087(val);
    const int x = 8087;
    std::cout << 8087;
    for (int i = 0; i < 8087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8087(int& val) {
    const int x = 8087;
    std::cout << 8087;
    for (int i = 0; i < 8087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8088(int& val) {
    func_in_8088(val);
    const int x = 8088;
    std::cout << 8088;
    for (int i = 0; i < 8088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8088(int& val) {
    const int x = 8088;
    std::cout << 8088;
    for (int i = 0; i < 8088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8089(int& val) {
    func_in_8089(val);
    const int x = 8089;
    std::cout << 8089;
    for (int i = 0; i < 8089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8089(int& val) {
    const int x = 8089;
    std::cout << 8089;
    for (int i = 0; i < 8089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8090(int& val) {
    func_in_8090(val);
    const int x = 8090;
    std::cout << 8090;
    for (int i = 0; i < 8090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8090(int& val) {
    const int x = 8090;
    std::cout << 8090;
    for (int i = 0; i < 8090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8091(int& val) {
    func_in_8091(val);
    const int x = 8091;
    std::cout << 8091;
    for (int i = 0; i < 8091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8091(int& val) {
    const int x = 8091;
    std::cout << 8091;
    for (int i = 0; i < 8091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8092(int& val) {
    func_in_8092(val);
    const int x = 8092;
    std::cout << 8092;
    for (int i = 0; i < 8092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8092(int& val) {
    const int x = 8092;
    std::cout << 8092;
    for (int i = 0; i < 8092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8093(int& val) {
    func_in_8093(val);
    const int x = 8093;
    std::cout << 8093;
    for (int i = 0; i < 8093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8093(int& val) {
    const int x = 8093;
    std::cout << 8093;
    for (int i = 0; i < 8093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8094(int& val) {
    func_in_8094(val);
    const int x = 8094;
    std::cout << 8094;
    for (int i = 0; i < 8094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8094(int& val) {
    const int x = 8094;
    std::cout << 8094;
    for (int i = 0; i < 8094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8095(int& val) {
    func_in_8095(val);
    const int x = 8095;
    std::cout << 8095;
    for (int i = 0; i < 8095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8095(int& val) {
    const int x = 8095;
    std::cout << 8095;
    for (int i = 0; i < 8095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8096(int& val) {
    func_in_8096(val);
    const int x = 8096;
    std::cout << 8096;
    for (int i = 0; i < 8096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8096(int& val) {
    const int x = 8096;
    std::cout << 8096;
    for (int i = 0; i < 8096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8097(int& val) {
    func_in_8097(val);
    const int x = 8097;
    std::cout << 8097;
    for (int i = 0; i < 8097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8097(int& val) {
    const int x = 8097;
    std::cout << 8097;
    for (int i = 0; i < 8097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8098(int& val) {
    func_in_8098(val);
    const int x = 8098;
    std::cout << 8098;
    for (int i = 0; i < 8098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8098(int& val) {
    const int x = 8098;
    std::cout << 8098;
    for (int i = 0; i < 8098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8099(int& val) {
    func_in_8099(val);
    const int x = 8099;
    std::cout << 8099;
    for (int i = 0; i < 8099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8099(int& val) {
    const int x = 8099;
    std::cout << 8099;
    for (int i = 0; i < 8099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8100(int& val) {
    func_in_8100(val);
    const int x = 8100;
    std::cout << 8100;
    for (int i = 0; i < 8100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8100(int& val) {
    const int x = 8100;
    std::cout << 8100;
    for (int i = 0; i < 8100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8101(int& val) {
    func_in_8101(val);
    const int x = 8101;
    std::cout << 8101;
    for (int i = 0; i < 8101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8101(int& val) {
    const int x = 8101;
    std::cout << 8101;
    for (int i = 0; i < 8101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8102(int& val) {
    func_in_8102(val);
    const int x = 8102;
    std::cout << 8102;
    for (int i = 0; i < 8102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8102(int& val) {
    const int x = 8102;
    std::cout << 8102;
    for (int i = 0; i < 8102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8103(int& val) {
    func_in_8103(val);
    const int x = 8103;
    std::cout << 8103;
    for (int i = 0; i < 8103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8103(int& val) {
    const int x = 8103;
    std::cout << 8103;
    for (int i = 0; i < 8103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8104(int& val) {
    func_in_8104(val);
    const int x = 8104;
    std::cout << 8104;
    for (int i = 0; i < 8104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8104(int& val) {
    const int x = 8104;
    std::cout << 8104;
    for (int i = 0; i < 8104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8105(int& val) {
    func_in_8105(val);
    const int x = 8105;
    std::cout << 8105;
    for (int i = 0; i < 8105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8105(int& val) {
    const int x = 8105;
    std::cout << 8105;
    for (int i = 0; i < 8105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8106(int& val) {
    func_in_8106(val);
    const int x = 8106;
    std::cout << 8106;
    for (int i = 0; i < 8106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8106(int& val) {
    const int x = 8106;
    std::cout << 8106;
    for (int i = 0; i < 8106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8107(int& val) {
    func_in_8107(val);
    const int x = 8107;
    std::cout << 8107;
    for (int i = 0; i < 8107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8107(int& val) {
    const int x = 8107;
    std::cout << 8107;
    for (int i = 0; i < 8107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8108(int& val) {
    func_in_8108(val);
    const int x = 8108;
    std::cout << 8108;
    for (int i = 0; i < 8108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8108(int& val) {
    const int x = 8108;
    std::cout << 8108;
    for (int i = 0; i < 8108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8109(int& val) {
    func_in_8109(val);
    const int x = 8109;
    std::cout << 8109;
    for (int i = 0; i < 8109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8109(int& val) {
    const int x = 8109;
    std::cout << 8109;
    for (int i = 0; i < 8109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8110(int& val) {
    func_in_8110(val);
    const int x = 8110;
    std::cout << 8110;
    for (int i = 0; i < 8110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8110(int& val) {
    const int x = 8110;
    std::cout << 8110;
    for (int i = 0; i < 8110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8111(int& val) {
    func_in_8111(val);
    const int x = 8111;
    std::cout << 8111;
    for (int i = 0; i < 8111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8111(int& val) {
    const int x = 8111;
    std::cout << 8111;
    for (int i = 0; i < 8111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8112(int& val) {
    func_in_8112(val);
    const int x = 8112;
    std::cout << 8112;
    for (int i = 0; i < 8112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8112(int& val) {
    const int x = 8112;
    std::cout << 8112;
    for (int i = 0; i < 8112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8113(int& val) {
    func_in_8113(val);
    const int x = 8113;
    std::cout << 8113;
    for (int i = 0; i < 8113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8113(int& val) {
    const int x = 8113;
    std::cout << 8113;
    for (int i = 0; i < 8113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8114(int& val) {
    func_in_8114(val);
    const int x = 8114;
    std::cout << 8114;
    for (int i = 0; i < 8114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8114(int& val) {
    const int x = 8114;
    std::cout << 8114;
    for (int i = 0; i < 8114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8115(int& val) {
    func_in_8115(val);
    const int x = 8115;
    std::cout << 8115;
    for (int i = 0; i < 8115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8115(int& val) {
    const int x = 8115;
    std::cout << 8115;
    for (int i = 0; i < 8115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8116(int& val) {
    func_in_8116(val);
    const int x = 8116;
    std::cout << 8116;
    for (int i = 0; i < 8116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8116(int& val) {
    const int x = 8116;
    std::cout << 8116;
    for (int i = 0; i < 8116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8117(int& val) {
    func_in_8117(val);
    const int x = 8117;
    std::cout << 8117;
    for (int i = 0; i < 8117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8117(int& val) {
    const int x = 8117;
    std::cout << 8117;
    for (int i = 0; i < 8117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8118(int& val) {
    func_in_8118(val);
    const int x = 8118;
    std::cout << 8118;
    for (int i = 0; i < 8118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8118(int& val) {
    const int x = 8118;
    std::cout << 8118;
    for (int i = 0; i < 8118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8119(int& val) {
    func_in_8119(val);
    const int x = 8119;
    std::cout << 8119;
    for (int i = 0; i < 8119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8119(int& val) {
    const int x = 8119;
    std::cout << 8119;
    for (int i = 0; i < 8119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8120(int& val) {
    func_in_8120(val);
    const int x = 8120;
    std::cout << 8120;
    for (int i = 0; i < 8120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8120(int& val) {
    const int x = 8120;
    std::cout << 8120;
    for (int i = 0; i < 8120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8121(int& val) {
    func_in_8121(val);
    const int x = 8121;
    std::cout << 8121;
    for (int i = 0; i < 8121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8121(int& val) {
    const int x = 8121;
    std::cout << 8121;
    for (int i = 0; i < 8121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8122(int& val) {
    func_in_8122(val);
    const int x = 8122;
    std::cout << 8122;
    for (int i = 0; i < 8122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8122(int& val) {
    const int x = 8122;
    std::cout << 8122;
    for (int i = 0; i < 8122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8123(int& val) {
    func_in_8123(val);
    const int x = 8123;
    std::cout << 8123;
    for (int i = 0; i < 8123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8123(int& val) {
    const int x = 8123;
    std::cout << 8123;
    for (int i = 0; i < 8123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8124(int& val) {
    func_in_8124(val);
    const int x = 8124;
    std::cout << 8124;
    for (int i = 0; i < 8124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8124(int& val) {
    const int x = 8124;
    std::cout << 8124;
    for (int i = 0; i < 8124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8125(int& val) {
    func_in_8125(val);
    const int x = 8125;
    std::cout << 8125;
    for (int i = 0; i < 8125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8125(int& val) {
    const int x = 8125;
    std::cout << 8125;
    for (int i = 0; i < 8125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8126(int& val) {
    func_in_8126(val);
    const int x = 8126;
    std::cout << 8126;
    for (int i = 0; i < 8126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8126(int& val) {
    const int x = 8126;
    std::cout << 8126;
    for (int i = 0; i < 8126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8127(int& val) {
    func_in_8127(val);
    const int x = 8127;
    std::cout << 8127;
    for (int i = 0; i < 8127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8127(int& val) {
    const int x = 8127;
    std::cout << 8127;
    for (int i = 0; i < 8127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8128(int& val) {
    func_in_8128(val);
    const int x = 8128;
    std::cout << 8128;
    for (int i = 0; i < 8128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8128(int& val) {
    const int x = 8128;
    std::cout << 8128;
    for (int i = 0; i < 8128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8129(int& val) {
    func_in_8129(val);
    const int x = 8129;
    std::cout << 8129;
    for (int i = 0; i < 8129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8129(int& val) {
    const int x = 8129;
    std::cout << 8129;
    for (int i = 0; i < 8129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8130(int& val) {
    func_in_8130(val);
    const int x = 8130;
    std::cout << 8130;
    for (int i = 0; i < 8130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8130(int& val) {
    const int x = 8130;
    std::cout << 8130;
    for (int i = 0; i < 8130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8131(int& val) {
    func_in_8131(val);
    const int x = 8131;
    std::cout << 8131;
    for (int i = 0; i < 8131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8131(int& val) {
    const int x = 8131;
    std::cout << 8131;
    for (int i = 0; i < 8131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8132(int& val) {
    func_in_8132(val);
    const int x = 8132;
    std::cout << 8132;
    for (int i = 0; i < 8132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8132(int& val) {
    const int x = 8132;
    std::cout << 8132;
    for (int i = 0; i < 8132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8133(int& val) {
    func_in_8133(val);
    const int x = 8133;
    std::cout << 8133;
    for (int i = 0; i < 8133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8133(int& val) {
    const int x = 8133;
    std::cout << 8133;
    for (int i = 0; i < 8133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8134(int& val) {
    func_in_8134(val);
    const int x = 8134;
    std::cout << 8134;
    for (int i = 0; i < 8134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8134(int& val) {
    const int x = 8134;
    std::cout << 8134;
    for (int i = 0; i < 8134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8135(int& val) {
    func_in_8135(val);
    const int x = 8135;
    std::cout << 8135;
    for (int i = 0; i < 8135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8135(int& val) {
    const int x = 8135;
    std::cout << 8135;
    for (int i = 0; i < 8135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8136(int& val) {
    func_in_8136(val);
    const int x = 8136;
    std::cout << 8136;
    for (int i = 0; i < 8136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8136(int& val) {
    const int x = 8136;
    std::cout << 8136;
    for (int i = 0; i < 8136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8137(int& val) {
    func_in_8137(val);
    const int x = 8137;
    std::cout << 8137;
    for (int i = 0; i < 8137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8137(int& val) {
    const int x = 8137;
    std::cout << 8137;
    for (int i = 0; i < 8137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8138(int& val) {
    func_in_8138(val);
    const int x = 8138;
    std::cout << 8138;
    for (int i = 0; i < 8138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8138(int& val) {
    const int x = 8138;
    std::cout << 8138;
    for (int i = 0; i < 8138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8139(int& val) {
    func_in_8139(val);
    const int x = 8139;
    std::cout << 8139;
    for (int i = 0; i < 8139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8139(int& val) {
    const int x = 8139;
    std::cout << 8139;
    for (int i = 0; i < 8139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8140(int& val) {
    func_in_8140(val);
    const int x = 8140;
    std::cout << 8140;
    for (int i = 0; i < 8140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8140(int& val) {
    const int x = 8140;
    std::cout << 8140;
    for (int i = 0; i < 8140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8141(int& val) {
    func_in_8141(val);
    const int x = 8141;
    std::cout << 8141;
    for (int i = 0; i < 8141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8141(int& val) {
    const int x = 8141;
    std::cout << 8141;
    for (int i = 0; i < 8141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8142(int& val) {
    func_in_8142(val);
    const int x = 8142;
    std::cout << 8142;
    for (int i = 0; i < 8142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8142(int& val) {
    const int x = 8142;
    std::cout << 8142;
    for (int i = 0; i < 8142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8143(int& val) {
    func_in_8143(val);
    const int x = 8143;
    std::cout << 8143;
    for (int i = 0; i < 8143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8143(int& val) {
    const int x = 8143;
    std::cout << 8143;
    for (int i = 0; i < 8143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8144(int& val) {
    func_in_8144(val);
    const int x = 8144;
    std::cout << 8144;
    for (int i = 0; i < 8144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8144(int& val) {
    const int x = 8144;
    std::cout << 8144;
    for (int i = 0; i < 8144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8145(int& val) {
    func_in_8145(val);
    const int x = 8145;
    std::cout << 8145;
    for (int i = 0; i < 8145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8145(int& val) {
    const int x = 8145;
    std::cout << 8145;
    for (int i = 0; i < 8145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8146(int& val) {
    func_in_8146(val);
    const int x = 8146;
    std::cout << 8146;
    for (int i = 0; i < 8146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8146(int& val) {
    const int x = 8146;
    std::cout << 8146;
    for (int i = 0; i < 8146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8147(int& val) {
    func_in_8147(val);
    const int x = 8147;
    std::cout << 8147;
    for (int i = 0; i < 8147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8147(int& val) {
    const int x = 8147;
    std::cout << 8147;
    for (int i = 0; i < 8147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8148(int& val) {
    func_in_8148(val);
    const int x = 8148;
    std::cout << 8148;
    for (int i = 0; i < 8148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8148(int& val) {
    const int x = 8148;
    std::cout << 8148;
    for (int i = 0; i < 8148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8149(int& val) {
    func_in_8149(val);
    const int x = 8149;
    std::cout << 8149;
    for (int i = 0; i < 8149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8149(int& val) {
    const int x = 8149;
    std::cout << 8149;
    for (int i = 0; i < 8149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8150(int& val) {
    func_in_8150(val);
    const int x = 8150;
    std::cout << 8150;
    for (int i = 0; i < 8150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8150(int& val) {
    const int x = 8150;
    std::cout << 8150;
    for (int i = 0; i < 8150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8151(int& val) {
    func_in_8151(val);
    const int x = 8151;
    std::cout << 8151;
    for (int i = 0; i < 8151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8151(int& val) {
    const int x = 8151;
    std::cout << 8151;
    for (int i = 0; i < 8151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8152(int& val) {
    func_in_8152(val);
    const int x = 8152;
    std::cout << 8152;
    for (int i = 0; i < 8152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8152(int& val) {
    const int x = 8152;
    std::cout << 8152;
    for (int i = 0; i < 8152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8153(int& val) {
    func_in_8153(val);
    const int x = 8153;
    std::cout << 8153;
    for (int i = 0; i < 8153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8153(int& val) {
    const int x = 8153;
    std::cout << 8153;
    for (int i = 0; i < 8153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8154(int& val) {
    func_in_8154(val);
    const int x = 8154;
    std::cout << 8154;
    for (int i = 0; i < 8154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8154(int& val) {
    const int x = 8154;
    std::cout << 8154;
    for (int i = 0; i < 8154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8155(int& val) {
    func_in_8155(val);
    const int x = 8155;
    std::cout << 8155;
    for (int i = 0; i < 8155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8155(int& val) {
    const int x = 8155;
    std::cout << 8155;
    for (int i = 0; i < 8155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8156(int& val) {
    func_in_8156(val);
    const int x = 8156;
    std::cout << 8156;
    for (int i = 0; i < 8156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8156(int& val) {
    const int x = 8156;
    std::cout << 8156;
    for (int i = 0; i < 8156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8157(int& val) {
    func_in_8157(val);
    const int x = 8157;
    std::cout << 8157;
    for (int i = 0; i < 8157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8157(int& val) {
    const int x = 8157;
    std::cout << 8157;
    for (int i = 0; i < 8157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8158(int& val) {
    func_in_8158(val);
    const int x = 8158;
    std::cout << 8158;
    for (int i = 0; i < 8158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8158(int& val) {
    const int x = 8158;
    std::cout << 8158;
    for (int i = 0; i < 8158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8159(int& val) {
    func_in_8159(val);
    const int x = 8159;
    std::cout << 8159;
    for (int i = 0; i < 8159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8159(int& val) {
    const int x = 8159;
    std::cout << 8159;
    for (int i = 0; i < 8159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8160(int& val) {
    func_in_8160(val);
    const int x = 8160;
    std::cout << 8160;
    for (int i = 0; i < 8160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8160(int& val) {
    const int x = 8160;
    std::cout << 8160;
    for (int i = 0; i < 8160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8161(int& val) {
    func_in_8161(val);
    const int x = 8161;
    std::cout << 8161;
    for (int i = 0; i < 8161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8161(int& val) {
    const int x = 8161;
    std::cout << 8161;
    for (int i = 0; i < 8161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8162(int& val) {
    func_in_8162(val);
    const int x = 8162;
    std::cout << 8162;
    for (int i = 0; i < 8162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8162(int& val) {
    const int x = 8162;
    std::cout << 8162;
    for (int i = 0; i < 8162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8163(int& val) {
    func_in_8163(val);
    const int x = 8163;
    std::cout << 8163;
    for (int i = 0; i < 8163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8163(int& val) {
    const int x = 8163;
    std::cout << 8163;
    for (int i = 0; i < 8163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8164(int& val) {
    func_in_8164(val);
    const int x = 8164;
    std::cout << 8164;
    for (int i = 0; i < 8164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8164(int& val) {
    const int x = 8164;
    std::cout << 8164;
    for (int i = 0; i < 8164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8165(int& val) {
    func_in_8165(val);
    const int x = 8165;
    std::cout << 8165;
    for (int i = 0; i < 8165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8165(int& val) {
    const int x = 8165;
    std::cout << 8165;
    for (int i = 0; i < 8165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8166(int& val) {
    func_in_8166(val);
    const int x = 8166;
    std::cout << 8166;
    for (int i = 0; i < 8166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8166(int& val) {
    const int x = 8166;
    std::cout << 8166;
    for (int i = 0; i < 8166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8167(int& val) {
    func_in_8167(val);
    const int x = 8167;
    std::cout << 8167;
    for (int i = 0; i < 8167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8167(int& val) {
    const int x = 8167;
    std::cout << 8167;
    for (int i = 0; i < 8167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8168(int& val) {
    func_in_8168(val);
    const int x = 8168;
    std::cout << 8168;
    for (int i = 0; i < 8168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8168(int& val) {
    const int x = 8168;
    std::cout << 8168;
    for (int i = 0; i < 8168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8169(int& val) {
    func_in_8169(val);
    const int x = 8169;
    std::cout << 8169;
    for (int i = 0; i < 8169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8169(int& val) {
    const int x = 8169;
    std::cout << 8169;
    for (int i = 0; i < 8169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8170(int& val) {
    func_in_8170(val);
    const int x = 8170;
    std::cout << 8170;
    for (int i = 0; i < 8170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8170(int& val) {
    const int x = 8170;
    std::cout << 8170;
    for (int i = 0; i < 8170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8171(int& val) {
    func_in_8171(val);
    const int x = 8171;
    std::cout << 8171;
    for (int i = 0; i < 8171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8171(int& val) {
    const int x = 8171;
    std::cout << 8171;
    for (int i = 0; i < 8171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8172(int& val) {
    func_in_8172(val);
    const int x = 8172;
    std::cout << 8172;
    for (int i = 0; i < 8172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8172(int& val) {
    const int x = 8172;
    std::cout << 8172;
    for (int i = 0; i < 8172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8173(int& val) {
    func_in_8173(val);
    const int x = 8173;
    std::cout << 8173;
    for (int i = 0; i < 8173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8173(int& val) {
    const int x = 8173;
    std::cout << 8173;
    for (int i = 0; i < 8173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8174(int& val) {
    func_in_8174(val);
    const int x = 8174;
    std::cout << 8174;
    for (int i = 0; i < 8174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8174(int& val) {
    const int x = 8174;
    std::cout << 8174;
    for (int i = 0; i < 8174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8175(int& val) {
    func_in_8175(val);
    const int x = 8175;
    std::cout << 8175;
    for (int i = 0; i < 8175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8175(int& val) {
    const int x = 8175;
    std::cout << 8175;
    for (int i = 0; i < 8175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8176(int& val) {
    func_in_8176(val);
    const int x = 8176;
    std::cout << 8176;
    for (int i = 0; i < 8176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8176(int& val) {
    const int x = 8176;
    std::cout << 8176;
    for (int i = 0; i < 8176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8177(int& val) {
    func_in_8177(val);
    const int x = 8177;
    std::cout << 8177;
    for (int i = 0; i < 8177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8177(int& val) {
    const int x = 8177;
    std::cout << 8177;
    for (int i = 0; i < 8177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8178(int& val) {
    func_in_8178(val);
    const int x = 8178;
    std::cout << 8178;
    for (int i = 0; i < 8178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8178(int& val) {
    const int x = 8178;
    std::cout << 8178;
    for (int i = 0; i < 8178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8179(int& val) {
    func_in_8179(val);
    const int x = 8179;
    std::cout << 8179;
    for (int i = 0; i < 8179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8179(int& val) {
    const int x = 8179;
    std::cout << 8179;
    for (int i = 0; i < 8179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8180(int& val) {
    func_in_8180(val);
    const int x = 8180;
    std::cout << 8180;
    for (int i = 0; i < 8180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8180(int& val) {
    const int x = 8180;
    std::cout << 8180;
    for (int i = 0; i < 8180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8181(int& val) {
    func_in_8181(val);
    const int x = 8181;
    std::cout << 8181;
    for (int i = 0; i < 8181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8181(int& val) {
    const int x = 8181;
    std::cout << 8181;
    for (int i = 0; i < 8181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8182(int& val) {
    func_in_8182(val);
    const int x = 8182;
    std::cout << 8182;
    for (int i = 0; i < 8182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8182(int& val) {
    const int x = 8182;
    std::cout << 8182;
    for (int i = 0; i < 8182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8183(int& val) {
    func_in_8183(val);
    const int x = 8183;
    std::cout << 8183;
    for (int i = 0; i < 8183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8183(int& val) {
    const int x = 8183;
    std::cout << 8183;
    for (int i = 0; i < 8183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8184(int& val) {
    func_in_8184(val);
    const int x = 8184;
    std::cout << 8184;
    for (int i = 0; i < 8184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8184(int& val) {
    const int x = 8184;
    std::cout << 8184;
    for (int i = 0; i < 8184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8185(int& val) {
    func_in_8185(val);
    const int x = 8185;
    std::cout << 8185;
    for (int i = 0; i < 8185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8185(int& val) {
    const int x = 8185;
    std::cout << 8185;
    for (int i = 0; i < 8185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8186(int& val) {
    func_in_8186(val);
    const int x = 8186;
    std::cout << 8186;
    for (int i = 0; i < 8186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8186(int& val) {
    const int x = 8186;
    std::cout << 8186;
    for (int i = 0; i < 8186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8187(int& val) {
    func_in_8187(val);
    const int x = 8187;
    std::cout << 8187;
    for (int i = 0; i < 8187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8187(int& val) {
    const int x = 8187;
    std::cout << 8187;
    for (int i = 0; i < 8187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8188(int& val) {
    func_in_8188(val);
    const int x = 8188;
    std::cout << 8188;
    for (int i = 0; i < 8188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8188(int& val) {
    const int x = 8188;
    std::cout << 8188;
    for (int i = 0; i < 8188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8189(int& val) {
    func_in_8189(val);
    const int x = 8189;
    std::cout << 8189;
    for (int i = 0; i < 8189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8189(int& val) {
    const int x = 8189;
    std::cout << 8189;
    for (int i = 0; i < 8189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8190(int& val) {
    func_in_8190(val);
    const int x = 8190;
    std::cout << 8190;
    for (int i = 0; i < 8190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8190(int& val) {
    const int x = 8190;
    std::cout << 8190;
    for (int i = 0; i < 8190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8191(int& val) {
    func_in_8191(val);
    const int x = 8191;
    std::cout << 8191;
    for (int i = 0; i < 8191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8191(int& val) {
    const int x = 8191;
    std::cout << 8191;
    for (int i = 0; i < 8191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8192(int& val) {
    func_in_8192(val);
    const int x = 8192;
    std::cout << 8192;
    for (int i = 0; i < 8192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8192(int& val) {
    const int x = 8192;
    std::cout << 8192;
    for (int i = 0; i < 8192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8193(int& val) {
    func_in_8193(val);
    const int x = 8193;
    std::cout << 8193;
    for (int i = 0; i < 8193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8193(int& val) {
    const int x = 8193;
    std::cout << 8193;
    for (int i = 0; i < 8193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8194(int& val) {
    func_in_8194(val);
    const int x = 8194;
    std::cout << 8194;
    for (int i = 0; i < 8194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8194(int& val) {
    const int x = 8194;
    std::cout << 8194;
    for (int i = 0; i < 8194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8195(int& val) {
    func_in_8195(val);
    const int x = 8195;
    std::cout << 8195;
    for (int i = 0; i < 8195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8195(int& val) {
    const int x = 8195;
    std::cout << 8195;
    for (int i = 0; i < 8195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8196(int& val) {
    func_in_8196(val);
    const int x = 8196;
    std::cout << 8196;
    for (int i = 0; i < 8196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8196(int& val) {
    const int x = 8196;
    std::cout << 8196;
    for (int i = 0; i < 8196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8197(int& val) {
    func_in_8197(val);
    const int x = 8197;
    std::cout << 8197;
    for (int i = 0; i < 8197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8197(int& val) {
    const int x = 8197;
    std::cout << 8197;
    for (int i = 0; i < 8197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8198(int& val) {
    func_in_8198(val);
    const int x = 8198;
    std::cout << 8198;
    for (int i = 0; i < 8198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8198(int& val) {
    const int x = 8198;
    std::cout << 8198;
    for (int i = 0; i < 8198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8199(int& val) {
    func_in_8199(val);
    const int x = 8199;
    std::cout << 8199;
    for (int i = 0; i < 8199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8199(int& val) {
    const int x = 8199;
    std::cout << 8199;
    for (int i = 0; i < 8199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8200(int& val) {
    func_in_8200(val);
    const int x = 8200;
    std::cout << 8200;
    for (int i = 0; i < 8200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8200(int& val) {
    const int x = 8200;
    std::cout << 8200;
    for (int i = 0; i < 8200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8201(int& val) {
    func_in_8201(val);
    const int x = 8201;
    std::cout << 8201;
    for (int i = 0; i < 8201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8201(int& val) {
    const int x = 8201;
    std::cout << 8201;
    for (int i = 0; i < 8201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8202(int& val) {
    func_in_8202(val);
    const int x = 8202;
    std::cout << 8202;
    for (int i = 0; i < 8202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8202(int& val) {
    const int x = 8202;
    std::cout << 8202;
    for (int i = 0; i < 8202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8203(int& val) {
    func_in_8203(val);
    const int x = 8203;
    std::cout << 8203;
    for (int i = 0; i < 8203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8203(int& val) {
    const int x = 8203;
    std::cout << 8203;
    for (int i = 0; i < 8203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8204(int& val) {
    func_in_8204(val);
    const int x = 8204;
    std::cout << 8204;
    for (int i = 0; i < 8204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8204(int& val) {
    const int x = 8204;
    std::cout << 8204;
    for (int i = 0; i < 8204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8205(int& val) {
    func_in_8205(val);
    const int x = 8205;
    std::cout << 8205;
    for (int i = 0; i < 8205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8205(int& val) {
    const int x = 8205;
    std::cout << 8205;
    for (int i = 0; i < 8205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8206(int& val) {
    func_in_8206(val);
    const int x = 8206;
    std::cout << 8206;
    for (int i = 0; i < 8206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8206(int& val) {
    const int x = 8206;
    std::cout << 8206;
    for (int i = 0; i < 8206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8207(int& val) {
    func_in_8207(val);
    const int x = 8207;
    std::cout << 8207;
    for (int i = 0; i < 8207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8207(int& val) {
    const int x = 8207;
    std::cout << 8207;
    for (int i = 0; i < 8207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8208(int& val) {
    func_in_8208(val);
    const int x = 8208;
    std::cout << 8208;
    for (int i = 0; i < 8208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8208(int& val) {
    const int x = 8208;
    std::cout << 8208;
    for (int i = 0; i < 8208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8209(int& val) {
    func_in_8209(val);
    const int x = 8209;
    std::cout << 8209;
    for (int i = 0; i < 8209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8209(int& val) {
    const int x = 8209;
    std::cout << 8209;
    for (int i = 0; i < 8209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8210(int& val) {
    func_in_8210(val);
    const int x = 8210;
    std::cout << 8210;
    for (int i = 0; i < 8210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8210(int& val) {
    const int x = 8210;
    std::cout << 8210;
    for (int i = 0; i < 8210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8211(int& val) {
    func_in_8211(val);
    const int x = 8211;
    std::cout << 8211;
    for (int i = 0; i < 8211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8211(int& val) {
    const int x = 8211;
    std::cout << 8211;
    for (int i = 0; i < 8211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8212(int& val) {
    func_in_8212(val);
    const int x = 8212;
    std::cout << 8212;
    for (int i = 0; i < 8212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8212(int& val) {
    const int x = 8212;
    std::cout << 8212;
    for (int i = 0; i < 8212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8213(int& val) {
    func_in_8213(val);
    const int x = 8213;
    std::cout << 8213;
    for (int i = 0; i < 8213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8213(int& val) {
    const int x = 8213;
    std::cout << 8213;
    for (int i = 0; i < 8213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8214(int& val) {
    func_in_8214(val);
    const int x = 8214;
    std::cout << 8214;
    for (int i = 0; i < 8214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8214(int& val) {
    const int x = 8214;
    std::cout << 8214;
    for (int i = 0; i < 8214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8215(int& val) {
    func_in_8215(val);
    const int x = 8215;
    std::cout << 8215;
    for (int i = 0; i < 8215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8215(int& val) {
    const int x = 8215;
    std::cout << 8215;
    for (int i = 0; i < 8215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8216(int& val) {
    func_in_8216(val);
    const int x = 8216;
    std::cout << 8216;
    for (int i = 0; i < 8216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8216(int& val) {
    const int x = 8216;
    std::cout << 8216;
    for (int i = 0; i < 8216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8217(int& val) {
    func_in_8217(val);
    const int x = 8217;
    std::cout << 8217;
    for (int i = 0; i < 8217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8217(int& val) {
    const int x = 8217;
    std::cout << 8217;
    for (int i = 0; i < 8217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8218(int& val) {
    func_in_8218(val);
    const int x = 8218;
    std::cout << 8218;
    for (int i = 0; i < 8218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8218(int& val) {
    const int x = 8218;
    std::cout << 8218;
    for (int i = 0; i < 8218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8219(int& val) {
    func_in_8219(val);
    const int x = 8219;
    std::cout << 8219;
    for (int i = 0; i < 8219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8219(int& val) {
    const int x = 8219;
    std::cout << 8219;
    for (int i = 0; i < 8219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8220(int& val) {
    func_in_8220(val);
    const int x = 8220;
    std::cout << 8220;
    for (int i = 0; i < 8220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8220(int& val) {
    const int x = 8220;
    std::cout << 8220;
    for (int i = 0; i < 8220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8221(int& val) {
    func_in_8221(val);
    const int x = 8221;
    std::cout << 8221;
    for (int i = 0; i < 8221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8221(int& val) {
    const int x = 8221;
    std::cout << 8221;
    for (int i = 0; i < 8221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8222(int& val) {
    func_in_8222(val);
    const int x = 8222;
    std::cout << 8222;
    for (int i = 0; i < 8222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8222(int& val) {
    const int x = 8222;
    std::cout << 8222;
    for (int i = 0; i < 8222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8223(int& val) {
    func_in_8223(val);
    const int x = 8223;
    std::cout << 8223;
    for (int i = 0; i < 8223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8223(int& val) {
    const int x = 8223;
    std::cout << 8223;
    for (int i = 0; i < 8223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8224(int& val) {
    func_in_8224(val);
    const int x = 8224;
    std::cout << 8224;
    for (int i = 0; i < 8224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8224(int& val) {
    const int x = 8224;
    std::cout << 8224;
    for (int i = 0; i < 8224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8225(int& val) {
    func_in_8225(val);
    const int x = 8225;
    std::cout << 8225;
    for (int i = 0; i < 8225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8225(int& val) {
    const int x = 8225;
    std::cout << 8225;
    for (int i = 0; i < 8225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8226(int& val) {
    func_in_8226(val);
    const int x = 8226;
    std::cout << 8226;
    for (int i = 0; i < 8226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8226(int& val) {
    const int x = 8226;
    std::cout << 8226;
    for (int i = 0; i < 8226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8227(int& val) {
    func_in_8227(val);
    const int x = 8227;
    std::cout << 8227;
    for (int i = 0; i < 8227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8227(int& val) {
    const int x = 8227;
    std::cout << 8227;
    for (int i = 0; i < 8227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8228(int& val) {
    func_in_8228(val);
    const int x = 8228;
    std::cout << 8228;
    for (int i = 0; i < 8228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8228(int& val) {
    const int x = 8228;
    std::cout << 8228;
    for (int i = 0; i < 8228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8229(int& val) {
    func_in_8229(val);
    const int x = 8229;
    std::cout << 8229;
    for (int i = 0; i < 8229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8229(int& val) {
    const int x = 8229;
    std::cout << 8229;
    for (int i = 0; i < 8229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8230(int& val) {
    func_in_8230(val);
    const int x = 8230;
    std::cout << 8230;
    for (int i = 0; i < 8230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8230(int& val) {
    const int x = 8230;
    std::cout << 8230;
    for (int i = 0; i < 8230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8231(int& val) {
    func_in_8231(val);
    const int x = 8231;
    std::cout << 8231;
    for (int i = 0; i < 8231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8231(int& val) {
    const int x = 8231;
    std::cout << 8231;
    for (int i = 0; i < 8231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8232(int& val) {
    func_in_8232(val);
    const int x = 8232;
    std::cout << 8232;
    for (int i = 0; i < 8232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8232(int& val) {
    const int x = 8232;
    std::cout << 8232;
    for (int i = 0; i < 8232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8233(int& val) {
    func_in_8233(val);
    const int x = 8233;
    std::cout << 8233;
    for (int i = 0; i < 8233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8233(int& val) {
    const int x = 8233;
    std::cout << 8233;
    for (int i = 0; i < 8233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8234(int& val) {
    func_in_8234(val);
    const int x = 8234;
    std::cout << 8234;
    for (int i = 0; i < 8234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8234(int& val) {
    const int x = 8234;
    std::cout << 8234;
    for (int i = 0; i < 8234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8235(int& val) {
    func_in_8235(val);
    const int x = 8235;
    std::cout << 8235;
    for (int i = 0; i < 8235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8235(int& val) {
    const int x = 8235;
    std::cout << 8235;
    for (int i = 0; i < 8235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8236(int& val) {
    func_in_8236(val);
    const int x = 8236;
    std::cout << 8236;
    for (int i = 0; i < 8236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8236(int& val) {
    const int x = 8236;
    std::cout << 8236;
    for (int i = 0; i < 8236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8237(int& val) {
    func_in_8237(val);
    const int x = 8237;
    std::cout << 8237;
    for (int i = 0; i < 8237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8237(int& val) {
    const int x = 8237;
    std::cout << 8237;
    for (int i = 0; i < 8237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8238(int& val) {
    func_in_8238(val);
    const int x = 8238;
    std::cout << 8238;
    for (int i = 0; i < 8238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8238(int& val) {
    const int x = 8238;
    std::cout << 8238;
    for (int i = 0; i < 8238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8239(int& val) {
    func_in_8239(val);
    const int x = 8239;
    std::cout << 8239;
    for (int i = 0; i < 8239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8239(int& val) {
    const int x = 8239;
    std::cout << 8239;
    for (int i = 0; i < 8239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8240(int& val) {
    func_in_8240(val);
    const int x = 8240;
    std::cout << 8240;
    for (int i = 0; i < 8240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8240(int& val) {
    const int x = 8240;
    std::cout << 8240;
    for (int i = 0; i < 8240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8241(int& val) {
    func_in_8241(val);
    const int x = 8241;
    std::cout << 8241;
    for (int i = 0; i < 8241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8241(int& val) {
    const int x = 8241;
    std::cout << 8241;
    for (int i = 0; i < 8241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8242(int& val) {
    func_in_8242(val);
    const int x = 8242;
    std::cout << 8242;
    for (int i = 0; i < 8242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8242(int& val) {
    const int x = 8242;
    std::cout << 8242;
    for (int i = 0; i < 8242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8243(int& val) {
    func_in_8243(val);
    const int x = 8243;
    std::cout << 8243;
    for (int i = 0; i < 8243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8243(int& val) {
    const int x = 8243;
    std::cout << 8243;
    for (int i = 0; i < 8243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8244(int& val) {
    func_in_8244(val);
    const int x = 8244;
    std::cout << 8244;
    for (int i = 0; i < 8244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8244(int& val) {
    const int x = 8244;
    std::cout << 8244;
    for (int i = 0; i < 8244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8245(int& val) {
    func_in_8245(val);
    const int x = 8245;
    std::cout << 8245;
    for (int i = 0; i < 8245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8245(int& val) {
    const int x = 8245;
    std::cout << 8245;
    for (int i = 0; i < 8245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8246(int& val) {
    func_in_8246(val);
    const int x = 8246;
    std::cout << 8246;
    for (int i = 0; i < 8246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8246(int& val) {
    const int x = 8246;
    std::cout << 8246;
    for (int i = 0; i < 8246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8247(int& val) {
    func_in_8247(val);
    const int x = 8247;
    std::cout << 8247;
    for (int i = 0; i < 8247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8247(int& val) {
    const int x = 8247;
    std::cout << 8247;
    for (int i = 0; i < 8247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8248(int& val) {
    func_in_8248(val);
    const int x = 8248;
    std::cout << 8248;
    for (int i = 0; i < 8248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8248(int& val) {
    const int x = 8248;
    std::cout << 8248;
    for (int i = 0; i < 8248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8249(int& val) {
    func_in_8249(val);
    const int x = 8249;
    std::cout << 8249;
    for (int i = 0; i < 8249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8249(int& val) {
    const int x = 8249;
    std::cout << 8249;
    for (int i = 0; i < 8249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8250(int& val) {
    func_in_8250(val);
    const int x = 8250;
    std::cout << 8250;
    for (int i = 0; i < 8250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8250(int& val) {
    const int x = 8250;
    std::cout << 8250;
    for (int i = 0; i < 8250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8251(int& val) {
    func_in_8251(val);
    const int x = 8251;
    std::cout << 8251;
    for (int i = 0; i < 8251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8251(int& val) {
    const int x = 8251;
    std::cout << 8251;
    for (int i = 0; i < 8251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8252(int& val) {
    func_in_8252(val);
    const int x = 8252;
    std::cout << 8252;
    for (int i = 0; i < 8252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8252(int& val) {
    const int x = 8252;
    std::cout << 8252;
    for (int i = 0; i < 8252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8253(int& val) {
    func_in_8253(val);
    const int x = 8253;
    std::cout << 8253;
    for (int i = 0; i < 8253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8253(int& val) {
    const int x = 8253;
    std::cout << 8253;
    for (int i = 0; i < 8253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8254(int& val) {
    func_in_8254(val);
    const int x = 8254;
    std::cout << 8254;
    for (int i = 0; i < 8254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8254(int& val) {
    const int x = 8254;
    std::cout << 8254;
    for (int i = 0; i < 8254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8255(int& val) {
    func_in_8255(val);
    const int x = 8255;
    std::cout << 8255;
    for (int i = 0; i < 8255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8255(int& val) {
    const int x = 8255;
    std::cout << 8255;
    for (int i = 0; i < 8255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8256(int& val) {
    func_in_8256(val);
    const int x = 8256;
    std::cout << 8256;
    for (int i = 0; i < 8256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8256(int& val) {
    const int x = 8256;
    std::cout << 8256;
    for (int i = 0; i < 8256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8257(int& val) {
    func_in_8257(val);
    const int x = 8257;
    std::cout << 8257;
    for (int i = 0; i < 8257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8257(int& val) {
    const int x = 8257;
    std::cout << 8257;
    for (int i = 0; i < 8257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8258(int& val) {
    func_in_8258(val);
    const int x = 8258;
    std::cout << 8258;
    for (int i = 0; i < 8258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8258(int& val) {
    const int x = 8258;
    std::cout << 8258;
    for (int i = 0; i < 8258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8259(int& val) {
    func_in_8259(val);
    const int x = 8259;
    std::cout << 8259;
    for (int i = 0; i < 8259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8259(int& val) {
    const int x = 8259;
    std::cout << 8259;
    for (int i = 0; i < 8259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8260(int& val) {
    func_in_8260(val);
    const int x = 8260;
    std::cout << 8260;
    for (int i = 0; i < 8260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8260(int& val) {
    const int x = 8260;
    std::cout << 8260;
    for (int i = 0; i < 8260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8261(int& val) {
    func_in_8261(val);
    const int x = 8261;
    std::cout << 8261;
    for (int i = 0; i < 8261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8261(int& val) {
    const int x = 8261;
    std::cout << 8261;
    for (int i = 0; i < 8261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8262(int& val) {
    func_in_8262(val);
    const int x = 8262;
    std::cout << 8262;
    for (int i = 0; i < 8262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8262(int& val) {
    const int x = 8262;
    std::cout << 8262;
    for (int i = 0; i < 8262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8263(int& val) {
    func_in_8263(val);
    const int x = 8263;
    std::cout << 8263;
    for (int i = 0; i < 8263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8263(int& val) {
    const int x = 8263;
    std::cout << 8263;
    for (int i = 0; i < 8263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8264(int& val) {
    func_in_8264(val);
    const int x = 8264;
    std::cout << 8264;
    for (int i = 0; i < 8264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8264(int& val) {
    const int x = 8264;
    std::cout << 8264;
    for (int i = 0; i < 8264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8265(int& val) {
    func_in_8265(val);
    const int x = 8265;
    std::cout << 8265;
    for (int i = 0; i < 8265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8265(int& val) {
    const int x = 8265;
    std::cout << 8265;
    for (int i = 0; i < 8265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8266(int& val) {
    func_in_8266(val);
    const int x = 8266;
    std::cout << 8266;
    for (int i = 0; i < 8266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8266(int& val) {
    const int x = 8266;
    std::cout << 8266;
    for (int i = 0; i < 8266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8267(int& val) {
    func_in_8267(val);
    const int x = 8267;
    std::cout << 8267;
    for (int i = 0; i < 8267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8267(int& val) {
    const int x = 8267;
    std::cout << 8267;
    for (int i = 0; i < 8267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8268(int& val) {
    func_in_8268(val);
    const int x = 8268;
    std::cout << 8268;
    for (int i = 0; i < 8268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8268(int& val) {
    const int x = 8268;
    std::cout << 8268;
    for (int i = 0; i < 8268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8269(int& val) {
    func_in_8269(val);
    const int x = 8269;
    std::cout << 8269;
    for (int i = 0; i < 8269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8269(int& val) {
    const int x = 8269;
    std::cout << 8269;
    for (int i = 0; i < 8269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8270(int& val) {
    func_in_8270(val);
    const int x = 8270;
    std::cout << 8270;
    for (int i = 0; i < 8270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8270(int& val) {
    const int x = 8270;
    std::cout << 8270;
    for (int i = 0; i < 8270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8271(int& val) {
    func_in_8271(val);
    const int x = 8271;
    std::cout << 8271;
    for (int i = 0; i < 8271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8271(int& val) {
    const int x = 8271;
    std::cout << 8271;
    for (int i = 0; i < 8271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8272(int& val) {
    func_in_8272(val);
    const int x = 8272;
    std::cout << 8272;
    for (int i = 0; i < 8272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8272(int& val) {
    const int x = 8272;
    std::cout << 8272;
    for (int i = 0; i < 8272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8273(int& val) {
    func_in_8273(val);
    const int x = 8273;
    std::cout << 8273;
    for (int i = 0; i < 8273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8273(int& val) {
    const int x = 8273;
    std::cout << 8273;
    for (int i = 0; i < 8273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8274(int& val) {
    func_in_8274(val);
    const int x = 8274;
    std::cout << 8274;
    for (int i = 0; i < 8274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8274(int& val) {
    const int x = 8274;
    std::cout << 8274;
    for (int i = 0; i < 8274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8275(int& val) {
    func_in_8275(val);
    const int x = 8275;
    std::cout << 8275;
    for (int i = 0; i < 8275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8275(int& val) {
    const int x = 8275;
    std::cout << 8275;
    for (int i = 0; i < 8275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8276(int& val) {
    func_in_8276(val);
    const int x = 8276;
    std::cout << 8276;
    for (int i = 0; i < 8276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8276(int& val) {
    const int x = 8276;
    std::cout << 8276;
    for (int i = 0; i < 8276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8277(int& val) {
    func_in_8277(val);
    const int x = 8277;
    std::cout << 8277;
    for (int i = 0; i < 8277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8277(int& val) {
    const int x = 8277;
    std::cout << 8277;
    for (int i = 0; i < 8277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8278(int& val) {
    func_in_8278(val);
    const int x = 8278;
    std::cout << 8278;
    for (int i = 0; i < 8278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8278(int& val) {
    const int x = 8278;
    std::cout << 8278;
    for (int i = 0; i < 8278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8279(int& val) {
    func_in_8279(val);
    const int x = 8279;
    std::cout << 8279;
    for (int i = 0; i < 8279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8279(int& val) {
    const int x = 8279;
    std::cout << 8279;
    for (int i = 0; i < 8279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8280(int& val) {
    func_in_8280(val);
    const int x = 8280;
    std::cout << 8280;
    for (int i = 0; i < 8280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8280(int& val) {
    const int x = 8280;
    std::cout << 8280;
    for (int i = 0; i < 8280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8281(int& val) {
    func_in_8281(val);
    const int x = 8281;
    std::cout << 8281;
    for (int i = 0; i < 8281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8281(int& val) {
    const int x = 8281;
    std::cout << 8281;
    for (int i = 0; i < 8281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8282(int& val) {
    func_in_8282(val);
    const int x = 8282;
    std::cout << 8282;
    for (int i = 0; i < 8282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8282(int& val) {
    const int x = 8282;
    std::cout << 8282;
    for (int i = 0; i < 8282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8283(int& val) {
    func_in_8283(val);
    const int x = 8283;
    std::cout << 8283;
    for (int i = 0; i < 8283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8283(int& val) {
    const int x = 8283;
    std::cout << 8283;
    for (int i = 0; i < 8283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8284(int& val) {
    func_in_8284(val);
    const int x = 8284;
    std::cout << 8284;
    for (int i = 0; i < 8284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8284(int& val) {
    const int x = 8284;
    std::cout << 8284;
    for (int i = 0; i < 8284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8285(int& val) {
    func_in_8285(val);
    const int x = 8285;
    std::cout << 8285;
    for (int i = 0; i < 8285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8285(int& val) {
    const int x = 8285;
    std::cout << 8285;
    for (int i = 0; i < 8285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8286(int& val) {
    func_in_8286(val);
    const int x = 8286;
    std::cout << 8286;
    for (int i = 0; i < 8286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8286(int& val) {
    const int x = 8286;
    std::cout << 8286;
    for (int i = 0; i < 8286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8287(int& val) {
    func_in_8287(val);
    const int x = 8287;
    std::cout << 8287;
    for (int i = 0; i < 8287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8287(int& val) {
    const int x = 8287;
    std::cout << 8287;
    for (int i = 0; i < 8287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8288(int& val) {
    func_in_8288(val);
    const int x = 8288;
    std::cout << 8288;
    for (int i = 0; i < 8288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8288(int& val) {
    const int x = 8288;
    std::cout << 8288;
    for (int i = 0; i < 8288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8289(int& val) {
    func_in_8289(val);
    const int x = 8289;
    std::cout << 8289;
    for (int i = 0; i < 8289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8289(int& val) {
    const int x = 8289;
    std::cout << 8289;
    for (int i = 0; i < 8289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8290(int& val) {
    func_in_8290(val);
    const int x = 8290;
    std::cout << 8290;
    for (int i = 0; i < 8290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8290(int& val) {
    const int x = 8290;
    std::cout << 8290;
    for (int i = 0; i < 8290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8291(int& val) {
    func_in_8291(val);
    const int x = 8291;
    std::cout << 8291;
    for (int i = 0; i < 8291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8291(int& val) {
    const int x = 8291;
    std::cout << 8291;
    for (int i = 0; i < 8291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8292(int& val) {
    func_in_8292(val);
    const int x = 8292;
    std::cout << 8292;
    for (int i = 0; i < 8292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8292(int& val) {
    const int x = 8292;
    std::cout << 8292;
    for (int i = 0; i < 8292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8293(int& val) {
    func_in_8293(val);
    const int x = 8293;
    std::cout << 8293;
    for (int i = 0; i < 8293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8293(int& val) {
    const int x = 8293;
    std::cout << 8293;
    for (int i = 0; i < 8293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8294(int& val) {
    func_in_8294(val);
    const int x = 8294;
    std::cout << 8294;
    for (int i = 0; i < 8294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8294(int& val) {
    const int x = 8294;
    std::cout << 8294;
    for (int i = 0; i < 8294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8295(int& val) {
    func_in_8295(val);
    const int x = 8295;
    std::cout << 8295;
    for (int i = 0; i < 8295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8295(int& val) {
    const int x = 8295;
    std::cout << 8295;
    for (int i = 0; i < 8295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8296(int& val) {
    func_in_8296(val);
    const int x = 8296;
    std::cout << 8296;
    for (int i = 0; i < 8296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8296(int& val) {
    const int x = 8296;
    std::cout << 8296;
    for (int i = 0; i < 8296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8297(int& val) {
    func_in_8297(val);
    const int x = 8297;
    std::cout << 8297;
    for (int i = 0; i < 8297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8297(int& val) {
    const int x = 8297;
    std::cout << 8297;
    for (int i = 0; i < 8297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8298(int& val) {
    func_in_8298(val);
    const int x = 8298;
    std::cout << 8298;
    for (int i = 0; i < 8298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8298(int& val) {
    const int x = 8298;
    std::cout << 8298;
    for (int i = 0; i < 8298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8299(int& val) {
    func_in_8299(val);
    const int x = 8299;
    std::cout << 8299;
    for (int i = 0; i < 8299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8299(int& val) {
    const int x = 8299;
    std::cout << 8299;
    for (int i = 0; i < 8299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8300(int& val) {
    func_in_8300(val);
    const int x = 8300;
    std::cout << 8300;
    for (int i = 0; i < 8300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8300(int& val) {
    const int x = 8300;
    std::cout << 8300;
    for (int i = 0; i < 8300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8301(int& val) {
    func_in_8301(val);
    const int x = 8301;
    std::cout << 8301;
    for (int i = 0; i < 8301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8301(int& val) {
    const int x = 8301;
    std::cout << 8301;
    for (int i = 0; i < 8301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8302(int& val) {
    func_in_8302(val);
    const int x = 8302;
    std::cout << 8302;
    for (int i = 0; i < 8302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8302(int& val) {
    const int x = 8302;
    std::cout << 8302;
    for (int i = 0; i < 8302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8303(int& val) {
    func_in_8303(val);
    const int x = 8303;
    std::cout << 8303;
    for (int i = 0; i < 8303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8303(int& val) {
    const int x = 8303;
    std::cout << 8303;
    for (int i = 0; i < 8303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8304(int& val) {
    func_in_8304(val);
    const int x = 8304;
    std::cout << 8304;
    for (int i = 0; i < 8304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8304(int& val) {
    const int x = 8304;
    std::cout << 8304;
    for (int i = 0; i < 8304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8305(int& val) {
    func_in_8305(val);
    const int x = 8305;
    std::cout << 8305;
    for (int i = 0; i < 8305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8305(int& val) {
    const int x = 8305;
    std::cout << 8305;
    for (int i = 0; i < 8305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8306(int& val) {
    func_in_8306(val);
    const int x = 8306;
    std::cout << 8306;
    for (int i = 0; i < 8306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8306(int& val) {
    const int x = 8306;
    std::cout << 8306;
    for (int i = 0; i < 8306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8307(int& val) {
    func_in_8307(val);
    const int x = 8307;
    std::cout << 8307;
    for (int i = 0; i < 8307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8307(int& val) {
    const int x = 8307;
    std::cout << 8307;
    for (int i = 0; i < 8307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8308(int& val) {
    func_in_8308(val);
    const int x = 8308;
    std::cout << 8308;
    for (int i = 0; i < 8308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8308(int& val) {
    const int x = 8308;
    std::cout << 8308;
    for (int i = 0; i < 8308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8309(int& val) {
    func_in_8309(val);
    const int x = 8309;
    std::cout << 8309;
    for (int i = 0; i < 8309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8309(int& val) {
    const int x = 8309;
    std::cout << 8309;
    for (int i = 0; i < 8309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8310(int& val) {
    func_in_8310(val);
    const int x = 8310;
    std::cout << 8310;
    for (int i = 0; i < 8310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8310(int& val) {
    const int x = 8310;
    std::cout << 8310;
    for (int i = 0; i < 8310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8311(int& val) {
    func_in_8311(val);
    const int x = 8311;
    std::cout << 8311;
    for (int i = 0; i < 8311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8311(int& val) {
    const int x = 8311;
    std::cout << 8311;
    for (int i = 0; i < 8311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8312(int& val) {
    func_in_8312(val);
    const int x = 8312;
    std::cout << 8312;
    for (int i = 0; i < 8312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8312(int& val) {
    const int x = 8312;
    std::cout << 8312;
    for (int i = 0; i < 8312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8313(int& val) {
    func_in_8313(val);
    const int x = 8313;
    std::cout << 8313;
    for (int i = 0; i < 8313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8313(int& val) {
    const int x = 8313;
    std::cout << 8313;
    for (int i = 0; i < 8313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8314(int& val) {
    func_in_8314(val);
    const int x = 8314;
    std::cout << 8314;
    for (int i = 0; i < 8314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8314(int& val) {
    const int x = 8314;
    std::cout << 8314;
    for (int i = 0; i < 8314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8315(int& val) {
    func_in_8315(val);
    const int x = 8315;
    std::cout << 8315;
    for (int i = 0; i < 8315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8315(int& val) {
    const int x = 8315;
    std::cout << 8315;
    for (int i = 0; i < 8315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8316(int& val) {
    func_in_8316(val);
    const int x = 8316;
    std::cout << 8316;
    for (int i = 0; i < 8316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8316(int& val) {
    const int x = 8316;
    std::cout << 8316;
    for (int i = 0; i < 8316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8317(int& val) {
    func_in_8317(val);
    const int x = 8317;
    std::cout << 8317;
    for (int i = 0; i < 8317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8317(int& val) {
    const int x = 8317;
    std::cout << 8317;
    for (int i = 0; i < 8317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8318(int& val) {
    func_in_8318(val);
    const int x = 8318;
    std::cout << 8318;
    for (int i = 0; i < 8318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8318(int& val) {
    const int x = 8318;
    std::cout << 8318;
    for (int i = 0; i < 8318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8319(int& val) {
    func_in_8319(val);
    const int x = 8319;
    std::cout << 8319;
    for (int i = 0; i < 8319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8319(int& val) {
    const int x = 8319;
    std::cout << 8319;
    for (int i = 0; i < 8319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8320(int& val) {
    func_in_8320(val);
    const int x = 8320;
    std::cout << 8320;
    for (int i = 0; i < 8320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8320(int& val) {
    const int x = 8320;
    std::cout << 8320;
    for (int i = 0; i < 8320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8321(int& val) {
    func_in_8321(val);
    const int x = 8321;
    std::cout << 8321;
    for (int i = 0; i < 8321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8321(int& val) {
    const int x = 8321;
    std::cout << 8321;
    for (int i = 0; i < 8321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8322(int& val) {
    func_in_8322(val);
    const int x = 8322;
    std::cout << 8322;
    for (int i = 0; i < 8322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8322(int& val) {
    const int x = 8322;
    std::cout << 8322;
    for (int i = 0; i < 8322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8323(int& val) {
    func_in_8323(val);
    const int x = 8323;
    std::cout << 8323;
    for (int i = 0; i < 8323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8323(int& val) {
    const int x = 8323;
    std::cout << 8323;
    for (int i = 0; i < 8323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8324(int& val) {
    func_in_8324(val);
    const int x = 8324;
    std::cout << 8324;
    for (int i = 0; i < 8324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8324(int& val) {
    const int x = 8324;
    std::cout << 8324;
    for (int i = 0; i < 8324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8325(int& val) {
    func_in_8325(val);
    const int x = 8325;
    std::cout << 8325;
    for (int i = 0; i < 8325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8325(int& val) {
    const int x = 8325;
    std::cout << 8325;
    for (int i = 0; i < 8325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8326(int& val) {
    func_in_8326(val);
    const int x = 8326;
    std::cout << 8326;
    for (int i = 0; i < 8326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8326(int& val) {
    const int x = 8326;
    std::cout << 8326;
    for (int i = 0; i < 8326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8327(int& val) {
    func_in_8327(val);
    const int x = 8327;
    std::cout << 8327;
    for (int i = 0; i < 8327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8327(int& val) {
    const int x = 8327;
    std::cout << 8327;
    for (int i = 0; i < 8327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8328(int& val) {
    func_in_8328(val);
    const int x = 8328;
    std::cout << 8328;
    for (int i = 0; i < 8328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8328(int& val) {
    const int x = 8328;
    std::cout << 8328;
    for (int i = 0; i < 8328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8329(int& val) {
    func_in_8329(val);
    const int x = 8329;
    std::cout << 8329;
    for (int i = 0; i < 8329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8329(int& val) {
    const int x = 8329;
    std::cout << 8329;
    for (int i = 0; i < 8329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8330(int& val) {
    func_in_8330(val);
    const int x = 8330;
    std::cout << 8330;
    for (int i = 0; i < 8330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8330(int& val) {
    const int x = 8330;
    std::cout << 8330;
    for (int i = 0; i < 8330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8331(int& val) {
    func_in_8331(val);
    const int x = 8331;
    std::cout << 8331;
    for (int i = 0; i < 8331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8331(int& val) {
    const int x = 8331;
    std::cout << 8331;
    for (int i = 0; i < 8331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8332(int& val) {
    func_in_8332(val);
    const int x = 8332;
    std::cout << 8332;
    for (int i = 0; i < 8332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8332(int& val) {
    const int x = 8332;
    std::cout << 8332;
    for (int i = 0; i < 8332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8333(int& val) {
    func_in_8333(val);
    const int x = 8333;
    std::cout << 8333;
    for (int i = 0; i < 8333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8333(int& val) {
    const int x = 8333;
    std::cout << 8333;
    for (int i = 0; i < 8333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8334(int& val) {
    func_in_8334(val);
    const int x = 8334;
    std::cout << 8334;
    for (int i = 0; i < 8334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8334(int& val) {
    const int x = 8334;
    std::cout << 8334;
    for (int i = 0; i < 8334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8335(int& val) {
    func_in_8335(val);
    const int x = 8335;
    std::cout << 8335;
    for (int i = 0; i < 8335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8335(int& val) {
    const int x = 8335;
    std::cout << 8335;
    for (int i = 0; i < 8335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8336(int& val) {
    func_in_8336(val);
    const int x = 8336;
    std::cout << 8336;
    for (int i = 0; i < 8336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8336(int& val) {
    const int x = 8336;
    std::cout << 8336;
    for (int i = 0; i < 8336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8337(int& val) {
    func_in_8337(val);
    const int x = 8337;
    std::cout << 8337;
    for (int i = 0; i < 8337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8337(int& val) {
    const int x = 8337;
    std::cout << 8337;
    for (int i = 0; i < 8337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8338(int& val) {
    func_in_8338(val);
    const int x = 8338;
    std::cout << 8338;
    for (int i = 0; i < 8338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8338(int& val) {
    const int x = 8338;
    std::cout << 8338;
    for (int i = 0; i < 8338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8339(int& val) {
    func_in_8339(val);
    const int x = 8339;
    std::cout << 8339;
    for (int i = 0; i < 8339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8339(int& val) {
    const int x = 8339;
    std::cout << 8339;
    for (int i = 0; i < 8339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8340(int& val) {
    func_in_8340(val);
    const int x = 8340;
    std::cout << 8340;
    for (int i = 0; i < 8340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8340(int& val) {
    const int x = 8340;
    std::cout << 8340;
    for (int i = 0; i < 8340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8341(int& val) {
    func_in_8341(val);
    const int x = 8341;
    std::cout << 8341;
    for (int i = 0; i < 8341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8341(int& val) {
    const int x = 8341;
    std::cout << 8341;
    for (int i = 0; i < 8341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8342(int& val) {
    func_in_8342(val);
    const int x = 8342;
    std::cout << 8342;
    for (int i = 0; i < 8342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8342(int& val) {
    const int x = 8342;
    std::cout << 8342;
    for (int i = 0; i < 8342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8343(int& val) {
    func_in_8343(val);
    const int x = 8343;
    std::cout << 8343;
    for (int i = 0; i < 8343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8343(int& val) {
    const int x = 8343;
    std::cout << 8343;
    for (int i = 0; i < 8343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8344(int& val) {
    func_in_8344(val);
    const int x = 8344;
    std::cout << 8344;
    for (int i = 0; i < 8344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8344(int& val) {
    const int x = 8344;
    std::cout << 8344;
    for (int i = 0; i < 8344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8345(int& val) {
    func_in_8345(val);
    const int x = 8345;
    std::cout << 8345;
    for (int i = 0; i < 8345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8345(int& val) {
    const int x = 8345;
    std::cout << 8345;
    for (int i = 0; i < 8345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8346(int& val) {
    func_in_8346(val);
    const int x = 8346;
    std::cout << 8346;
    for (int i = 0; i < 8346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8346(int& val) {
    const int x = 8346;
    std::cout << 8346;
    for (int i = 0; i < 8346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8347(int& val) {
    func_in_8347(val);
    const int x = 8347;
    std::cout << 8347;
    for (int i = 0; i < 8347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8347(int& val) {
    const int x = 8347;
    std::cout << 8347;
    for (int i = 0; i < 8347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8348(int& val) {
    func_in_8348(val);
    const int x = 8348;
    std::cout << 8348;
    for (int i = 0; i < 8348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8348(int& val) {
    const int x = 8348;
    std::cout << 8348;
    for (int i = 0; i < 8348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8349(int& val) {
    func_in_8349(val);
    const int x = 8349;
    std::cout << 8349;
    for (int i = 0; i < 8349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8349(int& val) {
    const int x = 8349;
    std::cout << 8349;
    for (int i = 0; i < 8349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8350(int& val) {
    func_in_8350(val);
    const int x = 8350;
    std::cout << 8350;
    for (int i = 0; i < 8350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8350(int& val) {
    const int x = 8350;
    std::cout << 8350;
    for (int i = 0; i < 8350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8351(int& val) {
    func_in_8351(val);
    const int x = 8351;
    std::cout << 8351;
    for (int i = 0; i < 8351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8351(int& val) {
    const int x = 8351;
    std::cout << 8351;
    for (int i = 0; i < 8351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8352(int& val) {
    func_in_8352(val);
    const int x = 8352;
    std::cout << 8352;
    for (int i = 0; i < 8352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8352(int& val) {
    const int x = 8352;
    std::cout << 8352;
    for (int i = 0; i < 8352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8353(int& val) {
    func_in_8353(val);
    const int x = 8353;
    std::cout << 8353;
    for (int i = 0; i < 8353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8353(int& val) {
    const int x = 8353;
    std::cout << 8353;
    for (int i = 0; i < 8353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8354(int& val) {
    func_in_8354(val);
    const int x = 8354;
    std::cout << 8354;
    for (int i = 0; i < 8354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8354(int& val) {
    const int x = 8354;
    std::cout << 8354;
    for (int i = 0; i < 8354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8355(int& val) {
    func_in_8355(val);
    const int x = 8355;
    std::cout << 8355;
    for (int i = 0; i < 8355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8355(int& val) {
    const int x = 8355;
    std::cout << 8355;
    for (int i = 0; i < 8355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8356(int& val) {
    func_in_8356(val);
    const int x = 8356;
    std::cout << 8356;
    for (int i = 0; i < 8356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8356(int& val) {
    const int x = 8356;
    std::cout << 8356;
    for (int i = 0; i < 8356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8357(int& val) {
    func_in_8357(val);
    const int x = 8357;
    std::cout << 8357;
    for (int i = 0; i < 8357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8357(int& val) {
    const int x = 8357;
    std::cout << 8357;
    for (int i = 0; i < 8357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8358(int& val) {
    func_in_8358(val);
    const int x = 8358;
    std::cout << 8358;
    for (int i = 0; i < 8358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8358(int& val) {
    const int x = 8358;
    std::cout << 8358;
    for (int i = 0; i < 8358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8359(int& val) {
    func_in_8359(val);
    const int x = 8359;
    std::cout << 8359;
    for (int i = 0; i < 8359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8359(int& val) {
    const int x = 8359;
    std::cout << 8359;
    for (int i = 0; i < 8359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8360(int& val) {
    func_in_8360(val);
    const int x = 8360;
    std::cout << 8360;
    for (int i = 0; i < 8360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8360(int& val) {
    const int x = 8360;
    std::cout << 8360;
    for (int i = 0; i < 8360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8361(int& val) {
    func_in_8361(val);
    const int x = 8361;
    std::cout << 8361;
    for (int i = 0; i < 8361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8361(int& val) {
    const int x = 8361;
    std::cout << 8361;
    for (int i = 0; i < 8361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8362(int& val) {
    func_in_8362(val);
    const int x = 8362;
    std::cout << 8362;
    for (int i = 0; i < 8362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8362(int& val) {
    const int x = 8362;
    std::cout << 8362;
    for (int i = 0; i < 8362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8363(int& val) {
    func_in_8363(val);
    const int x = 8363;
    std::cout << 8363;
    for (int i = 0; i < 8363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8363(int& val) {
    const int x = 8363;
    std::cout << 8363;
    for (int i = 0; i < 8363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8364(int& val) {
    func_in_8364(val);
    const int x = 8364;
    std::cout << 8364;
    for (int i = 0; i < 8364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8364(int& val) {
    const int x = 8364;
    std::cout << 8364;
    for (int i = 0; i < 8364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8365(int& val) {
    func_in_8365(val);
    const int x = 8365;
    std::cout << 8365;
    for (int i = 0; i < 8365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8365(int& val) {
    const int x = 8365;
    std::cout << 8365;
    for (int i = 0; i < 8365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8366(int& val) {
    func_in_8366(val);
    const int x = 8366;
    std::cout << 8366;
    for (int i = 0; i < 8366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8366(int& val) {
    const int x = 8366;
    std::cout << 8366;
    for (int i = 0; i < 8366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8367(int& val) {
    func_in_8367(val);
    const int x = 8367;
    std::cout << 8367;
    for (int i = 0; i < 8367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8367(int& val) {
    const int x = 8367;
    std::cout << 8367;
    for (int i = 0; i < 8367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8368(int& val) {
    func_in_8368(val);
    const int x = 8368;
    std::cout << 8368;
    for (int i = 0; i < 8368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8368(int& val) {
    const int x = 8368;
    std::cout << 8368;
    for (int i = 0; i < 8368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8369(int& val) {
    func_in_8369(val);
    const int x = 8369;
    std::cout << 8369;
    for (int i = 0; i < 8369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8369(int& val) {
    const int x = 8369;
    std::cout << 8369;
    for (int i = 0; i < 8369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8370(int& val) {
    func_in_8370(val);
    const int x = 8370;
    std::cout << 8370;
    for (int i = 0; i < 8370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8370(int& val) {
    const int x = 8370;
    std::cout << 8370;
    for (int i = 0; i < 8370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8371(int& val) {
    func_in_8371(val);
    const int x = 8371;
    std::cout << 8371;
    for (int i = 0; i < 8371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8371(int& val) {
    const int x = 8371;
    std::cout << 8371;
    for (int i = 0; i < 8371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8372(int& val) {
    func_in_8372(val);
    const int x = 8372;
    std::cout << 8372;
    for (int i = 0; i < 8372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8372(int& val) {
    const int x = 8372;
    std::cout << 8372;
    for (int i = 0; i < 8372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8373(int& val) {
    func_in_8373(val);
    const int x = 8373;
    std::cout << 8373;
    for (int i = 0; i < 8373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8373(int& val) {
    const int x = 8373;
    std::cout << 8373;
    for (int i = 0; i < 8373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8374(int& val) {
    func_in_8374(val);
    const int x = 8374;
    std::cout << 8374;
    for (int i = 0; i < 8374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8374(int& val) {
    const int x = 8374;
    std::cout << 8374;
    for (int i = 0; i < 8374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8375(int& val) {
    func_in_8375(val);
    const int x = 8375;
    std::cout << 8375;
    for (int i = 0; i < 8375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8375(int& val) {
    const int x = 8375;
    std::cout << 8375;
    for (int i = 0; i < 8375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8376(int& val) {
    func_in_8376(val);
    const int x = 8376;
    std::cout << 8376;
    for (int i = 0; i < 8376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8376(int& val) {
    const int x = 8376;
    std::cout << 8376;
    for (int i = 0; i < 8376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8377(int& val) {
    func_in_8377(val);
    const int x = 8377;
    std::cout << 8377;
    for (int i = 0; i < 8377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8377(int& val) {
    const int x = 8377;
    std::cout << 8377;
    for (int i = 0; i < 8377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8378(int& val) {
    func_in_8378(val);
    const int x = 8378;
    std::cout << 8378;
    for (int i = 0; i < 8378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8378(int& val) {
    const int x = 8378;
    std::cout << 8378;
    for (int i = 0; i < 8378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8379(int& val) {
    func_in_8379(val);
    const int x = 8379;
    std::cout << 8379;
    for (int i = 0; i < 8379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8379(int& val) {
    const int x = 8379;
    std::cout << 8379;
    for (int i = 0; i < 8379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8380(int& val) {
    func_in_8380(val);
    const int x = 8380;
    std::cout << 8380;
    for (int i = 0; i < 8380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8380(int& val) {
    const int x = 8380;
    std::cout << 8380;
    for (int i = 0; i < 8380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8381(int& val) {
    func_in_8381(val);
    const int x = 8381;
    std::cout << 8381;
    for (int i = 0; i < 8381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8381(int& val) {
    const int x = 8381;
    std::cout << 8381;
    for (int i = 0; i < 8381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8382(int& val) {
    func_in_8382(val);
    const int x = 8382;
    std::cout << 8382;
    for (int i = 0; i < 8382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8382(int& val) {
    const int x = 8382;
    std::cout << 8382;
    for (int i = 0; i < 8382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8383(int& val) {
    func_in_8383(val);
    const int x = 8383;
    std::cout << 8383;
    for (int i = 0; i < 8383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8383(int& val) {
    const int x = 8383;
    std::cout << 8383;
    for (int i = 0; i < 8383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8384(int& val) {
    func_in_8384(val);
    const int x = 8384;
    std::cout << 8384;
    for (int i = 0; i < 8384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8384(int& val) {
    const int x = 8384;
    std::cout << 8384;
    for (int i = 0; i < 8384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8385(int& val) {
    func_in_8385(val);
    const int x = 8385;
    std::cout << 8385;
    for (int i = 0; i < 8385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8385(int& val) {
    const int x = 8385;
    std::cout << 8385;
    for (int i = 0; i < 8385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8386(int& val) {
    func_in_8386(val);
    const int x = 8386;
    std::cout << 8386;
    for (int i = 0; i < 8386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8386(int& val) {
    const int x = 8386;
    std::cout << 8386;
    for (int i = 0; i < 8386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8387(int& val) {
    func_in_8387(val);
    const int x = 8387;
    std::cout << 8387;
    for (int i = 0; i < 8387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8387(int& val) {
    const int x = 8387;
    std::cout << 8387;
    for (int i = 0; i < 8387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8388(int& val) {
    func_in_8388(val);
    const int x = 8388;
    std::cout << 8388;
    for (int i = 0; i < 8388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8388(int& val) {
    const int x = 8388;
    std::cout << 8388;
    for (int i = 0; i < 8388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8389(int& val) {
    func_in_8389(val);
    const int x = 8389;
    std::cout << 8389;
    for (int i = 0; i < 8389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8389(int& val) {
    const int x = 8389;
    std::cout << 8389;
    for (int i = 0; i < 8389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8390(int& val) {
    func_in_8390(val);
    const int x = 8390;
    std::cout << 8390;
    for (int i = 0; i < 8390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8390(int& val) {
    const int x = 8390;
    std::cout << 8390;
    for (int i = 0; i < 8390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8391(int& val) {
    func_in_8391(val);
    const int x = 8391;
    std::cout << 8391;
    for (int i = 0; i < 8391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8391(int& val) {
    const int x = 8391;
    std::cout << 8391;
    for (int i = 0; i < 8391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8392(int& val) {
    func_in_8392(val);
    const int x = 8392;
    std::cout << 8392;
    for (int i = 0; i < 8392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8392(int& val) {
    const int x = 8392;
    std::cout << 8392;
    for (int i = 0; i < 8392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8393(int& val) {
    func_in_8393(val);
    const int x = 8393;
    std::cout << 8393;
    for (int i = 0; i < 8393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8393(int& val) {
    const int x = 8393;
    std::cout << 8393;
    for (int i = 0; i < 8393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8394(int& val) {
    func_in_8394(val);
    const int x = 8394;
    std::cout << 8394;
    for (int i = 0; i < 8394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8394(int& val) {
    const int x = 8394;
    std::cout << 8394;
    for (int i = 0; i < 8394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8395(int& val) {
    func_in_8395(val);
    const int x = 8395;
    std::cout << 8395;
    for (int i = 0; i < 8395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8395(int& val) {
    const int x = 8395;
    std::cout << 8395;
    for (int i = 0; i < 8395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8396(int& val) {
    func_in_8396(val);
    const int x = 8396;
    std::cout << 8396;
    for (int i = 0; i < 8396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8396(int& val) {
    const int x = 8396;
    std::cout << 8396;
    for (int i = 0; i < 8396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8397(int& val) {
    func_in_8397(val);
    const int x = 8397;
    std::cout << 8397;
    for (int i = 0; i < 8397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8397(int& val) {
    const int x = 8397;
    std::cout << 8397;
    for (int i = 0; i < 8397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8398(int& val) {
    func_in_8398(val);
    const int x = 8398;
    std::cout << 8398;
    for (int i = 0; i < 8398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8398(int& val) {
    const int x = 8398;
    std::cout << 8398;
    for (int i = 0; i < 8398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8399(int& val) {
    func_in_8399(val);
    const int x = 8399;
    std::cout << 8399;
    for (int i = 0; i < 8399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8399(int& val) {
    const int x = 8399;
    std::cout << 8399;
    for (int i = 0; i < 8399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8400(int& val) {
    func_in_8400(val);
    const int x = 8400;
    std::cout << 8400;
    for (int i = 0; i < 8400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8400(int& val) {
    const int x = 8400;
    std::cout << 8400;
    for (int i = 0; i < 8400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8401(int& val) {
    func_in_8401(val);
    const int x = 8401;
    std::cout << 8401;
    for (int i = 0; i < 8401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8401(int& val) {
    const int x = 8401;
    std::cout << 8401;
    for (int i = 0; i < 8401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8402(int& val) {
    func_in_8402(val);
    const int x = 8402;
    std::cout << 8402;
    for (int i = 0; i < 8402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8402(int& val) {
    const int x = 8402;
    std::cout << 8402;
    for (int i = 0; i < 8402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8403(int& val) {
    func_in_8403(val);
    const int x = 8403;
    std::cout << 8403;
    for (int i = 0; i < 8403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8403(int& val) {
    const int x = 8403;
    std::cout << 8403;
    for (int i = 0; i < 8403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8404(int& val) {
    func_in_8404(val);
    const int x = 8404;
    std::cout << 8404;
    for (int i = 0; i < 8404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8404(int& val) {
    const int x = 8404;
    std::cout << 8404;
    for (int i = 0; i < 8404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8405(int& val) {
    func_in_8405(val);
    const int x = 8405;
    std::cout << 8405;
    for (int i = 0; i < 8405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8405(int& val) {
    const int x = 8405;
    std::cout << 8405;
    for (int i = 0; i < 8405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8406(int& val) {
    func_in_8406(val);
    const int x = 8406;
    std::cout << 8406;
    for (int i = 0; i < 8406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8406(int& val) {
    const int x = 8406;
    std::cout << 8406;
    for (int i = 0; i < 8406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8407(int& val) {
    func_in_8407(val);
    const int x = 8407;
    std::cout << 8407;
    for (int i = 0; i < 8407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8407(int& val) {
    const int x = 8407;
    std::cout << 8407;
    for (int i = 0; i < 8407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8408(int& val) {
    func_in_8408(val);
    const int x = 8408;
    std::cout << 8408;
    for (int i = 0; i < 8408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8408(int& val) {
    const int x = 8408;
    std::cout << 8408;
    for (int i = 0; i < 8408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8409(int& val) {
    func_in_8409(val);
    const int x = 8409;
    std::cout << 8409;
    for (int i = 0; i < 8409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8409(int& val) {
    const int x = 8409;
    std::cout << 8409;
    for (int i = 0; i < 8409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8410(int& val) {
    func_in_8410(val);
    const int x = 8410;
    std::cout << 8410;
    for (int i = 0; i < 8410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8410(int& val) {
    const int x = 8410;
    std::cout << 8410;
    for (int i = 0; i < 8410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8411(int& val) {
    func_in_8411(val);
    const int x = 8411;
    std::cout << 8411;
    for (int i = 0; i < 8411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8411(int& val) {
    const int x = 8411;
    std::cout << 8411;
    for (int i = 0; i < 8411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8412(int& val) {
    func_in_8412(val);
    const int x = 8412;
    std::cout << 8412;
    for (int i = 0; i < 8412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8412(int& val) {
    const int x = 8412;
    std::cout << 8412;
    for (int i = 0; i < 8412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8413(int& val) {
    func_in_8413(val);
    const int x = 8413;
    std::cout << 8413;
    for (int i = 0; i < 8413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8413(int& val) {
    const int x = 8413;
    std::cout << 8413;
    for (int i = 0; i < 8413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8414(int& val) {
    func_in_8414(val);
    const int x = 8414;
    std::cout << 8414;
    for (int i = 0; i < 8414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8414(int& val) {
    const int x = 8414;
    std::cout << 8414;
    for (int i = 0; i < 8414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8415(int& val) {
    func_in_8415(val);
    const int x = 8415;
    std::cout << 8415;
    for (int i = 0; i < 8415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8415(int& val) {
    const int x = 8415;
    std::cout << 8415;
    for (int i = 0; i < 8415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8416(int& val) {
    func_in_8416(val);
    const int x = 8416;
    std::cout << 8416;
    for (int i = 0; i < 8416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8416(int& val) {
    const int x = 8416;
    std::cout << 8416;
    for (int i = 0; i < 8416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8417(int& val) {
    func_in_8417(val);
    const int x = 8417;
    std::cout << 8417;
    for (int i = 0; i < 8417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8417(int& val) {
    const int x = 8417;
    std::cout << 8417;
    for (int i = 0; i < 8417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8418(int& val) {
    func_in_8418(val);
    const int x = 8418;
    std::cout << 8418;
    for (int i = 0; i < 8418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8418(int& val) {
    const int x = 8418;
    std::cout << 8418;
    for (int i = 0; i < 8418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8419(int& val) {
    func_in_8419(val);
    const int x = 8419;
    std::cout << 8419;
    for (int i = 0; i < 8419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8419(int& val) {
    const int x = 8419;
    std::cout << 8419;
    for (int i = 0; i < 8419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8420(int& val) {
    func_in_8420(val);
    const int x = 8420;
    std::cout << 8420;
    for (int i = 0; i < 8420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8420(int& val) {
    const int x = 8420;
    std::cout << 8420;
    for (int i = 0; i < 8420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8421(int& val) {
    func_in_8421(val);
    const int x = 8421;
    std::cout << 8421;
    for (int i = 0; i < 8421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8421(int& val) {
    const int x = 8421;
    std::cout << 8421;
    for (int i = 0; i < 8421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8422(int& val) {
    func_in_8422(val);
    const int x = 8422;
    std::cout << 8422;
    for (int i = 0; i < 8422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8422(int& val) {
    const int x = 8422;
    std::cout << 8422;
    for (int i = 0; i < 8422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8423(int& val) {
    func_in_8423(val);
    const int x = 8423;
    std::cout << 8423;
    for (int i = 0; i < 8423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8423(int& val) {
    const int x = 8423;
    std::cout << 8423;
    for (int i = 0; i < 8423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8424(int& val) {
    func_in_8424(val);
    const int x = 8424;
    std::cout << 8424;
    for (int i = 0; i < 8424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8424(int& val) {
    const int x = 8424;
    std::cout << 8424;
    for (int i = 0; i < 8424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8425(int& val) {
    func_in_8425(val);
    const int x = 8425;
    std::cout << 8425;
    for (int i = 0; i < 8425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8425(int& val) {
    const int x = 8425;
    std::cout << 8425;
    for (int i = 0; i < 8425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8426(int& val) {
    func_in_8426(val);
    const int x = 8426;
    std::cout << 8426;
    for (int i = 0; i < 8426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8426(int& val) {
    const int x = 8426;
    std::cout << 8426;
    for (int i = 0; i < 8426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8427(int& val) {
    func_in_8427(val);
    const int x = 8427;
    std::cout << 8427;
    for (int i = 0; i < 8427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8427(int& val) {
    const int x = 8427;
    std::cout << 8427;
    for (int i = 0; i < 8427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8428(int& val) {
    func_in_8428(val);
    const int x = 8428;
    std::cout << 8428;
    for (int i = 0; i < 8428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8428(int& val) {
    const int x = 8428;
    std::cout << 8428;
    for (int i = 0; i < 8428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8429(int& val) {
    func_in_8429(val);
    const int x = 8429;
    std::cout << 8429;
    for (int i = 0; i < 8429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8429(int& val) {
    const int x = 8429;
    std::cout << 8429;
    for (int i = 0; i < 8429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8430(int& val) {
    func_in_8430(val);
    const int x = 8430;
    std::cout << 8430;
    for (int i = 0; i < 8430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8430(int& val) {
    const int x = 8430;
    std::cout << 8430;
    for (int i = 0; i < 8430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8431(int& val) {
    func_in_8431(val);
    const int x = 8431;
    std::cout << 8431;
    for (int i = 0; i < 8431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8431(int& val) {
    const int x = 8431;
    std::cout << 8431;
    for (int i = 0; i < 8431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8432(int& val) {
    func_in_8432(val);
    const int x = 8432;
    std::cout << 8432;
    for (int i = 0; i < 8432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8432(int& val) {
    const int x = 8432;
    std::cout << 8432;
    for (int i = 0; i < 8432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8433(int& val) {
    func_in_8433(val);
    const int x = 8433;
    std::cout << 8433;
    for (int i = 0; i < 8433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8433(int& val) {
    const int x = 8433;
    std::cout << 8433;
    for (int i = 0; i < 8433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8434(int& val) {
    func_in_8434(val);
    const int x = 8434;
    std::cout << 8434;
    for (int i = 0; i < 8434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8434(int& val) {
    const int x = 8434;
    std::cout << 8434;
    for (int i = 0; i < 8434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8435(int& val) {
    func_in_8435(val);
    const int x = 8435;
    std::cout << 8435;
    for (int i = 0; i < 8435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8435(int& val) {
    const int x = 8435;
    std::cout << 8435;
    for (int i = 0; i < 8435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8436(int& val) {
    func_in_8436(val);
    const int x = 8436;
    std::cout << 8436;
    for (int i = 0; i < 8436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8436(int& val) {
    const int x = 8436;
    std::cout << 8436;
    for (int i = 0; i < 8436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8437(int& val) {
    func_in_8437(val);
    const int x = 8437;
    std::cout << 8437;
    for (int i = 0; i < 8437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8437(int& val) {
    const int x = 8437;
    std::cout << 8437;
    for (int i = 0; i < 8437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8438(int& val) {
    func_in_8438(val);
    const int x = 8438;
    std::cout << 8438;
    for (int i = 0; i < 8438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8438(int& val) {
    const int x = 8438;
    std::cout << 8438;
    for (int i = 0; i < 8438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8439(int& val) {
    func_in_8439(val);
    const int x = 8439;
    std::cout << 8439;
    for (int i = 0; i < 8439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8439(int& val) {
    const int x = 8439;
    std::cout << 8439;
    for (int i = 0; i < 8439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8440(int& val) {
    func_in_8440(val);
    const int x = 8440;
    std::cout << 8440;
    for (int i = 0; i < 8440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8440(int& val) {
    const int x = 8440;
    std::cout << 8440;
    for (int i = 0; i < 8440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8441(int& val) {
    func_in_8441(val);
    const int x = 8441;
    std::cout << 8441;
    for (int i = 0; i < 8441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8441(int& val) {
    const int x = 8441;
    std::cout << 8441;
    for (int i = 0; i < 8441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8442(int& val) {
    func_in_8442(val);
    const int x = 8442;
    std::cout << 8442;
    for (int i = 0; i < 8442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8442(int& val) {
    const int x = 8442;
    std::cout << 8442;
    for (int i = 0; i < 8442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8443(int& val) {
    func_in_8443(val);
    const int x = 8443;
    std::cout << 8443;
    for (int i = 0; i < 8443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8443(int& val) {
    const int x = 8443;
    std::cout << 8443;
    for (int i = 0; i < 8443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8444(int& val) {
    func_in_8444(val);
    const int x = 8444;
    std::cout << 8444;
    for (int i = 0; i < 8444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8444(int& val) {
    const int x = 8444;
    std::cout << 8444;
    for (int i = 0; i < 8444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8445(int& val) {
    func_in_8445(val);
    const int x = 8445;
    std::cout << 8445;
    for (int i = 0; i < 8445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8445(int& val) {
    const int x = 8445;
    std::cout << 8445;
    for (int i = 0; i < 8445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8446(int& val) {
    func_in_8446(val);
    const int x = 8446;
    std::cout << 8446;
    for (int i = 0; i < 8446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8446(int& val) {
    const int x = 8446;
    std::cout << 8446;
    for (int i = 0; i < 8446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8447(int& val) {
    func_in_8447(val);
    const int x = 8447;
    std::cout << 8447;
    for (int i = 0; i < 8447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8447(int& val) {
    const int x = 8447;
    std::cout << 8447;
    for (int i = 0; i < 8447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8448(int& val) {
    func_in_8448(val);
    const int x = 8448;
    std::cout << 8448;
    for (int i = 0; i < 8448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8448(int& val) {
    const int x = 8448;
    std::cout << 8448;
    for (int i = 0; i < 8448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8449(int& val) {
    func_in_8449(val);
    const int x = 8449;
    std::cout << 8449;
    for (int i = 0; i < 8449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8449(int& val) {
    const int x = 8449;
    std::cout << 8449;
    for (int i = 0; i < 8449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8450(int& val) {
    func_in_8450(val);
    const int x = 8450;
    std::cout << 8450;
    for (int i = 0; i < 8450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8450(int& val) {
    const int x = 8450;
    std::cout << 8450;
    for (int i = 0; i < 8450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8451(int& val) {
    func_in_8451(val);
    const int x = 8451;
    std::cout << 8451;
    for (int i = 0; i < 8451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8451(int& val) {
    const int x = 8451;
    std::cout << 8451;
    for (int i = 0; i < 8451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8452(int& val) {
    func_in_8452(val);
    const int x = 8452;
    std::cout << 8452;
    for (int i = 0; i < 8452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8452(int& val) {
    const int x = 8452;
    std::cout << 8452;
    for (int i = 0; i < 8452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8453(int& val) {
    func_in_8453(val);
    const int x = 8453;
    std::cout << 8453;
    for (int i = 0; i < 8453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8453(int& val) {
    const int x = 8453;
    std::cout << 8453;
    for (int i = 0; i < 8453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8454(int& val) {
    func_in_8454(val);
    const int x = 8454;
    std::cout << 8454;
    for (int i = 0; i < 8454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8454(int& val) {
    const int x = 8454;
    std::cout << 8454;
    for (int i = 0; i < 8454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8455(int& val) {
    func_in_8455(val);
    const int x = 8455;
    std::cout << 8455;
    for (int i = 0; i < 8455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8455(int& val) {
    const int x = 8455;
    std::cout << 8455;
    for (int i = 0; i < 8455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8456(int& val) {
    func_in_8456(val);
    const int x = 8456;
    std::cout << 8456;
    for (int i = 0; i < 8456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8456(int& val) {
    const int x = 8456;
    std::cout << 8456;
    for (int i = 0; i < 8456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8457(int& val) {
    func_in_8457(val);
    const int x = 8457;
    std::cout << 8457;
    for (int i = 0; i < 8457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8457(int& val) {
    const int x = 8457;
    std::cout << 8457;
    for (int i = 0; i < 8457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8458(int& val) {
    func_in_8458(val);
    const int x = 8458;
    std::cout << 8458;
    for (int i = 0; i < 8458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8458(int& val) {
    const int x = 8458;
    std::cout << 8458;
    for (int i = 0; i < 8458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8459(int& val) {
    func_in_8459(val);
    const int x = 8459;
    std::cout << 8459;
    for (int i = 0; i < 8459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8459(int& val) {
    const int x = 8459;
    std::cout << 8459;
    for (int i = 0; i < 8459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8460(int& val) {
    func_in_8460(val);
    const int x = 8460;
    std::cout << 8460;
    for (int i = 0; i < 8460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8460(int& val) {
    const int x = 8460;
    std::cout << 8460;
    for (int i = 0; i < 8460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8461(int& val) {
    func_in_8461(val);
    const int x = 8461;
    std::cout << 8461;
    for (int i = 0; i < 8461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8461(int& val) {
    const int x = 8461;
    std::cout << 8461;
    for (int i = 0; i < 8461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8462(int& val) {
    func_in_8462(val);
    const int x = 8462;
    std::cout << 8462;
    for (int i = 0; i < 8462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8462(int& val) {
    const int x = 8462;
    std::cout << 8462;
    for (int i = 0; i < 8462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8463(int& val) {
    func_in_8463(val);
    const int x = 8463;
    std::cout << 8463;
    for (int i = 0; i < 8463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8463(int& val) {
    const int x = 8463;
    std::cout << 8463;
    for (int i = 0; i < 8463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8464(int& val) {
    func_in_8464(val);
    const int x = 8464;
    std::cout << 8464;
    for (int i = 0; i < 8464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8464(int& val) {
    const int x = 8464;
    std::cout << 8464;
    for (int i = 0; i < 8464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8465(int& val) {
    func_in_8465(val);
    const int x = 8465;
    std::cout << 8465;
    for (int i = 0; i < 8465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8465(int& val) {
    const int x = 8465;
    std::cout << 8465;
    for (int i = 0; i < 8465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8466(int& val) {
    func_in_8466(val);
    const int x = 8466;
    std::cout << 8466;
    for (int i = 0; i < 8466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8466(int& val) {
    const int x = 8466;
    std::cout << 8466;
    for (int i = 0; i < 8466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8467(int& val) {
    func_in_8467(val);
    const int x = 8467;
    std::cout << 8467;
    for (int i = 0; i < 8467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8467(int& val) {
    const int x = 8467;
    std::cout << 8467;
    for (int i = 0; i < 8467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8468(int& val) {
    func_in_8468(val);
    const int x = 8468;
    std::cout << 8468;
    for (int i = 0; i < 8468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8468(int& val) {
    const int x = 8468;
    std::cout << 8468;
    for (int i = 0; i < 8468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8469(int& val) {
    func_in_8469(val);
    const int x = 8469;
    std::cout << 8469;
    for (int i = 0; i < 8469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8469(int& val) {
    const int x = 8469;
    std::cout << 8469;
    for (int i = 0; i < 8469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8470(int& val) {
    func_in_8470(val);
    const int x = 8470;
    std::cout << 8470;
    for (int i = 0; i < 8470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8470(int& val) {
    const int x = 8470;
    std::cout << 8470;
    for (int i = 0; i < 8470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8471(int& val) {
    func_in_8471(val);
    const int x = 8471;
    std::cout << 8471;
    for (int i = 0; i < 8471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8471(int& val) {
    const int x = 8471;
    std::cout << 8471;
    for (int i = 0; i < 8471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8472(int& val) {
    func_in_8472(val);
    const int x = 8472;
    std::cout << 8472;
    for (int i = 0; i < 8472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8472(int& val) {
    const int x = 8472;
    std::cout << 8472;
    for (int i = 0; i < 8472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8473(int& val) {
    func_in_8473(val);
    const int x = 8473;
    std::cout << 8473;
    for (int i = 0; i < 8473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8473(int& val) {
    const int x = 8473;
    std::cout << 8473;
    for (int i = 0; i < 8473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8474(int& val) {
    func_in_8474(val);
    const int x = 8474;
    std::cout << 8474;
    for (int i = 0; i < 8474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8474(int& val) {
    const int x = 8474;
    std::cout << 8474;
    for (int i = 0; i < 8474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8475(int& val) {
    func_in_8475(val);
    const int x = 8475;
    std::cout << 8475;
    for (int i = 0; i < 8475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8475(int& val) {
    const int x = 8475;
    std::cout << 8475;
    for (int i = 0; i < 8475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8476(int& val) {
    func_in_8476(val);
    const int x = 8476;
    std::cout << 8476;
    for (int i = 0; i < 8476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8476(int& val) {
    const int x = 8476;
    std::cout << 8476;
    for (int i = 0; i < 8476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8477(int& val) {
    func_in_8477(val);
    const int x = 8477;
    std::cout << 8477;
    for (int i = 0; i < 8477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8477(int& val) {
    const int x = 8477;
    std::cout << 8477;
    for (int i = 0; i < 8477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8478(int& val) {
    func_in_8478(val);
    const int x = 8478;
    std::cout << 8478;
    for (int i = 0; i < 8478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8478(int& val) {
    const int x = 8478;
    std::cout << 8478;
    for (int i = 0; i < 8478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8479(int& val) {
    func_in_8479(val);
    const int x = 8479;
    std::cout << 8479;
    for (int i = 0; i < 8479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8479(int& val) {
    const int x = 8479;
    std::cout << 8479;
    for (int i = 0; i < 8479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8480(int& val) {
    func_in_8480(val);
    const int x = 8480;
    std::cout << 8480;
    for (int i = 0; i < 8480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8480(int& val) {
    const int x = 8480;
    std::cout << 8480;
    for (int i = 0; i < 8480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8481(int& val) {
    func_in_8481(val);
    const int x = 8481;
    std::cout << 8481;
    for (int i = 0; i < 8481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8481(int& val) {
    const int x = 8481;
    std::cout << 8481;
    for (int i = 0; i < 8481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8482(int& val) {
    func_in_8482(val);
    const int x = 8482;
    std::cout << 8482;
    for (int i = 0; i < 8482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8482(int& val) {
    const int x = 8482;
    std::cout << 8482;
    for (int i = 0; i < 8482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8483(int& val) {
    func_in_8483(val);
    const int x = 8483;
    std::cout << 8483;
    for (int i = 0; i < 8483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8483(int& val) {
    const int x = 8483;
    std::cout << 8483;
    for (int i = 0; i < 8483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8484(int& val) {
    func_in_8484(val);
    const int x = 8484;
    std::cout << 8484;
    for (int i = 0; i < 8484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8484(int& val) {
    const int x = 8484;
    std::cout << 8484;
    for (int i = 0; i < 8484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8485(int& val) {
    func_in_8485(val);
    const int x = 8485;
    std::cout << 8485;
    for (int i = 0; i < 8485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8485(int& val) {
    const int x = 8485;
    std::cout << 8485;
    for (int i = 0; i < 8485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8486(int& val) {
    func_in_8486(val);
    const int x = 8486;
    std::cout << 8486;
    for (int i = 0; i < 8486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8486(int& val) {
    const int x = 8486;
    std::cout << 8486;
    for (int i = 0; i < 8486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8487(int& val) {
    func_in_8487(val);
    const int x = 8487;
    std::cout << 8487;
    for (int i = 0; i < 8487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8487(int& val) {
    const int x = 8487;
    std::cout << 8487;
    for (int i = 0; i < 8487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8488(int& val) {
    func_in_8488(val);
    const int x = 8488;
    std::cout << 8488;
    for (int i = 0; i < 8488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8488(int& val) {
    const int x = 8488;
    std::cout << 8488;
    for (int i = 0; i < 8488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8489(int& val) {
    func_in_8489(val);
    const int x = 8489;
    std::cout << 8489;
    for (int i = 0; i < 8489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8489(int& val) {
    const int x = 8489;
    std::cout << 8489;
    for (int i = 0; i < 8489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8490(int& val) {
    func_in_8490(val);
    const int x = 8490;
    std::cout << 8490;
    for (int i = 0; i < 8490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8490(int& val) {
    const int x = 8490;
    std::cout << 8490;
    for (int i = 0; i < 8490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8491(int& val) {
    func_in_8491(val);
    const int x = 8491;
    std::cout << 8491;
    for (int i = 0; i < 8491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8491(int& val) {
    const int x = 8491;
    std::cout << 8491;
    for (int i = 0; i < 8491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8492(int& val) {
    func_in_8492(val);
    const int x = 8492;
    std::cout << 8492;
    for (int i = 0; i < 8492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8492(int& val) {
    const int x = 8492;
    std::cout << 8492;
    for (int i = 0; i < 8492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8493(int& val) {
    func_in_8493(val);
    const int x = 8493;
    std::cout << 8493;
    for (int i = 0; i < 8493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8493(int& val) {
    const int x = 8493;
    std::cout << 8493;
    for (int i = 0; i < 8493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8494(int& val) {
    func_in_8494(val);
    const int x = 8494;
    std::cout << 8494;
    for (int i = 0; i < 8494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8494(int& val) {
    const int x = 8494;
    std::cout << 8494;
    for (int i = 0; i < 8494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8495(int& val) {
    func_in_8495(val);
    const int x = 8495;
    std::cout << 8495;
    for (int i = 0; i < 8495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8495(int& val) {
    const int x = 8495;
    std::cout << 8495;
    for (int i = 0; i < 8495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8496(int& val) {
    func_in_8496(val);
    const int x = 8496;
    std::cout << 8496;
    for (int i = 0; i < 8496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8496(int& val) {
    const int x = 8496;
    std::cout << 8496;
    for (int i = 0; i < 8496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8497(int& val) {
    func_in_8497(val);
    const int x = 8497;
    std::cout << 8497;
    for (int i = 0; i < 8497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8497(int& val) {
    const int x = 8497;
    std::cout << 8497;
    for (int i = 0; i < 8497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8498(int& val) {
    func_in_8498(val);
    const int x = 8498;
    std::cout << 8498;
    for (int i = 0; i < 8498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8498(int& val) {
    const int x = 8498;
    std::cout << 8498;
    for (int i = 0; i < 8498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8499(int& val) {
    func_in_8499(val);
    const int x = 8499;
    std::cout << 8499;
    for (int i = 0; i < 8499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8499(int& val) {
    const int x = 8499;
    std::cout << 8499;
    for (int i = 0; i < 8499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8500(int& val) {
    func_in_8500(val);
    const int x = 8500;
    std::cout << 8500;
    for (int i = 0; i < 8500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8500(int& val) {
    const int x = 8500;
    std::cout << 8500;
    for (int i = 0; i < 8500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8501(int& val) {
    func_in_8501(val);
    const int x = 8501;
    std::cout << 8501;
    for (int i = 0; i < 8501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8501(int& val) {
    const int x = 8501;
    std::cout << 8501;
    for (int i = 0; i < 8501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8502(int& val) {
    func_in_8502(val);
    const int x = 8502;
    std::cout << 8502;
    for (int i = 0; i < 8502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8502(int& val) {
    const int x = 8502;
    std::cout << 8502;
    for (int i = 0; i < 8502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8503(int& val) {
    func_in_8503(val);
    const int x = 8503;
    std::cout << 8503;
    for (int i = 0; i < 8503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8503(int& val) {
    const int x = 8503;
    std::cout << 8503;
    for (int i = 0; i < 8503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8504(int& val) {
    func_in_8504(val);
    const int x = 8504;
    std::cout << 8504;
    for (int i = 0; i < 8504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8504(int& val) {
    const int x = 8504;
    std::cout << 8504;
    for (int i = 0; i < 8504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8505(int& val) {
    func_in_8505(val);
    const int x = 8505;
    std::cout << 8505;
    for (int i = 0; i < 8505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8505(int& val) {
    const int x = 8505;
    std::cout << 8505;
    for (int i = 0; i < 8505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8506(int& val) {
    func_in_8506(val);
    const int x = 8506;
    std::cout << 8506;
    for (int i = 0; i < 8506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8506(int& val) {
    const int x = 8506;
    std::cout << 8506;
    for (int i = 0; i < 8506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8507(int& val) {
    func_in_8507(val);
    const int x = 8507;
    std::cout << 8507;
    for (int i = 0; i < 8507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8507(int& val) {
    const int x = 8507;
    std::cout << 8507;
    for (int i = 0; i < 8507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8508(int& val) {
    func_in_8508(val);
    const int x = 8508;
    std::cout << 8508;
    for (int i = 0; i < 8508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8508(int& val) {
    const int x = 8508;
    std::cout << 8508;
    for (int i = 0; i < 8508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8509(int& val) {
    func_in_8509(val);
    const int x = 8509;
    std::cout << 8509;
    for (int i = 0; i < 8509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8509(int& val) {
    const int x = 8509;
    std::cout << 8509;
    for (int i = 0; i < 8509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8510(int& val) {
    func_in_8510(val);
    const int x = 8510;
    std::cout << 8510;
    for (int i = 0; i < 8510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8510(int& val) {
    const int x = 8510;
    std::cout << 8510;
    for (int i = 0; i < 8510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8511(int& val) {
    func_in_8511(val);
    const int x = 8511;
    std::cout << 8511;
    for (int i = 0; i < 8511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8511(int& val) {
    const int x = 8511;
    std::cout << 8511;
    for (int i = 0; i < 8511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8512(int& val) {
    func_in_8512(val);
    const int x = 8512;
    std::cout << 8512;
    for (int i = 0; i < 8512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8512(int& val) {
    const int x = 8512;
    std::cout << 8512;
    for (int i = 0; i < 8512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8513(int& val) {
    func_in_8513(val);
    const int x = 8513;
    std::cout << 8513;
    for (int i = 0; i < 8513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8513(int& val) {
    const int x = 8513;
    std::cout << 8513;
    for (int i = 0; i < 8513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8514(int& val) {
    func_in_8514(val);
    const int x = 8514;
    std::cout << 8514;
    for (int i = 0; i < 8514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8514(int& val) {
    const int x = 8514;
    std::cout << 8514;
    for (int i = 0; i < 8514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8515(int& val) {
    func_in_8515(val);
    const int x = 8515;
    std::cout << 8515;
    for (int i = 0; i < 8515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8515(int& val) {
    const int x = 8515;
    std::cout << 8515;
    for (int i = 0; i < 8515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8516(int& val) {
    func_in_8516(val);
    const int x = 8516;
    std::cout << 8516;
    for (int i = 0; i < 8516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8516(int& val) {
    const int x = 8516;
    std::cout << 8516;
    for (int i = 0; i < 8516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8517(int& val) {
    func_in_8517(val);
    const int x = 8517;
    std::cout << 8517;
    for (int i = 0; i < 8517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8517(int& val) {
    const int x = 8517;
    std::cout << 8517;
    for (int i = 0; i < 8517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8518(int& val) {
    func_in_8518(val);
    const int x = 8518;
    std::cout << 8518;
    for (int i = 0; i < 8518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8518(int& val) {
    const int x = 8518;
    std::cout << 8518;
    for (int i = 0; i < 8518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8519(int& val) {
    func_in_8519(val);
    const int x = 8519;
    std::cout << 8519;
    for (int i = 0; i < 8519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8519(int& val) {
    const int x = 8519;
    std::cout << 8519;
    for (int i = 0; i < 8519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8520(int& val) {
    func_in_8520(val);
    const int x = 8520;
    std::cout << 8520;
    for (int i = 0; i < 8520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8520(int& val) {
    const int x = 8520;
    std::cout << 8520;
    for (int i = 0; i < 8520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8521(int& val) {
    func_in_8521(val);
    const int x = 8521;
    std::cout << 8521;
    for (int i = 0; i < 8521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8521(int& val) {
    const int x = 8521;
    std::cout << 8521;
    for (int i = 0; i < 8521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8522(int& val) {
    func_in_8522(val);
    const int x = 8522;
    std::cout << 8522;
    for (int i = 0; i < 8522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8522(int& val) {
    const int x = 8522;
    std::cout << 8522;
    for (int i = 0; i < 8522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8523(int& val) {
    func_in_8523(val);
    const int x = 8523;
    std::cout << 8523;
    for (int i = 0; i < 8523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8523(int& val) {
    const int x = 8523;
    std::cout << 8523;
    for (int i = 0; i < 8523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8524(int& val) {
    func_in_8524(val);
    const int x = 8524;
    std::cout << 8524;
    for (int i = 0; i < 8524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8524(int& val) {
    const int x = 8524;
    std::cout << 8524;
    for (int i = 0; i < 8524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8525(int& val) {
    func_in_8525(val);
    const int x = 8525;
    std::cout << 8525;
    for (int i = 0; i < 8525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8525(int& val) {
    const int x = 8525;
    std::cout << 8525;
    for (int i = 0; i < 8525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8526(int& val) {
    func_in_8526(val);
    const int x = 8526;
    std::cout << 8526;
    for (int i = 0; i < 8526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8526(int& val) {
    const int x = 8526;
    std::cout << 8526;
    for (int i = 0; i < 8526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8527(int& val) {
    func_in_8527(val);
    const int x = 8527;
    std::cout << 8527;
    for (int i = 0; i < 8527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8527(int& val) {
    const int x = 8527;
    std::cout << 8527;
    for (int i = 0; i < 8527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8528(int& val) {
    func_in_8528(val);
    const int x = 8528;
    std::cout << 8528;
    for (int i = 0; i < 8528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8528(int& val) {
    const int x = 8528;
    std::cout << 8528;
    for (int i = 0; i < 8528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8529(int& val) {
    func_in_8529(val);
    const int x = 8529;
    std::cout << 8529;
    for (int i = 0; i < 8529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8529(int& val) {
    const int x = 8529;
    std::cout << 8529;
    for (int i = 0; i < 8529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8530(int& val) {
    func_in_8530(val);
    const int x = 8530;
    std::cout << 8530;
    for (int i = 0; i < 8530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8530(int& val) {
    const int x = 8530;
    std::cout << 8530;
    for (int i = 0; i < 8530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8531(int& val) {
    func_in_8531(val);
    const int x = 8531;
    std::cout << 8531;
    for (int i = 0; i < 8531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8531(int& val) {
    const int x = 8531;
    std::cout << 8531;
    for (int i = 0; i < 8531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8532(int& val) {
    func_in_8532(val);
    const int x = 8532;
    std::cout << 8532;
    for (int i = 0; i < 8532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8532(int& val) {
    const int x = 8532;
    std::cout << 8532;
    for (int i = 0; i < 8532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8533(int& val) {
    func_in_8533(val);
    const int x = 8533;
    std::cout << 8533;
    for (int i = 0; i < 8533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8533(int& val) {
    const int x = 8533;
    std::cout << 8533;
    for (int i = 0; i < 8533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8534(int& val) {
    func_in_8534(val);
    const int x = 8534;
    std::cout << 8534;
    for (int i = 0; i < 8534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8534(int& val) {
    const int x = 8534;
    std::cout << 8534;
    for (int i = 0; i < 8534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8535(int& val) {
    func_in_8535(val);
    const int x = 8535;
    std::cout << 8535;
    for (int i = 0; i < 8535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8535(int& val) {
    const int x = 8535;
    std::cout << 8535;
    for (int i = 0; i < 8535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8536(int& val) {
    func_in_8536(val);
    const int x = 8536;
    std::cout << 8536;
    for (int i = 0; i < 8536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8536(int& val) {
    const int x = 8536;
    std::cout << 8536;
    for (int i = 0; i < 8536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8537(int& val) {
    func_in_8537(val);
    const int x = 8537;
    std::cout << 8537;
    for (int i = 0; i < 8537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8537(int& val) {
    const int x = 8537;
    std::cout << 8537;
    for (int i = 0; i < 8537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8538(int& val) {
    func_in_8538(val);
    const int x = 8538;
    std::cout << 8538;
    for (int i = 0; i < 8538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8538(int& val) {
    const int x = 8538;
    std::cout << 8538;
    for (int i = 0; i < 8538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8539(int& val) {
    func_in_8539(val);
    const int x = 8539;
    std::cout << 8539;
    for (int i = 0; i < 8539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8539(int& val) {
    const int x = 8539;
    std::cout << 8539;
    for (int i = 0; i < 8539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8540(int& val) {
    func_in_8540(val);
    const int x = 8540;
    std::cout << 8540;
    for (int i = 0; i < 8540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8540(int& val) {
    const int x = 8540;
    std::cout << 8540;
    for (int i = 0; i < 8540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8541(int& val) {
    func_in_8541(val);
    const int x = 8541;
    std::cout << 8541;
    for (int i = 0; i < 8541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8541(int& val) {
    const int x = 8541;
    std::cout << 8541;
    for (int i = 0; i < 8541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8542(int& val) {
    func_in_8542(val);
    const int x = 8542;
    std::cout << 8542;
    for (int i = 0; i < 8542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8542(int& val) {
    const int x = 8542;
    std::cout << 8542;
    for (int i = 0; i < 8542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8543(int& val) {
    func_in_8543(val);
    const int x = 8543;
    std::cout << 8543;
    for (int i = 0; i < 8543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8543(int& val) {
    const int x = 8543;
    std::cout << 8543;
    for (int i = 0; i < 8543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8544(int& val) {
    func_in_8544(val);
    const int x = 8544;
    std::cout << 8544;
    for (int i = 0; i < 8544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8544(int& val) {
    const int x = 8544;
    std::cout << 8544;
    for (int i = 0; i < 8544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8545(int& val) {
    func_in_8545(val);
    const int x = 8545;
    std::cout << 8545;
    for (int i = 0; i < 8545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8545(int& val) {
    const int x = 8545;
    std::cout << 8545;
    for (int i = 0; i < 8545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8546(int& val) {
    func_in_8546(val);
    const int x = 8546;
    std::cout << 8546;
    for (int i = 0; i < 8546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8546(int& val) {
    const int x = 8546;
    std::cout << 8546;
    for (int i = 0; i < 8546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8547(int& val) {
    func_in_8547(val);
    const int x = 8547;
    std::cout << 8547;
    for (int i = 0; i < 8547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8547(int& val) {
    const int x = 8547;
    std::cout << 8547;
    for (int i = 0; i < 8547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8548(int& val) {
    func_in_8548(val);
    const int x = 8548;
    std::cout << 8548;
    for (int i = 0; i < 8548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8548(int& val) {
    const int x = 8548;
    std::cout << 8548;
    for (int i = 0; i < 8548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8549(int& val) {
    func_in_8549(val);
    const int x = 8549;
    std::cout << 8549;
    for (int i = 0; i < 8549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8549(int& val) {
    const int x = 8549;
    std::cout << 8549;
    for (int i = 0; i < 8549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8550(int& val) {
    func_in_8550(val);
    const int x = 8550;
    std::cout << 8550;
    for (int i = 0; i < 8550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8550(int& val) {
    const int x = 8550;
    std::cout << 8550;
    for (int i = 0; i < 8550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8551(int& val) {
    func_in_8551(val);
    const int x = 8551;
    std::cout << 8551;
    for (int i = 0; i < 8551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8551(int& val) {
    const int x = 8551;
    std::cout << 8551;
    for (int i = 0; i < 8551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8552(int& val) {
    func_in_8552(val);
    const int x = 8552;
    std::cout << 8552;
    for (int i = 0; i < 8552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8552(int& val) {
    const int x = 8552;
    std::cout << 8552;
    for (int i = 0; i < 8552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8553(int& val) {
    func_in_8553(val);
    const int x = 8553;
    std::cout << 8553;
    for (int i = 0; i < 8553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8553(int& val) {
    const int x = 8553;
    std::cout << 8553;
    for (int i = 0; i < 8553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8554(int& val) {
    func_in_8554(val);
    const int x = 8554;
    std::cout << 8554;
    for (int i = 0; i < 8554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8554(int& val) {
    const int x = 8554;
    std::cout << 8554;
    for (int i = 0; i < 8554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8555(int& val) {
    func_in_8555(val);
    const int x = 8555;
    std::cout << 8555;
    for (int i = 0; i < 8555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8555(int& val) {
    const int x = 8555;
    std::cout << 8555;
    for (int i = 0; i < 8555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8556(int& val) {
    func_in_8556(val);
    const int x = 8556;
    std::cout << 8556;
    for (int i = 0; i < 8556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8556(int& val) {
    const int x = 8556;
    std::cout << 8556;
    for (int i = 0; i < 8556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8557(int& val) {
    func_in_8557(val);
    const int x = 8557;
    std::cout << 8557;
    for (int i = 0; i < 8557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8557(int& val) {
    const int x = 8557;
    std::cout << 8557;
    for (int i = 0; i < 8557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8558(int& val) {
    func_in_8558(val);
    const int x = 8558;
    std::cout << 8558;
    for (int i = 0; i < 8558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8558(int& val) {
    const int x = 8558;
    std::cout << 8558;
    for (int i = 0; i < 8558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8559(int& val) {
    func_in_8559(val);
    const int x = 8559;
    std::cout << 8559;
    for (int i = 0; i < 8559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8559(int& val) {
    const int x = 8559;
    std::cout << 8559;
    for (int i = 0; i < 8559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8560(int& val) {
    func_in_8560(val);
    const int x = 8560;
    std::cout << 8560;
    for (int i = 0; i < 8560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8560(int& val) {
    const int x = 8560;
    std::cout << 8560;
    for (int i = 0; i < 8560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8561(int& val) {
    func_in_8561(val);
    const int x = 8561;
    std::cout << 8561;
    for (int i = 0; i < 8561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8561(int& val) {
    const int x = 8561;
    std::cout << 8561;
    for (int i = 0; i < 8561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8562(int& val) {
    func_in_8562(val);
    const int x = 8562;
    std::cout << 8562;
    for (int i = 0; i < 8562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8562(int& val) {
    const int x = 8562;
    std::cout << 8562;
    for (int i = 0; i < 8562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8563(int& val) {
    func_in_8563(val);
    const int x = 8563;
    std::cout << 8563;
    for (int i = 0; i < 8563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8563(int& val) {
    const int x = 8563;
    std::cout << 8563;
    for (int i = 0; i < 8563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8564(int& val) {
    func_in_8564(val);
    const int x = 8564;
    std::cout << 8564;
    for (int i = 0; i < 8564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8564(int& val) {
    const int x = 8564;
    std::cout << 8564;
    for (int i = 0; i < 8564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8565(int& val) {
    func_in_8565(val);
    const int x = 8565;
    std::cout << 8565;
    for (int i = 0; i < 8565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8565(int& val) {
    const int x = 8565;
    std::cout << 8565;
    for (int i = 0; i < 8565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8566(int& val) {
    func_in_8566(val);
    const int x = 8566;
    std::cout << 8566;
    for (int i = 0; i < 8566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8566(int& val) {
    const int x = 8566;
    std::cout << 8566;
    for (int i = 0; i < 8566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8567(int& val) {
    func_in_8567(val);
    const int x = 8567;
    std::cout << 8567;
    for (int i = 0; i < 8567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8567(int& val) {
    const int x = 8567;
    std::cout << 8567;
    for (int i = 0; i < 8567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8568(int& val) {
    func_in_8568(val);
    const int x = 8568;
    std::cout << 8568;
    for (int i = 0; i < 8568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8568(int& val) {
    const int x = 8568;
    std::cout << 8568;
    for (int i = 0; i < 8568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8569(int& val) {
    func_in_8569(val);
    const int x = 8569;
    std::cout << 8569;
    for (int i = 0; i < 8569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8569(int& val) {
    const int x = 8569;
    std::cout << 8569;
    for (int i = 0; i < 8569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8570(int& val) {
    func_in_8570(val);
    const int x = 8570;
    std::cout << 8570;
    for (int i = 0; i < 8570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8570(int& val) {
    const int x = 8570;
    std::cout << 8570;
    for (int i = 0; i < 8570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8571(int& val) {
    func_in_8571(val);
    const int x = 8571;
    std::cout << 8571;
    for (int i = 0; i < 8571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8571(int& val) {
    const int x = 8571;
    std::cout << 8571;
    for (int i = 0; i < 8571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8572(int& val) {
    func_in_8572(val);
    const int x = 8572;
    std::cout << 8572;
    for (int i = 0; i < 8572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8572(int& val) {
    const int x = 8572;
    std::cout << 8572;
    for (int i = 0; i < 8572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8573(int& val) {
    func_in_8573(val);
    const int x = 8573;
    std::cout << 8573;
    for (int i = 0; i < 8573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8573(int& val) {
    const int x = 8573;
    std::cout << 8573;
    for (int i = 0; i < 8573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8574(int& val) {
    func_in_8574(val);
    const int x = 8574;
    std::cout << 8574;
    for (int i = 0; i < 8574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8574(int& val) {
    const int x = 8574;
    std::cout << 8574;
    for (int i = 0; i < 8574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8575(int& val) {
    func_in_8575(val);
    const int x = 8575;
    std::cout << 8575;
    for (int i = 0; i < 8575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8575(int& val) {
    const int x = 8575;
    std::cout << 8575;
    for (int i = 0; i < 8575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8576(int& val) {
    func_in_8576(val);
    const int x = 8576;
    std::cout << 8576;
    for (int i = 0; i < 8576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8576(int& val) {
    const int x = 8576;
    std::cout << 8576;
    for (int i = 0; i < 8576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8577(int& val) {
    func_in_8577(val);
    const int x = 8577;
    std::cout << 8577;
    for (int i = 0; i < 8577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8577(int& val) {
    const int x = 8577;
    std::cout << 8577;
    for (int i = 0; i < 8577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8578(int& val) {
    func_in_8578(val);
    const int x = 8578;
    std::cout << 8578;
    for (int i = 0; i < 8578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8578(int& val) {
    const int x = 8578;
    std::cout << 8578;
    for (int i = 0; i < 8578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8579(int& val) {
    func_in_8579(val);
    const int x = 8579;
    std::cout << 8579;
    for (int i = 0; i < 8579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8579(int& val) {
    const int x = 8579;
    std::cout << 8579;
    for (int i = 0; i < 8579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8580(int& val) {
    func_in_8580(val);
    const int x = 8580;
    std::cout << 8580;
    for (int i = 0; i < 8580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8580(int& val) {
    const int x = 8580;
    std::cout << 8580;
    for (int i = 0; i < 8580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8581(int& val) {
    func_in_8581(val);
    const int x = 8581;
    std::cout << 8581;
    for (int i = 0; i < 8581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8581(int& val) {
    const int x = 8581;
    std::cout << 8581;
    for (int i = 0; i < 8581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8582(int& val) {
    func_in_8582(val);
    const int x = 8582;
    std::cout << 8582;
    for (int i = 0; i < 8582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8582(int& val) {
    const int x = 8582;
    std::cout << 8582;
    for (int i = 0; i < 8582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8583(int& val) {
    func_in_8583(val);
    const int x = 8583;
    std::cout << 8583;
    for (int i = 0; i < 8583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8583(int& val) {
    const int x = 8583;
    std::cout << 8583;
    for (int i = 0; i < 8583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8584(int& val) {
    func_in_8584(val);
    const int x = 8584;
    std::cout << 8584;
    for (int i = 0; i < 8584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8584(int& val) {
    const int x = 8584;
    std::cout << 8584;
    for (int i = 0; i < 8584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8585(int& val) {
    func_in_8585(val);
    const int x = 8585;
    std::cout << 8585;
    for (int i = 0; i < 8585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8585(int& val) {
    const int x = 8585;
    std::cout << 8585;
    for (int i = 0; i < 8585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8586(int& val) {
    func_in_8586(val);
    const int x = 8586;
    std::cout << 8586;
    for (int i = 0; i < 8586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8586(int& val) {
    const int x = 8586;
    std::cout << 8586;
    for (int i = 0; i < 8586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8587(int& val) {
    func_in_8587(val);
    const int x = 8587;
    std::cout << 8587;
    for (int i = 0; i < 8587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8587(int& val) {
    const int x = 8587;
    std::cout << 8587;
    for (int i = 0; i < 8587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8588(int& val) {
    func_in_8588(val);
    const int x = 8588;
    std::cout << 8588;
    for (int i = 0; i < 8588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8588(int& val) {
    const int x = 8588;
    std::cout << 8588;
    for (int i = 0; i < 8588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8589(int& val) {
    func_in_8589(val);
    const int x = 8589;
    std::cout << 8589;
    for (int i = 0; i < 8589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8589(int& val) {
    const int x = 8589;
    std::cout << 8589;
    for (int i = 0; i < 8589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8590(int& val) {
    func_in_8590(val);
    const int x = 8590;
    std::cout << 8590;
    for (int i = 0; i < 8590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8590(int& val) {
    const int x = 8590;
    std::cout << 8590;
    for (int i = 0; i < 8590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8591(int& val) {
    func_in_8591(val);
    const int x = 8591;
    std::cout << 8591;
    for (int i = 0; i < 8591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8591(int& val) {
    const int x = 8591;
    std::cout << 8591;
    for (int i = 0; i < 8591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8592(int& val) {
    func_in_8592(val);
    const int x = 8592;
    std::cout << 8592;
    for (int i = 0; i < 8592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8592(int& val) {
    const int x = 8592;
    std::cout << 8592;
    for (int i = 0; i < 8592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8593(int& val) {
    func_in_8593(val);
    const int x = 8593;
    std::cout << 8593;
    for (int i = 0; i < 8593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8593(int& val) {
    const int x = 8593;
    std::cout << 8593;
    for (int i = 0; i < 8593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8594(int& val) {
    func_in_8594(val);
    const int x = 8594;
    std::cout << 8594;
    for (int i = 0; i < 8594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8594(int& val) {
    const int x = 8594;
    std::cout << 8594;
    for (int i = 0; i < 8594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8595(int& val) {
    func_in_8595(val);
    const int x = 8595;
    std::cout << 8595;
    for (int i = 0; i < 8595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8595(int& val) {
    const int x = 8595;
    std::cout << 8595;
    for (int i = 0; i < 8595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8596(int& val) {
    func_in_8596(val);
    const int x = 8596;
    std::cout << 8596;
    for (int i = 0; i < 8596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8596(int& val) {
    const int x = 8596;
    std::cout << 8596;
    for (int i = 0; i < 8596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8597(int& val) {
    func_in_8597(val);
    const int x = 8597;
    std::cout << 8597;
    for (int i = 0; i < 8597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8597(int& val) {
    const int x = 8597;
    std::cout << 8597;
    for (int i = 0; i < 8597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8598(int& val) {
    func_in_8598(val);
    const int x = 8598;
    std::cout << 8598;
    for (int i = 0; i < 8598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8598(int& val) {
    const int x = 8598;
    std::cout << 8598;
    for (int i = 0; i < 8598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8599(int& val) {
    func_in_8599(val);
    const int x = 8599;
    std::cout << 8599;
    for (int i = 0; i < 8599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8599(int& val) {
    const int x = 8599;
    std::cout << 8599;
    for (int i = 0; i < 8599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8600(int& val) {
    func_in_8600(val);
    const int x = 8600;
    std::cout << 8600;
    for (int i = 0; i < 8600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8600(int& val) {
    const int x = 8600;
    std::cout << 8600;
    for (int i = 0; i < 8600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8601(int& val) {
    func_in_8601(val);
    const int x = 8601;
    std::cout << 8601;
    for (int i = 0; i < 8601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8601(int& val) {
    const int x = 8601;
    std::cout << 8601;
    for (int i = 0; i < 8601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8602(int& val) {
    func_in_8602(val);
    const int x = 8602;
    std::cout << 8602;
    for (int i = 0; i < 8602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8602(int& val) {
    const int x = 8602;
    std::cout << 8602;
    for (int i = 0; i < 8602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8603(int& val) {
    func_in_8603(val);
    const int x = 8603;
    std::cout << 8603;
    for (int i = 0; i < 8603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8603(int& val) {
    const int x = 8603;
    std::cout << 8603;
    for (int i = 0; i < 8603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8604(int& val) {
    func_in_8604(val);
    const int x = 8604;
    std::cout << 8604;
    for (int i = 0; i < 8604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8604(int& val) {
    const int x = 8604;
    std::cout << 8604;
    for (int i = 0; i < 8604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8605(int& val) {
    func_in_8605(val);
    const int x = 8605;
    std::cout << 8605;
    for (int i = 0; i < 8605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8605(int& val) {
    const int x = 8605;
    std::cout << 8605;
    for (int i = 0; i < 8605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8606(int& val) {
    func_in_8606(val);
    const int x = 8606;
    std::cout << 8606;
    for (int i = 0; i < 8606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8606(int& val) {
    const int x = 8606;
    std::cout << 8606;
    for (int i = 0; i < 8606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8607(int& val) {
    func_in_8607(val);
    const int x = 8607;
    std::cout << 8607;
    for (int i = 0; i < 8607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8607(int& val) {
    const int x = 8607;
    std::cout << 8607;
    for (int i = 0; i < 8607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8608(int& val) {
    func_in_8608(val);
    const int x = 8608;
    std::cout << 8608;
    for (int i = 0; i < 8608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8608(int& val) {
    const int x = 8608;
    std::cout << 8608;
    for (int i = 0; i < 8608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8609(int& val) {
    func_in_8609(val);
    const int x = 8609;
    std::cout << 8609;
    for (int i = 0; i < 8609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8609(int& val) {
    const int x = 8609;
    std::cout << 8609;
    for (int i = 0; i < 8609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8610(int& val) {
    func_in_8610(val);
    const int x = 8610;
    std::cout << 8610;
    for (int i = 0; i < 8610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8610(int& val) {
    const int x = 8610;
    std::cout << 8610;
    for (int i = 0; i < 8610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8611(int& val) {
    func_in_8611(val);
    const int x = 8611;
    std::cout << 8611;
    for (int i = 0; i < 8611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8611(int& val) {
    const int x = 8611;
    std::cout << 8611;
    for (int i = 0; i < 8611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8612(int& val) {
    func_in_8612(val);
    const int x = 8612;
    std::cout << 8612;
    for (int i = 0; i < 8612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8612(int& val) {
    const int x = 8612;
    std::cout << 8612;
    for (int i = 0; i < 8612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8613(int& val) {
    func_in_8613(val);
    const int x = 8613;
    std::cout << 8613;
    for (int i = 0; i < 8613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8613(int& val) {
    const int x = 8613;
    std::cout << 8613;
    for (int i = 0; i < 8613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8614(int& val) {
    func_in_8614(val);
    const int x = 8614;
    std::cout << 8614;
    for (int i = 0; i < 8614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8614(int& val) {
    const int x = 8614;
    std::cout << 8614;
    for (int i = 0; i < 8614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8615(int& val) {
    func_in_8615(val);
    const int x = 8615;
    std::cout << 8615;
    for (int i = 0; i < 8615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8615(int& val) {
    const int x = 8615;
    std::cout << 8615;
    for (int i = 0; i < 8615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8616(int& val) {
    func_in_8616(val);
    const int x = 8616;
    std::cout << 8616;
    for (int i = 0; i < 8616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8616(int& val) {
    const int x = 8616;
    std::cout << 8616;
    for (int i = 0; i < 8616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8617(int& val) {
    func_in_8617(val);
    const int x = 8617;
    std::cout << 8617;
    for (int i = 0; i < 8617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8617(int& val) {
    const int x = 8617;
    std::cout << 8617;
    for (int i = 0; i < 8617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8618(int& val) {
    func_in_8618(val);
    const int x = 8618;
    std::cout << 8618;
    for (int i = 0; i < 8618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8618(int& val) {
    const int x = 8618;
    std::cout << 8618;
    for (int i = 0; i < 8618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8619(int& val) {
    func_in_8619(val);
    const int x = 8619;
    std::cout << 8619;
    for (int i = 0; i < 8619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8619(int& val) {
    const int x = 8619;
    std::cout << 8619;
    for (int i = 0; i < 8619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8620(int& val) {
    func_in_8620(val);
    const int x = 8620;
    std::cout << 8620;
    for (int i = 0; i < 8620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8620(int& val) {
    const int x = 8620;
    std::cout << 8620;
    for (int i = 0; i < 8620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8621(int& val) {
    func_in_8621(val);
    const int x = 8621;
    std::cout << 8621;
    for (int i = 0; i < 8621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8621(int& val) {
    const int x = 8621;
    std::cout << 8621;
    for (int i = 0; i < 8621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8622(int& val) {
    func_in_8622(val);
    const int x = 8622;
    std::cout << 8622;
    for (int i = 0; i < 8622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8622(int& val) {
    const int x = 8622;
    std::cout << 8622;
    for (int i = 0; i < 8622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8623(int& val) {
    func_in_8623(val);
    const int x = 8623;
    std::cout << 8623;
    for (int i = 0; i < 8623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8623(int& val) {
    const int x = 8623;
    std::cout << 8623;
    for (int i = 0; i < 8623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8624(int& val) {
    func_in_8624(val);
    const int x = 8624;
    std::cout << 8624;
    for (int i = 0; i < 8624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8624(int& val) {
    const int x = 8624;
    std::cout << 8624;
    for (int i = 0; i < 8624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8625(int& val) {
    func_in_8625(val);
    const int x = 8625;
    std::cout << 8625;
    for (int i = 0; i < 8625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8625(int& val) {
    const int x = 8625;
    std::cout << 8625;
    for (int i = 0; i < 8625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8626(int& val) {
    func_in_8626(val);
    const int x = 8626;
    std::cout << 8626;
    for (int i = 0; i < 8626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8626(int& val) {
    const int x = 8626;
    std::cout << 8626;
    for (int i = 0; i < 8626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8627(int& val) {
    func_in_8627(val);
    const int x = 8627;
    std::cout << 8627;
    for (int i = 0; i < 8627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8627(int& val) {
    const int x = 8627;
    std::cout << 8627;
    for (int i = 0; i < 8627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8628(int& val) {
    func_in_8628(val);
    const int x = 8628;
    std::cout << 8628;
    for (int i = 0; i < 8628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8628(int& val) {
    const int x = 8628;
    std::cout << 8628;
    for (int i = 0; i < 8628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8629(int& val) {
    func_in_8629(val);
    const int x = 8629;
    std::cout << 8629;
    for (int i = 0; i < 8629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8629(int& val) {
    const int x = 8629;
    std::cout << 8629;
    for (int i = 0; i < 8629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8630(int& val) {
    func_in_8630(val);
    const int x = 8630;
    std::cout << 8630;
    for (int i = 0; i < 8630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8630(int& val) {
    const int x = 8630;
    std::cout << 8630;
    for (int i = 0; i < 8630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8631(int& val) {
    func_in_8631(val);
    const int x = 8631;
    std::cout << 8631;
    for (int i = 0; i < 8631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8631(int& val) {
    const int x = 8631;
    std::cout << 8631;
    for (int i = 0; i < 8631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8632(int& val) {
    func_in_8632(val);
    const int x = 8632;
    std::cout << 8632;
    for (int i = 0; i < 8632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8632(int& val) {
    const int x = 8632;
    std::cout << 8632;
    for (int i = 0; i < 8632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8633(int& val) {
    func_in_8633(val);
    const int x = 8633;
    std::cout << 8633;
    for (int i = 0; i < 8633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8633(int& val) {
    const int x = 8633;
    std::cout << 8633;
    for (int i = 0; i < 8633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8634(int& val) {
    func_in_8634(val);
    const int x = 8634;
    std::cout << 8634;
    for (int i = 0; i < 8634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8634(int& val) {
    const int x = 8634;
    std::cout << 8634;
    for (int i = 0; i < 8634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8635(int& val) {
    func_in_8635(val);
    const int x = 8635;
    std::cout << 8635;
    for (int i = 0; i < 8635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8635(int& val) {
    const int x = 8635;
    std::cout << 8635;
    for (int i = 0; i < 8635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8636(int& val) {
    func_in_8636(val);
    const int x = 8636;
    std::cout << 8636;
    for (int i = 0; i < 8636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8636(int& val) {
    const int x = 8636;
    std::cout << 8636;
    for (int i = 0; i < 8636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8637(int& val) {
    func_in_8637(val);
    const int x = 8637;
    std::cout << 8637;
    for (int i = 0; i < 8637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8637(int& val) {
    const int x = 8637;
    std::cout << 8637;
    for (int i = 0; i < 8637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8638(int& val) {
    func_in_8638(val);
    const int x = 8638;
    std::cout << 8638;
    for (int i = 0; i < 8638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8638(int& val) {
    const int x = 8638;
    std::cout << 8638;
    for (int i = 0; i < 8638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8639(int& val) {
    func_in_8639(val);
    const int x = 8639;
    std::cout << 8639;
    for (int i = 0; i < 8639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8639(int& val) {
    const int x = 8639;
    std::cout << 8639;
    for (int i = 0; i < 8639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8640(int& val) {
    func_in_8640(val);
    const int x = 8640;
    std::cout << 8640;
    for (int i = 0; i < 8640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8640(int& val) {
    const int x = 8640;
    std::cout << 8640;
    for (int i = 0; i < 8640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8641(int& val) {
    func_in_8641(val);
    const int x = 8641;
    std::cout << 8641;
    for (int i = 0; i < 8641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8641(int& val) {
    const int x = 8641;
    std::cout << 8641;
    for (int i = 0; i < 8641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8642(int& val) {
    func_in_8642(val);
    const int x = 8642;
    std::cout << 8642;
    for (int i = 0; i < 8642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8642(int& val) {
    const int x = 8642;
    std::cout << 8642;
    for (int i = 0; i < 8642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8643(int& val) {
    func_in_8643(val);
    const int x = 8643;
    std::cout << 8643;
    for (int i = 0; i < 8643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8643(int& val) {
    const int x = 8643;
    std::cout << 8643;
    for (int i = 0; i < 8643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8644(int& val) {
    func_in_8644(val);
    const int x = 8644;
    std::cout << 8644;
    for (int i = 0; i < 8644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8644(int& val) {
    const int x = 8644;
    std::cout << 8644;
    for (int i = 0; i < 8644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8645(int& val) {
    func_in_8645(val);
    const int x = 8645;
    std::cout << 8645;
    for (int i = 0; i < 8645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8645(int& val) {
    const int x = 8645;
    std::cout << 8645;
    for (int i = 0; i < 8645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8646(int& val) {
    func_in_8646(val);
    const int x = 8646;
    std::cout << 8646;
    for (int i = 0; i < 8646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8646(int& val) {
    const int x = 8646;
    std::cout << 8646;
    for (int i = 0; i < 8646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8647(int& val) {
    func_in_8647(val);
    const int x = 8647;
    std::cout << 8647;
    for (int i = 0; i < 8647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8647(int& val) {
    const int x = 8647;
    std::cout << 8647;
    for (int i = 0; i < 8647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8648(int& val) {
    func_in_8648(val);
    const int x = 8648;
    std::cout << 8648;
    for (int i = 0; i < 8648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8648(int& val) {
    const int x = 8648;
    std::cout << 8648;
    for (int i = 0; i < 8648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8649(int& val) {
    func_in_8649(val);
    const int x = 8649;
    std::cout << 8649;
    for (int i = 0; i < 8649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8649(int& val) {
    const int x = 8649;
    std::cout << 8649;
    for (int i = 0; i < 8649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8650(int& val) {
    func_in_8650(val);
    const int x = 8650;
    std::cout << 8650;
    for (int i = 0; i < 8650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8650(int& val) {
    const int x = 8650;
    std::cout << 8650;
    for (int i = 0; i < 8650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8651(int& val) {
    func_in_8651(val);
    const int x = 8651;
    std::cout << 8651;
    for (int i = 0; i < 8651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8651(int& val) {
    const int x = 8651;
    std::cout << 8651;
    for (int i = 0; i < 8651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8652(int& val) {
    func_in_8652(val);
    const int x = 8652;
    std::cout << 8652;
    for (int i = 0; i < 8652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8652(int& val) {
    const int x = 8652;
    std::cout << 8652;
    for (int i = 0; i < 8652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8653(int& val) {
    func_in_8653(val);
    const int x = 8653;
    std::cout << 8653;
    for (int i = 0; i < 8653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8653(int& val) {
    const int x = 8653;
    std::cout << 8653;
    for (int i = 0; i < 8653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8654(int& val) {
    func_in_8654(val);
    const int x = 8654;
    std::cout << 8654;
    for (int i = 0; i < 8654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8654(int& val) {
    const int x = 8654;
    std::cout << 8654;
    for (int i = 0; i < 8654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8655(int& val) {
    func_in_8655(val);
    const int x = 8655;
    std::cout << 8655;
    for (int i = 0; i < 8655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8655(int& val) {
    const int x = 8655;
    std::cout << 8655;
    for (int i = 0; i < 8655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8656(int& val) {
    func_in_8656(val);
    const int x = 8656;
    std::cout << 8656;
    for (int i = 0; i < 8656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8656(int& val) {
    const int x = 8656;
    std::cout << 8656;
    for (int i = 0; i < 8656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8657(int& val) {
    func_in_8657(val);
    const int x = 8657;
    std::cout << 8657;
    for (int i = 0; i < 8657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8657(int& val) {
    const int x = 8657;
    std::cout << 8657;
    for (int i = 0; i < 8657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8658(int& val) {
    func_in_8658(val);
    const int x = 8658;
    std::cout << 8658;
    for (int i = 0; i < 8658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8658(int& val) {
    const int x = 8658;
    std::cout << 8658;
    for (int i = 0; i < 8658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8659(int& val) {
    func_in_8659(val);
    const int x = 8659;
    std::cout << 8659;
    for (int i = 0; i < 8659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8659(int& val) {
    const int x = 8659;
    std::cout << 8659;
    for (int i = 0; i < 8659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8660(int& val) {
    func_in_8660(val);
    const int x = 8660;
    std::cout << 8660;
    for (int i = 0; i < 8660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8660(int& val) {
    const int x = 8660;
    std::cout << 8660;
    for (int i = 0; i < 8660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8661(int& val) {
    func_in_8661(val);
    const int x = 8661;
    std::cout << 8661;
    for (int i = 0; i < 8661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8661(int& val) {
    const int x = 8661;
    std::cout << 8661;
    for (int i = 0; i < 8661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8662(int& val) {
    func_in_8662(val);
    const int x = 8662;
    std::cout << 8662;
    for (int i = 0; i < 8662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8662(int& val) {
    const int x = 8662;
    std::cout << 8662;
    for (int i = 0; i < 8662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8663(int& val) {
    func_in_8663(val);
    const int x = 8663;
    std::cout << 8663;
    for (int i = 0; i < 8663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8663(int& val) {
    const int x = 8663;
    std::cout << 8663;
    for (int i = 0; i < 8663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8664(int& val) {
    func_in_8664(val);
    const int x = 8664;
    std::cout << 8664;
    for (int i = 0; i < 8664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8664(int& val) {
    const int x = 8664;
    std::cout << 8664;
    for (int i = 0; i < 8664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8665(int& val) {
    func_in_8665(val);
    const int x = 8665;
    std::cout << 8665;
    for (int i = 0; i < 8665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8665(int& val) {
    const int x = 8665;
    std::cout << 8665;
    for (int i = 0; i < 8665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8666(int& val) {
    func_in_8666(val);
    const int x = 8666;
    std::cout << 8666;
    for (int i = 0; i < 8666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8666(int& val) {
    const int x = 8666;
    std::cout << 8666;
    for (int i = 0; i < 8666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8667(int& val) {
    func_in_8667(val);
    const int x = 8667;
    std::cout << 8667;
    for (int i = 0; i < 8667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8667(int& val) {
    const int x = 8667;
    std::cout << 8667;
    for (int i = 0; i < 8667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8668(int& val) {
    func_in_8668(val);
    const int x = 8668;
    std::cout << 8668;
    for (int i = 0; i < 8668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8668(int& val) {
    const int x = 8668;
    std::cout << 8668;
    for (int i = 0; i < 8668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8669(int& val) {
    func_in_8669(val);
    const int x = 8669;
    std::cout << 8669;
    for (int i = 0; i < 8669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8669(int& val) {
    const int x = 8669;
    std::cout << 8669;
    for (int i = 0; i < 8669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8670(int& val) {
    func_in_8670(val);
    const int x = 8670;
    std::cout << 8670;
    for (int i = 0; i < 8670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8670(int& val) {
    const int x = 8670;
    std::cout << 8670;
    for (int i = 0; i < 8670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8671(int& val) {
    func_in_8671(val);
    const int x = 8671;
    std::cout << 8671;
    for (int i = 0; i < 8671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8671(int& val) {
    const int x = 8671;
    std::cout << 8671;
    for (int i = 0; i < 8671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8672(int& val) {
    func_in_8672(val);
    const int x = 8672;
    std::cout << 8672;
    for (int i = 0; i < 8672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8672(int& val) {
    const int x = 8672;
    std::cout << 8672;
    for (int i = 0; i < 8672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8673(int& val) {
    func_in_8673(val);
    const int x = 8673;
    std::cout << 8673;
    for (int i = 0; i < 8673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8673(int& val) {
    const int x = 8673;
    std::cout << 8673;
    for (int i = 0; i < 8673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8674(int& val) {
    func_in_8674(val);
    const int x = 8674;
    std::cout << 8674;
    for (int i = 0; i < 8674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8674(int& val) {
    const int x = 8674;
    std::cout << 8674;
    for (int i = 0; i < 8674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8675(int& val) {
    func_in_8675(val);
    const int x = 8675;
    std::cout << 8675;
    for (int i = 0; i < 8675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8675(int& val) {
    const int x = 8675;
    std::cout << 8675;
    for (int i = 0; i < 8675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8676(int& val) {
    func_in_8676(val);
    const int x = 8676;
    std::cout << 8676;
    for (int i = 0; i < 8676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8676(int& val) {
    const int x = 8676;
    std::cout << 8676;
    for (int i = 0; i < 8676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8677(int& val) {
    func_in_8677(val);
    const int x = 8677;
    std::cout << 8677;
    for (int i = 0; i < 8677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8677(int& val) {
    const int x = 8677;
    std::cout << 8677;
    for (int i = 0; i < 8677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8678(int& val) {
    func_in_8678(val);
    const int x = 8678;
    std::cout << 8678;
    for (int i = 0; i < 8678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8678(int& val) {
    const int x = 8678;
    std::cout << 8678;
    for (int i = 0; i < 8678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8679(int& val) {
    func_in_8679(val);
    const int x = 8679;
    std::cout << 8679;
    for (int i = 0; i < 8679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8679(int& val) {
    const int x = 8679;
    std::cout << 8679;
    for (int i = 0; i < 8679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8680(int& val) {
    func_in_8680(val);
    const int x = 8680;
    std::cout << 8680;
    for (int i = 0; i < 8680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8680(int& val) {
    const int x = 8680;
    std::cout << 8680;
    for (int i = 0; i < 8680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8681(int& val) {
    func_in_8681(val);
    const int x = 8681;
    std::cout << 8681;
    for (int i = 0; i < 8681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8681(int& val) {
    const int x = 8681;
    std::cout << 8681;
    for (int i = 0; i < 8681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8682(int& val) {
    func_in_8682(val);
    const int x = 8682;
    std::cout << 8682;
    for (int i = 0; i < 8682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8682(int& val) {
    const int x = 8682;
    std::cout << 8682;
    for (int i = 0; i < 8682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8683(int& val) {
    func_in_8683(val);
    const int x = 8683;
    std::cout << 8683;
    for (int i = 0; i < 8683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8683(int& val) {
    const int x = 8683;
    std::cout << 8683;
    for (int i = 0; i < 8683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8684(int& val) {
    func_in_8684(val);
    const int x = 8684;
    std::cout << 8684;
    for (int i = 0; i < 8684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8684(int& val) {
    const int x = 8684;
    std::cout << 8684;
    for (int i = 0; i < 8684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8685(int& val) {
    func_in_8685(val);
    const int x = 8685;
    std::cout << 8685;
    for (int i = 0; i < 8685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8685(int& val) {
    const int x = 8685;
    std::cout << 8685;
    for (int i = 0; i < 8685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8686(int& val) {
    func_in_8686(val);
    const int x = 8686;
    std::cout << 8686;
    for (int i = 0; i < 8686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8686(int& val) {
    const int x = 8686;
    std::cout << 8686;
    for (int i = 0; i < 8686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8687(int& val) {
    func_in_8687(val);
    const int x = 8687;
    std::cout << 8687;
    for (int i = 0; i < 8687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8687(int& val) {
    const int x = 8687;
    std::cout << 8687;
    for (int i = 0; i < 8687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8688(int& val) {
    func_in_8688(val);
    const int x = 8688;
    std::cout << 8688;
    for (int i = 0; i < 8688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8688(int& val) {
    const int x = 8688;
    std::cout << 8688;
    for (int i = 0; i < 8688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8689(int& val) {
    func_in_8689(val);
    const int x = 8689;
    std::cout << 8689;
    for (int i = 0; i < 8689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8689(int& val) {
    const int x = 8689;
    std::cout << 8689;
    for (int i = 0; i < 8689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8690(int& val) {
    func_in_8690(val);
    const int x = 8690;
    std::cout << 8690;
    for (int i = 0; i < 8690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8690(int& val) {
    const int x = 8690;
    std::cout << 8690;
    for (int i = 0; i < 8690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8691(int& val) {
    func_in_8691(val);
    const int x = 8691;
    std::cout << 8691;
    for (int i = 0; i < 8691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8691(int& val) {
    const int x = 8691;
    std::cout << 8691;
    for (int i = 0; i < 8691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8692(int& val) {
    func_in_8692(val);
    const int x = 8692;
    std::cout << 8692;
    for (int i = 0; i < 8692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8692(int& val) {
    const int x = 8692;
    std::cout << 8692;
    for (int i = 0; i < 8692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8693(int& val) {
    func_in_8693(val);
    const int x = 8693;
    std::cout << 8693;
    for (int i = 0; i < 8693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8693(int& val) {
    const int x = 8693;
    std::cout << 8693;
    for (int i = 0; i < 8693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8694(int& val) {
    func_in_8694(val);
    const int x = 8694;
    std::cout << 8694;
    for (int i = 0; i < 8694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8694(int& val) {
    const int x = 8694;
    std::cout << 8694;
    for (int i = 0; i < 8694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8695(int& val) {
    func_in_8695(val);
    const int x = 8695;
    std::cout << 8695;
    for (int i = 0; i < 8695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8695(int& val) {
    const int x = 8695;
    std::cout << 8695;
    for (int i = 0; i < 8695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8696(int& val) {
    func_in_8696(val);
    const int x = 8696;
    std::cout << 8696;
    for (int i = 0; i < 8696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8696(int& val) {
    const int x = 8696;
    std::cout << 8696;
    for (int i = 0; i < 8696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8697(int& val) {
    func_in_8697(val);
    const int x = 8697;
    std::cout << 8697;
    for (int i = 0; i < 8697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8697(int& val) {
    const int x = 8697;
    std::cout << 8697;
    for (int i = 0; i < 8697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8698(int& val) {
    func_in_8698(val);
    const int x = 8698;
    std::cout << 8698;
    for (int i = 0; i < 8698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8698(int& val) {
    const int x = 8698;
    std::cout << 8698;
    for (int i = 0; i < 8698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8699(int& val) {
    func_in_8699(val);
    const int x = 8699;
    std::cout << 8699;
    for (int i = 0; i < 8699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8699(int& val) {
    const int x = 8699;
    std::cout << 8699;
    for (int i = 0; i < 8699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8700(int& val) {
    func_in_8700(val);
    const int x = 8700;
    std::cout << 8700;
    for (int i = 0; i < 8700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8700(int& val) {
    const int x = 8700;
    std::cout << 8700;
    for (int i = 0; i < 8700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8701(int& val) {
    func_in_8701(val);
    const int x = 8701;
    std::cout << 8701;
    for (int i = 0; i < 8701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8701(int& val) {
    const int x = 8701;
    std::cout << 8701;
    for (int i = 0; i < 8701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8702(int& val) {
    func_in_8702(val);
    const int x = 8702;
    std::cout << 8702;
    for (int i = 0; i < 8702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8702(int& val) {
    const int x = 8702;
    std::cout << 8702;
    for (int i = 0; i < 8702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8703(int& val) {
    func_in_8703(val);
    const int x = 8703;
    std::cout << 8703;
    for (int i = 0; i < 8703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8703(int& val) {
    const int x = 8703;
    std::cout << 8703;
    for (int i = 0; i < 8703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8704(int& val) {
    func_in_8704(val);
    const int x = 8704;
    std::cout << 8704;
    for (int i = 0; i < 8704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8704(int& val) {
    const int x = 8704;
    std::cout << 8704;
    for (int i = 0; i < 8704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8705(int& val) {
    func_in_8705(val);
    const int x = 8705;
    std::cout << 8705;
    for (int i = 0; i < 8705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8705(int& val) {
    const int x = 8705;
    std::cout << 8705;
    for (int i = 0; i < 8705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8706(int& val) {
    func_in_8706(val);
    const int x = 8706;
    std::cout << 8706;
    for (int i = 0; i < 8706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8706(int& val) {
    const int x = 8706;
    std::cout << 8706;
    for (int i = 0; i < 8706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8707(int& val) {
    func_in_8707(val);
    const int x = 8707;
    std::cout << 8707;
    for (int i = 0; i < 8707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8707(int& val) {
    const int x = 8707;
    std::cout << 8707;
    for (int i = 0; i < 8707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8708(int& val) {
    func_in_8708(val);
    const int x = 8708;
    std::cout << 8708;
    for (int i = 0; i < 8708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8708(int& val) {
    const int x = 8708;
    std::cout << 8708;
    for (int i = 0; i < 8708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8709(int& val) {
    func_in_8709(val);
    const int x = 8709;
    std::cout << 8709;
    for (int i = 0; i < 8709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8709(int& val) {
    const int x = 8709;
    std::cout << 8709;
    for (int i = 0; i < 8709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8710(int& val) {
    func_in_8710(val);
    const int x = 8710;
    std::cout << 8710;
    for (int i = 0; i < 8710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8710(int& val) {
    const int x = 8710;
    std::cout << 8710;
    for (int i = 0; i < 8710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8711(int& val) {
    func_in_8711(val);
    const int x = 8711;
    std::cout << 8711;
    for (int i = 0; i < 8711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8711(int& val) {
    const int x = 8711;
    std::cout << 8711;
    for (int i = 0; i < 8711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8712(int& val) {
    func_in_8712(val);
    const int x = 8712;
    std::cout << 8712;
    for (int i = 0; i < 8712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8712(int& val) {
    const int x = 8712;
    std::cout << 8712;
    for (int i = 0; i < 8712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8713(int& val) {
    func_in_8713(val);
    const int x = 8713;
    std::cout << 8713;
    for (int i = 0; i < 8713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8713(int& val) {
    const int x = 8713;
    std::cout << 8713;
    for (int i = 0; i < 8713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8714(int& val) {
    func_in_8714(val);
    const int x = 8714;
    std::cout << 8714;
    for (int i = 0; i < 8714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8714(int& val) {
    const int x = 8714;
    std::cout << 8714;
    for (int i = 0; i < 8714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8715(int& val) {
    func_in_8715(val);
    const int x = 8715;
    std::cout << 8715;
    for (int i = 0; i < 8715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8715(int& val) {
    const int x = 8715;
    std::cout << 8715;
    for (int i = 0; i < 8715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8716(int& val) {
    func_in_8716(val);
    const int x = 8716;
    std::cout << 8716;
    for (int i = 0; i < 8716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8716(int& val) {
    const int x = 8716;
    std::cout << 8716;
    for (int i = 0; i < 8716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8717(int& val) {
    func_in_8717(val);
    const int x = 8717;
    std::cout << 8717;
    for (int i = 0; i < 8717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8717(int& val) {
    const int x = 8717;
    std::cout << 8717;
    for (int i = 0; i < 8717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8718(int& val) {
    func_in_8718(val);
    const int x = 8718;
    std::cout << 8718;
    for (int i = 0; i < 8718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8718(int& val) {
    const int x = 8718;
    std::cout << 8718;
    for (int i = 0; i < 8718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8719(int& val) {
    func_in_8719(val);
    const int x = 8719;
    std::cout << 8719;
    for (int i = 0; i < 8719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8719(int& val) {
    const int x = 8719;
    std::cout << 8719;
    for (int i = 0; i < 8719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8720(int& val) {
    func_in_8720(val);
    const int x = 8720;
    std::cout << 8720;
    for (int i = 0; i < 8720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8720(int& val) {
    const int x = 8720;
    std::cout << 8720;
    for (int i = 0; i < 8720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8721(int& val) {
    func_in_8721(val);
    const int x = 8721;
    std::cout << 8721;
    for (int i = 0; i < 8721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8721(int& val) {
    const int x = 8721;
    std::cout << 8721;
    for (int i = 0; i < 8721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8722(int& val) {
    func_in_8722(val);
    const int x = 8722;
    std::cout << 8722;
    for (int i = 0; i < 8722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8722(int& val) {
    const int x = 8722;
    std::cout << 8722;
    for (int i = 0; i < 8722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8723(int& val) {
    func_in_8723(val);
    const int x = 8723;
    std::cout << 8723;
    for (int i = 0; i < 8723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8723(int& val) {
    const int x = 8723;
    std::cout << 8723;
    for (int i = 0; i < 8723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8724(int& val) {
    func_in_8724(val);
    const int x = 8724;
    std::cout << 8724;
    for (int i = 0; i < 8724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8724(int& val) {
    const int x = 8724;
    std::cout << 8724;
    for (int i = 0; i < 8724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8725(int& val) {
    func_in_8725(val);
    const int x = 8725;
    std::cout << 8725;
    for (int i = 0; i < 8725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8725(int& val) {
    const int x = 8725;
    std::cout << 8725;
    for (int i = 0; i < 8725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8726(int& val) {
    func_in_8726(val);
    const int x = 8726;
    std::cout << 8726;
    for (int i = 0; i < 8726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8726(int& val) {
    const int x = 8726;
    std::cout << 8726;
    for (int i = 0; i < 8726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8727(int& val) {
    func_in_8727(val);
    const int x = 8727;
    std::cout << 8727;
    for (int i = 0; i < 8727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8727(int& val) {
    const int x = 8727;
    std::cout << 8727;
    for (int i = 0; i < 8727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8728(int& val) {
    func_in_8728(val);
    const int x = 8728;
    std::cout << 8728;
    for (int i = 0; i < 8728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8728(int& val) {
    const int x = 8728;
    std::cout << 8728;
    for (int i = 0; i < 8728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8729(int& val) {
    func_in_8729(val);
    const int x = 8729;
    std::cout << 8729;
    for (int i = 0; i < 8729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8729(int& val) {
    const int x = 8729;
    std::cout << 8729;
    for (int i = 0; i < 8729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8730(int& val) {
    func_in_8730(val);
    const int x = 8730;
    std::cout << 8730;
    for (int i = 0; i < 8730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8730(int& val) {
    const int x = 8730;
    std::cout << 8730;
    for (int i = 0; i < 8730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8731(int& val) {
    func_in_8731(val);
    const int x = 8731;
    std::cout << 8731;
    for (int i = 0; i < 8731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8731(int& val) {
    const int x = 8731;
    std::cout << 8731;
    for (int i = 0; i < 8731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8732(int& val) {
    func_in_8732(val);
    const int x = 8732;
    std::cout << 8732;
    for (int i = 0; i < 8732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8732(int& val) {
    const int x = 8732;
    std::cout << 8732;
    for (int i = 0; i < 8732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8733(int& val) {
    func_in_8733(val);
    const int x = 8733;
    std::cout << 8733;
    for (int i = 0; i < 8733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8733(int& val) {
    const int x = 8733;
    std::cout << 8733;
    for (int i = 0; i < 8733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8734(int& val) {
    func_in_8734(val);
    const int x = 8734;
    std::cout << 8734;
    for (int i = 0; i < 8734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8734(int& val) {
    const int x = 8734;
    std::cout << 8734;
    for (int i = 0; i < 8734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8735(int& val) {
    func_in_8735(val);
    const int x = 8735;
    std::cout << 8735;
    for (int i = 0; i < 8735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8735(int& val) {
    const int x = 8735;
    std::cout << 8735;
    for (int i = 0; i < 8735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8736(int& val) {
    func_in_8736(val);
    const int x = 8736;
    std::cout << 8736;
    for (int i = 0; i < 8736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8736(int& val) {
    const int x = 8736;
    std::cout << 8736;
    for (int i = 0; i < 8736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8737(int& val) {
    func_in_8737(val);
    const int x = 8737;
    std::cout << 8737;
    for (int i = 0; i < 8737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8737(int& val) {
    const int x = 8737;
    std::cout << 8737;
    for (int i = 0; i < 8737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8738(int& val) {
    func_in_8738(val);
    const int x = 8738;
    std::cout << 8738;
    for (int i = 0; i < 8738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8738(int& val) {
    const int x = 8738;
    std::cout << 8738;
    for (int i = 0; i < 8738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8739(int& val) {
    func_in_8739(val);
    const int x = 8739;
    std::cout << 8739;
    for (int i = 0; i < 8739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8739(int& val) {
    const int x = 8739;
    std::cout << 8739;
    for (int i = 0; i < 8739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8740(int& val) {
    func_in_8740(val);
    const int x = 8740;
    std::cout << 8740;
    for (int i = 0; i < 8740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8740(int& val) {
    const int x = 8740;
    std::cout << 8740;
    for (int i = 0; i < 8740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8741(int& val) {
    func_in_8741(val);
    const int x = 8741;
    std::cout << 8741;
    for (int i = 0; i < 8741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8741(int& val) {
    const int x = 8741;
    std::cout << 8741;
    for (int i = 0; i < 8741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8742(int& val) {
    func_in_8742(val);
    const int x = 8742;
    std::cout << 8742;
    for (int i = 0; i < 8742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8742(int& val) {
    const int x = 8742;
    std::cout << 8742;
    for (int i = 0; i < 8742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8743(int& val) {
    func_in_8743(val);
    const int x = 8743;
    std::cout << 8743;
    for (int i = 0; i < 8743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8743(int& val) {
    const int x = 8743;
    std::cout << 8743;
    for (int i = 0; i < 8743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8744(int& val) {
    func_in_8744(val);
    const int x = 8744;
    std::cout << 8744;
    for (int i = 0; i < 8744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8744(int& val) {
    const int x = 8744;
    std::cout << 8744;
    for (int i = 0; i < 8744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8745(int& val) {
    func_in_8745(val);
    const int x = 8745;
    std::cout << 8745;
    for (int i = 0; i < 8745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8745(int& val) {
    const int x = 8745;
    std::cout << 8745;
    for (int i = 0; i < 8745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8746(int& val) {
    func_in_8746(val);
    const int x = 8746;
    std::cout << 8746;
    for (int i = 0; i < 8746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8746(int& val) {
    const int x = 8746;
    std::cout << 8746;
    for (int i = 0; i < 8746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8747(int& val) {
    func_in_8747(val);
    const int x = 8747;
    std::cout << 8747;
    for (int i = 0; i < 8747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8747(int& val) {
    const int x = 8747;
    std::cout << 8747;
    for (int i = 0; i < 8747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8748(int& val) {
    func_in_8748(val);
    const int x = 8748;
    std::cout << 8748;
    for (int i = 0; i < 8748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8748(int& val) {
    const int x = 8748;
    std::cout << 8748;
    for (int i = 0; i < 8748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8749(int& val) {
    func_in_8749(val);
    const int x = 8749;
    std::cout << 8749;
    for (int i = 0; i < 8749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8749(int& val) {
    const int x = 8749;
    std::cout << 8749;
    for (int i = 0; i < 8749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8750(int& val) {
    func_in_8750(val);
    const int x = 8750;
    std::cout << 8750;
    for (int i = 0; i < 8750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8750(int& val) {
    const int x = 8750;
    std::cout << 8750;
    for (int i = 0; i < 8750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8751(int& val) {
    func_in_8751(val);
    const int x = 8751;
    std::cout << 8751;
    for (int i = 0; i < 8751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8751(int& val) {
    const int x = 8751;
    std::cout << 8751;
    for (int i = 0; i < 8751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8752(int& val) {
    func_in_8752(val);
    const int x = 8752;
    std::cout << 8752;
    for (int i = 0; i < 8752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8752(int& val) {
    const int x = 8752;
    std::cout << 8752;
    for (int i = 0; i < 8752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8753(int& val) {
    func_in_8753(val);
    const int x = 8753;
    std::cout << 8753;
    for (int i = 0; i < 8753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8753(int& val) {
    const int x = 8753;
    std::cout << 8753;
    for (int i = 0; i < 8753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8754(int& val) {
    func_in_8754(val);
    const int x = 8754;
    std::cout << 8754;
    for (int i = 0; i < 8754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8754(int& val) {
    const int x = 8754;
    std::cout << 8754;
    for (int i = 0; i < 8754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8755(int& val) {
    func_in_8755(val);
    const int x = 8755;
    std::cout << 8755;
    for (int i = 0; i < 8755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8755(int& val) {
    const int x = 8755;
    std::cout << 8755;
    for (int i = 0; i < 8755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8756(int& val) {
    func_in_8756(val);
    const int x = 8756;
    std::cout << 8756;
    for (int i = 0; i < 8756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8756(int& val) {
    const int x = 8756;
    std::cout << 8756;
    for (int i = 0; i < 8756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8757(int& val) {
    func_in_8757(val);
    const int x = 8757;
    std::cout << 8757;
    for (int i = 0; i < 8757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8757(int& val) {
    const int x = 8757;
    std::cout << 8757;
    for (int i = 0; i < 8757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8758(int& val) {
    func_in_8758(val);
    const int x = 8758;
    std::cout << 8758;
    for (int i = 0; i < 8758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8758(int& val) {
    const int x = 8758;
    std::cout << 8758;
    for (int i = 0; i < 8758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8759(int& val) {
    func_in_8759(val);
    const int x = 8759;
    std::cout << 8759;
    for (int i = 0; i < 8759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8759(int& val) {
    const int x = 8759;
    std::cout << 8759;
    for (int i = 0; i < 8759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8760(int& val) {
    func_in_8760(val);
    const int x = 8760;
    std::cout << 8760;
    for (int i = 0; i < 8760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8760(int& val) {
    const int x = 8760;
    std::cout << 8760;
    for (int i = 0; i < 8760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8761(int& val) {
    func_in_8761(val);
    const int x = 8761;
    std::cout << 8761;
    for (int i = 0; i < 8761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8761(int& val) {
    const int x = 8761;
    std::cout << 8761;
    for (int i = 0; i < 8761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8762(int& val) {
    func_in_8762(val);
    const int x = 8762;
    std::cout << 8762;
    for (int i = 0; i < 8762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8762(int& val) {
    const int x = 8762;
    std::cout << 8762;
    for (int i = 0; i < 8762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8763(int& val) {
    func_in_8763(val);
    const int x = 8763;
    std::cout << 8763;
    for (int i = 0; i < 8763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8763(int& val) {
    const int x = 8763;
    std::cout << 8763;
    for (int i = 0; i < 8763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8764(int& val) {
    func_in_8764(val);
    const int x = 8764;
    std::cout << 8764;
    for (int i = 0; i < 8764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8764(int& val) {
    const int x = 8764;
    std::cout << 8764;
    for (int i = 0; i < 8764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8765(int& val) {
    func_in_8765(val);
    const int x = 8765;
    std::cout << 8765;
    for (int i = 0; i < 8765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8765(int& val) {
    const int x = 8765;
    std::cout << 8765;
    for (int i = 0; i < 8765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8766(int& val) {
    func_in_8766(val);
    const int x = 8766;
    std::cout << 8766;
    for (int i = 0; i < 8766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8766(int& val) {
    const int x = 8766;
    std::cout << 8766;
    for (int i = 0; i < 8766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8767(int& val) {
    func_in_8767(val);
    const int x = 8767;
    std::cout << 8767;
    for (int i = 0; i < 8767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8767(int& val) {
    const int x = 8767;
    std::cout << 8767;
    for (int i = 0; i < 8767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8768(int& val) {
    func_in_8768(val);
    const int x = 8768;
    std::cout << 8768;
    for (int i = 0; i < 8768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8768(int& val) {
    const int x = 8768;
    std::cout << 8768;
    for (int i = 0; i < 8768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8769(int& val) {
    func_in_8769(val);
    const int x = 8769;
    std::cout << 8769;
    for (int i = 0; i < 8769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8769(int& val) {
    const int x = 8769;
    std::cout << 8769;
    for (int i = 0; i < 8769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8770(int& val) {
    func_in_8770(val);
    const int x = 8770;
    std::cout << 8770;
    for (int i = 0; i < 8770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8770(int& val) {
    const int x = 8770;
    std::cout << 8770;
    for (int i = 0; i < 8770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8771(int& val) {
    func_in_8771(val);
    const int x = 8771;
    std::cout << 8771;
    for (int i = 0; i < 8771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8771(int& val) {
    const int x = 8771;
    std::cout << 8771;
    for (int i = 0; i < 8771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8772(int& val) {
    func_in_8772(val);
    const int x = 8772;
    std::cout << 8772;
    for (int i = 0; i < 8772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8772(int& val) {
    const int x = 8772;
    std::cout << 8772;
    for (int i = 0; i < 8772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8773(int& val) {
    func_in_8773(val);
    const int x = 8773;
    std::cout << 8773;
    for (int i = 0; i < 8773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8773(int& val) {
    const int x = 8773;
    std::cout << 8773;
    for (int i = 0; i < 8773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8774(int& val) {
    func_in_8774(val);
    const int x = 8774;
    std::cout << 8774;
    for (int i = 0; i < 8774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8774(int& val) {
    const int x = 8774;
    std::cout << 8774;
    for (int i = 0; i < 8774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8775(int& val) {
    func_in_8775(val);
    const int x = 8775;
    std::cout << 8775;
    for (int i = 0; i < 8775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8775(int& val) {
    const int x = 8775;
    std::cout << 8775;
    for (int i = 0; i < 8775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8776(int& val) {
    func_in_8776(val);
    const int x = 8776;
    std::cout << 8776;
    for (int i = 0; i < 8776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8776(int& val) {
    const int x = 8776;
    std::cout << 8776;
    for (int i = 0; i < 8776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8777(int& val) {
    func_in_8777(val);
    const int x = 8777;
    std::cout << 8777;
    for (int i = 0; i < 8777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8777(int& val) {
    const int x = 8777;
    std::cout << 8777;
    for (int i = 0; i < 8777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8778(int& val) {
    func_in_8778(val);
    const int x = 8778;
    std::cout << 8778;
    for (int i = 0; i < 8778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8778(int& val) {
    const int x = 8778;
    std::cout << 8778;
    for (int i = 0; i < 8778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8779(int& val) {
    func_in_8779(val);
    const int x = 8779;
    std::cout << 8779;
    for (int i = 0; i < 8779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8779(int& val) {
    const int x = 8779;
    std::cout << 8779;
    for (int i = 0; i < 8779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8780(int& val) {
    func_in_8780(val);
    const int x = 8780;
    std::cout << 8780;
    for (int i = 0; i < 8780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8780(int& val) {
    const int x = 8780;
    std::cout << 8780;
    for (int i = 0; i < 8780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8781(int& val) {
    func_in_8781(val);
    const int x = 8781;
    std::cout << 8781;
    for (int i = 0; i < 8781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8781(int& val) {
    const int x = 8781;
    std::cout << 8781;
    for (int i = 0; i < 8781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8782(int& val) {
    func_in_8782(val);
    const int x = 8782;
    std::cout << 8782;
    for (int i = 0; i < 8782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8782(int& val) {
    const int x = 8782;
    std::cout << 8782;
    for (int i = 0; i < 8782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8783(int& val) {
    func_in_8783(val);
    const int x = 8783;
    std::cout << 8783;
    for (int i = 0; i < 8783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8783(int& val) {
    const int x = 8783;
    std::cout << 8783;
    for (int i = 0; i < 8783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8784(int& val) {
    func_in_8784(val);
    const int x = 8784;
    std::cout << 8784;
    for (int i = 0; i < 8784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8784(int& val) {
    const int x = 8784;
    std::cout << 8784;
    for (int i = 0; i < 8784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8785(int& val) {
    func_in_8785(val);
    const int x = 8785;
    std::cout << 8785;
    for (int i = 0; i < 8785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8785(int& val) {
    const int x = 8785;
    std::cout << 8785;
    for (int i = 0; i < 8785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8786(int& val) {
    func_in_8786(val);
    const int x = 8786;
    std::cout << 8786;
    for (int i = 0; i < 8786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8786(int& val) {
    const int x = 8786;
    std::cout << 8786;
    for (int i = 0; i < 8786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8787(int& val) {
    func_in_8787(val);
    const int x = 8787;
    std::cout << 8787;
    for (int i = 0; i < 8787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8787(int& val) {
    const int x = 8787;
    std::cout << 8787;
    for (int i = 0; i < 8787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8788(int& val) {
    func_in_8788(val);
    const int x = 8788;
    std::cout << 8788;
    for (int i = 0; i < 8788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8788(int& val) {
    const int x = 8788;
    std::cout << 8788;
    for (int i = 0; i < 8788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8789(int& val) {
    func_in_8789(val);
    const int x = 8789;
    std::cout << 8789;
    for (int i = 0; i < 8789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8789(int& val) {
    const int x = 8789;
    std::cout << 8789;
    for (int i = 0; i < 8789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8790(int& val) {
    func_in_8790(val);
    const int x = 8790;
    std::cout << 8790;
    for (int i = 0; i < 8790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8790(int& val) {
    const int x = 8790;
    std::cout << 8790;
    for (int i = 0; i < 8790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8791(int& val) {
    func_in_8791(val);
    const int x = 8791;
    std::cout << 8791;
    for (int i = 0; i < 8791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8791(int& val) {
    const int x = 8791;
    std::cout << 8791;
    for (int i = 0; i < 8791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8792(int& val) {
    func_in_8792(val);
    const int x = 8792;
    std::cout << 8792;
    for (int i = 0; i < 8792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8792(int& val) {
    const int x = 8792;
    std::cout << 8792;
    for (int i = 0; i < 8792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8793(int& val) {
    func_in_8793(val);
    const int x = 8793;
    std::cout << 8793;
    for (int i = 0; i < 8793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8793(int& val) {
    const int x = 8793;
    std::cout << 8793;
    for (int i = 0; i < 8793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8794(int& val) {
    func_in_8794(val);
    const int x = 8794;
    std::cout << 8794;
    for (int i = 0; i < 8794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8794(int& val) {
    const int x = 8794;
    std::cout << 8794;
    for (int i = 0; i < 8794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8795(int& val) {
    func_in_8795(val);
    const int x = 8795;
    std::cout << 8795;
    for (int i = 0; i < 8795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8795(int& val) {
    const int x = 8795;
    std::cout << 8795;
    for (int i = 0; i < 8795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8796(int& val) {
    func_in_8796(val);
    const int x = 8796;
    std::cout << 8796;
    for (int i = 0; i < 8796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8796(int& val) {
    const int x = 8796;
    std::cout << 8796;
    for (int i = 0; i < 8796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8797(int& val) {
    func_in_8797(val);
    const int x = 8797;
    std::cout << 8797;
    for (int i = 0; i < 8797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8797(int& val) {
    const int x = 8797;
    std::cout << 8797;
    for (int i = 0; i < 8797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8798(int& val) {
    func_in_8798(val);
    const int x = 8798;
    std::cout << 8798;
    for (int i = 0; i < 8798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8798(int& val) {
    const int x = 8798;
    std::cout << 8798;
    for (int i = 0; i < 8798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8799(int& val) {
    func_in_8799(val);
    const int x = 8799;
    std::cout << 8799;
    for (int i = 0; i < 8799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8799(int& val) {
    const int x = 8799;
    std::cout << 8799;
    for (int i = 0; i < 8799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8800(int& val) {
    func_in_8800(val);
    const int x = 8800;
    std::cout << 8800;
    for (int i = 0; i < 8800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8800(int& val) {
    const int x = 8800;
    std::cout << 8800;
    for (int i = 0; i < 8800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8801(int& val) {
    func_in_8801(val);
    const int x = 8801;
    std::cout << 8801;
    for (int i = 0; i < 8801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8801(int& val) {
    const int x = 8801;
    std::cout << 8801;
    for (int i = 0; i < 8801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8802(int& val) {
    func_in_8802(val);
    const int x = 8802;
    std::cout << 8802;
    for (int i = 0; i < 8802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8802(int& val) {
    const int x = 8802;
    std::cout << 8802;
    for (int i = 0; i < 8802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8803(int& val) {
    func_in_8803(val);
    const int x = 8803;
    std::cout << 8803;
    for (int i = 0; i < 8803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8803(int& val) {
    const int x = 8803;
    std::cout << 8803;
    for (int i = 0; i < 8803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8804(int& val) {
    func_in_8804(val);
    const int x = 8804;
    std::cout << 8804;
    for (int i = 0; i < 8804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8804(int& val) {
    const int x = 8804;
    std::cout << 8804;
    for (int i = 0; i < 8804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8805(int& val) {
    func_in_8805(val);
    const int x = 8805;
    std::cout << 8805;
    for (int i = 0; i < 8805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8805(int& val) {
    const int x = 8805;
    std::cout << 8805;
    for (int i = 0; i < 8805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8806(int& val) {
    func_in_8806(val);
    const int x = 8806;
    std::cout << 8806;
    for (int i = 0; i < 8806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8806(int& val) {
    const int x = 8806;
    std::cout << 8806;
    for (int i = 0; i < 8806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8807(int& val) {
    func_in_8807(val);
    const int x = 8807;
    std::cout << 8807;
    for (int i = 0; i < 8807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8807(int& val) {
    const int x = 8807;
    std::cout << 8807;
    for (int i = 0; i < 8807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8808(int& val) {
    func_in_8808(val);
    const int x = 8808;
    std::cout << 8808;
    for (int i = 0; i < 8808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8808(int& val) {
    const int x = 8808;
    std::cout << 8808;
    for (int i = 0; i < 8808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8809(int& val) {
    func_in_8809(val);
    const int x = 8809;
    std::cout << 8809;
    for (int i = 0; i < 8809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8809(int& val) {
    const int x = 8809;
    std::cout << 8809;
    for (int i = 0; i < 8809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8810(int& val) {
    func_in_8810(val);
    const int x = 8810;
    std::cout << 8810;
    for (int i = 0; i < 8810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8810(int& val) {
    const int x = 8810;
    std::cout << 8810;
    for (int i = 0; i < 8810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8811(int& val) {
    func_in_8811(val);
    const int x = 8811;
    std::cout << 8811;
    for (int i = 0; i < 8811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8811(int& val) {
    const int x = 8811;
    std::cout << 8811;
    for (int i = 0; i < 8811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8812(int& val) {
    func_in_8812(val);
    const int x = 8812;
    std::cout << 8812;
    for (int i = 0; i < 8812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8812(int& val) {
    const int x = 8812;
    std::cout << 8812;
    for (int i = 0; i < 8812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8813(int& val) {
    func_in_8813(val);
    const int x = 8813;
    std::cout << 8813;
    for (int i = 0; i < 8813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8813(int& val) {
    const int x = 8813;
    std::cout << 8813;
    for (int i = 0; i < 8813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8814(int& val) {
    func_in_8814(val);
    const int x = 8814;
    std::cout << 8814;
    for (int i = 0; i < 8814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8814(int& val) {
    const int x = 8814;
    std::cout << 8814;
    for (int i = 0; i < 8814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8815(int& val) {
    func_in_8815(val);
    const int x = 8815;
    std::cout << 8815;
    for (int i = 0; i < 8815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8815(int& val) {
    const int x = 8815;
    std::cout << 8815;
    for (int i = 0; i < 8815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8816(int& val) {
    func_in_8816(val);
    const int x = 8816;
    std::cout << 8816;
    for (int i = 0; i < 8816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8816(int& val) {
    const int x = 8816;
    std::cout << 8816;
    for (int i = 0; i < 8816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8817(int& val) {
    func_in_8817(val);
    const int x = 8817;
    std::cout << 8817;
    for (int i = 0; i < 8817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8817(int& val) {
    const int x = 8817;
    std::cout << 8817;
    for (int i = 0; i < 8817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8818(int& val) {
    func_in_8818(val);
    const int x = 8818;
    std::cout << 8818;
    for (int i = 0; i < 8818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8818(int& val) {
    const int x = 8818;
    std::cout << 8818;
    for (int i = 0; i < 8818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8819(int& val) {
    func_in_8819(val);
    const int x = 8819;
    std::cout << 8819;
    for (int i = 0; i < 8819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8819(int& val) {
    const int x = 8819;
    std::cout << 8819;
    for (int i = 0; i < 8819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8820(int& val) {
    func_in_8820(val);
    const int x = 8820;
    std::cout << 8820;
    for (int i = 0; i < 8820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8820(int& val) {
    const int x = 8820;
    std::cout << 8820;
    for (int i = 0; i < 8820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8821(int& val) {
    func_in_8821(val);
    const int x = 8821;
    std::cout << 8821;
    for (int i = 0; i < 8821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8821(int& val) {
    const int x = 8821;
    std::cout << 8821;
    for (int i = 0; i < 8821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8822(int& val) {
    func_in_8822(val);
    const int x = 8822;
    std::cout << 8822;
    for (int i = 0; i < 8822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8822(int& val) {
    const int x = 8822;
    std::cout << 8822;
    for (int i = 0; i < 8822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8823(int& val) {
    func_in_8823(val);
    const int x = 8823;
    std::cout << 8823;
    for (int i = 0; i < 8823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8823(int& val) {
    const int x = 8823;
    std::cout << 8823;
    for (int i = 0; i < 8823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8824(int& val) {
    func_in_8824(val);
    const int x = 8824;
    std::cout << 8824;
    for (int i = 0; i < 8824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8824(int& val) {
    const int x = 8824;
    std::cout << 8824;
    for (int i = 0; i < 8824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8825(int& val) {
    func_in_8825(val);
    const int x = 8825;
    std::cout << 8825;
    for (int i = 0; i < 8825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8825(int& val) {
    const int x = 8825;
    std::cout << 8825;
    for (int i = 0; i < 8825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8826(int& val) {
    func_in_8826(val);
    const int x = 8826;
    std::cout << 8826;
    for (int i = 0; i < 8826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8826(int& val) {
    const int x = 8826;
    std::cout << 8826;
    for (int i = 0; i < 8826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8827(int& val) {
    func_in_8827(val);
    const int x = 8827;
    std::cout << 8827;
    for (int i = 0; i < 8827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8827(int& val) {
    const int x = 8827;
    std::cout << 8827;
    for (int i = 0; i < 8827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8828(int& val) {
    func_in_8828(val);
    const int x = 8828;
    std::cout << 8828;
    for (int i = 0; i < 8828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8828(int& val) {
    const int x = 8828;
    std::cout << 8828;
    for (int i = 0; i < 8828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8829(int& val) {
    func_in_8829(val);
    const int x = 8829;
    std::cout << 8829;
    for (int i = 0; i < 8829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8829(int& val) {
    const int x = 8829;
    std::cout << 8829;
    for (int i = 0; i < 8829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8830(int& val) {
    func_in_8830(val);
    const int x = 8830;
    std::cout << 8830;
    for (int i = 0; i < 8830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8830(int& val) {
    const int x = 8830;
    std::cout << 8830;
    for (int i = 0; i < 8830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8831(int& val) {
    func_in_8831(val);
    const int x = 8831;
    std::cout << 8831;
    for (int i = 0; i < 8831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8831(int& val) {
    const int x = 8831;
    std::cout << 8831;
    for (int i = 0; i < 8831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8832(int& val) {
    func_in_8832(val);
    const int x = 8832;
    std::cout << 8832;
    for (int i = 0; i < 8832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8832(int& val) {
    const int x = 8832;
    std::cout << 8832;
    for (int i = 0; i < 8832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8833(int& val) {
    func_in_8833(val);
    const int x = 8833;
    std::cout << 8833;
    for (int i = 0; i < 8833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8833(int& val) {
    const int x = 8833;
    std::cout << 8833;
    for (int i = 0; i < 8833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8834(int& val) {
    func_in_8834(val);
    const int x = 8834;
    std::cout << 8834;
    for (int i = 0; i < 8834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8834(int& val) {
    const int x = 8834;
    std::cout << 8834;
    for (int i = 0; i < 8834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8835(int& val) {
    func_in_8835(val);
    const int x = 8835;
    std::cout << 8835;
    for (int i = 0; i < 8835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8835(int& val) {
    const int x = 8835;
    std::cout << 8835;
    for (int i = 0; i < 8835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8836(int& val) {
    func_in_8836(val);
    const int x = 8836;
    std::cout << 8836;
    for (int i = 0; i < 8836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8836(int& val) {
    const int x = 8836;
    std::cout << 8836;
    for (int i = 0; i < 8836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8837(int& val) {
    func_in_8837(val);
    const int x = 8837;
    std::cout << 8837;
    for (int i = 0; i < 8837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8837(int& val) {
    const int x = 8837;
    std::cout << 8837;
    for (int i = 0; i < 8837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8838(int& val) {
    func_in_8838(val);
    const int x = 8838;
    std::cout << 8838;
    for (int i = 0; i < 8838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8838(int& val) {
    const int x = 8838;
    std::cout << 8838;
    for (int i = 0; i < 8838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8839(int& val) {
    func_in_8839(val);
    const int x = 8839;
    std::cout << 8839;
    for (int i = 0; i < 8839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8839(int& val) {
    const int x = 8839;
    std::cout << 8839;
    for (int i = 0; i < 8839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8840(int& val) {
    func_in_8840(val);
    const int x = 8840;
    std::cout << 8840;
    for (int i = 0; i < 8840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8840(int& val) {
    const int x = 8840;
    std::cout << 8840;
    for (int i = 0; i < 8840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8841(int& val) {
    func_in_8841(val);
    const int x = 8841;
    std::cout << 8841;
    for (int i = 0; i < 8841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8841(int& val) {
    const int x = 8841;
    std::cout << 8841;
    for (int i = 0; i < 8841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8842(int& val) {
    func_in_8842(val);
    const int x = 8842;
    std::cout << 8842;
    for (int i = 0; i < 8842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8842(int& val) {
    const int x = 8842;
    std::cout << 8842;
    for (int i = 0; i < 8842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8843(int& val) {
    func_in_8843(val);
    const int x = 8843;
    std::cout << 8843;
    for (int i = 0; i < 8843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8843(int& val) {
    const int x = 8843;
    std::cout << 8843;
    for (int i = 0; i < 8843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8844(int& val) {
    func_in_8844(val);
    const int x = 8844;
    std::cout << 8844;
    for (int i = 0; i < 8844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8844(int& val) {
    const int x = 8844;
    std::cout << 8844;
    for (int i = 0; i < 8844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8845(int& val) {
    func_in_8845(val);
    const int x = 8845;
    std::cout << 8845;
    for (int i = 0; i < 8845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8845(int& val) {
    const int x = 8845;
    std::cout << 8845;
    for (int i = 0; i < 8845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8846(int& val) {
    func_in_8846(val);
    const int x = 8846;
    std::cout << 8846;
    for (int i = 0; i < 8846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8846(int& val) {
    const int x = 8846;
    std::cout << 8846;
    for (int i = 0; i < 8846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8847(int& val) {
    func_in_8847(val);
    const int x = 8847;
    std::cout << 8847;
    for (int i = 0; i < 8847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8847(int& val) {
    const int x = 8847;
    std::cout << 8847;
    for (int i = 0; i < 8847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8848(int& val) {
    func_in_8848(val);
    const int x = 8848;
    std::cout << 8848;
    for (int i = 0; i < 8848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8848(int& val) {
    const int x = 8848;
    std::cout << 8848;
    for (int i = 0; i < 8848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8849(int& val) {
    func_in_8849(val);
    const int x = 8849;
    std::cout << 8849;
    for (int i = 0; i < 8849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8849(int& val) {
    const int x = 8849;
    std::cout << 8849;
    for (int i = 0; i < 8849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8850(int& val) {
    func_in_8850(val);
    const int x = 8850;
    std::cout << 8850;
    for (int i = 0; i < 8850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8850(int& val) {
    const int x = 8850;
    std::cout << 8850;
    for (int i = 0; i < 8850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8851(int& val) {
    func_in_8851(val);
    const int x = 8851;
    std::cout << 8851;
    for (int i = 0; i < 8851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8851(int& val) {
    const int x = 8851;
    std::cout << 8851;
    for (int i = 0; i < 8851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8852(int& val) {
    func_in_8852(val);
    const int x = 8852;
    std::cout << 8852;
    for (int i = 0; i < 8852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8852(int& val) {
    const int x = 8852;
    std::cout << 8852;
    for (int i = 0; i < 8852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8853(int& val) {
    func_in_8853(val);
    const int x = 8853;
    std::cout << 8853;
    for (int i = 0; i < 8853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8853(int& val) {
    const int x = 8853;
    std::cout << 8853;
    for (int i = 0; i < 8853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8854(int& val) {
    func_in_8854(val);
    const int x = 8854;
    std::cout << 8854;
    for (int i = 0; i < 8854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8854(int& val) {
    const int x = 8854;
    std::cout << 8854;
    for (int i = 0; i < 8854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8855(int& val) {
    func_in_8855(val);
    const int x = 8855;
    std::cout << 8855;
    for (int i = 0; i < 8855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8855(int& val) {
    const int x = 8855;
    std::cout << 8855;
    for (int i = 0; i < 8855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8856(int& val) {
    func_in_8856(val);
    const int x = 8856;
    std::cout << 8856;
    for (int i = 0; i < 8856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8856(int& val) {
    const int x = 8856;
    std::cout << 8856;
    for (int i = 0; i < 8856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8857(int& val) {
    func_in_8857(val);
    const int x = 8857;
    std::cout << 8857;
    for (int i = 0; i < 8857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8857(int& val) {
    const int x = 8857;
    std::cout << 8857;
    for (int i = 0; i < 8857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8858(int& val) {
    func_in_8858(val);
    const int x = 8858;
    std::cout << 8858;
    for (int i = 0; i < 8858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8858(int& val) {
    const int x = 8858;
    std::cout << 8858;
    for (int i = 0; i < 8858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8859(int& val) {
    func_in_8859(val);
    const int x = 8859;
    std::cout << 8859;
    for (int i = 0; i < 8859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8859(int& val) {
    const int x = 8859;
    std::cout << 8859;
    for (int i = 0; i < 8859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8860(int& val) {
    func_in_8860(val);
    const int x = 8860;
    std::cout << 8860;
    for (int i = 0; i < 8860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8860(int& val) {
    const int x = 8860;
    std::cout << 8860;
    for (int i = 0; i < 8860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8861(int& val) {
    func_in_8861(val);
    const int x = 8861;
    std::cout << 8861;
    for (int i = 0; i < 8861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8861(int& val) {
    const int x = 8861;
    std::cout << 8861;
    for (int i = 0; i < 8861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8862(int& val) {
    func_in_8862(val);
    const int x = 8862;
    std::cout << 8862;
    for (int i = 0; i < 8862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8862(int& val) {
    const int x = 8862;
    std::cout << 8862;
    for (int i = 0; i < 8862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8863(int& val) {
    func_in_8863(val);
    const int x = 8863;
    std::cout << 8863;
    for (int i = 0; i < 8863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8863(int& val) {
    const int x = 8863;
    std::cout << 8863;
    for (int i = 0; i < 8863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8864(int& val) {
    func_in_8864(val);
    const int x = 8864;
    std::cout << 8864;
    for (int i = 0; i < 8864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8864(int& val) {
    const int x = 8864;
    std::cout << 8864;
    for (int i = 0; i < 8864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8865(int& val) {
    func_in_8865(val);
    const int x = 8865;
    std::cout << 8865;
    for (int i = 0; i < 8865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8865(int& val) {
    const int x = 8865;
    std::cout << 8865;
    for (int i = 0; i < 8865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8866(int& val) {
    func_in_8866(val);
    const int x = 8866;
    std::cout << 8866;
    for (int i = 0; i < 8866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8866(int& val) {
    const int x = 8866;
    std::cout << 8866;
    for (int i = 0; i < 8866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8867(int& val) {
    func_in_8867(val);
    const int x = 8867;
    std::cout << 8867;
    for (int i = 0; i < 8867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8867(int& val) {
    const int x = 8867;
    std::cout << 8867;
    for (int i = 0; i < 8867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8868(int& val) {
    func_in_8868(val);
    const int x = 8868;
    std::cout << 8868;
    for (int i = 0; i < 8868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8868(int& val) {
    const int x = 8868;
    std::cout << 8868;
    for (int i = 0; i < 8868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8869(int& val) {
    func_in_8869(val);
    const int x = 8869;
    std::cout << 8869;
    for (int i = 0; i < 8869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8869(int& val) {
    const int x = 8869;
    std::cout << 8869;
    for (int i = 0; i < 8869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8870(int& val) {
    func_in_8870(val);
    const int x = 8870;
    std::cout << 8870;
    for (int i = 0; i < 8870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8870(int& val) {
    const int x = 8870;
    std::cout << 8870;
    for (int i = 0; i < 8870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8871(int& val) {
    func_in_8871(val);
    const int x = 8871;
    std::cout << 8871;
    for (int i = 0; i < 8871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8871(int& val) {
    const int x = 8871;
    std::cout << 8871;
    for (int i = 0; i < 8871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8872(int& val) {
    func_in_8872(val);
    const int x = 8872;
    std::cout << 8872;
    for (int i = 0; i < 8872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8872(int& val) {
    const int x = 8872;
    std::cout << 8872;
    for (int i = 0; i < 8872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8873(int& val) {
    func_in_8873(val);
    const int x = 8873;
    std::cout << 8873;
    for (int i = 0; i < 8873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8873(int& val) {
    const int x = 8873;
    std::cout << 8873;
    for (int i = 0; i < 8873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8874(int& val) {
    func_in_8874(val);
    const int x = 8874;
    std::cout << 8874;
    for (int i = 0; i < 8874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8874(int& val) {
    const int x = 8874;
    std::cout << 8874;
    for (int i = 0; i < 8874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8875(int& val) {
    func_in_8875(val);
    const int x = 8875;
    std::cout << 8875;
    for (int i = 0; i < 8875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8875(int& val) {
    const int x = 8875;
    std::cout << 8875;
    for (int i = 0; i < 8875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8876(int& val) {
    func_in_8876(val);
    const int x = 8876;
    std::cout << 8876;
    for (int i = 0; i < 8876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8876(int& val) {
    const int x = 8876;
    std::cout << 8876;
    for (int i = 0; i < 8876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8877(int& val) {
    func_in_8877(val);
    const int x = 8877;
    std::cout << 8877;
    for (int i = 0; i < 8877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8877(int& val) {
    const int x = 8877;
    std::cout << 8877;
    for (int i = 0; i < 8877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8878(int& val) {
    func_in_8878(val);
    const int x = 8878;
    std::cout << 8878;
    for (int i = 0; i < 8878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8878(int& val) {
    const int x = 8878;
    std::cout << 8878;
    for (int i = 0; i < 8878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8879(int& val) {
    func_in_8879(val);
    const int x = 8879;
    std::cout << 8879;
    for (int i = 0; i < 8879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8879(int& val) {
    const int x = 8879;
    std::cout << 8879;
    for (int i = 0; i < 8879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8880(int& val) {
    func_in_8880(val);
    const int x = 8880;
    std::cout << 8880;
    for (int i = 0; i < 8880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8880(int& val) {
    const int x = 8880;
    std::cout << 8880;
    for (int i = 0; i < 8880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8881(int& val) {
    func_in_8881(val);
    const int x = 8881;
    std::cout << 8881;
    for (int i = 0; i < 8881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8881(int& val) {
    const int x = 8881;
    std::cout << 8881;
    for (int i = 0; i < 8881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8882(int& val) {
    func_in_8882(val);
    const int x = 8882;
    std::cout << 8882;
    for (int i = 0; i < 8882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8882(int& val) {
    const int x = 8882;
    std::cout << 8882;
    for (int i = 0; i < 8882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8883(int& val) {
    func_in_8883(val);
    const int x = 8883;
    std::cout << 8883;
    for (int i = 0; i < 8883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8883(int& val) {
    const int x = 8883;
    std::cout << 8883;
    for (int i = 0; i < 8883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8884(int& val) {
    func_in_8884(val);
    const int x = 8884;
    std::cout << 8884;
    for (int i = 0; i < 8884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8884(int& val) {
    const int x = 8884;
    std::cout << 8884;
    for (int i = 0; i < 8884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8885(int& val) {
    func_in_8885(val);
    const int x = 8885;
    std::cout << 8885;
    for (int i = 0; i < 8885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8885(int& val) {
    const int x = 8885;
    std::cout << 8885;
    for (int i = 0; i < 8885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8886(int& val) {
    func_in_8886(val);
    const int x = 8886;
    std::cout << 8886;
    for (int i = 0; i < 8886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8886(int& val) {
    const int x = 8886;
    std::cout << 8886;
    for (int i = 0; i < 8886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8887(int& val) {
    func_in_8887(val);
    const int x = 8887;
    std::cout << 8887;
    for (int i = 0; i < 8887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8887(int& val) {
    const int x = 8887;
    std::cout << 8887;
    for (int i = 0; i < 8887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8888(int& val) {
    func_in_8888(val);
    const int x = 8888;
    std::cout << 8888;
    for (int i = 0; i < 8888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8888(int& val) {
    const int x = 8888;
    std::cout << 8888;
    for (int i = 0; i < 8888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8889(int& val) {
    func_in_8889(val);
    const int x = 8889;
    std::cout << 8889;
    for (int i = 0; i < 8889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8889(int& val) {
    const int x = 8889;
    std::cout << 8889;
    for (int i = 0; i < 8889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8890(int& val) {
    func_in_8890(val);
    const int x = 8890;
    std::cout << 8890;
    for (int i = 0; i < 8890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8890(int& val) {
    const int x = 8890;
    std::cout << 8890;
    for (int i = 0; i < 8890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8891(int& val) {
    func_in_8891(val);
    const int x = 8891;
    std::cout << 8891;
    for (int i = 0; i < 8891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8891(int& val) {
    const int x = 8891;
    std::cout << 8891;
    for (int i = 0; i < 8891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8892(int& val) {
    func_in_8892(val);
    const int x = 8892;
    std::cout << 8892;
    for (int i = 0; i < 8892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8892(int& val) {
    const int x = 8892;
    std::cout << 8892;
    for (int i = 0; i < 8892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8893(int& val) {
    func_in_8893(val);
    const int x = 8893;
    std::cout << 8893;
    for (int i = 0; i < 8893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8893(int& val) {
    const int x = 8893;
    std::cout << 8893;
    for (int i = 0; i < 8893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8894(int& val) {
    func_in_8894(val);
    const int x = 8894;
    std::cout << 8894;
    for (int i = 0; i < 8894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8894(int& val) {
    const int x = 8894;
    std::cout << 8894;
    for (int i = 0; i < 8894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8895(int& val) {
    func_in_8895(val);
    const int x = 8895;
    std::cout << 8895;
    for (int i = 0; i < 8895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8895(int& val) {
    const int x = 8895;
    std::cout << 8895;
    for (int i = 0; i < 8895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8896(int& val) {
    func_in_8896(val);
    const int x = 8896;
    std::cout << 8896;
    for (int i = 0; i < 8896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8896(int& val) {
    const int x = 8896;
    std::cout << 8896;
    for (int i = 0; i < 8896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8897(int& val) {
    func_in_8897(val);
    const int x = 8897;
    std::cout << 8897;
    for (int i = 0; i < 8897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8897(int& val) {
    const int x = 8897;
    std::cout << 8897;
    for (int i = 0; i < 8897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8898(int& val) {
    func_in_8898(val);
    const int x = 8898;
    std::cout << 8898;
    for (int i = 0; i < 8898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8898(int& val) {
    const int x = 8898;
    std::cout << 8898;
    for (int i = 0; i < 8898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8899(int& val) {
    func_in_8899(val);
    const int x = 8899;
    std::cout << 8899;
    for (int i = 0; i < 8899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8899(int& val) {
    const int x = 8899;
    std::cout << 8899;
    for (int i = 0; i < 8899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8900(int& val) {
    func_in_8900(val);
    const int x = 8900;
    std::cout << 8900;
    for (int i = 0; i < 8900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8900(int& val) {
    const int x = 8900;
    std::cout << 8900;
    for (int i = 0; i < 8900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8901(int& val) {
    func_in_8901(val);
    const int x = 8901;
    std::cout << 8901;
    for (int i = 0; i < 8901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8901(int& val) {
    const int x = 8901;
    std::cout << 8901;
    for (int i = 0; i < 8901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8902(int& val) {
    func_in_8902(val);
    const int x = 8902;
    std::cout << 8902;
    for (int i = 0; i < 8902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8902(int& val) {
    const int x = 8902;
    std::cout << 8902;
    for (int i = 0; i < 8902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8903(int& val) {
    func_in_8903(val);
    const int x = 8903;
    std::cout << 8903;
    for (int i = 0; i < 8903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8903(int& val) {
    const int x = 8903;
    std::cout << 8903;
    for (int i = 0; i < 8903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8904(int& val) {
    func_in_8904(val);
    const int x = 8904;
    std::cout << 8904;
    for (int i = 0; i < 8904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8904(int& val) {
    const int x = 8904;
    std::cout << 8904;
    for (int i = 0; i < 8904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8905(int& val) {
    func_in_8905(val);
    const int x = 8905;
    std::cout << 8905;
    for (int i = 0; i < 8905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8905(int& val) {
    const int x = 8905;
    std::cout << 8905;
    for (int i = 0; i < 8905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8906(int& val) {
    func_in_8906(val);
    const int x = 8906;
    std::cout << 8906;
    for (int i = 0; i < 8906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8906(int& val) {
    const int x = 8906;
    std::cout << 8906;
    for (int i = 0; i < 8906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8907(int& val) {
    func_in_8907(val);
    const int x = 8907;
    std::cout << 8907;
    for (int i = 0; i < 8907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8907(int& val) {
    const int x = 8907;
    std::cout << 8907;
    for (int i = 0; i < 8907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8908(int& val) {
    func_in_8908(val);
    const int x = 8908;
    std::cout << 8908;
    for (int i = 0; i < 8908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8908(int& val) {
    const int x = 8908;
    std::cout << 8908;
    for (int i = 0; i < 8908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8909(int& val) {
    func_in_8909(val);
    const int x = 8909;
    std::cout << 8909;
    for (int i = 0; i < 8909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8909(int& val) {
    const int x = 8909;
    std::cout << 8909;
    for (int i = 0; i < 8909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8910(int& val) {
    func_in_8910(val);
    const int x = 8910;
    std::cout << 8910;
    for (int i = 0; i < 8910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8910(int& val) {
    const int x = 8910;
    std::cout << 8910;
    for (int i = 0; i < 8910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8911(int& val) {
    func_in_8911(val);
    const int x = 8911;
    std::cout << 8911;
    for (int i = 0; i < 8911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8911(int& val) {
    const int x = 8911;
    std::cout << 8911;
    for (int i = 0; i < 8911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8912(int& val) {
    func_in_8912(val);
    const int x = 8912;
    std::cout << 8912;
    for (int i = 0; i < 8912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8912(int& val) {
    const int x = 8912;
    std::cout << 8912;
    for (int i = 0; i < 8912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8913(int& val) {
    func_in_8913(val);
    const int x = 8913;
    std::cout << 8913;
    for (int i = 0; i < 8913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8913(int& val) {
    const int x = 8913;
    std::cout << 8913;
    for (int i = 0; i < 8913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8914(int& val) {
    func_in_8914(val);
    const int x = 8914;
    std::cout << 8914;
    for (int i = 0; i < 8914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8914(int& val) {
    const int x = 8914;
    std::cout << 8914;
    for (int i = 0; i < 8914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8915(int& val) {
    func_in_8915(val);
    const int x = 8915;
    std::cout << 8915;
    for (int i = 0; i < 8915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8915(int& val) {
    const int x = 8915;
    std::cout << 8915;
    for (int i = 0; i < 8915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8916(int& val) {
    func_in_8916(val);
    const int x = 8916;
    std::cout << 8916;
    for (int i = 0; i < 8916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8916(int& val) {
    const int x = 8916;
    std::cout << 8916;
    for (int i = 0; i < 8916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8917(int& val) {
    func_in_8917(val);
    const int x = 8917;
    std::cout << 8917;
    for (int i = 0; i < 8917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8917(int& val) {
    const int x = 8917;
    std::cout << 8917;
    for (int i = 0; i < 8917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8918(int& val) {
    func_in_8918(val);
    const int x = 8918;
    std::cout << 8918;
    for (int i = 0; i < 8918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8918(int& val) {
    const int x = 8918;
    std::cout << 8918;
    for (int i = 0; i < 8918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8919(int& val) {
    func_in_8919(val);
    const int x = 8919;
    std::cout << 8919;
    for (int i = 0; i < 8919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8919(int& val) {
    const int x = 8919;
    std::cout << 8919;
    for (int i = 0; i < 8919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8920(int& val) {
    func_in_8920(val);
    const int x = 8920;
    std::cout << 8920;
    for (int i = 0; i < 8920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8920(int& val) {
    const int x = 8920;
    std::cout << 8920;
    for (int i = 0; i < 8920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8921(int& val) {
    func_in_8921(val);
    const int x = 8921;
    std::cout << 8921;
    for (int i = 0; i < 8921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8921(int& val) {
    const int x = 8921;
    std::cout << 8921;
    for (int i = 0; i < 8921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8922(int& val) {
    func_in_8922(val);
    const int x = 8922;
    std::cout << 8922;
    for (int i = 0; i < 8922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8922(int& val) {
    const int x = 8922;
    std::cout << 8922;
    for (int i = 0; i < 8922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8923(int& val) {
    func_in_8923(val);
    const int x = 8923;
    std::cout << 8923;
    for (int i = 0; i < 8923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8923(int& val) {
    const int x = 8923;
    std::cout << 8923;
    for (int i = 0; i < 8923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8924(int& val) {
    func_in_8924(val);
    const int x = 8924;
    std::cout << 8924;
    for (int i = 0; i < 8924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8924(int& val) {
    const int x = 8924;
    std::cout << 8924;
    for (int i = 0; i < 8924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8925(int& val) {
    func_in_8925(val);
    const int x = 8925;
    std::cout << 8925;
    for (int i = 0; i < 8925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8925(int& val) {
    const int x = 8925;
    std::cout << 8925;
    for (int i = 0; i < 8925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8926(int& val) {
    func_in_8926(val);
    const int x = 8926;
    std::cout << 8926;
    for (int i = 0; i < 8926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8926(int& val) {
    const int x = 8926;
    std::cout << 8926;
    for (int i = 0; i < 8926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8927(int& val) {
    func_in_8927(val);
    const int x = 8927;
    std::cout << 8927;
    for (int i = 0; i < 8927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8927(int& val) {
    const int x = 8927;
    std::cout << 8927;
    for (int i = 0; i < 8927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8928(int& val) {
    func_in_8928(val);
    const int x = 8928;
    std::cout << 8928;
    for (int i = 0; i < 8928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8928(int& val) {
    const int x = 8928;
    std::cout << 8928;
    for (int i = 0; i < 8928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8929(int& val) {
    func_in_8929(val);
    const int x = 8929;
    std::cout << 8929;
    for (int i = 0; i < 8929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8929(int& val) {
    const int x = 8929;
    std::cout << 8929;
    for (int i = 0; i < 8929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8930(int& val) {
    func_in_8930(val);
    const int x = 8930;
    std::cout << 8930;
    for (int i = 0; i < 8930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8930(int& val) {
    const int x = 8930;
    std::cout << 8930;
    for (int i = 0; i < 8930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8931(int& val) {
    func_in_8931(val);
    const int x = 8931;
    std::cout << 8931;
    for (int i = 0; i < 8931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8931(int& val) {
    const int x = 8931;
    std::cout << 8931;
    for (int i = 0; i < 8931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8932(int& val) {
    func_in_8932(val);
    const int x = 8932;
    std::cout << 8932;
    for (int i = 0; i < 8932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8932(int& val) {
    const int x = 8932;
    std::cout << 8932;
    for (int i = 0; i < 8932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8933(int& val) {
    func_in_8933(val);
    const int x = 8933;
    std::cout << 8933;
    for (int i = 0; i < 8933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8933(int& val) {
    const int x = 8933;
    std::cout << 8933;
    for (int i = 0; i < 8933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8934(int& val) {
    func_in_8934(val);
    const int x = 8934;
    std::cout << 8934;
    for (int i = 0; i < 8934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8934(int& val) {
    const int x = 8934;
    std::cout << 8934;
    for (int i = 0; i < 8934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8935(int& val) {
    func_in_8935(val);
    const int x = 8935;
    std::cout << 8935;
    for (int i = 0; i < 8935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8935(int& val) {
    const int x = 8935;
    std::cout << 8935;
    for (int i = 0; i < 8935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8936(int& val) {
    func_in_8936(val);
    const int x = 8936;
    std::cout << 8936;
    for (int i = 0; i < 8936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8936(int& val) {
    const int x = 8936;
    std::cout << 8936;
    for (int i = 0; i < 8936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8937(int& val) {
    func_in_8937(val);
    const int x = 8937;
    std::cout << 8937;
    for (int i = 0; i < 8937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8937(int& val) {
    const int x = 8937;
    std::cout << 8937;
    for (int i = 0; i < 8937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8938(int& val) {
    func_in_8938(val);
    const int x = 8938;
    std::cout << 8938;
    for (int i = 0; i < 8938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8938(int& val) {
    const int x = 8938;
    std::cout << 8938;
    for (int i = 0; i < 8938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8939(int& val) {
    func_in_8939(val);
    const int x = 8939;
    std::cout << 8939;
    for (int i = 0; i < 8939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8939(int& val) {
    const int x = 8939;
    std::cout << 8939;
    for (int i = 0; i < 8939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8940(int& val) {
    func_in_8940(val);
    const int x = 8940;
    std::cout << 8940;
    for (int i = 0; i < 8940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8940(int& val) {
    const int x = 8940;
    std::cout << 8940;
    for (int i = 0; i < 8940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8941(int& val) {
    func_in_8941(val);
    const int x = 8941;
    std::cout << 8941;
    for (int i = 0; i < 8941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8941(int& val) {
    const int x = 8941;
    std::cout << 8941;
    for (int i = 0; i < 8941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8942(int& val) {
    func_in_8942(val);
    const int x = 8942;
    std::cout << 8942;
    for (int i = 0; i < 8942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8942(int& val) {
    const int x = 8942;
    std::cout << 8942;
    for (int i = 0; i < 8942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8943(int& val) {
    func_in_8943(val);
    const int x = 8943;
    std::cout << 8943;
    for (int i = 0; i < 8943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8943(int& val) {
    const int x = 8943;
    std::cout << 8943;
    for (int i = 0; i < 8943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8944(int& val) {
    func_in_8944(val);
    const int x = 8944;
    std::cout << 8944;
    for (int i = 0; i < 8944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8944(int& val) {
    const int x = 8944;
    std::cout << 8944;
    for (int i = 0; i < 8944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8945(int& val) {
    func_in_8945(val);
    const int x = 8945;
    std::cout << 8945;
    for (int i = 0; i < 8945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8945(int& val) {
    const int x = 8945;
    std::cout << 8945;
    for (int i = 0; i < 8945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8946(int& val) {
    func_in_8946(val);
    const int x = 8946;
    std::cout << 8946;
    for (int i = 0; i < 8946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8946(int& val) {
    const int x = 8946;
    std::cout << 8946;
    for (int i = 0; i < 8946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8947(int& val) {
    func_in_8947(val);
    const int x = 8947;
    std::cout << 8947;
    for (int i = 0; i < 8947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8947(int& val) {
    const int x = 8947;
    std::cout << 8947;
    for (int i = 0; i < 8947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8948(int& val) {
    func_in_8948(val);
    const int x = 8948;
    std::cout << 8948;
    for (int i = 0; i < 8948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8948(int& val) {
    const int x = 8948;
    std::cout << 8948;
    for (int i = 0; i < 8948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8949(int& val) {
    func_in_8949(val);
    const int x = 8949;
    std::cout << 8949;
    for (int i = 0; i < 8949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8949(int& val) {
    const int x = 8949;
    std::cout << 8949;
    for (int i = 0; i < 8949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8950(int& val) {
    func_in_8950(val);
    const int x = 8950;
    std::cout << 8950;
    for (int i = 0; i < 8950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8950(int& val) {
    const int x = 8950;
    std::cout << 8950;
    for (int i = 0; i < 8950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8951(int& val) {
    func_in_8951(val);
    const int x = 8951;
    std::cout << 8951;
    for (int i = 0; i < 8951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8951(int& val) {
    const int x = 8951;
    std::cout << 8951;
    for (int i = 0; i < 8951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8952(int& val) {
    func_in_8952(val);
    const int x = 8952;
    std::cout << 8952;
    for (int i = 0; i < 8952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8952(int& val) {
    const int x = 8952;
    std::cout << 8952;
    for (int i = 0; i < 8952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8953(int& val) {
    func_in_8953(val);
    const int x = 8953;
    std::cout << 8953;
    for (int i = 0; i < 8953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8953(int& val) {
    const int x = 8953;
    std::cout << 8953;
    for (int i = 0; i < 8953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8954(int& val) {
    func_in_8954(val);
    const int x = 8954;
    std::cout << 8954;
    for (int i = 0; i < 8954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8954(int& val) {
    const int x = 8954;
    std::cout << 8954;
    for (int i = 0; i < 8954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8955(int& val) {
    func_in_8955(val);
    const int x = 8955;
    std::cout << 8955;
    for (int i = 0; i < 8955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8955(int& val) {
    const int x = 8955;
    std::cout << 8955;
    for (int i = 0; i < 8955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8956(int& val) {
    func_in_8956(val);
    const int x = 8956;
    std::cout << 8956;
    for (int i = 0; i < 8956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8956(int& val) {
    const int x = 8956;
    std::cout << 8956;
    for (int i = 0; i < 8956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8957(int& val) {
    func_in_8957(val);
    const int x = 8957;
    std::cout << 8957;
    for (int i = 0; i < 8957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8957(int& val) {
    const int x = 8957;
    std::cout << 8957;
    for (int i = 0; i < 8957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8958(int& val) {
    func_in_8958(val);
    const int x = 8958;
    std::cout << 8958;
    for (int i = 0; i < 8958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8958(int& val) {
    const int x = 8958;
    std::cout << 8958;
    for (int i = 0; i < 8958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8959(int& val) {
    func_in_8959(val);
    const int x = 8959;
    std::cout << 8959;
    for (int i = 0; i < 8959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8959(int& val) {
    const int x = 8959;
    std::cout << 8959;
    for (int i = 0; i < 8959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8960(int& val) {
    func_in_8960(val);
    const int x = 8960;
    std::cout << 8960;
    for (int i = 0; i < 8960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8960(int& val) {
    const int x = 8960;
    std::cout << 8960;
    for (int i = 0; i < 8960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8961(int& val) {
    func_in_8961(val);
    const int x = 8961;
    std::cout << 8961;
    for (int i = 0; i < 8961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8961(int& val) {
    const int x = 8961;
    std::cout << 8961;
    for (int i = 0; i < 8961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8962(int& val) {
    func_in_8962(val);
    const int x = 8962;
    std::cout << 8962;
    for (int i = 0; i < 8962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8962(int& val) {
    const int x = 8962;
    std::cout << 8962;
    for (int i = 0; i < 8962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8963(int& val) {
    func_in_8963(val);
    const int x = 8963;
    std::cout << 8963;
    for (int i = 0; i < 8963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8963(int& val) {
    const int x = 8963;
    std::cout << 8963;
    for (int i = 0; i < 8963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8964(int& val) {
    func_in_8964(val);
    const int x = 8964;
    std::cout << 8964;
    for (int i = 0; i < 8964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8964(int& val) {
    const int x = 8964;
    std::cout << 8964;
    for (int i = 0; i < 8964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8965(int& val) {
    func_in_8965(val);
    const int x = 8965;
    std::cout << 8965;
    for (int i = 0; i < 8965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8965(int& val) {
    const int x = 8965;
    std::cout << 8965;
    for (int i = 0; i < 8965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8966(int& val) {
    func_in_8966(val);
    const int x = 8966;
    std::cout << 8966;
    for (int i = 0; i < 8966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8966(int& val) {
    const int x = 8966;
    std::cout << 8966;
    for (int i = 0; i < 8966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8967(int& val) {
    func_in_8967(val);
    const int x = 8967;
    std::cout << 8967;
    for (int i = 0; i < 8967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8967(int& val) {
    const int x = 8967;
    std::cout << 8967;
    for (int i = 0; i < 8967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8968(int& val) {
    func_in_8968(val);
    const int x = 8968;
    std::cout << 8968;
    for (int i = 0; i < 8968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8968(int& val) {
    const int x = 8968;
    std::cout << 8968;
    for (int i = 0; i < 8968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8969(int& val) {
    func_in_8969(val);
    const int x = 8969;
    std::cout << 8969;
    for (int i = 0; i < 8969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8969(int& val) {
    const int x = 8969;
    std::cout << 8969;
    for (int i = 0; i < 8969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8970(int& val) {
    func_in_8970(val);
    const int x = 8970;
    std::cout << 8970;
    for (int i = 0; i < 8970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8970(int& val) {
    const int x = 8970;
    std::cout << 8970;
    for (int i = 0; i < 8970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8971(int& val) {
    func_in_8971(val);
    const int x = 8971;
    std::cout << 8971;
    for (int i = 0; i < 8971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8971(int& val) {
    const int x = 8971;
    std::cout << 8971;
    for (int i = 0; i < 8971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8972(int& val) {
    func_in_8972(val);
    const int x = 8972;
    std::cout << 8972;
    for (int i = 0; i < 8972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8972(int& val) {
    const int x = 8972;
    std::cout << 8972;
    for (int i = 0; i < 8972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8973(int& val) {
    func_in_8973(val);
    const int x = 8973;
    std::cout << 8973;
    for (int i = 0; i < 8973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8973(int& val) {
    const int x = 8973;
    std::cout << 8973;
    for (int i = 0; i < 8973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8974(int& val) {
    func_in_8974(val);
    const int x = 8974;
    std::cout << 8974;
    for (int i = 0; i < 8974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8974(int& val) {
    const int x = 8974;
    std::cout << 8974;
    for (int i = 0; i < 8974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8975(int& val) {
    func_in_8975(val);
    const int x = 8975;
    std::cout << 8975;
    for (int i = 0; i < 8975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8975(int& val) {
    const int x = 8975;
    std::cout << 8975;
    for (int i = 0; i < 8975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8976(int& val) {
    func_in_8976(val);
    const int x = 8976;
    std::cout << 8976;
    for (int i = 0; i < 8976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8976(int& val) {
    const int x = 8976;
    std::cout << 8976;
    for (int i = 0; i < 8976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8977(int& val) {
    func_in_8977(val);
    const int x = 8977;
    std::cout << 8977;
    for (int i = 0; i < 8977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8977(int& val) {
    const int x = 8977;
    std::cout << 8977;
    for (int i = 0; i < 8977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8978(int& val) {
    func_in_8978(val);
    const int x = 8978;
    std::cout << 8978;
    for (int i = 0; i < 8978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8978(int& val) {
    const int x = 8978;
    std::cout << 8978;
    for (int i = 0; i < 8978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8979(int& val) {
    func_in_8979(val);
    const int x = 8979;
    std::cout << 8979;
    for (int i = 0; i < 8979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8979(int& val) {
    const int x = 8979;
    std::cout << 8979;
    for (int i = 0; i < 8979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8980(int& val) {
    func_in_8980(val);
    const int x = 8980;
    std::cout << 8980;
    for (int i = 0; i < 8980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8980(int& val) {
    const int x = 8980;
    std::cout << 8980;
    for (int i = 0; i < 8980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8981(int& val) {
    func_in_8981(val);
    const int x = 8981;
    std::cout << 8981;
    for (int i = 0; i < 8981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8981(int& val) {
    const int x = 8981;
    std::cout << 8981;
    for (int i = 0; i < 8981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8982(int& val) {
    func_in_8982(val);
    const int x = 8982;
    std::cout << 8982;
    for (int i = 0; i < 8982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8982(int& val) {
    const int x = 8982;
    std::cout << 8982;
    for (int i = 0; i < 8982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8983(int& val) {
    func_in_8983(val);
    const int x = 8983;
    std::cout << 8983;
    for (int i = 0; i < 8983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8983(int& val) {
    const int x = 8983;
    std::cout << 8983;
    for (int i = 0; i < 8983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8984(int& val) {
    func_in_8984(val);
    const int x = 8984;
    std::cout << 8984;
    for (int i = 0; i < 8984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8984(int& val) {
    const int x = 8984;
    std::cout << 8984;
    for (int i = 0; i < 8984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8985(int& val) {
    func_in_8985(val);
    const int x = 8985;
    std::cout << 8985;
    for (int i = 0; i < 8985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8985(int& val) {
    const int x = 8985;
    std::cout << 8985;
    for (int i = 0; i < 8985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8986(int& val) {
    func_in_8986(val);
    const int x = 8986;
    std::cout << 8986;
    for (int i = 0; i < 8986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8986(int& val) {
    const int x = 8986;
    std::cout << 8986;
    for (int i = 0; i < 8986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8987(int& val) {
    func_in_8987(val);
    const int x = 8987;
    std::cout << 8987;
    for (int i = 0; i < 8987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8987(int& val) {
    const int x = 8987;
    std::cout << 8987;
    for (int i = 0; i < 8987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8988(int& val) {
    func_in_8988(val);
    const int x = 8988;
    std::cout << 8988;
    for (int i = 0; i < 8988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8988(int& val) {
    const int x = 8988;
    std::cout << 8988;
    for (int i = 0; i < 8988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8989(int& val) {
    func_in_8989(val);
    const int x = 8989;
    std::cout << 8989;
    for (int i = 0; i < 8989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8989(int& val) {
    const int x = 8989;
    std::cout << 8989;
    for (int i = 0; i < 8989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8990(int& val) {
    func_in_8990(val);
    const int x = 8990;
    std::cout << 8990;
    for (int i = 0; i < 8990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8990(int& val) {
    const int x = 8990;
    std::cout << 8990;
    for (int i = 0; i < 8990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8991(int& val) {
    func_in_8991(val);
    const int x = 8991;
    std::cout << 8991;
    for (int i = 0; i < 8991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8991(int& val) {
    const int x = 8991;
    std::cout << 8991;
    for (int i = 0; i < 8991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8992(int& val) {
    func_in_8992(val);
    const int x = 8992;
    std::cout << 8992;
    for (int i = 0; i < 8992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8992(int& val) {
    const int x = 8992;
    std::cout << 8992;
    for (int i = 0; i < 8992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8993(int& val) {
    func_in_8993(val);
    const int x = 8993;
    std::cout << 8993;
    for (int i = 0; i < 8993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8993(int& val) {
    const int x = 8993;
    std::cout << 8993;
    for (int i = 0; i < 8993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8994(int& val) {
    func_in_8994(val);
    const int x = 8994;
    std::cout << 8994;
    for (int i = 0; i < 8994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8994(int& val) {
    const int x = 8994;
    std::cout << 8994;
    for (int i = 0; i < 8994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8995(int& val) {
    func_in_8995(val);
    const int x = 8995;
    std::cout << 8995;
    for (int i = 0; i < 8995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8995(int& val) {
    const int x = 8995;
    std::cout << 8995;
    for (int i = 0; i < 8995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8996(int& val) {
    func_in_8996(val);
    const int x = 8996;
    std::cout << 8996;
    for (int i = 0; i < 8996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8996(int& val) {
    const int x = 8996;
    std::cout << 8996;
    for (int i = 0; i < 8996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8997(int& val) {
    func_in_8997(val);
    const int x = 8997;
    std::cout << 8997;
    for (int i = 0; i < 8997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8997(int& val) {
    const int x = 8997;
    std::cout << 8997;
    for (int i = 0; i < 8997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8998(int& val) {
    func_in_8998(val);
    const int x = 8998;
    std::cout << 8998;
    for (int i = 0; i < 8998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8998(int& val) {
    const int x = 8998;
    std::cout << 8998;
    for (int i = 0; i < 8998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8999(int& val) {
    func_in_8999(val);
    const int x = 8999;
    std::cout << 8999;
    for (int i = 0; i < 8999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8999(int& val) {
    const int x = 8999;
    std::cout << 8999;
    for (int i = 0; i < 8999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9000(int& val) {
    func_in_9000(val);
    const int x = 9000;
    std::cout << 9000;
    for (int i = 0; i < 9000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9000(int& val) {
    const int x = 9000;
    std::cout << 9000;
    for (int i = 0; i < 9000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9001(int& val) {
    func_in_9001(val);
    const int x = 9001;
    std::cout << 9001;
    for (int i = 0; i < 9001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9001(int& val) {
    const int x = 9001;
    std::cout << 9001;
    for (int i = 0; i < 9001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9002(int& val) {
    func_in_9002(val);
    const int x = 9002;
    std::cout << 9002;
    for (int i = 0; i < 9002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9002(int& val) {
    const int x = 9002;
    std::cout << 9002;
    for (int i = 0; i < 9002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9003(int& val) {
    func_in_9003(val);
    const int x = 9003;
    std::cout << 9003;
    for (int i = 0; i < 9003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9003(int& val) {
    const int x = 9003;
    std::cout << 9003;
    for (int i = 0; i < 9003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9004(int& val) {
    func_in_9004(val);
    const int x = 9004;
    std::cout << 9004;
    for (int i = 0; i < 9004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9004(int& val) {
    const int x = 9004;
    std::cout << 9004;
    for (int i = 0; i < 9004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9005(int& val) {
    func_in_9005(val);
    const int x = 9005;
    std::cout << 9005;
    for (int i = 0; i < 9005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9005(int& val) {
    const int x = 9005;
    std::cout << 9005;
    for (int i = 0; i < 9005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9006(int& val) {
    func_in_9006(val);
    const int x = 9006;
    std::cout << 9006;
    for (int i = 0; i < 9006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9006(int& val) {
    const int x = 9006;
    std::cout << 9006;
    for (int i = 0; i < 9006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9007(int& val) {
    func_in_9007(val);
    const int x = 9007;
    std::cout << 9007;
    for (int i = 0; i < 9007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9007(int& val) {
    const int x = 9007;
    std::cout << 9007;
    for (int i = 0; i < 9007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9008(int& val) {
    func_in_9008(val);
    const int x = 9008;
    std::cout << 9008;
    for (int i = 0; i < 9008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9008(int& val) {
    const int x = 9008;
    std::cout << 9008;
    for (int i = 0; i < 9008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9009(int& val) {
    func_in_9009(val);
    const int x = 9009;
    std::cout << 9009;
    for (int i = 0; i < 9009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9009(int& val) {
    const int x = 9009;
    std::cout << 9009;
    for (int i = 0; i < 9009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9010(int& val) {
    func_in_9010(val);
    const int x = 9010;
    std::cout << 9010;
    for (int i = 0; i < 9010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9010(int& val) {
    const int x = 9010;
    std::cout << 9010;
    for (int i = 0; i < 9010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9011(int& val) {
    func_in_9011(val);
    const int x = 9011;
    std::cout << 9011;
    for (int i = 0; i < 9011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9011(int& val) {
    const int x = 9011;
    std::cout << 9011;
    for (int i = 0; i < 9011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9012(int& val) {
    func_in_9012(val);
    const int x = 9012;
    std::cout << 9012;
    for (int i = 0; i < 9012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9012(int& val) {
    const int x = 9012;
    std::cout << 9012;
    for (int i = 0; i < 9012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9013(int& val) {
    func_in_9013(val);
    const int x = 9013;
    std::cout << 9013;
    for (int i = 0; i < 9013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9013(int& val) {
    const int x = 9013;
    std::cout << 9013;
    for (int i = 0; i < 9013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9014(int& val) {
    func_in_9014(val);
    const int x = 9014;
    std::cout << 9014;
    for (int i = 0; i < 9014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9014(int& val) {
    const int x = 9014;
    std::cout << 9014;
    for (int i = 0; i < 9014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9015(int& val) {
    func_in_9015(val);
    const int x = 9015;
    std::cout << 9015;
    for (int i = 0; i < 9015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9015(int& val) {
    const int x = 9015;
    std::cout << 9015;
    for (int i = 0; i < 9015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9016(int& val) {
    func_in_9016(val);
    const int x = 9016;
    std::cout << 9016;
    for (int i = 0; i < 9016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9016(int& val) {
    const int x = 9016;
    std::cout << 9016;
    for (int i = 0; i < 9016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9017(int& val) {
    func_in_9017(val);
    const int x = 9017;
    std::cout << 9017;
    for (int i = 0; i < 9017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9017(int& val) {
    const int x = 9017;
    std::cout << 9017;
    for (int i = 0; i < 9017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9018(int& val) {
    func_in_9018(val);
    const int x = 9018;
    std::cout << 9018;
    for (int i = 0; i < 9018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9018(int& val) {
    const int x = 9018;
    std::cout << 9018;
    for (int i = 0; i < 9018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9019(int& val) {
    func_in_9019(val);
    const int x = 9019;
    std::cout << 9019;
    for (int i = 0; i < 9019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9019(int& val) {
    const int x = 9019;
    std::cout << 9019;
    for (int i = 0; i < 9019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9020(int& val) {
    func_in_9020(val);
    const int x = 9020;
    std::cout << 9020;
    for (int i = 0; i < 9020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9020(int& val) {
    const int x = 9020;
    std::cout << 9020;
    for (int i = 0; i < 9020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9021(int& val) {
    func_in_9021(val);
    const int x = 9021;
    std::cout << 9021;
    for (int i = 0; i < 9021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9021(int& val) {
    const int x = 9021;
    std::cout << 9021;
    for (int i = 0; i < 9021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9022(int& val) {
    func_in_9022(val);
    const int x = 9022;
    std::cout << 9022;
    for (int i = 0; i < 9022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9022(int& val) {
    const int x = 9022;
    std::cout << 9022;
    for (int i = 0; i < 9022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9023(int& val) {
    func_in_9023(val);
    const int x = 9023;
    std::cout << 9023;
    for (int i = 0; i < 9023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9023(int& val) {
    const int x = 9023;
    std::cout << 9023;
    for (int i = 0; i < 9023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9024(int& val) {
    func_in_9024(val);
    const int x = 9024;
    std::cout << 9024;
    for (int i = 0; i < 9024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9024(int& val) {
    const int x = 9024;
    std::cout << 9024;
    for (int i = 0; i < 9024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9025(int& val) {
    func_in_9025(val);
    const int x = 9025;
    std::cout << 9025;
    for (int i = 0; i < 9025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9025(int& val) {
    const int x = 9025;
    std::cout << 9025;
    for (int i = 0; i < 9025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9026(int& val) {
    func_in_9026(val);
    const int x = 9026;
    std::cout << 9026;
    for (int i = 0; i < 9026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9026(int& val) {
    const int x = 9026;
    std::cout << 9026;
    for (int i = 0; i < 9026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9027(int& val) {
    func_in_9027(val);
    const int x = 9027;
    std::cout << 9027;
    for (int i = 0; i < 9027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9027(int& val) {
    const int x = 9027;
    std::cout << 9027;
    for (int i = 0; i < 9027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9028(int& val) {
    func_in_9028(val);
    const int x = 9028;
    std::cout << 9028;
    for (int i = 0; i < 9028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9028(int& val) {
    const int x = 9028;
    std::cout << 9028;
    for (int i = 0; i < 9028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9029(int& val) {
    func_in_9029(val);
    const int x = 9029;
    std::cout << 9029;
    for (int i = 0; i < 9029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9029(int& val) {
    const int x = 9029;
    std::cout << 9029;
    for (int i = 0; i < 9029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9030(int& val) {
    func_in_9030(val);
    const int x = 9030;
    std::cout << 9030;
    for (int i = 0; i < 9030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9030(int& val) {
    const int x = 9030;
    std::cout << 9030;
    for (int i = 0; i < 9030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9031(int& val) {
    func_in_9031(val);
    const int x = 9031;
    std::cout << 9031;
    for (int i = 0; i < 9031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9031(int& val) {
    const int x = 9031;
    std::cout << 9031;
    for (int i = 0; i < 9031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9032(int& val) {
    func_in_9032(val);
    const int x = 9032;
    std::cout << 9032;
    for (int i = 0; i < 9032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9032(int& val) {
    const int x = 9032;
    std::cout << 9032;
    for (int i = 0; i < 9032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9033(int& val) {
    func_in_9033(val);
    const int x = 9033;
    std::cout << 9033;
    for (int i = 0; i < 9033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9033(int& val) {
    const int x = 9033;
    std::cout << 9033;
    for (int i = 0; i < 9033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9034(int& val) {
    func_in_9034(val);
    const int x = 9034;
    std::cout << 9034;
    for (int i = 0; i < 9034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9034(int& val) {
    const int x = 9034;
    std::cout << 9034;
    for (int i = 0; i < 9034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9035(int& val) {
    func_in_9035(val);
    const int x = 9035;
    std::cout << 9035;
    for (int i = 0; i < 9035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9035(int& val) {
    const int x = 9035;
    std::cout << 9035;
    for (int i = 0; i < 9035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9036(int& val) {
    func_in_9036(val);
    const int x = 9036;
    std::cout << 9036;
    for (int i = 0; i < 9036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9036(int& val) {
    const int x = 9036;
    std::cout << 9036;
    for (int i = 0; i < 9036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9037(int& val) {
    func_in_9037(val);
    const int x = 9037;
    std::cout << 9037;
    for (int i = 0; i < 9037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9037(int& val) {
    const int x = 9037;
    std::cout << 9037;
    for (int i = 0; i < 9037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9038(int& val) {
    func_in_9038(val);
    const int x = 9038;
    std::cout << 9038;
    for (int i = 0; i < 9038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9038(int& val) {
    const int x = 9038;
    std::cout << 9038;
    for (int i = 0; i < 9038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9039(int& val) {
    func_in_9039(val);
    const int x = 9039;
    std::cout << 9039;
    for (int i = 0; i < 9039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9039(int& val) {
    const int x = 9039;
    std::cout << 9039;
    for (int i = 0; i < 9039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9040(int& val) {
    func_in_9040(val);
    const int x = 9040;
    std::cout << 9040;
    for (int i = 0; i < 9040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9040(int& val) {
    const int x = 9040;
    std::cout << 9040;
    for (int i = 0; i < 9040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9041(int& val) {
    func_in_9041(val);
    const int x = 9041;
    std::cout << 9041;
    for (int i = 0; i < 9041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9041(int& val) {
    const int x = 9041;
    std::cout << 9041;
    for (int i = 0; i < 9041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9042(int& val) {
    func_in_9042(val);
    const int x = 9042;
    std::cout << 9042;
    for (int i = 0; i < 9042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9042(int& val) {
    const int x = 9042;
    std::cout << 9042;
    for (int i = 0; i < 9042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9043(int& val) {
    func_in_9043(val);
    const int x = 9043;
    std::cout << 9043;
    for (int i = 0; i < 9043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9043(int& val) {
    const int x = 9043;
    std::cout << 9043;
    for (int i = 0; i < 9043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9044(int& val) {
    func_in_9044(val);
    const int x = 9044;
    std::cout << 9044;
    for (int i = 0; i < 9044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9044(int& val) {
    const int x = 9044;
    std::cout << 9044;
    for (int i = 0; i < 9044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9045(int& val) {
    func_in_9045(val);
    const int x = 9045;
    std::cout << 9045;
    for (int i = 0; i < 9045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9045(int& val) {
    const int x = 9045;
    std::cout << 9045;
    for (int i = 0; i < 9045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9046(int& val) {
    func_in_9046(val);
    const int x = 9046;
    std::cout << 9046;
    for (int i = 0; i < 9046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9046(int& val) {
    const int x = 9046;
    std::cout << 9046;
    for (int i = 0; i < 9046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9047(int& val) {
    func_in_9047(val);
    const int x = 9047;
    std::cout << 9047;
    for (int i = 0; i < 9047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9047(int& val) {
    const int x = 9047;
    std::cout << 9047;
    for (int i = 0; i < 9047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9048(int& val) {
    func_in_9048(val);
    const int x = 9048;
    std::cout << 9048;
    for (int i = 0; i < 9048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9048(int& val) {
    const int x = 9048;
    std::cout << 9048;
    for (int i = 0; i < 9048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9049(int& val) {
    func_in_9049(val);
    const int x = 9049;
    std::cout << 9049;
    for (int i = 0; i < 9049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9049(int& val) {
    const int x = 9049;
    std::cout << 9049;
    for (int i = 0; i < 9049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9050(int& val) {
    func_in_9050(val);
    const int x = 9050;
    std::cout << 9050;
    for (int i = 0; i < 9050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9050(int& val) {
    const int x = 9050;
    std::cout << 9050;
    for (int i = 0; i < 9050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9051(int& val) {
    func_in_9051(val);
    const int x = 9051;
    std::cout << 9051;
    for (int i = 0; i < 9051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9051(int& val) {
    const int x = 9051;
    std::cout << 9051;
    for (int i = 0; i < 9051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9052(int& val) {
    func_in_9052(val);
    const int x = 9052;
    std::cout << 9052;
    for (int i = 0; i < 9052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9052(int& val) {
    const int x = 9052;
    std::cout << 9052;
    for (int i = 0; i < 9052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9053(int& val) {
    func_in_9053(val);
    const int x = 9053;
    std::cout << 9053;
    for (int i = 0; i < 9053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9053(int& val) {
    const int x = 9053;
    std::cout << 9053;
    for (int i = 0; i < 9053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9054(int& val) {
    func_in_9054(val);
    const int x = 9054;
    std::cout << 9054;
    for (int i = 0; i < 9054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9054(int& val) {
    const int x = 9054;
    std::cout << 9054;
    for (int i = 0; i < 9054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9055(int& val) {
    func_in_9055(val);
    const int x = 9055;
    std::cout << 9055;
    for (int i = 0; i < 9055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9055(int& val) {
    const int x = 9055;
    std::cout << 9055;
    for (int i = 0; i < 9055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9056(int& val) {
    func_in_9056(val);
    const int x = 9056;
    std::cout << 9056;
    for (int i = 0; i < 9056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9056(int& val) {
    const int x = 9056;
    std::cout << 9056;
    for (int i = 0; i < 9056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9057(int& val) {
    func_in_9057(val);
    const int x = 9057;
    std::cout << 9057;
    for (int i = 0; i < 9057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9057(int& val) {
    const int x = 9057;
    std::cout << 9057;
    for (int i = 0; i < 9057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9058(int& val) {
    func_in_9058(val);
    const int x = 9058;
    std::cout << 9058;
    for (int i = 0; i < 9058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9058(int& val) {
    const int x = 9058;
    std::cout << 9058;
    for (int i = 0; i < 9058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9059(int& val) {
    func_in_9059(val);
    const int x = 9059;
    std::cout << 9059;
    for (int i = 0; i < 9059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9059(int& val) {
    const int x = 9059;
    std::cout << 9059;
    for (int i = 0; i < 9059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9060(int& val) {
    func_in_9060(val);
    const int x = 9060;
    std::cout << 9060;
    for (int i = 0; i < 9060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9060(int& val) {
    const int x = 9060;
    std::cout << 9060;
    for (int i = 0; i < 9060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9061(int& val) {
    func_in_9061(val);
    const int x = 9061;
    std::cout << 9061;
    for (int i = 0; i < 9061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9061(int& val) {
    const int x = 9061;
    std::cout << 9061;
    for (int i = 0; i < 9061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9062(int& val) {
    func_in_9062(val);
    const int x = 9062;
    std::cout << 9062;
    for (int i = 0; i < 9062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9062(int& val) {
    const int x = 9062;
    std::cout << 9062;
    for (int i = 0; i < 9062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9063(int& val) {
    func_in_9063(val);
    const int x = 9063;
    std::cout << 9063;
    for (int i = 0; i < 9063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9063(int& val) {
    const int x = 9063;
    std::cout << 9063;
    for (int i = 0; i < 9063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9064(int& val) {
    func_in_9064(val);
    const int x = 9064;
    std::cout << 9064;
    for (int i = 0; i < 9064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9064(int& val) {
    const int x = 9064;
    std::cout << 9064;
    for (int i = 0; i < 9064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9065(int& val) {
    func_in_9065(val);
    const int x = 9065;
    std::cout << 9065;
    for (int i = 0; i < 9065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9065(int& val) {
    const int x = 9065;
    std::cout << 9065;
    for (int i = 0; i < 9065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9066(int& val) {
    func_in_9066(val);
    const int x = 9066;
    std::cout << 9066;
    for (int i = 0; i < 9066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9066(int& val) {
    const int x = 9066;
    std::cout << 9066;
    for (int i = 0; i < 9066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9067(int& val) {
    func_in_9067(val);
    const int x = 9067;
    std::cout << 9067;
    for (int i = 0; i < 9067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9067(int& val) {
    const int x = 9067;
    std::cout << 9067;
    for (int i = 0; i < 9067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9068(int& val) {
    func_in_9068(val);
    const int x = 9068;
    std::cout << 9068;
    for (int i = 0; i < 9068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9068(int& val) {
    const int x = 9068;
    std::cout << 9068;
    for (int i = 0; i < 9068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9069(int& val) {
    func_in_9069(val);
    const int x = 9069;
    std::cout << 9069;
    for (int i = 0; i < 9069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9069(int& val) {
    const int x = 9069;
    std::cout << 9069;
    for (int i = 0; i < 9069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9070(int& val) {
    func_in_9070(val);
    const int x = 9070;
    std::cout << 9070;
    for (int i = 0; i < 9070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9070(int& val) {
    const int x = 9070;
    std::cout << 9070;
    for (int i = 0; i < 9070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9071(int& val) {
    func_in_9071(val);
    const int x = 9071;
    std::cout << 9071;
    for (int i = 0; i < 9071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9071(int& val) {
    const int x = 9071;
    std::cout << 9071;
    for (int i = 0; i < 9071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9072(int& val) {
    func_in_9072(val);
    const int x = 9072;
    std::cout << 9072;
    for (int i = 0; i < 9072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9072(int& val) {
    const int x = 9072;
    std::cout << 9072;
    for (int i = 0; i < 9072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9073(int& val) {
    func_in_9073(val);
    const int x = 9073;
    std::cout << 9073;
    for (int i = 0; i < 9073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9073(int& val) {
    const int x = 9073;
    std::cout << 9073;
    for (int i = 0; i < 9073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9074(int& val) {
    func_in_9074(val);
    const int x = 9074;
    std::cout << 9074;
    for (int i = 0; i < 9074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9074(int& val) {
    const int x = 9074;
    std::cout << 9074;
    for (int i = 0; i < 9074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9075(int& val) {
    func_in_9075(val);
    const int x = 9075;
    std::cout << 9075;
    for (int i = 0; i < 9075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9075(int& val) {
    const int x = 9075;
    std::cout << 9075;
    for (int i = 0; i < 9075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9076(int& val) {
    func_in_9076(val);
    const int x = 9076;
    std::cout << 9076;
    for (int i = 0; i < 9076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9076(int& val) {
    const int x = 9076;
    std::cout << 9076;
    for (int i = 0; i < 9076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9077(int& val) {
    func_in_9077(val);
    const int x = 9077;
    std::cout << 9077;
    for (int i = 0; i < 9077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9077(int& val) {
    const int x = 9077;
    std::cout << 9077;
    for (int i = 0; i < 9077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9078(int& val) {
    func_in_9078(val);
    const int x = 9078;
    std::cout << 9078;
    for (int i = 0; i < 9078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9078(int& val) {
    const int x = 9078;
    std::cout << 9078;
    for (int i = 0; i < 9078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9079(int& val) {
    func_in_9079(val);
    const int x = 9079;
    std::cout << 9079;
    for (int i = 0; i < 9079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9079(int& val) {
    const int x = 9079;
    std::cout << 9079;
    for (int i = 0; i < 9079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9080(int& val) {
    func_in_9080(val);
    const int x = 9080;
    std::cout << 9080;
    for (int i = 0; i < 9080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9080(int& val) {
    const int x = 9080;
    std::cout << 9080;
    for (int i = 0; i < 9080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9081(int& val) {
    func_in_9081(val);
    const int x = 9081;
    std::cout << 9081;
    for (int i = 0; i < 9081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9081(int& val) {
    const int x = 9081;
    std::cout << 9081;
    for (int i = 0; i < 9081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9082(int& val) {
    func_in_9082(val);
    const int x = 9082;
    std::cout << 9082;
    for (int i = 0; i < 9082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9082(int& val) {
    const int x = 9082;
    std::cout << 9082;
    for (int i = 0; i < 9082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9083(int& val) {
    func_in_9083(val);
    const int x = 9083;
    std::cout << 9083;
    for (int i = 0; i < 9083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9083(int& val) {
    const int x = 9083;
    std::cout << 9083;
    for (int i = 0; i < 9083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9084(int& val) {
    func_in_9084(val);
    const int x = 9084;
    std::cout << 9084;
    for (int i = 0; i < 9084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9084(int& val) {
    const int x = 9084;
    std::cout << 9084;
    for (int i = 0; i < 9084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9085(int& val) {
    func_in_9085(val);
    const int x = 9085;
    std::cout << 9085;
    for (int i = 0; i < 9085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9085(int& val) {
    const int x = 9085;
    std::cout << 9085;
    for (int i = 0; i < 9085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9086(int& val) {
    func_in_9086(val);
    const int x = 9086;
    std::cout << 9086;
    for (int i = 0; i < 9086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9086(int& val) {
    const int x = 9086;
    std::cout << 9086;
    for (int i = 0; i < 9086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9087(int& val) {
    func_in_9087(val);
    const int x = 9087;
    std::cout << 9087;
    for (int i = 0; i < 9087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9087(int& val) {
    const int x = 9087;
    std::cout << 9087;
    for (int i = 0; i < 9087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9088(int& val) {
    func_in_9088(val);
    const int x = 9088;
    std::cout << 9088;
    for (int i = 0; i < 9088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9088(int& val) {
    const int x = 9088;
    std::cout << 9088;
    for (int i = 0; i < 9088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9089(int& val) {
    func_in_9089(val);
    const int x = 9089;
    std::cout << 9089;
    for (int i = 0; i < 9089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9089(int& val) {
    const int x = 9089;
    std::cout << 9089;
    for (int i = 0; i < 9089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9090(int& val) {
    func_in_9090(val);
    const int x = 9090;
    std::cout << 9090;
    for (int i = 0; i < 9090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9090(int& val) {
    const int x = 9090;
    std::cout << 9090;
    for (int i = 0; i < 9090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9091(int& val) {
    func_in_9091(val);
    const int x = 9091;
    std::cout << 9091;
    for (int i = 0; i < 9091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9091(int& val) {
    const int x = 9091;
    std::cout << 9091;
    for (int i = 0; i < 9091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9092(int& val) {
    func_in_9092(val);
    const int x = 9092;
    std::cout << 9092;
    for (int i = 0; i < 9092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9092(int& val) {
    const int x = 9092;
    std::cout << 9092;
    for (int i = 0; i < 9092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9093(int& val) {
    func_in_9093(val);
    const int x = 9093;
    std::cout << 9093;
    for (int i = 0; i < 9093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9093(int& val) {
    const int x = 9093;
    std::cout << 9093;
    for (int i = 0; i < 9093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9094(int& val) {
    func_in_9094(val);
    const int x = 9094;
    std::cout << 9094;
    for (int i = 0; i < 9094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9094(int& val) {
    const int x = 9094;
    std::cout << 9094;
    for (int i = 0; i < 9094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9095(int& val) {
    func_in_9095(val);
    const int x = 9095;
    std::cout << 9095;
    for (int i = 0; i < 9095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9095(int& val) {
    const int x = 9095;
    std::cout << 9095;
    for (int i = 0; i < 9095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9096(int& val) {
    func_in_9096(val);
    const int x = 9096;
    std::cout << 9096;
    for (int i = 0; i < 9096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9096(int& val) {
    const int x = 9096;
    std::cout << 9096;
    for (int i = 0; i < 9096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9097(int& val) {
    func_in_9097(val);
    const int x = 9097;
    std::cout << 9097;
    for (int i = 0; i < 9097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9097(int& val) {
    const int x = 9097;
    std::cout << 9097;
    for (int i = 0; i < 9097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9098(int& val) {
    func_in_9098(val);
    const int x = 9098;
    std::cout << 9098;
    for (int i = 0; i < 9098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9098(int& val) {
    const int x = 9098;
    std::cout << 9098;
    for (int i = 0; i < 9098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9099(int& val) {
    func_in_9099(val);
    const int x = 9099;
    std::cout << 9099;
    for (int i = 0; i < 9099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9099(int& val) {
    const int x = 9099;
    std::cout << 9099;
    for (int i = 0; i < 9099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9100(int& val) {
    func_in_9100(val);
    const int x = 9100;
    std::cout << 9100;
    for (int i = 0; i < 9100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9100(int& val) {
    const int x = 9100;
    std::cout << 9100;
    for (int i = 0; i < 9100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9101(int& val) {
    func_in_9101(val);
    const int x = 9101;
    std::cout << 9101;
    for (int i = 0; i < 9101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9101(int& val) {
    const int x = 9101;
    std::cout << 9101;
    for (int i = 0; i < 9101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9102(int& val) {
    func_in_9102(val);
    const int x = 9102;
    std::cout << 9102;
    for (int i = 0; i < 9102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9102(int& val) {
    const int x = 9102;
    std::cout << 9102;
    for (int i = 0; i < 9102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9103(int& val) {
    func_in_9103(val);
    const int x = 9103;
    std::cout << 9103;
    for (int i = 0; i < 9103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9103(int& val) {
    const int x = 9103;
    std::cout << 9103;
    for (int i = 0; i < 9103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9104(int& val) {
    func_in_9104(val);
    const int x = 9104;
    std::cout << 9104;
    for (int i = 0; i < 9104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9104(int& val) {
    const int x = 9104;
    std::cout << 9104;
    for (int i = 0; i < 9104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9105(int& val) {
    func_in_9105(val);
    const int x = 9105;
    std::cout << 9105;
    for (int i = 0; i < 9105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9105(int& val) {
    const int x = 9105;
    std::cout << 9105;
    for (int i = 0; i < 9105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9106(int& val) {
    func_in_9106(val);
    const int x = 9106;
    std::cout << 9106;
    for (int i = 0; i < 9106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9106(int& val) {
    const int x = 9106;
    std::cout << 9106;
    for (int i = 0; i < 9106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9107(int& val) {
    func_in_9107(val);
    const int x = 9107;
    std::cout << 9107;
    for (int i = 0; i < 9107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9107(int& val) {
    const int x = 9107;
    std::cout << 9107;
    for (int i = 0; i < 9107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9108(int& val) {
    func_in_9108(val);
    const int x = 9108;
    std::cout << 9108;
    for (int i = 0; i < 9108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9108(int& val) {
    const int x = 9108;
    std::cout << 9108;
    for (int i = 0; i < 9108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9109(int& val) {
    func_in_9109(val);
    const int x = 9109;
    std::cout << 9109;
    for (int i = 0; i < 9109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9109(int& val) {
    const int x = 9109;
    std::cout << 9109;
    for (int i = 0; i < 9109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9110(int& val) {
    func_in_9110(val);
    const int x = 9110;
    std::cout << 9110;
    for (int i = 0; i < 9110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9110(int& val) {
    const int x = 9110;
    std::cout << 9110;
    for (int i = 0; i < 9110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9111(int& val) {
    func_in_9111(val);
    const int x = 9111;
    std::cout << 9111;
    for (int i = 0; i < 9111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9111(int& val) {
    const int x = 9111;
    std::cout << 9111;
    for (int i = 0; i < 9111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9112(int& val) {
    func_in_9112(val);
    const int x = 9112;
    std::cout << 9112;
    for (int i = 0; i < 9112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9112(int& val) {
    const int x = 9112;
    std::cout << 9112;
    for (int i = 0; i < 9112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9113(int& val) {
    func_in_9113(val);
    const int x = 9113;
    std::cout << 9113;
    for (int i = 0; i < 9113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9113(int& val) {
    const int x = 9113;
    std::cout << 9113;
    for (int i = 0; i < 9113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9114(int& val) {
    func_in_9114(val);
    const int x = 9114;
    std::cout << 9114;
    for (int i = 0; i < 9114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9114(int& val) {
    const int x = 9114;
    std::cout << 9114;
    for (int i = 0; i < 9114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9115(int& val) {
    func_in_9115(val);
    const int x = 9115;
    std::cout << 9115;
    for (int i = 0; i < 9115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9115(int& val) {
    const int x = 9115;
    std::cout << 9115;
    for (int i = 0; i < 9115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9116(int& val) {
    func_in_9116(val);
    const int x = 9116;
    std::cout << 9116;
    for (int i = 0; i < 9116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9116(int& val) {
    const int x = 9116;
    std::cout << 9116;
    for (int i = 0; i < 9116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9117(int& val) {
    func_in_9117(val);
    const int x = 9117;
    std::cout << 9117;
    for (int i = 0; i < 9117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9117(int& val) {
    const int x = 9117;
    std::cout << 9117;
    for (int i = 0; i < 9117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9118(int& val) {
    func_in_9118(val);
    const int x = 9118;
    std::cout << 9118;
    for (int i = 0; i < 9118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9118(int& val) {
    const int x = 9118;
    std::cout << 9118;
    for (int i = 0; i < 9118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9119(int& val) {
    func_in_9119(val);
    const int x = 9119;
    std::cout << 9119;
    for (int i = 0; i < 9119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9119(int& val) {
    const int x = 9119;
    std::cout << 9119;
    for (int i = 0; i < 9119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9120(int& val) {
    func_in_9120(val);
    const int x = 9120;
    std::cout << 9120;
    for (int i = 0; i < 9120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9120(int& val) {
    const int x = 9120;
    std::cout << 9120;
    for (int i = 0; i < 9120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9121(int& val) {
    func_in_9121(val);
    const int x = 9121;
    std::cout << 9121;
    for (int i = 0; i < 9121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9121(int& val) {
    const int x = 9121;
    std::cout << 9121;
    for (int i = 0; i < 9121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9122(int& val) {
    func_in_9122(val);
    const int x = 9122;
    std::cout << 9122;
    for (int i = 0; i < 9122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9122(int& val) {
    const int x = 9122;
    std::cout << 9122;
    for (int i = 0; i < 9122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9123(int& val) {
    func_in_9123(val);
    const int x = 9123;
    std::cout << 9123;
    for (int i = 0; i < 9123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9123(int& val) {
    const int x = 9123;
    std::cout << 9123;
    for (int i = 0; i < 9123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9124(int& val) {
    func_in_9124(val);
    const int x = 9124;
    std::cout << 9124;
    for (int i = 0; i < 9124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9124(int& val) {
    const int x = 9124;
    std::cout << 9124;
    for (int i = 0; i < 9124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9125(int& val) {
    func_in_9125(val);
    const int x = 9125;
    std::cout << 9125;
    for (int i = 0; i < 9125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9125(int& val) {
    const int x = 9125;
    std::cout << 9125;
    for (int i = 0; i < 9125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9126(int& val) {
    func_in_9126(val);
    const int x = 9126;
    std::cout << 9126;
    for (int i = 0; i < 9126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9126(int& val) {
    const int x = 9126;
    std::cout << 9126;
    for (int i = 0; i < 9126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9127(int& val) {
    func_in_9127(val);
    const int x = 9127;
    std::cout << 9127;
    for (int i = 0; i < 9127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9127(int& val) {
    const int x = 9127;
    std::cout << 9127;
    for (int i = 0; i < 9127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9128(int& val) {
    func_in_9128(val);
    const int x = 9128;
    std::cout << 9128;
    for (int i = 0; i < 9128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9128(int& val) {
    const int x = 9128;
    std::cout << 9128;
    for (int i = 0; i < 9128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9129(int& val) {
    func_in_9129(val);
    const int x = 9129;
    std::cout << 9129;
    for (int i = 0; i < 9129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9129(int& val) {
    const int x = 9129;
    std::cout << 9129;
    for (int i = 0; i < 9129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9130(int& val) {
    func_in_9130(val);
    const int x = 9130;
    std::cout << 9130;
    for (int i = 0; i < 9130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9130(int& val) {
    const int x = 9130;
    std::cout << 9130;
    for (int i = 0; i < 9130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9131(int& val) {
    func_in_9131(val);
    const int x = 9131;
    std::cout << 9131;
    for (int i = 0; i < 9131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9131(int& val) {
    const int x = 9131;
    std::cout << 9131;
    for (int i = 0; i < 9131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9132(int& val) {
    func_in_9132(val);
    const int x = 9132;
    std::cout << 9132;
    for (int i = 0; i < 9132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9132(int& val) {
    const int x = 9132;
    std::cout << 9132;
    for (int i = 0; i < 9132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9133(int& val) {
    func_in_9133(val);
    const int x = 9133;
    std::cout << 9133;
    for (int i = 0; i < 9133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9133(int& val) {
    const int x = 9133;
    std::cout << 9133;
    for (int i = 0; i < 9133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9134(int& val) {
    func_in_9134(val);
    const int x = 9134;
    std::cout << 9134;
    for (int i = 0; i < 9134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9134(int& val) {
    const int x = 9134;
    std::cout << 9134;
    for (int i = 0; i < 9134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9135(int& val) {
    func_in_9135(val);
    const int x = 9135;
    std::cout << 9135;
    for (int i = 0; i < 9135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9135(int& val) {
    const int x = 9135;
    std::cout << 9135;
    for (int i = 0; i < 9135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9136(int& val) {
    func_in_9136(val);
    const int x = 9136;
    std::cout << 9136;
    for (int i = 0; i < 9136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9136(int& val) {
    const int x = 9136;
    std::cout << 9136;
    for (int i = 0; i < 9136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9137(int& val) {
    func_in_9137(val);
    const int x = 9137;
    std::cout << 9137;
    for (int i = 0; i < 9137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9137(int& val) {
    const int x = 9137;
    std::cout << 9137;
    for (int i = 0; i < 9137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9138(int& val) {
    func_in_9138(val);
    const int x = 9138;
    std::cout << 9138;
    for (int i = 0; i < 9138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9138(int& val) {
    const int x = 9138;
    std::cout << 9138;
    for (int i = 0; i < 9138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9139(int& val) {
    func_in_9139(val);
    const int x = 9139;
    std::cout << 9139;
    for (int i = 0; i < 9139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9139(int& val) {
    const int x = 9139;
    std::cout << 9139;
    for (int i = 0; i < 9139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9140(int& val) {
    func_in_9140(val);
    const int x = 9140;
    std::cout << 9140;
    for (int i = 0; i < 9140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9140(int& val) {
    const int x = 9140;
    std::cout << 9140;
    for (int i = 0; i < 9140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9141(int& val) {
    func_in_9141(val);
    const int x = 9141;
    std::cout << 9141;
    for (int i = 0; i < 9141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9141(int& val) {
    const int x = 9141;
    std::cout << 9141;
    for (int i = 0; i < 9141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9142(int& val) {
    func_in_9142(val);
    const int x = 9142;
    std::cout << 9142;
    for (int i = 0; i < 9142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9142(int& val) {
    const int x = 9142;
    std::cout << 9142;
    for (int i = 0; i < 9142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9143(int& val) {
    func_in_9143(val);
    const int x = 9143;
    std::cout << 9143;
    for (int i = 0; i < 9143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9143(int& val) {
    const int x = 9143;
    std::cout << 9143;
    for (int i = 0; i < 9143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9144(int& val) {
    func_in_9144(val);
    const int x = 9144;
    std::cout << 9144;
    for (int i = 0; i < 9144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9144(int& val) {
    const int x = 9144;
    std::cout << 9144;
    for (int i = 0; i < 9144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9145(int& val) {
    func_in_9145(val);
    const int x = 9145;
    std::cout << 9145;
    for (int i = 0; i < 9145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9145(int& val) {
    const int x = 9145;
    std::cout << 9145;
    for (int i = 0; i < 9145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9146(int& val) {
    func_in_9146(val);
    const int x = 9146;
    std::cout << 9146;
    for (int i = 0; i < 9146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9146(int& val) {
    const int x = 9146;
    std::cout << 9146;
    for (int i = 0; i < 9146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9147(int& val) {
    func_in_9147(val);
    const int x = 9147;
    std::cout << 9147;
    for (int i = 0; i < 9147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9147(int& val) {
    const int x = 9147;
    std::cout << 9147;
    for (int i = 0; i < 9147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9148(int& val) {
    func_in_9148(val);
    const int x = 9148;
    std::cout << 9148;
    for (int i = 0; i < 9148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9148(int& val) {
    const int x = 9148;
    std::cout << 9148;
    for (int i = 0; i < 9148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9149(int& val) {
    func_in_9149(val);
    const int x = 9149;
    std::cout << 9149;
    for (int i = 0; i < 9149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9149(int& val) {
    const int x = 9149;
    std::cout << 9149;
    for (int i = 0; i < 9149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9150(int& val) {
    func_in_9150(val);
    const int x = 9150;
    std::cout << 9150;
    for (int i = 0; i < 9150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9150(int& val) {
    const int x = 9150;
    std::cout << 9150;
    for (int i = 0; i < 9150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9151(int& val) {
    func_in_9151(val);
    const int x = 9151;
    std::cout << 9151;
    for (int i = 0; i < 9151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9151(int& val) {
    const int x = 9151;
    std::cout << 9151;
    for (int i = 0; i < 9151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9152(int& val) {
    func_in_9152(val);
    const int x = 9152;
    std::cout << 9152;
    for (int i = 0; i < 9152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9152(int& val) {
    const int x = 9152;
    std::cout << 9152;
    for (int i = 0; i < 9152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9153(int& val) {
    func_in_9153(val);
    const int x = 9153;
    std::cout << 9153;
    for (int i = 0; i < 9153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9153(int& val) {
    const int x = 9153;
    std::cout << 9153;
    for (int i = 0; i < 9153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9154(int& val) {
    func_in_9154(val);
    const int x = 9154;
    std::cout << 9154;
    for (int i = 0; i < 9154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9154(int& val) {
    const int x = 9154;
    std::cout << 9154;
    for (int i = 0; i < 9154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9155(int& val) {
    func_in_9155(val);
    const int x = 9155;
    std::cout << 9155;
    for (int i = 0; i < 9155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9155(int& val) {
    const int x = 9155;
    std::cout << 9155;
    for (int i = 0; i < 9155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9156(int& val) {
    func_in_9156(val);
    const int x = 9156;
    std::cout << 9156;
    for (int i = 0; i < 9156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9156(int& val) {
    const int x = 9156;
    std::cout << 9156;
    for (int i = 0; i < 9156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9157(int& val) {
    func_in_9157(val);
    const int x = 9157;
    std::cout << 9157;
    for (int i = 0; i < 9157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9157(int& val) {
    const int x = 9157;
    std::cout << 9157;
    for (int i = 0; i < 9157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9158(int& val) {
    func_in_9158(val);
    const int x = 9158;
    std::cout << 9158;
    for (int i = 0; i < 9158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9158(int& val) {
    const int x = 9158;
    std::cout << 9158;
    for (int i = 0; i < 9158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9159(int& val) {
    func_in_9159(val);
    const int x = 9159;
    std::cout << 9159;
    for (int i = 0; i < 9159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9159(int& val) {
    const int x = 9159;
    std::cout << 9159;
    for (int i = 0; i < 9159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9160(int& val) {
    func_in_9160(val);
    const int x = 9160;
    std::cout << 9160;
    for (int i = 0; i < 9160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9160(int& val) {
    const int x = 9160;
    std::cout << 9160;
    for (int i = 0; i < 9160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9161(int& val) {
    func_in_9161(val);
    const int x = 9161;
    std::cout << 9161;
    for (int i = 0; i < 9161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9161(int& val) {
    const int x = 9161;
    std::cout << 9161;
    for (int i = 0; i < 9161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9162(int& val) {
    func_in_9162(val);
    const int x = 9162;
    std::cout << 9162;
    for (int i = 0; i < 9162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9162(int& val) {
    const int x = 9162;
    std::cout << 9162;
    for (int i = 0; i < 9162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9163(int& val) {
    func_in_9163(val);
    const int x = 9163;
    std::cout << 9163;
    for (int i = 0; i < 9163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9163(int& val) {
    const int x = 9163;
    std::cout << 9163;
    for (int i = 0; i < 9163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9164(int& val) {
    func_in_9164(val);
    const int x = 9164;
    std::cout << 9164;
    for (int i = 0; i < 9164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9164(int& val) {
    const int x = 9164;
    std::cout << 9164;
    for (int i = 0; i < 9164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9165(int& val) {
    func_in_9165(val);
    const int x = 9165;
    std::cout << 9165;
    for (int i = 0; i < 9165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9165(int& val) {
    const int x = 9165;
    std::cout << 9165;
    for (int i = 0; i < 9165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9166(int& val) {
    func_in_9166(val);
    const int x = 9166;
    std::cout << 9166;
    for (int i = 0; i < 9166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9166(int& val) {
    const int x = 9166;
    std::cout << 9166;
    for (int i = 0; i < 9166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9167(int& val) {
    func_in_9167(val);
    const int x = 9167;
    std::cout << 9167;
    for (int i = 0; i < 9167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9167(int& val) {
    const int x = 9167;
    std::cout << 9167;
    for (int i = 0; i < 9167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9168(int& val) {
    func_in_9168(val);
    const int x = 9168;
    std::cout << 9168;
    for (int i = 0; i < 9168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9168(int& val) {
    const int x = 9168;
    std::cout << 9168;
    for (int i = 0; i < 9168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9169(int& val) {
    func_in_9169(val);
    const int x = 9169;
    std::cout << 9169;
    for (int i = 0; i < 9169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9169(int& val) {
    const int x = 9169;
    std::cout << 9169;
    for (int i = 0; i < 9169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9170(int& val) {
    func_in_9170(val);
    const int x = 9170;
    std::cout << 9170;
    for (int i = 0; i < 9170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9170(int& val) {
    const int x = 9170;
    std::cout << 9170;
    for (int i = 0; i < 9170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9171(int& val) {
    func_in_9171(val);
    const int x = 9171;
    std::cout << 9171;
    for (int i = 0; i < 9171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9171(int& val) {
    const int x = 9171;
    std::cout << 9171;
    for (int i = 0; i < 9171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9172(int& val) {
    func_in_9172(val);
    const int x = 9172;
    std::cout << 9172;
    for (int i = 0; i < 9172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9172(int& val) {
    const int x = 9172;
    std::cout << 9172;
    for (int i = 0; i < 9172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9173(int& val) {
    func_in_9173(val);
    const int x = 9173;
    std::cout << 9173;
    for (int i = 0; i < 9173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9173(int& val) {
    const int x = 9173;
    std::cout << 9173;
    for (int i = 0; i < 9173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9174(int& val) {
    func_in_9174(val);
    const int x = 9174;
    std::cout << 9174;
    for (int i = 0; i < 9174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9174(int& val) {
    const int x = 9174;
    std::cout << 9174;
    for (int i = 0; i < 9174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9175(int& val) {
    func_in_9175(val);
    const int x = 9175;
    std::cout << 9175;
    for (int i = 0; i < 9175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9175(int& val) {
    const int x = 9175;
    std::cout << 9175;
    for (int i = 0; i < 9175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9176(int& val) {
    func_in_9176(val);
    const int x = 9176;
    std::cout << 9176;
    for (int i = 0; i < 9176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9176(int& val) {
    const int x = 9176;
    std::cout << 9176;
    for (int i = 0; i < 9176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9177(int& val) {
    func_in_9177(val);
    const int x = 9177;
    std::cout << 9177;
    for (int i = 0; i < 9177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9177(int& val) {
    const int x = 9177;
    std::cout << 9177;
    for (int i = 0; i < 9177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9178(int& val) {
    func_in_9178(val);
    const int x = 9178;
    std::cout << 9178;
    for (int i = 0; i < 9178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9178(int& val) {
    const int x = 9178;
    std::cout << 9178;
    for (int i = 0; i < 9178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9179(int& val) {
    func_in_9179(val);
    const int x = 9179;
    std::cout << 9179;
    for (int i = 0; i < 9179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9179(int& val) {
    const int x = 9179;
    std::cout << 9179;
    for (int i = 0; i < 9179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9180(int& val) {
    func_in_9180(val);
    const int x = 9180;
    std::cout << 9180;
    for (int i = 0; i < 9180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9180(int& val) {
    const int x = 9180;
    std::cout << 9180;
    for (int i = 0; i < 9180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9181(int& val) {
    func_in_9181(val);
    const int x = 9181;
    std::cout << 9181;
    for (int i = 0; i < 9181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9181(int& val) {
    const int x = 9181;
    std::cout << 9181;
    for (int i = 0; i < 9181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9182(int& val) {
    func_in_9182(val);
    const int x = 9182;
    std::cout << 9182;
    for (int i = 0; i < 9182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9182(int& val) {
    const int x = 9182;
    std::cout << 9182;
    for (int i = 0; i < 9182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9183(int& val) {
    func_in_9183(val);
    const int x = 9183;
    std::cout << 9183;
    for (int i = 0; i < 9183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9183(int& val) {
    const int x = 9183;
    std::cout << 9183;
    for (int i = 0; i < 9183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9184(int& val) {
    func_in_9184(val);
    const int x = 9184;
    std::cout << 9184;
    for (int i = 0; i < 9184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9184(int& val) {
    const int x = 9184;
    std::cout << 9184;
    for (int i = 0; i < 9184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9185(int& val) {
    func_in_9185(val);
    const int x = 9185;
    std::cout << 9185;
    for (int i = 0; i < 9185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9185(int& val) {
    const int x = 9185;
    std::cout << 9185;
    for (int i = 0; i < 9185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9186(int& val) {
    func_in_9186(val);
    const int x = 9186;
    std::cout << 9186;
    for (int i = 0; i < 9186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9186(int& val) {
    const int x = 9186;
    std::cout << 9186;
    for (int i = 0; i < 9186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9187(int& val) {
    func_in_9187(val);
    const int x = 9187;
    std::cout << 9187;
    for (int i = 0; i < 9187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9187(int& val) {
    const int x = 9187;
    std::cout << 9187;
    for (int i = 0; i < 9187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9188(int& val) {
    func_in_9188(val);
    const int x = 9188;
    std::cout << 9188;
    for (int i = 0; i < 9188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9188(int& val) {
    const int x = 9188;
    std::cout << 9188;
    for (int i = 0; i < 9188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9189(int& val) {
    func_in_9189(val);
    const int x = 9189;
    std::cout << 9189;
    for (int i = 0; i < 9189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9189(int& val) {
    const int x = 9189;
    std::cout << 9189;
    for (int i = 0; i < 9189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9190(int& val) {
    func_in_9190(val);
    const int x = 9190;
    std::cout << 9190;
    for (int i = 0; i < 9190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9190(int& val) {
    const int x = 9190;
    std::cout << 9190;
    for (int i = 0; i < 9190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9191(int& val) {
    func_in_9191(val);
    const int x = 9191;
    std::cout << 9191;
    for (int i = 0; i < 9191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9191(int& val) {
    const int x = 9191;
    std::cout << 9191;
    for (int i = 0; i < 9191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9192(int& val) {
    func_in_9192(val);
    const int x = 9192;
    std::cout << 9192;
    for (int i = 0; i < 9192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9192(int& val) {
    const int x = 9192;
    std::cout << 9192;
    for (int i = 0; i < 9192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9193(int& val) {
    func_in_9193(val);
    const int x = 9193;
    std::cout << 9193;
    for (int i = 0; i < 9193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9193(int& val) {
    const int x = 9193;
    std::cout << 9193;
    for (int i = 0; i < 9193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9194(int& val) {
    func_in_9194(val);
    const int x = 9194;
    std::cout << 9194;
    for (int i = 0; i < 9194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9194(int& val) {
    const int x = 9194;
    std::cout << 9194;
    for (int i = 0; i < 9194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9195(int& val) {
    func_in_9195(val);
    const int x = 9195;
    std::cout << 9195;
    for (int i = 0; i < 9195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9195(int& val) {
    const int x = 9195;
    std::cout << 9195;
    for (int i = 0; i < 9195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9196(int& val) {
    func_in_9196(val);
    const int x = 9196;
    std::cout << 9196;
    for (int i = 0; i < 9196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9196(int& val) {
    const int x = 9196;
    std::cout << 9196;
    for (int i = 0; i < 9196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9197(int& val) {
    func_in_9197(val);
    const int x = 9197;
    std::cout << 9197;
    for (int i = 0; i < 9197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9197(int& val) {
    const int x = 9197;
    std::cout << 9197;
    for (int i = 0; i < 9197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9198(int& val) {
    func_in_9198(val);
    const int x = 9198;
    std::cout << 9198;
    for (int i = 0; i < 9198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9198(int& val) {
    const int x = 9198;
    std::cout << 9198;
    for (int i = 0; i < 9198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9199(int& val) {
    func_in_9199(val);
    const int x = 9199;
    std::cout << 9199;
    for (int i = 0; i < 9199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9199(int& val) {
    const int x = 9199;
    std::cout << 9199;
    for (int i = 0; i < 9199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9200(int& val) {
    func_in_9200(val);
    const int x = 9200;
    std::cout << 9200;
    for (int i = 0; i < 9200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9200(int& val) {
    const int x = 9200;
    std::cout << 9200;
    for (int i = 0; i < 9200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9201(int& val) {
    func_in_9201(val);
    const int x = 9201;
    std::cout << 9201;
    for (int i = 0; i < 9201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9201(int& val) {
    const int x = 9201;
    std::cout << 9201;
    for (int i = 0; i < 9201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9202(int& val) {
    func_in_9202(val);
    const int x = 9202;
    std::cout << 9202;
    for (int i = 0; i < 9202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9202(int& val) {
    const int x = 9202;
    std::cout << 9202;
    for (int i = 0; i < 9202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9203(int& val) {
    func_in_9203(val);
    const int x = 9203;
    std::cout << 9203;
    for (int i = 0; i < 9203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9203(int& val) {
    const int x = 9203;
    std::cout << 9203;
    for (int i = 0; i < 9203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9204(int& val) {
    func_in_9204(val);
    const int x = 9204;
    std::cout << 9204;
    for (int i = 0; i < 9204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9204(int& val) {
    const int x = 9204;
    std::cout << 9204;
    for (int i = 0; i < 9204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9205(int& val) {
    func_in_9205(val);
    const int x = 9205;
    std::cout << 9205;
    for (int i = 0; i < 9205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9205(int& val) {
    const int x = 9205;
    std::cout << 9205;
    for (int i = 0; i < 9205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9206(int& val) {
    func_in_9206(val);
    const int x = 9206;
    std::cout << 9206;
    for (int i = 0; i < 9206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9206(int& val) {
    const int x = 9206;
    std::cout << 9206;
    for (int i = 0; i < 9206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9207(int& val) {
    func_in_9207(val);
    const int x = 9207;
    std::cout << 9207;
    for (int i = 0; i < 9207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9207(int& val) {
    const int x = 9207;
    std::cout << 9207;
    for (int i = 0; i < 9207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9208(int& val) {
    func_in_9208(val);
    const int x = 9208;
    std::cout << 9208;
    for (int i = 0; i < 9208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9208(int& val) {
    const int x = 9208;
    std::cout << 9208;
    for (int i = 0; i < 9208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9209(int& val) {
    func_in_9209(val);
    const int x = 9209;
    std::cout << 9209;
    for (int i = 0; i < 9209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9209(int& val) {
    const int x = 9209;
    std::cout << 9209;
    for (int i = 0; i < 9209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9210(int& val) {
    func_in_9210(val);
    const int x = 9210;
    std::cout << 9210;
    for (int i = 0; i < 9210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9210(int& val) {
    const int x = 9210;
    std::cout << 9210;
    for (int i = 0; i < 9210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9211(int& val) {
    func_in_9211(val);
    const int x = 9211;
    std::cout << 9211;
    for (int i = 0; i < 9211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9211(int& val) {
    const int x = 9211;
    std::cout << 9211;
    for (int i = 0; i < 9211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9212(int& val) {
    func_in_9212(val);
    const int x = 9212;
    std::cout << 9212;
    for (int i = 0; i < 9212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9212(int& val) {
    const int x = 9212;
    std::cout << 9212;
    for (int i = 0; i < 9212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9213(int& val) {
    func_in_9213(val);
    const int x = 9213;
    std::cout << 9213;
    for (int i = 0; i < 9213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9213(int& val) {
    const int x = 9213;
    std::cout << 9213;
    for (int i = 0; i < 9213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9214(int& val) {
    func_in_9214(val);
    const int x = 9214;
    std::cout << 9214;
    for (int i = 0; i < 9214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9214(int& val) {
    const int x = 9214;
    std::cout << 9214;
    for (int i = 0; i < 9214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9215(int& val) {
    func_in_9215(val);
    const int x = 9215;
    std::cout << 9215;
    for (int i = 0; i < 9215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9215(int& val) {
    const int x = 9215;
    std::cout << 9215;
    for (int i = 0; i < 9215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9216(int& val) {
    func_in_9216(val);
    const int x = 9216;
    std::cout << 9216;
    for (int i = 0; i < 9216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9216(int& val) {
    const int x = 9216;
    std::cout << 9216;
    for (int i = 0; i < 9216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9217(int& val) {
    func_in_9217(val);
    const int x = 9217;
    std::cout << 9217;
    for (int i = 0; i < 9217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9217(int& val) {
    const int x = 9217;
    std::cout << 9217;
    for (int i = 0; i < 9217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9218(int& val) {
    func_in_9218(val);
    const int x = 9218;
    std::cout << 9218;
    for (int i = 0; i < 9218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9218(int& val) {
    const int x = 9218;
    std::cout << 9218;
    for (int i = 0; i < 9218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9219(int& val) {
    func_in_9219(val);
    const int x = 9219;
    std::cout << 9219;
    for (int i = 0; i < 9219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9219(int& val) {
    const int x = 9219;
    std::cout << 9219;
    for (int i = 0; i < 9219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9220(int& val) {
    func_in_9220(val);
    const int x = 9220;
    std::cout << 9220;
    for (int i = 0; i < 9220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9220(int& val) {
    const int x = 9220;
    std::cout << 9220;
    for (int i = 0; i < 9220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9221(int& val) {
    func_in_9221(val);
    const int x = 9221;
    std::cout << 9221;
    for (int i = 0; i < 9221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9221(int& val) {
    const int x = 9221;
    std::cout << 9221;
    for (int i = 0; i < 9221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9222(int& val) {
    func_in_9222(val);
    const int x = 9222;
    std::cout << 9222;
    for (int i = 0; i < 9222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9222(int& val) {
    const int x = 9222;
    std::cout << 9222;
    for (int i = 0; i < 9222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9223(int& val) {
    func_in_9223(val);
    const int x = 9223;
    std::cout << 9223;
    for (int i = 0; i < 9223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9223(int& val) {
    const int x = 9223;
    std::cout << 9223;
    for (int i = 0; i < 9223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9224(int& val) {
    func_in_9224(val);
    const int x = 9224;
    std::cout << 9224;
    for (int i = 0; i < 9224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9224(int& val) {
    const int x = 9224;
    std::cout << 9224;
    for (int i = 0; i < 9224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9225(int& val) {
    func_in_9225(val);
    const int x = 9225;
    std::cout << 9225;
    for (int i = 0; i < 9225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9225(int& val) {
    const int x = 9225;
    std::cout << 9225;
    for (int i = 0; i < 9225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9226(int& val) {
    func_in_9226(val);
    const int x = 9226;
    std::cout << 9226;
    for (int i = 0; i < 9226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9226(int& val) {
    const int x = 9226;
    std::cout << 9226;
    for (int i = 0; i < 9226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9227(int& val) {
    func_in_9227(val);
    const int x = 9227;
    std::cout << 9227;
    for (int i = 0; i < 9227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9227(int& val) {
    const int x = 9227;
    std::cout << 9227;
    for (int i = 0; i < 9227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9228(int& val) {
    func_in_9228(val);
    const int x = 9228;
    std::cout << 9228;
    for (int i = 0; i < 9228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9228(int& val) {
    const int x = 9228;
    std::cout << 9228;
    for (int i = 0; i < 9228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9229(int& val) {
    func_in_9229(val);
    const int x = 9229;
    std::cout << 9229;
    for (int i = 0; i < 9229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9229(int& val) {
    const int x = 9229;
    std::cout << 9229;
    for (int i = 0; i < 9229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9230(int& val) {
    func_in_9230(val);
    const int x = 9230;
    std::cout << 9230;
    for (int i = 0; i < 9230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9230(int& val) {
    const int x = 9230;
    std::cout << 9230;
    for (int i = 0; i < 9230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9231(int& val) {
    func_in_9231(val);
    const int x = 9231;
    std::cout << 9231;
    for (int i = 0; i < 9231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9231(int& val) {
    const int x = 9231;
    std::cout << 9231;
    for (int i = 0; i < 9231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9232(int& val) {
    func_in_9232(val);
    const int x = 9232;
    std::cout << 9232;
    for (int i = 0; i < 9232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9232(int& val) {
    const int x = 9232;
    std::cout << 9232;
    for (int i = 0; i < 9232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9233(int& val) {
    func_in_9233(val);
    const int x = 9233;
    std::cout << 9233;
    for (int i = 0; i < 9233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9233(int& val) {
    const int x = 9233;
    std::cout << 9233;
    for (int i = 0; i < 9233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9234(int& val) {
    func_in_9234(val);
    const int x = 9234;
    std::cout << 9234;
    for (int i = 0; i < 9234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9234(int& val) {
    const int x = 9234;
    std::cout << 9234;
    for (int i = 0; i < 9234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9235(int& val) {
    func_in_9235(val);
    const int x = 9235;
    std::cout << 9235;
    for (int i = 0; i < 9235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9235(int& val) {
    const int x = 9235;
    std::cout << 9235;
    for (int i = 0; i < 9235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9236(int& val) {
    func_in_9236(val);
    const int x = 9236;
    std::cout << 9236;
    for (int i = 0; i < 9236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9236(int& val) {
    const int x = 9236;
    std::cout << 9236;
    for (int i = 0; i < 9236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9237(int& val) {
    func_in_9237(val);
    const int x = 9237;
    std::cout << 9237;
    for (int i = 0; i < 9237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9237(int& val) {
    const int x = 9237;
    std::cout << 9237;
    for (int i = 0; i < 9237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9238(int& val) {
    func_in_9238(val);
    const int x = 9238;
    std::cout << 9238;
    for (int i = 0; i < 9238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9238(int& val) {
    const int x = 9238;
    std::cout << 9238;
    for (int i = 0; i < 9238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9239(int& val) {
    func_in_9239(val);
    const int x = 9239;
    std::cout << 9239;
    for (int i = 0; i < 9239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9239(int& val) {
    const int x = 9239;
    std::cout << 9239;
    for (int i = 0; i < 9239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9240(int& val) {
    func_in_9240(val);
    const int x = 9240;
    std::cout << 9240;
    for (int i = 0; i < 9240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9240(int& val) {
    const int x = 9240;
    std::cout << 9240;
    for (int i = 0; i < 9240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9241(int& val) {
    func_in_9241(val);
    const int x = 9241;
    std::cout << 9241;
    for (int i = 0; i < 9241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9241(int& val) {
    const int x = 9241;
    std::cout << 9241;
    for (int i = 0; i < 9241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9242(int& val) {
    func_in_9242(val);
    const int x = 9242;
    std::cout << 9242;
    for (int i = 0; i < 9242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9242(int& val) {
    const int x = 9242;
    std::cout << 9242;
    for (int i = 0; i < 9242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9243(int& val) {
    func_in_9243(val);
    const int x = 9243;
    std::cout << 9243;
    for (int i = 0; i < 9243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9243(int& val) {
    const int x = 9243;
    std::cout << 9243;
    for (int i = 0; i < 9243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9244(int& val) {
    func_in_9244(val);
    const int x = 9244;
    std::cout << 9244;
    for (int i = 0; i < 9244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9244(int& val) {
    const int x = 9244;
    std::cout << 9244;
    for (int i = 0; i < 9244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9245(int& val) {
    func_in_9245(val);
    const int x = 9245;
    std::cout << 9245;
    for (int i = 0; i < 9245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9245(int& val) {
    const int x = 9245;
    std::cout << 9245;
    for (int i = 0; i < 9245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9246(int& val) {
    func_in_9246(val);
    const int x = 9246;
    std::cout << 9246;
    for (int i = 0; i < 9246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9246(int& val) {
    const int x = 9246;
    std::cout << 9246;
    for (int i = 0; i < 9246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9247(int& val) {
    func_in_9247(val);
    const int x = 9247;
    std::cout << 9247;
    for (int i = 0; i < 9247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9247(int& val) {
    const int x = 9247;
    std::cout << 9247;
    for (int i = 0; i < 9247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9248(int& val) {
    func_in_9248(val);
    const int x = 9248;
    std::cout << 9248;
    for (int i = 0; i < 9248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9248(int& val) {
    const int x = 9248;
    std::cout << 9248;
    for (int i = 0; i < 9248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9249(int& val) {
    func_in_9249(val);
    const int x = 9249;
    std::cout << 9249;
    for (int i = 0; i < 9249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9249(int& val) {
    const int x = 9249;
    std::cout << 9249;
    for (int i = 0; i < 9249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9250(int& val) {
    func_in_9250(val);
    const int x = 9250;
    std::cout << 9250;
    for (int i = 0; i < 9250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9250(int& val) {
    const int x = 9250;
    std::cout << 9250;
    for (int i = 0; i < 9250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9251(int& val) {
    func_in_9251(val);
    const int x = 9251;
    std::cout << 9251;
    for (int i = 0; i < 9251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9251(int& val) {
    const int x = 9251;
    std::cout << 9251;
    for (int i = 0; i < 9251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9252(int& val) {
    func_in_9252(val);
    const int x = 9252;
    std::cout << 9252;
    for (int i = 0; i < 9252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9252(int& val) {
    const int x = 9252;
    std::cout << 9252;
    for (int i = 0; i < 9252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9253(int& val) {
    func_in_9253(val);
    const int x = 9253;
    std::cout << 9253;
    for (int i = 0; i < 9253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9253(int& val) {
    const int x = 9253;
    std::cout << 9253;
    for (int i = 0; i < 9253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9254(int& val) {
    func_in_9254(val);
    const int x = 9254;
    std::cout << 9254;
    for (int i = 0; i < 9254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9254(int& val) {
    const int x = 9254;
    std::cout << 9254;
    for (int i = 0; i < 9254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9255(int& val) {
    func_in_9255(val);
    const int x = 9255;
    std::cout << 9255;
    for (int i = 0; i < 9255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9255(int& val) {
    const int x = 9255;
    std::cout << 9255;
    for (int i = 0; i < 9255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9256(int& val) {
    func_in_9256(val);
    const int x = 9256;
    std::cout << 9256;
    for (int i = 0; i < 9256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9256(int& val) {
    const int x = 9256;
    std::cout << 9256;
    for (int i = 0; i < 9256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9257(int& val) {
    func_in_9257(val);
    const int x = 9257;
    std::cout << 9257;
    for (int i = 0; i < 9257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9257(int& val) {
    const int x = 9257;
    std::cout << 9257;
    for (int i = 0; i < 9257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9258(int& val) {
    func_in_9258(val);
    const int x = 9258;
    std::cout << 9258;
    for (int i = 0; i < 9258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9258(int& val) {
    const int x = 9258;
    std::cout << 9258;
    for (int i = 0; i < 9258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9259(int& val) {
    func_in_9259(val);
    const int x = 9259;
    std::cout << 9259;
    for (int i = 0; i < 9259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9259(int& val) {
    const int x = 9259;
    std::cout << 9259;
    for (int i = 0; i < 9259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9260(int& val) {
    func_in_9260(val);
    const int x = 9260;
    std::cout << 9260;
    for (int i = 0; i < 9260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9260(int& val) {
    const int x = 9260;
    std::cout << 9260;
    for (int i = 0; i < 9260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9261(int& val) {
    func_in_9261(val);
    const int x = 9261;
    std::cout << 9261;
    for (int i = 0; i < 9261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9261(int& val) {
    const int x = 9261;
    std::cout << 9261;
    for (int i = 0; i < 9261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9262(int& val) {
    func_in_9262(val);
    const int x = 9262;
    std::cout << 9262;
    for (int i = 0; i < 9262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9262(int& val) {
    const int x = 9262;
    std::cout << 9262;
    for (int i = 0; i < 9262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9263(int& val) {
    func_in_9263(val);
    const int x = 9263;
    std::cout << 9263;
    for (int i = 0; i < 9263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9263(int& val) {
    const int x = 9263;
    std::cout << 9263;
    for (int i = 0; i < 9263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9264(int& val) {
    func_in_9264(val);
    const int x = 9264;
    std::cout << 9264;
    for (int i = 0; i < 9264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9264(int& val) {
    const int x = 9264;
    std::cout << 9264;
    for (int i = 0; i < 9264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9265(int& val) {
    func_in_9265(val);
    const int x = 9265;
    std::cout << 9265;
    for (int i = 0; i < 9265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9265(int& val) {
    const int x = 9265;
    std::cout << 9265;
    for (int i = 0; i < 9265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9266(int& val) {
    func_in_9266(val);
    const int x = 9266;
    std::cout << 9266;
    for (int i = 0; i < 9266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9266(int& val) {
    const int x = 9266;
    std::cout << 9266;
    for (int i = 0; i < 9266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9267(int& val) {
    func_in_9267(val);
    const int x = 9267;
    std::cout << 9267;
    for (int i = 0; i < 9267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9267(int& val) {
    const int x = 9267;
    std::cout << 9267;
    for (int i = 0; i < 9267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9268(int& val) {
    func_in_9268(val);
    const int x = 9268;
    std::cout << 9268;
    for (int i = 0; i < 9268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9268(int& val) {
    const int x = 9268;
    std::cout << 9268;
    for (int i = 0; i < 9268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9269(int& val) {
    func_in_9269(val);
    const int x = 9269;
    std::cout << 9269;
    for (int i = 0; i < 9269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9269(int& val) {
    const int x = 9269;
    std::cout << 9269;
    for (int i = 0; i < 9269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9270(int& val) {
    func_in_9270(val);
    const int x = 9270;
    std::cout << 9270;
    for (int i = 0; i < 9270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9270(int& val) {
    const int x = 9270;
    std::cout << 9270;
    for (int i = 0; i < 9270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9271(int& val) {
    func_in_9271(val);
    const int x = 9271;
    std::cout << 9271;
    for (int i = 0; i < 9271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9271(int& val) {
    const int x = 9271;
    std::cout << 9271;
    for (int i = 0; i < 9271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9272(int& val) {
    func_in_9272(val);
    const int x = 9272;
    std::cout << 9272;
    for (int i = 0; i < 9272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9272(int& val) {
    const int x = 9272;
    std::cout << 9272;
    for (int i = 0; i < 9272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9273(int& val) {
    func_in_9273(val);
    const int x = 9273;
    std::cout << 9273;
    for (int i = 0; i < 9273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9273(int& val) {
    const int x = 9273;
    std::cout << 9273;
    for (int i = 0; i < 9273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9274(int& val) {
    func_in_9274(val);
    const int x = 9274;
    std::cout << 9274;
    for (int i = 0; i < 9274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9274(int& val) {
    const int x = 9274;
    std::cout << 9274;
    for (int i = 0; i < 9274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9275(int& val) {
    func_in_9275(val);
    const int x = 9275;
    std::cout << 9275;
    for (int i = 0; i < 9275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9275(int& val) {
    const int x = 9275;
    std::cout << 9275;
    for (int i = 0; i < 9275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9276(int& val) {
    func_in_9276(val);
    const int x = 9276;
    std::cout << 9276;
    for (int i = 0; i < 9276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9276(int& val) {
    const int x = 9276;
    std::cout << 9276;
    for (int i = 0; i < 9276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9277(int& val) {
    func_in_9277(val);
    const int x = 9277;
    std::cout << 9277;
    for (int i = 0; i < 9277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9277(int& val) {
    const int x = 9277;
    std::cout << 9277;
    for (int i = 0; i < 9277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9278(int& val) {
    func_in_9278(val);
    const int x = 9278;
    std::cout << 9278;
    for (int i = 0; i < 9278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9278(int& val) {
    const int x = 9278;
    std::cout << 9278;
    for (int i = 0; i < 9278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9279(int& val) {
    func_in_9279(val);
    const int x = 9279;
    std::cout << 9279;
    for (int i = 0; i < 9279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9279(int& val) {
    const int x = 9279;
    std::cout << 9279;
    for (int i = 0; i < 9279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9280(int& val) {
    func_in_9280(val);
    const int x = 9280;
    std::cout << 9280;
    for (int i = 0; i < 9280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9280(int& val) {
    const int x = 9280;
    std::cout << 9280;
    for (int i = 0; i < 9280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9281(int& val) {
    func_in_9281(val);
    const int x = 9281;
    std::cout << 9281;
    for (int i = 0; i < 9281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9281(int& val) {
    const int x = 9281;
    std::cout << 9281;
    for (int i = 0; i < 9281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9282(int& val) {
    func_in_9282(val);
    const int x = 9282;
    std::cout << 9282;
    for (int i = 0; i < 9282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9282(int& val) {
    const int x = 9282;
    std::cout << 9282;
    for (int i = 0; i < 9282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9283(int& val) {
    func_in_9283(val);
    const int x = 9283;
    std::cout << 9283;
    for (int i = 0; i < 9283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9283(int& val) {
    const int x = 9283;
    std::cout << 9283;
    for (int i = 0; i < 9283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9284(int& val) {
    func_in_9284(val);
    const int x = 9284;
    std::cout << 9284;
    for (int i = 0; i < 9284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9284(int& val) {
    const int x = 9284;
    std::cout << 9284;
    for (int i = 0; i < 9284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9285(int& val) {
    func_in_9285(val);
    const int x = 9285;
    std::cout << 9285;
    for (int i = 0; i < 9285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9285(int& val) {
    const int x = 9285;
    std::cout << 9285;
    for (int i = 0; i < 9285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9286(int& val) {
    func_in_9286(val);
    const int x = 9286;
    std::cout << 9286;
    for (int i = 0; i < 9286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9286(int& val) {
    const int x = 9286;
    std::cout << 9286;
    for (int i = 0; i < 9286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9287(int& val) {
    func_in_9287(val);
    const int x = 9287;
    std::cout << 9287;
    for (int i = 0; i < 9287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9287(int& val) {
    const int x = 9287;
    std::cout << 9287;
    for (int i = 0; i < 9287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9288(int& val) {
    func_in_9288(val);
    const int x = 9288;
    std::cout << 9288;
    for (int i = 0; i < 9288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9288(int& val) {
    const int x = 9288;
    std::cout << 9288;
    for (int i = 0; i < 9288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9289(int& val) {
    func_in_9289(val);
    const int x = 9289;
    std::cout << 9289;
    for (int i = 0; i < 9289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9289(int& val) {
    const int x = 9289;
    std::cout << 9289;
    for (int i = 0; i < 9289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9290(int& val) {
    func_in_9290(val);
    const int x = 9290;
    std::cout << 9290;
    for (int i = 0; i < 9290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9290(int& val) {
    const int x = 9290;
    std::cout << 9290;
    for (int i = 0; i < 9290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9291(int& val) {
    func_in_9291(val);
    const int x = 9291;
    std::cout << 9291;
    for (int i = 0; i < 9291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9291(int& val) {
    const int x = 9291;
    std::cout << 9291;
    for (int i = 0; i < 9291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9292(int& val) {
    func_in_9292(val);
    const int x = 9292;
    std::cout << 9292;
    for (int i = 0; i < 9292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9292(int& val) {
    const int x = 9292;
    std::cout << 9292;
    for (int i = 0; i < 9292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9293(int& val) {
    func_in_9293(val);
    const int x = 9293;
    std::cout << 9293;
    for (int i = 0; i < 9293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9293(int& val) {
    const int x = 9293;
    std::cout << 9293;
    for (int i = 0; i < 9293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9294(int& val) {
    func_in_9294(val);
    const int x = 9294;
    std::cout << 9294;
    for (int i = 0; i < 9294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9294(int& val) {
    const int x = 9294;
    std::cout << 9294;
    for (int i = 0; i < 9294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9295(int& val) {
    func_in_9295(val);
    const int x = 9295;
    std::cout << 9295;
    for (int i = 0; i < 9295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9295(int& val) {
    const int x = 9295;
    std::cout << 9295;
    for (int i = 0; i < 9295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9296(int& val) {
    func_in_9296(val);
    const int x = 9296;
    std::cout << 9296;
    for (int i = 0; i < 9296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9296(int& val) {
    const int x = 9296;
    std::cout << 9296;
    for (int i = 0; i < 9296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9297(int& val) {
    func_in_9297(val);
    const int x = 9297;
    std::cout << 9297;
    for (int i = 0; i < 9297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9297(int& val) {
    const int x = 9297;
    std::cout << 9297;
    for (int i = 0; i < 9297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9298(int& val) {
    func_in_9298(val);
    const int x = 9298;
    std::cout << 9298;
    for (int i = 0; i < 9298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9298(int& val) {
    const int x = 9298;
    std::cout << 9298;
    for (int i = 0; i < 9298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9299(int& val) {
    func_in_9299(val);
    const int x = 9299;
    std::cout << 9299;
    for (int i = 0; i < 9299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9299(int& val) {
    const int x = 9299;
    std::cout << 9299;
    for (int i = 0; i < 9299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9300(int& val) {
    func_in_9300(val);
    const int x = 9300;
    std::cout << 9300;
    for (int i = 0; i < 9300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9300(int& val) {
    const int x = 9300;
    std::cout << 9300;
    for (int i = 0; i < 9300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9301(int& val) {
    func_in_9301(val);
    const int x = 9301;
    std::cout << 9301;
    for (int i = 0; i < 9301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9301(int& val) {
    const int x = 9301;
    std::cout << 9301;
    for (int i = 0; i < 9301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9302(int& val) {
    func_in_9302(val);
    const int x = 9302;
    std::cout << 9302;
    for (int i = 0; i < 9302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9302(int& val) {
    const int x = 9302;
    std::cout << 9302;
    for (int i = 0; i < 9302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9303(int& val) {
    func_in_9303(val);
    const int x = 9303;
    std::cout << 9303;
    for (int i = 0; i < 9303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9303(int& val) {
    const int x = 9303;
    std::cout << 9303;
    for (int i = 0; i < 9303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9304(int& val) {
    func_in_9304(val);
    const int x = 9304;
    std::cout << 9304;
    for (int i = 0; i < 9304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9304(int& val) {
    const int x = 9304;
    std::cout << 9304;
    for (int i = 0; i < 9304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9305(int& val) {
    func_in_9305(val);
    const int x = 9305;
    std::cout << 9305;
    for (int i = 0; i < 9305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9305(int& val) {
    const int x = 9305;
    std::cout << 9305;
    for (int i = 0; i < 9305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9306(int& val) {
    func_in_9306(val);
    const int x = 9306;
    std::cout << 9306;
    for (int i = 0; i < 9306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9306(int& val) {
    const int x = 9306;
    std::cout << 9306;
    for (int i = 0; i < 9306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9307(int& val) {
    func_in_9307(val);
    const int x = 9307;
    std::cout << 9307;
    for (int i = 0; i < 9307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9307(int& val) {
    const int x = 9307;
    std::cout << 9307;
    for (int i = 0; i < 9307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9308(int& val) {
    func_in_9308(val);
    const int x = 9308;
    std::cout << 9308;
    for (int i = 0; i < 9308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9308(int& val) {
    const int x = 9308;
    std::cout << 9308;
    for (int i = 0; i < 9308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9309(int& val) {
    func_in_9309(val);
    const int x = 9309;
    std::cout << 9309;
    for (int i = 0; i < 9309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9309(int& val) {
    const int x = 9309;
    std::cout << 9309;
    for (int i = 0; i < 9309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9310(int& val) {
    func_in_9310(val);
    const int x = 9310;
    std::cout << 9310;
    for (int i = 0; i < 9310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9310(int& val) {
    const int x = 9310;
    std::cout << 9310;
    for (int i = 0; i < 9310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9311(int& val) {
    func_in_9311(val);
    const int x = 9311;
    std::cout << 9311;
    for (int i = 0; i < 9311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9311(int& val) {
    const int x = 9311;
    std::cout << 9311;
    for (int i = 0; i < 9311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9312(int& val) {
    func_in_9312(val);
    const int x = 9312;
    std::cout << 9312;
    for (int i = 0; i < 9312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9312(int& val) {
    const int x = 9312;
    std::cout << 9312;
    for (int i = 0; i < 9312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9313(int& val) {
    func_in_9313(val);
    const int x = 9313;
    std::cout << 9313;
    for (int i = 0; i < 9313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9313(int& val) {
    const int x = 9313;
    std::cout << 9313;
    for (int i = 0; i < 9313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9314(int& val) {
    func_in_9314(val);
    const int x = 9314;
    std::cout << 9314;
    for (int i = 0; i < 9314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9314(int& val) {
    const int x = 9314;
    std::cout << 9314;
    for (int i = 0; i < 9314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9315(int& val) {
    func_in_9315(val);
    const int x = 9315;
    std::cout << 9315;
    for (int i = 0; i < 9315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9315(int& val) {
    const int x = 9315;
    std::cout << 9315;
    for (int i = 0; i < 9315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9316(int& val) {
    func_in_9316(val);
    const int x = 9316;
    std::cout << 9316;
    for (int i = 0; i < 9316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9316(int& val) {
    const int x = 9316;
    std::cout << 9316;
    for (int i = 0; i < 9316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9317(int& val) {
    func_in_9317(val);
    const int x = 9317;
    std::cout << 9317;
    for (int i = 0; i < 9317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9317(int& val) {
    const int x = 9317;
    std::cout << 9317;
    for (int i = 0; i < 9317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9318(int& val) {
    func_in_9318(val);
    const int x = 9318;
    std::cout << 9318;
    for (int i = 0; i < 9318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9318(int& val) {
    const int x = 9318;
    std::cout << 9318;
    for (int i = 0; i < 9318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9319(int& val) {
    func_in_9319(val);
    const int x = 9319;
    std::cout << 9319;
    for (int i = 0; i < 9319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9319(int& val) {
    const int x = 9319;
    std::cout << 9319;
    for (int i = 0; i < 9319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9320(int& val) {
    func_in_9320(val);
    const int x = 9320;
    std::cout << 9320;
    for (int i = 0; i < 9320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9320(int& val) {
    const int x = 9320;
    std::cout << 9320;
    for (int i = 0; i < 9320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9321(int& val) {
    func_in_9321(val);
    const int x = 9321;
    std::cout << 9321;
    for (int i = 0; i < 9321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9321(int& val) {
    const int x = 9321;
    std::cout << 9321;
    for (int i = 0; i < 9321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9322(int& val) {
    func_in_9322(val);
    const int x = 9322;
    std::cout << 9322;
    for (int i = 0; i < 9322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9322(int& val) {
    const int x = 9322;
    std::cout << 9322;
    for (int i = 0; i < 9322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9323(int& val) {
    func_in_9323(val);
    const int x = 9323;
    std::cout << 9323;
    for (int i = 0; i < 9323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9323(int& val) {
    const int x = 9323;
    std::cout << 9323;
    for (int i = 0; i < 9323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9324(int& val) {
    func_in_9324(val);
    const int x = 9324;
    std::cout << 9324;
    for (int i = 0; i < 9324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9324(int& val) {
    const int x = 9324;
    std::cout << 9324;
    for (int i = 0; i < 9324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9325(int& val) {
    func_in_9325(val);
    const int x = 9325;
    std::cout << 9325;
    for (int i = 0; i < 9325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9325(int& val) {
    const int x = 9325;
    std::cout << 9325;
    for (int i = 0; i < 9325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9326(int& val) {
    func_in_9326(val);
    const int x = 9326;
    std::cout << 9326;
    for (int i = 0; i < 9326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9326(int& val) {
    const int x = 9326;
    std::cout << 9326;
    for (int i = 0; i < 9326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9327(int& val) {
    func_in_9327(val);
    const int x = 9327;
    std::cout << 9327;
    for (int i = 0; i < 9327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9327(int& val) {
    const int x = 9327;
    std::cout << 9327;
    for (int i = 0; i < 9327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9328(int& val) {
    func_in_9328(val);
    const int x = 9328;
    std::cout << 9328;
    for (int i = 0; i < 9328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9328(int& val) {
    const int x = 9328;
    std::cout << 9328;
    for (int i = 0; i < 9328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9329(int& val) {
    func_in_9329(val);
    const int x = 9329;
    std::cout << 9329;
    for (int i = 0; i < 9329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9329(int& val) {
    const int x = 9329;
    std::cout << 9329;
    for (int i = 0; i < 9329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9330(int& val) {
    func_in_9330(val);
    const int x = 9330;
    std::cout << 9330;
    for (int i = 0; i < 9330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9330(int& val) {
    const int x = 9330;
    std::cout << 9330;
    for (int i = 0; i < 9330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9331(int& val) {
    func_in_9331(val);
    const int x = 9331;
    std::cout << 9331;
    for (int i = 0; i < 9331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9331(int& val) {
    const int x = 9331;
    std::cout << 9331;
    for (int i = 0; i < 9331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9332(int& val) {
    func_in_9332(val);
    const int x = 9332;
    std::cout << 9332;
    for (int i = 0; i < 9332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9332(int& val) {
    const int x = 9332;
    std::cout << 9332;
    for (int i = 0; i < 9332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9333(int& val) {
    func_in_9333(val);
    const int x = 9333;
    std::cout << 9333;
    for (int i = 0; i < 9333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9333(int& val) {
    const int x = 9333;
    std::cout << 9333;
    for (int i = 0; i < 9333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9334(int& val) {
    func_in_9334(val);
    const int x = 9334;
    std::cout << 9334;
    for (int i = 0; i < 9334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9334(int& val) {
    const int x = 9334;
    std::cout << 9334;
    for (int i = 0; i < 9334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9335(int& val) {
    func_in_9335(val);
    const int x = 9335;
    std::cout << 9335;
    for (int i = 0; i < 9335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9335(int& val) {
    const int x = 9335;
    std::cout << 9335;
    for (int i = 0; i < 9335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9336(int& val) {
    func_in_9336(val);
    const int x = 9336;
    std::cout << 9336;
    for (int i = 0; i < 9336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9336(int& val) {
    const int x = 9336;
    std::cout << 9336;
    for (int i = 0; i < 9336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9337(int& val) {
    func_in_9337(val);
    const int x = 9337;
    std::cout << 9337;
    for (int i = 0; i < 9337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9337(int& val) {
    const int x = 9337;
    std::cout << 9337;
    for (int i = 0; i < 9337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9338(int& val) {
    func_in_9338(val);
    const int x = 9338;
    std::cout << 9338;
    for (int i = 0; i < 9338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9338(int& val) {
    const int x = 9338;
    std::cout << 9338;
    for (int i = 0; i < 9338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9339(int& val) {
    func_in_9339(val);
    const int x = 9339;
    std::cout << 9339;
    for (int i = 0; i < 9339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9339(int& val) {
    const int x = 9339;
    std::cout << 9339;
    for (int i = 0; i < 9339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9340(int& val) {
    func_in_9340(val);
    const int x = 9340;
    std::cout << 9340;
    for (int i = 0; i < 9340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9340(int& val) {
    const int x = 9340;
    std::cout << 9340;
    for (int i = 0; i < 9340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9341(int& val) {
    func_in_9341(val);
    const int x = 9341;
    std::cout << 9341;
    for (int i = 0; i < 9341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9341(int& val) {
    const int x = 9341;
    std::cout << 9341;
    for (int i = 0; i < 9341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9342(int& val) {
    func_in_9342(val);
    const int x = 9342;
    std::cout << 9342;
    for (int i = 0; i < 9342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9342(int& val) {
    const int x = 9342;
    std::cout << 9342;
    for (int i = 0; i < 9342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9343(int& val) {
    func_in_9343(val);
    const int x = 9343;
    std::cout << 9343;
    for (int i = 0; i < 9343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9343(int& val) {
    const int x = 9343;
    std::cout << 9343;
    for (int i = 0; i < 9343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9344(int& val) {
    func_in_9344(val);
    const int x = 9344;
    std::cout << 9344;
    for (int i = 0; i < 9344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9344(int& val) {
    const int x = 9344;
    std::cout << 9344;
    for (int i = 0; i < 9344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9345(int& val) {
    func_in_9345(val);
    const int x = 9345;
    std::cout << 9345;
    for (int i = 0; i < 9345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9345(int& val) {
    const int x = 9345;
    std::cout << 9345;
    for (int i = 0; i < 9345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9346(int& val) {
    func_in_9346(val);
    const int x = 9346;
    std::cout << 9346;
    for (int i = 0; i < 9346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9346(int& val) {
    const int x = 9346;
    std::cout << 9346;
    for (int i = 0; i < 9346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9347(int& val) {
    func_in_9347(val);
    const int x = 9347;
    std::cout << 9347;
    for (int i = 0; i < 9347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9347(int& val) {
    const int x = 9347;
    std::cout << 9347;
    for (int i = 0; i < 9347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9348(int& val) {
    func_in_9348(val);
    const int x = 9348;
    std::cout << 9348;
    for (int i = 0; i < 9348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9348(int& val) {
    const int x = 9348;
    std::cout << 9348;
    for (int i = 0; i < 9348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9349(int& val) {
    func_in_9349(val);
    const int x = 9349;
    std::cout << 9349;
    for (int i = 0; i < 9349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9349(int& val) {
    const int x = 9349;
    std::cout << 9349;
    for (int i = 0; i < 9349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9350(int& val) {
    func_in_9350(val);
    const int x = 9350;
    std::cout << 9350;
    for (int i = 0; i < 9350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9350(int& val) {
    const int x = 9350;
    std::cout << 9350;
    for (int i = 0; i < 9350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9351(int& val) {
    func_in_9351(val);
    const int x = 9351;
    std::cout << 9351;
    for (int i = 0; i < 9351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9351(int& val) {
    const int x = 9351;
    std::cout << 9351;
    for (int i = 0; i < 9351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9352(int& val) {
    func_in_9352(val);
    const int x = 9352;
    std::cout << 9352;
    for (int i = 0; i < 9352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9352(int& val) {
    const int x = 9352;
    std::cout << 9352;
    for (int i = 0; i < 9352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9353(int& val) {
    func_in_9353(val);
    const int x = 9353;
    std::cout << 9353;
    for (int i = 0; i < 9353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9353(int& val) {
    const int x = 9353;
    std::cout << 9353;
    for (int i = 0; i < 9353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9354(int& val) {
    func_in_9354(val);
    const int x = 9354;
    std::cout << 9354;
    for (int i = 0; i < 9354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9354(int& val) {
    const int x = 9354;
    std::cout << 9354;
    for (int i = 0; i < 9354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9355(int& val) {
    func_in_9355(val);
    const int x = 9355;
    std::cout << 9355;
    for (int i = 0; i < 9355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9355(int& val) {
    const int x = 9355;
    std::cout << 9355;
    for (int i = 0; i < 9355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9356(int& val) {
    func_in_9356(val);
    const int x = 9356;
    std::cout << 9356;
    for (int i = 0; i < 9356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9356(int& val) {
    const int x = 9356;
    std::cout << 9356;
    for (int i = 0; i < 9356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9357(int& val) {
    func_in_9357(val);
    const int x = 9357;
    std::cout << 9357;
    for (int i = 0; i < 9357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9357(int& val) {
    const int x = 9357;
    std::cout << 9357;
    for (int i = 0; i < 9357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9358(int& val) {
    func_in_9358(val);
    const int x = 9358;
    std::cout << 9358;
    for (int i = 0; i < 9358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9358(int& val) {
    const int x = 9358;
    std::cout << 9358;
    for (int i = 0; i < 9358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9359(int& val) {
    func_in_9359(val);
    const int x = 9359;
    std::cout << 9359;
    for (int i = 0; i < 9359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9359(int& val) {
    const int x = 9359;
    std::cout << 9359;
    for (int i = 0; i < 9359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9360(int& val) {
    func_in_9360(val);
    const int x = 9360;
    std::cout << 9360;
    for (int i = 0; i < 9360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9360(int& val) {
    const int x = 9360;
    std::cout << 9360;
    for (int i = 0; i < 9360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9361(int& val) {
    func_in_9361(val);
    const int x = 9361;
    std::cout << 9361;
    for (int i = 0; i < 9361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9361(int& val) {
    const int x = 9361;
    std::cout << 9361;
    for (int i = 0; i < 9361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9362(int& val) {
    func_in_9362(val);
    const int x = 9362;
    std::cout << 9362;
    for (int i = 0; i < 9362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9362(int& val) {
    const int x = 9362;
    std::cout << 9362;
    for (int i = 0; i < 9362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9363(int& val) {
    func_in_9363(val);
    const int x = 9363;
    std::cout << 9363;
    for (int i = 0; i < 9363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9363(int& val) {
    const int x = 9363;
    std::cout << 9363;
    for (int i = 0; i < 9363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9364(int& val) {
    func_in_9364(val);
    const int x = 9364;
    std::cout << 9364;
    for (int i = 0; i < 9364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9364(int& val) {
    const int x = 9364;
    std::cout << 9364;
    for (int i = 0; i < 9364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9365(int& val) {
    func_in_9365(val);
    const int x = 9365;
    std::cout << 9365;
    for (int i = 0; i < 9365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9365(int& val) {
    const int x = 9365;
    std::cout << 9365;
    for (int i = 0; i < 9365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9366(int& val) {
    func_in_9366(val);
    const int x = 9366;
    std::cout << 9366;
    for (int i = 0; i < 9366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9366(int& val) {
    const int x = 9366;
    std::cout << 9366;
    for (int i = 0; i < 9366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9367(int& val) {
    func_in_9367(val);
    const int x = 9367;
    std::cout << 9367;
    for (int i = 0; i < 9367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9367(int& val) {
    const int x = 9367;
    std::cout << 9367;
    for (int i = 0; i < 9367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9368(int& val) {
    func_in_9368(val);
    const int x = 9368;
    std::cout << 9368;
    for (int i = 0; i < 9368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9368(int& val) {
    const int x = 9368;
    std::cout << 9368;
    for (int i = 0; i < 9368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9369(int& val) {
    func_in_9369(val);
    const int x = 9369;
    std::cout << 9369;
    for (int i = 0; i < 9369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9369(int& val) {
    const int x = 9369;
    std::cout << 9369;
    for (int i = 0; i < 9369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9370(int& val) {
    func_in_9370(val);
    const int x = 9370;
    std::cout << 9370;
    for (int i = 0; i < 9370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9370(int& val) {
    const int x = 9370;
    std::cout << 9370;
    for (int i = 0; i < 9370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9371(int& val) {
    func_in_9371(val);
    const int x = 9371;
    std::cout << 9371;
    for (int i = 0; i < 9371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9371(int& val) {
    const int x = 9371;
    std::cout << 9371;
    for (int i = 0; i < 9371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9372(int& val) {
    func_in_9372(val);
    const int x = 9372;
    std::cout << 9372;
    for (int i = 0; i < 9372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9372(int& val) {
    const int x = 9372;
    std::cout << 9372;
    for (int i = 0; i < 9372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9373(int& val) {
    func_in_9373(val);
    const int x = 9373;
    std::cout << 9373;
    for (int i = 0; i < 9373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9373(int& val) {
    const int x = 9373;
    std::cout << 9373;
    for (int i = 0; i < 9373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9374(int& val) {
    func_in_9374(val);
    const int x = 9374;
    std::cout << 9374;
    for (int i = 0; i < 9374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9374(int& val) {
    const int x = 9374;
    std::cout << 9374;
    for (int i = 0; i < 9374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9375(int& val) {
    func_in_9375(val);
    const int x = 9375;
    std::cout << 9375;
    for (int i = 0; i < 9375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9375(int& val) {
    const int x = 9375;
    std::cout << 9375;
    for (int i = 0; i < 9375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9376(int& val) {
    func_in_9376(val);
    const int x = 9376;
    std::cout << 9376;
    for (int i = 0; i < 9376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9376(int& val) {
    const int x = 9376;
    std::cout << 9376;
    for (int i = 0; i < 9376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9377(int& val) {
    func_in_9377(val);
    const int x = 9377;
    std::cout << 9377;
    for (int i = 0; i < 9377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9377(int& val) {
    const int x = 9377;
    std::cout << 9377;
    for (int i = 0; i < 9377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9378(int& val) {
    func_in_9378(val);
    const int x = 9378;
    std::cout << 9378;
    for (int i = 0; i < 9378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9378(int& val) {
    const int x = 9378;
    std::cout << 9378;
    for (int i = 0; i < 9378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9379(int& val) {
    func_in_9379(val);
    const int x = 9379;
    std::cout << 9379;
    for (int i = 0; i < 9379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9379(int& val) {
    const int x = 9379;
    std::cout << 9379;
    for (int i = 0; i < 9379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9380(int& val) {
    func_in_9380(val);
    const int x = 9380;
    std::cout << 9380;
    for (int i = 0; i < 9380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9380(int& val) {
    const int x = 9380;
    std::cout << 9380;
    for (int i = 0; i < 9380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9381(int& val) {
    func_in_9381(val);
    const int x = 9381;
    std::cout << 9381;
    for (int i = 0; i < 9381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9381(int& val) {
    const int x = 9381;
    std::cout << 9381;
    for (int i = 0; i < 9381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9382(int& val) {
    func_in_9382(val);
    const int x = 9382;
    std::cout << 9382;
    for (int i = 0; i < 9382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9382(int& val) {
    const int x = 9382;
    std::cout << 9382;
    for (int i = 0; i < 9382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9383(int& val) {
    func_in_9383(val);
    const int x = 9383;
    std::cout << 9383;
    for (int i = 0; i < 9383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9383(int& val) {
    const int x = 9383;
    std::cout << 9383;
    for (int i = 0; i < 9383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9384(int& val) {
    func_in_9384(val);
    const int x = 9384;
    std::cout << 9384;
    for (int i = 0; i < 9384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9384(int& val) {
    const int x = 9384;
    std::cout << 9384;
    for (int i = 0; i < 9384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9385(int& val) {
    func_in_9385(val);
    const int x = 9385;
    std::cout << 9385;
    for (int i = 0; i < 9385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9385(int& val) {
    const int x = 9385;
    std::cout << 9385;
    for (int i = 0; i < 9385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9386(int& val) {
    func_in_9386(val);
    const int x = 9386;
    std::cout << 9386;
    for (int i = 0; i < 9386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9386(int& val) {
    const int x = 9386;
    std::cout << 9386;
    for (int i = 0; i < 9386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9387(int& val) {
    func_in_9387(val);
    const int x = 9387;
    std::cout << 9387;
    for (int i = 0; i < 9387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9387(int& val) {
    const int x = 9387;
    std::cout << 9387;
    for (int i = 0; i < 9387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9388(int& val) {
    func_in_9388(val);
    const int x = 9388;
    std::cout << 9388;
    for (int i = 0; i < 9388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9388(int& val) {
    const int x = 9388;
    std::cout << 9388;
    for (int i = 0; i < 9388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9389(int& val) {
    func_in_9389(val);
    const int x = 9389;
    std::cout << 9389;
    for (int i = 0; i < 9389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9389(int& val) {
    const int x = 9389;
    std::cout << 9389;
    for (int i = 0; i < 9389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9390(int& val) {
    func_in_9390(val);
    const int x = 9390;
    std::cout << 9390;
    for (int i = 0; i < 9390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9390(int& val) {
    const int x = 9390;
    std::cout << 9390;
    for (int i = 0; i < 9390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9391(int& val) {
    func_in_9391(val);
    const int x = 9391;
    std::cout << 9391;
    for (int i = 0; i < 9391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9391(int& val) {
    const int x = 9391;
    std::cout << 9391;
    for (int i = 0; i < 9391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9392(int& val) {
    func_in_9392(val);
    const int x = 9392;
    std::cout << 9392;
    for (int i = 0; i < 9392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9392(int& val) {
    const int x = 9392;
    std::cout << 9392;
    for (int i = 0; i < 9392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9393(int& val) {
    func_in_9393(val);
    const int x = 9393;
    std::cout << 9393;
    for (int i = 0; i < 9393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9393(int& val) {
    const int x = 9393;
    std::cout << 9393;
    for (int i = 0; i < 9393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9394(int& val) {
    func_in_9394(val);
    const int x = 9394;
    std::cout << 9394;
    for (int i = 0; i < 9394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9394(int& val) {
    const int x = 9394;
    std::cout << 9394;
    for (int i = 0; i < 9394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9395(int& val) {
    func_in_9395(val);
    const int x = 9395;
    std::cout << 9395;
    for (int i = 0; i < 9395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9395(int& val) {
    const int x = 9395;
    std::cout << 9395;
    for (int i = 0; i < 9395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9396(int& val) {
    func_in_9396(val);
    const int x = 9396;
    std::cout << 9396;
    for (int i = 0; i < 9396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9396(int& val) {
    const int x = 9396;
    std::cout << 9396;
    for (int i = 0; i < 9396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9397(int& val) {
    func_in_9397(val);
    const int x = 9397;
    std::cout << 9397;
    for (int i = 0; i < 9397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9397(int& val) {
    const int x = 9397;
    std::cout << 9397;
    for (int i = 0; i < 9397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9398(int& val) {
    func_in_9398(val);
    const int x = 9398;
    std::cout << 9398;
    for (int i = 0; i < 9398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9398(int& val) {
    const int x = 9398;
    std::cout << 9398;
    for (int i = 0; i < 9398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9399(int& val) {
    func_in_9399(val);
    const int x = 9399;
    std::cout << 9399;
    for (int i = 0; i < 9399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9399(int& val) {
    const int x = 9399;
    std::cout << 9399;
    for (int i = 0; i < 9399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9400(int& val) {
    func_in_9400(val);
    const int x = 9400;
    std::cout << 9400;
    for (int i = 0; i < 9400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9400(int& val) {
    const int x = 9400;
    std::cout << 9400;
    for (int i = 0; i < 9400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9401(int& val) {
    func_in_9401(val);
    const int x = 9401;
    std::cout << 9401;
    for (int i = 0; i < 9401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9401(int& val) {
    const int x = 9401;
    std::cout << 9401;
    for (int i = 0; i < 9401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9402(int& val) {
    func_in_9402(val);
    const int x = 9402;
    std::cout << 9402;
    for (int i = 0; i < 9402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9402(int& val) {
    const int x = 9402;
    std::cout << 9402;
    for (int i = 0; i < 9402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9403(int& val) {
    func_in_9403(val);
    const int x = 9403;
    std::cout << 9403;
    for (int i = 0; i < 9403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9403(int& val) {
    const int x = 9403;
    std::cout << 9403;
    for (int i = 0; i < 9403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9404(int& val) {
    func_in_9404(val);
    const int x = 9404;
    std::cout << 9404;
    for (int i = 0; i < 9404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9404(int& val) {
    const int x = 9404;
    std::cout << 9404;
    for (int i = 0; i < 9404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9405(int& val) {
    func_in_9405(val);
    const int x = 9405;
    std::cout << 9405;
    for (int i = 0; i < 9405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9405(int& val) {
    const int x = 9405;
    std::cout << 9405;
    for (int i = 0; i < 9405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9406(int& val) {
    func_in_9406(val);
    const int x = 9406;
    std::cout << 9406;
    for (int i = 0; i < 9406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9406(int& val) {
    const int x = 9406;
    std::cout << 9406;
    for (int i = 0; i < 9406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9407(int& val) {
    func_in_9407(val);
    const int x = 9407;
    std::cout << 9407;
    for (int i = 0; i < 9407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9407(int& val) {
    const int x = 9407;
    std::cout << 9407;
    for (int i = 0; i < 9407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9408(int& val) {
    func_in_9408(val);
    const int x = 9408;
    std::cout << 9408;
    for (int i = 0; i < 9408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9408(int& val) {
    const int x = 9408;
    std::cout << 9408;
    for (int i = 0; i < 9408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9409(int& val) {
    func_in_9409(val);
    const int x = 9409;
    std::cout << 9409;
    for (int i = 0; i < 9409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9409(int& val) {
    const int x = 9409;
    std::cout << 9409;
    for (int i = 0; i < 9409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9410(int& val) {
    func_in_9410(val);
    const int x = 9410;
    std::cout << 9410;
    for (int i = 0; i < 9410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9410(int& val) {
    const int x = 9410;
    std::cout << 9410;
    for (int i = 0; i < 9410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9411(int& val) {
    func_in_9411(val);
    const int x = 9411;
    std::cout << 9411;
    for (int i = 0; i < 9411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9411(int& val) {
    const int x = 9411;
    std::cout << 9411;
    for (int i = 0; i < 9411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9412(int& val) {
    func_in_9412(val);
    const int x = 9412;
    std::cout << 9412;
    for (int i = 0; i < 9412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9412(int& val) {
    const int x = 9412;
    std::cout << 9412;
    for (int i = 0; i < 9412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9413(int& val) {
    func_in_9413(val);
    const int x = 9413;
    std::cout << 9413;
    for (int i = 0; i < 9413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9413(int& val) {
    const int x = 9413;
    std::cout << 9413;
    for (int i = 0; i < 9413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9414(int& val) {
    func_in_9414(val);
    const int x = 9414;
    std::cout << 9414;
    for (int i = 0; i < 9414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9414(int& val) {
    const int x = 9414;
    std::cout << 9414;
    for (int i = 0; i < 9414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9415(int& val) {
    func_in_9415(val);
    const int x = 9415;
    std::cout << 9415;
    for (int i = 0; i < 9415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9415(int& val) {
    const int x = 9415;
    std::cout << 9415;
    for (int i = 0; i < 9415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9416(int& val) {
    func_in_9416(val);
    const int x = 9416;
    std::cout << 9416;
    for (int i = 0; i < 9416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9416(int& val) {
    const int x = 9416;
    std::cout << 9416;
    for (int i = 0; i < 9416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9417(int& val) {
    func_in_9417(val);
    const int x = 9417;
    std::cout << 9417;
    for (int i = 0; i < 9417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9417(int& val) {
    const int x = 9417;
    std::cout << 9417;
    for (int i = 0; i < 9417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9418(int& val) {
    func_in_9418(val);
    const int x = 9418;
    std::cout << 9418;
    for (int i = 0; i < 9418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9418(int& val) {
    const int x = 9418;
    std::cout << 9418;
    for (int i = 0; i < 9418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9419(int& val) {
    func_in_9419(val);
    const int x = 9419;
    std::cout << 9419;
    for (int i = 0; i < 9419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9419(int& val) {
    const int x = 9419;
    std::cout << 9419;
    for (int i = 0; i < 9419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9420(int& val) {
    func_in_9420(val);
    const int x = 9420;
    std::cout << 9420;
    for (int i = 0; i < 9420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9420(int& val) {
    const int x = 9420;
    std::cout << 9420;
    for (int i = 0; i < 9420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9421(int& val) {
    func_in_9421(val);
    const int x = 9421;
    std::cout << 9421;
    for (int i = 0; i < 9421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9421(int& val) {
    const int x = 9421;
    std::cout << 9421;
    for (int i = 0; i < 9421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9422(int& val) {
    func_in_9422(val);
    const int x = 9422;
    std::cout << 9422;
    for (int i = 0; i < 9422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9422(int& val) {
    const int x = 9422;
    std::cout << 9422;
    for (int i = 0; i < 9422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9423(int& val) {
    func_in_9423(val);
    const int x = 9423;
    std::cout << 9423;
    for (int i = 0; i < 9423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9423(int& val) {
    const int x = 9423;
    std::cout << 9423;
    for (int i = 0; i < 9423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9424(int& val) {
    func_in_9424(val);
    const int x = 9424;
    std::cout << 9424;
    for (int i = 0; i < 9424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9424(int& val) {
    const int x = 9424;
    std::cout << 9424;
    for (int i = 0; i < 9424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9425(int& val) {
    func_in_9425(val);
    const int x = 9425;
    std::cout << 9425;
    for (int i = 0; i < 9425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9425(int& val) {
    const int x = 9425;
    std::cout << 9425;
    for (int i = 0; i < 9425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9426(int& val) {
    func_in_9426(val);
    const int x = 9426;
    std::cout << 9426;
    for (int i = 0; i < 9426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9426(int& val) {
    const int x = 9426;
    std::cout << 9426;
    for (int i = 0; i < 9426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9427(int& val) {
    func_in_9427(val);
    const int x = 9427;
    std::cout << 9427;
    for (int i = 0; i < 9427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9427(int& val) {
    const int x = 9427;
    std::cout << 9427;
    for (int i = 0; i < 9427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9428(int& val) {
    func_in_9428(val);
    const int x = 9428;
    std::cout << 9428;
    for (int i = 0; i < 9428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9428(int& val) {
    const int x = 9428;
    std::cout << 9428;
    for (int i = 0; i < 9428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9429(int& val) {
    func_in_9429(val);
    const int x = 9429;
    std::cout << 9429;
    for (int i = 0; i < 9429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9429(int& val) {
    const int x = 9429;
    std::cout << 9429;
    for (int i = 0; i < 9429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9430(int& val) {
    func_in_9430(val);
    const int x = 9430;
    std::cout << 9430;
    for (int i = 0; i < 9430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9430(int& val) {
    const int x = 9430;
    std::cout << 9430;
    for (int i = 0; i < 9430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9431(int& val) {
    func_in_9431(val);
    const int x = 9431;
    std::cout << 9431;
    for (int i = 0; i < 9431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9431(int& val) {
    const int x = 9431;
    std::cout << 9431;
    for (int i = 0; i < 9431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9432(int& val) {
    func_in_9432(val);
    const int x = 9432;
    std::cout << 9432;
    for (int i = 0; i < 9432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9432(int& val) {
    const int x = 9432;
    std::cout << 9432;
    for (int i = 0; i < 9432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9433(int& val) {
    func_in_9433(val);
    const int x = 9433;
    std::cout << 9433;
    for (int i = 0; i < 9433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9433(int& val) {
    const int x = 9433;
    std::cout << 9433;
    for (int i = 0; i < 9433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9434(int& val) {
    func_in_9434(val);
    const int x = 9434;
    std::cout << 9434;
    for (int i = 0; i < 9434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9434(int& val) {
    const int x = 9434;
    std::cout << 9434;
    for (int i = 0; i < 9434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9435(int& val) {
    func_in_9435(val);
    const int x = 9435;
    std::cout << 9435;
    for (int i = 0; i < 9435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9435(int& val) {
    const int x = 9435;
    std::cout << 9435;
    for (int i = 0; i < 9435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9436(int& val) {
    func_in_9436(val);
    const int x = 9436;
    std::cout << 9436;
    for (int i = 0; i < 9436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9436(int& val) {
    const int x = 9436;
    std::cout << 9436;
    for (int i = 0; i < 9436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9437(int& val) {
    func_in_9437(val);
    const int x = 9437;
    std::cout << 9437;
    for (int i = 0; i < 9437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9437(int& val) {
    const int x = 9437;
    std::cout << 9437;
    for (int i = 0; i < 9437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9438(int& val) {
    func_in_9438(val);
    const int x = 9438;
    std::cout << 9438;
    for (int i = 0; i < 9438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9438(int& val) {
    const int x = 9438;
    std::cout << 9438;
    for (int i = 0; i < 9438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9439(int& val) {
    func_in_9439(val);
    const int x = 9439;
    std::cout << 9439;
    for (int i = 0; i < 9439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9439(int& val) {
    const int x = 9439;
    std::cout << 9439;
    for (int i = 0; i < 9439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9440(int& val) {
    func_in_9440(val);
    const int x = 9440;
    std::cout << 9440;
    for (int i = 0; i < 9440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9440(int& val) {
    const int x = 9440;
    std::cout << 9440;
    for (int i = 0; i < 9440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9441(int& val) {
    func_in_9441(val);
    const int x = 9441;
    std::cout << 9441;
    for (int i = 0; i < 9441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9441(int& val) {
    const int x = 9441;
    std::cout << 9441;
    for (int i = 0; i < 9441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9442(int& val) {
    func_in_9442(val);
    const int x = 9442;
    std::cout << 9442;
    for (int i = 0; i < 9442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9442(int& val) {
    const int x = 9442;
    std::cout << 9442;
    for (int i = 0; i < 9442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9443(int& val) {
    func_in_9443(val);
    const int x = 9443;
    std::cout << 9443;
    for (int i = 0; i < 9443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9443(int& val) {
    const int x = 9443;
    std::cout << 9443;
    for (int i = 0; i < 9443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9444(int& val) {
    func_in_9444(val);
    const int x = 9444;
    std::cout << 9444;
    for (int i = 0; i < 9444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9444(int& val) {
    const int x = 9444;
    std::cout << 9444;
    for (int i = 0; i < 9444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9445(int& val) {
    func_in_9445(val);
    const int x = 9445;
    std::cout << 9445;
    for (int i = 0; i < 9445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9445(int& val) {
    const int x = 9445;
    std::cout << 9445;
    for (int i = 0; i < 9445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9446(int& val) {
    func_in_9446(val);
    const int x = 9446;
    std::cout << 9446;
    for (int i = 0; i < 9446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9446(int& val) {
    const int x = 9446;
    std::cout << 9446;
    for (int i = 0; i < 9446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9447(int& val) {
    func_in_9447(val);
    const int x = 9447;
    std::cout << 9447;
    for (int i = 0; i < 9447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9447(int& val) {
    const int x = 9447;
    std::cout << 9447;
    for (int i = 0; i < 9447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9448(int& val) {
    func_in_9448(val);
    const int x = 9448;
    std::cout << 9448;
    for (int i = 0; i < 9448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9448(int& val) {
    const int x = 9448;
    std::cout << 9448;
    for (int i = 0; i < 9448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9449(int& val) {
    func_in_9449(val);
    const int x = 9449;
    std::cout << 9449;
    for (int i = 0; i < 9449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9449(int& val) {
    const int x = 9449;
    std::cout << 9449;
    for (int i = 0; i < 9449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9450(int& val) {
    func_in_9450(val);
    const int x = 9450;
    std::cout << 9450;
    for (int i = 0; i < 9450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9450(int& val) {
    const int x = 9450;
    std::cout << 9450;
    for (int i = 0; i < 9450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9451(int& val) {
    func_in_9451(val);
    const int x = 9451;
    std::cout << 9451;
    for (int i = 0; i < 9451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9451(int& val) {
    const int x = 9451;
    std::cout << 9451;
    for (int i = 0; i < 9451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9452(int& val) {
    func_in_9452(val);
    const int x = 9452;
    std::cout << 9452;
    for (int i = 0; i < 9452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9452(int& val) {
    const int x = 9452;
    std::cout << 9452;
    for (int i = 0; i < 9452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9453(int& val) {
    func_in_9453(val);
    const int x = 9453;
    std::cout << 9453;
    for (int i = 0; i < 9453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9453(int& val) {
    const int x = 9453;
    std::cout << 9453;
    for (int i = 0; i < 9453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9454(int& val) {
    func_in_9454(val);
    const int x = 9454;
    std::cout << 9454;
    for (int i = 0; i < 9454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9454(int& val) {
    const int x = 9454;
    std::cout << 9454;
    for (int i = 0; i < 9454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9455(int& val) {
    func_in_9455(val);
    const int x = 9455;
    std::cout << 9455;
    for (int i = 0; i < 9455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9455(int& val) {
    const int x = 9455;
    std::cout << 9455;
    for (int i = 0; i < 9455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9456(int& val) {
    func_in_9456(val);
    const int x = 9456;
    std::cout << 9456;
    for (int i = 0; i < 9456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9456(int& val) {
    const int x = 9456;
    std::cout << 9456;
    for (int i = 0; i < 9456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9457(int& val) {
    func_in_9457(val);
    const int x = 9457;
    std::cout << 9457;
    for (int i = 0; i < 9457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9457(int& val) {
    const int x = 9457;
    std::cout << 9457;
    for (int i = 0; i < 9457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9458(int& val) {
    func_in_9458(val);
    const int x = 9458;
    std::cout << 9458;
    for (int i = 0; i < 9458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9458(int& val) {
    const int x = 9458;
    std::cout << 9458;
    for (int i = 0; i < 9458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9459(int& val) {
    func_in_9459(val);
    const int x = 9459;
    std::cout << 9459;
    for (int i = 0; i < 9459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9459(int& val) {
    const int x = 9459;
    std::cout << 9459;
    for (int i = 0; i < 9459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9460(int& val) {
    func_in_9460(val);
    const int x = 9460;
    std::cout << 9460;
    for (int i = 0; i < 9460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9460(int& val) {
    const int x = 9460;
    std::cout << 9460;
    for (int i = 0; i < 9460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9461(int& val) {
    func_in_9461(val);
    const int x = 9461;
    std::cout << 9461;
    for (int i = 0; i < 9461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9461(int& val) {
    const int x = 9461;
    std::cout << 9461;
    for (int i = 0; i < 9461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9462(int& val) {
    func_in_9462(val);
    const int x = 9462;
    std::cout << 9462;
    for (int i = 0; i < 9462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9462(int& val) {
    const int x = 9462;
    std::cout << 9462;
    for (int i = 0; i < 9462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9463(int& val) {
    func_in_9463(val);
    const int x = 9463;
    std::cout << 9463;
    for (int i = 0; i < 9463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9463(int& val) {
    const int x = 9463;
    std::cout << 9463;
    for (int i = 0; i < 9463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9464(int& val) {
    func_in_9464(val);
    const int x = 9464;
    std::cout << 9464;
    for (int i = 0; i < 9464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9464(int& val) {
    const int x = 9464;
    std::cout << 9464;
    for (int i = 0; i < 9464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9465(int& val) {
    func_in_9465(val);
    const int x = 9465;
    std::cout << 9465;
    for (int i = 0; i < 9465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9465(int& val) {
    const int x = 9465;
    std::cout << 9465;
    for (int i = 0; i < 9465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9466(int& val) {
    func_in_9466(val);
    const int x = 9466;
    std::cout << 9466;
    for (int i = 0; i < 9466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9466(int& val) {
    const int x = 9466;
    std::cout << 9466;
    for (int i = 0; i < 9466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9467(int& val) {
    func_in_9467(val);
    const int x = 9467;
    std::cout << 9467;
    for (int i = 0; i < 9467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9467(int& val) {
    const int x = 9467;
    std::cout << 9467;
    for (int i = 0; i < 9467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9468(int& val) {
    func_in_9468(val);
    const int x = 9468;
    std::cout << 9468;
    for (int i = 0; i < 9468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9468(int& val) {
    const int x = 9468;
    std::cout << 9468;
    for (int i = 0; i < 9468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9469(int& val) {
    func_in_9469(val);
    const int x = 9469;
    std::cout << 9469;
    for (int i = 0; i < 9469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9469(int& val) {
    const int x = 9469;
    std::cout << 9469;
    for (int i = 0; i < 9469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9470(int& val) {
    func_in_9470(val);
    const int x = 9470;
    std::cout << 9470;
    for (int i = 0; i < 9470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9470(int& val) {
    const int x = 9470;
    std::cout << 9470;
    for (int i = 0; i < 9470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9471(int& val) {
    func_in_9471(val);
    const int x = 9471;
    std::cout << 9471;
    for (int i = 0; i < 9471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9471(int& val) {
    const int x = 9471;
    std::cout << 9471;
    for (int i = 0; i < 9471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9472(int& val) {
    func_in_9472(val);
    const int x = 9472;
    std::cout << 9472;
    for (int i = 0; i < 9472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9472(int& val) {
    const int x = 9472;
    std::cout << 9472;
    for (int i = 0; i < 9472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9473(int& val) {
    func_in_9473(val);
    const int x = 9473;
    std::cout << 9473;
    for (int i = 0; i < 9473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9473(int& val) {
    const int x = 9473;
    std::cout << 9473;
    for (int i = 0; i < 9473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9474(int& val) {
    func_in_9474(val);
    const int x = 9474;
    std::cout << 9474;
    for (int i = 0; i < 9474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9474(int& val) {
    const int x = 9474;
    std::cout << 9474;
    for (int i = 0; i < 9474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9475(int& val) {
    func_in_9475(val);
    const int x = 9475;
    std::cout << 9475;
    for (int i = 0; i < 9475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9475(int& val) {
    const int x = 9475;
    std::cout << 9475;
    for (int i = 0; i < 9475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9476(int& val) {
    func_in_9476(val);
    const int x = 9476;
    std::cout << 9476;
    for (int i = 0; i < 9476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9476(int& val) {
    const int x = 9476;
    std::cout << 9476;
    for (int i = 0; i < 9476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9477(int& val) {
    func_in_9477(val);
    const int x = 9477;
    std::cout << 9477;
    for (int i = 0; i < 9477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9477(int& val) {
    const int x = 9477;
    std::cout << 9477;
    for (int i = 0; i < 9477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9478(int& val) {
    func_in_9478(val);
    const int x = 9478;
    std::cout << 9478;
    for (int i = 0; i < 9478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9478(int& val) {
    const int x = 9478;
    std::cout << 9478;
    for (int i = 0; i < 9478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9479(int& val) {
    func_in_9479(val);
    const int x = 9479;
    std::cout << 9479;
    for (int i = 0; i < 9479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9479(int& val) {
    const int x = 9479;
    std::cout << 9479;
    for (int i = 0; i < 9479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9480(int& val) {
    func_in_9480(val);
    const int x = 9480;
    std::cout << 9480;
    for (int i = 0; i < 9480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9480(int& val) {
    const int x = 9480;
    std::cout << 9480;
    for (int i = 0; i < 9480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9481(int& val) {
    func_in_9481(val);
    const int x = 9481;
    std::cout << 9481;
    for (int i = 0; i < 9481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9481(int& val) {
    const int x = 9481;
    std::cout << 9481;
    for (int i = 0; i < 9481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9482(int& val) {
    func_in_9482(val);
    const int x = 9482;
    std::cout << 9482;
    for (int i = 0; i < 9482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9482(int& val) {
    const int x = 9482;
    std::cout << 9482;
    for (int i = 0; i < 9482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9483(int& val) {
    func_in_9483(val);
    const int x = 9483;
    std::cout << 9483;
    for (int i = 0; i < 9483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9483(int& val) {
    const int x = 9483;
    std::cout << 9483;
    for (int i = 0; i < 9483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9484(int& val) {
    func_in_9484(val);
    const int x = 9484;
    std::cout << 9484;
    for (int i = 0; i < 9484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9484(int& val) {
    const int x = 9484;
    std::cout << 9484;
    for (int i = 0; i < 9484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9485(int& val) {
    func_in_9485(val);
    const int x = 9485;
    std::cout << 9485;
    for (int i = 0; i < 9485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9485(int& val) {
    const int x = 9485;
    std::cout << 9485;
    for (int i = 0; i < 9485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9486(int& val) {
    func_in_9486(val);
    const int x = 9486;
    std::cout << 9486;
    for (int i = 0; i < 9486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9486(int& val) {
    const int x = 9486;
    std::cout << 9486;
    for (int i = 0; i < 9486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9487(int& val) {
    func_in_9487(val);
    const int x = 9487;
    std::cout << 9487;
    for (int i = 0; i < 9487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9487(int& val) {
    const int x = 9487;
    std::cout << 9487;
    for (int i = 0; i < 9487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9488(int& val) {
    func_in_9488(val);
    const int x = 9488;
    std::cout << 9488;
    for (int i = 0; i < 9488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9488(int& val) {
    const int x = 9488;
    std::cout << 9488;
    for (int i = 0; i < 9488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9489(int& val) {
    func_in_9489(val);
    const int x = 9489;
    std::cout << 9489;
    for (int i = 0; i < 9489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9489(int& val) {
    const int x = 9489;
    std::cout << 9489;
    for (int i = 0; i < 9489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9490(int& val) {
    func_in_9490(val);
    const int x = 9490;
    std::cout << 9490;
    for (int i = 0; i < 9490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9490(int& val) {
    const int x = 9490;
    std::cout << 9490;
    for (int i = 0; i < 9490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9491(int& val) {
    func_in_9491(val);
    const int x = 9491;
    std::cout << 9491;
    for (int i = 0; i < 9491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9491(int& val) {
    const int x = 9491;
    std::cout << 9491;
    for (int i = 0; i < 9491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9492(int& val) {
    func_in_9492(val);
    const int x = 9492;
    std::cout << 9492;
    for (int i = 0; i < 9492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9492(int& val) {
    const int x = 9492;
    std::cout << 9492;
    for (int i = 0; i < 9492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9493(int& val) {
    func_in_9493(val);
    const int x = 9493;
    std::cout << 9493;
    for (int i = 0; i < 9493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9493(int& val) {
    const int x = 9493;
    std::cout << 9493;
    for (int i = 0; i < 9493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9494(int& val) {
    func_in_9494(val);
    const int x = 9494;
    std::cout << 9494;
    for (int i = 0; i < 9494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9494(int& val) {
    const int x = 9494;
    std::cout << 9494;
    for (int i = 0; i < 9494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9495(int& val) {
    func_in_9495(val);
    const int x = 9495;
    std::cout << 9495;
    for (int i = 0; i < 9495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9495(int& val) {
    const int x = 9495;
    std::cout << 9495;
    for (int i = 0; i < 9495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9496(int& val) {
    func_in_9496(val);
    const int x = 9496;
    std::cout << 9496;
    for (int i = 0; i < 9496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9496(int& val) {
    const int x = 9496;
    std::cout << 9496;
    for (int i = 0; i < 9496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9497(int& val) {
    func_in_9497(val);
    const int x = 9497;
    std::cout << 9497;
    for (int i = 0; i < 9497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9497(int& val) {
    const int x = 9497;
    std::cout << 9497;
    for (int i = 0; i < 9497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9498(int& val) {
    func_in_9498(val);
    const int x = 9498;
    std::cout << 9498;
    for (int i = 0; i < 9498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9498(int& val) {
    const int x = 9498;
    std::cout << 9498;
    for (int i = 0; i < 9498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9499(int& val) {
    func_in_9499(val);
    const int x = 9499;
    std::cout << 9499;
    for (int i = 0; i < 9499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9499(int& val) {
    const int x = 9499;
    std::cout << 9499;
    for (int i = 0; i < 9499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9500(int& val) {
    func_in_9500(val);
    const int x = 9500;
    std::cout << 9500;
    for (int i = 0; i < 9500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9500(int& val) {
    const int x = 9500;
    std::cout << 9500;
    for (int i = 0; i < 9500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9501(int& val) {
    func_in_9501(val);
    const int x = 9501;
    std::cout << 9501;
    for (int i = 0; i < 9501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9501(int& val) {
    const int x = 9501;
    std::cout << 9501;
    for (int i = 0; i < 9501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9502(int& val) {
    func_in_9502(val);
    const int x = 9502;
    std::cout << 9502;
    for (int i = 0; i < 9502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9502(int& val) {
    const int x = 9502;
    std::cout << 9502;
    for (int i = 0; i < 9502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9503(int& val) {
    func_in_9503(val);
    const int x = 9503;
    std::cout << 9503;
    for (int i = 0; i < 9503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9503(int& val) {
    const int x = 9503;
    std::cout << 9503;
    for (int i = 0; i < 9503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9504(int& val) {
    func_in_9504(val);
    const int x = 9504;
    std::cout << 9504;
    for (int i = 0; i < 9504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9504(int& val) {
    const int x = 9504;
    std::cout << 9504;
    for (int i = 0; i < 9504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9505(int& val) {
    func_in_9505(val);
    const int x = 9505;
    std::cout << 9505;
    for (int i = 0; i < 9505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9505(int& val) {
    const int x = 9505;
    std::cout << 9505;
    for (int i = 0; i < 9505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9506(int& val) {
    func_in_9506(val);
    const int x = 9506;
    std::cout << 9506;
    for (int i = 0; i < 9506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9506(int& val) {
    const int x = 9506;
    std::cout << 9506;
    for (int i = 0; i < 9506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9507(int& val) {
    func_in_9507(val);
    const int x = 9507;
    std::cout << 9507;
    for (int i = 0; i < 9507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9507(int& val) {
    const int x = 9507;
    std::cout << 9507;
    for (int i = 0; i < 9507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9508(int& val) {
    func_in_9508(val);
    const int x = 9508;
    std::cout << 9508;
    for (int i = 0; i < 9508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9508(int& val) {
    const int x = 9508;
    std::cout << 9508;
    for (int i = 0; i < 9508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9509(int& val) {
    func_in_9509(val);
    const int x = 9509;
    std::cout << 9509;
    for (int i = 0; i < 9509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9509(int& val) {
    const int x = 9509;
    std::cout << 9509;
    for (int i = 0; i < 9509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9510(int& val) {
    func_in_9510(val);
    const int x = 9510;
    std::cout << 9510;
    for (int i = 0; i < 9510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9510(int& val) {
    const int x = 9510;
    std::cout << 9510;
    for (int i = 0; i < 9510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9511(int& val) {
    func_in_9511(val);
    const int x = 9511;
    std::cout << 9511;
    for (int i = 0; i < 9511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9511(int& val) {
    const int x = 9511;
    std::cout << 9511;
    for (int i = 0; i < 9511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9512(int& val) {
    func_in_9512(val);
    const int x = 9512;
    std::cout << 9512;
    for (int i = 0; i < 9512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9512(int& val) {
    const int x = 9512;
    std::cout << 9512;
    for (int i = 0; i < 9512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9513(int& val) {
    func_in_9513(val);
    const int x = 9513;
    std::cout << 9513;
    for (int i = 0; i < 9513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9513(int& val) {
    const int x = 9513;
    std::cout << 9513;
    for (int i = 0; i < 9513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9514(int& val) {
    func_in_9514(val);
    const int x = 9514;
    std::cout << 9514;
    for (int i = 0; i < 9514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9514(int& val) {
    const int x = 9514;
    std::cout << 9514;
    for (int i = 0; i < 9514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9515(int& val) {
    func_in_9515(val);
    const int x = 9515;
    std::cout << 9515;
    for (int i = 0; i < 9515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9515(int& val) {
    const int x = 9515;
    std::cout << 9515;
    for (int i = 0; i < 9515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9516(int& val) {
    func_in_9516(val);
    const int x = 9516;
    std::cout << 9516;
    for (int i = 0; i < 9516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9516(int& val) {
    const int x = 9516;
    std::cout << 9516;
    for (int i = 0; i < 9516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9517(int& val) {
    func_in_9517(val);
    const int x = 9517;
    std::cout << 9517;
    for (int i = 0; i < 9517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9517(int& val) {
    const int x = 9517;
    std::cout << 9517;
    for (int i = 0; i < 9517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9518(int& val) {
    func_in_9518(val);
    const int x = 9518;
    std::cout << 9518;
    for (int i = 0; i < 9518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9518(int& val) {
    const int x = 9518;
    std::cout << 9518;
    for (int i = 0; i < 9518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9519(int& val) {
    func_in_9519(val);
    const int x = 9519;
    std::cout << 9519;
    for (int i = 0; i < 9519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9519(int& val) {
    const int x = 9519;
    std::cout << 9519;
    for (int i = 0; i < 9519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9520(int& val) {
    func_in_9520(val);
    const int x = 9520;
    std::cout << 9520;
    for (int i = 0; i < 9520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9520(int& val) {
    const int x = 9520;
    std::cout << 9520;
    for (int i = 0; i < 9520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9521(int& val) {
    func_in_9521(val);
    const int x = 9521;
    std::cout << 9521;
    for (int i = 0; i < 9521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9521(int& val) {
    const int x = 9521;
    std::cout << 9521;
    for (int i = 0; i < 9521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9522(int& val) {
    func_in_9522(val);
    const int x = 9522;
    std::cout << 9522;
    for (int i = 0; i < 9522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9522(int& val) {
    const int x = 9522;
    std::cout << 9522;
    for (int i = 0; i < 9522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9523(int& val) {
    func_in_9523(val);
    const int x = 9523;
    std::cout << 9523;
    for (int i = 0; i < 9523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9523(int& val) {
    const int x = 9523;
    std::cout << 9523;
    for (int i = 0; i < 9523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9524(int& val) {
    func_in_9524(val);
    const int x = 9524;
    std::cout << 9524;
    for (int i = 0; i < 9524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9524(int& val) {
    const int x = 9524;
    std::cout << 9524;
    for (int i = 0; i < 9524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9525(int& val) {
    func_in_9525(val);
    const int x = 9525;
    std::cout << 9525;
    for (int i = 0; i < 9525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9525(int& val) {
    const int x = 9525;
    std::cout << 9525;
    for (int i = 0; i < 9525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9526(int& val) {
    func_in_9526(val);
    const int x = 9526;
    std::cout << 9526;
    for (int i = 0; i < 9526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9526(int& val) {
    const int x = 9526;
    std::cout << 9526;
    for (int i = 0; i < 9526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9527(int& val) {
    func_in_9527(val);
    const int x = 9527;
    std::cout << 9527;
    for (int i = 0; i < 9527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9527(int& val) {
    const int x = 9527;
    std::cout << 9527;
    for (int i = 0; i < 9527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9528(int& val) {
    func_in_9528(val);
    const int x = 9528;
    std::cout << 9528;
    for (int i = 0; i < 9528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9528(int& val) {
    const int x = 9528;
    std::cout << 9528;
    for (int i = 0; i < 9528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9529(int& val) {
    func_in_9529(val);
    const int x = 9529;
    std::cout << 9529;
    for (int i = 0; i < 9529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9529(int& val) {
    const int x = 9529;
    std::cout << 9529;
    for (int i = 0; i < 9529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9530(int& val) {
    func_in_9530(val);
    const int x = 9530;
    std::cout << 9530;
    for (int i = 0; i < 9530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9530(int& val) {
    const int x = 9530;
    std::cout << 9530;
    for (int i = 0; i < 9530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9531(int& val) {
    func_in_9531(val);
    const int x = 9531;
    std::cout << 9531;
    for (int i = 0; i < 9531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9531(int& val) {
    const int x = 9531;
    std::cout << 9531;
    for (int i = 0; i < 9531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9532(int& val) {
    func_in_9532(val);
    const int x = 9532;
    std::cout << 9532;
    for (int i = 0; i < 9532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9532(int& val) {
    const int x = 9532;
    std::cout << 9532;
    for (int i = 0; i < 9532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9533(int& val) {
    func_in_9533(val);
    const int x = 9533;
    std::cout << 9533;
    for (int i = 0; i < 9533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9533(int& val) {
    const int x = 9533;
    std::cout << 9533;
    for (int i = 0; i < 9533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9534(int& val) {
    func_in_9534(val);
    const int x = 9534;
    std::cout << 9534;
    for (int i = 0; i < 9534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9534(int& val) {
    const int x = 9534;
    std::cout << 9534;
    for (int i = 0; i < 9534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9535(int& val) {
    func_in_9535(val);
    const int x = 9535;
    std::cout << 9535;
    for (int i = 0; i < 9535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9535(int& val) {
    const int x = 9535;
    std::cout << 9535;
    for (int i = 0; i < 9535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9536(int& val) {
    func_in_9536(val);
    const int x = 9536;
    std::cout << 9536;
    for (int i = 0; i < 9536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9536(int& val) {
    const int x = 9536;
    std::cout << 9536;
    for (int i = 0; i < 9536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9537(int& val) {
    func_in_9537(val);
    const int x = 9537;
    std::cout << 9537;
    for (int i = 0; i < 9537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9537(int& val) {
    const int x = 9537;
    std::cout << 9537;
    for (int i = 0; i < 9537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9538(int& val) {
    func_in_9538(val);
    const int x = 9538;
    std::cout << 9538;
    for (int i = 0; i < 9538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9538(int& val) {
    const int x = 9538;
    std::cout << 9538;
    for (int i = 0; i < 9538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9539(int& val) {
    func_in_9539(val);
    const int x = 9539;
    std::cout << 9539;
    for (int i = 0; i < 9539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9539(int& val) {
    const int x = 9539;
    std::cout << 9539;
    for (int i = 0; i < 9539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9540(int& val) {
    func_in_9540(val);
    const int x = 9540;
    std::cout << 9540;
    for (int i = 0; i < 9540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9540(int& val) {
    const int x = 9540;
    std::cout << 9540;
    for (int i = 0; i < 9540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9541(int& val) {
    func_in_9541(val);
    const int x = 9541;
    std::cout << 9541;
    for (int i = 0; i < 9541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9541(int& val) {
    const int x = 9541;
    std::cout << 9541;
    for (int i = 0; i < 9541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9542(int& val) {
    func_in_9542(val);
    const int x = 9542;
    std::cout << 9542;
    for (int i = 0; i < 9542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9542(int& val) {
    const int x = 9542;
    std::cout << 9542;
    for (int i = 0; i < 9542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9543(int& val) {
    func_in_9543(val);
    const int x = 9543;
    std::cout << 9543;
    for (int i = 0; i < 9543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9543(int& val) {
    const int x = 9543;
    std::cout << 9543;
    for (int i = 0; i < 9543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9544(int& val) {
    func_in_9544(val);
    const int x = 9544;
    std::cout << 9544;
    for (int i = 0; i < 9544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9544(int& val) {
    const int x = 9544;
    std::cout << 9544;
    for (int i = 0; i < 9544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9545(int& val) {
    func_in_9545(val);
    const int x = 9545;
    std::cout << 9545;
    for (int i = 0; i < 9545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9545(int& val) {
    const int x = 9545;
    std::cout << 9545;
    for (int i = 0; i < 9545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9546(int& val) {
    func_in_9546(val);
    const int x = 9546;
    std::cout << 9546;
    for (int i = 0; i < 9546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9546(int& val) {
    const int x = 9546;
    std::cout << 9546;
    for (int i = 0; i < 9546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9547(int& val) {
    func_in_9547(val);
    const int x = 9547;
    std::cout << 9547;
    for (int i = 0; i < 9547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9547(int& val) {
    const int x = 9547;
    std::cout << 9547;
    for (int i = 0; i < 9547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9548(int& val) {
    func_in_9548(val);
    const int x = 9548;
    std::cout << 9548;
    for (int i = 0; i < 9548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9548(int& val) {
    const int x = 9548;
    std::cout << 9548;
    for (int i = 0; i < 9548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9549(int& val) {
    func_in_9549(val);
    const int x = 9549;
    std::cout << 9549;
    for (int i = 0; i < 9549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9549(int& val) {
    const int x = 9549;
    std::cout << 9549;
    for (int i = 0; i < 9549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9550(int& val) {
    func_in_9550(val);
    const int x = 9550;
    std::cout << 9550;
    for (int i = 0; i < 9550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9550(int& val) {
    const int x = 9550;
    std::cout << 9550;
    for (int i = 0; i < 9550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9551(int& val) {
    func_in_9551(val);
    const int x = 9551;
    std::cout << 9551;
    for (int i = 0; i < 9551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9551(int& val) {
    const int x = 9551;
    std::cout << 9551;
    for (int i = 0; i < 9551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9552(int& val) {
    func_in_9552(val);
    const int x = 9552;
    std::cout << 9552;
    for (int i = 0; i < 9552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9552(int& val) {
    const int x = 9552;
    std::cout << 9552;
    for (int i = 0; i < 9552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9553(int& val) {
    func_in_9553(val);
    const int x = 9553;
    std::cout << 9553;
    for (int i = 0; i < 9553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9553(int& val) {
    const int x = 9553;
    std::cout << 9553;
    for (int i = 0; i < 9553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9554(int& val) {
    func_in_9554(val);
    const int x = 9554;
    std::cout << 9554;
    for (int i = 0; i < 9554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9554(int& val) {
    const int x = 9554;
    std::cout << 9554;
    for (int i = 0; i < 9554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9555(int& val) {
    func_in_9555(val);
    const int x = 9555;
    std::cout << 9555;
    for (int i = 0; i < 9555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9555(int& val) {
    const int x = 9555;
    std::cout << 9555;
    for (int i = 0; i < 9555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9556(int& val) {
    func_in_9556(val);
    const int x = 9556;
    std::cout << 9556;
    for (int i = 0; i < 9556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9556(int& val) {
    const int x = 9556;
    std::cout << 9556;
    for (int i = 0; i < 9556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9557(int& val) {
    func_in_9557(val);
    const int x = 9557;
    std::cout << 9557;
    for (int i = 0; i < 9557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9557(int& val) {
    const int x = 9557;
    std::cout << 9557;
    for (int i = 0; i < 9557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9558(int& val) {
    func_in_9558(val);
    const int x = 9558;
    std::cout << 9558;
    for (int i = 0; i < 9558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9558(int& val) {
    const int x = 9558;
    std::cout << 9558;
    for (int i = 0; i < 9558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9559(int& val) {
    func_in_9559(val);
    const int x = 9559;
    std::cout << 9559;
    for (int i = 0; i < 9559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9559(int& val) {
    const int x = 9559;
    std::cout << 9559;
    for (int i = 0; i < 9559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9560(int& val) {
    func_in_9560(val);
    const int x = 9560;
    std::cout << 9560;
    for (int i = 0; i < 9560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9560(int& val) {
    const int x = 9560;
    std::cout << 9560;
    for (int i = 0; i < 9560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9561(int& val) {
    func_in_9561(val);
    const int x = 9561;
    std::cout << 9561;
    for (int i = 0; i < 9561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9561(int& val) {
    const int x = 9561;
    std::cout << 9561;
    for (int i = 0; i < 9561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9562(int& val) {
    func_in_9562(val);
    const int x = 9562;
    std::cout << 9562;
    for (int i = 0; i < 9562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9562(int& val) {
    const int x = 9562;
    std::cout << 9562;
    for (int i = 0; i < 9562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9563(int& val) {
    func_in_9563(val);
    const int x = 9563;
    std::cout << 9563;
    for (int i = 0; i < 9563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9563(int& val) {
    const int x = 9563;
    std::cout << 9563;
    for (int i = 0; i < 9563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9564(int& val) {
    func_in_9564(val);
    const int x = 9564;
    std::cout << 9564;
    for (int i = 0; i < 9564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9564(int& val) {
    const int x = 9564;
    std::cout << 9564;
    for (int i = 0; i < 9564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9565(int& val) {
    func_in_9565(val);
    const int x = 9565;
    std::cout << 9565;
    for (int i = 0; i < 9565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9565(int& val) {
    const int x = 9565;
    std::cout << 9565;
    for (int i = 0; i < 9565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9566(int& val) {
    func_in_9566(val);
    const int x = 9566;
    std::cout << 9566;
    for (int i = 0; i < 9566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9566(int& val) {
    const int x = 9566;
    std::cout << 9566;
    for (int i = 0; i < 9566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9567(int& val) {
    func_in_9567(val);
    const int x = 9567;
    std::cout << 9567;
    for (int i = 0; i < 9567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9567(int& val) {
    const int x = 9567;
    std::cout << 9567;
    for (int i = 0; i < 9567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9568(int& val) {
    func_in_9568(val);
    const int x = 9568;
    std::cout << 9568;
    for (int i = 0; i < 9568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9568(int& val) {
    const int x = 9568;
    std::cout << 9568;
    for (int i = 0; i < 9568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9569(int& val) {
    func_in_9569(val);
    const int x = 9569;
    std::cout << 9569;
    for (int i = 0; i < 9569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9569(int& val) {
    const int x = 9569;
    std::cout << 9569;
    for (int i = 0; i < 9569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9570(int& val) {
    func_in_9570(val);
    const int x = 9570;
    std::cout << 9570;
    for (int i = 0; i < 9570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9570(int& val) {
    const int x = 9570;
    std::cout << 9570;
    for (int i = 0; i < 9570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9571(int& val) {
    func_in_9571(val);
    const int x = 9571;
    std::cout << 9571;
    for (int i = 0; i < 9571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9571(int& val) {
    const int x = 9571;
    std::cout << 9571;
    for (int i = 0; i < 9571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9572(int& val) {
    func_in_9572(val);
    const int x = 9572;
    std::cout << 9572;
    for (int i = 0; i < 9572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9572(int& val) {
    const int x = 9572;
    std::cout << 9572;
    for (int i = 0; i < 9572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9573(int& val) {
    func_in_9573(val);
    const int x = 9573;
    std::cout << 9573;
    for (int i = 0; i < 9573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9573(int& val) {
    const int x = 9573;
    std::cout << 9573;
    for (int i = 0; i < 9573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9574(int& val) {
    func_in_9574(val);
    const int x = 9574;
    std::cout << 9574;
    for (int i = 0; i < 9574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9574(int& val) {
    const int x = 9574;
    std::cout << 9574;
    for (int i = 0; i < 9574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9575(int& val) {
    func_in_9575(val);
    const int x = 9575;
    std::cout << 9575;
    for (int i = 0; i < 9575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9575(int& val) {
    const int x = 9575;
    std::cout << 9575;
    for (int i = 0; i < 9575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9576(int& val) {
    func_in_9576(val);
    const int x = 9576;
    std::cout << 9576;
    for (int i = 0; i < 9576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9576(int& val) {
    const int x = 9576;
    std::cout << 9576;
    for (int i = 0; i < 9576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9577(int& val) {
    func_in_9577(val);
    const int x = 9577;
    std::cout << 9577;
    for (int i = 0; i < 9577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9577(int& val) {
    const int x = 9577;
    std::cout << 9577;
    for (int i = 0; i < 9577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9578(int& val) {
    func_in_9578(val);
    const int x = 9578;
    std::cout << 9578;
    for (int i = 0; i < 9578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9578(int& val) {
    const int x = 9578;
    std::cout << 9578;
    for (int i = 0; i < 9578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9579(int& val) {
    func_in_9579(val);
    const int x = 9579;
    std::cout << 9579;
    for (int i = 0; i < 9579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9579(int& val) {
    const int x = 9579;
    std::cout << 9579;
    for (int i = 0; i < 9579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9580(int& val) {
    func_in_9580(val);
    const int x = 9580;
    std::cout << 9580;
    for (int i = 0; i < 9580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9580(int& val) {
    const int x = 9580;
    std::cout << 9580;
    for (int i = 0; i < 9580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9581(int& val) {
    func_in_9581(val);
    const int x = 9581;
    std::cout << 9581;
    for (int i = 0; i < 9581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9581(int& val) {
    const int x = 9581;
    std::cout << 9581;
    for (int i = 0; i < 9581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9582(int& val) {
    func_in_9582(val);
    const int x = 9582;
    std::cout << 9582;
    for (int i = 0; i < 9582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9582(int& val) {
    const int x = 9582;
    std::cout << 9582;
    for (int i = 0; i < 9582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9583(int& val) {
    func_in_9583(val);
    const int x = 9583;
    std::cout << 9583;
    for (int i = 0; i < 9583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9583(int& val) {
    const int x = 9583;
    std::cout << 9583;
    for (int i = 0; i < 9583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9584(int& val) {
    func_in_9584(val);
    const int x = 9584;
    std::cout << 9584;
    for (int i = 0; i < 9584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9584(int& val) {
    const int x = 9584;
    std::cout << 9584;
    for (int i = 0; i < 9584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9585(int& val) {
    func_in_9585(val);
    const int x = 9585;
    std::cout << 9585;
    for (int i = 0; i < 9585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9585(int& val) {
    const int x = 9585;
    std::cout << 9585;
    for (int i = 0; i < 9585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9586(int& val) {
    func_in_9586(val);
    const int x = 9586;
    std::cout << 9586;
    for (int i = 0; i < 9586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9586(int& val) {
    const int x = 9586;
    std::cout << 9586;
    for (int i = 0; i < 9586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9587(int& val) {
    func_in_9587(val);
    const int x = 9587;
    std::cout << 9587;
    for (int i = 0; i < 9587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9587(int& val) {
    const int x = 9587;
    std::cout << 9587;
    for (int i = 0; i < 9587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9588(int& val) {
    func_in_9588(val);
    const int x = 9588;
    std::cout << 9588;
    for (int i = 0; i < 9588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9588(int& val) {
    const int x = 9588;
    std::cout << 9588;
    for (int i = 0; i < 9588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9589(int& val) {
    func_in_9589(val);
    const int x = 9589;
    std::cout << 9589;
    for (int i = 0; i < 9589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9589(int& val) {
    const int x = 9589;
    std::cout << 9589;
    for (int i = 0; i < 9589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9590(int& val) {
    func_in_9590(val);
    const int x = 9590;
    std::cout << 9590;
    for (int i = 0; i < 9590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9590(int& val) {
    const int x = 9590;
    std::cout << 9590;
    for (int i = 0; i < 9590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9591(int& val) {
    func_in_9591(val);
    const int x = 9591;
    std::cout << 9591;
    for (int i = 0; i < 9591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9591(int& val) {
    const int x = 9591;
    std::cout << 9591;
    for (int i = 0; i < 9591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9592(int& val) {
    func_in_9592(val);
    const int x = 9592;
    std::cout << 9592;
    for (int i = 0; i < 9592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9592(int& val) {
    const int x = 9592;
    std::cout << 9592;
    for (int i = 0; i < 9592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9593(int& val) {
    func_in_9593(val);
    const int x = 9593;
    std::cout << 9593;
    for (int i = 0; i < 9593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9593(int& val) {
    const int x = 9593;
    std::cout << 9593;
    for (int i = 0; i < 9593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9594(int& val) {
    func_in_9594(val);
    const int x = 9594;
    std::cout << 9594;
    for (int i = 0; i < 9594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9594(int& val) {
    const int x = 9594;
    std::cout << 9594;
    for (int i = 0; i < 9594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9595(int& val) {
    func_in_9595(val);
    const int x = 9595;
    std::cout << 9595;
    for (int i = 0; i < 9595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9595(int& val) {
    const int x = 9595;
    std::cout << 9595;
    for (int i = 0; i < 9595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9596(int& val) {
    func_in_9596(val);
    const int x = 9596;
    std::cout << 9596;
    for (int i = 0; i < 9596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9596(int& val) {
    const int x = 9596;
    std::cout << 9596;
    for (int i = 0; i < 9596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9597(int& val) {
    func_in_9597(val);
    const int x = 9597;
    std::cout << 9597;
    for (int i = 0; i < 9597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9597(int& val) {
    const int x = 9597;
    std::cout << 9597;
    for (int i = 0; i < 9597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9598(int& val) {
    func_in_9598(val);
    const int x = 9598;
    std::cout << 9598;
    for (int i = 0; i < 9598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9598(int& val) {
    const int x = 9598;
    std::cout << 9598;
    for (int i = 0; i < 9598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9599(int& val) {
    func_in_9599(val);
    const int x = 9599;
    std::cout << 9599;
    for (int i = 0; i < 9599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9599(int& val) {
    const int x = 9599;
    std::cout << 9599;
    for (int i = 0; i < 9599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9600(int& val) {
    func_in_9600(val);
    const int x = 9600;
    std::cout << 9600;
    for (int i = 0; i < 9600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9600(int& val) {
    const int x = 9600;
    std::cout << 9600;
    for (int i = 0; i < 9600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9601(int& val) {
    func_in_9601(val);
    const int x = 9601;
    std::cout << 9601;
    for (int i = 0; i < 9601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9601(int& val) {
    const int x = 9601;
    std::cout << 9601;
    for (int i = 0; i < 9601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9602(int& val) {
    func_in_9602(val);
    const int x = 9602;
    std::cout << 9602;
    for (int i = 0; i < 9602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9602(int& val) {
    const int x = 9602;
    std::cout << 9602;
    for (int i = 0; i < 9602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9603(int& val) {
    func_in_9603(val);
    const int x = 9603;
    std::cout << 9603;
    for (int i = 0; i < 9603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9603(int& val) {
    const int x = 9603;
    std::cout << 9603;
    for (int i = 0; i < 9603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9604(int& val) {
    func_in_9604(val);
    const int x = 9604;
    std::cout << 9604;
    for (int i = 0; i < 9604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9604(int& val) {
    const int x = 9604;
    std::cout << 9604;
    for (int i = 0; i < 9604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9605(int& val) {
    func_in_9605(val);
    const int x = 9605;
    std::cout << 9605;
    for (int i = 0; i < 9605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9605(int& val) {
    const int x = 9605;
    std::cout << 9605;
    for (int i = 0; i < 9605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9606(int& val) {
    func_in_9606(val);
    const int x = 9606;
    std::cout << 9606;
    for (int i = 0; i < 9606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9606(int& val) {
    const int x = 9606;
    std::cout << 9606;
    for (int i = 0; i < 9606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9607(int& val) {
    func_in_9607(val);
    const int x = 9607;
    std::cout << 9607;
    for (int i = 0; i < 9607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9607(int& val) {
    const int x = 9607;
    std::cout << 9607;
    for (int i = 0; i < 9607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9608(int& val) {
    func_in_9608(val);
    const int x = 9608;
    std::cout << 9608;
    for (int i = 0; i < 9608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9608(int& val) {
    const int x = 9608;
    std::cout << 9608;
    for (int i = 0; i < 9608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9609(int& val) {
    func_in_9609(val);
    const int x = 9609;
    std::cout << 9609;
    for (int i = 0; i < 9609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9609(int& val) {
    const int x = 9609;
    std::cout << 9609;
    for (int i = 0; i < 9609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9610(int& val) {
    func_in_9610(val);
    const int x = 9610;
    std::cout << 9610;
    for (int i = 0; i < 9610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9610(int& val) {
    const int x = 9610;
    std::cout << 9610;
    for (int i = 0; i < 9610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9611(int& val) {
    func_in_9611(val);
    const int x = 9611;
    std::cout << 9611;
    for (int i = 0; i < 9611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9611(int& val) {
    const int x = 9611;
    std::cout << 9611;
    for (int i = 0; i < 9611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9612(int& val) {
    func_in_9612(val);
    const int x = 9612;
    std::cout << 9612;
    for (int i = 0; i < 9612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9612(int& val) {
    const int x = 9612;
    std::cout << 9612;
    for (int i = 0; i < 9612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9613(int& val) {
    func_in_9613(val);
    const int x = 9613;
    std::cout << 9613;
    for (int i = 0; i < 9613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9613(int& val) {
    const int x = 9613;
    std::cout << 9613;
    for (int i = 0; i < 9613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9614(int& val) {
    func_in_9614(val);
    const int x = 9614;
    std::cout << 9614;
    for (int i = 0; i < 9614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9614(int& val) {
    const int x = 9614;
    std::cout << 9614;
    for (int i = 0; i < 9614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9615(int& val) {
    func_in_9615(val);
    const int x = 9615;
    std::cout << 9615;
    for (int i = 0; i < 9615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9615(int& val) {
    const int x = 9615;
    std::cout << 9615;
    for (int i = 0; i < 9615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9616(int& val) {
    func_in_9616(val);
    const int x = 9616;
    std::cout << 9616;
    for (int i = 0; i < 9616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9616(int& val) {
    const int x = 9616;
    std::cout << 9616;
    for (int i = 0; i < 9616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9617(int& val) {
    func_in_9617(val);
    const int x = 9617;
    std::cout << 9617;
    for (int i = 0; i < 9617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9617(int& val) {
    const int x = 9617;
    std::cout << 9617;
    for (int i = 0; i < 9617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9618(int& val) {
    func_in_9618(val);
    const int x = 9618;
    std::cout << 9618;
    for (int i = 0; i < 9618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9618(int& val) {
    const int x = 9618;
    std::cout << 9618;
    for (int i = 0; i < 9618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9619(int& val) {
    func_in_9619(val);
    const int x = 9619;
    std::cout << 9619;
    for (int i = 0; i < 9619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9619(int& val) {
    const int x = 9619;
    std::cout << 9619;
    for (int i = 0; i < 9619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9620(int& val) {
    func_in_9620(val);
    const int x = 9620;
    std::cout << 9620;
    for (int i = 0; i < 9620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9620(int& val) {
    const int x = 9620;
    std::cout << 9620;
    for (int i = 0; i < 9620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9621(int& val) {
    func_in_9621(val);
    const int x = 9621;
    std::cout << 9621;
    for (int i = 0; i < 9621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9621(int& val) {
    const int x = 9621;
    std::cout << 9621;
    for (int i = 0; i < 9621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9622(int& val) {
    func_in_9622(val);
    const int x = 9622;
    std::cout << 9622;
    for (int i = 0; i < 9622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9622(int& val) {
    const int x = 9622;
    std::cout << 9622;
    for (int i = 0; i < 9622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9623(int& val) {
    func_in_9623(val);
    const int x = 9623;
    std::cout << 9623;
    for (int i = 0; i < 9623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9623(int& val) {
    const int x = 9623;
    std::cout << 9623;
    for (int i = 0; i < 9623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9624(int& val) {
    func_in_9624(val);
    const int x = 9624;
    std::cout << 9624;
    for (int i = 0; i < 9624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9624(int& val) {
    const int x = 9624;
    std::cout << 9624;
    for (int i = 0; i < 9624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9625(int& val) {
    func_in_9625(val);
    const int x = 9625;
    std::cout << 9625;
    for (int i = 0; i < 9625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9625(int& val) {
    const int x = 9625;
    std::cout << 9625;
    for (int i = 0; i < 9625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9626(int& val) {
    func_in_9626(val);
    const int x = 9626;
    std::cout << 9626;
    for (int i = 0; i < 9626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9626(int& val) {
    const int x = 9626;
    std::cout << 9626;
    for (int i = 0; i < 9626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9627(int& val) {
    func_in_9627(val);
    const int x = 9627;
    std::cout << 9627;
    for (int i = 0; i < 9627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9627(int& val) {
    const int x = 9627;
    std::cout << 9627;
    for (int i = 0; i < 9627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9628(int& val) {
    func_in_9628(val);
    const int x = 9628;
    std::cout << 9628;
    for (int i = 0; i < 9628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9628(int& val) {
    const int x = 9628;
    std::cout << 9628;
    for (int i = 0; i < 9628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9629(int& val) {
    func_in_9629(val);
    const int x = 9629;
    std::cout << 9629;
    for (int i = 0; i < 9629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9629(int& val) {
    const int x = 9629;
    std::cout << 9629;
    for (int i = 0; i < 9629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9630(int& val) {
    func_in_9630(val);
    const int x = 9630;
    std::cout << 9630;
    for (int i = 0; i < 9630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9630(int& val) {
    const int x = 9630;
    std::cout << 9630;
    for (int i = 0; i < 9630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9631(int& val) {
    func_in_9631(val);
    const int x = 9631;
    std::cout << 9631;
    for (int i = 0; i < 9631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9631(int& val) {
    const int x = 9631;
    std::cout << 9631;
    for (int i = 0; i < 9631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9632(int& val) {
    func_in_9632(val);
    const int x = 9632;
    std::cout << 9632;
    for (int i = 0; i < 9632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9632(int& val) {
    const int x = 9632;
    std::cout << 9632;
    for (int i = 0; i < 9632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9633(int& val) {
    func_in_9633(val);
    const int x = 9633;
    std::cout << 9633;
    for (int i = 0; i < 9633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9633(int& val) {
    const int x = 9633;
    std::cout << 9633;
    for (int i = 0; i < 9633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9634(int& val) {
    func_in_9634(val);
    const int x = 9634;
    std::cout << 9634;
    for (int i = 0; i < 9634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9634(int& val) {
    const int x = 9634;
    std::cout << 9634;
    for (int i = 0; i < 9634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9635(int& val) {
    func_in_9635(val);
    const int x = 9635;
    std::cout << 9635;
    for (int i = 0; i < 9635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9635(int& val) {
    const int x = 9635;
    std::cout << 9635;
    for (int i = 0; i < 9635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9636(int& val) {
    func_in_9636(val);
    const int x = 9636;
    std::cout << 9636;
    for (int i = 0; i < 9636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9636(int& val) {
    const int x = 9636;
    std::cout << 9636;
    for (int i = 0; i < 9636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9637(int& val) {
    func_in_9637(val);
    const int x = 9637;
    std::cout << 9637;
    for (int i = 0; i < 9637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9637(int& val) {
    const int x = 9637;
    std::cout << 9637;
    for (int i = 0; i < 9637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9638(int& val) {
    func_in_9638(val);
    const int x = 9638;
    std::cout << 9638;
    for (int i = 0; i < 9638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9638(int& val) {
    const int x = 9638;
    std::cout << 9638;
    for (int i = 0; i < 9638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9639(int& val) {
    func_in_9639(val);
    const int x = 9639;
    std::cout << 9639;
    for (int i = 0; i < 9639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9639(int& val) {
    const int x = 9639;
    std::cout << 9639;
    for (int i = 0; i < 9639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9640(int& val) {
    func_in_9640(val);
    const int x = 9640;
    std::cout << 9640;
    for (int i = 0; i < 9640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9640(int& val) {
    const int x = 9640;
    std::cout << 9640;
    for (int i = 0; i < 9640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9641(int& val) {
    func_in_9641(val);
    const int x = 9641;
    std::cout << 9641;
    for (int i = 0; i < 9641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9641(int& val) {
    const int x = 9641;
    std::cout << 9641;
    for (int i = 0; i < 9641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9642(int& val) {
    func_in_9642(val);
    const int x = 9642;
    std::cout << 9642;
    for (int i = 0; i < 9642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9642(int& val) {
    const int x = 9642;
    std::cout << 9642;
    for (int i = 0; i < 9642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9643(int& val) {
    func_in_9643(val);
    const int x = 9643;
    std::cout << 9643;
    for (int i = 0; i < 9643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9643(int& val) {
    const int x = 9643;
    std::cout << 9643;
    for (int i = 0; i < 9643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9644(int& val) {
    func_in_9644(val);
    const int x = 9644;
    std::cout << 9644;
    for (int i = 0; i < 9644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9644(int& val) {
    const int x = 9644;
    std::cout << 9644;
    for (int i = 0; i < 9644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9645(int& val) {
    func_in_9645(val);
    const int x = 9645;
    std::cout << 9645;
    for (int i = 0; i < 9645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9645(int& val) {
    const int x = 9645;
    std::cout << 9645;
    for (int i = 0; i < 9645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9646(int& val) {
    func_in_9646(val);
    const int x = 9646;
    std::cout << 9646;
    for (int i = 0; i < 9646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9646(int& val) {
    const int x = 9646;
    std::cout << 9646;
    for (int i = 0; i < 9646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9647(int& val) {
    func_in_9647(val);
    const int x = 9647;
    std::cout << 9647;
    for (int i = 0; i < 9647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9647(int& val) {
    const int x = 9647;
    std::cout << 9647;
    for (int i = 0; i < 9647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9648(int& val) {
    func_in_9648(val);
    const int x = 9648;
    std::cout << 9648;
    for (int i = 0; i < 9648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9648(int& val) {
    const int x = 9648;
    std::cout << 9648;
    for (int i = 0; i < 9648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9649(int& val) {
    func_in_9649(val);
    const int x = 9649;
    std::cout << 9649;
    for (int i = 0; i < 9649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9649(int& val) {
    const int x = 9649;
    std::cout << 9649;
    for (int i = 0; i < 9649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9650(int& val) {
    func_in_9650(val);
    const int x = 9650;
    std::cout << 9650;
    for (int i = 0; i < 9650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9650(int& val) {
    const int x = 9650;
    std::cout << 9650;
    for (int i = 0; i < 9650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9651(int& val) {
    func_in_9651(val);
    const int x = 9651;
    std::cout << 9651;
    for (int i = 0; i < 9651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9651(int& val) {
    const int x = 9651;
    std::cout << 9651;
    for (int i = 0; i < 9651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9652(int& val) {
    func_in_9652(val);
    const int x = 9652;
    std::cout << 9652;
    for (int i = 0; i < 9652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9652(int& val) {
    const int x = 9652;
    std::cout << 9652;
    for (int i = 0; i < 9652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9653(int& val) {
    func_in_9653(val);
    const int x = 9653;
    std::cout << 9653;
    for (int i = 0; i < 9653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9653(int& val) {
    const int x = 9653;
    std::cout << 9653;
    for (int i = 0; i < 9653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9654(int& val) {
    func_in_9654(val);
    const int x = 9654;
    std::cout << 9654;
    for (int i = 0; i < 9654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9654(int& val) {
    const int x = 9654;
    std::cout << 9654;
    for (int i = 0; i < 9654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9655(int& val) {
    func_in_9655(val);
    const int x = 9655;
    std::cout << 9655;
    for (int i = 0; i < 9655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9655(int& val) {
    const int x = 9655;
    std::cout << 9655;
    for (int i = 0; i < 9655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9656(int& val) {
    func_in_9656(val);
    const int x = 9656;
    std::cout << 9656;
    for (int i = 0; i < 9656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9656(int& val) {
    const int x = 9656;
    std::cout << 9656;
    for (int i = 0; i < 9656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9657(int& val) {
    func_in_9657(val);
    const int x = 9657;
    std::cout << 9657;
    for (int i = 0; i < 9657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9657(int& val) {
    const int x = 9657;
    std::cout << 9657;
    for (int i = 0; i < 9657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9658(int& val) {
    func_in_9658(val);
    const int x = 9658;
    std::cout << 9658;
    for (int i = 0; i < 9658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9658(int& val) {
    const int x = 9658;
    std::cout << 9658;
    for (int i = 0; i < 9658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9659(int& val) {
    func_in_9659(val);
    const int x = 9659;
    std::cout << 9659;
    for (int i = 0; i < 9659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9659(int& val) {
    const int x = 9659;
    std::cout << 9659;
    for (int i = 0; i < 9659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9660(int& val) {
    func_in_9660(val);
    const int x = 9660;
    std::cout << 9660;
    for (int i = 0; i < 9660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9660(int& val) {
    const int x = 9660;
    std::cout << 9660;
    for (int i = 0; i < 9660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9661(int& val) {
    func_in_9661(val);
    const int x = 9661;
    std::cout << 9661;
    for (int i = 0; i < 9661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9661(int& val) {
    const int x = 9661;
    std::cout << 9661;
    for (int i = 0; i < 9661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9662(int& val) {
    func_in_9662(val);
    const int x = 9662;
    std::cout << 9662;
    for (int i = 0; i < 9662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9662(int& val) {
    const int x = 9662;
    std::cout << 9662;
    for (int i = 0; i < 9662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9663(int& val) {
    func_in_9663(val);
    const int x = 9663;
    std::cout << 9663;
    for (int i = 0; i < 9663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9663(int& val) {
    const int x = 9663;
    std::cout << 9663;
    for (int i = 0; i < 9663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9664(int& val) {
    func_in_9664(val);
    const int x = 9664;
    std::cout << 9664;
    for (int i = 0; i < 9664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9664(int& val) {
    const int x = 9664;
    std::cout << 9664;
    for (int i = 0; i < 9664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9665(int& val) {
    func_in_9665(val);
    const int x = 9665;
    std::cout << 9665;
    for (int i = 0; i < 9665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9665(int& val) {
    const int x = 9665;
    std::cout << 9665;
    for (int i = 0; i < 9665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9666(int& val) {
    func_in_9666(val);
    const int x = 9666;
    std::cout << 9666;
    for (int i = 0; i < 9666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9666(int& val) {
    const int x = 9666;
    std::cout << 9666;
    for (int i = 0; i < 9666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9667(int& val) {
    func_in_9667(val);
    const int x = 9667;
    std::cout << 9667;
    for (int i = 0; i < 9667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9667(int& val) {
    const int x = 9667;
    std::cout << 9667;
    for (int i = 0; i < 9667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9668(int& val) {
    func_in_9668(val);
    const int x = 9668;
    std::cout << 9668;
    for (int i = 0; i < 9668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9668(int& val) {
    const int x = 9668;
    std::cout << 9668;
    for (int i = 0; i < 9668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9669(int& val) {
    func_in_9669(val);
    const int x = 9669;
    std::cout << 9669;
    for (int i = 0; i < 9669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9669(int& val) {
    const int x = 9669;
    std::cout << 9669;
    for (int i = 0; i < 9669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9670(int& val) {
    func_in_9670(val);
    const int x = 9670;
    std::cout << 9670;
    for (int i = 0; i < 9670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9670(int& val) {
    const int x = 9670;
    std::cout << 9670;
    for (int i = 0; i < 9670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9671(int& val) {
    func_in_9671(val);
    const int x = 9671;
    std::cout << 9671;
    for (int i = 0; i < 9671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9671(int& val) {
    const int x = 9671;
    std::cout << 9671;
    for (int i = 0; i < 9671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9672(int& val) {
    func_in_9672(val);
    const int x = 9672;
    std::cout << 9672;
    for (int i = 0; i < 9672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9672(int& val) {
    const int x = 9672;
    std::cout << 9672;
    for (int i = 0; i < 9672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9673(int& val) {
    func_in_9673(val);
    const int x = 9673;
    std::cout << 9673;
    for (int i = 0; i < 9673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9673(int& val) {
    const int x = 9673;
    std::cout << 9673;
    for (int i = 0; i < 9673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9674(int& val) {
    func_in_9674(val);
    const int x = 9674;
    std::cout << 9674;
    for (int i = 0; i < 9674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9674(int& val) {
    const int x = 9674;
    std::cout << 9674;
    for (int i = 0; i < 9674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9675(int& val) {
    func_in_9675(val);
    const int x = 9675;
    std::cout << 9675;
    for (int i = 0; i < 9675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9675(int& val) {
    const int x = 9675;
    std::cout << 9675;
    for (int i = 0; i < 9675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9676(int& val) {
    func_in_9676(val);
    const int x = 9676;
    std::cout << 9676;
    for (int i = 0; i < 9676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9676(int& val) {
    const int x = 9676;
    std::cout << 9676;
    for (int i = 0; i < 9676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9677(int& val) {
    func_in_9677(val);
    const int x = 9677;
    std::cout << 9677;
    for (int i = 0; i < 9677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9677(int& val) {
    const int x = 9677;
    std::cout << 9677;
    for (int i = 0; i < 9677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9678(int& val) {
    func_in_9678(val);
    const int x = 9678;
    std::cout << 9678;
    for (int i = 0; i < 9678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9678(int& val) {
    const int x = 9678;
    std::cout << 9678;
    for (int i = 0; i < 9678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9679(int& val) {
    func_in_9679(val);
    const int x = 9679;
    std::cout << 9679;
    for (int i = 0; i < 9679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9679(int& val) {
    const int x = 9679;
    std::cout << 9679;
    for (int i = 0; i < 9679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9680(int& val) {
    func_in_9680(val);
    const int x = 9680;
    std::cout << 9680;
    for (int i = 0; i < 9680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9680(int& val) {
    const int x = 9680;
    std::cout << 9680;
    for (int i = 0; i < 9680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9681(int& val) {
    func_in_9681(val);
    const int x = 9681;
    std::cout << 9681;
    for (int i = 0; i < 9681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9681(int& val) {
    const int x = 9681;
    std::cout << 9681;
    for (int i = 0; i < 9681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9682(int& val) {
    func_in_9682(val);
    const int x = 9682;
    std::cout << 9682;
    for (int i = 0; i < 9682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9682(int& val) {
    const int x = 9682;
    std::cout << 9682;
    for (int i = 0; i < 9682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9683(int& val) {
    func_in_9683(val);
    const int x = 9683;
    std::cout << 9683;
    for (int i = 0; i < 9683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9683(int& val) {
    const int x = 9683;
    std::cout << 9683;
    for (int i = 0; i < 9683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9684(int& val) {
    func_in_9684(val);
    const int x = 9684;
    std::cout << 9684;
    for (int i = 0; i < 9684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9684(int& val) {
    const int x = 9684;
    std::cout << 9684;
    for (int i = 0; i < 9684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9685(int& val) {
    func_in_9685(val);
    const int x = 9685;
    std::cout << 9685;
    for (int i = 0; i < 9685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9685(int& val) {
    const int x = 9685;
    std::cout << 9685;
    for (int i = 0; i < 9685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9686(int& val) {
    func_in_9686(val);
    const int x = 9686;
    std::cout << 9686;
    for (int i = 0; i < 9686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9686(int& val) {
    const int x = 9686;
    std::cout << 9686;
    for (int i = 0; i < 9686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9687(int& val) {
    func_in_9687(val);
    const int x = 9687;
    std::cout << 9687;
    for (int i = 0; i < 9687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9687(int& val) {
    const int x = 9687;
    std::cout << 9687;
    for (int i = 0; i < 9687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9688(int& val) {
    func_in_9688(val);
    const int x = 9688;
    std::cout << 9688;
    for (int i = 0; i < 9688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9688(int& val) {
    const int x = 9688;
    std::cout << 9688;
    for (int i = 0; i < 9688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9689(int& val) {
    func_in_9689(val);
    const int x = 9689;
    std::cout << 9689;
    for (int i = 0; i < 9689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9689(int& val) {
    const int x = 9689;
    std::cout << 9689;
    for (int i = 0; i < 9689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9690(int& val) {
    func_in_9690(val);
    const int x = 9690;
    std::cout << 9690;
    for (int i = 0; i < 9690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9690(int& val) {
    const int x = 9690;
    std::cout << 9690;
    for (int i = 0; i < 9690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9691(int& val) {
    func_in_9691(val);
    const int x = 9691;
    std::cout << 9691;
    for (int i = 0; i < 9691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9691(int& val) {
    const int x = 9691;
    std::cout << 9691;
    for (int i = 0; i < 9691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9692(int& val) {
    func_in_9692(val);
    const int x = 9692;
    std::cout << 9692;
    for (int i = 0; i < 9692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9692(int& val) {
    const int x = 9692;
    std::cout << 9692;
    for (int i = 0; i < 9692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9693(int& val) {
    func_in_9693(val);
    const int x = 9693;
    std::cout << 9693;
    for (int i = 0; i < 9693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9693(int& val) {
    const int x = 9693;
    std::cout << 9693;
    for (int i = 0; i < 9693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9694(int& val) {
    func_in_9694(val);
    const int x = 9694;
    std::cout << 9694;
    for (int i = 0; i < 9694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9694(int& val) {
    const int x = 9694;
    std::cout << 9694;
    for (int i = 0; i < 9694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9695(int& val) {
    func_in_9695(val);
    const int x = 9695;
    std::cout << 9695;
    for (int i = 0; i < 9695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9695(int& val) {
    const int x = 9695;
    std::cout << 9695;
    for (int i = 0; i < 9695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9696(int& val) {
    func_in_9696(val);
    const int x = 9696;
    std::cout << 9696;
    for (int i = 0; i < 9696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9696(int& val) {
    const int x = 9696;
    std::cout << 9696;
    for (int i = 0; i < 9696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9697(int& val) {
    func_in_9697(val);
    const int x = 9697;
    std::cout << 9697;
    for (int i = 0; i < 9697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9697(int& val) {
    const int x = 9697;
    std::cout << 9697;
    for (int i = 0; i < 9697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9698(int& val) {
    func_in_9698(val);
    const int x = 9698;
    std::cout << 9698;
    for (int i = 0; i < 9698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9698(int& val) {
    const int x = 9698;
    std::cout << 9698;
    for (int i = 0; i < 9698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9699(int& val) {
    func_in_9699(val);
    const int x = 9699;
    std::cout << 9699;
    for (int i = 0; i < 9699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9699(int& val) {
    const int x = 9699;
    std::cout << 9699;
    for (int i = 0; i < 9699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9700(int& val) {
    func_in_9700(val);
    const int x = 9700;
    std::cout << 9700;
    for (int i = 0; i < 9700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9700(int& val) {
    const int x = 9700;
    std::cout << 9700;
    for (int i = 0; i < 9700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9701(int& val) {
    func_in_9701(val);
    const int x = 9701;
    std::cout << 9701;
    for (int i = 0; i < 9701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9701(int& val) {
    const int x = 9701;
    std::cout << 9701;
    for (int i = 0; i < 9701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9702(int& val) {
    func_in_9702(val);
    const int x = 9702;
    std::cout << 9702;
    for (int i = 0; i < 9702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9702(int& val) {
    const int x = 9702;
    std::cout << 9702;
    for (int i = 0; i < 9702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9703(int& val) {
    func_in_9703(val);
    const int x = 9703;
    std::cout << 9703;
    for (int i = 0; i < 9703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9703(int& val) {
    const int x = 9703;
    std::cout << 9703;
    for (int i = 0; i < 9703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9704(int& val) {
    func_in_9704(val);
    const int x = 9704;
    std::cout << 9704;
    for (int i = 0; i < 9704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9704(int& val) {
    const int x = 9704;
    std::cout << 9704;
    for (int i = 0; i < 9704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9705(int& val) {
    func_in_9705(val);
    const int x = 9705;
    std::cout << 9705;
    for (int i = 0; i < 9705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9705(int& val) {
    const int x = 9705;
    std::cout << 9705;
    for (int i = 0; i < 9705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9706(int& val) {
    func_in_9706(val);
    const int x = 9706;
    std::cout << 9706;
    for (int i = 0; i < 9706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9706(int& val) {
    const int x = 9706;
    std::cout << 9706;
    for (int i = 0; i < 9706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9707(int& val) {
    func_in_9707(val);
    const int x = 9707;
    std::cout << 9707;
    for (int i = 0; i < 9707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9707(int& val) {
    const int x = 9707;
    std::cout << 9707;
    for (int i = 0; i < 9707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9708(int& val) {
    func_in_9708(val);
    const int x = 9708;
    std::cout << 9708;
    for (int i = 0; i < 9708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9708(int& val) {
    const int x = 9708;
    std::cout << 9708;
    for (int i = 0; i < 9708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9709(int& val) {
    func_in_9709(val);
    const int x = 9709;
    std::cout << 9709;
    for (int i = 0; i < 9709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9709(int& val) {
    const int x = 9709;
    std::cout << 9709;
    for (int i = 0; i < 9709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9710(int& val) {
    func_in_9710(val);
    const int x = 9710;
    std::cout << 9710;
    for (int i = 0; i < 9710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9710(int& val) {
    const int x = 9710;
    std::cout << 9710;
    for (int i = 0; i < 9710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9711(int& val) {
    func_in_9711(val);
    const int x = 9711;
    std::cout << 9711;
    for (int i = 0; i < 9711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9711(int& val) {
    const int x = 9711;
    std::cout << 9711;
    for (int i = 0; i < 9711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9712(int& val) {
    func_in_9712(val);
    const int x = 9712;
    std::cout << 9712;
    for (int i = 0; i < 9712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9712(int& val) {
    const int x = 9712;
    std::cout << 9712;
    for (int i = 0; i < 9712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9713(int& val) {
    func_in_9713(val);
    const int x = 9713;
    std::cout << 9713;
    for (int i = 0; i < 9713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9713(int& val) {
    const int x = 9713;
    std::cout << 9713;
    for (int i = 0; i < 9713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9714(int& val) {
    func_in_9714(val);
    const int x = 9714;
    std::cout << 9714;
    for (int i = 0; i < 9714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9714(int& val) {
    const int x = 9714;
    std::cout << 9714;
    for (int i = 0; i < 9714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9715(int& val) {
    func_in_9715(val);
    const int x = 9715;
    std::cout << 9715;
    for (int i = 0; i < 9715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9715(int& val) {
    const int x = 9715;
    std::cout << 9715;
    for (int i = 0; i < 9715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9716(int& val) {
    func_in_9716(val);
    const int x = 9716;
    std::cout << 9716;
    for (int i = 0; i < 9716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9716(int& val) {
    const int x = 9716;
    std::cout << 9716;
    for (int i = 0; i < 9716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9717(int& val) {
    func_in_9717(val);
    const int x = 9717;
    std::cout << 9717;
    for (int i = 0; i < 9717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9717(int& val) {
    const int x = 9717;
    std::cout << 9717;
    for (int i = 0; i < 9717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9718(int& val) {
    func_in_9718(val);
    const int x = 9718;
    std::cout << 9718;
    for (int i = 0; i < 9718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9718(int& val) {
    const int x = 9718;
    std::cout << 9718;
    for (int i = 0; i < 9718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9719(int& val) {
    func_in_9719(val);
    const int x = 9719;
    std::cout << 9719;
    for (int i = 0; i < 9719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9719(int& val) {
    const int x = 9719;
    std::cout << 9719;
    for (int i = 0; i < 9719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9720(int& val) {
    func_in_9720(val);
    const int x = 9720;
    std::cout << 9720;
    for (int i = 0; i < 9720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9720(int& val) {
    const int x = 9720;
    std::cout << 9720;
    for (int i = 0; i < 9720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9721(int& val) {
    func_in_9721(val);
    const int x = 9721;
    std::cout << 9721;
    for (int i = 0; i < 9721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9721(int& val) {
    const int x = 9721;
    std::cout << 9721;
    for (int i = 0; i < 9721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9722(int& val) {
    func_in_9722(val);
    const int x = 9722;
    std::cout << 9722;
    for (int i = 0; i < 9722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9722(int& val) {
    const int x = 9722;
    std::cout << 9722;
    for (int i = 0; i < 9722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9723(int& val) {
    func_in_9723(val);
    const int x = 9723;
    std::cout << 9723;
    for (int i = 0; i < 9723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9723(int& val) {
    const int x = 9723;
    std::cout << 9723;
    for (int i = 0; i < 9723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9724(int& val) {
    func_in_9724(val);
    const int x = 9724;
    std::cout << 9724;
    for (int i = 0; i < 9724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9724(int& val) {
    const int x = 9724;
    std::cout << 9724;
    for (int i = 0; i < 9724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9725(int& val) {
    func_in_9725(val);
    const int x = 9725;
    std::cout << 9725;
    for (int i = 0; i < 9725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9725(int& val) {
    const int x = 9725;
    std::cout << 9725;
    for (int i = 0; i < 9725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9726(int& val) {
    func_in_9726(val);
    const int x = 9726;
    std::cout << 9726;
    for (int i = 0; i < 9726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9726(int& val) {
    const int x = 9726;
    std::cout << 9726;
    for (int i = 0; i < 9726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9727(int& val) {
    func_in_9727(val);
    const int x = 9727;
    std::cout << 9727;
    for (int i = 0; i < 9727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9727(int& val) {
    const int x = 9727;
    std::cout << 9727;
    for (int i = 0; i < 9727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9728(int& val) {
    func_in_9728(val);
    const int x = 9728;
    std::cout << 9728;
    for (int i = 0; i < 9728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9728(int& val) {
    const int x = 9728;
    std::cout << 9728;
    for (int i = 0; i < 9728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9729(int& val) {
    func_in_9729(val);
    const int x = 9729;
    std::cout << 9729;
    for (int i = 0; i < 9729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9729(int& val) {
    const int x = 9729;
    std::cout << 9729;
    for (int i = 0; i < 9729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9730(int& val) {
    func_in_9730(val);
    const int x = 9730;
    std::cout << 9730;
    for (int i = 0; i < 9730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9730(int& val) {
    const int x = 9730;
    std::cout << 9730;
    for (int i = 0; i < 9730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9731(int& val) {
    func_in_9731(val);
    const int x = 9731;
    std::cout << 9731;
    for (int i = 0; i < 9731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9731(int& val) {
    const int x = 9731;
    std::cout << 9731;
    for (int i = 0; i < 9731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9732(int& val) {
    func_in_9732(val);
    const int x = 9732;
    std::cout << 9732;
    for (int i = 0; i < 9732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9732(int& val) {
    const int x = 9732;
    std::cout << 9732;
    for (int i = 0; i < 9732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9733(int& val) {
    func_in_9733(val);
    const int x = 9733;
    std::cout << 9733;
    for (int i = 0; i < 9733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9733(int& val) {
    const int x = 9733;
    std::cout << 9733;
    for (int i = 0; i < 9733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9734(int& val) {
    func_in_9734(val);
    const int x = 9734;
    std::cout << 9734;
    for (int i = 0; i < 9734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9734(int& val) {
    const int x = 9734;
    std::cout << 9734;
    for (int i = 0; i < 9734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9735(int& val) {
    func_in_9735(val);
    const int x = 9735;
    std::cout << 9735;
    for (int i = 0; i < 9735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9735(int& val) {
    const int x = 9735;
    std::cout << 9735;
    for (int i = 0; i < 9735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9736(int& val) {
    func_in_9736(val);
    const int x = 9736;
    std::cout << 9736;
    for (int i = 0; i < 9736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9736(int& val) {
    const int x = 9736;
    std::cout << 9736;
    for (int i = 0; i < 9736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9737(int& val) {
    func_in_9737(val);
    const int x = 9737;
    std::cout << 9737;
    for (int i = 0; i < 9737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9737(int& val) {
    const int x = 9737;
    std::cout << 9737;
    for (int i = 0; i < 9737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9738(int& val) {
    func_in_9738(val);
    const int x = 9738;
    std::cout << 9738;
    for (int i = 0; i < 9738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9738(int& val) {
    const int x = 9738;
    std::cout << 9738;
    for (int i = 0; i < 9738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9739(int& val) {
    func_in_9739(val);
    const int x = 9739;
    std::cout << 9739;
    for (int i = 0; i < 9739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9739(int& val) {
    const int x = 9739;
    std::cout << 9739;
    for (int i = 0; i < 9739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9740(int& val) {
    func_in_9740(val);
    const int x = 9740;
    std::cout << 9740;
    for (int i = 0; i < 9740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9740(int& val) {
    const int x = 9740;
    std::cout << 9740;
    for (int i = 0; i < 9740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9741(int& val) {
    func_in_9741(val);
    const int x = 9741;
    std::cout << 9741;
    for (int i = 0; i < 9741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9741(int& val) {
    const int x = 9741;
    std::cout << 9741;
    for (int i = 0; i < 9741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9742(int& val) {
    func_in_9742(val);
    const int x = 9742;
    std::cout << 9742;
    for (int i = 0; i < 9742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9742(int& val) {
    const int x = 9742;
    std::cout << 9742;
    for (int i = 0; i < 9742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9743(int& val) {
    func_in_9743(val);
    const int x = 9743;
    std::cout << 9743;
    for (int i = 0; i < 9743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9743(int& val) {
    const int x = 9743;
    std::cout << 9743;
    for (int i = 0; i < 9743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9744(int& val) {
    func_in_9744(val);
    const int x = 9744;
    std::cout << 9744;
    for (int i = 0; i < 9744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9744(int& val) {
    const int x = 9744;
    std::cout << 9744;
    for (int i = 0; i < 9744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9745(int& val) {
    func_in_9745(val);
    const int x = 9745;
    std::cout << 9745;
    for (int i = 0; i < 9745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9745(int& val) {
    const int x = 9745;
    std::cout << 9745;
    for (int i = 0; i < 9745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9746(int& val) {
    func_in_9746(val);
    const int x = 9746;
    std::cout << 9746;
    for (int i = 0; i < 9746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9746(int& val) {
    const int x = 9746;
    std::cout << 9746;
    for (int i = 0; i < 9746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9747(int& val) {
    func_in_9747(val);
    const int x = 9747;
    std::cout << 9747;
    for (int i = 0; i < 9747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9747(int& val) {
    const int x = 9747;
    std::cout << 9747;
    for (int i = 0; i < 9747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9748(int& val) {
    func_in_9748(val);
    const int x = 9748;
    std::cout << 9748;
    for (int i = 0; i < 9748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9748(int& val) {
    const int x = 9748;
    std::cout << 9748;
    for (int i = 0; i < 9748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9749(int& val) {
    func_in_9749(val);
    const int x = 9749;
    std::cout << 9749;
    for (int i = 0; i < 9749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9749(int& val) {
    const int x = 9749;
    std::cout << 9749;
    for (int i = 0; i < 9749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9750(int& val) {
    func_in_9750(val);
    const int x = 9750;
    std::cout << 9750;
    for (int i = 0; i < 9750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9750(int& val) {
    const int x = 9750;
    std::cout << 9750;
    for (int i = 0; i < 9750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9751(int& val) {
    func_in_9751(val);
    const int x = 9751;
    std::cout << 9751;
    for (int i = 0; i < 9751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9751(int& val) {
    const int x = 9751;
    std::cout << 9751;
    for (int i = 0; i < 9751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9752(int& val) {
    func_in_9752(val);
    const int x = 9752;
    std::cout << 9752;
    for (int i = 0; i < 9752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9752(int& val) {
    const int x = 9752;
    std::cout << 9752;
    for (int i = 0; i < 9752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9753(int& val) {
    func_in_9753(val);
    const int x = 9753;
    std::cout << 9753;
    for (int i = 0; i < 9753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9753(int& val) {
    const int x = 9753;
    std::cout << 9753;
    for (int i = 0; i < 9753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9754(int& val) {
    func_in_9754(val);
    const int x = 9754;
    std::cout << 9754;
    for (int i = 0; i < 9754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9754(int& val) {
    const int x = 9754;
    std::cout << 9754;
    for (int i = 0; i < 9754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9755(int& val) {
    func_in_9755(val);
    const int x = 9755;
    std::cout << 9755;
    for (int i = 0; i < 9755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9755(int& val) {
    const int x = 9755;
    std::cout << 9755;
    for (int i = 0; i < 9755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9756(int& val) {
    func_in_9756(val);
    const int x = 9756;
    std::cout << 9756;
    for (int i = 0; i < 9756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9756(int& val) {
    const int x = 9756;
    std::cout << 9756;
    for (int i = 0; i < 9756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9757(int& val) {
    func_in_9757(val);
    const int x = 9757;
    std::cout << 9757;
    for (int i = 0; i < 9757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9757(int& val) {
    const int x = 9757;
    std::cout << 9757;
    for (int i = 0; i < 9757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9758(int& val) {
    func_in_9758(val);
    const int x = 9758;
    std::cout << 9758;
    for (int i = 0; i < 9758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9758(int& val) {
    const int x = 9758;
    std::cout << 9758;
    for (int i = 0; i < 9758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9759(int& val) {
    func_in_9759(val);
    const int x = 9759;
    std::cout << 9759;
    for (int i = 0; i < 9759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9759(int& val) {
    const int x = 9759;
    std::cout << 9759;
    for (int i = 0; i < 9759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9760(int& val) {
    func_in_9760(val);
    const int x = 9760;
    std::cout << 9760;
    for (int i = 0; i < 9760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9760(int& val) {
    const int x = 9760;
    std::cout << 9760;
    for (int i = 0; i < 9760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9761(int& val) {
    func_in_9761(val);
    const int x = 9761;
    std::cout << 9761;
    for (int i = 0; i < 9761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9761(int& val) {
    const int x = 9761;
    std::cout << 9761;
    for (int i = 0; i < 9761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9762(int& val) {
    func_in_9762(val);
    const int x = 9762;
    std::cout << 9762;
    for (int i = 0; i < 9762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9762(int& val) {
    const int x = 9762;
    std::cout << 9762;
    for (int i = 0; i < 9762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9763(int& val) {
    func_in_9763(val);
    const int x = 9763;
    std::cout << 9763;
    for (int i = 0; i < 9763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9763(int& val) {
    const int x = 9763;
    std::cout << 9763;
    for (int i = 0; i < 9763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9764(int& val) {
    func_in_9764(val);
    const int x = 9764;
    std::cout << 9764;
    for (int i = 0; i < 9764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9764(int& val) {
    const int x = 9764;
    std::cout << 9764;
    for (int i = 0; i < 9764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9765(int& val) {
    func_in_9765(val);
    const int x = 9765;
    std::cout << 9765;
    for (int i = 0; i < 9765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9765(int& val) {
    const int x = 9765;
    std::cout << 9765;
    for (int i = 0; i < 9765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9766(int& val) {
    func_in_9766(val);
    const int x = 9766;
    std::cout << 9766;
    for (int i = 0; i < 9766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9766(int& val) {
    const int x = 9766;
    std::cout << 9766;
    for (int i = 0; i < 9766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9767(int& val) {
    func_in_9767(val);
    const int x = 9767;
    std::cout << 9767;
    for (int i = 0; i < 9767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9767(int& val) {
    const int x = 9767;
    std::cout << 9767;
    for (int i = 0; i < 9767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9768(int& val) {
    func_in_9768(val);
    const int x = 9768;
    std::cout << 9768;
    for (int i = 0; i < 9768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9768(int& val) {
    const int x = 9768;
    std::cout << 9768;
    for (int i = 0; i < 9768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9769(int& val) {
    func_in_9769(val);
    const int x = 9769;
    std::cout << 9769;
    for (int i = 0; i < 9769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9769(int& val) {
    const int x = 9769;
    std::cout << 9769;
    for (int i = 0; i < 9769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9770(int& val) {
    func_in_9770(val);
    const int x = 9770;
    std::cout << 9770;
    for (int i = 0; i < 9770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9770(int& val) {
    const int x = 9770;
    std::cout << 9770;
    for (int i = 0; i < 9770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9771(int& val) {
    func_in_9771(val);
    const int x = 9771;
    std::cout << 9771;
    for (int i = 0; i < 9771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9771(int& val) {
    const int x = 9771;
    std::cout << 9771;
    for (int i = 0; i < 9771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9772(int& val) {
    func_in_9772(val);
    const int x = 9772;
    std::cout << 9772;
    for (int i = 0; i < 9772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9772(int& val) {
    const int x = 9772;
    std::cout << 9772;
    for (int i = 0; i < 9772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9773(int& val) {
    func_in_9773(val);
    const int x = 9773;
    std::cout << 9773;
    for (int i = 0; i < 9773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9773(int& val) {
    const int x = 9773;
    std::cout << 9773;
    for (int i = 0; i < 9773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9774(int& val) {
    func_in_9774(val);
    const int x = 9774;
    std::cout << 9774;
    for (int i = 0; i < 9774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9774(int& val) {
    const int x = 9774;
    std::cout << 9774;
    for (int i = 0; i < 9774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9775(int& val) {
    func_in_9775(val);
    const int x = 9775;
    std::cout << 9775;
    for (int i = 0; i < 9775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9775(int& val) {
    const int x = 9775;
    std::cout << 9775;
    for (int i = 0; i < 9775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9776(int& val) {
    func_in_9776(val);
    const int x = 9776;
    std::cout << 9776;
    for (int i = 0; i < 9776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9776(int& val) {
    const int x = 9776;
    std::cout << 9776;
    for (int i = 0; i < 9776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9777(int& val) {
    func_in_9777(val);
    const int x = 9777;
    std::cout << 9777;
    for (int i = 0; i < 9777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9777(int& val) {
    const int x = 9777;
    std::cout << 9777;
    for (int i = 0; i < 9777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9778(int& val) {
    func_in_9778(val);
    const int x = 9778;
    std::cout << 9778;
    for (int i = 0; i < 9778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9778(int& val) {
    const int x = 9778;
    std::cout << 9778;
    for (int i = 0; i < 9778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9779(int& val) {
    func_in_9779(val);
    const int x = 9779;
    std::cout << 9779;
    for (int i = 0; i < 9779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9779(int& val) {
    const int x = 9779;
    std::cout << 9779;
    for (int i = 0; i < 9779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9780(int& val) {
    func_in_9780(val);
    const int x = 9780;
    std::cout << 9780;
    for (int i = 0; i < 9780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9780(int& val) {
    const int x = 9780;
    std::cout << 9780;
    for (int i = 0; i < 9780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9781(int& val) {
    func_in_9781(val);
    const int x = 9781;
    std::cout << 9781;
    for (int i = 0; i < 9781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9781(int& val) {
    const int x = 9781;
    std::cout << 9781;
    for (int i = 0; i < 9781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9782(int& val) {
    func_in_9782(val);
    const int x = 9782;
    std::cout << 9782;
    for (int i = 0; i < 9782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9782(int& val) {
    const int x = 9782;
    std::cout << 9782;
    for (int i = 0; i < 9782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9783(int& val) {
    func_in_9783(val);
    const int x = 9783;
    std::cout << 9783;
    for (int i = 0; i < 9783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9783(int& val) {
    const int x = 9783;
    std::cout << 9783;
    for (int i = 0; i < 9783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9784(int& val) {
    func_in_9784(val);
    const int x = 9784;
    std::cout << 9784;
    for (int i = 0; i < 9784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9784(int& val) {
    const int x = 9784;
    std::cout << 9784;
    for (int i = 0; i < 9784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9785(int& val) {
    func_in_9785(val);
    const int x = 9785;
    std::cout << 9785;
    for (int i = 0; i < 9785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9785(int& val) {
    const int x = 9785;
    std::cout << 9785;
    for (int i = 0; i < 9785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9786(int& val) {
    func_in_9786(val);
    const int x = 9786;
    std::cout << 9786;
    for (int i = 0; i < 9786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9786(int& val) {
    const int x = 9786;
    std::cout << 9786;
    for (int i = 0; i < 9786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9787(int& val) {
    func_in_9787(val);
    const int x = 9787;
    std::cout << 9787;
    for (int i = 0; i < 9787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9787(int& val) {
    const int x = 9787;
    std::cout << 9787;
    for (int i = 0; i < 9787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9788(int& val) {
    func_in_9788(val);
    const int x = 9788;
    std::cout << 9788;
    for (int i = 0; i < 9788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9788(int& val) {
    const int x = 9788;
    std::cout << 9788;
    for (int i = 0; i < 9788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9789(int& val) {
    func_in_9789(val);
    const int x = 9789;
    std::cout << 9789;
    for (int i = 0; i < 9789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9789(int& val) {
    const int x = 9789;
    std::cout << 9789;
    for (int i = 0; i < 9789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9790(int& val) {
    func_in_9790(val);
    const int x = 9790;
    std::cout << 9790;
    for (int i = 0; i < 9790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9790(int& val) {
    const int x = 9790;
    std::cout << 9790;
    for (int i = 0; i < 9790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9791(int& val) {
    func_in_9791(val);
    const int x = 9791;
    std::cout << 9791;
    for (int i = 0; i < 9791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9791(int& val) {
    const int x = 9791;
    std::cout << 9791;
    for (int i = 0; i < 9791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9792(int& val) {
    func_in_9792(val);
    const int x = 9792;
    std::cout << 9792;
    for (int i = 0; i < 9792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9792(int& val) {
    const int x = 9792;
    std::cout << 9792;
    for (int i = 0; i < 9792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9793(int& val) {
    func_in_9793(val);
    const int x = 9793;
    std::cout << 9793;
    for (int i = 0; i < 9793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9793(int& val) {
    const int x = 9793;
    std::cout << 9793;
    for (int i = 0; i < 9793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9794(int& val) {
    func_in_9794(val);
    const int x = 9794;
    std::cout << 9794;
    for (int i = 0; i < 9794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9794(int& val) {
    const int x = 9794;
    std::cout << 9794;
    for (int i = 0; i < 9794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9795(int& val) {
    func_in_9795(val);
    const int x = 9795;
    std::cout << 9795;
    for (int i = 0; i < 9795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9795(int& val) {
    const int x = 9795;
    std::cout << 9795;
    for (int i = 0; i < 9795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9796(int& val) {
    func_in_9796(val);
    const int x = 9796;
    std::cout << 9796;
    for (int i = 0; i < 9796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9796(int& val) {
    const int x = 9796;
    std::cout << 9796;
    for (int i = 0; i < 9796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9797(int& val) {
    func_in_9797(val);
    const int x = 9797;
    std::cout << 9797;
    for (int i = 0; i < 9797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9797(int& val) {
    const int x = 9797;
    std::cout << 9797;
    for (int i = 0; i < 9797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9798(int& val) {
    func_in_9798(val);
    const int x = 9798;
    std::cout << 9798;
    for (int i = 0; i < 9798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9798(int& val) {
    const int x = 9798;
    std::cout << 9798;
    for (int i = 0; i < 9798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9799(int& val) {
    func_in_9799(val);
    const int x = 9799;
    std::cout << 9799;
    for (int i = 0; i < 9799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9799(int& val) {
    const int x = 9799;
    std::cout << 9799;
    for (int i = 0; i < 9799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9800(int& val) {
    func_in_9800(val);
    const int x = 9800;
    std::cout << 9800;
    for (int i = 0; i < 9800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9800(int& val) {
    const int x = 9800;
    std::cout << 9800;
    for (int i = 0; i < 9800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9801(int& val) {
    func_in_9801(val);
    const int x = 9801;
    std::cout << 9801;
    for (int i = 0; i < 9801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9801(int& val) {
    const int x = 9801;
    std::cout << 9801;
    for (int i = 0; i < 9801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9802(int& val) {
    func_in_9802(val);
    const int x = 9802;
    std::cout << 9802;
    for (int i = 0; i < 9802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9802(int& val) {
    const int x = 9802;
    std::cout << 9802;
    for (int i = 0; i < 9802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9803(int& val) {
    func_in_9803(val);
    const int x = 9803;
    std::cout << 9803;
    for (int i = 0; i < 9803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9803(int& val) {
    const int x = 9803;
    std::cout << 9803;
    for (int i = 0; i < 9803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9804(int& val) {
    func_in_9804(val);
    const int x = 9804;
    std::cout << 9804;
    for (int i = 0; i < 9804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9804(int& val) {
    const int x = 9804;
    std::cout << 9804;
    for (int i = 0; i < 9804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9805(int& val) {
    func_in_9805(val);
    const int x = 9805;
    std::cout << 9805;
    for (int i = 0; i < 9805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9805(int& val) {
    const int x = 9805;
    std::cout << 9805;
    for (int i = 0; i < 9805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9806(int& val) {
    func_in_9806(val);
    const int x = 9806;
    std::cout << 9806;
    for (int i = 0; i < 9806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9806(int& val) {
    const int x = 9806;
    std::cout << 9806;
    for (int i = 0; i < 9806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9807(int& val) {
    func_in_9807(val);
    const int x = 9807;
    std::cout << 9807;
    for (int i = 0; i < 9807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9807(int& val) {
    const int x = 9807;
    std::cout << 9807;
    for (int i = 0; i < 9807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9808(int& val) {
    func_in_9808(val);
    const int x = 9808;
    std::cout << 9808;
    for (int i = 0; i < 9808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9808(int& val) {
    const int x = 9808;
    std::cout << 9808;
    for (int i = 0; i < 9808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9809(int& val) {
    func_in_9809(val);
    const int x = 9809;
    std::cout << 9809;
    for (int i = 0; i < 9809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9809(int& val) {
    const int x = 9809;
    std::cout << 9809;
    for (int i = 0; i < 9809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9810(int& val) {
    func_in_9810(val);
    const int x = 9810;
    std::cout << 9810;
    for (int i = 0; i < 9810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9810(int& val) {
    const int x = 9810;
    std::cout << 9810;
    for (int i = 0; i < 9810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9811(int& val) {
    func_in_9811(val);
    const int x = 9811;
    std::cout << 9811;
    for (int i = 0; i < 9811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9811(int& val) {
    const int x = 9811;
    std::cout << 9811;
    for (int i = 0; i < 9811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9812(int& val) {
    func_in_9812(val);
    const int x = 9812;
    std::cout << 9812;
    for (int i = 0; i < 9812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9812(int& val) {
    const int x = 9812;
    std::cout << 9812;
    for (int i = 0; i < 9812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9813(int& val) {
    func_in_9813(val);
    const int x = 9813;
    std::cout << 9813;
    for (int i = 0; i < 9813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9813(int& val) {
    const int x = 9813;
    std::cout << 9813;
    for (int i = 0; i < 9813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9814(int& val) {
    func_in_9814(val);
    const int x = 9814;
    std::cout << 9814;
    for (int i = 0; i < 9814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9814(int& val) {
    const int x = 9814;
    std::cout << 9814;
    for (int i = 0; i < 9814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9815(int& val) {
    func_in_9815(val);
    const int x = 9815;
    std::cout << 9815;
    for (int i = 0; i < 9815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9815(int& val) {
    const int x = 9815;
    std::cout << 9815;
    for (int i = 0; i < 9815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9816(int& val) {
    func_in_9816(val);
    const int x = 9816;
    std::cout << 9816;
    for (int i = 0; i < 9816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9816(int& val) {
    const int x = 9816;
    std::cout << 9816;
    for (int i = 0; i < 9816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9817(int& val) {
    func_in_9817(val);
    const int x = 9817;
    std::cout << 9817;
    for (int i = 0; i < 9817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9817(int& val) {
    const int x = 9817;
    std::cout << 9817;
    for (int i = 0; i < 9817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9818(int& val) {
    func_in_9818(val);
    const int x = 9818;
    std::cout << 9818;
    for (int i = 0; i < 9818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9818(int& val) {
    const int x = 9818;
    std::cout << 9818;
    for (int i = 0; i < 9818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9819(int& val) {
    func_in_9819(val);
    const int x = 9819;
    std::cout << 9819;
    for (int i = 0; i < 9819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9819(int& val) {
    const int x = 9819;
    std::cout << 9819;
    for (int i = 0; i < 9819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9820(int& val) {
    func_in_9820(val);
    const int x = 9820;
    std::cout << 9820;
    for (int i = 0; i < 9820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9820(int& val) {
    const int x = 9820;
    std::cout << 9820;
    for (int i = 0; i < 9820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9821(int& val) {
    func_in_9821(val);
    const int x = 9821;
    std::cout << 9821;
    for (int i = 0; i < 9821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9821(int& val) {
    const int x = 9821;
    std::cout << 9821;
    for (int i = 0; i < 9821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9822(int& val) {
    func_in_9822(val);
    const int x = 9822;
    std::cout << 9822;
    for (int i = 0; i < 9822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9822(int& val) {
    const int x = 9822;
    std::cout << 9822;
    for (int i = 0; i < 9822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9823(int& val) {
    func_in_9823(val);
    const int x = 9823;
    std::cout << 9823;
    for (int i = 0; i < 9823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9823(int& val) {
    const int x = 9823;
    std::cout << 9823;
    for (int i = 0; i < 9823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9824(int& val) {
    func_in_9824(val);
    const int x = 9824;
    std::cout << 9824;
    for (int i = 0; i < 9824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9824(int& val) {
    const int x = 9824;
    std::cout << 9824;
    for (int i = 0; i < 9824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9825(int& val) {
    func_in_9825(val);
    const int x = 9825;
    std::cout << 9825;
    for (int i = 0; i < 9825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9825(int& val) {
    const int x = 9825;
    std::cout << 9825;
    for (int i = 0; i < 9825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9826(int& val) {
    func_in_9826(val);
    const int x = 9826;
    std::cout << 9826;
    for (int i = 0; i < 9826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9826(int& val) {
    const int x = 9826;
    std::cout << 9826;
    for (int i = 0; i < 9826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9827(int& val) {
    func_in_9827(val);
    const int x = 9827;
    std::cout << 9827;
    for (int i = 0; i < 9827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9827(int& val) {
    const int x = 9827;
    std::cout << 9827;
    for (int i = 0; i < 9827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9828(int& val) {
    func_in_9828(val);
    const int x = 9828;
    std::cout << 9828;
    for (int i = 0; i < 9828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9828(int& val) {
    const int x = 9828;
    std::cout << 9828;
    for (int i = 0; i < 9828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9829(int& val) {
    func_in_9829(val);
    const int x = 9829;
    std::cout << 9829;
    for (int i = 0; i < 9829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9829(int& val) {
    const int x = 9829;
    std::cout << 9829;
    for (int i = 0; i < 9829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9830(int& val) {
    func_in_9830(val);
    const int x = 9830;
    std::cout << 9830;
    for (int i = 0; i < 9830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9830(int& val) {
    const int x = 9830;
    std::cout << 9830;
    for (int i = 0; i < 9830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9831(int& val) {
    func_in_9831(val);
    const int x = 9831;
    std::cout << 9831;
    for (int i = 0; i < 9831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9831(int& val) {
    const int x = 9831;
    std::cout << 9831;
    for (int i = 0; i < 9831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9832(int& val) {
    func_in_9832(val);
    const int x = 9832;
    std::cout << 9832;
    for (int i = 0; i < 9832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9832(int& val) {
    const int x = 9832;
    std::cout << 9832;
    for (int i = 0; i < 9832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9833(int& val) {
    func_in_9833(val);
    const int x = 9833;
    std::cout << 9833;
    for (int i = 0; i < 9833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9833(int& val) {
    const int x = 9833;
    std::cout << 9833;
    for (int i = 0; i < 9833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9834(int& val) {
    func_in_9834(val);
    const int x = 9834;
    std::cout << 9834;
    for (int i = 0; i < 9834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9834(int& val) {
    const int x = 9834;
    std::cout << 9834;
    for (int i = 0; i < 9834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9835(int& val) {
    func_in_9835(val);
    const int x = 9835;
    std::cout << 9835;
    for (int i = 0; i < 9835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9835(int& val) {
    const int x = 9835;
    std::cout << 9835;
    for (int i = 0; i < 9835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9836(int& val) {
    func_in_9836(val);
    const int x = 9836;
    std::cout << 9836;
    for (int i = 0; i < 9836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9836(int& val) {
    const int x = 9836;
    std::cout << 9836;
    for (int i = 0; i < 9836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9837(int& val) {
    func_in_9837(val);
    const int x = 9837;
    std::cout << 9837;
    for (int i = 0; i < 9837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9837(int& val) {
    const int x = 9837;
    std::cout << 9837;
    for (int i = 0; i < 9837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9838(int& val) {
    func_in_9838(val);
    const int x = 9838;
    std::cout << 9838;
    for (int i = 0; i < 9838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9838(int& val) {
    const int x = 9838;
    std::cout << 9838;
    for (int i = 0; i < 9838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9839(int& val) {
    func_in_9839(val);
    const int x = 9839;
    std::cout << 9839;
    for (int i = 0; i < 9839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9839(int& val) {
    const int x = 9839;
    std::cout << 9839;
    for (int i = 0; i < 9839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9840(int& val) {
    func_in_9840(val);
    const int x = 9840;
    std::cout << 9840;
    for (int i = 0; i < 9840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9840(int& val) {
    const int x = 9840;
    std::cout << 9840;
    for (int i = 0; i < 9840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9841(int& val) {
    func_in_9841(val);
    const int x = 9841;
    std::cout << 9841;
    for (int i = 0; i < 9841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9841(int& val) {
    const int x = 9841;
    std::cout << 9841;
    for (int i = 0; i < 9841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9842(int& val) {
    func_in_9842(val);
    const int x = 9842;
    std::cout << 9842;
    for (int i = 0; i < 9842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9842(int& val) {
    const int x = 9842;
    std::cout << 9842;
    for (int i = 0; i < 9842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9843(int& val) {
    func_in_9843(val);
    const int x = 9843;
    std::cout << 9843;
    for (int i = 0; i < 9843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9843(int& val) {
    const int x = 9843;
    std::cout << 9843;
    for (int i = 0; i < 9843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9844(int& val) {
    func_in_9844(val);
    const int x = 9844;
    std::cout << 9844;
    for (int i = 0; i < 9844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9844(int& val) {
    const int x = 9844;
    std::cout << 9844;
    for (int i = 0; i < 9844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9845(int& val) {
    func_in_9845(val);
    const int x = 9845;
    std::cout << 9845;
    for (int i = 0; i < 9845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9845(int& val) {
    const int x = 9845;
    std::cout << 9845;
    for (int i = 0; i < 9845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9846(int& val) {
    func_in_9846(val);
    const int x = 9846;
    std::cout << 9846;
    for (int i = 0; i < 9846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9846(int& val) {
    const int x = 9846;
    std::cout << 9846;
    for (int i = 0; i < 9846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9847(int& val) {
    func_in_9847(val);
    const int x = 9847;
    std::cout << 9847;
    for (int i = 0; i < 9847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9847(int& val) {
    const int x = 9847;
    std::cout << 9847;
    for (int i = 0; i < 9847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9848(int& val) {
    func_in_9848(val);
    const int x = 9848;
    std::cout << 9848;
    for (int i = 0; i < 9848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9848(int& val) {
    const int x = 9848;
    std::cout << 9848;
    for (int i = 0; i < 9848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9849(int& val) {
    func_in_9849(val);
    const int x = 9849;
    std::cout << 9849;
    for (int i = 0; i < 9849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9849(int& val) {
    const int x = 9849;
    std::cout << 9849;
    for (int i = 0; i < 9849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9850(int& val) {
    func_in_9850(val);
    const int x = 9850;
    std::cout << 9850;
    for (int i = 0; i < 9850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9850(int& val) {
    const int x = 9850;
    std::cout << 9850;
    for (int i = 0; i < 9850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9851(int& val) {
    func_in_9851(val);
    const int x = 9851;
    std::cout << 9851;
    for (int i = 0; i < 9851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9851(int& val) {
    const int x = 9851;
    std::cout << 9851;
    for (int i = 0; i < 9851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9852(int& val) {
    func_in_9852(val);
    const int x = 9852;
    std::cout << 9852;
    for (int i = 0; i < 9852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9852(int& val) {
    const int x = 9852;
    std::cout << 9852;
    for (int i = 0; i < 9852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9853(int& val) {
    func_in_9853(val);
    const int x = 9853;
    std::cout << 9853;
    for (int i = 0; i < 9853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9853(int& val) {
    const int x = 9853;
    std::cout << 9853;
    for (int i = 0; i < 9853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9854(int& val) {
    func_in_9854(val);
    const int x = 9854;
    std::cout << 9854;
    for (int i = 0; i < 9854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9854(int& val) {
    const int x = 9854;
    std::cout << 9854;
    for (int i = 0; i < 9854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9855(int& val) {
    func_in_9855(val);
    const int x = 9855;
    std::cout << 9855;
    for (int i = 0; i < 9855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9855(int& val) {
    const int x = 9855;
    std::cout << 9855;
    for (int i = 0; i < 9855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9856(int& val) {
    func_in_9856(val);
    const int x = 9856;
    std::cout << 9856;
    for (int i = 0; i < 9856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9856(int& val) {
    const int x = 9856;
    std::cout << 9856;
    for (int i = 0; i < 9856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9857(int& val) {
    func_in_9857(val);
    const int x = 9857;
    std::cout << 9857;
    for (int i = 0; i < 9857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9857(int& val) {
    const int x = 9857;
    std::cout << 9857;
    for (int i = 0; i < 9857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9858(int& val) {
    func_in_9858(val);
    const int x = 9858;
    std::cout << 9858;
    for (int i = 0; i < 9858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9858(int& val) {
    const int x = 9858;
    std::cout << 9858;
    for (int i = 0; i < 9858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9859(int& val) {
    func_in_9859(val);
    const int x = 9859;
    std::cout << 9859;
    for (int i = 0; i < 9859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9859(int& val) {
    const int x = 9859;
    std::cout << 9859;
    for (int i = 0; i < 9859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9860(int& val) {
    func_in_9860(val);
    const int x = 9860;
    std::cout << 9860;
    for (int i = 0; i < 9860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9860(int& val) {
    const int x = 9860;
    std::cout << 9860;
    for (int i = 0; i < 9860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9861(int& val) {
    func_in_9861(val);
    const int x = 9861;
    std::cout << 9861;
    for (int i = 0; i < 9861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9861(int& val) {
    const int x = 9861;
    std::cout << 9861;
    for (int i = 0; i < 9861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9862(int& val) {
    func_in_9862(val);
    const int x = 9862;
    std::cout << 9862;
    for (int i = 0; i < 9862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9862(int& val) {
    const int x = 9862;
    std::cout << 9862;
    for (int i = 0; i < 9862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9863(int& val) {
    func_in_9863(val);
    const int x = 9863;
    std::cout << 9863;
    for (int i = 0; i < 9863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9863(int& val) {
    const int x = 9863;
    std::cout << 9863;
    for (int i = 0; i < 9863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9864(int& val) {
    func_in_9864(val);
    const int x = 9864;
    std::cout << 9864;
    for (int i = 0; i < 9864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9864(int& val) {
    const int x = 9864;
    std::cout << 9864;
    for (int i = 0; i < 9864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9865(int& val) {
    func_in_9865(val);
    const int x = 9865;
    std::cout << 9865;
    for (int i = 0; i < 9865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9865(int& val) {
    const int x = 9865;
    std::cout << 9865;
    for (int i = 0; i < 9865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9866(int& val) {
    func_in_9866(val);
    const int x = 9866;
    std::cout << 9866;
    for (int i = 0; i < 9866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9866(int& val) {
    const int x = 9866;
    std::cout << 9866;
    for (int i = 0; i < 9866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9867(int& val) {
    func_in_9867(val);
    const int x = 9867;
    std::cout << 9867;
    for (int i = 0; i < 9867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9867(int& val) {
    const int x = 9867;
    std::cout << 9867;
    for (int i = 0; i < 9867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9868(int& val) {
    func_in_9868(val);
    const int x = 9868;
    std::cout << 9868;
    for (int i = 0; i < 9868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9868(int& val) {
    const int x = 9868;
    std::cout << 9868;
    for (int i = 0; i < 9868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9869(int& val) {
    func_in_9869(val);
    const int x = 9869;
    std::cout << 9869;
    for (int i = 0; i < 9869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9869(int& val) {
    const int x = 9869;
    std::cout << 9869;
    for (int i = 0; i < 9869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9870(int& val) {
    func_in_9870(val);
    const int x = 9870;
    std::cout << 9870;
    for (int i = 0; i < 9870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9870(int& val) {
    const int x = 9870;
    std::cout << 9870;
    for (int i = 0; i < 9870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9871(int& val) {
    func_in_9871(val);
    const int x = 9871;
    std::cout << 9871;
    for (int i = 0; i < 9871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9871(int& val) {
    const int x = 9871;
    std::cout << 9871;
    for (int i = 0; i < 9871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9872(int& val) {
    func_in_9872(val);
    const int x = 9872;
    std::cout << 9872;
    for (int i = 0; i < 9872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9872(int& val) {
    const int x = 9872;
    std::cout << 9872;
    for (int i = 0; i < 9872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9873(int& val) {
    func_in_9873(val);
    const int x = 9873;
    std::cout << 9873;
    for (int i = 0; i < 9873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9873(int& val) {
    const int x = 9873;
    std::cout << 9873;
    for (int i = 0; i < 9873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9874(int& val) {
    func_in_9874(val);
    const int x = 9874;
    std::cout << 9874;
    for (int i = 0; i < 9874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9874(int& val) {
    const int x = 9874;
    std::cout << 9874;
    for (int i = 0; i < 9874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9875(int& val) {
    func_in_9875(val);
    const int x = 9875;
    std::cout << 9875;
    for (int i = 0; i < 9875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9875(int& val) {
    const int x = 9875;
    std::cout << 9875;
    for (int i = 0; i < 9875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9876(int& val) {
    func_in_9876(val);
    const int x = 9876;
    std::cout << 9876;
    for (int i = 0; i < 9876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9876(int& val) {
    const int x = 9876;
    std::cout << 9876;
    for (int i = 0; i < 9876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9877(int& val) {
    func_in_9877(val);
    const int x = 9877;
    std::cout << 9877;
    for (int i = 0; i < 9877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9877(int& val) {
    const int x = 9877;
    std::cout << 9877;
    for (int i = 0; i < 9877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9878(int& val) {
    func_in_9878(val);
    const int x = 9878;
    std::cout << 9878;
    for (int i = 0; i < 9878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9878(int& val) {
    const int x = 9878;
    std::cout << 9878;
    for (int i = 0; i < 9878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9879(int& val) {
    func_in_9879(val);
    const int x = 9879;
    std::cout << 9879;
    for (int i = 0; i < 9879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9879(int& val) {
    const int x = 9879;
    std::cout << 9879;
    for (int i = 0; i < 9879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9880(int& val) {
    func_in_9880(val);
    const int x = 9880;
    std::cout << 9880;
    for (int i = 0; i < 9880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9880(int& val) {
    const int x = 9880;
    std::cout << 9880;
    for (int i = 0; i < 9880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9881(int& val) {
    func_in_9881(val);
    const int x = 9881;
    std::cout << 9881;
    for (int i = 0; i < 9881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9881(int& val) {
    const int x = 9881;
    std::cout << 9881;
    for (int i = 0; i < 9881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9882(int& val) {
    func_in_9882(val);
    const int x = 9882;
    std::cout << 9882;
    for (int i = 0; i < 9882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9882(int& val) {
    const int x = 9882;
    std::cout << 9882;
    for (int i = 0; i < 9882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9883(int& val) {
    func_in_9883(val);
    const int x = 9883;
    std::cout << 9883;
    for (int i = 0; i < 9883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9883(int& val) {
    const int x = 9883;
    std::cout << 9883;
    for (int i = 0; i < 9883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9884(int& val) {
    func_in_9884(val);
    const int x = 9884;
    std::cout << 9884;
    for (int i = 0; i < 9884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9884(int& val) {
    const int x = 9884;
    std::cout << 9884;
    for (int i = 0; i < 9884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9885(int& val) {
    func_in_9885(val);
    const int x = 9885;
    std::cout << 9885;
    for (int i = 0; i < 9885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9885(int& val) {
    const int x = 9885;
    std::cout << 9885;
    for (int i = 0; i < 9885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9886(int& val) {
    func_in_9886(val);
    const int x = 9886;
    std::cout << 9886;
    for (int i = 0; i < 9886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9886(int& val) {
    const int x = 9886;
    std::cout << 9886;
    for (int i = 0; i < 9886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9887(int& val) {
    func_in_9887(val);
    const int x = 9887;
    std::cout << 9887;
    for (int i = 0; i < 9887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9887(int& val) {
    const int x = 9887;
    std::cout << 9887;
    for (int i = 0; i < 9887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9888(int& val) {
    func_in_9888(val);
    const int x = 9888;
    std::cout << 9888;
    for (int i = 0; i < 9888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9888(int& val) {
    const int x = 9888;
    std::cout << 9888;
    for (int i = 0; i < 9888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9889(int& val) {
    func_in_9889(val);
    const int x = 9889;
    std::cout << 9889;
    for (int i = 0; i < 9889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9889(int& val) {
    const int x = 9889;
    std::cout << 9889;
    for (int i = 0; i < 9889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9890(int& val) {
    func_in_9890(val);
    const int x = 9890;
    std::cout << 9890;
    for (int i = 0; i < 9890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9890(int& val) {
    const int x = 9890;
    std::cout << 9890;
    for (int i = 0; i < 9890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9891(int& val) {
    func_in_9891(val);
    const int x = 9891;
    std::cout << 9891;
    for (int i = 0; i < 9891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9891(int& val) {
    const int x = 9891;
    std::cout << 9891;
    for (int i = 0; i < 9891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9892(int& val) {
    func_in_9892(val);
    const int x = 9892;
    std::cout << 9892;
    for (int i = 0; i < 9892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9892(int& val) {
    const int x = 9892;
    std::cout << 9892;
    for (int i = 0; i < 9892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9893(int& val) {
    func_in_9893(val);
    const int x = 9893;
    std::cout << 9893;
    for (int i = 0; i < 9893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9893(int& val) {
    const int x = 9893;
    std::cout << 9893;
    for (int i = 0; i < 9893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9894(int& val) {
    func_in_9894(val);
    const int x = 9894;
    std::cout << 9894;
    for (int i = 0; i < 9894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9894(int& val) {
    const int x = 9894;
    std::cout << 9894;
    for (int i = 0; i < 9894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9895(int& val) {
    func_in_9895(val);
    const int x = 9895;
    std::cout << 9895;
    for (int i = 0; i < 9895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9895(int& val) {
    const int x = 9895;
    std::cout << 9895;
    for (int i = 0; i < 9895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9896(int& val) {
    func_in_9896(val);
    const int x = 9896;
    std::cout << 9896;
    for (int i = 0; i < 9896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9896(int& val) {
    const int x = 9896;
    std::cout << 9896;
    for (int i = 0; i < 9896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9897(int& val) {
    func_in_9897(val);
    const int x = 9897;
    std::cout << 9897;
    for (int i = 0; i < 9897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9897(int& val) {
    const int x = 9897;
    std::cout << 9897;
    for (int i = 0; i < 9897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9898(int& val) {
    func_in_9898(val);
    const int x = 9898;
    std::cout << 9898;
    for (int i = 0; i < 9898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9898(int& val) {
    const int x = 9898;
    std::cout << 9898;
    for (int i = 0; i < 9898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9899(int& val) {
    func_in_9899(val);
    const int x = 9899;
    std::cout << 9899;
    for (int i = 0; i < 9899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9899(int& val) {
    const int x = 9899;
    std::cout << 9899;
    for (int i = 0; i < 9899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9900(int& val) {
    func_in_9900(val);
    const int x = 9900;
    std::cout << 9900;
    for (int i = 0; i < 9900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9900(int& val) {
    const int x = 9900;
    std::cout << 9900;
    for (int i = 0; i < 9900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9901(int& val) {
    func_in_9901(val);
    const int x = 9901;
    std::cout << 9901;
    for (int i = 0; i < 9901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9901(int& val) {
    const int x = 9901;
    std::cout << 9901;
    for (int i = 0; i < 9901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9902(int& val) {
    func_in_9902(val);
    const int x = 9902;
    std::cout << 9902;
    for (int i = 0; i < 9902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9902(int& val) {
    const int x = 9902;
    std::cout << 9902;
    for (int i = 0; i < 9902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9903(int& val) {
    func_in_9903(val);
    const int x = 9903;
    std::cout << 9903;
    for (int i = 0; i < 9903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9903(int& val) {
    const int x = 9903;
    std::cout << 9903;
    for (int i = 0; i < 9903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9904(int& val) {
    func_in_9904(val);
    const int x = 9904;
    std::cout << 9904;
    for (int i = 0; i < 9904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9904(int& val) {
    const int x = 9904;
    std::cout << 9904;
    for (int i = 0; i < 9904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9905(int& val) {
    func_in_9905(val);
    const int x = 9905;
    std::cout << 9905;
    for (int i = 0; i < 9905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9905(int& val) {
    const int x = 9905;
    std::cout << 9905;
    for (int i = 0; i < 9905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9906(int& val) {
    func_in_9906(val);
    const int x = 9906;
    std::cout << 9906;
    for (int i = 0; i < 9906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9906(int& val) {
    const int x = 9906;
    std::cout << 9906;
    for (int i = 0; i < 9906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9907(int& val) {
    func_in_9907(val);
    const int x = 9907;
    std::cout << 9907;
    for (int i = 0; i < 9907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9907(int& val) {
    const int x = 9907;
    std::cout << 9907;
    for (int i = 0; i < 9907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9908(int& val) {
    func_in_9908(val);
    const int x = 9908;
    std::cout << 9908;
    for (int i = 0; i < 9908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9908(int& val) {
    const int x = 9908;
    std::cout << 9908;
    for (int i = 0; i < 9908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9909(int& val) {
    func_in_9909(val);
    const int x = 9909;
    std::cout << 9909;
    for (int i = 0; i < 9909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9909(int& val) {
    const int x = 9909;
    std::cout << 9909;
    for (int i = 0; i < 9909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9910(int& val) {
    func_in_9910(val);
    const int x = 9910;
    std::cout << 9910;
    for (int i = 0; i < 9910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9910(int& val) {
    const int x = 9910;
    std::cout << 9910;
    for (int i = 0; i < 9910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9911(int& val) {
    func_in_9911(val);
    const int x = 9911;
    std::cout << 9911;
    for (int i = 0; i < 9911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9911(int& val) {
    const int x = 9911;
    std::cout << 9911;
    for (int i = 0; i < 9911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9912(int& val) {
    func_in_9912(val);
    const int x = 9912;
    std::cout << 9912;
    for (int i = 0; i < 9912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9912(int& val) {
    const int x = 9912;
    std::cout << 9912;
    for (int i = 0; i < 9912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9913(int& val) {
    func_in_9913(val);
    const int x = 9913;
    std::cout << 9913;
    for (int i = 0; i < 9913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9913(int& val) {
    const int x = 9913;
    std::cout << 9913;
    for (int i = 0; i < 9913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9914(int& val) {
    func_in_9914(val);
    const int x = 9914;
    std::cout << 9914;
    for (int i = 0; i < 9914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9914(int& val) {
    const int x = 9914;
    std::cout << 9914;
    for (int i = 0; i < 9914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9915(int& val) {
    func_in_9915(val);
    const int x = 9915;
    std::cout << 9915;
    for (int i = 0; i < 9915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9915(int& val) {
    const int x = 9915;
    std::cout << 9915;
    for (int i = 0; i < 9915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9916(int& val) {
    func_in_9916(val);
    const int x = 9916;
    std::cout << 9916;
    for (int i = 0; i < 9916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9916(int& val) {
    const int x = 9916;
    std::cout << 9916;
    for (int i = 0; i < 9916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9917(int& val) {
    func_in_9917(val);
    const int x = 9917;
    std::cout << 9917;
    for (int i = 0; i < 9917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9917(int& val) {
    const int x = 9917;
    std::cout << 9917;
    for (int i = 0; i < 9917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9918(int& val) {
    func_in_9918(val);
    const int x = 9918;
    std::cout << 9918;
    for (int i = 0; i < 9918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9918(int& val) {
    const int x = 9918;
    std::cout << 9918;
    for (int i = 0; i < 9918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9919(int& val) {
    func_in_9919(val);
    const int x = 9919;
    std::cout << 9919;
    for (int i = 0; i < 9919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9919(int& val) {
    const int x = 9919;
    std::cout << 9919;
    for (int i = 0; i < 9919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9920(int& val) {
    func_in_9920(val);
    const int x = 9920;
    std::cout << 9920;
    for (int i = 0; i < 9920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9920(int& val) {
    const int x = 9920;
    std::cout << 9920;
    for (int i = 0; i < 9920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9921(int& val) {
    func_in_9921(val);
    const int x = 9921;
    std::cout << 9921;
    for (int i = 0; i < 9921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9921(int& val) {
    const int x = 9921;
    std::cout << 9921;
    for (int i = 0; i < 9921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9922(int& val) {
    func_in_9922(val);
    const int x = 9922;
    std::cout << 9922;
    for (int i = 0; i < 9922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9922(int& val) {
    const int x = 9922;
    std::cout << 9922;
    for (int i = 0; i < 9922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9923(int& val) {
    func_in_9923(val);
    const int x = 9923;
    std::cout << 9923;
    for (int i = 0; i < 9923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9923(int& val) {
    const int x = 9923;
    std::cout << 9923;
    for (int i = 0; i < 9923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9924(int& val) {
    func_in_9924(val);
    const int x = 9924;
    std::cout << 9924;
    for (int i = 0; i < 9924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9924(int& val) {
    const int x = 9924;
    std::cout << 9924;
    for (int i = 0; i < 9924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9925(int& val) {
    func_in_9925(val);
    const int x = 9925;
    std::cout << 9925;
    for (int i = 0; i < 9925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9925(int& val) {
    const int x = 9925;
    std::cout << 9925;
    for (int i = 0; i < 9925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9926(int& val) {
    func_in_9926(val);
    const int x = 9926;
    std::cout << 9926;
    for (int i = 0; i < 9926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9926(int& val) {
    const int x = 9926;
    std::cout << 9926;
    for (int i = 0; i < 9926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9927(int& val) {
    func_in_9927(val);
    const int x = 9927;
    std::cout << 9927;
    for (int i = 0; i < 9927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9927(int& val) {
    const int x = 9927;
    std::cout << 9927;
    for (int i = 0; i < 9927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9928(int& val) {
    func_in_9928(val);
    const int x = 9928;
    std::cout << 9928;
    for (int i = 0; i < 9928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9928(int& val) {
    const int x = 9928;
    std::cout << 9928;
    for (int i = 0; i < 9928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9929(int& val) {
    func_in_9929(val);
    const int x = 9929;
    std::cout << 9929;
    for (int i = 0; i < 9929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9929(int& val) {
    const int x = 9929;
    std::cout << 9929;
    for (int i = 0; i < 9929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9930(int& val) {
    func_in_9930(val);
    const int x = 9930;
    std::cout << 9930;
    for (int i = 0; i < 9930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9930(int& val) {
    const int x = 9930;
    std::cout << 9930;
    for (int i = 0; i < 9930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9931(int& val) {
    func_in_9931(val);
    const int x = 9931;
    std::cout << 9931;
    for (int i = 0; i < 9931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9931(int& val) {
    const int x = 9931;
    std::cout << 9931;
    for (int i = 0; i < 9931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9932(int& val) {
    func_in_9932(val);
    const int x = 9932;
    std::cout << 9932;
    for (int i = 0; i < 9932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9932(int& val) {
    const int x = 9932;
    std::cout << 9932;
    for (int i = 0; i < 9932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9933(int& val) {
    func_in_9933(val);
    const int x = 9933;
    std::cout << 9933;
    for (int i = 0; i < 9933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9933(int& val) {
    const int x = 9933;
    std::cout << 9933;
    for (int i = 0; i < 9933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9934(int& val) {
    func_in_9934(val);
    const int x = 9934;
    std::cout << 9934;
    for (int i = 0; i < 9934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9934(int& val) {
    const int x = 9934;
    std::cout << 9934;
    for (int i = 0; i < 9934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9935(int& val) {
    func_in_9935(val);
    const int x = 9935;
    std::cout << 9935;
    for (int i = 0; i < 9935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9935(int& val) {
    const int x = 9935;
    std::cout << 9935;
    for (int i = 0; i < 9935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9936(int& val) {
    func_in_9936(val);
    const int x = 9936;
    std::cout << 9936;
    for (int i = 0; i < 9936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9936(int& val) {
    const int x = 9936;
    std::cout << 9936;
    for (int i = 0; i < 9936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9937(int& val) {
    func_in_9937(val);
    const int x = 9937;
    std::cout << 9937;
    for (int i = 0; i < 9937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9937(int& val) {
    const int x = 9937;
    std::cout << 9937;
    for (int i = 0; i < 9937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9938(int& val) {
    func_in_9938(val);
    const int x = 9938;
    std::cout << 9938;
    for (int i = 0; i < 9938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9938(int& val) {
    const int x = 9938;
    std::cout << 9938;
    for (int i = 0; i < 9938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9939(int& val) {
    func_in_9939(val);
    const int x = 9939;
    std::cout << 9939;
    for (int i = 0; i < 9939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9939(int& val) {
    const int x = 9939;
    std::cout << 9939;
    for (int i = 0; i < 9939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9940(int& val) {
    func_in_9940(val);
    const int x = 9940;
    std::cout << 9940;
    for (int i = 0; i < 9940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9940(int& val) {
    const int x = 9940;
    std::cout << 9940;
    for (int i = 0; i < 9940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9941(int& val) {
    func_in_9941(val);
    const int x = 9941;
    std::cout << 9941;
    for (int i = 0; i < 9941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9941(int& val) {
    const int x = 9941;
    std::cout << 9941;
    for (int i = 0; i < 9941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9942(int& val) {
    func_in_9942(val);
    const int x = 9942;
    std::cout << 9942;
    for (int i = 0; i < 9942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9942(int& val) {
    const int x = 9942;
    std::cout << 9942;
    for (int i = 0; i < 9942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9943(int& val) {
    func_in_9943(val);
    const int x = 9943;
    std::cout << 9943;
    for (int i = 0; i < 9943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9943(int& val) {
    const int x = 9943;
    std::cout << 9943;
    for (int i = 0; i < 9943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9944(int& val) {
    func_in_9944(val);
    const int x = 9944;
    std::cout << 9944;
    for (int i = 0; i < 9944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9944(int& val) {
    const int x = 9944;
    std::cout << 9944;
    for (int i = 0; i < 9944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9945(int& val) {
    func_in_9945(val);
    const int x = 9945;
    std::cout << 9945;
    for (int i = 0; i < 9945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9945(int& val) {
    const int x = 9945;
    std::cout << 9945;
    for (int i = 0; i < 9945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9946(int& val) {
    func_in_9946(val);
    const int x = 9946;
    std::cout << 9946;
    for (int i = 0; i < 9946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9946(int& val) {
    const int x = 9946;
    std::cout << 9946;
    for (int i = 0; i < 9946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9947(int& val) {
    func_in_9947(val);
    const int x = 9947;
    std::cout << 9947;
    for (int i = 0; i < 9947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9947(int& val) {
    const int x = 9947;
    std::cout << 9947;
    for (int i = 0; i < 9947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9948(int& val) {
    func_in_9948(val);
    const int x = 9948;
    std::cout << 9948;
    for (int i = 0; i < 9948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9948(int& val) {
    const int x = 9948;
    std::cout << 9948;
    for (int i = 0; i < 9948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9949(int& val) {
    func_in_9949(val);
    const int x = 9949;
    std::cout << 9949;
    for (int i = 0; i < 9949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9949(int& val) {
    const int x = 9949;
    std::cout << 9949;
    for (int i = 0; i < 9949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9950(int& val) {
    func_in_9950(val);
    const int x = 9950;
    std::cout << 9950;
    for (int i = 0; i < 9950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9950(int& val) {
    const int x = 9950;
    std::cout << 9950;
    for (int i = 0; i < 9950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9951(int& val) {
    func_in_9951(val);
    const int x = 9951;
    std::cout << 9951;
    for (int i = 0; i < 9951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9951(int& val) {
    const int x = 9951;
    std::cout << 9951;
    for (int i = 0; i < 9951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9952(int& val) {
    func_in_9952(val);
    const int x = 9952;
    std::cout << 9952;
    for (int i = 0; i < 9952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9952(int& val) {
    const int x = 9952;
    std::cout << 9952;
    for (int i = 0; i < 9952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9953(int& val) {
    func_in_9953(val);
    const int x = 9953;
    std::cout << 9953;
    for (int i = 0; i < 9953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9953(int& val) {
    const int x = 9953;
    std::cout << 9953;
    for (int i = 0; i < 9953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9954(int& val) {
    func_in_9954(val);
    const int x = 9954;
    std::cout << 9954;
    for (int i = 0; i < 9954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9954(int& val) {
    const int x = 9954;
    std::cout << 9954;
    for (int i = 0; i < 9954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9955(int& val) {
    func_in_9955(val);
    const int x = 9955;
    std::cout << 9955;
    for (int i = 0; i < 9955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9955(int& val) {
    const int x = 9955;
    std::cout << 9955;
    for (int i = 0; i < 9955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9956(int& val) {
    func_in_9956(val);
    const int x = 9956;
    std::cout << 9956;
    for (int i = 0; i < 9956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9956(int& val) {
    const int x = 9956;
    std::cout << 9956;
    for (int i = 0; i < 9956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9957(int& val) {
    func_in_9957(val);
    const int x = 9957;
    std::cout << 9957;
    for (int i = 0; i < 9957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9957(int& val) {
    const int x = 9957;
    std::cout << 9957;
    for (int i = 0; i < 9957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9958(int& val) {
    func_in_9958(val);
    const int x = 9958;
    std::cout << 9958;
    for (int i = 0; i < 9958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9958(int& val) {
    const int x = 9958;
    std::cout << 9958;
    for (int i = 0; i < 9958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9959(int& val) {
    func_in_9959(val);
    const int x = 9959;
    std::cout << 9959;
    for (int i = 0; i < 9959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9959(int& val) {
    const int x = 9959;
    std::cout << 9959;
    for (int i = 0; i < 9959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9960(int& val) {
    func_in_9960(val);
    const int x = 9960;
    std::cout << 9960;
    for (int i = 0; i < 9960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9960(int& val) {
    const int x = 9960;
    std::cout << 9960;
    for (int i = 0; i < 9960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9961(int& val) {
    func_in_9961(val);
    const int x = 9961;
    std::cout << 9961;
    for (int i = 0; i < 9961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9961(int& val) {
    const int x = 9961;
    std::cout << 9961;
    for (int i = 0; i < 9961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9962(int& val) {
    func_in_9962(val);
    const int x = 9962;
    std::cout << 9962;
    for (int i = 0; i < 9962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9962(int& val) {
    const int x = 9962;
    std::cout << 9962;
    for (int i = 0; i < 9962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9963(int& val) {
    func_in_9963(val);
    const int x = 9963;
    std::cout << 9963;
    for (int i = 0; i < 9963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9963(int& val) {
    const int x = 9963;
    std::cout << 9963;
    for (int i = 0; i < 9963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9964(int& val) {
    func_in_9964(val);
    const int x = 9964;
    std::cout << 9964;
    for (int i = 0; i < 9964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9964(int& val) {
    const int x = 9964;
    std::cout << 9964;
    for (int i = 0; i < 9964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9965(int& val) {
    func_in_9965(val);
    const int x = 9965;
    std::cout << 9965;
    for (int i = 0; i < 9965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9965(int& val) {
    const int x = 9965;
    std::cout << 9965;
    for (int i = 0; i < 9965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9966(int& val) {
    func_in_9966(val);
    const int x = 9966;
    std::cout << 9966;
    for (int i = 0; i < 9966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9966(int& val) {
    const int x = 9966;
    std::cout << 9966;
    for (int i = 0; i < 9966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9967(int& val) {
    func_in_9967(val);
    const int x = 9967;
    std::cout << 9967;
    for (int i = 0; i < 9967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9967(int& val) {
    const int x = 9967;
    std::cout << 9967;
    for (int i = 0; i < 9967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9968(int& val) {
    func_in_9968(val);
    const int x = 9968;
    std::cout << 9968;
    for (int i = 0; i < 9968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9968(int& val) {
    const int x = 9968;
    std::cout << 9968;
    for (int i = 0; i < 9968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9969(int& val) {
    func_in_9969(val);
    const int x = 9969;
    std::cout << 9969;
    for (int i = 0; i < 9969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9969(int& val) {
    const int x = 9969;
    std::cout << 9969;
    for (int i = 0; i < 9969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9970(int& val) {
    func_in_9970(val);
    const int x = 9970;
    std::cout << 9970;
    for (int i = 0; i < 9970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9970(int& val) {
    const int x = 9970;
    std::cout << 9970;
    for (int i = 0; i < 9970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9971(int& val) {
    func_in_9971(val);
    const int x = 9971;
    std::cout << 9971;
    for (int i = 0; i < 9971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9971(int& val) {
    const int x = 9971;
    std::cout << 9971;
    for (int i = 0; i < 9971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9972(int& val) {
    func_in_9972(val);
    const int x = 9972;
    std::cout << 9972;
    for (int i = 0; i < 9972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9972(int& val) {
    const int x = 9972;
    std::cout << 9972;
    for (int i = 0; i < 9972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9973(int& val) {
    func_in_9973(val);
    const int x = 9973;
    std::cout << 9973;
    for (int i = 0; i < 9973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9973(int& val) {
    const int x = 9973;
    std::cout << 9973;
    for (int i = 0; i < 9973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9974(int& val) {
    func_in_9974(val);
    const int x = 9974;
    std::cout << 9974;
    for (int i = 0; i < 9974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9974(int& val) {
    const int x = 9974;
    std::cout << 9974;
    for (int i = 0; i < 9974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9975(int& val) {
    func_in_9975(val);
    const int x = 9975;
    std::cout << 9975;
    for (int i = 0; i < 9975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9975(int& val) {
    const int x = 9975;
    std::cout << 9975;
    for (int i = 0; i < 9975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9976(int& val) {
    func_in_9976(val);
    const int x = 9976;
    std::cout << 9976;
    for (int i = 0; i < 9976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9976(int& val) {
    const int x = 9976;
    std::cout << 9976;
    for (int i = 0; i < 9976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9977(int& val) {
    func_in_9977(val);
    const int x = 9977;
    std::cout << 9977;
    for (int i = 0; i < 9977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9977(int& val) {
    const int x = 9977;
    std::cout << 9977;
    for (int i = 0; i < 9977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9978(int& val) {
    func_in_9978(val);
    const int x = 9978;
    std::cout << 9978;
    for (int i = 0; i < 9978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9978(int& val) {
    const int x = 9978;
    std::cout << 9978;
    for (int i = 0; i < 9978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9979(int& val) {
    func_in_9979(val);
    const int x = 9979;
    std::cout << 9979;
    for (int i = 0; i < 9979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9979(int& val) {
    const int x = 9979;
    std::cout << 9979;
    for (int i = 0; i < 9979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9980(int& val) {
    func_in_9980(val);
    const int x = 9980;
    std::cout << 9980;
    for (int i = 0; i < 9980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9980(int& val) {
    const int x = 9980;
    std::cout << 9980;
    for (int i = 0; i < 9980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9981(int& val) {
    func_in_9981(val);
    const int x = 9981;
    std::cout << 9981;
    for (int i = 0; i < 9981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9981(int& val) {
    const int x = 9981;
    std::cout << 9981;
    for (int i = 0; i < 9981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9982(int& val) {
    func_in_9982(val);
    const int x = 9982;
    std::cout << 9982;
    for (int i = 0; i < 9982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9982(int& val) {
    const int x = 9982;
    std::cout << 9982;
    for (int i = 0; i < 9982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9983(int& val) {
    func_in_9983(val);
    const int x = 9983;
    std::cout << 9983;
    for (int i = 0; i < 9983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9983(int& val) {
    const int x = 9983;
    std::cout << 9983;
    for (int i = 0; i < 9983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9984(int& val) {
    func_in_9984(val);
    const int x = 9984;
    std::cout << 9984;
    for (int i = 0; i < 9984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9984(int& val) {
    const int x = 9984;
    std::cout << 9984;
    for (int i = 0; i < 9984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9985(int& val) {
    func_in_9985(val);
    const int x = 9985;
    std::cout << 9985;
    for (int i = 0; i < 9985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9985(int& val) {
    const int x = 9985;
    std::cout << 9985;
    for (int i = 0; i < 9985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9986(int& val) {
    func_in_9986(val);
    const int x = 9986;
    std::cout << 9986;
    for (int i = 0; i < 9986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9986(int& val) {
    const int x = 9986;
    std::cout << 9986;
    for (int i = 0; i < 9986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9987(int& val) {
    func_in_9987(val);
    const int x = 9987;
    std::cout << 9987;
    for (int i = 0; i < 9987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9987(int& val) {
    const int x = 9987;
    std::cout << 9987;
    for (int i = 0; i < 9987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9988(int& val) {
    func_in_9988(val);
    const int x = 9988;
    std::cout << 9988;
    for (int i = 0; i < 9988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9988(int& val) {
    const int x = 9988;
    std::cout << 9988;
    for (int i = 0; i < 9988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9989(int& val) {
    func_in_9989(val);
    const int x = 9989;
    std::cout << 9989;
    for (int i = 0; i < 9989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9989(int& val) {
    const int x = 9989;
    std::cout << 9989;
    for (int i = 0; i < 9989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9990(int& val) {
    func_in_9990(val);
    const int x = 9990;
    std::cout << 9990;
    for (int i = 0; i < 9990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9990(int& val) {
    const int x = 9990;
    std::cout << 9990;
    for (int i = 0; i < 9990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9991(int& val) {
    func_in_9991(val);
    const int x = 9991;
    std::cout << 9991;
    for (int i = 0; i < 9991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9991(int& val) {
    const int x = 9991;
    std::cout << 9991;
    for (int i = 0; i < 9991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9992(int& val) {
    func_in_9992(val);
    const int x = 9992;
    std::cout << 9992;
    for (int i = 0; i < 9992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9992(int& val) {
    const int x = 9992;
    std::cout << 9992;
    for (int i = 0; i < 9992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9993(int& val) {
    func_in_9993(val);
    const int x = 9993;
    std::cout << 9993;
    for (int i = 0; i < 9993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9993(int& val) {
    const int x = 9993;
    std::cout << 9993;
    for (int i = 0; i < 9993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9994(int& val) {
    func_in_9994(val);
    const int x = 9994;
    std::cout << 9994;
    for (int i = 0; i < 9994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9994(int& val) {
    const int x = 9994;
    std::cout << 9994;
    for (int i = 0; i < 9994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9995(int& val) {
    func_in_9995(val);
    const int x = 9995;
    std::cout << 9995;
    for (int i = 0; i < 9995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9995(int& val) {
    const int x = 9995;
    std::cout << 9995;
    for (int i = 0; i < 9995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9996(int& val) {
    func_in_9996(val);
    const int x = 9996;
    std::cout << 9996;
    for (int i = 0; i < 9996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9996(int& val) {
    const int x = 9996;
    std::cout << 9996;
    for (int i = 0; i < 9996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9997(int& val) {
    func_in_9997(val);
    const int x = 9997;
    std::cout << 9997;
    for (int i = 0; i < 9997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9997(int& val) {
    const int x = 9997;
    std::cout << 9997;
    for (int i = 0; i < 9997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9998(int& val) {
    func_in_9998(val);
    const int x = 9998;
    std::cout << 9998;
    for (int i = 0; i < 9998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9998(int& val) {
    const int x = 9998;
    std::cout << 9998;
    for (int i = 0; i < 9998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9999(int& val) {
    func_in_9999(val);
    const int x = 9999;
    std::cout << 9999;
    for (int i = 0; i < 9999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9999(int& val) {
    const int x = 9999;
    std::cout << 9999;
    for (int i = 0; i < 9999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10000(int& val) {
    func_in_10000(val);
    const int x = 10000;
    std::cout << 10000;
    for (int i = 0; i < 10000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10000(int& val) {
    const int x = 10000;
    std::cout << 10000;
    for (int i = 0; i < 10000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10001(int& val) {
    func_in_10001(val);
    const int x = 10001;
    std::cout << 10001;
    for (int i = 0; i < 10001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10001(int& val) {
    const int x = 10001;
    std::cout << 10001;
    for (int i = 0; i < 10001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10002(int& val) {
    func_in_10002(val);
    const int x = 10002;
    std::cout << 10002;
    for (int i = 0; i < 10002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10002(int& val) {
    const int x = 10002;
    std::cout << 10002;
    for (int i = 0; i < 10002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10003(int& val) {
    func_in_10003(val);
    const int x = 10003;
    std::cout << 10003;
    for (int i = 0; i < 10003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10003(int& val) {
    const int x = 10003;
    std::cout << 10003;
    for (int i = 0; i < 10003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10004(int& val) {
    func_in_10004(val);
    const int x = 10004;
    std::cout << 10004;
    for (int i = 0; i < 10004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10004(int& val) {
    const int x = 10004;
    std::cout << 10004;
    for (int i = 0; i < 10004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10005(int& val) {
    func_in_10005(val);
    const int x = 10005;
    std::cout << 10005;
    for (int i = 0; i < 10005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10005(int& val) {
    const int x = 10005;
    std::cout << 10005;
    for (int i = 0; i < 10005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10006(int& val) {
    func_in_10006(val);
    const int x = 10006;
    std::cout << 10006;
    for (int i = 0; i < 10006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10006(int& val) {
    const int x = 10006;
    std::cout << 10006;
    for (int i = 0; i < 10006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10007(int& val) {
    func_in_10007(val);
    const int x = 10007;
    std::cout << 10007;
    for (int i = 0; i < 10007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10007(int& val) {
    const int x = 10007;
    std::cout << 10007;
    for (int i = 0; i < 10007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10008(int& val) {
    func_in_10008(val);
    const int x = 10008;
    std::cout << 10008;
    for (int i = 0; i < 10008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10008(int& val) {
    const int x = 10008;
    std::cout << 10008;
    for (int i = 0; i < 10008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10009(int& val) {
    func_in_10009(val);
    const int x = 10009;
    std::cout << 10009;
    for (int i = 0; i < 10009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10009(int& val) {
    const int x = 10009;
    std::cout << 10009;
    for (int i = 0; i < 10009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10010(int& val) {
    func_in_10010(val);
    const int x = 10010;
    std::cout << 10010;
    for (int i = 0; i < 10010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10010(int& val) {
    const int x = 10010;
    std::cout << 10010;
    for (int i = 0; i < 10010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10011(int& val) {
    func_in_10011(val);
    const int x = 10011;
    std::cout << 10011;
    for (int i = 0; i < 10011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10011(int& val) {
    const int x = 10011;
    std::cout << 10011;
    for (int i = 0; i < 10011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10012(int& val) {
    func_in_10012(val);
    const int x = 10012;
    std::cout << 10012;
    for (int i = 0; i < 10012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10012(int& val) {
    const int x = 10012;
    std::cout << 10012;
    for (int i = 0; i < 10012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10013(int& val) {
    func_in_10013(val);
    const int x = 10013;
    std::cout << 10013;
    for (int i = 0; i < 10013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10013(int& val) {
    const int x = 10013;
    std::cout << 10013;
    for (int i = 0; i < 10013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10014(int& val) {
    func_in_10014(val);
    const int x = 10014;
    std::cout << 10014;
    for (int i = 0; i < 10014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10014(int& val) {
    const int x = 10014;
    std::cout << 10014;
    for (int i = 0; i < 10014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10015(int& val) {
    func_in_10015(val);
    const int x = 10015;
    std::cout << 10015;
    for (int i = 0; i < 10015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10015(int& val) {
    const int x = 10015;
    std::cout << 10015;
    for (int i = 0; i < 10015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10016(int& val) {
    func_in_10016(val);
    const int x = 10016;
    std::cout << 10016;
    for (int i = 0; i < 10016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10016(int& val) {
    const int x = 10016;
    std::cout << 10016;
    for (int i = 0; i < 10016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10017(int& val) {
    func_in_10017(val);
    const int x = 10017;
    std::cout << 10017;
    for (int i = 0; i < 10017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10017(int& val) {
    const int x = 10017;
    std::cout << 10017;
    for (int i = 0; i < 10017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10018(int& val) {
    func_in_10018(val);
    const int x = 10018;
    std::cout << 10018;
    for (int i = 0; i < 10018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10018(int& val) {
    const int x = 10018;
    std::cout << 10018;
    for (int i = 0; i < 10018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10019(int& val) {
    func_in_10019(val);
    const int x = 10019;
    std::cout << 10019;
    for (int i = 0; i < 10019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10019(int& val) {
    const int x = 10019;
    std::cout << 10019;
    for (int i = 0; i < 10019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10020(int& val) {
    func_in_10020(val);
    const int x = 10020;
    std::cout << 10020;
    for (int i = 0; i < 10020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10020(int& val) {
    const int x = 10020;
    std::cout << 10020;
    for (int i = 0; i < 10020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10021(int& val) {
    func_in_10021(val);
    const int x = 10021;
    std::cout << 10021;
    for (int i = 0; i < 10021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10021(int& val) {
    const int x = 10021;
    std::cout << 10021;
    for (int i = 0; i < 10021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10022(int& val) {
    func_in_10022(val);
    const int x = 10022;
    std::cout << 10022;
    for (int i = 0; i < 10022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10022(int& val) {
    const int x = 10022;
    std::cout << 10022;
    for (int i = 0; i < 10022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10023(int& val) {
    func_in_10023(val);
    const int x = 10023;
    std::cout << 10023;
    for (int i = 0; i < 10023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10023(int& val) {
    const int x = 10023;
    std::cout << 10023;
    for (int i = 0; i < 10023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10024(int& val) {
    func_in_10024(val);
    const int x = 10024;
    std::cout << 10024;
    for (int i = 0; i < 10024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10024(int& val) {
    const int x = 10024;
    std::cout << 10024;
    for (int i = 0; i < 10024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10025(int& val) {
    func_in_10025(val);
    const int x = 10025;
    std::cout << 10025;
    for (int i = 0; i < 10025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10025(int& val) {
    const int x = 10025;
    std::cout << 10025;
    for (int i = 0; i < 10025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10026(int& val) {
    func_in_10026(val);
    const int x = 10026;
    std::cout << 10026;
    for (int i = 0; i < 10026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10026(int& val) {
    const int x = 10026;
    std::cout << 10026;
    for (int i = 0; i < 10026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10027(int& val) {
    func_in_10027(val);
    const int x = 10027;
    std::cout << 10027;
    for (int i = 0; i < 10027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10027(int& val) {
    const int x = 10027;
    std::cout << 10027;
    for (int i = 0; i < 10027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10028(int& val) {
    func_in_10028(val);
    const int x = 10028;
    std::cout << 10028;
    for (int i = 0; i < 10028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10028(int& val) {
    const int x = 10028;
    std::cout << 10028;
    for (int i = 0; i < 10028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10029(int& val) {
    func_in_10029(val);
    const int x = 10029;
    std::cout << 10029;
    for (int i = 0; i < 10029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10029(int& val) {
    const int x = 10029;
    std::cout << 10029;
    for (int i = 0; i < 10029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10030(int& val) {
    func_in_10030(val);
    const int x = 10030;
    std::cout << 10030;
    for (int i = 0; i < 10030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10030(int& val) {
    const int x = 10030;
    std::cout << 10030;
    for (int i = 0; i < 10030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10031(int& val) {
    func_in_10031(val);
    const int x = 10031;
    std::cout << 10031;
    for (int i = 0; i < 10031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10031(int& val) {
    const int x = 10031;
    std::cout << 10031;
    for (int i = 0; i < 10031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10032(int& val) {
    func_in_10032(val);
    const int x = 10032;
    std::cout << 10032;
    for (int i = 0; i < 10032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10032(int& val) {
    const int x = 10032;
    std::cout << 10032;
    for (int i = 0; i < 10032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10033(int& val) {
    func_in_10033(val);
    const int x = 10033;
    std::cout << 10033;
    for (int i = 0; i < 10033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10033(int& val) {
    const int x = 10033;
    std::cout << 10033;
    for (int i = 0; i < 10033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10034(int& val) {
    func_in_10034(val);
    const int x = 10034;
    std::cout << 10034;
    for (int i = 0; i < 10034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10034(int& val) {
    const int x = 10034;
    std::cout << 10034;
    for (int i = 0; i < 10034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10035(int& val) {
    func_in_10035(val);
    const int x = 10035;
    std::cout << 10035;
    for (int i = 0; i < 10035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10035(int& val) {
    const int x = 10035;
    std::cout << 10035;
    for (int i = 0; i < 10035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10036(int& val) {
    func_in_10036(val);
    const int x = 10036;
    std::cout << 10036;
    for (int i = 0; i < 10036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10036(int& val) {
    const int x = 10036;
    std::cout << 10036;
    for (int i = 0; i < 10036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10037(int& val) {
    func_in_10037(val);
    const int x = 10037;
    std::cout << 10037;
    for (int i = 0; i < 10037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10037(int& val) {
    const int x = 10037;
    std::cout << 10037;
    for (int i = 0; i < 10037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10038(int& val) {
    func_in_10038(val);
    const int x = 10038;
    std::cout << 10038;
    for (int i = 0; i < 10038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10038(int& val) {
    const int x = 10038;
    std::cout << 10038;
    for (int i = 0; i < 10038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10039(int& val) {
    func_in_10039(val);
    const int x = 10039;
    std::cout << 10039;
    for (int i = 0; i < 10039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10039(int& val) {
    const int x = 10039;
    std::cout << 10039;
    for (int i = 0; i < 10039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10040(int& val) {
    func_in_10040(val);
    const int x = 10040;
    std::cout << 10040;
    for (int i = 0; i < 10040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10040(int& val) {
    const int x = 10040;
    std::cout << 10040;
    for (int i = 0; i < 10040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10041(int& val) {
    func_in_10041(val);
    const int x = 10041;
    std::cout << 10041;
    for (int i = 0; i < 10041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10041(int& val) {
    const int x = 10041;
    std::cout << 10041;
    for (int i = 0; i < 10041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10042(int& val) {
    func_in_10042(val);
    const int x = 10042;
    std::cout << 10042;
    for (int i = 0; i < 10042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10042(int& val) {
    const int x = 10042;
    std::cout << 10042;
    for (int i = 0; i < 10042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10043(int& val) {
    func_in_10043(val);
    const int x = 10043;
    std::cout << 10043;
    for (int i = 0; i < 10043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10043(int& val) {
    const int x = 10043;
    std::cout << 10043;
    for (int i = 0; i < 10043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10044(int& val) {
    func_in_10044(val);
    const int x = 10044;
    std::cout << 10044;
    for (int i = 0; i < 10044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10044(int& val) {
    const int x = 10044;
    std::cout << 10044;
    for (int i = 0; i < 10044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10045(int& val) {
    func_in_10045(val);
    const int x = 10045;
    std::cout << 10045;
    for (int i = 0; i < 10045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10045(int& val) {
    const int x = 10045;
    std::cout << 10045;
    for (int i = 0; i < 10045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10046(int& val) {
    func_in_10046(val);
    const int x = 10046;
    std::cout << 10046;
    for (int i = 0; i < 10046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10046(int& val) {
    const int x = 10046;
    std::cout << 10046;
    for (int i = 0; i < 10046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10047(int& val) {
    func_in_10047(val);
    const int x = 10047;
    std::cout << 10047;
    for (int i = 0; i < 10047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10047(int& val) {
    const int x = 10047;
    std::cout << 10047;
    for (int i = 0; i < 10047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10048(int& val) {
    func_in_10048(val);
    const int x = 10048;
    std::cout << 10048;
    for (int i = 0; i < 10048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10048(int& val) {
    const int x = 10048;
    std::cout << 10048;
    for (int i = 0; i < 10048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10049(int& val) {
    func_in_10049(val);
    const int x = 10049;
    std::cout << 10049;
    for (int i = 0; i < 10049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10049(int& val) {
    const int x = 10049;
    std::cout << 10049;
    for (int i = 0; i < 10049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10050(int& val) {
    func_in_10050(val);
    const int x = 10050;
    std::cout << 10050;
    for (int i = 0; i < 10050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10050(int& val) {
    const int x = 10050;
    std::cout << 10050;
    for (int i = 0; i < 10050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10051(int& val) {
    func_in_10051(val);
    const int x = 10051;
    std::cout << 10051;
    for (int i = 0; i < 10051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10051(int& val) {
    const int x = 10051;
    std::cout << 10051;
    for (int i = 0; i < 10051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10052(int& val) {
    func_in_10052(val);
    const int x = 10052;
    std::cout << 10052;
    for (int i = 0; i < 10052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10052(int& val) {
    const int x = 10052;
    std::cout << 10052;
    for (int i = 0; i < 10052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10053(int& val) {
    func_in_10053(val);
    const int x = 10053;
    std::cout << 10053;
    for (int i = 0; i < 10053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10053(int& val) {
    const int x = 10053;
    std::cout << 10053;
    for (int i = 0; i < 10053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10054(int& val) {
    func_in_10054(val);
    const int x = 10054;
    std::cout << 10054;
    for (int i = 0; i < 10054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10054(int& val) {
    const int x = 10054;
    std::cout << 10054;
    for (int i = 0; i < 10054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10055(int& val) {
    func_in_10055(val);
    const int x = 10055;
    std::cout << 10055;
    for (int i = 0; i < 10055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10055(int& val) {
    const int x = 10055;
    std::cout << 10055;
    for (int i = 0; i < 10055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10056(int& val) {
    func_in_10056(val);
    const int x = 10056;
    std::cout << 10056;
    for (int i = 0; i < 10056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10056(int& val) {
    const int x = 10056;
    std::cout << 10056;
    for (int i = 0; i < 10056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10057(int& val) {
    func_in_10057(val);
    const int x = 10057;
    std::cout << 10057;
    for (int i = 0; i < 10057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10057(int& val) {
    const int x = 10057;
    std::cout << 10057;
    for (int i = 0; i < 10057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10058(int& val) {
    func_in_10058(val);
    const int x = 10058;
    std::cout << 10058;
    for (int i = 0; i < 10058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10058(int& val) {
    const int x = 10058;
    std::cout << 10058;
    for (int i = 0; i < 10058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10059(int& val) {
    func_in_10059(val);
    const int x = 10059;
    std::cout << 10059;
    for (int i = 0; i < 10059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10059(int& val) {
    const int x = 10059;
    std::cout << 10059;
    for (int i = 0; i < 10059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10060(int& val) {
    func_in_10060(val);
    const int x = 10060;
    std::cout << 10060;
    for (int i = 0; i < 10060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10060(int& val) {
    const int x = 10060;
    std::cout << 10060;
    for (int i = 0; i < 10060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10061(int& val) {
    func_in_10061(val);
    const int x = 10061;
    std::cout << 10061;
    for (int i = 0; i < 10061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10061(int& val) {
    const int x = 10061;
    std::cout << 10061;
    for (int i = 0; i < 10061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10062(int& val) {
    func_in_10062(val);
    const int x = 10062;
    std::cout << 10062;
    for (int i = 0; i < 10062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10062(int& val) {
    const int x = 10062;
    std::cout << 10062;
    for (int i = 0; i < 10062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10063(int& val) {
    func_in_10063(val);
    const int x = 10063;
    std::cout << 10063;
    for (int i = 0; i < 10063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10063(int& val) {
    const int x = 10063;
    std::cout << 10063;
    for (int i = 0; i < 10063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10064(int& val) {
    func_in_10064(val);
    const int x = 10064;
    std::cout << 10064;
    for (int i = 0; i < 10064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10064(int& val) {
    const int x = 10064;
    std::cout << 10064;
    for (int i = 0; i < 10064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10065(int& val) {
    func_in_10065(val);
    const int x = 10065;
    std::cout << 10065;
    for (int i = 0; i < 10065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10065(int& val) {
    const int x = 10065;
    std::cout << 10065;
    for (int i = 0; i < 10065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10066(int& val) {
    func_in_10066(val);
    const int x = 10066;
    std::cout << 10066;
    for (int i = 0; i < 10066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10066(int& val) {
    const int x = 10066;
    std::cout << 10066;
    for (int i = 0; i < 10066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10067(int& val) {
    func_in_10067(val);
    const int x = 10067;
    std::cout << 10067;
    for (int i = 0; i < 10067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10067(int& val) {
    const int x = 10067;
    std::cout << 10067;
    for (int i = 0; i < 10067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10068(int& val) {
    func_in_10068(val);
    const int x = 10068;
    std::cout << 10068;
    for (int i = 0; i < 10068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10068(int& val) {
    const int x = 10068;
    std::cout << 10068;
    for (int i = 0; i < 10068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10069(int& val) {
    func_in_10069(val);
    const int x = 10069;
    std::cout << 10069;
    for (int i = 0; i < 10069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10069(int& val) {
    const int x = 10069;
    std::cout << 10069;
    for (int i = 0; i < 10069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10070(int& val) {
    func_in_10070(val);
    const int x = 10070;
    std::cout << 10070;
    for (int i = 0; i < 10070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10070(int& val) {
    const int x = 10070;
    std::cout << 10070;
    for (int i = 0; i < 10070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10071(int& val) {
    func_in_10071(val);
    const int x = 10071;
    std::cout << 10071;
    for (int i = 0; i < 10071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10071(int& val) {
    const int x = 10071;
    std::cout << 10071;
    for (int i = 0; i < 10071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10072(int& val) {
    func_in_10072(val);
    const int x = 10072;
    std::cout << 10072;
    for (int i = 0; i < 10072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10072(int& val) {
    const int x = 10072;
    std::cout << 10072;
    for (int i = 0; i < 10072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10073(int& val) {
    func_in_10073(val);
    const int x = 10073;
    std::cout << 10073;
    for (int i = 0; i < 10073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10073(int& val) {
    const int x = 10073;
    std::cout << 10073;
    for (int i = 0; i < 10073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10074(int& val) {
    func_in_10074(val);
    const int x = 10074;
    std::cout << 10074;
    for (int i = 0; i < 10074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10074(int& val) {
    const int x = 10074;
    std::cout << 10074;
    for (int i = 0; i < 10074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10075(int& val) {
    func_in_10075(val);
    const int x = 10075;
    std::cout << 10075;
    for (int i = 0; i < 10075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10075(int& val) {
    const int x = 10075;
    std::cout << 10075;
    for (int i = 0; i < 10075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10076(int& val) {
    func_in_10076(val);
    const int x = 10076;
    std::cout << 10076;
    for (int i = 0; i < 10076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10076(int& val) {
    const int x = 10076;
    std::cout << 10076;
    for (int i = 0; i < 10076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10077(int& val) {
    func_in_10077(val);
    const int x = 10077;
    std::cout << 10077;
    for (int i = 0; i < 10077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10077(int& val) {
    const int x = 10077;
    std::cout << 10077;
    for (int i = 0; i < 10077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10078(int& val) {
    func_in_10078(val);
    const int x = 10078;
    std::cout << 10078;
    for (int i = 0; i < 10078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10078(int& val) {
    const int x = 10078;
    std::cout << 10078;
    for (int i = 0; i < 10078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10079(int& val) {
    func_in_10079(val);
    const int x = 10079;
    std::cout << 10079;
    for (int i = 0; i < 10079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10079(int& val) {
    const int x = 10079;
    std::cout << 10079;
    for (int i = 0; i < 10079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10080(int& val) {
    func_in_10080(val);
    const int x = 10080;
    std::cout << 10080;
    for (int i = 0; i < 10080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10080(int& val) {
    const int x = 10080;
    std::cout << 10080;
    for (int i = 0; i < 10080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10081(int& val) {
    func_in_10081(val);
    const int x = 10081;
    std::cout << 10081;
    for (int i = 0; i < 10081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10081(int& val) {
    const int x = 10081;
    std::cout << 10081;
    for (int i = 0; i < 10081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10082(int& val) {
    func_in_10082(val);
    const int x = 10082;
    std::cout << 10082;
    for (int i = 0; i < 10082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10082(int& val) {
    const int x = 10082;
    std::cout << 10082;
    for (int i = 0; i < 10082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10083(int& val) {
    func_in_10083(val);
    const int x = 10083;
    std::cout << 10083;
    for (int i = 0; i < 10083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10083(int& val) {
    const int x = 10083;
    std::cout << 10083;
    for (int i = 0; i < 10083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10084(int& val) {
    func_in_10084(val);
    const int x = 10084;
    std::cout << 10084;
    for (int i = 0; i < 10084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10084(int& val) {
    const int x = 10084;
    std::cout << 10084;
    for (int i = 0; i < 10084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10085(int& val) {
    func_in_10085(val);
    const int x = 10085;
    std::cout << 10085;
    for (int i = 0; i < 10085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10085(int& val) {
    const int x = 10085;
    std::cout << 10085;
    for (int i = 0; i < 10085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10086(int& val) {
    func_in_10086(val);
    const int x = 10086;
    std::cout << 10086;
    for (int i = 0; i < 10086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10086(int& val) {
    const int x = 10086;
    std::cout << 10086;
    for (int i = 0; i < 10086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10087(int& val) {
    func_in_10087(val);
    const int x = 10087;
    std::cout << 10087;
    for (int i = 0; i < 10087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10087(int& val) {
    const int x = 10087;
    std::cout << 10087;
    for (int i = 0; i < 10087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10088(int& val) {
    func_in_10088(val);
    const int x = 10088;
    std::cout << 10088;
    for (int i = 0; i < 10088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10088(int& val) {
    const int x = 10088;
    std::cout << 10088;
    for (int i = 0; i < 10088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10089(int& val) {
    func_in_10089(val);
    const int x = 10089;
    std::cout << 10089;
    for (int i = 0; i < 10089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10089(int& val) {
    const int x = 10089;
    std::cout << 10089;
    for (int i = 0; i < 10089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10090(int& val) {
    func_in_10090(val);
    const int x = 10090;
    std::cout << 10090;
    for (int i = 0; i < 10090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10090(int& val) {
    const int x = 10090;
    std::cout << 10090;
    for (int i = 0; i < 10090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10091(int& val) {
    func_in_10091(val);
    const int x = 10091;
    std::cout << 10091;
    for (int i = 0; i < 10091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10091(int& val) {
    const int x = 10091;
    std::cout << 10091;
    for (int i = 0; i < 10091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10092(int& val) {
    func_in_10092(val);
    const int x = 10092;
    std::cout << 10092;
    for (int i = 0; i < 10092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10092(int& val) {
    const int x = 10092;
    std::cout << 10092;
    for (int i = 0; i < 10092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10093(int& val) {
    func_in_10093(val);
    const int x = 10093;
    std::cout << 10093;
    for (int i = 0; i < 10093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10093(int& val) {
    const int x = 10093;
    std::cout << 10093;
    for (int i = 0; i < 10093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10094(int& val) {
    func_in_10094(val);
    const int x = 10094;
    std::cout << 10094;
    for (int i = 0; i < 10094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10094(int& val) {
    const int x = 10094;
    std::cout << 10094;
    for (int i = 0; i < 10094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10095(int& val) {
    func_in_10095(val);
    const int x = 10095;
    std::cout << 10095;
    for (int i = 0; i < 10095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10095(int& val) {
    const int x = 10095;
    std::cout << 10095;
    for (int i = 0; i < 10095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10096(int& val) {
    func_in_10096(val);
    const int x = 10096;
    std::cout << 10096;
    for (int i = 0; i < 10096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10096(int& val) {
    const int x = 10096;
    std::cout << 10096;
    for (int i = 0; i < 10096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10097(int& val) {
    func_in_10097(val);
    const int x = 10097;
    std::cout << 10097;
    for (int i = 0; i < 10097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10097(int& val) {
    const int x = 10097;
    std::cout << 10097;
    for (int i = 0; i < 10097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10098(int& val) {
    func_in_10098(val);
    const int x = 10098;
    std::cout << 10098;
    for (int i = 0; i < 10098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10098(int& val) {
    const int x = 10098;
    std::cout << 10098;
    for (int i = 0; i < 10098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10099(int& val) {
    func_in_10099(val);
    const int x = 10099;
    std::cout << 10099;
    for (int i = 0; i < 10099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10099(int& val) {
    const int x = 10099;
    std::cout << 10099;
    for (int i = 0; i < 10099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10100(int& val) {
    func_in_10100(val);
    const int x = 10100;
    std::cout << 10100;
    for (int i = 0; i < 10100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10100(int& val) {
    const int x = 10100;
    std::cout << 10100;
    for (int i = 0; i < 10100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10101(int& val) {
    func_in_10101(val);
    const int x = 10101;
    std::cout << 10101;
    for (int i = 0; i < 10101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10101(int& val) {
    const int x = 10101;
    std::cout << 10101;
    for (int i = 0; i < 10101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10102(int& val) {
    func_in_10102(val);
    const int x = 10102;
    std::cout << 10102;
    for (int i = 0; i < 10102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10102(int& val) {
    const int x = 10102;
    std::cout << 10102;
    for (int i = 0; i < 10102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10103(int& val) {
    func_in_10103(val);
    const int x = 10103;
    std::cout << 10103;
    for (int i = 0; i < 10103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10103(int& val) {
    const int x = 10103;
    std::cout << 10103;
    for (int i = 0; i < 10103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10104(int& val) {
    func_in_10104(val);
    const int x = 10104;
    std::cout << 10104;
    for (int i = 0; i < 10104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10104(int& val) {
    const int x = 10104;
    std::cout << 10104;
    for (int i = 0; i < 10104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10105(int& val) {
    func_in_10105(val);
    const int x = 10105;
    std::cout << 10105;
    for (int i = 0; i < 10105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10105(int& val) {
    const int x = 10105;
    std::cout << 10105;
    for (int i = 0; i < 10105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10106(int& val) {
    func_in_10106(val);
    const int x = 10106;
    std::cout << 10106;
    for (int i = 0; i < 10106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10106(int& val) {
    const int x = 10106;
    std::cout << 10106;
    for (int i = 0; i < 10106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10107(int& val) {
    func_in_10107(val);
    const int x = 10107;
    std::cout << 10107;
    for (int i = 0; i < 10107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10107(int& val) {
    const int x = 10107;
    std::cout << 10107;
    for (int i = 0; i < 10107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10108(int& val) {
    func_in_10108(val);
    const int x = 10108;
    std::cout << 10108;
    for (int i = 0; i < 10108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10108(int& val) {
    const int x = 10108;
    std::cout << 10108;
    for (int i = 0; i < 10108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10109(int& val) {
    func_in_10109(val);
    const int x = 10109;
    std::cout << 10109;
    for (int i = 0; i < 10109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10109(int& val) {
    const int x = 10109;
    std::cout << 10109;
    for (int i = 0; i < 10109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10110(int& val) {
    func_in_10110(val);
    const int x = 10110;
    std::cout << 10110;
    for (int i = 0; i < 10110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10110(int& val) {
    const int x = 10110;
    std::cout << 10110;
    for (int i = 0; i < 10110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10111(int& val) {
    func_in_10111(val);
    const int x = 10111;
    std::cout << 10111;
    for (int i = 0; i < 10111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10111(int& val) {
    const int x = 10111;
    std::cout << 10111;
    for (int i = 0; i < 10111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10112(int& val) {
    func_in_10112(val);
    const int x = 10112;
    std::cout << 10112;
    for (int i = 0; i < 10112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10112(int& val) {
    const int x = 10112;
    std::cout << 10112;
    for (int i = 0; i < 10112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10113(int& val) {
    func_in_10113(val);
    const int x = 10113;
    std::cout << 10113;
    for (int i = 0; i < 10113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10113(int& val) {
    const int x = 10113;
    std::cout << 10113;
    for (int i = 0; i < 10113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10114(int& val) {
    func_in_10114(val);
    const int x = 10114;
    std::cout << 10114;
    for (int i = 0; i < 10114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10114(int& val) {
    const int x = 10114;
    std::cout << 10114;
    for (int i = 0; i < 10114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10115(int& val) {
    func_in_10115(val);
    const int x = 10115;
    std::cout << 10115;
    for (int i = 0; i < 10115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10115(int& val) {
    const int x = 10115;
    std::cout << 10115;
    for (int i = 0; i < 10115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10116(int& val) {
    func_in_10116(val);
    const int x = 10116;
    std::cout << 10116;
    for (int i = 0; i < 10116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10116(int& val) {
    const int x = 10116;
    std::cout << 10116;
    for (int i = 0; i < 10116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10117(int& val) {
    func_in_10117(val);
    const int x = 10117;
    std::cout << 10117;
    for (int i = 0; i < 10117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10117(int& val) {
    const int x = 10117;
    std::cout << 10117;
    for (int i = 0; i < 10117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10118(int& val) {
    func_in_10118(val);
    const int x = 10118;
    std::cout << 10118;
    for (int i = 0; i < 10118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10118(int& val) {
    const int x = 10118;
    std::cout << 10118;
    for (int i = 0; i < 10118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10119(int& val) {
    func_in_10119(val);
    const int x = 10119;
    std::cout << 10119;
    for (int i = 0; i < 10119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10119(int& val) {
    const int x = 10119;
    std::cout << 10119;
    for (int i = 0; i < 10119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10120(int& val) {
    func_in_10120(val);
    const int x = 10120;
    std::cout << 10120;
    for (int i = 0; i < 10120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10120(int& val) {
    const int x = 10120;
    std::cout << 10120;
    for (int i = 0; i < 10120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10121(int& val) {
    func_in_10121(val);
    const int x = 10121;
    std::cout << 10121;
    for (int i = 0; i < 10121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10121(int& val) {
    const int x = 10121;
    std::cout << 10121;
    for (int i = 0; i < 10121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10122(int& val) {
    func_in_10122(val);
    const int x = 10122;
    std::cout << 10122;
    for (int i = 0; i < 10122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10122(int& val) {
    const int x = 10122;
    std::cout << 10122;
    for (int i = 0; i < 10122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10123(int& val) {
    func_in_10123(val);
    const int x = 10123;
    std::cout << 10123;
    for (int i = 0; i < 10123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10123(int& val) {
    const int x = 10123;
    std::cout << 10123;
    for (int i = 0; i < 10123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10124(int& val) {
    func_in_10124(val);
    const int x = 10124;
    std::cout << 10124;
    for (int i = 0; i < 10124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10124(int& val) {
    const int x = 10124;
    std::cout << 10124;
    for (int i = 0; i < 10124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10125(int& val) {
    func_in_10125(val);
    const int x = 10125;
    std::cout << 10125;
    for (int i = 0; i < 10125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10125(int& val) {
    const int x = 10125;
    std::cout << 10125;
    for (int i = 0; i < 10125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10126(int& val) {
    func_in_10126(val);
    const int x = 10126;
    std::cout << 10126;
    for (int i = 0; i < 10126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10126(int& val) {
    const int x = 10126;
    std::cout << 10126;
    for (int i = 0; i < 10126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10127(int& val) {
    func_in_10127(val);
    const int x = 10127;
    std::cout << 10127;
    for (int i = 0; i < 10127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10127(int& val) {
    const int x = 10127;
    std::cout << 10127;
    for (int i = 0; i < 10127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10128(int& val) {
    func_in_10128(val);
    const int x = 10128;
    std::cout << 10128;
    for (int i = 0; i < 10128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10128(int& val) {
    const int x = 10128;
    std::cout << 10128;
    for (int i = 0; i < 10128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10129(int& val) {
    func_in_10129(val);
    const int x = 10129;
    std::cout << 10129;
    for (int i = 0; i < 10129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10129(int& val) {
    const int x = 10129;
    std::cout << 10129;
    for (int i = 0; i < 10129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10130(int& val) {
    func_in_10130(val);
    const int x = 10130;
    std::cout << 10130;
    for (int i = 0; i < 10130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10130(int& val) {
    const int x = 10130;
    std::cout << 10130;
    for (int i = 0; i < 10130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10131(int& val) {
    func_in_10131(val);
    const int x = 10131;
    std::cout << 10131;
    for (int i = 0; i < 10131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10131(int& val) {
    const int x = 10131;
    std::cout << 10131;
    for (int i = 0; i < 10131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10132(int& val) {
    func_in_10132(val);
    const int x = 10132;
    std::cout << 10132;
    for (int i = 0; i < 10132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10132(int& val) {
    const int x = 10132;
    std::cout << 10132;
    for (int i = 0; i < 10132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10133(int& val) {
    func_in_10133(val);
    const int x = 10133;
    std::cout << 10133;
    for (int i = 0; i < 10133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10133(int& val) {
    const int x = 10133;
    std::cout << 10133;
    for (int i = 0; i < 10133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10134(int& val) {
    func_in_10134(val);
    const int x = 10134;
    std::cout << 10134;
    for (int i = 0; i < 10134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10134(int& val) {
    const int x = 10134;
    std::cout << 10134;
    for (int i = 0; i < 10134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10135(int& val) {
    func_in_10135(val);
    const int x = 10135;
    std::cout << 10135;
    for (int i = 0; i < 10135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10135(int& val) {
    const int x = 10135;
    std::cout << 10135;
    for (int i = 0; i < 10135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10136(int& val) {
    func_in_10136(val);
    const int x = 10136;
    std::cout << 10136;
    for (int i = 0; i < 10136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10136(int& val) {
    const int x = 10136;
    std::cout << 10136;
    for (int i = 0; i < 10136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10137(int& val) {
    func_in_10137(val);
    const int x = 10137;
    std::cout << 10137;
    for (int i = 0; i < 10137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10137(int& val) {
    const int x = 10137;
    std::cout << 10137;
    for (int i = 0; i < 10137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10138(int& val) {
    func_in_10138(val);
    const int x = 10138;
    std::cout << 10138;
    for (int i = 0; i < 10138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10138(int& val) {
    const int x = 10138;
    std::cout << 10138;
    for (int i = 0; i < 10138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10139(int& val) {
    func_in_10139(val);
    const int x = 10139;
    std::cout << 10139;
    for (int i = 0; i < 10139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10139(int& val) {
    const int x = 10139;
    std::cout << 10139;
    for (int i = 0; i < 10139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10140(int& val) {
    func_in_10140(val);
    const int x = 10140;
    std::cout << 10140;
    for (int i = 0; i < 10140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10140(int& val) {
    const int x = 10140;
    std::cout << 10140;
    for (int i = 0; i < 10140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10141(int& val) {
    func_in_10141(val);
    const int x = 10141;
    std::cout << 10141;
    for (int i = 0; i < 10141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10141(int& val) {
    const int x = 10141;
    std::cout << 10141;
    for (int i = 0; i < 10141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10142(int& val) {
    func_in_10142(val);
    const int x = 10142;
    std::cout << 10142;
    for (int i = 0; i < 10142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10142(int& val) {
    const int x = 10142;
    std::cout << 10142;
    for (int i = 0; i < 10142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10143(int& val) {
    func_in_10143(val);
    const int x = 10143;
    std::cout << 10143;
    for (int i = 0; i < 10143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10143(int& val) {
    const int x = 10143;
    std::cout << 10143;
    for (int i = 0; i < 10143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10144(int& val) {
    func_in_10144(val);
    const int x = 10144;
    std::cout << 10144;
    for (int i = 0; i < 10144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10144(int& val) {
    const int x = 10144;
    std::cout << 10144;
    for (int i = 0; i < 10144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10145(int& val) {
    func_in_10145(val);
    const int x = 10145;
    std::cout << 10145;
    for (int i = 0; i < 10145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10145(int& val) {
    const int x = 10145;
    std::cout << 10145;
    for (int i = 0; i < 10145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10146(int& val) {
    func_in_10146(val);
    const int x = 10146;
    std::cout << 10146;
    for (int i = 0; i < 10146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10146(int& val) {
    const int x = 10146;
    std::cout << 10146;
    for (int i = 0; i < 10146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10147(int& val) {
    func_in_10147(val);
    const int x = 10147;
    std::cout << 10147;
    for (int i = 0; i < 10147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10147(int& val) {
    const int x = 10147;
    std::cout << 10147;
    for (int i = 0; i < 10147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10148(int& val) {
    func_in_10148(val);
    const int x = 10148;
    std::cout << 10148;
    for (int i = 0; i < 10148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10148(int& val) {
    const int x = 10148;
    std::cout << 10148;
    for (int i = 0; i < 10148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10149(int& val) {
    func_in_10149(val);
    const int x = 10149;
    std::cout << 10149;
    for (int i = 0; i < 10149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10149(int& val) {
    const int x = 10149;
    std::cout << 10149;
    for (int i = 0; i < 10149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10150(int& val) {
    func_in_10150(val);
    const int x = 10150;
    std::cout << 10150;
    for (int i = 0; i < 10150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10150(int& val) {
    const int x = 10150;
    std::cout << 10150;
    for (int i = 0; i < 10150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10151(int& val) {
    func_in_10151(val);
    const int x = 10151;
    std::cout << 10151;
    for (int i = 0; i < 10151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10151(int& val) {
    const int x = 10151;
    std::cout << 10151;
    for (int i = 0; i < 10151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10152(int& val) {
    func_in_10152(val);
    const int x = 10152;
    std::cout << 10152;
    for (int i = 0; i < 10152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10152(int& val) {
    const int x = 10152;
    std::cout << 10152;
    for (int i = 0; i < 10152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10153(int& val) {
    func_in_10153(val);
    const int x = 10153;
    std::cout << 10153;
    for (int i = 0; i < 10153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10153(int& val) {
    const int x = 10153;
    std::cout << 10153;
    for (int i = 0; i < 10153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10154(int& val) {
    func_in_10154(val);
    const int x = 10154;
    std::cout << 10154;
    for (int i = 0; i < 10154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10154(int& val) {
    const int x = 10154;
    std::cout << 10154;
    for (int i = 0; i < 10154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10155(int& val) {
    func_in_10155(val);
    const int x = 10155;
    std::cout << 10155;
    for (int i = 0; i < 10155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10155(int& val) {
    const int x = 10155;
    std::cout << 10155;
    for (int i = 0; i < 10155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10156(int& val) {
    func_in_10156(val);
    const int x = 10156;
    std::cout << 10156;
    for (int i = 0; i < 10156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10156(int& val) {
    const int x = 10156;
    std::cout << 10156;
    for (int i = 0; i < 10156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10157(int& val) {
    func_in_10157(val);
    const int x = 10157;
    std::cout << 10157;
    for (int i = 0; i < 10157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10157(int& val) {
    const int x = 10157;
    std::cout << 10157;
    for (int i = 0; i < 10157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10158(int& val) {
    func_in_10158(val);
    const int x = 10158;
    std::cout << 10158;
    for (int i = 0; i < 10158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10158(int& val) {
    const int x = 10158;
    std::cout << 10158;
    for (int i = 0; i < 10158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10159(int& val) {
    func_in_10159(val);
    const int x = 10159;
    std::cout << 10159;
    for (int i = 0; i < 10159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10159(int& val) {
    const int x = 10159;
    std::cout << 10159;
    for (int i = 0; i < 10159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10160(int& val) {
    func_in_10160(val);
    const int x = 10160;
    std::cout << 10160;
    for (int i = 0; i < 10160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10160(int& val) {
    const int x = 10160;
    std::cout << 10160;
    for (int i = 0; i < 10160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10161(int& val) {
    func_in_10161(val);
    const int x = 10161;
    std::cout << 10161;
    for (int i = 0; i < 10161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10161(int& val) {
    const int x = 10161;
    std::cout << 10161;
    for (int i = 0; i < 10161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10162(int& val) {
    func_in_10162(val);
    const int x = 10162;
    std::cout << 10162;
    for (int i = 0; i < 10162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10162(int& val) {
    const int x = 10162;
    std::cout << 10162;
    for (int i = 0; i < 10162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10163(int& val) {
    func_in_10163(val);
    const int x = 10163;
    std::cout << 10163;
    for (int i = 0; i < 10163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10163(int& val) {
    const int x = 10163;
    std::cout << 10163;
    for (int i = 0; i < 10163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10164(int& val) {
    func_in_10164(val);
    const int x = 10164;
    std::cout << 10164;
    for (int i = 0; i < 10164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10164(int& val) {
    const int x = 10164;
    std::cout << 10164;
    for (int i = 0; i < 10164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10165(int& val) {
    func_in_10165(val);
    const int x = 10165;
    std::cout << 10165;
    for (int i = 0; i < 10165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10165(int& val) {
    const int x = 10165;
    std::cout << 10165;
    for (int i = 0; i < 10165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10166(int& val) {
    func_in_10166(val);
    const int x = 10166;
    std::cout << 10166;
    for (int i = 0; i < 10166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10166(int& val) {
    const int x = 10166;
    std::cout << 10166;
    for (int i = 0; i < 10166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10167(int& val) {
    func_in_10167(val);
    const int x = 10167;
    std::cout << 10167;
    for (int i = 0; i < 10167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10167(int& val) {
    const int x = 10167;
    std::cout << 10167;
    for (int i = 0; i < 10167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10168(int& val) {
    func_in_10168(val);
    const int x = 10168;
    std::cout << 10168;
    for (int i = 0; i < 10168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10168(int& val) {
    const int x = 10168;
    std::cout << 10168;
    for (int i = 0; i < 10168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10169(int& val) {
    func_in_10169(val);
    const int x = 10169;
    std::cout << 10169;
    for (int i = 0; i < 10169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10169(int& val) {
    const int x = 10169;
    std::cout << 10169;
    for (int i = 0; i < 10169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10170(int& val) {
    func_in_10170(val);
    const int x = 10170;
    std::cout << 10170;
    for (int i = 0; i < 10170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10170(int& val) {
    const int x = 10170;
    std::cout << 10170;
    for (int i = 0; i < 10170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10171(int& val) {
    func_in_10171(val);
    const int x = 10171;
    std::cout << 10171;
    for (int i = 0; i < 10171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10171(int& val) {
    const int x = 10171;
    std::cout << 10171;
    for (int i = 0; i < 10171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10172(int& val) {
    func_in_10172(val);
    const int x = 10172;
    std::cout << 10172;
    for (int i = 0; i < 10172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10172(int& val) {
    const int x = 10172;
    std::cout << 10172;
    for (int i = 0; i < 10172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10173(int& val) {
    func_in_10173(val);
    const int x = 10173;
    std::cout << 10173;
    for (int i = 0; i < 10173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10173(int& val) {
    const int x = 10173;
    std::cout << 10173;
    for (int i = 0; i < 10173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10174(int& val) {
    func_in_10174(val);
    const int x = 10174;
    std::cout << 10174;
    for (int i = 0; i < 10174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10174(int& val) {
    const int x = 10174;
    std::cout << 10174;
    for (int i = 0; i < 10174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10175(int& val) {
    func_in_10175(val);
    const int x = 10175;
    std::cout << 10175;
    for (int i = 0; i < 10175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10175(int& val) {
    const int x = 10175;
    std::cout << 10175;
    for (int i = 0; i < 10175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10176(int& val) {
    func_in_10176(val);
    const int x = 10176;
    std::cout << 10176;
    for (int i = 0; i < 10176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10176(int& val) {
    const int x = 10176;
    std::cout << 10176;
    for (int i = 0; i < 10176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10177(int& val) {
    func_in_10177(val);
    const int x = 10177;
    std::cout << 10177;
    for (int i = 0; i < 10177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10177(int& val) {
    const int x = 10177;
    std::cout << 10177;
    for (int i = 0; i < 10177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10178(int& val) {
    func_in_10178(val);
    const int x = 10178;
    std::cout << 10178;
    for (int i = 0; i < 10178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10178(int& val) {
    const int x = 10178;
    std::cout << 10178;
    for (int i = 0; i < 10178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10179(int& val) {
    func_in_10179(val);
    const int x = 10179;
    std::cout << 10179;
    for (int i = 0; i < 10179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10179(int& val) {
    const int x = 10179;
    std::cout << 10179;
    for (int i = 0; i < 10179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10180(int& val) {
    func_in_10180(val);
    const int x = 10180;
    std::cout << 10180;
    for (int i = 0; i < 10180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10180(int& val) {
    const int x = 10180;
    std::cout << 10180;
    for (int i = 0; i < 10180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10181(int& val) {
    func_in_10181(val);
    const int x = 10181;
    std::cout << 10181;
    for (int i = 0; i < 10181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10181(int& val) {
    const int x = 10181;
    std::cout << 10181;
    for (int i = 0; i < 10181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10182(int& val) {
    func_in_10182(val);
    const int x = 10182;
    std::cout << 10182;
    for (int i = 0; i < 10182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10182(int& val) {
    const int x = 10182;
    std::cout << 10182;
    for (int i = 0; i < 10182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10183(int& val) {
    func_in_10183(val);
    const int x = 10183;
    std::cout << 10183;
    for (int i = 0; i < 10183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10183(int& val) {
    const int x = 10183;
    std::cout << 10183;
    for (int i = 0; i < 10183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10184(int& val) {
    func_in_10184(val);
    const int x = 10184;
    std::cout << 10184;
    for (int i = 0; i < 10184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10184(int& val) {
    const int x = 10184;
    std::cout << 10184;
    for (int i = 0; i < 10184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10185(int& val) {
    func_in_10185(val);
    const int x = 10185;
    std::cout << 10185;
    for (int i = 0; i < 10185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10185(int& val) {
    const int x = 10185;
    std::cout << 10185;
    for (int i = 0; i < 10185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10186(int& val) {
    func_in_10186(val);
    const int x = 10186;
    std::cout << 10186;
    for (int i = 0; i < 10186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10186(int& val) {
    const int x = 10186;
    std::cout << 10186;
    for (int i = 0; i < 10186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10187(int& val) {
    func_in_10187(val);
    const int x = 10187;
    std::cout << 10187;
    for (int i = 0; i < 10187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10187(int& val) {
    const int x = 10187;
    std::cout << 10187;
    for (int i = 0; i < 10187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10188(int& val) {
    func_in_10188(val);
    const int x = 10188;
    std::cout << 10188;
    for (int i = 0; i < 10188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10188(int& val) {
    const int x = 10188;
    std::cout << 10188;
    for (int i = 0; i < 10188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10189(int& val) {
    func_in_10189(val);
    const int x = 10189;
    std::cout << 10189;
    for (int i = 0; i < 10189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10189(int& val) {
    const int x = 10189;
    std::cout << 10189;
    for (int i = 0; i < 10189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10190(int& val) {
    func_in_10190(val);
    const int x = 10190;
    std::cout << 10190;
    for (int i = 0; i < 10190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10190(int& val) {
    const int x = 10190;
    std::cout << 10190;
    for (int i = 0; i < 10190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10191(int& val) {
    func_in_10191(val);
    const int x = 10191;
    std::cout << 10191;
    for (int i = 0; i < 10191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10191(int& val) {
    const int x = 10191;
    std::cout << 10191;
    for (int i = 0; i < 10191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10192(int& val) {
    func_in_10192(val);
    const int x = 10192;
    std::cout << 10192;
    for (int i = 0; i < 10192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10192(int& val) {
    const int x = 10192;
    std::cout << 10192;
    for (int i = 0; i < 10192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10193(int& val) {
    func_in_10193(val);
    const int x = 10193;
    std::cout << 10193;
    for (int i = 0; i < 10193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10193(int& val) {
    const int x = 10193;
    std::cout << 10193;
    for (int i = 0; i < 10193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10194(int& val) {
    func_in_10194(val);
    const int x = 10194;
    std::cout << 10194;
    for (int i = 0; i < 10194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10194(int& val) {
    const int x = 10194;
    std::cout << 10194;
    for (int i = 0; i < 10194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10195(int& val) {
    func_in_10195(val);
    const int x = 10195;
    std::cout << 10195;
    for (int i = 0; i < 10195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10195(int& val) {
    const int x = 10195;
    std::cout << 10195;
    for (int i = 0; i < 10195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10196(int& val) {
    func_in_10196(val);
    const int x = 10196;
    std::cout << 10196;
    for (int i = 0; i < 10196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10196(int& val) {
    const int x = 10196;
    std::cout << 10196;
    for (int i = 0; i < 10196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10197(int& val) {
    func_in_10197(val);
    const int x = 10197;
    std::cout << 10197;
    for (int i = 0; i < 10197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10197(int& val) {
    const int x = 10197;
    std::cout << 10197;
    for (int i = 0; i < 10197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10198(int& val) {
    func_in_10198(val);
    const int x = 10198;
    std::cout << 10198;
    for (int i = 0; i < 10198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10198(int& val) {
    const int x = 10198;
    std::cout << 10198;
    for (int i = 0; i < 10198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10199(int& val) {
    func_in_10199(val);
    const int x = 10199;
    std::cout << 10199;
    for (int i = 0; i < 10199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10199(int& val) {
    const int x = 10199;
    std::cout << 10199;
    for (int i = 0; i < 10199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10200(int& val) {
    func_in_10200(val);
    const int x = 10200;
    std::cout << 10200;
    for (int i = 0; i < 10200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10200(int& val) {
    const int x = 10200;
    std::cout << 10200;
    for (int i = 0; i < 10200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10201(int& val) {
    func_in_10201(val);
    const int x = 10201;
    std::cout << 10201;
    for (int i = 0; i < 10201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10201(int& val) {
    const int x = 10201;
    std::cout << 10201;
    for (int i = 0; i < 10201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10202(int& val) {
    func_in_10202(val);
    const int x = 10202;
    std::cout << 10202;
    for (int i = 0; i < 10202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10202(int& val) {
    const int x = 10202;
    std::cout << 10202;
    for (int i = 0; i < 10202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10203(int& val) {
    func_in_10203(val);
    const int x = 10203;
    std::cout << 10203;
    for (int i = 0; i < 10203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10203(int& val) {
    const int x = 10203;
    std::cout << 10203;
    for (int i = 0; i < 10203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10204(int& val) {
    func_in_10204(val);
    const int x = 10204;
    std::cout << 10204;
    for (int i = 0; i < 10204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10204(int& val) {
    const int x = 10204;
    std::cout << 10204;
    for (int i = 0; i < 10204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10205(int& val) {
    func_in_10205(val);
    const int x = 10205;
    std::cout << 10205;
    for (int i = 0; i < 10205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10205(int& val) {
    const int x = 10205;
    std::cout << 10205;
    for (int i = 0; i < 10205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10206(int& val) {
    func_in_10206(val);
    const int x = 10206;
    std::cout << 10206;
    for (int i = 0; i < 10206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10206(int& val) {
    const int x = 10206;
    std::cout << 10206;
    for (int i = 0; i < 10206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10207(int& val) {
    func_in_10207(val);
    const int x = 10207;
    std::cout << 10207;
    for (int i = 0; i < 10207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10207(int& val) {
    const int x = 10207;
    std::cout << 10207;
    for (int i = 0; i < 10207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10208(int& val) {
    func_in_10208(val);
    const int x = 10208;
    std::cout << 10208;
    for (int i = 0; i < 10208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10208(int& val) {
    const int x = 10208;
    std::cout << 10208;
    for (int i = 0; i < 10208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10209(int& val) {
    func_in_10209(val);
    const int x = 10209;
    std::cout << 10209;
    for (int i = 0; i < 10209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10209(int& val) {
    const int x = 10209;
    std::cout << 10209;
    for (int i = 0; i < 10209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10210(int& val) {
    func_in_10210(val);
    const int x = 10210;
    std::cout << 10210;
    for (int i = 0; i < 10210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10210(int& val) {
    const int x = 10210;
    std::cout << 10210;
    for (int i = 0; i < 10210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10211(int& val) {
    func_in_10211(val);
    const int x = 10211;
    std::cout << 10211;
    for (int i = 0; i < 10211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10211(int& val) {
    const int x = 10211;
    std::cout << 10211;
    for (int i = 0; i < 10211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10212(int& val) {
    func_in_10212(val);
    const int x = 10212;
    std::cout << 10212;
    for (int i = 0; i < 10212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10212(int& val) {
    const int x = 10212;
    std::cout << 10212;
    for (int i = 0; i < 10212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10213(int& val) {
    func_in_10213(val);
    const int x = 10213;
    std::cout << 10213;
    for (int i = 0; i < 10213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10213(int& val) {
    const int x = 10213;
    std::cout << 10213;
    for (int i = 0; i < 10213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10214(int& val) {
    func_in_10214(val);
    const int x = 10214;
    std::cout << 10214;
    for (int i = 0; i < 10214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10214(int& val) {
    const int x = 10214;
    std::cout << 10214;
    for (int i = 0; i < 10214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10215(int& val) {
    func_in_10215(val);
    const int x = 10215;
    std::cout << 10215;
    for (int i = 0; i < 10215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10215(int& val) {
    const int x = 10215;
    std::cout << 10215;
    for (int i = 0; i < 10215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10216(int& val) {
    func_in_10216(val);
    const int x = 10216;
    std::cout << 10216;
    for (int i = 0; i < 10216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10216(int& val) {
    const int x = 10216;
    std::cout << 10216;
    for (int i = 0; i < 10216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10217(int& val) {
    func_in_10217(val);
    const int x = 10217;
    std::cout << 10217;
    for (int i = 0; i < 10217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10217(int& val) {
    const int x = 10217;
    std::cout << 10217;
    for (int i = 0; i < 10217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10218(int& val) {
    func_in_10218(val);
    const int x = 10218;
    std::cout << 10218;
    for (int i = 0; i < 10218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10218(int& val) {
    const int x = 10218;
    std::cout << 10218;
    for (int i = 0; i < 10218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10219(int& val) {
    func_in_10219(val);
    const int x = 10219;
    std::cout << 10219;
    for (int i = 0; i < 10219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10219(int& val) {
    const int x = 10219;
    std::cout << 10219;
    for (int i = 0; i < 10219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10220(int& val) {
    func_in_10220(val);
    const int x = 10220;
    std::cout << 10220;
    for (int i = 0; i < 10220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10220(int& val) {
    const int x = 10220;
    std::cout << 10220;
    for (int i = 0; i < 10220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10221(int& val) {
    func_in_10221(val);
    const int x = 10221;
    std::cout << 10221;
    for (int i = 0; i < 10221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10221(int& val) {
    const int x = 10221;
    std::cout << 10221;
    for (int i = 0; i < 10221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10222(int& val) {
    func_in_10222(val);
    const int x = 10222;
    std::cout << 10222;
    for (int i = 0; i < 10222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10222(int& val) {
    const int x = 10222;
    std::cout << 10222;
    for (int i = 0; i < 10222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10223(int& val) {
    func_in_10223(val);
    const int x = 10223;
    std::cout << 10223;
    for (int i = 0; i < 10223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10223(int& val) {
    const int x = 10223;
    std::cout << 10223;
    for (int i = 0; i < 10223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10224(int& val) {
    func_in_10224(val);
    const int x = 10224;
    std::cout << 10224;
    for (int i = 0; i < 10224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10224(int& val) {
    const int x = 10224;
    std::cout << 10224;
    for (int i = 0; i < 10224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10225(int& val) {
    func_in_10225(val);
    const int x = 10225;
    std::cout << 10225;
    for (int i = 0; i < 10225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10225(int& val) {
    const int x = 10225;
    std::cout << 10225;
    for (int i = 0; i < 10225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10226(int& val) {
    func_in_10226(val);
    const int x = 10226;
    std::cout << 10226;
    for (int i = 0; i < 10226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10226(int& val) {
    const int x = 10226;
    std::cout << 10226;
    for (int i = 0; i < 10226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10227(int& val) {
    func_in_10227(val);
    const int x = 10227;
    std::cout << 10227;
    for (int i = 0; i < 10227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10227(int& val) {
    const int x = 10227;
    std::cout << 10227;
    for (int i = 0; i < 10227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10228(int& val) {
    func_in_10228(val);
    const int x = 10228;
    std::cout << 10228;
    for (int i = 0; i < 10228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10228(int& val) {
    const int x = 10228;
    std::cout << 10228;
    for (int i = 0; i < 10228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10229(int& val) {
    func_in_10229(val);
    const int x = 10229;
    std::cout << 10229;
    for (int i = 0; i < 10229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10229(int& val) {
    const int x = 10229;
    std::cout << 10229;
    for (int i = 0; i < 10229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10230(int& val) {
    func_in_10230(val);
    const int x = 10230;
    std::cout << 10230;
    for (int i = 0; i < 10230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10230(int& val) {
    const int x = 10230;
    std::cout << 10230;
    for (int i = 0; i < 10230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10231(int& val) {
    func_in_10231(val);
    const int x = 10231;
    std::cout << 10231;
    for (int i = 0; i < 10231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10231(int& val) {
    const int x = 10231;
    std::cout << 10231;
    for (int i = 0; i < 10231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10232(int& val) {
    func_in_10232(val);
    const int x = 10232;
    std::cout << 10232;
    for (int i = 0; i < 10232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10232(int& val) {
    const int x = 10232;
    std::cout << 10232;
    for (int i = 0; i < 10232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10233(int& val) {
    func_in_10233(val);
    const int x = 10233;
    std::cout << 10233;
    for (int i = 0; i < 10233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10233(int& val) {
    const int x = 10233;
    std::cout << 10233;
    for (int i = 0; i < 10233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10234(int& val) {
    func_in_10234(val);
    const int x = 10234;
    std::cout << 10234;
    for (int i = 0; i < 10234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10234(int& val) {
    const int x = 10234;
    std::cout << 10234;
    for (int i = 0; i < 10234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10235(int& val) {
    func_in_10235(val);
    const int x = 10235;
    std::cout << 10235;
    for (int i = 0; i < 10235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10235(int& val) {
    const int x = 10235;
    std::cout << 10235;
    for (int i = 0; i < 10235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10236(int& val) {
    func_in_10236(val);
    const int x = 10236;
    std::cout << 10236;
    for (int i = 0; i < 10236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10236(int& val) {
    const int x = 10236;
    std::cout << 10236;
    for (int i = 0; i < 10236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10237(int& val) {
    func_in_10237(val);
    const int x = 10237;
    std::cout << 10237;
    for (int i = 0; i < 10237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10237(int& val) {
    const int x = 10237;
    std::cout << 10237;
    for (int i = 0; i < 10237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10238(int& val) {
    func_in_10238(val);
    const int x = 10238;
    std::cout << 10238;
    for (int i = 0; i < 10238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10238(int& val) {
    const int x = 10238;
    std::cout << 10238;
    for (int i = 0; i < 10238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10239(int& val) {
    func_in_10239(val);
    const int x = 10239;
    std::cout << 10239;
    for (int i = 0; i < 10239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10239(int& val) {
    const int x = 10239;
    std::cout << 10239;
    for (int i = 0; i < 10239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10240(int& val) {
    func_in_10240(val);
    const int x = 10240;
    std::cout << 10240;
    for (int i = 0; i < 10240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10240(int& val) {
    const int x = 10240;
    std::cout << 10240;
    for (int i = 0; i < 10240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10241(int& val) {
    func_in_10241(val);
    const int x = 10241;
    std::cout << 10241;
    for (int i = 0; i < 10241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10241(int& val) {
    const int x = 10241;
    std::cout << 10241;
    for (int i = 0; i < 10241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10242(int& val) {
    func_in_10242(val);
    const int x = 10242;
    std::cout << 10242;
    for (int i = 0; i < 10242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10242(int& val) {
    const int x = 10242;
    std::cout << 10242;
    for (int i = 0; i < 10242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10243(int& val) {
    func_in_10243(val);
    const int x = 10243;
    std::cout << 10243;
    for (int i = 0; i < 10243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10243(int& val) {
    const int x = 10243;
    std::cout << 10243;
    for (int i = 0; i < 10243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10244(int& val) {
    func_in_10244(val);
    const int x = 10244;
    std::cout << 10244;
    for (int i = 0; i < 10244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10244(int& val) {
    const int x = 10244;
    std::cout << 10244;
    for (int i = 0; i < 10244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10245(int& val) {
    func_in_10245(val);
    const int x = 10245;
    std::cout << 10245;
    for (int i = 0; i < 10245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10245(int& val) {
    const int x = 10245;
    std::cout << 10245;
    for (int i = 0; i < 10245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10246(int& val) {
    func_in_10246(val);
    const int x = 10246;
    std::cout << 10246;
    for (int i = 0; i < 10246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10246(int& val) {
    const int x = 10246;
    std::cout << 10246;
    for (int i = 0; i < 10246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10247(int& val) {
    func_in_10247(val);
    const int x = 10247;
    std::cout << 10247;
    for (int i = 0; i < 10247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10247(int& val) {
    const int x = 10247;
    std::cout << 10247;
    for (int i = 0; i < 10247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10248(int& val) {
    func_in_10248(val);
    const int x = 10248;
    std::cout << 10248;
    for (int i = 0; i < 10248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10248(int& val) {
    const int x = 10248;
    std::cout << 10248;
    for (int i = 0; i < 10248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10249(int& val) {
    func_in_10249(val);
    const int x = 10249;
    std::cout << 10249;
    for (int i = 0; i < 10249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10249(int& val) {
    const int x = 10249;
    std::cout << 10249;
    for (int i = 0; i < 10249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10250(int& val) {
    func_in_10250(val);
    const int x = 10250;
    std::cout << 10250;
    for (int i = 0; i < 10250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10250(int& val) {
    const int x = 10250;
    std::cout << 10250;
    for (int i = 0; i < 10250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10251(int& val) {
    func_in_10251(val);
    const int x = 10251;
    std::cout << 10251;
    for (int i = 0; i < 10251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10251(int& val) {
    const int x = 10251;
    std::cout << 10251;
    for (int i = 0; i < 10251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10252(int& val) {
    func_in_10252(val);
    const int x = 10252;
    std::cout << 10252;
    for (int i = 0; i < 10252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10252(int& val) {
    const int x = 10252;
    std::cout << 10252;
    for (int i = 0; i < 10252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10253(int& val) {
    func_in_10253(val);
    const int x = 10253;
    std::cout << 10253;
    for (int i = 0; i < 10253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10253(int& val) {
    const int x = 10253;
    std::cout << 10253;
    for (int i = 0; i < 10253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10254(int& val) {
    func_in_10254(val);
    const int x = 10254;
    std::cout << 10254;
    for (int i = 0; i < 10254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10254(int& val) {
    const int x = 10254;
    std::cout << 10254;
    for (int i = 0; i < 10254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10255(int& val) {
    func_in_10255(val);
    const int x = 10255;
    std::cout << 10255;
    for (int i = 0; i < 10255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10255(int& val) {
    const int x = 10255;
    std::cout << 10255;
    for (int i = 0; i < 10255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10256(int& val) {
    func_in_10256(val);
    const int x = 10256;
    std::cout << 10256;
    for (int i = 0; i < 10256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10256(int& val) {
    const int x = 10256;
    std::cout << 10256;
    for (int i = 0; i < 10256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10257(int& val) {
    func_in_10257(val);
    const int x = 10257;
    std::cout << 10257;
    for (int i = 0; i < 10257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10257(int& val) {
    const int x = 10257;
    std::cout << 10257;
    for (int i = 0; i < 10257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10258(int& val) {
    func_in_10258(val);
    const int x = 10258;
    std::cout << 10258;
    for (int i = 0; i < 10258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10258(int& val) {
    const int x = 10258;
    std::cout << 10258;
    for (int i = 0; i < 10258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10259(int& val) {
    func_in_10259(val);
    const int x = 10259;
    std::cout << 10259;
    for (int i = 0; i < 10259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10259(int& val) {
    const int x = 10259;
    std::cout << 10259;
    for (int i = 0; i < 10259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10260(int& val) {
    func_in_10260(val);
    const int x = 10260;
    std::cout << 10260;
    for (int i = 0; i < 10260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10260(int& val) {
    const int x = 10260;
    std::cout << 10260;
    for (int i = 0; i < 10260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10261(int& val) {
    func_in_10261(val);
    const int x = 10261;
    std::cout << 10261;
    for (int i = 0; i < 10261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10261(int& val) {
    const int x = 10261;
    std::cout << 10261;
    for (int i = 0; i < 10261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10262(int& val) {
    func_in_10262(val);
    const int x = 10262;
    std::cout << 10262;
    for (int i = 0; i < 10262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10262(int& val) {
    const int x = 10262;
    std::cout << 10262;
    for (int i = 0; i < 10262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10263(int& val) {
    func_in_10263(val);
    const int x = 10263;
    std::cout << 10263;
    for (int i = 0; i < 10263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10263(int& val) {
    const int x = 10263;
    std::cout << 10263;
    for (int i = 0; i < 10263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10264(int& val) {
    func_in_10264(val);
    const int x = 10264;
    std::cout << 10264;
    for (int i = 0; i < 10264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10264(int& val) {
    const int x = 10264;
    std::cout << 10264;
    for (int i = 0; i < 10264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10265(int& val) {
    func_in_10265(val);
    const int x = 10265;
    std::cout << 10265;
    for (int i = 0; i < 10265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10265(int& val) {
    const int x = 10265;
    std::cout << 10265;
    for (int i = 0; i < 10265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10266(int& val) {
    func_in_10266(val);
    const int x = 10266;
    std::cout << 10266;
    for (int i = 0; i < 10266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10266(int& val) {
    const int x = 10266;
    std::cout << 10266;
    for (int i = 0; i < 10266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10267(int& val) {
    func_in_10267(val);
    const int x = 10267;
    std::cout << 10267;
    for (int i = 0; i < 10267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10267(int& val) {
    const int x = 10267;
    std::cout << 10267;
    for (int i = 0; i < 10267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10268(int& val) {
    func_in_10268(val);
    const int x = 10268;
    std::cout << 10268;
    for (int i = 0; i < 10268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10268(int& val) {
    const int x = 10268;
    std::cout << 10268;
    for (int i = 0; i < 10268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10269(int& val) {
    func_in_10269(val);
    const int x = 10269;
    std::cout << 10269;
    for (int i = 0; i < 10269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10269(int& val) {
    const int x = 10269;
    std::cout << 10269;
    for (int i = 0; i < 10269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10270(int& val) {
    func_in_10270(val);
    const int x = 10270;
    std::cout << 10270;
    for (int i = 0; i < 10270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10270(int& val) {
    const int x = 10270;
    std::cout << 10270;
    for (int i = 0; i < 10270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10271(int& val) {
    func_in_10271(val);
    const int x = 10271;
    std::cout << 10271;
    for (int i = 0; i < 10271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10271(int& val) {
    const int x = 10271;
    std::cout << 10271;
    for (int i = 0; i < 10271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10272(int& val) {
    func_in_10272(val);
    const int x = 10272;
    std::cout << 10272;
    for (int i = 0; i < 10272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10272(int& val) {
    const int x = 10272;
    std::cout << 10272;
    for (int i = 0; i < 10272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10273(int& val) {
    func_in_10273(val);
    const int x = 10273;
    std::cout << 10273;
    for (int i = 0; i < 10273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10273(int& val) {
    const int x = 10273;
    std::cout << 10273;
    for (int i = 0; i < 10273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10274(int& val) {
    func_in_10274(val);
    const int x = 10274;
    std::cout << 10274;
    for (int i = 0; i < 10274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10274(int& val) {
    const int x = 10274;
    std::cout << 10274;
    for (int i = 0; i < 10274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10275(int& val) {
    func_in_10275(val);
    const int x = 10275;
    std::cout << 10275;
    for (int i = 0; i < 10275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10275(int& val) {
    const int x = 10275;
    std::cout << 10275;
    for (int i = 0; i < 10275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10276(int& val) {
    func_in_10276(val);
    const int x = 10276;
    std::cout << 10276;
    for (int i = 0; i < 10276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10276(int& val) {
    const int x = 10276;
    std::cout << 10276;
    for (int i = 0; i < 10276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10277(int& val) {
    func_in_10277(val);
    const int x = 10277;
    std::cout << 10277;
    for (int i = 0; i < 10277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10277(int& val) {
    const int x = 10277;
    std::cout << 10277;
    for (int i = 0; i < 10277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10278(int& val) {
    func_in_10278(val);
    const int x = 10278;
    std::cout << 10278;
    for (int i = 0; i < 10278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10278(int& val) {
    const int x = 10278;
    std::cout << 10278;
    for (int i = 0; i < 10278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10279(int& val) {
    func_in_10279(val);
    const int x = 10279;
    std::cout << 10279;
    for (int i = 0; i < 10279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10279(int& val) {
    const int x = 10279;
    std::cout << 10279;
    for (int i = 0; i < 10279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10280(int& val) {
    func_in_10280(val);
    const int x = 10280;
    std::cout << 10280;
    for (int i = 0; i < 10280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10280(int& val) {
    const int x = 10280;
    std::cout << 10280;
    for (int i = 0; i < 10280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10281(int& val) {
    func_in_10281(val);
    const int x = 10281;
    std::cout << 10281;
    for (int i = 0; i < 10281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10281(int& val) {
    const int x = 10281;
    std::cout << 10281;
    for (int i = 0; i < 10281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10282(int& val) {
    func_in_10282(val);
    const int x = 10282;
    std::cout << 10282;
    for (int i = 0; i < 10282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10282(int& val) {
    const int x = 10282;
    std::cout << 10282;
    for (int i = 0; i < 10282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10283(int& val) {
    func_in_10283(val);
    const int x = 10283;
    std::cout << 10283;
    for (int i = 0; i < 10283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10283(int& val) {
    const int x = 10283;
    std::cout << 10283;
    for (int i = 0; i < 10283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10284(int& val) {
    func_in_10284(val);
    const int x = 10284;
    std::cout << 10284;
    for (int i = 0; i < 10284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10284(int& val) {
    const int x = 10284;
    std::cout << 10284;
    for (int i = 0; i < 10284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10285(int& val) {
    func_in_10285(val);
    const int x = 10285;
    std::cout << 10285;
    for (int i = 0; i < 10285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10285(int& val) {
    const int x = 10285;
    std::cout << 10285;
    for (int i = 0; i < 10285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10286(int& val) {
    func_in_10286(val);
    const int x = 10286;
    std::cout << 10286;
    for (int i = 0; i < 10286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10286(int& val) {
    const int x = 10286;
    std::cout << 10286;
    for (int i = 0; i < 10286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10287(int& val) {
    func_in_10287(val);
    const int x = 10287;
    std::cout << 10287;
    for (int i = 0; i < 10287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10287(int& val) {
    const int x = 10287;
    std::cout << 10287;
    for (int i = 0; i < 10287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10288(int& val) {
    func_in_10288(val);
    const int x = 10288;
    std::cout << 10288;
    for (int i = 0; i < 10288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10288(int& val) {
    const int x = 10288;
    std::cout << 10288;
    for (int i = 0; i < 10288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10289(int& val) {
    func_in_10289(val);
    const int x = 10289;
    std::cout << 10289;
    for (int i = 0; i < 10289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10289(int& val) {
    const int x = 10289;
    std::cout << 10289;
    for (int i = 0; i < 10289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10290(int& val) {
    func_in_10290(val);
    const int x = 10290;
    std::cout << 10290;
    for (int i = 0; i < 10290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10290(int& val) {
    const int x = 10290;
    std::cout << 10290;
    for (int i = 0; i < 10290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10291(int& val) {
    func_in_10291(val);
    const int x = 10291;
    std::cout << 10291;
    for (int i = 0; i < 10291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10291(int& val) {
    const int x = 10291;
    std::cout << 10291;
    for (int i = 0; i < 10291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10292(int& val) {
    func_in_10292(val);
    const int x = 10292;
    std::cout << 10292;
    for (int i = 0; i < 10292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10292(int& val) {
    const int x = 10292;
    std::cout << 10292;
    for (int i = 0; i < 10292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10293(int& val) {
    func_in_10293(val);
    const int x = 10293;
    std::cout << 10293;
    for (int i = 0; i < 10293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10293(int& val) {
    const int x = 10293;
    std::cout << 10293;
    for (int i = 0; i < 10293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10294(int& val) {
    func_in_10294(val);
    const int x = 10294;
    std::cout << 10294;
    for (int i = 0; i < 10294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10294(int& val) {
    const int x = 10294;
    std::cout << 10294;
    for (int i = 0; i < 10294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10295(int& val) {
    func_in_10295(val);
    const int x = 10295;
    std::cout << 10295;
    for (int i = 0; i < 10295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10295(int& val) {
    const int x = 10295;
    std::cout << 10295;
    for (int i = 0; i < 10295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10296(int& val) {
    func_in_10296(val);
    const int x = 10296;
    std::cout << 10296;
    for (int i = 0; i < 10296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10296(int& val) {
    const int x = 10296;
    std::cout << 10296;
    for (int i = 0; i < 10296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10297(int& val) {
    func_in_10297(val);
    const int x = 10297;
    std::cout << 10297;
    for (int i = 0; i < 10297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10297(int& val) {
    const int x = 10297;
    std::cout << 10297;
    for (int i = 0; i < 10297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10298(int& val) {
    func_in_10298(val);
    const int x = 10298;
    std::cout << 10298;
    for (int i = 0; i < 10298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10298(int& val) {
    const int x = 10298;
    std::cout << 10298;
    for (int i = 0; i < 10298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10299(int& val) {
    func_in_10299(val);
    const int x = 10299;
    std::cout << 10299;
    for (int i = 0; i < 10299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10299(int& val) {
    const int x = 10299;
    std::cout << 10299;
    for (int i = 0; i < 10299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10300(int& val) {
    func_in_10300(val);
    const int x = 10300;
    std::cout << 10300;
    for (int i = 0; i < 10300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10300(int& val) {
    const int x = 10300;
    std::cout << 10300;
    for (int i = 0; i < 10300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10301(int& val) {
    func_in_10301(val);
    const int x = 10301;
    std::cout << 10301;
    for (int i = 0; i < 10301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10301(int& val) {
    const int x = 10301;
    std::cout << 10301;
    for (int i = 0; i < 10301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10302(int& val) {
    func_in_10302(val);
    const int x = 10302;
    std::cout << 10302;
    for (int i = 0; i < 10302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10302(int& val) {
    const int x = 10302;
    std::cout << 10302;
    for (int i = 0; i < 10302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10303(int& val) {
    func_in_10303(val);
    const int x = 10303;
    std::cout << 10303;
    for (int i = 0; i < 10303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10303(int& val) {
    const int x = 10303;
    std::cout << 10303;
    for (int i = 0; i < 10303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10304(int& val) {
    func_in_10304(val);
    const int x = 10304;
    std::cout << 10304;
    for (int i = 0; i < 10304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10304(int& val) {
    const int x = 10304;
    std::cout << 10304;
    for (int i = 0; i < 10304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10305(int& val) {
    func_in_10305(val);
    const int x = 10305;
    std::cout << 10305;
    for (int i = 0; i < 10305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10305(int& val) {
    const int x = 10305;
    std::cout << 10305;
    for (int i = 0; i < 10305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10306(int& val) {
    func_in_10306(val);
    const int x = 10306;
    std::cout << 10306;
    for (int i = 0; i < 10306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10306(int& val) {
    const int x = 10306;
    std::cout << 10306;
    for (int i = 0; i < 10306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10307(int& val) {
    func_in_10307(val);
    const int x = 10307;
    std::cout << 10307;
    for (int i = 0; i < 10307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10307(int& val) {
    const int x = 10307;
    std::cout << 10307;
    for (int i = 0; i < 10307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10308(int& val) {
    func_in_10308(val);
    const int x = 10308;
    std::cout << 10308;
    for (int i = 0; i < 10308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10308(int& val) {
    const int x = 10308;
    std::cout << 10308;
    for (int i = 0; i < 10308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10309(int& val) {
    func_in_10309(val);
    const int x = 10309;
    std::cout << 10309;
    for (int i = 0; i < 10309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10309(int& val) {
    const int x = 10309;
    std::cout << 10309;
    for (int i = 0; i < 10309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10310(int& val) {
    func_in_10310(val);
    const int x = 10310;
    std::cout << 10310;
    for (int i = 0; i < 10310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10310(int& val) {
    const int x = 10310;
    std::cout << 10310;
    for (int i = 0; i < 10310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10311(int& val) {
    func_in_10311(val);
    const int x = 10311;
    std::cout << 10311;
    for (int i = 0; i < 10311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10311(int& val) {
    const int x = 10311;
    std::cout << 10311;
    for (int i = 0; i < 10311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10312(int& val) {
    func_in_10312(val);
    const int x = 10312;
    std::cout << 10312;
    for (int i = 0; i < 10312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10312(int& val) {
    const int x = 10312;
    std::cout << 10312;
    for (int i = 0; i < 10312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10313(int& val) {
    func_in_10313(val);
    const int x = 10313;
    std::cout << 10313;
    for (int i = 0; i < 10313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10313(int& val) {
    const int x = 10313;
    std::cout << 10313;
    for (int i = 0; i < 10313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10314(int& val) {
    func_in_10314(val);
    const int x = 10314;
    std::cout << 10314;
    for (int i = 0; i < 10314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10314(int& val) {
    const int x = 10314;
    std::cout << 10314;
    for (int i = 0; i < 10314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10315(int& val) {
    func_in_10315(val);
    const int x = 10315;
    std::cout << 10315;
    for (int i = 0; i < 10315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10315(int& val) {
    const int x = 10315;
    std::cout << 10315;
    for (int i = 0; i < 10315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10316(int& val) {
    func_in_10316(val);
    const int x = 10316;
    std::cout << 10316;
    for (int i = 0; i < 10316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10316(int& val) {
    const int x = 10316;
    std::cout << 10316;
    for (int i = 0; i < 10316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10317(int& val) {
    func_in_10317(val);
    const int x = 10317;
    std::cout << 10317;
    for (int i = 0; i < 10317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10317(int& val) {
    const int x = 10317;
    std::cout << 10317;
    for (int i = 0; i < 10317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10318(int& val) {
    func_in_10318(val);
    const int x = 10318;
    std::cout << 10318;
    for (int i = 0; i < 10318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10318(int& val) {
    const int x = 10318;
    std::cout << 10318;
    for (int i = 0; i < 10318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10319(int& val) {
    func_in_10319(val);
    const int x = 10319;
    std::cout << 10319;
    for (int i = 0; i < 10319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10319(int& val) {
    const int x = 10319;
    std::cout << 10319;
    for (int i = 0; i < 10319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10320(int& val) {
    func_in_10320(val);
    const int x = 10320;
    std::cout << 10320;
    for (int i = 0; i < 10320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10320(int& val) {
    const int x = 10320;
    std::cout << 10320;
    for (int i = 0; i < 10320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10321(int& val) {
    func_in_10321(val);
    const int x = 10321;
    std::cout << 10321;
    for (int i = 0; i < 10321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10321(int& val) {
    const int x = 10321;
    std::cout << 10321;
    for (int i = 0; i < 10321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10322(int& val) {
    func_in_10322(val);
    const int x = 10322;
    std::cout << 10322;
    for (int i = 0; i < 10322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10322(int& val) {
    const int x = 10322;
    std::cout << 10322;
    for (int i = 0; i < 10322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10323(int& val) {
    func_in_10323(val);
    const int x = 10323;
    std::cout << 10323;
    for (int i = 0; i < 10323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10323(int& val) {
    const int x = 10323;
    std::cout << 10323;
    for (int i = 0; i < 10323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10324(int& val) {
    func_in_10324(val);
    const int x = 10324;
    std::cout << 10324;
    for (int i = 0; i < 10324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10324(int& val) {
    const int x = 10324;
    std::cout << 10324;
    for (int i = 0; i < 10324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10325(int& val) {
    func_in_10325(val);
    const int x = 10325;
    std::cout << 10325;
    for (int i = 0; i < 10325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10325(int& val) {
    const int x = 10325;
    std::cout << 10325;
    for (int i = 0; i < 10325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10326(int& val) {
    func_in_10326(val);
    const int x = 10326;
    std::cout << 10326;
    for (int i = 0; i < 10326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10326(int& val) {
    const int x = 10326;
    std::cout << 10326;
    for (int i = 0; i < 10326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10327(int& val) {
    func_in_10327(val);
    const int x = 10327;
    std::cout << 10327;
    for (int i = 0; i < 10327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10327(int& val) {
    const int x = 10327;
    std::cout << 10327;
    for (int i = 0; i < 10327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10328(int& val) {
    func_in_10328(val);
    const int x = 10328;
    std::cout << 10328;
    for (int i = 0; i < 10328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10328(int& val) {
    const int x = 10328;
    std::cout << 10328;
    for (int i = 0; i < 10328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10329(int& val) {
    func_in_10329(val);
    const int x = 10329;
    std::cout << 10329;
    for (int i = 0; i < 10329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10329(int& val) {
    const int x = 10329;
    std::cout << 10329;
    for (int i = 0; i < 10329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10330(int& val) {
    func_in_10330(val);
    const int x = 10330;
    std::cout << 10330;
    for (int i = 0; i < 10330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10330(int& val) {
    const int x = 10330;
    std::cout << 10330;
    for (int i = 0; i < 10330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10331(int& val) {
    func_in_10331(val);
    const int x = 10331;
    std::cout << 10331;
    for (int i = 0; i < 10331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10331(int& val) {
    const int x = 10331;
    std::cout << 10331;
    for (int i = 0; i < 10331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10332(int& val) {
    func_in_10332(val);
    const int x = 10332;
    std::cout << 10332;
    for (int i = 0; i < 10332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10332(int& val) {
    const int x = 10332;
    std::cout << 10332;
    for (int i = 0; i < 10332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10333(int& val) {
    func_in_10333(val);
    const int x = 10333;
    std::cout << 10333;
    for (int i = 0; i < 10333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10333(int& val) {
    const int x = 10333;
    std::cout << 10333;
    for (int i = 0; i < 10333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10334(int& val) {
    func_in_10334(val);
    const int x = 10334;
    std::cout << 10334;
    for (int i = 0; i < 10334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10334(int& val) {
    const int x = 10334;
    std::cout << 10334;
    for (int i = 0; i < 10334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10335(int& val) {
    func_in_10335(val);
    const int x = 10335;
    std::cout << 10335;
    for (int i = 0; i < 10335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10335(int& val) {
    const int x = 10335;
    std::cout << 10335;
    for (int i = 0; i < 10335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10336(int& val) {
    func_in_10336(val);
    const int x = 10336;
    std::cout << 10336;
    for (int i = 0; i < 10336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10336(int& val) {
    const int x = 10336;
    std::cout << 10336;
    for (int i = 0; i < 10336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10337(int& val) {
    func_in_10337(val);
    const int x = 10337;
    std::cout << 10337;
    for (int i = 0; i < 10337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10337(int& val) {
    const int x = 10337;
    std::cout << 10337;
    for (int i = 0; i < 10337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10338(int& val) {
    func_in_10338(val);
    const int x = 10338;
    std::cout << 10338;
    for (int i = 0; i < 10338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10338(int& val) {
    const int x = 10338;
    std::cout << 10338;
    for (int i = 0; i < 10338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10339(int& val) {
    func_in_10339(val);
    const int x = 10339;
    std::cout << 10339;
    for (int i = 0; i < 10339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10339(int& val) {
    const int x = 10339;
    std::cout << 10339;
    for (int i = 0; i < 10339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10340(int& val) {
    func_in_10340(val);
    const int x = 10340;
    std::cout << 10340;
    for (int i = 0; i < 10340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10340(int& val) {
    const int x = 10340;
    std::cout << 10340;
    for (int i = 0; i < 10340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10341(int& val) {
    func_in_10341(val);
    const int x = 10341;
    std::cout << 10341;
    for (int i = 0; i < 10341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10341(int& val) {
    const int x = 10341;
    std::cout << 10341;
    for (int i = 0; i < 10341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10342(int& val) {
    func_in_10342(val);
    const int x = 10342;
    std::cout << 10342;
    for (int i = 0; i < 10342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10342(int& val) {
    const int x = 10342;
    std::cout << 10342;
    for (int i = 0; i < 10342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10343(int& val) {
    func_in_10343(val);
    const int x = 10343;
    std::cout << 10343;
    for (int i = 0; i < 10343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10343(int& val) {
    const int x = 10343;
    std::cout << 10343;
    for (int i = 0; i < 10343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10344(int& val) {
    func_in_10344(val);
    const int x = 10344;
    std::cout << 10344;
    for (int i = 0; i < 10344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10344(int& val) {
    const int x = 10344;
    std::cout << 10344;
    for (int i = 0; i < 10344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10345(int& val) {
    func_in_10345(val);
    const int x = 10345;
    std::cout << 10345;
    for (int i = 0; i < 10345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10345(int& val) {
    const int x = 10345;
    std::cout << 10345;
    for (int i = 0; i < 10345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10346(int& val) {
    func_in_10346(val);
    const int x = 10346;
    std::cout << 10346;
    for (int i = 0; i < 10346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10346(int& val) {
    const int x = 10346;
    std::cout << 10346;
    for (int i = 0; i < 10346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10347(int& val) {
    func_in_10347(val);
    const int x = 10347;
    std::cout << 10347;
    for (int i = 0; i < 10347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10347(int& val) {
    const int x = 10347;
    std::cout << 10347;
    for (int i = 0; i < 10347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10348(int& val) {
    func_in_10348(val);
    const int x = 10348;
    std::cout << 10348;
    for (int i = 0; i < 10348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10348(int& val) {
    const int x = 10348;
    std::cout << 10348;
    for (int i = 0; i < 10348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10349(int& val) {
    func_in_10349(val);
    const int x = 10349;
    std::cout << 10349;
    for (int i = 0; i < 10349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10349(int& val) {
    const int x = 10349;
    std::cout << 10349;
    for (int i = 0; i < 10349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10350(int& val) {
    func_in_10350(val);
    const int x = 10350;
    std::cout << 10350;
    for (int i = 0; i < 10350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10350(int& val) {
    const int x = 10350;
    std::cout << 10350;
    for (int i = 0; i < 10350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10351(int& val) {
    func_in_10351(val);
    const int x = 10351;
    std::cout << 10351;
    for (int i = 0; i < 10351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10351(int& val) {
    const int x = 10351;
    std::cout << 10351;
    for (int i = 0; i < 10351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10352(int& val) {
    func_in_10352(val);
    const int x = 10352;
    std::cout << 10352;
    for (int i = 0; i < 10352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10352(int& val) {
    const int x = 10352;
    std::cout << 10352;
    for (int i = 0; i < 10352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10353(int& val) {
    func_in_10353(val);
    const int x = 10353;
    std::cout << 10353;
    for (int i = 0; i < 10353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10353(int& val) {
    const int x = 10353;
    std::cout << 10353;
    for (int i = 0; i < 10353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10354(int& val) {
    func_in_10354(val);
    const int x = 10354;
    std::cout << 10354;
    for (int i = 0; i < 10354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10354(int& val) {
    const int x = 10354;
    std::cout << 10354;
    for (int i = 0; i < 10354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10355(int& val) {
    func_in_10355(val);
    const int x = 10355;
    std::cout << 10355;
    for (int i = 0; i < 10355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10355(int& val) {
    const int x = 10355;
    std::cout << 10355;
    for (int i = 0; i < 10355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10356(int& val) {
    func_in_10356(val);
    const int x = 10356;
    std::cout << 10356;
    for (int i = 0; i < 10356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10356(int& val) {
    const int x = 10356;
    std::cout << 10356;
    for (int i = 0; i < 10356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10357(int& val) {
    func_in_10357(val);
    const int x = 10357;
    std::cout << 10357;
    for (int i = 0; i < 10357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10357(int& val) {
    const int x = 10357;
    std::cout << 10357;
    for (int i = 0; i < 10357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10358(int& val) {
    func_in_10358(val);
    const int x = 10358;
    std::cout << 10358;
    for (int i = 0; i < 10358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10358(int& val) {
    const int x = 10358;
    std::cout << 10358;
    for (int i = 0; i < 10358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10359(int& val) {
    func_in_10359(val);
    const int x = 10359;
    std::cout << 10359;
    for (int i = 0; i < 10359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10359(int& val) {
    const int x = 10359;
    std::cout << 10359;
    for (int i = 0; i < 10359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10360(int& val) {
    func_in_10360(val);
    const int x = 10360;
    std::cout << 10360;
    for (int i = 0; i < 10360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10360(int& val) {
    const int x = 10360;
    std::cout << 10360;
    for (int i = 0; i < 10360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10361(int& val) {
    func_in_10361(val);
    const int x = 10361;
    std::cout << 10361;
    for (int i = 0; i < 10361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10361(int& val) {
    const int x = 10361;
    std::cout << 10361;
    for (int i = 0; i < 10361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10362(int& val) {
    func_in_10362(val);
    const int x = 10362;
    std::cout << 10362;
    for (int i = 0; i < 10362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10362(int& val) {
    const int x = 10362;
    std::cout << 10362;
    for (int i = 0; i < 10362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10363(int& val) {
    func_in_10363(val);
    const int x = 10363;
    std::cout << 10363;
    for (int i = 0; i < 10363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10363(int& val) {
    const int x = 10363;
    std::cout << 10363;
    for (int i = 0; i < 10363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10364(int& val) {
    func_in_10364(val);
    const int x = 10364;
    std::cout << 10364;
    for (int i = 0; i < 10364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10364(int& val) {
    const int x = 10364;
    std::cout << 10364;
    for (int i = 0; i < 10364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10365(int& val) {
    func_in_10365(val);
    const int x = 10365;
    std::cout << 10365;
    for (int i = 0; i < 10365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10365(int& val) {
    const int x = 10365;
    std::cout << 10365;
    for (int i = 0; i < 10365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10366(int& val) {
    func_in_10366(val);
    const int x = 10366;
    std::cout << 10366;
    for (int i = 0; i < 10366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10366(int& val) {
    const int x = 10366;
    std::cout << 10366;
    for (int i = 0; i < 10366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10367(int& val) {
    func_in_10367(val);
    const int x = 10367;
    std::cout << 10367;
    for (int i = 0; i < 10367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10367(int& val) {
    const int x = 10367;
    std::cout << 10367;
    for (int i = 0; i < 10367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10368(int& val) {
    func_in_10368(val);
    const int x = 10368;
    std::cout << 10368;
    for (int i = 0; i < 10368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10368(int& val) {
    const int x = 10368;
    std::cout << 10368;
    for (int i = 0; i < 10368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10369(int& val) {
    func_in_10369(val);
    const int x = 10369;
    std::cout << 10369;
    for (int i = 0; i < 10369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10369(int& val) {
    const int x = 10369;
    std::cout << 10369;
    for (int i = 0; i < 10369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10370(int& val) {
    func_in_10370(val);
    const int x = 10370;
    std::cout << 10370;
    for (int i = 0; i < 10370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10370(int& val) {
    const int x = 10370;
    std::cout << 10370;
    for (int i = 0; i < 10370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10371(int& val) {
    func_in_10371(val);
    const int x = 10371;
    std::cout << 10371;
    for (int i = 0; i < 10371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10371(int& val) {
    const int x = 10371;
    std::cout << 10371;
    for (int i = 0; i < 10371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10372(int& val) {
    func_in_10372(val);
    const int x = 10372;
    std::cout << 10372;
    for (int i = 0; i < 10372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10372(int& val) {
    const int x = 10372;
    std::cout << 10372;
    for (int i = 0; i < 10372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10373(int& val) {
    func_in_10373(val);
    const int x = 10373;
    std::cout << 10373;
    for (int i = 0; i < 10373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10373(int& val) {
    const int x = 10373;
    std::cout << 10373;
    for (int i = 0; i < 10373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10374(int& val) {
    func_in_10374(val);
    const int x = 10374;
    std::cout << 10374;
    for (int i = 0; i < 10374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10374(int& val) {
    const int x = 10374;
    std::cout << 10374;
    for (int i = 0; i < 10374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10375(int& val) {
    func_in_10375(val);
    const int x = 10375;
    std::cout << 10375;
    for (int i = 0; i < 10375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10375(int& val) {
    const int x = 10375;
    std::cout << 10375;
    for (int i = 0; i < 10375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10376(int& val) {
    func_in_10376(val);
    const int x = 10376;
    std::cout << 10376;
    for (int i = 0; i < 10376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10376(int& val) {
    const int x = 10376;
    std::cout << 10376;
    for (int i = 0; i < 10376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10377(int& val) {
    func_in_10377(val);
    const int x = 10377;
    std::cout << 10377;
    for (int i = 0; i < 10377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10377(int& val) {
    const int x = 10377;
    std::cout << 10377;
    for (int i = 0; i < 10377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10378(int& val) {
    func_in_10378(val);
    const int x = 10378;
    std::cout << 10378;
    for (int i = 0; i < 10378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10378(int& val) {
    const int x = 10378;
    std::cout << 10378;
    for (int i = 0; i < 10378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10379(int& val) {
    func_in_10379(val);
    const int x = 10379;
    std::cout << 10379;
    for (int i = 0; i < 10379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10379(int& val) {
    const int x = 10379;
    std::cout << 10379;
    for (int i = 0; i < 10379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10380(int& val) {
    func_in_10380(val);
    const int x = 10380;
    std::cout << 10380;
    for (int i = 0; i < 10380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10380(int& val) {
    const int x = 10380;
    std::cout << 10380;
    for (int i = 0; i < 10380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10381(int& val) {
    func_in_10381(val);
    const int x = 10381;
    std::cout << 10381;
    for (int i = 0; i < 10381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10381(int& val) {
    const int x = 10381;
    std::cout << 10381;
    for (int i = 0; i < 10381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10382(int& val) {
    func_in_10382(val);
    const int x = 10382;
    std::cout << 10382;
    for (int i = 0; i < 10382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10382(int& val) {
    const int x = 10382;
    std::cout << 10382;
    for (int i = 0; i < 10382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10383(int& val) {
    func_in_10383(val);
    const int x = 10383;
    std::cout << 10383;
    for (int i = 0; i < 10383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10383(int& val) {
    const int x = 10383;
    std::cout << 10383;
    for (int i = 0; i < 10383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10384(int& val) {
    func_in_10384(val);
    const int x = 10384;
    std::cout << 10384;
    for (int i = 0; i < 10384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10384(int& val) {
    const int x = 10384;
    std::cout << 10384;
    for (int i = 0; i < 10384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10385(int& val) {
    func_in_10385(val);
    const int x = 10385;
    std::cout << 10385;
    for (int i = 0; i < 10385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10385(int& val) {
    const int x = 10385;
    std::cout << 10385;
    for (int i = 0; i < 10385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10386(int& val) {
    func_in_10386(val);
    const int x = 10386;
    std::cout << 10386;
    for (int i = 0; i < 10386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10386(int& val) {
    const int x = 10386;
    std::cout << 10386;
    for (int i = 0; i < 10386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10387(int& val) {
    func_in_10387(val);
    const int x = 10387;
    std::cout << 10387;
    for (int i = 0; i < 10387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10387(int& val) {
    const int x = 10387;
    std::cout << 10387;
    for (int i = 0; i < 10387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10388(int& val) {
    func_in_10388(val);
    const int x = 10388;
    std::cout << 10388;
    for (int i = 0; i < 10388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10388(int& val) {
    const int x = 10388;
    std::cout << 10388;
    for (int i = 0; i < 10388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10389(int& val) {
    func_in_10389(val);
    const int x = 10389;
    std::cout << 10389;
    for (int i = 0; i < 10389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10389(int& val) {
    const int x = 10389;
    std::cout << 10389;
    for (int i = 0; i < 10389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10390(int& val) {
    func_in_10390(val);
    const int x = 10390;
    std::cout << 10390;
    for (int i = 0; i < 10390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10390(int& val) {
    const int x = 10390;
    std::cout << 10390;
    for (int i = 0; i < 10390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10391(int& val) {
    func_in_10391(val);
    const int x = 10391;
    std::cout << 10391;
    for (int i = 0; i < 10391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10391(int& val) {
    const int x = 10391;
    std::cout << 10391;
    for (int i = 0; i < 10391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10392(int& val) {
    func_in_10392(val);
    const int x = 10392;
    std::cout << 10392;
    for (int i = 0; i < 10392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10392(int& val) {
    const int x = 10392;
    std::cout << 10392;
    for (int i = 0; i < 10392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10393(int& val) {
    func_in_10393(val);
    const int x = 10393;
    std::cout << 10393;
    for (int i = 0; i < 10393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10393(int& val) {
    const int x = 10393;
    std::cout << 10393;
    for (int i = 0; i < 10393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10394(int& val) {
    func_in_10394(val);
    const int x = 10394;
    std::cout << 10394;
    for (int i = 0; i < 10394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10394(int& val) {
    const int x = 10394;
    std::cout << 10394;
    for (int i = 0; i < 10394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10395(int& val) {
    func_in_10395(val);
    const int x = 10395;
    std::cout << 10395;
    for (int i = 0; i < 10395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10395(int& val) {
    const int x = 10395;
    std::cout << 10395;
    for (int i = 0; i < 10395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10396(int& val) {
    func_in_10396(val);
    const int x = 10396;
    std::cout << 10396;
    for (int i = 0; i < 10396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10396(int& val) {
    const int x = 10396;
    std::cout << 10396;
    for (int i = 0; i < 10396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10397(int& val) {
    func_in_10397(val);
    const int x = 10397;
    std::cout << 10397;
    for (int i = 0; i < 10397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10397(int& val) {
    const int x = 10397;
    std::cout << 10397;
    for (int i = 0; i < 10397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10398(int& val) {
    func_in_10398(val);
    const int x = 10398;
    std::cout << 10398;
    for (int i = 0; i < 10398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10398(int& val) {
    const int x = 10398;
    std::cout << 10398;
    for (int i = 0; i < 10398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10399(int& val) {
    func_in_10399(val);
    const int x = 10399;
    std::cout << 10399;
    for (int i = 0; i < 10399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10399(int& val) {
    const int x = 10399;
    std::cout << 10399;
    for (int i = 0; i < 10399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10400(int& val) {
    func_in_10400(val);
    const int x = 10400;
    std::cout << 10400;
    for (int i = 0; i < 10400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10400(int& val) {
    const int x = 10400;
    std::cout << 10400;
    for (int i = 0; i < 10400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10401(int& val) {
    func_in_10401(val);
    const int x = 10401;
    std::cout << 10401;
    for (int i = 0; i < 10401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10401(int& val) {
    const int x = 10401;
    std::cout << 10401;
    for (int i = 0; i < 10401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10402(int& val) {
    func_in_10402(val);
    const int x = 10402;
    std::cout << 10402;
    for (int i = 0; i < 10402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10402(int& val) {
    const int x = 10402;
    std::cout << 10402;
    for (int i = 0; i < 10402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10403(int& val) {
    func_in_10403(val);
    const int x = 10403;
    std::cout << 10403;
    for (int i = 0; i < 10403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10403(int& val) {
    const int x = 10403;
    std::cout << 10403;
    for (int i = 0; i < 10403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10404(int& val) {
    func_in_10404(val);
    const int x = 10404;
    std::cout << 10404;
    for (int i = 0; i < 10404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10404(int& val) {
    const int x = 10404;
    std::cout << 10404;
    for (int i = 0; i < 10404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10405(int& val) {
    func_in_10405(val);
    const int x = 10405;
    std::cout << 10405;
    for (int i = 0; i < 10405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10405(int& val) {
    const int x = 10405;
    std::cout << 10405;
    for (int i = 0; i < 10405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10406(int& val) {
    func_in_10406(val);
    const int x = 10406;
    std::cout << 10406;
    for (int i = 0; i < 10406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10406(int& val) {
    const int x = 10406;
    std::cout << 10406;
    for (int i = 0; i < 10406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10407(int& val) {
    func_in_10407(val);
    const int x = 10407;
    std::cout << 10407;
    for (int i = 0; i < 10407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10407(int& val) {
    const int x = 10407;
    std::cout << 10407;
    for (int i = 0; i < 10407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10408(int& val) {
    func_in_10408(val);
    const int x = 10408;
    std::cout << 10408;
    for (int i = 0; i < 10408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10408(int& val) {
    const int x = 10408;
    std::cout << 10408;
    for (int i = 0; i < 10408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10409(int& val) {
    func_in_10409(val);
    const int x = 10409;
    std::cout << 10409;
    for (int i = 0; i < 10409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10409(int& val) {
    const int x = 10409;
    std::cout << 10409;
    for (int i = 0; i < 10409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10410(int& val) {
    func_in_10410(val);
    const int x = 10410;
    std::cout << 10410;
    for (int i = 0; i < 10410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10410(int& val) {
    const int x = 10410;
    std::cout << 10410;
    for (int i = 0; i < 10410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10411(int& val) {
    func_in_10411(val);
    const int x = 10411;
    std::cout << 10411;
    for (int i = 0; i < 10411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10411(int& val) {
    const int x = 10411;
    std::cout << 10411;
    for (int i = 0; i < 10411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10412(int& val) {
    func_in_10412(val);
    const int x = 10412;
    std::cout << 10412;
    for (int i = 0; i < 10412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10412(int& val) {
    const int x = 10412;
    std::cout << 10412;
    for (int i = 0; i < 10412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10413(int& val) {
    func_in_10413(val);
    const int x = 10413;
    std::cout << 10413;
    for (int i = 0; i < 10413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10413(int& val) {
    const int x = 10413;
    std::cout << 10413;
    for (int i = 0; i < 10413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10414(int& val) {
    func_in_10414(val);
    const int x = 10414;
    std::cout << 10414;
    for (int i = 0; i < 10414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10414(int& val) {
    const int x = 10414;
    std::cout << 10414;
    for (int i = 0; i < 10414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10415(int& val) {
    func_in_10415(val);
    const int x = 10415;
    std::cout << 10415;
    for (int i = 0; i < 10415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10415(int& val) {
    const int x = 10415;
    std::cout << 10415;
    for (int i = 0; i < 10415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10416(int& val) {
    func_in_10416(val);
    const int x = 10416;
    std::cout << 10416;
    for (int i = 0; i < 10416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10416(int& val) {
    const int x = 10416;
    std::cout << 10416;
    for (int i = 0; i < 10416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10417(int& val) {
    func_in_10417(val);
    const int x = 10417;
    std::cout << 10417;
    for (int i = 0; i < 10417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10417(int& val) {
    const int x = 10417;
    std::cout << 10417;
    for (int i = 0; i < 10417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10418(int& val) {
    func_in_10418(val);
    const int x = 10418;
    std::cout << 10418;
    for (int i = 0; i < 10418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10418(int& val) {
    const int x = 10418;
    std::cout << 10418;
    for (int i = 0; i < 10418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10419(int& val) {
    func_in_10419(val);
    const int x = 10419;
    std::cout << 10419;
    for (int i = 0; i < 10419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10419(int& val) {
    const int x = 10419;
    std::cout << 10419;
    for (int i = 0; i < 10419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10420(int& val) {
    func_in_10420(val);
    const int x = 10420;
    std::cout << 10420;
    for (int i = 0; i < 10420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10420(int& val) {
    const int x = 10420;
    std::cout << 10420;
    for (int i = 0; i < 10420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10421(int& val) {
    func_in_10421(val);
    const int x = 10421;
    std::cout << 10421;
    for (int i = 0; i < 10421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10421(int& val) {
    const int x = 10421;
    std::cout << 10421;
    for (int i = 0; i < 10421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10422(int& val) {
    func_in_10422(val);
    const int x = 10422;
    std::cout << 10422;
    for (int i = 0; i < 10422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10422(int& val) {
    const int x = 10422;
    std::cout << 10422;
    for (int i = 0; i < 10422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10423(int& val) {
    func_in_10423(val);
    const int x = 10423;
    std::cout << 10423;
    for (int i = 0; i < 10423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10423(int& val) {
    const int x = 10423;
    std::cout << 10423;
    for (int i = 0; i < 10423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10424(int& val) {
    func_in_10424(val);
    const int x = 10424;
    std::cout << 10424;
    for (int i = 0; i < 10424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10424(int& val) {
    const int x = 10424;
    std::cout << 10424;
    for (int i = 0; i < 10424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10425(int& val) {
    func_in_10425(val);
    const int x = 10425;
    std::cout << 10425;
    for (int i = 0; i < 10425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10425(int& val) {
    const int x = 10425;
    std::cout << 10425;
    for (int i = 0; i < 10425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10426(int& val) {
    func_in_10426(val);
    const int x = 10426;
    std::cout << 10426;
    for (int i = 0; i < 10426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10426(int& val) {
    const int x = 10426;
    std::cout << 10426;
    for (int i = 0; i < 10426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10427(int& val) {
    func_in_10427(val);
    const int x = 10427;
    std::cout << 10427;
    for (int i = 0; i < 10427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10427(int& val) {
    const int x = 10427;
    std::cout << 10427;
    for (int i = 0; i < 10427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10428(int& val) {
    func_in_10428(val);
    const int x = 10428;
    std::cout << 10428;
    for (int i = 0; i < 10428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10428(int& val) {
    const int x = 10428;
    std::cout << 10428;
    for (int i = 0; i < 10428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10429(int& val) {
    func_in_10429(val);
    const int x = 10429;
    std::cout << 10429;
    for (int i = 0; i < 10429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10429(int& val) {
    const int x = 10429;
    std::cout << 10429;
    for (int i = 0; i < 10429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10430(int& val) {
    func_in_10430(val);
    const int x = 10430;
    std::cout << 10430;
    for (int i = 0; i < 10430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10430(int& val) {
    const int x = 10430;
    std::cout << 10430;
    for (int i = 0; i < 10430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10431(int& val) {
    func_in_10431(val);
    const int x = 10431;
    std::cout << 10431;
    for (int i = 0; i < 10431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10431(int& val) {
    const int x = 10431;
    std::cout << 10431;
    for (int i = 0; i < 10431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10432(int& val) {
    func_in_10432(val);
    const int x = 10432;
    std::cout << 10432;
    for (int i = 0; i < 10432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10432(int& val) {
    const int x = 10432;
    std::cout << 10432;
    for (int i = 0; i < 10432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10433(int& val) {
    func_in_10433(val);
    const int x = 10433;
    std::cout << 10433;
    for (int i = 0; i < 10433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10433(int& val) {
    const int x = 10433;
    std::cout << 10433;
    for (int i = 0; i < 10433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10434(int& val) {
    func_in_10434(val);
    const int x = 10434;
    std::cout << 10434;
    for (int i = 0; i < 10434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10434(int& val) {
    const int x = 10434;
    std::cout << 10434;
    for (int i = 0; i < 10434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10435(int& val) {
    func_in_10435(val);
    const int x = 10435;
    std::cout << 10435;
    for (int i = 0; i < 10435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10435(int& val) {
    const int x = 10435;
    std::cout << 10435;
    for (int i = 0; i < 10435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10436(int& val) {
    func_in_10436(val);
    const int x = 10436;
    std::cout << 10436;
    for (int i = 0; i < 10436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10436(int& val) {
    const int x = 10436;
    std::cout << 10436;
    for (int i = 0; i < 10436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10437(int& val) {
    func_in_10437(val);
    const int x = 10437;
    std::cout << 10437;
    for (int i = 0; i < 10437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10437(int& val) {
    const int x = 10437;
    std::cout << 10437;
    for (int i = 0; i < 10437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10438(int& val) {
    func_in_10438(val);
    const int x = 10438;
    std::cout << 10438;
    for (int i = 0; i < 10438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10438(int& val) {
    const int x = 10438;
    std::cout << 10438;
    for (int i = 0; i < 10438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10439(int& val) {
    func_in_10439(val);
    const int x = 10439;
    std::cout << 10439;
    for (int i = 0; i < 10439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10439(int& val) {
    const int x = 10439;
    std::cout << 10439;
    for (int i = 0; i < 10439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10440(int& val) {
    func_in_10440(val);
    const int x = 10440;
    std::cout << 10440;
    for (int i = 0; i < 10440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10440(int& val) {
    const int x = 10440;
    std::cout << 10440;
    for (int i = 0; i < 10440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10441(int& val) {
    func_in_10441(val);
    const int x = 10441;
    std::cout << 10441;
    for (int i = 0; i < 10441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10441(int& val) {
    const int x = 10441;
    std::cout << 10441;
    for (int i = 0; i < 10441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10442(int& val) {
    func_in_10442(val);
    const int x = 10442;
    std::cout << 10442;
    for (int i = 0; i < 10442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10442(int& val) {
    const int x = 10442;
    std::cout << 10442;
    for (int i = 0; i < 10442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10443(int& val) {
    func_in_10443(val);
    const int x = 10443;
    std::cout << 10443;
    for (int i = 0; i < 10443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10443(int& val) {
    const int x = 10443;
    std::cout << 10443;
    for (int i = 0; i < 10443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10444(int& val) {
    func_in_10444(val);
    const int x = 10444;
    std::cout << 10444;
    for (int i = 0; i < 10444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10444(int& val) {
    const int x = 10444;
    std::cout << 10444;
    for (int i = 0; i < 10444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10445(int& val) {
    func_in_10445(val);
    const int x = 10445;
    std::cout << 10445;
    for (int i = 0; i < 10445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10445(int& val) {
    const int x = 10445;
    std::cout << 10445;
    for (int i = 0; i < 10445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10446(int& val) {
    func_in_10446(val);
    const int x = 10446;
    std::cout << 10446;
    for (int i = 0; i < 10446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10446(int& val) {
    const int x = 10446;
    std::cout << 10446;
    for (int i = 0; i < 10446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10447(int& val) {
    func_in_10447(val);
    const int x = 10447;
    std::cout << 10447;
    for (int i = 0; i < 10447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10447(int& val) {
    const int x = 10447;
    std::cout << 10447;
    for (int i = 0; i < 10447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10448(int& val) {
    func_in_10448(val);
    const int x = 10448;
    std::cout << 10448;
    for (int i = 0; i < 10448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10448(int& val) {
    const int x = 10448;
    std::cout << 10448;
    for (int i = 0; i < 10448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10449(int& val) {
    func_in_10449(val);
    const int x = 10449;
    std::cout << 10449;
    for (int i = 0; i < 10449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10449(int& val) {
    const int x = 10449;
    std::cout << 10449;
    for (int i = 0; i < 10449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10450(int& val) {
    func_in_10450(val);
    const int x = 10450;
    std::cout << 10450;
    for (int i = 0; i < 10450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10450(int& val) {
    const int x = 10450;
    std::cout << 10450;
    for (int i = 0; i < 10450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10451(int& val) {
    func_in_10451(val);
    const int x = 10451;
    std::cout << 10451;
    for (int i = 0; i < 10451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10451(int& val) {
    const int x = 10451;
    std::cout << 10451;
    for (int i = 0; i < 10451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10452(int& val) {
    func_in_10452(val);
    const int x = 10452;
    std::cout << 10452;
    for (int i = 0; i < 10452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10452(int& val) {
    const int x = 10452;
    std::cout << 10452;
    for (int i = 0; i < 10452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10453(int& val) {
    func_in_10453(val);
    const int x = 10453;
    std::cout << 10453;
    for (int i = 0; i < 10453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10453(int& val) {
    const int x = 10453;
    std::cout << 10453;
    for (int i = 0; i < 10453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10454(int& val) {
    func_in_10454(val);
    const int x = 10454;
    std::cout << 10454;
    for (int i = 0; i < 10454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10454(int& val) {
    const int x = 10454;
    std::cout << 10454;
    for (int i = 0; i < 10454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10455(int& val) {
    func_in_10455(val);
    const int x = 10455;
    std::cout << 10455;
    for (int i = 0; i < 10455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10455(int& val) {
    const int x = 10455;
    std::cout << 10455;
    for (int i = 0; i < 10455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10456(int& val) {
    func_in_10456(val);
    const int x = 10456;
    std::cout << 10456;
    for (int i = 0; i < 10456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10456(int& val) {
    const int x = 10456;
    std::cout << 10456;
    for (int i = 0; i < 10456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10457(int& val) {
    func_in_10457(val);
    const int x = 10457;
    std::cout << 10457;
    for (int i = 0; i < 10457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10457(int& val) {
    const int x = 10457;
    std::cout << 10457;
    for (int i = 0; i < 10457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10458(int& val) {
    func_in_10458(val);
    const int x = 10458;
    std::cout << 10458;
    for (int i = 0; i < 10458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10458(int& val) {
    const int x = 10458;
    std::cout << 10458;
    for (int i = 0; i < 10458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10459(int& val) {
    func_in_10459(val);
    const int x = 10459;
    std::cout << 10459;
    for (int i = 0; i < 10459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10459(int& val) {
    const int x = 10459;
    std::cout << 10459;
    for (int i = 0; i < 10459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10460(int& val) {
    func_in_10460(val);
    const int x = 10460;
    std::cout << 10460;
    for (int i = 0; i < 10460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10460(int& val) {
    const int x = 10460;
    std::cout << 10460;
    for (int i = 0; i < 10460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10461(int& val) {
    func_in_10461(val);
    const int x = 10461;
    std::cout << 10461;
    for (int i = 0; i < 10461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10461(int& val) {
    const int x = 10461;
    std::cout << 10461;
    for (int i = 0; i < 10461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10462(int& val) {
    func_in_10462(val);
    const int x = 10462;
    std::cout << 10462;
    for (int i = 0; i < 10462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10462(int& val) {
    const int x = 10462;
    std::cout << 10462;
    for (int i = 0; i < 10462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10463(int& val) {
    func_in_10463(val);
    const int x = 10463;
    std::cout << 10463;
    for (int i = 0; i < 10463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10463(int& val) {
    const int x = 10463;
    std::cout << 10463;
    for (int i = 0; i < 10463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10464(int& val) {
    func_in_10464(val);
    const int x = 10464;
    std::cout << 10464;
    for (int i = 0; i < 10464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10464(int& val) {
    const int x = 10464;
    std::cout << 10464;
    for (int i = 0; i < 10464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10465(int& val) {
    func_in_10465(val);
    const int x = 10465;
    std::cout << 10465;
    for (int i = 0; i < 10465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10465(int& val) {
    const int x = 10465;
    std::cout << 10465;
    for (int i = 0; i < 10465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10466(int& val) {
    func_in_10466(val);
    const int x = 10466;
    std::cout << 10466;
    for (int i = 0; i < 10466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10466(int& val) {
    const int x = 10466;
    std::cout << 10466;
    for (int i = 0; i < 10466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10467(int& val) {
    func_in_10467(val);
    const int x = 10467;
    std::cout << 10467;
    for (int i = 0; i < 10467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10467(int& val) {
    const int x = 10467;
    std::cout << 10467;
    for (int i = 0; i < 10467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10468(int& val) {
    func_in_10468(val);
    const int x = 10468;
    std::cout << 10468;
    for (int i = 0; i < 10468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10468(int& val) {
    const int x = 10468;
    std::cout << 10468;
    for (int i = 0; i < 10468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10469(int& val) {
    func_in_10469(val);
    const int x = 10469;
    std::cout << 10469;
    for (int i = 0; i < 10469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10469(int& val) {
    const int x = 10469;
    std::cout << 10469;
    for (int i = 0; i < 10469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10470(int& val) {
    func_in_10470(val);
    const int x = 10470;
    std::cout << 10470;
    for (int i = 0; i < 10470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10470(int& val) {
    const int x = 10470;
    std::cout << 10470;
    for (int i = 0; i < 10470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10471(int& val) {
    func_in_10471(val);
    const int x = 10471;
    std::cout << 10471;
    for (int i = 0; i < 10471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10471(int& val) {
    const int x = 10471;
    std::cout << 10471;
    for (int i = 0; i < 10471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10472(int& val) {
    func_in_10472(val);
    const int x = 10472;
    std::cout << 10472;
    for (int i = 0; i < 10472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10472(int& val) {
    const int x = 10472;
    std::cout << 10472;
    for (int i = 0; i < 10472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10473(int& val) {
    func_in_10473(val);
    const int x = 10473;
    std::cout << 10473;
    for (int i = 0; i < 10473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10473(int& val) {
    const int x = 10473;
    std::cout << 10473;
    for (int i = 0; i < 10473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10474(int& val) {
    func_in_10474(val);
    const int x = 10474;
    std::cout << 10474;
    for (int i = 0; i < 10474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10474(int& val) {
    const int x = 10474;
    std::cout << 10474;
    for (int i = 0; i < 10474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10475(int& val) {
    func_in_10475(val);
    const int x = 10475;
    std::cout << 10475;
    for (int i = 0; i < 10475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10475(int& val) {
    const int x = 10475;
    std::cout << 10475;
    for (int i = 0; i < 10475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10476(int& val) {
    func_in_10476(val);
    const int x = 10476;
    std::cout << 10476;
    for (int i = 0; i < 10476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10476(int& val) {
    const int x = 10476;
    std::cout << 10476;
    for (int i = 0; i < 10476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10477(int& val) {
    func_in_10477(val);
    const int x = 10477;
    std::cout << 10477;
    for (int i = 0; i < 10477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10477(int& val) {
    const int x = 10477;
    std::cout << 10477;
    for (int i = 0; i < 10477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10478(int& val) {
    func_in_10478(val);
    const int x = 10478;
    std::cout << 10478;
    for (int i = 0; i < 10478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10478(int& val) {
    const int x = 10478;
    std::cout << 10478;
    for (int i = 0; i < 10478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10479(int& val) {
    func_in_10479(val);
    const int x = 10479;
    std::cout << 10479;
    for (int i = 0; i < 10479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10479(int& val) {
    const int x = 10479;
    std::cout << 10479;
    for (int i = 0; i < 10479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10480(int& val) {
    func_in_10480(val);
    const int x = 10480;
    std::cout << 10480;
    for (int i = 0; i < 10480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10480(int& val) {
    const int x = 10480;
    std::cout << 10480;
    for (int i = 0; i < 10480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10481(int& val) {
    func_in_10481(val);
    const int x = 10481;
    std::cout << 10481;
    for (int i = 0; i < 10481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10481(int& val) {
    const int x = 10481;
    std::cout << 10481;
    for (int i = 0; i < 10481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10482(int& val) {
    func_in_10482(val);
    const int x = 10482;
    std::cout << 10482;
    for (int i = 0; i < 10482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10482(int& val) {
    const int x = 10482;
    std::cout << 10482;
    for (int i = 0; i < 10482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10483(int& val) {
    func_in_10483(val);
    const int x = 10483;
    std::cout << 10483;
    for (int i = 0; i < 10483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10483(int& val) {
    const int x = 10483;
    std::cout << 10483;
    for (int i = 0; i < 10483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10484(int& val) {
    func_in_10484(val);
    const int x = 10484;
    std::cout << 10484;
    for (int i = 0; i < 10484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10484(int& val) {
    const int x = 10484;
    std::cout << 10484;
    for (int i = 0; i < 10484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10485(int& val) {
    func_in_10485(val);
    const int x = 10485;
    std::cout << 10485;
    for (int i = 0; i < 10485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10485(int& val) {
    const int x = 10485;
    std::cout << 10485;
    for (int i = 0; i < 10485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10486(int& val) {
    func_in_10486(val);
    const int x = 10486;
    std::cout << 10486;
    for (int i = 0; i < 10486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10486(int& val) {
    const int x = 10486;
    std::cout << 10486;
    for (int i = 0; i < 10486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10487(int& val) {
    func_in_10487(val);
    const int x = 10487;
    std::cout << 10487;
    for (int i = 0; i < 10487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10487(int& val) {
    const int x = 10487;
    std::cout << 10487;
    for (int i = 0; i < 10487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10488(int& val) {
    func_in_10488(val);
    const int x = 10488;
    std::cout << 10488;
    for (int i = 0; i < 10488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10488(int& val) {
    const int x = 10488;
    std::cout << 10488;
    for (int i = 0; i < 10488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10489(int& val) {
    func_in_10489(val);
    const int x = 10489;
    std::cout << 10489;
    for (int i = 0; i < 10489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10489(int& val) {
    const int x = 10489;
    std::cout << 10489;
    for (int i = 0; i < 10489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10490(int& val) {
    func_in_10490(val);
    const int x = 10490;
    std::cout << 10490;
    for (int i = 0; i < 10490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10490(int& val) {
    const int x = 10490;
    std::cout << 10490;
    for (int i = 0; i < 10490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10491(int& val) {
    func_in_10491(val);
    const int x = 10491;
    std::cout << 10491;
    for (int i = 0; i < 10491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10491(int& val) {
    const int x = 10491;
    std::cout << 10491;
    for (int i = 0; i < 10491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10492(int& val) {
    func_in_10492(val);
    const int x = 10492;
    std::cout << 10492;
    for (int i = 0; i < 10492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10492(int& val) {
    const int x = 10492;
    std::cout << 10492;
    for (int i = 0; i < 10492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10493(int& val) {
    func_in_10493(val);
    const int x = 10493;
    std::cout << 10493;
    for (int i = 0; i < 10493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10493(int& val) {
    const int x = 10493;
    std::cout << 10493;
    for (int i = 0; i < 10493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10494(int& val) {
    func_in_10494(val);
    const int x = 10494;
    std::cout << 10494;
    for (int i = 0; i < 10494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10494(int& val) {
    const int x = 10494;
    std::cout << 10494;
    for (int i = 0; i < 10494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10495(int& val) {
    func_in_10495(val);
    const int x = 10495;
    std::cout << 10495;
    for (int i = 0; i < 10495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10495(int& val) {
    const int x = 10495;
    std::cout << 10495;
    for (int i = 0; i < 10495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10496(int& val) {
    func_in_10496(val);
    const int x = 10496;
    std::cout << 10496;
    for (int i = 0; i < 10496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10496(int& val) {
    const int x = 10496;
    std::cout << 10496;
    for (int i = 0; i < 10496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10497(int& val) {
    func_in_10497(val);
    const int x = 10497;
    std::cout << 10497;
    for (int i = 0; i < 10497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10497(int& val) {
    const int x = 10497;
    std::cout << 10497;
    for (int i = 0; i < 10497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10498(int& val) {
    func_in_10498(val);
    const int x = 10498;
    std::cout << 10498;
    for (int i = 0; i < 10498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10498(int& val) {
    const int x = 10498;
    std::cout << 10498;
    for (int i = 0; i < 10498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10499(int& val) {
    func_in_10499(val);
    const int x = 10499;
    std::cout << 10499;
    for (int i = 0; i < 10499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10499(int& val) {
    const int x = 10499;
    std::cout << 10499;
    for (int i = 0; i < 10499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10500(int& val) {
    func_in_10500(val);
    const int x = 10500;
    std::cout << 10500;
    for (int i = 0; i < 10500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10500(int& val) {
    const int x = 10500;
    std::cout << 10500;
    for (int i = 0; i < 10500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10501(int& val) {
    func_in_10501(val);
    const int x = 10501;
    std::cout << 10501;
    for (int i = 0; i < 10501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10501(int& val) {
    const int x = 10501;
    std::cout << 10501;
    for (int i = 0; i < 10501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10502(int& val) {
    func_in_10502(val);
    const int x = 10502;
    std::cout << 10502;
    for (int i = 0; i < 10502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10502(int& val) {
    const int x = 10502;
    std::cout << 10502;
    for (int i = 0; i < 10502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10503(int& val) {
    func_in_10503(val);
    const int x = 10503;
    std::cout << 10503;
    for (int i = 0; i < 10503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10503(int& val) {
    const int x = 10503;
    std::cout << 10503;
    for (int i = 0; i < 10503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10504(int& val) {
    func_in_10504(val);
    const int x = 10504;
    std::cout << 10504;
    for (int i = 0; i < 10504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10504(int& val) {
    const int x = 10504;
    std::cout << 10504;
    for (int i = 0; i < 10504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10505(int& val) {
    func_in_10505(val);
    const int x = 10505;
    std::cout << 10505;
    for (int i = 0; i < 10505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10505(int& val) {
    const int x = 10505;
    std::cout << 10505;
    for (int i = 0; i < 10505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10506(int& val) {
    func_in_10506(val);
    const int x = 10506;
    std::cout << 10506;
    for (int i = 0; i < 10506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10506(int& val) {
    const int x = 10506;
    std::cout << 10506;
    for (int i = 0; i < 10506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10507(int& val) {
    func_in_10507(val);
    const int x = 10507;
    std::cout << 10507;
    for (int i = 0; i < 10507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10507(int& val) {
    const int x = 10507;
    std::cout << 10507;
    for (int i = 0; i < 10507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10508(int& val) {
    func_in_10508(val);
    const int x = 10508;
    std::cout << 10508;
    for (int i = 0; i < 10508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10508(int& val) {
    const int x = 10508;
    std::cout << 10508;
    for (int i = 0; i < 10508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10509(int& val) {
    func_in_10509(val);
    const int x = 10509;
    std::cout << 10509;
    for (int i = 0; i < 10509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10509(int& val) {
    const int x = 10509;
    std::cout << 10509;
    for (int i = 0; i < 10509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10510(int& val) {
    func_in_10510(val);
    const int x = 10510;
    std::cout << 10510;
    for (int i = 0; i < 10510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10510(int& val) {
    const int x = 10510;
    std::cout << 10510;
    for (int i = 0; i < 10510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10511(int& val) {
    func_in_10511(val);
    const int x = 10511;
    std::cout << 10511;
    for (int i = 0; i < 10511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10511(int& val) {
    const int x = 10511;
    std::cout << 10511;
    for (int i = 0; i < 10511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10512(int& val) {
    func_in_10512(val);
    const int x = 10512;
    std::cout << 10512;
    for (int i = 0; i < 10512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10512(int& val) {
    const int x = 10512;
    std::cout << 10512;
    for (int i = 0; i < 10512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10513(int& val) {
    func_in_10513(val);
    const int x = 10513;
    std::cout << 10513;
    for (int i = 0; i < 10513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10513(int& val) {
    const int x = 10513;
    std::cout << 10513;
    for (int i = 0; i < 10513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10514(int& val) {
    func_in_10514(val);
    const int x = 10514;
    std::cout << 10514;
    for (int i = 0; i < 10514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10514(int& val) {
    const int x = 10514;
    std::cout << 10514;
    for (int i = 0; i < 10514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10515(int& val) {
    func_in_10515(val);
    const int x = 10515;
    std::cout << 10515;
    for (int i = 0; i < 10515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10515(int& val) {
    const int x = 10515;
    std::cout << 10515;
    for (int i = 0; i < 10515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10516(int& val) {
    func_in_10516(val);
    const int x = 10516;
    std::cout << 10516;
    for (int i = 0; i < 10516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10516(int& val) {
    const int x = 10516;
    std::cout << 10516;
    for (int i = 0; i < 10516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10517(int& val) {
    func_in_10517(val);
    const int x = 10517;
    std::cout << 10517;
    for (int i = 0; i < 10517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10517(int& val) {
    const int x = 10517;
    std::cout << 10517;
    for (int i = 0; i < 10517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10518(int& val) {
    func_in_10518(val);
    const int x = 10518;
    std::cout << 10518;
    for (int i = 0; i < 10518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10518(int& val) {
    const int x = 10518;
    std::cout << 10518;
    for (int i = 0; i < 10518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10519(int& val) {
    func_in_10519(val);
    const int x = 10519;
    std::cout << 10519;
    for (int i = 0; i < 10519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10519(int& val) {
    const int x = 10519;
    std::cout << 10519;
    for (int i = 0; i < 10519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10520(int& val) {
    func_in_10520(val);
    const int x = 10520;
    std::cout << 10520;
    for (int i = 0; i < 10520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10520(int& val) {
    const int x = 10520;
    std::cout << 10520;
    for (int i = 0; i < 10520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10521(int& val) {
    func_in_10521(val);
    const int x = 10521;
    std::cout << 10521;
    for (int i = 0; i < 10521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10521(int& val) {
    const int x = 10521;
    std::cout << 10521;
    for (int i = 0; i < 10521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10522(int& val) {
    func_in_10522(val);
    const int x = 10522;
    std::cout << 10522;
    for (int i = 0; i < 10522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10522(int& val) {
    const int x = 10522;
    std::cout << 10522;
    for (int i = 0; i < 10522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10523(int& val) {
    func_in_10523(val);
    const int x = 10523;
    std::cout << 10523;
    for (int i = 0; i < 10523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10523(int& val) {
    const int x = 10523;
    std::cout << 10523;
    for (int i = 0; i < 10523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10524(int& val) {
    func_in_10524(val);
    const int x = 10524;
    std::cout << 10524;
    for (int i = 0; i < 10524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10524(int& val) {
    const int x = 10524;
    std::cout << 10524;
    for (int i = 0; i < 10524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10525(int& val) {
    func_in_10525(val);
    const int x = 10525;
    std::cout << 10525;
    for (int i = 0; i < 10525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10525(int& val) {
    const int x = 10525;
    std::cout << 10525;
    for (int i = 0; i < 10525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10526(int& val) {
    func_in_10526(val);
    const int x = 10526;
    std::cout << 10526;
    for (int i = 0; i < 10526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10526(int& val) {
    const int x = 10526;
    std::cout << 10526;
    for (int i = 0; i < 10526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10527(int& val) {
    func_in_10527(val);
    const int x = 10527;
    std::cout << 10527;
    for (int i = 0; i < 10527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10527(int& val) {
    const int x = 10527;
    std::cout << 10527;
    for (int i = 0; i < 10527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10528(int& val) {
    func_in_10528(val);
    const int x = 10528;
    std::cout << 10528;
    for (int i = 0; i < 10528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10528(int& val) {
    const int x = 10528;
    std::cout << 10528;
    for (int i = 0; i < 10528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10529(int& val) {
    func_in_10529(val);
    const int x = 10529;
    std::cout << 10529;
    for (int i = 0; i < 10529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10529(int& val) {
    const int x = 10529;
    std::cout << 10529;
    for (int i = 0; i < 10529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10530(int& val) {
    func_in_10530(val);
    const int x = 10530;
    std::cout << 10530;
    for (int i = 0; i < 10530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10530(int& val) {
    const int x = 10530;
    std::cout << 10530;
    for (int i = 0; i < 10530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10531(int& val) {
    func_in_10531(val);
    const int x = 10531;
    std::cout << 10531;
    for (int i = 0; i < 10531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10531(int& val) {
    const int x = 10531;
    std::cout << 10531;
    for (int i = 0; i < 10531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10532(int& val) {
    func_in_10532(val);
    const int x = 10532;
    std::cout << 10532;
    for (int i = 0; i < 10532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10532(int& val) {
    const int x = 10532;
    std::cout << 10532;
    for (int i = 0; i < 10532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10533(int& val) {
    func_in_10533(val);
    const int x = 10533;
    std::cout << 10533;
    for (int i = 0; i < 10533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10533(int& val) {
    const int x = 10533;
    std::cout << 10533;
    for (int i = 0; i < 10533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10534(int& val) {
    func_in_10534(val);
    const int x = 10534;
    std::cout << 10534;
    for (int i = 0; i < 10534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10534(int& val) {
    const int x = 10534;
    std::cout << 10534;
    for (int i = 0; i < 10534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10535(int& val) {
    func_in_10535(val);
    const int x = 10535;
    std::cout << 10535;
    for (int i = 0; i < 10535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10535(int& val) {
    const int x = 10535;
    std::cout << 10535;
    for (int i = 0; i < 10535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10536(int& val) {
    func_in_10536(val);
    const int x = 10536;
    std::cout << 10536;
    for (int i = 0; i < 10536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10536(int& val) {
    const int x = 10536;
    std::cout << 10536;
    for (int i = 0; i < 10536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10537(int& val) {
    func_in_10537(val);
    const int x = 10537;
    std::cout << 10537;
    for (int i = 0; i < 10537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10537(int& val) {
    const int x = 10537;
    std::cout << 10537;
    for (int i = 0; i < 10537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10538(int& val) {
    func_in_10538(val);
    const int x = 10538;
    std::cout << 10538;
    for (int i = 0; i < 10538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10538(int& val) {
    const int x = 10538;
    std::cout << 10538;
    for (int i = 0; i < 10538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10539(int& val) {
    func_in_10539(val);
    const int x = 10539;
    std::cout << 10539;
    for (int i = 0; i < 10539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10539(int& val) {
    const int x = 10539;
    std::cout << 10539;
    for (int i = 0; i < 10539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10540(int& val) {
    func_in_10540(val);
    const int x = 10540;
    std::cout << 10540;
    for (int i = 0; i < 10540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10540(int& val) {
    const int x = 10540;
    std::cout << 10540;
    for (int i = 0; i < 10540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10541(int& val) {
    func_in_10541(val);
    const int x = 10541;
    std::cout << 10541;
    for (int i = 0; i < 10541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10541(int& val) {
    const int x = 10541;
    std::cout << 10541;
    for (int i = 0; i < 10541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10542(int& val) {
    func_in_10542(val);
    const int x = 10542;
    std::cout << 10542;
    for (int i = 0; i < 10542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10542(int& val) {
    const int x = 10542;
    std::cout << 10542;
    for (int i = 0; i < 10542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10543(int& val) {
    func_in_10543(val);
    const int x = 10543;
    std::cout << 10543;
    for (int i = 0; i < 10543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10543(int& val) {
    const int x = 10543;
    std::cout << 10543;
    for (int i = 0; i < 10543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10544(int& val) {
    func_in_10544(val);
    const int x = 10544;
    std::cout << 10544;
    for (int i = 0; i < 10544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10544(int& val) {
    const int x = 10544;
    std::cout << 10544;
    for (int i = 0; i < 10544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10545(int& val) {
    func_in_10545(val);
    const int x = 10545;
    std::cout << 10545;
    for (int i = 0; i < 10545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10545(int& val) {
    const int x = 10545;
    std::cout << 10545;
    for (int i = 0; i < 10545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10546(int& val) {
    func_in_10546(val);
    const int x = 10546;
    std::cout << 10546;
    for (int i = 0; i < 10546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10546(int& val) {
    const int x = 10546;
    std::cout << 10546;
    for (int i = 0; i < 10546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10547(int& val) {
    func_in_10547(val);
    const int x = 10547;
    std::cout << 10547;
    for (int i = 0; i < 10547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10547(int& val) {
    const int x = 10547;
    std::cout << 10547;
    for (int i = 0; i < 10547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10548(int& val) {
    func_in_10548(val);
    const int x = 10548;
    std::cout << 10548;
    for (int i = 0; i < 10548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10548(int& val) {
    const int x = 10548;
    std::cout << 10548;
    for (int i = 0; i < 10548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10549(int& val) {
    func_in_10549(val);
    const int x = 10549;
    std::cout << 10549;
    for (int i = 0; i < 10549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10549(int& val) {
    const int x = 10549;
    std::cout << 10549;
    for (int i = 0; i < 10549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10550(int& val) {
    func_in_10550(val);
    const int x = 10550;
    std::cout << 10550;
    for (int i = 0; i < 10550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10550(int& val) {
    const int x = 10550;
    std::cout << 10550;
    for (int i = 0; i < 10550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10551(int& val) {
    func_in_10551(val);
    const int x = 10551;
    std::cout << 10551;
    for (int i = 0; i < 10551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10551(int& val) {
    const int x = 10551;
    std::cout << 10551;
    for (int i = 0; i < 10551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10552(int& val) {
    func_in_10552(val);
    const int x = 10552;
    std::cout << 10552;
    for (int i = 0; i < 10552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10552(int& val) {
    const int x = 10552;
    std::cout << 10552;
    for (int i = 0; i < 10552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10553(int& val) {
    func_in_10553(val);
    const int x = 10553;
    std::cout << 10553;
    for (int i = 0; i < 10553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10553(int& val) {
    const int x = 10553;
    std::cout << 10553;
    for (int i = 0; i < 10553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10554(int& val) {
    func_in_10554(val);
    const int x = 10554;
    std::cout << 10554;
    for (int i = 0; i < 10554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10554(int& val) {
    const int x = 10554;
    std::cout << 10554;
    for (int i = 0; i < 10554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10555(int& val) {
    func_in_10555(val);
    const int x = 10555;
    std::cout << 10555;
    for (int i = 0; i < 10555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10555(int& val) {
    const int x = 10555;
    std::cout << 10555;
    for (int i = 0; i < 10555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10556(int& val) {
    func_in_10556(val);
    const int x = 10556;
    std::cout << 10556;
    for (int i = 0; i < 10556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10556(int& val) {
    const int x = 10556;
    std::cout << 10556;
    for (int i = 0; i < 10556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10557(int& val) {
    func_in_10557(val);
    const int x = 10557;
    std::cout << 10557;
    for (int i = 0; i < 10557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10557(int& val) {
    const int x = 10557;
    std::cout << 10557;
    for (int i = 0; i < 10557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10558(int& val) {
    func_in_10558(val);
    const int x = 10558;
    std::cout << 10558;
    for (int i = 0; i < 10558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10558(int& val) {
    const int x = 10558;
    std::cout << 10558;
    for (int i = 0; i < 10558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10559(int& val) {
    func_in_10559(val);
    const int x = 10559;
    std::cout << 10559;
    for (int i = 0; i < 10559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10559(int& val) {
    const int x = 10559;
    std::cout << 10559;
    for (int i = 0; i < 10559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10560(int& val) {
    func_in_10560(val);
    const int x = 10560;
    std::cout << 10560;
    for (int i = 0; i < 10560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10560(int& val) {
    const int x = 10560;
    std::cout << 10560;
    for (int i = 0; i < 10560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10561(int& val) {
    func_in_10561(val);
    const int x = 10561;
    std::cout << 10561;
    for (int i = 0; i < 10561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10561(int& val) {
    const int x = 10561;
    std::cout << 10561;
    for (int i = 0; i < 10561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10562(int& val) {
    func_in_10562(val);
    const int x = 10562;
    std::cout << 10562;
    for (int i = 0; i < 10562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10562(int& val) {
    const int x = 10562;
    std::cout << 10562;
    for (int i = 0; i < 10562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10563(int& val) {
    func_in_10563(val);
    const int x = 10563;
    std::cout << 10563;
    for (int i = 0; i < 10563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10563(int& val) {
    const int x = 10563;
    std::cout << 10563;
    for (int i = 0; i < 10563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10564(int& val) {
    func_in_10564(val);
    const int x = 10564;
    std::cout << 10564;
    for (int i = 0; i < 10564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10564(int& val) {
    const int x = 10564;
    std::cout << 10564;
    for (int i = 0; i < 10564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10565(int& val) {
    func_in_10565(val);
    const int x = 10565;
    std::cout << 10565;
    for (int i = 0; i < 10565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10565(int& val) {
    const int x = 10565;
    std::cout << 10565;
    for (int i = 0; i < 10565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10566(int& val) {
    func_in_10566(val);
    const int x = 10566;
    std::cout << 10566;
    for (int i = 0; i < 10566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10566(int& val) {
    const int x = 10566;
    std::cout << 10566;
    for (int i = 0; i < 10566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10567(int& val) {
    func_in_10567(val);
    const int x = 10567;
    std::cout << 10567;
    for (int i = 0; i < 10567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10567(int& val) {
    const int x = 10567;
    std::cout << 10567;
    for (int i = 0; i < 10567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10568(int& val) {
    func_in_10568(val);
    const int x = 10568;
    std::cout << 10568;
    for (int i = 0; i < 10568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10568(int& val) {
    const int x = 10568;
    std::cout << 10568;
    for (int i = 0; i < 10568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10569(int& val) {
    func_in_10569(val);
    const int x = 10569;
    std::cout << 10569;
    for (int i = 0; i < 10569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10569(int& val) {
    const int x = 10569;
    std::cout << 10569;
    for (int i = 0; i < 10569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10570(int& val) {
    func_in_10570(val);
    const int x = 10570;
    std::cout << 10570;
    for (int i = 0; i < 10570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10570(int& val) {
    const int x = 10570;
    std::cout << 10570;
    for (int i = 0; i < 10570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10571(int& val) {
    func_in_10571(val);
    const int x = 10571;
    std::cout << 10571;
    for (int i = 0; i < 10571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10571(int& val) {
    const int x = 10571;
    std::cout << 10571;
    for (int i = 0; i < 10571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10572(int& val) {
    func_in_10572(val);
    const int x = 10572;
    std::cout << 10572;
    for (int i = 0; i < 10572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10572(int& val) {
    const int x = 10572;
    std::cout << 10572;
    for (int i = 0; i < 10572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10573(int& val) {
    func_in_10573(val);
    const int x = 10573;
    std::cout << 10573;
    for (int i = 0; i < 10573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10573(int& val) {
    const int x = 10573;
    std::cout << 10573;
    for (int i = 0; i < 10573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10574(int& val) {
    func_in_10574(val);
    const int x = 10574;
    std::cout << 10574;
    for (int i = 0; i < 10574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10574(int& val) {
    const int x = 10574;
    std::cout << 10574;
    for (int i = 0; i < 10574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10575(int& val) {
    func_in_10575(val);
    const int x = 10575;
    std::cout << 10575;
    for (int i = 0; i < 10575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10575(int& val) {
    const int x = 10575;
    std::cout << 10575;
    for (int i = 0; i < 10575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10576(int& val) {
    func_in_10576(val);
    const int x = 10576;
    std::cout << 10576;
    for (int i = 0; i < 10576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10576(int& val) {
    const int x = 10576;
    std::cout << 10576;
    for (int i = 0; i < 10576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10577(int& val) {
    func_in_10577(val);
    const int x = 10577;
    std::cout << 10577;
    for (int i = 0; i < 10577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10577(int& val) {
    const int x = 10577;
    std::cout << 10577;
    for (int i = 0; i < 10577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10578(int& val) {
    func_in_10578(val);
    const int x = 10578;
    std::cout << 10578;
    for (int i = 0; i < 10578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10578(int& val) {
    const int x = 10578;
    std::cout << 10578;
    for (int i = 0; i < 10578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10579(int& val) {
    func_in_10579(val);
    const int x = 10579;
    std::cout << 10579;
    for (int i = 0; i < 10579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10579(int& val) {
    const int x = 10579;
    std::cout << 10579;
    for (int i = 0; i < 10579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10580(int& val) {
    func_in_10580(val);
    const int x = 10580;
    std::cout << 10580;
    for (int i = 0; i < 10580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10580(int& val) {
    const int x = 10580;
    std::cout << 10580;
    for (int i = 0; i < 10580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10581(int& val) {
    func_in_10581(val);
    const int x = 10581;
    std::cout << 10581;
    for (int i = 0; i < 10581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10581(int& val) {
    const int x = 10581;
    std::cout << 10581;
    for (int i = 0; i < 10581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10582(int& val) {
    func_in_10582(val);
    const int x = 10582;
    std::cout << 10582;
    for (int i = 0; i < 10582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10582(int& val) {
    const int x = 10582;
    std::cout << 10582;
    for (int i = 0; i < 10582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10583(int& val) {
    func_in_10583(val);
    const int x = 10583;
    std::cout << 10583;
    for (int i = 0; i < 10583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10583(int& val) {
    const int x = 10583;
    std::cout << 10583;
    for (int i = 0; i < 10583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10584(int& val) {
    func_in_10584(val);
    const int x = 10584;
    std::cout << 10584;
    for (int i = 0; i < 10584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10584(int& val) {
    const int x = 10584;
    std::cout << 10584;
    for (int i = 0; i < 10584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10585(int& val) {
    func_in_10585(val);
    const int x = 10585;
    std::cout << 10585;
    for (int i = 0; i < 10585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10585(int& val) {
    const int x = 10585;
    std::cout << 10585;
    for (int i = 0; i < 10585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10586(int& val) {
    func_in_10586(val);
    const int x = 10586;
    std::cout << 10586;
    for (int i = 0; i < 10586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10586(int& val) {
    const int x = 10586;
    std::cout << 10586;
    for (int i = 0; i < 10586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10587(int& val) {
    func_in_10587(val);
    const int x = 10587;
    std::cout << 10587;
    for (int i = 0; i < 10587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10587(int& val) {
    const int x = 10587;
    std::cout << 10587;
    for (int i = 0; i < 10587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10588(int& val) {
    func_in_10588(val);
    const int x = 10588;
    std::cout << 10588;
    for (int i = 0; i < 10588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10588(int& val) {
    const int x = 10588;
    std::cout << 10588;
    for (int i = 0; i < 10588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10589(int& val) {
    func_in_10589(val);
    const int x = 10589;
    std::cout << 10589;
    for (int i = 0; i < 10589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10589(int& val) {
    const int x = 10589;
    std::cout << 10589;
    for (int i = 0; i < 10589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10590(int& val) {
    func_in_10590(val);
    const int x = 10590;
    std::cout << 10590;
    for (int i = 0; i < 10590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10590(int& val) {
    const int x = 10590;
    std::cout << 10590;
    for (int i = 0; i < 10590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10591(int& val) {
    func_in_10591(val);
    const int x = 10591;
    std::cout << 10591;
    for (int i = 0; i < 10591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10591(int& val) {
    const int x = 10591;
    std::cout << 10591;
    for (int i = 0; i < 10591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10592(int& val) {
    func_in_10592(val);
    const int x = 10592;
    std::cout << 10592;
    for (int i = 0; i < 10592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10592(int& val) {
    const int x = 10592;
    std::cout << 10592;
    for (int i = 0; i < 10592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10593(int& val) {
    func_in_10593(val);
    const int x = 10593;
    std::cout << 10593;
    for (int i = 0; i < 10593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10593(int& val) {
    const int x = 10593;
    std::cout << 10593;
    for (int i = 0; i < 10593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10594(int& val) {
    func_in_10594(val);
    const int x = 10594;
    std::cout << 10594;
    for (int i = 0; i < 10594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10594(int& val) {
    const int x = 10594;
    std::cout << 10594;
    for (int i = 0; i < 10594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10595(int& val) {
    func_in_10595(val);
    const int x = 10595;
    std::cout << 10595;
    for (int i = 0; i < 10595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10595(int& val) {
    const int x = 10595;
    std::cout << 10595;
    for (int i = 0; i < 10595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10596(int& val) {
    func_in_10596(val);
    const int x = 10596;
    std::cout << 10596;
    for (int i = 0; i < 10596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10596(int& val) {
    const int x = 10596;
    std::cout << 10596;
    for (int i = 0; i < 10596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10597(int& val) {
    func_in_10597(val);
    const int x = 10597;
    std::cout << 10597;
    for (int i = 0; i < 10597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10597(int& val) {
    const int x = 10597;
    std::cout << 10597;
    for (int i = 0; i < 10597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10598(int& val) {
    func_in_10598(val);
    const int x = 10598;
    std::cout << 10598;
    for (int i = 0; i < 10598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10598(int& val) {
    const int x = 10598;
    std::cout << 10598;
    for (int i = 0; i < 10598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10599(int& val) {
    func_in_10599(val);
    const int x = 10599;
    std::cout << 10599;
    for (int i = 0; i < 10599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10599(int& val) {
    const int x = 10599;
    std::cout << 10599;
    for (int i = 0; i < 10599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10600(int& val) {
    func_in_10600(val);
    const int x = 10600;
    std::cout << 10600;
    for (int i = 0; i < 10600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10600(int& val) {
    const int x = 10600;
    std::cout << 10600;
    for (int i = 0; i < 10600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10601(int& val) {
    func_in_10601(val);
    const int x = 10601;
    std::cout << 10601;
    for (int i = 0; i < 10601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10601(int& val) {
    const int x = 10601;
    std::cout << 10601;
    for (int i = 0; i < 10601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10602(int& val) {
    func_in_10602(val);
    const int x = 10602;
    std::cout << 10602;
    for (int i = 0; i < 10602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10602(int& val) {
    const int x = 10602;
    std::cout << 10602;
    for (int i = 0; i < 10602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10603(int& val) {
    func_in_10603(val);
    const int x = 10603;
    std::cout << 10603;
    for (int i = 0; i < 10603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10603(int& val) {
    const int x = 10603;
    std::cout << 10603;
    for (int i = 0; i < 10603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10604(int& val) {
    func_in_10604(val);
    const int x = 10604;
    std::cout << 10604;
    for (int i = 0; i < 10604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10604(int& val) {
    const int x = 10604;
    std::cout << 10604;
    for (int i = 0; i < 10604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10605(int& val) {
    func_in_10605(val);
    const int x = 10605;
    std::cout << 10605;
    for (int i = 0; i < 10605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10605(int& val) {
    const int x = 10605;
    std::cout << 10605;
    for (int i = 0; i < 10605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10606(int& val) {
    func_in_10606(val);
    const int x = 10606;
    std::cout << 10606;
    for (int i = 0; i < 10606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10606(int& val) {
    const int x = 10606;
    std::cout << 10606;
    for (int i = 0; i < 10606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10607(int& val) {
    func_in_10607(val);
    const int x = 10607;
    std::cout << 10607;
    for (int i = 0; i < 10607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10607(int& val) {
    const int x = 10607;
    std::cout << 10607;
    for (int i = 0; i < 10607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10608(int& val) {
    func_in_10608(val);
    const int x = 10608;
    std::cout << 10608;
    for (int i = 0; i < 10608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10608(int& val) {
    const int x = 10608;
    std::cout << 10608;
    for (int i = 0; i < 10608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10609(int& val) {
    func_in_10609(val);
    const int x = 10609;
    std::cout << 10609;
    for (int i = 0; i < 10609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10609(int& val) {
    const int x = 10609;
    std::cout << 10609;
    for (int i = 0; i < 10609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10610(int& val) {
    func_in_10610(val);
    const int x = 10610;
    std::cout << 10610;
    for (int i = 0; i < 10610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10610(int& val) {
    const int x = 10610;
    std::cout << 10610;
    for (int i = 0; i < 10610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10611(int& val) {
    func_in_10611(val);
    const int x = 10611;
    std::cout << 10611;
    for (int i = 0; i < 10611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10611(int& val) {
    const int x = 10611;
    std::cout << 10611;
    for (int i = 0; i < 10611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10612(int& val) {
    func_in_10612(val);
    const int x = 10612;
    std::cout << 10612;
    for (int i = 0; i < 10612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10612(int& val) {
    const int x = 10612;
    std::cout << 10612;
    for (int i = 0; i < 10612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10613(int& val) {
    func_in_10613(val);
    const int x = 10613;
    std::cout << 10613;
    for (int i = 0; i < 10613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10613(int& val) {
    const int x = 10613;
    std::cout << 10613;
    for (int i = 0; i < 10613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10614(int& val) {
    func_in_10614(val);
    const int x = 10614;
    std::cout << 10614;
    for (int i = 0; i < 10614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10614(int& val) {
    const int x = 10614;
    std::cout << 10614;
    for (int i = 0; i < 10614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10615(int& val) {
    func_in_10615(val);
    const int x = 10615;
    std::cout << 10615;
    for (int i = 0; i < 10615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10615(int& val) {
    const int x = 10615;
    std::cout << 10615;
    for (int i = 0; i < 10615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10616(int& val) {
    func_in_10616(val);
    const int x = 10616;
    std::cout << 10616;
    for (int i = 0; i < 10616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10616(int& val) {
    const int x = 10616;
    std::cout << 10616;
    for (int i = 0; i < 10616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10617(int& val) {
    func_in_10617(val);
    const int x = 10617;
    std::cout << 10617;
    for (int i = 0; i < 10617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10617(int& val) {
    const int x = 10617;
    std::cout << 10617;
    for (int i = 0; i < 10617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10618(int& val) {
    func_in_10618(val);
    const int x = 10618;
    std::cout << 10618;
    for (int i = 0; i < 10618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10618(int& val) {
    const int x = 10618;
    std::cout << 10618;
    for (int i = 0; i < 10618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10619(int& val) {
    func_in_10619(val);
    const int x = 10619;
    std::cout << 10619;
    for (int i = 0; i < 10619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10619(int& val) {
    const int x = 10619;
    std::cout << 10619;
    for (int i = 0; i < 10619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10620(int& val) {
    func_in_10620(val);
    const int x = 10620;
    std::cout << 10620;
    for (int i = 0; i < 10620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10620(int& val) {
    const int x = 10620;
    std::cout << 10620;
    for (int i = 0; i < 10620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10621(int& val) {
    func_in_10621(val);
    const int x = 10621;
    std::cout << 10621;
    for (int i = 0; i < 10621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10621(int& val) {
    const int x = 10621;
    std::cout << 10621;
    for (int i = 0; i < 10621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10622(int& val) {
    func_in_10622(val);
    const int x = 10622;
    std::cout << 10622;
    for (int i = 0; i < 10622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10622(int& val) {
    const int x = 10622;
    std::cout << 10622;
    for (int i = 0; i < 10622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10623(int& val) {
    func_in_10623(val);
    const int x = 10623;
    std::cout << 10623;
    for (int i = 0; i < 10623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10623(int& val) {
    const int x = 10623;
    std::cout << 10623;
    for (int i = 0; i < 10623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10624(int& val) {
    func_in_10624(val);
    const int x = 10624;
    std::cout << 10624;
    for (int i = 0; i < 10624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10624(int& val) {
    const int x = 10624;
    std::cout << 10624;
    for (int i = 0; i < 10624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10625(int& val) {
    func_in_10625(val);
    const int x = 10625;
    std::cout << 10625;
    for (int i = 0; i < 10625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10625(int& val) {
    const int x = 10625;
    std::cout << 10625;
    for (int i = 0; i < 10625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10626(int& val) {
    func_in_10626(val);
    const int x = 10626;
    std::cout << 10626;
    for (int i = 0; i < 10626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10626(int& val) {
    const int x = 10626;
    std::cout << 10626;
    for (int i = 0; i < 10626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10627(int& val) {
    func_in_10627(val);
    const int x = 10627;
    std::cout << 10627;
    for (int i = 0; i < 10627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10627(int& val) {
    const int x = 10627;
    std::cout << 10627;
    for (int i = 0; i < 10627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10628(int& val) {
    func_in_10628(val);
    const int x = 10628;
    std::cout << 10628;
    for (int i = 0; i < 10628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10628(int& val) {
    const int x = 10628;
    std::cout << 10628;
    for (int i = 0; i < 10628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10629(int& val) {
    func_in_10629(val);
    const int x = 10629;
    std::cout << 10629;
    for (int i = 0; i < 10629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10629(int& val) {
    const int x = 10629;
    std::cout << 10629;
    for (int i = 0; i < 10629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10630(int& val) {
    func_in_10630(val);
    const int x = 10630;
    std::cout << 10630;
    for (int i = 0; i < 10630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10630(int& val) {
    const int x = 10630;
    std::cout << 10630;
    for (int i = 0; i < 10630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10631(int& val) {
    func_in_10631(val);
    const int x = 10631;
    std::cout << 10631;
    for (int i = 0; i < 10631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10631(int& val) {
    const int x = 10631;
    std::cout << 10631;
    for (int i = 0; i < 10631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10632(int& val) {
    func_in_10632(val);
    const int x = 10632;
    std::cout << 10632;
    for (int i = 0; i < 10632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10632(int& val) {
    const int x = 10632;
    std::cout << 10632;
    for (int i = 0; i < 10632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10633(int& val) {
    func_in_10633(val);
    const int x = 10633;
    std::cout << 10633;
    for (int i = 0; i < 10633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10633(int& val) {
    const int x = 10633;
    std::cout << 10633;
    for (int i = 0; i < 10633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10634(int& val) {
    func_in_10634(val);
    const int x = 10634;
    std::cout << 10634;
    for (int i = 0; i < 10634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10634(int& val) {
    const int x = 10634;
    std::cout << 10634;
    for (int i = 0; i < 10634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10635(int& val) {
    func_in_10635(val);
    const int x = 10635;
    std::cout << 10635;
    for (int i = 0; i < 10635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10635(int& val) {
    const int x = 10635;
    std::cout << 10635;
    for (int i = 0; i < 10635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10636(int& val) {
    func_in_10636(val);
    const int x = 10636;
    std::cout << 10636;
    for (int i = 0; i < 10636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10636(int& val) {
    const int x = 10636;
    std::cout << 10636;
    for (int i = 0; i < 10636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10637(int& val) {
    func_in_10637(val);
    const int x = 10637;
    std::cout << 10637;
    for (int i = 0; i < 10637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10637(int& val) {
    const int x = 10637;
    std::cout << 10637;
    for (int i = 0; i < 10637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10638(int& val) {
    func_in_10638(val);
    const int x = 10638;
    std::cout << 10638;
    for (int i = 0; i < 10638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10638(int& val) {
    const int x = 10638;
    std::cout << 10638;
    for (int i = 0; i < 10638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10639(int& val) {
    func_in_10639(val);
    const int x = 10639;
    std::cout << 10639;
    for (int i = 0; i < 10639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10639(int& val) {
    const int x = 10639;
    std::cout << 10639;
    for (int i = 0; i < 10639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10640(int& val) {
    func_in_10640(val);
    const int x = 10640;
    std::cout << 10640;
    for (int i = 0; i < 10640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10640(int& val) {
    const int x = 10640;
    std::cout << 10640;
    for (int i = 0; i < 10640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10641(int& val) {
    func_in_10641(val);
    const int x = 10641;
    std::cout << 10641;
    for (int i = 0; i < 10641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10641(int& val) {
    const int x = 10641;
    std::cout << 10641;
    for (int i = 0; i < 10641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10642(int& val) {
    func_in_10642(val);
    const int x = 10642;
    std::cout << 10642;
    for (int i = 0; i < 10642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10642(int& val) {
    const int x = 10642;
    std::cout << 10642;
    for (int i = 0; i < 10642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10643(int& val) {
    func_in_10643(val);
    const int x = 10643;
    std::cout << 10643;
    for (int i = 0; i < 10643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10643(int& val) {
    const int x = 10643;
    std::cout << 10643;
    for (int i = 0; i < 10643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10644(int& val) {
    func_in_10644(val);
    const int x = 10644;
    std::cout << 10644;
    for (int i = 0; i < 10644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10644(int& val) {
    const int x = 10644;
    std::cout << 10644;
    for (int i = 0; i < 10644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10645(int& val) {
    func_in_10645(val);
    const int x = 10645;
    std::cout << 10645;
    for (int i = 0; i < 10645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10645(int& val) {
    const int x = 10645;
    std::cout << 10645;
    for (int i = 0; i < 10645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10646(int& val) {
    func_in_10646(val);
    const int x = 10646;
    std::cout << 10646;
    for (int i = 0; i < 10646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10646(int& val) {
    const int x = 10646;
    std::cout << 10646;
    for (int i = 0; i < 10646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10647(int& val) {
    func_in_10647(val);
    const int x = 10647;
    std::cout << 10647;
    for (int i = 0; i < 10647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10647(int& val) {
    const int x = 10647;
    std::cout << 10647;
    for (int i = 0; i < 10647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10648(int& val) {
    func_in_10648(val);
    const int x = 10648;
    std::cout << 10648;
    for (int i = 0; i < 10648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10648(int& val) {
    const int x = 10648;
    std::cout << 10648;
    for (int i = 0; i < 10648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10649(int& val) {
    func_in_10649(val);
    const int x = 10649;
    std::cout << 10649;
    for (int i = 0; i < 10649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10649(int& val) {
    const int x = 10649;
    std::cout << 10649;
    for (int i = 0; i < 10649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10650(int& val) {
    func_in_10650(val);
    const int x = 10650;
    std::cout << 10650;
    for (int i = 0; i < 10650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10650(int& val) {
    const int x = 10650;
    std::cout << 10650;
    for (int i = 0; i < 10650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10651(int& val) {
    func_in_10651(val);
    const int x = 10651;
    std::cout << 10651;
    for (int i = 0; i < 10651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10651(int& val) {
    const int x = 10651;
    std::cout << 10651;
    for (int i = 0; i < 10651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10652(int& val) {
    func_in_10652(val);
    const int x = 10652;
    std::cout << 10652;
    for (int i = 0; i < 10652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10652(int& val) {
    const int x = 10652;
    std::cout << 10652;
    for (int i = 0; i < 10652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10653(int& val) {
    func_in_10653(val);
    const int x = 10653;
    std::cout << 10653;
    for (int i = 0; i < 10653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10653(int& val) {
    const int x = 10653;
    std::cout << 10653;
    for (int i = 0; i < 10653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10654(int& val) {
    func_in_10654(val);
    const int x = 10654;
    std::cout << 10654;
    for (int i = 0; i < 10654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10654(int& val) {
    const int x = 10654;
    std::cout << 10654;
    for (int i = 0; i < 10654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10655(int& val) {
    func_in_10655(val);
    const int x = 10655;
    std::cout << 10655;
    for (int i = 0; i < 10655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10655(int& val) {
    const int x = 10655;
    std::cout << 10655;
    for (int i = 0; i < 10655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10656(int& val) {
    func_in_10656(val);
    const int x = 10656;
    std::cout << 10656;
    for (int i = 0; i < 10656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10656(int& val) {
    const int x = 10656;
    std::cout << 10656;
    for (int i = 0; i < 10656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10657(int& val) {
    func_in_10657(val);
    const int x = 10657;
    std::cout << 10657;
    for (int i = 0; i < 10657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10657(int& val) {
    const int x = 10657;
    std::cout << 10657;
    for (int i = 0; i < 10657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10658(int& val) {
    func_in_10658(val);
    const int x = 10658;
    std::cout << 10658;
    for (int i = 0; i < 10658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10658(int& val) {
    const int x = 10658;
    std::cout << 10658;
    for (int i = 0; i < 10658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10659(int& val) {
    func_in_10659(val);
    const int x = 10659;
    std::cout << 10659;
    for (int i = 0; i < 10659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10659(int& val) {
    const int x = 10659;
    std::cout << 10659;
    for (int i = 0; i < 10659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10660(int& val) {
    func_in_10660(val);
    const int x = 10660;
    std::cout << 10660;
    for (int i = 0; i < 10660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10660(int& val) {
    const int x = 10660;
    std::cout << 10660;
    for (int i = 0; i < 10660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10661(int& val) {
    func_in_10661(val);
    const int x = 10661;
    std::cout << 10661;
    for (int i = 0; i < 10661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10661(int& val) {
    const int x = 10661;
    std::cout << 10661;
    for (int i = 0; i < 10661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10662(int& val) {
    func_in_10662(val);
    const int x = 10662;
    std::cout << 10662;
    for (int i = 0; i < 10662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10662(int& val) {
    const int x = 10662;
    std::cout << 10662;
    for (int i = 0; i < 10662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10663(int& val) {
    func_in_10663(val);
    const int x = 10663;
    std::cout << 10663;
    for (int i = 0; i < 10663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10663(int& val) {
    const int x = 10663;
    std::cout << 10663;
    for (int i = 0; i < 10663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10664(int& val) {
    func_in_10664(val);
    const int x = 10664;
    std::cout << 10664;
    for (int i = 0; i < 10664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10664(int& val) {
    const int x = 10664;
    std::cout << 10664;
    for (int i = 0; i < 10664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10665(int& val) {
    func_in_10665(val);
    const int x = 10665;
    std::cout << 10665;
    for (int i = 0; i < 10665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10665(int& val) {
    const int x = 10665;
    std::cout << 10665;
    for (int i = 0; i < 10665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10666(int& val) {
    func_in_10666(val);
    const int x = 10666;
    std::cout << 10666;
    for (int i = 0; i < 10666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10666(int& val) {
    const int x = 10666;
    std::cout << 10666;
    for (int i = 0; i < 10666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10667(int& val) {
    func_in_10667(val);
    const int x = 10667;
    std::cout << 10667;
    for (int i = 0; i < 10667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10667(int& val) {
    const int x = 10667;
    std::cout << 10667;
    for (int i = 0; i < 10667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10668(int& val) {
    func_in_10668(val);
    const int x = 10668;
    std::cout << 10668;
    for (int i = 0; i < 10668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10668(int& val) {
    const int x = 10668;
    std::cout << 10668;
    for (int i = 0; i < 10668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10669(int& val) {
    func_in_10669(val);
    const int x = 10669;
    std::cout << 10669;
    for (int i = 0; i < 10669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10669(int& val) {
    const int x = 10669;
    std::cout << 10669;
    for (int i = 0; i < 10669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10670(int& val) {
    func_in_10670(val);
    const int x = 10670;
    std::cout << 10670;
    for (int i = 0; i < 10670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10670(int& val) {
    const int x = 10670;
    std::cout << 10670;
    for (int i = 0; i < 10670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10671(int& val) {
    func_in_10671(val);
    const int x = 10671;
    std::cout << 10671;
    for (int i = 0; i < 10671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10671(int& val) {
    const int x = 10671;
    std::cout << 10671;
    for (int i = 0; i < 10671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10672(int& val) {
    func_in_10672(val);
    const int x = 10672;
    std::cout << 10672;
    for (int i = 0; i < 10672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10672(int& val) {
    const int x = 10672;
    std::cout << 10672;
    for (int i = 0; i < 10672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10673(int& val) {
    func_in_10673(val);
    const int x = 10673;
    std::cout << 10673;
    for (int i = 0; i < 10673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10673(int& val) {
    const int x = 10673;
    std::cout << 10673;
    for (int i = 0; i < 10673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10674(int& val) {
    func_in_10674(val);
    const int x = 10674;
    std::cout << 10674;
    for (int i = 0; i < 10674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10674(int& val) {
    const int x = 10674;
    std::cout << 10674;
    for (int i = 0; i < 10674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10675(int& val) {
    func_in_10675(val);
    const int x = 10675;
    std::cout << 10675;
    for (int i = 0; i < 10675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10675(int& val) {
    const int x = 10675;
    std::cout << 10675;
    for (int i = 0; i < 10675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10676(int& val) {
    func_in_10676(val);
    const int x = 10676;
    std::cout << 10676;
    for (int i = 0; i < 10676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10676(int& val) {
    const int x = 10676;
    std::cout << 10676;
    for (int i = 0; i < 10676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10677(int& val) {
    func_in_10677(val);
    const int x = 10677;
    std::cout << 10677;
    for (int i = 0; i < 10677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10677(int& val) {
    const int x = 10677;
    std::cout << 10677;
    for (int i = 0; i < 10677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10678(int& val) {
    func_in_10678(val);
    const int x = 10678;
    std::cout << 10678;
    for (int i = 0; i < 10678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10678(int& val) {
    const int x = 10678;
    std::cout << 10678;
    for (int i = 0; i < 10678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10679(int& val) {
    func_in_10679(val);
    const int x = 10679;
    std::cout << 10679;
    for (int i = 0; i < 10679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10679(int& val) {
    const int x = 10679;
    std::cout << 10679;
    for (int i = 0; i < 10679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10680(int& val) {
    func_in_10680(val);
    const int x = 10680;
    std::cout << 10680;
    for (int i = 0; i < 10680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10680(int& val) {
    const int x = 10680;
    std::cout << 10680;
    for (int i = 0; i < 10680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10681(int& val) {
    func_in_10681(val);
    const int x = 10681;
    std::cout << 10681;
    for (int i = 0; i < 10681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10681(int& val) {
    const int x = 10681;
    std::cout << 10681;
    for (int i = 0; i < 10681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10682(int& val) {
    func_in_10682(val);
    const int x = 10682;
    std::cout << 10682;
    for (int i = 0; i < 10682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10682(int& val) {
    const int x = 10682;
    std::cout << 10682;
    for (int i = 0; i < 10682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10683(int& val) {
    func_in_10683(val);
    const int x = 10683;
    std::cout << 10683;
    for (int i = 0; i < 10683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10683(int& val) {
    const int x = 10683;
    std::cout << 10683;
    for (int i = 0; i < 10683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10684(int& val) {
    func_in_10684(val);
    const int x = 10684;
    std::cout << 10684;
    for (int i = 0; i < 10684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10684(int& val) {
    const int x = 10684;
    std::cout << 10684;
    for (int i = 0; i < 10684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10685(int& val) {
    func_in_10685(val);
    const int x = 10685;
    std::cout << 10685;
    for (int i = 0; i < 10685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10685(int& val) {
    const int x = 10685;
    std::cout << 10685;
    for (int i = 0; i < 10685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10686(int& val) {
    func_in_10686(val);
    const int x = 10686;
    std::cout << 10686;
    for (int i = 0; i < 10686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10686(int& val) {
    const int x = 10686;
    std::cout << 10686;
    for (int i = 0; i < 10686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10687(int& val) {
    func_in_10687(val);
    const int x = 10687;
    std::cout << 10687;
    for (int i = 0; i < 10687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10687(int& val) {
    const int x = 10687;
    std::cout << 10687;
    for (int i = 0; i < 10687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10688(int& val) {
    func_in_10688(val);
    const int x = 10688;
    std::cout << 10688;
    for (int i = 0; i < 10688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10688(int& val) {
    const int x = 10688;
    std::cout << 10688;
    for (int i = 0; i < 10688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10689(int& val) {
    func_in_10689(val);
    const int x = 10689;
    std::cout << 10689;
    for (int i = 0; i < 10689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10689(int& val) {
    const int x = 10689;
    std::cout << 10689;
    for (int i = 0; i < 10689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10690(int& val) {
    func_in_10690(val);
    const int x = 10690;
    std::cout << 10690;
    for (int i = 0; i < 10690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10690(int& val) {
    const int x = 10690;
    std::cout << 10690;
    for (int i = 0; i < 10690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10691(int& val) {
    func_in_10691(val);
    const int x = 10691;
    std::cout << 10691;
    for (int i = 0; i < 10691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10691(int& val) {
    const int x = 10691;
    std::cout << 10691;
    for (int i = 0; i < 10691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10692(int& val) {
    func_in_10692(val);
    const int x = 10692;
    std::cout << 10692;
    for (int i = 0; i < 10692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10692(int& val) {
    const int x = 10692;
    std::cout << 10692;
    for (int i = 0; i < 10692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10693(int& val) {
    func_in_10693(val);
    const int x = 10693;
    std::cout << 10693;
    for (int i = 0; i < 10693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10693(int& val) {
    const int x = 10693;
    std::cout << 10693;
    for (int i = 0; i < 10693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10694(int& val) {
    func_in_10694(val);
    const int x = 10694;
    std::cout << 10694;
    for (int i = 0; i < 10694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10694(int& val) {
    const int x = 10694;
    std::cout << 10694;
    for (int i = 0; i < 10694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10695(int& val) {
    func_in_10695(val);
    const int x = 10695;
    std::cout << 10695;
    for (int i = 0; i < 10695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10695(int& val) {
    const int x = 10695;
    std::cout << 10695;
    for (int i = 0; i < 10695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10696(int& val) {
    func_in_10696(val);
    const int x = 10696;
    std::cout << 10696;
    for (int i = 0; i < 10696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10696(int& val) {
    const int x = 10696;
    std::cout << 10696;
    for (int i = 0; i < 10696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10697(int& val) {
    func_in_10697(val);
    const int x = 10697;
    std::cout << 10697;
    for (int i = 0; i < 10697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10697(int& val) {
    const int x = 10697;
    std::cout << 10697;
    for (int i = 0; i < 10697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10698(int& val) {
    func_in_10698(val);
    const int x = 10698;
    std::cout << 10698;
    for (int i = 0; i < 10698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10698(int& val) {
    const int x = 10698;
    std::cout << 10698;
    for (int i = 0; i < 10698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10699(int& val) {
    func_in_10699(val);
    const int x = 10699;
    std::cout << 10699;
    for (int i = 0; i < 10699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10699(int& val) {
    const int x = 10699;
    std::cout << 10699;
    for (int i = 0; i < 10699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10700(int& val) {
    func_in_10700(val);
    const int x = 10700;
    std::cout << 10700;
    for (int i = 0; i < 10700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10700(int& val) {
    const int x = 10700;
    std::cout << 10700;
    for (int i = 0; i < 10700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10701(int& val) {
    func_in_10701(val);
    const int x = 10701;
    std::cout << 10701;
    for (int i = 0; i < 10701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10701(int& val) {
    const int x = 10701;
    std::cout << 10701;
    for (int i = 0; i < 10701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10702(int& val) {
    func_in_10702(val);
    const int x = 10702;
    std::cout << 10702;
    for (int i = 0; i < 10702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10702(int& val) {
    const int x = 10702;
    std::cout << 10702;
    for (int i = 0; i < 10702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10703(int& val) {
    func_in_10703(val);
    const int x = 10703;
    std::cout << 10703;
    for (int i = 0; i < 10703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10703(int& val) {
    const int x = 10703;
    std::cout << 10703;
    for (int i = 0; i < 10703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10704(int& val) {
    func_in_10704(val);
    const int x = 10704;
    std::cout << 10704;
    for (int i = 0; i < 10704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10704(int& val) {
    const int x = 10704;
    std::cout << 10704;
    for (int i = 0; i < 10704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10705(int& val) {
    func_in_10705(val);
    const int x = 10705;
    std::cout << 10705;
    for (int i = 0; i < 10705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10705(int& val) {
    const int x = 10705;
    std::cout << 10705;
    for (int i = 0; i < 10705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10706(int& val) {
    func_in_10706(val);
    const int x = 10706;
    std::cout << 10706;
    for (int i = 0; i < 10706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10706(int& val) {
    const int x = 10706;
    std::cout << 10706;
    for (int i = 0; i < 10706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10707(int& val) {
    func_in_10707(val);
    const int x = 10707;
    std::cout << 10707;
    for (int i = 0; i < 10707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10707(int& val) {
    const int x = 10707;
    std::cout << 10707;
    for (int i = 0; i < 10707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10708(int& val) {
    func_in_10708(val);
    const int x = 10708;
    std::cout << 10708;
    for (int i = 0; i < 10708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10708(int& val) {
    const int x = 10708;
    std::cout << 10708;
    for (int i = 0; i < 10708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10709(int& val) {
    func_in_10709(val);
    const int x = 10709;
    std::cout << 10709;
    for (int i = 0; i < 10709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10709(int& val) {
    const int x = 10709;
    std::cout << 10709;
    for (int i = 0; i < 10709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10710(int& val) {
    func_in_10710(val);
    const int x = 10710;
    std::cout << 10710;
    for (int i = 0; i < 10710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10710(int& val) {
    const int x = 10710;
    std::cout << 10710;
    for (int i = 0; i < 10710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10711(int& val) {
    func_in_10711(val);
    const int x = 10711;
    std::cout << 10711;
    for (int i = 0; i < 10711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10711(int& val) {
    const int x = 10711;
    std::cout << 10711;
    for (int i = 0; i < 10711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10712(int& val) {
    func_in_10712(val);
    const int x = 10712;
    std::cout << 10712;
    for (int i = 0; i < 10712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10712(int& val) {
    const int x = 10712;
    std::cout << 10712;
    for (int i = 0; i < 10712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10713(int& val) {
    func_in_10713(val);
    const int x = 10713;
    std::cout << 10713;
    for (int i = 0; i < 10713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10713(int& val) {
    const int x = 10713;
    std::cout << 10713;
    for (int i = 0; i < 10713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10714(int& val) {
    func_in_10714(val);
    const int x = 10714;
    std::cout << 10714;
    for (int i = 0; i < 10714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10714(int& val) {
    const int x = 10714;
    std::cout << 10714;
    for (int i = 0; i < 10714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10715(int& val) {
    func_in_10715(val);
    const int x = 10715;
    std::cout << 10715;
    for (int i = 0; i < 10715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10715(int& val) {
    const int x = 10715;
    std::cout << 10715;
    for (int i = 0; i < 10715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10716(int& val) {
    func_in_10716(val);
    const int x = 10716;
    std::cout << 10716;
    for (int i = 0; i < 10716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10716(int& val) {
    const int x = 10716;
    std::cout << 10716;
    for (int i = 0; i < 10716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10717(int& val) {
    func_in_10717(val);
    const int x = 10717;
    std::cout << 10717;
    for (int i = 0; i < 10717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10717(int& val) {
    const int x = 10717;
    std::cout << 10717;
    for (int i = 0; i < 10717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10718(int& val) {
    func_in_10718(val);
    const int x = 10718;
    std::cout << 10718;
    for (int i = 0; i < 10718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10718(int& val) {
    const int x = 10718;
    std::cout << 10718;
    for (int i = 0; i < 10718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10719(int& val) {
    func_in_10719(val);
    const int x = 10719;
    std::cout << 10719;
    for (int i = 0; i < 10719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10719(int& val) {
    const int x = 10719;
    std::cout << 10719;
    for (int i = 0; i < 10719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10720(int& val) {
    func_in_10720(val);
    const int x = 10720;
    std::cout << 10720;
    for (int i = 0; i < 10720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10720(int& val) {
    const int x = 10720;
    std::cout << 10720;
    for (int i = 0; i < 10720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10721(int& val) {
    func_in_10721(val);
    const int x = 10721;
    std::cout << 10721;
    for (int i = 0; i < 10721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10721(int& val) {
    const int x = 10721;
    std::cout << 10721;
    for (int i = 0; i < 10721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10722(int& val) {
    func_in_10722(val);
    const int x = 10722;
    std::cout << 10722;
    for (int i = 0; i < 10722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10722(int& val) {
    const int x = 10722;
    std::cout << 10722;
    for (int i = 0; i < 10722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10723(int& val) {
    func_in_10723(val);
    const int x = 10723;
    std::cout << 10723;
    for (int i = 0; i < 10723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10723(int& val) {
    const int x = 10723;
    std::cout << 10723;
    for (int i = 0; i < 10723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10724(int& val) {
    func_in_10724(val);
    const int x = 10724;
    std::cout << 10724;
    for (int i = 0; i < 10724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10724(int& val) {
    const int x = 10724;
    std::cout << 10724;
    for (int i = 0; i < 10724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10725(int& val) {
    func_in_10725(val);
    const int x = 10725;
    std::cout << 10725;
    for (int i = 0; i < 10725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10725(int& val) {
    const int x = 10725;
    std::cout << 10725;
    for (int i = 0; i < 10725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10726(int& val) {
    func_in_10726(val);
    const int x = 10726;
    std::cout << 10726;
    for (int i = 0; i < 10726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10726(int& val) {
    const int x = 10726;
    std::cout << 10726;
    for (int i = 0; i < 10726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10727(int& val) {
    func_in_10727(val);
    const int x = 10727;
    std::cout << 10727;
    for (int i = 0; i < 10727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10727(int& val) {
    const int x = 10727;
    std::cout << 10727;
    for (int i = 0; i < 10727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10728(int& val) {
    func_in_10728(val);
    const int x = 10728;
    std::cout << 10728;
    for (int i = 0; i < 10728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10728(int& val) {
    const int x = 10728;
    std::cout << 10728;
    for (int i = 0; i < 10728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10729(int& val) {
    func_in_10729(val);
    const int x = 10729;
    std::cout << 10729;
    for (int i = 0; i < 10729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10729(int& val) {
    const int x = 10729;
    std::cout << 10729;
    for (int i = 0; i < 10729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10730(int& val) {
    func_in_10730(val);
    const int x = 10730;
    std::cout << 10730;
    for (int i = 0; i < 10730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10730(int& val) {
    const int x = 10730;
    std::cout << 10730;
    for (int i = 0; i < 10730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10731(int& val) {
    func_in_10731(val);
    const int x = 10731;
    std::cout << 10731;
    for (int i = 0; i < 10731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10731(int& val) {
    const int x = 10731;
    std::cout << 10731;
    for (int i = 0; i < 10731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10732(int& val) {
    func_in_10732(val);
    const int x = 10732;
    std::cout << 10732;
    for (int i = 0; i < 10732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10732(int& val) {
    const int x = 10732;
    std::cout << 10732;
    for (int i = 0; i < 10732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10733(int& val) {
    func_in_10733(val);
    const int x = 10733;
    std::cout << 10733;
    for (int i = 0; i < 10733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10733(int& val) {
    const int x = 10733;
    std::cout << 10733;
    for (int i = 0; i < 10733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10734(int& val) {
    func_in_10734(val);
    const int x = 10734;
    std::cout << 10734;
    for (int i = 0; i < 10734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10734(int& val) {
    const int x = 10734;
    std::cout << 10734;
    for (int i = 0; i < 10734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10735(int& val) {
    func_in_10735(val);
    const int x = 10735;
    std::cout << 10735;
    for (int i = 0; i < 10735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10735(int& val) {
    const int x = 10735;
    std::cout << 10735;
    for (int i = 0; i < 10735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10736(int& val) {
    func_in_10736(val);
    const int x = 10736;
    std::cout << 10736;
    for (int i = 0; i < 10736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10736(int& val) {
    const int x = 10736;
    std::cout << 10736;
    for (int i = 0; i < 10736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10737(int& val) {
    func_in_10737(val);
    const int x = 10737;
    std::cout << 10737;
    for (int i = 0; i < 10737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10737(int& val) {
    const int x = 10737;
    std::cout << 10737;
    for (int i = 0; i < 10737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10738(int& val) {
    func_in_10738(val);
    const int x = 10738;
    std::cout << 10738;
    for (int i = 0; i < 10738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10738(int& val) {
    const int x = 10738;
    std::cout << 10738;
    for (int i = 0; i < 10738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10739(int& val) {
    func_in_10739(val);
    const int x = 10739;
    std::cout << 10739;
    for (int i = 0; i < 10739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10739(int& val) {
    const int x = 10739;
    std::cout << 10739;
    for (int i = 0; i < 10739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10740(int& val) {
    func_in_10740(val);
    const int x = 10740;
    std::cout << 10740;
    for (int i = 0; i < 10740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10740(int& val) {
    const int x = 10740;
    std::cout << 10740;
    for (int i = 0; i < 10740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10741(int& val) {
    func_in_10741(val);
    const int x = 10741;
    std::cout << 10741;
    for (int i = 0; i < 10741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10741(int& val) {
    const int x = 10741;
    std::cout << 10741;
    for (int i = 0; i < 10741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10742(int& val) {
    func_in_10742(val);
    const int x = 10742;
    std::cout << 10742;
    for (int i = 0; i < 10742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10742(int& val) {
    const int x = 10742;
    std::cout << 10742;
    for (int i = 0; i < 10742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10743(int& val) {
    func_in_10743(val);
    const int x = 10743;
    std::cout << 10743;
    for (int i = 0; i < 10743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10743(int& val) {
    const int x = 10743;
    std::cout << 10743;
    for (int i = 0; i < 10743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10744(int& val) {
    func_in_10744(val);
    const int x = 10744;
    std::cout << 10744;
    for (int i = 0; i < 10744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10744(int& val) {
    const int x = 10744;
    std::cout << 10744;
    for (int i = 0; i < 10744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10745(int& val) {
    func_in_10745(val);
    const int x = 10745;
    std::cout << 10745;
    for (int i = 0; i < 10745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10745(int& val) {
    const int x = 10745;
    std::cout << 10745;
    for (int i = 0; i < 10745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10746(int& val) {
    func_in_10746(val);
    const int x = 10746;
    std::cout << 10746;
    for (int i = 0; i < 10746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10746(int& val) {
    const int x = 10746;
    std::cout << 10746;
    for (int i = 0; i < 10746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10747(int& val) {
    func_in_10747(val);
    const int x = 10747;
    std::cout << 10747;
    for (int i = 0; i < 10747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10747(int& val) {
    const int x = 10747;
    std::cout << 10747;
    for (int i = 0; i < 10747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10748(int& val) {
    func_in_10748(val);
    const int x = 10748;
    std::cout << 10748;
    for (int i = 0; i < 10748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10748(int& val) {
    const int x = 10748;
    std::cout << 10748;
    for (int i = 0; i < 10748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10749(int& val) {
    func_in_10749(val);
    const int x = 10749;
    std::cout << 10749;
    for (int i = 0; i < 10749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10749(int& val) {
    const int x = 10749;
    std::cout << 10749;
    for (int i = 0; i < 10749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10750(int& val) {
    func_in_10750(val);
    const int x = 10750;
    std::cout << 10750;
    for (int i = 0; i < 10750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10750(int& val) {
    const int x = 10750;
    std::cout << 10750;
    for (int i = 0; i < 10750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10751(int& val) {
    func_in_10751(val);
    const int x = 10751;
    std::cout << 10751;
    for (int i = 0; i < 10751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10751(int& val) {
    const int x = 10751;
    std::cout << 10751;
    for (int i = 0; i < 10751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10752(int& val) {
    func_in_10752(val);
    const int x = 10752;
    std::cout << 10752;
    for (int i = 0; i < 10752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10752(int& val) {
    const int x = 10752;
    std::cout << 10752;
    for (int i = 0; i < 10752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10753(int& val) {
    func_in_10753(val);
    const int x = 10753;
    std::cout << 10753;
    for (int i = 0; i < 10753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10753(int& val) {
    const int x = 10753;
    std::cout << 10753;
    for (int i = 0; i < 10753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10754(int& val) {
    func_in_10754(val);
    const int x = 10754;
    std::cout << 10754;
    for (int i = 0; i < 10754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10754(int& val) {
    const int x = 10754;
    std::cout << 10754;
    for (int i = 0; i < 10754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10755(int& val) {
    func_in_10755(val);
    const int x = 10755;
    std::cout << 10755;
    for (int i = 0; i < 10755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10755(int& val) {
    const int x = 10755;
    std::cout << 10755;
    for (int i = 0; i < 10755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10756(int& val) {
    func_in_10756(val);
    const int x = 10756;
    std::cout << 10756;
    for (int i = 0; i < 10756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10756(int& val) {
    const int x = 10756;
    std::cout << 10756;
    for (int i = 0; i < 10756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10757(int& val) {
    func_in_10757(val);
    const int x = 10757;
    std::cout << 10757;
    for (int i = 0; i < 10757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10757(int& val) {
    const int x = 10757;
    std::cout << 10757;
    for (int i = 0; i < 10757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10758(int& val) {
    func_in_10758(val);
    const int x = 10758;
    std::cout << 10758;
    for (int i = 0; i < 10758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10758(int& val) {
    const int x = 10758;
    std::cout << 10758;
    for (int i = 0; i < 10758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10759(int& val) {
    func_in_10759(val);
    const int x = 10759;
    std::cout << 10759;
    for (int i = 0; i < 10759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10759(int& val) {
    const int x = 10759;
    std::cout << 10759;
    for (int i = 0; i < 10759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10760(int& val) {
    func_in_10760(val);
    const int x = 10760;
    std::cout << 10760;
    for (int i = 0; i < 10760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10760(int& val) {
    const int x = 10760;
    std::cout << 10760;
    for (int i = 0; i < 10760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10761(int& val) {
    func_in_10761(val);
    const int x = 10761;
    std::cout << 10761;
    for (int i = 0; i < 10761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10761(int& val) {
    const int x = 10761;
    std::cout << 10761;
    for (int i = 0; i < 10761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10762(int& val) {
    func_in_10762(val);
    const int x = 10762;
    std::cout << 10762;
    for (int i = 0; i < 10762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10762(int& val) {
    const int x = 10762;
    std::cout << 10762;
    for (int i = 0; i < 10762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10763(int& val) {
    func_in_10763(val);
    const int x = 10763;
    std::cout << 10763;
    for (int i = 0; i < 10763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10763(int& val) {
    const int x = 10763;
    std::cout << 10763;
    for (int i = 0; i < 10763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10764(int& val) {
    func_in_10764(val);
    const int x = 10764;
    std::cout << 10764;
    for (int i = 0; i < 10764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10764(int& val) {
    const int x = 10764;
    std::cout << 10764;
    for (int i = 0; i < 10764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10765(int& val) {
    func_in_10765(val);
    const int x = 10765;
    std::cout << 10765;
    for (int i = 0; i < 10765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10765(int& val) {
    const int x = 10765;
    std::cout << 10765;
    for (int i = 0; i < 10765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10766(int& val) {
    func_in_10766(val);
    const int x = 10766;
    std::cout << 10766;
    for (int i = 0; i < 10766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10766(int& val) {
    const int x = 10766;
    std::cout << 10766;
    for (int i = 0; i < 10766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10767(int& val) {
    func_in_10767(val);
    const int x = 10767;
    std::cout << 10767;
    for (int i = 0; i < 10767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10767(int& val) {
    const int x = 10767;
    std::cout << 10767;
    for (int i = 0; i < 10767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10768(int& val) {
    func_in_10768(val);
    const int x = 10768;
    std::cout << 10768;
    for (int i = 0; i < 10768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10768(int& val) {
    const int x = 10768;
    std::cout << 10768;
    for (int i = 0; i < 10768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10769(int& val) {
    func_in_10769(val);
    const int x = 10769;
    std::cout << 10769;
    for (int i = 0; i < 10769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10769(int& val) {
    const int x = 10769;
    std::cout << 10769;
    for (int i = 0; i < 10769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10770(int& val) {
    func_in_10770(val);
    const int x = 10770;
    std::cout << 10770;
    for (int i = 0; i < 10770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10770(int& val) {
    const int x = 10770;
    std::cout << 10770;
    for (int i = 0; i < 10770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10771(int& val) {
    func_in_10771(val);
    const int x = 10771;
    std::cout << 10771;
    for (int i = 0; i < 10771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10771(int& val) {
    const int x = 10771;
    std::cout << 10771;
    for (int i = 0; i < 10771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10772(int& val) {
    func_in_10772(val);
    const int x = 10772;
    std::cout << 10772;
    for (int i = 0; i < 10772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10772(int& val) {
    const int x = 10772;
    std::cout << 10772;
    for (int i = 0; i < 10772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10773(int& val) {
    func_in_10773(val);
    const int x = 10773;
    std::cout << 10773;
    for (int i = 0; i < 10773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10773(int& val) {
    const int x = 10773;
    std::cout << 10773;
    for (int i = 0; i < 10773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10774(int& val) {
    func_in_10774(val);
    const int x = 10774;
    std::cout << 10774;
    for (int i = 0; i < 10774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10774(int& val) {
    const int x = 10774;
    std::cout << 10774;
    for (int i = 0; i < 10774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10775(int& val) {
    func_in_10775(val);
    const int x = 10775;
    std::cout << 10775;
    for (int i = 0; i < 10775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10775(int& val) {
    const int x = 10775;
    std::cout << 10775;
    for (int i = 0; i < 10775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10776(int& val) {
    func_in_10776(val);
    const int x = 10776;
    std::cout << 10776;
    for (int i = 0; i < 10776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10776(int& val) {
    const int x = 10776;
    std::cout << 10776;
    for (int i = 0; i < 10776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10777(int& val) {
    func_in_10777(val);
    const int x = 10777;
    std::cout << 10777;
    for (int i = 0; i < 10777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10777(int& val) {
    const int x = 10777;
    std::cout << 10777;
    for (int i = 0; i < 10777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10778(int& val) {
    func_in_10778(val);
    const int x = 10778;
    std::cout << 10778;
    for (int i = 0; i < 10778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10778(int& val) {
    const int x = 10778;
    std::cout << 10778;
    for (int i = 0; i < 10778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10779(int& val) {
    func_in_10779(val);
    const int x = 10779;
    std::cout << 10779;
    for (int i = 0; i < 10779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10779(int& val) {
    const int x = 10779;
    std::cout << 10779;
    for (int i = 0; i < 10779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10780(int& val) {
    func_in_10780(val);
    const int x = 10780;
    std::cout << 10780;
    for (int i = 0; i < 10780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10780(int& val) {
    const int x = 10780;
    std::cout << 10780;
    for (int i = 0; i < 10780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10781(int& val) {
    func_in_10781(val);
    const int x = 10781;
    std::cout << 10781;
    for (int i = 0; i < 10781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10781(int& val) {
    const int x = 10781;
    std::cout << 10781;
    for (int i = 0; i < 10781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10782(int& val) {
    func_in_10782(val);
    const int x = 10782;
    std::cout << 10782;
    for (int i = 0; i < 10782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10782(int& val) {
    const int x = 10782;
    std::cout << 10782;
    for (int i = 0; i < 10782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10783(int& val) {
    func_in_10783(val);
    const int x = 10783;
    std::cout << 10783;
    for (int i = 0; i < 10783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10783(int& val) {
    const int x = 10783;
    std::cout << 10783;
    for (int i = 0; i < 10783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10784(int& val) {
    func_in_10784(val);
    const int x = 10784;
    std::cout << 10784;
    for (int i = 0; i < 10784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10784(int& val) {
    const int x = 10784;
    std::cout << 10784;
    for (int i = 0; i < 10784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10785(int& val) {
    func_in_10785(val);
    const int x = 10785;
    std::cout << 10785;
    for (int i = 0; i < 10785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10785(int& val) {
    const int x = 10785;
    std::cout << 10785;
    for (int i = 0; i < 10785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10786(int& val) {
    func_in_10786(val);
    const int x = 10786;
    std::cout << 10786;
    for (int i = 0; i < 10786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10786(int& val) {
    const int x = 10786;
    std::cout << 10786;
    for (int i = 0; i < 10786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10787(int& val) {
    func_in_10787(val);
    const int x = 10787;
    std::cout << 10787;
    for (int i = 0; i < 10787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10787(int& val) {
    const int x = 10787;
    std::cout << 10787;
    for (int i = 0; i < 10787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10788(int& val) {
    func_in_10788(val);
    const int x = 10788;
    std::cout << 10788;
    for (int i = 0; i < 10788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10788(int& val) {
    const int x = 10788;
    std::cout << 10788;
    for (int i = 0; i < 10788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10789(int& val) {
    func_in_10789(val);
    const int x = 10789;
    std::cout << 10789;
    for (int i = 0; i < 10789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10789(int& val) {
    const int x = 10789;
    std::cout << 10789;
    for (int i = 0; i < 10789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10790(int& val) {
    func_in_10790(val);
    const int x = 10790;
    std::cout << 10790;
    for (int i = 0; i < 10790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10790(int& val) {
    const int x = 10790;
    std::cout << 10790;
    for (int i = 0; i < 10790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10791(int& val) {
    func_in_10791(val);
    const int x = 10791;
    std::cout << 10791;
    for (int i = 0; i < 10791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10791(int& val) {
    const int x = 10791;
    std::cout << 10791;
    for (int i = 0; i < 10791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10792(int& val) {
    func_in_10792(val);
    const int x = 10792;
    std::cout << 10792;
    for (int i = 0; i < 10792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10792(int& val) {
    const int x = 10792;
    std::cout << 10792;
    for (int i = 0; i < 10792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10793(int& val) {
    func_in_10793(val);
    const int x = 10793;
    std::cout << 10793;
    for (int i = 0; i < 10793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10793(int& val) {
    const int x = 10793;
    std::cout << 10793;
    for (int i = 0; i < 10793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10794(int& val) {
    func_in_10794(val);
    const int x = 10794;
    std::cout << 10794;
    for (int i = 0; i < 10794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10794(int& val) {
    const int x = 10794;
    std::cout << 10794;
    for (int i = 0; i < 10794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10795(int& val) {
    func_in_10795(val);
    const int x = 10795;
    std::cout << 10795;
    for (int i = 0; i < 10795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10795(int& val) {
    const int x = 10795;
    std::cout << 10795;
    for (int i = 0; i < 10795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10796(int& val) {
    func_in_10796(val);
    const int x = 10796;
    std::cout << 10796;
    for (int i = 0; i < 10796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10796(int& val) {
    const int x = 10796;
    std::cout << 10796;
    for (int i = 0; i < 10796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10797(int& val) {
    func_in_10797(val);
    const int x = 10797;
    std::cout << 10797;
    for (int i = 0; i < 10797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10797(int& val) {
    const int x = 10797;
    std::cout << 10797;
    for (int i = 0; i < 10797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10798(int& val) {
    func_in_10798(val);
    const int x = 10798;
    std::cout << 10798;
    for (int i = 0; i < 10798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10798(int& val) {
    const int x = 10798;
    std::cout << 10798;
    for (int i = 0; i < 10798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10799(int& val) {
    func_in_10799(val);
    const int x = 10799;
    std::cout << 10799;
    for (int i = 0; i < 10799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10799(int& val) {
    const int x = 10799;
    std::cout << 10799;
    for (int i = 0; i < 10799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10800(int& val) {
    func_in_10800(val);
    const int x = 10800;
    std::cout << 10800;
    for (int i = 0; i < 10800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10800(int& val) {
    const int x = 10800;
    std::cout << 10800;
    for (int i = 0; i < 10800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10801(int& val) {
    func_in_10801(val);
    const int x = 10801;
    std::cout << 10801;
    for (int i = 0; i < 10801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10801(int& val) {
    const int x = 10801;
    std::cout << 10801;
    for (int i = 0; i < 10801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10802(int& val) {
    func_in_10802(val);
    const int x = 10802;
    std::cout << 10802;
    for (int i = 0; i < 10802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10802(int& val) {
    const int x = 10802;
    std::cout << 10802;
    for (int i = 0; i < 10802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10803(int& val) {
    func_in_10803(val);
    const int x = 10803;
    std::cout << 10803;
    for (int i = 0; i < 10803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10803(int& val) {
    const int x = 10803;
    std::cout << 10803;
    for (int i = 0; i < 10803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10804(int& val) {
    func_in_10804(val);
    const int x = 10804;
    std::cout << 10804;
    for (int i = 0; i < 10804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10804(int& val) {
    const int x = 10804;
    std::cout << 10804;
    for (int i = 0; i < 10804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10805(int& val) {
    func_in_10805(val);
    const int x = 10805;
    std::cout << 10805;
    for (int i = 0; i < 10805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10805(int& val) {
    const int x = 10805;
    std::cout << 10805;
    for (int i = 0; i < 10805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10806(int& val) {
    func_in_10806(val);
    const int x = 10806;
    std::cout << 10806;
    for (int i = 0; i < 10806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10806(int& val) {
    const int x = 10806;
    std::cout << 10806;
    for (int i = 0; i < 10806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10807(int& val) {
    func_in_10807(val);
    const int x = 10807;
    std::cout << 10807;
    for (int i = 0; i < 10807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10807(int& val) {
    const int x = 10807;
    std::cout << 10807;
    for (int i = 0; i < 10807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10808(int& val) {
    func_in_10808(val);
    const int x = 10808;
    std::cout << 10808;
    for (int i = 0; i < 10808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10808(int& val) {
    const int x = 10808;
    std::cout << 10808;
    for (int i = 0; i < 10808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10809(int& val) {
    func_in_10809(val);
    const int x = 10809;
    std::cout << 10809;
    for (int i = 0; i < 10809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10809(int& val) {
    const int x = 10809;
    std::cout << 10809;
    for (int i = 0; i < 10809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10810(int& val) {
    func_in_10810(val);
    const int x = 10810;
    std::cout << 10810;
    for (int i = 0; i < 10810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10810(int& val) {
    const int x = 10810;
    std::cout << 10810;
    for (int i = 0; i < 10810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10811(int& val) {
    func_in_10811(val);
    const int x = 10811;
    std::cout << 10811;
    for (int i = 0; i < 10811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10811(int& val) {
    const int x = 10811;
    std::cout << 10811;
    for (int i = 0; i < 10811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10812(int& val) {
    func_in_10812(val);
    const int x = 10812;
    std::cout << 10812;
    for (int i = 0; i < 10812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10812(int& val) {
    const int x = 10812;
    std::cout << 10812;
    for (int i = 0; i < 10812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10813(int& val) {
    func_in_10813(val);
    const int x = 10813;
    std::cout << 10813;
    for (int i = 0; i < 10813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10813(int& val) {
    const int x = 10813;
    std::cout << 10813;
    for (int i = 0; i < 10813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10814(int& val) {
    func_in_10814(val);
    const int x = 10814;
    std::cout << 10814;
    for (int i = 0; i < 10814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10814(int& val) {
    const int x = 10814;
    std::cout << 10814;
    for (int i = 0; i < 10814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10815(int& val) {
    func_in_10815(val);
    const int x = 10815;
    std::cout << 10815;
    for (int i = 0; i < 10815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10815(int& val) {
    const int x = 10815;
    std::cout << 10815;
    for (int i = 0; i < 10815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10816(int& val) {
    func_in_10816(val);
    const int x = 10816;
    std::cout << 10816;
    for (int i = 0; i < 10816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10816(int& val) {
    const int x = 10816;
    std::cout << 10816;
    for (int i = 0; i < 10816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10817(int& val) {
    func_in_10817(val);
    const int x = 10817;
    std::cout << 10817;
    for (int i = 0; i < 10817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10817(int& val) {
    const int x = 10817;
    std::cout << 10817;
    for (int i = 0; i < 10817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10818(int& val) {
    func_in_10818(val);
    const int x = 10818;
    std::cout << 10818;
    for (int i = 0; i < 10818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10818(int& val) {
    const int x = 10818;
    std::cout << 10818;
    for (int i = 0; i < 10818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10819(int& val) {
    func_in_10819(val);
    const int x = 10819;
    std::cout << 10819;
    for (int i = 0; i < 10819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10819(int& val) {
    const int x = 10819;
    std::cout << 10819;
    for (int i = 0; i < 10819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10820(int& val) {
    func_in_10820(val);
    const int x = 10820;
    std::cout << 10820;
    for (int i = 0; i < 10820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10820(int& val) {
    const int x = 10820;
    std::cout << 10820;
    for (int i = 0; i < 10820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10821(int& val) {
    func_in_10821(val);
    const int x = 10821;
    std::cout << 10821;
    for (int i = 0; i < 10821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10821(int& val) {
    const int x = 10821;
    std::cout << 10821;
    for (int i = 0; i < 10821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10822(int& val) {
    func_in_10822(val);
    const int x = 10822;
    std::cout << 10822;
    for (int i = 0; i < 10822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10822(int& val) {
    const int x = 10822;
    std::cout << 10822;
    for (int i = 0; i < 10822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10823(int& val) {
    func_in_10823(val);
    const int x = 10823;
    std::cout << 10823;
    for (int i = 0; i < 10823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10823(int& val) {
    const int x = 10823;
    std::cout << 10823;
    for (int i = 0; i < 10823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10824(int& val) {
    func_in_10824(val);
    const int x = 10824;
    std::cout << 10824;
    for (int i = 0; i < 10824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10824(int& val) {
    const int x = 10824;
    std::cout << 10824;
    for (int i = 0; i < 10824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10825(int& val) {
    func_in_10825(val);
    const int x = 10825;
    std::cout << 10825;
    for (int i = 0; i < 10825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10825(int& val) {
    const int x = 10825;
    std::cout << 10825;
    for (int i = 0; i < 10825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10826(int& val) {
    func_in_10826(val);
    const int x = 10826;
    std::cout << 10826;
    for (int i = 0; i < 10826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10826(int& val) {
    const int x = 10826;
    std::cout << 10826;
    for (int i = 0; i < 10826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10827(int& val) {
    func_in_10827(val);
    const int x = 10827;
    std::cout << 10827;
    for (int i = 0; i < 10827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10827(int& val) {
    const int x = 10827;
    std::cout << 10827;
    for (int i = 0; i < 10827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10828(int& val) {
    func_in_10828(val);
    const int x = 10828;
    std::cout << 10828;
    for (int i = 0; i < 10828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10828(int& val) {
    const int x = 10828;
    std::cout << 10828;
    for (int i = 0; i < 10828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10829(int& val) {
    func_in_10829(val);
    const int x = 10829;
    std::cout << 10829;
    for (int i = 0; i < 10829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10829(int& val) {
    const int x = 10829;
    std::cout << 10829;
    for (int i = 0; i < 10829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10830(int& val) {
    func_in_10830(val);
    const int x = 10830;
    std::cout << 10830;
    for (int i = 0; i < 10830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10830(int& val) {
    const int x = 10830;
    std::cout << 10830;
    for (int i = 0; i < 10830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10831(int& val) {
    func_in_10831(val);
    const int x = 10831;
    std::cout << 10831;
    for (int i = 0; i < 10831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10831(int& val) {
    const int x = 10831;
    std::cout << 10831;
    for (int i = 0; i < 10831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10832(int& val) {
    func_in_10832(val);
    const int x = 10832;
    std::cout << 10832;
    for (int i = 0; i < 10832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10832(int& val) {
    const int x = 10832;
    std::cout << 10832;
    for (int i = 0; i < 10832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10833(int& val) {
    func_in_10833(val);
    const int x = 10833;
    std::cout << 10833;
    for (int i = 0; i < 10833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10833(int& val) {
    const int x = 10833;
    std::cout << 10833;
    for (int i = 0; i < 10833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10834(int& val) {
    func_in_10834(val);
    const int x = 10834;
    std::cout << 10834;
    for (int i = 0; i < 10834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10834(int& val) {
    const int x = 10834;
    std::cout << 10834;
    for (int i = 0; i < 10834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10835(int& val) {
    func_in_10835(val);
    const int x = 10835;
    std::cout << 10835;
    for (int i = 0; i < 10835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10835(int& val) {
    const int x = 10835;
    std::cout << 10835;
    for (int i = 0; i < 10835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10836(int& val) {
    func_in_10836(val);
    const int x = 10836;
    std::cout << 10836;
    for (int i = 0; i < 10836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10836(int& val) {
    const int x = 10836;
    std::cout << 10836;
    for (int i = 0; i < 10836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10837(int& val) {
    func_in_10837(val);
    const int x = 10837;
    std::cout << 10837;
    for (int i = 0; i < 10837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10837(int& val) {
    const int x = 10837;
    std::cout << 10837;
    for (int i = 0; i < 10837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10838(int& val) {
    func_in_10838(val);
    const int x = 10838;
    std::cout << 10838;
    for (int i = 0; i < 10838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10838(int& val) {
    const int x = 10838;
    std::cout << 10838;
    for (int i = 0; i < 10838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10839(int& val) {
    func_in_10839(val);
    const int x = 10839;
    std::cout << 10839;
    for (int i = 0; i < 10839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10839(int& val) {
    const int x = 10839;
    std::cout << 10839;
    for (int i = 0; i < 10839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10840(int& val) {
    func_in_10840(val);
    const int x = 10840;
    std::cout << 10840;
    for (int i = 0; i < 10840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10840(int& val) {
    const int x = 10840;
    std::cout << 10840;
    for (int i = 0; i < 10840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10841(int& val) {
    func_in_10841(val);
    const int x = 10841;
    std::cout << 10841;
    for (int i = 0; i < 10841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10841(int& val) {
    const int x = 10841;
    std::cout << 10841;
    for (int i = 0; i < 10841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10842(int& val) {
    func_in_10842(val);
    const int x = 10842;
    std::cout << 10842;
    for (int i = 0; i < 10842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10842(int& val) {
    const int x = 10842;
    std::cout << 10842;
    for (int i = 0; i < 10842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10843(int& val) {
    func_in_10843(val);
    const int x = 10843;
    std::cout << 10843;
    for (int i = 0; i < 10843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10843(int& val) {
    const int x = 10843;
    std::cout << 10843;
    for (int i = 0; i < 10843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10844(int& val) {
    func_in_10844(val);
    const int x = 10844;
    std::cout << 10844;
    for (int i = 0; i < 10844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10844(int& val) {
    const int x = 10844;
    std::cout << 10844;
    for (int i = 0; i < 10844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10845(int& val) {
    func_in_10845(val);
    const int x = 10845;
    std::cout << 10845;
    for (int i = 0; i < 10845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10845(int& val) {
    const int x = 10845;
    std::cout << 10845;
    for (int i = 0; i < 10845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10846(int& val) {
    func_in_10846(val);
    const int x = 10846;
    std::cout << 10846;
    for (int i = 0; i < 10846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10846(int& val) {
    const int x = 10846;
    std::cout << 10846;
    for (int i = 0; i < 10846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10847(int& val) {
    func_in_10847(val);
    const int x = 10847;
    std::cout << 10847;
    for (int i = 0; i < 10847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10847(int& val) {
    const int x = 10847;
    std::cout << 10847;
    for (int i = 0; i < 10847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10848(int& val) {
    func_in_10848(val);
    const int x = 10848;
    std::cout << 10848;
    for (int i = 0; i < 10848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10848(int& val) {
    const int x = 10848;
    std::cout << 10848;
    for (int i = 0; i < 10848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10849(int& val) {
    func_in_10849(val);
    const int x = 10849;
    std::cout << 10849;
    for (int i = 0; i < 10849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10849(int& val) {
    const int x = 10849;
    std::cout << 10849;
    for (int i = 0; i < 10849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10850(int& val) {
    func_in_10850(val);
    const int x = 10850;
    std::cout << 10850;
    for (int i = 0; i < 10850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10850(int& val) {
    const int x = 10850;
    std::cout << 10850;
    for (int i = 0; i < 10850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10851(int& val) {
    func_in_10851(val);
    const int x = 10851;
    std::cout << 10851;
    for (int i = 0; i < 10851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10851(int& val) {
    const int x = 10851;
    std::cout << 10851;
    for (int i = 0; i < 10851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10852(int& val) {
    func_in_10852(val);
    const int x = 10852;
    std::cout << 10852;
    for (int i = 0; i < 10852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10852(int& val) {
    const int x = 10852;
    std::cout << 10852;
    for (int i = 0; i < 10852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10853(int& val) {
    func_in_10853(val);
    const int x = 10853;
    std::cout << 10853;
    for (int i = 0; i < 10853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10853(int& val) {
    const int x = 10853;
    std::cout << 10853;
    for (int i = 0; i < 10853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10854(int& val) {
    func_in_10854(val);
    const int x = 10854;
    std::cout << 10854;
    for (int i = 0; i < 10854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10854(int& val) {
    const int x = 10854;
    std::cout << 10854;
    for (int i = 0; i < 10854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10855(int& val) {
    func_in_10855(val);
    const int x = 10855;
    std::cout << 10855;
    for (int i = 0; i < 10855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10855(int& val) {
    const int x = 10855;
    std::cout << 10855;
    for (int i = 0; i < 10855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10856(int& val) {
    func_in_10856(val);
    const int x = 10856;
    std::cout << 10856;
    for (int i = 0; i < 10856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10856(int& val) {
    const int x = 10856;
    std::cout << 10856;
    for (int i = 0; i < 10856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10857(int& val) {
    func_in_10857(val);
    const int x = 10857;
    std::cout << 10857;
    for (int i = 0; i < 10857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10857(int& val) {
    const int x = 10857;
    std::cout << 10857;
    for (int i = 0; i < 10857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10858(int& val) {
    func_in_10858(val);
    const int x = 10858;
    std::cout << 10858;
    for (int i = 0; i < 10858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10858(int& val) {
    const int x = 10858;
    std::cout << 10858;
    for (int i = 0; i < 10858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10859(int& val) {
    func_in_10859(val);
    const int x = 10859;
    std::cout << 10859;
    for (int i = 0; i < 10859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10859(int& val) {
    const int x = 10859;
    std::cout << 10859;
    for (int i = 0; i < 10859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10860(int& val) {
    func_in_10860(val);
    const int x = 10860;
    std::cout << 10860;
    for (int i = 0; i < 10860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10860(int& val) {
    const int x = 10860;
    std::cout << 10860;
    for (int i = 0; i < 10860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10861(int& val) {
    func_in_10861(val);
    const int x = 10861;
    std::cout << 10861;
    for (int i = 0; i < 10861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10861(int& val) {
    const int x = 10861;
    std::cout << 10861;
    for (int i = 0; i < 10861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10862(int& val) {
    func_in_10862(val);
    const int x = 10862;
    std::cout << 10862;
    for (int i = 0; i < 10862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10862(int& val) {
    const int x = 10862;
    std::cout << 10862;
    for (int i = 0; i < 10862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10863(int& val) {
    func_in_10863(val);
    const int x = 10863;
    std::cout << 10863;
    for (int i = 0; i < 10863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10863(int& val) {
    const int x = 10863;
    std::cout << 10863;
    for (int i = 0; i < 10863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10864(int& val) {
    func_in_10864(val);
    const int x = 10864;
    std::cout << 10864;
    for (int i = 0; i < 10864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10864(int& val) {
    const int x = 10864;
    std::cout << 10864;
    for (int i = 0; i < 10864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10865(int& val) {
    func_in_10865(val);
    const int x = 10865;
    std::cout << 10865;
    for (int i = 0; i < 10865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10865(int& val) {
    const int x = 10865;
    std::cout << 10865;
    for (int i = 0; i < 10865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10866(int& val) {
    func_in_10866(val);
    const int x = 10866;
    std::cout << 10866;
    for (int i = 0; i < 10866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10866(int& val) {
    const int x = 10866;
    std::cout << 10866;
    for (int i = 0; i < 10866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10867(int& val) {
    func_in_10867(val);
    const int x = 10867;
    std::cout << 10867;
    for (int i = 0; i < 10867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10867(int& val) {
    const int x = 10867;
    std::cout << 10867;
    for (int i = 0; i < 10867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10868(int& val) {
    func_in_10868(val);
    const int x = 10868;
    std::cout << 10868;
    for (int i = 0; i < 10868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10868(int& val) {
    const int x = 10868;
    std::cout << 10868;
    for (int i = 0; i < 10868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10869(int& val) {
    func_in_10869(val);
    const int x = 10869;
    std::cout << 10869;
    for (int i = 0; i < 10869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10869(int& val) {
    const int x = 10869;
    std::cout << 10869;
    for (int i = 0; i < 10869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10870(int& val) {
    func_in_10870(val);
    const int x = 10870;
    std::cout << 10870;
    for (int i = 0; i < 10870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10870(int& val) {
    const int x = 10870;
    std::cout << 10870;
    for (int i = 0; i < 10870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10871(int& val) {
    func_in_10871(val);
    const int x = 10871;
    std::cout << 10871;
    for (int i = 0; i < 10871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10871(int& val) {
    const int x = 10871;
    std::cout << 10871;
    for (int i = 0; i < 10871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10872(int& val) {
    func_in_10872(val);
    const int x = 10872;
    std::cout << 10872;
    for (int i = 0; i < 10872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10872(int& val) {
    const int x = 10872;
    std::cout << 10872;
    for (int i = 0; i < 10872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10873(int& val) {
    func_in_10873(val);
    const int x = 10873;
    std::cout << 10873;
    for (int i = 0; i < 10873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10873(int& val) {
    const int x = 10873;
    std::cout << 10873;
    for (int i = 0; i < 10873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10874(int& val) {
    func_in_10874(val);
    const int x = 10874;
    std::cout << 10874;
    for (int i = 0; i < 10874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10874(int& val) {
    const int x = 10874;
    std::cout << 10874;
    for (int i = 0; i < 10874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10875(int& val) {
    func_in_10875(val);
    const int x = 10875;
    std::cout << 10875;
    for (int i = 0; i < 10875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10875(int& val) {
    const int x = 10875;
    std::cout << 10875;
    for (int i = 0; i < 10875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10876(int& val) {
    func_in_10876(val);
    const int x = 10876;
    std::cout << 10876;
    for (int i = 0; i < 10876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10876(int& val) {
    const int x = 10876;
    std::cout << 10876;
    for (int i = 0; i < 10876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10877(int& val) {
    func_in_10877(val);
    const int x = 10877;
    std::cout << 10877;
    for (int i = 0; i < 10877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10877(int& val) {
    const int x = 10877;
    std::cout << 10877;
    for (int i = 0; i < 10877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10878(int& val) {
    func_in_10878(val);
    const int x = 10878;
    std::cout << 10878;
    for (int i = 0; i < 10878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10878(int& val) {
    const int x = 10878;
    std::cout << 10878;
    for (int i = 0; i < 10878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10879(int& val) {
    func_in_10879(val);
    const int x = 10879;
    std::cout << 10879;
    for (int i = 0; i < 10879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10879(int& val) {
    const int x = 10879;
    std::cout << 10879;
    for (int i = 0; i < 10879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10880(int& val) {
    func_in_10880(val);
    const int x = 10880;
    std::cout << 10880;
    for (int i = 0; i < 10880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10880(int& val) {
    const int x = 10880;
    std::cout << 10880;
    for (int i = 0; i < 10880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10881(int& val) {
    func_in_10881(val);
    const int x = 10881;
    std::cout << 10881;
    for (int i = 0; i < 10881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10881(int& val) {
    const int x = 10881;
    std::cout << 10881;
    for (int i = 0; i < 10881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10882(int& val) {
    func_in_10882(val);
    const int x = 10882;
    std::cout << 10882;
    for (int i = 0; i < 10882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10882(int& val) {
    const int x = 10882;
    std::cout << 10882;
    for (int i = 0; i < 10882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10883(int& val) {
    func_in_10883(val);
    const int x = 10883;
    std::cout << 10883;
    for (int i = 0; i < 10883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10883(int& val) {
    const int x = 10883;
    std::cout << 10883;
    for (int i = 0; i < 10883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10884(int& val) {
    func_in_10884(val);
    const int x = 10884;
    std::cout << 10884;
    for (int i = 0; i < 10884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10884(int& val) {
    const int x = 10884;
    std::cout << 10884;
    for (int i = 0; i < 10884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10885(int& val) {
    func_in_10885(val);
    const int x = 10885;
    std::cout << 10885;
    for (int i = 0; i < 10885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10885(int& val) {
    const int x = 10885;
    std::cout << 10885;
    for (int i = 0; i < 10885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10886(int& val) {
    func_in_10886(val);
    const int x = 10886;
    std::cout << 10886;
    for (int i = 0; i < 10886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10886(int& val) {
    const int x = 10886;
    std::cout << 10886;
    for (int i = 0; i < 10886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10887(int& val) {
    func_in_10887(val);
    const int x = 10887;
    std::cout << 10887;
    for (int i = 0; i < 10887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10887(int& val) {
    const int x = 10887;
    std::cout << 10887;
    for (int i = 0; i < 10887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10888(int& val) {
    func_in_10888(val);
    const int x = 10888;
    std::cout << 10888;
    for (int i = 0; i < 10888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10888(int& val) {
    const int x = 10888;
    std::cout << 10888;
    for (int i = 0; i < 10888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10889(int& val) {
    func_in_10889(val);
    const int x = 10889;
    std::cout << 10889;
    for (int i = 0; i < 10889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10889(int& val) {
    const int x = 10889;
    std::cout << 10889;
    for (int i = 0; i < 10889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10890(int& val) {
    func_in_10890(val);
    const int x = 10890;
    std::cout << 10890;
    for (int i = 0; i < 10890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10890(int& val) {
    const int x = 10890;
    std::cout << 10890;
    for (int i = 0; i < 10890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10891(int& val) {
    func_in_10891(val);
    const int x = 10891;
    std::cout << 10891;
    for (int i = 0; i < 10891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10891(int& val) {
    const int x = 10891;
    std::cout << 10891;
    for (int i = 0; i < 10891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10892(int& val) {
    func_in_10892(val);
    const int x = 10892;
    std::cout << 10892;
    for (int i = 0; i < 10892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10892(int& val) {
    const int x = 10892;
    std::cout << 10892;
    for (int i = 0; i < 10892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10893(int& val) {
    func_in_10893(val);
    const int x = 10893;
    std::cout << 10893;
    for (int i = 0; i < 10893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10893(int& val) {
    const int x = 10893;
    std::cout << 10893;
    for (int i = 0; i < 10893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10894(int& val) {
    func_in_10894(val);
    const int x = 10894;
    std::cout << 10894;
    for (int i = 0; i < 10894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10894(int& val) {
    const int x = 10894;
    std::cout << 10894;
    for (int i = 0; i < 10894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10895(int& val) {
    func_in_10895(val);
    const int x = 10895;
    std::cout << 10895;
    for (int i = 0; i < 10895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10895(int& val) {
    const int x = 10895;
    std::cout << 10895;
    for (int i = 0; i < 10895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10896(int& val) {
    func_in_10896(val);
    const int x = 10896;
    std::cout << 10896;
    for (int i = 0; i < 10896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10896(int& val) {
    const int x = 10896;
    std::cout << 10896;
    for (int i = 0; i < 10896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10897(int& val) {
    func_in_10897(val);
    const int x = 10897;
    std::cout << 10897;
    for (int i = 0; i < 10897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10897(int& val) {
    const int x = 10897;
    std::cout << 10897;
    for (int i = 0; i < 10897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10898(int& val) {
    func_in_10898(val);
    const int x = 10898;
    std::cout << 10898;
    for (int i = 0; i < 10898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10898(int& val) {
    const int x = 10898;
    std::cout << 10898;
    for (int i = 0; i < 10898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10899(int& val) {
    func_in_10899(val);
    const int x = 10899;
    std::cout << 10899;
    for (int i = 0; i < 10899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10899(int& val) {
    const int x = 10899;
    std::cout << 10899;
    for (int i = 0; i < 10899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10900(int& val) {
    func_in_10900(val);
    const int x = 10900;
    std::cout << 10900;
    for (int i = 0; i < 10900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10900(int& val) {
    const int x = 10900;
    std::cout << 10900;
    for (int i = 0; i < 10900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10901(int& val) {
    func_in_10901(val);
    const int x = 10901;
    std::cout << 10901;
    for (int i = 0; i < 10901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10901(int& val) {
    const int x = 10901;
    std::cout << 10901;
    for (int i = 0; i < 10901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10902(int& val) {
    func_in_10902(val);
    const int x = 10902;
    std::cout << 10902;
    for (int i = 0; i < 10902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10902(int& val) {
    const int x = 10902;
    std::cout << 10902;
    for (int i = 0; i < 10902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10903(int& val) {
    func_in_10903(val);
    const int x = 10903;
    std::cout << 10903;
    for (int i = 0; i < 10903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10903(int& val) {
    const int x = 10903;
    std::cout << 10903;
    for (int i = 0; i < 10903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10904(int& val) {
    func_in_10904(val);
    const int x = 10904;
    std::cout << 10904;
    for (int i = 0; i < 10904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10904(int& val) {
    const int x = 10904;
    std::cout << 10904;
    for (int i = 0; i < 10904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10905(int& val) {
    func_in_10905(val);
    const int x = 10905;
    std::cout << 10905;
    for (int i = 0; i < 10905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10905(int& val) {
    const int x = 10905;
    std::cout << 10905;
    for (int i = 0; i < 10905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10906(int& val) {
    func_in_10906(val);
    const int x = 10906;
    std::cout << 10906;
    for (int i = 0; i < 10906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10906(int& val) {
    const int x = 10906;
    std::cout << 10906;
    for (int i = 0; i < 10906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10907(int& val) {
    func_in_10907(val);
    const int x = 10907;
    std::cout << 10907;
    for (int i = 0; i < 10907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10907(int& val) {
    const int x = 10907;
    std::cout << 10907;
    for (int i = 0; i < 10907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10908(int& val) {
    func_in_10908(val);
    const int x = 10908;
    std::cout << 10908;
    for (int i = 0; i < 10908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10908(int& val) {
    const int x = 10908;
    std::cout << 10908;
    for (int i = 0; i < 10908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10909(int& val) {
    func_in_10909(val);
    const int x = 10909;
    std::cout << 10909;
    for (int i = 0; i < 10909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10909(int& val) {
    const int x = 10909;
    std::cout << 10909;
    for (int i = 0; i < 10909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10910(int& val) {
    func_in_10910(val);
    const int x = 10910;
    std::cout << 10910;
    for (int i = 0; i < 10910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10910(int& val) {
    const int x = 10910;
    std::cout << 10910;
    for (int i = 0; i < 10910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10911(int& val) {
    func_in_10911(val);
    const int x = 10911;
    std::cout << 10911;
    for (int i = 0; i < 10911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10911(int& val) {
    const int x = 10911;
    std::cout << 10911;
    for (int i = 0; i < 10911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10912(int& val) {
    func_in_10912(val);
    const int x = 10912;
    std::cout << 10912;
    for (int i = 0; i < 10912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10912(int& val) {
    const int x = 10912;
    std::cout << 10912;
    for (int i = 0; i < 10912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10913(int& val) {
    func_in_10913(val);
    const int x = 10913;
    std::cout << 10913;
    for (int i = 0; i < 10913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10913(int& val) {
    const int x = 10913;
    std::cout << 10913;
    for (int i = 0; i < 10913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10914(int& val) {
    func_in_10914(val);
    const int x = 10914;
    std::cout << 10914;
    for (int i = 0; i < 10914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10914(int& val) {
    const int x = 10914;
    std::cout << 10914;
    for (int i = 0; i < 10914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10915(int& val) {
    func_in_10915(val);
    const int x = 10915;
    std::cout << 10915;
    for (int i = 0; i < 10915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10915(int& val) {
    const int x = 10915;
    std::cout << 10915;
    for (int i = 0; i < 10915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10916(int& val) {
    func_in_10916(val);
    const int x = 10916;
    std::cout << 10916;
    for (int i = 0; i < 10916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10916(int& val) {
    const int x = 10916;
    std::cout << 10916;
    for (int i = 0; i < 10916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10917(int& val) {
    func_in_10917(val);
    const int x = 10917;
    std::cout << 10917;
    for (int i = 0; i < 10917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10917(int& val) {
    const int x = 10917;
    std::cout << 10917;
    for (int i = 0; i < 10917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10918(int& val) {
    func_in_10918(val);
    const int x = 10918;
    std::cout << 10918;
    for (int i = 0; i < 10918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10918(int& val) {
    const int x = 10918;
    std::cout << 10918;
    for (int i = 0; i < 10918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10919(int& val) {
    func_in_10919(val);
    const int x = 10919;
    std::cout << 10919;
    for (int i = 0; i < 10919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10919(int& val) {
    const int x = 10919;
    std::cout << 10919;
    for (int i = 0; i < 10919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10920(int& val) {
    func_in_10920(val);
    const int x = 10920;
    std::cout << 10920;
    for (int i = 0; i < 10920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10920(int& val) {
    const int x = 10920;
    std::cout << 10920;
    for (int i = 0; i < 10920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10921(int& val) {
    func_in_10921(val);
    const int x = 10921;
    std::cout << 10921;
    for (int i = 0; i < 10921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10921(int& val) {
    const int x = 10921;
    std::cout << 10921;
    for (int i = 0; i < 10921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10922(int& val) {
    func_in_10922(val);
    const int x = 10922;
    std::cout << 10922;
    for (int i = 0; i < 10922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10922(int& val) {
    const int x = 10922;
    std::cout << 10922;
    for (int i = 0; i < 10922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10923(int& val) {
    func_in_10923(val);
    const int x = 10923;
    std::cout << 10923;
    for (int i = 0; i < 10923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10923(int& val) {
    const int x = 10923;
    std::cout << 10923;
    for (int i = 0; i < 10923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10924(int& val) {
    func_in_10924(val);
    const int x = 10924;
    std::cout << 10924;
    for (int i = 0; i < 10924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10924(int& val) {
    const int x = 10924;
    std::cout << 10924;
    for (int i = 0; i < 10924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10925(int& val) {
    func_in_10925(val);
    const int x = 10925;
    std::cout << 10925;
    for (int i = 0; i < 10925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10925(int& val) {
    const int x = 10925;
    std::cout << 10925;
    for (int i = 0; i < 10925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10926(int& val) {
    func_in_10926(val);
    const int x = 10926;
    std::cout << 10926;
    for (int i = 0; i < 10926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10926(int& val) {
    const int x = 10926;
    std::cout << 10926;
    for (int i = 0; i < 10926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10927(int& val) {
    func_in_10927(val);
    const int x = 10927;
    std::cout << 10927;
    for (int i = 0; i < 10927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10927(int& val) {
    const int x = 10927;
    std::cout << 10927;
    for (int i = 0; i < 10927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10928(int& val) {
    func_in_10928(val);
    const int x = 10928;
    std::cout << 10928;
    for (int i = 0; i < 10928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10928(int& val) {
    const int x = 10928;
    std::cout << 10928;
    for (int i = 0; i < 10928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10929(int& val) {
    func_in_10929(val);
    const int x = 10929;
    std::cout << 10929;
    for (int i = 0; i < 10929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10929(int& val) {
    const int x = 10929;
    std::cout << 10929;
    for (int i = 0; i < 10929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10930(int& val) {
    func_in_10930(val);
    const int x = 10930;
    std::cout << 10930;
    for (int i = 0; i < 10930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10930(int& val) {
    const int x = 10930;
    std::cout << 10930;
    for (int i = 0; i < 10930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10931(int& val) {
    func_in_10931(val);
    const int x = 10931;
    std::cout << 10931;
    for (int i = 0; i < 10931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10931(int& val) {
    const int x = 10931;
    std::cout << 10931;
    for (int i = 0; i < 10931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10932(int& val) {
    func_in_10932(val);
    const int x = 10932;
    std::cout << 10932;
    for (int i = 0; i < 10932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10932(int& val) {
    const int x = 10932;
    std::cout << 10932;
    for (int i = 0; i < 10932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10933(int& val) {
    func_in_10933(val);
    const int x = 10933;
    std::cout << 10933;
    for (int i = 0; i < 10933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10933(int& val) {
    const int x = 10933;
    std::cout << 10933;
    for (int i = 0; i < 10933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10934(int& val) {
    func_in_10934(val);
    const int x = 10934;
    std::cout << 10934;
    for (int i = 0; i < 10934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10934(int& val) {
    const int x = 10934;
    std::cout << 10934;
    for (int i = 0; i < 10934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10935(int& val) {
    func_in_10935(val);
    const int x = 10935;
    std::cout << 10935;
    for (int i = 0; i < 10935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10935(int& val) {
    const int x = 10935;
    std::cout << 10935;
    for (int i = 0; i < 10935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10936(int& val) {
    func_in_10936(val);
    const int x = 10936;
    std::cout << 10936;
    for (int i = 0; i < 10936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10936(int& val) {
    const int x = 10936;
    std::cout << 10936;
    for (int i = 0; i < 10936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10937(int& val) {
    func_in_10937(val);
    const int x = 10937;
    std::cout << 10937;
    for (int i = 0; i < 10937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10937(int& val) {
    const int x = 10937;
    std::cout << 10937;
    for (int i = 0; i < 10937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10938(int& val) {
    func_in_10938(val);
    const int x = 10938;
    std::cout << 10938;
    for (int i = 0; i < 10938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10938(int& val) {
    const int x = 10938;
    std::cout << 10938;
    for (int i = 0; i < 10938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10939(int& val) {
    func_in_10939(val);
    const int x = 10939;
    std::cout << 10939;
    for (int i = 0; i < 10939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10939(int& val) {
    const int x = 10939;
    std::cout << 10939;
    for (int i = 0; i < 10939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10940(int& val) {
    func_in_10940(val);
    const int x = 10940;
    std::cout << 10940;
    for (int i = 0; i < 10940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10940(int& val) {
    const int x = 10940;
    std::cout << 10940;
    for (int i = 0; i < 10940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10941(int& val) {
    func_in_10941(val);
    const int x = 10941;
    std::cout << 10941;
    for (int i = 0; i < 10941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10941(int& val) {
    const int x = 10941;
    std::cout << 10941;
    for (int i = 0; i < 10941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10942(int& val) {
    func_in_10942(val);
    const int x = 10942;
    std::cout << 10942;
    for (int i = 0; i < 10942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10942(int& val) {
    const int x = 10942;
    std::cout << 10942;
    for (int i = 0; i < 10942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10943(int& val) {
    func_in_10943(val);
    const int x = 10943;
    std::cout << 10943;
    for (int i = 0; i < 10943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10943(int& val) {
    const int x = 10943;
    std::cout << 10943;
    for (int i = 0; i < 10943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10944(int& val) {
    func_in_10944(val);
    const int x = 10944;
    std::cout << 10944;
    for (int i = 0; i < 10944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10944(int& val) {
    const int x = 10944;
    std::cout << 10944;
    for (int i = 0; i < 10944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10945(int& val) {
    func_in_10945(val);
    const int x = 10945;
    std::cout << 10945;
    for (int i = 0; i < 10945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10945(int& val) {
    const int x = 10945;
    std::cout << 10945;
    for (int i = 0; i < 10945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10946(int& val) {
    func_in_10946(val);
    const int x = 10946;
    std::cout << 10946;
    for (int i = 0; i < 10946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10946(int& val) {
    const int x = 10946;
    std::cout << 10946;
    for (int i = 0; i < 10946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10947(int& val) {
    func_in_10947(val);
    const int x = 10947;
    std::cout << 10947;
    for (int i = 0; i < 10947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10947(int& val) {
    const int x = 10947;
    std::cout << 10947;
    for (int i = 0; i < 10947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10948(int& val) {
    func_in_10948(val);
    const int x = 10948;
    std::cout << 10948;
    for (int i = 0; i < 10948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10948(int& val) {
    const int x = 10948;
    std::cout << 10948;
    for (int i = 0; i < 10948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10949(int& val) {
    func_in_10949(val);
    const int x = 10949;
    std::cout << 10949;
    for (int i = 0; i < 10949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10949(int& val) {
    const int x = 10949;
    std::cout << 10949;
    for (int i = 0; i < 10949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10950(int& val) {
    func_in_10950(val);
    const int x = 10950;
    std::cout << 10950;
    for (int i = 0; i < 10950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10950(int& val) {
    const int x = 10950;
    std::cout << 10950;
    for (int i = 0; i < 10950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10951(int& val) {
    func_in_10951(val);
    const int x = 10951;
    std::cout << 10951;
    for (int i = 0; i < 10951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10951(int& val) {
    const int x = 10951;
    std::cout << 10951;
    for (int i = 0; i < 10951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10952(int& val) {
    func_in_10952(val);
    const int x = 10952;
    std::cout << 10952;
    for (int i = 0; i < 10952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10952(int& val) {
    const int x = 10952;
    std::cout << 10952;
    for (int i = 0; i < 10952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10953(int& val) {
    func_in_10953(val);
    const int x = 10953;
    std::cout << 10953;
    for (int i = 0; i < 10953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10953(int& val) {
    const int x = 10953;
    std::cout << 10953;
    for (int i = 0; i < 10953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10954(int& val) {
    func_in_10954(val);
    const int x = 10954;
    std::cout << 10954;
    for (int i = 0; i < 10954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10954(int& val) {
    const int x = 10954;
    std::cout << 10954;
    for (int i = 0; i < 10954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10955(int& val) {
    func_in_10955(val);
    const int x = 10955;
    std::cout << 10955;
    for (int i = 0; i < 10955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10955(int& val) {
    const int x = 10955;
    std::cout << 10955;
    for (int i = 0; i < 10955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10956(int& val) {
    func_in_10956(val);
    const int x = 10956;
    std::cout << 10956;
    for (int i = 0; i < 10956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10956(int& val) {
    const int x = 10956;
    std::cout << 10956;
    for (int i = 0; i < 10956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10957(int& val) {
    func_in_10957(val);
    const int x = 10957;
    std::cout << 10957;
    for (int i = 0; i < 10957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10957(int& val) {
    const int x = 10957;
    std::cout << 10957;
    for (int i = 0; i < 10957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10958(int& val) {
    func_in_10958(val);
    const int x = 10958;
    std::cout << 10958;
    for (int i = 0; i < 10958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10958(int& val) {
    const int x = 10958;
    std::cout << 10958;
    for (int i = 0; i < 10958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10959(int& val) {
    func_in_10959(val);
    const int x = 10959;
    std::cout << 10959;
    for (int i = 0; i < 10959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10959(int& val) {
    const int x = 10959;
    std::cout << 10959;
    for (int i = 0; i < 10959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10960(int& val) {
    func_in_10960(val);
    const int x = 10960;
    std::cout << 10960;
    for (int i = 0; i < 10960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10960(int& val) {
    const int x = 10960;
    std::cout << 10960;
    for (int i = 0; i < 10960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10961(int& val) {
    func_in_10961(val);
    const int x = 10961;
    std::cout << 10961;
    for (int i = 0; i < 10961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10961(int& val) {
    const int x = 10961;
    std::cout << 10961;
    for (int i = 0; i < 10961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10962(int& val) {
    func_in_10962(val);
    const int x = 10962;
    std::cout << 10962;
    for (int i = 0; i < 10962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10962(int& val) {
    const int x = 10962;
    std::cout << 10962;
    for (int i = 0; i < 10962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10963(int& val) {
    func_in_10963(val);
    const int x = 10963;
    std::cout << 10963;
    for (int i = 0; i < 10963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10963(int& val) {
    const int x = 10963;
    std::cout << 10963;
    for (int i = 0; i < 10963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10964(int& val) {
    func_in_10964(val);
    const int x = 10964;
    std::cout << 10964;
    for (int i = 0; i < 10964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10964(int& val) {
    const int x = 10964;
    std::cout << 10964;
    for (int i = 0; i < 10964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10965(int& val) {
    func_in_10965(val);
    const int x = 10965;
    std::cout << 10965;
    for (int i = 0; i < 10965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10965(int& val) {
    const int x = 10965;
    std::cout << 10965;
    for (int i = 0; i < 10965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10966(int& val) {
    func_in_10966(val);
    const int x = 10966;
    std::cout << 10966;
    for (int i = 0; i < 10966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10966(int& val) {
    const int x = 10966;
    std::cout << 10966;
    for (int i = 0; i < 10966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10967(int& val) {
    func_in_10967(val);
    const int x = 10967;
    std::cout << 10967;
    for (int i = 0; i < 10967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10967(int& val) {
    const int x = 10967;
    std::cout << 10967;
    for (int i = 0; i < 10967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10968(int& val) {
    func_in_10968(val);
    const int x = 10968;
    std::cout << 10968;
    for (int i = 0; i < 10968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10968(int& val) {
    const int x = 10968;
    std::cout << 10968;
    for (int i = 0; i < 10968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10969(int& val) {
    func_in_10969(val);
    const int x = 10969;
    std::cout << 10969;
    for (int i = 0; i < 10969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10969(int& val) {
    const int x = 10969;
    std::cout << 10969;
    for (int i = 0; i < 10969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10970(int& val) {
    func_in_10970(val);
    const int x = 10970;
    std::cout << 10970;
    for (int i = 0; i < 10970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10970(int& val) {
    const int x = 10970;
    std::cout << 10970;
    for (int i = 0; i < 10970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10971(int& val) {
    func_in_10971(val);
    const int x = 10971;
    std::cout << 10971;
    for (int i = 0; i < 10971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10971(int& val) {
    const int x = 10971;
    std::cout << 10971;
    for (int i = 0; i < 10971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10972(int& val) {
    func_in_10972(val);
    const int x = 10972;
    std::cout << 10972;
    for (int i = 0; i < 10972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10972(int& val) {
    const int x = 10972;
    std::cout << 10972;
    for (int i = 0; i < 10972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10973(int& val) {
    func_in_10973(val);
    const int x = 10973;
    std::cout << 10973;
    for (int i = 0; i < 10973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10973(int& val) {
    const int x = 10973;
    std::cout << 10973;
    for (int i = 0; i < 10973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10974(int& val) {
    func_in_10974(val);
    const int x = 10974;
    std::cout << 10974;
    for (int i = 0; i < 10974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10974(int& val) {
    const int x = 10974;
    std::cout << 10974;
    for (int i = 0; i < 10974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10975(int& val) {
    func_in_10975(val);
    const int x = 10975;
    std::cout << 10975;
    for (int i = 0; i < 10975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10975(int& val) {
    const int x = 10975;
    std::cout << 10975;
    for (int i = 0; i < 10975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10976(int& val) {
    func_in_10976(val);
    const int x = 10976;
    std::cout << 10976;
    for (int i = 0; i < 10976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10976(int& val) {
    const int x = 10976;
    std::cout << 10976;
    for (int i = 0; i < 10976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10977(int& val) {
    func_in_10977(val);
    const int x = 10977;
    std::cout << 10977;
    for (int i = 0; i < 10977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10977(int& val) {
    const int x = 10977;
    std::cout << 10977;
    for (int i = 0; i < 10977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10978(int& val) {
    func_in_10978(val);
    const int x = 10978;
    std::cout << 10978;
    for (int i = 0; i < 10978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10978(int& val) {
    const int x = 10978;
    std::cout << 10978;
    for (int i = 0; i < 10978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10979(int& val) {
    func_in_10979(val);
    const int x = 10979;
    std::cout << 10979;
    for (int i = 0; i < 10979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10979(int& val) {
    const int x = 10979;
    std::cout << 10979;
    for (int i = 0; i < 10979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10980(int& val) {
    func_in_10980(val);
    const int x = 10980;
    std::cout << 10980;
    for (int i = 0; i < 10980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10980(int& val) {
    const int x = 10980;
    std::cout << 10980;
    for (int i = 0; i < 10980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10981(int& val) {
    func_in_10981(val);
    const int x = 10981;
    std::cout << 10981;
    for (int i = 0; i < 10981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10981(int& val) {
    const int x = 10981;
    std::cout << 10981;
    for (int i = 0; i < 10981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10982(int& val) {
    func_in_10982(val);
    const int x = 10982;
    std::cout << 10982;
    for (int i = 0; i < 10982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10982(int& val) {
    const int x = 10982;
    std::cout << 10982;
    for (int i = 0; i < 10982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10983(int& val) {
    func_in_10983(val);
    const int x = 10983;
    std::cout << 10983;
    for (int i = 0; i < 10983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10983(int& val) {
    const int x = 10983;
    std::cout << 10983;
    for (int i = 0; i < 10983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10984(int& val) {
    func_in_10984(val);
    const int x = 10984;
    std::cout << 10984;
    for (int i = 0; i < 10984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10984(int& val) {
    const int x = 10984;
    std::cout << 10984;
    for (int i = 0; i < 10984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10985(int& val) {
    func_in_10985(val);
    const int x = 10985;
    std::cout << 10985;
    for (int i = 0; i < 10985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10985(int& val) {
    const int x = 10985;
    std::cout << 10985;
    for (int i = 0; i < 10985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10986(int& val) {
    func_in_10986(val);
    const int x = 10986;
    std::cout << 10986;
    for (int i = 0; i < 10986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10986(int& val) {
    const int x = 10986;
    std::cout << 10986;
    for (int i = 0; i < 10986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10987(int& val) {
    func_in_10987(val);
    const int x = 10987;
    std::cout << 10987;
    for (int i = 0; i < 10987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10987(int& val) {
    const int x = 10987;
    std::cout << 10987;
    for (int i = 0; i < 10987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10988(int& val) {
    func_in_10988(val);
    const int x = 10988;
    std::cout << 10988;
    for (int i = 0; i < 10988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10988(int& val) {
    const int x = 10988;
    std::cout << 10988;
    for (int i = 0; i < 10988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10989(int& val) {
    func_in_10989(val);
    const int x = 10989;
    std::cout << 10989;
    for (int i = 0; i < 10989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10989(int& val) {
    const int x = 10989;
    std::cout << 10989;
    for (int i = 0; i < 10989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10990(int& val) {
    func_in_10990(val);
    const int x = 10990;
    std::cout << 10990;
    for (int i = 0; i < 10990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10990(int& val) {
    const int x = 10990;
    std::cout << 10990;
    for (int i = 0; i < 10990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10991(int& val) {
    func_in_10991(val);
    const int x = 10991;
    std::cout << 10991;
    for (int i = 0; i < 10991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10991(int& val) {
    const int x = 10991;
    std::cout << 10991;
    for (int i = 0; i < 10991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10992(int& val) {
    func_in_10992(val);
    const int x = 10992;
    std::cout << 10992;
    for (int i = 0; i < 10992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10992(int& val) {
    const int x = 10992;
    std::cout << 10992;
    for (int i = 0; i < 10992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10993(int& val) {
    func_in_10993(val);
    const int x = 10993;
    std::cout << 10993;
    for (int i = 0; i < 10993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10993(int& val) {
    const int x = 10993;
    std::cout << 10993;
    for (int i = 0; i < 10993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10994(int& val) {
    func_in_10994(val);
    const int x = 10994;
    std::cout << 10994;
    for (int i = 0; i < 10994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10994(int& val) {
    const int x = 10994;
    std::cout << 10994;
    for (int i = 0; i < 10994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10995(int& val) {
    func_in_10995(val);
    const int x = 10995;
    std::cout << 10995;
    for (int i = 0; i < 10995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10995(int& val) {
    const int x = 10995;
    std::cout << 10995;
    for (int i = 0; i < 10995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10996(int& val) {
    func_in_10996(val);
    const int x = 10996;
    std::cout << 10996;
    for (int i = 0; i < 10996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10996(int& val) {
    const int x = 10996;
    std::cout << 10996;
    for (int i = 0; i < 10996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10997(int& val) {
    func_in_10997(val);
    const int x = 10997;
    std::cout << 10997;
    for (int i = 0; i < 10997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10997(int& val) {
    const int x = 10997;
    std::cout << 10997;
    for (int i = 0; i < 10997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10998(int& val) {
    func_in_10998(val);
    const int x = 10998;
    std::cout << 10998;
    for (int i = 0; i < 10998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10998(int& val) {
    const int x = 10998;
    std::cout << 10998;
    for (int i = 0; i < 10998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10999(int& val) {
    func_in_10999(val);
    const int x = 10999;
    std::cout << 10999;
    for (int i = 0; i < 10999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10999(int& val) {
    const int x = 10999;
    std::cout << 10999;
    for (int i = 0; i < 10999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11000(int& val) {
    func_in_11000(val);
    const int x = 11000;
    std::cout << 11000;
    for (int i = 0; i < 11000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11000(int& val) {
    const int x = 11000;
    std::cout << 11000;
    for (int i = 0; i < 11000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11001(int& val) {
    func_in_11001(val);
    const int x = 11001;
    std::cout << 11001;
    for (int i = 0; i < 11001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11001(int& val) {
    const int x = 11001;
    std::cout << 11001;
    for (int i = 0; i < 11001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11002(int& val) {
    func_in_11002(val);
    const int x = 11002;
    std::cout << 11002;
    for (int i = 0; i < 11002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11002(int& val) {
    const int x = 11002;
    std::cout << 11002;
    for (int i = 0; i < 11002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11003(int& val) {
    func_in_11003(val);
    const int x = 11003;
    std::cout << 11003;
    for (int i = 0; i < 11003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11003(int& val) {
    const int x = 11003;
    std::cout << 11003;
    for (int i = 0; i < 11003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11004(int& val) {
    func_in_11004(val);
    const int x = 11004;
    std::cout << 11004;
    for (int i = 0; i < 11004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11004(int& val) {
    const int x = 11004;
    std::cout << 11004;
    for (int i = 0; i < 11004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11005(int& val) {
    func_in_11005(val);
    const int x = 11005;
    std::cout << 11005;
    for (int i = 0; i < 11005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11005(int& val) {
    const int x = 11005;
    std::cout << 11005;
    for (int i = 0; i < 11005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11006(int& val) {
    func_in_11006(val);
    const int x = 11006;
    std::cout << 11006;
    for (int i = 0; i < 11006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11006(int& val) {
    const int x = 11006;
    std::cout << 11006;
    for (int i = 0; i < 11006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11007(int& val) {
    func_in_11007(val);
    const int x = 11007;
    std::cout << 11007;
    for (int i = 0; i < 11007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11007(int& val) {
    const int x = 11007;
    std::cout << 11007;
    for (int i = 0; i < 11007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11008(int& val) {
    func_in_11008(val);
    const int x = 11008;
    std::cout << 11008;
    for (int i = 0; i < 11008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11008(int& val) {
    const int x = 11008;
    std::cout << 11008;
    for (int i = 0; i < 11008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11009(int& val) {
    func_in_11009(val);
    const int x = 11009;
    std::cout << 11009;
    for (int i = 0; i < 11009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11009(int& val) {
    const int x = 11009;
    std::cout << 11009;
    for (int i = 0; i < 11009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11010(int& val) {
    func_in_11010(val);
    const int x = 11010;
    std::cout << 11010;
    for (int i = 0; i < 11010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11010(int& val) {
    const int x = 11010;
    std::cout << 11010;
    for (int i = 0; i < 11010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11011(int& val) {
    func_in_11011(val);
    const int x = 11011;
    std::cout << 11011;
    for (int i = 0; i < 11011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11011(int& val) {
    const int x = 11011;
    std::cout << 11011;
    for (int i = 0; i < 11011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11012(int& val) {
    func_in_11012(val);
    const int x = 11012;
    std::cout << 11012;
    for (int i = 0; i < 11012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11012(int& val) {
    const int x = 11012;
    std::cout << 11012;
    for (int i = 0; i < 11012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11013(int& val) {
    func_in_11013(val);
    const int x = 11013;
    std::cout << 11013;
    for (int i = 0; i < 11013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11013(int& val) {
    const int x = 11013;
    std::cout << 11013;
    for (int i = 0; i < 11013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11014(int& val) {
    func_in_11014(val);
    const int x = 11014;
    std::cout << 11014;
    for (int i = 0; i < 11014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11014(int& val) {
    const int x = 11014;
    std::cout << 11014;
    for (int i = 0; i < 11014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11015(int& val) {
    func_in_11015(val);
    const int x = 11015;
    std::cout << 11015;
    for (int i = 0; i < 11015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11015(int& val) {
    const int x = 11015;
    std::cout << 11015;
    for (int i = 0; i < 11015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11016(int& val) {
    func_in_11016(val);
    const int x = 11016;
    std::cout << 11016;
    for (int i = 0; i < 11016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11016(int& val) {
    const int x = 11016;
    std::cout << 11016;
    for (int i = 0; i < 11016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11017(int& val) {
    func_in_11017(val);
    const int x = 11017;
    std::cout << 11017;
    for (int i = 0; i < 11017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11017(int& val) {
    const int x = 11017;
    std::cout << 11017;
    for (int i = 0; i < 11017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11018(int& val) {
    func_in_11018(val);
    const int x = 11018;
    std::cout << 11018;
    for (int i = 0; i < 11018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11018(int& val) {
    const int x = 11018;
    std::cout << 11018;
    for (int i = 0; i < 11018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11019(int& val) {
    func_in_11019(val);
    const int x = 11019;
    std::cout << 11019;
    for (int i = 0; i < 11019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11019(int& val) {
    const int x = 11019;
    std::cout << 11019;
    for (int i = 0; i < 11019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11020(int& val) {
    func_in_11020(val);
    const int x = 11020;
    std::cout << 11020;
    for (int i = 0; i < 11020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11020(int& val) {
    const int x = 11020;
    std::cout << 11020;
    for (int i = 0; i < 11020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11021(int& val) {
    func_in_11021(val);
    const int x = 11021;
    std::cout << 11021;
    for (int i = 0; i < 11021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11021(int& val) {
    const int x = 11021;
    std::cout << 11021;
    for (int i = 0; i < 11021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11022(int& val) {
    func_in_11022(val);
    const int x = 11022;
    std::cout << 11022;
    for (int i = 0; i < 11022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11022(int& val) {
    const int x = 11022;
    std::cout << 11022;
    for (int i = 0; i < 11022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11023(int& val) {
    func_in_11023(val);
    const int x = 11023;
    std::cout << 11023;
    for (int i = 0; i < 11023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11023(int& val) {
    const int x = 11023;
    std::cout << 11023;
    for (int i = 0; i < 11023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11024(int& val) {
    func_in_11024(val);
    const int x = 11024;
    std::cout << 11024;
    for (int i = 0; i < 11024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11024(int& val) {
    const int x = 11024;
    std::cout << 11024;
    for (int i = 0; i < 11024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11025(int& val) {
    func_in_11025(val);
    const int x = 11025;
    std::cout << 11025;
    for (int i = 0; i < 11025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11025(int& val) {
    const int x = 11025;
    std::cout << 11025;
    for (int i = 0; i < 11025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11026(int& val) {
    func_in_11026(val);
    const int x = 11026;
    std::cout << 11026;
    for (int i = 0; i < 11026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11026(int& val) {
    const int x = 11026;
    std::cout << 11026;
    for (int i = 0; i < 11026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11027(int& val) {
    func_in_11027(val);
    const int x = 11027;
    std::cout << 11027;
    for (int i = 0; i < 11027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11027(int& val) {
    const int x = 11027;
    std::cout << 11027;
    for (int i = 0; i < 11027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11028(int& val) {
    func_in_11028(val);
    const int x = 11028;
    std::cout << 11028;
    for (int i = 0; i < 11028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11028(int& val) {
    const int x = 11028;
    std::cout << 11028;
    for (int i = 0; i < 11028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11029(int& val) {
    func_in_11029(val);
    const int x = 11029;
    std::cout << 11029;
    for (int i = 0; i < 11029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11029(int& val) {
    const int x = 11029;
    std::cout << 11029;
    for (int i = 0; i < 11029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11030(int& val) {
    func_in_11030(val);
    const int x = 11030;
    std::cout << 11030;
    for (int i = 0; i < 11030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11030(int& val) {
    const int x = 11030;
    std::cout << 11030;
    for (int i = 0; i < 11030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11031(int& val) {
    func_in_11031(val);
    const int x = 11031;
    std::cout << 11031;
    for (int i = 0; i < 11031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11031(int& val) {
    const int x = 11031;
    std::cout << 11031;
    for (int i = 0; i < 11031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11032(int& val) {
    func_in_11032(val);
    const int x = 11032;
    std::cout << 11032;
    for (int i = 0; i < 11032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11032(int& val) {
    const int x = 11032;
    std::cout << 11032;
    for (int i = 0; i < 11032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11033(int& val) {
    func_in_11033(val);
    const int x = 11033;
    std::cout << 11033;
    for (int i = 0; i < 11033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11033(int& val) {
    const int x = 11033;
    std::cout << 11033;
    for (int i = 0; i < 11033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11034(int& val) {
    func_in_11034(val);
    const int x = 11034;
    std::cout << 11034;
    for (int i = 0; i < 11034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11034(int& val) {
    const int x = 11034;
    std::cout << 11034;
    for (int i = 0; i < 11034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11035(int& val) {
    func_in_11035(val);
    const int x = 11035;
    std::cout << 11035;
    for (int i = 0; i < 11035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11035(int& val) {
    const int x = 11035;
    std::cout << 11035;
    for (int i = 0; i < 11035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11036(int& val) {
    func_in_11036(val);
    const int x = 11036;
    std::cout << 11036;
    for (int i = 0; i < 11036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11036(int& val) {
    const int x = 11036;
    std::cout << 11036;
    for (int i = 0; i < 11036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11037(int& val) {
    func_in_11037(val);
    const int x = 11037;
    std::cout << 11037;
    for (int i = 0; i < 11037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11037(int& val) {
    const int x = 11037;
    std::cout << 11037;
    for (int i = 0; i < 11037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11038(int& val) {
    func_in_11038(val);
    const int x = 11038;
    std::cout << 11038;
    for (int i = 0; i < 11038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11038(int& val) {
    const int x = 11038;
    std::cout << 11038;
    for (int i = 0; i < 11038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11039(int& val) {
    func_in_11039(val);
    const int x = 11039;
    std::cout << 11039;
    for (int i = 0; i < 11039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11039(int& val) {
    const int x = 11039;
    std::cout << 11039;
    for (int i = 0; i < 11039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11040(int& val) {
    func_in_11040(val);
    const int x = 11040;
    std::cout << 11040;
    for (int i = 0; i < 11040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11040(int& val) {
    const int x = 11040;
    std::cout << 11040;
    for (int i = 0; i < 11040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11041(int& val) {
    func_in_11041(val);
    const int x = 11041;
    std::cout << 11041;
    for (int i = 0; i < 11041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11041(int& val) {
    const int x = 11041;
    std::cout << 11041;
    for (int i = 0; i < 11041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11042(int& val) {
    func_in_11042(val);
    const int x = 11042;
    std::cout << 11042;
    for (int i = 0; i < 11042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11042(int& val) {
    const int x = 11042;
    std::cout << 11042;
    for (int i = 0; i < 11042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11043(int& val) {
    func_in_11043(val);
    const int x = 11043;
    std::cout << 11043;
    for (int i = 0; i < 11043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11043(int& val) {
    const int x = 11043;
    std::cout << 11043;
    for (int i = 0; i < 11043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11044(int& val) {
    func_in_11044(val);
    const int x = 11044;
    std::cout << 11044;
    for (int i = 0; i < 11044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11044(int& val) {
    const int x = 11044;
    std::cout << 11044;
    for (int i = 0; i < 11044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11045(int& val) {
    func_in_11045(val);
    const int x = 11045;
    std::cout << 11045;
    for (int i = 0; i < 11045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11045(int& val) {
    const int x = 11045;
    std::cout << 11045;
    for (int i = 0; i < 11045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11046(int& val) {
    func_in_11046(val);
    const int x = 11046;
    std::cout << 11046;
    for (int i = 0; i < 11046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11046(int& val) {
    const int x = 11046;
    std::cout << 11046;
    for (int i = 0; i < 11046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11047(int& val) {
    func_in_11047(val);
    const int x = 11047;
    std::cout << 11047;
    for (int i = 0; i < 11047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11047(int& val) {
    const int x = 11047;
    std::cout << 11047;
    for (int i = 0; i < 11047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11048(int& val) {
    func_in_11048(val);
    const int x = 11048;
    std::cout << 11048;
    for (int i = 0; i < 11048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11048(int& val) {
    const int x = 11048;
    std::cout << 11048;
    for (int i = 0; i < 11048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11049(int& val) {
    func_in_11049(val);
    const int x = 11049;
    std::cout << 11049;
    for (int i = 0; i < 11049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11049(int& val) {
    const int x = 11049;
    std::cout << 11049;
    for (int i = 0; i < 11049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11050(int& val) {
    func_in_11050(val);
    const int x = 11050;
    std::cout << 11050;
    for (int i = 0; i < 11050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11050(int& val) {
    const int x = 11050;
    std::cout << 11050;
    for (int i = 0; i < 11050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11051(int& val) {
    func_in_11051(val);
    const int x = 11051;
    std::cout << 11051;
    for (int i = 0; i < 11051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11051(int& val) {
    const int x = 11051;
    std::cout << 11051;
    for (int i = 0; i < 11051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11052(int& val) {
    func_in_11052(val);
    const int x = 11052;
    std::cout << 11052;
    for (int i = 0; i < 11052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11052(int& val) {
    const int x = 11052;
    std::cout << 11052;
    for (int i = 0; i < 11052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11053(int& val) {
    func_in_11053(val);
    const int x = 11053;
    std::cout << 11053;
    for (int i = 0; i < 11053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11053(int& val) {
    const int x = 11053;
    std::cout << 11053;
    for (int i = 0; i < 11053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11054(int& val) {
    func_in_11054(val);
    const int x = 11054;
    std::cout << 11054;
    for (int i = 0; i < 11054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11054(int& val) {
    const int x = 11054;
    std::cout << 11054;
    for (int i = 0; i < 11054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11055(int& val) {
    func_in_11055(val);
    const int x = 11055;
    std::cout << 11055;
    for (int i = 0; i < 11055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11055(int& val) {
    const int x = 11055;
    std::cout << 11055;
    for (int i = 0; i < 11055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11056(int& val) {
    func_in_11056(val);
    const int x = 11056;
    std::cout << 11056;
    for (int i = 0; i < 11056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11056(int& val) {
    const int x = 11056;
    std::cout << 11056;
    for (int i = 0; i < 11056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11057(int& val) {
    func_in_11057(val);
    const int x = 11057;
    std::cout << 11057;
    for (int i = 0; i < 11057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11057(int& val) {
    const int x = 11057;
    std::cout << 11057;
    for (int i = 0; i < 11057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11058(int& val) {
    func_in_11058(val);
    const int x = 11058;
    std::cout << 11058;
    for (int i = 0; i < 11058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11058(int& val) {
    const int x = 11058;
    std::cout << 11058;
    for (int i = 0; i < 11058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11059(int& val) {
    func_in_11059(val);
    const int x = 11059;
    std::cout << 11059;
    for (int i = 0; i < 11059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11059(int& val) {
    const int x = 11059;
    std::cout << 11059;
    for (int i = 0; i < 11059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11060(int& val) {
    func_in_11060(val);
    const int x = 11060;
    std::cout << 11060;
    for (int i = 0; i < 11060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11060(int& val) {
    const int x = 11060;
    std::cout << 11060;
    for (int i = 0; i < 11060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11061(int& val) {
    func_in_11061(val);
    const int x = 11061;
    std::cout << 11061;
    for (int i = 0; i < 11061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11061(int& val) {
    const int x = 11061;
    std::cout << 11061;
    for (int i = 0; i < 11061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11062(int& val) {
    func_in_11062(val);
    const int x = 11062;
    std::cout << 11062;
    for (int i = 0; i < 11062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11062(int& val) {
    const int x = 11062;
    std::cout << 11062;
    for (int i = 0; i < 11062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11063(int& val) {
    func_in_11063(val);
    const int x = 11063;
    std::cout << 11063;
    for (int i = 0; i < 11063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11063(int& val) {
    const int x = 11063;
    std::cout << 11063;
    for (int i = 0; i < 11063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11064(int& val) {
    func_in_11064(val);
    const int x = 11064;
    std::cout << 11064;
    for (int i = 0; i < 11064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11064(int& val) {
    const int x = 11064;
    std::cout << 11064;
    for (int i = 0; i < 11064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11065(int& val) {
    func_in_11065(val);
    const int x = 11065;
    std::cout << 11065;
    for (int i = 0; i < 11065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11065(int& val) {
    const int x = 11065;
    std::cout << 11065;
    for (int i = 0; i < 11065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11066(int& val) {
    func_in_11066(val);
    const int x = 11066;
    std::cout << 11066;
    for (int i = 0; i < 11066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11066(int& val) {
    const int x = 11066;
    std::cout << 11066;
    for (int i = 0; i < 11066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11067(int& val) {
    func_in_11067(val);
    const int x = 11067;
    std::cout << 11067;
    for (int i = 0; i < 11067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11067(int& val) {
    const int x = 11067;
    std::cout << 11067;
    for (int i = 0; i < 11067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11068(int& val) {
    func_in_11068(val);
    const int x = 11068;
    std::cout << 11068;
    for (int i = 0; i < 11068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11068(int& val) {
    const int x = 11068;
    std::cout << 11068;
    for (int i = 0; i < 11068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11069(int& val) {
    func_in_11069(val);
    const int x = 11069;
    std::cout << 11069;
    for (int i = 0; i < 11069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11069(int& val) {
    const int x = 11069;
    std::cout << 11069;
    for (int i = 0; i < 11069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11070(int& val) {
    func_in_11070(val);
    const int x = 11070;
    std::cout << 11070;
    for (int i = 0; i < 11070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11070(int& val) {
    const int x = 11070;
    std::cout << 11070;
    for (int i = 0; i < 11070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11071(int& val) {
    func_in_11071(val);
    const int x = 11071;
    std::cout << 11071;
    for (int i = 0; i < 11071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11071(int& val) {
    const int x = 11071;
    std::cout << 11071;
    for (int i = 0; i < 11071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11072(int& val) {
    func_in_11072(val);
    const int x = 11072;
    std::cout << 11072;
    for (int i = 0; i < 11072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11072(int& val) {
    const int x = 11072;
    std::cout << 11072;
    for (int i = 0; i < 11072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11073(int& val) {
    func_in_11073(val);
    const int x = 11073;
    std::cout << 11073;
    for (int i = 0; i < 11073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11073(int& val) {
    const int x = 11073;
    std::cout << 11073;
    for (int i = 0; i < 11073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11074(int& val) {
    func_in_11074(val);
    const int x = 11074;
    std::cout << 11074;
    for (int i = 0; i < 11074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11074(int& val) {
    const int x = 11074;
    std::cout << 11074;
    for (int i = 0; i < 11074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11075(int& val) {
    func_in_11075(val);
    const int x = 11075;
    std::cout << 11075;
    for (int i = 0; i < 11075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11075(int& val) {
    const int x = 11075;
    std::cout << 11075;
    for (int i = 0; i < 11075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11076(int& val) {
    func_in_11076(val);
    const int x = 11076;
    std::cout << 11076;
    for (int i = 0; i < 11076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11076(int& val) {
    const int x = 11076;
    std::cout << 11076;
    for (int i = 0; i < 11076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11077(int& val) {
    func_in_11077(val);
    const int x = 11077;
    std::cout << 11077;
    for (int i = 0; i < 11077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11077(int& val) {
    const int x = 11077;
    std::cout << 11077;
    for (int i = 0; i < 11077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11078(int& val) {
    func_in_11078(val);
    const int x = 11078;
    std::cout << 11078;
    for (int i = 0; i < 11078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11078(int& val) {
    const int x = 11078;
    std::cout << 11078;
    for (int i = 0; i < 11078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11079(int& val) {
    func_in_11079(val);
    const int x = 11079;
    std::cout << 11079;
    for (int i = 0; i < 11079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11079(int& val) {
    const int x = 11079;
    std::cout << 11079;
    for (int i = 0; i < 11079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11080(int& val) {
    func_in_11080(val);
    const int x = 11080;
    std::cout << 11080;
    for (int i = 0; i < 11080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11080(int& val) {
    const int x = 11080;
    std::cout << 11080;
    for (int i = 0; i < 11080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11081(int& val) {
    func_in_11081(val);
    const int x = 11081;
    std::cout << 11081;
    for (int i = 0; i < 11081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11081(int& val) {
    const int x = 11081;
    std::cout << 11081;
    for (int i = 0; i < 11081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11082(int& val) {
    func_in_11082(val);
    const int x = 11082;
    std::cout << 11082;
    for (int i = 0; i < 11082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11082(int& val) {
    const int x = 11082;
    std::cout << 11082;
    for (int i = 0; i < 11082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11083(int& val) {
    func_in_11083(val);
    const int x = 11083;
    std::cout << 11083;
    for (int i = 0; i < 11083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11083(int& val) {
    const int x = 11083;
    std::cout << 11083;
    for (int i = 0; i < 11083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11084(int& val) {
    func_in_11084(val);
    const int x = 11084;
    std::cout << 11084;
    for (int i = 0; i < 11084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11084(int& val) {
    const int x = 11084;
    std::cout << 11084;
    for (int i = 0; i < 11084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11085(int& val) {
    func_in_11085(val);
    const int x = 11085;
    std::cout << 11085;
    for (int i = 0; i < 11085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11085(int& val) {
    const int x = 11085;
    std::cout << 11085;
    for (int i = 0; i < 11085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11086(int& val) {
    func_in_11086(val);
    const int x = 11086;
    std::cout << 11086;
    for (int i = 0; i < 11086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11086(int& val) {
    const int x = 11086;
    std::cout << 11086;
    for (int i = 0; i < 11086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11087(int& val) {
    func_in_11087(val);
    const int x = 11087;
    std::cout << 11087;
    for (int i = 0; i < 11087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11087(int& val) {
    const int x = 11087;
    std::cout << 11087;
    for (int i = 0; i < 11087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11088(int& val) {
    func_in_11088(val);
    const int x = 11088;
    std::cout << 11088;
    for (int i = 0; i < 11088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11088(int& val) {
    const int x = 11088;
    std::cout << 11088;
    for (int i = 0; i < 11088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11089(int& val) {
    func_in_11089(val);
    const int x = 11089;
    std::cout << 11089;
    for (int i = 0; i < 11089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11089(int& val) {
    const int x = 11089;
    std::cout << 11089;
    for (int i = 0; i < 11089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11090(int& val) {
    func_in_11090(val);
    const int x = 11090;
    std::cout << 11090;
    for (int i = 0; i < 11090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11090(int& val) {
    const int x = 11090;
    std::cout << 11090;
    for (int i = 0; i < 11090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11091(int& val) {
    func_in_11091(val);
    const int x = 11091;
    std::cout << 11091;
    for (int i = 0; i < 11091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11091(int& val) {
    const int x = 11091;
    std::cout << 11091;
    for (int i = 0; i < 11091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11092(int& val) {
    func_in_11092(val);
    const int x = 11092;
    std::cout << 11092;
    for (int i = 0; i < 11092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11092(int& val) {
    const int x = 11092;
    std::cout << 11092;
    for (int i = 0; i < 11092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11093(int& val) {
    func_in_11093(val);
    const int x = 11093;
    std::cout << 11093;
    for (int i = 0; i < 11093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11093(int& val) {
    const int x = 11093;
    std::cout << 11093;
    for (int i = 0; i < 11093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11094(int& val) {
    func_in_11094(val);
    const int x = 11094;
    std::cout << 11094;
    for (int i = 0; i < 11094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11094(int& val) {
    const int x = 11094;
    std::cout << 11094;
    for (int i = 0; i < 11094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11095(int& val) {
    func_in_11095(val);
    const int x = 11095;
    std::cout << 11095;
    for (int i = 0; i < 11095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11095(int& val) {
    const int x = 11095;
    std::cout << 11095;
    for (int i = 0; i < 11095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11096(int& val) {
    func_in_11096(val);
    const int x = 11096;
    std::cout << 11096;
    for (int i = 0; i < 11096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11096(int& val) {
    const int x = 11096;
    std::cout << 11096;
    for (int i = 0; i < 11096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11097(int& val) {
    func_in_11097(val);
    const int x = 11097;
    std::cout << 11097;
    for (int i = 0; i < 11097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11097(int& val) {
    const int x = 11097;
    std::cout << 11097;
    for (int i = 0; i < 11097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11098(int& val) {
    func_in_11098(val);
    const int x = 11098;
    std::cout << 11098;
    for (int i = 0; i < 11098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11098(int& val) {
    const int x = 11098;
    std::cout << 11098;
    for (int i = 0; i < 11098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11099(int& val) {
    func_in_11099(val);
    const int x = 11099;
    std::cout << 11099;
    for (int i = 0; i < 11099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11099(int& val) {
    const int x = 11099;
    std::cout << 11099;
    for (int i = 0; i < 11099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11100(int& val) {
    func_in_11100(val);
    const int x = 11100;
    std::cout << 11100;
    for (int i = 0; i < 11100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11100(int& val) {
    const int x = 11100;
    std::cout << 11100;
    for (int i = 0; i < 11100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11101(int& val) {
    func_in_11101(val);
    const int x = 11101;
    std::cout << 11101;
    for (int i = 0; i < 11101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11101(int& val) {
    const int x = 11101;
    std::cout << 11101;
    for (int i = 0; i < 11101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11102(int& val) {
    func_in_11102(val);
    const int x = 11102;
    std::cout << 11102;
    for (int i = 0; i < 11102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11102(int& val) {
    const int x = 11102;
    std::cout << 11102;
    for (int i = 0; i < 11102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11103(int& val) {
    func_in_11103(val);
    const int x = 11103;
    std::cout << 11103;
    for (int i = 0; i < 11103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11103(int& val) {
    const int x = 11103;
    std::cout << 11103;
    for (int i = 0; i < 11103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11104(int& val) {
    func_in_11104(val);
    const int x = 11104;
    std::cout << 11104;
    for (int i = 0; i < 11104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11104(int& val) {
    const int x = 11104;
    std::cout << 11104;
    for (int i = 0; i < 11104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11105(int& val) {
    func_in_11105(val);
    const int x = 11105;
    std::cout << 11105;
    for (int i = 0; i < 11105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11105(int& val) {
    const int x = 11105;
    std::cout << 11105;
    for (int i = 0; i < 11105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11106(int& val) {
    func_in_11106(val);
    const int x = 11106;
    std::cout << 11106;
    for (int i = 0; i < 11106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11106(int& val) {
    const int x = 11106;
    std::cout << 11106;
    for (int i = 0; i < 11106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11107(int& val) {
    func_in_11107(val);
    const int x = 11107;
    std::cout << 11107;
    for (int i = 0; i < 11107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11107(int& val) {
    const int x = 11107;
    std::cout << 11107;
    for (int i = 0; i < 11107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11108(int& val) {
    func_in_11108(val);
    const int x = 11108;
    std::cout << 11108;
    for (int i = 0; i < 11108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11108(int& val) {
    const int x = 11108;
    std::cout << 11108;
    for (int i = 0; i < 11108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11109(int& val) {
    func_in_11109(val);
    const int x = 11109;
    std::cout << 11109;
    for (int i = 0; i < 11109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11109(int& val) {
    const int x = 11109;
    std::cout << 11109;
    for (int i = 0; i < 11109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11110(int& val) {
    func_in_11110(val);
    const int x = 11110;
    std::cout << 11110;
    for (int i = 0; i < 11110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11110(int& val) {
    const int x = 11110;
    std::cout << 11110;
    for (int i = 0; i < 11110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11111(int& val) {
    func_in_11111(val);
    const int x = 11111;
    std::cout << 11111;
    for (int i = 0; i < 11111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11111(int& val) {
    const int x = 11111;
    std::cout << 11111;
    for (int i = 0; i < 11111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11112(int& val) {
    func_in_11112(val);
    const int x = 11112;
    std::cout << 11112;
    for (int i = 0; i < 11112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11112(int& val) {
    const int x = 11112;
    std::cout << 11112;
    for (int i = 0; i < 11112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11113(int& val) {
    func_in_11113(val);
    const int x = 11113;
    std::cout << 11113;
    for (int i = 0; i < 11113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11113(int& val) {
    const int x = 11113;
    std::cout << 11113;
    for (int i = 0; i < 11113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11114(int& val) {
    func_in_11114(val);
    const int x = 11114;
    std::cout << 11114;
    for (int i = 0; i < 11114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11114(int& val) {
    const int x = 11114;
    std::cout << 11114;
    for (int i = 0; i < 11114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11115(int& val) {
    func_in_11115(val);
    const int x = 11115;
    std::cout << 11115;
    for (int i = 0; i < 11115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11115(int& val) {
    const int x = 11115;
    std::cout << 11115;
    for (int i = 0; i < 11115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11116(int& val) {
    func_in_11116(val);
    const int x = 11116;
    std::cout << 11116;
    for (int i = 0; i < 11116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11116(int& val) {
    const int x = 11116;
    std::cout << 11116;
    for (int i = 0; i < 11116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11117(int& val) {
    func_in_11117(val);
    const int x = 11117;
    std::cout << 11117;
    for (int i = 0; i < 11117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11117(int& val) {
    const int x = 11117;
    std::cout << 11117;
    for (int i = 0; i < 11117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11118(int& val) {
    func_in_11118(val);
    const int x = 11118;
    std::cout << 11118;
    for (int i = 0; i < 11118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11118(int& val) {
    const int x = 11118;
    std::cout << 11118;
    for (int i = 0; i < 11118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11119(int& val) {
    func_in_11119(val);
    const int x = 11119;
    std::cout << 11119;
    for (int i = 0; i < 11119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11119(int& val) {
    const int x = 11119;
    std::cout << 11119;
    for (int i = 0; i < 11119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11120(int& val) {
    func_in_11120(val);
    const int x = 11120;
    std::cout << 11120;
    for (int i = 0; i < 11120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11120(int& val) {
    const int x = 11120;
    std::cout << 11120;
    for (int i = 0; i < 11120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11121(int& val) {
    func_in_11121(val);
    const int x = 11121;
    std::cout << 11121;
    for (int i = 0; i < 11121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11121(int& val) {
    const int x = 11121;
    std::cout << 11121;
    for (int i = 0; i < 11121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11122(int& val) {
    func_in_11122(val);
    const int x = 11122;
    std::cout << 11122;
    for (int i = 0; i < 11122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11122(int& val) {
    const int x = 11122;
    std::cout << 11122;
    for (int i = 0; i < 11122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11123(int& val) {
    func_in_11123(val);
    const int x = 11123;
    std::cout << 11123;
    for (int i = 0; i < 11123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11123(int& val) {
    const int x = 11123;
    std::cout << 11123;
    for (int i = 0; i < 11123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11124(int& val) {
    func_in_11124(val);
    const int x = 11124;
    std::cout << 11124;
    for (int i = 0; i < 11124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11124(int& val) {
    const int x = 11124;
    std::cout << 11124;
    for (int i = 0; i < 11124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11125(int& val) {
    func_in_11125(val);
    const int x = 11125;
    std::cout << 11125;
    for (int i = 0; i < 11125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11125(int& val) {
    const int x = 11125;
    std::cout << 11125;
    for (int i = 0; i < 11125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11126(int& val) {
    func_in_11126(val);
    const int x = 11126;
    std::cout << 11126;
    for (int i = 0; i < 11126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11126(int& val) {
    const int x = 11126;
    std::cout << 11126;
    for (int i = 0; i < 11126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11127(int& val) {
    func_in_11127(val);
    const int x = 11127;
    std::cout << 11127;
    for (int i = 0; i < 11127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11127(int& val) {
    const int x = 11127;
    std::cout << 11127;
    for (int i = 0; i < 11127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11128(int& val) {
    func_in_11128(val);
    const int x = 11128;
    std::cout << 11128;
    for (int i = 0; i < 11128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11128(int& val) {
    const int x = 11128;
    std::cout << 11128;
    for (int i = 0; i < 11128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11129(int& val) {
    func_in_11129(val);
    const int x = 11129;
    std::cout << 11129;
    for (int i = 0; i < 11129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11129(int& val) {
    const int x = 11129;
    std::cout << 11129;
    for (int i = 0; i < 11129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11130(int& val) {
    func_in_11130(val);
    const int x = 11130;
    std::cout << 11130;
    for (int i = 0; i < 11130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11130(int& val) {
    const int x = 11130;
    std::cout << 11130;
    for (int i = 0; i < 11130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11131(int& val) {
    func_in_11131(val);
    const int x = 11131;
    std::cout << 11131;
    for (int i = 0; i < 11131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11131(int& val) {
    const int x = 11131;
    std::cout << 11131;
    for (int i = 0; i < 11131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11132(int& val) {
    func_in_11132(val);
    const int x = 11132;
    std::cout << 11132;
    for (int i = 0; i < 11132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11132(int& val) {
    const int x = 11132;
    std::cout << 11132;
    for (int i = 0; i < 11132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11133(int& val) {
    func_in_11133(val);
    const int x = 11133;
    std::cout << 11133;
    for (int i = 0; i < 11133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11133(int& val) {
    const int x = 11133;
    std::cout << 11133;
    for (int i = 0; i < 11133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11134(int& val) {
    func_in_11134(val);
    const int x = 11134;
    std::cout << 11134;
    for (int i = 0; i < 11134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11134(int& val) {
    const int x = 11134;
    std::cout << 11134;
    for (int i = 0; i < 11134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11135(int& val) {
    func_in_11135(val);
    const int x = 11135;
    std::cout << 11135;
    for (int i = 0; i < 11135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11135(int& val) {
    const int x = 11135;
    std::cout << 11135;
    for (int i = 0; i < 11135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11136(int& val) {
    func_in_11136(val);
    const int x = 11136;
    std::cout << 11136;
    for (int i = 0; i < 11136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11136(int& val) {
    const int x = 11136;
    std::cout << 11136;
    for (int i = 0; i < 11136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11137(int& val) {
    func_in_11137(val);
    const int x = 11137;
    std::cout << 11137;
    for (int i = 0; i < 11137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11137(int& val) {
    const int x = 11137;
    std::cout << 11137;
    for (int i = 0; i < 11137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11138(int& val) {
    func_in_11138(val);
    const int x = 11138;
    std::cout << 11138;
    for (int i = 0; i < 11138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11138(int& val) {
    const int x = 11138;
    std::cout << 11138;
    for (int i = 0; i < 11138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11139(int& val) {
    func_in_11139(val);
    const int x = 11139;
    std::cout << 11139;
    for (int i = 0; i < 11139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11139(int& val) {
    const int x = 11139;
    std::cout << 11139;
    for (int i = 0; i < 11139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11140(int& val) {
    func_in_11140(val);
    const int x = 11140;
    std::cout << 11140;
    for (int i = 0; i < 11140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11140(int& val) {
    const int x = 11140;
    std::cout << 11140;
    for (int i = 0; i < 11140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11141(int& val) {
    func_in_11141(val);
    const int x = 11141;
    std::cout << 11141;
    for (int i = 0; i < 11141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11141(int& val) {
    const int x = 11141;
    std::cout << 11141;
    for (int i = 0; i < 11141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11142(int& val) {
    func_in_11142(val);
    const int x = 11142;
    std::cout << 11142;
    for (int i = 0; i < 11142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11142(int& val) {
    const int x = 11142;
    std::cout << 11142;
    for (int i = 0; i < 11142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11143(int& val) {
    func_in_11143(val);
    const int x = 11143;
    std::cout << 11143;
    for (int i = 0; i < 11143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11143(int& val) {
    const int x = 11143;
    std::cout << 11143;
    for (int i = 0; i < 11143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11144(int& val) {
    func_in_11144(val);
    const int x = 11144;
    std::cout << 11144;
    for (int i = 0; i < 11144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11144(int& val) {
    const int x = 11144;
    std::cout << 11144;
    for (int i = 0; i < 11144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11145(int& val) {
    func_in_11145(val);
    const int x = 11145;
    std::cout << 11145;
    for (int i = 0; i < 11145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11145(int& val) {
    const int x = 11145;
    std::cout << 11145;
    for (int i = 0; i < 11145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11146(int& val) {
    func_in_11146(val);
    const int x = 11146;
    std::cout << 11146;
    for (int i = 0; i < 11146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11146(int& val) {
    const int x = 11146;
    std::cout << 11146;
    for (int i = 0; i < 11146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11147(int& val) {
    func_in_11147(val);
    const int x = 11147;
    std::cout << 11147;
    for (int i = 0; i < 11147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11147(int& val) {
    const int x = 11147;
    std::cout << 11147;
    for (int i = 0; i < 11147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11148(int& val) {
    func_in_11148(val);
    const int x = 11148;
    std::cout << 11148;
    for (int i = 0; i < 11148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11148(int& val) {
    const int x = 11148;
    std::cout << 11148;
    for (int i = 0; i < 11148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11149(int& val) {
    func_in_11149(val);
    const int x = 11149;
    std::cout << 11149;
    for (int i = 0; i < 11149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11149(int& val) {
    const int x = 11149;
    std::cout << 11149;
    for (int i = 0; i < 11149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11150(int& val) {
    func_in_11150(val);
    const int x = 11150;
    std::cout << 11150;
    for (int i = 0; i < 11150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11150(int& val) {
    const int x = 11150;
    std::cout << 11150;
    for (int i = 0; i < 11150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11151(int& val) {
    func_in_11151(val);
    const int x = 11151;
    std::cout << 11151;
    for (int i = 0; i < 11151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11151(int& val) {
    const int x = 11151;
    std::cout << 11151;
    for (int i = 0; i < 11151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11152(int& val) {
    func_in_11152(val);
    const int x = 11152;
    std::cout << 11152;
    for (int i = 0; i < 11152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11152(int& val) {
    const int x = 11152;
    std::cout << 11152;
    for (int i = 0; i < 11152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11153(int& val) {
    func_in_11153(val);
    const int x = 11153;
    std::cout << 11153;
    for (int i = 0; i < 11153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11153(int& val) {
    const int x = 11153;
    std::cout << 11153;
    for (int i = 0; i < 11153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11154(int& val) {
    func_in_11154(val);
    const int x = 11154;
    std::cout << 11154;
    for (int i = 0; i < 11154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11154(int& val) {
    const int x = 11154;
    std::cout << 11154;
    for (int i = 0; i < 11154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11155(int& val) {
    func_in_11155(val);
    const int x = 11155;
    std::cout << 11155;
    for (int i = 0; i < 11155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11155(int& val) {
    const int x = 11155;
    std::cout << 11155;
    for (int i = 0; i < 11155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11156(int& val) {
    func_in_11156(val);
    const int x = 11156;
    std::cout << 11156;
    for (int i = 0; i < 11156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11156(int& val) {
    const int x = 11156;
    std::cout << 11156;
    for (int i = 0; i < 11156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11157(int& val) {
    func_in_11157(val);
    const int x = 11157;
    std::cout << 11157;
    for (int i = 0; i < 11157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11157(int& val) {
    const int x = 11157;
    std::cout << 11157;
    for (int i = 0; i < 11157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11158(int& val) {
    func_in_11158(val);
    const int x = 11158;
    std::cout << 11158;
    for (int i = 0; i < 11158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11158(int& val) {
    const int x = 11158;
    std::cout << 11158;
    for (int i = 0; i < 11158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11159(int& val) {
    func_in_11159(val);
    const int x = 11159;
    std::cout << 11159;
    for (int i = 0; i < 11159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11159(int& val) {
    const int x = 11159;
    std::cout << 11159;
    for (int i = 0; i < 11159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11160(int& val) {
    func_in_11160(val);
    const int x = 11160;
    std::cout << 11160;
    for (int i = 0; i < 11160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11160(int& val) {
    const int x = 11160;
    std::cout << 11160;
    for (int i = 0; i < 11160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11161(int& val) {
    func_in_11161(val);
    const int x = 11161;
    std::cout << 11161;
    for (int i = 0; i < 11161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11161(int& val) {
    const int x = 11161;
    std::cout << 11161;
    for (int i = 0; i < 11161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11162(int& val) {
    func_in_11162(val);
    const int x = 11162;
    std::cout << 11162;
    for (int i = 0; i < 11162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11162(int& val) {
    const int x = 11162;
    std::cout << 11162;
    for (int i = 0; i < 11162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11163(int& val) {
    func_in_11163(val);
    const int x = 11163;
    std::cout << 11163;
    for (int i = 0; i < 11163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11163(int& val) {
    const int x = 11163;
    std::cout << 11163;
    for (int i = 0; i < 11163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11164(int& val) {
    func_in_11164(val);
    const int x = 11164;
    std::cout << 11164;
    for (int i = 0; i < 11164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11164(int& val) {
    const int x = 11164;
    std::cout << 11164;
    for (int i = 0; i < 11164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11165(int& val) {
    func_in_11165(val);
    const int x = 11165;
    std::cout << 11165;
    for (int i = 0; i < 11165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11165(int& val) {
    const int x = 11165;
    std::cout << 11165;
    for (int i = 0; i < 11165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11166(int& val) {
    func_in_11166(val);
    const int x = 11166;
    std::cout << 11166;
    for (int i = 0; i < 11166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11166(int& val) {
    const int x = 11166;
    std::cout << 11166;
    for (int i = 0; i < 11166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11167(int& val) {
    func_in_11167(val);
    const int x = 11167;
    std::cout << 11167;
    for (int i = 0; i < 11167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11167(int& val) {
    const int x = 11167;
    std::cout << 11167;
    for (int i = 0; i < 11167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11168(int& val) {
    func_in_11168(val);
    const int x = 11168;
    std::cout << 11168;
    for (int i = 0; i < 11168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11168(int& val) {
    const int x = 11168;
    std::cout << 11168;
    for (int i = 0; i < 11168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11169(int& val) {
    func_in_11169(val);
    const int x = 11169;
    std::cout << 11169;
    for (int i = 0; i < 11169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11169(int& val) {
    const int x = 11169;
    std::cout << 11169;
    for (int i = 0; i < 11169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11170(int& val) {
    func_in_11170(val);
    const int x = 11170;
    std::cout << 11170;
    for (int i = 0; i < 11170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11170(int& val) {
    const int x = 11170;
    std::cout << 11170;
    for (int i = 0; i < 11170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11171(int& val) {
    func_in_11171(val);
    const int x = 11171;
    std::cout << 11171;
    for (int i = 0; i < 11171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11171(int& val) {
    const int x = 11171;
    std::cout << 11171;
    for (int i = 0; i < 11171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11172(int& val) {
    func_in_11172(val);
    const int x = 11172;
    std::cout << 11172;
    for (int i = 0; i < 11172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11172(int& val) {
    const int x = 11172;
    std::cout << 11172;
    for (int i = 0; i < 11172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11173(int& val) {
    func_in_11173(val);
    const int x = 11173;
    std::cout << 11173;
    for (int i = 0; i < 11173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11173(int& val) {
    const int x = 11173;
    std::cout << 11173;
    for (int i = 0; i < 11173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11174(int& val) {
    func_in_11174(val);
    const int x = 11174;
    std::cout << 11174;
    for (int i = 0; i < 11174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11174(int& val) {
    const int x = 11174;
    std::cout << 11174;
    for (int i = 0; i < 11174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11175(int& val) {
    func_in_11175(val);
    const int x = 11175;
    std::cout << 11175;
    for (int i = 0; i < 11175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11175(int& val) {
    const int x = 11175;
    std::cout << 11175;
    for (int i = 0; i < 11175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11176(int& val) {
    func_in_11176(val);
    const int x = 11176;
    std::cout << 11176;
    for (int i = 0; i < 11176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11176(int& val) {
    const int x = 11176;
    std::cout << 11176;
    for (int i = 0; i < 11176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11177(int& val) {
    func_in_11177(val);
    const int x = 11177;
    std::cout << 11177;
    for (int i = 0; i < 11177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11177(int& val) {
    const int x = 11177;
    std::cout << 11177;
    for (int i = 0; i < 11177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11178(int& val) {
    func_in_11178(val);
    const int x = 11178;
    std::cout << 11178;
    for (int i = 0; i < 11178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11178(int& val) {
    const int x = 11178;
    std::cout << 11178;
    for (int i = 0; i < 11178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11179(int& val) {
    func_in_11179(val);
    const int x = 11179;
    std::cout << 11179;
    for (int i = 0; i < 11179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11179(int& val) {
    const int x = 11179;
    std::cout << 11179;
    for (int i = 0; i < 11179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11180(int& val) {
    func_in_11180(val);
    const int x = 11180;
    std::cout << 11180;
    for (int i = 0; i < 11180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11180(int& val) {
    const int x = 11180;
    std::cout << 11180;
    for (int i = 0; i < 11180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11181(int& val) {
    func_in_11181(val);
    const int x = 11181;
    std::cout << 11181;
    for (int i = 0; i < 11181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11181(int& val) {
    const int x = 11181;
    std::cout << 11181;
    for (int i = 0; i < 11181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11182(int& val) {
    func_in_11182(val);
    const int x = 11182;
    std::cout << 11182;
    for (int i = 0; i < 11182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11182(int& val) {
    const int x = 11182;
    std::cout << 11182;
    for (int i = 0; i < 11182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11183(int& val) {
    func_in_11183(val);
    const int x = 11183;
    std::cout << 11183;
    for (int i = 0; i < 11183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11183(int& val) {
    const int x = 11183;
    std::cout << 11183;
    for (int i = 0; i < 11183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11184(int& val) {
    func_in_11184(val);
    const int x = 11184;
    std::cout << 11184;
    for (int i = 0; i < 11184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11184(int& val) {
    const int x = 11184;
    std::cout << 11184;
    for (int i = 0; i < 11184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11185(int& val) {
    func_in_11185(val);
    const int x = 11185;
    std::cout << 11185;
    for (int i = 0; i < 11185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11185(int& val) {
    const int x = 11185;
    std::cout << 11185;
    for (int i = 0; i < 11185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11186(int& val) {
    func_in_11186(val);
    const int x = 11186;
    std::cout << 11186;
    for (int i = 0; i < 11186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11186(int& val) {
    const int x = 11186;
    std::cout << 11186;
    for (int i = 0; i < 11186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11187(int& val) {
    func_in_11187(val);
    const int x = 11187;
    std::cout << 11187;
    for (int i = 0; i < 11187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11187(int& val) {
    const int x = 11187;
    std::cout << 11187;
    for (int i = 0; i < 11187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11188(int& val) {
    func_in_11188(val);
    const int x = 11188;
    std::cout << 11188;
    for (int i = 0; i < 11188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11188(int& val) {
    const int x = 11188;
    std::cout << 11188;
    for (int i = 0; i < 11188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11189(int& val) {
    func_in_11189(val);
    const int x = 11189;
    std::cout << 11189;
    for (int i = 0; i < 11189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11189(int& val) {
    const int x = 11189;
    std::cout << 11189;
    for (int i = 0; i < 11189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11190(int& val) {
    func_in_11190(val);
    const int x = 11190;
    std::cout << 11190;
    for (int i = 0; i < 11190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11190(int& val) {
    const int x = 11190;
    std::cout << 11190;
    for (int i = 0; i < 11190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11191(int& val) {
    func_in_11191(val);
    const int x = 11191;
    std::cout << 11191;
    for (int i = 0; i < 11191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11191(int& val) {
    const int x = 11191;
    std::cout << 11191;
    for (int i = 0; i < 11191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11192(int& val) {
    func_in_11192(val);
    const int x = 11192;
    std::cout << 11192;
    for (int i = 0; i < 11192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11192(int& val) {
    const int x = 11192;
    std::cout << 11192;
    for (int i = 0; i < 11192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11193(int& val) {
    func_in_11193(val);
    const int x = 11193;
    std::cout << 11193;
    for (int i = 0; i < 11193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11193(int& val) {
    const int x = 11193;
    std::cout << 11193;
    for (int i = 0; i < 11193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11194(int& val) {
    func_in_11194(val);
    const int x = 11194;
    std::cout << 11194;
    for (int i = 0; i < 11194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11194(int& val) {
    const int x = 11194;
    std::cout << 11194;
    for (int i = 0; i < 11194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11195(int& val) {
    func_in_11195(val);
    const int x = 11195;
    std::cout << 11195;
    for (int i = 0; i < 11195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11195(int& val) {
    const int x = 11195;
    std::cout << 11195;
    for (int i = 0; i < 11195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11196(int& val) {
    func_in_11196(val);
    const int x = 11196;
    std::cout << 11196;
    for (int i = 0; i < 11196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11196(int& val) {
    const int x = 11196;
    std::cout << 11196;
    for (int i = 0; i < 11196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11197(int& val) {
    func_in_11197(val);
    const int x = 11197;
    std::cout << 11197;
    for (int i = 0; i < 11197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11197(int& val) {
    const int x = 11197;
    std::cout << 11197;
    for (int i = 0; i < 11197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11198(int& val) {
    func_in_11198(val);
    const int x = 11198;
    std::cout << 11198;
    for (int i = 0; i < 11198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11198(int& val) {
    const int x = 11198;
    std::cout << 11198;
    for (int i = 0; i < 11198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11199(int& val) {
    func_in_11199(val);
    const int x = 11199;
    std::cout << 11199;
    for (int i = 0; i < 11199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11199(int& val) {
    const int x = 11199;
    std::cout << 11199;
    for (int i = 0; i < 11199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11200(int& val) {
    func_in_11200(val);
    const int x = 11200;
    std::cout << 11200;
    for (int i = 0; i < 11200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11200(int& val) {
    const int x = 11200;
    std::cout << 11200;
    for (int i = 0; i < 11200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11201(int& val) {
    func_in_11201(val);
    const int x = 11201;
    std::cout << 11201;
    for (int i = 0; i < 11201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11201(int& val) {
    const int x = 11201;
    std::cout << 11201;
    for (int i = 0; i < 11201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11202(int& val) {
    func_in_11202(val);
    const int x = 11202;
    std::cout << 11202;
    for (int i = 0; i < 11202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11202(int& val) {
    const int x = 11202;
    std::cout << 11202;
    for (int i = 0; i < 11202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11203(int& val) {
    func_in_11203(val);
    const int x = 11203;
    std::cout << 11203;
    for (int i = 0; i < 11203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11203(int& val) {
    const int x = 11203;
    std::cout << 11203;
    for (int i = 0; i < 11203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11204(int& val) {
    func_in_11204(val);
    const int x = 11204;
    std::cout << 11204;
    for (int i = 0; i < 11204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11204(int& val) {
    const int x = 11204;
    std::cout << 11204;
    for (int i = 0; i < 11204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11205(int& val) {
    func_in_11205(val);
    const int x = 11205;
    std::cout << 11205;
    for (int i = 0; i < 11205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11205(int& val) {
    const int x = 11205;
    std::cout << 11205;
    for (int i = 0; i < 11205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11206(int& val) {
    func_in_11206(val);
    const int x = 11206;
    std::cout << 11206;
    for (int i = 0; i < 11206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11206(int& val) {
    const int x = 11206;
    std::cout << 11206;
    for (int i = 0; i < 11206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11207(int& val) {
    func_in_11207(val);
    const int x = 11207;
    std::cout << 11207;
    for (int i = 0; i < 11207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11207(int& val) {
    const int x = 11207;
    std::cout << 11207;
    for (int i = 0; i < 11207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11208(int& val) {
    func_in_11208(val);
    const int x = 11208;
    std::cout << 11208;
    for (int i = 0; i < 11208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11208(int& val) {
    const int x = 11208;
    std::cout << 11208;
    for (int i = 0; i < 11208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11209(int& val) {
    func_in_11209(val);
    const int x = 11209;
    std::cout << 11209;
    for (int i = 0; i < 11209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11209(int& val) {
    const int x = 11209;
    std::cout << 11209;
    for (int i = 0; i < 11209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11210(int& val) {
    func_in_11210(val);
    const int x = 11210;
    std::cout << 11210;
    for (int i = 0; i < 11210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11210(int& val) {
    const int x = 11210;
    std::cout << 11210;
    for (int i = 0; i < 11210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11211(int& val) {
    func_in_11211(val);
    const int x = 11211;
    std::cout << 11211;
    for (int i = 0; i < 11211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11211(int& val) {
    const int x = 11211;
    std::cout << 11211;
    for (int i = 0; i < 11211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11212(int& val) {
    func_in_11212(val);
    const int x = 11212;
    std::cout << 11212;
    for (int i = 0; i < 11212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11212(int& val) {
    const int x = 11212;
    std::cout << 11212;
    for (int i = 0; i < 11212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11213(int& val) {
    func_in_11213(val);
    const int x = 11213;
    std::cout << 11213;
    for (int i = 0; i < 11213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11213(int& val) {
    const int x = 11213;
    std::cout << 11213;
    for (int i = 0; i < 11213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11214(int& val) {
    func_in_11214(val);
    const int x = 11214;
    std::cout << 11214;
    for (int i = 0; i < 11214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11214(int& val) {
    const int x = 11214;
    std::cout << 11214;
    for (int i = 0; i < 11214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11215(int& val) {
    func_in_11215(val);
    const int x = 11215;
    std::cout << 11215;
    for (int i = 0; i < 11215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11215(int& val) {
    const int x = 11215;
    std::cout << 11215;
    for (int i = 0; i < 11215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11216(int& val) {
    func_in_11216(val);
    const int x = 11216;
    std::cout << 11216;
    for (int i = 0; i < 11216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11216(int& val) {
    const int x = 11216;
    std::cout << 11216;
    for (int i = 0; i < 11216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11217(int& val) {
    func_in_11217(val);
    const int x = 11217;
    std::cout << 11217;
    for (int i = 0; i < 11217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11217(int& val) {
    const int x = 11217;
    std::cout << 11217;
    for (int i = 0; i < 11217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11218(int& val) {
    func_in_11218(val);
    const int x = 11218;
    std::cout << 11218;
    for (int i = 0; i < 11218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11218(int& val) {
    const int x = 11218;
    std::cout << 11218;
    for (int i = 0; i < 11218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11219(int& val) {
    func_in_11219(val);
    const int x = 11219;
    std::cout << 11219;
    for (int i = 0; i < 11219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11219(int& val) {
    const int x = 11219;
    std::cout << 11219;
    for (int i = 0; i < 11219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11220(int& val) {
    func_in_11220(val);
    const int x = 11220;
    std::cout << 11220;
    for (int i = 0; i < 11220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11220(int& val) {
    const int x = 11220;
    std::cout << 11220;
    for (int i = 0; i < 11220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11221(int& val) {
    func_in_11221(val);
    const int x = 11221;
    std::cout << 11221;
    for (int i = 0; i < 11221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11221(int& val) {
    const int x = 11221;
    std::cout << 11221;
    for (int i = 0; i < 11221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11222(int& val) {
    func_in_11222(val);
    const int x = 11222;
    std::cout << 11222;
    for (int i = 0; i < 11222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11222(int& val) {
    const int x = 11222;
    std::cout << 11222;
    for (int i = 0; i < 11222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11223(int& val) {
    func_in_11223(val);
    const int x = 11223;
    std::cout << 11223;
    for (int i = 0; i < 11223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11223(int& val) {
    const int x = 11223;
    std::cout << 11223;
    for (int i = 0; i < 11223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11224(int& val) {
    func_in_11224(val);
    const int x = 11224;
    std::cout << 11224;
    for (int i = 0; i < 11224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11224(int& val) {
    const int x = 11224;
    std::cout << 11224;
    for (int i = 0; i < 11224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11225(int& val) {
    func_in_11225(val);
    const int x = 11225;
    std::cout << 11225;
    for (int i = 0; i < 11225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11225(int& val) {
    const int x = 11225;
    std::cout << 11225;
    for (int i = 0; i < 11225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11226(int& val) {
    func_in_11226(val);
    const int x = 11226;
    std::cout << 11226;
    for (int i = 0; i < 11226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11226(int& val) {
    const int x = 11226;
    std::cout << 11226;
    for (int i = 0; i < 11226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11227(int& val) {
    func_in_11227(val);
    const int x = 11227;
    std::cout << 11227;
    for (int i = 0; i < 11227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11227(int& val) {
    const int x = 11227;
    std::cout << 11227;
    for (int i = 0; i < 11227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11228(int& val) {
    func_in_11228(val);
    const int x = 11228;
    std::cout << 11228;
    for (int i = 0; i < 11228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11228(int& val) {
    const int x = 11228;
    std::cout << 11228;
    for (int i = 0; i < 11228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11229(int& val) {
    func_in_11229(val);
    const int x = 11229;
    std::cout << 11229;
    for (int i = 0; i < 11229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11229(int& val) {
    const int x = 11229;
    std::cout << 11229;
    for (int i = 0; i < 11229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11230(int& val) {
    func_in_11230(val);
    const int x = 11230;
    std::cout << 11230;
    for (int i = 0; i < 11230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11230(int& val) {
    const int x = 11230;
    std::cout << 11230;
    for (int i = 0; i < 11230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11231(int& val) {
    func_in_11231(val);
    const int x = 11231;
    std::cout << 11231;
    for (int i = 0; i < 11231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11231(int& val) {
    const int x = 11231;
    std::cout << 11231;
    for (int i = 0; i < 11231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11232(int& val) {
    func_in_11232(val);
    const int x = 11232;
    std::cout << 11232;
    for (int i = 0; i < 11232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11232(int& val) {
    const int x = 11232;
    std::cout << 11232;
    for (int i = 0; i < 11232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11233(int& val) {
    func_in_11233(val);
    const int x = 11233;
    std::cout << 11233;
    for (int i = 0; i < 11233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11233(int& val) {
    const int x = 11233;
    std::cout << 11233;
    for (int i = 0; i < 11233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11234(int& val) {
    func_in_11234(val);
    const int x = 11234;
    std::cout << 11234;
    for (int i = 0; i < 11234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11234(int& val) {
    const int x = 11234;
    std::cout << 11234;
    for (int i = 0; i < 11234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11235(int& val) {
    func_in_11235(val);
    const int x = 11235;
    std::cout << 11235;
    for (int i = 0; i < 11235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11235(int& val) {
    const int x = 11235;
    std::cout << 11235;
    for (int i = 0; i < 11235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11236(int& val) {
    func_in_11236(val);
    const int x = 11236;
    std::cout << 11236;
    for (int i = 0; i < 11236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11236(int& val) {
    const int x = 11236;
    std::cout << 11236;
    for (int i = 0; i < 11236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11237(int& val) {
    func_in_11237(val);
    const int x = 11237;
    std::cout << 11237;
    for (int i = 0; i < 11237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11237(int& val) {
    const int x = 11237;
    std::cout << 11237;
    for (int i = 0; i < 11237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11238(int& val) {
    func_in_11238(val);
    const int x = 11238;
    std::cout << 11238;
    for (int i = 0; i < 11238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11238(int& val) {
    const int x = 11238;
    std::cout << 11238;
    for (int i = 0; i < 11238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11239(int& val) {
    func_in_11239(val);
    const int x = 11239;
    std::cout << 11239;
    for (int i = 0; i < 11239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11239(int& val) {
    const int x = 11239;
    std::cout << 11239;
    for (int i = 0; i < 11239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11240(int& val) {
    func_in_11240(val);
    const int x = 11240;
    std::cout << 11240;
    for (int i = 0; i < 11240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11240(int& val) {
    const int x = 11240;
    std::cout << 11240;
    for (int i = 0; i < 11240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11241(int& val) {
    func_in_11241(val);
    const int x = 11241;
    std::cout << 11241;
    for (int i = 0; i < 11241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11241(int& val) {
    const int x = 11241;
    std::cout << 11241;
    for (int i = 0; i < 11241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11242(int& val) {
    func_in_11242(val);
    const int x = 11242;
    std::cout << 11242;
    for (int i = 0; i < 11242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11242(int& val) {
    const int x = 11242;
    std::cout << 11242;
    for (int i = 0; i < 11242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11243(int& val) {
    func_in_11243(val);
    const int x = 11243;
    std::cout << 11243;
    for (int i = 0; i < 11243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11243(int& val) {
    const int x = 11243;
    std::cout << 11243;
    for (int i = 0; i < 11243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11244(int& val) {
    func_in_11244(val);
    const int x = 11244;
    std::cout << 11244;
    for (int i = 0; i < 11244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11244(int& val) {
    const int x = 11244;
    std::cout << 11244;
    for (int i = 0; i < 11244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11245(int& val) {
    func_in_11245(val);
    const int x = 11245;
    std::cout << 11245;
    for (int i = 0; i < 11245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11245(int& val) {
    const int x = 11245;
    std::cout << 11245;
    for (int i = 0; i < 11245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11246(int& val) {
    func_in_11246(val);
    const int x = 11246;
    std::cout << 11246;
    for (int i = 0; i < 11246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11246(int& val) {
    const int x = 11246;
    std::cout << 11246;
    for (int i = 0; i < 11246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11247(int& val) {
    func_in_11247(val);
    const int x = 11247;
    std::cout << 11247;
    for (int i = 0; i < 11247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11247(int& val) {
    const int x = 11247;
    std::cout << 11247;
    for (int i = 0; i < 11247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11248(int& val) {
    func_in_11248(val);
    const int x = 11248;
    std::cout << 11248;
    for (int i = 0; i < 11248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11248(int& val) {
    const int x = 11248;
    std::cout << 11248;
    for (int i = 0; i < 11248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11249(int& val) {
    func_in_11249(val);
    const int x = 11249;
    std::cout << 11249;
    for (int i = 0; i < 11249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11249(int& val) {
    const int x = 11249;
    std::cout << 11249;
    for (int i = 0; i < 11249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11250(int& val) {
    func_in_11250(val);
    const int x = 11250;
    std::cout << 11250;
    for (int i = 0; i < 11250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11250(int& val) {
    const int x = 11250;
    std::cout << 11250;
    for (int i = 0; i < 11250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11251(int& val) {
    func_in_11251(val);
    const int x = 11251;
    std::cout << 11251;
    for (int i = 0; i < 11251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11251(int& val) {
    const int x = 11251;
    std::cout << 11251;
    for (int i = 0; i < 11251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11252(int& val) {
    func_in_11252(val);
    const int x = 11252;
    std::cout << 11252;
    for (int i = 0; i < 11252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11252(int& val) {
    const int x = 11252;
    std::cout << 11252;
    for (int i = 0; i < 11252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11253(int& val) {
    func_in_11253(val);
    const int x = 11253;
    std::cout << 11253;
    for (int i = 0; i < 11253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11253(int& val) {
    const int x = 11253;
    std::cout << 11253;
    for (int i = 0; i < 11253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11254(int& val) {
    func_in_11254(val);
    const int x = 11254;
    std::cout << 11254;
    for (int i = 0; i < 11254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11254(int& val) {
    const int x = 11254;
    std::cout << 11254;
    for (int i = 0; i < 11254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11255(int& val) {
    func_in_11255(val);
    const int x = 11255;
    std::cout << 11255;
    for (int i = 0; i < 11255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11255(int& val) {
    const int x = 11255;
    std::cout << 11255;
    for (int i = 0; i < 11255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11256(int& val) {
    func_in_11256(val);
    const int x = 11256;
    std::cout << 11256;
    for (int i = 0; i < 11256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11256(int& val) {
    const int x = 11256;
    std::cout << 11256;
    for (int i = 0; i < 11256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11257(int& val) {
    func_in_11257(val);
    const int x = 11257;
    std::cout << 11257;
    for (int i = 0; i < 11257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11257(int& val) {
    const int x = 11257;
    std::cout << 11257;
    for (int i = 0; i < 11257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11258(int& val) {
    func_in_11258(val);
    const int x = 11258;
    std::cout << 11258;
    for (int i = 0; i < 11258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11258(int& val) {
    const int x = 11258;
    std::cout << 11258;
    for (int i = 0; i < 11258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11259(int& val) {
    func_in_11259(val);
    const int x = 11259;
    std::cout << 11259;
    for (int i = 0; i < 11259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11259(int& val) {
    const int x = 11259;
    std::cout << 11259;
    for (int i = 0; i < 11259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11260(int& val) {
    func_in_11260(val);
    const int x = 11260;
    std::cout << 11260;
    for (int i = 0; i < 11260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11260(int& val) {
    const int x = 11260;
    std::cout << 11260;
    for (int i = 0; i < 11260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11261(int& val) {
    func_in_11261(val);
    const int x = 11261;
    std::cout << 11261;
    for (int i = 0; i < 11261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11261(int& val) {
    const int x = 11261;
    std::cout << 11261;
    for (int i = 0; i < 11261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11262(int& val) {
    func_in_11262(val);
    const int x = 11262;
    std::cout << 11262;
    for (int i = 0; i < 11262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11262(int& val) {
    const int x = 11262;
    std::cout << 11262;
    for (int i = 0; i < 11262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11263(int& val) {
    func_in_11263(val);
    const int x = 11263;
    std::cout << 11263;
    for (int i = 0; i < 11263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11263(int& val) {
    const int x = 11263;
    std::cout << 11263;
    for (int i = 0; i < 11263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11264(int& val) {
    func_in_11264(val);
    const int x = 11264;
    std::cout << 11264;
    for (int i = 0; i < 11264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11264(int& val) {
    const int x = 11264;
    std::cout << 11264;
    for (int i = 0; i < 11264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11265(int& val) {
    func_in_11265(val);
    const int x = 11265;
    std::cout << 11265;
    for (int i = 0; i < 11265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11265(int& val) {
    const int x = 11265;
    std::cout << 11265;
    for (int i = 0; i < 11265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11266(int& val) {
    func_in_11266(val);
    const int x = 11266;
    std::cout << 11266;
    for (int i = 0; i < 11266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11266(int& val) {
    const int x = 11266;
    std::cout << 11266;
    for (int i = 0; i < 11266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11267(int& val) {
    func_in_11267(val);
    const int x = 11267;
    std::cout << 11267;
    for (int i = 0; i < 11267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11267(int& val) {
    const int x = 11267;
    std::cout << 11267;
    for (int i = 0; i < 11267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11268(int& val) {
    func_in_11268(val);
    const int x = 11268;
    std::cout << 11268;
    for (int i = 0; i < 11268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11268(int& val) {
    const int x = 11268;
    std::cout << 11268;
    for (int i = 0; i < 11268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11269(int& val) {
    func_in_11269(val);
    const int x = 11269;
    std::cout << 11269;
    for (int i = 0; i < 11269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11269(int& val) {
    const int x = 11269;
    std::cout << 11269;
    for (int i = 0; i < 11269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11270(int& val) {
    func_in_11270(val);
    const int x = 11270;
    std::cout << 11270;
    for (int i = 0; i < 11270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11270(int& val) {
    const int x = 11270;
    std::cout << 11270;
    for (int i = 0; i < 11270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11271(int& val) {
    func_in_11271(val);
    const int x = 11271;
    std::cout << 11271;
    for (int i = 0; i < 11271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11271(int& val) {
    const int x = 11271;
    std::cout << 11271;
    for (int i = 0; i < 11271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11272(int& val) {
    func_in_11272(val);
    const int x = 11272;
    std::cout << 11272;
    for (int i = 0; i < 11272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11272(int& val) {
    const int x = 11272;
    std::cout << 11272;
    for (int i = 0; i < 11272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11273(int& val) {
    func_in_11273(val);
    const int x = 11273;
    std::cout << 11273;
    for (int i = 0; i < 11273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11273(int& val) {
    const int x = 11273;
    std::cout << 11273;
    for (int i = 0; i < 11273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11274(int& val) {
    func_in_11274(val);
    const int x = 11274;
    std::cout << 11274;
    for (int i = 0; i < 11274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11274(int& val) {
    const int x = 11274;
    std::cout << 11274;
    for (int i = 0; i < 11274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11275(int& val) {
    func_in_11275(val);
    const int x = 11275;
    std::cout << 11275;
    for (int i = 0; i < 11275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11275(int& val) {
    const int x = 11275;
    std::cout << 11275;
    for (int i = 0; i < 11275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11276(int& val) {
    func_in_11276(val);
    const int x = 11276;
    std::cout << 11276;
    for (int i = 0; i < 11276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11276(int& val) {
    const int x = 11276;
    std::cout << 11276;
    for (int i = 0; i < 11276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11277(int& val) {
    func_in_11277(val);
    const int x = 11277;
    std::cout << 11277;
    for (int i = 0; i < 11277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11277(int& val) {
    const int x = 11277;
    std::cout << 11277;
    for (int i = 0; i < 11277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11278(int& val) {
    func_in_11278(val);
    const int x = 11278;
    std::cout << 11278;
    for (int i = 0; i < 11278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11278(int& val) {
    const int x = 11278;
    std::cout << 11278;
    for (int i = 0; i < 11278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11279(int& val) {
    func_in_11279(val);
    const int x = 11279;
    std::cout << 11279;
    for (int i = 0; i < 11279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11279(int& val) {
    const int x = 11279;
    std::cout << 11279;
    for (int i = 0; i < 11279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11280(int& val) {
    func_in_11280(val);
    const int x = 11280;
    std::cout << 11280;
    for (int i = 0; i < 11280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11280(int& val) {
    const int x = 11280;
    std::cout << 11280;
    for (int i = 0; i < 11280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11281(int& val) {
    func_in_11281(val);
    const int x = 11281;
    std::cout << 11281;
    for (int i = 0; i < 11281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11281(int& val) {
    const int x = 11281;
    std::cout << 11281;
    for (int i = 0; i < 11281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11282(int& val) {
    func_in_11282(val);
    const int x = 11282;
    std::cout << 11282;
    for (int i = 0; i < 11282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11282(int& val) {
    const int x = 11282;
    std::cout << 11282;
    for (int i = 0; i < 11282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11283(int& val) {
    func_in_11283(val);
    const int x = 11283;
    std::cout << 11283;
    for (int i = 0; i < 11283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11283(int& val) {
    const int x = 11283;
    std::cout << 11283;
    for (int i = 0; i < 11283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11284(int& val) {
    func_in_11284(val);
    const int x = 11284;
    std::cout << 11284;
    for (int i = 0; i < 11284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11284(int& val) {
    const int x = 11284;
    std::cout << 11284;
    for (int i = 0; i < 11284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11285(int& val) {
    func_in_11285(val);
    const int x = 11285;
    std::cout << 11285;
    for (int i = 0; i < 11285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11285(int& val) {
    const int x = 11285;
    std::cout << 11285;
    for (int i = 0; i < 11285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11286(int& val) {
    func_in_11286(val);
    const int x = 11286;
    std::cout << 11286;
    for (int i = 0; i < 11286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11286(int& val) {
    const int x = 11286;
    std::cout << 11286;
    for (int i = 0; i < 11286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11287(int& val) {
    func_in_11287(val);
    const int x = 11287;
    std::cout << 11287;
    for (int i = 0; i < 11287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11287(int& val) {
    const int x = 11287;
    std::cout << 11287;
    for (int i = 0; i < 11287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11288(int& val) {
    func_in_11288(val);
    const int x = 11288;
    std::cout << 11288;
    for (int i = 0; i < 11288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11288(int& val) {
    const int x = 11288;
    std::cout << 11288;
    for (int i = 0; i < 11288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11289(int& val) {
    func_in_11289(val);
    const int x = 11289;
    std::cout << 11289;
    for (int i = 0; i < 11289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11289(int& val) {
    const int x = 11289;
    std::cout << 11289;
    for (int i = 0; i < 11289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11290(int& val) {
    func_in_11290(val);
    const int x = 11290;
    std::cout << 11290;
    for (int i = 0; i < 11290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11290(int& val) {
    const int x = 11290;
    std::cout << 11290;
    for (int i = 0; i < 11290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11291(int& val) {
    func_in_11291(val);
    const int x = 11291;
    std::cout << 11291;
    for (int i = 0; i < 11291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11291(int& val) {
    const int x = 11291;
    std::cout << 11291;
    for (int i = 0; i < 11291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11292(int& val) {
    func_in_11292(val);
    const int x = 11292;
    std::cout << 11292;
    for (int i = 0; i < 11292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11292(int& val) {
    const int x = 11292;
    std::cout << 11292;
    for (int i = 0; i < 11292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11293(int& val) {
    func_in_11293(val);
    const int x = 11293;
    std::cout << 11293;
    for (int i = 0; i < 11293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11293(int& val) {
    const int x = 11293;
    std::cout << 11293;
    for (int i = 0; i < 11293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11294(int& val) {
    func_in_11294(val);
    const int x = 11294;
    std::cout << 11294;
    for (int i = 0; i < 11294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11294(int& val) {
    const int x = 11294;
    std::cout << 11294;
    for (int i = 0; i < 11294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11295(int& val) {
    func_in_11295(val);
    const int x = 11295;
    std::cout << 11295;
    for (int i = 0; i < 11295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11295(int& val) {
    const int x = 11295;
    std::cout << 11295;
    for (int i = 0; i < 11295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11296(int& val) {
    func_in_11296(val);
    const int x = 11296;
    std::cout << 11296;
    for (int i = 0; i < 11296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11296(int& val) {
    const int x = 11296;
    std::cout << 11296;
    for (int i = 0; i < 11296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11297(int& val) {
    func_in_11297(val);
    const int x = 11297;
    std::cout << 11297;
    for (int i = 0; i < 11297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11297(int& val) {
    const int x = 11297;
    std::cout << 11297;
    for (int i = 0; i < 11297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11298(int& val) {
    func_in_11298(val);
    const int x = 11298;
    std::cout << 11298;
    for (int i = 0; i < 11298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11298(int& val) {
    const int x = 11298;
    std::cout << 11298;
    for (int i = 0; i < 11298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11299(int& val) {
    func_in_11299(val);
    const int x = 11299;
    std::cout << 11299;
    for (int i = 0; i < 11299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11299(int& val) {
    const int x = 11299;
    std::cout << 11299;
    for (int i = 0; i < 11299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11300(int& val) {
    func_in_11300(val);
    const int x = 11300;
    std::cout << 11300;
    for (int i = 0; i < 11300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11300(int& val) {
    const int x = 11300;
    std::cout << 11300;
    for (int i = 0; i < 11300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11301(int& val) {
    func_in_11301(val);
    const int x = 11301;
    std::cout << 11301;
    for (int i = 0; i < 11301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11301(int& val) {
    const int x = 11301;
    std::cout << 11301;
    for (int i = 0; i < 11301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11302(int& val) {
    func_in_11302(val);
    const int x = 11302;
    std::cout << 11302;
    for (int i = 0; i < 11302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11302(int& val) {
    const int x = 11302;
    std::cout << 11302;
    for (int i = 0; i < 11302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11303(int& val) {
    func_in_11303(val);
    const int x = 11303;
    std::cout << 11303;
    for (int i = 0; i < 11303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11303(int& val) {
    const int x = 11303;
    std::cout << 11303;
    for (int i = 0; i < 11303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11304(int& val) {
    func_in_11304(val);
    const int x = 11304;
    std::cout << 11304;
    for (int i = 0; i < 11304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11304(int& val) {
    const int x = 11304;
    std::cout << 11304;
    for (int i = 0; i < 11304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11305(int& val) {
    func_in_11305(val);
    const int x = 11305;
    std::cout << 11305;
    for (int i = 0; i < 11305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11305(int& val) {
    const int x = 11305;
    std::cout << 11305;
    for (int i = 0; i < 11305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11306(int& val) {
    func_in_11306(val);
    const int x = 11306;
    std::cout << 11306;
    for (int i = 0; i < 11306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11306(int& val) {
    const int x = 11306;
    std::cout << 11306;
    for (int i = 0; i < 11306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11307(int& val) {
    func_in_11307(val);
    const int x = 11307;
    std::cout << 11307;
    for (int i = 0; i < 11307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11307(int& val) {
    const int x = 11307;
    std::cout << 11307;
    for (int i = 0; i < 11307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11308(int& val) {
    func_in_11308(val);
    const int x = 11308;
    std::cout << 11308;
    for (int i = 0; i < 11308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11308(int& val) {
    const int x = 11308;
    std::cout << 11308;
    for (int i = 0; i < 11308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11309(int& val) {
    func_in_11309(val);
    const int x = 11309;
    std::cout << 11309;
    for (int i = 0; i < 11309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11309(int& val) {
    const int x = 11309;
    std::cout << 11309;
    for (int i = 0; i < 11309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11310(int& val) {
    func_in_11310(val);
    const int x = 11310;
    std::cout << 11310;
    for (int i = 0; i < 11310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11310(int& val) {
    const int x = 11310;
    std::cout << 11310;
    for (int i = 0; i < 11310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11311(int& val) {
    func_in_11311(val);
    const int x = 11311;
    std::cout << 11311;
    for (int i = 0; i < 11311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11311(int& val) {
    const int x = 11311;
    std::cout << 11311;
    for (int i = 0; i < 11311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11312(int& val) {
    func_in_11312(val);
    const int x = 11312;
    std::cout << 11312;
    for (int i = 0; i < 11312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11312(int& val) {
    const int x = 11312;
    std::cout << 11312;
    for (int i = 0; i < 11312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11313(int& val) {
    func_in_11313(val);
    const int x = 11313;
    std::cout << 11313;
    for (int i = 0; i < 11313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11313(int& val) {
    const int x = 11313;
    std::cout << 11313;
    for (int i = 0; i < 11313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11314(int& val) {
    func_in_11314(val);
    const int x = 11314;
    std::cout << 11314;
    for (int i = 0; i < 11314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11314(int& val) {
    const int x = 11314;
    std::cout << 11314;
    for (int i = 0; i < 11314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11315(int& val) {
    func_in_11315(val);
    const int x = 11315;
    std::cout << 11315;
    for (int i = 0; i < 11315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11315(int& val) {
    const int x = 11315;
    std::cout << 11315;
    for (int i = 0; i < 11315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11316(int& val) {
    func_in_11316(val);
    const int x = 11316;
    std::cout << 11316;
    for (int i = 0; i < 11316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11316(int& val) {
    const int x = 11316;
    std::cout << 11316;
    for (int i = 0; i < 11316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11317(int& val) {
    func_in_11317(val);
    const int x = 11317;
    std::cout << 11317;
    for (int i = 0; i < 11317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11317(int& val) {
    const int x = 11317;
    std::cout << 11317;
    for (int i = 0; i < 11317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11318(int& val) {
    func_in_11318(val);
    const int x = 11318;
    std::cout << 11318;
    for (int i = 0; i < 11318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11318(int& val) {
    const int x = 11318;
    std::cout << 11318;
    for (int i = 0; i < 11318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11319(int& val) {
    func_in_11319(val);
    const int x = 11319;
    std::cout << 11319;
    for (int i = 0; i < 11319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11319(int& val) {
    const int x = 11319;
    std::cout << 11319;
    for (int i = 0; i < 11319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11320(int& val) {
    func_in_11320(val);
    const int x = 11320;
    std::cout << 11320;
    for (int i = 0; i < 11320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11320(int& val) {
    const int x = 11320;
    std::cout << 11320;
    for (int i = 0; i < 11320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11321(int& val) {
    func_in_11321(val);
    const int x = 11321;
    std::cout << 11321;
    for (int i = 0; i < 11321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11321(int& val) {
    const int x = 11321;
    std::cout << 11321;
    for (int i = 0; i < 11321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11322(int& val) {
    func_in_11322(val);
    const int x = 11322;
    std::cout << 11322;
    for (int i = 0; i < 11322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11322(int& val) {
    const int x = 11322;
    std::cout << 11322;
    for (int i = 0; i < 11322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11323(int& val) {
    func_in_11323(val);
    const int x = 11323;
    std::cout << 11323;
    for (int i = 0; i < 11323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11323(int& val) {
    const int x = 11323;
    std::cout << 11323;
    for (int i = 0; i < 11323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11324(int& val) {
    func_in_11324(val);
    const int x = 11324;
    std::cout << 11324;
    for (int i = 0; i < 11324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11324(int& val) {
    const int x = 11324;
    std::cout << 11324;
    for (int i = 0; i < 11324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11325(int& val) {
    func_in_11325(val);
    const int x = 11325;
    std::cout << 11325;
    for (int i = 0; i < 11325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11325(int& val) {
    const int x = 11325;
    std::cout << 11325;
    for (int i = 0; i < 11325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11326(int& val) {
    func_in_11326(val);
    const int x = 11326;
    std::cout << 11326;
    for (int i = 0; i < 11326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11326(int& val) {
    const int x = 11326;
    std::cout << 11326;
    for (int i = 0; i < 11326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11327(int& val) {
    func_in_11327(val);
    const int x = 11327;
    std::cout << 11327;
    for (int i = 0; i < 11327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11327(int& val) {
    const int x = 11327;
    std::cout << 11327;
    for (int i = 0; i < 11327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11328(int& val) {
    func_in_11328(val);
    const int x = 11328;
    std::cout << 11328;
    for (int i = 0; i < 11328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11328(int& val) {
    const int x = 11328;
    std::cout << 11328;
    for (int i = 0; i < 11328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11329(int& val) {
    func_in_11329(val);
    const int x = 11329;
    std::cout << 11329;
    for (int i = 0; i < 11329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11329(int& val) {
    const int x = 11329;
    std::cout << 11329;
    for (int i = 0; i < 11329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11330(int& val) {
    func_in_11330(val);
    const int x = 11330;
    std::cout << 11330;
    for (int i = 0; i < 11330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11330(int& val) {
    const int x = 11330;
    std::cout << 11330;
    for (int i = 0; i < 11330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11331(int& val) {
    func_in_11331(val);
    const int x = 11331;
    std::cout << 11331;
    for (int i = 0; i < 11331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11331(int& val) {
    const int x = 11331;
    std::cout << 11331;
    for (int i = 0; i < 11331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11332(int& val) {
    func_in_11332(val);
    const int x = 11332;
    std::cout << 11332;
    for (int i = 0; i < 11332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11332(int& val) {
    const int x = 11332;
    std::cout << 11332;
    for (int i = 0; i < 11332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11333(int& val) {
    func_in_11333(val);
    const int x = 11333;
    std::cout << 11333;
    for (int i = 0; i < 11333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11333(int& val) {
    const int x = 11333;
    std::cout << 11333;
    for (int i = 0; i < 11333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11334(int& val) {
    func_in_11334(val);
    const int x = 11334;
    std::cout << 11334;
    for (int i = 0; i < 11334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11334(int& val) {
    const int x = 11334;
    std::cout << 11334;
    for (int i = 0; i < 11334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11335(int& val) {
    func_in_11335(val);
    const int x = 11335;
    std::cout << 11335;
    for (int i = 0; i < 11335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11335(int& val) {
    const int x = 11335;
    std::cout << 11335;
    for (int i = 0; i < 11335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11336(int& val) {
    func_in_11336(val);
    const int x = 11336;
    std::cout << 11336;
    for (int i = 0; i < 11336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11336(int& val) {
    const int x = 11336;
    std::cout << 11336;
    for (int i = 0; i < 11336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11337(int& val) {
    func_in_11337(val);
    const int x = 11337;
    std::cout << 11337;
    for (int i = 0; i < 11337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11337(int& val) {
    const int x = 11337;
    std::cout << 11337;
    for (int i = 0; i < 11337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11338(int& val) {
    func_in_11338(val);
    const int x = 11338;
    std::cout << 11338;
    for (int i = 0; i < 11338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11338(int& val) {
    const int x = 11338;
    std::cout << 11338;
    for (int i = 0; i < 11338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11339(int& val) {
    func_in_11339(val);
    const int x = 11339;
    std::cout << 11339;
    for (int i = 0; i < 11339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11339(int& val) {
    const int x = 11339;
    std::cout << 11339;
    for (int i = 0; i < 11339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11340(int& val) {
    func_in_11340(val);
    const int x = 11340;
    std::cout << 11340;
    for (int i = 0; i < 11340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11340(int& val) {
    const int x = 11340;
    std::cout << 11340;
    for (int i = 0; i < 11340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11341(int& val) {
    func_in_11341(val);
    const int x = 11341;
    std::cout << 11341;
    for (int i = 0; i < 11341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11341(int& val) {
    const int x = 11341;
    std::cout << 11341;
    for (int i = 0; i < 11341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11342(int& val) {
    func_in_11342(val);
    const int x = 11342;
    std::cout << 11342;
    for (int i = 0; i < 11342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11342(int& val) {
    const int x = 11342;
    std::cout << 11342;
    for (int i = 0; i < 11342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11343(int& val) {
    func_in_11343(val);
    const int x = 11343;
    std::cout << 11343;
    for (int i = 0; i < 11343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11343(int& val) {
    const int x = 11343;
    std::cout << 11343;
    for (int i = 0; i < 11343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11344(int& val) {
    func_in_11344(val);
    const int x = 11344;
    std::cout << 11344;
    for (int i = 0; i < 11344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11344(int& val) {
    const int x = 11344;
    std::cout << 11344;
    for (int i = 0; i < 11344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11345(int& val) {
    func_in_11345(val);
    const int x = 11345;
    std::cout << 11345;
    for (int i = 0; i < 11345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11345(int& val) {
    const int x = 11345;
    std::cout << 11345;
    for (int i = 0; i < 11345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11346(int& val) {
    func_in_11346(val);
    const int x = 11346;
    std::cout << 11346;
    for (int i = 0; i < 11346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11346(int& val) {
    const int x = 11346;
    std::cout << 11346;
    for (int i = 0; i < 11346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11347(int& val) {
    func_in_11347(val);
    const int x = 11347;
    std::cout << 11347;
    for (int i = 0; i < 11347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11347(int& val) {
    const int x = 11347;
    std::cout << 11347;
    for (int i = 0; i < 11347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11348(int& val) {
    func_in_11348(val);
    const int x = 11348;
    std::cout << 11348;
    for (int i = 0; i < 11348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11348(int& val) {
    const int x = 11348;
    std::cout << 11348;
    for (int i = 0; i < 11348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11349(int& val) {
    func_in_11349(val);
    const int x = 11349;
    std::cout << 11349;
    for (int i = 0; i < 11349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11349(int& val) {
    const int x = 11349;
    std::cout << 11349;
    for (int i = 0; i < 11349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11350(int& val) {
    func_in_11350(val);
    const int x = 11350;
    std::cout << 11350;
    for (int i = 0; i < 11350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11350(int& val) {
    const int x = 11350;
    std::cout << 11350;
    for (int i = 0; i < 11350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11351(int& val) {
    func_in_11351(val);
    const int x = 11351;
    std::cout << 11351;
    for (int i = 0; i < 11351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11351(int& val) {
    const int x = 11351;
    std::cout << 11351;
    for (int i = 0; i < 11351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11352(int& val) {
    func_in_11352(val);
    const int x = 11352;
    std::cout << 11352;
    for (int i = 0; i < 11352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11352(int& val) {
    const int x = 11352;
    std::cout << 11352;
    for (int i = 0; i < 11352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11353(int& val) {
    func_in_11353(val);
    const int x = 11353;
    std::cout << 11353;
    for (int i = 0; i < 11353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11353(int& val) {
    const int x = 11353;
    std::cout << 11353;
    for (int i = 0; i < 11353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11354(int& val) {
    func_in_11354(val);
    const int x = 11354;
    std::cout << 11354;
    for (int i = 0; i < 11354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11354(int& val) {
    const int x = 11354;
    std::cout << 11354;
    for (int i = 0; i < 11354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11355(int& val) {
    func_in_11355(val);
    const int x = 11355;
    std::cout << 11355;
    for (int i = 0; i < 11355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11355(int& val) {
    const int x = 11355;
    std::cout << 11355;
    for (int i = 0; i < 11355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11356(int& val) {
    func_in_11356(val);
    const int x = 11356;
    std::cout << 11356;
    for (int i = 0; i < 11356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11356(int& val) {
    const int x = 11356;
    std::cout << 11356;
    for (int i = 0; i < 11356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11357(int& val) {
    func_in_11357(val);
    const int x = 11357;
    std::cout << 11357;
    for (int i = 0; i < 11357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11357(int& val) {
    const int x = 11357;
    std::cout << 11357;
    for (int i = 0; i < 11357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11358(int& val) {
    func_in_11358(val);
    const int x = 11358;
    std::cout << 11358;
    for (int i = 0; i < 11358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11358(int& val) {
    const int x = 11358;
    std::cout << 11358;
    for (int i = 0; i < 11358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11359(int& val) {
    func_in_11359(val);
    const int x = 11359;
    std::cout << 11359;
    for (int i = 0; i < 11359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11359(int& val) {
    const int x = 11359;
    std::cout << 11359;
    for (int i = 0; i < 11359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11360(int& val) {
    func_in_11360(val);
    const int x = 11360;
    std::cout << 11360;
    for (int i = 0; i < 11360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11360(int& val) {
    const int x = 11360;
    std::cout << 11360;
    for (int i = 0; i < 11360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11361(int& val) {
    func_in_11361(val);
    const int x = 11361;
    std::cout << 11361;
    for (int i = 0; i < 11361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11361(int& val) {
    const int x = 11361;
    std::cout << 11361;
    for (int i = 0; i < 11361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11362(int& val) {
    func_in_11362(val);
    const int x = 11362;
    std::cout << 11362;
    for (int i = 0; i < 11362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11362(int& val) {
    const int x = 11362;
    std::cout << 11362;
    for (int i = 0; i < 11362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11363(int& val) {
    func_in_11363(val);
    const int x = 11363;
    std::cout << 11363;
    for (int i = 0; i < 11363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11363(int& val) {
    const int x = 11363;
    std::cout << 11363;
    for (int i = 0; i < 11363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11364(int& val) {
    func_in_11364(val);
    const int x = 11364;
    std::cout << 11364;
    for (int i = 0; i < 11364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11364(int& val) {
    const int x = 11364;
    std::cout << 11364;
    for (int i = 0; i < 11364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11365(int& val) {
    func_in_11365(val);
    const int x = 11365;
    std::cout << 11365;
    for (int i = 0; i < 11365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11365(int& val) {
    const int x = 11365;
    std::cout << 11365;
    for (int i = 0; i < 11365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11366(int& val) {
    func_in_11366(val);
    const int x = 11366;
    std::cout << 11366;
    for (int i = 0; i < 11366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11366(int& val) {
    const int x = 11366;
    std::cout << 11366;
    for (int i = 0; i < 11366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11367(int& val) {
    func_in_11367(val);
    const int x = 11367;
    std::cout << 11367;
    for (int i = 0; i < 11367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11367(int& val) {
    const int x = 11367;
    std::cout << 11367;
    for (int i = 0; i < 11367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11368(int& val) {
    func_in_11368(val);
    const int x = 11368;
    std::cout << 11368;
    for (int i = 0; i < 11368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11368(int& val) {
    const int x = 11368;
    std::cout << 11368;
    for (int i = 0; i < 11368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11369(int& val) {
    func_in_11369(val);
    const int x = 11369;
    std::cout << 11369;
    for (int i = 0; i < 11369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11369(int& val) {
    const int x = 11369;
    std::cout << 11369;
    for (int i = 0; i < 11369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11370(int& val) {
    func_in_11370(val);
    const int x = 11370;
    std::cout << 11370;
    for (int i = 0; i < 11370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11370(int& val) {
    const int x = 11370;
    std::cout << 11370;
    for (int i = 0; i < 11370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11371(int& val) {
    func_in_11371(val);
    const int x = 11371;
    std::cout << 11371;
    for (int i = 0; i < 11371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11371(int& val) {
    const int x = 11371;
    std::cout << 11371;
    for (int i = 0; i < 11371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11372(int& val) {
    func_in_11372(val);
    const int x = 11372;
    std::cout << 11372;
    for (int i = 0; i < 11372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11372(int& val) {
    const int x = 11372;
    std::cout << 11372;
    for (int i = 0; i < 11372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11373(int& val) {
    func_in_11373(val);
    const int x = 11373;
    std::cout << 11373;
    for (int i = 0; i < 11373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11373(int& val) {
    const int x = 11373;
    std::cout << 11373;
    for (int i = 0; i < 11373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11374(int& val) {
    func_in_11374(val);
    const int x = 11374;
    std::cout << 11374;
    for (int i = 0; i < 11374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11374(int& val) {
    const int x = 11374;
    std::cout << 11374;
    for (int i = 0; i < 11374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11375(int& val) {
    func_in_11375(val);
    const int x = 11375;
    std::cout << 11375;
    for (int i = 0; i < 11375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11375(int& val) {
    const int x = 11375;
    std::cout << 11375;
    for (int i = 0; i < 11375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11376(int& val) {
    func_in_11376(val);
    const int x = 11376;
    std::cout << 11376;
    for (int i = 0; i < 11376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11376(int& val) {
    const int x = 11376;
    std::cout << 11376;
    for (int i = 0; i < 11376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11377(int& val) {
    func_in_11377(val);
    const int x = 11377;
    std::cout << 11377;
    for (int i = 0; i < 11377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11377(int& val) {
    const int x = 11377;
    std::cout << 11377;
    for (int i = 0; i < 11377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11378(int& val) {
    func_in_11378(val);
    const int x = 11378;
    std::cout << 11378;
    for (int i = 0; i < 11378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11378(int& val) {
    const int x = 11378;
    std::cout << 11378;
    for (int i = 0; i < 11378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11379(int& val) {
    func_in_11379(val);
    const int x = 11379;
    std::cout << 11379;
    for (int i = 0; i < 11379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11379(int& val) {
    const int x = 11379;
    std::cout << 11379;
    for (int i = 0; i < 11379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11380(int& val) {
    func_in_11380(val);
    const int x = 11380;
    std::cout << 11380;
    for (int i = 0; i < 11380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11380(int& val) {
    const int x = 11380;
    std::cout << 11380;
    for (int i = 0; i < 11380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11381(int& val) {
    func_in_11381(val);
    const int x = 11381;
    std::cout << 11381;
    for (int i = 0; i < 11381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11381(int& val) {
    const int x = 11381;
    std::cout << 11381;
    for (int i = 0; i < 11381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11382(int& val) {
    func_in_11382(val);
    const int x = 11382;
    std::cout << 11382;
    for (int i = 0; i < 11382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11382(int& val) {
    const int x = 11382;
    std::cout << 11382;
    for (int i = 0; i < 11382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11383(int& val) {
    func_in_11383(val);
    const int x = 11383;
    std::cout << 11383;
    for (int i = 0; i < 11383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11383(int& val) {
    const int x = 11383;
    std::cout << 11383;
    for (int i = 0; i < 11383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11384(int& val) {
    func_in_11384(val);
    const int x = 11384;
    std::cout << 11384;
    for (int i = 0; i < 11384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11384(int& val) {
    const int x = 11384;
    std::cout << 11384;
    for (int i = 0; i < 11384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11385(int& val) {
    func_in_11385(val);
    const int x = 11385;
    std::cout << 11385;
    for (int i = 0; i < 11385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11385(int& val) {
    const int x = 11385;
    std::cout << 11385;
    for (int i = 0; i < 11385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11386(int& val) {
    func_in_11386(val);
    const int x = 11386;
    std::cout << 11386;
    for (int i = 0; i < 11386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11386(int& val) {
    const int x = 11386;
    std::cout << 11386;
    for (int i = 0; i < 11386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11387(int& val) {
    func_in_11387(val);
    const int x = 11387;
    std::cout << 11387;
    for (int i = 0; i < 11387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11387(int& val) {
    const int x = 11387;
    std::cout << 11387;
    for (int i = 0; i < 11387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11388(int& val) {
    func_in_11388(val);
    const int x = 11388;
    std::cout << 11388;
    for (int i = 0; i < 11388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11388(int& val) {
    const int x = 11388;
    std::cout << 11388;
    for (int i = 0; i < 11388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11389(int& val) {
    func_in_11389(val);
    const int x = 11389;
    std::cout << 11389;
    for (int i = 0; i < 11389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11389(int& val) {
    const int x = 11389;
    std::cout << 11389;
    for (int i = 0; i < 11389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11390(int& val) {
    func_in_11390(val);
    const int x = 11390;
    std::cout << 11390;
    for (int i = 0; i < 11390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11390(int& val) {
    const int x = 11390;
    std::cout << 11390;
    for (int i = 0; i < 11390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11391(int& val) {
    func_in_11391(val);
    const int x = 11391;
    std::cout << 11391;
    for (int i = 0; i < 11391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11391(int& val) {
    const int x = 11391;
    std::cout << 11391;
    for (int i = 0; i < 11391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11392(int& val) {
    func_in_11392(val);
    const int x = 11392;
    std::cout << 11392;
    for (int i = 0; i < 11392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11392(int& val) {
    const int x = 11392;
    std::cout << 11392;
    for (int i = 0; i < 11392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11393(int& val) {
    func_in_11393(val);
    const int x = 11393;
    std::cout << 11393;
    for (int i = 0; i < 11393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11393(int& val) {
    const int x = 11393;
    std::cout << 11393;
    for (int i = 0; i < 11393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11394(int& val) {
    func_in_11394(val);
    const int x = 11394;
    std::cout << 11394;
    for (int i = 0; i < 11394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11394(int& val) {
    const int x = 11394;
    std::cout << 11394;
    for (int i = 0; i < 11394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11395(int& val) {
    func_in_11395(val);
    const int x = 11395;
    std::cout << 11395;
    for (int i = 0; i < 11395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11395(int& val) {
    const int x = 11395;
    std::cout << 11395;
    for (int i = 0; i < 11395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11396(int& val) {
    func_in_11396(val);
    const int x = 11396;
    std::cout << 11396;
    for (int i = 0; i < 11396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11396(int& val) {
    const int x = 11396;
    std::cout << 11396;
    for (int i = 0; i < 11396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11397(int& val) {
    func_in_11397(val);
    const int x = 11397;
    std::cout << 11397;
    for (int i = 0; i < 11397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11397(int& val) {
    const int x = 11397;
    std::cout << 11397;
    for (int i = 0; i < 11397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11398(int& val) {
    func_in_11398(val);
    const int x = 11398;
    std::cout << 11398;
    for (int i = 0; i < 11398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11398(int& val) {
    const int x = 11398;
    std::cout << 11398;
    for (int i = 0; i < 11398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11399(int& val) {
    func_in_11399(val);
    const int x = 11399;
    std::cout << 11399;
    for (int i = 0; i < 11399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11399(int& val) {
    const int x = 11399;
    std::cout << 11399;
    for (int i = 0; i < 11399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11400(int& val) {
    func_in_11400(val);
    const int x = 11400;
    std::cout << 11400;
    for (int i = 0; i < 11400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11400(int& val) {
    const int x = 11400;
    std::cout << 11400;
    for (int i = 0; i < 11400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11401(int& val) {
    func_in_11401(val);
    const int x = 11401;
    std::cout << 11401;
    for (int i = 0; i < 11401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11401(int& val) {
    const int x = 11401;
    std::cout << 11401;
    for (int i = 0; i < 11401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11402(int& val) {
    func_in_11402(val);
    const int x = 11402;
    std::cout << 11402;
    for (int i = 0; i < 11402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11402(int& val) {
    const int x = 11402;
    std::cout << 11402;
    for (int i = 0; i < 11402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11403(int& val) {
    func_in_11403(val);
    const int x = 11403;
    std::cout << 11403;
    for (int i = 0; i < 11403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11403(int& val) {
    const int x = 11403;
    std::cout << 11403;
    for (int i = 0; i < 11403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11404(int& val) {
    func_in_11404(val);
    const int x = 11404;
    std::cout << 11404;
    for (int i = 0; i < 11404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11404(int& val) {
    const int x = 11404;
    std::cout << 11404;
    for (int i = 0; i < 11404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11405(int& val) {
    func_in_11405(val);
    const int x = 11405;
    std::cout << 11405;
    for (int i = 0; i < 11405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11405(int& val) {
    const int x = 11405;
    std::cout << 11405;
    for (int i = 0; i < 11405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11406(int& val) {
    func_in_11406(val);
    const int x = 11406;
    std::cout << 11406;
    for (int i = 0; i < 11406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11406(int& val) {
    const int x = 11406;
    std::cout << 11406;
    for (int i = 0; i < 11406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11407(int& val) {
    func_in_11407(val);
    const int x = 11407;
    std::cout << 11407;
    for (int i = 0; i < 11407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11407(int& val) {
    const int x = 11407;
    std::cout << 11407;
    for (int i = 0; i < 11407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11408(int& val) {
    func_in_11408(val);
    const int x = 11408;
    std::cout << 11408;
    for (int i = 0; i < 11408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11408(int& val) {
    const int x = 11408;
    std::cout << 11408;
    for (int i = 0; i < 11408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11409(int& val) {
    func_in_11409(val);
    const int x = 11409;
    std::cout << 11409;
    for (int i = 0; i < 11409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11409(int& val) {
    const int x = 11409;
    std::cout << 11409;
    for (int i = 0; i < 11409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11410(int& val) {
    func_in_11410(val);
    const int x = 11410;
    std::cout << 11410;
    for (int i = 0; i < 11410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11410(int& val) {
    const int x = 11410;
    std::cout << 11410;
    for (int i = 0; i < 11410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11411(int& val) {
    func_in_11411(val);
    const int x = 11411;
    std::cout << 11411;
    for (int i = 0; i < 11411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11411(int& val) {
    const int x = 11411;
    std::cout << 11411;
    for (int i = 0; i < 11411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11412(int& val) {
    func_in_11412(val);
    const int x = 11412;
    std::cout << 11412;
    for (int i = 0; i < 11412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11412(int& val) {
    const int x = 11412;
    std::cout << 11412;
    for (int i = 0; i < 11412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11413(int& val) {
    func_in_11413(val);
    const int x = 11413;
    std::cout << 11413;
    for (int i = 0; i < 11413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11413(int& val) {
    const int x = 11413;
    std::cout << 11413;
    for (int i = 0; i < 11413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11414(int& val) {
    func_in_11414(val);
    const int x = 11414;
    std::cout << 11414;
    for (int i = 0; i < 11414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11414(int& val) {
    const int x = 11414;
    std::cout << 11414;
    for (int i = 0; i < 11414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11415(int& val) {
    func_in_11415(val);
    const int x = 11415;
    std::cout << 11415;
    for (int i = 0; i < 11415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11415(int& val) {
    const int x = 11415;
    std::cout << 11415;
    for (int i = 0; i < 11415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11416(int& val) {
    func_in_11416(val);
    const int x = 11416;
    std::cout << 11416;
    for (int i = 0; i < 11416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11416(int& val) {
    const int x = 11416;
    std::cout << 11416;
    for (int i = 0; i < 11416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11417(int& val) {
    func_in_11417(val);
    const int x = 11417;
    std::cout << 11417;
    for (int i = 0; i < 11417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11417(int& val) {
    const int x = 11417;
    std::cout << 11417;
    for (int i = 0; i < 11417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11418(int& val) {
    func_in_11418(val);
    const int x = 11418;
    std::cout << 11418;
    for (int i = 0; i < 11418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11418(int& val) {
    const int x = 11418;
    std::cout << 11418;
    for (int i = 0; i < 11418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11419(int& val) {
    func_in_11419(val);
    const int x = 11419;
    std::cout << 11419;
    for (int i = 0; i < 11419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11419(int& val) {
    const int x = 11419;
    std::cout << 11419;
    for (int i = 0; i < 11419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11420(int& val) {
    func_in_11420(val);
    const int x = 11420;
    std::cout << 11420;
    for (int i = 0; i < 11420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11420(int& val) {
    const int x = 11420;
    std::cout << 11420;
    for (int i = 0; i < 11420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11421(int& val) {
    func_in_11421(val);
    const int x = 11421;
    std::cout << 11421;
    for (int i = 0; i < 11421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11421(int& val) {
    const int x = 11421;
    std::cout << 11421;
    for (int i = 0; i < 11421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11422(int& val) {
    func_in_11422(val);
    const int x = 11422;
    std::cout << 11422;
    for (int i = 0; i < 11422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11422(int& val) {
    const int x = 11422;
    std::cout << 11422;
    for (int i = 0; i < 11422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11423(int& val) {
    func_in_11423(val);
    const int x = 11423;
    std::cout << 11423;
    for (int i = 0; i < 11423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11423(int& val) {
    const int x = 11423;
    std::cout << 11423;
    for (int i = 0; i < 11423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11424(int& val) {
    func_in_11424(val);
    const int x = 11424;
    std::cout << 11424;
    for (int i = 0; i < 11424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11424(int& val) {
    const int x = 11424;
    std::cout << 11424;
    for (int i = 0; i < 11424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11425(int& val) {
    func_in_11425(val);
    const int x = 11425;
    std::cout << 11425;
    for (int i = 0; i < 11425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11425(int& val) {
    const int x = 11425;
    std::cout << 11425;
    for (int i = 0; i < 11425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11426(int& val) {
    func_in_11426(val);
    const int x = 11426;
    std::cout << 11426;
    for (int i = 0; i < 11426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11426(int& val) {
    const int x = 11426;
    std::cout << 11426;
    for (int i = 0; i < 11426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11427(int& val) {
    func_in_11427(val);
    const int x = 11427;
    std::cout << 11427;
    for (int i = 0; i < 11427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11427(int& val) {
    const int x = 11427;
    std::cout << 11427;
    for (int i = 0; i < 11427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11428(int& val) {
    func_in_11428(val);
    const int x = 11428;
    std::cout << 11428;
    for (int i = 0; i < 11428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11428(int& val) {
    const int x = 11428;
    std::cout << 11428;
    for (int i = 0; i < 11428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11429(int& val) {
    func_in_11429(val);
    const int x = 11429;
    std::cout << 11429;
    for (int i = 0; i < 11429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11429(int& val) {
    const int x = 11429;
    std::cout << 11429;
    for (int i = 0; i < 11429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11430(int& val) {
    func_in_11430(val);
    const int x = 11430;
    std::cout << 11430;
    for (int i = 0; i < 11430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11430(int& val) {
    const int x = 11430;
    std::cout << 11430;
    for (int i = 0; i < 11430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11431(int& val) {
    func_in_11431(val);
    const int x = 11431;
    std::cout << 11431;
    for (int i = 0; i < 11431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11431(int& val) {
    const int x = 11431;
    std::cout << 11431;
    for (int i = 0; i < 11431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11432(int& val) {
    func_in_11432(val);
    const int x = 11432;
    std::cout << 11432;
    for (int i = 0; i < 11432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11432(int& val) {
    const int x = 11432;
    std::cout << 11432;
    for (int i = 0; i < 11432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11433(int& val) {
    func_in_11433(val);
    const int x = 11433;
    std::cout << 11433;
    for (int i = 0; i < 11433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11433(int& val) {
    const int x = 11433;
    std::cout << 11433;
    for (int i = 0; i < 11433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11434(int& val) {
    func_in_11434(val);
    const int x = 11434;
    std::cout << 11434;
    for (int i = 0; i < 11434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11434(int& val) {
    const int x = 11434;
    std::cout << 11434;
    for (int i = 0; i < 11434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11435(int& val) {
    func_in_11435(val);
    const int x = 11435;
    std::cout << 11435;
    for (int i = 0; i < 11435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11435(int& val) {
    const int x = 11435;
    std::cout << 11435;
    for (int i = 0; i < 11435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11436(int& val) {
    func_in_11436(val);
    const int x = 11436;
    std::cout << 11436;
    for (int i = 0; i < 11436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11436(int& val) {
    const int x = 11436;
    std::cout << 11436;
    for (int i = 0; i < 11436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11437(int& val) {
    func_in_11437(val);
    const int x = 11437;
    std::cout << 11437;
    for (int i = 0; i < 11437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11437(int& val) {
    const int x = 11437;
    std::cout << 11437;
    for (int i = 0; i < 11437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11438(int& val) {
    func_in_11438(val);
    const int x = 11438;
    std::cout << 11438;
    for (int i = 0; i < 11438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11438(int& val) {
    const int x = 11438;
    std::cout << 11438;
    for (int i = 0; i < 11438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11439(int& val) {
    func_in_11439(val);
    const int x = 11439;
    std::cout << 11439;
    for (int i = 0; i < 11439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11439(int& val) {
    const int x = 11439;
    std::cout << 11439;
    for (int i = 0; i < 11439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11440(int& val) {
    func_in_11440(val);
    const int x = 11440;
    std::cout << 11440;
    for (int i = 0; i < 11440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11440(int& val) {
    const int x = 11440;
    std::cout << 11440;
    for (int i = 0; i < 11440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11441(int& val) {
    func_in_11441(val);
    const int x = 11441;
    std::cout << 11441;
    for (int i = 0; i < 11441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11441(int& val) {
    const int x = 11441;
    std::cout << 11441;
    for (int i = 0; i < 11441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11442(int& val) {
    func_in_11442(val);
    const int x = 11442;
    std::cout << 11442;
    for (int i = 0; i < 11442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11442(int& val) {
    const int x = 11442;
    std::cout << 11442;
    for (int i = 0; i < 11442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11443(int& val) {
    func_in_11443(val);
    const int x = 11443;
    std::cout << 11443;
    for (int i = 0; i < 11443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11443(int& val) {
    const int x = 11443;
    std::cout << 11443;
    for (int i = 0; i < 11443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11444(int& val) {
    func_in_11444(val);
    const int x = 11444;
    std::cout << 11444;
    for (int i = 0; i < 11444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11444(int& val) {
    const int x = 11444;
    std::cout << 11444;
    for (int i = 0; i < 11444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11445(int& val) {
    func_in_11445(val);
    const int x = 11445;
    std::cout << 11445;
    for (int i = 0; i < 11445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11445(int& val) {
    const int x = 11445;
    std::cout << 11445;
    for (int i = 0; i < 11445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11446(int& val) {
    func_in_11446(val);
    const int x = 11446;
    std::cout << 11446;
    for (int i = 0; i < 11446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11446(int& val) {
    const int x = 11446;
    std::cout << 11446;
    for (int i = 0; i < 11446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11447(int& val) {
    func_in_11447(val);
    const int x = 11447;
    std::cout << 11447;
    for (int i = 0; i < 11447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11447(int& val) {
    const int x = 11447;
    std::cout << 11447;
    for (int i = 0; i < 11447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11448(int& val) {
    func_in_11448(val);
    const int x = 11448;
    std::cout << 11448;
    for (int i = 0; i < 11448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11448(int& val) {
    const int x = 11448;
    std::cout << 11448;
    for (int i = 0; i < 11448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11449(int& val) {
    func_in_11449(val);
    const int x = 11449;
    std::cout << 11449;
    for (int i = 0; i < 11449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11449(int& val) {
    const int x = 11449;
    std::cout << 11449;
    for (int i = 0; i < 11449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11450(int& val) {
    func_in_11450(val);
    const int x = 11450;
    std::cout << 11450;
    for (int i = 0; i < 11450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11450(int& val) {
    const int x = 11450;
    std::cout << 11450;
    for (int i = 0; i < 11450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11451(int& val) {
    func_in_11451(val);
    const int x = 11451;
    std::cout << 11451;
    for (int i = 0; i < 11451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11451(int& val) {
    const int x = 11451;
    std::cout << 11451;
    for (int i = 0; i < 11451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11452(int& val) {
    func_in_11452(val);
    const int x = 11452;
    std::cout << 11452;
    for (int i = 0; i < 11452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11452(int& val) {
    const int x = 11452;
    std::cout << 11452;
    for (int i = 0; i < 11452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11453(int& val) {
    func_in_11453(val);
    const int x = 11453;
    std::cout << 11453;
    for (int i = 0; i < 11453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11453(int& val) {
    const int x = 11453;
    std::cout << 11453;
    for (int i = 0; i < 11453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11454(int& val) {
    func_in_11454(val);
    const int x = 11454;
    std::cout << 11454;
    for (int i = 0; i < 11454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11454(int& val) {
    const int x = 11454;
    std::cout << 11454;
    for (int i = 0; i < 11454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11455(int& val) {
    func_in_11455(val);
    const int x = 11455;
    std::cout << 11455;
    for (int i = 0; i < 11455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11455(int& val) {
    const int x = 11455;
    std::cout << 11455;
    for (int i = 0; i < 11455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11456(int& val) {
    func_in_11456(val);
    const int x = 11456;
    std::cout << 11456;
    for (int i = 0; i < 11456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11456(int& val) {
    const int x = 11456;
    std::cout << 11456;
    for (int i = 0; i < 11456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11457(int& val) {
    func_in_11457(val);
    const int x = 11457;
    std::cout << 11457;
    for (int i = 0; i < 11457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11457(int& val) {
    const int x = 11457;
    std::cout << 11457;
    for (int i = 0; i < 11457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11458(int& val) {
    func_in_11458(val);
    const int x = 11458;
    std::cout << 11458;
    for (int i = 0; i < 11458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11458(int& val) {
    const int x = 11458;
    std::cout << 11458;
    for (int i = 0; i < 11458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11459(int& val) {
    func_in_11459(val);
    const int x = 11459;
    std::cout << 11459;
    for (int i = 0; i < 11459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11459(int& val) {
    const int x = 11459;
    std::cout << 11459;
    for (int i = 0; i < 11459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11460(int& val) {
    func_in_11460(val);
    const int x = 11460;
    std::cout << 11460;
    for (int i = 0; i < 11460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11460(int& val) {
    const int x = 11460;
    std::cout << 11460;
    for (int i = 0; i < 11460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11461(int& val) {
    func_in_11461(val);
    const int x = 11461;
    std::cout << 11461;
    for (int i = 0; i < 11461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11461(int& val) {
    const int x = 11461;
    std::cout << 11461;
    for (int i = 0; i < 11461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11462(int& val) {
    func_in_11462(val);
    const int x = 11462;
    std::cout << 11462;
    for (int i = 0; i < 11462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11462(int& val) {
    const int x = 11462;
    std::cout << 11462;
    for (int i = 0; i < 11462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11463(int& val) {
    func_in_11463(val);
    const int x = 11463;
    std::cout << 11463;
    for (int i = 0; i < 11463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11463(int& val) {
    const int x = 11463;
    std::cout << 11463;
    for (int i = 0; i < 11463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11464(int& val) {
    func_in_11464(val);
    const int x = 11464;
    std::cout << 11464;
    for (int i = 0; i < 11464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11464(int& val) {
    const int x = 11464;
    std::cout << 11464;
    for (int i = 0; i < 11464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11465(int& val) {
    func_in_11465(val);
    const int x = 11465;
    std::cout << 11465;
    for (int i = 0; i < 11465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11465(int& val) {
    const int x = 11465;
    std::cout << 11465;
    for (int i = 0; i < 11465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11466(int& val) {
    func_in_11466(val);
    const int x = 11466;
    std::cout << 11466;
    for (int i = 0; i < 11466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11466(int& val) {
    const int x = 11466;
    std::cout << 11466;
    for (int i = 0; i < 11466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11467(int& val) {
    func_in_11467(val);
    const int x = 11467;
    std::cout << 11467;
    for (int i = 0; i < 11467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11467(int& val) {
    const int x = 11467;
    std::cout << 11467;
    for (int i = 0; i < 11467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11468(int& val) {
    func_in_11468(val);
    const int x = 11468;
    std::cout << 11468;
    for (int i = 0; i < 11468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11468(int& val) {
    const int x = 11468;
    std::cout << 11468;
    for (int i = 0; i < 11468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11469(int& val) {
    func_in_11469(val);
    const int x = 11469;
    std::cout << 11469;
    for (int i = 0; i < 11469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11469(int& val) {
    const int x = 11469;
    std::cout << 11469;
    for (int i = 0; i < 11469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11470(int& val) {
    func_in_11470(val);
    const int x = 11470;
    std::cout << 11470;
    for (int i = 0; i < 11470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11470(int& val) {
    const int x = 11470;
    std::cout << 11470;
    for (int i = 0; i < 11470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11471(int& val) {
    func_in_11471(val);
    const int x = 11471;
    std::cout << 11471;
    for (int i = 0; i < 11471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11471(int& val) {
    const int x = 11471;
    std::cout << 11471;
    for (int i = 0; i < 11471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11472(int& val) {
    func_in_11472(val);
    const int x = 11472;
    std::cout << 11472;
    for (int i = 0; i < 11472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11472(int& val) {
    const int x = 11472;
    std::cout << 11472;
    for (int i = 0; i < 11472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11473(int& val) {
    func_in_11473(val);
    const int x = 11473;
    std::cout << 11473;
    for (int i = 0; i < 11473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11473(int& val) {
    const int x = 11473;
    std::cout << 11473;
    for (int i = 0; i < 11473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11474(int& val) {
    func_in_11474(val);
    const int x = 11474;
    std::cout << 11474;
    for (int i = 0; i < 11474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11474(int& val) {
    const int x = 11474;
    std::cout << 11474;
    for (int i = 0; i < 11474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11475(int& val) {
    func_in_11475(val);
    const int x = 11475;
    std::cout << 11475;
    for (int i = 0; i < 11475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11475(int& val) {
    const int x = 11475;
    std::cout << 11475;
    for (int i = 0; i < 11475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11476(int& val) {
    func_in_11476(val);
    const int x = 11476;
    std::cout << 11476;
    for (int i = 0; i < 11476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11476(int& val) {
    const int x = 11476;
    std::cout << 11476;
    for (int i = 0; i < 11476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11477(int& val) {
    func_in_11477(val);
    const int x = 11477;
    std::cout << 11477;
    for (int i = 0; i < 11477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11477(int& val) {
    const int x = 11477;
    std::cout << 11477;
    for (int i = 0; i < 11477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11478(int& val) {
    func_in_11478(val);
    const int x = 11478;
    std::cout << 11478;
    for (int i = 0; i < 11478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11478(int& val) {
    const int x = 11478;
    std::cout << 11478;
    for (int i = 0; i < 11478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11479(int& val) {
    func_in_11479(val);
    const int x = 11479;
    std::cout << 11479;
    for (int i = 0; i < 11479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11479(int& val) {
    const int x = 11479;
    std::cout << 11479;
    for (int i = 0; i < 11479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11480(int& val) {
    func_in_11480(val);
    const int x = 11480;
    std::cout << 11480;
    for (int i = 0; i < 11480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11480(int& val) {
    const int x = 11480;
    std::cout << 11480;
    for (int i = 0; i < 11480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11481(int& val) {
    func_in_11481(val);
    const int x = 11481;
    std::cout << 11481;
    for (int i = 0; i < 11481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11481(int& val) {
    const int x = 11481;
    std::cout << 11481;
    for (int i = 0; i < 11481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11482(int& val) {
    func_in_11482(val);
    const int x = 11482;
    std::cout << 11482;
    for (int i = 0; i < 11482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11482(int& val) {
    const int x = 11482;
    std::cout << 11482;
    for (int i = 0; i < 11482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11483(int& val) {
    func_in_11483(val);
    const int x = 11483;
    std::cout << 11483;
    for (int i = 0; i < 11483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11483(int& val) {
    const int x = 11483;
    std::cout << 11483;
    for (int i = 0; i < 11483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11484(int& val) {
    func_in_11484(val);
    const int x = 11484;
    std::cout << 11484;
    for (int i = 0; i < 11484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11484(int& val) {
    const int x = 11484;
    std::cout << 11484;
    for (int i = 0; i < 11484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11485(int& val) {
    func_in_11485(val);
    const int x = 11485;
    std::cout << 11485;
    for (int i = 0; i < 11485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11485(int& val) {
    const int x = 11485;
    std::cout << 11485;
    for (int i = 0; i < 11485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11486(int& val) {
    func_in_11486(val);
    const int x = 11486;
    std::cout << 11486;
    for (int i = 0; i < 11486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11486(int& val) {
    const int x = 11486;
    std::cout << 11486;
    for (int i = 0; i < 11486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11487(int& val) {
    func_in_11487(val);
    const int x = 11487;
    std::cout << 11487;
    for (int i = 0; i < 11487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11487(int& val) {
    const int x = 11487;
    std::cout << 11487;
    for (int i = 0; i < 11487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11488(int& val) {
    func_in_11488(val);
    const int x = 11488;
    std::cout << 11488;
    for (int i = 0; i < 11488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11488(int& val) {
    const int x = 11488;
    std::cout << 11488;
    for (int i = 0; i < 11488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11489(int& val) {
    func_in_11489(val);
    const int x = 11489;
    std::cout << 11489;
    for (int i = 0; i < 11489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11489(int& val) {
    const int x = 11489;
    std::cout << 11489;
    for (int i = 0; i < 11489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11490(int& val) {
    func_in_11490(val);
    const int x = 11490;
    std::cout << 11490;
    for (int i = 0; i < 11490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11490(int& val) {
    const int x = 11490;
    std::cout << 11490;
    for (int i = 0; i < 11490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11491(int& val) {
    func_in_11491(val);
    const int x = 11491;
    std::cout << 11491;
    for (int i = 0; i < 11491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11491(int& val) {
    const int x = 11491;
    std::cout << 11491;
    for (int i = 0; i < 11491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11492(int& val) {
    func_in_11492(val);
    const int x = 11492;
    std::cout << 11492;
    for (int i = 0; i < 11492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11492(int& val) {
    const int x = 11492;
    std::cout << 11492;
    for (int i = 0; i < 11492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11493(int& val) {
    func_in_11493(val);
    const int x = 11493;
    std::cout << 11493;
    for (int i = 0; i < 11493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11493(int& val) {
    const int x = 11493;
    std::cout << 11493;
    for (int i = 0; i < 11493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11494(int& val) {
    func_in_11494(val);
    const int x = 11494;
    std::cout << 11494;
    for (int i = 0; i < 11494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11494(int& val) {
    const int x = 11494;
    std::cout << 11494;
    for (int i = 0; i < 11494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11495(int& val) {
    func_in_11495(val);
    const int x = 11495;
    std::cout << 11495;
    for (int i = 0; i < 11495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11495(int& val) {
    const int x = 11495;
    std::cout << 11495;
    for (int i = 0; i < 11495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11496(int& val) {
    func_in_11496(val);
    const int x = 11496;
    std::cout << 11496;
    for (int i = 0; i < 11496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11496(int& val) {
    const int x = 11496;
    std::cout << 11496;
    for (int i = 0; i < 11496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11497(int& val) {
    func_in_11497(val);
    const int x = 11497;
    std::cout << 11497;
    for (int i = 0; i < 11497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11497(int& val) {
    const int x = 11497;
    std::cout << 11497;
    for (int i = 0; i < 11497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11498(int& val) {
    func_in_11498(val);
    const int x = 11498;
    std::cout << 11498;
    for (int i = 0; i < 11498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11498(int& val) {
    const int x = 11498;
    std::cout << 11498;
    for (int i = 0; i < 11498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11499(int& val) {
    func_in_11499(val);
    const int x = 11499;
    std::cout << 11499;
    for (int i = 0; i < 11499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11499(int& val) {
    const int x = 11499;
    std::cout << 11499;
    for (int i = 0; i < 11499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11500(int& val) {
    func_in_11500(val);
    const int x = 11500;
    std::cout << 11500;
    for (int i = 0; i < 11500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11500(int& val) {
    const int x = 11500;
    std::cout << 11500;
    for (int i = 0; i < 11500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11501(int& val) {
    func_in_11501(val);
    const int x = 11501;
    std::cout << 11501;
    for (int i = 0; i < 11501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11501(int& val) {
    const int x = 11501;
    std::cout << 11501;
    for (int i = 0; i < 11501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11502(int& val) {
    func_in_11502(val);
    const int x = 11502;
    std::cout << 11502;
    for (int i = 0; i < 11502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11502(int& val) {
    const int x = 11502;
    std::cout << 11502;
    for (int i = 0; i < 11502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11503(int& val) {
    func_in_11503(val);
    const int x = 11503;
    std::cout << 11503;
    for (int i = 0; i < 11503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11503(int& val) {
    const int x = 11503;
    std::cout << 11503;
    for (int i = 0; i < 11503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11504(int& val) {
    func_in_11504(val);
    const int x = 11504;
    std::cout << 11504;
    for (int i = 0; i < 11504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11504(int& val) {
    const int x = 11504;
    std::cout << 11504;
    for (int i = 0; i < 11504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11505(int& val) {
    func_in_11505(val);
    const int x = 11505;
    std::cout << 11505;
    for (int i = 0; i < 11505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11505(int& val) {
    const int x = 11505;
    std::cout << 11505;
    for (int i = 0; i < 11505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11506(int& val) {
    func_in_11506(val);
    const int x = 11506;
    std::cout << 11506;
    for (int i = 0; i < 11506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11506(int& val) {
    const int x = 11506;
    std::cout << 11506;
    for (int i = 0; i < 11506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11507(int& val) {
    func_in_11507(val);
    const int x = 11507;
    std::cout << 11507;
    for (int i = 0; i < 11507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11507(int& val) {
    const int x = 11507;
    std::cout << 11507;
    for (int i = 0; i < 11507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11508(int& val) {
    func_in_11508(val);
    const int x = 11508;
    std::cout << 11508;
    for (int i = 0; i < 11508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11508(int& val) {
    const int x = 11508;
    std::cout << 11508;
    for (int i = 0; i < 11508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11509(int& val) {
    func_in_11509(val);
    const int x = 11509;
    std::cout << 11509;
    for (int i = 0; i < 11509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11509(int& val) {
    const int x = 11509;
    std::cout << 11509;
    for (int i = 0; i < 11509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11510(int& val) {
    func_in_11510(val);
    const int x = 11510;
    std::cout << 11510;
    for (int i = 0; i < 11510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11510(int& val) {
    const int x = 11510;
    std::cout << 11510;
    for (int i = 0; i < 11510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11511(int& val) {
    func_in_11511(val);
    const int x = 11511;
    std::cout << 11511;
    for (int i = 0; i < 11511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11511(int& val) {
    const int x = 11511;
    std::cout << 11511;
    for (int i = 0; i < 11511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11512(int& val) {
    func_in_11512(val);
    const int x = 11512;
    std::cout << 11512;
    for (int i = 0; i < 11512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11512(int& val) {
    const int x = 11512;
    std::cout << 11512;
    for (int i = 0; i < 11512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11513(int& val) {
    func_in_11513(val);
    const int x = 11513;
    std::cout << 11513;
    for (int i = 0; i < 11513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11513(int& val) {
    const int x = 11513;
    std::cout << 11513;
    for (int i = 0; i < 11513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11514(int& val) {
    func_in_11514(val);
    const int x = 11514;
    std::cout << 11514;
    for (int i = 0; i < 11514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11514(int& val) {
    const int x = 11514;
    std::cout << 11514;
    for (int i = 0; i < 11514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11515(int& val) {
    func_in_11515(val);
    const int x = 11515;
    std::cout << 11515;
    for (int i = 0; i < 11515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11515(int& val) {
    const int x = 11515;
    std::cout << 11515;
    for (int i = 0; i < 11515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11516(int& val) {
    func_in_11516(val);
    const int x = 11516;
    std::cout << 11516;
    for (int i = 0; i < 11516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11516(int& val) {
    const int x = 11516;
    std::cout << 11516;
    for (int i = 0; i < 11516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11517(int& val) {
    func_in_11517(val);
    const int x = 11517;
    std::cout << 11517;
    for (int i = 0; i < 11517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11517(int& val) {
    const int x = 11517;
    std::cout << 11517;
    for (int i = 0; i < 11517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11518(int& val) {
    func_in_11518(val);
    const int x = 11518;
    std::cout << 11518;
    for (int i = 0; i < 11518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11518(int& val) {
    const int x = 11518;
    std::cout << 11518;
    for (int i = 0; i < 11518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11519(int& val) {
    func_in_11519(val);
    const int x = 11519;
    std::cout << 11519;
    for (int i = 0; i < 11519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11519(int& val) {
    const int x = 11519;
    std::cout << 11519;
    for (int i = 0; i < 11519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11520(int& val) {
    func_in_11520(val);
    const int x = 11520;
    std::cout << 11520;
    for (int i = 0; i < 11520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11520(int& val) {
    const int x = 11520;
    std::cout << 11520;
    for (int i = 0; i < 11520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11521(int& val) {
    func_in_11521(val);
    const int x = 11521;
    std::cout << 11521;
    for (int i = 0; i < 11521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11521(int& val) {
    const int x = 11521;
    std::cout << 11521;
    for (int i = 0; i < 11521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11522(int& val) {
    func_in_11522(val);
    const int x = 11522;
    std::cout << 11522;
    for (int i = 0; i < 11522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11522(int& val) {
    const int x = 11522;
    std::cout << 11522;
    for (int i = 0; i < 11522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11523(int& val) {
    func_in_11523(val);
    const int x = 11523;
    std::cout << 11523;
    for (int i = 0; i < 11523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11523(int& val) {
    const int x = 11523;
    std::cout << 11523;
    for (int i = 0; i < 11523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11524(int& val) {
    func_in_11524(val);
    const int x = 11524;
    std::cout << 11524;
    for (int i = 0; i < 11524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11524(int& val) {
    const int x = 11524;
    std::cout << 11524;
    for (int i = 0; i < 11524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11525(int& val) {
    func_in_11525(val);
    const int x = 11525;
    std::cout << 11525;
    for (int i = 0; i < 11525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11525(int& val) {
    const int x = 11525;
    std::cout << 11525;
    for (int i = 0; i < 11525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11526(int& val) {
    func_in_11526(val);
    const int x = 11526;
    std::cout << 11526;
    for (int i = 0; i < 11526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11526(int& val) {
    const int x = 11526;
    std::cout << 11526;
    for (int i = 0; i < 11526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11527(int& val) {
    func_in_11527(val);
    const int x = 11527;
    std::cout << 11527;
    for (int i = 0; i < 11527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11527(int& val) {
    const int x = 11527;
    std::cout << 11527;
    for (int i = 0; i < 11527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11528(int& val) {
    func_in_11528(val);
    const int x = 11528;
    std::cout << 11528;
    for (int i = 0; i < 11528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11528(int& val) {
    const int x = 11528;
    std::cout << 11528;
    for (int i = 0; i < 11528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11529(int& val) {
    func_in_11529(val);
    const int x = 11529;
    std::cout << 11529;
    for (int i = 0; i < 11529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11529(int& val) {
    const int x = 11529;
    std::cout << 11529;
    for (int i = 0; i < 11529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11530(int& val) {
    func_in_11530(val);
    const int x = 11530;
    std::cout << 11530;
    for (int i = 0; i < 11530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11530(int& val) {
    const int x = 11530;
    std::cout << 11530;
    for (int i = 0; i < 11530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11531(int& val) {
    func_in_11531(val);
    const int x = 11531;
    std::cout << 11531;
    for (int i = 0; i < 11531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11531(int& val) {
    const int x = 11531;
    std::cout << 11531;
    for (int i = 0; i < 11531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11532(int& val) {
    func_in_11532(val);
    const int x = 11532;
    std::cout << 11532;
    for (int i = 0; i < 11532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11532(int& val) {
    const int x = 11532;
    std::cout << 11532;
    for (int i = 0; i < 11532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11533(int& val) {
    func_in_11533(val);
    const int x = 11533;
    std::cout << 11533;
    for (int i = 0; i < 11533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11533(int& val) {
    const int x = 11533;
    std::cout << 11533;
    for (int i = 0; i < 11533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11534(int& val) {
    func_in_11534(val);
    const int x = 11534;
    std::cout << 11534;
    for (int i = 0; i < 11534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11534(int& val) {
    const int x = 11534;
    std::cout << 11534;
    for (int i = 0; i < 11534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11535(int& val) {
    func_in_11535(val);
    const int x = 11535;
    std::cout << 11535;
    for (int i = 0; i < 11535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11535(int& val) {
    const int x = 11535;
    std::cout << 11535;
    for (int i = 0; i < 11535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11536(int& val) {
    func_in_11536(val);
    const int x = 11536;
    std::cout << 11536;
    for (int i = 0; i < 11536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11536(int& val) {
    const int x = 11536;
    std::cout << 11536;
    for (int i = 0; i < 11536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11537(int& val) {
    func_in_11537(val);
    const int x = 11537;
    std::cout << 11537;
    for (int i = 0; i < 11537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11537(int& val) {
    const int x = 11537;
    std::cout << 11537;
    for (int i = 0; i < 11537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11538(int& val) {
    func_in_11538(val);
    const int x = 11538;
    std::cout << 11538;
    for (int i = 0; i < 11538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11538(int& val) {
    const int x = 11538;
    std::cout << 11538;
    for (int i = 0; i < 11538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11539(int& val) {
    func_in_11539(val);
    const int x = 11539;
    std::cout << 11539;
    for (int i = 0; i < 11539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11539(int& val) {
    const int x = 11539;
    std::cout << 11539;
    for (int i = 0; i < 11539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11540(int& val) {
    func_in_11540(val);
    const int x = 11540;
    std::cout << 11540;
    for (int i = 0; i < 11540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11540(int& val) {
    const int x = 11540;
    std::cout << 11540;
    for (int i = 0; i < 11540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11541(int& val) {
    func_in_11541(val);
    const int x = 11541;
    std::cout << 11541;
    for (int i = 0; i < 11541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11541(int& val) {
    const int x = 11541;
    std::cout << 11541;
    for (int i = 0; i < 11541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11542(int& val) {
    func_in_11542(val);
    const int x = 11542;
    std::cout << 11542;
    for (int i = 0; i < 11542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11542(int& val) {
    const int x = 11542;
    std::cout << 11542;
    for (int i = 0; i < 11542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11543(int& val) {
    func_in_11543(val);
    const int x = 11543;
    std::cout << 11543;
    for (int i = 0; i < 11543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11543(int& val) {
    const int x = 11543;
    std::cout << 11543;
    for (int i = 0; i < 11543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11544(int& val) {
    func_in_11544(val);
    const int x = 11544;
    std::cout << 11544;
    for (int i = 0; i < 11544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11544(int& val) {
    const int x = 11544;
    std::cout << 11544;
    for (int i = 0; i < 11544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11545(int& val) {
    func_in_11545(val);
    const int x = 11545;
    std::cout << 11545;
    for (int i = 0; i < 11545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11545(int& val) {
    const int x = 11545;
    std::cout << 11545;
    for (int i = 0; i < 11545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11546(int& val) {
    func_in_11546(val);
    const int x = 11546;
    std::cout << 11546;
    for (int i = 0; i < 11546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11546(int& val) {
    const int x = 11546;
    std::cout << 11546;
    for (int i = 0; i < 11546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11547(int& val) {
    func_in_11547(val);
    const int x = 11547;
    std::cout << 11547;
    for (int i = 0; i < 11547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11547(int& val) {
    const int x = 11547;
    std::cout << 11547;
    for (int i = 0; i < 11547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11548(int& val) {
    func_in_11548(val);
    const int x = 11548;
    std::cout << 11548;
    for (int i = 0; i < 11548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11548(int& val) {
    const int x = 11548;
    std::cout << 11548;
    for (int i = 0; i < 11548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11549(int& val) {
    func_in_11549(val);
    const int x = 11549;
    std::cout << 11549;
    for (int i = 0; i < 11549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11549(int& val) {
    const int x = 11549;
    std::cout << 11549;
    for (int i = 0; i < 11549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11550(int& val) {
    func_in_11550(val);
    const int x = 11550;
    std::cout << 11550;
    for (int i = 0; i < 11550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11550(int& val) {
    const int x = 11550;
    std::cout << 11550;
    for (int i = 0; i < 11550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11551(int& val) {
    func_in_11551(val);
    const int x = 11551;
    std::cout << 11551;
    for (int i = 0; i < 11551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11551(int& val) {
    const int x = 11551;
    std::cout << 11551;
    for (int i = 0; i < 11551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11552(int& val) {
    func_in_11552(val);
    const int x = 11552;
    std::cout << 11552;
    for (int i = 0; i < 11552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11552(int& val) {
    const int x = 11552;
    std::cout << 11552;
    for (int i = 0; i < 11552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11553(int& val) {
    func_in_11553(val);
    const int x = 11553;
    std::cout << 11553;
    for (int i = 0; i < 11553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11553(int& val) {
    const int x = 11553;
    std::cout << 11553;
    for (int i = 0; i < 11553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11554(int& val) {
    func_in_11554(val);
    const int x = 11554;
    std::cout << 11554;
    for (int i = 0; i < 11554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11554(int& val) {
    const int x = 11554;
    std::cout << 11554;
    for (int i = 0; i < 11554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11555(int& val) {
    func_in_11555(val);
    const int x = 11555;
    std::cout << 11555;
    for (int i = 0; i < 11555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11555(int& val) {
    const int x = 11555;
    std::cout << 11555;
    for (int i = 0; i < 11555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11556(int& val) {
    func_in_11556(val);
    const int x = 11556;
    std::cout << 11556;
    for (int i = 0; i < 11556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11556(int& val) {
    const int x = 11556;
    std::cout << 11556;
    for (int i = 0; i < 11556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11557(int& val) {
    func_in_11557(val);
    const int x = 11557;
    std::cout << 11557;
    for (int i = 0; i < 11557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11557(int& val) {
    const int x = 11557;
    std::cout << 11557;
    for (int i = 0; i < 11557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11558(int& val) {
    func_in_11558(val);
    const int x = 11558;
    std::cout << 11558;
    for (int i = 0; i < 11558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11558(int& val) {
    const int x = 11558;
    std::cout << 11558;
    for (int i = 0; i < 11558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11559(int& val) {
    func_in_11559(val);
    const int x = 11559;
    std::cout << 11559;
    for (int i = 0; i < 11559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11559(int& val) {
    const int x = 11559;
    std::cout << 11559;
    for (int i = 0; i < 11559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11560(int& val) {
    func_in_11560(val);
    const int x = 11560;
    std::cout << 11560;
    for (int i = 0; i < 11560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11560(int& val) {
    const int x = 11560;
    std::cout << 11560;
    for (int i = 0; i < 11560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11561(int& val) {
    func_in_11561(val);
    const int x = 11561;
    std::cout << 11561;
    for (int i = 0; i < 11561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11561(int& val) {
    const int x = 11561;
    std::cout << 11561;
    for (int i = 0; i < 11561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11562(int& val) {
    func_in_11562(val);
    const int x = 11562;
    std::cout << 11562;
    for (int i = 0; i < 11562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11562(int& val) {
    const int x = 11562;
    std::cout << 11562;
    for (int i = 0; i < 11562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11563(int& val) {
    func_in_11563(val);
    const int x = 11563;
    std::cout << 11563;
    for (int i = 0; i < 11563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11563(int& val) {
    const int x = 11563;
    std::cout << 11563;
    for (int i = 0; i < 11563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11564(int& val) {
    func_in_11564(val);
    const int x = 11564;
    std::cout << 11564;
    for (int i = 0; i < 11564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11564(int& val) {
    const int x = 11564;
    std::cout << 11564;
    for (int i = 0; i < 11564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11565(int& val) {
    func_in_11565(val);
    const int x = 11565;
    std::cout << 11565;
    for (int i = 0; i < 11565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11565(int& val) {
    const int x = 11565;
    std::cout << 11565;
    for (int i = 0; i < 11565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11566(int& val) {
    func_in_11566(val);
    const int x = 11566;
    std::cout << 11566;
    for (int i = 0; i < 11566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11566(int& val) {
    const int x = 11566;
    std::cout << 11566;
    for (int i = 0; i < 11566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11567(int& val) {
    func_in_11567(val);
    const int x = 11567;
    std::cout << 11567;
    for (int i = 0; i < 11567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11567(int& val) {
    const int x = 11567;
    std::cout << 11567;
    for (int i = 0; i < 11567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11568(int& val) {
    func_in_11568(val);
    const int x = 11568;
    std::cout << 11568;
    for (int i = 0; i < 11568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11568(int& val) {
    const int x = 11568;
    std::cout << 11568;
    for (int i = 0; i < 11568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11569(int& val) {
    func_in_11569(val);
    const int x = 11569;
    std::cout << 11569;
    for (int i = 0; i < 11569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11569(int& val) {
    const int x = 11569;
    std::cout << 11569;
    for (int i = 0; i < 11569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11570(int& val) {
    func_in_11570(val);
    const int x = 11570;
    std::cout << 11570;
    for (int i = 0; i < 11570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11570(int& val) {
    const int x = 11570;
    std::cout << 11570;
    for (int i = 0; i < 11570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11571(int& val) {
    func_in_11571(val);
    const int x = 11571;
    std::cout << 11571;
    for (int i = 0; i < 11571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11571(int& val) {
    const int x = 11571;
    std::cout << 11571;
    for (int i = 0; i < 11571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11572(int& val) {
    func_in_11572(val);
    const int x = 11572;
    std::cout << 11572;
    for (int i = 0; i < 11572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11572(int& val) {
    const int x = 11572;
    std::cout << 11572;
    for (int i = 0; i < 11572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11573(int& val) {
    func_in_11573(val);
    const int x = 11573;
    std::cout << 11573;
    for (int i = 0; i < 11573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11573(int& val) {
    const int x = 11573;
    std::cout << 11573;
    for (int i = 0; i < 11573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11574(int& val) {
    func_in_11574(val);
    const int x = 11574;
    std::cout << 11574;
    for (int i = 0; i < 11574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11574(int& val) {
    const int x = 11574;
    std::cout << 11574;
    for (int i = 0; i < 11574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11575(int& val) {
    func_in_11575(val);
    const int x = 11575;
    std::cout << 11575;
    for (int i = 0; i < 11575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11575(int& val) {
    const int x = 11575;
    std::cout << 11575;
    for (int i = 0; i < 11575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11576(int& val) {
    func_in_11576(val);
    const int x = 11576;
    std::cout << 11576;
    for (int i = 0; i < 11576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11576(int& val) {
    const int x = 11576;
    std::cout << 11576;
    for (int i = 0; i < 11576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11577(int& val) {
    func_in_11577(val);
    const int x = 11577;
    std::cout << 11577;
    for (int i = 0; i < 11577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11577(int& val) {
    const int x = 11577;
    std::cout << 11577;
    for (int i = 0; i < 11577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11578(int& val) {
    func_in_11578(val);
    const int x = 11578;
    std::cout << 11578;
    for (int i = 0; i < 11578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11578(int& val) {
    const int x = 11578;
    std::cout << 11578;
    for (int i = 0; i < 11578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11579(int& val) {
    func_in_11579(val);
    const int x = 11579;
    std::cout << 11579;
    for (int i = 0; i < 11579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11579(int& val) {
    const int x = 11579;
    std::cout << 11579;
    for (int i = 0; i < 11579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11580(int& val) {
    func_in_11580(val);
    const int x = 11580;
    std::cout << 11580;
    for (int i = 0; i < 11580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11580(int& val) {
    const int x = 11580;
    std::cout << 11580;
    for (int i = 0; i < 11580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11581(int& val) {
    func_in_11581(val);
    const int x = 11581;
    std::cout << 11581;
    for (int i = 0; i < 11581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11581(int& val) {
    const int x = 11581;
    std::cout << 11581;
    for (int i = 0; i < 11581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11582(int& val) {
    func_in_11582(val);
    const int x = 11582;
    std::cout << 11582;
    for (int i = 0; i < 11582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11582(int& val) {
    const int x = 11582;
    std::cout << 11582;
    for (int i = 0; i < 11582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11583(int& val) {
    func_in_11583(val);
    const int x = 11583;
    std::cout << 11583;
    for (int i = 0; i < 11583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11583(int& val) {
    const int x = 11583;
    std::cout << 11583;
    for (int i = 0; i < 11583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11584(int& val) {
    func_in_11584(val);
    const int x = 11584;
    std::cout << 11584;
    for (int i = 0; i < 11584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11584(int& val) {
    const int x = 11584;
    std::cout << 11584;
    for (int i = 0; i < 11584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11585(int& val) {
    func_in_11585(val);
    const int x = 11585;
    std::cout << 11585;
    for (int i = 0; i < 11585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11585(int& val) {
    const int x = 11585;
    std::cout << 11585;
    for (int i = 0; i < 11585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11586(int& val) {
    func_in_11586(val);
    const int x = 11586;
    std::cout << 11586;
    for (int i = 0; i < 11586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11586(int& val) {
    const int x = 11586;
    std::cout << 11586;
    for (int i = 0; i < 11586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11587(int& val) {
    func_in_11587(val);
    const int x = 11587;
    std::cout << 11587;
    for (int i = 0; i < 11587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11587(int& val) {
    const int x = 11587;
    std::cout << 11587;
    for (int i = 0; i < 11587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11588(int& val) {
    func_in_11588(val);
    const int x = 11588;
    std::cout << 11588;
    for (int i = 0; i < 11588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11588(int& val) {
    const int x = 11588;
    std::cout << 11588;
    for (int i = 0; i < 11588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11589(int& val) {
    func_in_11589(val);
    const int x = 11589;
    std::cout << 11589;
    for (int i = 0; i < 11589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11589(int& val) {
    const int x = 11589;
    std::cout << 11589;
    for (int i = 0; i < 11589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11590(int& val) {
    func_in_11590(val);
    const int x = 11590;
    std::cout << 11590;
    for (int i = 0; i < 11590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11590(int& val) {
    const int x = 11590;
    std::cout << 11590;
    for (int i = 0; i < 11590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11591(int& val) {
    func_in_11591(val);
    const int x = 11591;
    std::cout << 11591;
    for (int i = 0; i < 11591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11591(int& val) {
    const int x = 11591;
    std::cout << 11591;
    for (int i = 0; i < 11591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11592(int& val) {
    func_in_11592(val);
    const int x = 11592;
    std::cout << 11592;
    for (int i = 0; i < 11592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11592(int& val) {
    const int x = 11592;
    std::cout << 11592;
    for (int i = 0; i < 11592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11593(int& val) {
    func_in_11593(val);
    const int x = 11593;
    std::cout << 11593;
    for (int i = 0; i < 11593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11593(int& val) {
    const int x = 11593;
    std::cout << 11593;
    for (int i = 0; i < 11593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11594(int& val) {
    func_in_11594(val);
    const int x = 11594;
    std::cout << 11594;
    for (int i = 0; i < 11594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11594(int& val) {
    const int x = 11594;
    std::cout << 11594;
    for (int i = 0; i < 11594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11595(int& val) {
    func_in_11595(val);
    const int x = 11595;
    std::cout << 11595;
    for (int i = 0; i < 11595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11595(int& val) {
    const int x = 11595;
    std::cout << 11595;
    for (int i = 0; i < 11595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11596(int& val) {
    func_in_11596(val);
    const int x = 11596;
    std::cout << 11596;
    for (int i = 0; i < 11596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11596(int& val) {
    const int x = 11596;
    std::cout << 11596;
    for (int i = 0; i < 11596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11597(int& val) {
    func_in_11597(val);
    const int x = 11597;
    std::cout << 11597;
    for (int i = 0; i < 11597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11597(int& val) {
    const int x = 11597;
    std::cout << 11597;
    for (int i = 0; i < 11597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11598(int& val) {
    func_in_11598(val);
    const int x = 11598;
    std::cout << 11598;
    for (int i = 0; i < 11598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11598(int& val) {
    const int x = 11598;
    std::cout << 11598;
    for (int i = 0; i < 11598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11599(int& val) {
    func_in_11599(val);
    const int x = 11599;
    std::cout << 11599;
    for (int i = 0; i < 11599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11599(int& val) {
    const int x = 11599;
    std::cout << 11599;
    for (int i = 0; i < 11599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11600(int& val) {
    func_in_11600(val);
    const int x = 11600;
    std::cout << 11600;
    for (int i = 0; i < 11600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11600(int& val) {
    const int x = 11600;
    std::cout << 11600;
    for (int i = 0; i < 11600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11601(int& val) {
    func_in_11601(val);
    const int x = 11601;
    std::cout << 11601;
    for (int i = 0; i < 11601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11601(int& val) {
    const int x = 11601;
    std::cout << 11601;
    for (int i = 0; i < 11601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11602(int& val) {
    func_in_11602(val);
    const int x = 11602;
    std::cout << 11602;
    for (int i = 0; i < 11602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11602(int& val) {
    const int x = 11602;
    std::cout << 11602;
    for (int i = 0; i < 11602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11603(int& val) {
    func_in_11603(val);
    const int x = 11603;
    std::cout << 11603;
    for (int i = 0; i < 11603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11603(int& val) {
    const int x = 11603;
    std::cout << 11603;
    for (int i = 0; i < 11603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11604(int& val) {
    func_in_11604(val);
    const int x = 11604;
    std::cout << 11604;
    for (int i = 0; i < 11604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11604(int& val) {
    const int x = 11604;
    std::cout << 11604;
    for (int i = 0; i < 11604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11605(int& val) {
    func_in_11605(val);
    const int x = 11605;
    std::cout << 11605;
    for (int i = 0; i < 11605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11605(int& val) {
    const int x = 11605;
    std::cout << 11605;
    for (int i = 0; i < 11605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11606(int& val) {
    func_in_11606(val);
    const int x = 11606;
    std::cout << 11606;
    for (int i = 0; i < 11606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11606(int& val) {
    const int x = 11606;
    std::cout << 11606;
    for (int i = 0; i < 11606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11607(int& val) {
    func_in_11607(val);
    const int x = 11607;
    std::cout << 11607;
    for (int i = 0; i < 11607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11607(int& val) {
    const int x = 11607;
    std::cout << 11607;
    for (int i = 0; i < 11607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11608(int& val) {
    func_in_11608(val);
    const int x = 11608;
    std::cout << 11608;
    for (int i = 0; i < 11608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11608(int& val) {
    const int x = 11608;
    std::cout << 11608;
    for (int i = 0; i < 11608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11609(int& val) {
    func_in_11609(val);
    const int x = 11609;
    std::cout << 11609;
    for (int i = 0; i < 11609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11609(int& val) {
    const int x = 11609;
    std::cout << 11609;
    for (int i = 0; i < 11609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11610(int& val) {
    func_in_11610(val);
    const int x = 11610;
    std::cout << 11610;
    for (int i = 0; i < 11610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11610(int& val) {
    const int x = 11610;
    std::cout << 11610;
    for (int i = 0; i < 11610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11611(int& val) {
    func_in_11611(val);
    const int x = 11611;
    std::cout << 11611;
    for (int i = 0; i < 11611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11611(int& val) {
    const int x = 11611;
    std::cout << 11611;
    for (int i = 0; i < 11611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11612(int& val) {
    func_in_11612(val);
    const int x = 11612;
    std::cout << 11612;
    for (int i = 0; i < 11612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11612(int& val) {
    const int x = 11612;
    std::cout << 11612;
    for (int i = 0; i < 11612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11613(int& val) {
    func_in_11613(val);
    const int x = 11613;
    std::cout << 11613;
    for (int i = 0; i < 11613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11613(int& val) {
    const int x = 11613;
    std::cout << 11613;
    for (int i = 0; i < 11613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11614(int& val) {
    func_in_11614(val);
    const int x = 11614;
    std::cout << 11614;
    for (int i = 0; i < 11614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11614(int& val) {
    const int x = 11614;
    std::cout << 11614;
    for (int i = 0; i < 11614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11615(int& val) {
    func_in_11615(val);
    const int x = 11615;
    std::cout << 11615;
    for (int i = 0; i < 11615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11615(int& val) {
    const int x = 11615;
    std::cout << 11615;
    for (int i = 0; i < 11615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11616(int& val) {
    func_in_11616(val);
    const int x = 11616;
    std::cout << 11616;
    for (int i = 0; i < 11616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11616(int& val) {
    const int x = 11616;
    std::cout << 11616;
    for (int i = 0; i < 11616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11617(int& val) {
    func_in_11617(val);
    const int x = 11617;
    std::cout << 11617;
    for (int i = 0; i < 11617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11617(int& val) {
    const int x = 11617;
    std::cout << 11617;
    for (int i = 0; i < 11617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11618(int& val) {
    func_in_11618(val);
    const int x = 11618;
    std::cout << 11618;
    for (int i = 0; i < 11618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11618(int& val) {
    const int x = 11618;
    std::cout << 11618;
    for (int i = 0; i < 11618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11619(int& val) {
    func_in_11619(val);
    const int x = 11619;
    std::cout << 11619;
    for (int i = 0; i < 11619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11619(int& val) {
    const int x = 11619;
    std::cout << 11619;
    for (int i = 0; i < 11619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11620(int& val) {
    func_in_11620(val);
    const int x = 11620;
    std::cout << 11620;
    for (int i = 0; i < 11620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11620(int& val) {
    const int x = 11620;
    std::cout << 11620;
    for (int i = 0; i < 11620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11621(int& val) {
    func_in_11621(val);
    const int x = 11621;
    std::cout << 11621;
    for (int i = 0; i < 11621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11621(int& val) {
    const int x = 11621;
    std::cout << 11621;
    for (int i = 0; i < 11621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11622(int& val) {
    func_in_11622(val);
    const int x = 11622;
    std::cout << 11622;
    for (int i = 0; i < 11622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11622(int& val) {
    const int x = 11622;
    std::cout << 11622;
    for (int i = 0; i < 11622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11623(int& val) {
    func_in_11623(val);
    const int x = 11623;
    std::cout << 11623;
    for (int i = 0; i < 11623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11623(int& val) {
    const int x = 11623;
    std::cout << 11623;
    for (int i = 0; i < 11623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11624(int& val) {
    func_in_11624(val);
    const int x = 11624;
    std::cout << 11624;
    for (int i = 0; i < 11624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11624(int& val) {
    const int x = 11624;
    std::cout << 11624;
    for (int i = 0; i < 11624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11625(int& val) {
    func_in_11625(val);
    const int x = 11625;
    std::cout << 11625;
    for (int i = 0; i < 11625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11625(int& val) {
    const int x = 11625;
    std::cout << 11625;
    for (int i = 0; i < 11625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11626(int& val) {
    func_in_11626(val);
    const int x = 11626;
    std::cout << 11626;
    for (int i = 0; i < 11626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11626(int& val) {
    const int x = 11626;
    std::cout << 11626;
    for (int i = 0; i < 11626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11627(int& val) {
    func_in_11627(val);
    const int x = 11627;
    std::cout << 11627;
    for (int i = 0; i < 11627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11627(int& val) {
    const int x = 11627;
    std::cout << 11627;
    for (int i = 0; i < 11627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11628(int& val) {
    func_in_11628(val);
    const int x = 11628;
    std::cout << 11628;
    for (int i = 0; i < 11628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11628(int& val) {
    const int x = 11628;
    std::cout << 11628;
    for (int i = 0; i < 11628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11629(int& val) {
    func_in_11629(val);
    const int x = 11629;
    std::cout << 11629;
    for (int i = 0; i < 11629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11629(int& val) {
    const int x = 11629;
    std::cout << 11629;
    for (int i = 0; i < 11629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11630(int& val) {
    func_in_11630(val);
    const int x = 11630;
    std::cout << 11630;
    for (int i = 0; i < 11630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11630(int& val) {
    const int x = 11630;
    std::cout << 11630;
    for (int i = 0; i < 11630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11631(int& val) {
    func_in_11631(val);
    const int x = 11631;
    std::cout << 11631;
    for (int i = 0; i < 11631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11631(int& val) {
    const int x = 11631;
    std::cout << 11631;
    for (int i = 0; i < 11631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11632(int& val) {
    func_in_11632(val);
    const int x = 11632;
    std::cout << 11632;
    for (int i = 0; i < 11632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11632(int& val) {
    const int x = 11632;
    std::cout << 11632;
    for (int i = 0; i < 11632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11633(int& val) {
    func_in_11633(val);
    const int x = 11633;
    std::cout << 11633;
    for (int i = 0; i < 11633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11633(int& val) {
    const int x = 11633;
    std::cout << 11633;
    for (int i = 0; i < 11633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11634(int& val) {
    func_in_11634(val);
    const int x = 11634;
    std::cout << 11634;
    for (int i = 0; i < 11634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11634(int& val) {
    const int x = 11634;
    std::cout << 11634;
    for (int i = 0; i < 11634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11635(int& val) {
    func_in_11635(val);
    const int x = 11635;
    std::cout << 11635;
    for (int i = 0; i < 11635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11635(int& val) {
    const int x = 11635;
    std::cout << 11635;
    for (int i = 0; i < 11635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11636(int& val) {
    func_in_11636(val);
    const int x = 11636;
    std::cout << 11636;
    for (int i = 0; i < 11636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11636(int& val) {
    const int x = 11636;
    std::cout << 11636;
    for (int i = 0; i < 11636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11637(int& val) {
    func_in_11637(val);
    const int x = 11637;
    std::cout << 11637;
    for (int i = 0; i < 11637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11637(int& val) {
    const int x = 11637;
    std::cout << 11637;
    for (int i = 0; i < 11637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11638(int& val) {
    func_in_11638(val);
    const int x = 11638;
    std::cout << 11638;
    for (int i = 0; i < 11638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11638(int& val) {
    const int x = 11638;
    std::cout << 11638;
    for (int i = 0; i < 11638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11639(int& val) {
    func_in_11639(val);
    const int x = 11639;
    std::cout << 11639;
    for (int i = 0; i < 11639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11639(int& val) {
    const int x = 11639;
    std::cout << 11639;
    for (int i = 0; i < 11639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11640(int& val) {
    func_in_11640(val);
    const int x = 11640;
    std::cout << 11640;
    for (int i = 0; i < 11640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11640(int& val) {
    const int x = 11640;
    std::cout << 11640;
    for (int i = 0; i < 11640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11641(int& val) {
    func_in_11641(val);
    const int x = 11641;
    std::cout << 11641;
    for (int i = 0; i < 11641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11641(int& val) {
    const int x = 11641;
    std::cout << 11641;
    for (int i = 0; i < 11641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11642(int& val) {
    func_in_11642(val);
    const int x = 11642;
    std::cout << 11642;
    for (int i = 0; i < 11642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11642(int& val) {
    const int x = 11642;
    std::cout << 11642;
    for (int i = 0; i < 11642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11643(int& val) {
    func_in_11643(val);
    const int x = 11643;
    std::cout << 11643;
    for (int i = 0; i < 11643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11643(int& val) {
    const int x = 11643;
    std::cout << 11643;
    for (int i = 0; i < 11643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11644(int& val) {
    func_in_11644(val);
    const int x = 11644;
    std::cout << 11644;
    for (int i = 0; i < 11644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11644(int& val) {
    const int x = 11644;
    std::cout << 11644;
    for (int i = 0; i < 11644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11645(int& val) {
    func_in_11645(val);
    const int x = 11645;
    std::cout << 11645;
    for (int i = 0; i < 11645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11645(int& val) {
    const int x = 11645;
    std::cout << 11645;
    for (int i = 0; i < 11645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11646(int& val) {
    func_in_11646(val);
    const int x = 11646;
    std::cout << 11646;
    for (int i = 0; i < 11646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11646(int& val) {
    const int x = 11646;
    std::cout << 11646;
    for (int i = 0; i < 11646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11647(int& val) {
    func_in_11647(val);
    const int x = 11647;
    std::cout << 11647;
    for (int i = 0; i < 11647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11647(int& val) {
    const int x = 11647;
    std::cout << 11647;
    for (int i = 0; i < 11647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11648(int& val) {
    func_in_11648(val);
    const int x = 11648;
    std::cout << 11648;
    for (int i = 0; i < 11648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11648(int& val) {
    const int x = 11648;
    std::cout << 11648;
    for (int i = 0; i < 11648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11649(int& val) {
    func_in_11649(val);
    const int x = 11649;
    std::cout << 11649;
    for (int i = 0; i < 11649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11649(int& val) {
    const int x = 11649;
    std::cout << 11649;
    for (int i = 0; i < 11649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11650(int& val) {
    func_in_11650(val);
    const int x = 11650;
    std::cout << 11650;
    for (int i = 0; i < 11650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11650(int& val) {
    const int x = 11650;
    std::cout << 11650;
    for (int i = 0; i < 11650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11651(int& val) {
    func_in_11651(val);
    const int x = 11651;
    std::cout << 11651;
    for (int i = 0; i < 11651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11651(int& val) {
    const int x = 11651;
    std::cout << 11651;
    for (int i = 0; i < 11651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11652(int& val) {
    func_in_11652(val);
    const int x = 11652;
    std::cout << 11652;
    for (int i = 0; i < 11652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11652(int& val) {
    const int x = 11652;
    std::cout << 11652;
    for (int i = 0; i < 11652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11653(int& val) {
    func_in_11653(val);
    const int x = 11653;
    std::cout << 11653;
    for (int i = 0; i < 11653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11653(int& val) {
    const int x = 11653;
    std::cout << 11653;
    for (int i = 0; i < 11653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11654(int& val) {
    func_in_11654(val);
    const int x = 11654;
    std::cout << 11654;
    for (int i = 0; i < 11654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11654(int& val) {
    const int x = 11654;
    std::cout << 11654;
    for (int i = 0; i < 11654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11655(int& val) {
    func_in_11655(val);
    const int x = 11655;
    std::cout << 11655;
    for (int i = 0; i < 11655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11655(int& val) {
    const int x = 11655;
    std::cout << 11655;
    for (int i = 0; i < 11655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11656(int& val) {
    func_in_11656(val);
    const int x = 11656;
    std::cout << 11656;
    for (int i = 0; i < 11656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11656(int& val) {
    const int x = 11656;
    std::cout << 11656;
    for (int i = 0; i < 11656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11657(int& val) {
    func_in_11657(val);
    const int x = 11657;
    std::cout << 11657;
    for (int i = 0; i < 11657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11657(int& val) {
    const int x = 11657;
    std::cout << 11657;
    for (int i = 0; i < 11657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11658(int& val) {
    func_in_11658(val);
    const int x = 11658;
    std::cout << 11658;
    for (int i = 0; i < 11658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11658(int& val) {
    const int x = 11658;
    std::cout << 11658;
    for (int i = 0; i < 11658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11659(int& val) {
    func_in_11659(val);
    const int x = 11659;
    std::cout << 11659;
    for (int i = 0; i < 11659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11659(int& val) {
    const int x = 11659;
    std::cout << 11659;
    for (int i = 0; i < 11659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11660(int& val) {
    func_in_11660(val);
    const int x = 11660;
    std::cout << 11660;
    for (int i = 0; i < 11660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11660(int& val) {
    const int x = 11660;
    std::cout << 11660;
    for (int i = 0; i < 11660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11661(int& val) {
    func_in_11661(val);
    const int x = 11661;
    std::cout << 11661;
    for (int i = 0; i < 11661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11661(int& val) {
    const int x = 11661;
    std::cout << 11661;
    for (int i = 0; i < 11661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11662(int& val) {
    func_in_11662(val);
    const int x = 11662;
    std::cout << 11662;
    for (int i = 0; i < 11662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11662(int& val) {
    const int x = 11662;
    std::cout << 11662;
    for (int i = 0; i < 11662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11663(int& val) {
    func_in_11663(val);
    const int x = 11663;
    std::cout << 11663;
    for (int i = 0; i < 11663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11663(int& val) {
    const int x = 11663;
    std::cout << 11663;
    for (int i = 0; i < 11663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11664(int& val) {
    func_in_11664(val);
    const int x = 11664;
    std::cout << 11664;
    for (int i = 0; i < 11664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11664(int& val) {
    const int x = 11664;
    std::cout << 11664;
    for (int i = 0; i < 11664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11665(int& val) {
    func_in_11665(val);
    const int x = 11665;
    std::cout << 11665;
    for (int i = 0; i < 11665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11665(int& val) {
    const int x = 11665;
    std::cout << 11665;
    for (int i = 0; i < 11665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11666(int& val) {
    func_in_11666(val);
    const int x = 11666;
    std::cout << 11666;
    for (int i = 0; i < 11666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11666(int& val) {
    const int x = 11666;
    std::cout << 11666;
    for (int i = 0; i < 11666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11667(int& val) {
    func_in_11667(val);
    const int x = 11667;
    std::cout << 11667;
    for (int i = 0; i < 11667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11667(int& val) {
    const int x = 11667;
    std::cout << 11667;
    for (int i = 0; i < 11667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11668(int& val) {
    func_in_11668(val);
    const int x = 11668;
    std::cout << 11668;
    for (int i = 0; i < 11668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11668(int& val) {
    const int x = 11668;
    std::cout << 11668;
    for (int i = 0; i < 11668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11669(int& val) {
    func_in_11669(val);
    const int x = 11669;
    std::cout << 11669;
    for (int i = 0; i < 11669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11669(int& val) {
    const int x = 11669;
    std::cout << 11669;
    for (int i = 0; i < 11669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11670(int& val) {
    func_in_11670(val);
    const int x = 11670;
    std::cout << 11670;
    for (int i = 0; i < 11670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11670(int& val) {
    const int x = 11670;
    std::cout << 11670;
    for (int i = 0; i < 11670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11671(int& val) {
    func_in_11671(val);
    const int x = 11671;
    std::cout << 11671;
    for (int i = 0; i < 11671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11671(int& val) {
    const int x = 11671;
    std::cout << 11671;
    for (int i = 0; i < 11671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11672(int& val) {
    func_in_11672(val);
    const int x = 11672;
    std::cout << 11672;
    for (int i = 0; i < 11672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11672(int& val) {
    const int x = 11672;
    std::cout << 11672;
    for (int i = 0; i < 11672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11673(int& val) {
    func_in_11673(val);
    const int x = 11673;
    std::cout << 11673;
    for (int i = 0; i < 11673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11673(int& val) {
    const int x = 11673;
    std::cout << 11673;
    for (int i = 0; i < 11673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11674(int& val) {
    func_in_11674(val);
    const int x = 11674;
    std::cout << 11674;
    for (int i = 0; i < 11674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11674(int& val) {
    const int x = 11674;
    std::cout << 11674;
    for (int i = 0; i < 11674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11675(int& val) {
    func_in_11675(val);
    const int x = 11675;
    std::cout << 11675;
    for (int i = 0; i < 11675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11675(int& val) {
    const int x = 11675;
    std::cout << 11675;
    for (int i = 0; i < 11675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11676(int& val) {
    func_in_11676(val);
    const int x = 11676;
    std::cout << 11676;
    for (int i = 0; i < 11676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11676(int& val) {
    const int x = 11676;
    std::cout << 11676;
    for (int i = 0; i < 11676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11677(int& val) {
    func_in_11677(val);
    const int x = 11677;
    std::cout << 11677;
    for (int i = 0; i < 11677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11677(int& val) {
    const int x = 11677;
    std::cout << 11677;
    for (int i = 0; i < 11677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11678(int& val) {
    func_in_11678(val);
    const int x = 11678;
    std::cout << 11678;
    for (int i = 0; i < 11678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11678(int& val) {
    const int x = 11678;
    std::cout << 11678;
    for (int i = 0; i < 11678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11679(int& val) {
    func_in_11679(val);
    const int x = 11679;
    std::cout << 11679;
    for (int i = 0; i < 11679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11679(int& val) {
    const int x = 11679;
    std::cout << 11679;
    for (int i = 0; i < 11679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11680(int& val) {
    func_in_11680(val);
    const int x = 11680;
    std::cout << 11680;
    for (int i = 0; i < 11680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11680(int& val) {
    const int x = 11680;
    std::cout << 11680;
    for (int i = 0; i < 11680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11681(int& val) {
    func_in_11681(val);
    const int x = 11681;
    std::cout << 11681;
    for (int i = 0; i < 11681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11681(int& val) {
    const int x = 11681;
    std::cout << 11681;
    for (int i = 0; i < 11681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11682(int& val) {
    func_in_11682(val);
    const int x = 11682;
    std::cout << 11682;
    for (int i = 0; i < 11682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11682(int& val) {
    const int x = 11682;
    std::cout << 11682;
    for (int i = 0; i < 11682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11683(int& val) {
    func_in_11683(val);
    const int x = 11683;
    std::cout << 11683;
    for (int i = 0; i < 11683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11683(int& val) {
    const int x = 11683;
    std::cout << 11683;
    for (int i = 0; i < 11683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11684(int& val) {
    func_in_11684(val);
    const int x = 11684;
    std::cout << 11684;
    for (int i = 0; i < 11684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11684(int& val) {
    const int x = 11684;
    std::cout << 11684;
    for (int i = 0; i < 11684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11685(int& val) {
    func_in_11685(val);
    const int x = 11685;
    std::cout << 11685;
    for (int i = 0; i < 11685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11685(int& val) {
    const int x = 11685;
    std::cout << 11685;
    for (int i = 0; i < 11685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11686(int& val) {
    func_in_11686(val);
    const int x = 11686;
    std::cout << 11686;
    for (int i = 0; i < 11686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11686(int& val) {
    const int x = 11686;
    std::cout << 11686;
    for (int i = 0; i < 11686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11687(int& val) {
    func_in_11687(val);
    const int x = 11687;
    std::cout << 11687;
    for (int i = 0; i < 11687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11687(int& val) {
    const int x = 11687;
    std::cout << 11687;
    for (int i = 0; i < 11687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11688(int& val) {
    func_in_11688(val);
    const int x = 11688;
    std::cout << 11688;
    for (int i = 0; i < 11688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11688(int& val) {
    const int x = 11688;
    std::cout << 11688;
    for (int i = 0; i < 11688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11689(int& val) {
    func_in_11689(val);
    const int x = 11689;
    std::cout << 11689;
    for (int i = 0; i < 11689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11689(int& val) {
    const int x = 11689;
    std::cout << 11689;
    for (int i = 0; i < 11689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11690(int& val) {
    func_in_11690(val);
    const int x = 11690;
    std::cout << 11690;
    for (int i = 0; i < 11690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11690(int& val) {
    const int x = 11690;
    std::cout << 11690;
    for (int i = 0; i < 11690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11691(int& val) {
    func_in_11691(val);
    const int x = 11691;
    std::cout << 11691;
    for (int i = 0; i < 11691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11691(int& val) {
    const int x = 11691;
    std::cout << 11691;
    for (int i = 0; i < 11691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11692(int& val) {
    func_in_11692(val);
    const int x = 11692;
    std::cout << 11692;
    for (int i = 0; i < 11692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11692(int& val) {
    const int x = 11692;
    std::cout << 11692;
    for (int i = 0; i < 11692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11693(int& val) {
    func_in_11693(val);
    const int x = 11693;
    std::cout << 11693;
    for (int i = 0; i < 11693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11693(int& val) {
    const int x = 11693;
    std::cout << 11693;
    for (int i = 0; i < 11693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11694(int& val) {
    func_in_11694(val);
    const int x = 11694;
    std::cout << 11694;
    for (int i = 0; i < 11694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11694(int& val) {
    const int x = 11694;
    std::cout << 11694;
    for (int i = 0; i < 11694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11695(int& val) {
    func_in_11695(val);
    const int x = 11695;
    std::cout << 11695;
    for (int i = 0; i < 11695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11695(int& val) {
    const int x = 11695;
    std::cout << 11695;
    for (int i = 0; i < 11695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11696(int& val) {
    func_in_11696(val);
    const int x = 11696;
    std::cout << 11696;
    for (int i = 0; i < 11696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11696(int& val) {
    const int x = 11696;
    std::cout << 11696;
    for (int i = 0; i < 11696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11697(int& val) {
    func_in_11697(val);
    const int x = 11697;
    std::cout << 11697;
    for (int i = 0; i < 11697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11697(int& val) {
    const int x = 11697;
    std::cout << 11697;
    for (int i = 0; i < 11697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11698(int& val) {
    func_in_11698(val);
    const int x = 11698;
    std::cout << 11698;
    for (int i = 0; i < 11698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11698(int& val) {
    const int x = 11698;
    std::cout << 11698;
    for (int i = 0; i < 11698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11699(int& val) {
    func_in_11699(val);
    const int x = 11699;
    std::cout << 11699;
    for (int i = 0; i < 11699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11699(int& val) {
    const int x = 11699;
    std::cout << 11699;
    for (int i = 0; i < 11699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11700(int& val) {
    func_in_11700(val);
    const int x = 11700;
    std::cout << 11700;
    for (int i = 0; i < 11700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11700(int& val) {
    const int x = 11700;
    std::cout << 11700;
    for (int i = 0; i < 11700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11701(int& val) {
    func_in_11701(val);
    const int x = 11701;
    std::cout << 11701;
    for (int i = 0; i < 11701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11701(int& val) {
    const int x = 11701;
    std::cout << 11701;
    for (int i = 0; i < 11701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11702(int& val) {
    func_in_11702(val);
    const int x = 11702;
    std::cout << 11702;
    for (int i = 0; i < 11702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11702(int& val) {
    const int x = 11702;
    std::cout << 11702;
    for (int i = 0; i < 11702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11703(int& val) {
    func_in_11703(val);
    const int x = 11703;
    std::cout << 11703;
    for (int i = 0; i < 11703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11703(int& val) {
    const int x = 11703;
    std::cout << 11703;
    for (int i = 0; i < 11703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11704(int& val) {
    func_in_11704(val);
    const int x = 11704;
    std::cout << 11704;
    for (int i = 0; i < 11704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11704(int& val) {
    const int x = 11704;
    std::cout << 11704;
    for (int i = 0; i < 11704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11705(int& val) {
    func_in_11705(val);
    const int x = 11705;
    std::cout << 11705;
    for (int i = 0; i < 11705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11705(int& val) {
    const int x = 11705;
    std::cout << 11705;
    for (int i = 0; i < 11705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11706(int& val) {
    func_in_11706(val);
    const int x = 11706;
    std::cout << 11706;
    for (int i = 0; i < 11706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11706(int& val) {
    const int x = 11706;
    std::cout << 11706;
    for (int i = 0; i < 11706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11707(int& val) {
    func_in_11707(val);
    const int x = 11707;
    std::cout << 11707;
    for (int i = 0; i < 11707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11707(int& val) {
    const int x = 11707;
    std::cout << 11707;
    for (int i = 0; i < 11707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11708(int& val) {
    func_in_11708(val);
    const int x = 11708;
    std::cout << 11708;
    for (int i = 0; i < 11708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11708(int& val) {
    const int x = 11708;
    std::cout << 11708;
    for (int i = 0; i < 11708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11709(int& val) {
    func_in_11709(val);
    const int x = 11709;
    std::cout << 11709;
    for (int i = 0; i < 11709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11709(int& val) {
    const int x = 11709;
    std::cout << 11709;
    for (int i = 0; i < 11709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11710(int& val) {
    func_in_11710(val);
    const int x = 11710;
    std::cout << 11710;
    for (int i = 0; i < 11710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11710(int& val) {
    const int x = 11710;
    std::cout << 11710;
    for (int i = 0; i < 11710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11711(int& val) {
    func_in_11711(val);
    const int x = 11711;
    std::cout << 11711;
    for (int i = 0; i < 11711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11711(int& val) {
    const int x = 11711;
    std::cout << 11711;
    for (int i = 0; i < 11711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11712(int& val) {
    func_in_11712(val);
    const int x = 11712;
    std::cout << 11712;
    for (int i = 0; i < 11712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11712(int& val) {
    const int x = 11712;
    std::cout << 11712;
    for (int i = 0; i < 11712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11713(int& val) {
    func_in_11713(val);
    const int x = 11713;
    std::cout << 11713;
    for (int i = 0; i < 11713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11713(int& val) {
    const int x = 11713;
    std::cout << 11713;
    for (int i = 0; i < 11713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11714(int& val) {
    func_in_11714(val);
    const int x = 11714;
    std::cout << 11714;
    for (int i = 0; i < 11714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11714(int& val) {
    const int x = 11714;
    std::cout << 11714;
    for (int i = 0; i < 11714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11715(int& val) {
    func_in_11715(val);
    const int x = 11715;
    std::cout << 11715;
    for (int i = 0; i < 11715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11715(int& val) {
    const int x = 11715;
    std::cout << 11715;
    for (int i = 0; i < 11715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11716(int& val) {
    func_in_11716(val);
    const int x = 11716;
    std::cout << 11716;
    for (int i = 0; i < 11716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11716(int& val) {
    const int x = 11716;
    std::cout << 11716;
    for (int i = 0; i < 11716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11717(int& val) {
    func_in_11717(val);
    const int x = 11717;
    std::cout << 11717;
    for (int i = 0; i < 11717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11717(int& val) {
    const int x = 11717;
    std::cout << 11717;
    for (int i = 0; i < 11717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11718(int& val) {
    func_in_11718(val);
    const int x = 11718;
    std::cout << 11718;
    for (int i = 0; i < 11718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11718(int& val) {
    const int x = 11718;
    std::cout << 11718;
    for (int i = 0; i < 11718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11719(int& val) {
    func_in_11719(val);
    const int x = 11719;
    std::cout << 11719;
    for (int i = 0; i < 11719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11719(int& val) {
    const int x = 11719;
    std::cout << 11719;
    for (int i = 0; i < 11719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11720(int& val) {
    func_in_11720(val);
    const int x = 11720;
    std::cout << 11720;
    for (int i = 0; i < 11720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11720(int& val) {
    const int x = 11720;
    std::cout << 11720;
    for (int i = 0; i < 11720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11721(int& val) {
    func_in_11721(val);
    const int x = 11721;
    std::cout << 11721;
    for (int i = 0; i < 11721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11721(int& val) {
    const int x = 11721;
    std::cout << 11721;
    for (int i = 0; i < 11721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11722(int& val) {
    func_in_11722(val);
    const int x = 11722;
    std::cout << 11722;
    for (int i = 0; i < 11722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11722(int& val) {
    const int x = 11722;
    std::cout << 11722;
    for (int i = 0; i < 11722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11723(int& val) {
    func_in_11723(val);
    const int x = 11723;
    std::cout << 11723;
    for (int i = 0; i < 11723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11723(int& val) {
    const int x = 11723;
    std::cout << 11723;
    for (int i = 0; i < 11723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11724(int& val) {
    func_in_11724(val);
    const int x = 11724;
    std::cout << 11724;
    for (int i = 0; i < 11724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11724(int& val) {
    const int x = 11724;
    std::cout << 11724;
    for (int i = 0; i < 11724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11725(int& val) {
    func_in_11725(val);
    const int x = 11725;
    std::cout << 11725;
    for (int i = 0; i < 11725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11725(int& val) {
    const int x = 11725;
    std::cout << 11725;
    for (int i = 0; i < 11725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11726(int& val) {
    func_in_11726(val);
    const int x = 11726;
    std::cout << 11726;
    for (int i = 0; i < 11726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11726(int& val) {
    const int x = 11726;
    std::cout << 11726;
    for (int i = 0; i < 11726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11727(int& val) {
    func_in_11727(val);
    const int x = 11727;
    std::cout << 11727;
    for (int i = 0; i < 11727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11727(int& val) {
    const int x = 11727;
    std::cout << 11727;
    for (int i = 0; i < 11727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11728(int& val) {
    func_in_11728(val);
    const int x = 11728;
    std::cout << 11728;
    for (int i = 0; i < 11728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11728(int& val) {
    const int x = 11728;
    std::cout << 11728;
    for (int i = 0; i < 11728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11729(int& val) {
    func_in_11729(val);
    const int x = 11729;
    std::cout << 11729;
    for (int i = 0; i < 11729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11729(int& val) {
    const int x = 11729;
    std::cout << 11729;
    for (int i = 0; i < 11729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11730(int& val) {
    func_in_11730(val);
    const int x = 11730;
    std::cout << 11730;
    for (int i = 0; i < 11730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11730(int& val) {
    const int x = 11730;
    std::cout << 11730;
    for (int i = 0; i < 11730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11731(int& val) {
    func_in_11731(val);
    const int x = 11731;
    std::cout << 11731;
    for (int i = 0; i < 11731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11731(int& val) {
    const int x = 11731;
    std::cout << 11731;
    for (int i = 0; i < 11731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11732(int& val) {
    func_in_11732(val);
    const int x = 11732;
    std::cout << 11732;
    for (int i = 0; i < 11732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11732(int& val) {
    const int x = 11732;
    std::cout << 11732;
    for (int i = 0; i < 11732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11733(int& val) {
    func_in_11733(val);
    const int x = 11733;
    std::cout << 11733;
    for (int i = 0; i < 11733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11733(int& val) {
    const int x = 11733;
    std::cout << 11733;
    for (int i = 0; i < 11733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11734(int& val) {
    func_in_11734(val);
    const int x = 11734;
    std::cout << 11734;
    for (int i = 0; i < 11734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11734(int& val) {
    const int x = 11734;
    std::cout << 11734;
    for (int i = 0; i < 11734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11735(int& val) {
    func_in_11735(val);
    const int x = 11735;
    std::cout << 11735;
    for (int i = 0; i < 11735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11735(int& val) {
    const int x = 11735;
    std::cout << 11735;
    for (int i = 0; i < 11735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11736(int& val) {
    func_in_11736(val);
    const int x = 11736;
    std::cout << 11736;
    for (int i = 0; i < 11736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11736(int& val) {
    const int x = 11736;
    std::cout << 11736;
    for (int i = 0; i < 11736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11737(int& val) {
    func_in_11737(val);
    const int x = 11737;
    std::cout << 11737;
    for (int i = 0; i < 11737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11737(int& val) {
    const int x = 11737;
    std::cout << 11737;
    for (int i = 0; i < 11737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11738(int& val) {
    func_in_11738(val);
    const int x = 11738;
    std::cout << 11738;
    for (int i = 0; i < 11738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11738(int& val) {
    const int x = 11738;
    std::cout << 11738;
    for (int i = 0; i < 11738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11739(int& val) {
    func_in_11739(val);
    const int x = 11739;
    std::cout << 11739;
    for (int i = 0; i < 11739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11739(int& val) {
    const int x = 11739;
    std::cout << 11739;
    for (int i = 0; i < 11739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11740(int& val) {
    func_in_11740(val);
    const int x = 11740;
    std::cout << 11740;
    for (int i = 0; i < 11740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11740(int& val) {
    const int x = 11740;
    std::cout << 11740;
    for (int i = 0; i < 11740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11741(int& val) {
    func_in_11741(val);
    const int x = 11741;
    std::cout << 11741;
    for (int i = 0; i < 11741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11741(int& val) {
    const int x = 11741;
    std::cout << 11741;
    for (int i = 0; i < 11741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11742(int& val) {
    func_in_11742(val);
    const int x = 11742;
    std::cout << 11742;
    for (int i = 0; i < 11742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11742(int& val) {
    const int x = 11742;
    std::cout << 11742;
    for (int i = 0; i < 11742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11743(int& val) {
    func_in_11743(val);
    const int x = 11743;
    std::cout << 11743;
    for (int i = 0; i < 11743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11743(int& val) {
    const int x = 11743;
    std::cout << 11743;
    for (int i = 0; i < 11743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11744(int& val) {
    func_in_11744(val);
    const int x = 11744;
    std::cout << 11744;
    for (int i = 0; i < 11744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11744(int& val) {
    const int x = 11744;
    std::cout << 11744;
    for (int i = 0; i < 11744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11745(int& val) {
    func_in_11745(val);
    const int x = 11745;
    std::cout << 11745;
    for (int i = 0; i < 11745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11745(int& val) {
    const int x = 11745;
    std::cout << 11745;
    for (int i = 0; i < 11745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11746(int& val) {
    func_in_11746(val);
    const int x = 11746;
    std::cout << 11746;
    for (int i = 0; i < 11746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11746(int& val) {
    const int x = 11746;
    std::cout << 11746;
    for (int i = 0; i < 11746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11747(int& val) {
    func_in_11747(val);
    const int x = 11747;
    std::cout << 11747;
    for (int i = 0; i < 11747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11747(int& val) {
    const int x = 11747;
    std::cout << 11747;
    for (int i = 0; i < 11747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11748(int& val) {
    func_in_11748(val);
    const int x = 11748;
    std::cout << 11748;
    for (int i = 0; i < 11748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11748(int& val) {
    const int x = 11748;
    std::cout << 11748;
    for (int i = 0; i < 11748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11749(int& val) {
    func_in_11749(val);
    const int x = 11749;
    std::cout << 11749;
    for (int i = 0; i < 11749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11749(int& val) {
    const int x = 11749;
    std::cout << 11749;
    for (int i = 0; i < 11749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11750(int& val) {
    func_in_11750(val);
    const int x = 11750;
    std::cout << 11750;
    for (int i = 0; i < 11750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11750(int& val) {
    const int x = 11750;
    std::cout << 11750;
    for (int i = 0; i < 11750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11751(int& val) {
    func_in_11751(val);
    const int x = 11751;
    std::cout << 11751;
    for (int i = 0; i < 11751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11751(int& val) {
    const int x = 11751;
    std::cout << 11751;
    for (int i = 0; i < 11751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11752(int& val) {
    func_in_11752(val);
    const int x = 11752;
    std::cout << 11752;
    for (int i = 0; i < 11752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11752(int& val) {
    const int x = 11752;
    std::cout << 11752;
    for (int i = 0; i < 11752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11753(int& val) {
    func_in_11753(val);
    const int x = 11753;
    std::cout << 11753;
    for (int i = 0; i < 11753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11753(int& val) {
    const int x = 11753;
    std::cout << 11753;
    for (int i = 0; i < 11753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11754(int& val) {
    func_in_11754(val);
    const int x = 11754;
    std::cout << 11754;
    for (int i = 0; i < 11754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11754(int& val) {
    const int x = 11754;
    std::cout << 11754;
    for (int i = 0; i < 11754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11755(int& val) {
    func_in_11755(val);
    const int x = 11755;
    std::cout << 11755;
    for (int i = 0; i < 11755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11755(int& val) {
    const int x = 11755;
    std::cout << 11755;
    for (int i = 0; i < 11755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11756(int& val) {
    func_in_11756(val);
    const int x = 11756;
    std::cout << 11756;
    for (int i = 0; i < 11756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11756(int& val) {
    const int x = 11756;
    std::cout << 11756;
    for (int i = 0; i < 11756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11757(int& val) {
    func_in_11757(val);
    const int x = 11757;
    std::cout << 11757;
    for (int i = 0; i < 11757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11757(int& val) {
    const int x = 11757;
    std::cout << 11757;
    for (int i = 0; i < 11757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11758(int& val) {
    func_in_11758(val);
    const int x = 11758;
    std::cout << 11758;
    for (int i = 0; i < 11758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11758(int& val) {
    const int x = 11758;
    std::cout << 11758;
    for (int i = 0; i < 11758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11759(int& val) {
    func_in_11759(val);
    const int x = 11759;
    std::cout << 11759;
    for (int i = 0; i < 11759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11759(int& val) {
    const int x = 11759;
    std::cout << 11759;
    for (int i = 0; i < 11759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11760(int& val) {
    func_in_11760(val);
    const int x = 11760;
    std::cout << 11760;
    for (int i = 0; i < 11760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11760(int& val) {
    const int x = 11760;
    std::cout << 11760;
    for (int i = 0; i < 11760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11761(int& val) {
    func_in_11761(val);
    const int x = 11761;
    std::cout << 11761;
    for (int i = 0; i < 11761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11761(int& val) {
    const int x = 11761;
    std::cout << 11761;
    for (int i = 0; i < 11761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11762(int& val) {
    func_in_11762(val);
    const int x = 11762;
    std::cout << 11762;
    for (int i = 0; i < 11762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11762(int& val) {
    const int x = 11762;
    std::cout << 11762;
    for (int i = 0; i < 11762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11763(int& val) {
    func_in_11763(val);
    const int x = 11763;
    std::cout << 11763;
    for (int i = 0; i < 11763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11763(int& val) {
    const int x = 11763;
    std::cout << 11763;
    for (int i = 0; i < 11763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11764(int& val) {
    func_in_11764(val);
    const int x = 11764;
    std::cout << 11764;
    for (int i = 0; i < 11764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11764(int& val) {
    const int x = 11764;
    std::cout << 11764;
    for (int i = 0; i < 11764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11765(int& val) {
    func_in_11765(val);
    const int x = 11765;
    std::cout << 11765;
    for (int i = 0; i < 11765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11765(int& val) {
    const int x = 11765;
    std::cout << 11765;
    for (int i = 0; i < 11765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11766(int& val) {
    func_in_11766(val);
    const int x = 11766;
    std::cout << 11766;
    for (int i = 0; i < 11766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11766(int& val) {
    const int x = 11766;
    std::cout << 11766;
    for (int i = 0; i < 11766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11767(int& val) {
    func_in_11767(val);
    const int x = 11767;
    std::cout << 11767;
    for (int i = 0; i < 11767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11767(int& val) {
    const int x = 11767;
    std::cout << 11767;
    for (int i = 0; i < 11767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11768(int& val) {
    func_in_11768(val);
    const int x = 11768;
    std::cout << 11768;
    for (int i = 0; i < 11768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11768(int& val) {
    const int x = 11768;
    std::cout << 11768;
    for (int i = 0; i < 11768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11769(int& val) {
    func_in_11769(val);
    const int x = 11769;
    std::cout << 11769;
    for (int i = 0; i < 11769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11769(int& val) {
    const int x = 11769;
    std::cout << 11769;
    for (int i = 0; i < 11769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11770(int& val) {
    func_in_11770(val);
    const int x = 11770;
    std::cout << 11770;
    for (int i = 0; i < 11770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11770(int& val) {
    const int x = 11770;
    std::cout << 11770;
    for (int i = 0; i < 11770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11771(int& val) {
    func_in_11771(val);
    const int x = 11771;
    std::cout << 11771;
    for (int i = 0; i < 11771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11771(int& val) {
    const int x = 11771;
    std::cout << 11771;
    for (int i = 0; i < 11771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11772(int& val) {
    func_in_11772(val);
    const int x = 11772;
    std::cout << 11772;
    for (int i = 0; i < 11772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11772(int& val) {
    const int x = 11772;
    std::cout << 11772;
    for (int i = 0; i < 11772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11773(int& val) {
    func_in_11773(val);
    const int x = 11773;
    std::cout << 11773;
    for (int i = 0; i < 11773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11773(int& val) {
    const int x = 11773;
    std::cout << 11773;
    for (int i = 0; i < 11773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11774(int& val) {
    func_in_11774(val);
    const int x = 11774;
    std::cout << 11774;
    for (int i = 0; i < 11774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11774(int& val) {
    const int x = 11774;
    std::cout << 11774;
    for (int i = 0; i < 11774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11775(int& val) {
    func_in_11775(val);
    const int x = 11775;
    std::cout << 11775;
    for (int i = 0; i < 11775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11775(int& val) {
    const int x = 11775;
    std::cout << 11775;
    for (int i = 0; i < 11775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11776(int& val) {
    func_in_11776(val);
    const int x = 11776;
    std::cout << 11776;
    for (int i = 0; i < 11776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11776(int& val) {
    const int x = 11776;
    std::cout << 11776;
    for (int i = 0; i < 11776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11777(int& val) {
    func_in_11777(val);
    const int x = 11777;
    std::cout << 11777;
    for (int i = 0; i < 11777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11777(int& val) {
    const int x = 11777;
    std::cout << 11777;
    for (int i = 0; i < 11777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11778(int& val) {
    func_in_11778(val);
    const int x = 11778;
    std::cout << 11778;
    for (int i = 0; i < 11778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11778(int& val) {
    const int x = 11778;
    std::cout << 11778;
    for (int i = 0; i < 11778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11779(int& val) {
    func_in_11779(val);
    const int x = 11779;
    std::cout << 11779;
    for (int i = 0; i < 11779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11779(int& val) {
    const int x = 11779;
    std::cout << 11779;
    for (int i = 0; i < 11779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11780(int& val) {
    func_in_11780(val);
    const int x = 11780;
    std::cout << 11780;
    for (int i = 0; i < 11780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11780(int& val) {
    const int x = 11780;
    std::cout << 11780;
    for (int i = 0; i < 11780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11781(int& val) {
    func_in_11781(val);
    const int x = 11781;
    std::cout << 11781;
    for (int i = 0; i < 11781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11781(int& val) {
    const int x = 11781;
    std::cout << 11781;
    for (int i = 0; i < 11781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11782(int& val) {
    func_in_11782(val);
    const int x = 11782;
    std::cout << 11782;
    for (int i = 0; i < 11782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11782(int& val) {
    const int x = 11782;
    std::cout << 11782;
    for (int i = 0; i < 11782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11783(int& val) {
    func_in_11783(val);
    const int x = 11783;
    std::cout << 11783;
    for (int i = 0; i < 11783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11783(int& val) {
    const int x = 11783;
    std::cout << 11783;
    for (int i = 0; i < 11783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11784(int& val) {
    func_in_11784(val);
    const int x = 11784;
    std::cout << 11784;
    for (int i = 0; i < 11784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11784(int& val) {
    const int x = 11784;
    std::cout << 11784;
    for (int i = 0; i < 11784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11785(int& val) {
    func_in_11785(val);
    const int x = 11785;
    std::cout << 11785;
    for (int i = 0; i < 11785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11785(int& val) {
    const int x = 11785;
    std::cout << 11785;
    for (int i = 0; i < 11785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11786(int& val) {
    func_in_11786(val);
    const int x = 11786;
    std::cout << 11786;
    for (int i = 0; i < 11786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11786(int& val) {
    const int x = 11786;
    std::cout << 11786;
    for (int i = 0; i < 11786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11787(int& val) {
    func_in_11787(val);
    const int x = 11787;
    std::cout << 11787;
    for (int i = 0; i < 11787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11787(int& val) {
    const int x = 11787;
    std::cout << 11787;
    for (int i = 0; i < 11787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11788(int& val) {
    func_in_11788(val);
    const int x = 11788;
    std::cout << 11788;
    for (int i = 0; i < 11788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11788(int& val) {
    const int x = 11788;
    std::cout << 11788;
    for (int i = 0; i < 11788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11789(int& val) {
    func_in_11789(val);
    const int x = 11789;
    std::cout << 11789;
    for (int i = 0; i < 11789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11789(int& val) {
    const int x = 11789;
    std::cout << 11789;
    for (int i = 0; i < 11789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11790(int& val) {
    func_in_11790(val);
    const int x = 11790;
    std::cout << 11790;
    for (int i = 0; i < 11790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11790(int& val) {
    const int x = 11790;
    std::cout << 11790;
    for (int i = 0; i < 11790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11791(int& val) {
    func_in_11791(val);
    const int x = 11791;
    std::cout << 11791;
    for (int i = 0; i < 11791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11791(int& val) {
    const int x = 11791;
    std::cout << 11791;
    for (int i = 0; i < 11791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11792(int& val) {
    func_in_11792(val);
    const int x = 11792;
    std::cout << 11792;
    for (int i = 0; i < 11792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11792(int& val) {
    const int x = 11792;
    std::cout << 11792;
    for (int i = 0; i < 11792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11793(int& val) {
    func_in_11793(val);
    const int x = 11793;
    std::cout << 11793;
    for (int i = 0; i < 11793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11793(int& val) {
    const int x = 11793;
    std::cout << 11793;
    for (int i = 0; i < 11793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11794(int& val) {
    func_in_11794(val);
    const int x = 11794;
    std::cout << 11794;
    for (int i = 0; i < 11794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11794(int& val) {
    const int x = 11794;
    std::cout << 11794;
    for (int i = 0; i < 11794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11795(int& val) {
    func_in_11795(val);
    const int x = 11795;
    std::cout << 11795;
    for (int i = 0; i < 11795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11795(int& val) {
    const int x = 11795;
    std::cout << 11795;
    for (int i = 0; i < 11795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11796(int& val) {
    func_in_11796(val);
    const int x = 11796;
    std::cout << 11796;
    for (int i = 0; i < 11796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11796(int& val) {
    const int x = 11796;
    std::cout << 11796;
    for (int i = 0; i < 11796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11797(int& val) {
    func_in_11797(val);
    const int x = 11797;
    std::cout << 11797;
    for (int i = 0; i < 11797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11797(int& val) {
    const int x = 11797;
    std::cout << 11797;
    for (int i = 0; i < 11797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11798(int& val) {
    func_in_11798(val);
    const int x = 11798;
    std::cout << 11798;
    for (int i = 0; i < 11798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11798(int& val) {
    const int x = 11798;
    std::cout << 11798;
    for (int i = 0; i < 11798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11799(int& val) {
    func_in_11799(val);
    const int x = 11799;
    std::cout << 11799;
    for (int i = 0; i < 11799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11799(int& val) {
    const int x = 11799;
    std::cout << 11799;
    for (int i = 0; i < 11799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11800(int& val) {
    func_in_11800(val);
    const int x = 11800;
    std::cout << 11800;
    for (int i = 0; i < 11800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11800(int& val) {
    const int x = 11800;
    std::cout << 11800;
    for (int i = 0; i < 11800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11801(int& val) {
    func_in_11801(val);
    const int x = 11801;
    std::cout << 11801;
    for (int i = 0; i < 11801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11801(int& val) {
    const int x = 11801;
    std::cout << 11801;
    for (int i = 0; i < 11801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11802(int& val) {
    func_in_11802(val);
    const int x = 11802;
    std::cout << 11802;
    for (int i = 0; i < 11802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11802(int& val) {
    const int x = 11802;
    std::cout << 11802;
    for (int i = 0; i < 11802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11803(int& val) {
    func_in_11803(val);
    const int x = 11803;
    std::cout << 11803;
    for (int i = 0; i < 11803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11803(int& val) {
    const int x = 11803;
    std::cout << 11803;
    for (int i = 0; i < 11803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11804(int& val) {
    func_in_11804(val);
    const int x = 11804;
    std::cout << 11804;
    for (int i = 0; i < 11804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11804(int& val) {
    const int x = 11804;
    std::cout << 11804;
    for (int i = 0; i < 11804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11805(int& val) {
    func_in_11805(val);
    const int x = 11805;
    std::cout << 11805;
    for (int i = 0; i < 11805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11805(int& val) {
    const int x = 11805;
    std::cout << 11805;
    for (int i = 0; i < 11805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11806(int& val) {
    func_in_11806(val);
    const int x = 11806;
    std::cout << 11806;
    for (int i = 0; i < 11806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11806(int& val) {
    const int x = 11806;
    std::cout << 11806;
    for (int i = 0; i < 11806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11807(int& val) {
    func_in_11807(val);
    const int x = 11807;
    std::cout << 11807;
    for (int i = 0; i < 11807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11807(int& val) {
    const int x = 11807;
    std::cout << 11807;
    for (int i = 0; i < 11807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11808(int& val) {
    func_in_11808(val);
    const int x = 11808;
    std::cout << 11808;
    for (int i = 0; i < 11808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11808(int& val) {
    const int x = 11808;
    std::cout << 11808;
    for (int i = 0; i < 11808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11809(int& val) {
    func_in_11809(val);
    const int x = 11809;
    std::cout << 11809;
    for (int i = 0; i < 11809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11809(int& val) {
    const int x = 11809;
    std::cout << 11809;
    for (int i = 0; i < 11809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11810(int& val) {
    func_in_11810(val);
    const int x = 11810;
    std::cout << 11810;
    for (int i = 0; i < 11810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11810(int& val) {
    const int x = 11810;
    std::cout << 11810;
    for (int i = 0; i < 11810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11811(int& val) {
    func_in_11811(val);
    const int x = 11811;
    std::cout << 11811;
    for (int i = 0; i < 11811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11811(int& val) {
    const int x = 11811;
    std::cout << 11811;
    for (int i = 0; i < 11811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11812(int& val) {
    func_in_11812(val);
    const int x = 11812;
    std::cout << 11812;
    for (int i = 0; i < 11812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11812(int& val) {
    const int x = 11812;
    std::cout << 11812;
    for (int i = 0; i < 11812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11813(int& val) {
    func_in_11813(val);
    const int x = 11813;
    std::cout << 11813;
    for (int i = 0; i < 11813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11813(int& val) {
    const int x = 11813;
    std::cout << 11813;
    for (int i = 0; i < 11813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11814(int& val) {
    func_in_11814(val);
    const int x = 11814;
    std::cout << 11814;
    for (int i = 0; i < 11814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11814(int& val) {
    const int x = 11814;
    std::cout << 11814;
    for (int i = 0; i < 11814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11815(int& val) {
    func_in_11815(val);
    const int x = 11815;
    std::cout << 11815;
    for (int i = 0; i < 11815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11815(int& val) {
    const int x = 11815;
    std::cout << 11815;
    for (int i = 0; i < 11815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11816(int& val) {
    func_in_11816(val);
    const int x = 11816;
    std::cout << 11816;
    for (int i = 0; i < 11816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11816(int& val) {
    const int x = 11816;
    std::cout << 11816;
    for (int i = 0; i < 11816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11817(int& val) {
    func_in_11817(val);
    const int x = 11817;
    std::cout << 11817;
    for (int i = 0; i < 11817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11817(int& val) {
    const int x = 11817;
    std::cout << 11817;
    for (int i = 0; i < 11817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11818(int& val) {
    func_in_11818(val);
    const int x = 11818;
    std::cout << 11818;
    for (int i = 0; i < 11818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11818(int& val) {
    const int x = 11818;
    std::cout << 11818;
    for (int i = 0; i < 11818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11819(int& val) {
    func_in_11819(val);
    const int x = 11819;
    std::cout << 11819;
    for (int i = 0; i < 11819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11819(int& val) {
    const int x = 11819;
    std::cout << 11819;
    for (int i = 0; i < 11819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11820(int& val) {
    func_in_11820(val);
    const int x = 11820;
    std::cout << 11820;
    for (int i = 0; i < 11820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11820(int& val) {
    const int x = 11820;
    std::cout << 11820;
    for (int i = 0; i < 11820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11821(int& val) {
    func_in_11821(val);
    const int x = 11821;
    std::cout << 11821;
    for (int i = 0; i < 11821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11821(int& val) {
    const int x = 11821;
    std::cout << 11821;
    for (int i = 0; i < 11821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11822(int& val) {
    func_in_11822(val);
    const int x = 11822;
    std::cout << 11822;
    for (int i = 0; i < 11822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11822(int& val) {
    const int x = 11822;
    std::cout << 11822;
    for (int i = 0; i < 11822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11823(int& val) {
    func_in_11823(val);
    const int x = 11823;
    std::cout << 11823;
    for (int i = 0; i < 11823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11823(int& val) {
    const int x = 11823;
    std::cout << 11823;
    for (int i = 0; i < 11823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11824(int& val) {
    func_in_11824(val);
    const int x = 11824;
    std::cout << 11824;
    for (int i = 0; i < 11824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11824(int& val) {
    const int x = 11824;
    std::cout << 11824;
    for (int i = 0; i < 11824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11825(int& val) {
    func_in_11825(val);
    const int x = 11825;
    std::cout << 11825;
    for (int i = 0; i < 11825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11825(int& val) {
    const int x = 11825;
    std::cout << 11825;
    for (int i = 0; i < 11825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11826(int& val) {
    func_in_11826(val);
    const int x = 11826;
    std::cout << 11826;
    for (int i = 0; i < 11826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11826(int& val) {
    const int x = 11826;
    std::cout << 11826;
    for (int i = 0; i < 11826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11827(int& val) {
    func_in_11827(val);
    const int x = 11827;
    std::cout << 11827;
    for (int i = 0; i < 11827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11827(int& val) {
    const int x = 11827;
    std::cout << 11827;
    for (int i = 0; i < 11827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11828(int& val) {
    func_in_11828(val);
    const int x = 11828;
    std::cout << 11828;
    for (int i = 0; i < 11828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11828(int& val) {
    const int x = 11828;
    std::cout << 11828;
    for (int i = 0; i < 11828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11829(int& val) {
    func_in_11829(val);
    const int x = 11829;
    std::cout << 11829;
    for (int i = 0; i < 11829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11829(int& val) {
    const int x = 11829;
    std::cout << 11829;
    for (int i = 0; i < 11829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11830(int& val) {
    func_in_11830(val);
    const int x = 11830;
    std::cout << 11830;
    for (int i = 0; i < 11830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11830(int& val) {
    const int x = 11830;
    std::cout << 11830;
    for (int i = 0; i < 11830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11831(int& val) {
    func_in_11831(val);
    const int x = 11831;
    std::cout << 11831;
    for (int i = 0; i < 11831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11831(int& val) {
    const int x = 11831;
    std::cout << 11831;
    for (int i = 0; i < 11831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11832(int& val) {
    func_in_11832(val);
    const int x = 11832;
    std::cout << 11832;
    for (int i = 0; i < 11832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11832(int& val) {
    const int x = 11832;
    std::cout << 11832;
    for (int i = 0; i < 11832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11833(int& val) {
    func_in_11833(val);
    const int x = 11833;
    std::cout << 11833;
    for (int i = 0; i < 11833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11833(int& val) {
    const int x = 11833;
    std::cout << 11833;
    for (int i = 0; i < 11833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11834(int& val) {
    func_in_11834(val);
    const int x = 11834;
    std::cout << 11834;
    for (int i = 0; i < 11834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11834(int& val) {
    const int x = 11834;
    std::cout << 11834;
    for (int i = 0; i < 11834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11835(int& val) {
    func_in_11835(val);
    const int x = 11835;
    std::cout << 11835;
    for (int i = 0; i < 11835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11835(int& val) {
    const int x = 11835;
    std::cout << 11835;
    for (int i = 0; i < 11835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11836(int& val) {
    func_in_11836(val);
    const int x = 11836;
    std::cout << 11836;
    for (int i = 0; i < 11836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11836(int& val) {
    const int x = 11836;
    std::cout << 11836;
    for (int i = 0; i < 11836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11837(int& val) {
    func_in_11837(val);
    const int x = 11837;
    std::cout << 11837;
    for (int i = 0; i < 11837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11837(int& val) {
    const int x = 11837;
    std::cout << 11837;
    for (int i = 0; i < 11837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11838(int& val) {
    func_in_11838(val);
    const int x = 11838;
    std::cout << 11838;
    for (int i = 0; i < 11838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11838(int& val) {
    const int x = 11838;
    std::cout << 11838;
    for (int i = 0; i < 11838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11839(int& val) {
    func_in_11839(val);
    const int x = 11839;
    std::cout << 11839;
    for (int i = 0; i < 11839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11839(int& val) {
    const int x = 11839;
    std::cout << 11839;
    for (int i = 0; i < 11839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11840(int& val) {
    func_in_11840(val);
    const int x = 11840;
    std::cout << 11840;
    for (int i = 0; i < 11840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11840(int& val) {
    const int x = 11840;
    std::cout << 11840;
    for (int i = 0; i < 11840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11841(int& val) {
    func_in_11841(val);
    const int x = 11841;
    std::cout << 11841;
    for (int i = 0; i < 11841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11841(int& val) {
    const int x = 11841;
    std::cout << 11841;
    for (int i = 0; i < 11841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11842(int& val) {
    func_in_11842(val);
    const int x = 11842;
    std::cout << 11842;
    for (int i = 0; i < 11842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11842(int& val) {
    const int x = 11842;
    std::cout << 11842;
    for (int i = 0; i < 11842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11843(int& val) {
    func_in_11843(val);
    const int x = 11843;
    std::cout << 11843;
    for (int i = 0; i < 11843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11843(int& val) {
    const int x = 11843;
    std::cout << 11843;
    for (int i = 0; i < 11843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11844(int& val) {
    func_in_11844(val);
    const int x = 11844;
    std::cout << 11844;
    for (int i = 0; i < 11844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11844(int& val) {
    const int x = 11844;
    std::cout << 11844;
    for (int i = 0; i < 11844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11845(int& val) {
    func_in_11845(val);
    const int x = 11845;
    std::cout << 11845;
    for (int i = 0; i < 11845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11845(int& val) {
    const int x = 11845;
    std::cout << 11845;
    for (int i = 0; i < 11845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11846(int& val) {
    func_in_11846(val);
    const int x = 11846;
    std::cout << 11846;
    for (int i = 0; i < 11846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11846(int& val) {
    const int x = 11846;
    std::cout << 11846;
    for (int i = 0; i < 11846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11847(int& val) {
    func_in_11847(val);
    const int x = 11847;
    std::cout << 11847;
    for (int i = 0; i < 11847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11847(int& val) {
    const int x = 11847;
    std::cout << 11847;
    for (int i = 0; i < 11847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11848(int& val) {
    func_in_11848(val);
    const int x = 11848;
    std::cout << 11848;
    for (int i = 0; i < 11848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11848(int& val) {
    const int x = 11848;
    std::cout << 11848;
    for (int i = 0; i < 11848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11849(int& val) {
    func_in_11849(val);
    const int x = 11849;
    std::cout << 11849;
    for (int i = 0; i < 11849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11849(int& val) {
    const int x = 11849;
    std::cout << 11849;
    for (int i = 0; i < 11849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11850(int& val) {
    func_in_11850(val);
    const int x = 11850;
    std::cout << 11850;
    for (int i = 0; i < 11850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11850(int& val) {
    const int x = 11850;
    std::cout << 11850;
    for (int i = 0; i < 11850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11851(int& val) {
    func_in_11851(val);
    const int x = 11851;
    std::cout << 11851;
    for (int i = 0; i < 11851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11851(int& val) {
    const int x = 11851;
    std::cout << 11851;
    for (int i = 0; i < 11851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11852(int& val) {
    func_in_11852(val);
    const int x = 11852;
    std::cout << 11852;
    for (int i = 0; i < 11852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11852(int& val) {
    const int x = 11852;
    std::cout << 11852;
    for (int i = 0; i < 11852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11853(int& val) {
    func_in_11853(val);
    const int x = 11853;
    std::cout << 11853;
    for (int i = 0; i < 11853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11853(int& val) {
    const int x = 11853;
    std::cout << 11853;
    for (int i = 0; i < 11853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11854(int& val) {
    func_in_11854(val);
    const int x = 11854;
    std::cout << 11854;
    for (int i = 0; i < 11854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11854(int& val) {
    const int x = 11854;
    std::cout << 11854;
    for (int i = 0; i < 11854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11855(int& val) {
    func_in_11855(val);
    const int x = 11855;
    std::cout << 11855;
    for (int i = 0; i < 11855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11855(int& val) {
    const int x = 11855;
    std::cout << 11855;
    for (int i = 0; i < 11855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11856(int& val) {
    func_in_11856(val);
    const int x = 11856;
    std::cout << 11856;
    for (int i = 0; i < 11856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11856(int& val) {
    const int x = 11856;
    std::cout << 11856;
    for (int i = 0; i < 11856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11857(int& val) {
    func_in_11857(val);
    const int x = 11857;
    std::cout << 11857;
    for (int i = 0; i < 11857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11857(int& val) {
    const int x = 11857;
    std::cout << 11857;
    for (int i = 0; i < 11857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11858(int& val) {
    func_in_11858(val);
    const int x = 11858;
    std::cout << 11858;
    for (int i = 0; i < 11858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11858(int& val) {
    const int x = 11858;
    std::cout << 11858;
    for (int i = 0; i < 11858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11859(int& val) {
    func_in_11859(val);
    const int x = 11859;
    std::cout << 11859;
    for (int i = 0; i < 11859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11859(int& val) {
    const int x = 11859;
    std::cout << 11859;
    for (int i = 0; i < 11859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11860(int& val) {
    func_in_11860(val);
    const int x = 11860;
    std::cout << 11860;
    for (int i = 0; i < 11860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11860(int& val) {
    const int x = 11860;
    std::cout << 11860;
    for (int i = 0; i < 11860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11861(int& val) {
    func_in_11861(val);
    const int x = 11861;
    std::cout << 11861;
    for (int i = 0; i < 11861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11861(int& val) {
    const int x = 11861;
    std::cout << 11861;
    for (int i = 0; i < 11861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11862(int& val) {
    func_in_11862(val);
    const int x = 11862;
    std::cout << 11862;
    for (int i = 0; i < 11862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11862(int& val) {
    const int x = 11862;
    std::cout << 11862;
    for (int i = 0; i < 11862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11863(int& val) {
    func_in_11863(val);
    const int x = 11863;
    std::cout << 11863;
    for (int i = 0; i < 11863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11863(int& val) {
    const int x = 11863;
    std::cout << 11863;
    for (int i = 0; i < 11863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11864(int& val) {
    func_in_11864(val);
    const int x = 11864;
    std::cout << 11864;
    for (int i = 0; i < 11864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11864(int& val) {
    const int x = 11864;
    std::cout << 11864;
    for (int i = 0; i < 11864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11865(int& val) {
    func_in_11865(val);
    const int x = 11865;
    std::cout << 11865;
    for (int i = 0; i < 11865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11865(int& val) {
    const int x = 11865;
    std::cout << 11865;
    for (int i = 0; i < 11865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11866(int& val) {
    func_in_11866(val);
    const int x = 11866;
    std::cout << 11866;
    for (int i = 0; i < 11866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11866(int& val) {
    const int x = 11866;
    std::cout << 11866;
    for (int i = 0; i < 11866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11867(int& val) {
    func_in_11867(val);
    const int x = 11867;
    std::cout << 11867;
    for (int i = 0; i < 11867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11867(int& val) {
    const int x = 11867;
    std::cout << 11867;
    for (int i = 0; i < 11867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11868(int& val) {
    func_in_11868(val);
    const int x = 11868;
    std::cout << 11868;
    for (int i = 0; i < 11868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11868(int& val) {
    const int x = 11868;
    std::cout << 11868;
    for (int i = 0; i < 11868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11869(int& val) {
    func_in_11869(val);
    const int x = 11869;
    std::cout << 11869;
    for (int i = 0; i < 11869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11869(int& val) {
    const int x = 11869;
    std::cout << 11869;
    for (int i = 0; i < 11869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11870(int& val) {
    func_in_11870(val);
    const int x = 11870;
    std::cout << 11870;
    for (int i = 0; i < 11870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11870(int& val) {
    const int x = 11870;
    std::cout << 11870;
    for (int i = 0; i < 11870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11871(int& val) {
    func_in_11871(val);
    const int x = 11871;
    std::cout << 11871;
    for (int i = 0; i < 11871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11871(int& val) {
    const int x = 11871;
    std::cout << 11871;
    for (int i = 0; i < 11871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11872(int& val) {
    func_in_11872(val);
    const int x = 11872;
    std::cout << 11872;
    for (int i = 0; i < 11872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11872(int& val) {
    const int x = 11872;
    std::cout << 11872;
    for (int i = 0; i < 11872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11873(int& val) {
    func_in_11873(val);
    const int x = 11873;
    std::cout << 11873;
    for (int i = 0; i < 11873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11873(int& val) {
    const int x = 11873;
    std::cout << 11873;
    for (int i = 0; i < 11873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11874(int& val) {
    func_in_11874(val);
    const int x = 11874;
    std::cout << 11874;
    for (int i = 0; i < 11874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11874(int& val) {
    const int x = 11874;
    std::cout << 11874;
    for (int i = 0; i < 11874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11875(int& val) {
    func_in_11875(val);
    const int x = 11875;
    std::cout << 11875;
    for (int i = 0; i < 11875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11875(int& val) {
    const int x = 11875;
    std::cout << 11875;
    for (int i = 0; i < 11875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11876(int& val) {
    func_in_11876(val);
    const int x = 11876;
    std::cout << 11876;
    for (int i = 0; i < 11876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11876(int& val) {
    const int x = 11876;
    std::cout << 11876;
    for (int i = 0; i < 11876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11877(int& val) {
    func_in_11877(val);
    const int x = 11877;
    std::cout << 11877;
    for (int i = 0; i < 11877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11877(int& val) {
    const int x = 11877;
    std::cout << 11877;
    for (int i = 0; i < 11877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11878(int& val) {
    func_in_11878(val);
    const int x = 11878;
    std::cout << 11878;
    for (int i = 0; i < 11878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11878(int& val) {
    const int x = 11878;
    std::cout << 11878;
    for (int i = 0; i < 11878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11879(int& val) {
    func_in_11879(val);
    const int x = 11879;
    std::cout << 11879;
    for (int i = 0; i < 11879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11879(int& val) {
    const int x = 11879;
    std::cout << 11879;
    for (int i = 0; i < 11879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11880(int& val) {
    func_in_11880(val);
    const int x = 11880;
    std::cout << 11880;
    for (int i = 0; i < 11880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11880(int& val) {
    const int x = 11880;
    std::cout << 11880;
    for (int i = 0; i < 11880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11881(int& val) {
    func_in_11881(val);
    const int x = 11881;
    std::cout << 11881;
    for (int i = 0; i < 11881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11881(int& val) {
    const int x = 11881;
    std::cout << 11881;
    for (int i = 0; i < 11881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11882(int& val) {
    func_in_11882(val);
    const int x = 11882;
    std::cout << 11882;
    for (int i = 0; i < 11882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11882(int& val) {
    const int x = 11882;
    std::cout << 11882;
    for (int i = 0; i < 11882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11883(int& val) {
    func_in_11883(val);
    const int x = 11883;
    std::cout << 11883;
    for (int i = 0; i < 11883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11883(int& val) {
    const int x = 11883;
    std::cout << 11883;
    for (int i = 0; i < 11883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11884(int& val) {
    func_in_11884(val);
    const int x = 11884;
    std::cout << 11884;
    for (int i = 0; i < 11884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11884(int& val) {
    const int x = 11884;
    std::cout << 11884;
    for (int i = 0; i < 11884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11885(int& val) {
    func_in_11885(val);
    const int x = 11885;
    std::cout << 11885;
    for (int i = 0; i < 11885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11885(int& val) {
    const int x = 11885;
    std::cout << 11885;
    for (int i = 0; i < 11885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11886(int& val) {
    func_in_11886(val);
    const int x = 11886;
    std::cout << 11886;
    for (int i = 0; i < 11886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11886(int& val) {
    const int x = 11886;
    std::cout << 11886;
    for (int i = 0; i < 11886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11887(int& val) {
    func_in_11887(val);
    const int x = 11887;
    std::cout << 11887;
    for (int i = 0; i < 11887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11887(int& val) {
    const int x = 11887;
    std::cout << 11887;
    for (int i = 0; i < 11887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11888(int& val) {
    func_in_11888(val);
    const int x = 11888;
    std::cout << 11888;
    for (int i = 0; i < 11888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11888(int& val) {
    const int x = 11888;
    std::cout << 11888;
    for (int i = 0; i < 11888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11889(int& val) {
    func_in_11889(val);
    const int x = 11889;
    std::cout << 11889;
    for (int i = 0; i < 11889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11889(int& val) {
    const int x = 11889;
    std::cout << 11889;
    for (int i = 0; i < 11889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11890(int& val) {
    func_in_11890(val);
    const int x = 11890;
    std::cout << 11890;
    for (int i = 0; i < 11890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11890(int& val) {
    const int x = 11890;
    std::cout << 11890;
    for (int i = 0; i < 11890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11891(int& val) {
    func_in_11891(val);
    const int x = 11891;
    std::cout << 11891;
    for (int i = 0; i < 11891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11891(int& val) {
    const int x = 11891;
    std::cout << 11891;
    for (int i = 0; i < 11891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11892(int& val) {
    func_in_11892(val);
    const int x = 11892;
    std::cout << 11892;
    for (int i = 0; i < 11892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11892(int& val) {
    const int x = 11892;
    std::cout << 11892;
    for (int i = 0; i < 11892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11893(int& val) {
    func_in_11893(val);
    const int x = 11893;
    std::cout << 11893;
    for (int i = 0; i < 11893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11893(int& val) {
    const int x = 11893;
    std::cout << 11893;
    for (int i = 0; i < 11893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11894(int& val) {
    func_in_11894(val);
    const int x = 11894;
    std::cout << 11894;
    for (int i = 0; i < 11894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11894(int& val) {
    const int x = 11894;
    std::cout << 11894;
    for (int i = 0; i < 11894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11895(int& val) {
    func_in_11895(val);
    const int x = 11895;
    std::cout << 11895;
    for (int i = 0; i < 11895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11895(int& val) {
    const int x = 11895;
    std::cout << 11895;
    for (int i = 0; i < 11895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11896(int& val) {
    func_in_11896(val);
    const int x = 11896;
    std::cout << 11896;
    for (int i = 0; i < 11896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11896(int& val) {
    const int x = 11896;
    std::cout << 11896;
    for (int i = 0; i < 11896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11897(int& val) {
    func_in_11897(val);
    const int x = 11897;
    std::cout << 11897;
    for (int i = 0; i < 11897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11897(int& val) {
    const int x = 11897;
    std::cout << 11897;
    for (int i = 0; i < 11897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11898(int& val) {
    func_in_11898(val);
    const int x = 11898;
    std::cout << 11898;
    for (int i = 0; i < 11898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11898(int& val) {
    const int x = 11898;
    std::cout << 11898;
    for (int i = 0; i < 11898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11899(int& val) {
    func_in_11899(val);
    const int x = 11899;
    std::cout << 11899;
    for (int i = 0; i < 11899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11899(int& val) {
    const int x = 11899;
    std::cout << 11899;
    for (int i = 0; i < 11899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11900(int& val) {
    func_in_11900(val);
    const int x = 11900;
    std::cout << 11900;
    for (int i = 0; i < 11900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11900(int& val) {
    const int x = 11900;
    std::cout << 11900;
    for (int i = 0; i < 11900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11901(int& val) {
    func_in_11901(val);
    const int x = 11901;
    std::cout << 11901;
    for (int i = 0; i < 11901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11901(int& val) {
    const int x = 11901;
    std::cout << 11901;
    for (int i = 0; i < 11901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11902(int& val) {
    func_in_11902(val);
    const int x = 11902;
    std::cout << 11902;
    for (int i = 0; i < 11902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11902(int& val) {
    const int x = 11902;
    std::cout << 11902;
    for (int i = 0; i < 11902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11903(int& val) {
    func_in_11903(val);
    const int x = 11903;
    std::cout << 11903;
    for (int i = 0; i < 11903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11903(int& val) {
    const int x = 11903;
    std::cout << 11903;
    for (int i = 0; i < 11903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11904(int& val) {
    func_in_11904(val);
    const int x = 11904;
    std::cout << 11904;
    for (int i = 0; i < 11904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11904(int& val) {
    const int x = 11904;
    std::cout << 11904;
    for (int i = 0; i < 11904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11905(int& val) {
    func_in_11905(val);
    const int x = 11905;
    std::cout << 11905;
    for (int i = 0; i < 11905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11905(int& val) {
    const int x = 11905;
    std::cout << 11905;
    for (int i = 0; i < 11905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11906(int& val) {
    func_in_11906(val);
    const int x = 11906;
    std::cout << 11906;
    for (int i = 0; i < 11906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11906(int& val) {
    const int x = 11906;
    std::cout << 11906;
    for (int i = 0; i < 11906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11907(int& val) {
    func_in_11907(val);
    const int x = 11907;
    std::cout << 11907;
    for (int i = 0; i < 11907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11907(int& val) {
    const int x = 11907;
    std::cout << 11907;
    for (int i = 0; i < 11907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11908(int& val) {
    func_in_11908(val);
    const int x = 11908;
    std::cout << 11908;
    for (int i = 0; i < 11908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11908(int& val) {
    const int x = 11908;
    std::cout << 11908;
    for (int i = 0; i < 11908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11909(int& val) {
    func_in_11909(val);
    const int x = 11909;
    std::cout << 11909;
    for (int i = 0; i < 11909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11909(int& val) {
    const int x = 11909;
    std::cout << 11909;
    for (int i = 0; i < 11909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11910(int& val) {
    func_in_11910(val);
    const int x = 11910;
    std::cout << 11910;
    for (int i = 0; i < 11910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11910(int& val) {
    const int x = 11910;
    std::cout << 11910;
    for (int i = 0; i < 11910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11911(int& val) {
    func_in_11911(val);
    const int x = 11911;
    std::cout << 11911;
    for (int i = 0; i < 11911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11911(int& val) {
    const int x = 11911;
    std::cout << 11911;
    for (int i = 0; i < 11911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11912(int& val) {
    func_in_11912(val);
    const int x = 11912;
    std::cout << 11912;
    for (int i = 0; i < 11912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11912(int& val) {
    const int x = 11912;
    std::cout << 11912;
    for (int i = 0; i < 11912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11913(int& val) {
    func_in_11913(val);
    const int x = 11913;
    std::cout << 11913;
    for (int i = 0; i < 11913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11913(int& val) {
    const int x = 11913;
    std::cout << 11913;
    for (int i = 0; i < 11913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11914(int& val) {
    func_in_11914(val);
    const int x = 11914;
    std::cout << 11914;
    for (int i = 0; i < 11914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11914(int& val) {
    const int x = 11914;
    std::cout << 11914;
    for (int i = 0; i < 11914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11915(int& val) {
    func_in_11915(val);
    const int x = 11915;
    std::cout << 11915;
    for (int i = 0; i < 11915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11915(int& val) {
    const int x = 11915;
    std::cout << 11915;
    for (int i = 0; i < 11915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11916(int& val) {
    func_in_11916(val);
    const int x = 11916;
    std::cout << 11916;
    for (int i = 0; i < 11916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11916(int& val) {
    const int x = 11916;
    std::cout << 11916;
    for (int i = 0; i < 11916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11917(int& val) {
    func_in_11917(val);
    const int x = 11917;
    std::cout << 11917;
    for (int i = 0; i < 11917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11917(int& val) {
    const int x = 11917;
    std::cout << 11917;
    for (int i = 0; i < 11917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11918(int& val) {
    func_in_11918(val);
    const int x = 11918;
    std::cout << 11918;
    for (int i = 0; i < 11918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11918(int& val) {
    const int x = 11918;
    std::cout << 11918;
    for (int i = 0; i < 11918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11919(int& val) {
    func_in_11919(val);
    const int x = 11919;
    std::cout << 11919;
    for (int i = 0; i < 11919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11919(int& val) {
    const int x = 11919;
    std::cout << 11919;
    for (int i = 0; i < 11919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11920(int& val) {
    func_in_11920(val);
    const int x = 11920;
    std::cout << 11920;
    for (int i = 0; i < 11920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11920(int& val) {
    const int x = 11920;
    std::cout << 11920;
    for (int i = 0; i < 11920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11921(int& val) {
    func_in_11921(val);
    const int x = 11921;
    std::cout << 11921;
    for (int i = 0; i < 11921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11921(int& val) {
    const int x = 11921;
    std::cout << 11921;
    for (int i = 0; i < 11921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11922(int& val) {
    func_in_11922(val);
    const int x = 11922;
    std::cout << 11922;
    for (int i = 0; i < 11922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11922(int& val) {
    const int x = 11922;
    std::cout << 11922;
    for (int i = 0; i < 11922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11923(int& val) {
    func_in_11923(val);
    const int x = 11923;
    std::cout << 11923;
    for (int i = 0; i < 11923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11923(int& val) {
    const int x = 11923;
    std::cout << 11923;
    for (int i = 0; i < 11923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11924(int& val) {
    func_in_11924(val);
    const int x = 11924;
    std::cout << 11924;
    for (int i = 0; i < 11924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11924(int& val) {
    const int x = 11924;
    std::cout << 11924;
    for (int i = 0; i < 11924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11925(int& val) {
    func_in_11925(val);
    const int x = 11925;
    std::cout << 11925;
    for (int i = 0; i < 11925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11925(int& val) {
    const int x = 11925;
    std::cout << 11925;
    for (int i = 0; i < 11925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11926(int& val) {
    func_in_11926(val);
    const int x = 11926;
    std::cout << 11926;
    for (int i = 0; i < 11926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11926(int& val) {
    const int x = 11926;
    std::cout << 11926;
    for (int i = 0; i < 11926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11927(int& val) {
    func_in_11927(val);
    const int x = 11927;
    std::cout << 11927;
    for (int i = 0; i < 11927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11927(int& val) {
    const int x = 11927;
    std::cout << 11927;
    for (int i = 0; i < 11927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11928(int& val) {
    func_in_11928(val);
    const int x = 11928;
    std::cout << 11928;
    for (int i = 0; i < 11928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11928(int& val) {
    const int x = 11928;
    std::cout << 11928;
    for (int i = 0; i < 11928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11929(int& val) {
    func_in_11929(val);
    const int x = 11929;
    std::cout << 11929;
    for (int i = 0; i < 11929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11929(int& val) {
    const int x = 11929;
    std::cout << 11929;
    for (int i = 0; i < 11929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11930(int& val) {
    func_in_11930(val);
    const int x = 11930;
    std::cout << 11930;
    for (int i = 0; i < 11930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11930(int& val) {
    const int x = 11930;
    std::cout << 11930;
    for (int i = 0; i < 11930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11931(int& val) {
    func_in_11931(val);
    const int x = 11931;
    std::cout << 11931;
    for (int i = 0; i < 11931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11931(int& val) {
    const int x = 11931;
    std::cout << 11931;
    for (int i = 0; i < 11931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11932(int& val) {
    func_in_11932(val);
    const int x = 11932;
    std::cout << 11932;
    for (int i = 0; i < 11932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11932(int& val) {
    const int x = 11932;
    std::cout << 11932;
    for (int i = 0; i < 11932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11933(int& val) {
    func_in_11933(val);
    const int x = 11933;
    std::cout << 11933;
    for (int i = 0; i < 11933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11933(int& val) {
    const int x = 11933;
    std::cout << 11933;
    for (int i = 0; i < 11933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11934(int& val) {
    func_in_11934(val);
    const int x = 11934;
    std::cout << 11934;
    for (int i = 0; i < 11934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11934(int& val) {
    const int x = 11934;
    std::cout << 11934;
    for (int i = 0; i < 11934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11935(int& val) {
    func_in_11935(val);
    const int x = 11935;
    std::cout << 11935;
    for (int i = 0; i < 11935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11935(int& val) {
    const int x = 11935;
    std::cout << 11935;
    for (int i = 0; i < 11935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11936(int& val) {
    func_in_11936(val);
    const int x = 11936;
    std::cout << 11936;
    for (int i = 0; i < 11936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11936(int& val) {
    const int x = 11936;
    std::cout << 11936;
    for (int i = 0; i < 11936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11937(int& val) {
    func_in_11937(val);
    const int x = 11937;
    std::cout << 11937;
    for (int i = 0; i < 11937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11937(int& val) {
    const int x = 11937;
    std::cout << 11937;
    for (int i = 0; i < 11937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11938(int& val) {
    func_in_11938(val);
    const int x = 11938;
    std::cout << 11938;
    for (int i = 0; i < 11938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11938(int& val) {
    const int x = 11938;
    std::cout << 11938;
    for (int i = 0; i < 11938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11939(int& val) {
    func_in_11939(val);
    const int x = 11939;
    std::cout << 11939;
    for (int i = 0; i < 11939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11939(int& val) {
    const int x = 11939;
    std::cout << 11939;
    for (int i = 0; i < 11939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11940(int& val) {
    func_in_11940(val);
    const int x = 11940;
    std::cout << 11940;
    for (int i = 0; i < 11940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11940(int& val) {
    const int x = 11940;
    std::cout << 11940;
    for (int i = 0; i < 11940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11941(int& val) {
    func_in_11941(val);
    const int x = 11941;
    std::cout << 11941;
    for (int i = 0; i < 11941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11941(int& val) {
    const int x = 11941;
    std::cout << 11941;
    for (int i = 0; i < 11941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11942(int& val) {
    func_in_11942(val);
    const int x = 11942;
    std::cout << 11942;
    for (int i = 0; i < 11942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11942(int& val) {
    const int x = 11942;
    std::cout << 11942;
    for (int i = 0; i < 11942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11943(int& val) {
    func_in_11943(val);
    const int x = 11943;
    std::cout << 11943;
    for (int i = 0; i < 11943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11943(int& val) {
    const int x = 11943;
    std::cout << 11943;
    for (int i = 0; i < 11943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11944(int& val) {
    func_in_11944(val);
    const int x = 11944;
    std::cout << 11944;
    for (int i = 0; i < 11944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11944(int& val) {
    const int x = 11944;
    std::cout << 11944;
    for (int i = 0; i < 11944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11945(int& val) {
    func_in_11945(val);
    const int x = 11945;
    std::cout << 11945;
    for (int i = 0; i < 11945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11945(int& val) {
    const int x = 11945;
    std::cout << 11945;
    for (int i = 0; i < 11945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11946(int& val) {
    func_in_11946(val);
    const int x = 11946;
    std::cout << 11946;
    for (int i = 0; i < 11946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11946(int& val) {
    const int x = 11946;
    std::cout << 11946;
    for (int i = 0; i < 11946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11947(int& val) {
    func_in_11947(val);
    const int x = 11947;
    std::cout << 11947;
    for (int i = 0; i < 11947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11947(int& val) {
    const int x = 11947;
    std::cout << 11947;
    for (int i = 0; i < 11947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11948(int& val) {
    func_in_11948(val);
    const int x = 11948;
    std::cout << 11948;
    for (int i = 0; i < 11948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11948(int& val) {
    const int x = 11948;
    std::cout << 11948;
    for (int i = 0; i < 11948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11949(int& val) {
    func_in_11949(val);
    const int x = 11949;
    std::cout << 11949;
    for (int i = 0; i < 11949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11949(int& val) {
    const int x = 11949;
    std::cout << 11949;
    for (int i = 0; i < 11949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11950(int& val) {
    func_in_11950(val);
    const int x = 11950;
    std::cout << 11950;
    for (int i = 0; i < 11950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11950(int& val) {
    const int x = 11950;
    std::cout << 11950;
    for (int i = 0; i < 11950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11951(int& val) {
    func_in_11951(val);
    const int x = 11951;
    std::cout << 11951;
    for (int i = 0; i < 11951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11951(int& val) {
    const int x = 11951;
    std::cout << 11951;
    for (int i = 0; i < 11951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11952(int& val) {
    func_in_11952(val);
    const int x = 11952;
    std::cout << 11952;
    for (int i = 0; i < 11952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11952(int& val) {
    const int x = 11952;
    std::cout << 11952;
    for (int i = 0; i < 11952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11953(int& val) {
    func_in_11953(val);
    const int x = 11953;
    std::cout << 11953;
    for (int i = 0; i < 11953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11953(int& val) {
    const int x = 11953;
    std::cout << 11953;
    for (int i = 0; i < 11953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11954(int& val) {
    func_in_11954(val);
    const int x = 11954;
    std::cout << 11954;
    for (int i = 0; i < 11954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11954(int& val) {
    const int x = 11954;
    std::cout << 11954;
    for (int i = 0; i < 11954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11955(int& val) {
    func_in_11955(val);
    const int x = 11955;
    std::cout << 11955;
    for (int i = 0; i < 11955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11955(int& val) {
    const int x = 11955;
    std::cout << 11955;
    for (int i = 0; i < 11955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11956(int& val) {
    func_in_11956(val);
    const int x = 11956;
    std::cout << 11956;
    for (int i = 0; i < 11956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11956(int& val) {
    const int x = 11956;
    std::cout << 11956;
    for (int i = 0; i < 11956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11957(int& val) {
    func_in_11957(val);
    const int x = 11957;
    std::cout << 11957;
    for (int i = 0; i < 11957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11957(int& val) {
    const int x = 11957;
    std::cout << 11957;
    for (int i = 0; i < 11957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11958(int& val) {
    func_in_11958(val);
    const int x = 11958;
    std::cout << 11958;
    for (int i = 0; i < 11958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11958(int& val) {
    const int x = 11958;
    std::cout << 11958;
    for (int i = 0; i < 11958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11959(int& val) {
    func_in_11959(val);
    const int x = 11959;
    std::cout << 11959;
    for (int i = 0; i < 11959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11959(int& val) {
    const int x = 11959;
    std::cout << 11959;
    for (int i = 0; i < 11959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11960(int& val) {
    func_in_11960(val);
    const int x = 11960;
    std::cout << 11960;
    for (int i = 0; i < 11960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11960(int& val) {
    const int x = 11960;
    std::cout << 11960;
    for (int i = 0; i < 11960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11961(int& val) {
    func_in_11961(val);
    const int x = 11961;
    std::cout << 11961;
    for (int i = 0; i < 11961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11961(int& val) {
    const int x = 11961;
    std::cout << 11961;
    for (int i = 0; i < 11961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11962(int& val) {
    func_in_11962(val);
    const int x = 11962;
    std::cout << 11962;
    for (int i = 0; i < 11962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11962(int& val) {
    const int x = 11962;
    std::cout << 11962;
    for (int i = 0; i < 11962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11963(int& val) {
    func_in_11963(val);
    const int x = 11963;
    std::cout << 11963;
    for (int i = 0; i < 11963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11963(int& val) {
    const int x = 11963;
    std::cout << 11963;
    for (int i = 0; i < 11963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11964(int& val) {
    func_in_11964(val);
    const int x = 11964;
    std::cout << 11964;
    for (int i = 0; i < 11964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11964(int& val) {
    const int x = 11964;
    std::cout << 11964;
    for (int i = 0; i < 11964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11965(int& val) {
    func_in_11965(val);
    const int x = 11965;
    std::cout << 11965;
    for (int i = 0; i < 11965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11965(int& val) {
    const int x = 11965;
    std::cout << 11965;
    for (int i = 0; i < 11965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11966(int& val) {
    func_in_11966(val);
    const int x = 11966;
    std::cout << 11966;
    for (int i = 0; i < 11966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11966(int& val) {
    const int x = 11966;
    std::cout << 11966;
    for (int i = 0; i < 11966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11967(int& val) {
    func_in_11967(val);
    const int x = 11967;
    std::cout << 11967;
    for (int i = 0; i < 11967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11967(int& val) {
    const int x = 11967;
    std::cout << 11967;
    for (int i = 0; i < 11967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11968(int& val) {
    func_in_11968(val);
    const int x = 11968;
    std::cout << 11968;
    for (int i = 0; i < 11968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11968(int& val) {
    const int x = 11968;
    std::cout << 11968;
    for (int i = 0; i < 11968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11969(int& val) {
    func_in_11969(val);
    const int x = 11969;
    std::cout << 11969;
    for (int i = 0; i < 11969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11969(int& val) {
    const int x = 11969;
    std::cout << 11969;
    for (int i = 0; i < 11969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11970(int& val) {
    func_in_11970(val);
    const int x = 11970;
    std::cout << 11970;
    for (int i = 0; i < 11970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11970(int& val) {
    const int x = 11970;
    std::cout << 11970;
    for (int i = 0; i < 11970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11971(int& val) {
    func_in_11971(val);
    const int x = 11971;
    std::cout << 11971;
    for (int i = 0; i < 11971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11971(int& val) {
    const int x = 11971;
    std::cout << 11971;
    for (int i = 0; i < 11971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11972(int& val) {
    func_in_11972(val);
    const int x = 11972;
    std::cout << 11972;
    for (int i = 0; i < 11972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11972(int& val) {
    const int x = 11972;
    std::cout << 11972;
    for (int i = 0; i < 11972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11973(int& val) {
    func_in_11973(val);
    const int x = 11973;
    std::cout << 11973;
    for (int i = 0; i < 11973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11973(int& val) {
    const int x = 11973;
    std::cout << 11973;
    for (int i = 0; i < 11973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11974(int& val) {
    func_in_11974(val);
    const int x = 11974;
    std::cout << 11974;
    for (int i = 0; i < 11974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11974(int& val) {
    const int x = 11974;
    std::cout << 11974;
    for (int i = 0; i < 11974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11975(int& val) {
    func_in_11975(val);
    const int x = 11975;
    std::cout << 11975;
    for (int i = 0; i < 11975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11975(int& val) {
    const int x = 11975;
    std::cout << 11975;
    for (int i = 0; i < 11975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11976(int& val) {
    func_in_11976(val);
    const int x = 11976;
    std::cout << 11976;
    for (int i = 0; i < 11976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11976(int& val) {
    const int x = 11976;
    std::cout << 11976;
    for (int i = 0; i < 11976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11977(int& val) {
    func_in_11977(val);
    const int x = 11977;
    std::cout << 11977;
    for (int i = 0; i < 11977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11977(int& val) {
    const int x = 11977;
    std::cout << 11977;
    for (int i = 0; i < 11977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11978(int& val) {
    func_in_11978(val);
    const int x = 11978;
    std::cout << 11978;
    for (int i = 0; i < 11978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11978(int& val) {
    const int x = 11978;
    std::cout << 11978;
    for (int i = 0; i < 11978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11979(int& val) {
    func_in_11979(val);
    const int x = 11979;
    std::cout << 11979;
    for (int i = 0; i < 11979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11979(int& val) {
    const int x = 11979;
    std::cout << 11979;
    for (int i = 0; i < 11979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11980(int& val) {
    func_in_11980(val);
    const int x = 11980;
    std::cout << 11980;
    for (int i = 0; i < 11980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11980(int& val) {
    const int x = 11980;
    std::cout << 11980;
    for (int i = 0; i < 11980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11981(int& val) {
    func_in_11981(val);
    const int x = 11981;
    std::cout << 11981;
    for (int i = 0; i < 11981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11981(int& val) {
    const int x = 11981;
    std::cout << 11981;
    for (int i = 0; i < 11981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11982(int& val) {
    func_in_11982(val);
    const int x = 11982;
    std::cout << 11982;
    for (int i = 0; i < 11982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11982(int& val) {
    const int x = 11982;
    std::cout << 11982;
    for (int i = 0; i < 11982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11983(int& val) {
    func_in_11983(val);
    const int x = 11983;
    std::cout << 11983;
    for (int i = 0; i < 11983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11983(int& val) {
    const int x = 11983;
    std::cout << 11983;
    for (int i = 0; i < 11983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11984(int& val) {
    func_in_11984(val);
    const int x = 11984;
    std::cout << 11984;
    for (int i = 0; i < 11984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11984(int& val) {
    const int x = 11984;
    std::cout << 11984;
    for (int i = 0; i < 11984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11985(int& val) {
    func_in_11985(val);
    const int x = 11985;
    std::cout << 11985;
    for (int i = 0; i < 11985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11985(int& val) {
    const int x = 11985;
    std::cout << 11985;
    for (int i = 0; i < 11985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11986(int& val) {
    func_in_11986(val);
    const int x = 11986;
    std::cout << 11986;
    for (int i = 0; i < 11986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11986(int& val) {
    const int x = 11986;
    std::cout << 11986;
    for (int i = 0; i < 11986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11987(int& val) {
    func_in_11987(val);
    const int x = 11987;
    std::cout << 11987;
    for (int i = 0; i < 11987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11987(int& val) {
    const int x = 11987;
    std::cout << 11987;
    for (int i = 0; i < 11987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11988(int& val) {
    func_in_11988(val);
    const int x = 11988;
    std::cout << 11988;
    for (int i = 0; i < 11988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11988(int& val) {
    const int x = 11988;
    std::cout << 11988;
    for (int i = 0; i < 11988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11989(int& val) {
    func_in_11989(val);
    const int x = 11989;
    std::cout << 11989;
    for (int i = 0; i < 11989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11989(int& val) {
    const int x = 11989;
    std::cout << 11989;
    for (int i = 0; i < 11989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11990(int& val) {
    func_in_11990(val);
    const int x = 11990;
    std::cout << 11990;
    for (int i = 0; i < 11990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11990(int& val) {
    const int x = 11990;
    std::cout << 11990;
    for (int i = 0; i < 11990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11991(int& val) {
    func_in_11991(val);
    const int x = 11991;
    std::cout << 11991;
    for (int i = 0; i < 11991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11991(int& val) {
    const int x = 11991;
    std::cout << 11991;
    for (int i = 0; i < 11991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11992(int& val) {
    func_in_11992(val);
    const int x = 11992;
    std::cout << 11992;
    for (int i = 0; i < 11992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11992(int& val) {
    const int x = 11992;
    std::cout << 11992;
    for (int i = 0; i < 11992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11993(int& val) {
    func_in_11993(val);
    const int x = 11993;
    std::cout << 11993;
    for (int i = 0; i < 11993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11993(int& val) {
    const int x = 11993;
    std::cout << 11993;
    for (int i = 0; i < 11993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11994(int& val) {
    func_in_11994(val);
    const int x = 11994;
    std::cout << 11994;
    for (int i = 0; i < 11994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11994(int& val) {
    const int x = 11994;
    std::cout << 11994;
    for (int i = 0; i < 11994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11995(int& val) {
    func_in_11995(val);
    const int x = 11995;
    std::cout << 11995;
    for (int i = 0; i < 11995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11995(int& val) {
    const int x = 11995;
    std::cout << 11995;
    for (int i = 0; i < 11995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11996(int& val) {
    func_in_11996(val);
    const int x = 11996;
    std::cout << 11996;
    for (int i = 0; i < 11996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11996(int& val) {
    const int x = 11996;
    std::cout << 11996;
    for (int i = 0; i < 11996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11997(int& val) {
    func_in_11997(val);
    const int x = 11997;
    std::cout << 11997;
    for (int i = 0; i < 11997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11997(int& val) {
    const int x = 11997;
    std::cout << 11997;
    for (int i = 0; i < 11997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11998(int& val) {
    func_in_11998(val);
    const int x = 11998;
    std::cout << 11998;
    for (int i = 0; i < 11998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11998(int& val) {
    const int x = 11998;
    std::cout << 11998;
    for (int i = 0; i < 11998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11999(int& val) {
    func_in_11999(val);
    const int x = 11999;
    std::cout << 11999;
    for (int i = 0; i < 11999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11999(int& val) {
    const int x = 11999;
    std::cout << 11999;
    for (int i = 0; i < 11999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
